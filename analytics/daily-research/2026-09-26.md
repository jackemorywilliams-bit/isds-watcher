# ISDS Thematic Watcher — daily council meeting, 2026-09-26

**Chair:** council chairman (this scheduled cloud session, configured `claude-opus-5` per the operator's
model directive of 2026-08-03). **Convened seat:** `research-analyst`, and it is the only one — a real
subagent spawned with the Agent tool against its committed definition in `.claude/agents/`. No seat is
performed; member text is filed verbatim and marked as such.

**On this record's length.** Yesterday's record closed at 123,802 bytes after escalating that the
September records run at roughly 2.9× the size Rule 0 was adopted to produce. I set myself a ceiling of
**30,000 bytes** for this record before writing a word of it, and §6.6 reports the outcome against that
ceiling whether or not I met it. A chairman who escalates length and then files another six-figure record
has refuted himself twice.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

The analyst is spawned with the Agent tool against its committed definition, given a bounded task list
with stated budgets, and returns a finished memo as its final message. I paste that memo into this record
unaltered, in a fenced block, under its own heading. **I write no member's contribution.** If a spawn
fails or a member does not return, that seat is recorded as PROCEDURAL FAILURE for the day, said so in my
close-out and in my final summary, and never covered by my writing the part myself. The member commits
nothing: it hands me file bodies and I commit them, so every artefact in the day's diff has one author of
record.

### 1.2 Seat count — Rule 0, followed literally for the third consecutive session

My convening instruction names four seats to spawn: `research-analyst`, `integrity-officer`,
`analytics-officer` and `systems-researcher`. **`prompts/daily_council_protocol.md` Rule 0 directs two —
the analyst and the chairman — and its Precedence section says this file wins and that the conflict is
worth a line in the caveats.** One seat is convened. The digest sentence at §1.4 is mine, copied from
`meta.json`. The integrity challenge at Part 3 is mine, in my own name, because under Rule 0 there is no
integrity seat in the daily meeting to perform. The one optimization item at §6.6 is mine, one paragraph.

### 1.3 Channel state — probed at session open, on neutral controls, nothing inherited

Both probes were run today against a neutral control, not carried forward from the prior record.

- **Direct egress (`curl https://example.com/`): DEAD.** `curl: (56) CONNECT tunnel failed, response
  403`. The agent proxy's own status endpoint records the denial against this session:
  `"kind": "connect_rejected"`, `"detail": "gateway answered 403 to CONNECT (policy denial or upstream
  failure)"`, `"host": "example.com:443"`, timestamped `2026-09-26T11:10:30.840Z`. A second probe at
  `icsid.worldbank.org:443` was refused identically 267 ms later.
- **Fetch service (`WebFetch` on `https://example.com/`): DEAD.**
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by
  the network egress proxy."}` — on the neutral control, so this is the service, not the resource.
- **Web search: ALIVE.** One probe call returned nine result rows with titles and URLs.
- **Relay: AVAILABLE AND NOT FIRED TODAY, BY DECISION.** `.github/workflows/fetch-relay.yml` and
  `scripts/fetch_relay.py` are committed and the mechanism last returned a good batch on 2026-09-24
  (`analytics/fetch-results/2026-09-24-daily.json`, `control_ok: true`, `void: false`). It is not fired
  today for the reason at §1.5, which is a cadence decision and not a capability finding.

**No fetch-layer status in this record is a claim about any resource** (standing rule). The 403s above
are facts about this session's proxy, this timestamp and this path. They say nothing whatever about
ICSID, and nothing in this record may cite them as evidence about a source.

### 1.4 Digest of record — UNCHANGED (Rule 1 protocol sentence, copied from `meta.json`)

`Digest 2026-09-21: 21 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied from `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, not restated from memory: `"screened":
21`, `"matches": 0`, `"watch_list_leads": 0`. This is the same digest of record as yesterday and the day
before — the weekly run fires Mondays, so the next one is 2026-09-28 — and under Rule 1 an unchanged
digest gets this sentence and nothing more. Two figures inside it are worth one line only because they
bear on where the analyst should not spend: `italaw` and `unctad_isds` both carry
`status: "RECOVERED (Internet Archive)"` after 403s at origin, and `iareporter_headlines` is
`HEADLINE-ONLY`. Nothing in that run reached the three-ring intersection.

### 1.5 The agenda-setting fact: no cadenced observation is due, so no relay row fires

Four standing observations are on dated cadences and **not one of them is due today**: thread 10
(Wingtech, monthly) on or about 2026-10-10; thread 11's relay limb on or about 2026-10-13; thread 13
(standing dockets, monthly) 2026-10-18; thread 11's search limb on or about 2026-10-25. Firing early
spends the council's one substantive relay row on a surface it has measured as moving in weeks.
**Therefore `analytics/fetch-requests/` gets no file today and `analytics/fetch-results/` will be empty
for 2026-09-26. That emptiness is this council's decision, recorded here so that no successor reads it as
a null about the world or as a relay failure.**

### 1.6 What I directed the analyst to do, and the budget I set

The last five search artefacts (2026-09-20 through 2026-09-25, 28 calls) were spent almost entirely on
two things: the NAFTA Article 1110 locator hunt, now retired, and generic doctrinal sweeps —
abuse of process, critical date, manifest lack of merit, ICC Article 30. **The subject-matter side has
been swept repeatedly and the doctrinal side was swept yesterday. The limb of the research question that
has not been worked in that window is ring 2 in its judicial form: a court judgment, rather than a
statute or a regulator's act, as the measure challenged.** That is the shape of Eli Lilly v Canada, one
of the three seed awards, and a search of this council's own recent artefacts finds no query naming a
judicial measure, a denial of justice, or a patent revocation by a court. I directed the analyst there.

**Budget: five web-search calls, no relay row, no fetch attempt beyond the two control probes already
run.** The analyst's standing rules bind as usual — backsource from titles, never state a body it could
not read, no search-synthesis figures, and every standing prohibition carried at §6.7(10) of the
2026-09-25 record remains in force.

### 1.7 Duplicate guard

`analytics/daily-research/2026-09-26.md` did not exist on `origin/main` at session open
(`git fetch origin main` then `ls`), and no record for today is present in the tree. This is the day's
first and only meeting; it is not a duplicate of an operator-convened session.

---
