# ISDS Thematic Watcher — daily council meeting, 2026-09-21

**Chair:** council chairman (this session, `claude-opus-5` per the operator's model directive of
2026-08-03). **Convened seats:** `research-analyst` and `integrity-officer` — REAL subagents spawned
with the Agent tool against their committed definitions in `.claude/agents/`. No seat is performed.
Where a member's text appears it is filed VERBATIM and is marked as such.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

Each member is spawned with the Agent tool against its committed definition, is given its own bounded
task list, and returns a finished memo as its final message. I paste that memo into this record
unaltered, inside a fenced block, under the seat's own heading. **I write no member's contribution.**
If a spawn fails or a member does not return, that seat is recorded as PROCEDURAL FAILURE for the day
and the failure is stated both in my close-out and in my final summary — it is never covered by my
writing the part myself. Members do not commit; they hand me file bodies and I commit them, so every
artefact in the day's diff has one author of record.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

I inherited no channel status. I re-probed, as the convening instruction requires, and both probes
were on neutral controls rather than on research hosts, so that nothing in this section can be misread
as a statement about a source.

| Probe | Target (neutral control) | Result |
|---|---|---|
| `curl` egress | `https://example.com` | `curl: (56) CONNECT tunnel failed, response 403` |
| `curl` egress | `https://en.wikipedia.org/wiki/Main_Page` | `curl: (56) CONNECT tunnel failed, response 403` |
| fetch service | `https://example.com` | `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by the network egress proxy."}` |
| `WebSearch` | ISDS/IP query, 9 result rows returned | **ALIVE** |

**No state change: this session has no fetch of its own, for the twentieth consecutive session.** One
detail is worth a line because it is a change in the *failure message* and not in the *fact*: the
fetch service now answers a neutral control with a typed `EGRESS_BLOCKED` error naming the proxy,
where earlier sessions recorded an undifferentiated 403 on every URL. That is a clearer diagnosis of
the same closed door, and it is a fact about this container, **not** about any resource on the
internet. The standing rule holds without amendment: **a tool-layer outcome is never a claim about a
resource.**

**The day's retrieval instruments are therefore two, and both are alive.** (a) `WebSearch`, native to
this session, which returns engine titles and snippets and **never a document body**. (b) The
**fetch-relay** — `.github/workflows/fetch-relay.yml`, adopted after the 2026-08-03 records this
session's convening instruction points at — which fires on a pushed request file under
`analytics/fetch-requests/`, runs `scripts/fetch_relay.py` on a GitHub-hosted runner that does have
network, and commits a reduction (outcome, status, `content_type`, `bytes`, `sha256`, and a
400-character excerpt) back to `analytics/fetch-results/`. Git is the one network capability this
container is proven to have. **No fetch-dependent item is stranded today**, and I do not record one as
such.

### 1.3 Duplicate guard

`git show origin/main:analytics/daily-research/2026-09-21.md` returns *"path ... does not exist in
'origin/main'"*. No record for today had been convened manually. This meeting is not a duplicate.

### 1.4 What this session inherits, and the agenda it sets

The 2026-09-20 close-out left one gap marker open with a mechanical closure condition, and that
condition is the whole of today's priority. The marker is
**`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested`** — whether *any* host on
`ALLOWED_HOSTS` serves NAFTA Chapter 11 in a content type from which `excerpt_of` can produce text is
**untested**, because the one candidate tried, `investmentpolicy.unctad.org`, refused the relay seven
times out of seven. **Its closure condition, fixed in advance and not re-negotiable today: one `ok`
row with a non-empty excerpt at any allowlisted locator.** It matters because it is the channel
underneath **escalation 2**, which has been an ask of the operator for ten days: does NAFTA Article
1110 — the expropriation article of a seed-award treaty — carry an IP-specific internal limitation at
a paragraph (7)? If the council can read its own seed treaty's expropriation chapter, that escalation
stops being an ask of Emory.

Yesterday's next step 2 forbids re-firing at `investmentpolicy.unctad.org` as a batch, and the reason
is evidential rather than dispositional: fifteen `ok` rows all dated 2026-08-09 or earlier, twelve
`refused`/403 from 2026-08-10 through 2026-09-17, and seven more refusals yesterday across two path
shapes never previously tried — **a six-week regime indifferent to path across all five shapes so far
attempted.** That prohibition is binding today. It also *defines* today's search problem, and the
definition is the useful part: the analyst must find the chapter at an allowlisted host that is **not**
that one, or report honestly that no such locator can be identified.

**The agenda, in three bounded tasks, with budgets I set here:**

1. **TASK 1 — the NAFTA channel question (primary).** Up to **six** `WebSearch` calls to identify a
   locator for NAFTA Chapter 11 / Article 1110 served in HTML or plain text at an allowlisted host
   other than `investmentpolicy.unctad.org`. Docket-page-before-document-hunt applies. Every call is
   recorded in the **pre-filed** skeleton at `analytics/search-results/2026-09-21.json`.
2. **TASK 2 — design the relay batch.** At most **nine** requested rows, pre-registered per
   **outcome class** (`ok` / `refused` / `robots_disallowed` / `no_contact`) and never per document,
   with the pairing discipline (`find:""` companion on any row that could produce a null) and the
   closing-bracket control. I fire it; the analyst does not.
3. **TASK 3 — the record's substance.** Candidate claims with marks, method and error notes, and an
   honest statement of what today does and does not establish — including the null case.

### 1.5 Spend checkpoint (placed here, taken after the vetting round at §4)

The checkpoint is a real stop, not a ritual: after the integrity officer's return I decide whether the
relay batch is fired at all. It is fired only if TASK 1 produced a locator at an allowlisted host that
was **identified by a source rather than guessed** (METHODOLOGY Part VIII) and survived vetting. If it
did not, the honest close is a null day with the gap marker re-carried open, and I will write it that
way rather than fire a batch to have something to report.

---

## 2. Research analyst — filed verbatim

Spawned with the Agent tool against `.claude/agents/research-analyst.md`. It returned a finished memo
as its final message. **Everything between the two rules below is the analyst's text, unaltered. I
wrote none of it.** Its two artefacts arrived in the working tree uncommitted, as the return-path
protocol requires; I validated both as JSON before filing (7 calls / 90 rows in the search record;
9 urls and a 13,166-character `note` in the request) and I commit them under my own hand.

---

# RESEARCH ANALYST — 2026-09-21

## 0. What this session had, and what it did with it

No fetch of its own — I inherited your probe and did not re-run it; that is a fact about this container and not about any resource. `WebSearch` alive. Seven invocations of it, six returning rows. Two files written to the working tree, **uncommitted**, for you to commit or discard:

- `/home/user/isds-watcher/analytics/search-results/2026-09-21.json` (41,676 bytes — your pre-filed skeleton, filled)
- `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json` (9 rows, note 13,166 chars)

All figures below carry their commit. **HEAD is `256cb6e`. `analytics/fetch-results/` holds 70 files at `256cb6e`** — the same count you recorded at `798c0f7`, so no result file has landed since; it was 69 at `8b05d3c`. `analytics/insights.jsonl` is 198 lines at `256cb6e`.

**Extension 40a discharge, stated expressly because I restate figures nearby: the `0919-N2` render double-count is UNCHANGED.** I re-measured it myself rather than inheriting it: `grep -c "Article 1110"` returns **1 line** in each of `METHODOLOGY.md` and `docs/methodology.html` at `256cb6e`, and the single `METHODOLOGY.md` line carries the string **twice** ("under NAFTA Article 1110" and "either Article 1105 or Article 1110"). **1 line / 2 occurrences each. The correction stands.**

---

## 1. TASK 1 — the search. Result: a candidate, and something worse than a candidate found in our own record.

### 1.1 The two mandatory search parameters cannot both be passed. This is not an excuse; it is an instrument fact, and it re-scopes a sustained objection.

My first invocation was your instruction executed literally — `allowed_domains: ["www.italaw.com","italaw.com"]` **and** `blocked_domains: ["dailyjus.com","unis.unvienna.org"]`. It returned, verbatim:

> `Error: Cannot specify both allowed_domains and blocked_domains in the same request`

Zero rows. **Your two binding instructions — "use `allowed_domains` to restrict to candidate allowlisted hosts" and "`blocked_domains` on EVERY call, MECHANICALLY" — are not jointly satisfiable at the tool layer.**

**The resolution I adopted, stated so the officer may rule against it.** On every call I passed a single-host or small `allowed_domains` list containing **neither** prohibited host, which excludes them by construction rather than by parameter; and I then **screened all 90 returned rows** for both. **`dailyjus.com`: 0 rows. `unis.unvienna.org`: 0 rows.** The residual risk is named, not waved away: `analytics/insights.jsonl:198` records that `allowed_domains` did not constrain one multi-host result set on 2026-09-20, so exclusion-by-construction is not a guarantee — which is exactly why I report the row screen instead of promising the parameter.

**And this re-scopes `0920-B6`.** I opened `analytics/search-results/2026-09-20.json` and read the filters on all seven calls. **S1, S2, S3, S4 and S6 all passed `allowed_domains` — so on five of the seven calls, passing `blocked_domains` was mechanically impossible.** Only **S5** (no filter, passed nothing) and **S7** (no `allowed_domains`, passed `["dailyjus.com"]` — one of the two) were calls where compliance was available. **The honest denominator for yesterday's "0 of 7" is 2, not 7.** I state this against the interest of tidiness and in favour of my predecessor in this seat: the sustained objection is correct as to S5 and S7 and **over-scoped as to S1–S4 and S6**, and neither the officer nor the chair could have known that, because nobody had tried to pass both.

Budget treatment, declared: I count the erroring invocation as an invocation made but **not** as one of the six searches you budgeted, because it returned zero rows. If the officer counts it, the session used 7 of 6 and I accept that finding without contest.

### 1.2 `allowed_domains` admits subdomains. One consequence bites this project directly.

Three observations this session, all in the filed artefact:

- S1 passed exactly `["unctad.org"]` → **4 of 10 rows are at `investmentpolicy.unctad.org`**, a different host.
- S3 passed exactly `["italaw.com"]` → **10 of 10 rows are at `www.italaw.com`**.
- S4 passed `pca-cpa.org` → one row at **`files.pca-cpa.org`**.

**The consequence that matters: `files.pca-cpa.org` is NOT on `ALLOWED_HOSTS` while `pca-cpa.org` is.** A search restricted to an allowlisted host can hand back a locator the relay may not fetch, and it did so today. I screened all 90 rows against `ALLOWED_HOSTS` as read at `256cb6e`: **89 in set, 1 out** (that PCA row). No batch row was proposed for it.

**This does NOT retire yesterday's leak finding, and per 40a I say so expressly: `analytics/insights.jsonl:198` is UNCHANGED.** The four out-of-set rows it records — `cdn.clinicaltrials.gov` ×2, `image-ppubs.uspto.gov`, `www.ncbi.nlm.nih.gov` — are not subdomains of any value in that call's list, so suffix-matching does not explain them. Today refines that finding; it does not absorb it.

### 1.3 The six calls, and what they bought

| call | `allowed_domains` | rows | what it bought |
|---|---|---|---|
| S1 | `unctad.org` | 10 | 6 rows at the bare host `unctad.org` (never contacted by this relay, zero rows ever) — **all six `/system/files/official-document/*.pdf`**. No NAFTA-chapter candidate in a readable type. |
| S2 | `digitallibrary.un.org` | 40 | Four internal result blocks in one invocation. Records **about** NAFTA (a 1997 study, ECLAC papers); **no treaty-text record of NAFTA at all.** Null. |
| S3 | `italaw.com` | 10 | Ten PDFs. **Result 1 is the finding at §1.4.** |
| S4 | UNCITRAL / undocs / docs.un / un.org / ICSID / PCA | 10 | Ten PDFs — nine ICSID party-exhibit PDFs, one out-of-allowlist PCA file. **Null for every UN-family host and for ICSID and PCA.** |
| S5 | `www.italaw.com` | 10 | **Docket-page-before-document-hunt, and it worked.** Two HTML-shaped landing pages. |
| S6 | `www.italaw.com` | 10 | `/investment-treaties/nafta` returned again; three `/cases/NNN` HTML pages. |

Host tally over all 90 rows: `digitallibrary.un.org` 40, `www.italaw.com` 30, `icsid.worldbank.org` 9, `unctad.org` 6, `investmentpolicy.unctad.org` 4, `files.pca-cpa.org` 1.

**Every engine summary is quarantined in its own field and used nowhere.** S4's is the dangerous one — narrative prose purporting to state what Article 1110(2) requires. **No treaty text was retrieved by this session and none is stated anywhere in this memo.**

### 1.4 What Task 1 found

**Two HTML-shaped locators at an allowlisted host, both identified by a source and neither guessed:**

- `https://www.italaw.com/investment-treaties/nafta` — engine title *"NAFTA - Investment treaties Profile & Case History"* (S5 result 6; returned again at S6 result 1)
- `https://www.italaw.com/investment-treaties` — engine title *"Investment Treaties | italaw"* (S5 result 1)

**Every caveat I can put on that, I put on it.** S5 and S6 are **one engine twice, not two sources** — same session, same seat, same single-host filter (`0920-B5`, sustained). An engine's indexed title is the **engine's**, not the document's. Nothing establishes that either URL serves `text/html`, and **nothing whatever establishes that either page carries one word of Chapter Eleven** — "Profile & Case History" is, on its face, a case list. The engine's prose asserting that the index page provides *"Full text of Investment Treaties"* is the engine's and is relied on nowhere; today's batch **tests** it rather than assuming it.

**And a third locator, which cannot close the gap but which I think is the day's sharpest finding.** S3 result 1 returned `https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf` under the engine title:

> `NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble`

I then scanned this project's own committed artefact for that string. **The identical eleven-word title is carried by three distinct URLs across two hosts:** `investmentpolicy.unctad.org/.../treaty-files/2412/download` (2026-09-20 S1 r4), `www.italaw.com/.../laws/italaw6187(6).pdf` (2026-09-20 S3 r9), and `italaw6187(14).pdf` (today). **Escalation 2 currently sends Emory to UNCTAD `2412` on the strength of exactly that title. The title does not individuate the file.** That does not make `2412` the wrong place to look; it means the title is not what makes it the right one. It is `0920-N5`'s lesson in a second currency: adjacency in a database is not identity, and **an engine title is not a file identity either.**

### 1.5 The thing I found in our own record, and it is against this council

While checking whether `/investment-treaties/<slug>` was a new path shape here, it turned out **not to be new at all.**

`https://www.italaw.com/investment-treaties/china-germany-bit-2003` has been in this repository since **2026-06-26**, at three loci at `256cb6e`: `STATE_OF_THE_ANSWER.md` (the China–Germany BIT (2003) entry, cited by text and commit per `0918-B7`), `analytics/daily-research/2026-06-26.md:41`, and `analytics/insights.jsonl:10`.

And on 2026-08-05 a seat of this council wrote (`analytics/daily-research/2026-08-05.md:205`):

> "`www.italaw.com` is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT — `STATE_OF_THE_ANSWER.md:40` cites `https://www.italaw.com/investment-treaties/china-germany-bit-2003`."

That seat then **correctly** declined to construct a `china-switzerland-bit-2009` sibling by slug analogy, because that is discovery. **It did not fire the URL the record already held. Nobody has, in the 47 days since** — `grep` over `analytics/fetch-requests/` and `analytics/fetch-results/` returns zero rows at that URL at `256cb6e`.

**Stated so it is not overread, and this is the `0920-B7` species of error I am trying not to repeat: no seat of this council has ever RETRIEVED that page.** The 2026-06-26 entry that carries the URL describes the direct treaty PDF as *"not directly readable (403)"*. What the record holds is a **locator**; the characterisation of it as a treaty-text page is a seat's, not a retrieval. I do not assert that italaw serves treaty text. I assert that we have had a testable locator of the target shape for 87 days and never sent one row at it.

**That row is now row 6 of the batch, and it is the row I would keep if I could keep only one** — because it is the only URL of the target path shape whose existence is attested by our own committed record rather than by a search engine's index, and it separates **slug** from **shape** inside one run.

---

## 2. TASK 2 — the batch. Nine rows, ready to fire.

Complete body at `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json`. All nine pass `scripts.fetch_relay.validate()` (I ran it). 9 requested + 1 prepended = 10 ≤ `MAX_URLS` 12, no truncation. Eight substantive rows at one host at 3.0s `MIN_INTERVAL_SECONDS` ≈ 24s of politeness; the runner has time.

```json
{
  "urls": [
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": ""},
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": "1110"},
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": "Chapter Eleven"},
    {"url": "https://www.italaw.com/investment-treaties", "find": ""},
    {"url": "https://www.italaw.com/investment-treaties", "find": "NAFTA"},
    {"url": "https://www.italaw.com/investment-treaties/china-germany-bit-2003", "find": ""},
    {"url": "https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf", "find": ""},
    {"url": "https://www.italaw.com/sites/default/files/laws/italaw6187(6).pdf", "find": ""},
    {"url": "https://example.com/", "find": "Example Domain"}
  ]
}
```

The `note` field is 13,166 characters and is on disk in full, ready to write verbatim. **Its substance, which I will not let be summarised away:**

- **My prior, stated before any row returns and against my own interest.** At `www.italaw.com`, across all 70 committed reductions at `256cb6e`, this relay holds **12 rows: 4 `ok`/200, every one `application/pdf` with excerpt length 0, every one under `/sites/default/files/case-documents/`; and 8 `refused`/403** covering the **bare site root** `https://www.italaw.com/` (2026-08-03), `/cases/8154` (×3), `/cases/documents/10912` (×2) and `/cases/11573` (×2, 2026-09-12). **Eight of eight non-PDF rows ever fired at this host have been refused, the site root among them. I EXPECT ROWS 1–6 TO REFUSE AND ROWS 7–8 TO ANSWER.** If that is how it falls, **this batch does not close the gap and I will say so.** The one thing that makes rows 1–6 worth firing rather than zero rows: `/investment-treaties/` is a shape never tried here, and italaw's refusals are already known to be **path-discriminating within a single run, client held constant** — `analytics/insights.jsonl:73` (2026-08-18), which I cite rather than re-deriving, so a host-wide reading is not available to any seat.
- **Per-outcome-class pre-registration, never per document.** Four groups plus the bracket; for each, what `ok`+html, `ok`+non-text, `ok`+empty-find, `refused`, `robots_disallowed` and `no_contact` each license and — more of the note than not — what they do not. `content_type` and `bytes` are recorded whatever the body is; **a refusal has no body to type, and its nulls mean that and nothing else.** The `robots_disallowed`/`refused` distinction is carried to `src/sources/base.py:339` and `analytics/insights.jsonl:167`.
- **The Group D pair is pre-registered to discriminate either way:** if `italaw6187(6)` and `(14)` differ in bytes or sha256, two distinct files carry one identical engine title; if they are identical, one byte-stream sits at two paths. **On either branch the title does not individuate, and on neither branch is any file identified as NAFTA.**
- **Pairing discipline, checkable:** rows 2 and 3 are companioned by row 1; row 5 by row 4; rows 6–8 already carry `find:""`; row 9's companion is the runner's own prepended control.
- **The closing bracket** is row 9, `find: "Example Domain"`, distinct from the prepended control: the prepended row fires with no find and proves **reachability only**; row 9 proves **the find path executed**. If row 9 is `ok` with `find_matched` false, every empty excerpt in the batch is uninterpretable and the batch is void on that axis whatever else it says.
- **`control_ok: false` voids every row, including the ones that look like answers.**
- **Not discovery, and I expect to be checked on it.** Every URL was identified by a source before it was requested, with call and result index given in the note. No URL was constructed by slug analogy, pattern or guess; `china-switzerland-bit-2009`, the URL the 2026-08-05 seat rightly refused to build, is not in this batch.
- **Concentration risk, declared:** all eight substantive rows are at one host. That is not a design preference — it is the honest consequence of six searches finding no non-PDF NAFTA Chapter Eleven candidate at any other allowlisted host. **If italaw refuses everything, this batch returns no information about any resource, and I pre-register that as a real and reasonably likely outcome.**

**I declined the one permitted UNCTAD `treaty-files/2412` diagnostic row.** Your steer plus my own arithmetic: 23 refusals against 15 `ok` all dated 2026-08-09 or earlier, a regime that has not moved in six weeks. The slot went to row 6, which tests a shape nobody has tested rather than re-testing a host everybody has.

### 2.1 A defect in the gap's closure condition, recorded before firing and NOT re-negotiated

You fixed it as **"one `ok` row with a non-empty excerpt at any allowlisted locator."** Read literally, **that condition is already satisfied by 134 `ok` rows with non-empty excerpts at `https://example.com/`**, which is on `ALLOWED_HOSTS` at `scripts/fetch_relay.py:67-68` (marked *"Neutral control only — never a research source"*). So the literal reading cannot be the intended one.

**I do not amend your condition.** I record, before firing, the reading I act on — the one the slug itself requires: *an `ok` row with a non-empty excerpt at an allowlisted locator **that serves NAFTA Chapter Eleven***. Under that reading **only rows 1–3 could close it**, and even they close it only if the page turns out to carry the chapter's text rather than a case history — **which this batch cannot determine.** Rows 4–5 name an index, row 6 a different treaty, rows 7–8 are PDF-suffixed, row 9 is a control. This is here so that whoever grades the batch grades it against a stated reading rather than picking one afterwards.

---

## 3. TASK 3 — substance

### 3.1 What today establishes, and what it does not

**Establishes:** that `WebSearch` cannot honour both of this council's mandatory filter parameters at once, and that the sustained `0920-B6` denominator is 2 and not 7; that `allowed_domains` admits subdomains, one of which today was off `ALLOWED_HOSTS`; that one engine title sits on three distinct locators across two hosts; that a testable locator of the target path shape has been in this repository since 2026-06-26 and has never been fired.

**Does not establish, and nothing today moves an inch toward:** anything about what NAFTA Article 1110 says, whether a paragraph (7) exists, or whether any IP-specific internal limitation is carried anywhere. **No treaty text has been read by any seat of this council.** `GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread` stands untouched, and **escalation 2 remains an ask of Emory whatever these nine rows do.**

**The theme itself is quiet at the three-ring intersection for an eleventh consecutive session, and I did not spend a query on it.** My whole six-call budget went to the channel question at your direction. **I therefore report no search for new investor–State matter at the intersection of IP as a covered investment, a regulatory or judicial measure, and a live jurisdictional doctrine, and I make no claim about whether one exists.** Honest form: **not searched today**, which is weaker than "not found in accessible sources" and I will not upgrade it.

### 3.2 Errors reported against myself

1. **I drafted the pre-registration with a wrong pinpoint** — `scripts/fetch_relay.py:122-123` for the content-type guard, which is at **`:124-125`**. Caught by reading the file rather than by being told, and corrected **before** the file was fired, which is the only window in which a pre-registration may be corrected at all.
2. **I drafted a day-count of "forty-eight days" for 2026-08-05 → 2026-09-21. It is 47.** Corrected before firing. I computed it instead of feeling it, which is the only reason I caught it.
3. **My first draft of the closure-condition paragraph said rows 1–6 could close the gap. Row 6 names a different treaty and could not close a NAFTA gap on any reading.** Corrected before firing.
4. **My first batch draft spent a row on `https://www.italaw.com/cases/715` as a negative control and did not contain the China–Germany row at all** — because I had not yet screened our own record for the path shape I was about to call new. I found it on a novelty check I nearly skipped. **This is `0920-N5` again in my own hands: I read a search return against one proposition and did not read the repository against the same one.** The cure is not a name; it is that the screen ran. It ran late.
5. **Unspent budget, declared:** six budgeted searches, six used, plus one invocation that errored. Nothing held back, nothing exceeded except as at §1.1.

### 3.3 Carrying-span marks

**Entry 1**
- **P** — `WebSearch` refuses `allowed_domains` and `blocked_domains` in the same request.
- **Q** — `Error: Cannot specify both allowed_domains and blocked_domains in the same request` [the tool's own return this session, recorded verbatim at `analytics/search-results/2026-09-21.json`, call `S0`, field `error_verbatim`]
- **D** — the instrument's own error return, 2026-09-21. Not a litigated matter; the artefact is the retrieval tool this session runs on. Stage: instrument. Outcome on the point cited: the invocation returned zero rows.
- **V** — checked 2026-09-21. **Rigid designators present and strong:** the parameter names `allowed_domains` and `blocked_domains`, which no field can paraphrase away. **Weakness declared: the source is my own return, filed in the same memo, on one invocation; I did not re-run it, so I cannot say the refusal is deterministic rather than conditional.** **Independent corroboration that is not mine:** `analytics/search-results/2026-09-20.json` shows `blocked_domains: null` on **all five** calls that passed `allowed_domains` (S1–S4, S6) and non-null only on the one call that passed none (S7) — a pattern consistent with the exclusivity and recorded by a different seat before the exclusivity was known. No exit taken.

**Entry 2**
- **P** — `allowed_domains` admits subdomains of the value passed, and one such row today is at a host not on `ALLOWED_HOSTS`.
- **Q** — `"allowed_domains": ["unctad.org"]` against `"host": "investmentpolicy.unctad.org"`; and `"url": "https://files.pca-cpa.org/pcadocs/..."` under a list containing `pca-cpa.org` [`analytics/search-results/2026-09-21.json`, calls S1 and S4]
- **D** — my own search record, 2026-09-21. **Outcome on the point cited: it establishes what the engine returned under those filters, and nothing about any resource.**
- **V** — checked 2026-09-21 by a programmatic host-derivation over all 90 rows. Rigid designators: `unctad.org`, `investmentpolicy.unctad.org`, `files.pca-cpa.org`. Three observations (S1, S3, S4), **not independent** — one engine, one session. **Referent clause for the finding this does not retire:** `analytics/insights.jsonl:198`'s four out-of-set rows are not subdomains of any value in that call's list, so suffix-matching does not explain them and **that finding is unchanged.** No exit taken.

**Entry 3**
- **P** — One identical eleven-word engine title is carried by three distinct URLs across two hosts.
- **Q** — `"title": "NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble"` at `.../treaty-files/2412/download` and at `.../laws/italaw6187(6).pdf` [`analytics/search-results/2026-09-20.json`, S1 result 4 and S3 result 9], and at `.../laws/italaw6187(14).pdf` [`analytics/search-results/2026-09-21.json`, S3 result 1]
- **D** — two committed search artefacts of this project, read at `256cb6e`. **Outcome: establishes what one engine returned on two days. It establishes nothing about the three files, and in particular does not establish that any of them is NAFTA.**
- **V** — checked 2026-09-21 by a scan of the 2026-09-20 artefact for titles containing `NORTH AMERICAN`, returning **4 rows**. **Referent clause for the two that are not carriers:** S3 r5 and S3 r8 carry the different string `IN THE ARBITRATION UNDER CHAPTER 11 OF THE NORTH AMERICAN FREE TRADE AGREEMENT` at case-document URLs. The exact-string carriers are 2 in that file and 1 in today's — three in total. No exit taken.

**Entry 4**
- **P** — `https://www.italaw.com/investment-treaties/china-germany-bit-2003` has been in this repository's committed record since 2026-06-26, and no relay row has ever been fired at it.
- **Q** — `[(italaw)](https://www.italaw.com/investment-treaties/china-germany-bit-2003)` [`STATE_OF_THE_ANSWER.md`, the China–Germany BIT (2003) entry, at `256cb6e` — cited by text and commit, not by line, per `0918-B7`], and the same URL in the `sources` array of [`analytics/insights.jsonl:10`, dated 2026-06-26].
- **D** — this repository at `256cb6e`, read 2026-09-21.
- **V** — checked 2026-09-21. **First limb:** the URL screens to carriers at `STATE_OF_THE_ANSWER.md`, `analytics/daily-research/2026-06-26.md:41` and `analytics/insights.jsonl:10`; a fourth locus, `analytics/daily-research/2026-08-05.md:205`, restates it. **Referent clause for the nearby nonzero that is not a carrier:** `analytics/daily-research/2026-08-05.md:364` names a *`china-switzerland`* URL of the same shape and names it **as rejected**, being a constructed URL; it is a different string and supports nothing here. **Second limb (the absence):** `grep -rc "china-germany-bit-2003" analytics/fetch-requests/ analytics/fetch-results/` returns a hit only in **today's own request file**, which I wrote. **Verdict on the absence limb is weaker for want of a rigid designator — an absence has none, and I write that down as the rule requires; it is stated as a fact about this repository only.** No exit taken.

### 3.4 Gap markers

`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested — Whether any host on ALLOWED_HOSTS serves NAFTA Chapter Eleven in a content type from which excerpt_of can produce text remains UNRESOLVED at the moment I file. Today's six searches produced HTML-shaped candidates at www.italaw.com at engine-title strength only; nothing was retrieved. The batch at analytics/fetch-requests/2026-09-21-daily.json tests it, and my pre-registered prior is that rows 1-6 refuse.`

`GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread — Unchanged and untouched by this session. No treaty body has been read by any seat.`

**I open no third slug.** The closure-condition ambiguity at §2.1 is a defect in a marker's wording, not a persistent gap, and opening a slug for it would inflate the escalation counter against something one sentence from you fixes.

### 3.5 Insights — dedup screen printed FIRST

Screened against `analytics/insights.jsonl` (198 lines at `256cb6e`) and `STATE_OF_THE_ANSWER.md`:

| term | insights.jsonl | STATE_OF_THE_ANSWER.md |
|---|---|---|
| `mutually exclusive` | 0 | 0 |
| `blocked_domains` | 1 | 1 |
| `allowed_domains` | 2 | 1 |
| `subdomain` | 0 | 0 |
| `suffix` | 1 | 0 |
| `engine title` | 0 | 0 |
| `indexed title` | 0 | 2 |
| `italaw6187` | 0 | 0 |
| `/laws/` | 0 | 0 |
| `investment-treaties` | 4 | 4 |
| `path-sensitive` | 0 | 0 |
| `path shape` | 2 | 0 |
| `non-PDF` | 0 | — |
| `one ok row` | 0 | — |

**Referent clauses for every nonzero I rely on.** `allowed_domains` / `blocked_domains` — lines 196 and 198, both 2026-09-20; **:198 is the leak finding and I am not re-logging it**, and **:196 is the UNCTAD locator finding**. `suffix` — line 121, a Tethyan `ARB/12/1` post-award block, unrelated. `path shape` — line 82 (Hela Schwarz award access) and line 197 (the UNCTAD `treaty-files` path); neither is italaw. `investment-treaties` (4/4) — **all four are UNCTAD IIA-database URL segments** for the China–Germany, China–Netherlands and China–Switzerland BITs and the Wingtech entry; **none is italaw's `/investment-treaties/` path**, which is why my Entry 4 carriers are the `sources` array of `:10` and not these.

**One thing the screen caught that I would otherwise have logged wrongly.** `analytics/insights.jsonl:73` (2026-08-18) **already holds** that italaw's refusals are path-discriminating within a single run, client held constant, with the byte-level evidence. **I do not re-log it and I cite it instead.** Re-verification is not corroboration (taxonomy 16).

**Four insights clear the screen. I log four, and no fifth.**

```json
{"date": "2026-09-21", "thread_id": "INSTRUMENT-websearch-allowed-and-blocked-domains-are-mutually-exclusive", "ring": "Ring 1 / instrument", "insight": "THE COUNCIL'S TWO MANDATORY SEARCH PARAMETERS CANNOT BOTH BE PASSED, AND THAT RE-SCOPES A SUSTAINED OBJECTION AGAINST A PRIOR SEAT. WebSearch returns, verbatim, 'Error: Cannot specify both allowed_domains and blocked_domains in the same request' and yields zero rows. The 2026-09-20 close-out next step 6 required blocked_domains ['dailyjus.com','unis.unvienna.org'] on EVERY call, and the same brief required allowed_domains to scope calls to candidate allowlisted hosts. THEY ARE NOT JOINTLY SATISFIABLE. CONSEQUENCE FOR 0920-B6, WHICH WAS SUSTAINED ON A DENOMINATOR OF SEVEN: reading the filters recorded in analytics/search-results/2026-09-20.json, calls S1, S2, S3, S4 and S6 all passed allowed_domains, so on FIVE OF THE SEVEN CALLS COMPLIANCE WAS MECHANICALLY IMPOSSIBLE. Only S5 (no filters at all) and S7 (blocked_domains ['dailyjus.com'], one of the two required hosts) were calls where compliance was available. THE HONEST DENOMINATOR IS 2, NOT 7 - the objection is correct as to S5 and S7 and over-scoped as to the rest, and nobody could have known because nobody had tried to pass both. THE SUBSTITUTE PRACTICE ADOPTED TODAY AND OFFERED FOR ADOPTION: pass a single-host or small allowed_domains list containing NEITHER prohibited host, which excludes them by construction, and then SCREEN every returned row for both and report the count. Today: 90 rows, dailyjus.com 0, unis.unvienna.org 0. THE RESIDUAL RISK IS NAMED AND NOT WAVED AWAY: analytics/insights.jsonl:198 records that allowed_domains did not constrain one multi-host result set, so exclusion-by-construction is not a guarantee, which is why the screen is reported rather than promised. WEAKNESS: one invocation, not re-run; whether the refusal is deterministic or conditional is untested.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json"], "confidence": 0.9}
{"date": "2026-09-21", "thread_id": "INSTRUMENT-websearch-allowed-domains-admits-subdomains-including-non-allowlisted-hosts", "ring": "Ring 1 / instrument", "insight": "allowed_domains MATCHES SUBDOMAINS, AND TODAY IT HANDED THIS COUNCIL A LOCATOR ITS OWN RELAY MAY NOT FETCH. Three observations, all in analytics/search-results/2026-09-21.json: call S1 passed exactly ['unctad.org'] and 4 of 10 returned rows are at investmentpolicy.unctad.org; call S3 passed exactly ['italaw.com'] and 10 of 10 rows are at www.italaw.com; call S4 passed 'pca-cpa.org' and returned https://files.pca-cpa.org/pcadocs/... THE CONSEQUENCE THAT BITES: files.pca-cpa.org is NOT on scripts/fetch_relay.py ALLOWED_HOSTS while pca-cpa.org IS, so a search a seat believes it has restricted to an allowlisted host can return a locator validate() will reject. A screen of all 90 rows against ALLOWED_HOSTS at commit 256cb6e returns 89 in set and 1 out, and no batch row was proposed for the one. SECOND CONSEQUENCE, FOR HOW WE READ OUR OWN SCOPING: a single-host allowed_domains call has NOT searched only that host, so 'this host carries nothing on X' is not available from such a call - which is the same bar analytics/insights.jsonl:198 set by a different route. THIS DOES NOT RETIRE THAT FINDING AND IT IS UNCHANGED: the four out-of-set rows it records (cdn.clinicaltrials.gov x2, image-ppubs.uspto.gov, www.ncbi.nlm.nih.gov) are not subdomains of any value in that call's list, so suffix-matching does not explain them. Today refines it; it does not absorb it.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py"], "confidence": 0.85}
{"date": "2026-09-21", "thread_id": "METHOD-an-engine-title-does-not-individuate-a-file", "ring": "Ring 1 / instrument; bears on escalation 2", "insight": "ONE ENGINE TITLE, THREE DISTINCT LOCATORS, TWO HOSTS - AND ESCALATION 2 IS CURRENTLY BANKED ON THAT TITLE. The exact eleven-word string 'NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble' is carried by https://investmentpolicy.unctad.org/international-investment-agreements/treaty-files/2412/download (analytics/search-results/2026-09-20.json, S1 result 4), by https://www.italaw.com/sites/default/files/laws/italaw6187(6).pdf (same file, S3 result 9), and by https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf (analytics/search-results/2026-09-21.json, S3 result 1). The 2026-09-20 close-out next step 3 sends the operator to 2412 on the strength of that title, on 0920-N5's reasoning that it is 'the only returned UNCTAD row whose engine title names the agreement in full'. THAT REMAINS TRUE OF THE UNCTAD ROWS AND IS NO LONGER TRUE OF THE CORPUS: the title is shared, so it cannot be what makes 2412 the right place to look. THIS IS 0920-N5'S LESSON IN A SECOND CURRENCY - adjacency in a database is not identity, and an engine title is not a file identity either. WHAT THIS IS NOT: it does not make 2412 the wrong locator, it does not identify any of the three files, and no body was retrieved by any seat. The 2026-09-21 batch tests the two italaw members of the set against each other on content_type, bytes and sha256; on either branch - differing or identical - the title fails to individuate. SCREEN: a scan of the 2026-09-20 artefact for titles containing 'NORTH AMERICAN' returns 4 rows; the two that are not carriers hold the different string 'IN THE ARBITRATION UNDER CHAPTER 11 OF THE NORTH AMERICAN FREE TRADE AGREEMENT' at case-document URLs.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json"], "confidence": 0.9}
{"date": "2026-09-21", "thread_id": "INSTRUMENT-italaw-investment-treaties-path-shape-held-in-our-own-record-since-2026-06-26-and-never-fired", "ring": "Ring 1 / instrument; Ring 3 treaty access", "insight": "THIS COUNCIL HAS HELD A TESTABLE LOCATOR OF THE TARGET PATH SHAPE FOR EIGHTY-SEVEN DAYS AND HAS NEVER SENT ONE ROW AT IT. https://www.italaw.com/investment-treaties/china-germany-bit-2003 sits in this repository's committed record since 2026-06-26 at three loci at commit 256cb6e - the China-Germany BIT (2003) entry in STATE_OF_THE_ANSWER.md, analytics/daily-research/2026-06-26.md:41, and the sources array of analytics/insights.jsonl:10 - and a grep over analytics/fetch-requests/ and analytics/fetch-results/ returns ZERO rows at it. On 2026-08-05 a seat wrote 'www.italaw.com is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT' (analytics/daily-research/2026-08-05.md:205) and then correctly declined to CONSTRUCT a china-switzerland sibling by slug analogy, because that is discovery - AND DID NOT FIRE THE URL THE RECORD ALREADY HELD. Nobody has in the 47 days since. STATED SO IT IS NOT OVERREAD, BECAUSE THIS IS THE 0920-B7 SPECIES OF ERROR: NO SEAT HAS EVER RETRIEVED THAT PAGE. The 2026-06-26 entry describes the direct treaty PDF as 'not directly readable (403)'. What the record holds is a LOCATOR; calling it a treaty-text page is a seat's characterisation, not a retrieval, and nothing here asserts what italaw serves. SEPARATE AND WEAKER LIMB, AT ENGINE-TITLE STRENGTH ONLY: six bounded searches today returned two HTML-shaped locators of the same shape at the same allowlisted host - https://www.italaw.com/investment-treaties/nafta (S5 r6, returned again S6 r1) and https://www.italaw.com/investment-treaties (S5 r1). S5 and S6 are ONE ENGINE TWICE, NOT TWO SOURCES (0920-B5). An engine's indexed title is the engine's. 'Profile & Case History' is on its face a case list, and NOTHING ESTABLISHES THAT EITHER PAGE CARRIES ONE WORD OF CHAPTER ELEVEN. MY PRIOR, PRE-REGISTERED BEFORE FIRING: these refuse - 8 of 8 non-PDF rows ever fired at www.italaw.com have been refused, the bare site root among them, across 12 rows at 70 committed reductions at 256cb6e. THE CITABLE PRIOR FINDING I AM NOT RE-LOGGING: italaw's path discrimination within a single run, client held constant, is already at analytics/insights.jsonl:73 (2026-08-18).", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/STATE_OF_THE_ANSWER.md", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/insights.jsonl", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/daily-research/2026-08-05.md", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json"], "confidence": 0.85}
```

### 3.6 `candidate_claims`

```json
{"candidate_claims": [
  {"claim_text": "The WebSearch tool available to this session rejects any request that specifies both allowed_domains and blocked_domains, returning an error and zero results.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "Error: Cannot specify both allowed_domains and blocked_domains in the same request",
   "supporting_locator": "analytics/search-results/2026-09-21.json, call S0, field error_verbatim; one invocation, 2026-09-21, not re-run"},

  {"claim_text": "On the 2026-09-20 search record, all five calls that passed allowed_domains carry blocked_domains null, and the only call carrying a non-null blocked_domains passed no allowed_domains.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"allowed_domains\": [\"investmentpolicy.unctad.org\"], \"blocked_domains\": null",
   "supporting_locator": "analytics/search-results/2026-09-20.json, calls S1-S7, read at 256cb6e"},

  {"claim_text": "A WebSearch call passing allowed_domains exactly ['unctad.org'] returned four of ten result rows at the host investmentpolicy.unctad.org.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"allowed_domains\": [\"unctad.org\"]",
   "supporting_locator": "analytics/search-results/2026-09-21.json, call S1; host derivation over all 90 rows run programmatically 2026-09-21"},

  {"claim_text": "The host files.pca-cpa.org is not a member of ALLOWED_HOSTS in scripts/fetch_relay.py, while pca-cpa.org and www.pca-cpa.org are.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"pca-cpa.org\",\n    \"www.pca-cpa.org\",",
   "supporting_locator": "scripts/fetch_relay.py:64-65, read in the working tree at 256cb6e"},

  {"claim_text": "The string 'NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble' appears as the engine-supplied title of three distinct URLs across two hosts in this project's two committed search artefacts.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"title\": \"NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble\"",
   "supporting_locator": "analytics/search-results/2026-09-20.json S1 result 4 and S3 result 9; analytics/search-results/2026-09-21.json S3 result 1. THIS IS A CLAIM ABOUT ENGINE TITLES AND NOT ABOUT THE THREE FILES, NONE OF WHICH HAS BEEN RETRIEVED BY ANY SEAT."},

  {"claim_text": "This repository has carried the URL https://www.italaw.com/investment-treaties/china-germany-bit-2003 since 2026-06-26, and no relay request or result file records any row fired at it.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/insights.jsonl",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"https://www.italaw.com/investment-treaties/china-germany-bit-2003\"",
   "supporting_locator": "analytics/insights.jsonl:10 (2026-06-26); also the China-Germany BIT (2003) entry of STATE_OF_THE_ANSWER.md and analytics/daily-research/2026-06-26.md:41, all at 256cb6e. Absence limb: grep over analytics/fetch-requests/ and analytics/fetch-results/ at 256cb6e returns no prior row."},

  {"claim_text": "Across all 70 committed reductions in analytics/fetch-results/ at commit 256cb6e, www.italaw.com has returned four outcome-'ok' rows, every one content_type application/pdf with an excerpt of length zero, and eight outcome-'refused' rows at status 403, every one at a non-PDF path including the bare site root.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/fetch-results/2026-09-12-daily.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"outcome\": \"refused\", \"status\": 403",
   "supporting_locator": "Enumerated by a json.load walk over all 70 committed result files on 2026-09-21 at 256cb6e; rows at 2026-08-03-control.json, 2026-08-17/18/19 and 2026-09-12-daily.json. Independently re-derives the figure recorded as 0920-N3 at a different commit."},

  {"claim_text": "Six bounded searches restricted to allowlisted hosts on 2026-09-21 returned no locator for NAFTA Chapter Eleven in any non-PDF shape at any of unctad.org, digitallibrary.un.org, uncitral.un.org, undocs.org, docs.un.org, www.un.org, icsid.worldbank.org or pca-cpa.org.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"result_row_count\": 90",
   "supporting_locator": "analytics/search-results/2026-09-21.json, calls S1, S2 and S4. THIS IS A STATEMENT ABOUT WHAT ONE ENGINE RETURNED TO SIX QUERIES IN ONE SESSION. It is 'not found in accessible sources' and never 'does not exist', and per the same file's own suffix-matching record a domain-scoped call has not searched only the named hosts."},

  {"claim_text": "The gap-closure condition as fixed on 2026-09-20 - one ok row with a non-empty excerpt at any allowlisted locator - is, read literally, already satisfied by 134 ok rows with non-empty excerpts at https://example.com/, which is a member of ALLOWED_HOSTS.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "# Neutral control only — never a research source.\n    \"example.com\",",
   "supporting_locator": "scripts/fetch_relay.py:67-68 at 256cb6e; the 134 figure by a json.load walk over all 70 committed result files, 2026-09-21. Offered as a defect in the condition's wording, not as a closure."}
]}
```

### 3.7 Method note (self-training mandate)

**The formulation that worked was the one that asked the site for its own map, not for the document.** S5's query — `italaw.com browse investment treaties and laws index page` — named **navigation vocabulary** ("browse", "index page") and returned, at results 1 and 6, the only two HTML-shaped candidates of the entire session. The four calls that named the doctrine or the treaty (S1, S2, S3, S4) returned **70 rows and not one non-PDF candidate between them.** That extends 2026-09-20's lesson ("when the target is a database record, query the database's vocabulary") one step further and in a more useful direction: **when you need a page rather than a file, query the site's navigation, not its subject matter.** Docket-page-before-document-hunt is not only a rule about what to request — **it is a rule about how to phrase the query.**

Second note, and it is the more transferable one. **The single most valuable locator in today's batch came from no search at all.** It came from a `grep` I ran to check whether a path shape was new, and it had been sitting in our own record for 87 days. **Before spending a search budget on whether a host serves a shape, screen the repository for that shape.** Yesterday's method note said to check our own fetch-results before scoping a search to a host; today's is the same instruction pointed one directory further out — **check the record, not just the reductions.**

---

## 4. For your part

1. **Fire the batch.** `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json` is final and validated. **Do not edit the note.** My prior is on the record: rows 1–6 refuse, rows 7–8 answer, and if that is how it falls **the gap does not close and I will say so in those words.**
2. **Commit the search artefact.** `/home/user/isds-watcher/analytics/search-results/2026-09-21.json` — your skeleton, filled per call as the calls returned, no block edited after its call. **A declared departure you should rule on: I did not paste its 90 result rows into this memo.** The header, the three screens, the parameter-conflict record and every call's purpose, query, filters, independence statement and quarantined summary are above or in the file; the file on disk is the complete, unedited body and is committable as-is. I judged 41KB of JSON inside the daily record worse for the record than a pointer to the file the skeleton was created to be. **If you want it inline, say so and I am wrong, not inconvenienced.**
3. **`0920-B6` needs your ruling, not mine.** I have given you the evidence that its denominator is 2 and not 7. **I do not propose that it be withdrawn** — S5 and S7 were breaches and the practice it protects is sound. I propose the finding be **re-scoped on the record** and that the carried instruction at next step 6 be **rewritten** into a form a seat can actually execute: *pass an `allowed_domains` list containing neither prohibited host, and screen and report every returned row.*
4. **Escalation 2 should be amended before it goes to Emory again, and this is the one concrete thing today buys him.** It currently points at UNCTAD `treaty-files/2412` on the strength of an engine title now known to sit on three locators across two hosts. **Add the italaw siblings and say what each is worth:** `https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf` and `italaw6187(6).pdf` carry the identical title at a host that has answered this relay 200 four times out of four on PDF paths, against UNCTAD's 23 refusals since 10 August. **He has a browser and a PDF reader; we have neither.** The question is unchanged and still costs him two minutes: **does Article 1110 contain an IP-specific internal limitation at a paragraph (7)?**
5. **One sentence from you fixes the closure condition** (§2.1). I did not re-negotiate it and I will not.
6. **Prohibitions:** all carried, none touched. Zero budget spent on the EU Ring-2 object, the Ecuador slug, the Grosse Ruse-Khan slug (**no twelfth row**), A/81/17, the monitored-author check, the Vadi citation, the ARB/24/16 docket, `dailyjus.com` and `unis.unvienna.org` (**0 rows at either across all 90**). The struck Achmea ground is not restated; the Spentech passes stay quarantined; no thread is referred to by array index; the 2026-08-25 asymmetric licence is carried unreleased and nothing today bears on it.

---

*End of the research analyst's verbatim return.*
