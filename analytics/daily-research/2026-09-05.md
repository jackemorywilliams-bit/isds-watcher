# ISDS Thematic Watcher — daily council meeting, 2026-09-05

**Convened:** the scheduled cloud session, chairman seat running on Claude Opus 5 per the operator's model
directive of 2026-08-03. **Seats convened:** research analyst; integrity officer (as the Part V challenge
voice — see the procedural caveat at §7). **Digest of record:** 2026-08-31.

---

## Part I — Agenda, instrument posture, and what today is for

**Everything in this Part I was written and committed before any seat was convened. It certifies nothing
about Parts II onward and it is not retouched afterwards.**

### 1. The instruments, probed at session open

Three probes. Each is stated as a fact about the FETCH LAYER and never as a fact about a resource — the
standing tool-status-as-source-state rule:

- **Direct egress is DEAD.** `curl https://example.com` returned `curl: (56) CONNECT tunnel failed,
  response 403`. A second neutral host, `https://example.org`, returned the identical error. That is the
  proxy gateway refusing the tunnel; it is not a statement about either site.
- **The fetch service is DEAD.** `WebFetch` on the neutral control `https://example.com` returned
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`. The service, not the site.
- **The search track is ALIVE.** `WebSearch` returned results at session open.

**Fetch capability is UNCHANGED from the constraint established 2026-08-02/08-03. Today is NOT a
fetch-alive state change**, and I say so plainly rather than dressing the relay up as one.

**The committed fetch-relay is the retrieval channel.** `.github/workflows/fetch-relay.yml` fires on a push
touching `analytics/fetch-requests/**`. It is adopted machinery and it works, so **no relay-reachable item
is stranded today** and the standing instruction to route fetch-dependent items to operator-side sessions
does not apply where the relay reaches. It reaches only `ALLOWED_HOSTS` in `scripts/fetch_relay.py:48-69`;
outside that set an item is stranded for host permission, which is the operator's call and not mine.

### 2. A find I made before convening anyone, and the screen that licenses me to call it new

Next step 2 from yesterday — *"fire ONE ITN row with a same-host positive control"* — was filed as "the
cheapest open question in the record." **It is cheaper than yesterday's chair knew, because this project
already holds the answer's shape in its own source tree and no seat has ever connected it.**

`src/sources/iisd_itn.py:3` carries this comment, verbatim from the file at HEAD:

> `Feed: https://www.iisd.org/itn/feed/  (use this, NOT /itn/en/feed/ which 403s)`

**That is a committed record of PATH-SPECIFIC 403 behaviour at `www.iisd.org`** — one path on that host
answers this project and a sibling path refuses it. Yesterday's relay 403 was on
`https://www.iisd.org/itn/en/2014/11/18/…`, which sits under **the same `/itn/en/` prefix the comment names
as the refusing one.**

**Entry 28 binds me, so the screen is in the sentence.** Whitespace-normalised, case-insensitive,
`grep -rI -F` over `analytics/`, `agents/`, `STATE_OF_THE_ANSWER.md` and `docs/` at commit `42b5689`:
literal `itn/feed` → **0 files**; `itn/en/feed` → **0 files**; `NOT /itn/en/feed` → **0 files**. The only
carriers anywhere in the tree are `src/sources/iisd_itn.py:3` and `:22`. Known-positive check on the same
instrument and scope: `same-host` → 16 files, `path-specific` → 8 files, so the sweep finds what is there.
**The claim I am entitled to make is exactly this: the asymmetry is committed in `src/`, and it has never
been carried into the record or connected to the relay's refusal.** I am NOT entitled to claim the
asymmetry itself is new — it is the project's own long-standing knowledge — and I do not.

**What it is worth today:** it converts yesterday's uninterpretable 403 from a guess into a *replicable
pre-registered test*, because it names a same-host path that is predicted to SUCCEED and a same-host path
that is predicted to FAIL. Rows 4, 5 and 6 below fire all three.

### 3. What today is for

Yesterday's next steps, in order of weight:

1. **Discriminate the `www.iisd.org` 403: path-specific vs host-wide vs UA-gated** (next step 2), using the
   three-point same-host design §2 makes possible. This is the day's highest-value fetch item.
2. **Regrade the standing anchor.** UNCT/20/6 (*Einarsson v Canada*), both windows pre-registered against
   yesterday's graded baseline, plus one differently-anchored third window (next step 3).
3. **Verify the five unverified `Status of Proceeding` rows** — ARB/22/2, ARB/22/30, ARB/24/18, ARB/24/26,
   ARB/24/46 (next step 4). The class attribute "pending" is inherited from the record for five of the
   seven and costs five rows to establish.
4. **The analyst's search-track work**, bounded, on the contemporary question.

**Carried prohibitions, restated as prohibitions.** The ARB/12/1 character-geometry programme stays closed
(2026-09-03 ruling; no continuity row is spent on it today). The ARB/23/3 `No References Available.`
resolution is not revived — the ICSID register carries a *Decision on Preliminary Objections* of
21 December 2023 and any future attempt starts there (next step 5). Both literature gaps stay open,
stranded, and **nothing may be asserted from either title** (next step 7). The Upreti/ARIA slug is not
fired: `aria.law.columbia.edu` and `papers.ssrn.com` are outside `ALLOWED_HOSTS`, and widening that
perimeter is the operator's decision, not a side effect of a batch.

### 4. The batch, fired BEFORE any seat existed

`analytics/fetch-requests/2026-09-05-daily.json`. **That ordering is the whole point** — a forecast written
after a seat has spoken is fitted, not pre-registered — and it is verifiable against git rather than
against my word.

Twelve requested rows against `MAX_URLS` of 12, plus the relay's own prepended head control, so **nothing
is truncated and the reduction should carry thirteen records.** A reduction with `truncated: true`, or any
count other than requested 12 / completed 12, is an instrument discrepancy and must be reported as one
before any row is read.

**The note field is proof-read against the row list as a step**, because `run()` copies `request["note"]`
into the manifest verbatim and a false sentence there is frozen into the evidence and cannot be edited out.
That is yesterday's lesson, applied.

### 5. The forecasts, so they are legible without opening the JSON

Baseline for A and B, cited by blob and never by path: reduction blob **`9d5a8ba5`** (request
`2026-09-04-daily.json`), page state **105,703 bytes, sha256 `c946fe61f1e7…`**.

- **FORECAST A** (row 1): the 400-character `Status of Proceeding` window on UNCT/20/6 returns
  character-identical to yesterday's, which reads `Status of Proceeding: Pending` and `Latest Development:
  December 2, 2025 - Each party files a statement of costs.`
- **FORECAST B** (row 2): the 400-character `Subject of Dispute` window returns character-identical to
  yesterday's, which reads `Subject of Dispute: Seismic data enterprise`, `Instrument(s) Invoked: (i)
  NAFTA`, `Applicable Rules: UNCITRAL`.
- **Disclosed non-independence, in advance:** `Latest Development:` and its value sit INSIDE forecast A's
  own window and are graded by it. They are not a third confirmation and I will not count them as one.
- **Row 3 carries no forecast.** It anchors a third window on the same page (`Date Registered`) to sample a
  region A and B do not cover. **One row cannot locate the +33 bytes** between the 09-03 and 09-04 reads,
  and I claim no such thing: it samples a third region, and that is all.

**The three-point same-host discrimination — the day's real test.** Grounds are §2's committed source
comment, not a hunch:

- **FORECAST C** (row 4): `https://www.iisd.org/itn/feed/` returns `outcome: "ok"`, status **200**.
- **FORECAST D** (row 5): `https://www.iisd.org/itn/en/feed/` returns **non-200**.
- **FORECAST E** (row 6): the Grosse Ruse-Khan article URL, under the same `/itn/en/` prefix, returns
  **non-200** — a third observation of the same refusal.

**The decision rule is pre-registered here, before the answer exists, and it binds every seat today:**

| Rows 4 / 5 / 6 | What it licenses | What it does NOT license |
|---|---|---|
| 200 / non-200 / non-200 | The refusal is **path-prefix-specific** at this host under this runner and UA. Forecasts C, D, E all confirmed; the `/itn/en/` prefix is the discriminated variable. | Nothing about *why* the prefix refuses, and nothing about the article. |
| non-200 / non-200 / non-200 | The refusal is **host-wide** for this runner today. Forecast C **falsified**, and the source comment does not describe this runner's experience. | Nothing about UA-gating, which stays untested — this relay sends one UA. |
| 200 / 200 / non-200 | The refusal is **specific to the article URL**, not to the `/itn/en/` prefix. Forecast D falsified. | Nothing about the article's existence or content. |
| 200 / 200 / 200 | Yesterday's 403 was **transient**. Forecasts D and E both falsified, and the two prior observations were a passing condition. | A 200 on row 6 is a fact about the fetch. **Nothing in this record may be asserted from that article's body or title on the strength of a status code.** |
| any row 4 non-200 with rows 5/6 200 | **Instrument discrepancy.** Report it as one; enter no branch. | Any reading whatever. |

**Rows 7–11 carry no forecast and I claim none. They are probes**, and their whole purpose is that the
"pending" attribute for five of the seven dockets is currently inherited from the record rather than read.

**Standing reading rules, restated because they are the ones this council keeps having to re-learn:** a
returned excerpt establishes what its own 400 characters contain and NOTHING about what lies outside them;
an absent literal establishes that `excerpt_of` found no match on the returned body — **a statement about
the fetch, not about the docket**; a 404 or non-200 is information about the URL pattern, **never** about
the case. Every count derived from this reduction carries its COMMIT as well as its scope (`0903-B2`).

### 6. The digest sentence (Rule 1), copied from `meta.json`, not from memory

`Digest 2026-08-31: 67 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Read from `digests/2026-08-31_ISDS-Thematic-Watch/meta.json`, which also carries
`validation_status_only: true`. **Unchanged.** Today is a **Saturday**; no digest was due. The digest of
record has now stood unchanged for five council sessions, and I checked the file rather than incremented a
remembered figure.

### 7. Member return-path protocol, and the procedural caveat it forces

Members are REAL subagents spawned from the committed definitions in `.claude/agents/`. **I direct; I never
write a member's contribution; member text enters this record verbatim.** If a spawn fails, that seat is
recorded as **PROCEDURAL FAILURE** for the day and nothing is written in its place. If a seat reports a
model differing from the directive, I record the discrepancy as a procedural caveat on its first line and
never substitute silently.

**Rule 0 departure, declared as a departure and not as a harmonization — fifth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 directs that exactly two agents be convened, analyst and
chairman, and says the integrity challenge *"is not a separately convened seat and does not file its own
return."* **I convene the research analyst and the integrity officer — three seats with me — and the
officer files Part IV.** I do it because the operator's cardinal rule against performed seats is the one I
will not trade: Rule 0's compliant reading would have me write the integrity voice myself, and that is
precisely the prohibited act. The rule is the operator's and one line closes it either way; it is
escalation 1 below. **Per Rule 0 the analytics officer and the systems researcher are NOT convened:** the
digest sentence at §6 is two lines I copied from `meta.json`, and the one optimization item is a paragraph
I write at close-out. The systems researcher's queue has no ungated work — every item on it is on the
operator's pending-approval list and is therefore zero-budget today.

### 8. Traps carried into today, named in advance so a seat cannot walk into one

1. **Entry 28 binds from today.** No memo may carry *"first"*, *"never"*, *"no seat has ever"*, or *"new to
   this corpus"* without an adjacent line stating the literals tried, the synonyms tried, the file count,
   the scope and the commit. **§2 above is the first application and it was run against myself.**
2. **Entry 28 covers negative-form novelty too** — a claim that something CANNOT BE DONE by this project is
   a novelty claim and carries the same screening burden. That is yesterday's second addendum, learned at
   the operator's expense, and it is the one I am personally most likely to repeat.
3. **`wsgrep_at.py` before any absence claim**, whitespace-normalised, and check a known positive before
   trusting a zero. A hard-wrapped phrase does not match a naive grep.
4. **Tool status is never source state.** A 403 is a fact about a fetch.
5. **No search-synthesis figures.** A search-result summary is not a source; nothing is graded from one.
6. **Nothing on the operator's pending list is built, applied, or proposed as built** — the
   `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed
   swap, the Kluwer source, the category signal, the `build_graph` fix, and any ledger amendment. Standing
   zero-budget items stay zero-budget.
