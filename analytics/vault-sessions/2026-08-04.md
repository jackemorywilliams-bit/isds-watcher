# Vault session — 2026-08-04

**Two archivist sessions ran on this date and both records are kept.** The first is the
scheduled currency audit (landed on `main` in PR #51); the second was commissioned by the
Carrying-Span Rule implementation session and covers that change set. Neither supersedes
the other, and merging them into one narrative would misdate findings that were made
hours apart against different trees. Where they disagree, the later one is marked.

---

## Session 1 — scheduled currency audit

**Audited against `b76f6c3`** (merge of PR #49, `origin/main` at session open). Previous
archivist session: 2026-08-03, anchored at `6a5cd2e`.

This is the first session that could run the currency check as a *query* rather than a
judgement, because the 2026-08-03 session adopted the snapshot-anchor convention. Running it
was the right call: the query's first result was that the session which adopted it did not
finish landing its own work.

---

## What I audited, and how

One command set the agenda:

```
git log 6a5cd2e..b76f6c3 -- .claude/agents/ agents/ src/models.py \
    views/isds-workflow-3d/workflow.json analytics/daily-research/ analytics/council-log.md
```

From there: every agent definition against `src/models.py`; every `agents/` vault note against
the council records of 2026-08-01 through 2026-08-04 and the two 2026-08-03 special sessions;
the flowchart manifest against the validator; `HANDOFF.md`, `README.md`, `COUNCIL.md` and
`docs/` against what the project now does; and every remote branch tip against `origin/main`
with `git merge-base --is-ancestor`.

---

## The finding this session exists to catch

**The 2026-08-03 session's change set landed only in part, and three of its recorded claims
are false against `main`.** Its own note reached `main` through the conflict resolution of
`8705f7a`. Nothing else did.

- It recorded creating a `Project Machinery` note. That note exists in **no commit** —
  `git log --all -- 'agents/Project Machinery.md'` is empty — yet three places linked to it,
  so the vault carried three dead links for a day.
- It recorded bringing the chairman, analyst, integrity-officer and analytics-officer notes
  "current with three sessions of adopted rules". Three of the four contained **zero**
  2026-08 content when I opened them.
- Its audit item 4 recorded that "the canonical seventeen-entry table now lives in
  `agents/integrity-officer.md`". That note's taxonomy section read *"extended to ten"*.

This is not a bookkeeping complaint, and the cost is measurable rather than hypothetical. The
fix item 4 claimed to make was the fix for the integrity officer reciting a stale taxonomy in
session. Because it never landed, the defect recurred **twice more** — at
`analytics/daily-research/2026-08-03.md:185` and `2026-08-04.md:535`, each opening from the
same ten-item list while the seat had in fact adopted thirteen further entries.

It is the same failure as the one that prompted this standing session, one layer subtler: on
07-31 the work sat on an unmerged branch; on 08-03 the branch merged and most of the work
inside it did not survive a conflicted merge. Both times the note that would have prevented
the next failure was real, correct, and invisible. **A vault change is not made until it is on
`main`, and the session claiming it must verify it there.** Adopted here; the next session's
first act is to re-run the currency query against what this one claims.

---

## Drift found and fixed

**Agent context — the priority.**

- `agents/integrity-officer.md` — the canonical fabrication taxonomy landed for real:
  **23 entries**, one citation per entry, with the seat directed to read the table rather
  than restate the list. Thirteen entries had never reached the note: entry 11 (`4d5c562`),
  12–14 (`82692a2`), 15–17 (`e9716c8`), 18–23 plus entry 17's extension to *mis-located*
  (`51bb7a2`).
- `agents/council-chairman.md` — four session-protocol rules recorded for the first time:
  the standing-conventions block in the delegation template (`4d5c562`,
  `analytics/daily-research/2026-08-01.md:492`); probe-the-instruments-before-the-agenda
  (`8756859`, `2026-08-04.md:13`); an-objection-is-a-claim-like-any-other (`1109993`,
  `analytics/council-sessions/2026-08-03-verification-system.md:420`); and
  quote-the-whole-line (`51bb7a2`, `2026-08-04.md:762`).
- `agents/research-analyst.md` — three rules recorded: the carrying-span rule (`56cbb75`,
  `analytics/council-sessions/2026-08-03-proposition-rule.md:1450`), carried with its own
  recorded status of **adopted and unvalidated** (`:1617`) rather than flattened into
  "adopted"; the four relay method rules (`51bb7a2`, `2026-08-04.md:761`); and
  no-search-synthesis-figures (`8756859`, `2026-08-04.md:50`).
- **Snapshot anchors on all twelve notes.** The convention was adopted vault-wide on 08-03
  and applied to one note; `grep -c "Audited against" agents/*.md` returned 1 of 12. It now
  returns an anchor for every note, which is what makes step 1 of this session's own contract
  executable next time.
- Models verified clean. Every `.claude/agents/*.md` frontmatter matches its vault note and
  `src/models.py`: chairman and analyst `claude-opus-5`, the four utility seats
  `claude-opus-4-8`, systems-designer and site-experience declaring none. The only commits
  touching `.claude/agents/` or `prompts/` since `6a5cd2e` append a four-line managed `Map:`
  block to `prompts/daily_council_protocol.md` — recorded in `agents/analytics-officer.md`
  so the diff is never mistaken for a protocol amendment.

**Flowchart currency.**

- `agents/Agent Registry.md` recorded "28 nodes, 40 edges" since 07-31. The manifest reads
  **30 nodes, 44 edges** (`0e7d0f7`, `16ab9d9`), and the two builder cards moved from machine
  rows 7–8 to **9–10** to clear the corridor the relay's answer crosses. Corrected.
  `node tools/isds-workflow-3d/validate.mjs` exits 0 and confirms both the manifest and the
  generated SVG; all ten `agents/<name>` targets still resolve.
- The vault called the chart "v3.0", which is `21f0240`'s commit-message naming.
  `meta.version` reads **2.2**. The registry now follows the manifest.
- The overflow defect escalated on 08-03 is **closed** (`1476821`; validator reports "all
  described, no overlaps").

**`HANDOFF.md` — three false statements, all fixed here because this file is mine.**

- Line 29 read heavy tier `claude-opus-4-8` against `HEAVY_MODEL = "claude-opus-5"` in
  `src/models.py`, and against its own line 163. This is the half of `939deaa` that was never
  applied; the 08-03 session recorded fixing it and did not.
- The digest classifier was placed "in `src/classify.py`" when `src/models.py` defines
  `DIGEST_CLASSIFIER_MODEL` and `src/classify.py:58` imports it.
- The `italaw` row asserted in the present tense that the zero-streak guard "flags it
  `DEGRADED`". `state/source_health.json` records `zero_streak: 1`, and with
  `DEGRADED_AFTER = 3` (`src/source_health.py:33`) on a weekly cron the earliest possible flag
  is **2026-08-17**. Corrected on the systems seat's escalation 5 of the 08-04 record, verified
  by me against the code and the state file.

**Records.**

- `agents/Project Change Log.md` — entries added for 2026-08-01 through 2026-08-04, including
  the fetch relay, which the vault had never recorded at all despite its being the largest
  architecture change of the week. The 2026-08-03 block, which had been filed out of order
  between 07-31 and 07-30 in a newest-first log, is now in sequence.
- `agents/Workflow Threads.md` — refreshed at `b76f6c3`. **B1 closes** (the source-health
  guard ran live on 08-03), **C9 closes** (`984f5eb` is on `main`; no PR is open), C7 and C8
  re-verified open, and a new section **F** carries this session's orphan findings.

---

## Orphan check

Every remote branch tip tested with `git merge-base --is-ancestor <tip> origin/main`.
Fifteen fail. Eleven are June-dated feature branches whose content reached `main` by other
routes; four matter, and only one is a genuine accident.

**ACTION — seventeen of your own verification marks have never reached `main`.**
`origin/chore/operator-marks-2026-07-27` (`6f9e1da`, "operator chat-verification sweep —
37/40 verified, Hela Schwarz characterization rejected and corrected") is not an ancestor of
`origin/main`. Its `analytics/verification_ledger.jsonl` holds **38 operator marks and 40
claims**; `main`'s holds **21 marks and 37 claims** (`8891c21`). Seventeen marks and three
claims you made on 2026-07-27 have been sitting unmerged for eight days. Two of them are
marks `HANDOFF.md:147-152` still describes as "awaiting your CLI mark":
`5c25faf36673d6f3` (China–Germany BIT Art. 1(d), `--verified` against the official treaty
text) and `7dd2f272f130f859` (*Hela Schwarz*, `--rejected` — the A&O Shearman page says
jurisdiction *and merits*, not the failure-to-withdraw framing). I did **not** merge this. The
ledger is operator-owned and its integrity is the foundation everything else rests on; a
one-line `git merge` of your own branch is your call, not mine. No council ruling anywhere in
the record disposes of this branch, which is why I am raising it rather than recording it as
settled.

**FOR EMORY — the 2026-08-03 standing-rules council record is gone from version control.**
The 08-03 session escalated `3d31de8` / `analytics/council-sessions/2026-08-03-standing-rules.md`
(887 lines) as sitting on a worktree branch. Today `git cat-file -t 3d31de8` returns *"Not a
valid object name"* and no remote branch carries the file: the worktree belonged to an
ephemeral container that has since been reclaimed. The two rules it adopted — third-party
retrieval, and blocked-vs-quiet source status — **are** on `main` in code (`0091ade`,
`fe02f39`) and later sessions cite them, so the project runs on them correctly. What is lost
is the reasoning and the objections behind them. Nothing in the repository recovers it. If a
transcript or scratchpad copy exists outside git, committing it is the only fix; otherwise the
two rules should be restated from the sessions that cite them, which is council work.

**TO VERIFY — three cloud-run branches are still on origin, four days after the council
recommended removing them.** `origin/claude/sweet-mccarthy-8mouy6` (07-30), `-i95s3k` (07-31)
and `-d5kgmw` (08-01) each carry a complete parallel council record for a date `main` also has
a record for. These are **not** accidents: `analytics/daily-research/2026-08-01.md:452` rules
them non-canonical parallel artifacts, preserved by reference, because they ran
`claude-sonnet-4-6` in seats assigned other models — I checked the ruling before writing them
up, and it changed the finding from "orphaned" to "deliberately preserved". The same close-out
(`:487`, item 2) recommends archiving or deleting them so a parallel record cannot later be
mistaken for canon. Still unactioned. Every future check of this kind has to re-derive their
status from a ruling buried in one day's close-out.

---

## Escalations to other seats

Precise, with file and line, and not touched by me because they are not mine.

**NEEDS YOU — `COUNCIL.md:68` carries a false statement about the digest classifier.** It
locates the model id "in `src/classify.py`" when `src/models.py` defines
`DIGEST_CLASSIFIER_MODEL` and `src/classify.py:58` imports it. Identical to the `HANDOFF.md`
defect I fixed above; `COUNCIL.md` is outside this seat's paths. One-line fix, yours or
whoever you assign it to.

**For the systems designer, on your go-ahead — three machinery defects, all re-verified open
today.**

1. `views/isds-workflow-3d/workflow.json`, the `quality-bar` card, cites
   `src/config.py: threshold 40 / floor 25`. The value lives at `fingerprint.yaml:5`.
   Regenerated from the manifest, never hand-edited.
2. `views/isds-workflow-3d/view.js` has no freshness guard where the generated SVG has a
   fail-closed one.
3. `scripts/build_graph.py:195` still anchors its replacement to the **first** start marker in
   a file, so a note quoting that marker in prose is destroyed on the second run — the defect
   that cost `agents/obsidian-archivist.md` 92 lines. I verified today that no note in scope
   quotes the marker, so the vault is byte-stable; the guarantee remains conditional on a
   convention no mechanism enforces.

**Card-model conflict, open since 2026-07-31 and now harder to see.** The `systems-designer`
and `site-experience` cards assert a model no configuration file carries — neither definition
declares a `model:` key. `939deaa` rewrote both from "Claude Fable 5" to "Claude Opus 5". The
defect is unchanged; it now *reads* as legitimate, because two other seats genuinely hold
Opus 5. Resolution is a `model:` line in each definition or a card reading "inherits the
invoking session". Your call.

---

## What I could not verify

Stated plainly rather than omitted.

- **Whether the seventeen unmerged ledger marks were withheld deliberately.** I searched the
  record for any ruling, escalation or mention disposing of
  `chore/operator-marks-2026-07-27` and found none. Absence of a ruling is not proof there was
  no reason. I am reporting the branch's contents, not asserting an oversight.
- **Whether the lost standing-rules record survives outside git.** I can only see the
  repository. A scratchpad or transcript copy may exist; I cannot check.
- **The eleven June-dated unmerged branches.** I confirmed their tips are not ancestors of
  `main` and did not audit each for unlanded content. Their dates and subjects match work the
  change log records as landed, and the recent branches were where risk actually lived, so I
  spent the check there. If you want the older ones swept, say so and I will.
- **Which model this session actually ran on.** The council's own records note that the
  harness's `opus` alias cannot be resolved to an exact version in-session. I record the same
  limitation rather than assert a version.
- **The published site.** I verified that `scripts/build_site.py` reads only `METHODOLOGY.md`
  and `digests/`, and that no vault note name or card `target` appears anywhere under `docs/`.
  I did not audit the rendered pages for content drift; that is the site-experience seat's
  surface and the 08-04 council record already carries two open professor-facing defects
  against it.
- **No external research this session.** The self-training mandate's second half — surveying
  current Obsidian/PKM practice — was not exercised. Saying so is the point: the one
  generalizable convention this session produced (a vault change is not made until it is on
  `main`) came from this project's own failures, and dressing it as external practice would be
  a fabrication of exactly the kind the integrity officer's taxonomy entry 1 names.

---

## Housekeeping

`python3 scripts/build_graph.py` run over the vault: 107 notes in scope, 215 edges, **0
orphans**. Managed `Map:` blocks added to eight files that had never been graphed — the four
newest daily records, the two 2026-08-03 council sessions, `analytics/fetch-requests/README.md`
and this directory's README. Before running it I confirmed no note in scope contains more than
one start marker, which is the precondition the open `build_graph` hazard leaves the vault
depending on.

**TO VERIFY — this session's own commit fired the fetch relay, and that is a path filter,
not an accident of mine.** `.github/workflows/fetch-relay.yml:16-18` triggers on
`paths: 'analytics/fetch-requests/**'`. `scripts/build_graph.py` added a managed `Map:` block
to `analytics/fetch-requests/README.md` — four lines of vault markup, no request file — and
the push fired a relay runner on PR #51. No request was added, so nothing was fetched, but any
future edit to that directory's documentation will do the same. The narrow fix is to scope the
filter to request files (`analytics/fetch-requests/*.json`) rather than the whole directory.
Machinery, so escalated rather than changed: `.github/` is outside this seat's paths.

Six `WARN`s for exceeding the four-link cap, all expected and all index notes:
`agents/Agent Registry.md` (11), `agents/Workflow Threads.md` (9),
`agents/Project Change Log.md` (6), `agents/obsidian-archivist.md` (6),
`agents/integrity-officer.md` (5) and the pre-existing `think-tank/multi-agent/_MOC.md` (8).
The last three are new this session and are recorded here so they are never mistaken for
drift. A `WARN` naming an ordinary per-agent note *would* be drift; there are none.

<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->

---

## Session 2 — the Carrying-Span Rule implementation

**Audited against** `b76f6c3` (merge of PR #49, the carrying-span council record) plus the
uncommitted Carrying-Span Rule change set in the working tree on branch
`feat/carrying-span-rule`. The slice audited this session: the three agent definitions that
change under that rule, their vault notes, the registry, and the fabrication taxonomy the
council's R8 named as a live stale-context bug.

The rule's adoption record is `analytics/council-sessions/2026-08-03-proposition-rule.md`
(`56cbb75`). **Ruling R3 is the adopted text.** Part 3 §1 of that record is the
pre-amendment draft and is not the rule.

---

## What changed in the vault

- **`agents/research-analyst.md`** — the Carrying-Span Rule added as canonical training item
  3, with the two things about it that are easy to state wrongly written down: it exists
  because a rule that *covered* these citations was not applied (R1), not because a rule's
  scope excluded them; and it reaches three of five uses, which the record forbids rounding
  up. Adopted-method-rule entry and a dated change-log entry added.
- **`agents/research-editor.md`** — the rule added as canonical training item 4, scoped to
  clause 6, which is this seat's: relational and superlative claims about a source are
  propositions and need their own span. Recorded with R4's limit on that clause — it is a
  labelling rule, not a catch, and it caught none of the five uses.
- **`agents/integrity-officer.md`** — three changes, below.
- **`agents/Agent Registry.md`** — roster, flowchart-card mirror, method rules, graph note,
  change log.
- **`agents/Project Change Log.md`** — the dated 2026-08-04 entry carrying the causal chain,
  and one amendment to the note's own citation convention so that a change recorded before
  it is committed must name its file path *and* its branch, in those words.

Nothing in a managed `graph:auto` block was hand-edited. `build_graph.py --dry-run` after
the edits returns to its pre-session baseline exactly: 0 orphans, the same four WARNs, and
the same four notes carrying links to nonexistent notes — every one of them pre-existing.
Two links I wrote during the session did create a fifth WARN and a fifth broken link; both
were caught by the dry-run and removed before finishing.

## The stale-context bug R8 found — closed on the vault side

R8's finding, verified again this session: `.claude/agents/integrity-officer.md` enumerated
**five** fabrication patterns in its self-training mandate while the vault taxonomy in
`agents/integrity-officer.md` stood at **ten**, and the officer reported that the prompt it
was actually instantiated with carried the five. The definition's mandate now points at the
vault note instead of enumerating, so the list cannot drift out of the definition again.

**The taxonomy's current count was ten. It is now twenty.**

- Six original, plus four added 2026-07-31 (`15c8131`) = the ten that were there.
- **Nine more were written by the officer on 2026-08-03 (`56cbb75`) and had never reached
  the vault** — *source-apt-in-topic-not-in-content, deferred-ruling-as-holding,
  homonym-hit-as-confirmation, synonym-zero-as-refutation, near-docket substitution,
  verification-statement-as-performance, narrow-in-the-entry-wide-in-the-body,
  annotation-vagueness-as-compliance, unscoped-rule-read-as-scoped*. This is the item Part 8
  §5 of the council record lists, and it was carried unapplied for a second day. Synced.
- **One new: *amendment-stripping*.** A rule lifted from a session record in its pre-vetting
  draft form, carrying the adoption date and the word *binding*, with every objection that
  conditioned adoption silently absent. Proposed in the integrity officer's vetting note for
  this implementation session — that note is in-session and is not a committed artifact,
  which the taxonomy entry says rather than leaving it to be discovered. Its instance is this
  change set: the designer's pre-amendment draft sits at Part 3 §1 of the council record
  under the heading "THE RULE", 1,150 lines above the ruling that supersedes it, and taking
  it would have shipped a binding rule missing step 3's same-matter exit, the `V`-mark
  referent clause and its parity check, the no-rigid-designator label, and item 5's finality
  condition. It was caught in vetting, and the guard the catch produced is standing at
  `prompts/carrying_span_rule.md:3-7`.

A note on what the mandate rewrite costs: the definition no longer carries the patterns, so
an instantiated prompt no longer contains them and the seat must go read the vault note. That
trades a list that silently drifts for a read that can be silently skipped. It is only the
better trade while the vault note stays current — which is why the count now lives in the
section heading and every extension is dated.

## Drift found and fixed

- **The registry's flowchart-card table was stale on five rows.** `939deaa` moved `chairman`,
  `minutes`, `analyst`, `systems-designer` and `site-experience` to "Model: Claude Opus 5" in
  `views/isds-workflow-3d/workflow.json` and updated the registry's roster table, but not the
  registry's mirror of the cards, which still read "Claude Fable 5". Re-read from
  `workflow.json` and corrected, along with two wrong row numbers.
- **The registry asserted that no `build_graph` WARN names a per-agent note.** That was true
  on 2026-07-31 and is not true now: `agents/obsidian-archivist.md` reports 6 direct links
  against the cap of 4. Corrected and recorded.
- **The taxonomy sync**, above.
- **`agents/Project Change Log.md` was out of its own stated order.** The note's header
  promises newest-first; the file read 2026-07-31, then 2026-08-03, then 2026-07-30, because
  `939deaa` inserted its section above 2026-07-30 rather than at the top. Sorted strictly
  newest-first. No entry's text was edited, no entry changed date, and the trailing managed
  block is untouched.
- **Three seats' canonical prompts** in the roster now include `prompts/carrying_span_rule.md`,
  matching what the three definitions say.

## Found and left alone

Recorded here, not silently rewritten. The first three are factual inconsistencies inside
the new change set; the rest are standards the council did not vote to change.

> **Items 1–3 were RESOLVED the same session, after this note was written.** The archivist
> read the change set while the integrity officer's second vetting note was being applied,
> so all three describe a state that no longer exists. The officer independently reached the
> same finding and made it blocking: four files each stated the script's scope differently.
> The fix was to give the scope a single authority — `scripts/check_marks.py`'s module
> docstring — with `prompts/`, the template and the officer's definition pointing at it
> instead of restating it, and to make the CLI report what it *exercised* on the files given
> rather than name tiers unconditionally. Today's run now prints "mark parity 33 entries —
> 33 in the legacy form, where this reduces to verification-statement presence, because
> P/Q/D/V are absent; nonzero-referent 0 screen records (nothing to check); degenerate
> uniformity 0 four-mark entries (nothing to check)." Item 3's substantive point — that a
> green run today establishes one thing only — is now stated by the tool itself. The
> findings are kept rather than deleted because they were correct when made.

1. **`prompts/carrying_span_rule.md:172-173` understates its own enforcement.** It says
   `scripts/check_marks.py` "implements tier 2 only. Tiers 3–5 are adopted as mechanical and
   are **not implemented**." The shipped script implements tiers 2, 3 and 5 — its docstring
   says so, the Amendment 2 check is at `scripts/check_marks.py:233-248` and the degenerate
   uniformity check at `:250-263`, and its own success line at `:324` prints "R5 tiers 2, 3,
   5". This sentence is not in the council record — `56cbb75` contains neither "tier 2 only"
   nor "not implemented" — so it is not protected council text; it is a description of an
   earlier state of the script that the finished script overtook. It is wrong in the safe
   direction, and it is wrong in the binding rule file.
2. **`lit-review/BIBLIOGRAPHY_TEMPLATE.md:146-148` says the same thing** — that the script
   "does **not** check the nonzero-referent clause". It does.
3. **The success line asserts more than today's run exercised.** `check_marks.py` prints
   "Mechanical checks pass (R5 tiers 2, 3, 5)" unconditionally, but tiers 3 and 5 and most of
   tier 2 only run for entries in the four-mark form. Run today over both committed
   bibliographies: 33 entries, 33 with a verification statement, **0 in the four-mark form**.
   So today's green run establishes one thing — each entry carries exactly one verification
   statement — while the line under it names three tiers. That is the shape of
   *tool-status-as-source-state*, the pattern this project already has on record, and the
   script's own docstring warns against exactly this ("overstating a check is how a green run
   comes to mean more than it should"). Scripts are gated on Emory's sign-off, so this is
   reported rather than patched.
4. **The memos still state only the old standard.** `lit-review/ferguson-memo.md:69` and
   `lit-review/kim-memo.md:68` carry the §VII verification note with its limbs (a), (b), (c)
   — the rule that already existed and was not applied. Neither memo mentions the
   Carrying-Span Rule or the four marks. R3 *adds* a standard; it does not repeal that one,
   and the council did not vote to rewrite the memos' methodological note. Left as written.
5. **`HUMAN_REVIEW.md` states limb (c) operatively but for a different artifact.** Step 2 of
   "What a review cycle entails" is limb (c) in procedural form — open the source and ask
   whether it supports the claim — scoped to sampled cited claims from the digests and
   briefs, not to the lit-review bibliography. Part 8 §4 of the council record proposes
   extending that scope, with the officer's demanded label that it would make the rule
   *recorded, not enforced*. That decision has not been made, and this session did not make
   it.
6. **`METHODOLOGY.md:56` is distinguished, correctly, and untouched.** Its "at least one
   direct verbatim quote" is an anchor requirement, not a support requirement. The
   distinguishing sentence Part 8 §3 asked for is in the rule's own scope paragraph, which is
   where the council put it; `METHODOLOGY.md` itself is unchanged.
7. **`prompts/council_calibration.md:18` does not conflict.** "A line presented as a verbatim
   source quote must be verbatim from an accessible body, or marked N/A" is a rule about
   whether a quote is authentic. The Carrying-Span Rule is about whether an authentic quote
   carries the proposition it is attached to. Same family, different question, no
   contradiction.
8. **`agents/Workflow Threads.md` is four days stale.** Its snapshot is "2026-07-31, at
   `e153ce3` plus the open PR #33", and several sessions have landed since. Not refreshed
   this session — doing it honestly means re-reading `state/research_log.json`,
   `analytics/optimization-log.md` and four daily-research records, which is its own slice.
9. **`agents/obsidian-archivist.md` carries a broken link** to a `Project Machinery` note
   that does not exist in the vault, and exceeds the direct-link cap at 6. Recorded, not
   fixed by inventing the missing note. The archivist's own note is the next slice due.

## What the enforcement establishes — stated as the record requires

`scripts/check_marks.py` establishes **entry SHAPE only**. It does not establish that a span
exists in its source — that is tier 4, and it is done by hand — and it cannot establish that
a span carries its proposition, which is entailment, the rule's declared load, left by R6
exactly where it was. The enforcement surface for the failure that actually occurred was Dr.
Benavides's margin, and it still is.

The rule is **ADOPTED and UNVALIDATED** (R8). It becomes validated when one number is
reported: **`N changed / M screened`** — entries where the rule fired and the citation
actually changed, over entries screened. Read the officer's way, which inverts the naive
reading: **`M > 0` with `N = 0` is a positive detection of performance, not evidence of clean
work.** On the three known instances the true rate is at least 3/3.

Today `M` is 0. No entry in either bibliography is in the four-mark form yet, so the rule has
not been executed on anything.

## One fact the council record has since been overtaken on

R9 ruled that the bibliographies contain zero entries in version control, and gave the rule
no repository precondition partly for that reason. The ruling stands, but its premise no
longer describes the repository: the restore landed at `5b51cd9` (2026-08-03 20:06, one
minute after the council record's own commit `56cbb75`) on a parallel branch, and merged the
next morning at `42374f8`. Both memos are committed, 33 entries between them. That is the
condition Part 8 §1 named — it moves four of five enforcement mechanisms from
human-countable to mechanical, which is what makes `.github/workflows/lit-review.yml`
meaningful rather than aspirational.

## Could not verify

- **The integrity officer's vetting note for this session is not a committed artifact.** The
  *amendment-stripping* pattern is recorded on the strength of what the note proposed, plus
  the instance, which *is* verifiable from files: the pre-amendment draft at Part 3 §1 of
  `56cbb75`, and the standing guard at `prompts/carrying_span_rule.md:3-7`. The taxonomy
  entry says so on its face.
- **Whether the three original failing entries took the form `check_marks.py` describes
  cannot be established from this repository.** They never existed in version control; only
  the corrected entries do.
- **Eleven notes await their managed `graph:auto` block** — the two files this change set
  created, both 2026-08-03 council records, two READMEs, this record, and the four August
  daily-research records. A `build_graph` run is whole-vault, and performing it here would
  put nine files unrelated to the rule into a branch scoped to it. The list is written into
  the registry so the pending run is not lost.

## Escalation

NEEDS YOU: two flowchart cards assert a model that no configuration file carries.
`views/isds-workflow-3d/workflow.json` gives `systems-designer` and `site-experience`
"Model: Claude Opus 5", but neither `.claude/agents/systems-designer.md` nor
`.claude/agents/site-experience.md` declares a `model:` key, and `src/models.py` does not
cover repository-side seats. This was escalated on 2026-07-31 when the same two cards read
"Claude Fable 5"; `939deaa` then rewrote them to Opus 5 alongside the seats that do have
configuration, so the unsourced assertion was updated rather than resolved. The fix is one of
two lines and only you can pick it: add `model: opus` to each definition, or change the cards
to read "inherits the invoking session". The chart is not hand-edited to make the discrepancy
disappear.
