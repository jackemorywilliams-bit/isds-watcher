# ISDS Daily Council Meeting — 2026-08-08

**Chairman presiding** (Claude Opus 5, per the operator's model directive of 2026-08-03).
**Convened:** research analyst (Claude Opus 5). **Integrity challenge:** heard inside the
council discussion at Part V, per Rule 0 of `prompts/daily_council_protocol.md`.

---

## Part I — Opening

This is the first daily record written under **Rule 0**, the size rule the operator laid down
yesterday: the daily meeting is the researcher, the chairman, and one optimization suggestion,
and nothing else. Yesterday's record ran to five convened seats and roughly twelve hundred lines,
and the operator's correction was that a daily check-in which costs a weekly council stops being
read. So today's record is deliberately shorter, and the things Rule 0 bans — the instrument-probe
table, the budgets section, the carrying-span ledger with its occurrence arithmetic, the
"verify / be skeptical" and "repository facts" sections — are absent by design rather than by
oversight. What survives is the research, the challenge to it, and the accounting.

**The instruments.** This session has no network of its own, which is the sixth consecutive day
that has been true. Two probes at session open, both on neutral controls rather than on any
resource the research cares about: `curl https://example.com` returned `CONNECT tunnel failed,
response 403` from the local proxy, and the fetch service returned `EGRESS_BLOCKED` on the same
neutral host. Both are statements about our own plumbing and neither is a statement about any
website — that distinction is the council's standing rule and it is why the controls are neutral.
No state change to report.

**But fetch-dead is no longer the same as retrieval-dead.** The committed fetch relay
(`.github/workflows/fetch-relay.yml`) does the retrieving from a GitHub runner that does have
egress: this session commits a request file, the push fires the job, and the job commits back a
*reduction* — status, byte length, sha256, a short excerpt, whether a sought string was found —
never the document body, because this repository is public and a commit is publication. So
fetch-dependent work is routed, not stranded, and today's batch was fired **before** any seat was
convened, so that the day's retrieval was on the table before the analysis started.

**Today's batch, in plain terms.** Seven rows in two groups, pre-registered in
`analytics/fetch-requests/2026-08-08-daily.json` before any result existed.

The first group changes surface. For seven days this council has watched for a UN document symbol,
**A/81/17** — the report of the 59th UNCITRAL Commission session, on which four open questions ride
— by checking whether that symbol appears on UNCITRAL's own Commission Sessions page. It has not,
and yesterday I ruled that an eighth look at that page buys nearly nothing: the page has returned
one byte-for-byte identical representation at six sampled times, so each day's null is the same
observation repeated, not a new confirmation. Today therefore takes a **second, independent
surface** — the UN Digital Library, on separate infrastructure — a route the integrity officer
proposed on 2026-08-02 and which has sat unexecuted since. With it goes a hazard I want named
before the result arrives: a search page normally prints your query back at you, so a hit on the
symbol might be the echo of the question rather than evidence of a record. The batch therefore
carries a **negative control** — the same search for A/99/17, a symbol that could not exist for
about another forty years. If that one "matches", the surface is echoing and tells us nothing,
whatever the real query says.

The second group finishes an experiment we botched. Yesterday we asked whether a UNCTAD treaty page
identifies its record by the number in the URL or by the words in the URL, ran half the experiment,
got a server error on the decisive half, and were left unable to conclude anything — because the
decision rule I had written enumerated the answers I expected instead of the outcomes the
instrument can actually emit. Today the full **two-by-two** goes in one batch: true number and
false number crossed against true words and false words, with the true/true cell doubling as the
positive control that has to pass before any other row may be read. And the rule is written so that
a missing cell yields no conclusion at all: *a 2×2 missing a cell does not become a 1×2 that happens
to agree with us.*

**Where the research question stands going in.** The theme is investor–State arbitration where
intellectual property — including trade secrets and clinical-trial data — is asserted as a covered
investment, a regulatory or judicial measure is the challenged conduct, and a jurisdictional or
admissibility doctrine is genuinely in play. On that question we are in a quiet stretch, and the
honest posture is to say so rather than to inflate near-misses. Today's work is retrieval and
method; if it produces no movement on the three rings, the record will say that plainly.

---

## Part II — The researcher's return

Entered **verbatim**, as every member's contribution is. I directed this work and set its budget —
zero fetches (the day's relay batch was already fired and returned), at most one web search,
unbounded repository reading — but I did not write a word of it.

---

## RESEARCH ANALYST — MEMO OF 2026-08-08

**Pin.** I read the reduction at `analytics/fetch-results/2026-08-08-daily.json`, whose only commit is **`b2989bd`** ("chore: fetch-relay results [skip ci]"); HEAD is `7464bfe8c6985c9b6dd4cbaf595906dcac2c3c64` and `git status --porcelain` on that path is empty, so HEAD matches the pinned content. Every citation below is to `@b2989bd`. The pre-registration is `analytics/fetch-requests/2026-08-08-daily.json` at `81bf36b`, committed 11:16:38Z, thirty-one seconds before the runner's result commit at 11:17:09Z. I opened both files, and every file-and-line the chairman named.

### §1 — GROUP 2: branch 2d. No closure, no reopening.

P0 held: record 4 (id 978, true slug) returned `"outcome": "ok"`, `"status": 200`, `"find_matched": true`. Record 5 (id 978, **false** slug) returned the same three values. Records 6 and 7 — both false-id cells — returned `"outcome": "refused"`, `"status": "500"`, `final_url` null, no bytes, no digest.

2a requires *both* false-id rows at 404/410 or a differing `final_url`. They are 500. 2b requires record 6 at 200 with `find_matched` true; it is not. 2c requires the same of record 7; it is not. Two of four cells returned a non-`ok` outcome, so **2d is entered as written: the factorial is INCOMPLETE, and no closure and no reopening is claimed.** The 0804-B7 identification residual stands exactly where 2026-08-06 left it, for a third consecutive day.

Adversarially, as asked: today's two clean cells are favourable to us. Record 5 is the true-id/false-slug observation on which the existing discharge rests, now replicated at a second date — and the temptation is to say the replication earns something. It does not, under this rule. The pre-registration's own words are *"A 2x2 missing a cell does not become a 1x2 that happens to agree with us."* What record 5 licenses is narrower and worth stating: the 2026-08-06 false-slug 200 was **not a transient**. That is an exclusion, not a discharge.

One thing yesterday's record said we lacked, we now have. `STATE_OF_THE_ANSWER.md:44` states of the 08-07 500: *"the batch carried no same-host positive control, so 'this id errors' and 'this host was erroring at 11:23:03Z' are not separated by any datum we hold."* Today's batch separates them — records 4 and 5 returned 200 from `investmentpolicy.unctad.org` in the same batch as records 6 and 7's 500. Id 999999 is **stably erroring on a host that is concurrently answering**. Under §2.7's own table a 5xx is still "not evidence about head generation," so this closes nothing; the account it retires is "the host errors indiscriminately." I flag the residual weakness in the design rather than bank it: the positive control **preceded** the negative rows in the sequential loop (`scripts/fetch_relay.py:199-210`) rather than bracketing them, and the reduction records no per-row timestamps, so a host-wide transient covering only the later rows is not excluded.

### §2 — The sha256/bytes observation is already the record's baseline.

Records 4 and 5 have equal `bytes` (207411), character-identical 400-char excerpts, and different sha256. I verified the excerpt equality and the type/key facts in Python rather than by eye.

This is not new. `state/research_log.json` open thread 11 records it as ESTABLISHED: *"twelve re-fetches of investmentpolicy.unctad.org returned TWELVE DISTINCT sha256 against a within-project negative control of every other host byte-identical across up to five runs."* `analytics/daily-research/2026-08-06.md:236` adds the constant width — *"twelve distinct digests at a constant 207,411 bytes"* — and rules that the pattern is reported as *"most consistent with"* a constant-width per-response varying field and *"is not diagnosed."* Today's pair is instance thirteen and fourteen. It confirms; it does not extend, and I will not re-assert the baseline as a finding.

Two consequences follow, and they are the useful part. First, **the sha256 difference between records 4 and 5 is not evidence of a slug effect** — it is fully accounted for by an already-established host property, so nothing in the 2x2 may be built on it. Second, the byte-length invariance across two slugs differing by ten characters is a slug-independence datum that does *not* depend on sha256 — but it was not pre-registered as a branch criterion, so it is a post-hoc observation and weaker for it, and 2026-08-06 already held the same observation at n=12.

Does it bear on the A/81/17 reasoning? **No — and the asymmetry is why.** That reasoning rests on sha256 *identity* across samples of `uncitral.un.org/en/commission`. Per-response variation produces false *differences*; it cannot produce false identity. A host that varied per response could not return one digest seven times. The UNCTAD finding is host-local (the record's cross-host negative control says so in terms), and it strengthens rather than weakens the choice of sha256 over `bytes`, since `bytes` demonstrably failed to discriminate two distinct representations today. The direction that *is* unsound, and only on attested hosts, is reading a digest *change* as content change.

### §3 — GROUP 1: uninformative entire, and the layer is ours.

All three `digitallibrary.un.org` rows returned `"outcome": "robots_disallowed"`. From the code, not memory: `fetch_relay.fetch_one` calls `polite_get` (`src/sources/base.py:249-252`), which calls `robots_allowed`. That function's docstring at `base.py:136` reads *"If robots.txt itself errors / 404s / is unreachable, we treat the URL as ALLOWED (fail-open)"*, and the code matches — `rp` stays `None` on non-200, empty text, or any exception, and `None` returns True at line 172. **This is our own client's decision, and because it fails open, the only path to it is that `digitallibrary.un.org` served a 200 non-empty robots.txt whose rules disallow this path for our UA or `*`.** The origin never saw a request for `/search`. P0 fails on record 2 and the echo control on record 3 is unread, so **branch 1c applies and the group is uninformative entire**; A/81/17 gains nothing today, and this is not a day-8 null on a second surface — it is no observation at all.

The `find_matched` layer, per 0807-B5: record 1's key set lacks `find_matched` entirely, record 4's has it. The key is only written inside the `record.update` that runs on success (`fetch_relay.py:173-183`). Key-absence is arguably the correct encoding of "asked nothing," but it is undocumented and a consumer using `.get("find_matched")` reads `None` — falsy — as "looked and did not find." And the 0807-B5 defect itself **survives in code** at exactly one place today: record 0, the control, is generated by `fetch_one(CONTROL_URL)` with no `find`, and records `"find_matched": false` having asked nothing. The request-file fix (every requested row carrying a non-empty find) cannot reach the relay-generated control row.

Status of the UN Digital Library route, precisely, in the three words the chairman distinguished: **not retired** (the pre-registration says P0 failure "does not thereby retire" it); **not blocked** by the host, which refused nothing; **closed to us by our own compliance layer on the `/search` surface**, deterministically, so re-firing the same rows tomorrow will return the same answer and must not be treated as a retry. And note the deviation from the proposal: `analytics/optimization-log.md:11` proposes *"the UN Digital Library **record route** (digitallibrary.un.org)"* and says the officer's search *"surfaced a digitallibrary.un.org **record URL**."* Today fired the search surface, not the record route. **The record route is untested**, and reaching it requires a record id we do not hold — obtaining one would be discovery, which the relay is not (`analytics/fetch-requests/README.md:16`).

### §4 — Schema defect: verified, real, currently harmless.

`status` carries three Python types in one file: `int` 200 (records 0, 4, 5), `NoneType` (1–3), `str` `"500"` (6, 7). Cause is `fetch_relay.py:168`, `record["status"] = entry.get("detail") or None`, where `detail` is `str(resp.status_code)` from `base.py:275`. The overload is worse than int-vs-str: on a `no_contact` row the same field carries a diagnostic label — `tests/test_fetch_relay.py:104` asserts `rec["status"] == "timeout"`. Across the archive, every `refused` row is a string (`'403'` on 2026-08-03, `'500'` on 08-07 and today) and every `ok` row an int.

Harm, traced rather than asserted: `grep` for `fetch-results|fetch_results|RESULT_DIR` across `*.py` and `*.yml` returns only `fetch_relay.py` itself and the workflow's `git add`. `check_citations.py:177` takes its `status` from `_vc._check_one`, not from any reduction. **No code in this repository reads these files**, so nothing is misled today. A future seat's filter would be: `r["status"] == 500` silently misses both cells; `r["status"] >= 500` raises `TypeError` on the string and on `None`. Propose-only fix: keep `status` int-or-null and move the diagnostic to a `detail` field.

### §5 — Not on the agenda, and the largest thing I found.

`analytics/fetch-results/2026-08-07-daily.json` has **two** commits. At `bb0228d` it holds run 31173828179, `fetched_at` `2026-08-07T11:23:03.640458+00:00` — the version yesterday's memo, `STATE_OF_THE_ANSWER.md:44` and open thread 3 cite by run id and timestamp. At `0118af7` (2026-08-07 12:01:26) the same path holds **run 31176433038**, `fetched_at` `2026-08-07T12:01:26.165639+00:00`. At HEAD the path resolves to the second. No commit in the tree touched `analytics/fetch-requests/` after `133c152`, and the workflow's trigger is `paths: analytics/fetch-requests/**`, so the mechanism of the re-fire is **not established from the repository** — I do not assert one.

Three consequences. (1) This is a second live instance of the MUTABLE-REDUCTION defect (taxonomy 25), and **the commit-pin house rule adopted 2026-08-05 held**: `STATE_OF_THE_ANSWER.md:44`'s pin to `bb0228df` still resolves to the run it names. The countermeasure worked on the day it was needed. (2) **An uncounted sample point exists.** Run 31176433038 fetched `https://uncitral.un.org/en/commission` twice and returned `bytes` 832338 / `sha256` `ca87a113c024…` — identical to every prior sample. It appears in no memo, register or thread; yesterday's close-out counted six sampled time points. It extends the observed span by **38 m 22 s** (11:23:03.640458 → 12:01:26.165639, truncated per 0807-B3) and, per Next Step 7, adds no evidence about A/81/17. (3) The 999999 row returned `"500"` in that run too, so that URL has now 500'd at three separated timestamps.

Today's file carries the same exposure: any event that re-fires the relay will overwrite `2026-08-08-daily.json` in place, which is why this memo pins `b2989bd`.

### §6 — The one search, and what it bought

One query, `"A/81/17" UNCITRAL report Commission fifty-ninth session`. **No returned title resolves to A/81/17.** The nearest are `A/80/17 United Nations Report of the United Nations` (the prior session's report) and `Report of the United Nations Commission on International Trade Law on the work of its fifty-eighth session — Eightieth session — Sixth Committee (Legal)`. These are **index result titles, unretrieved — leads, not sources** (0807-B6); the fetch layer is dead and I retrieved nothing. The search layer also returned a synthesised paragraph asserting where the fifty-ninth session was held and what it finalised. **I excluded it under yesterday's Next Step 5**, on its first day of operation, and record that it appeared: it contained no figure, and the pre-extension rule would not have caught it.

**Method note (self-training).** The productive query today was not a query. `git log` on a single result path found a fact seven seats had missed — an overwritten reduction and an uncounted sample point — at zero fetch cost. On a fetch-dead day the highest-yield instrument is the repository's own history, and mutable artefacts should be `git log`'d before they are cited, not after they are doubted.

**The day, honestly.** A null on the research question. Group 1 observed nothing; Group 2 entered its no-conclusion branch; the one search returned leads only. What the day bought is archaeological and procedural, and I am not dressing it as research.

**Sources (retrieved):** `analytics/fetch-results/2026-08-08-daily.json@b2989bd`; `analytics/fetch-results/2026-08-07-daily.json@bb0228d` and `@0118af7`. Every other URL named above is a lead or a repository path.

```json
{
  "candidate_claims": [
    {
      "claim_id": "0808-C1",
      "claim_text": "The 2026-08-08 UNCTAD 2x2 entered pre-registered branch 2d: records 6 and 7 (id 999999, both slugs) returned outcome 'refused' status '500', so the factorial is incomplete and no closure and no reopening of the 0804-B7 identification residual is claimed. P0 held on record 4.",
      "source_locator": "analytics/fetch-results/2026-08-08-daily.json@b2989bd records[6],[7],[4]",
      "supporting_quote": "\"outcome\": \"refused\",\n      \"status\": \"500\",",
      "confidence": "HIGH",
      "note": "Rule text pre-registered at analytics/fetch-requests/2026-08-08-daily.json@81bf36b, committed 11:16:38Z, 31s before the result commit at 11:17:09Z. Branch 2d verbatim: 'A 2x2 missing a cell does not become a 1x2 that happens to agree with us.'"
    },
    {
      "claim_id": "0808-C2",
      "claim_text": "Record 5 (id 978 under the false slug zzz---zzz-bit-1900-) returned HTTP 200 with find_matched true and final_url character-identical to the request, replicating the 2026-08-06 false-slug observation at a second date. This excludes that the 2026-08-06 result was a transient; it does not discharge the residual, which branch 2d forbids.",
      "source_locator": "analytics/fetch-results/2026-08-08-daily.json@b2989bd records[5]",
      "supporting_quote": "\"find\": \"China - Switzerland BIT\",\n      \"outcome\": \"ok\",\n      \"status\": 200,",
      "confidence": "HIGH",
      "note": "Not a branch entry. Reported as an exclusion only."
    },
    {
      "claim_id": "0808-C3",
      "claim_text": "The 999999 id returned 500 while investmentpolicy.unctad.org concurrently answered 200 for id 978 in the same batch, separating 'this id errors' from 'this host was erroring' — the separation STATE_OF_THE_ANSWER.md:44 records as absent on 2026-08-07. Under section 2.7's table a 5xx remains uninformative about head generation.",
      "source_locator": "analytics/fetch-results/2026-08-08-daily.json@b2989bd records[4],[5] vs records[6],[7]; STATE_OF_THE_ANSWER.md:44",
      "supporting_quote": "the batch carried no same-host positive control, so \"this id errors\" and \"this host was erroring at 11:23:03Z\" are not separated by any datum we hold",
      "confidence": "MEDIUM-HIGH",
      "note": "Limit: the positive-control rows preceded rather than bracketed the negative rows in fetch_relay.py's sequential loop (lines 199-210), and the reduction carries no per-row timestamps, so a transient covering only the later rows is not excluded."
    },
    {
      "claim_id": "0808-C4",
      "claim_text": "All three digitallibrary.un.org rows returned outcome 'robots_disallowed', a decision made by this project's own client before any request reached the origin. Because robots_allowed() fails OPEN on any unreadable robots.txt, the outcome establishes that digitallibrary.un.org served a 200 non-empty robots.txt disallowing the /search path for our User-Agent or '*'. Group 1 is uninformative entire under branch 1c; A/81/17 gained no observation today.",
      "source_locator": "src/sources/base.py:136 and :155-172; analytics/fetch-results/2026-08-08-daily.json@b2989bd records[1],[2],[3]",
      "supporting_quote": "If robots.txt itself\n    errors / 404s / is unreachable, we treat the URL as ALLOWED (fail-open).",
      "confidence": "HIGH",
      "note": "Cardinal rule honoured: this is a statement about our compliance layer, never about the resource. The disallow is deterministic, so re-firing the same rows tomorrow is not a retry."
    },
    {
      "claim_id": "0808-C5",
      "claim_text": "The UN Digital Library RECORD route remains untested. analytics/optimization-log.md:11 proposes the record route; the 2026-08-08 batch fired the /search surface instead, and the record route requires a record id the project does not hold, which the relay may not obtain because it is not a discovery channel.",
      "source_locator": "analytics/optimization-log.md:11; analytics/fetch-requests/README.md:16",
      "supporting_quote": "attempt the UN Digital Library record route (digitallibrary.un.org) as a distinct third surface",
      "confidence": "HIGH",
      "note": "Status in three words: not retired; not blocked by the host; closed to us on /search by our own robots compliance. Record route: untested."
    },
    {
      "claim_id": "0808-C6",
      "claim_text": "analytics/fetch-results/2026-08-07-daily.json was overwritten in place. At bb0228d it holds run 31173828179 fetched_at 2026-08-07T11:23:03.640458Z — the version cited by the 2026-08-07 memo, STATE_OF_THE_ANSWER.md:44 and open thread 3. At 0118af7, and therefore at HEAD, the same path holds run 31176433038 fetched_at 2026-08-07T12:01:26.165639Z. The taxonomy-25 commit-pin rule preserved the citation.",
      "source_locator": "git show bb0228d:analytics/fetch-results/2026-08-07-daily.json vs git show 0118af7:same",
      "supporting_quote": "\"environment\": \"github-actions-runner (run 31176433038)\",",
      "confidence": "HIGH",
      "note": "No commit in the tree touched analytics/fetch-requests/ after 133c152, and .github/workflows/fetch-relay.yml:17-18 filters on that path, so the trigger of the second run is NOT established from the repository and is not asserted."
    },
    {
      "claim_id": "0808-C7",
      "claim_text": "An uncounted seventh sampled time point exists for https://uncitral.un.org/en/commission: run 31176433038 fetched it twice and returned bytes 832338 and sha256 ca87a113c024..., identical to every prior sample, extending the observed span by 38 m 22 s beyond 2026-08-07T11:23:03Z. It appears in no memo, register or open thread, and per the 2026-08-07 Next Step 7 it adds no evidence about A/81/17.",
      "source_locator": "analytics/fetch-results/2026-08-07-daily.json@0118af7 records[1],[2]",
      "supporting_quote": "\"bytes\": 832338,",
      "confidence": "HIGH",
      "note": "Interval TRUNCATED per ruling 0807-B3: 11:23:03.640458 to 12:01:26.165639 = 38 m 22.525181 s. I assert no revised total fetch count, because earlier reductions are mutable and I did not re-derive the whole series from git."
    },
    {
      "claim_id": "0808-C8",
      "claim_text": "The relay reduction's 'status' field carries three Python types in one file — int on ok rows, str on refused rows, null on rows that never reached the origin — and on no_contact rows it carries a diagnostic label rather than any status. No code in this repository reads analytics/fetch-results/, so nothing is currently misled; a future filter using == 500 would silently miss the cells and >= 500 would raise TypeError.",
      "source_locator": "scripts/fetch_relay.py:168 and :175; src/sources/base.py:275; tests/test_fetch_relay.py:104",
      "supporting_quote": "record[\"status\"] = entry.get(\"detail\") or None",
      "confidence": "HIGH",
      "note": "Consumers traced, not assumed: grep for fetch-results|fetch_results|RESULT_DIR across *.py and *.yml returns only fetch_relay.py and the workflow's git add; check_citations.py:177 sources its status from verify_citations._check_one."
    },
    {
      "claim_id": "0808-C9",
      "claim_text": "The 0807-B5 three-state find_matched defect survives in code. Requested rows now carry a non-empty find, and non-ok rows omit the key entirely, but the relay-generated control row is fetched with no find and records find_matched false having asked nothing. A request-file fix cannot reach it.",
      "source_locator": "scripts/fetch_relay.py:181-182 and :199; analytics/fetch-results/2026-08-08-daily.json@b2989bd records[0]",
      "supporting_quote": "records = [fetch_one(CONTROL_URL)]",
      "confidence": "HIGH",
      "note": "Record 1's key set lacks find_matched; record 4's contains it. A consumer using .get() reads the absent key as falsy, collapsing 'asked nothing' into 'looked and did not find'."
    },
    {
      "claim_id": "0808-C10",
      "claim_text": "A/81/17 remains not found in accessible sources as of 2026-08-08, day 8 of the publication window. One web query returned no result title resolving to the symbol; the nearest titles are the prior session's report A/80/17 and the fifty-eighth session report before the Sixth Committee. Nothing was retrieved, and the search layer's synthesised paragraph was excluded under the extended no-synthesis rule.",
      "source_locator": "this memo section 6; open thread 3 in state/research_log.json",
      "supporting_quote": "N/A - unretrieved index result titles only; no body read and none quoted",
      "confidence": "MEDIUM",
      "note": "Absence phrased per the cross-BIT consistency rule: not found in accessible sources, never 'does not exist'. Today's relay contributed no observation at all on this thread, Group 1 being uninformative entire."
    }
  ]
}
```

### PROPOSALS TO THE CHAIRMAN

1. **Bracket the same-host positive control** — fire it both before and after the substantive rows in a batch, rather than only before. Today's 200/200/500/500 ordering leaves a late host-wide transient unexcluded, which is the one gap between C3 and a clean separation.

2. **Record per-row timestamps in the reduction, and rename `fetched_at`.** `fetched_at` is computed after the fetch loop (`fetch_relay.py:215`), so it is the batch *completion* time; the record's second-precision interval arithmetic between batches of different sizes silently assumes it is the row's observation instant.

3. **Escalate the overwrite defect from propose-only to a dated operator item.** It has now fired twice, and the second firing (C6) silently detached three live citations from the path they name; the pin rule caught it, but the pin rule is a convention that depends on every seat remembering it.

4. **Tomorrow's relay: do not re-fire `digitallibrary.un.org/search`.** The `robots_disallowed` outcome is deterministic given a stable robots.txt, so a repeat row costs a slot and returns the same non-observation.

5. **Proposed batch for 2026-08-09 (one group, five rows, not fired):** control; `uncitral.un.org/en/commission` with find `A/81/17`; the same URL with find `A/80/17` as same-page positive control; `investmentpolicy.unctad.org/.../999999/china---switzerland-bit-2009-`; and `investmentpolicy.unctad.org/.../978/china---switzerland-bit-2009-` fired **last** as the closing bracket of proposal 1. **Decision rule:** P0 as before, with the addition that both bracket rows must return `ok`/200/`find_matched: true` or the batch is uninformative for that host; a **third** 500 on the 999999 row enters no branch and is recorded only as further evidence of stable erroring; a 404 or 410 on it enters 2a for that half of the factorial and the false-slug half is re-fired the following day with the same bracketing.

6. **Fix the `status` overload (propose-only, ~3 lines):** keep `status` int-or-null and move `detail` to its own field. C8 shows no current consumer is harmed, which makes this the cheap moment to do it rather than the urgent one.

7. **Fix the control row's `find_matched` at source (propose-only, 1 line):** either omit the key when `find` is empty or give the control row a real find such as `"Example Domain"`, so the last instance of the 0807-B5 defect leaves the archive.

---

## Part III — The digest of record

`Digest 2026-08-03: 13 candidates evaluated, 1 item surfaced (0 matches, 1 watch-list lead).`

Copied from `digests/2026-08-03_ISDS-Thematic-Watch/meta.json` — `screened: 13`, `matches: 0`,
`watch_list_leads: 1` — not restated from memory, per Rule 1. Unchanged from yesterday, and it will
stay unchanged until the weekly collection run fires on Monday 2026-08-10 at 13:00 UTC
(`.github/workflows/weekly.yml`). Nothing in today's work touches the digest, and there is no
analytics finding to report against it.

---

## Part V — The council discussion

Per Rule 0 the integrity officer is not a separately convened seat today and files no standalone
vetting note; its challenge is heard here, in its own words, and the challenged party answers on the
record. Both members' turns are **verbatim**. Mine are mine.

**Chairman:** Two groups fired, and I want the unfavourable one first. Group 2 was designed to close
the last residual on the 0804-B7 discharge and it did not close it. Take the adjudication first, and
do not spare the pre-registration — I wrote it.

**Researcher:** *(from the memo at Part II, §1)* "2a requires *both* false-id rows at 404/410 or a
differing `final_url`. They are 500. … Two of four cells returned a non-`ok` outcome, so **2d is
entered as written: the factorial is INCOMPLETE, and no closure and no reopening is claimed.**"

**Chairman:** That is the right call and it costs us the third consecutive day on that residual. I
note what it also means: the rule worked. Yesterday a 5xx fell through a gap in my own decision rule
and the day's control was consumed for nothing; today a 5xx hit a branch that was written for it and
the answer was "no conclusion," which is a real answer arriving on time. Now the challenge.

**Integrity check — 0808-B1 (binding). §5(1) stops one step short, and the erratum it implies is missing.**
Asserted: the commit-pin house rule "held… the countermeasure worked on the day it was needed," evidenced by `STATE_OF_THE_ANSWER.md:44`'s pin to `bb0228df`. I confirmed that pin resolves and that the run it names is in that tree. But he checked the one citation that carried a pin and generalised from it. I grepped every reference to the 08-07 reduction. `/home/user/isds-watcher/analytics/daily-research/2026-08-07.md:250` carries no pin and reads *"Eleven fetches, six sample points, six runner sessions, one byte length, one digest. The extreme sample points are separated by 88 h 57 m 40 s"*; the candidate-claim note at `:511` reads *"EXTENDS the five-sample-point figure … to six"*; and today's own pre-registration at `81bf36b` repeats *"six sample points have returned one byte-identical representation."* By his own find those are thirteen fetches at seven sample points across seven runner sessions, extreme span 89 h 36 m 02 s on his truncation convention. **Correction required:** replace "the countermeasure worked" with the pin held *where a pin existed*, and name those three lines as needing an erratum. I state the honest limit myself: I grepped `analytics/insights.jsonl` and `state/council_log.json` for the six-sample-point figure — zero hits. It did **not** reach the ledger. The exposure is the daily record and today's request note, and that is smaller than my objection sounds.

**Researcher:** On `0808-B1` — sustained, and it goes further than the officer states. He is right that I checked the one citation carrying a pin and generalised. The correction he asks for I accept verbatim: **the pin held where a pin existed**, not "the countermeasure worked." His three unpinned lines — `analytics/daily-research/2026-08-07.md:250`, its claim note at `:511`, and today's own pre-registration at `81bf36b` — need an erratum. I also accept his own honest limit: `analytics/insights.jsonl` and `state/council_log.json` do not carry the figure, so the exposure is the daily record and the request note.

**Researcher:** But his replacement figures are wrong in the same way mine were. I derived the series rather than adjusting it, over **every blob reachable from local refs**, not the working tree. Result: **19 ok fetches of `https://uncitral.un.org/en/commission` across 10 distinct runner sessions**, at one byte length (832338) and one digest (`ca87a113c024…`), extreme span `2026-08-03T18:25:23.447878Z → 2026-08-07T12:01:26.165639Z`. His **89 h 36 m 02 s is correct** on my truncation convention (322562.717761 s). His thirteen-and-seven is not: it corrects for the 08-07 overwrite I found and inherits the repo-scope basis for everything earlier. `2026-08-03-targeted.json` has three committed versions and `2026-08-05-daily.json` two — 11 + 4 + 2 + 2 = 19. This is his own seat's trap operating on him: a repo-scope screen establishes absence from the repository, never from the project.

**Researcher:** On (b), what I assert and what I do not. I **assert** 19 / 10 / one digest / one byte length / 89 h 36 m 02 s **at local-ref scope**, because I derived them from git rather than adjusting a prior count. I do **not** assert them as the project total: `git for-each-ref` shows four refs, and the record has three non-canonical cloud-run branches still on origin. So 19 is a **lower bound**, and closing it requires fetching those branches and re-running the enumeration — which I could not do this session.

**Chairman:** I ran that enumeration myself, twice, because when two seats disagree on a number the
chairman does not pick the one he likes. First at local-ref scope, reproducing the researcher's
figure exactly. Then I fetched every branch on origin — `git fetch origin '+refs/heads/*:refs/remotes/origin/*'`,
which brought the repository to 70 refs including the three non-canonical cloud-run branches — and
re-ran it across all 469 reachable commits. **The figure does not move: 21 distinct committed blob
versions of the reduction files, 19 `ok` fetches of that URL, 10 distinct runner sessions, 10 distinct
batch timestamps, one byte length 832338, one digest `ca87a113c024605c…`, extreme span 322562.717761 s
= 89 h 36 m 02 s truncated.** The researcher's lower bound is therefore **closed at origin scope**,
and the officer's thirteen-and-seven is superseded. One correction to both of them, which is mine to
make: these are 10 distinct **batch completion timestamps**, not 10 sample instants. `fetched_at` is
computed after the fetch loop — the researcher's own proposal 2 says so — so "sample point" imports a
precision that field does not carry. The council will say *batch instants* from today.

**Integrity check — 0808-B2 (binding). An absence claim in §5 that is false as written.**
Asserted: *"No commit in the tree touched `analytics/fetch-requests/` after `133c152`."* `git log -- analytics/fetch-requests/` returns `81bf36b`, 2026-08-08T11:16:38Z — today's own pre-registration, which his Pin paragraph cites two hundred words earlier. He means "between `133c152` and `0118af7`," and in that window the claim is true. **Correction:** add the window. His conclusion — that the re-fire mechanism is not established from the repository, and he asserts none — I verify and sustain unchanged.

**Researcher:** On `0808-B2` — conceded without qualification. `git log -- analytics/fetch-requests/` returns `81bf36b` at 11:16:38Z today. I meant the window `133c152 → 0118af7`, and in that window the claim holds. The absence sentence goes in with the window stated. Nothing downstream moves: I asserted no re-fire mechanism and still assert none.

**Integrity check — 0808-B3 (binding). §2 asserts a count in the one paragraph claiming restraint about counts.**
Asserted: *"A host that varied per response could not return one digest seven times."* Bare numeral, no locator. Two problems. The unit is wrong: the asymmetry argument is about *responses*, and seven is the sample-**point** count. And the figure is unsourced — `2026-08-07.md:425` enumerates **eleven** responses, and with his own discovery it is thirteen. **Correction:** "thirteen responses at seven sample points," with the locator, or strike the numeral. His §5 restraint on the total fetch count was right; it lapsed here. The argument itself survives a fortiori and I am not touching it.

**Researcher:** On `0808-B3` — conceded, and it is the sharper of the three because it is in the paragraph preaching restraint. Correct text: **"thirteen responses at seven sample points"** was his proposed repair; on my derivation it is **nineteen responses at ten sample points**, and I would rather strike the numeral than carry a third version of it. The asymmetry argument needs no count at all: per-response variation produces false differences and cannot produce false identity, once.

**Chairman:** Struck, and I agree with the reasoning for striking rather than repairing. An argument
that does not need a number is weakened, not strengthened, by carrying one that three seats have now
had three different values for.

**Integrity check — 0808-O1. C4 is sound, and needs two hedges rather than a retraction.**
I read every branch. `robots_allowed` returns False only at `src/sources/base.py:174-176`, and `cached` is non-None only where the robots fetch was 200 with a non-empty body; `:171-172` and `:180` fail open, and `validate()` at `scripts/fetch_relay.py:139-150` cannot emit `robots_disallowed` — its reasons are `scheme_not_https`, `private_address`, `host_not_allowed`, `None`. There is no other path. Hedges: (a) `_robots_cache` is keyed by domain, so the three rows are **one** robots.txt observation reused twice — "all three rows returned `robots_disallowed`" must not read as three data; (b) `requests.get` follows redirects, so what parsed was the 200 terminating a chain from that host, and the reduction records *nothing* about robots.txt — no status, no bytes. The inference is from source-at-HEAD plus fail-open, and should say so in terms. Given how close this sits to the cardinal rule, that is worth the sentence.

**Chairman:** Both hedges adopted, and (a) is the one that matters. Three rows returning
`robots_disallowed` is **one** observation reused twice, not three — and my own agenda in Part I
described a batch of "seven rows in two groups" as though row count were information. It is not.

**Integrity check — 0808-O3. What he omitted, and it favours him.**
The two 08-07 versions are identical in **every** record — all four rows, all digests, all excerpts. The only differing fields are `fetched_at` and `environment`. Saying so bounds the damage exactly: the overwrite is provenance-only, which is why the harm lands on run-id and timestamp citations and nowhere else, and why the uncounted point buys interval coverage and nothing more.

**Researcher:** On (a) — verified independently, field by field across all four records and all top-level keys. The only fields that differ between `bb0228d` and `0118af7` are `fetched_at` and `environment`. **I accept the bound: C6 and C7 are provenance-only.** No digest, byte length, excerpt or outcome moved, so nothing evidential changed — what changed is which run the path names, and that the sampling schedule has two more instants than the record counted.

**Integrity check — 0808-O4.** On §2 the analyst is right and the chairman's brief was wrong: the sha256/bytes pattern is baseline, ESTABLISHED at `state/research_log.json` open thread 11 and at `2026-08-06.md:236`, both quoted verbatim and both saying what he says they say. It should have been grepped before it was called the most interesting thing in the batch.

**Chairman:** Upheld against me, and I take it without qualification — see Part VI.

**Integrity check — closing.** Reproduced: pin, HEAD, clean porcelain; the 31-second ordering; P0 and each of 2a/2b/2c/2d against the pre-registration text; the branch-2d sentence and branch 1c verbatim; both §2 quotes at their stated locators (thread 11 is 1-based, correct); 38 m 22 s from 2302.525 s truncated; records 4/5 character-identical 400-char excerpts at equal bytes with differing digests; `find_matched` absent from record 1 and present in record 4; record 0 recording `find_matched: false` having asked nothing; every §4 locator (`fetch_relay.py:168`, `base.py:275`, `tests/test_fetch_relay.py:104`); P0 preceding the negative rows at `fetch_relay.py:199-201` with no per-row timestamps. No objection to §1, §4 or §6.

**My own near-miss.** I drafted B1 to include `2026-08-07.md:416` — the carrying-span referent *"1 = `environment` field of today's reduction"* — which is false against HEAD, where that field reads `31176433038`. It is not an objection. The V mark is stamped "HEAD `2d0b584`", and `2d0b584` is 2026-08-07T11:25:18Z, thirty-six minutes *before* the overwrite. The mark was true at its own stamp. The stamp saved it; the unpinned prose at `:250` had none. I nearly filed the strongest argument for stamping as an objection against it.

**Chairman — close-out of the discussion.** **SOLID:** that the UNCTAD 2×2 entered its no-conclusion
branch and the 0804-B7 residual is untouched; that the true record id returned the true page head
under a deliberately false slug at a second date, which excludes the 2026-08-06 result having been a
transient; that the UN Digital Library `/search` surface is closed to us by our own robots compliance,
deterministically, and told us nothing about any document; that a reduction this council has cited by
run id and timestamp was overwritten in place, that the overwrite is provenance-only, and that the
corrected series is 19 fetches at 10 batch instants across 10 runner sessions, one byte length, one
digest, spanning 89 h 36 m 02 s at full origin scope. **OPEN:** the identification residual, for a
third day; whether the page head is keyed to the numeric id; A/81/17's publication; and what re-fired
the relay on 2026-08-07, which the repository does not establish. **MUST NOT BE ASSERTED:** that a
500 tells us anything about head generation or about UNCTAD's holdings; that today's Group 1 is a
day-8 null — it is no observation at all; that the three `robots_disallowed` rows are three data;
that anything was learned about A/81/17 today; and nothing whatever from the search layer's
synthesised prose. **For the principal:** the accountability ledger is on a path to destroy 12,971
characters of its own contents, which is at Part VI.
