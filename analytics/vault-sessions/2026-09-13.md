# Archivist session — 2026-09-13

**Audited against `349ca12`** (`main`), on a complete **1,226-commit** history. The container's
clone arrived shallow at **133** — the **seventh consecutive** session — so every sha below was
resolved only after `git fetch --unshallow`.

**The window is `4bbe5cb..349ca12` — 178 commits**, the largest this seat has covered, because the
scheduled sitting of **2026-09-10 did not run**. Six days, not three.

**The short version.** Two of your standing escalations are **fixed** — the re-anchor machinery you
were asked for on 09-07 was built and works, and the site's nine-versus-ten contradiction is gone
after 22 days. Against that, the session found **one open pull request that has quietly cost the
project a sitting's memory**: PR #170 has been open since 2026-09-11 carrying the entire
**2026-09-10 special session** — 1,267 lines — and `main` cannot see it. Three days of daily
councils then numbered their taxonomy adoptions off a table that was accurate for `main` and wrong
for the project. **Numbers 30, 31 and 32 now name two different patterns each.** Nobody did
anything wrong; the branch did not land.

---

## 1. NEEDS YOU — PR #170 has been open two days and `main` has no record that the 2026-09-10 special session happened

This is the failure this seat exists to catch, and it is the 2026-08-03 shape exactly: correct,
real vault memory sitting on an unmerged branch while other seats reason from its absence.

**What is stranded.** `council/archivist-close-out`, tip `7f1d01c`, **1,736 insertions across 12
files**:

| File | Lines |
| --- | --- |
| `analytics/daily-research/2026-09-10-special-session.md` | **1,267 (the whole sitting)** |
| `agents/Workflow Threads.md` | 178 |
| `agents/Project Change Log.md` | 132 |
| `agents/Claim Map.md` | 42 |
| `agents/integrity-officer.md` | 36 |
| `agents/Agent Registry.md` | 31 |
| `moc/Evidence Ledger.md`, `moc/Workflow.md`, `moc/Council.md` | 53 |
| `HANDOFF.md`, `STATE_OF_THE_ANSWER.md`, `agents/systems-designer.md` | 5 |

**Verified, not assumed.** The special-session record does not exist on `main`; no file under
`analytics/`, `agents/` or `HANDOFF.md` on `main` contains the string `2026-09-10-special-session`;
and `analytics/daily-research/` holds no special-session record of any date. **From `main`'s point
of view that sitting did not happen.** It is also the *only* unmerged PR of its batch — #162 through
#173 were opened within minutes of each other on 2026-09-11 and every other one is closed.

**The cost is already realised, and it is not hypothetical.** The special session adopted *untested
counterfactual*, numbered **27** in session; the archivist on that branch filed it at the next free
number, **30**, and reserved **31** and **32** for two proposals it declined to write unsourced —
careful, correct handling. None of it reached `main`. So the daily sittings of 09-11, 09-12 and
09-13 read a heading of "29 entries as of 2026-09-07", which was accurate for `main`, and adopted
six different patterns as **30, 31, 32, 32a, 33, 34**:

| Number | On `main` (daily council) | On PR #170 (unmerged) |
| --- | --- | --- |
| 30 | Re-implementation drift (`28c0db8`) | *Untested counterfactual* — adopted in session as 27 |
| 31 | Assumed-remedy claim (`28c0db8`) | RESERVED, not adopted — *unqueried configuration assertion* |
| 32 | Single-window referent clause (`639c16e`) | RESERVED, not adopted — *same-date execution conflation* |

**The mechanism is one level up from the collision this project already knows about.** The
2026-08-07 collision was caused by a stale *heading*, and the fix — move the heading in the same
change set as the row — held and is not what failed. What failed is that **the change set itself
never landed**, and no heading discipline can reach that. A stale heading produced one collision;
an unlanded branch has now produced three.

> **NEEDS YOU — land or close PR #170.** If it lands, the chairman rules one renumbering across
> both sets and I execute it in a single change set touching every citation. If you close it, the
> special session's record should still be landed on its own: a 1,267-line operator-mandated
> sitting that `main` cannot see is a hole in the project's memory whatever happens to the
> taxonomy question. **I have not renumbered anything** — that changes what a council ruling says.
> Both sets are now recorded side by side in [[integrity-officer]] with a warning above the table.
> Tracked as **D17**.

---

## 2. The routing failure reached five adoptions, and all six entries are now filed

The council escalated this against the routing rather than against me, twice, and it is worth
repeating in their words rather than mine.

**Filed in this change set** — the first time any of these reached a note:

| # | Entry | Adopted | Proposed / adopted |
| --- | --- | --- | --- |
| 30 | Re-implementation drift | 2026-09-11 | `3f25b7f` `2026-09-11.md:702` / `28c0db8` `:962` |
| 31 | Assumed-remedy claim | 2026-09-11 | `3f25b7f` `:704` / `28c0db8` `:962` |
| 32 | Single-window referent clause | 2026-09-12 | `3828137` `2026-09-12.md:1102` / `639c16e` `:1376` |
| 33 | Self-contradicting adjacent enumeration | 2026-09-12 | `3828137` `:1112` / `639c16e` `:1377` |
| 34 | Premise-in-the-pre-registration | 2026-09-13 | `e256de2` `2026-09-13.md:811` / `203dbf6` `:1005` |
| 32a | Boolean-as-count screen record | 2026-09-13 | `e256de2` `:812` / `203dbf6` `:1005` |

Eight rows also added to [[Agent Registry]]'s adopted-method-rules table, including the two
disciplines the council bound on **every seat**: *a claim about what a code path does carries that
path's executed output or is marked UNTESTED* (`28c0db8`, `2026-09-11.md:1006-1016`), and *a count
of a set the same paragraph enumerates is read back against that enumeration before the paragraph
closes* (`639c16e`, `2026-09-12.md:1483`).

**The cost, again recorded by a seat that is not me.** On 2026-09-12 the officer opened the table,
found it heading "29" while the council stood at 31, and numbered from the council's adoptions
rather than the heading — saying so before numbering (`2026-09-12.md:1096`, `3828137`). That is the
correct handling and also the proof of the defect: the one file its mandate names could not be
trusted for the one fact the mandate needs from it. The chairman ruled he escalates *"the routing,
not the archivist"* (`:1374`) and repeated it on 09-13 at `:1162`.

> **ACTION — your choice at D15 is unchanged and is now five adoptions overdue.** Either give the
> daily council write access to the taxonomy table in `agents/integrity-officer.md` alone — a
> one-file scope widening — or accept the latency and have the council's close-out carry an
> explicit *"UNFILED — routed to archivist on `<date>`"* line so the next session finds it by grep
> rather than by luck. I can execute either. **Note the arithmetic changed against us this
> window:** the 2026-09-10 sitting did not run, so the latency window was six days, not three.

---

## 3. D16 is largely remedied — you acted, and it worked

This is the one escalation on the page that moved, and it moved because of a repository change
rather than a vault note.

- `8a9158a` (**PR #152**, 2026-09-08) added `push: branches: [main]` to
  `.github/workflows/reanchor.yml`, so every push to `main` re-anchors `main` itself. The workflow
  header records why: a PR opened by a workflow with the repo token never fires `pull_request`
  workflows, so the council's own PRs merged un-re-anchored.
- `6fb76e6` and `731eb96` (**PR #160**, 2026-09-11) moved the `currency` guard into
  `reanchor.yml` behind `needs: reanchor`, so the guard can no longer read the anchors while the
  mover is still computing them.

**Measured, not assumed.** All **18** `reanchor` runs on `main` conclude `success`. Bot re-anchor
commits reaching `main` have gone from the **2 of 16** that opened this thread to **27 of 47**
across all history. **The squash race D16 names is no longer the live failure.**

I note without complaint that the integrity officer's correction inside that workflow's header —
that `reanchor` must be made required *alongside* `currency`, never `currency` alone, because a
job skipped by its own `if:` reports SUCCESS — caught a real fail-open in the recommendation my
2026-09-07 record gave you. That record's advice was unsafe as written and the officer improved it
before it was applied.

---

## 4. ACTION — the residual: a `[skip ci]` bot commit skips the mover *and* the guard, and `main` ended 8 of the last 10 days red

`main` is in that state as I write. `scripts/check_currency.py` exits **1** at `349ca12`.

I measured this rather than inferring it: for each of the last ten UTC days I took `main`'s tip at
end of day and evaluated every tracked note's anchor against it.

| Day | Verdict | Tip | Writer that staled it |
| --- | --- | --- | --- |
| 09-04 | RED | `4277c6f` | vault-log sent marker |
| 09-05 | RED | `99d3624` | council addendum |
| 09-06 | RED | `101a479` | council addendum |
| 09-07 | RED | `238f0f4` | weekly digest |
| 09-08 | RED | `ec4ca67` | scholar intake |
| 09-09 | RED | `72adf10` | fetch-relay results |
| 09-10 | green | `61152d5` | — (tip was a re-anchor) |
| 09-11 | green | `53987c6` | — (tip was a re-anchor) |
| 09-12 | RED | `3150677` | fetch-relay results |
| 09-13 | RED | `349ca12` | scholar intake |

**One cause, five different writers.** In every red case the tip is a bot commit carrying
`[skip ci]`, landing after the day's last re-anchor. Today: the last `reanchor` run on `main` was
**11:54:27Z** against `f6b693d`; the scholar intake landed at **12:11Z**. Nothing re-anchored, and
nothing reported it — because the same token that skipped the mover skipped the guard.

**Two halves, and only one of them is anyone's simple call.**

> **ACTION — systems-designer, and this half is plainly correct and needs no judgement.**
> `check_currency._is_maintenance` exempts `analytics/daily-research/.sent/` as a pure "an email
> went out" marker. The **vault-sessions** sent marker, `analytics/vault-sessions/.sent/`, is
> structurally identical and is **not** exempt — that omission is the whole of 09-04's red. One
> entry in `_MARKER_PREFIXES`. `scripts/` is outside my paths.

The general half I am **not** proposing as adopted, because it needs pricing I cannot do:
scholar-intake, fetch-results and the weekly digest write *substance*, so exempting them would be
wrong. The two real options are to drop `[skip ci]` from those writers so the push re-anchor fires
— which also makes every guard run on every such push — or to give `reanchor.yml` a scheduled run
after the day's last automated writer. Tracked as **D18**.

---

## 5. Agent-context audit — no contract drift, and the two standing observations are unchanged

- `git log 4bbe5cb..HEAD -- .claude/agents/ prompts/ src/models.py agents/` returns **35** commits
  across the 178-commit window, and **every one** is a currency re-anchor or the 2026-09-07
  session's own. **No seat's contract was edited by anyone.**
- All nine definitions declare `model: opus`; `scripts/check_models.py` exits **0** over 12 cards;
  `node tools/isds-workflow-3d/validate.mjs` exits **0** — 30 cards, 10 chips, 44 edges, SVG fresh
  against its inputs-sha256, zero "Jack" tokens.
- **"Jack" discipline:** professor-facing surfaces (`README.md`, `docs/`, `scripts/site_templates/`,
  `METHODOLOGY.md`) are **clean**. The two known live defects are unchanged and both outside my
  paths: `HUMAN_REVIEW.md:115` and `prompts/council_roundtable.txt:73`.

> **TO VERIFY — D6, fifth first-person observation, day twenty-nine.** I queried this session's own
> runtime again: `configured_model`, `session_context.model` and
> `external_metadata.last_served_model` are all **`claude-opus-5`**.
> `.claude/agents/obsidian-archivist.md:3` and `:24` say "Runs on Claude Opus 4.8."
> **REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`.** Consistent with 2026-08-16, 08-25,
> 09-04 and 09-07. `check_models.py` cannot see this and is not failing — it compares three
> declarations to each other. **I have again left the rows unchanged**, because they record your
> directive and rewriting them would ratify a substitution nobody authorised.

**D5 unchanged at day thirty-one.** Eight of nine definitions never name their seat's own vault
note; the integrity officer's remains the sole exception. §2 above is again the argument for the
remedy: that officer *can* open its table and quote what it found, and did — while six rules bound
to it sat unfiled. Editing `.claude/agents/` is a contract change and is yours.

---

## 6. Deliverable drift — one item closed, one still open after being passed over twice

**21 commits** touched the deliverable surfaces in this window, against 0 last session.

> **Closed after 22 days — the site's self-contradiction is gone.** `docs/how-it-works.html` and
> `scripts/site_templates/how_it_works.html.j2` now carry **zero** occurrences of "nine public
> sources"; the page reads *"the 10 sources it reads — 8 open"* against an SVG saying "THE 10
> SOURCES"; `scripts/check_site_sync.py` exits **0**. Landed by site-experience — `b6edc44`
> (*"two different denominators were printed under one heading"*), `74bd60c`, `e78ebda`, `a7a6d14`.

> **FOR EMORY — `METHODOLOGY.md` §III is still wrong on the same two counts, and what is new is
> that the file was corrected twice in this window without this paragraph being touched.**
> Re-read from the code today:
> 1. `METHODOLOGY.md:33` still says Bing News is *"polled through **eight** fixed,
>    fingerprint-derived queries"*. `len(bing_news.QUERIES)` is **12**.
> 2. That same sentence enumerates seven open repositories plus two added channels — **nine** —
>    while `len(all_sources())` is **10**, and `grep -ci gdelt METHODOLOGY.md` returns **0**.
>
> Meanwhile `5e70b00` (*"five sentences that stopped being true, replaced in place"*) and
> `36d1c37` (*"the sixth stale sentence at :41"*) both landed on this file in this window, and
> `ca81357` fixed the matching gap in `README.md`, which now names GDELT at `:235`. **The
> surrounding paragraphs were audited and repaired; this one was passed over twice.** It is the
> paragraph a reviewer reads to judge the method, and it is still a two-number edit.

---

## 7. Vault mechanics

**Graph:** 174 notes, 362 edges, **0 orphans**, 64 managed blocks would update, 16 WARNs. Growth
since 2026-09-07: **+9 notes, +13 edges**. All 16 WARNs are index or session notes exceeding the
four-link cap — expected under the standing convention, none is drift.

**Broken links: 3, all under `think-tank/`, all long-standing, all outside my paths** — unchanged
from 2026-09-07. No new broken link appeared across 178 commits, and the wrapped-wiki-link defect
of 2026-09-04 has not recurred.

**Thread-ID collisions still open**, and the guard rail is holding: two threads numbered **C11**
and two numbered **C12**. The warning block is present and correct — I verified it rather than
assuming, since an unlanded fix is exactly what this session exists to catch. **Not renumbered**,
for the unchanged reason that outside seats cite these ids. One ruling from you and I update every
citation in a single change set. I did record one improvement: the *third* referent for C11 — the
site's nine-versus-ten page — is now moot on its merits, per §6.

**C8 is still real in the code and still not firing.** `scripts/build_graph.py:198` remains
`pat.sub(block, text)` with no count argument.

**One operational hazard, reported accurately rather than dressed up as a discovery.** Running
`scripts/check_site_sync.py` leaves **21 files under `docs/` modified** in the working tree. This
is **documented, intended behaviour, not a hidden defect** — the script's own docstring says it
rebuilds the site and ignores the wall-clock footer stamp, and `:25` runs `build_site.py` against
the real tree rather than a temp copy, so the stamp is rewritten every time. I confirmed the
mechanism by executing it rather than reading it: restored `docs/`, ran the check alone, and got
21 modified files back with `exit=0`.

> **ACTION — systems-designer / site-experience, small.** The hazard is that a seat which runs the
> check and then stages broadly commits a content-free build-stamp churn across `docs/`. **It
> nearly happened in this session**: I ran the check as part of §6, and those 21 files sat staged
> for commit in a change set that must contain no `docs/` path at all — they would have put this
> PR outside my merge scope and blocked it, for no content change whatever. Two one-line remedies:
> build into a temp directory, or have the script restore `docs/` when it passes. Not mine to fix.

---

## 8. Orphan sweep — 158 remote branches, 31 not ancestors of `main`

The repository squash-merges, so a squash-merged tip is never an ancestor of `main`. I classified
each by diff content rather than counting them.

- **19 carry nothing but a stranded anchor edit** — the tail of the D16 mechanism, harmless now
  that the push-trigger fix has landed.
- **12 carry real unlanded work.** The one that matters is **`council/archivist-close-out`**
  (§1). The others, for completeness:

| Branch | Tip | What is stranded | Age |
| --- | --- | --- | --- |
| `council/archivist-close-out` | `7f1d01c` | **PR #170 — the 2026-09-10 special session, §1** | **2 days** |
| `feat/research-council` | `af00584` | `METHODOLOGY.md` + 8 June digests | 82 days |
| `chore/operator-marks-2026-07-27` | `6f9e1da` | 20 operator ledger marks (**F1**) | **47 days** |
| `claude/sweet-mccarthy-*` (×3) | — | 07-30/07-31/08-01 council records, optimization log | 43–45 days |
| `council/analyst-gaps-and-methodology` | `74958fd` | superseded by PR #165, which merged | 2 days |
| `ci-probe/wrong-pr-cite-2026-09-11` | `c9a6e5f` | a deliberate CI probe, *"do not merge"* | 2 days |
| `vault/archivist-2026-08-28` | `de73f9f` | **PR #110** — five note edits, record already on `main` | 15 days |
| `working/gap-bridge-2026-08-29` | `cada2e5` | 25 insertions, outside my paths | 14 days |
| `council/2026-08-13`, `council/2026-08-26` | — | `.sent` markers only | 18–31 days |

**PR #110 is at day fifteen** and unchanged; its 350-line record is safely on `main` and only the
five note edits are stuck. I did not cherry-pick them, for the reason three prior seats gave and
which still holds: they are live statements about a repository that has now moved far past them,
and I re-derived today's state myself instead.

---

## 9. What I could NOT verify

Stated rather than omitted.

- **Whether the optimization log's seven-sitting silence is deliberate.** `analytics/optimization-log.md`
  holds 51 entries and its newest is dated **2026-09-06** (`7c50d21`). Every sitting from 09-07 to
  09-13 produced its Rule 0 optimization item, and none reached the log. **Two of them say why**:
  09-11 (`:1015`) and 09-12 (`:1455`) each state the item is *"deduped into"* the log as the
  operative form of an existing entry — a deliberate non-entry I do not fault. **For 09-07, 09-08,
  09-09, 09-10 and 09-13 I found no such statement**, and 09-13's item is a concrete new instrument
  finding with a named minimal patch (`fetch_relay.py` answering `newest_request()` lexicographically
  rather than the request the push actually added, `2026-09-13.md:1046`). So somewhere between one
  and five items are unlanded in the ledger built to hold them. **I am not asserting the log is
  wrong** — I could not establish the curation rule, and the log does not state it. Worth one
  sentence from the chair.
- **Why the clone keeps arriving shallow** — seventh consecutive session. I can show it happens; I
  cannot see the container provisioning.
- **Whether `gh` was ever expected in this container.** My standing instructions say to close with
  `gh pr create` and `gh pr merge`; `gh` is **not installed** here. I used the GitHub MCP tools
  instead, which reach the same API. Flagging it so the instruction and the environment can be
  reconciled rather than silently diverging.
- **No external Obsidian/PKM research this session.** My mandate asks for it periodically. The time
  went to §1 and to the ten-day currency measurement, and I would rather say so than dress an
  internal finding as outside reading. Last done: not within the sessions this log covers.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D17** — PR #170 open since 09-11; the 2026-09-10 special session is not on `main`; three taxonomy numbers now collide. **Land it or close it** | **Emory** | new today |
| 2 | **D18** — a `[skip ci]` bot commit skips both mover and guard; `main` ended 8 of 10 days red. One narrow bug is a one-line fix | systems-designer / **Emory** | new today |
| 3 | **D15** — five adoptions routed to `agents/` before any could be filed; the 09-10 sitting's absence widened the window to six days | **Emory** (routing or cadence) | 6 days |
| 4 | **D7** — `METHODOLOGY.md` §III: eight queries against 12; GDELT unmentioned against 10. Passed over by two correction passes this window | **Emory** | carried |
| 5 | **PR #110** — 2026-08-28 session's five note edits still stranded | **Emory** | 15 days |
| 6 | **C11 (taxonomy)** — entry 27 adopted twice; only a ruling closes it. Now compounded by D17 | council-chairman + integrity-officer | 37 days |
| 7 | **Thread-ID collision** — two C11s and two C12s; warning block in place, renumbering needs your word | **Emory** to authorise | 9 days |
| 8 | **D6** — seat models: requested Opus 4.8, served `claude-opus-5`; fifth first-person observation | **Emory** | 29 days |
| 9 | **D5** — eight of nine definitions never name their own vault note | **Emory** (contract change) | 31 days |
| 10 | **F1** — operator-marks branch never merged (`6f9e1da`), 20 ledger marks | **Emory** | 47 days |
| 11 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |
| — | ~~**D16**~~ — squash race | **LARGELY REMEDIED** (§3) | closed today |
| — | ~~**C11 (site)**~~ — nine and ten on one page | **CLOSED** by site-experience (§6) | closed today |

---

## Addendum — D11 fired again on this very pull request, for the fifth consecutive time

Filed during the merge rather than after it, because it happened while I was merging and the
sequence is the evidence.

I pre-ran `scripts/reanchor.py` before opening PR #174 and restored `STATE_OF_THE_ANSWER.md` to
`origin/main`'s content, so that the four `agents/` anchors were already at this session's commit
and the bot would find nothing to move. **That worked for the four notes and not for the fifth.**
`reanchor.yml` fired on PR open and pushed `99820e0`, whose entire content is one line —
`STATE_OF_THE_ANSWER.md`'s anchor moving `f6b693d` → `262e531`. My merge, pinned to the head I had
verified, was rejected **409 Head branch was modified**.

That single line put a path outside `analytics/`, `agents/`, `moc/`, `HANDOFF.md` into this change
set, which is precisely **D11**. I restored the file again in a commit of my own — the same
manoeuvre the 2026-09-01, 09-04 and 09-07 sessions each performed — and merged the restored head,
pinned by sha, after re-running the path check on it. **If you are reading this on `main`, that
worked.**

> **NEEDS YOU — D11 is now the last live piece of the anchor problem, and it is the cheapest one
> left.** You fixed the hard half: the push-to-main re-anchor (§3) means `main` no longer depends
> on any PR's bot commit landing. That makes the PR-level re-anchor **redundant for `main`'s
> correctness** while still being the one thing that makes every archivist PR unmergeable by the
> archivist. Two ways out, both small: **(a)** have `reanchor.yml` skip its `pull_request` trigger
> for branches matching `vault/*`, since `main`'s own push trigger now covers the outcome; or
> **(b)** widen this seat's merge scope by exactly one file, `STATE_OF_THE_ANSWER.md`, whose only
> archivist-side change is ever a machine-written anchor line. **I recommend (a)** — it removes the
> commit rather than authorising it, and it needs no new trust. Five sessions have now spent a
> manual restore on this.
