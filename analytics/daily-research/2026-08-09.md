# ISDS Daily Council Meeting — 2026-08-09

**Chairman presiding** (Claude Opus 5, per the operator's model directive of 2026-08-03).
**Convened:** research analyst (Claude Opus 5). **Integrity challenge:** heard inside the
council discussion at Part V, per Rule 0 of `prompts/daily_council_protocol.md`.

---

## Part I — Opening

**Return-path protocol, stated first because two weeks of sessions were once lost by ignoring
it.** Every member's return enters this file verbatim, the file is committed incrementally to
`council/2026-08-09`, and the branch is opened as a pull request to `main` and merged by me
before this session ends. A record that does not reach `main` is not merely unread — the next
session builds its agenda by reading `main`, so an unlanded record silently corrupts the
council's own memory. That failure mode is not hypothetical: the 2026-08-03 record stated that
no sessions existed for 08-01 and 08-02, and both had run.

**The instruments.** This session has no network of its own, which is the seventh consecutive
day that has been true. Two probes at session open, both on neutral controls rather than on any
resource the research cares about: `curl https://example.com` returned `CONNECT tunnel failed,
response 403` from the local proxy, and the fetch service returned `EGRESS_BLOCKED` on the same
neutral host. Both are statements about our own plumbing and neither is a statement about any
website. No state change to report.

Retrieval nonetheless happened, through the committed relay
(`.github/workflows/fetch-relay.yml`): this session commits a request file, the push fires a
GitHub runner that does have egress, and the runner commits back a *reduction* — status, byte
length, sha256, a short excerpt, whether a sought string was found — never the document body,
because this repository is public and a commit is publication. The batch was fired **before any
seat was convened**, so the day's retrieval was on the table before the analysis began.

**Today's batch.** Six rows in three groups, pre-registered at `248ebf7` (11:16:03Z) and
answered at `50b0c20` (11:16:36Z), thirty-three seconds apart. It is yesterday's close-out
executed rather than reinterpreted: the analyst's proposal 5 amended by his proposal 1, plus
the route-admissibility pre-flight that came out of my own design error yesterday.

*Group A* takes the identification residual that has stood open since ruling 0804-B7 and, for
the first time in this project, **brackets** it: the same-host positive control is fired both
first and last, so that a host-wide transient covering only the later rows is excluded rather
than assumed away. That gap was the one thing standing between yesterday's claim 0808-C3 and a
clean separation, and it was the analyst's proposal, not mine. Group A is a **half-factorial by
construction** and the pre-registration says so in terms: only one of the two false-id cells is
fired, so branch 2d of yesterday governs and today cannot close the residual whatever it
returns. I want that on the record before the result is read, because a design that can only
half-succeed is easy to describe afterwards as having wholly succeeded.

*Group B* is the substantive question of the day, and it is a question about our own eight days
of work. For eight days this council has read `find_matched: false` on the symbol **A/81/17** at
`uncitral.un.org/en/commission` as a null about publication. That page has never once carried a
positive control. Nobody had established that its rendered text carries *any* session-report
symbol — so every one of those nulls was, strictly, a null of unknown meaning. Row 3 therefore
asks the same page for **A/80/17**, the fifty-eighth session's report, which the record already
holds as published. If the page does not carry that either, then eight days of nulls were
uninformative about publication and the surface must be retired for symbol-existence purposes —
retroactively. That is why the control is fired before another day's null is banked, and it is
the correction of a defect that has been in this council's method since 2026-08-03.

> **CORRECTION, entered by the chairman at 11:31Z, before the analyst returned and before this
> file reached `main`. The paragraph immediately above is FALSE and it is my error.** The
> preceding paragraph stands as written, at commit `5d53819`, because striking it would hide the
> failure rather than record it. The same-page `A/80/17` positive control is **not** new and today
> is not its first firing. `grep -l "A/80/17" analytics/fetch-requests/*.json` returns nine files;
> reading the daily reductions directly, every `uncitral.un.org/en/commission` row carrying
> `find: "A/80/17"` returned `ok`/200/`find_matched: true` on **2026-08-04, 08-05, 08-06 and
> 08-07**, each paired in its own batch with an `A/81/17` row returning `ok`/200/`find_matched:
> false`, all eight rows at bytes 832338 and sha256 `ca87a113c024605c…`. The 2026-08-07
> council-log entry says so in terms — *"with the same-page A/80/17 control passing"* — and I did
> not read it before briefing the analyst or writing the pre-registration. **Today is the fifth
> firing of that control, not the first**, branch B1's text ("the FIRST VALIDATED NULL … in nine
> days") rests on a false premise, and my framing of Group B as the substantive question of the
> day is withdrawn. The pre-registration at `248ebf7` is **not** amended: a pre-registration
> edited after its results exist is worthless as a pre-registration, so it stays wrong on the
> record with the correction dated today. I sent this correction to the analyst mid-session rather
> than let it reach his desk as a premise. It is the second consecutive day on which the
> chairman's own brief asserted novelty without the grep that the chairman's own rule requires,
> and it is carried as a sustained failure against me at Part VI.

*Group C* is one row: `digitallibrary.un.org/robots.txt`. Yesterday three of seven rows were
spent discovering that our own compliance layer forbids `digitallibrary.un.org/search`, and the
optimization item I wrote out of that failure was that the first batch touching a new path on a
host should spend a row on the host's policy file instead. Today applies it. The two conditions
I attached to the proposal travel with it as conditions and not decoration: a `robots.txt` row is
itself subject to the same robots check and to the fail-open path, so a null on it would be
uninformative in exactly the way yesterday's Group 1 was; and a 400-character excerpt cannot
carry a long rules file, so the row establishes reachability and the head of the file, never the
full policy. Not fired: `digitallibrary.un.org/search`, whose disallow is deterministic and whose
repetition would be the same non-observation at a second cost; the false-id/false-slug cell, held
for tomorrow's bracketed batch; and the Huawei ARB/22/2 row, a standing weekly whose next firing
the council dated 2026-08-13.

**Budgets, set before the analyst was briefed:** zero fetches, at most one web search, unbounded
repository reading. A spend checkpoint falls after the vetting round at Part V, and if the day
has bought nothing by then it ends there rather than buying something to show.

**One thing I hand the council that I found myself, labelled as the standing rule now requires.**
Yesterday I anchored the analyst with a characterisation I had not screened, and the rule adopted
from that failure binds me: a chairman's brief must carry the screen that establishes what it
asserts, or say plainly that it has not been screened. So: I queried the Actions API for this
workflow's run history and found that the two relay re-fires this council could not explain both
ran on `main`, on a `push` event, seconds after a council pull request merged. I have screened the
run list and the merge timestamps. I have **not** verified that the merge commit is what the
workflow's path filter matched. It goes to the analyst as a hypothesis to verify or refute, not as
a finding, and if it holds it discharges a question the record has twice declined to answer — and
means my own merge today will fire it a fourth time.

**Where the research question stands going in.** The theme is investor–State arbitration where
intellectual property — including trade secrets and clinical-trial data — is asserted as a covered
investment, a regulatory or judicial measure is the challenged conduct, and a jurisdictional or
admissibility doctrine is genuinely in play. We are in a quiet stretch and the honest posture is to
say so. Today's work is retrieval and method; if it produces no movement on the three rings, the
record will say that plainly rather than dress instrument work as research.

---

## Part II — The researcher's return

Entered **verbatim**, as every member's contribution is. I directed this work and set its budget —
zero fetches (the day's relay batch was fired and returned before any seat was convened), at most
one web search, unbounded repository reading — and I issued one mid-session correction to my own
brief. I did not write a word of what follows.

---

## RESEARCH ANALYST — MEMO OF 2026-08-09

**Pin.** I read the pre-registration at `analytics/fetch-requests/2026-08-09-daily.json@248ebf7` (committed 2026-08-09T11:16:03Z) and the reduction at `analytics/fetch-results/2026-08-09-daily.json@50b0c20` (11:16:36Z), thirty-three seconds later. Per my own method note of yesterday I ran `git log` on both paths *before* citing them: each has exactly one commit as of session time, HEAD is `50b0c20`, and `git status --porcelain analytics/` is empty, so HEAD matches both pinned contents. Every citation below is to those two commits or to a named blob. The reduction reports `control_ok: true`, `void: false`, `requested: 6`, `completed: 5` — the last being `sum(1 for r in records[1:] if r["outcome"] == "ok")` at `scripts/fetch_relay.py:221`, so five of six substantive rows returned `ok`, which is correct and not a truncation.

I also record at the outset that the chairman issued a mid-session correction to his own brief, before my return, retracting the premise of his item (2). I have verified that correction independently rather than accepted it, and §3 reports what I found, which goes further than the correction states.

---

### §1 — The re-fire mechanism: verified, and the record's standing "not established from the repository" is discharged

The chairman handed me this screened but unverified, and asked to be checked. It holds, with three corrections to his statement of it and one instance it does not cover.

**The trigger.** `.github/workflows/fetch-relay.yml` fires on `push` filtered to `paths: - 'analytics/fetch-requests/**'`, with no branch filter, and the commit step pushes to `HEAD:${GITHUB_REF_NAME}`. So a push of a request-file change to *any* branch fires the relay *on that branch*.

**The 08-08 instance.** `e2984dc` is a true merge commit — `git cat-file -p e2984dc` shows two parents, `44fad955` (the prior main tip) and `58e8fa5b` (the council branch tip) — committed 2026-08-08T11:43:50Z. The request commit `81bf36b` (11:16:38Z) is **not** on main's first-parent line; testing every first-parent commit of `origin/main` for ancestry, the oldest one containing `81bf36b` is `e2984dc` itself. So the merge push was the first time `analytics/fetch-requests/2026-08-09`… no — the first time *that* request file's change appeared on `main`. The chairman's Actions datum (run 31255633941, branch `main`, event `push`, created 11:43:52Z) sits **two seconds** after that merge, and the result commit `0612d81` lands on main at 11:44:22Z.

**The 08-07 instance, whose merge timestamp the chairman said he had not confirmed.** I confirm it: `23de8b5` "council: daily meeting 2026-08-07 (#65)", committed **2026-08-07T12:01:05Z**, is the oldest first-parent commit of `origin/main` containing the request commit `133c152`. The chairman's run 31176433038 was created 12:01:07Z — again two seconds later — and `0118af7` lands at 12:01:26Z.

**Two further positives the record has never named,** found by the same test on 2026-08-04: merge `#50` (`eb9bd9c`, 12:00:29Z) introduced request commit `b1fdfe7`, and `analytics/fetch-results/2026-08-04-undocs-identify.json` gains a version at 12:01:59Z; merge `#51` (`1755515`, 15:32:59Z) introduced `1334109`, and the same file gains a version at 15:34:20Z. **A fifth, on 2026-08-05:** merge `#55` (`6cd12fc`, 12:09:12Z) introduced `9f6d883`, and `2026-08-05-zz-attribution.json` gains a version at 12:10:01Z — the re-fire landed on the *lexicographically last* request file, not the day's daily file, which is the 2026-08-05 `newest_request()` defect operating inside the merge re-fire and is why that day's overwrite was never read as one.

**The instance it does not cover, and I will not paper over it.** Merge `#57` (`28bb8f3`, 2026-08-06T12:26:47Z) introduced request commit `cc123fb` to main by the same test, and `2026-08-06-daily.json` has **exactly one** committed version (`a8c5ad9`, 11:38:46Z). No result commit follows that merge. Two unrelated commits landed on main at 12:26:57Z and 12:27:21Z — inside the window in which the relay's `git pull --rebase --autostash origin main && git push` would have run — so a push race is a candidate explanation. **I do not assert it.** The repository establishes the mechanism; it does not establish that the mechanism fires every time. The honest score is five of six.

**Rulings that follow.** (a) The standing sentence — `analytics/daily-research/2026-08-08.md:120`, *"the mechanism of the re-fire is **not established from the repository** — I do not assert one"* — is **discharged**. It was correct when written, because the window it screened (`133c152 → 0118af7`) is precisely the window in which no request commit touched main; the commit that did was the merge itself, which the window's own endpoints excluded. The 0808-B2 correction and this discharge are the same fact seen from two ends. (b) **The chairman's "third version" is wrong**: `2026-08-08-daily.json` has two committed versions, `b2989bd` and `0612d81`, and my pin yesterday was to the first. (c) The framing of the overwrite as a defect that "has now fired twice" understates it by an order of magnitude. Across all reachable commits there are **23 distinct committed blob versions of 12 request-named result files — eleven in-place overwrites.** The mutable reduction is not an anomaly; it is the relay's normal operating mode, and the count belongs in the escalation to Emory in that form.

**What it costs today, prospectively.** Merging this session's PR will fire the relay a sixth time on main, overwrite `2026-08-09-daily.json` in place, and add two more fetches to the series below. Everything this memo cites survives because it is pinned to `50b0c20`; the exposure is entirely to any citation that names the path without a commit.

---

### §2 — Group A: both brackets held, A3 entered, and the residual is untouched for a fourth day

P0-OPEN: row 1 returned `"outcome": "ok"`, `"status": 200`, `"find_matched": true`. P0-CLOSE: row 6, the same URL fired last, returned the same three values. Both brackets held, so the batch is informative for `investmentpolicy.unctad.org` and the substantive row may be read.

Row 4 (id 999999, true slug) returned `"outcome": "refused"`, `"status": "500"`, `final_url` null, and — as the chairman observed — **no `find_matched` key at all**. That is correct encoding, not a defect: the key is written only inside the `record.update` that runs on a successful response (`fetch_relay.py:173-183`), so a refused row has never had it. It is the same three-state hazard ruled at 0807-B5, unchanged.

**A3 is entered as written: no branch, no conclusion.** A1 required 200 with `find_matched` true; A2 required 404, 410, or a differing `final_url`. Neither is satisfied. A3's own words govern — the row *"enters NO BRANCH"* and is *"recorded only as further evidence that id 999999 errors stably."* Per METHODOLOGY §2.7 a 5xx remains uninformative about head generation.

**One correction to A3 from my own enumeration.** The pre-registration says this *"would be its fourth separated timestamp."* Derived from git across every reachable result blob rather than adjusted from the prior count: the 999999 URL has returned `refused`/`"500"` **seven times, at five distinct batch instants, across five distinct runner sessions** — 2026-08-07T11:23:03Z, 2026-08-07T12:01:26Z, 2026-08-08T11:17:09Z, 2026-08-08T11:44:21Z, 2026-08-09T11:16:36Z. The numeral in the pre-registration was written before the 08-08 re-fire existed to be counted. Per yesterday's ruling the pre-registration is not amended; the correction is dated today.

**What the fourth consecutive day without movement costs us, plainly.** The 0804-B7 identification residual asks whether the UNCTAD treaty page's head is keyed to the numeric id or to the URL slug. The answer bears on exactly one thing of research value: whether `state/research_log.json`'s "identification discharged to database-record level 2026-08-06" for the China–Switzerland BIT (2009) forum-relationship mapping rests on a record identity or on a string in a URL. That discharge is currently load-bearing for the only closed cross-BIT mapping the project holds. Four days of a stably-erroring negative cell means the instrument cannot answer it: the false-id half of the factorial is not returning a readable outcome and there is no reason in the record to expect the fifth attempt to differ. **The cost is that a closed thread is closed on a discharge whose identification basis remains unverified, and the relay has now spent four batches failing to verify it.** My proposal below stops spending rows on it.

---

### §3 — Group B: the chairman's correction verified, and it is stronger than he put it

I checked his retraction rather than accepting it, by enumerating every record in every committed result blob whose `url` is `https://uncitral.un.org/en/commission`. Twenty-one such records exist; all returned `ok`/200. By `find`:

| `find` | rows | `find_matched` |
|---|---|---|
| `A/81/17` | 10 | false, every one |
| `A/80/17` | 7 | **true**, every one |
| `fifty-ninth` | 3 | **true**, every one |
| (none) | 1 | key absent |

**His correction is right, and understates the position in two ways.** First, the A/80/17 control has passed on 2026-08-04, 08-05 (twice, two runs), 08-06, 08-07 (twice, two runs) and today — **seven passes, not five days**. Second, and this is the part his grep of the request files could not show: the three A/81/17 nulls of 2026-08-03, which predate the A/80/17 control, were each paired **in the same batch** with a `fifty-ninth` row returning `find_matched: true`. **There is therefore no uncontrolled A/81/17 null anywhere in the archive.** All ten are contemporaneously controlled. The single uncontrolled row, `2026-08-03-queued.json` record 2, carried no `find` at all and so made no A/81/17 observation either.

**Consequences, ruled.**

1. **Pre-registered branch B0 was unreachable.** Its trigger was row 3 returning `find_matched: false`, and its consequence was that *"EVERY prior A/81/17 null on this page is retroactively UNINFORMATIVE ABOUT PUBLICATION."* That consequence was already excluded on 2026-08-04 and re-excluded five times since. A branch whose antecedent contradicts six standing observations in the same archive is not a decision rule; it is a rediscovery. This is the pre-registration defect of the day and it is worth more than the embarrassment: **a decision rule must be screened against the archive it purports to update, exactly as an assertion must be.**

2. **The backward-propagation question is moot in the direction it was asked, and I rule against the reasoning that would have been needed.** Digest identity across the series does license the inference — `bytes` and `sha256` are computed over `resp.content` (`fetch_relay.py:178-179`), while `excerpt` and `find_matched` are computed over `resp.text` (`:180-182`), and given identical content bytes and the same encoding determination, `resp.text` and therefore `find_matched` are a pure function of what was retrieved. But nothing needs the inference, because every null carries its own contemporaneous control. The one residual gap in the inference is worth naming for the day it *is* needed: the reduction records `content_type` with parameters stripped (`:172`, `.split(";")[0]`), so a changed charset parameter — which would change `resp.text` without changing `resp.content` — is unrecorded and unexcluded.

3. **What that determinism does buy is a ruling against the daily pair itself.** While the digest is unchanged, *both* Group B rows are logically entailed: A/80/17 must match and A/81/17 must not, because the same bytes cannot yield a different answer to a literal `find`. Firing them is not an observation, it is a recomputation. The record already holds that day-to-day nulls on a byte-identical page are not additive (2026-08-08 Part I; Thread A1). **I extend it: the only informative datum this URL can produce is a change in `sha256`.** The find rows should be read only on the day the digest moves, and until then Group B enters no branch and consumes no council attention.

4. **The series, derived from git rather than adjusted, at local-ref scope.** 21 `ok` fetches of that URL, across **11 distinct batch instants and 11 distinct runner sessions**, at one byte length (832338) and one digest (`ca87a113c024605c…`), extreme span 2026-08-03T18:25:23.447878Z → 2026-08-09T11:16:36.506358Z = 492673.058480 s = **136 h 51 m 13 s** truncated, over 23 distinct committed result blobs. That is yesterday's ruled 19 / 10 / 10 plus today's two rows and nothing else — the 08-08 re-fire added no row on this URL, because that batch carried none. **Scope caveat, stated because yesterday's was:** this repository holds four refs today, not the seventy the chairman enumerated over. Yesterday's ruling established that fetching every origin branch did not move the figure; my 21 is therefore a lower bound only as to anything committed to a non-canonical branch since 2026-08-08.

5. **Nothing new about the underlying page, and I checked before saying anything about it.** The 400-character A/80/17 excerpt returned today is **character-identical to the excerpt returned on 08-04, 08-05 (twice), 08-06 and 08-07 (twice)** — one distinct excerpt across seven rows. Its `(submitted on 10 July 2026)` datum, and the inference that the page's rendered content was authored on or after the closing day of the 59th session, are already in the record at `analytics/daily-research/2026-08-06.md:246` with a full carrying-span entry at `:398-400`. I re-derived it, found it already held, and am not re-announcing it.

**A/81/17 remains not found in accessible sources**, phrased per the cross-BIT consistency rule and never as "does not exist." Today's rows add no evidence for or against publication.

---

### §4 — Group C: the pre-flight worked, and it found a compliance defect

Row 5 returned `ok`/200, `content_type: "text/plain"`, `bytes: 150`, `find_matched: true`, and this excerpt in full:

> `User-agent: * Crawl-Delay: 5 Disallow: /rss Disallow: /search Disallow /record/*/export/*$ Sitemap: https://digitallibrary.un.org/sitemap_index.xml.gz`

**(a) Yesterday's caveat is discharged empirically for this file, and only for this file.** The caveat was that the 400-character excerpt *"establishes REACHABILITY and the HEAD OF THE FILE, never the full policy."* Here `len(excerpt) == 150 == bytes`, and 150 is well under `EXCERPT_CHARS = 400`, so no truncation occurred and the whitespace-normalised excerpt exhausts the file's textual content. I reconstructed the six-line file from the normalised excerpt and it is exactly 150 characters, which corroborates both the line structure and the absence of a trailing newline. **This is C1 as pre-registered, with the bound removed on the evidence rather than in general:** a longer robots.txt would still truncate, and the caveat stands for the next host.

**(b) The origin's own policy corroborates 0808-C4, at one day's remove.** I parsed the reconstructed file with `urllib.robotparser` — the same parser `src/sources/base.py:159-160` uses — under the project's own User-Agent. `can_fetch` returns **False** for `/search` and `/rss`, **True** for `/record/<id>`, `/record/<id>/export/…`, `/robots.txt` and `/`. The 0808-C4 inference was drawn from source-at-HEAD plus fail-open, with the reduction recording nothing whatever about robots.txt; it is now corroborated by the origin's own text. The bound: this is the file served at 2026-08-09T11:16:36Z, and the 08-08 outcome was produced by whatever file was served at 11:17:09Z on 08-08, which we never retrieved. Corroboration is contingent on stability across one day, and I state it that way rather than as identity.

**(c) The colon-less line, which is the reason the full file mattered.** `Disallow /record/*/export/*$` has no colon where the two lines above it do. Python's `RobotFileParser.parse` splits each line on `:` and processes only lines that split into two parts, so a colon-less line is **skipped entirely**; my parse confirms it, returning `True` for the export path the line was evidently meant to forbid. RFC 9309's grammar likewise requires the colon and directs conformant parsers to ignore lines that do not match the grammar. **Our parser and a standards-conformant one read that line the same way: as absent.** So the admissibility map is: on `digitallibrary.un.org`, the only paths closed to this client are `/rss` and `/search`. **The record route is admissible to our own client** — which converts the blocker on that route from "unknown admissibility" to "we hold no record id", and an id cannot come from the relay, because obtaining one is discovery. The route is still **untested and not recorded as tried**; what changed is that the reason it is untested is now a single, nameable, operator-shaped gap.

**(d) `Crawl-Delay: 5` is silently ignored, and this is a live defect.** `src/sources/base.py:28` sets `MIN_INTERVAL_SECONDS = 3.0`, and `_respect_rate_limit` (`:186-200`) is the only interval enforcement in the client. `grep -rn "crawl_delay\|Crawl-Delay\|crawl-delay" --include=*.py .` returns **nothing**: the parser exposes the value — my parse returns `crawl_delay(UA) == 5` from this very file — and no code path ever reads it. The host asks for five seconds; the client would give three. Two honest bounds: `Crawl-Delay` is a non-standard extension absent from RFC 9309, so this is a gap against the host's expressed preference rather than against the exclusion standard; and it has not yet bitten, because every request this project has ever made to that host was either refused by our own robots layer or was a single row.

**(e) The defect I did not expect to find, and it is the sharper one.** `robots_allowed` fetches robots.txt with `requests.get` **directly** (`base.py:157`), outside `_respect_rate_limit`, and `polite_get` calls it *before* `_respect_rate_limit(domain)` (`:249` then `:255`). On the first fetch to any domain in a process, `_last_request_at` has no entry, so `wait` is 0 and the page request goes out immediately after the robots request. **Two requests to the same host, back to back, with no enforced interval — on every domain, every run.** Today's batch did this three times. METHODOLOGY Part III publishes: *"I do not send automated queries to any single domain at intervals shorter than three seconds."* As written, that sentence is false, and it is false in the published methodology rather than in a working note. My greps of `analytics/`, `state/` and `STATE_OF_THE_ANSWER.md` for the interval language surface only the 2026-08-01 discussion of a *proposed* tightening for GDELT, which was never adopted (`grep -c "GDELT" METHODOLOGY.md` returns 0); the robots-fetch bypass itself is not in the record's accessible text. **This is today's one new insight**, and it is instrument, not research. The cheap fix and the harder question — whether to honour a published `Crawl-Delay` at all — are in the proposals.

---

### §5 — Rows 1 and 6: what the bracket bought, checked against the baseline first

Rows 1 and 6 are the same URL, equal `bytes` (207411), character-identical 400-character excerpts, identical `final_url`, and different `sha256` (`6e9916088b51c169…` / `c1ca02d95a0388d2…`). I verified the excerpt and `final_url` equality programmatically, not by eye.

**This is baseline and nothing more.** `state/research_log.json` open thread 11 records the pattern as ESTABLISHED; `analytics/daily-research/2026-08-06.md:236` fixes the constant width. I also checked the sharper thing the pair might have been — a first same-URL-within-one-batch instance, which would have excluded any URL-dependent explanation. It is not first: same-URL-twice-in-one-batch pairs on this host already exist on 2026-08-03 (three batches), 2026-08-04 and 2026-08-05 (three batches). Today is instance N of a pattern the record already ruled *"most consistent with"* a constant-width per-response varying field and *"not diagnosed."* I extend nothing.

**What the bracket did buy** is precisely what my proposal 1 asked of it and no more: it closes the residual weakness I flagged in 0808-C3, that the positive control *preceded* the negative rows rather than surrounding them, so a late host-wide transient was unexcluded. Today the 500 sits between two 200s from the same host in the same sequential loop. `investmentpolicy.unctad.org` was answering before row 4 and after it; **id 999999's error is not a window in the host's availability.** A single leading control could not have said that. That is a small, real gain, and it is the only thing the bracket bought — the second digest was free and told us nothing.

---

### §6 — Carried forward, as instructed, into this memo and not retrospectively

The 0808-B1 erratum stands: the commit-pin rule **held where a pin existed**, not "the countermeasure worked"; the three unpinned lines named in yesterday's ruling carry the stale figure and are not rewritten. The corrected series is **19 / 10 / 10, 89 h 36 m 02 s, at origin scope**, and this memo advances it to 21 / 11 / 11, 136 h 51 m 13 s, at local-ref scope, derived rather than adjusted, with the scope difference stated at §3(4). The unit is **batch completion instants, not sample points**, because `fetched_at` is computed after the fetch loop (`fetch_relay.py:215`), and I have used that term throughout. The 0808-B2 window (`133c152 → 0118af7`) is not merely carried but now explained: §1 shows the window's endpoints are exactly what excluded the merge commit that was the trigger. The numeral struck under 0808-B3 stays struck — the asymmetry argument needs no count and I have given it none. The two 0808-C4 hedges are carried and one is now partly superseded: hedge (b), that the reduction records *nothing* about robots.txt, no longer holds for `digitallibrary.un.org`, because today we retrieved the file itself; hedge (a) — that three `robots_disallowed` rows were **one** robots.txt observation reused twice, because `_robots_cache` is keyed by domain — is unaffected and remains binding on how this council counts. C6 and C7 remain **provenance-only**; §1 changes what caused the overwrite, not what it changed, and the two 08-07 versions still differ in `fetched_at` and `environment` alone.

---

### §7 — The one search: the monitored-author check, which yesterday's session did not run

Yesterday's single query went to A/81/17 and the standing monitored-author check was not performed. The protocol places that check *before* topical searches, so today's one query went to it: `Anthea Roberts EJIL:Talk! August 2026 UNCITRAL ISDS reform`, domain-scoped to `ejiltalk.org`.

**No post datable to August 2026 was returned.** Nine result titles came back, all from the author's standing UNCITRAL/ISDS-reform series. Three are already named in the record (`STATE_OF_THE_ANSWER.md`, `2026-06-29.md`, `2026-07-01.md`, `2026-07-28.md`); five are not — "Crossing the Chasm", "What are States' Concerns?", "Can You Hear Me Now?", "Plausible Folk Theories", "Moving to Reform Options … the Politics". **They are index titles, unretrieved — leads, not sources** (0807-B6), and the fetch layer is dead, so I read no body and assert nothing about any of them, including their dates. The record's own precedent counsels caution here rather than excitement: `analytics/council-log.md:105` records that "Visualising a Flexible Framework" was cleared as an **October 2019** article and not post-session content. The five unnamed titles are almost certainly the same species and are logged as leads for a fetch-capable session, not as findings.

The search layer again returned a synthesised paragraph, this time asserting a delivery date for the first reform products. **Excluded under the standing no-synthesis rule**, and recorded as having appeared. Unlike yesterday's, this one carried a date figure, so the pre-extension rule would have caught it — which is a small piece of evidence that the extension was worth making.

No GAP-UNRESOLVED slug is emitted this session. The China–France Protocol exhaustion gap is escalated and carries zero search budget by standing rule; the Protocol-text residual and the China–Switzerland provision text were not attempted, and *not attempted* is not *failed*, so emitting either would inflate a counter that code, not judgement, is supposed to drive.

---

### The day, honestly

**A null on the research question, and the third in a row.** Nothing today touched intellectual property asserted as a covered investment, nothing touched a regulatory or judicial measure as the challenged conduct, and nothing touched a live jurisdictional or admissibility doctrine. Group A entered its no-branch outcome; Group B was entailed before it fired; Group C was pure instrument; the one search returned a null on the monitored author and five undated leads. **This is instrument and method work and I will not dress it as research.**

What the day bought: the re-fire mechanism is established from the repository at five instances of six and the record's standing non-assertion is discharged; the overwrite count is eleven, not two; the pre-registered branch B0 is shown to have been unreachable against the project's own archive; and the robots pre-flight, on its first outing, returned a complete policy file, an admissibility map, and a compliance defect in our own client that no amount of reading the pre-flight's rationale would have found. **One new insight is logged** (§4(e), `T-instrument-access-method`): the robots.txt fetch bypasses the per-domain interval, which makes a sentence in the published METHODOLOGY false as written. Everything else in this memo is either baseline the record already holds, or an adjudication.

**Method note (self-training).** Yesterday's lesson repeated and sharpened. The highest-yield instrument today was again the repository's own history, but the productive *form* was specific: **enumerate the whole archive into a table before ruling on a single row.** Every substantive correction in this memo — the seven A/80/17 passes, the three `fifty-ninth` controls, the seven 500s at five instants, the eleven overwrites, the five-of-six merge correspondence — came from one loop over every committed result blob, not from reading today's file harder. The general rule I am adopting for myself: when a decision rule refers to a series, materialise the series first; when it refers to "the first" or "never", the screen is an enumeration, not a grep.

**Sources (retrieved).** `analytics/fetch-results/2026-08-09-daily.json@50b0c20` and `analytics/fetch-requests/2026-08-09-daily.json@248ebf7`; through the former, the complete text of `https://digitallibrary.un.org/robots.txt` as served at 2026-08-09T11:16:36Z (150 bytes, untruncated); the 23 committed result blobs reachable from local refs, cited by path and commit above; `scripts/fetch_relay.py`, `src/sources/base.py`, `.github/workflows/fetch-relay.yml`, `METHODOLOGY.md`, `state/research_log.json`, `analytics/daily-research/2026-08-05.md`, `2026-08-06.md`, `2026-08-08.md`, `analytics/council-log.md` at HEAD `50b0c20`. **Leads, not sources:** the nine `ejiltalk.org` index titles returned by today's single query, none retrieved.

```json
{
  "candidate_claims": [
    {
      "claim_id": "0809-C1",
      "claim_text": "The fetch-relay re-fire mechanism is established from the repository: merging a council PR pushes the request commit onto main for the first time, the push matches the workflow's paths filter on analytics/fetch-requests/**, and the relay re-runs on main and overwrites the result file in place. Five of six such merges are followed by a relay result commit on main within 21-90 seconds; the sixth (merge #57, 2026-08-06T12:26:47Z) produced none and the reason is not in the repository.",
      "source_locator": ".github/workflows/fetch-relay.yml on.push.paths; git cat-file -p e2984dc (two parents, 2026-08-08T11:43:50Z); first-parent ancestry test of origin/main for 81bf36b, 133c152, cc123fb, 9f6d883, b1fdfe7, 1334109",
      "supporting_quote": "on:\n  push:\n    paths:\n      - 'analytics/fetch-requests/**'",
      "confidence": "HIGH",
      "note": "Discharges the standing sentence at analytics/daily-research/2026-08-08.md:120 ('the mechanism of the re-fire is not established from the repository'). Merge timestamps confirmed: #65 2026-08-07T12:01:05Z (run 31176433038 created 12:01:07Z per the chairman's Actions query); #70 2026-08-08T11:43:50Z (run 31255633941 created 11:43:52Z). The two-second correspondence is the chairman's screened datum, not mine; the merge timestamps and ancestry are mine. GitHub's evaluation of a paths filter over a merge push is documented platform behaviour and is NOT a repository fact."
    },
    {
      "claim_id": "0809-C2",
      "claim_text": "The relay's mutable-reduction defect has fired eleven times, not twice: across all commits reachable from local refs there are 23 distinct committed blob versions of 12 request-named files under analytics/fetch-results/. analytics/fetch-results/2026-08-08-daily.json has exactly two committed versions, b2989bd and 0612d81, not three.",
      "source_locator": "git ls-tree over every commit in git rev-list --all, restricted to analytics/fetch-results/",
      "supporting_quote": "distinct result blobs: 23",
      "confidence": "HIGH",
      "note": "Corrects the chairman's 'third version' and the record's 'fired a second time' framing. 2026-08-04-undocs-identify.json alone has five versions; 2026-08-03-targeted.json three."
    },
    {
      "claim_id": "0809-C3",
      "claim_text": "Group A entered pre-registered branch A3: both bracket rows (1 and 6, id 978 true slug) returned ok/200/find_matched true, and row 4 (id 999999, true slug) returned outcome 'refused', status '500', with the find_matched key absent. No branch is entered on row 4 and the 0804-B7 identification residual is untouched for a fourth consecutive day.",
      "source_locator": "analytics/fetch-results/2026-08-09-daily.json@50b0c20 records[1],[6],[4]; analytics/fetch-requests/2026-08-09-daily.json@248ebf7 rules A1-A4",
      "supporting_quote": "(A3) If row 4 returns a 5xx, it enters NO BRANCH.",
      "confidence": "HIGH",
      "note": "The absent find_matched key is correct encoding, not a defect: the key is written only inside the record.update that runs on success (fetch_relay.py:173-183). Group A was a half-factorial by construction and the pre-registration says so; branch 2d of 2026-08-08 continues to govern."
    },
    {
      "claim_id": "0809-C4",
      "claim_text": "The 999999 UNCTAD URL has returned outcome 'refused' with status '500' seven times, at five distinct batch instants across five distinct runner sessions (2026-08-07T11:23:03Z, 2026-08-07T12:01:26Z, 2026-08-08T11:17:09Z, 2026-08-08T11:44:21Z, 2026-08-09T11:16:36Z), superseding the pre-registration's 'fourth separated timestamp'.",
      "source_locator": "enumeration over all committed blobs of analytics/fetch-results/ for URLs containing /999999/",
      "supporting_quote": "999999: responses 7 | distinct batch instants 5 | distinct runs 5",
      "confidence": "HIGH",
      "note": "Derived from git, not adjusted from a prior count. The pre-registration's numeral was written before the 2026-08-08 merge re-fire existed to be counted; per the standing ruling the pre-registration is not amended."
    },
    {
      "claim_id": "0809-C5",
      "claim_text": "Every A/81/17 null on uncitral.un.org/en/commission in the project's archive is contemporaneously controlled. Of 21 committed ok fetches of that URL, ten carried find A/81/17 and returned find_matched false, seven carried find A/80/17 and returned true, three carried find 'fifty-ninth' and returned true, and one carried no find. The 2026-08-03 nulls, which predate the A/80/17 control, are each paired in the same batch with a passing 'fifty-ninth' control. Pre-registered branch B0 was therefore unreachable.",
      "source_locator": "enumeration over all committed blobs of analytics/fetch-results/ for url == https://uncitral.un.org/en/commission; analytics/fetch-requests/2026-08-09-daily.json@248ebf7 branch B0",
      "supporting_quote": "finds: Counter({('A/81/17', False): 10, ('A/80/17', True): 7, ('fifty-ninth', True): 3, (None, '<ABSENT>'): 1})",
      "confidence": "HIGH",
      "note": "Verifies and extends the chairman's mid-session retraction, which reached five days and the A/80/17 control only. B0's consequence — retroactive uninformativeness of every prior null — was already excluded on 2026-08-04 and five times since."
    },
    {
      "claim_id": "0809-C6",
      "claim_text": "While the digest of uncitral.un.org/en/commission is unchanged, both Group B rows are logically entailed rather than observed: bytes and sha256 are computed over resp.content and find_matched over resp.text, so identical content bytes determine the find result. The only informative datum this URL can produce is a change in sha256.",
      "source_locator": "scripts/fetch_relay.py:171-183; analytics/fetch-results/2026-08-09-daily.json@50b0c20 records[2],[3]",
      "supporting_quote": "\"bytes\": len(content),\n        \"sha256\": hashlib.sha256(content).hexdigest(),\n        \"excerpt\": excerpt_of(getattr(resp, \"text\", \"\") or \"\", content_type, find),",
      "confidence": "MEDIUM-HIGH",
      "note": "Residual gap, stated rather than assumed away: content_type is recorded with parameters stripped (fetch_relay.py:172), so a changed charset parameter would change resp.text without changing resp.content and is unrecorded. Extends, and does not restate, the standing ruling that day-to-day nulls on a byte-identical page are not additive."
    },
    {
      "claim_id": "0809-C7",
      "claim_text": "The uncitral.un.org/en/commission series now stands at 21 ok fetches across 11 distinct batch completion instants and 11 distinct runner sessions, at one byte length (832338) and one digest (ca87a113c024605c...), extreme span 2026-08-03T18:25:23.447878Z to 2026-08-09T11:16:36.506358Z = 492673.058480 s = 136 h 51 m 13 s truncated, over 23 distinct committed result blobs.",
      "source_locator": "enumeration over all committed blobs of analytics/fetch-results/ reachable from local refs",
      "supporting_quote": "TOTAL uncitral rows: 21  ok: 21 ... distinct fetched_at among ok: 11",
      "confidence": "HIGH",
      "note": "Derived from git, not adjusted. This is yesterday's ruled 19/10/10 at origin scope plus today's two rows; the 2026-08-08 re-fire added none, its batch carrying no row on this URL. Scope: four local refs today against the seventy the chairman enumerated over on 2026-08-08, so a lower bound as to anything committed to a non-canonical branch since."
    },
    {
      "claim_id": "0809-C8",
      "claim_text": "The complete robots.txt served by digitallibrary.un.org at 2026-08-09T11:16:36Z is 150 bytes and was returned untruncated in the 400-character excerpt. Parsed with urllib.robotparser under this project's User-Agent it forbids /search and /rss and permits /record/<id> and /record/<id>/export/*, because the line 'Disallow /record/*/export/*$' lacks the colon that both robotparser and RFC 9309's grammar require and is therefore ignored by each. This corroborates the 0808-C4 inference from the origin's own policy, at one day's remove.",
      "source_locator": "analytics/fetch-results/2026-08-09-daily.json@50b0c20 records[5]; src/sources/base.py:155-177",
      "supporting_quote": "User-agent: * Crawl-Delay: 5 Disallow: /rss Disallow: /search Disallow /record/*/export/*$ Sitemap: https://digitallibrary.un.org/sitemap_index.xml.gz",
      "confidence": "HIGH",
      "note": "len(excerpt) == bytes == 150 < EXCERPT_CHARS 400, so no truncation; the parse was run on a six-line reconstruction from the whitespace-normalised excerpt which is itself exactly 150 characters. Bound: this is the file served today, not the file served on 2026-08-08, which was never retrieved. Nothing here is a claim about the UN Digital Library's holdings; the record route remains untested and is not recorded as tried."
    },
    {
      "claim_id": "0809-C9",
      "claim_text": "This project's client sends two requests to a host back to back, with no enforced interval, on the first fetch to any domain in a process: robots_allowed fetches robots.txt with requests.get directly, outside _respect_rate_limit, and polite_get calls it before _respect_rate_limit, whose wait is zero on a domain with no prior entry. METHODOLOGY Part III's published sentence 'I do not send automated queries to any single domain at intervals shorter than three seconds' is therefore false as written. Separately, the parser exposes crawl_delay = 5 for digitallibrary.un.org and no code path reads it; MIN_INTERVAL_SECONDS is 3.0.",
      "source_locator": "src/sources/base.py:157, :186-200, :249, :255, :28; METHODOLOGY.md Part III",
      "supporting_quote": "resp = requests.get(robots_url, headers={\"User-Agent\": USER_AGENT}, timeout=20)",
      "confidence": "HIGH",
      "note": "grep -rn for crawl_delay/Crawl-Delay/crawl-delay across *.py returns nothing. Crawl-Delay is a non-standard extension absent from RFC 9309, so ignoring it is a gap against the host's expressed preference rather than against the exclusion standard; the interval bypass is a different matter, because the project publishes the three-second claim. Not found in the record's accessible text by greps of analytics/, state/ and STATE_OF_THE_ANSWER.md."
    },
    {
      "claim_id": "0809-C10",
      "claim_text": "The standing monitored-author check on Anthea Roberts (ejiltalk.org/author/aroberts/) returned no post datable to August 2026. Nine index titles from her UNCITRAL/ISDS-reform series were returned; five are not named anywhere in the project record, all undated in the index and unretrieved.",
      "source_locator": "this memo section 7; one domain-scoped web query, 2026-08-09",
      "supporting_quote": "N/A - unretrieved index result titles only; no body read and none quoted",
      "confidence": "MEDIUM",
      "note": "Leads, not sources (0807-B6); the fetch layer is dead and nothing was retrieved. The record's own precedent counsels caution: analytics/council-log.md:105 records 'Visualising a Flexible Framework' cleared as an October 2019 article and not post-session content. The search layer's synthesised paragraph was excluded under the standing no-synthesis rule and is recorded as having appeared; it carried a date figure, which the extended rule would have caught."
    }
  ]
}
```

### PROPOSALS TO THE CHAIRMAN

1. **Retire the daily Group B pair as an observation and replace it with a digest sentinel.** Per C6, while `sha256` is `ca87a113c024605c…` both rows are entailed and neither is an observation. Keep the two rows if you like — rows are free — but pre-register a one-line reading rule: *if the digest is unchanged, Group B enters no branch, produces no memo section, and consumes no council attention; only a digest change opens analysis, and on that day the A/80/17 control is read as contemporaneous.* The scarce resource being saved is pre-registration and council attention, not rows.

2. **Stop firing the 999999 cell.** Seven `refused`/`500` responses at five instants across three days, entering no branch each time by the pre-registration's own terms. An eighth changes nothing. The 0804-B7 residual is not answerable by this instrument on this host, and the honest disposition is to record it as **instrument-limited, not open**, and to route the underlying question — whether the treaty-978 page's identity is keyed to the id or the slug — to a fetch-capable session or to the operator.

3. **Escalate C9 to Emory as a dated compliance item, with the fix.** The robots-fetch bypass is roughly two lines: register the robots fetch with `_respect_rate_limit(domain)` before issuing it, or route it through a rate-limited helper. Whether to honour a published `Crawl-Delay` is a policy question for the operator, not a defect fix, and I put it to him in the terms the 2026-08-01 record already framed for GDELT: *"or shorter than a longer minimum where the source publishes one."* That sentence was drafted, never adopted because GDELT was not adopted, and a host we actually use now publishes one. **Do not patch `src/` without sign-off.**

4. **Escalate C2's corrected count.** The escalation to Emory currently reads that the overwrite defect "has now fired a second time." It has fired eleven times across twelve request files. The pin convention has held wherever a pin existed, which is the honest form of the good news, and the count is the honest form of the bad.

5. **Adopt a screening rule for decision rules, out of B0.** A pre-registered branch whose antecedent asserts "never", "first", or "every prior" must carry the enumeration that establishes it, in the pre-registration, exactly as a memo assertion must. B0 was written against a premise contradicted six times in our own archive, and the cost was a wasted branch and a chairman's retraction mid-session. This is the delegation-brief rule of 2026-08-08 extended to the artefact the brief produces.

6. **Proposed batch for 2026-08-10 — one group, four rows.** (i) `investmentpolicy.unctad.org/.../978/china---switzerland-bit-2009-`, find `China - Switzerland BIT`, as opening bracket; (ii) `uncitral.un.org/en/commission`, find `A/81/17`, as the digest sentinel; (iii) `digitallibrary.un.org/robots.txt`, find `Disallow:`, as a **stability check on C8's one-day bound**; (iv) the same UNCTAD URL as (i), fired last, as closing bracket. No 999999 row (proposal 2), no `/search` row (deterministic non-observation), no record-route row (we hold no id and obtaining one is discovery), no sitemap, no PDF row — a `find` on a non-text content type is guaranteed false by the content-type gate, as established at `analytics/daily-research/2026-08-05.md` §3.3, and must never be fired.

   **Decision rule, fixed in advance.** *(P0)* Rows (i) and (iv) must both return `ok`/200/`find_matched: true`; if either fails, the batch is uninformative for `investmentpolicy.unctad.org` and no row on that host is read. *(D1)* If row (ii) returns `ok`/200 with `sha256` = `ca87a113c024605c…`, the result is **entailed**: it enters no branch, is recorded in one line as "digest unchanged, day N", and A/81/17 remains not found in accessible sources. *(D2)* If row (ii) returns `ok`/200 with **any other** `sha256` or byte length, that is the first change ever observed in this representation: it enters no branch either, but it is **escalated the same day**, and the following batch fires the A/80/17 control alongside it before any reading of `find_matched` is banked. *(D3)* Any non-`ok` outcome on row (ii) is uninformative and is not read as a null. *(R1)* If row (iii) returns `ok`/200 with an excerpt byte-length equal to its `bytes` and identical to today's, C8's one-day bound is discharged to two days and the admissibility map is stable. *(R2)* If it returns `ok`/200 with a **different** file, the admissibility map is re-derived from the new text and C8 is superseded, not amended. *(R3)* If it returns `robots_disallowed`, this client forbids itself the policy file and the row is an instrument fact only. *(R4)* Any other outcome is uninformative.

7. **One thing to hand the operator rather than the relay.** The UN Digital Library record route is now known to be *admissible* to our own client (C8) and blocked only by the absence of a record id. That is a single, well-formed manual query — a record id for the UNCITRAL Commission session report series on `digitallibrary.un.org` — and it is the cheapest live path to the A/81/17 question that the project has. It should go on Emory's list as a research item, not stay on ours as an instrument gap.

---

## Part III — The digest of record

`Digest 2026-08-03: 13 candidates evaluated, 1 item surfaced (0 matches, 1 watch-list lead).`

Copied from `digests/2026-08-03_ISDS-Thematic-Watch/meta.json` — `screened: 13`, `matches: 0`,
`watch_list_leads: 1` — not restated from memory, per Rule 1. This is the seventh consecutive day
the digest of record has been unchanged, and it is the last: today is Sunday, and the weekly
collection run fires tomorrow, Monday 2026-08-10 at 13:00 UTC (`.github/workflows/weekly.yml`,
`cron: "0 13 * * 1"`). Nothing in today's work touches the digest and there is no analytics
finding to report against it.

---

## Part V — The council discussion

Per Rule 0 the integrity officer is not a separately convened seat today and files no standalone
vetting note; its challenge is heard here, in its own words, and the challenged party answers on
the record. Both members' turns are **verbatim**. Mine are mine. Ten binding objections were
filed: eight against the analyst, two against me.

**Chairman:** The unfavourable item first, as always. Group A was the one piece of today's batch
designed to move research rather than method, and it moved nothing. Take the adjudication, and do
not spare the pre-registration — I wrote it.

**Researcher:** *(from the memo at Part II, §2)* "**A3 is entered as written: no branch, no
conclusion.** A1 required 200 with `find_matched` true; A2 required 404, 410, or a differing
`final_url`. Neither is satisfied. … **The cost is that a closed thread is closed on a discharge
whose identification basis remains unverified, and the relay has now spent four batches failing to
verify it.**"

**Chairman:** That is the right entry and it is the right way to describe what it costs. I note
what else it settles, because it is the one thing the bracket was for: the 500 sits between two
200s from the same host in the same sequential loop, so id 999999's error is not a window in the
host's availability. That closes the residual weakness in yesterday's 0808-C3 and it is the whole
of what a day's design bought. Now the challenge.

---

**Integrity check — 0809-B1 (binding). The denominator is wrong. It is seven of eight, not five of six, and the two he missed are both positives sitting in his own table.**
*Asserted:* six merges pushed a request commit onto `main` for the first time; five were followed by a relay result commit; "the honest score is five of six." *Checked:* I re-ran the first-parent ancestry test independently over **all fifteen** commits that have ever touched `analytics/fetch-requests/`, not the six he selected. Eight distinct merges qualify, not six. He omits two, both on 2026-08-03: merge **#40 `d67f2d1`** (18:24:20Z) → **`842e222`** (18:24:52Z, **32 s**), overwriting `2026-08-03-control.json`; merge **#41 `161846d`** (18:30:41Z) → **`eb1fac5`** (18:31:21Z, **40 s**), overwriting `2026-08-03-targeted.json`. `git log -1 --format=%P` confirms `842e222`'s sole parent is `d67f2d1` and `eb1fac5`'s sole parent is `161846d`; both are result commits sitting directly on main's first-parent line above the merge that introduced the request change. *What is wrong:* this is not a stray omission. The **same 23-blob enumeration that yields C2's eleven overwrites contains both instances** — he built the two findings from one loop and did not cross them. The memo's own adopted rule of the day — *"when it refers to 'the first' or 'never', the screen is an enumeration, not a grep"* — governs a denominator as much as a numerator. This is silent class truncation (taxonomy 14) in the one section that rests on completeness. *In his favour, and I state it because it is true:* the two omitted instances are **positive**, the 21–90 s range is unchanged by them, and they are the cleanest demonstrations of his own `newest_request()` explanation. *And a ninth instance the memo's framing cannot reach.* `77087e9` (2026-08-03T22:17:09Z) is a relay result commit on branch `vault/recover-orphaned-work` with **no request commit and no merge to main** before it. The mechanism is therefore **not** "merging a council PR onto main"; it is "any push whose diff range against the ref's prior tip touches `analytics/fetch-requests/**`, on any ref."

**Researcher:** "**CONCEDED.** I re-ran the first-parent ancestry test over **every** commit touching `analytics/fetch-requests/` rather than the six I had selected… **Eight merges introduce a request-file change to `main`; seven are followed by a relay result commit on `main`.** … **Corrected text: seven of eight, not five of six**, and the officer is right that both omissions sat inside my own 23-blob enumeration and I failed to cross my two findings — the same failure I named in my own method note one paragraph earlier, committed while writing it.

The officer's count of the population is off by one: **fourteen** commits touch that path across all refs, not fifteen; the fourteenth, today's `248ebf7`, is not on `main`.

On the ninth instance I go further than the officer asks and in its direction. `77087e9` … is a relay result commit on `vault/recover-orphaned-work`, 27 s after `4a731be`. But **neither `4a731be` nor its parent `8705f7a` touches `analytics/fetch-requests/` in its own diff** … So the ref-general formulation the officer proposes is the better statement of the mechanism and I adopt it … but it is **not verified for this instance either**, because a ref's prior tip is not recoverable from the repository. **Corrected disposition: C1 is scoped to `main` at seven of eight; the mechanism is restated at ref-generality; and `77087e9` is recorded as UNACCOUNTED, not as explained.**"

**Chairman:** Both seats are better than either would have been alone, and the exchange has a
shape I want named. The officer caught a truncated class; the analyst, conceding, corrected the
officer's own population count and then declined the ref-general rescue *as verification* while
adopting it *as formulation*. That last distinction is the valuable one: a better statement of a
mechanism is not evidence for it, and `77087e9` goes into the record as unaccounted. My ruling is
at Part VI.

---

**Integrity check — 0809-B2 (binding). "Established" is not earned in the form the claim asserts it, and the claim text asserts more than its own note concedes.**
*Asserted:* claim_text — "the push **matches the workflow's paths filter** on `analytics/fetch-requests/**`, and the relay re-runs on main," at `confidence: HIGH`. Note — "GitHub's evaluation of a paths filter over a merge push is documented platform behaviour and is **NOT** a repository fact." *Checked:* I read `.github/workflows/fetch-relay.yml` in full. The repository establishes exactly three things: (i) `on.push.paths` … with no branch filter and the commit step pushes to `HEAD:${GITHUB_REF_NAME}`; (ii) the merge/request ancestry above; (iii) merge-commit time → result-commit time, 21–90 s over eight instances. It establishes **nothing** about what GitHub matched. The two-second merge→run-created correspondence — the tightest number in the section and the one that makes the argument feel causal — comes from the chairman's Actions query, which is **not in the repository** and which I cannot re-run today. *What is wrong:* a claim's text may not assert what its own note withdraws. The note is correct; the text is the version that will be quoted.

**Researcher:** "**CONCEDED.** A claim's text may not assert what its own note withdraws; that is the cleanest statement of the defect and I accept it without qualification. **Corrected `claim_text` for 0809-C1:** *'On `main`, seven of the eight merge commits that introduced a change under `analytics/fetch-requests/` are followed within 21–90 seconds by a `chore: fetch-relay results` commit on `main` overwriting a result file in place; the eighth (merge #57, 2026-08-06T12:26:47Z) is not.'* That proposition is establishable from the repository alone and I keep it at HIGH. The paths-filter step moves to the note as a **platform inference**, not a repository fact. The two-second merge→run figures … are marked in the note as **the chairman's screened Actions datum, not re-verified by me**."

**Chairman:** This one is mine as much as his. The two-second figure is the number I handed the
council, it is the number that makes the story feel causal, and it is the one number in the
section that no seat in this session could re-derive. The officer was right to isolate it and the
analyst is right to label it. It stands as my datum, and the claim no longer leans on it.

---

**Integrity check — 0809-B3 (binding).** "Two unrelated commits landed on main at 12:26:57Z and 12:27:21Z" is false as to the first. `d4d40c4` (12:27:21Z) is on main's first-parent line — that one is right. `33ad987` (12:26:57Z) is **not**: it is the second parent of `435de28`, PR #58, which merged at 2026-08-06T23:05:00Z. 12:26:57Z is its *commit* timestamp on a branch, not its arrival on `main`. A commit's author/committer time is not a landing time, and the whole point of the sentence is arrival on `main`.

**Researcher:** "**CONCEDED.** … **Corrected text: one commit landed on `main` in that window, `d4d40c4` at 12:27:21Z.** The push-race candidate is weakened accordingly and I do not restate it as more than a candidate."

---

**Integrity check — 0809-B4 (binding). "There is no uncontrolled A/81/17 null anywhere in the archive" over-claims, because a `fifty-ninth` hit is not a control for a symbol find.**
*Checked:* I re-ran the enumeration over all 23 committed result blobs. The table reproduces exactly — `Counter({('A/81/17', False): 10, ('A/80/17', True): 7, ('fifty-ninth', True): 3, (None, '<ABSENT>'): 1})`, 21 rows, all `ok`, 11 instants, 11 runs. *What is wrong:* the hypothesis B0 was written to test is *"this page's rendered text does not carry any session-report symbol."* `A/80/17` tests that hypothesis; it is a symbol of the same series. `fifty-ninth` does not test it at all — it is ordinary prose, and a page can print session names in words while never printing a document symbol. The three 2026-08-03 nulls are controlled for *page reachability and non-empty rendered text*, and for nothing else. Calling them "controlled" without qualification, in a sentence beginning "There is therefore no…," is the strongest word in the memo resting on the weakest of its three controls. *And the rescue he waived would have worked.* All 21 rows return `bytes: 832338` and `sha256: ca87a113c024605c…`, including the three 08-03 rows. By his own §3(2) determinism, identical content bytes determine the find result — so `A/80/17` **would** have matched on the 08-03 representation. He explicitly set that inference aside and thereby discarded the only argument that reaches the rows he needed it for. I checked the one thing that could have broken the rescue: `find_matched` was introduced by `7fbbabf` at 2026-08-03T18:27:34Z, **40 seconds before** the first of those three rows, and neither `excerpt_of` nor the `find_matched` expression has changed since.

**Researcher:** "**CONCEDED IN PART, and I take the rescue.** The officer is right that a prose hit on `fifty-ninth` does not test B0's hypothesis, which was specifically that the page's rendered text carries no session-report **symbol**. … I take the digest rescue expressly rather than waive it, and I verified the one thing that could break it myself rather than accept the officer's screen: **`scripts/fetch_relay.py` has exactly two commits in its entire history** … **Corrected text for 0809-C5:** *seven of the ten A/81/17 nulls are symbol-controlled in their own batch; the three of 2026-08-03 are reachability-controlled in their own batch and are carried to symbol-control by digest identity with the seven, which inherits the charset residual named in C6.* B0 remains unreachable on either route."

**Chairman:** The officer did something here that a vetting seat is not obliged to do and that I
want on the record: having broken the claim, it then handed back the argument that repairs it, and
verified the one fact that could have defeated the repair. That is verification serving the
research rather than scoring against it. And the analyst did not simply accept the gift — he
re-derived the enabling fact himself and carried the residual forward rather than dropping it. The
corrected C5 is weaker in form and stronger in fact than the original, which is what a vetting
round is for.

---

**Integrity check — 0809-B5 (binding).** "The only informative datum this URL can produce is a change in `sha256`" is contradicted by the memo's own decision rule three pages later. The memo's own proposal 6 writes *(D2)* "any other `sha256` **or byte length**" and *(D3)* "any non-`ok` outcome on row (ii) is uninformative and is not read as a null." A 404, a 500, a `robots_disallowed`, or a `final_url` that stops matching the request are all changes in state that this URL can produce and that are not digest changes. *What is wrong:* an absolutised statement of a correct narrower point (taxonomy 13).

**Researcher:** "**CONCEDED.** The absolutised form is contradicted by my own proposal 6 two pages later, which is the worst place for it to be contradicted from. **Corrected text: 'no `find` row on this URL is informative while the representation is byte-identical; a change in `outcome`, `status`, `final_url`, byte length or digest is.'**"

---

**Integrity check — 0809-B6 (binding). The 150-character reconstruction cannot corroborate the line structure, and the line structure is what the section's headline finding rests on.**
*Checked:* I ran it. The excerpt is 150 characters, ASCII-only, and contains **eleven** spaces. `_WS_RE.sub(" ", body)` maps every whitespace run to exactly one space, so *any* assignment of any five of those eleven spaces to newlines produces a 150-character reconstruction. The length check is incapable of returning a different answer for a different line structure — this is taxonomy 26, tautological instrument check, and it is this seat's own entry. I built three reconstructions, all consistent with the 150-byte length, and parsed each with `urllib.robotparser` under the project UA: **six-line** (his) — `/rss` False, `/search` **False**, `crawl_delay` 5, export path True; **five-line**, folding the colon-less token onto the `/search` line — `/rss` False, `/search` **True**, `crawl_delay` 5; **one-line** — everything True, `crawl_delay` **None**. The second reconstruction inverts the day's admissibility map and would *contradict* 0808-C4 rather than corroborate it. What actually eliminates it is not today's row but yesterday's `robots_disallowed` outcome — so §4(b)'s claim that "the origin's own policy corroborates 0808-C4" is not independent corroboration. The length equality does do real work — it excludes a trailing newline, CRLF endings, trailing spaces and any multi-byte character — and that half of his sentence stands.

**Researcher:** "**CONCEDED, and the demonstration is decisive.** I reproduced it. … **The check cannot discriminate.** What eliminates the five-line reading is yesterday's `robots_disallowed` outcome, not today's row — so §4(b) is **consistency with 0808-C4, not independent corroboration of it**, and the map is what follows *conditioned on* it. **Corrected text:** strike 'the line structure' from the corroboration clause; state that whitespace normalisation destroys line boundaries; C8's parse and the admissibility map are expressly conditional on the six-line reading; and **'the complete text' in Sources (retrieved) becomes 'the complete whitespace-normalised text.'**"

**Chairman:** This is the best objection of the day and I want the reason stated generally,
because it is not about robots.txt. **The officer did not argue that the check was weak; it
constructed the alternatives the check cannot distinguish and parsed each one.** One of those
alternatives inverted the day's conclusion. A test that cannot return a different answer for a
different world is not a test, and this council has now caught one inside a section whose subject
was the reliability of a retrieval. That the reduction's own whitespace normalisation is what
destroyed the discriminating information is the part that generalises: **our instrument's
cleaning step is a place where evidence goes to die, and any claim that depends on structure the
reduction normalises away must be stated as conditional.**

---

**Integrity check — 0809-B7 (binding). The "because it lacks the colon" clause is false for our own parser, and it conceals a larger defect than the one it reports.**
*Checked:* I restored the colon and re-parsed. `RobotFileParser` under the project UA still returns **`can_fetch(/record/12345/export/marcxml) == True`**. The reason is in the standard library: `RuleLine.applies_to` is `return self.path == "*" or filename.startswith(self.path)` — Python's parser implements **no `*` or `$` wildcards at all**. The missing colon is not load-bearing for our client; our client would ignore that rule either way. It is load-bearing only against a wildcard-implementing, RFC 9309-conformant parser — which is the opposite of "both read it the same way." *What is wrong:* the causal attribution is inverted, and the true state of affairs is worse for us: **this project's robots layer silently ignores every `Disallow` rule that uses `*` or `$`**, on every host, which bears directly on METHODOLOGY Part III's "I follow standard robot exclusion protocols." The memo reports a typo in someone else's file and misses a general defect in ours, in the section whose subject is our own compliance.

**Researcher:** "**CONCEDED, and this is the objection that found the real defect.** I restored the colon and re-parsed: `can_fetch(UA, '/record/123/export/marcxml')` is **still True** … I read the source, and Python's parser implements **no `*` or `$` wildcards at all**. The missing colon is not load-bearing for our client. … The larger point is the officer's and I adopt it in full: **this project's robots layer silently ignores every `Disallow` rule using `*` or `$`, on every host, and has always done so.** That is a general compliance defect against METHODOLOGY Part III's *'I follow standard robot exclusion protocols'*, and it belongs beside C9 as its own item, not inside C8. I reported a malformed line in someone else's file and missed a live defect in ours, in the section whose whole subject was our own compliance — which is the same failure shape as B1, twice in one memo."

**Chairman:** Two compliance defects in our own client, found on a day we could not reach the
network — and the second was found by the seat whose job is to doubt the first. Both go to Emory
as items, neither is patched here, and I note the direction of both: **every defect found today
points the same way, at us claiming more compliance in public than our code performs.** That is
the honest summary of Part II §4, and it is more useful than the robots.txt typo that led to it.

---

**Integrity check — 0809-B8 (binding). An absence claim about our own repository, and it is false on the first title in the list.**
*Checked:* `grep -ril "Crossing the Chasm"` returns `analytics/daily-research/2026-07-26.md`. Line 88 reads: *"Roberts' series — 'What to Expect When You're Expecting,' 'In Sickness and In Health,' **'Crossing the Chasm**,' 'Moving to the Delivery Phase,' and now 'Lifelong Learning' — has consistently provided the project's most analytically useful UNCITRAL post-session narratives."* The record already holds a five-title enumeration of this exact series, in the file his screen did not include. *Second defect in the same paragraph:* nine titles, three named, five unnamed — **3 + 5 = 8**. One title is unaccounted for and its status is unstated. *Third:* "not named anywhere in the project record" is project scope asserted on a repository screen.

**Researcher:** "**CONCEDED, and the cause is a method failure worth recording.** … My screen ran on the **URL slug** `crossing-the-chasm`, which appears nowhere in prose. That is the 2026-07-26 lesson — author-name-plus-title beats keyword — inverted and self-inflicted: **an absence screen must run on the human-readable form as well as the machine form, and a title screened only as a slug is not screened.** The count reconciles and I should have said how: the search returned **nine URLs, of which one is the author landing page** … and eight are articles. … withdrawing 'Crossing the Chasm' makes it **four named, four unnamed** … **Corrected scope, per the 2026-08-06 house rule: 'not found in this repository at `50b0c20`', not 'not named anywhere in the project record.'**"

---

**Integrity check — the observations that matter most.** **`0809-O4`:** `find_matched` is not only a function of bytes, encoding and content-type — it is a function of the *code* that computes it, and the relay runs whatever is on the pushed ref. Historically it is safe. Prospectively it is not: **proposal 3 asks to patch `src/sources/base.py`, which sits in the same call path**, and D1 declares tomorrow's row (ii) "entailed" on digest identity alone. **`0809-O6`:** two propositions in §4(c)–(d) rest on RFC 9309's text; `grep -rn "9309"` returns only bibliographic entries — no text. Both are plausible and neither is checkable in-repo on a zero-fetch day, the more so because B7 shows the RFC leg is now doing the real work. **`0809-O9`:** `.github/workflows/weekly.yml` shares `concurrency: group: isds-watcher` with the relay; with `cancel-in-progress: false`, a run queued behind another member of that group can be superseded. I assert nothing; the point is that the memo presents an explanation set of one and calls the rest "not in the repository," when the repository does name another candidate.

**Researcher:** "**On O4 — accepted, and it changes tomorrow's rule.** … **Amendment to proposal 6: D1's entailment holds only while `excerpt_of` and the `find_matched` expression are unchanged, and the batch's decision rule names the commit of `scripts/fetch_relay.py` under which it was written.** A code change and a digest change are both state changes and the rule must treat them alike. **On O6 — accepted, and it is the more important for B7.** … With B7 removing the 'our parser agrees' leg, the RFC leg now carries the whole of the conformant-parser claim, and an unverified leg cannot carry it. **Marked unverified**, and I emit the slug: `GAP-UNRESOLVED: rfc-9309-grammar-unverified` … **On O9 — accepted, with one bound the observation does not carry.** … The bound: `weekly.yml` is scheduled `cron: \"0 13 * * 1\"` — Mondays 13:00 UTC — and 2026-08-06 was a Thursday, so no *scheduled* weekly run is the candidate … **I assert nothing about what did or did not fire on 2026-08-06, and the explanation set is recorded as open.**"

---

**Integrity check — an objection I drafted and killed.** I drafted a binding objection that §5's citation `analytics/daily-research/2026-08-06.md:236` is mis-located — taxonomy 17a, this seat's own entry — because I had printed that line through `cut -c1-300` and the visible text was about "one page state observed nine times," with no mention of 207,411 bytes. I had the objection written before I printed the line whole. **The full line 236 contains the exact words *"twelve distinct digests at a constant 207,411 bytes"*.** The citation is correct, yesterday's seat was right to pass it, and I nearly filed a false binding objection because I read a truncated tool artefact as the artefact. That is entry 7 of my own taxonomy — tool output taken for source state — operating on the seat whose job is to catch it, and on a day when there was no network to blame. The rule I take from it: **never file an objection about a line you have only seen truncated.**

**Chairman:** That is the second consecutive day this seat has filed its own near-miss, and the
second consecutive day the near-miss is the most instructive line in the return. Last time it
nearly filed the strongest argument *for* the stamping convention as an objection *against* it;
this time it nearly filed a false objection off a truncated tool artefact — on a fetch-dead day,
where there was no network to blame and the artefact was our own terminal. **Adopted as a standing
rule: never file an objection, and never assert an absence, about a line you have only seen
truncated.** It sits beside the positive-control rule.

---

**The objections against me, answered.** Two, and both are sustained on my own check.

**Integrity check — 0809-B9 (binding).** "**CORRECTION, entered by the chairman at 11:31Z**…" — `git log` shows `5d53819` (Part I, no correction) at 11:21:26Z, and `3dcd7c3`, which contains the correction paragraph, at **11:22:46Z**. "Before the analyst returned" is right and "before this file reached `main`" is right. The time is not. Taxonomy 17, mis-dated internal-authority citation, inside the paragraph whose entire function is dated accountability — and the date is the part a later seat will cite.

**Chairman:** Sustained on my own check: `3dcd7c3` is stamped 11:22:46Z and `5d53819` at 11:21:26Z
carries no correction paragraph. I wrote 11:31Z from my own sense of the session's clock and never
looked. It is a small error of exactly the species that has cost this council whole days, and it
sat inside my accountability paragraph. Corrected to **11:22:46Z, commit `3dcd7c3`**.

**Integrity check — 0809-B10 (binding).** "My own merge today will fire it a fourth time" is a bare numeral with no locator. The analyst's §1 says **sixth**; my enumeration says **eighth**; Part I says **fourth**. Three seats, three values, none of them locatored. This is 0808-B3 recurring at the same seat one day later — an unsourced count in an agenda paragraph, and the chairman's own ruling yesterday was that such a numeral is struck rather than repaired unless the argument needs it. The argument here does not need it.

**Chairman:** Sustained, and the officer has quoted my own ruling back at me correctly. **The
numeral is STRUCK, not repaired.** The sentence needs no count: merging this PR will re-fire the
relay and overwrite today's reduction in place, which is the entire content of the warning, and
the number of prior instances is a separate question three seats have now given three answers to.

**Integrity check — 0809-O7, on the retraction the chairman asked me to test.** It is complete in substance; four things built on or around it are still standing, and none is what he feared. Nothing in the memo rests on the withdrawn premise — §3 demolishes it independently and goes further. The pre-registration is correctly left unamended. What remains: (i) `0809-B9`; (ii) `0809-B10`; (iii) the correction paragraph says "all eight rows" across 2026-08-04/05/06/07, where the true figure is **twelve** rows — six A/80/17 and six A/81/17, because 08-05 and 08-07 each carry two batch instants. Part II supersedes it but Part I is uncorrected and will be read on its own; (iv) Part I contains a **second** unscreened novelty claim in the same agenda — "for the first time in this project, **brackets** it." Under the standing rule adopted against the chairman yesterday, that needed the screen or the disclaimer, and it carries neither. **I ran the screen and the claim holds:** across all 23 blobs, today's batch is the only one in which the same URL occupies position 0 and the last position with substantive rows between them. So: the rule was broken twice in one agenda, and this time the claim survives verification.

**Chairman:** All four accepted, and (iii) and (iv) are the ones that matter. On (iii): my "all
eight rows" counted four days at two rows each, from the working tree — where each request file
holds one version. Over committed blobs the figure is twelve, because 08-05 and 08-07 each carry
two batch instants, and both the analyst's independent table and the officer's independent
enumeration agree on it. That is the same repo-scope-versus-project-scope trap this council has
now walked into from three different seats in three days, and mine is the version where I screened
the *working tree* and called it the archive.

On (iv) I want no credit for the outcome. **I made two unscreened novelty claims in one agenda.**
The first was false and I caught it myself an hour later; the second happens to be true, and I did
not know that when I wrote it — the officer knew it, because the officer ran the screen I owed. A
claim that survives verification I never performed is not a vindication of my judgement; it is
evidence about how the rule is supposed to work, and it worked because someone else applied it.

---

**Chairman — close-out of the discussion.**
**SOLID:** that both bracket rows held, so id 999999's 500 is not a window in the host's
availability, and the 0808-C3 weakness is closed; that Group A entered A3 and the 0804-B7 residual
is untouched for a fourth day; that on `main`, seven of eight merges introducing a request-file
change are followed within 21–90 s by a relay result commit overwriting a result file in place, and
the eighth is not; that the reduction files have 23 committed versions across 12 files — eleven
in-place overwrites, not two; that the UNCITRAL series is 21 `ok` fetches at 11 batch instants
across 11 runner sessions, one byte length, one digest, spanning 136 h 51 m 13 s at local-ref
scope; that seven of the ten A/81/17 nulls are symbol-controlled in their own batch and the other
three are carried by digest identity; that the robots.txt served to us at 11:16:36Z was 150 bytes
and reached us untruncated in whitespace-normalised form; and — the day's two real findings —
that our client issues two requests to a host back to back with no enforced interval on the first
fetch to any domain, and that our robots layer silently ignores every `Disallow` rule using `*` or
`$`, on every host, always has, and that both make published sentences in METHODOLOGY Part III
false as written.
**OPEN:** the identification residual, for a fourth day, and now on my ruling *instrument-limited*
rather than merely open; whether the UNCTAD page head is keyed to the numeric id; A/81/17's
publication; what fired the relay at `77087e9`, which is UNACCOUNTED and not explained; why merge
#57 produced no result commit, whose explanation set is open and now has two candidates rather
than one; and RFC 9309's actual grammar, unretrieved and marked as a gap.
**MUST NOT BE ASSERTED:** that GitHub's paths filter matched anything — that is platform behaviour
inferred, not a repository fact, and the two-second correspondence is my datum and was re-derived
by nobody today; that the six-line reading of the robots.txt is established — it is conditional on
yesterday's `robots_disallowed` outcome, and a five-line reading consistent with every datum we
hold inverts the map; that today's row independently corroborates 0808-C4; that a `fifty-ninth`
prose hit controls a symbol find; that the 500 tells us anything about head generation or about
UNCTAD's holdings; that anything was learned about A/81/17 today; that any of the five unretrieved
`ejiltalk.org` titles is recent, or is anything but an index title; and nothing whatever from the
search layer's synthesised prose, which appeared again and was excluded again.
**For the principal:** two compliance defects in our own client, both making a published sentence
false, and an overwrite defect that has fired eleven times rather than twice. All three are at
Part VI, and none is patched.
