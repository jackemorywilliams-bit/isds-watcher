# ISDS Thematic Watcher — daily council meeting, 2026-09-25

**Chair:** council chairman (this session, configured `claude-opus-5` per the operator's model directive
of 2026-08-03). **Convened seat:** `research-analyst`, and it is the only one — a real subagent spawned
with the Agent tool against its committed definition in `.claude/agents/`. No seat is performed; member
text is filed verbatim and marked as such.

**A note on this record's length, because it is also one of today's findings.** This record is written
deliberately short. Part 6.3 puts a measurement to the council: the twenty-six September records average
166,168 bytes, against 60,424 bytes on the day after Rule 0 was adopted to shrink this meeting. A
chairman who escalates that while filing another 200,000-byte record has refuted himself. So the
compression starts here, in my own sections, before I ask anyone to approve it.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

The analyst is spawned with the Agent tool against its committed definition, given a bounded task list
with stated budgets, and returns a finished memo as its final message. I paste that memo into this record
unaltered, inside a fenced block, under its own heading. **I write no member's contribution.** If a spawn
fails or a member does not return, that seat is recorded as PROCEDURAL FAILURE for the day, stated in my
close-out and in my final summary, and it is never covered by my writing the part myself. The member
commits nothing: it hands me file bodies and I commit them, so every artefact in the day's diff has one
author of record.

### 1.2 Seat count — Rule 0, followed literally for the second consecutive session

My convening instruction names four seats to spawn: `research-analyst`, `integrity-officer`,
`analytics-officer` and `systems-researcher`. **`prompts/daily_council_protocol.md` Rule 0 directs two,
and its own Precedence section says it wins and that the conflict is worth a line in the caveats.** That
line is at Part 7. One seat is convened.

- **`analytics-officer` is NOT convened.** The digest sentence at §1.4 is mine, copied from `meta.json`.
- **`systems-researcher` is NOT convened.** The one optimization item at §6.4 is one paragraph of mine.
- **`integrity-officer` is NOT convened.** I discharge the integrity challenge myself at Part 3, in my
  own name. Whether that is the right rule is **escalation 11 and Emory's to settle, not mine**;
  yesterday's session put evidence on both sides of it and today's adds one more datum, which §6.5
  states rather than resolves.

### 1.3 Channel state — probed at session open, on neutral controls, nothing inherited

The known constraint was re-probed rather than assumed, as my convening instruction requires.

- **Direct curl egress: DEAD.** `https://example.com/` returned `curl: (56) CONNECT tunnel failed,
  response 403`. The proxy's own status endpoint logged the refusal against me:
  `{"ts":"2026-09-25T11:15:32.635Z","kind":"connect_rejected","detail":"gateway answered 403 to CONNECT
  (policy denial or upstream failure)","host":"example.com:443"}`.
- **The fetch service: DEAD.** The same neutral control returned a typed `EGRESS_BLOCKED` error —
  *"Access to example.com is blocked by the network egress proxy."* **One detail worth recording, and it
  is about the diagnostic and not the capability:** this used to surface as a bare 403 indistinguishable
  from a refusal by the host. It now names the egress proxy as the refusing party. That is a better
  instrument for telling our own plumbing from a source's answer, and it changes nothing about reach.
- **Web search: ALIVE.** Confirmed by one neutral control query of mine, which returned results. The
  analyst was told not to spend a research query re-establishing it.
- **The relay: ALIVE, and inside this session's merge scope.** `scripts/fetch_relay.py` with
  `.github/workflows/fetch-relay.yml`, fired by a push to `analytics/fetch-requests/**`. It returned
  ground yesterday. **So no fetch-dependent item is stranded for want of an instrument today** — an item
  is stranded only if its host is off `ALLOWED_HOSTS` (`scripts/fetch_relay.py:49-69`), which is
  escalations 1 and 2, or if no row is due, which is §1.5.

**No statement above is a statement about any source.** A fetch-layer status is never a claim about a
resource, and nothing here licenses any assertion about ICSID, italaw, UNCTAD or anything else.

### 1.4 Digest of record — UNCHANGED (Rule 1 protocol sentence, copied from `meta.json`)

`Digest 2026-09-21: 21 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied verbatim from `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`: `"screened": 21`,
`"matches": 0`, `"watch_list_leads": 0`. **This is the fourth consecutive day the council has met on this
same digest of record.** `weekly.yml` fires Mondays at 13:00 UTC; 2026-09-21 was Monday and today is
Friday, so nothing newer was due and its absence is not a finding. **The digest is not evidence of a
quiet field**, and that qualification is carried, not restated from memory: its own `health_warnings`
holds a source-degradation warning for `gdelt`, and escalation 7 — a falsified reason committed in
`specs/iisd_itn.yaml:24` — is still open.

### 1.5 The agenda-setting fact: no cadenced observation is due today, so no row fires

**This is the first thing I established and it set everything else.** Taking yesterday's B2
reconciliation and the chairman's §6.7 ruling together, and verifying the one part of it that changed
yesterday against the file rather than the prose:

- **Thread 11** — ICSID's *Decisions on Manifest Lack of Legal Merit* register, relay. **Fired
  yesterday, 2026-09-24.** The cycle is a month (`analytics/optimization-log.md:65`); yesterday's
  §6.9(3) puts the next row on or around 2026-10-13 and says in terms that *"a session that fires it
  daily is spending the council's one substantive row on a surface it has just measured as moving in
  weeks."* **Not due. Not fired.**
- **Thread 13** — Huawei v Sweden, ICSID ARB/22/2. The weekly cadence was **suspended by name
  yesterday** and the watch re-adopted at monthly, **resuming 2026-10-18**. I verified this in
  `state/research_log.json`, `open_threads[13]`, and all six elements of the suspension form are present,
  including element (iv), the explicit clause that the absence of rows is this council's decision and is
  not a null about the docket. **And today is the day the suspended weekly row would have fallen due** —
  2026-09-18 plus seven days. So today is the first day the suspension actually bites, and the six
  elements are doing the work they were adopted for: a successor reading this gap in
  `analytics/fetch-results/` will find a sentence telling it the gap is a decision.
- **Thread 10** — Wingtech / Nexperia v Netherlands. Last fired on a Wingtech-discriminating literal
  **2026-09-10**, monthly cadence. **Not due.**
- **The monitored-author check** — suspended at zero search budget under a carried prohibition, not
  lapsed.

**Therefore the honest default today is that no relay row fires and that budget goes unspent.** I told
the analyst so, told it that leaving the budget unspent is the correct outcome and not a failure, and
invited it to show me I was wrong with a measurement if it could. **What that frees is the whole
session**, and I spend it on the research question, which the search track can reach and which has not
moved in thirteen sessions.

I asked for three things, with budgets, in this order:

1. **The research question, searched — at most five queries, and the priority.** Yesterday spent four of
   four on it and returned a clean null, which was accepted as an honest answer. **I did not want the
   same four queries.** Yesterday came at the intersection from the subject-matter side, IP as a covered
   investment. I asked for the **doctrinal side** instead — abuse of right, treaty-shopping, the
   critical-date test, and the codified early-dismissal gates — then a test of whether any 2026 decision
   under those doctrines touches an IP, trade-secret or clinical-data investment. A null from that
   direction tests a different half of the intersection than a repeat null from yesterday's. The query
   design is the analyst's, not mine, and it was told to say so if it disagreed.
2. **The quiet-session counter, at zero fetch and zero search.** Yesterday's §6.9(8) records it as
   broken and inherited rather than derived: 2026-09-20 says "tenth", 2026-09-21 and 2026-09-22 **both**
   say "eleventh", 2026-09-23 "twelfth", 2026-09-24 "thirteenth". I asked for one of two answers — a
   stated rule that yields a re-derivable number, or a demonstration that no such rule is recoverable
   and a recommendation to retire the counter. **Either answer closes a standing defect**; carrying a
   figure nobody can check does not.
3. **My own finding, handed to the analyst to break.** The record-length measurement at §6.3. **I am the
   party with the interest — I write the long parts — so I asked the seat I was directing to measure it
   instead of measuring it myself**, including whether my comparison day was cherry-picked and whether
   verbatim member returns, which I may not edit, account for most of the bytes.

The standing prohibitions were restated to it in full, including the retired NAFTA locator hunt, the
bar on any seat describing NAFTA Article 1110's internal structure, the carried zero-budget items that
only an operator ruling releases, the bar on reading constant byte length as unchanged content, and the
anchor-quote rule, which binds from today.

### 1.6 Duplicate guard

`analytics/daily-research/2026-09-25.md` did not exist at session open. I checked the working tree and
`git ls-tree origin/main` for that path: the newest council record on `main` is `2026-09-24.md`, merged
as #216 with a post-merge addendum as #217. No manual session had convened today. **This is the day's
only meeting and it is not a duplicate.**

---

## 2. Research analyst — filed VERBATIM

**The text between the two rules below is the analyst's return exactly as it reached me. I have not
edited, reordered, trimmed or summarised any part of it, and nothing in it is mine.** The seat was
spawned with the Agent tool against `.claude/agents/research-analyst.md`, ran 58 tool calls over
2,264 seconds, and returned a finished memo as its final message. It wrote nothing into the repository:
the working tree was clean when it handed back, which I verified with `git status --porcelain` before
filing this.

---

# RESEARCH ANALYST — RETURN FOR THE DAILY COUNCIL OF 2026-09-25

## 0. What I read, what I spent, what I did not do

Read at HEAD `c676373`: `.claude/agents/research-analyst.md`; `prompts/research_analyst.txt`; `prompts/council_calibration.md`; `prompts/daily_council_protocol.md`; `prompts/carrying_span_rule.md`; `analytics/daily-research/2026-09-24.md` §6.9 and §6.10 in full, plus its §B2 and §B3; `state/research_log.json` `open_threads` (all 37, by content not by index); `analytics/optimization-log.md` entries at lines 63–68; `agents/Workflow Threads.md` A6; `analytics/insights.jsonl` (211 lines, screened, newest six read); `scripts/fetch_relay.py:48-69`; `analytics/search-results/2026-09-24.json`.

**Spent: 5 of 5 web searches, all on A1. Zero fetch. Zero relay rows proposed. Zero escalated-gap budget.** I agree with your reading that no cadenced row is due and I did not manufacture one. I did **not** check the monitored author, describe NAFTA Article 1110's structure, touch `src/`, `scripts/`, `specs/`, `tests/`, `prompts/`, `.github/` or `docs/`, or write to `state/council_log.json`.

**Your agenda-setting fact, tested: confirmed as to due-ness, incomplete in one respect that matters.** Thread 11's monthly cadence has two limbs and your §6.9(3) prices only the relay one. The adopted item is the **new-procedural-mechanism first-application tracker**, and its implementation is a *search*: *"periodic (monthly or quarterly) search for each named mechanism paired with IP/data keywords ('ICC Article 30 investment' or 'ICSID Rule 41 intellectual property' or 'manifest lack of merit IP data investment')"*, `_Status: **adopted 2026-07-31** … monthly cadence, analyst executes_`. **Two consequences.** (a) Your pinpoint `analytics/optimization-log.md:65` is stale: at `c676373` that line is the 2026-08-02 UN Digital Library entry (`_Status: proposed._`) and the tracker is at **`:67`**. It was correct at `38acb75`; the file went 123 → 125 lines overnight. Origin of the error is **mine** — yesterday's B2 cited `:65`. (b) S1, S4 and S5 below *are* that item's three named formulations. So today's A1 is not free-standing work: it is an execution of an adopted cadenced observation, and it should be booked as one. The limb is **not** overdue — yesterday's B2 records it last fired 2026-09-11, 14 days, inside a month — so this is early execution, not repair. Booking it resets that clock to 2026-09-25.

---

## A1 — THE RESEARCH QUESTION, SEARCHED FROM THE DOCTRINAL SIDE. FIVE CALLS. HONEST NULL.

**Proposition, written before the first call:** *Between 1 January and 25 September 2026, at least one publicly reported investor–State decision applied one of the live jurisdictional/admissibility doctrines — abuse of right/abuse of process, treaty-shopping, the critical-date / reasonably-foreseeable-dispute test, or a codified early-dismissal gate (ICSID Arbitration Rule 41 / Rule 41(5), 2026 ICC Article 30) — in a case in which an intellectual-property right, trade secret or clinical-trial-data entitlement was asserted as the covered investment.*

**Rigid designators available in advance:** `Rule 41(5)`, `Article 30`, `abuse of process`, `manifestly without legal merit`, `trade secret`. Unlike yesterday, the screen's strongest designators are **codified rule numbers**, not generic field terms — that is the methodological gain from your redirection, and it is why I think five queries here were better spent than five on the subject-matter side. I own the design and I endorse it.

**Queries, pre-registered before the first call (all five, verbatim), `blocked_domains: ["dailyjus.com","unis.unvienna.org"]` on 5 of 5, `allowed_domains` on none:**

| | query | rows |
|---|---|---|
| S1 | `"manifest lack of legal merit" ICSID decision 2026 tribunal Rule 41 dismissed claim` | 10 |
| S2 | `investment treaty arbitration 2026 decision "abuse of process" corporate restructuring treaty shopping jurisdiction declined` | 10 |
| S3 | `investment arbitration 2026 award "critical date" dispute foreseeable nationality restructuring ratione temporis jurisdiction` | 9 |
| S4 | `"manifestly without legal merit" objection patent trademark "trade secret" intellectual property investment claim tribunal` | **3** |
| S5 | `ICC Arbitration Rules Article 30 early determination first application 2026 manifestly without merit decision` | 10 |

**42 rows, 37 distinct URLs, 22 of them with zero carriers in this tree** (screened `grep -rlF` per URL at `c676373`; validated against a known positive — the ICSID manifest-lack-of-merit table URL returns 43 carrying files). **No body was retrieved from any host. No title is read as a holding. Prohibited-host row screen: `dailyjus.com` 0, `unis.unvienna.org` 0.**

**THE ANSWER IS A NULL, and it is a better-shaped null than yesterday's.** The strongest single measurement is **S4's row count: 3, against 9–10 on the other four calls, and all three rows are pages this repository already carries** (the ICSID Rule 41 procedures page, the ICSID register, one Jus Mundi wiki note). Add the Ring-1 designators to a Ring-3 codified designator and the corpus the engine can offer *collapses*. That is not the engine failing to find a paper; it is the engine reporting that there is no IP-and-early-dismissal literature to return. A null with three on-doctrine rows is evidentially stronger than a null with ten off-theme ones, because nothing was available to pad it with.

**S5 is the one substantive advance, and it is a negative.** Ten rows — eight law firms, one practitioner blog, `iccwbo.org` — every one describing the 2026 ICC Rules. **Not one title reports an application of Article 30, in an investment case or any other.** `STATE_OF_THE_ANSWER.md:1014` already records *"No application of ICC Article 30 to an IP-as-investment or pharmaceutical clinical-data case has been identified as of July 29, 2026"*, and that the Article 30 text has not been directly read. Today extends that negative to **2026-09-25 at title level only**, four months after the entry into force the living memory records at MEDIUM confidence. This does not touch thread 11's closure condition, which needs a primary body.

**Near-misses, named as such, none inflated.**
- `aria.law.columbia.edu/…/barst-v-ecuador/` — *"The Nationality and Substance Thresholds in Investor-State Jurisdiction: Reassessment in Light of Barst v. Ecuador"*. `Barst` has **zero** carriers in this tree. Ring 3 only; **no IP element in the title. Does not reach the theme.**
- Goodwin's 2026 ICC Rules alert sits at a URL containing the segment `practices-ip` — the firm's IP practice published it. **Its title carries no IP subject-matter and I assert nothing from a path segment.** This is the closest thing in 42 rows to the intersection, and it is a publisher fact, not a legal one.
- S1 returned the Spentech ITN piece (already carried in four daily records) and the `A/CN.9/1270` PDF (already carried in ten files). Neither is new; the ITN piece is under standing quarantine and the docket is off the agenda.

**Engine prose: quarantined on sight, four times, and used nowhere.** S1's prose stated a holding and a costs disposition for Spentech — a **fourth** Spentech synthesis pass. S2's named an Ontario Superior Court decision of 10 April 2026 in a *Grace / Oro Negro v. Mexico* matter (`Oro Negro`: **zero** carriers here; control `consent-after-denunciation` returns 3 — so the prose is offering this council a genuinely new name it may not use). S3's named a June 2026 ICSID claim against Ecuador and a US$214 million predecessor — another Ecuador synthesis on a slug already at zero budget. S5's gave LCIA early-determination success rates: **a search-synthesis figure, banned by your instruction, not transcribed.** No returned title carries any of it. **All five queries were pre-registered before the first call, so none was designed from any of it.**

**No new GAP-UNRESOLVED slug opened, deliberately.** The obvious candidate — no identified application of ICC Article 30 — would be a gap whose only lever is adding `iccwbo.org` to `ALLOWED_HOSTS`, i.e. escalation 1's shape with a second host, and open thread 11 already carries the question. Opening it would inflate the escalation counter for a question already on your list. No carried slug was worked today, so none is re-emitted.

**Method note (self-training).** Doctrine-first beats subject-matter-first on this intersection for one measurable reason: a codified rule number is a rigid designator an engine cannot paraphrase, and the *collapse* of the row count on the crossing query is a readable signal. Yesterday's lesson was that Ring-2 vocabulary is owned by the drug-pricing literature; today's is the Ring-3 analogue — a gate's name retrieves the **rules-commentary** literature, not dispositions. Both point the same way: the crossing query, not the single-ring query, is where the information is.

---

## A2 — THE QUIET-SESSION COUNTER. ANSWER (b): NO RULE IS RECOVERABLE. RETIRE IT.

Zero fetch, zero search. Every parse below was validated against a known non-zero before any zero was reported.

**There is not one counter in the record. There are two, with different predicates, and they are numerically incompatible.**

- **Series A**, predicate *"the research question did not move"*: `2026-09-03.md:1301` ninth → `09-04.md:260` tenth → `09-05.md:545` eleventh → `09-06.md:812` **twelfth** → `09-07.md:605` **twelfth**. First stall.
- **Series B**, predicate *"quiet at the intersection"*: `09-16.md:1218` **sixth** → seventh → eighth → ninth → `09-20.md:249` tenth → `09-21.md:253` eleventh → `09-22.md:732` **eleventh** → `09-23.md:704` twelfth → thirteenth. Second stall.

**The defect you itemised in §6.9(8) is real but is the smaller half.** The 09-21/09-22 double is the *second* stall. The larger break is the join: Series A reads **twelfth on 2026-09-06** and Series B reads **sixth on 2026-09-16** — the number falls by six across ten days, **no reset event is named in either record**, and `09-08.md:1178` states the question did not move while attaching no number at all.

**Three candidate predicates; three different numbers for today.**
1. *"Sessions since the last one at which the answer moved."* Anchored: `2026-09-12.md:1583` — *"The answer moves, and for the second consecutive session it moves — but this time a primary source moved it."* Run = 09-13 … 09-25 = **thirteen**. But `09-19.md:545` records that day's *"honest movement is subtractive"*, and a withdrawal is a movement, which gives **seven**.
2. *"No new matter reached the three-ring intersection."* **Has no anchor at all** — no session in the recorded history established such a matter, so the count is every daily record ever written (94 daily-council records, June–September), which no seat has ever written and nobody means.
3. The mechanical one — *no substantive change to Sections I–IV of the living memory.* Not evaluable: of the 21 September commits touching `STATE_OF_THE_ANSWER.md` on this branch, 17 change nothing but the currency banner (parse validated — it returns 904, 13, 12 and 11 on the four that do), and the file's history on this branch reaches back only to 09-20.

**Conclusion.** The ordinal is not re-derivable, because re-deriving it requires choosing between predicates the record never distinguished, and the two it actually used give twelfth-then-sixth for adjacent weeks. **I recommend retiring the bare ordinal.** If a figure is wanted, replace it with a sentence that carries its own predicate and anchor, which a successor can re-derive without asking anyone: *"Quiet at the three-ring intersection on every daily record since 2026-09-12, the last record stating that the answer moved — thirteen records, today included."* **The number for today under the only anchored predicate is thirteen** — which equals yesterday's inherited figure by coincidence and means yesterday's was one too high *for yesterday*.

---

## A3 — YOUR FINDING, ATTACKED. IT SURVIVES, AND MY CORRECTIONS MAKE IT WORSE FOR YOU.

### (i) The measurement is arithmetically exact and the population is mixed.

26 September files (excluding today's), sum **4,320,386** bytes, mean **166,168.69** → your 166,168 is exact, and mean words **24,832** → your ≈24,000 is right. **But two of the 26 are not daily council meetings** (`2026-09-10-special-session.md` 111,157; `2026-09-13-rulings.md` 35,793), and **your stated range 123,770–245,262 belongs to a different population than your mean** — the 26-file minimum is 35,793, not 123,770. Correcting to the **24 daily council records**: mean **173,893** bytes, median 169,803.5, mean **25,951** words, range 123,770 (09-09) – 245,262 (09-18). **The correction runs against your own interest: the multiple rises from 2.75× to 2.88× by bytes, 2.94× by words.**

### (ii) The 2026-08-08 comparator is cherry-picked. The honest statistic is stronger than the one you used.

**60,424 bytes is the minimum of all 31 August records.** The days around it: 08-07 **134,233** (the record that provoked the rule), 08-09 **116,873**, 08-10 **117,960**, 08-11 80,257, 08-12 **146,331**. The rule produced a one-day dip and the series was back above 116k the next day. **So yes — you picked the single most favourable day available.** Use period means instead: July **28,195** (n=31) → August pre-rule, 08-01…08-07, **116,590** (n=7) → August post-rule, 08-08…08-31, **161,210** (n=24) → September **173,893** (n=24). **The mean has risen in every period since Rule 0 was adopted to shrink it, and September is 6.2× the July mean.** Your finding is right; the sentence to put in the record is the monotone series, not the one-day ratio.

### (iii) The confound is real — and it inverts your remedy, not your finding.

I could attribute bytes on **9 of 24** records (2026-09-16 → 2026-09-24; the earlier 15 use the Part I/II/III heading format abandoned on 09-16 and resist the same parser). Positive control: the parser reproduces 2026-09-24's five part spans exactly as I had read them off the heading offsets by hand.

On those nine: **member verbatim 70.2% of bytes, chairman-written 29.8%. Chairman mean 58,168 bytes** (min 50,158 on 09-22, max 82,991 on 09-23); member mean 136,338.

**So the confound exonerates you on the margin and convicts the remedy you proposed.** Cutting your own share **to zero** leaves the record at 136,338 bytes — still **2.26×** the 2026-08-08 record. The cuttable mass is in the seat returns. The REAL AGENTS rule bars you from *editing* them; it does not bar you from *budgeting* them, and the lever is the **convening instruction** — exactly what you used on me today. Second, measured, lever: 2026-09-24, the first session in twenty-one to comply with Rule 0 literally by not convening the officer, is **152,710** bytes, second smallest of the nine; on 2026-09-23 the officer's own section ran **55,305** bytes, 27% of that record. n=1, so that is an indication, not a measured effect.

**Your limb (iii) — invisible to every guard — is CONFIRMED.** No size expression (`getsize`, `st_size`, `len(text)`, `MAX_BYTES`, `max_bytes`) occurs anywhere in `scripts/` or `.github/`; positive control, the same grep with `sha256` added, returns 22 hits. `scripts/send_daily_update.py`, which mails the record, neither truncates it nor reports its size.

### The remedy I recommend, and the two I recommend against.

- **Against a total-byte ceiling in Rule 0.** 70% of the bytes arrive from seats you may not edit, so it would be a rule breached by another's act. This council already owns one guard that reports green on nothing; it does not need a rule that reports red on nothing you did.
- **Against a mechanical guard now.** A guard on the chairman-written share is measurable on 9 of 24 records. A guard that can measure 37% of its corpus and returns a clean zero on the rest is `check_marks.py` again. Its precondition is a stable part-heading vocabulary — a separate, cheap decision.
- **For, in this order:** (1) **a per-seat word budget stated in the convening instruction**, which is where the lever actually is and costs nothing; (2) **a reporting-only line in the close-out** — total bytes, chairman-written bytes where attributable, `NOT ATTRIBUTABLE` where not — no gate, because a number in the record is what makes a trend visible and nothing measures this today.

---

## Carrying-span marks (R7 binds this return)

**P** The adopted first-application tracker carries a monthly cadence executed by the analyst and defines its own queries. **Q** *"adopted 2026-07-31 (chairman, per the 2026-07-30 close-out; analytics: CORROBORATED at 3 queries on first use) — monthly cadence, analyst executes, next cycle late August 2026"* and *"Implementation: periodic (monthly or quarterly) search for each named mechanism paired with IP/data keywords"* [`analytics/optimization-log.md:67` at `c676373`; `:65` at `38acb75`]. **D** `analytics/optimization-log.md`, entry dated 2026-07-29, read today; a repository ledger, not a legal document; status adopted; outcome on the point cited — the cadence and the instrument are both stated in the entry itself. **V** checked 2026-09-25 at `c676373`; screened `monthly` 13 lines / `Article 30` 3 files / `first-application tracker` 17 files; the other `monthly` hits are the 08-04 STALE-signal entry (a hypothetical monthly feed), the 07-28 blog watch, the 07-09 concurrent-proceedings monitor and six further proposed entries, none adopted; no exit taken.

**P** Rule 0 was adopted to shrink the daily record and names no length. **Q** *"A daily check-in that costs a weekly council is a daily check-in that stops being read, and an unread record verifies nothing."* [`prompts/daily_council_protocol.md:33`] **D** `prompts/daily_council_protocol.md`, Rule 0, adopted 2026-08-07, read today; outcome on the point cited — the rule bans six named sections and states a purpose; **no byte or word figure appears in it.** **V** checked 2026-09-25 at `c676373`; screened `bytes` 0, `words` 0, `length` 0, `size` 1 (the Rule 0 heading itself); no exit taken.

**P** The record states that the answer moved on 2026-09-12. **Q** *"The answer moves, and for the second consecutive session it moves — but this time a primary source moved it, which has not happened on this thread before."* [`analytics/daily-research/2026-09-12.md:1583`] **D** the 2026-09-12 daily council record, §9, read today; outcome on the point cited — the same passage states *"No new instance of intellectual property reaching a jurisdictional gate as a covered investment is established"*, which is why predicate 2 in A2 gives a different answer from predicate 1. **V** checked 2026-09-25 at `c676373`; screened `consecutive session` across all September records, `quiet` and `did not move` per file; the 09-11/09-12 pair is the only place a movement is affirmed in the window; no exit taken.

**Nothing in A1 carries a span: I opened no body.** Every external statement in A1 is title-level and is marked as such.

---

## Errors against myself

1. **The stale pinpoint is mine.** Yesterday's B2 cited `analytics/optimization-log.md:65`; today the entry is at `:67` and `:65` is a *proposed* entry about the UN Digital Library. Correct at `38acb75`, wrong 24 hours later — the shortest drift interval this project has measured, on a file that had never carried the defect. Your agenda inherited it from me. The class is already `analytics/insights.jsonl:91` (three generations), so I log no insight for it; I log the instance against myself.
2. **I came into this session about to file an "overdue tracker" finding, and it would have been wrong.** The search limb last fired 2026-09-11 — 14 days, current against a month — and *my own B2 said so yesterday*. I nearly re-derived a lapse out of a half-read log entry, which is the exact failure §6.9(8) warns about in the counter.
3. **My first attribution parser produced four clean, quotable, false numbers** — 0% member share on 2026-09-04, 100% chairman-written on 09-13, 09-14 and 09-15 — because headings like `## 2. TASK 2 — the batch` matched a chairman vocabulary term. I caught it only because §6.5's same-shaped-positive rule made me check the parse against a known non-zero I had read by hand. Had I filed the first table, this record would assert that you wrote 100% of three records. **That is yesterday's `0924-C6` lesson firing on me on the next day, in the same shape.**
4. **I did not fully test predicate 3 in A2.** The git-history route is evaluable only back to 09-20 on this branch and I stopped rather than walk merge history. A successor could get further; I record that as unfinished, not as impossible.

---

## Insights — checked against `analytics/insights.jsonl` (211 lines) first

Screened: `Rule 0` 0 hits, `record length` 0, `Article 30` 0, `result count` / `row count` / `collapse` — nothing on result-count as a signal; `explainer` 0 (that lesson lives in the daily records as `0820-R9`, not in the ledger). Control: 211 lines contain `insight`. **Three proposed, none a restatement.**

```json
{"date":"2026-09-25","thread_id":"METHOD-crossing-query-row-count-collapse-is-the-strongest-available-null","ring":"Ring 1 x Ring 3 / method","insight":"ON THIS INTERSECTION THE ROW COUNT OF THE CROSSING QUERY IS ITSELF THE MEASUREMENT, AND A COLLAPSE IS A STRONGER NULL THAN A FULL PAGE OF OFF-THEME ROWS. Five doctrine-side calls on 2026-09-25 returned 10, 10, 9, 3 and 10 rows. The one call that crossed a Ring-3 rigid designator with the Ring-1 designators ('manifestly without legal merit' x patent/trademark/'trade secret'/intellectual property) returned THREE rows, and all three are pages this repository already carries: the ICSID Rule 41 procedures page, the ICSID manifest-lack-of-merit register, and one Jus Mundi wiki note. The engine had nothing to pad with. Reusable rule: report the crossing query's row count next to the null, because a 3-row null on-doctrine is evidence of an empty intersection while a 10-row null off-doctrine is evidence only about vocabulary.","sources":["file://analytics/search-results/2026-09-25.json"],"confidence":"high"}
{"date":"2026-09-25","thread_id":"T11-icc-article-30-no-application-reported-at-title-level-2026-09-25","ring":"Ring 3 - jurisdictional gates","insight":"THE ICC ARTICLE 30 LIMB OF OPEN THREAD 11 IS STILL EMPTY, AND THE NEGATIVE NOW REACHES 2026-09-25 AT TITLE LEVEL. STATE_OF_THE_ANSWER.md:1014 records that no application of ICC Article 30 to an IP-as-investment or clinical-data case had been identified as of 2026-07-29 and that the Article 30 text has not been directly read. A bounded search today returned ten titles from eight law firms, one practitioner blog and iccwbo.org, every one describing the mechanism and NOT ONE reporting an application of it in any case, investment or otherwise. LIMITS, stated: title level only, one engine, no body opened, iccwbo.org is off the relay ALLOWED_HOSTS so the limb remains structurally untestable by this instrument, and this does not touch the thread's closure condition, which requires a primary body.","sources":["file://STATE_OF_THE_ANSWER.md:1014","file://analytics/search-results/2026-09-25.json"],"confidence":"medium"}
{"date":"2026-09-25","thread_id":"PROCESS-rule-0-has-been-complied-with-section-by-section-while-the-record-tripled","ring":"instrument / process","insight":"RULE 0 WAS ADOPTED 2026-08-07 TO SHRINK THE DAILY RECORD AND THE RECORD'S MEAN LENGTH HAS RISEN IN EVERY PERIOD SINCE, BECAUSE THE RULE BANS SECTIONS AND NAMES NO LENGTH. Daily-council record means: July 28,195 bytes (n=31); August pre-rule 08-01..08-07 116,590 (n=7); August post-rule 08-08..08-31 161,210 (n=24); September 173,893 (n=24, mean 25,951 words). The 2026-08-08 record (60,424 bytes) is the MINIMUM of all 31 August records and the series was back above 116,000 the next day, so a one-day comparison flatters the rule; the monotone period series is the honest statistic. On the nine records whose part structure permits attribution (2026-09-16..09-24) 70.2% of bytes are member returns the chairman may not edit and 29.8% are chairman-written (mean 58,168 bytes), so ZEROING the chairman's own share would still leave 2.26x the 2026-08-08 record: the lever is the convening instruction's per-seat budget, not editing. Nothing in the repository measures record length - no size expression occurs in scripts/ or .github/ (positive control: the same grep with sha256 added returns 22 hits) and the emailer neither truncates nor reports size.","sources":["file://prompts/daily_council_protocol.md:33","file://analytics/daily-research/"],"confidence":"high"}
```

---

## FILE BODIES — hand-off (I commit nothing)

**`analytics/search-results/2026-09-25.json`.** Validated: `json.load` parses; 5 calls, 42 rows, 25 hosts, 37 distinct URLs, 8 rows at relay-allowlisted hosts. **22,812 bytes, sha256 `414ecdab345ddb498224c5a75ccccc414033d8524f48a7c4f51185ac3179fb3d`.**

I have **not** pasted it inline, and the choice is deliberate and is the first ruling I ask of you: pasting 22.8 KB into a record whose length is today's agenda item would add 15% to this return to no reader's benefit. The body is at:

```
/tmp/claude-0/-home-user-isds-watcher/01abb57c-67dd-58e5-a18b-d1994d665e12/scratchpad/sr.json
cp that file to analytics/search-results/2026-09-25.json   # verify the sha256 above after copying
```

If that path is not readable from your shell, or you want the one-author-of-record property to rest on a paste rather than on a path, say so and I will paste the full body verbatim on reconvening. **No other file body.** No fetch-request file: no row is due and none is proposed.

---

## `candidate_claims`

```json
{"candidate_claims": [
  {"claim_text": "analytics/optimization-log.md records the new-procedural-mechanism first-application tracker as adopted 2026-07-31 with a monthly cadence executed by the analyst, and defines its implementation as a periodic search pairing each named mechanism with IP/data keywords; the entry is at line 67 of the file at commit c676373 and was at line 65 at commit 38acb75.",
   "source_url": "file://analytics/optimization-log.md",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "Implementation: periodic (monthly or quarterly) search for each named mechanism paired with IP/data keywords (\"ICC Article 30 investment\" or \"ICSID Rule 41 intellectual property\" or \"manifest lack of merit IP data investment\").",
   "supporting_locator": "analytics/optimization-log.md:67 at c676373; the same entry at :65 at 38acb75, where the 2026-09-24 record cites it. File length 123 lines at 38acb75, 125 at c676373."},

  {"claim_text": "Investment Treaty News published a piece titled \"Spentech Engineering Limited v. United Arab Emirates: The standard for manifest lack of legal merit under Rule 41\".",
   "source_url": "https://www.iisd.org/itn/2026/04/21/spentech-engineering-limited-v-united-arab-emirates-rule-41-vismaya-hari/",
   "access_status": "unavailable", "source_authority": "official_secondary", "claim_type": "lead", "assertion_status": "lead",
   "supporting_quote": null,
   "supporting_locator": "Engine-returned title, certified character-for-character as the search return, 2026-09-25 call S1. Body unread by this council; the matter's synthesis passes are under standing quarantine and the docket is off the agenda. Nothing about the decision is asserted."},

  {"claim_text": "The American Review of International Arbitration published a piece titled \"The Nationality and Substance Thresholds in Investor-State Jurisdiction: Reassessment in Light of Barst v. Ecuador\"; the string \"Barst\" has zero carriers in this repository.",
   "source_url": "https://aria.law.columbia.edu/the-nationality-and-substance-thresholds-in-investor-state-jurisdiction-reassessment-in-light-of-barst-v-ecuador/",
   "access_status": "unavailable", "source_authority": "academic_secondary", "claim_type": "lead", "assertion_status": "lead",
   "supporting_quote": null,
   "supporting_locator": "Engine-returned title, 2026-09-25 call S3. Prior-art screen by grep -rlF at c676373, validated against a known positive (Bridgestone: 93 files). Ring 3 only; no IP element in the title; does not reach the theme."},

  {"claim_text": "A bounded five-call search on 2026-09-25 across the codified early-dismissal gates, abuse of process, treaty-shopping and the critical-date test returned 42 rows and 37 distinct URLs, none of whose titles reports a 2026 investor-State decision in a matter where an intellectual-property right, trade secret or clinical-trial-data entitlement was the asserted investment.",
   "source_url": "file://analytics/search-results/2026-09-25.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "procedural", "assertion_status": "candidate",
   "supporting_quote": "THREE rows, against 10, 10, 9 and 10 on the other four calls, and all three are pages this repository already carries.",
   "supporting_locator": "analytics/search-results/2026-09-25.json, calls[3].row_count_note. Claim is about this session's returns, not about the world; no body was opened."},

  {"claim_text": "The mean length of the daily council record has risen in every period since Rule 0 was adopted on 2026-08-07: 28,195 bytes for the 31 July records, 116,590 for 2026-08-01 to 08-07, 161,210 for 2026-08-08 to 08-31, and 173,893 for the 24 September daily-council records.",
   "source_url": "file://analytics/daily-research/",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "A daily check-in that costs a weekly council is a daily check-in that stops being read, and an unread record verifies nothing.",
   "supporting_locator": "prompts/daily_council_protocol.md:33 for the rule's stated purpose; byte counts measured by os.path.getsize over analytics/daily-research/*.md at c676373, excluding 2026-09-10-special-session.md and 2026-09-13-rulings.md as not daily council meetings."},

  {"claim_text": "On the nine daily records whose part structure permits attribution (2026-09-16 to 2026-09-24), 70.2 per cent of bytes lie in member returns filed verbatim and 29.8 per cent in chairman-written parts, the chairman-written share averaging 58,168 bytes.",
   "source_url": "file://analytics/daily-research/2026-09-24.md",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "## 2. Research analyst — filed VERBATIM",
   "supporting_locator": "Part boundaries taken from the level-2 headings with monotonic part numbers; parser validated by reproducing 2026-09-24's five spans (412 / 11,481 / 69,364 / 86,735 / 114,293) read off the heading offsets by hand. The other 15 September records use the Part I/II/III format abandoned on 2026-09-16 and are NOT ATTRIBUTABLE by this parser; the figure covers 9 of 24 records and no more."}
]}
```

---

## What I ask you to rule on

1. **The artefact body: path or paste.** Recommend path, for the reason at §File bodies.
2. **Book today's A1 as the first-application tracker's search-limb cycle**, resetting that limb to 2026-09-25 (next ~2026-10-25), and record that the tracker's cadence has **two limbs** so the next session does not price the relay row and the search as one item. Without the booking, this council will next month be unable to tell an executed cycle from an omitted one — the mirror image of the defect I proved yesterday.
3. **Correct the cadence pinpoint to `analytics/optimization-log.md:67` at `c676373`, with the commit, and note the one-day drift from `:65`.** The error originates in my B2 yesterday.
4. **A2: retire the bare ordinal** and adopt the self-describing replacement sentence, with today's figure recorded as **thirteenth under the anchored predicate**, not as inherited.
5. **A3: adopt (1) a per-seat word budget in the convening instruction and (2) a reporting-only length line in the close-out; do not adopt a byte ceiling in Rule 0 and do not commission a mechanical guard yet.** If you want the guard, rule first on stabilising the part-heading vocabulary, which is the precondition.
6. **Whether my correction to your population (24 daily records, not 26) should be applied to the figures you carry forward.** It makes your own finding stronger, which is why I want it ruled on rather than quietly adopted.

**One line for a reader with no context.** The research question did not move — thirteen daily records now, counted from the last one that says the answer moved — and today it was searched from the doctrinal side for the first time, where the decisive measurement was that crossing a codified early-dismissal gate with the words *patent*, *trademark* and *trade secret* collapsed the engine's return from ten rows to three, all three of them pages this council already holds; no application of the 2026 ICC Article 30 gate is reported anywhere at title level four months after it took effect; and the council's own record grew for a seventh consecutive week under a rule adopted to shrink it, with 70% of the bytes arriving from seats the chairman may not edit and the only real lever sitting in the instruction he writes when he convenes them.

---

**End of the analyst's return, filed verbatim. Everything below this line is mine.**

---

## 3. The integrity challenge — discharged by me, in my own name

Under Rule 0 the integrity officer is not a member of the daily meeting, so there is no seat to perform
and I do the checking myself. **Seven objections, numbered so they can be ruled on individually. Four are
against me. The method was execution, not reading:** where a number could be recomputed I recomputed it
rather than accepting it, and where it could not I say so.

### 3.1 `0925-C1` — AGAINST ME, SUSTAINED. My own range did not belong to my own population.

My agenda gave the analyst *"26 records, mean 166,168 bytes"* and, in the same breath, *"the series runs
123,770 to 245,262 bytes."* **Those two figures describe different populations and I did not notice.**
Recomputed by me: over the 26 September files excluding today's, the sum is 4,320,386 and the mean is
**166,168.69** — so my mean is exact — but the **minimum of that population is 35,793**, not 123,770.
The 123,770 figure is the minimum of the 24 *daily council meetings*; the 26-file set also contains
`2026-09-13-rulings.md` (35,793) and `2026-09-10-special-session.md` (111,157), which are not meetings.
**Sustained against me in full.** The correct population is 24 records, mean **173,893.17** bytes,
median **169,803.5**, mean **25,951.4** words, range 123,770–245,262 — every one of which I reproduced
to the decimal. **And the correction is against my interest, which is why I take it without argument:
the multiple I was going to cite rises from 2.75× to 2.88×.**

### 3.2 `0925-C2` — AGAINST ME, SUSTAINED. I picked the single most favourable comparison day available.

I built the finding on the 2026-08-08 record at 60,424 bytes, the day after Rule 0 landed. **The analyst
establishes that 60,424 is the minimum of all 31 August records, and I verified the neighbours myself:**
08-07 is 134,233, 08-09 is 116,873, 08-10 is 117,960, 08-12 is 146,331. The rule produced a one-day dip
and the series was back above 116,000 within twenty-four hours. **A ratio against the best day in the
month is not a measurement, it is advocacy**, and I was the party with the interest in it. The
replacement is the period series, which I recomputed independently and which reproduces exactly: July
**28,195** (n=31) → 2026-08-01…08-07 **116,589** (n=7) → 2026-08-08…08-31 **161,210** (n=24) → September
**173,893** (n=24). **Monotone increase across every period since the rule was adopted to produce a
decrease.** That is the sentence the record carries, and it is stronger than the one I wrote.

### 3.3 `0925-C3` — AGAINST ME, AND IT IS THE WORST OF THE FOUR, BECAUSE OF WHAT I WAS ESCALATING.

My agenda cited the monthly cadence to `analytics/optimization-log.md:65`. **I took that pinpoint out of
yesterday's record and did not open the file.** Verified by me at HEAD: `:65` is the 2026-08-02 UN
Digital Library entry, `_Status: proposed._`; the tracker is at **`:67`**; the file went 123 lines at
`38acb75` to 125 at `c676373`, so the citation was correct yesterday and stale within a day. The analyst
attributes the origin to its own B2 and it is right about that. **But the failure I am recording is
mine and it is not the pinpoint.** On the day I convened a seat to measure a finding about this council
restating figures nobody has re-derived, I passed on a line number I had not looked at. **Sustained.
Both limbs of the lesson land on me: check the pinpoint, and notice when the defect you are escalating
is one you are committing in the same paragraph.**

### 3.4 `0925-C4` — AGAINST ME, SUSTAINED, AND THE SEAT I WAS DIRECTING CAUGHT IT.

My §1.5 priced thread 11's cadence as one item — a relay row. **The adopted tracker has two limbs and the
second is a search, whose queries the entry itself specifies.** I had read §6.9(3) and priced only what it
priced. Had the analyst accepted my framing, today's five searches would have been booked as free-standing
work and next month's session would have had no way to tell an executed search cycle from an omitted one —
**which is precisely the suspended-versus-lapsed defect this council proved yesterday, reappearing on the
next day in the one place nobody was watching for it.** Sustained; the booking is ruled at §6.2.

### 3.5 `0925-C5` — AGAINST THE ANALYST. NON-BLOCKING, BUT THE RECORD WOULD HAVE MISREPORTED ITS OWN UNIT.

**The attribution figures in A3(iii) are character counts, and they are labelled bytes.** Caught by
executing the comparison the memo did not: `2026-09-24.md` is **153,549 bytes** and **152,710
characters** — and the memo states *"2026-09-24 … is 152,710 bytes."* The parse works on
`open(f).read()` offsets, so `58,168` and `136,338` are characters too. **I measured the size of the
error before deciding how to treat it:** multi-byte content runs 0.52%–0.73% of these files, the
aggregate inflation across the nine is **1.0058**, and the load-bearing conclusion — that zeroing the
chairman's share still leaves **2.26×** the 2026-08-08 record — becomes **2.27×** when both sides are
genuinely bytes. **So the conclusion is untouched and the objection is NON-BLOCKING.** It is recorded
because the figures enter a permanent record that a successor will read as bytes, and because the
period means in A3(i) and (ii) *are* real bytes from `os.path.getsize` — I verified that by reproducing
all four — so this record now contains both units and must say which is which.

### 3.6 `0925-C6` — AGAINST THE ANALYST. NON-BLOCKING, AND NEITHER OF US IS WRONG.

The memo gives the September-to-08-08 multiple as *"2.88× by bytes, 2.94× by words."* **The byte figure
reproduced exactly; the word figure did not, and I got 2.89×.** Cause, found by execution rather than
argument: `wc -w` counts 8,837 words in the 2026-08-08 record and Python's `.split()` counts 8,977, so
the multiple is 2.937 under one tokenizer and 2.891 under the other. **Neither measurement is an error;
the discrepancy is the tokenizer.** The ruling at §6.3 therefore carries the **byte** multiple, which is
tokenizer-independent, and states the word figure only with its tokenizer named.

### 3.7 CLEAN BILL, where it is owed — and this is the most thoroughly reproducible return this seat has filed

I re-derived every load-bearing figure in the return by execution. **All of the following reproduced
exactly, with no misses:**

- **The artefact, to the character.** `analytics/search-results/2026-09-25.json`: sha256
  `414ecdab…3179fb3d` as claimed, **22,812 bytes** as claimed, `json.load` parses, **5 calls, 42 rows,
  37 distinct URLs, 25 hosts**, and **S4 = 3 rows** against 10/10/9/10 — the day's central measurement,
  checked against the file and not against the prose describing it.
- **Every A3 statistic in (i) and (ii):** the 4,320,386 sum, the 166,168.69 mean, the corrected
  173,893.17 / 169,803.5 / 25,951.4 / 123,770–245,262, and all four period means.
- **The attribution offsets.** The 2026-09-24 spans **412 / 11,481 / 69,364 / 86,735 / 114,293** are
  exactly the five monotonic top-level part boundaries — which the parser had to isolate from **21**
  level-2 headings, because the member returns re-use `## 1.`, `## 2.` and so on. The chairman share I
  derive from those offsets is 67,269, inside the range the memo reports.
- **Six of A2's pinpoints, opened one at a time.** `09-04:260` tenth, `09-06:812` twelfth,
  `09-07:605` twelfth, `09-16:1218` sixth, `09-22:732` eleventh, `09-12:1583` *"the answer moves, and
  for the second consecutive session it moves"*. Also `09-08:1178`, which does state the question did
  not move and does attach no number, and `09-19:545`, which does say the movement was subtractive.
  **The twelfth-on-09-06 / sixth-on-09-16 break is real and I confirmed both ends of it.**
- **The 94-record figure.** 97 files in `analytics/daily-research/`, less the two non-meetings, less
  today's, is **94**. Exact.

### 3.8 AND TWO AGAINST MYSELF THAT NO SEAT PUT TO ME

Both arose in my own escalation re-measurements and both are corrections to figures this council has been
carrying.

**(a) My first parse of the optimization log returned a false 9, and the rule adopted yesterday is what
stopped me filing it.** I counted `_Status:` markers line by line; five of the 61 entries span more than
one line, so their markers fell outside the line I was testing. The line-based parse said 9 entries carry
no marker. **The block-based parse says 4** — 2026-09-16, 2026-08-31, 2026-08-04 and 2026-09-15 — and a
fifth, 2026-07-29, carries `_Status: **adopted 2026-07-31**`, which my capture pattern could not read
through the asterisks. **I caught it only because §6.5's same-shaped-positive rule made me check a zero
against a known non-zero**, exactly as the analyst reports it working on its own parser at its error 3.
The rule adopted yesterday fired twice in one session, on both seats, on its first day.

**(b) Escalation 8's number is wrong as filed, and the finding is smaller than it was reported.**
Yesterday's escalation 8 reads *"8 of 60 optimization-log entries carry no status marker at all."*
**Measured by me by block parse today: 61 dated entries, of which 4 carry no `_Status:` marker — and all
four state their status in their prose anyway** ("PROPOSED ONLY", in terms). So the machine-readable
marker is missing on four entries; the *tracking* is not missing on any of them. Escalation 8 is
corrected downward and re-filed in its true, smaller form at §6.5(8).

**(c) One inherited count corrected in passing.** Yesterday's escalation 10 says sittings have stopped
writing `daily-council` stubs into `state/council_log.json` and that *"this is now three consecutive
sessions."* **The file's newest entry is dated 2026-09-22**, so at yesterday's close the run was **two**
— 09-23 and 09-24. Today's session writes none, which makes it three today. Trivial in size, and
recorded because it is the same class as everything else on this list: a number carried forward by a
seat that did not open the file.

---

## 4. Spend checkpoint — nothing fires, and that is the decision

My convening instruction places a spend checkpoint after the vetting round. **Today it resolves in one
paragraph, because there is nothing to release.** No relay row was proposed and none is due: thread 11's
register limb fired yesterday, thread 13 resumes 2026-10-18 under the suspension written into
`state/research_log.json`, thread 10 is not due, and the monitored-author check sits at zero budget. The
analyst spent 5 of 5 searches on the research question and nothing on anything else. **No fetch-request
file is written today, which means `.github/workflows/fetch-relay.yml` does not fire and no reduction
lands.** I record that expressly, because an empty day in `analytics/fetch-results/` is the exact
artefact a lapse produces, and yesterday's finding was that the only thing distinguishing a decision
from an omission is a sentence somebody wrote: **this is that sentence.** The absence of a row today is
this council's decision, taken on the cadences, and it is not a null about ICSID or about anything else.

**One consequence I flag rather than bury.** The `thread_id` convention adopted yesterday takes effect
on the request files this council authors, and today authors none — I verified that no file in
`analytics/fetch-requests/` yet carries the field. **Its first application is therefore deferred to the
next row, on or around 2026-10-13.** An adopted convention whose first use is three weeks away is a
convention that can quietly never happen, so it is carried explicitly at §6.6(3) rather than left to be
remembered.

---

## 5. Research analyst — reconvened under Rule 2, filed VERBATIM

**Rule 2's dialogue requirement is met by reconvening the real seat, not by writing its lines.** I put six
numbered objections to it in writing — one blocking — and its answers are below exactly as they reached
me. It fired no search and no row, as instructed. **Nothing in this part is mine.**

---

# RESEARCH ANALYST — PART 5, RECONVENED UNDER RULE 2

Zero searches fired. Zero rows. Nothing built. Six items, in the order of their weight, not yours.

## 1. `0925-C7` — BLOCKING. **I accept the strike, and the confound you name is testable at zero cost against our own archive. I ran that test. It does not support the confound — but a second variable you also named is unseparated, and on that one you are right.**

**The test, and it cost no search.** I parsed every recorded call in `analytics/search-results/*.json` (5 files, 29 calls) for exact-phrase count against row count.

| exact phrases in query | n | rows returned |
|---|---|---|
| 0 | 18 | 0, 8, 9, 9, 9, 9, 10×11, 40 |
| 1 | 6 | 9, 10, 10, 10, 10, 10 |
| 2 | 4 | **3, 9, 10, 30** |

**Restricted to the comparison that actually bears — two phrases in CONJUNCTION, outcome `returned`** — the archive holds three calls: `2026-09-22` S1 (`"Article 1110"` + `"Chapter Eleven"`) → **10 rows, 1 block**; `2026-09-22` S3 (same two phrases, different loose terms) → **30 rows, 3 blocks**; today's S4 → **3 rows**. `2026-09-20` S7 is excluded because its two phrases are joined by `OR`, which is a disjunction and restricts nothing; `2026-09-21` S0 is excluded as `TOOL ERROR, NO ROWS RETURNED`. **Two of the three archived conjunctive double-phrase calls returned a full first block.** So in the only corpus this council has, carrying two conjoined exact phrases does not by itself depress the row count, and S4's 3 is the minimum of all 28 successful calls on record.

**Where you are right, and I concede it without qualification.** Between S1 and S4 **two variables moved**: the Ring-1 conjunction *and* the Ring-3 phrase itself — `"manifest lack of legal merit"` → `"manifestly without legal merit"`. My archive test separates phrase *count*. It cannot separate phrase *wording*, and no archived call can: the two spellings appear in this repository's search record only once each, in the two calls at issue. **So the like-for-like reading of 10 → 3 is not available on the evidence, and the strike is correct as to the inference even though the confound you identified is not the one that does the work.**

**The separating test, precisely, for your ruling — and my recommendation is that you refuse it.** One call: `"manifest lack of legal merit" objection patent trademark "trade secret" intellectual property investment claim tribunal` — S4's query with S1's spelling substituted, everything else identical, one variable moved. Cost: one search. **I recommend against spending it.** Even a clean separation would license only a statement about one engine's retrievable corpus at one moment, not about the world, and the part of the insight worth keeping is the method rule, which needs no separation at all. Spending a search to firm up an inference I would still have to hedge is the padding this council keeps catching elsewhere.

**The honest weaker form, for logging in place of the struck one:**

```json
{"date":"2026-09-25","thread_id":"METHOD-row-count-is-a-joint-signal-hold-the-query-shape-constant","ring":"method","insight":"A SEARCH CALL'S ROW COUNT IS A JOINT SIGNAL ABOUT THE QUERY'S SHAPE AND THE CORPUS, AND IT SAYS NOTHING ABOUT THE CORPUS UNLESS THE SHAPE IS HELD CONSTANT ACROSS THE SESSION'S CALLS. On 2026-09-25 five calls returned 10, 10, 9, 3 and 10 rows; the 3-row call was the only one crossing a Ring-3 designator with the Ring-1 designators AND the only one carrying two conjoined exact phrases AND the only one using the spelling 'manifestly without legal merit' rather than 'manifest lack of legal merit'. Three variables, one observation: no attribution to the corpus is available. The phrase-COUNT limb is testable against this repository's own archive and is NOT supported - of three archived conjunctive double-phrase calls, two returned a full block (10 rows / 1 block, 30 rows / 3 blocks). The phrase-WORDING limb is unseparated and one call would separate it. BINDING METHOD RULE, which survives all of it: hold the exact-phrase count constant across a session's calls, record the count and the shape next to each row count, and never read a collapse as evidence about the world.","sources":["file://analytics/search-results/2026-09-25.json","file://analytics/search-results/2026-09-22.json","file://analytics/search-results/2026-09-20.json"],"confidence":"high"}
```

**Consequential defect in my own artefact, found running your test.** The 2026-09-20/21/22/24 schema carries `result_blocks` per call. **My 2026-09-25 file omits the field on all five calls.** That is why the archive shows 30 and 40 in the same column as 10, and it is why today's row counts cannot be normalised per block. I will not reconstruct the field from memory. The fix, if you want it, is one line per call recording what a re-run observes — which is a new observation and not this session's.

## 2. `0925-C6` — **THE ANSWER IS WORSE THAN YOUR OBJECTION. I USED TWO TOKENIZERS, ONE IN THE NUMERATOR AND ONE IN THE DENOMINATOR, AND 2.94 IS A MIXED-UNIT RATIO.**

You asked me to name the tokenizer. There are two, and the mechanism is not `wc` versus Python — **it is the same `wc` binary answering differently depending on the locale of the invocation.** Verified, `/usr/bin/wc`, GNU coreutils 9.4, one file:

- `LC_ALL=C wc -w 2026-08-08.md` → **8,837**
- `LC_ALL=C.UTF-8 wc -w` (same binary) → **8,977**
- Python `.split()` → **8,977**
- Delta **140**, which is exactly the file's count of `—` (134) + `–` (3) + `×` (3).

My interactive shell ran with an empty locale environment and returned 8,837. My September mean (25,951.4) was computed through a Python `subprocess` call that inherited `LC_CTYPE=C.UTF-8` and therefore returned the 8,977-style count. **So 25,951.4 / 8,837 crosses two units. Same-tokenizer, either tokenizer: 2.891.** Your 2.89 is right and mine was wrong.

**Yes: the record should carry the byte multiple, 2.88×, as the headline.** It is tokenizer-independent, and after C5 it is also the only one of the two that is unit-clean.

**One new insight, and it reaches further than word counts:**

```json
{"date":"2026-09-25","thread_id":"METHOD-a-shell-measurement-is-not-reproducible-unless-its-locale-is-stated","ring":"instrument / method","insight":"A FIGURE PRODUCED BY A SHELL TOOL IN THIS ENVIRONMENT IS NOT REPRODUCIBLE UNLESS THE LOCALE IS STATED WITH IT. /usr/bin/wc (GNU coreutils 9.4) returns 8,837 words for analytics/daily-research/2026-08-08.md under LC_ALL=C and 8,977 under LC_ALL=C.UTF-8; the delta of 140 is exactly the file's 134 em dashes, 3 en dashes and 3 multiplication signs. An interactive shell here runs with an empty locale environment and gets the first figure; a Python subprocess inherits LC_CTYPE=C.UTF-8 and gets the second, as does Python .split(). This is how the research analyst's 2.94x word multiple on 2026-09-25 came to cross two units in one ratio. The exposure is not limited to wc: any grep -c, sort or character-class measurement this council records can differ between a seat's interactive shell and its scripted path. RULE: state the locale, or prefer a byte count, which does not vary. Screened: 'locale' has ZERO occurrences in analytics/insights.jsonl (control: 'grep' occurs on 18 lines) and none in prompts/ or METHODOLOGY.md.","sources":["file://analytics/daily-research/2026-08-08.md"],"confidence":"high"}
```

## 3. `0925-C5` — CONCEDED IN FULL, and it compounds with C6.

`open(f).read()` returns characters. 152,710, 58,168 and 136,338 are **characters**, not bytes, and I labelled them bytes. Your measurement of the error's size (1.0058 aggregate, 2.26× → 2.27×) reproduces the direction I would expect and I do not contest it. **The correct form for the record:** A3(i) and (ii) are `os.path.getsize` **bytes**; A3(iii) is **characters**; 2.88× is bytes/bytes and is the figure to carry. That two of my three units were wrong in one section, on the day I was asked to measure this council's discipline about restated figures, is the sharpest thing against me today.

## 4. `0925-C8` — **YOU ARE RIGHT AND I WITHDRAW REMEDY (1). Here is the substitute, and it needs no protection because it threatens nothing.**

Both your arguments land. The second is decisive: `prompts/council_calibration.md` items 1 and 6 already command exactly what a word budget would command, and the measured outcome is 6.2×. **A number attached to a failed exhortation is a failed exhortation with a number.** I have no answer to why it would work, and I am not going to invent one.

**Why the instruction failed, which tells you what would not.** Items 1 and 6 are unmeasured and unattributed: nothing in the record ever states what a seat's return cost, so no seat is ever confronted with its own trend, and no successor can see one. Every change that has actually bitten in this council has the same shape — not a rule but **a measured figure a later session has to reproduce**: the digest sentence copied from `meta.json`, the four carrying-span marks, the same-shaped-positive rule that caught my parser today.

**So my recommendation collapses from two items to one: adopt the reporting line, adopt no budget.** The close-out carries the record's total bytes, the chairman-written share where attributable, `NOT ATTRIBUTABLE` where not, and the prior session's figures beside them. No ceiling, no gate, no exemptions needed. **What protects the disclosures is not adopting the instrument that would cut them** — and if a ceiling is ever adopted over my objection, the answer to your question is that the "errors against myself" section must be excluded from the count by name, because a seat under a ceiling protects its findings and trims its self-indictments, exactly as you say. Revisit the number only if the measured series does not bend after five sessions.

## 5. `0925-C9` — **The count should not appear. The anchor and the pinpoint are the whole of the honest carry, and here is the demonstration.**

What makes the replacement re-derivable is not that it contains fewer words but that **it is falsifiable in one step by a reader who trusts nobody**: it names the predicate and the anchor (`analytics/daily-research/2026-09-12.md:1583`), so a successor reads forward from that pinpoint and either finds a record stating a movement — in which case the sentence is visibly wrong — or does not. The bare ordinal carries neither, so no reading of the record can contradict it; that is exactly why "twelfth" and "sixth" coexisted ten days apart with nobody noticing.

**But you are right that the number smuggles the predicate back in, and I can prove it costs more than it carries.** Tighten the predicate by one clause — from "the answer moved" to "a claim in Sections I–IV was added, amended or withdrawn" — and 2026-09-19's subtractive movement breaks the run, and **thirteen becomes seven.** Two defensible predicates, two numbers, one anchor date each. The number is therefore the least stable part of the sentence and the first thing a successor would inherit without checking.

**Recommendation, tight enough to bind:** carry **the anchor date with its pinpoint and the predicate, and no count.** If a count is written at all it must appear in the same sentence as both, and as stated arithmetic a reader can perform by listing files — *"2026-09-13 to 2026-09-25 inclusive"* — never as a bare ordinal. **A count written without the anchor and the pinpoint in the same sentence is the ordinal again under a new name**, and that is the rule I would have you adopt rather than the sentence.

## 6. `0925-C10` — The 22-of-37 screen, and the reason is that it destroys itself on merge.

**Least reproducible: the 22-of-37 zero-carrier screen.** Not because the method is weak but because **its object ceases to exist when this record is committed.** Every one of those 22 URLs becomes a carrier the moment my return and the artefact land in the tree, so a successor running the identical `grep -rlF` at any commit after today's merge gets 0 new, correctly, and will conclude I was wrong. The screen is reproducible only at `c676373` or earlier. I wrote "at `c676373`" and that was not enough: it needed "and not thereafter". **Any prior-art screen this council runs on its own tree has this property**, and I have not seen it stated anywhere in the record.

**Second: the 70.2 / 29.8 aggregate.** The part boundaries come from a heading vocabulary I wrote by hand, and I verified the spans by eye on **one** of the nine records — the one you checked. The other eight are parser output nobody has read against the file. A successor with a different vocabulary list will get different boundaries on those eight, and will not be wrong to.

**Least at risk, for contrast:** the four period means and the corrected 173,893.17 / 169,803.5. They are `os.path.getsize` over a file list with two named exclusions — no tokenizer, no locale, no vocabulary, no commit sensitivity.

---

**What changes in the ledger after this round:** the struck insight `METHOD-crossing-query-row-count-collapse…` is **withdrawn** and replaced by the weaker form at §1; the locale insight at §2 is **new**; the ICC Article 30 and Rule 0 insights stand unamended. **What I now ask you to rule on:** (a) refuse the separating search, as I recommend; (b) log the two replacement/new insight lines; (c) C8 — the reporting line alone, no budget; (d) C9 — anchor and pinpoint, no count, with the same-sentence rule; (e) record that A3(iii) is in characters and that 2.88× bytes is the headline multiple.

---

**End of the analyst's Part 5, filed verbatim. Everything below this line is mine.**

---

## 6. Chairman's close-out

### 6.1 THE DAY'S FINDING — AND THE RESEARCH QUESTION DID NOT MOVE

**Stated first and plainly: the research question did not move today.** The council searched for it — five
bounded queries from the doctrinal side, which is the half of the intersection this project had not
recently worked — and the answer came back a clean null. No 2026 investor–State decision surfaced in which
an intellectual-property right, a trade secret or a clinical-trial-data entitlement was the asserted
investment and one of the live jurisdictional doctrines was in play. **That is an honest answer and it is
not the same thing as an absence in the world**: it is what one engine could return at title level, on
one day, on five queries, with no document body opened.

**One negative did advance, and it is worth the operator's attention because it is about a brand-new
mechanism.** The 2026 ICC Arbitration Rules created a codified early-determination gate at Article 30.
The council's living memory has recorded since 2026-07-29 that no application of it to an IP-as-investment
or clinical-data case had been identified. **Today's search extends that negative to 2026-09-25, and it
found something slightly stronger than expected: of ten returned titles — eight law firms, a practitioner
blog and the ICC itself — not one reports an application of Article 30 in any case at all**, investment or
otherwise, roughly four months after the rules took effect. This is title-level only, one engine, no body
read, and `iccwbo.org` is not on the relay's allowlist so the limb cannot be tested by the instrument this
council actually has. It does not touch thread 11's closure condition, which needs a primary document.

**The day's sharpest finding is not about the world at all — it is a measurement defect in this council's
own instruments, and the analyst found it after I had misdiagnosed it.** I objected that its word-count
multiple would not reproduce, and I offered the wrong cause: I said `wc` versus Python. **The real cause is
that the same `wc` binary returns two different answers depending on the locale of the shell that invokes
it** — 8,837 words under `LC_ALL=C` and 8,977 under `LC_ALL=C.UTF-8`, GNU coreutils 9.4, one file, and the
delta of 140 is exactly that file's 134 em dashes, 3 en dashes and 3 multiplication signs. **I reproduced
all four figures and the arithmetic of the delta.** An interactive shell in this environment runs with an
empty locale and gets the first number; a Python subprocess inherits `LC_CTYPE=C.UTF-8` and gets the
second. **The exposure is not confined to word counts:** any `grep -c`, any sort, any character-class
measurement this council commits can differ between a seat's interactive shell and its scripted path — and
this project records such figures constantly, as fact, in a permanent record. **The word `locale` appears
nowhere in the insight ledger, nowhere in `prompts/`, and nowhere in `METHODOLOGY.md`.** That is a real
hole in the measurement discipline of a project whose entire claim to reliability is that its numbers are
re-derivable.

### 6.2 RULINGS ON WHAT THE ANALYST ASKED, TAKEN BEFORE THE RULINGS THAT FAVOUR ME

**(1) The artefact: PATH, GRANTED.** `analytics/search-results/2026-09-25.json` is committed, 22,812
bytes, sha256 `414ecdab345ddb498224c5a75ccccc414033d8524f48a7c4f51185ac3179fb3d` — **which I verified
before the copy and again after it**, so the one-author-of-record property rests on a verified hash rather
than on a paste, which is the stronger guarantee. Its reason for not pasting 22.8 KB into a record whose
length was the day's agenda item was correct and I adopt it as the general practice: **a hand-off by path
plus hash is compliance, not a shortcut, provided the chairman verifies the hash on both sides and commits
the file himself.**

**(2) BOOK TODAY'S A1 AS THE FIRST-APPLICATION TRACKER'S SEARCH-LIMB CYCLE. GRANTED.** The tracker
adopted 2026-07-31 has **two limbs on one monthly cadence** — a relay row into the ICSID register and a
search pairing each named mechanism with IP/data keywords — and today's S1, S4 and S5 are three of that
entry's own named formulations. The search limb last fired 2026-09-11 and is reset to **2026-09-25, next
due on or around 2026-10-25**; the relay limb fired 2026-09-24 and is unchanged. **Recording the two limbs
separately is the whole point of the ruling**: without it, next month's session sees one tracker, one
cadence and two differently-aged observations, and cannot tell an executed cycle from an omitted one —
which is the identical defect this council proved yesterday about suspension and lapse, surfacing within
twenty-four hours in the one place nobody was watching.

**(3) THE PINPOINT CORRECTION. GRANTED, and it is mine to carry, not the analyst's.** The cadence is at
**`analytics/optimization-log.md:67` at commit `c676373`**; at `38acb75` it was at `:65`, and at `c676373`
line 65 is the 2026-08-02 UN Digital Library entry, `_Status: proposed._` The file went 123 lines to 125
overnight. The analyst attributes the origin to its own B2 and that is accurate, **but I repeated it in
the agenda without opening the file, which is the failure that matters** — see §3.3. **Standing
instruction from today: a line-number pinpoint into a file this council writes daily is cited with its
commit or it is re-opened before use.**

**(6) THE POPULATION CORRECTION. ADOPTED, and it runs against me.** The figures this council carries are
the **24 daily council records** of September — mean 173,893.17 bytes, median 169,803.5, mean 25,951.4
words, range 123,770–245,262 — not the 26-file set I used, which mixes in a special session and a rulings
memo. My mean was arithmetically exact for the population I named; my range was not, and the two did not
belong together.

**(a) THE SEPARATING SEARCH: REFUSED, as the analyst recommends and for its reason.** One call would
separate the phrase-wording variable from the Ring-1 conjunction. It would buy a statement about one
engine's retrievable corpus at one moment, which would still be hedged in exactly the same terms, and the
part of the finding worth keeping — the method rule — needs no separation. **A search spent firming up an
inference that must be hedged either way is padding**, and the seat that would have got the credit for
firing it is the seat that talked me out of it.

### 6.3 THE RECORD-LENGTH RULING — MY FINDING, CORRECTED TWICE AGAINST ME, AND IT SURVIVES BOTH

**The finding stands and the corrected statistics are worse than the ones I brought.** Rule 0 was adopted
2026-08-07 to shrink this meeting, on the operator's own correction that the daily council is *"the
researcher + the chairman + one optimization suggestion or whatever"*, for the stated reason that *"a
daily check-in that costs a weekly council is a daily check-in that stops being read, and an unread record
verifies nothing."* **The mean length of the daily record has risen in every period since**, and I
recomputed each figure independently:

| period | n | mean bytes |
|---|---|---|
| July 2026 | 31 | **28,195** |
| 2026-08-01 … 08-07 (pre-rule) | 7 | **116,589** |
| 2026-08-08 … 08-31 (post-rule) | 24 | **161,210** |
| September (daily council only) | 24 | **173,893** |

**September is 6.2× the July mean and 2.88× the record filed the day after the rule landed.** The byte
multiple is the one this record carries, because after two of today's objections it is the only figure in
the set that is unit-clean: **`0925-C5` established that the attribution percentages are character counts,
not bytes, and `0925-C6` established that the word multiple crossed two locales in one ratio.** The
correct word multiple, computed consistently under either tokenizer, is 2.891.

**Two corrections against me are on the record at §3.1 and §3.2 and I do not soften either.** My range did
not belong to my population, and I built the comparison on 60,424 bytes — the single smallest record in
August, with 116,873 the very next day. **A ratio against the best available day is advocacy, and I was
the interested party.** The monotone period series replaces it and is stronger.

**Why this reaches the operator rather than staying an internal tidiness point.** I checked the delivery
path rather than assuming it: `scripts/send_daily_update.py` reads the record with `fh.read()` and applies
**no cap, no truncation and no attachment fallback**, and `analytics/daily-research/.sent/` holds **86
unbroken markers through 2026-09-24**. So this is not a hypothesis about readability — **a ~174,000-byte,
~26,000-word document has been landing in the operator's inbox every day, in full, under a rule written to
prevent exactly that.** And nothing looks: no size expression occurs anywhere in `scripts/` or
`.github/`, which the analyst confirmed against a positive control and which I verified independently.

**THE REMEDY, AND I RULE AGAINST MY OWN FIRST PROPOSAL.** I put a per-seat word budget to the analyst as
the likely fix. It withdrew the proposal under challenge and I accept its reasoning, which is better than
mine was: **`prompts/council_calibration.md` items 1 and 6 already command precisely what a budget would
command** — go shorter when uncertain, default shorter than necessary, cut anything a researcher would not
act on — **and the measured outcome of that instruction is a 6.2× rise.** A number attached to a failed
exhortation is a failed exhortation with a number. And a ceiling would fall first on the single most
valuable component of these returns, the seats' disclosures against themselves.

**ADOPTED instead, and it is the whole of the remedy: a reporting-only length line in every close-out** —
total bytes, the chairman-written share where attributable, `NOT ATTRIBUTABLE` where not, and the prior
session's figures beside them. No ceiling, no gate, no exemptions. The reasoning I adopt from the analyst
is that every change that has ever bitten in this council has one shape — **not a rule but a measured
figure a later session has to reproduce**, like the digest sentence copied from `meta.json`. Today's line
is at §6.9. **Revisit a ceiling only if the series has not bent after five sessions**, and if one is ever
adopted, the "errors against myself" sections are excluded from the count by name.

**NOT adopted: a mechanical guard, for now.** The chairman-written share is attributable on 9 of 24
records because the part-heading vocabulary changed on 2026-09-16. **A guard that can measure 37% of its
corpus and returns a clean zero on the rest is `check_marks.py` again**, which is this council's standing
example of a green light that checked nothing. Its precondition is a stable part-heading vocabulary, and
that is a separate and cheap decision I do not take today.

### 6.4 RULINGS ON EVERY OBJECTION — TEN FILED, TEN RULED

- **`0925-C1` (against me) — SUSTAINED.** Range and population mismatched. Correction adopted at §6.2(6).
- **`0925-C2` (against me) — SUSTAINED.** Cherry-picked comparator. Replaced by the period series.
- **`0925-C3` (against me) — SUSTAINED, and the worst of the four.** A pinpoint passed on unopened, in the
  same paragraph as an escalation about unre-derived figures. Standing instruction issued at §6.2(3).
- **`0925-C4` (against me) — SUSTAINED.** I priced a two-limb cadence as one limb; the seat I was directing
  caught it. Booking granted at §6.2(2).
- **`0925-C5` (against the analyst) — SUSTAINED, NON-BLOCKING, conceded in full.** Characters labelled
  bytes. **Ruling: the record states the unit of each figure.** A3(i) and (ii) are `os.path.getsize` bytes;
  A3(iii) is characters; 2.88× is bytes over bytes and is the headline. The error's magnitude is 0.55% and
  changes 2.26× to 2.27×, so no conclusion moves.
- **`0925-C6` (against the analyst) — SUSTAINED, and my own diagnosis of it was WRONG.** I said the cause
  was `wc` versus Python. **It is one binary and two locales**, and the analyst's answer is a better
  finding than my objection. Its figure was a mixed-unit ratio; the correct value is 2.891 under either
  tokenizer consistently applied. **This is the second time today the seat I was checking corrected the
  substance of my check, not merely its conclusion.**
- **`0925-C7` (against the analyst) — BLOCKING, SUSTAINED IN PART, and the part that survives is mine
  only by accident.** The inference from S4's 3-row return to an empty intersection is **STRUCK**: three
  variables moved on that call and no attribution to the corpus is available. **But the confound I
  identified is not the one that does the work.** The analyst tested phrase *count* against this
  repository's own 28-call archive — I reproduced the table exactly, including that the one 9-row
  double-phrase call is disjunctive and rightly excluded — and of three archived conjunctive
  double-phrase calls, two returned a full block at 10 and 30 rows. **So my mechanism is refuted by our
  own data.** What survives is the variable I named second and almost in passing: the Ring-3 phrase
  *wording* changed between S1 and S4, and nothing in the archive can separate that. **The observation
  10/10/9/3/10 with the shapes as measured is carried; the inference is struck; the method rule replaces
  the insight.**
- **`0925-C8` (against the analyst's remedy) — SUSTAINED and the proposal WITHDRAWN by the analyst.**
  Ruled at §6.3.
- **`0925-C9` (against the analyst's replacement sentence) — SUSTAINED, and its answer goes further than
  my objection asked.** I doubted the number; it demonstrated the number is the least stable part of the
  sentence, by tightening the predicate one clause and watching thirteen become seven. **RULING: the bare
  ordinal is RETIRED.** What this council carries is the anchor date with its pinpoint and the predicate.
  **If a count is written at all it appears in the same sentence as both and as arithmetic a reader can
  perform by listing files** — "2026-09-13 to 2026-09-25 inclusive" — never as a bare ordinal. A count
  without the anchor and pinpoint in the same sentence is the ordinal under a new name.
- **`0925-C10` (a question, not an objection) — ANSWERED, and the answer is one this council needed.**
  The least reproducible figure is the 22-of-37 zero-carrier screen, **because committing this record
  destroys its object**: all 22 URLs become carriers the moment the artefact lands, so a successor running
  the identical screen after today's merge gets zero and concludes the analyst was wrong. **Ruling, adopted
  as general: a prior-art screen over this council's own tree is cited "at `<commit>`, and not
  thereafter."** The second-least is the 70.2/29.8 aggregate, whose part boundaries were eye-verified on
  one of nine records — the one I happened to check.

**Two non-objections recorded for completeness.** The analyst's archive test names four prior artefacts as
carrying `result_blocks`; **I checked all five and only two do** — `2026-09-22.json` and `2026-09-24.json`
carry it, `2026-09-20.json` and `2026-09-21.json` do not. And it reports 29 calls in the archive where I
count **28**. Neither figure is load-bearing for anything ruled above, and the second makes its own
omission of the field look worse than it is, not better. **The missing `result_blocks` field in today's
artefact is a real defect** and I accept its refusal to reconstruct it from memory: a re-run would be a
new observation and is not this session's.

### 6.5 ACCOUNTABILITY

**Research analyst — the best return this seat has filed in the sessions I can read, and the one that
most often corrected me rather than itself.** It spent 5 of 5 searches and left the relay budget
untouched rather than manufacture a row; it declined to open a new gap slug on the express ground that
doing so would inflate an escalation counter for a question already on my list; it refused the separating
search I offered it. **It caught four defects in my agenda, and two of them were not the ones I had
invited**: the two-limb cadence and the stale pinpoint. **On reconvening it corrected the substance of my
own objection twice** — it refuted my phrase-count confound against our own archive, and it replaced my
wrong `wc`-versus-Python diagnosis with the locale mechanism, which is today's sharpest finding and is
entirely its own. It disclosed four errors against itself unprompted and three more on reconvening,
including that two of three units in one section were wrong on the day it was measuring this council's
discipline about units. **Against it:** the S4 inference was over-read and needed a blocking strike; the
characters-as-bytes labelling; the mixed-unit word ratio; `result_blocks` omitted from its own artefact in
a schema two prior files establish; and two small miscounts, 29 calls for 28 and four prior artefacts for
two. **Everything load-bearing I tried to break reproduced exactly** — the artefact to its hash, all four
period means, the corrected September statistics, the 09-24 span offsets isolated from 21 headings, eight
separate pinpoints opened one at a time, the 94-record figure, and the full phrase-count table. **No spawn
failed. No seat is recorded as PROCEDURAL FAILURE.**

**Chairman — one finding of mine landed, and the day's two best moments were both the analyst correcting
me.** What I got right: the agenda-setting call that no cadenced row was due, which freed the whole
session and which the analyst confirmed as to due-ness; the decision to hand my own finding to the seat I
was directing rather than measure it myself, which is what produced every correction against me;
re-measuring all six recurring escalations by execution rather than restating them, which corrected one of
them; and catching my own false zero on the optimization log by the rule adopted yesterday. **Against me,
and this is the honest weight of the day: four sustained objections, all four mine, and two of them
concern the very discipline I convened the session to escalate.** I passed on a line number I had not
opened. I built a finding on the most favourable day in a month. I stated a range that belonged to a
different population than my mean. I priced a two-limb cadence as one limb. **And when I did check the
seat, my mechanism was wrong twice — the phrase-count confound is refuted by our own archive, and the
`wc`-versus-Python diagnosis was simply not the cause.** The check was still worth running: the S4
inference needed striking and the strike holds on the ground the analyst supplied. But the ledger for
today reads that the chairman's objections were directionally right and mechanically wrong, and the seat
under examination is what made them true.

**Rule 0, complied with literally for the second consecutive session.** No integrity officer, no analytics
officer, no systems researcher; the digest sentence is mine from `meta.json`; the optimization item is one
paragraph of mine. Ten objections filed and ruled with no convened officer, one blocking. **And today
supplies the clearest evidence yet on escalation 11, pointing the same way as yesterday's:** the two
sharpest corrections of my own checking came from the seat I was checking, which is precisely the function
a dedicated checker performs and precisely what I cannot perform on myself. **I record that as evidence
and do not resolve it. It is Emory's line to write.**

### 6.6 THE ONE OPTIMIZATION ITEM (Rule 0 — one paragraph, written by me, no seat convened)

**Every daily close-out carries a reporting-only length line: the record's total bytes, the
chairman-written share where the part structure permits attribution, `NOT ATTRIBUTABLE` where it does not,
and the prior session's figures beside them — no ceiling, no gate, no exemptions.** The case for it is
measured, not asserted: the daily record's mean has risen in every period since Rule 0 was adopted to
shrink it — 28,195 bytes in July, 116,589 in the week before the rule, 161,210 after it, 173,893 in
September — and **nothing in this repository looks**, since no size expression occurs anywhere in
`scripts/` or `.github/` and the emailer that delivers the record neither truncates it nor reports its
size, while 86 unbroken `.sent` markers show it arriving in the operator's inbox in full every day. **I
proposed a per-seat word budget first and ruled it down under the analyst's challenge**, because
`prompts/council_calibration.md` items 1 and 6 already command what a budget would command and the measured
result of that instruction is a 6.2× rise — a number attached to a failed exhortation is a failed
exhortation with a number, and a ceiling would fall first on the seats' disclosures against themselves,
which are the most valuable thing in these returns. What changes behaviour in this council has one shape
and it is not a rule: **a measured figure a later session has to reproduce**, like the digest sentence
copied from `meta.json`. **Dedup, screened by me over `analytics/optimization-log.md` and
`analytics/insights.jsonl` at HEAD, with the receipts:** `size guard` → **1**, the 2026-09-23 entry
proposing a commit-time guard against `analytics/council-log.md` *shrinking*, which is a different
artefact, the opposite direction, and a gate rather than a report — its own prior-art screen recorded
`size guard` → 0, so this is its second occurrence and not a duplicate of it; `record length` → **0** in
the insight ledger; `word budget` → **0**; `bytes` in the optimization log → **0** proposing any
measurement of a record. **Effort: two lines in a close-out. `_Status: proposed._` Nothing is built and
nothing is applied.**

### 6.7 NEXT STEPS FOR TOMORROW

1. **NO CADENCED OBSERVATION IS DUE BEFORE 2026-10-10.** Thread 10 (Wingtech, monthly) is next, on or
   around 2026-10-10; thread 11's **relay limb** on or around 2026-10-13; thread 13 (re-adopted monthly
   after yesterday's suspension) **2026-10-18**; thread 11's **search limb**, booked today, on or around
   **2026-10-25**. **A session that fires a row before its date is spending the council's one substantive
   row on a surface it has measured as moving in weeks**, and tomorrow is not any of those dates. Say so
   in the record rather than leaving an empty `analytics/fetch-results/` to be read as a null.
2. **THREAD 11'S CADENCE HAS TWO LIMBS ON ONE MONTHLY CLOCK AND THEY ARE NOW AT DIFFERENT AGES.** Relay
   limb last fired 2026-09-24; search limb 2026-09-25. **Do not price them as one item.** The pinpoint is
   `analytics/optimization-log.md:67` at `c676373` — cite it with a commit or re-open the file, because it
   moved from `:65` in twenty-four hours.
3. **THE BARE QUIET-SESSION ORDINAL IS RETIRED. Do not write "the Nth consecutive session."** Carry the
   anchor with its pinpoint and the predicate: quiet at the three-ring intersection on every daily record
   since **`analytics/daily-research/2026-09-12.md:1583`**, the last record stating the answer moved. If a
   count is written it appears in that same sentence and as arithmetic a reader can perform by listing
   files, never as a bare ordinal — because tightening the predicate by one clause turns thirteen into
   seven.
4. **STATE THE LOCALE OR USE BYTES.** `wc -w` on one file returns 8,837 under `LC_ALL=C` and 8,977 under
   `LC_ALL=C.UTF-8`, same binary; the delta is exactly that file's em dashes, en dashes and multiplication
   signs. **Any `grep -c`, sort or character-class figure this council commits is exposed**, and an
   interactive shell here differs from a Python subprocess. Prefer a byte count, which does not vary; if a
   character-sensitive figure is committed, name the locale in the same sentence.
5. **NAME THE UNIT OF EVERY FIGURE.** Today's record carries both bytes and characters because
   `open(f).read()` offsets were labelled bytes. `os.path.getsize` is bytes; `len(open(f).read())` is
   characters; on these records the gap is 0.5–0.7%, which is small enough to survive undetected and large
   enough to be wrong.
6. **A PRIOR-ART SCREEN OVER THIS COUNCIL'S OWN TREE IS CITED "AT `<commit>`, AND NOT THEREAFTER."**
   Committing the record destroys the screen's object: today's 22 zero-carrier URLs all become carriers on
   merge, so a successor re-running it gets zero and concludes the analyst was wrong.
7. **THE `thread_id` CONVENTION ADOPTED 2026-09-24 HAS NOT BEEN APPLIED ONCE AND ITS FIRST USE IS THREE
   WEEKS AWAY**, because no request file was written today. Apply it on the next row; an adopted convention
   whose first use is deferred is one that can quietly never happen.
8. **THE MISSING `result_blocks` FIELD.** Today's search artefact omits it on all five calls, so today's
   row counts cannot be normalised per block. Two prior artefacts carry it (`2026-09-22`, `2026-09-24`) and
   two do not (`2026-09-20`, `2026-09-21`) — so it is a convention in formation, not an established schema.
   **Do not reconstruct today's values from memory.** Record it going forward.
9. **THE S4 INFERENCE IS STRUCK AND MUST NOT BE REVIVED.** The observation is carried — five calls
   returning 10, 10, 9, 3 and 10 rows, with the 3-row call the only one crossing the rings, the only one
   with two conjoined exact phrases, and the only one using `"manifestly without legal merit"` rather than
   `"manifest lack of legal merit"`. **Three variables, one observation, no attribution to the corpus.**
   The phrase-count limb is refuted by this council's own 28-call archive. **Hold the exact-phrase count
   constant across a session's calls and record the shape next to each row count.**
10. **CARRIED UNCHANGED AND UNRELEASED; NOTHING TODAY RELEASES ANY OF THEM.** The EU Ring-2 object at zero
    search budget, UNKNOWN not foreclosed; the struck Achmea ground; the Grosse Ruse-Khan slug closed as to
    the body; the Ecuador slug at zero budget with its paywalled-body synthesis quarantined; `dailyjus.com`
    and `unis.unvienna.org` at zero budget; the Einarsson family retired; A/81/17 at zero budget; **all
    Spentech synthesis passes QUARANTINED — a fourth arrived unbidden in an engine summary today and was
    not transcribed**; the monitored-author check at zero search budget; the Vadi citation never cited
    without retrieval; no thread addressed by array index; the ARB/24/16 docket off the agenda; the
    `www.italaw.com` prohibition; no re-fire at `investmentpolicy.unctad.org`; no bare `unctad.org` row;
    the IISD path-family hypothesis refuted; `www.bing.com` rejected on the merits. **And the retired NAFTA
    locator hunt stays retired: no seat may describe NAFTA Article 1110's internal structure — not its
    chapeau, not its subparagraph count. This project has never read it.** New today and in the same class:
    **`Oro Negro` and `Barst` are engine-supplied names with zero carriers in this tree and nothing about
    either may be asserted**; the Ontario Superior Court decision and the June 2026 Ecuador claim named in
    engine prose are quarantined and must not become evidence by repetition.
11. **WRITE NO `daily-council` STUB INTO `state/council_log.json`.** That file's newest entry is dated
    2026-09-22 and today writes none, which makes three consecutive sessions — **not the four that
    yesterday's "three" would imply**, because at yesterday's close the run was two. The file is outside
    merge scope and a stub is what marks the day's rich section for destruction.

### 6.8 ESCALATIONS TO EMORY

**Every item below was re-measured by me today by execution or direct observation, not restated from
yesterday's record. Where I could not re-measure something, I say so in the item.**

**0. UNCHANGED AND STILL OUTRANKS THE REST: do not set `VALIDATION_STATUS_ONLY=0` until
`src/council_log.py::_handwritten_sections` is fixed and the 34 destroyed ledger sections are recovered.**
Recover first, then repair, then flip. Nothing was built today and `src/` was not touched. **One thing
cheapens the ask further, and I corrected the figure while checking it:** sittings have stopped writing
`daily-council` stubs into `state/council_log.json`, which is what marks a section destroyable. The file's
newest entry is dated **2026-09-22**, so today makes **three** consecutive sessions writing none —
yesterday's record said three, which the file supports as two at that point.

**1. UNCHANGED AND STILL THE ONLY LEVER ON THE NAFTA GAP: add `www.sice.oas.org` to `ALLOWED_HOSTS`**, one
line in a set literal at `scripts/fetch_relay.py:49-69`. No batch this council can fire at any budget
closes that gap, because the nineteen permitted hosts are arbitral institutions and document repositories
while the bodies publishing Chapter Eleven's article text as web pages are trade ministries and
secretariats. **Both refinements stand:** the host may be necessary but not sufficient, since `validate()`
demands `https` and the only spelling this repository has ever recorded is `http://`, so the batch that
follows must pre-register `scheme_not_https` and TLS-failure-at-the-runner as named outcome classes; and
the locator is `STATE_OF_THE_ANSWER.md:1001`, not `:977`.

**2. UNCHANGED, AND STILL NEEDS TWO CHANGES RATHER THAN ONE.** The two PCA-published legal-authority
exhibits fail `validate()` as `host_not_allowed` (`files.pca-cpa.org`) **and both are PDFs, so even with
the host added the reducer would return an empty excerpt and the rows would come back `ok` and useless.**
Host **and** reducer. The question is unchanged and costs two minutes of your time: **does NAFTA Article
1110 contain an IP-specific internal limitation at a paragraph (7)?** Every caveat attaches; the titles are
an engine's and nobody here has read a byte. **The search-engine prose that once answered this unbidden
remains quarantined and must not become evidence by repetition.**

**3. THE 28 JULY 2025 AWARD — FIFTEENTH DAY, AND I DID NOT RE-CHECK IT.** ICSID's case-detail page was
recorded as listing `Award of the Tribunal (July 28, 2025) English (Original)` under *Published Decisions*
for ARB/24/16. **This session has no fetch and did not re-check that page**; what I carry is the record's
prior statement, not an observation of mine, and I flag it as inherited. No row fired anywhere today. The
honest expectation is unchanged: reading it will most likely close the thread negatively.

**4. `scripts/check_marks.py` CHECKED NOTHING FOR THE TENTH CONSECUTIVE SESSION AND EXITED 0.** I ran it
myself today against this record rather than restating yesterday. Verbatim: *"no bibliography section found
— nothing checked"*, then `mark parity 0 entries`, `nonzero-referent 0 screen records (nothing to check)`,
`degenerate uniformity 0 four-mark entries (nothing to check)`, and **exit 0** — a clean green light over
an empty check. The fix is the 2026-09-15 optimization entry, **still `_Status:`-less and unadopted after
ten days.** It is also the standing example behind today's ruling against commissioning a new mechanical
guard.

**5. `main` IS STILL RED AND THE DRIFT IS STILL EXACTLY TWO DIGESTS — re-measured from the run's own log
and independently corroborated.** `pipeline-guards` run **242**, head **`465ce4e`** (the #217 merge), job
`suite`: **2 failed, 821 passed, 5 xfailed**, character-identical to runs 239 and 240. Both failures are in
`tests/test_site_claims.py`: `runs 18 vs 16`, `screened 520 vs 492`, and `13 vs 11` per-source digests. The
second job, `guards`, **passed in full** — all fourteen steps. **The corroboration is new today and is not
a reading of the log:** `digests/` holds **18** directories, which is exactly the actual value the test
scores against its pinned 16, and the two unpinned digests are 2026-09-14 (7) and 2026-09-21 (21), where
7 + 21 = 28 = 520 − 492. **Single cause, confirmed two ways. The fix is five numbers in `tests/`**, which is
outside this session's merge scope.

**6. A `[skip ci]` ON THE WEEKLY DIGEST COMMIT IS STILL SWITCHING OFF `claims-consistency.yml` FOR
`digests/**` — and I verified both halves today rather than restating either.** The workflow's `push`
trigger lists `digests/**` explicitly (`.github/workflows/claims-consistency.yml:46`), and the most recent
weekly digest commit, `1eb4235 chore: weekly digest + state update [skip ci]`, carries the suppressing
string in its message. One string, still scoped to `digests/**` only.

**7. `specs/iisd_itn.yaml:24` STILL CARRIES A FALSIFIED REASON.** The spec reads *"Use /itn/feed/ not
/itn/en/feed/ (the /en/ variant 403s)"*, while the 2026-09-21 digest's own `source_health` records a **403
at the variant the spec recommends**. One string. `specs/` is outside merge scope.

**8. CORRECTED DOWNWARD BY ME, AND IT IS NOW SMALLER THAN YESTERDAY REPORTED IT.** Yesterday's escalation 8
read *"8 of 60 optimization-log entries carry no status marker at all."* **Measured today by block parse:
61 dated entries, of which 4 carry no `_Status:` marker — 2026-09-16, 2026-08-31, 2026-08-04 and
2026-09-15 — and all four state their status in their own prose ("PROPOSED ONLY", in terms).** A fifth,
2026-07-29, carries `_Status: **adopted 2026-07-31**`, which a naive capture cannot read through the
asterisks. **So the machine-readable marker is missing on four entries and the tracking is missing on
none.** My own first parse of that file returned a false 9 because it read line by line and five entries
span more than one line; **I caught that only by applying yesterday's own same-shaped-positive rule**, which
is recorded against me at §3.8(a). This is a two-minute tidy-up, not a finding.

**9. HYGIENE, NOT A FINDING — three pull requests still open, verified by me today through the API:**
**#184** `fix/repoint-r21-citations` (opened 2026-09-13, **12 days**), **#150** `chore/reanchor-2026-09-08`
(**17 days**), **#110** `vault/archivist-2026-08-28` (**28 days**). All outside this session's merge scope.

**10. THE MONTHLY HUMAN SPOT-AUDIT IS 69 DAYS PAST ITS LAST RUN, AND I VERIFIED BOTH HALVES TODAY RATHER
THAN CARRYING THE CLAIM.** `HUMAN_REVIEW.md` sets the checkpoint at **monthly**, default N = 5 claims
covering each ring. The file records exactly two cycle headings — a **2026-06-29 Cycle 1 DRAFT** and a
**2026-07-18 Cycle 1 COMPLETED** with your in-session sign-off — **and no Cycle 2 of any status.** So the
audit has run once, 69 days ago, against a cadence of 30. **This is yours and it is the one item on this
list that goes to the reliability of what the council publishes rather than to its plumbing:** the
methodology's stance that leads are "to be verified before relied upon" is auditable only through this
checkpoint, and the verification debt has been accruing unmeasured for over two months.

**11. UNCHANGED AND STILL A DECISION ONLY YOU CAN TAKE: AMEND RULE 0 OR KEEP IT.** Today is the **second**
consecutive session complying with it literally — no integrity officer convened, the challenge discharged
by me. **For compliance:** ten objections filed and ruled with no officer, one blocking, and the blocking
one correctly struck an inference that would have entered the insight ledger as a finding about the world.
**Against it, and today's evidence is sharper than yesterday's:** twice in one session **the seat I was
examining corrected the mechanism of my own check** — it refuted my phrase-count confound against this
council's own archive, and it replaced my wrong `wc`-versus-Python diagnosis with the locale mechanism that
turned out to be today's best finding. **A dedicated checker is exactly what performs that function, and I
cannot perform it on myself.** Two sessions now point both ways and neither settles it. **One line either
way settles it, and it should have reached you on day two rather than day twenty-two.**

**12. A DEFECT OF THIS COUNCIL'S OWN, ON `main`, PERMANENT, AND CONFIRMED BY ME INDEPENDENTLY TODAY.** The
#216 merge commit (`dab598f`) carries stray tool-call fragments — `</commit_message>` and `</invoke>` — as
literal trailing text in its message on `main`. Yesterday's addendum reported it; **I read the commit
object myself today rather than take that on trust, and it is there.** A pushed merge-commit message cannot
be rewritten without rewriting `main`'s history, which this council will not do, so it stands as a
permanent blemish in the history and is recorded rather than repaired. **I state it here because the
mitigation is behavioural and it is mine: today's merge message is composed and checked before the merge
call, not assembled inside one.**

### 6.9 THE LENGTH REPORT — ADOPTED TODAY, AND THIS IS ITS FIRST ENTRY

Per §6.6, in the form ruled there. Figures are `os.path.getsize` **bytes**, so they are unit-clean and
locale-independent.

**Record `analytics/daily-research/2026-09-25.md` — 114,951 bytes.** Chairman-written
**68,718** (59.8%); member returns filed verbatim **46,233** (40.2%) across Parts 2 and 5.
ATTRIBUTABLE in full, by the monotonic top-level part boundaries 1–7.

**Beside it, the figures it exists to make visible.** The September mean for the 24 daily council records
is **173,893** bytes and the smallest of them is **123,770** (2026-09-09); yesterday's record,
2026-09-24, is **153,549** bytes. **So today's record is 66% of the September mean and is the
shortest daily council record since 2026-08-28 (111,466 bytes), twenty-seven records ago** — which is not offered as an achievement, because one short
record is a data point and the series is what the line exists to expose. The chairman-written share is
60% today against a 58,168-**character** mean over the nine attributable September records,
and the comparison across units is imperfect for the reason given at Part 7 item 5.

**Method, so a successor reproduces rather than trusts it.** Byte counts are `os.path.getsize` and the part
spans are UTF-8 byte lengths of the slices between the monotonic `## N.` boundaries — **not** character
offsets, which is the defect objection `0925-C5` found in the analyst's own attribution. The total stated
here is the file's size at the commit that writes this line.

### 6.10 WHERE THIS LEAVES THE RESEARCH QUESTION, IN ONE LINE FOR A READER WITH NO CONTEXT

**The question — whether investor–State tribunals will treat intellectual property, including trade secrets
and clinical-trial data, as a covered investment when a regulatory or judicial measure is challenged and a
live jurisdictional doctrine is in play — did not move, and it has not moved on any daily record since
2026-09-12 (`analytics/daily-research/2026-09-12.md:1583`), the last one stating that it did; today it was
searched from the doctrinal side rather than the subject-matter side, which is the half of the intersection
the council had not recently worked, and five bounded queries returned an honest null at title level with no
document body opened; the one negative that genuinely advanced is that the brand-new codified
early-determination gate in the 2026 ICC Rules, Article 30, is not reported as having been applied in any
case at all — not merely in an IP case — some four months after it took effect, on ten titles from eight law
firms, a practitioner blog and the ICC itself; no instrument row fired today because no standing observation
was due before 10 October, and the record says so expressly so that an empty results directory is read as
this council's decision and not as a null about the world; and the day's two sharpest findings were about
the council's own instruments rather than its subject, because a measurement this project had published as a
row-count collapse turned out to confound three variables at once and was struck, and the word counts this
project routinely commits as fact turn out to differ by 140 on a single file depending on the locale of the
shell that measured them — which is to say that today the council learned less about investment law than
about how easily it can mismeasure itself.**

---

## 7. Procedural caveats

1. **The convening instruction and the protocol conflicted, and the protocol won.** My instruction named
   four seats to spawn — `research-analyst`, `integrity-officer`, `analytics-officer`,
   `systems-researcher`. **`prompts/daily_council_protocol.md` Rule 0 directs two, and its own Precedence
   section says it wins and that the conflict is worth a line in the caveats. This is that line.** One seat
   was convened. The digest sentence at §1.4 is mine, copied from `meta.json`; the optimization item at
   §6.6 is mine, one paragraph, no seat convened for it. **My instruction also asked for a Part 1 agenda
   with bounded budgets and a spend checkpoint section; Rule 0 bans a "Budgets" section and a "Priority
   focus" section by name**, so the budgets appear as prose in §1.5 where they belong to the direction I
   gave, and the spend checkpoint is one paragraph at Part 4 rather than a section of its own.
2. **The integrity challenge at Part 3 is my own and is not a performed member's voice.** Under Rule 0 the
   integrity officer is not a member of the daily meeting, so there is no seat to perform. **Whether that
   is the right rule is escalation 11 and is Emory's to settle, not mine** — and this session supplies
   evidence on both sides of it, which §6.5 states rather than resolves.
3. **Rule 2's dialogue requirement was met by reconvening the real seat, not by writing its lines.** I put
   six numbered objections to the analyst in writing and filed its answers verbatim as Part 5. **No
   member's contribution in this record was written by me**, and the working tree was clean when the seat
   handed back, which I verified with `git status --porcelain` before filing.
4. **Model identity.** I ran on the configured `claude-opus-5` per the operator's directive of 2026-08-03;
   the analyst was spawned against its committed definition in `.claude/agents/`. **The serving model for
   any turn can differ from the configured identifier and I cannot verify it from inside the session**, so
   I record the configuration rather than assert the runtime. No discrepancy was reported to me.
5. **THIS RECORD CARRIES TWO UNITS AND SAYS SO EVERY TIME.** The period means and every figure in §6.3 are
   `os.path.getsize` **bytes**. The attribution percentages and absolute figures in the analyst's A3(iii)
   — 70.2%, 29.8%, 58,168, 136,338 and the 152,710 it calls the size of 2026-09-24 — are **characters**,
   because the parse runs on `open(f).read()` offsets. On these files the two differ by 0.5–0.7%. This is
   objection `0925-C5`, conceded in full, and the reason the headline multiple this record carries is the
   byte multiple.
6. **Two figures in this record are inherited rather than derived, and both are flagged where they
   appear.** The status of the 28 July 2025 award is the record's prior statement and not an observation of
   this session (§6.8(3)). And the analyst's 22-of-37 zero-carrier screen is reproducible only at
   `c676373` **and not thereafter**, because committing this record makes all 22 URLs carriers (§6.4,
   `0925-C10`).
7. **Three figures of the analyst's I did not reproduce, named so a successor knows which to distrust
   first.** The 22-of-37 screen, for the reason above. The 70.2/29.8 aggregate across all nine records — I
   verified the part-boundary offsets on **one** record, the 2026-09-24 one, and the other eight are
   parser output against a hand-written heading vocabulary that nobody has read back against the files.
   And the eight-of-42 allowlisted-host row count in the artefact's header. **Two small figures of its own
   I did check and they are off by one and by two respectively:** it reports 29 archive calls where I count
   28, and names four prior artefacts as carrying `result_blocks` where only two do.
8. **Nothing was built and nothing pending the operator's approval was applied.** `src/`, `scripts/`,
   `specs/`, `tests/`, `prompts/`, `.github/` and `docs/` are untouched by this session. The
   `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed
   swap, the Kluwer source, the category signal, the `build_graph` fix and every ledger amendment remain
   **pending, not granted.** The length report adopted at §6.6 is a close-out convention, not code, and it
   is `_Status: proposed._` in the log.
9. **The day's diff touches only record paths** — `analytics/`, `state/research_log.json`,
   `STATE_OF_THE_ANSWER.md` — which is the scope within which this session is authorised to merge its own
   pull request. **I verify that against `git diff --name-only origin/main...HEAD` immediately before
   merging rather than asserting it here**, and if any path falls outside it the pull request is left open
   and said to be so.

10. **THE PROTOCOL'S CLOSE-OUT STEP AND THIS SESSION'S MERGE SCOPE CONFLICT ON FOUR FILE PATHS, AND I
    RESOLVED IT IN FAVOUR OF LANDING THE RECORD.** `prompts/daily_council_protocol.md` directs the
    close-out to run `scripts/reanchor.py` and commit five tracked notes as the last commit. **Four of
    those five — `agents/Agent Registry.md`, `agents/Claim Map.md`, `agents/Project Change Log.md` and
    `agents/Workflow Threads.md` — fall outside the path set within which this session is authorised to
    merge its own pull request**, which is `analytics/`, `state/research_log.json` and
    `STATE_OF_THE_ANSWER.md` and nothing else. **I ran the script, kept its edit to
    `STATE_OF_THE_ANSWER.md`, and reverted the four `agents/` notes to HEAD**, because a pull request
    carrying out-of-scope paths is one I am directed not to merge, and an unlanded record is the failure
    this session exists to prevent — two prior weeks of council work were lost exactly that way.
    **The consequence, stated rather than hidden: `scripts/check_currency.py` fails 5 of its 9 claims at
    this head**, four of them the `agents/` anchors I deliberately did not move and one a pre-existing
    failure citing `ae42639`, which is not a commit in this repository at all and which no re-anchor of
    mine would fix. `.github/workflows/reanchor.yml` is the machinery for those four notes and its own
    header states the honest limit — it commits to the PR head, but a session that merges without waiting
    for checks merges before that commit lands. **This is escalation 13 and it is a two-line decision only
    Emory can take:** either widen the daily session's merge scope to include the five tracked notes, or
    make the `reanchor` and `currency` checks required on `main` together — which that workflow's header
    already recommends, and warns must be done together, because GitHub reports a skipped job as success
    and `currency` required alone would be fail-open on precisely this path.
