# ISDS Daily Council Meeting — 2026-08-10

**Chairman presiding** (Claude Opus 5, per the operator's model directive of 2026-08-03).
**Convened:** research analyst (Claude Opus 5). **Integrity challenge:** heard inside the council
discussion at Part V, per Rule 0 of `prompts/daily_council_protocol.md`.

---

## Part I — Opening

**Return-path protocol, stated first because two weeks of sessions were once lost by ignoring it.**
Every member's return enters this file verbatim, the file is committed incrementally to
`council/2026-08-10`, and the branch is opened as a pull request to `main` and merged by me before
this session ends. A record that does not reach `main` is not merely unread — the next session
builds its agenda by reading `main`, so an unlanded record silently corrupts the council's own
memory. The failure mode is not hypothetical: the 2026-08-03 record stated that no sessions existed
for 08-01 and 08-02, and both had run.

**The instruments.** Two probes at session open, both on neutral controls rather than on any
resource the research cares about. `curl https://example.com` returned `CONNECT tunnel failed,
response 403` from the local proxy; the fetch service returned `EGRESS_BLOCKED` on the same neutral
host. The proxy's own status endpoint logged both denials as `connect_rejected` at 11:24:45Z. This
is the **eighth consecutive fetch-dead day**, both statements are about our own plumbing, and
neither is a statement about any website. **No state change to report at the fetch layer.**

Retrieval nonetheless happened, through the committed relay (`.github/workflows/fetch-relay.yml`):
this session commits a request file, the push fires a GitHub runner that does have egress, and the
runner commits back a *reduction* — status, byte length, sha256, a short excerpt, whether a sought
string was found — never the document body, because this repository is public and a commit is
publication. The batch was fired **before any seat was convened**.

**Today's batch, and it failed its own gate.** Four rows, the smallest batch this council has
fired, pre-registered at `f7675d3` and pushed at 11:27:45Z; run 31383611819 was created at
11:27:49Z, four seconds later; the reduction landed at `26dc05b`, `fetched_at`
2026-08-10T11:28:20.963607Z. It is yesterday's next step 1 executed rather than reinterpreted — the
analyst's proposal 6 as amended by ruling `0809-O4`, which required the pre-registration to name
the commit of `scripts/fetch_relay.py` under which its rules were written, because `find_matched` is
a function of the code as well as the bytes. It does: `7fbbabf`, unchanged since 2026-08-03.

The relay's own control passed (`example.com`, `ok`/200, `control_ok: true`, `void: false`). **Both
bracket rows did not.** Rows 1 and 4, the two firings of the UNCTAD China–Switzerland BIT URL that
open and close the window, both returned `refused` / **403**. Rows 2 and 3 returned `ok`/200.

My pre-registration, written before any result existed, says this in terms:

> **(P0)** Rows 1 AND 4 must both return outcome `ok`, HTTP 200, `find_matched` true, and the
> relay's own control row must return `control_ok` true. If either bracket row or the control fails,
> **THE BATCH IS UNINFORMATIVE ENTIRE**, no branch below is entered, and nothing is discharged
> either way. This is the whole function of bracketing and it is not to be softened once the numbers
> are visible.

**So the batch is void, and the digest sentinel and the robots stability check are void with it**,
notwithstanding that both returned cleanly. I am holding myself to the rule I wrote four minutes
before the data existed, and I am recording here, at the top, what it costs: the two rows that
worked bought nothing today.

There is an obvious objection to my own rule and I am putting it on the record before the analyst
returns, so that it cannot be mistaken for something discovered afterwards to rescue the day. The
bracket rows sit on `investmentpolicy.unctad.org`; rows 2 and 3 sit on `uncitral.un.org` and
`digitallibrary.un.org`. Runner-wide health is already controlled by the relay's own `example.com`
row, which passed. **A cross-host bracket used as a batch-wide gate may simply be a design error** —
the bracket's failure may be informative about UNCTAD and about nothing else. I am not applying that
reading today. A pre-registration reinterpreted after its results are visible is worthless as a
pre-registration, and the whole value of the last week's method work is that the council does not do
this. The question of what the gate *should* be goes to the analyst as the day's central question,
to be answered for tomorrow and not applied backwards to today.

**Budgets, set before the analyst was briefed:** zero fetches, at most one web search, unbounded
repository reading. A spend checkpoint falls after the vetting round at Part V; if the day has
bought nothing by then it ends there rather than buying something to show.

**Two things I hand the council that I found myself, each labelled as the standing rule requires —
a chairman's brief must carry the screen that establishes what it asserts, or say plainly that it
has not been screened.** I broke that rule twice in one document yesterday and it is carried against
me in the accountability record.

*First, screened.* Deriving the `uncitral.un.org/en/commission` series at session open I got 21
result blobs, 19 `ok` rows, 10 batch instants. Yesterday's postscript states 24 / 23 / 12. Neither
derivation is defective: this session's clone holds **only** `origin/main` and my own branch, where
yesterday's session also held its council branch. After an explicit
`git fetch origin '+refs/heads/*:refs/remotes/origin/*'` — 70 remote refs — my derivation reproduces
**24 / 23 / 12 exactly**, at the one byte length 832338 and the one digest `ca87a113c024605c…`
throughout. What I take from that is a defect in the council's own counting convention rather than in
any count: **the scope label "local-ref scope", used in every prior record, does not name a fixed
scope.** It names whatever refs the writing session happened to hold, which the next session cannot
reproduce and which a fresh cloud clone will always under-count — so a later reader re-deriving one
of our figures gets a *lower* number and has every reason to think the record inflated it. It is
handed to the analyst to verify or break, and if it holds, the replacement label and the procedure
that makes it reproducible are his to propose.

*Second, NOT screened, and labelled so.* `investmentpolicy.unctad.org` refused us twice today with a
403. My impression is that this host has returned `ok`/200 on every prior relay fetch of that URL,
which would make today a state change at the access layer. **I have not run that enumeration.** It
goes to the analyst as a hypothesis to establish or refute, and until he does, this record asserts
nothing about it. Whatever it turns out to be, it is a statement about our access at an instant and
never about what UNCTAD holds — the standing tool-status-as-source-state rule, which a double 403 is
exactly the shape of temptation for.

**Where the research question stands going in.** The theme is investor–State arbitration where
intellectual property — including trade secrets and clinical-trial data — is asserted as a covered
investment, a regulatory or judicial measure is the challenged conduct, and a jurisdictional or
admissibility doctrine is genuinely in play. The digest of record is unchanged. We are in a quiet
stretch and the honest posture is to say so. Today's work is instrument and method; if it produces
no movement on the three rings, this record will say that plainly rather than dress instrument work
as research.

---

## Part II — The researcher's return

Entered **verbatim**, as every member's contribution is. I directed this work and set its budget —
zero fetches (the day's relay batch was fired and returned before any seat was convened), at most
one web search, unbounded repository reading. He was asked to test my central question rather than
agree with it, and he was handed one screened claim of mine and one unscreened one, each labelled.
He broke the screened one.

---

### Daily council, 2026-08-10 · research analyst

---

### 0. Derivation pin (read this before any number below)

Every count in this memo was derived by `git rev-list <rev>` → `git ls-tree -r <commit> -- analytics/fetch-results/`, blobs deduplicated by object SHA, each blob parsed as JSON. Revs: `--all` = **73 refs** (71 `refs/remotes/origin/*`, 2 `refs/heads/*`) plus HEAD at `26dc05b9c0a60ad68ad8dffd4cc645ed6fbab92d`; `origin/main` = `2606639d8155a3858db6c1c1b2790bd858af894a`. **The clone is shallow**: `git rev-parse --is-shallow-repository` returns `true`, `.git/shallow` holds 10 grafts (earliest `6f1498b0` 2026-07-20, latest `5bc7bd37` 2026-08-04T11:19:04Z). Every history-walk count below is therefore a **lower bound** and no "never / first / every prior" claim in this record escapes that bound. Nothing in the record has previously said so.

Your "70 remote refs" reconciles: 69 heads stored by the `+refs/heads/*` fetch at 11:26:47Z, plus `origin/main` (updated separately at 11:24:38Z) = 70 at your instant; `origin/council/2026-08-10` was created by your own push at 11:27:47Z, giving the 71 I hold.

---

### 1. Your item 4 — I reproduce both your numbers, and the mechanism you gave for them is wrong

**Your conclusion survives. Your diagnosis does not.** Three findings, in order of how much they change tomorrow.

**(a) The 70-branch fetch delivered zero result blobs.** Object-level, not ref-level: of the 24 pre-today result blobs reachable from `origin/main`, **18 sit in the 2026-08-07T11:20:45Z clone pack** (`pack-bf5a5042…`) and **6 in the 11:24:20Z `fetch origin main` pack** (`pack-69a3cddd…`). **None** is in the 11:26:47Z all-branches pack (`pack-675acb32…`); today's own blob is loose. So `24 / 23 / 12` was fully derivable from `origin/main` at 11:24:38Z, before the fetch you say it required. Separately, `.git/logs/refs/remotes/origin/*` stamps that fetch at **11:26:47Z**, 67 seconds *after* the 11:25:40Z instant your pre-registration says it preceded. *Alternative this does not exclude:* object presence is not ref reachability — a ref-scoped command could still have seen less. That is exactly what happened, per (b).

**(b) `21 / 19 / 10` is a command artifact, not a scope artifact. I reproduced it exactly.**

| enumeration | blobs | ok rows | instants |
|---|---|---|---|
| `git rev-list origin/main` + `ls-tree` | 24 | 23 | 12 |
| `git log origin/main --format=%H -- analytics/fetch-results/` | **21** | **19** | **10** |
| `git log --all -- analytics/fetch-results/` | 25 | 24 | 13 (= your 24/23/12 + today) |

The difference is **git's path-limited history simplification**, which prunes commits TREESAME to a parent across a merge. At single-ref scope it hides exactly three blobs, and they are the three **earliest** reductions in the archive: `a8a7a73f` (`2026-08-03-control.json`, 18:23:13Z), `45ac1e3a` and `a2910dd6` (`2026-08-03-targeted.json`, 18:28:14Z and 18:31:21Z). A method that silently deletes the oldest evidence is precisely the wrong method for a "first ever" or "every prior" claim — and both of yesterday's such claims used enumerations of this family.

**(c) The one adjacent count I could break this way, I could not.** The officer's re-fire population is robust: commits touching `analytics/fetch-requests/` are **15** by exhaustive `rev-list --all` + name-only diff and **15** by `git log --all -- <path>`. (At `origin/main` scope the same `log` gives 11 — the hazard is real, it just did not bite there.)

**So: "local-ref scope" is worse than unreproducible; it is under-determined in two independent dimensions at once.** Replace it with a **DERIVATION PIN** carrying three mandatory fields: **COMMAND** (verbatim), **REVS** (resolved 40-char SHAs, never ref names — names move), **DEPTH** (shallow yes/no + graft list). Reproduction procedure: check out those SHAs, run that command. A later reader with a full clone may get *more*; more is the correct direction and the pin says so.

---

### 2. Your item 3 — the UNCTAD enumeration you asked for, and what today's 403 is not

Derivation pin as §0, `--all` scope, host `investmentpolicy.unctad.org`: **38 rows** across five distinct URLs — 28 `ok`/200, 8 `refused`/`"500"`, 2 `refused`/`"403"`.

The bracket URL (`…/978/china---switzerland-bit-2009-`): **25 rows, 23 `ok`/200 at 13 distinct batch instants across 13 distinct runner sessions**, span 2026-08-03T18:25:23.447878Z → 2026-08-09T12:10:47.756173Z (5 d 17:45:24.308295), plus today's two 403s. **Your impression is confirmed with its enumeration: before today, that URL had never returned a non-`ok` outcome — 23 for 23.** The find string `"China - Switzerland BIT"` had returned `find_matched: true` on all 6 prior rows carrying it.

**The 500/403 distinction matters, and it cuts your way.** All 8 prior refusals are `"500"` and all 8 are on `/999999/` cells — a bad id. Today is the first refusal on a URL that has ever answered, and the first 403 on this host. Corpus-wide the only other 403s are two `www.italaw.com` rows (2026-08-03).

**What the double 403 is evidence of, precisely.** `src/sources/base.py` reaches `_record_outcome(url, "refused", str(resp.status_code))` only after `if resp.status_code != 200:` — so the origin **answered** 403. It is not a robots refusal (`robots_disallowed`), not a transport failure (`no_contact`). Two requests, one batch instant, one runner, our UA.

**What it is not evidence of, and the alternatives it does not exclude.** (i) Not evidence about the treaty page or the BIT. (ii) Not evidence of a policy toward this project: `final_url` is `null` on refused rows, so a 403 terminating a redirect chain is indistinguishable from one at the requested host. (iii) Not distinguishable from an edge/WAF rule keyed to the runner's IP range or UA class — the reduction records no response headers. (iv) **And not excluded: our own known client defect as the cause.** `robots_allowed` fetches `robots.txt` with `requests.get` directly, outside `_respect_rate_limit`, and `polite_get` calls it *before* the interval (0809 finding (e)) — so every first contact with a host is two back-to-back requests. Against that: the same pattern produced 23 `ok` rows. I assert no cause.

**One new observation of the instrument, and it bears directly on §3.** Across those 23 `ok` rows the byte length is constant at **207411** and there are **23 distinct sha256 digests — no digest has ever repeated**. The UNCITRAL commission page is the exact inverse: 24 `ok` rows, 13 instants, one length (832338), **one** digest (`ca87a113c024605c…`). *Alternative not excluded:* fixed-width variable content of any kind (nonce, token, timestamp, rotating banner); the reduction keeps no body and I do not choose among them. The consequence is design-level: **a digest sentinel is meaningful on `uncitral.un.org` and structurally meaningless on `investmentpolicy.unctad.org`.**

---

### 3. Your central question — the bracket

**Was the design defective? Yes, three ways, and the second is worse than the one you asked about.**

**(i) Scope mismatch.** A bracket controls for the availability of the resource it brackets. Across hosts it controls only for what is common and host-invariant: that the runner has egress, that the client is the pinned build, that the process reached the end of the batch. All three are already covered — by the control row, by naming `scripts/fetch_relay.py@7fbbabf`, and by `requested`/`completed`/row count. It controls for **nothing** host-local: DNS/TLS to host B, host B's edge policy, its rate limiting, its availability. Those are the only things a bracket exists to exclude. A cross-host bracket is not a weak control for the hypothesis at issue; it is a control for a hypothesis nobody had.

**(ii) The bracket could not have worked even same-host, because the artifact has no clock.** In `fetch_relay.py` at the pinned commit, `fetched_at` is `datetime.now(timezone.utc).isoformat()` inside the manifest dict built **after** the loop — one batch-level completion instant, no per-row timestamps. The pre-registration's own words — *"the host answered normally across the window in which rows 2 and 3 were fired"* — name a window the record cannot exhibit. A passing bracket would have "established" it anyway. That is the defect I would fix first: **a design whose success is unfalsifiable is worse than one whose failure is.** A bracket can establish **order** (array index). It cannot establish **duration**.

**(iii) Your premise about the control is half-true.** `records = [fetch_one(CONTROL_URL)]` runs once, before any target. `control_ok` is an **opening sample** of runner health, not a bracket of it. So the runner-wide health you take as already controlled is controlled at the batch's first instant only — which is the very gap the bracket was invented to fill, and the one place a *host-neutral* bracket would be legitimate.

**The correct gate, prospectively.** G1 **runner gate**, batch-wide: the control row, upgraded to a control *pair* (fired first and last) — that is the one bracket that may legitimately gate rows on other hosts, because `example.com` makes no claim about any research host. G2 **host gate**, per host: a bracket gates only rows on its own host. G3 **declared edges**: each row names the gates it is conditional on; void propagates along declared edges only, never by batch membership. G4 **no "window" language** until the schema carries a per-row timestamp; until then, "ordered before/after" and nothing more.

**Why P0 is strictly dominated, in numbers.** The gate host had a perfect observed record — 23 for 23 across 13 instants and 13 runner sessions — and fired on its first outing. **No observed reliability bounds tomorrow's edge policy.** Coupling makes batch informativeness the product of every gate host's reliability while the gate's diagnostic reach stays at one host: today it converted a single-host event into the loss of four rows, two of which returned `ok`.

**Today's rule versus tomorrow's, sharply.** Today: **void, entire.** I endorse your holding without qualification, and I will name the cost so it is on the record rather than implied — under your permissive reading D1 *would* have been satisfied (row 2 returned exactly 832338 / `ca87a113c024605c…`) and C-1 too. The day's forgone purchase is one instant of UNCITRAL digest stability and one ~24-hour robots stability point. Both are cheap and both re-acquirable tomorrow. That is the whole price of holding the line, and it is far below the price of establishing that a pre-registration may be re-read once its numbers are visible. The cross-host argument is **correct on the merits and inadmissible today**; it becomes tomorrow's rule by being written into tomorrow's pre-registration before results exist, not by being applied to this one.

---

### 4. Your item 5 — the robots row adds nothing, and I checked whether it could

All three committed rows (2026-08-09T11:16:36Z, 12:10:47Z, 2026-08-10T11:28:20Z) carry the same 150 bytes, the same digest `23b0867eb3e320f9…`, and excerpt strings that are **character-for-character identical**, length 150. Verbatim, and correcting the briefing's truncation:

> `User-agent: * Crawl-Delay: 5 Disallow: /rss Disallow: /search Disallow /record/*/export/*$ Sitemap: https://digitallibrary.un.org/sitemap_index.xml.gz`

**Today's row adds nothing to B7 and I will not manufacture an increment.** It cannot: it is the same reduction, and the officer's 0809 ruling already established that the 150-length equality is **tautological** as to line structure (eleven spaces, any five of which may be newlines), so a third identical sample cannot discriminate the six-line reading from the five-line reading that would invert the admissibility map. Nor may it extend C8's bound — P0 voids C-1. The one thing I can state is a repository fact, not a branch: the three excerpts are string-identical. `Crawl-Delay: 5` is unread by any code path; `MIN_INTERVAL_SECONDS = 3.0` (`src/sources/base.py:28`); machine screen for `crawl.?delay|crawl_delay|request_rate` over `src/` and `scripts/` returns zero, and the human-readable screen returns only the record's own prose about the defect. Carried, not new.

**A related gap I can now state exactly.** `excerpt_of` returns `""` when `find` is set and not found, and `find_matched` is `bool(find) and bool(excerpt_of(...))` — the same test. So row 2's empty excerpt is mechanical, and the archive retains **no human-readable text at all** for the 832338-byte UNCITRAL page. The standing rule that an absence screen must run on the human-readable form as well as the machine form is, for the A/81/17 null, **unsatisfiable from the committed artifact**. A second reader can check only a boolean.

---

### 5. Carry-forward, with one figure moved and one method flag on the others

Carried forward as instructed, not retrospectively: **seven of eight** merges on `main` followed by a relay result commit; mechanism stated at **ref-generality**; **`77087e9` UNACCOUNTED**; **merge #57 open with two candidates**. Corrected **C5** (seven of ten A/81/17 nulls symbol-controlled in their own batch, three carried by digest identity, charset residual inherited). Corrected **C8**, conditional on the six-line reading, whitespace-normalised. **Four named / four unnamed** Roberts titles, scoped "not found in this repository at `50b0c20`". The **RFC 9309** gap.

**The overwrite count has moved, and it moved as predicted.** Derivation pin as §0: at `origin/main`, **24 blobs / 12 request-named paths / 12 in-place overwrites**; at `--all` including today, **25 / 13 / 12**. Yesterday's carried figure (23 / 12 / eleven) is superseded by exactly one event: `analytics/fetch-results/2026-08-09-daily.json` gained a second blob (`9283defa`, commit `c014b2d`, 12:10:47Z) over its first (`564c9a16`, `50b0c20`, 11:16:36Z) — **yesterday's own merge re-firing the relay and overwriting yesterday's result in place**, escalation 2 observed for a second consecutive day. **Method flag, per §1(b): if any of these carried counts was derived with a path-limited `git log`, it is a lower bound and the three earliest 2026-08-03 reductions are the ones it drops.** I re-derived the overwrite count and the request-commit population exhaustively; I did not re-derive C5, C8 or the Roberts count and do not assert them at a new scope.

---

### 6. The theme, and where the one search went

**The theme is quiet and I will say only that.** The digest of record stands unchanged at 2026-08-03. No screened item this session touches the three-ring intersection; the two theme threads with any live motion are A/81/17 publication (instrument-limited, reclassified 2026-08-09) and the China BIT provision texts (operator-held, zero budget). I manufacture no nexus.

I spent the one query on the **standing monitored-author check**, before any topical search, because my contract orders it each session and because declining a standing check on my own forecast of its yield is structurally the same move as re-reading a pre-registration once the numbers are in. Query: `Anthea Roberts EJIL:Talk! August 2026 UNCITRAL ISDS reform`, domain-scoped to `ejiltalk.org`. Nine URLs returned — one author landing page, eight articles. **Per the rule fixed in advance: no returned item's title or URL carries an August 2026 date, and the returned index is undated, so the finding is "no post datable to August 2026 in accessible search results at this instant" — never "she has not posted."** The search tool's own prose asserted a November 2023 most-recent article; that is the tool's assertion, carried by no source I read, and I do not adopt it.

**One genuinely new thing the check bought, and it is a rule, not a result.** I re-ran yesterday's absence screen on the four unnamed titles — *"UNCITRAL and ISDS Reforms: What are States' Concerns?"*, *"…(Online): Can You Hear Me Now?"*, *"…Plausible Folk Theories"*, *"…Moving to Reform Options … the Politics"* — in **both** the human-readable and slug forms. At `50b0c20`: **0 hits each**. At HEAD: **1 hit each, and in every case the sole hit is `analytics/daily-research/2026-08-09.md`** — yesterday's memo reporting their absence. **The repository now names them only because we wrote down that it did not.** Re-derived at HEAD the count would read eight-named / zero-unnamed, a false improvement produced entirely by the record ingesting its own search output. Yesterday's pin to `50b0c20` was therefore not a formality; it is the only thing keeping that count meaningful, and the general rule is: **an absence screen over this repository is self-falsifying on commit and must be pinned to the commit at which it ran, never re-run at HEAD and compared.**

`GAP-UNRESOLVED: rfc-9309-grammar-unverified — the RFC 9309 Disallow-line grammar and malformed-line rule remain asserted from memory; no accessible copy retrieved this session (zero fetch budget) and none is in the repository.`
`GAP-UNRESOLVED: china-switzerland-bit-2009-forum-provision-text — the BIT and Protocol texts remain unread; today's two refused/403 rows are the first access failure recorded against this residual, and are a statement about our access at one instant, not about the page.`

Escalated gaps (`china-france-bit-2007-protocol-exhaustion`; the operator's IIA-mapping items) received zero budget and are stated, not searched.

**Method note for the self-training file.** Author-name + title still beats keyword (2026-07-26 lesson, unchanged). Today's addition is a *derivation* lesson, not a search one: **`git log <rev> -- <path>` and `git rev-list <rev>` + `ls-tree` are different populations, and the difference is the oldest evidence.** Prefer the latter for any count, always.

---

### PROPOSALS TO THE CHAIRMAN

All propose-only. Nothing built, no `src/`, `.github/`, ledger or workflow change made or drafted.

1. **Adopt the DERIVATION PIN and retire "local-ref scope" outright.** Three mandatory fields on every count: COMMAND (verbatim), REVS (resolved 40-char SHAs), DEPTH (shallow flag + graft list). Actionable tomorrow at zero cost: it is a writing rule.
2. **Rule: no count derived by path-limited `git log`.** Enumerations use `git rev-list <rev>` + `ls-tree`. Where a past count's method is unrecorded, mark it a lower bound rather than re-deriving it silently. The three 2026-08-03 blobs are the named casualty class.
3. **Record that this clone is shallow, in the standing instrument section, today.** Ten grafts, boundaries 2026-07-20 → 2026-08-04T11:19:04Z. Every "never / first / every prior" claim in the record inherits this bound and none currently says so.
4. **Restructure the gate for tomorrow's pre-registration, before results exist:** G1 runner gate = a control *pair* (first and last), the only legitimate cross-host bracket; G2 host gates same-host only; G3 each row declares its gates and void propagates along declared edges; G4 no "window" language until per-row timestamps exist. Write it tomorrow, not into today's.
5. **Escalate to Emory as a schema item (propose-only): the reduction has no per-row clock.** `fetched_at` is one batch-completion instant. Until that changes, every bracket in this project's history establishes order and not duration, and any past sentence claiming a window should be re-read on that basis.
6. **Escalate as a second schema item: refused rows record `status` as a string while `ok` rows record it as an int** (corpus census, `--all`: 100 `ok`/int, 12 `refused`/str, 6 `robots_disallowed`/None), and refused rows record no `final_url` and no headers — so a redirect-terminal 403 is indistinguishable from an origin 403. Both are cheap to state now and expensive to reconstruct later.
7. **Do not put a digest sentinel on `investmentpolicy.unctad.org`.** 23 `ok` rows, 23 distinct digests, one constant length. On that host only outcome/status/`find_matched` carry information; on `uncitral.un.org` the digest does. Choose the sentinel to the host.
8. **Adopt the pinned-screen rule for absence claims:** every "not found in this repository" pins the commit it was screened at, and is never re-run at HEAD for comparison. Today's Roberts screen is the demonstration — 0 hits at `50b0c20`, 1 hit each at HEAD, and the hit is our own memo.
9. **Re-fire tomorrow's smallest possible batch under the new gate**: the UNCITRAL digest sentinel alone, gated only by the runner control pair, plus one UNCTAD 978 row gated on itself. That recovers today's forgone D1 and puts one further sample on the UNCTAD 403 without coupling the two.

---

### The day, honestly

The batch bought nothing it was fired to buy, and your P0 is the reason, correctly applied. What the day did buy is entirely method: your `21` versus `24` is a git command artifact, not a ref-breadth artifact — I reproduced both figures exactly and showed the 70-branch fetch delivered zero result blobs — and the artifact's casualties are the three oldest reductions in the archive, which is the worst possible three to lose. The bracket was defective in the way you suspected and in a worse way you did not raise: it had no clock to measure the window it claimed. The UNCTAD 403 is real, it is the first on that host and the first ever on a URL that had answered 23 for 23, and it is a statement about one instant and our access at it — I could not and did not establish a cause. The robots row added nothing and I say so. The theme was quiet; I did not dress it up. **No new research insight; standing watch** — the one new insight logged is instrument and method (§1(b), §6), and I would rather log that honestly than pad the ledger with a thirteenth restatement of a stable digest.

---

**Candidate claims `0810-C1` … `0810-C7`** are entered by the analyst in structured form and are reproduced in full in the vetting round at Part V, where each is put to the integrity challenge. Their headline propositions: **C1** the 21/24 divergence is a git-command artifact and not a ref-scope one, with the three dropped blobs named; **C2** the 70-branch fetch delivered zero result blobs and the enumeration was derivable before it; **C3** the UNCTAD bracket URL was 23-for-23 `ok` before today, at one byte length and 23 distinct digests, and today is the first 403 on that host; **C4** the reduction schema has no per-row clock and the control row is an opening sample, not a bracket; **C5** the three robots.txt retrievals are character-identical and today's adds nothing; **C6** the in-place overwrite count moves to twelve, by exactly one event — yesterday's own merge; **C7** an absence screen over this repository is falsified by committing the memo that reports it.

**Sources (retrieved).** `analytics/fetch-requests/2026-08-10-daily.json@f7675d3`; `analytics/fetch-results/2026-08-10-daily.json@26dc05b`; the 25 committed result blobs reachable at `--all` scope, cited by blob and commit above; `scripts/fetch_relay.py@7fbbabff2bc81535d1e849164fad207b7eb9a32a`; `src/sources/base.py`, `analytics/daily-research/2026-08-09.md`, `analytics/council-log.md`, `state/research_log.json` at HEAD `26dc05b`; `.git/logs/HEAD`, `.git/logs/refs/remotes/origin/main`, `.git/shallow`, `.git/objects/pack/*` (repository state, this session). **Leads, not sources:** the nine `ejiltalk.org` index URLs returned by today's single query — the Anthea Roberts author page, *Moving to the Delivery Phase*, *Crossing the Chasm*, *What to Expect When You're Expecting*, *Visualising a Flexible Framework*, *What are States' Concerns?*, *Can You Hear Me Now?*, *Plausible Folk Theories*, *Moving to Reform Options … the Politics* — none retrieved.

---

## Part III — The digest of record

**Digest 2026-08-03: 13 candidates evaluated, 1 items surfaced (0 matches, 1 watch-list leads).**
Numbers copied verbatim from `digests/2026-08-03_ISDS-Thematic-Watch/meta.json`, never restated from
memory, per Rule 1. The digest of record is **unchanged** for a seventh consecutive day, so this part
is short by design: no new candidate has been evaluated by the instrument since 2026-08-03, and
nothing in today's session moves a per-source yield. `health_warnings` is empty.

---
