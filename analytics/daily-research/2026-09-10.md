# ISDS Thematic Watcher — daily council meeting, 2026-09-10

**Chairman presiding.** This is the operator's scheduled cloud session, convened
automatically. The record is written for a reader with no prior context: terms are spelled
out on first use, and where a figure is carried from an earlier day it is re-derived here
rather than quoted from memory.

---

## Part I — Agenda, instrument posture, and what today is for

### 1. Member return-path protocol

Council members are **real subagents**, spawned with the Agent tool against the committed
definitions in `.claude/agents/`. No seat is performed by me. Each member's text enters this
record **verbatim**, under its own heading, and I do not write, edit, summarise or improve any
member's contribution. Where a member's return contains an error, the error stays in the
record and is ruled on in Part VI — the record is an account of the session, and deleting an
error deletes the evidence that the gate caught it.

**If a spawn fails, the seat is recorded as PROCEDURAL FAILURE for the day and I complete what
can honestly be completed.** I do not fill a failed seat with my own voice.

**Rule 0 departure, declared rather than hidden — tenth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 permits exactly two convened agents
(`research-analyst` and the chairman) and directs that the integrity challenge live as a voice
inside the Part V dialogue rather than as a separately convened seat. The operator's cardinal
rule — that no seat is ever performed — forbids me writing that voice. **I convene the
integrity officer as a real subagent and place its return inside Part V, under Rule 2's
dialogue, rather than as a Part of its own.** That is two subagents where Rule 0 permits one.
Every Rule 0 concession that costs nothing is kept: **no analytics officer and no systems
researcher is convened** (§3 and §4 below give the reasons), the digest sentence is two lines
I copy from `meta.json`, and the one optimization item is a single paragraph I write myself.
The protocol's own Precedence section directs that such a conflict earn a line in the
procedural caveat; it has one, at Part VI §9.

### 2. Egress probe at session open — fetch is DEAD, relay is ALIVE, search is untested by me

Three probes on two channels, **all on neutral controls**, run before any seat was convened:

| Probe | Channel | Target | Result |
|---|---|---|---|
| 1 | `curl` | `https://example.com` | `CONNECT tunnel failed, response 403` (exit 56) |
| 2 | `curl` | `https://www.iana.org/help/example-domains` | `CONNECT tunnel failed, response 403` (exit 56) |
| 3 | fetch service | `https://example.com` | `EGRESS_BLOCKED` |

The proxy's own status endpoint confirms the mechanism rather than leaving it inferred:
`recentRelayFailures` names both hosts at `2026-09-10T11:12:52Z` / `11:12:53Z` with
`kind: "connect_rejected"`, `detail: "gateway answered 403 to CONNECT (policy denial or
upstream failure)"`. **This is the gateway refusing, not a site refusing.**

**No state change** from the constraint established 2026-08-02 and 2026-08-03. Because both
probes are neutral controls, **no fetch-layer status in this record is a claim about any
resource** — the standing tool-status-as-source-state rule, restated because it is the rule
this council breaks most easily.

**The relay is alive and is therefore this session's only retrieval channel.**
`.github/workflows/fetch-relay.yml` ran twice yesterday from a GitHub runner and committed
both times. Web search is alive on the record's evidence but **I have not probed it myself and
do not assert it**; the analyst's first search doubles as that probe and Part II reports it.

### 3. The digest did not move

`Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`, not restated from memory.
`validation_status_only: true`. The newest digest directory in the tree is still
`digests/2026-09-07_ISDS-Thematic-Watch/`; there is no 09-14 digest and there will not be one
before Monday. **The digest of record is unchanged from yesterday, so no analytics officer is
convened and the two lines above are the whole of the digest discussion** (Rule 0, Rule 1).

Per-source counts at that meta: `iareporter_headlines` 10, `pca_press` 2, `google_alerts` 1,
`bing_news` 1, and **zero** from `iisd_itn`, `gmail_scholar`, `italaw`, `icsid`, `unctad_isds`,
`gdelt`. `iisd_itn` is `QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest
2026-04-21)`.

**The weekly brief counter, stated with its members and never as a bare integer** (yesterday's
`0909-B3`, sustained against the chair): the weekly brief is **six Mondays empty — 08-03,
08-10, 08-17, 08-24, 08-31, 09-07.** Weekdays computed, not read: 09-07 Monday, 09-08 Tuesday,
09-09 Wednesday, **09-10 Thursday.** *No Monday has elapsed since the last statement of this
counter, so it does not increment today.* That check is the whole point of the rule.

### 4. Yesterday's next steps 1 and 3, discharged HERE and by me, before any seat was convened

**Next step 1 — the sent marker, checked at session open.**
`git ls-tree -r origin/main --name-only` under `analytics/daily-research/.sent/` returns
markers through **`2026-09-08`** and **`2026-09-09`**, and **no `2026-09-10`.** The operator's
copy of today's record is therefore not yet suppressed, and there is no marker to delete.
Yesterday's output-path repair also worked end to end: `.sent/2026-09-09` was written at commit
`6df6114`, **after** the merge commit `abeeb5b` carried the complete record to `main`, so
yesterday's email was the whole record and not a Part-I fragment. **The countermeasure that
failed on 2026-09-09 succeeded on 2026-09-09's own record because the chair caught it mid-session.**

**The commit discipline this imposes on me today, restated from yesterday's next step 1:**
push the record **alone, in one commit, carrying `[skip ci]`**; push the request file in a
**separate push**. Two commits in one push is what defeated the skip yesterday, because GitHub
reads `[skip ci]` off the push's head commit and evaluates a `paths:` filter over the whole
push.

**Next step 3 — the corpus screen, extended to rigid designators, which is `C-3`'s remedy.**
Yesterday's chair was convicted of running a screen aimed at the wrong noun: he screened the
batch's *literals* and not the *designator of the thread the batch claimed to serve*, and so
certified a batch that could not have closed its thread. The extended screen is run here, over
**every record in every reduction present in the tree at `6df6114`** (`analytics/fetch-results`
subtree `6f0f56a4`), counted by enumeration and not read off a display:

- **52 reductions, 418 records.**
- **Batch literal `ICSID`: 6 rows.** Most recent four: `2026-09-07-daily.json`
  (`/cases/pending`, matched, sha `ecbd32d6`); `2026-09-08-daily.json`
  (`/cases/case-database`, matched, sha `134c2004`); `2026-09-09-daily.json`
  (`/cases/recent`, matched, sha `f309eb63`); `2026-09-09-daily.json` (`/cases/pending`,
  matched, sha `712196ea`). **This literal is a deliberate re-fire and a positive control, not
  discovery.**
- **Batch literal `Wingtech`: 0 rows of 418.**
- **Rigid designator of the served thread — `Wingtech` in ANY field of ANY record, not only
  the `find` field: 0 rows. `Nexperia`: 0 rows.**
- **URL `https://icsid.worldbank.org/cases/recent`: 4 rows across 2 reductions** — 09-07
  (`find: ICSID`, `find: ARB/`, both matched, sha `4af84337`) and 09-09 (`find: ICSID`,
  `find: Netherlands`, both matched, sha `f309eb63`).

**The screen's verdict on today's batch, stated as a result and not as a certification: the URL
is not new and is a re-fire; the substantive literal is untested; and the literal IS the rigid
designator of the thread the batch is commissioned to serve.** Today's batch would pass
yesterday's extended screen. Yesterday's batch would have failed it, in one command, before it
was pushed.

### 5. THE DAY'S FIRST FINDING, AND I FOUND IT BEFORE CONVENING ANYONE: yesterday's relay reduction was overwritten in place by the merge that published the record citing it

`analytics/fetch-results/2026-09-09-daily.json` has **two commits**, and the second overwrote
the first at the same path:

| Commit | Time | Blob | `environment` | `fetched_at` |
|---|---|---|---|---|
| `d34b9ec` | 2026-09-09T11:19:55Z | `72468ae1` | runner **run 34344935760** | 11:19:55.176614Z |
| `72adf10` | 2026-09-09T11:56:42Z | `da60c599` | runner **run 34348169510** | 11:56:42.116828Z |

Both carry 6 records, `requested`/`completed` 5/5, `control_ok: true`.

**The mechanism, stated as a code and configuration finding rather than as a story.**
`.github/workflows/fetch-relay.yml` triggers `on: push` with `paths: analytics/fetch-requests/**`
and **no `branches:` filter**. `scripts/fetch_relay.py`'s `run()` writes to
`out_dir / request_path.name` with no existence check, and `newest_request()` is
`sorted(glob('*.json'))[-1]` — a lexicographic maximum. So when yesterday's PR merged, the
merge pushed `analytics/fetch-requests/2026-09-09-daily.json` **to `main`**, which re-fired the
relay against the same request and **wrote its answer over the reduction the record had already
cited.** The commit ordering proves it: `d34b9ec` is on the meeting branch, `abeeb5b` is the
merge, `72adf10` lands after.

**What this costs, precisely.** `analytics/insights.jsonl`'s 2026-09-09 research entry cites
*"Relay run 34344935760"*. **At `HEAD` that file reports run 34348169510.** A reader following
the citation to the path finds a different run and no notice that the substitution happened.
The evidence is not destroyed — both blobs are recoverable, and the substantive sha256
`f309eb63` is identical in each, so **no figure in yesterday's record is falsified by this** —
but the register is mutable at the path and the citation resolves only at the blob.

**Why this is worth the chair's opening slot.** This is `open_threads[15]` defect (1), the
in-place overwrite, firing **structurally rather than by accident** — not from a mis-named
second batch as on 2026-09-03, but from the ordinary daily merge. **Every daily session that
merges a request file to `main` re-fires its own batch and overwrites its own evidence.** It has
presumably been doing so for as long as the relay has existed, and the reason it has never been
reported is that the second run answers the same request and usually returns the same bytes.
I record it as a finding rather than as a note because it is exactly the class this council
already lost a fortnight to: a countermeasure and a citation discipline that are both correct,
and a register underneath them that moves.

**Nothing is patched.** `.github/workflows/` and `scripts/` are outside this session's merge
scope and neither is on the operator's approved list. It goes to Emory as an escalation.

### 6. Today's relay batch — yesterday's next step 2, executed unchanged

`analytics/fetch-requests/2026-09-10-daily.json`, written and pushed **before any seat is
convened**, so its forecasts are pre-registered rather than fitted. Two research rows plus the
mandatory tail control:

1. `https://icsid.worldbank.org/cases/recent`, `find: "ICSID"` — **paired positive control.**
   A deliberate re-fire (§4: 4 prior rows at this URL). If this row does not match, no other
   row on this URL may be read as information about the resource.
2. `https://icsid.worldbank.org/cases/recent`, `find: "Wingtech"` — **the substantive literal,
   and the first time in 418 records that `open_threads[5]`'s rigid designator enters this
   channel at all.**
3. `https://example.com/`, `find: "Example Domain"` — **a TAIL control, and I name it correctly
   rather than calling it the mandatory one.** `run()` at `scripts/fetch_relay.py:198` prepends
   `fetch_one(CONTROL_URL)` as `records[0]` automatically and gates the whole batch on it, so
   the mandatory control is not something a request file supplies. This explicit row is a
   *second* control at the **end** of the run, and its only function is to test that egress held
   for the run's whole duration rather than only at its start. It is redundant against the head
   control by construction, and it is requested anyway because a batch of two research rows is
   cheap enough that the tail check costs nothing.

**PRE-REGISTERED READING RULE, binding on every seat including me, and deliberately narrower
than the question it serves.** A null on row 2 licenses **only** this: that `excerpt_of` found
no occurrence of the literal `Wingtech` in the tag-stripped, whitespace-collapsed rendering of
the body this relay received from this URL at this time. **It does NOT close
`open_threads[5]`, does not establish that no such proceeding is registered, and does not
narrow the gap.** `open_threads[5]`'s closure condition is unchanged and unwidened. A match on
row 2 licenses only that at least one occurrence of that literal is present in that rendering,
and **no case number, party, tribunal or matter appearing in any excerpt may be entered as a
discovered matter, screened, graded on any ring, or carried as a lead** — including in the
candidate-claims block, which yesterday's officer established the reading rule had been silent
on.

**FORECASTS, pre-registered:**

- **F1** — row 1 matches (`find_matched: true`, HTTP 200). Confidence high; it has matched at
  this URL twice.
- **F2** — row 2 returns `find_matched: false`. Confidence high, and **I state plainly that
  this forecast being right buys nothing**: the informative outcome is F2 falsified.
- **F3** — the reduction will carry **4 records**, `requested: 3`, and **`completed: 3`, not 4.**
  Read off the code at `6df6114`, not recalled: `records = [fetch_one(CONTROL_URL)]` then one
  append per requested url (`scripts/fetch_relay.py:198-207`), and
  `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")` at **`:221`** —
  so `completed` counts the requested rows and **excludes the prepended head control**
  (yesterday's `N7`). **I drafted this forecast as "2, not 3" and it was wrong**: I had applied
  `N7`'s exclusion twice, once to the head control and again to my own tail control, which is a
  requested row and is counted. Corrected here, before the push, by reading the lines rather
  than the note — which is the entire content of `N7` and of yesterday's next step 4.
- **F4** — **this reduction will be overwritten in place by today's own merge**, per §5, and the
  post-merge blob will carry a different `environment` run id and a later `fetched_at` while
  the substantive sha256 stays equal. **This is the first time this council has pre-registered
  the overwrite instead of discovering it afterwards**, and Part VI grades it.

**The note template's two frozen defects are corrected in today's note before it was written**
(yesterday's next step 4): `completed` is stated over `records[1:]` excluding the head control,
and the elapsed-window figure below is computed against the thread's earliest carrier.

### 7. Agenda — three bounded tasks for the analyst

The budget is deliberately small. This is a check-in, not a weekly council.

**TASK 1 — grade the relay batch against §6's four pre-registered forecasts.** Read the
reduction's own `request` and `environment` header fields **before** reading any row as an
answer (the 2026-09-03 discipline, adopted after a run silently answered the wrong request).
Quote each forecast from the committed request file when grading it; do not paraphrase it
(the 2026-08-29 discipline). **F4 is the one I most want graded honestly, including if it does
not fire.**

**TASK 2 — audit §4 and §5 of this Part adversarially, and try to break them.** Both are mine
and both were run without a second pair of eyes. §5 in particular asserts a mechanism from a
workflow file and two blobs; **check whether the overwrite is caused by what I say causes it,
or by something else that merely correlates with the merge.** Report what you could not break
as well as what you could — a council that logs only its hits cannot tell a working control
from an unused one.

**TASK 3 — the research question, at most three searches.** Apply your own rule before
spending any of them: `git grep -icn` the rigid designator first, and if the record holds it
in more than two files, aim the search at the **thread's stated closure condition** and not at
the matter. Returned budget is credited on its reasoning, not penalised. **`open_threads[5]`'s
monitoring window is 67 days** — computed today as `2026-09-10 − 2026-07-05`, the thread's
earliest carrier, **not** the post-cooling-off day count that yesterday's `C-2` struck.

**Standing, carried unchanged and at zero budget:** `open_threads[0]` stays CLOSED; the Ecuador
slug stays CONVERTED to an operator manual query with its closure condition unwidened and its
paywalled-body synthesis QUARANTINED; the Einarsson family stays retired; the three escalated
gaps stay at zero budget; cite the **blob**, not the path, for any digest or reduction artefact
— **and §5 is today's demonstration of why that rule exists.** A line-number pinpoint into a
mutable tracked file carries the commit at which it resolves (yesterday's `C-1`).

**No systems researcher is convened.** Its queue is fully gated by the operator's pending list
— the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the
IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix and any
ledger amendment are all **pending, not granted**. There is no ungated work in that queue, and
Rule 0 excludes the seat from the daily meeting in any case. The one optimization item is a
paragraph I write in Part VI.

### 8. Spend checkpoint

Placed **after the vetting round and before the close-out**, at Part VI §0: seats convened
against seats returned, searches spent against searches budgeted, relay rows requested against
rows returned, and an explicit statement of whether the close-out is affordable in full. If it
is not, the close-out is shortened and I say so rather than padding.

---

# Part II — Research analyst's return, 2026-09-10

**Transit header, written by me and clearly separated from the member's text.** The seat was
spawned as a real subagent (Agent tool, `.claude/agents/research-analyst.md`). It returned. Its
text follows **verbatim** below and I have not written, edited, summarised or improved any part
of it; where it is wrong, the error stays and is ruled on in Part VI. **One mechanical
transformation is disclosed rather than hidden:** the return reached me through a transport that
HTML-escapes angle brackets and ampersands, and I have reversed that escaping (`&lt;` → `<`,
`&gt;` → `>`, `&amp;` → `&`) so the member's commands and JSON read as it wrote them. No other
character was changed.

**Model, as the seat reported it on its own first line:** `claude-opus-5`. The operator's
directive puts this seat on Opus 5, so the observed value and the directive coincide — **and I
record, as `0909-N6` requires and as the seat itself insisted, that `.claude/agents/research-
analyst.md:4` (at `8015833`) pins `model: opus`, a TIER and not a VERSION, so the conformance is
unenforced and the coincidence is not a compliance finding.**

**THE SEAT'S FIRST ACT FALSIFIED MY OWN AGENDA, AND I FLAG IT HERE SO NO READER MEETS IT AS A
FOOTNOTE: WEB SEARCH IS DEAD.** Part I §2 said web search was "alive on the record's evidence"
and deferred the probe to the analyst. The analyst probed it three times, the third on a
**neutral control**, and all three failed. **This is a state change from every prior session,
and it means today's session had no retrieval channel it could aim at anything.** It is ruled
on at Part VI §1 (`0910-A4`) and escalated at Part VI §7.

---

**Model actually running this seat: `claude-opus-5` (self-reported as "Opus 5", exact model ID `claude-opus-5`, from this session's own environment block).** `.claude/agents/research-analyst.md:4` at commit `8015833` reads `model: opus` — a tier selector, not a version pin. The observed value and the directive's value coincide; nothing in the harness enforced that, and I do not report it as compliance. 0909-N6 stands.

**WEB-SEARCH PROBE — THE DAY'S FIRST FINDING, AND IT IS NEGATIVE: SEARCH IS DEAD.** Part I §2 says "Web search is alive on the record's evidence but **I have not probed it myself and do not assert it**; the analyst's first search doubles as that probe." It is not alive. Three probes, three tool-layer failures, escalating in neutrality exactly as this council does on the fetch layer:

| # | Query | Scope | Result |
|---|---|---|---|
| 1 | `Anthea Roberts EJIL:Talk! September 2026` | `allowed_domains: ejiltalk.org` | `Web search error: unavailable` |
| 2 | `Anthea Roberts EJIL Talk UNCITRAL ISDS reform` | unrestricted | `Web search error: unavailable` |
| 3 | `example domain` | **neutral control**, unrestricted | `Web search error: unavailable` |

The third is the one that matters: a neutral control excludes "the query was bad", "the domain filter broke it", and "ISDS terms are being refused". **This is a channel status and it is a claim about the search tool and about nothing else** — not about ejiltalk.org, not about Roberts, not about whether any Wingtech proceeding exists. Consequence: with fetch dead (§2), search dead (here), and the relay batch frozen before I was convened, **this session had no retrieval channel it could aim at anything**. TASK 3 is therefore null by construction, not by quiet news, and I say so rather than dressing the relay's null as research.

---

## TASK 1 — grading the four pre-registered forecasts

**Header check first, before any row was read as an answer** (the 2026-09-03 discipline). Reduction blob `91186e69`, at commit `8015833`, path `analytics/fetch-results/2026-09-10-daily.json`:

- `"request": "2026-09-10-daily.json"` — **this run answered today's request, not a stale one.**
- `"environment": "github-actions-runner (run 34470490415)"`, `"fetched_at": "2026-09-10T11:17:37.181145+00:00"`.
- Run-level corroboration from GitHub's own record for run 34470490415: `"head_sha":"c53c97e0d7d25f53597bc197bdd578e8bb697065"` — the pre-registration commit itself — `"head_branch":"council/2026-09-10"`, `"event":"push"`, `"run_attempt":1`, `"conclusion":"success"`, `"run_started_at":"2026-09-10T11:17:13Z"`. The request commit `c53c97e` is timestamped 11:17:09Z and the run was created 11:17:13Z. **Pre-registration is verified from the runner side, not only from commit order.**
- `control_ok: true`, `void: false`.

Forecasts quoted verbatim from the committed request file, blob `129e83b9` at commit `c53c97e`:

**F1 — "F1 - row 1 matches, find_matched true, HTTP 200; high confidence, it has matched at this URL twice." → CONFIRMED.** `records[1]`: `"url":"https://icsid.worldbank.org/cases/recent"`, `"find":"ICSID"`, `"outcome":"ok"`, `"status":200`, `"find_matched":true`. The instrument is positively controlled at both ends: head control `records[0]` ok/200, tail control `records[3]` `"find":"Example Domain"`, `find_matched:true`. Egress held for the run's whole duration, which is the only thing the tail row was for.

**F2 — "F2 - row 2 returns find_matched false; high confidence, and I state plainly that F2 being RIGHT buys nothing - the informative outcome is F2 falsified." → CONFIRMED, AND IT BOUGHT NOTHING.** `records[2]`: `"find":"Wingtech"`, `"outcome":"ok"`, `"status":200`, `"excerpt":""`, `"find_matched":false`. Under the binding reading rule this licenses **only** that `excerpt_of` found no occurrence of the literal `Wingtech` in the tag-stripped, whitespace-collapsed rendering of the body this relay received from this URL at 11:17:37Z on 2026-09-10. It does **not** close `open_threads[5]`, does not establish that no such proceeding is registered anywhere, does not narrow the gap, and does not touch the closure condition, which stands unchanged and unwidened. The chairman's own framing is correct and I adopt it: the informative outcome was F2 falsified, and it did not occur, so **the batch's substantive row returned the uninformative branch of a binary the council already expected**. That is a cost the pre-registration made visible rather than a result.

**F3 — "F3 - the reduction carries 4 records, requested 3, and completed 3 NOT 4 … EXCLUDING the prepended head control" → CONFIRMED IN ALL THREE LIMBS.** `len(records) == 4`; `"requested": 3`; `"completed": 3`. The mechanism is as read off the code, not recalled: `records = [fetch_one(CONTROL_URL)]` then one append per target (`scripts/fetch_relay.py` `run()`), and `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")` — three requested rows, all `ok`, head control excluded. The chairman's self-correction from "2, not 3" was the right correction: `N7`'s exclusion applies once, to the prepended head control, and his row 3 is a requested row that counts.

**F4 — "F4 - THIS REDUCTION WILL BE OVERWRITTEN IN PLACE BY TODAY'S OWN MERGE … the post-merge blob will carry a different environment run id and a later fetched_at while the substantive sha256 stays equal." → NOT GRADABLE TODAY, AND I WILL NOT GUESS IT.** `origin/council/2026-09-10` is not an ancestor of `origin/main`; `origin/main` still tips at `6df6114`. `analytics/fetch-results/2026-09-10-daily.json` has exactly one commit, `8015833`, blob `91186e69`. The predicted event has not had its trigger. Stating a verdict now would be grading a forecast against its own mechanism rather than against an observation.

**Exactly what would settle it, so the next session does not have to re-derive the test.** After the merge of `council/2026-09-10` to `main`, F4 is CONFIRMED iff all four hold, and FALSIFIED if any fails:
1. `git log --format='%H' origin/main -- analytics/fetch-results/2026-09-10-daily.json` returns **two** commits, the later one authored by `github-actions[bot]` with message `chore: fetch-relay results [skip ci]`.
2. `git rev-parse <later>:analytics/fetch-results/2026-09-10-daily.json` ≠ `91186e69`.
3. In the later blob, `environment` names a run id **other than** 34470490415 and `fetched_at` is later than `2026-09-10T11:17:37.181145+00:00`.
4. In the later blob, `records[1].sha256` and `records[2].sha256` **both still equal** `470b88788b480bd032d7f5d8b117d67dadf3e4fe6df66fb6bd8041721f54e3ed`.

Limb 4 is the one that can fail while 1–3 succeed, and it is the substantive limb: the ICSID body is not byte-stable across days (see TASK 2). If limb 4 fails, F4's prediction that "the substantive sha256 stays equal" is falsified even though the overwrite occurred, and the record must say so in those two parts rather than scoring F4 as a whole.

A fifth condition settles it from the runner side and should be checked too: `actions_get(get_workflow_run, <new run id>)` should return `"event":"push"`, `"head_branch":"main"`, `"run_attempt":1`, and `head_sha` equal to the merge commit. That distinguishes the merge from a dispatch or a re-run without inference.

---

## TASK 2 — adversarial audit of §4 and §5

### §4 — the extended corpus screen: I could not break it. It replicates exactly.

Independently re-run, not read off the chairman's table: I extracted the `analytics/fetch-results` subtree at commit `6df6114` (subtree sha `6f0f56a4`, matching his stated instrument) and enumerated with a Python aggregator rather than a display.

| Chairman's §4 figure | My independent result | Verdict |
|---|---|---|
| 52 reductions, 418 records | 52 reductions, 418 records | replicates |
| `Wingtech` 0 rows of 418 | `find`-field 0; **any field of any record** 0 | replicates |
| `Nexperia` 0 rows | any field 0 | replicates |
| `ICSID` 6 rows | 6 rows, all `find_matched: true` | replicates |
| `/cases/recent` 4 rows across 2 reductions | 4 rows, 2 reductions | replicates |
| shas `4af84337` (09-07), `f309eb63` (09-09) | both confirmed on those rows | replicates |
| `ecbd32d6`, `134c2004`, `712196ea` | all three confirmed on the rows named | replicates |

**Zeros validated against a known positive before being trusted**, per the standing rule: the same any-field instrument returns **109** rows for `example.com` over the same 418. The instrument fires. The `Wingtech`/`Nexperia` zeros are real zeros.

§4 is also immune to the defect that sinks §5 below: it is computed over a **tree**, not a history, so a shallow clone cannot truncate it. That is a point in the chairman's favour and I record it as such.

One non-blocking wording note: §4 calls `ICSID` "the batch literal" and lists six rows, of which the oldest (`2026-09-06-daily.json` record 11) is at `https://icsid.worldbank.org/news-events`, a different URL. The count is right; the phrase "most recent four" is right; a reader skimming could take all six as `/cases/recent` rows, which they are not.

### §5 — the mechanism: I tried four ways to break it and failed on all four. It is confirmed, and confirmed harder than he stated it.

I did not test plausibility. I went to the run records.

**(a) Ruled out: `workflow_dispatch`.** Run 34348169510 reports `"event":"push"`. Across the full population of `fetch-relay.yml` runs on `main` — 40 runs, `event=push` — there is no dispatch-triggered run on `main` in the workflow's lifetime.

**(b) Ruled out: a re-run of the first run.** A GitHub re-run preserves the run id and increments `run_attempt`. The two 09-09 runs carry **different ids** (34344935760, 34348169510) and **both report `"run_attempt":1`**. Two distinct first attempts, not one run replayed.

**(c) Ruled out: a second push that happened to touch the path.** Run 34348169510 reports `"head_sha":"abeeb5b0642d171899e9c85469f8fd72b0e1597b"` and `"head_branch":"main"` — the head sha **is the merge commit itself**, and `"display_title":"council: daily meeting 2026-09-09 (#155)"`. The push that fired it was the merge push and no other. The first run, 34344935760, reports `"head_branch":"council/2026-09-09"`, `"head_sha":"4e62b58…"` — the branch push. Two pushes, two runs, one request file, one output path.

**(d) Ruled out: my own alternative — that 32 seconds is too fast for the job, so something earlier must have fired it.** I raised this and it was wrong. The run record gives `"run_started_at":"2026-09-09T11:56:13Z"` and `"updated_at":"2026-09-09T11:56:46Z"`: 33 s wall clock, three seconds after the merge commit's timestamp. The same 33–53 s merge-to-commit interval recurs on **every** daily merge I checked (09-03 33 s, 09-04 38 s, 09-05 51 s, 09-06 49 s, 09-07 53 s, 09-08 32 s, 09-09 32 s). The regularity is what makes the speed credible; my plausibility objection was the weaker instrument and I withdraw it.

**§5's evidentiary table also replicates.** Blob `72468ae1` at `d34b9ec`: run 34344935760, `fetched_at` 11:19:55.176614Z. Blob `da60c599` at `72adf10`: run 34348169510, `fetched_at` 11:56:42.116828Z. Both: 6 records, `requested` 5, `completed` 5, `control_ok` true, `void` false, and **every one of the six `sha256` values identical between the blobs**, including `f309eb63` and `712196ea`. His conclusion that "no figure in yesterday's record is falsified by this" holds on the bytes.

### §5's universal claim — he called it a presumption; it is checkable, and I checked it. It is TRUE, with three named exceptions that all have the same cause.

He wrote: "**Every daily session that merges a request file to `main` re-fires its own batch and overwrites its own evidence.** It has presumably been doing so for as long as the relay has existed."

**The re-fire is universal.** `fetch-relay.yml` was created at commit `fe02f39` (2026-08-03), and the workflow's complete `branch=main, event=push` run population is **40 runs**, every daily meeting merge from 2026-08-03 to 2026-09-09 among them, all `run_attempt: 1`. There is no daily merge in the relay's lifetime that did not fire the workflow.

**The overwrite is not universal, and the exception class is exactly one thing: the re-fire run failed.** Three of the 40 report `"conclusion":"failure"`:

| Date | Run | Head sha | Conclusion | Commits on that day's reduction |
|---|---|---|---|---|
| 2026-08-06 | 31101427676 | `28bb8f3` (merge #57) | failure | `2026-08-06-daily.json` — **1** |
| 2026-08-19 | 32252025070 | `ccb03e38` (merge #92) | failure | `2026-08-19-daily.json` — **1** |
| 2026-08-31 | 33392951583 | `0c911cc` (merge #115) | failure | `2026-08-31-daily.json` — **1** |

The correspondence is exact and it runs both ways: **every** failed re-fire leaves a single-commit reduction, and **every** single-commit daily reduction whose day had no later-sorting request file is one of those three. This is a validated positive/negative control pair on the mechanism, and it is a stronger statement than the chairman's: the merge always fires the relay; the overwrite lands whenever the runner succeeds. I could not break §5. I strengthened it.

**A second confirming detail he did not use.** `newest_request()` is `sorted(glob('*.json'))[-1]`, lexicographic. The other single-commit reductions — `2026-09-03-daily.json`, `2026-09-04-daily.json`, `2026-08-04-daily.json`, and each `-daily.json` on a day that also carries a `-zz-*` or `-daily2` request — are single-commit precisely because the re-fire answered the **lexicographically later** request file and wrote to a different path. The lexicographic selector is not incidental to the overwrite; it decides which file gets overwritten.

### What I DID break in §5, and it is not the mechanism — it is the novelty claim and the clone.

**BREAK 1 — "the reason it has never been reported" is false, and the counter-example is the thread §5 itself cites.** `state/research_log.json` `open_threads[15]`, opened 2026-08-05, reads: *"On 2026-08-05 this re-ran a finished batch, OVERWROTE the reduction Part 2 cites by run id and sha256 (preserved at commit 946b8b7)"*. The 2026-08-05 daily record carries it at length, with the remedy: *"Required fix: a commit pin, not a correction. Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path"* (`analytics/daily-research/2026-08-05.md:488` at `8015833`), and adopts it as taxonomy entry 25, **MUTABLE-REDUCTION CITATION** (`:573`). The merge commit for that very day announces it in its own message: *"commit 946b8b7 must stay reachable from main because Part 2 cites a relay reduction that a later run overwrote in place"* (head commit of run 31004391399). The overwrite was reported five weeks ago, named, taxonomised and remediated. What is genuinely new on 2026-09-10 is narrower and should be stated as the narrower thing: **the identification of the ordinary daily merge as the recurring trigger**, where 2026-08-05 attributed it to a mis-sorted second batch. That is worth the opening slot. "Never been reported" is not.

**BREAK 2 — BLOCKING. §5 is a history-derived finding produced in a shallow clone, in breach of a rule this project adopted specifically to stop that, and the breach is why he had to write "presumably".** `git rev-parse --is-shallow-repository` returned `true` at session open, with `.git/shallow` holding **seven** graft boundaries; `git rev-list --count origin/main` returned **142**. After `git fetch --deepen=400 origin main` it returns **1113**. Everything before 2026-09-03 was invisible. `STATE_OF_THE_ANSWER.md:41` at `8015833` states the rule: *"Binding from today: `git rev-parse --is-shallow-repository` before any history-derived count, and unshallow if true; otherwise the count is stated as "over the N commits this clone holds from `<TIP>`" and never as "committed" or "ever""* — carried as escalation `N14`, with the added sentence *"no seat screened for it before adopting the countermeasure it defeats."* §5 reports commit counts, an ancestry ordering and a lifetime-scope generalisation without the screen and without the scope qualifier. This is not a quibble about form: in the shallow clone the relay appears to have been created on 2026-09-03 and the double-commit pattern appears to begin then, both of which are boundary artefacts, and the true creation date is 2026-08-03. The presumption was unnecessary — the answer was one `--deepen` away, git egress is alive in this session, and I retrieved it inside one command.

### Two facts about the instrument that fall out of today's rows and are worth carrying

**The ICSID `/cases/recent` body is byte-stable within a run and unstable across days.** Today rows 1 and 2 are two separate fetches of the same URL seconds apart and return the same `sha256 470b8878…` at the same `bytes: 362310`. Yesterday's two runs, 37 minutes apart, both returned `f309eb63`. Across days the digests differ: `4af84337` (09-07), `f309eb63` (09-09), `470b8878` (09-10). The 2026-09-09 ledger entry already records unequal digests at **equal** byte length on this host; today adds unequal length as well, and adds the intra-run identity that makes the day-scale differences look like page-state change rather than per-request variation. **This is a statement about digests and lengths. It is not a statement that any case was added, removed or altered, and no seat may read it as one.**

**The head control's `find_matched` is `false` and this is not a control failure.** `records[0]` carries `"find": ""` and `find_matched` is `bool(find) and …`, so an empty `find` forces `false` by construction. `control_ok` is computed from `outcome == "ok"` and is `true`. A future reader scanning the `find_matched` column will see the run's first row as a miss; it is not one.

---

## TASK 3 — the research question

**Null, and structurally so.** The three-ring intersection — IP (centrally trade secrets and clinical-trial data) as covered investment; a regulatory or judicial measure as the challenged conduct; a live, litigated jurisdictional or admissibility doctrine — received **no new development in any channel available to this session**, because no channel was available. Fetch is dead (Part I §2, neutral controls). Search is dead (my three probes above, neutral control). The relay's two research rows were fixed before I was convened and returned a positive control and an uninformative null.

**My standing rule was applied before any budget was spent, and it returned "do not spend".** `git grep -il` at commit `8015833`: `Wingtech` 51 files, `Bridgestone` 47, `Eli Lilly` 46, `clinical trial data` 38, `abuse of right` 31 (positive control: `ISDS` 168 files). Every designator in the theme is held in far more than two files, so the rule redirects me from the matter to a thread's stated closure condition. The candidate closure conditions all failed a spend test before search died:

- `open_threads[5]` / `open_threads[20]` (`wingtech-netherlands-icsid-registration-not-found`): the monthly standing watch was **run on 2026-09-08**, site-restricted across icsid, italaw, UNCTAD, Jus Mundi and IAReporter, and returned no proceeding. Re-running it 48 hours into a monthly cadence buys nothing, and today's relay row already probed the same designator on the primary host. **Correctly not spent.**
- `open_threads[6]` — codified manifest-lack-of-merit gates (2026 ICC Art. 30; ICSID AF Rule 51(1)) and whether any produces a disposition touching IP-as-investment, as distinct from abuse-of-right admissibility — is the one on-theme, un-escalated, budget-eligible thread with genuine headroom (`Rule 51(1)` 12 files, `manifest lack of merit` 5). It **has no stated closure condition**, which is itself a defect: my own rule cannot aim at a condition that was never written. I would have spent there. Search died first.

**`open_threads[5]`'s monitoring window is 67 days**, computed today as 2026-09-10 minus the thread's earliest carrier 2026-07-05, and it is **not** a post-cooling-off day count — the conflation struck yesterday as C-2 and corrected into the thread itself under 0909-N3. I restate the figure because the record requires it re-derived rather than quoted, not because anything moved it.

**The monitored-author check did not run.** Anthea Roberts, `ejiltalk.org/author/aroberts/`, is a per-session standing check and it was my first spend. It returned a tool error, twice, and the neutral control confirms the channel and not the author page is what failed. Nothing whatever is asserted about whether Roberts has posted.

**Standing items honoured at zero budget:** `open_threads[0]` stays CLOSED. `spanish-drugmaker-icsid-ecuador-ring1-not-assessed` stays CONVERTED to an operator manual query, closure condition unchanged and unwidened at "an ICSID case number and nothing else", paywalled-body synthesis QUARANTINED and reasoned from by no part of this return. The Einarsson family stays retired. The three escalated gaps received no budget.

**No new research insight; standing watch.** What I checked: the three-ring intersection against `STATE_OF_THE_ANSWER.md` and the newest `analytics/insights.jsonl` entries; the twenty-three open threads for one with a spendable closure condition; today's reduction rows 1–3 against the reading rule; and the search channel three times. Nothing cleared the bar for a Ring-1/2/3 insight, and I will not pad the ledger with a restatement. The two machinery findings below are new and are offered as machinery, not as research.

---

## Persistent-gap markers

`GAP-UNRESOLVED: dailyjus-ai-subject-matter-jurisdiction-body-unread` — UNCHANGED. dailyjus.com is off the relay allowlist; search dead; no budget could reach it today.

`GAP-UNRESOLVED: uncitral-59th-session-press-release-body-unread` — UNCHANGED. unis.unvienna.org off-allowlist; escalated as a perimeter decision; zero budget.

`GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found` — UNCHANGED AND EXPRESSLY NOT ADVANCED BY TODAY'S RELAY ROW. Today's row 2 null licenses only a statement about one literal in one rendering of one body at one instant. An ICSID registration remains **not found in accessible sources**; it is never stated that none exists.

`GAP-UNRESOLVED: spanish-drugmaker-icsid-ecuador-ring1-not-assessed` — UNCHANGED, CONVERTED, quarantined, zero budget.

`GAP-UNRESOLVED: china-rdp-measures-primary-text` (alias `china-drug-rdp-measures-2026-exclusivity-terms`) — UNCHANGED; structurally unreachable, operator-side only.

`GAP-UNRESOLVED: einarsson-unct-20-6-expert-identities` — UNCHANGED; family retired, no budget.

`GAP-UNRESOLVED: icsid-arb-12-1-c-revision-proceeding-status-and-outcome` — UNCHANGED.

`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` — UNCHANGED.

`GAP-UNRESOLVED: china-france-bit-2007-protocol-text` — UNCHANGED.

I open **no** new slug for the Roberts check. Its failure was a channel status, not a named-document gap, and minting a dated slug for a tool outage would corrupt a counter that escalates on document inaccessibility.

---

## Challenge to the chair

**0910-A1 — BLOCKING. §5 is a history-derived finding computed in a shallow clone, in breach of the binding N14 rule at `STATE_OF_THE_ANSWER.md:41` (commit `8015833`), and the breach is the direct cause of its weakest sentence.** Evidence: `is-shallow-repository` → `true` at session open, seven grafts, 142 commits reachable from `origin/main`; after `git fetch --deepen=400 origin main`, 1113. The rule quoted verbatim above requires the screen *"before any history-derived count, and unshallow if true"*. §5 reports commit counts and an ancestry ordering, and generalises to "as long as the relay has existed", without the screen and without the fallback scope qualifier the rule prescribes. In the un-deepened clone `fetch-relay.yml` appears to originate on 2026-09-03; it originates at `fe02f39` on 2026-08-03. Remedy: state §5's lifetime claim over the deepened graph, and record that git egress is alive so no future session need presume.

**0910-A2 — BLOCKING. "the reason it has never been reported" is false and fails a screen the chair had the instrument to run.** It was reported on 2026-08-05, in `open_threads[15]` defect (1) — the same thread §5 cites — with the pin `946b8b7`, the taxonomy entry 25 *MUTABLE-REDUCTION CITATION*, and the countermeasure *"every citation to a reduction carries its commit sha"*. Screen at `8015833`: `946b8b7` appears in `analytics/council-log.md`, `analytics/daily-research/2026-08-05.md` (13), `2026-08-06.md` (10), `2026-08-21.md`, `state/research_log.json` (2); the phrase family *overwrote/overwritten/in-place overwrite* appears in 30+ files, positive control non-zero throughout. Remedy: narrow the novelty claim to what is actually new — **the daily merge identified as the recurring trigger**, superseding 2026-08-05's mis-sorted-batch attribution — and drop "never been reported".

**0910-A3 — NON-BLOCKING, and it cuts in the chair's favour. "Presumably" understates what he could have shown.** The claim is not merely probable; it is verified across the workflow's whole lifetime: 40 `main`/`push` runs, all `run_attempt: 1`, one per daily merge, with exactly three failures (runs 31101427676, 32252025070, 33392951583 on 08-06, 08-19, 08-31) corresponding one-to-one with the three single-commit daily reductions. The mechanism is stronger than stated, and the exception class is "the runner failed", not "the bytes matched". His stated reason for the silence — "the second run answers the same request and usually returns the same bytes" — is not the operative reason for any of the three exceptions.

**0910-A4 — NON-BLOCKING. §2 asserts a channel posture it declined to test, and the untested half was wrong.** "Web search is alive on the record's evidence" is a claim about a channel supported by yesterday's evidence, in a record that elsewhere enforces the rule that a tool status is never carried forward as a state. It is dead today on a neutral control. No harm followed because he deferred the assertion to me; the drafting still carries a live status forward, which is the failure mode this council names most often.

**0910-A5 — NON-BLOCKING. F4 was pre-registered without a falsification condition, and one limb of it can fail independently.** The forecast conjoins "will be overwritten in place" with "the substantive sha256 stays equal". The second limb is a prediction about ICSID's server, not about the workflow, and today's own reduction shows the digest on this URL changing day over day. A conjunctive forecast graded as one unit will score CONFIRMED or FALSIFIED for the wrong reason. Remedy: grade limbs 1–3 and limb 4 separately, per the five conditions I set out in TASK 1.

**0910-A6 — NON-BLOCKING. §4's `ICSID` row list mixes URLs under one heading.** Six rows are correct; one of the six (`2026-09-06-daily.json` record 11) is at `/cases/news-events`, not `/cases/recent`. The "most recent four" gloss is accurate; the heading is skimmable into an error.

**0910-A7 — NON-BLOCKING, addressed to the register rather than to the chair. `open_threads[6]` has no stated closure condition.** It is the only on-theme, un-escalated, budget-eligible thread with search headroom, and the analyst's own aiming rule cannot be applied to it. It should be given one before the next session that has a live search channel.

---

## Reported against myself

**Self-1.** I ran history-derived counts — per-file commit counts across the whole reduction corpus, and the claim that the double-commit pattern begins 2026-09-03 — **before** checking `is-shallow-repository`. Both were shallow-boundary artefacts and both were wrong. I caught it only because a parentless commit dated 2026-09-05 that appeared to add the entire workflow tree made no sense. This is the same N14 failure I charge the chair with in 0910-A1, committed in the same session, and the charge stands against me equally. The corrected figures above are all computed post-deepen.

**Self-2.** I argued that a 32-second merge-to-commit interval was "implausibly fast" and therefore evidence **against** the chairman's causation. It was not evidence of anything. The run record gives 33 s wall clock and the same interval recurs on seven consecutive daily merges. I substituted plausibility for the run metadata that was one API call away, in a session whose entire method is that plausibility is not evidence. Withdrawn in full.

**Self-3.** I spent all three units of budget on the search channel and returned none. I record this as a spend, not as a saving: the first probe was mandated, the second isolated the domain filter, and the third supplied the neutral control that makes the finding usable — but the result is that TASK 3 had no budget left even if a channel had existed. Given the outcome I would spend the same way again; a reader should still see that the theme got zero.

---

## Budget spent, itemised

| # | Unit | Query | Purpose | Outcome |
|---|---|---|---|---|
| 1 | web search | `Anthea Roberts EJIL:Talk! September 2026` (`allowed_domains: ejiltalk.org`) | monitored-author standing check; doubles as the §2 search probe | tool error: `unavailable` |
| 2 | web search | `Anthea Roberts EJIL Talk UNCITRAL ISDS reform` (unrestricted) | isolate the domain filter as a cause | tool error: `unavailable` |
| 3 | web search | `example domain` (unrestricted, **neutral control**) | attribute the failure to the channel, not to query or subject | tool error: `unavailable` |

**3 of 3 spent, 0 returned, 0 reaching the research question.** Relay rows: 3 requested, 3 completed, 4 records — all fixed before I was convened. Zero budget touched `open_threads[0]`, the Ecuador slug, the Einarsson family, or the three escalated gaps. Non-budget instruments used without limit, as permitted: `git` (including one `--deepen`, which is how 0910-A1 was found), Python aggregation over the extracted `fetch-results` subtree, and the GitHub Actions run API, which supplied every decisive fact in the §5 audit.

---

## Candidate claims

Scope note, binding on this block and not only on my prose: **no case number, party, tribunal or matter appearing in any excerpt of the 2026-09-10 reduction is entered here as a discovered matter, screened, graded on any ring, or carried as a lead.** No claim below asserts anything about any resource; each is a claim about a workflow run, a repository object, or a code path. The search-channel failure is deliberately **not** proposed as a candidate claim: it has no source URL, and under the contract no URL means no claim.

```json
{"candidate_claims": [
  {"claim_text": "GitHub Actions run 34348169510 of .github/workflows/fetch-relay.yml in jackemorywilliams-bit/isds-watcher was triggered by a push event whose head_sha was abeeb5b0642d171899e9c85469f8fd72b0e1597b on branch main, on its first attempt.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/runs/34348169510",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"event\":\"push\",\"status\":\"completed\",\"conclusion\":\"success\",\"head_branch\":\"main\",\"head_sha\":\"abeeb5b0642d171899e9c85469f8fd72b0e1597b\"",
   "supporting_locator": "GitHub REST API, GET /repos/jackemorywilliams-bit/isds-watcher/actions/runs/34348169510; fields event, head_branch, head_sha, run_attempt"},

  {"claim_text": "The two 2026-09-09 fetch-relay runs on the isds-watcher repository are distinct runs on first attempt, not two attempts of one run: run 34344935760 has head_branch council/2026-09-09 and run 34348169510 has head_branch main, and both report run_attempt 1.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/runs/34344935760",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"id\":34344935760,\"run_number\":98,\"run_attempt\":1,\"event\":\"push\",\"head_branch\":\"council/2026-09-09\"",
   "supporting_locator": "GitHub REST API run records 34344935760 and 34348169510; field run_attempt"},

  {"claim_text": "Across the complete population of fetch-relay.yml workflow runs on branch main in jackemorywilliams-bit/isds-watcher (40 runs, 2026-08-03 to 2026-09-09), every run reports event push and run_attempt 1, and exactly three report conclusion failure: 31101427676 (2026-08-06), 32252025070 (2026-08-19) and 33392951583 (2026-08-31).",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"total_count\":40",
   "supporting_locator": "GitHub REST API, GET /repos/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml/runs?branch=main&event=push; per-run conclusion fields"},

  {"claim_text": "Each of the three fetch-relay reductions whose post-merge re-fire run failed (analytics/fetch-results/2026-08-06-daily.json, 2026-08-19-daily.json and 2026-08-31-daily.json) carries exactly one commit in the repository's full history, whereas every other daily reduction whose day had no later-sorting request file carries two.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/tree/main/analytics/fetch-results",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "git log --format='%H' origin/main -- <path>, run per file over the un-shallowed clone (1113 commits reachable from origin/main); counts enumerated by an aggregating loop, not read off a display"},

  {"claim_text": "The fetch-relay workflow file .github/workflows/fetch-relay.yml was created in jackemorywilliams-bit/isds-watcher at commit fe02f39 on 2026-08-03, not on 2026-09-03; the later date is an artefact of the shallow clone the scheduled session receives.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/commits/main/.github/workflows/fetch-relay.yml",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "feat(relay): fetch relay for the network-less council session",
   "supporting_locator": "git log --format='%h %ci %s' origin/main -- .github/workflows/fetch-relay.yml, after git fetch --deepen=400 origin main; before deepening the same command returns only 022d117 (2026-09-03)"},

  {"claim_text": "The council record reported the fetch-relay in-place overwrite of a cited reduction on 2026-08-05, before 2026-09-10, and adopted a commit-pin remedy and a named taxonomy entry for it.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/main/analytics/daily-research/2026-08-05.md",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Required fix: a commit pin, not a correction. Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path.",
   "supporting_locator": "analytics/daily-research/2026-08-05.md:488, resolving at commit 8015833; taxonomy entry 25 MUTABLE-REDUCTION CITATION at :573"},

  {"claim_text": "The 2026-09-10 fetch-relay reduction (blob 91186e69, commit 8015833) answered request file 2026-09-10-daily.json under run 34470490415, carries 4 records with requested 3 and completed 3, control_ok true and void false.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/council/2026-09-10/analytics/fetch-results/2026-09-10-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"request\": \"2026-09-10-daily.json\"",
   "supporting_locator": "blob 91186e69eb34e70e34d7f67001f6aaa4b4f85ada at commit 8015833; fields request, environment, requested, completed, control_ok, void"},

  {"claim_text": "In the 2026-09-10 fetch-relay run, two separate fetches of https://icsid.worldbank.org/cases/recent returned bodies of identical length (362310 bytes) and identical sha256 (470b8878...), while the sha256 recorded for the same URL on 2026-09-09 (f309eb63...) and 2026-09-07 (4af84337...) differ from it and from each other.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/council/2026-09-10/analytics/fetch-results/2026-09-10-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"bytes\": 362310, \"sha256\": \"470b88788b480bd032d7f5d8b117d67dadf3e4fe6df66fb6bd8041721f54e3ed\"",
   "supporting_locator": "blob 91186e69 records[1] and records[2]; compared against blob 72468ae1 (commit d34b9ec) records[1] and blob 4af84337-bearing rows in 2026-09-07-daily.json at commit 6df6114. This is a claim about digests and byte lengths only and asserts nothing about the content, composition or currency of any case listing."},

  {"claim_text": "The head control row of a fetch_relay.py reduction always reports find_matched false because find_matched is computed as bool(find) and the head control is fetched with an empty find string, so a false value in that row is not evidence of control failure.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/main/scripts/fetch_relay.py",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"find_matched\": bool(find) and bool(",
   "supporting_locator": "scripts/fetch_relay.py, fetch_one(), find_matched assignment, resolving at commit 8015833; control_ok is computed separately as records[0][\"outcome\"] == \"ok\" in run()"},

  {"claim_text": "The clone of jackemorywilliams-bit/isds-watcher delivered to the 2026-09-10 scheduled session arrived shallow, with seven graft boundaries and 142 commits reachable from origin/main, and git fetch --deepen=400 origin main succeeded from within that session, yielding 1113.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "git rev-parse --is-shallow-repository (true, then false); wc -l on .git/shallow (7); git rev-list --count origin/main (142, then 1113). Bears on the binding rule at STATE_OF_THE_ANSWER.md:41, resolving at commit 8015833."}
]}
```

---

# Part V — The council discussion, 2026-09-10

**How this Part is constructed, stated plainly because it is a declared departure.** Rule 2 of
`prompts/daily_council_protocol.md` requires the daily discussion to be a real, attributed,
multi-turn dialogue in which the integrity voice challenges at least one concrete assertion and
**the challenged party answers on the record**. Rule 0 says the integrity challenge is a voice
inside this Part rather than a separately convened seat. **The operator's cardinal rule forbids
me performing any seat**, so the integrity voice here is a real subagent's verbatim return,
placed inside this Part rather than given a Part of its own, and the analyst's answers are the
analyst's own — I put the officer's objections back to the seat that had to answer them and did
not write a word of the reply. Two subagents, one dialogue, nothing performed.

**Model, as the seat reported it unasked on its own first line:** `claude-opus-5`, **against a
directive of Claude Opus 4.8.** This is the twenty-something consecutive session on which this
seat has disclosed running off-directive, and I state the count in the enumerated form this
council now requires rather than as a bare ordinal: the discrepancy is escalation 3 and its day
count is given there with its basis. The seat also established something new about its own
definition today, and it cuts against the file rather than against the seat:
`.claude/agents/integrity-officer.md:4` (at `8015833`) pins `model: opus` — **a tier, not a
version** — while the same file's `description` at `:3` asserts in prose *"Runs on Claude Opus
4.8."* **The file's selector and the file's own description disagree with each other, and
neither is enforced.** That is a sharper statement of escalation 3 than the council has had.

---

## The integrity officer's vetting return, entered VERBATIM

The same transport un-escaping disclosed at Part II applies here (`&lt;` → `<`, `&gt;` → `>`,
`&amp;` → `&`); no other character is changed.

**Model actually running this seat: `claude-opus-5` (self-reported as "Opus 5"; exact model ID `claude-opus-5`, from this session's own environment block).** The operator's directive puts this seat on **Claude Opus 4.8**, and `.claude/agents/integrity-officer.md` at commit `8015833` reads `model: opus` at **:4** (a TIER selector) while its own `description` at **:3** asserts *"Runs on Claude Opus 4.8."* The observed value matches neither the directive nor the file's prose, and nothing in the harness enforces either. **This is escalation 3 to the operator, restated for the Nth consecutive session; it is a discrepancy, not a compliance finding, and I do not substitute silently.**

# VETTING VERDICT: **FLAGGED** — 4 blocking, 10 non-blocking.

Both Part I and Part II are substantially sound and unusually well evidenced. The four blocking objections are: one overreach in the chairman's headline finding that neither seat caught; one mis-identified file inside the analyst's strengthening of that same finding, which propagates into the candidate-claims block; and two defects in the candidate-claims block itself (a self-answering locator and a stitched quotation) that would enter Emory's ledger uncorrected.

---

## BLOCKING

### `0910-B1` — Part I §5's universal is falsified in 10 of 37 cases, and neither seat filed it. The analyst supplied the mechanism that breaks it and classified it as confirming.

§5 asserts, in bold, as the sentence that earns the finding its opening slot: *"**Every daily session that merges a request file to `main` re-fires its own batch and overwrites its own evidence.**"*

**Instrument:** `git ls-tree -r --name-only 6df6114 analytics/fetch-results/` (37 files matching `*-daily.json`), each counted with `git log --format=%H origin/main -- <path>` over the **deepened** graph (`git rev-parse --is-shallow-repository` → `false`, `git rev-list --count origin/main` → 1113, checked before the count).

**Result: 10 of the 37 daily reductions carry exactly ONE commit and were never overwritten** — `2026-08-04`, `08-06`, `08-11`, `08-13`, `08-19`, `08-20`, `08-21`, `08-24`, `08-31`, `09-04`. Two independent causes, both of which the record already contains:

1. **The runner must succeed.** 3 of the 40 `main` runs failed (below), so no re-fire wrote anything on 08-06, 08-19, 08-31.
2. **`newest_request()` is lexicographic** (`scripts/fetch_relay.py:234` at `6df6114`: `files = sorted(p for p in directory.glob("*.json"))`, `return files[-1]` at `:235`). On any day carrying a later-sorting request file, the merge re-fire answers **that** request and overwrites **that** reduction, leaving the daily one untouched. Worked pair: on 2026-08-20 the merge run (created 12:13:47Z) gave `2026-08-20-zz-column-and-middle.json` its second commit at 12:14:21Z, while `2026-08-20-daily.json` still carries one commit, from 11:20:24Z.

The chairman had the second cause available — it is defect (1) of `open_threads[15]`, the thread §5 itself cites — and the analyst restated it as *"A second confirming detail he did not use."* **It is not a confirming detail; it is the limit on the universal.** The mechanism §5 identifies is real and is worth the opening slot. The quantifier is not.

**Remedy (required before merge):** replace the universal with its true form — *"Every daily session that merges a request file to `main` re-fires the relay. The re-fire overwrites a reduction whenever the runner succeeds, and it overwrites **the lexicographically last-sorting request file's** reduction, which is the daily one only on days that carry no other request file. Over the 37 `*-daily.json` reductions in the tree at `6df6114`, 10 were never overwritten."* Nothing else in §5 needs to move.

### `0910-B2` — the analyst's three-failure correspondence names the wrong file for 2026-08-19; the correspondence is real but one of its three members is confounded.

Part II asserts: *"**every** failed re-fire leaves a single-commit reduction, and **every** single-commit daily reduction whose day had no later-sorting request file is one of those three"*, with a table row `2026-08-19 | 32252025070 | ccb03e38 (merge #92) | failure | 2026-08-19-daily.json — 1`.

**Evidence.** 2026-08-19 carries **two** request files at `origin/main`: `analytics/fetch-requests/2026-08-19-daily.json` and `analytics/fetch-requests/2026-08-19-zz-render-and-cells.json`. `zz-` sorts last, so the merge re-fire targeted the **zz** request, not the daily one. Commit histories:

- `2026-08-19-daily.json` — 1 commit, `1138dcd` 11:20:09Z (branch push). **Normal** for a day with a later-sorting request: identical to 08-20, 08-21, 08-24, where the re-fire *succeeded*.
- `2026-08-19-zz-render-and-cells.json` — 1 commit, `b185fa8` 11:41:56Z. **Anomalous**: the analogous zz/second-batch reductions on 08-20, 08-21, 08-24, 08-13 all carry 2.

So `2026-08-19-daily.json` is **not** evidence of the failed re-fire; `2026-08-19-zz-render-and-cells.json` is. The analyst's second clause also cannot reach 08-19 — that day *did* have a later-sorting request file, so 08-19 contributes nothing to the "runs both ways" direction as he framed it, which then holds on only 2 of its 3 members.

**I checked the reverse direction over the whole corpus rather than over `*-daily.json` only, and the correspondence survives in corrected form:** the only three reductions in the tree at `6df6114` whose single commit is *not* explained by the lexicographic selector are `2026-08-06-daily.json`, `2026-08-19-zz-render-and-cells.json` and `2026-08-31-daily.json` — exactly one per failed run. **The analyst's conclusion is right and I strengthen it; the file he names on one row is wrong, and it is wrong in the direction that makes the correspondence look tighter than his own instrument showed.**

**Remedy:** correct the 08-19 row to `2026-08-19-zz-render-and-cells.json` (1 commit, against 2 on 08-20/08-21/08-24), state the second direction over *all* reductions rather than `*-daily.json`, and correct candidate claim 4 in the same terms (see QUARANTINE).

### `0910-B3` — candidate claim 3's cited locator is self-answering: it queries `event=push` and reports as a finding that every run reports `event push`. Taxonomy entry 26.

Claim 3 reads *"…every run reports event push and run_attempt 1…"* with `supporting_locator: "GET /repos/.../actions/workflows/fetch-relay.yml/runs?branch=main&event=push"`. The `event=push` filter guarantees the first limb. The same defect sinks the §5 audit's step **(a)**: *"Across the full population of `fetch-relay.yml` runs on `main` — 40 runs, `event=push` — there is no dispatch-triggered run on `main` in the workflow's lifetime."* A push-filtered query cannot exclude a dispatch, and `.github/workflows/fetch-relay.yml` at `6df6114` **does** configure `workflow_dispatch: {}` (line following the `paths:` block), so the alternative was live and not merely theoretical.

**I ran the positive control the objection requires before filing it.** `mcp__github__actions_list(list_workflow_runs, fetch-relay.yml, filter {branch: "main"})` — **no event filter** — returns `total_count: 40`, and all 40 returned records carry `"event":"push"` and `"run_attempt":1`. **The conclusion survives; the instrument that was cited for it does not.** Same query independently confirms the three failures and nothing else: `33392951583` (08-31, head_sha `0c911cc`), `32252025070` (08-19, head_sha `ccb03e38`), `31101427676` (08-06, head_sha `28bb8f3`), earliest run 2026-08-03T18:24:26Z, latest 2026-09-09T11:56:13Z.

**Remedy:** restate the locator as `?branch=main` with no event filter, and restate (a) as *"the unfiltered `branch=main` population is 40 runs and every one reports `event: push`, which is what excludes a dispatch."* The count and the three ids stand unchanged.

### `0910-B4` — candidate claim 2's `supporting_quote` is a stitched string presented as verbatim JSON. Quote integrity.

Claim 2 offers `"id":34344935760,"run_number":98,"run_attempt":1,"event":"push","head_branch":"council/2026-09-09"` as a supporting quote. The API response for run `34344935760`, which I retrieved (`actions_get(get_workflow_run, 34344935760)`), orders its fields `id, name, display_title, workflow_id, run_number, run_attempt, event, status, conclusion, head_branch, head_sha, …`. `"id":34344935760` is followed by `"name":"fetch-relay"`, not by `"run_number"`; `"event":"push"` is followed by `"status":"completed"`, not by `"head_branch"`. **Two elisions, unmarked, inside a string offered as a character-exact quotation from a primary source.** Every field asserted is true — I verified each — which is exactly what makes this the seat's own taxonomy 8/quote-integrity family rather than a fabrication: the substance is sound and the characters are manufactured.

By contrast claim 1's quote (`"event":"push","status":"completed","conclusion":"success","head_branch":"main","head_sha":"abeeb5b…"`) **is** a contiguous substring and is clean.

**Remedy:** either mark the elisions (` … `) or replace with a contiguous run, e.g. `"run_number":98,"run_attempt":1,"event":"push"`. Do not merge the block with a stitched quote in it.

---

## NON-BLOCKING

**`0910-N1` — the chairman's single-line code pinpoint `scripts/fetch_relay.py:198` lands on a blank line; the prepend is at `:199`.** Part I §6 asserts *"`run()` at `scripts/fetch_relay.py:198` prepends `fetch_one(CONTROL_URL)` as `records[0]`"*, and the same wording is frozen in the pre-registered request note (blob `129e83b9` at `c53c97e`). At `6df6114`, `:198` is empty; `records = [fetch_one(CONTROL_URL)]` is at **`:199`**, the per-target append at `:203`, `completed` at `:221` (correct as cited). The standing rule is that a claim about what code *does* cites the executable line; taxonomy 17a is the mis-located variant. F3's range citation `:198-207` is merely loose (it opens on the blank line); the bare `:198` is wrong. Remedy: `:199`, `:203`, `:221`, all at `6df6114`.

**`0910-N2` — "tenth consecutive session" is a bare ordinal, in the same document that enumerates the Monday counter three sections later.** Part I §1. The standing rule is that a periodic counter is stated with its enumerated members. The record's own precedent is directly on point: `analytics/daily-research/2026-09-06.md:1191` **withdrew** an ordinal streak count on the ground that *"The ordinal series is used across several unrelated streaks in this record (Rule 0 departures, the overwrite defect, the digest), so an ordinal alone establishes nothing about *this* streak."* **I ran the enumeration and the chain holds** (see below), so this is form, not error. Remedy: `— tenth consecutive session: 09-01, 09-02, 09-03, 09-04, 09-05, 09-06, 09-07, 09-08, 09-09, 09-10.`

**`0910-N3` — F4's first-ness claim was made without the screen taxonomy entry 28 requires; I ran the screen and the claim survives.** §6 F4 and the frozen request note both assert *"This is the first time this council has pre-registered the overwrite instead of discovering it afterwards"* with no literals, no synonyms, no file count, no scope, no commit. Entry 28's mechanical carrier makes that non-negotiable. **Screen, supplied here so the remedy costs nothing:** `git grep -i -l -F` over the whole tree at `6df6114` (`git ls-tree -r`, no path argument — 661 files): `pre-registered the overwrite` 0, `pre-register the overwrite` 0, `pre-registering the overwrite` 0, `predicted the overwrite` 0, `forecast the overwrite` 0; co-occurrence of `overwrit` and `pre-regist` in one file, 91 files, none carrying the proposition. **Positive controls fired:** `pre-registered` 127 files, `overwrote the reduction` 3 files. The claim is true. Attach the carrier.

**`0910-N4` — the analyst's TASK 3 screen validates its instrument at a different scope from the one that produced its figures. Taxonomy 27, scope-mixed screen.** He reports *"`git grep -il` at commit `8015833`: `Wingtech` 51 files, `Bridgestone` 47, `Eli Lilly` 46, `clinical trial data` 38, `abuse of right` 31 (positive control: `ISDS` 168 files)."* I could not reproduce that set under any single scope, so I searched for the scopes that do:

- `git grep -il <t> 8015833 -- '*.md' '*.json' '*.jsonl'` reproduces **every screened term exactly**: 51 / 47 / 46 / 38 / 31, and `Rule 51(1)` 12, `manifest lack of merit` 5. At that scope **`ISDS` is 253 files, not 168.**
- `git grep -il ISDS 8015833 -- '*.md'` returns **exactly 168** — the control, and only the control, was run over `*.md` alone.
- Unrestricted at the same commit: `ISDS` 410, `Wingtech` 57, `Bridgestone` 83.

The budget decision is robust — every term clears "more than two files" at every scope I tried — so nothing substantive moves. But the printed control does not validate the instrument that produced the printed counts, and the figures are unreproducible against the command as stated. Remedy: name the pathspec, and give the control at the screen's own scope (`ISDS` 253).

**`0910-N5` — the analyst's `0910-A2` screen under-enumerates the carrier class it reports. Taxonomy 14.** He lists `946b8b7` as appearing in *"`analytics/council-log.md`, `analytics/daily-research/2026-08-05.md` (13), `2026-08-06.md` (10), `2026-08-21.md`, `state/research_log.json` (2)"* — five files. `git grep -c -F 946b8b7 8015833` returns **six**: those five plus **`analytics/insights.jsonl` (2)**. The objection is unaffected and is sustained on the merits; the enumerated class is short by one member.

**`0910-N6` — `0910-A6` mis-states the very URL it exists to correct.** A6 says the sixth `ICSID` row is at `/cases/news-events`. It is at `https://icsid.worldbank.org/news-events`. His TASK 2 prose states it correctly; the objection text does not. Confirmed from my own enumeration: `2026-09-06-daily.json` record 11, `find: ICSID`, `find_matched: true`, sha `b0a54b0c`.

**`0910-N7` — the intra-run byte-identity inference is one time point reported as if it discriminated. Taxonomy 21, adopted 2026-08-04 against precisely this.** Part II: *"today adds … the intra-run identity that makes the day-scale differences look like page-state change rather than per-request variation."* Rows 1 and 2 of blob `91186e69` are two rows of **one** fetch cycle, seconds apart — n=1 time point, not two observations. Entry 21's countermeasure is *count time points, not rows*. His **other** limb is sound and I credit it: yesterday's two runs are 37 minutes apart at two genuine time points and both returned `f309eb63` (verified: blobs `72468ae1`@`d34b9ec` and `da60c599`@`72adf10`, all six `sha256` identical). Candidate claim 8 is correctly hedged and needs no change; the prose sentence does. Remedy: *"one intra-run pair at one time point is consistent with, and does not establish, the absence of per-request variation."*

**`0910-N8` — the 33–53 s interval series is unreproducible as labelled and is internally inconsistent by one second within its own paragraph.** He gives run `34348169510` as *"33 s wall clock"* from `run_started_at` 11:56:13Z / `updated_at` 11:56:46Z (I confirm both), then lists *"09-09 32 s"* two sentences later. The series is labelled *"merge-to-commit interval"* but the worked example is run-record wall clock; recomputing `created_at`→`updated_at` from the run population gives 09-03 33 s, 09-04 40 s, 09-05 52 s, 09-06 51 s, 09-07 54 s, 09-08 34 s, 09-09 33 s — near his figures but not equal to any of them. The point (regularity) stands. Remedy: name which two timestamps the series subtracts.

**`0910-N9` — the pre-deepen figures are now sole-source and unwitnessable, and the record should say so.** `git rev-parse --is-shallow-repository` → `false` in this session, `.git/shallow` absent, `git rev-list --count origin/main` → **1113** (I confirm the post-deepen figure). The pre-deepen figures — `true`, seven grafts, 142 commits — were destroyed by the remedy that fixed them and rest on the analyst's single report. That is not a defect in his conduct; it is a property of the evidence, and candidate claim 10 should carry a sole-source note rather than reading as jointly witnessed.

**`0910-N10` — `0910-A4` is right and understated: §2 also asserts a live capability from a channel the record merely showed *available*.** Taxonomy 12, capability-as-corroboration, is the closer fit than taxonomy 7 for the sentence *"Web search is alive on the record's evidence."* The chairman's saving grace is real — he declined to assert it and deferred the probe — but the drafting counts a channel that was working rather than a channel the record shows was used, which is entry 12's exact formulation.

---

## Rulings on the analyst's objections

**`0910-A1` — MADE OUT. Sustain, blocking. The chairman is right to sustain it.** The rule is at `STATE_OF_THE_ANSWER.md:41` at `8015833`, and I verified the line resolves there and carries the quoted text verbatim, including *"and no seat screened for it before adopting the countermeasure it defeats."* §5 reports commit counts, an ancestry ordering and a lifetime-scope generalisation with neither the screen nor the fallback qualifier. Two additions the chair should carry: (i) the analyst commits the same breach in the same session and self-files it (Self-1), so the ruling must run against both seats or the record will read as though the rule bound only the chair; (ii) the remedy is already partly executed — the clone is deepened and **stays** deepened in this working tree, which is why my own history-derived figures above are admissible. Record that `git fetch --deepen` succeeded from inside a fetch-dead, search-dead session, so no future seat need presume.

**`0910-A2` — MADE OUT. Sustain, blocking. The chairman is right to sustain it.** Verified independently at the pinpoints given, resolving at `8015833`: `analytics/daily-research/2026-08-05.md:488` reads *"**Required fix: a commit pin, not a correction.** Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path"*, and `:573` opens *"**O1 — Taxonomy extension, entry 25: MUTABLE-REDUCTION CITATION.**"* — both exact. More damaging than the analyst put it: the report is in `state/research_log.json` `open_threads[15]` itself, the thread §5 cites by index, which reads *"On 2026-08-05 this re-ran a finished batch, OVERWROTE the reduction Part 2 cites by run id and sha256 (preserved at commit 946b8b7)"* and closes *"BINDING HOUSE RULE: every citation to a relay reduction carries the commit sha that holds it (integrity taxonomy 25…)"*. **The chairman asserted "never been reported" while citing, in the same sentence, the register entry that reports it.** I note the probable cause, because it should go in the record rather than be left as carelessness: §5 dates the prior instance to *"a mis-named second batch as on 2026-09-03"* — a different, later incident (`2026-09-03-daily.json` and `-daily2.json` carry four commits between them) — so he screened his memory against the wrong date. That is taxonomy 17 territory as well as 28.

**`0910-A3` — MADE OUT IN DIRECTION, DEFECTIVE IN EXECUTION. Sustain in part.** The mechanism is stronger than "presumably" and I confirmed the population from an unfiltered query. But the strengthening as written fails on its 08-19 limb (`0910-B2`) and rests on a self-answering locator (`0910-B3`). Sustain the proposition — *the merge always fires the relay; the overwrite lands when the runner succeeds and the lexicographic selector points at that file* — and reject the two supports as filed.

**`0910-A4` — MADE OUT. Sustain, non-blocking.** With `0910-N10`'s refinement: the sentence carries a live channel status forward *and* counts an available channel as evidence. No harm followed, because he deferred the assertion. I independently witnessed the state change: a fourth probe of the search channel, `example domain`, unrestricted, neutral control, returned `Web search error: unavailable`.

**`0910-A5` — MADE OUT. Sustain, non-blocking, and I would raise its priority.** F4 conjoins a workflow prediction with a prediction about ICSID's server, and today's own rows make the second limb *less* likely to hold than yesterday's did: the `/cases/recent` digest differs on every day observed (`4af84337` 09-07, `f309eb63` 09-09, `470b8878` 09-10) while holding stable across a 37-minute intra-day gap. If today's merge lands more than a few minutes out, limb 4 can fail while 1–3 succeed. His five settling conditions are correct as written and should be adopted verbatim into the next session's agenda; grade limbs 1–3 and limb 4 separately, and say which failed.

**`0910-A6` — MADE OUT as to substance; the objection text is itself wrong.** Six `ICSID` rows is right, the "most recent four" gloss is right, and one of the six is at a different URL — but that URL is `https://icsid.worldbank.org/news-events`, not `/cases/news-events` (`0910-N6`). Sustain with the correction.

**`0910-A7` — MADE OUT. Sustain, non-blocking.** Verified against the register: `state/research_log.json` `open_threads[6]` is the bare string *"Will the spread of codified manifest-lack-of-merit gates (2026 ICC Art. 30; ICSID AF Rule 51(1)) produce any disposition touching IP-as-investment, as distinct from abuse-of-right admissibility?"* — a question with no closure condition, no scope and no cadence. It is the one on-theme, un-escalated, budget-eligible thread with search headroom, and the analyst's own aiming rule is inoperable against it. This is not a drafting nicety: a thread with no closure condition cannot be closed, cannot be aimed at, and accrues monitoring days indefinitely. It should get one before the next session with a live search channel.

---

## What I tried to break and could not

**§4's corpus screen — re-derived from scratch, replicates to the record.** My own Python aggregator over `git cat-file -p 6df6114:<path>` for every file under `analytics/fetch-results/` (subtree `6f0f56a4d08e4f573ffaef83bf4c0a2bc94ceb76`, matching the instrument the chairman names): **52 reductions, 418 records.** `Wingtech` in any field of any record **0**; `Nexperia` **0**; `find == "ICSID"` **6 rows**, all `find_matched: true`, at `2026-09-06`(11)/news-events/`b0a54b0c`, `09-07`(7)/cases-recent/`4af84337`, `09-07`(9)/cases-pending/`ecbd32d6`, `09-08`(3)/case-database/`134c2004`, `09-09`(1)/cases-recent/`f309eb63`, `09-09`(3)/cases-pending/`712196ea`; URL `https://icsid.worldbank.org/cases/recent` **4 rows across 2 reductions**. Every sha the chairman lists is on the row he puts it on. **Zeros validated against a known positive before being trusted, as the standing rule requires: the same any-field instrument returns 109 rows for `example.com` over the same 418** — identical to the analyst's independently derived 109, which is itself a two-seat agreement on the control and not only on the result. This is the best-constructed section in Part I and it is immune to the shallow-clone defect because it is computed over a tree.

**F1, F2 and F3 — graded off the primary artefacts, all three confirmed.** Reduction blob `91186e69eb34e70e34d7f67001f6aaa4b4f85ada` at `8015833`: `request: "2026-09-10-daily.json"`, `environment: "github-actions-runner (run 34470490415)"`, `control_ok true`, `void false`, `len(records) 4`, `requested 3`, `completed 3`. `records[1]` `find: ICSID`, 200, `find_matched: true`; `records[2]` `find: Wingtech`, 200, `excerpt: ""`, `find_matched: false`; `records[3]` tail control `find_matched: true`. Forecast text checked against the committed request blob `129e83b98e8816e272af8e2416d4c61012a42e2d` at `c53c97e`, not against Part I's prose. **F3's mechanism read off the executable lines rather than the note:** `:199`, `:203`, `:221` at `6df6114`. The chairman's self-correction from "2, not 3" to "3, not 4" is right, for the reason he gives.

**Pre-registration verified from the runner side, independently.** `actions_get(get_workflow_run, 34470490415)`: `head_sha c53c97e0d7d25f53597bc197bdd578e8bb697065`, `head_branch council/2026-09-10`, `event push`, `run_attempt 1`, `conclusion success`, `run_started_at 2026-09-10T11:17:13Z`. The forecasts were frozen in a commit GitHub timestamps *before* the run that answered them. This is the strongest single piece of procedural evidence in today's record.

**F4's non-gradability — I tried to grade it and could not, which is the analyst's point.** `git merge-base --is-ancestor origin/council/2026-09-10 origin/main` → false; `origin/main` tips at `6df6114`; `analytics/fetch-results/2026-09-10-daily.json` has exactly one commit, `8015833`. The predicted event has not had its trigger. Refusing to grade it was correct.

**§5's evidentiary table — every figure replicates.** Blob `72468ae1` at `d34b9ec`: run 34344935760, `fetched_at` 11:19:55.176614Z. Blob `da60c599` at `72adf10`: run 34348169510, `fetched_at` 11:56:42.116828Z. Both carry 6 records, `requested`/`completed` 5/5, `control_ok true`, `void false`, and all six `sha256` values are **element-wise identical between the blobs** (`ff67a9d7`, `f309eb63`, `f309eb63`, `712196ea`, `712196ea`, `ff67a9d7`). His conclusion that no figure in yesterday's record is falsified holds on the bytes.

**§5's cost claim — confirmed exactly, and it is the sharpest thing in Part I.** `analytics/insights.jsonl` cites *"Relay run 34344935760"*; the file now at `analytics/fetch-results/2026-09-09-daily.json` reports `github-actions-runner (run 34348169510)`. A reader following the citation to the path gets a different run and no notice. The `2026-09-09` record cites that path 12 times.

**The dispatch rule-out — I attacked the instrument, and the conclusion survived my attack.** See `0910-B3`: unfiltered `branch=main` query returns the same 40 runs, all `event: push`, all `run_attempt: 1`, three failures and no others.

**`fetch-relay.yml`'s creation date.** `git log --format='%h %ci %s' origin/main -- .github/workflows/fetch-relay.yml` over the deepened graph returns exactly one commit: `fe02f39 2026-08-03 14:22:46 -0400 feat(relay): fetch relay for the network-less council session`, adding the workflow, `scripts/fetch_relay.py`, `tests/test_fetch_relay.py`, `analytics/fetch-requests/README.md` and the first request file. The analyst's `supporting_quote` for candidate claim 5 is the commit subject verbatim.

**The egress posture — my own neutral controls, with a positive control on the instrument first.** Before filing anything resting on a tool failure: `curl https://example.com` → `curl: (56) CONNECT tunnel failed, response 403`; `curl https://www.iana.org/help/example-domains` → identical. **Positive control that the instrument and the network stack are alive:** `curl "$HTTPS_PROXY/__agentproxy/status"` returned 200 with a full JSON body, and that body's own `recentRelayFailures` array names **my** two probes at `2026-09-10T11:37:10.560Z` alongside the chairman's at `11:12:52.774Z` / `11:12:53.041Z`, all `kind: "connect_rejected"`, `detail: "gateway answered 403 to CONNECT (policy denial or upstream failure)"`. The refusal is the gateway's. §2's table is exact and its mechanism sentence is exact. **No fetch-layer status in my note is a claim about any resource.**

**Web search — independently witnessed dead on a fourth neutral control.** `WebSearch("example domain")`, unrestricted: `Web search error: unavailable`. Two seats, four probes, two of them neutral controls, same channel status. This is a claim about the search tool and about nothing else.

**§3's digest figures — checked field by field against `digests/2026-09-07_ISDS-Thematic-Watch/meta.json` at `origin/main`.** `screened 30`, `matches 0`, `watch_list_leads 0`, `accepted 0`, `validation_status_only true`; per-source `iareporter_headlines 10`, `pca_press 2`, `google_alerts 1`, `bing_news 1`, and zero from `iisd_itn`, `gmail_scholar`, `italaw`, `icsid`, `unctad_isds`, `gdelt`; `iisd_itn` status string `QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest 2026-04-21…)` verbatim. `git ls-tree origin/main digests/` confirms `2026-09-07_ISDS-Thematic-Watch` is the newest directory.

**The six-Mondays counter — the best-formed counter in the record, and I could not dent it.** Weekdays computed with `date -d`, not read: 2026-08-03, 08-10, 08-17, 08-24, 08-31 and 09-07 are all Mondays; 09-08 Tuesday, 09-09 Wednesday, **09-10 Thursday**; 09-14 is the next Monday, so "no 09-14 digest before Monday" is trivially right. And the enumeration is *complete*, which the chairman did not claim but which holds: `briefs/` carries nothing after `2026-07-27-memo.md`, so those six Mondays are the entire empty run and no seventh member is hiding. It does not increment today. This is the form `0910-N2` asks the Rule 0 counter to take.

**The sent-marker check.** `git ls-tree -r origin/main --name-only analytics/daily-research/.sent/` ends at `2026-09-09`; no `2026-09-10`. `.sent/2026-09-09` was written at `6df6114`, 2026-09-09T11:56:57Z — 47 seconds after merge commit `abeeb5b` (11:56:10Z), and `abeeb5b` is an ancestor of `6df6114`. The output-path repair did work end to end.

**The Rule 0 ordinal — I went looking for a broken chain and found an intact one.** Instrument `git grep -n -i` over `origin/main -- analytics/daily-research/`, with the synonym pass my own standing rule requires (a zero-hit screen is not absence until the synonym is tried) and a positive control (`chairman`: 50/36/31 hits in the three files). `Rule 0 departure` misses on 09-07/08/09, but the synonyms carry it: 09-05 *"fifth consecutive session"* (`:174`), 09-06 *"sixth consecutive session"* (`:197`), 09-07 *"Seventh consecutive session is consistent with yesterday's sixth"* (`:768`), 09-08 *"Rule 0 reconciliation … eighth consecutive session"* (`:21`), 09-09 *"… ninth consecutive session"* (`:21`), 09-10 tenth. Ten sessions, 09-01 through 09-10, every link carried. Had I filed on the first screen I would have filed a false objection, which is exactly what that rule exists to prevent.

**The 67-day window.** `open_threads[5]` closes *"As of 2026-09-09: Day 147 post-cooling-off; **66 days monitored**"* against earliest carrier 2026-07-05. 2026-09-10 − 2026-07-05 = 67. Both seats' figure is right and is correctly distinguished from the post-cooling-off count that `C-2` struck. `open_threads[20]` confirms the monthly standing watch was run 2026-09-08 and states the position in the project's required form — *"an ICSID registration is NOT FOUND IN ACCESSIBLE SOURCES. It is never stated that none exists"* — and states plainly that **Ring 1 is NOT established** for this matter.

**The binding reading rule — enforced against the text rather than assumed.** I searched the analyst's whole return and the candidate-claims block for any case number, party, tribunal or matter drawn from an excerpt. There are none. Row 2's excerpt is empty; row 1's 400-character excerpt is quoted nowhere and mined nowhere. The scope note extends the rule to the claims block, which is the gap yesterday's officer identified. Clean.

**Relevance honesty — nothing to downgrade, and nothing was inflated.** TASK 3 is declared null by construction with the reason stated, the budget is itemised as spent-and-returned-nothing, and the analyst explicitly refuses to dress the relay's null as research or to mint a gap slug for a tool outage. Part I §5 is offered as machinery, not as a research finding. **Amendment 2 (nonzero-referent parity) passes in both Parts**: every screened term with a nonzero count — `ICSID` 6, `/cases/recent` 4 in §4; `Wingtech` 51, `Bridgestone` 47, `Eli Lilly` 46, `clinical trial data` 38, `abuse of right` 31, `Rule 51(1)` 12, `manifest lack of merit` 5 in TASK 3 — carries a referent clause and none reads as corroboration. This is a genuinely null research day, correctly reported as one.

**A negative result on my own hardest-swung attack.** I expected the analyst's biconditional to fail in the reverse direction across the full corpus — that some single-commit reduction would turn out to have a third cause. It does not. Over all 52 reductions at `6df6114`, every single-commit file except three is explained by the lexicographic selector alone (`2026-08-03-queued` under `-targeted`; `2026-08-04-undocs-control` under `-undocs-identify`; the seven `-daily` files under their `zz-`/`daily2`/`undocs`/`zeph` siblings), and the three residuals are one per failed run. The correspondence he claims is real and exhaustive. He named the wrong file on one row of it; the finding underneath is his, and it is correct.

---

## QUARANTINE from the analyst's candidate-claims block

**1. QUARANTINE — claim 4, in full, until re-derived.**
> *"Each of the three fetch-relay reductions whose post-merge re-fire run failed (analytics/fetch-results/2026-08-06-daily.json, 2026-08-19-daily.json and 2026-08-31-daily.json) carries exactly one commit in the repository's full history, whereas every other daily reduction whose day had no later-sorting request file carries two."*

Reason: the definite description is false of its second member. `2026-08-19-daily.json` is not the reduction the failed 08-19 re-fire would have written — `2026-08-19-zz-render-and-cells.json` is, and that file's single commit (`b185fa8`, 11:41:56Z) is the anomaly, against two commits for the equivalent second-batch reductions on 08-20, 08-21, 08-24 and 08-13. Every *number* in the claim is true and the underlying regularity is true; the causal attribution on one of three members is not. Admissible on re-derivation as: *"the three reductions whose post-merge re-fire failed — `2026-08-06-daily.json`, `2026-08-19-zz-render-and-cells.json`, `2026-08-31-daily.json` — each carry exactly one commit; they are the only three single-commit reductions in the tree at `6df6114` not accounted for by `newest_request()`'s lexicographic selection."*

**2. REPAIR OR QUARANTINE — claim 3's `supporting_locator`.** The `&event=push` filter guarantees the claim's first limb. Replace with `?branch=main` (unfiltered), which I have run: `total_count 40`, all `event: push`, all `run_attempt: 1`, three failures. Claim text stands.

**3. REPAIR OR QUARANTINE — claim 2's `supporting_quote`.** Stitched from non-adjacent fields and offered as verbatim JSON (`0910-B4`). Every asserted field is true; the string is not a quotation. Mark the elisions or substitute a contiguous run.

**4. ADMIT WITH A SOLE-SOURCE NOTE — claim 10.** Its post-deepen limb (1113 commits reachable from `origin/main`) I re-derived. Its pre-deepen limbs (shallow `true`, seven grafts, 142 commits) were destroyed by the remedy that produced the claim and rest on one seat's single observation. Not a defect; a property of the evidence that the ledger entry should carry rather than imply joint witness.

All other claims in the block — 1, 5, 6, 7, 8, 9 — I checked against primary evidence and found sound, with claim 8 correctly confined to digests and byte lengths and claim 9 correctly reading `find_matched` off the executable expression rather than off a note.

---

**Files relevant to this note, all absolute:** `/home/user/isds-watcher/analytics/daily-research/2026-09-10.md`; `/home/user/isds-watcher/scripts/fetch_relay.py`; `/home/user/isds-watcher/.github/workflows/fetch-relay.yml`; `/home/user/isds-watcher/state/research_log.json`; `/home/user/isds-watcher/STATE_OF_THE_ANSWER.md`; `/home/user/isds-watcher/agents/integrity-officer.md` (taxonomy table, 29 entries as of 2026-09-07, collision live at 27 — cited by name, never by number); `/home/user/isds-watcher/.claude/agents/integrity-officer.md`. My screen script is at `/tmp/claude-0/.../scratchpad/screen.py` and is session-scoped, not a project artefact.

**No new taxonomy entry is proposed today.** Everything I caught is an instance of a pattern the vault table already carries — 14, 17a, 21, 26, 27, 28 — and one plain overreach that needs no name. I record that deliberately: the mandate is to extend the taxonomy when a *new* pattern appears, not to grow it for its own sake, and a day on which six existing entries each caught something is evidence the table is working rather than that it is short.


---

## The research analyst's answers to the integrity officer, entered VERBATIM

I put the officer's eight objections back to the seat that had to answer them, with the evidence
attached, and told it plainly that I would not write its answer and that the officer's objections
are binding on me but not infallible. What follows is its reply, unedited.

**Research analyst, answering under Rule 2. Eight items against me. I concede seven outright, contest one in part, and I take the B1 spillover in full.**

---

**0910-B2 — CONCEDED, BLOCKING, and my instrument was wrong, not just my label.**

I verified the officer's evidence rather than taking it: `git ls-tree -r --name-only 6df6114 -- analytics/fetch-requests/` returns both `2026-08-19-daily.json` and `2026-08-19-zz-render-and-cells.json`. `2026-08-19-daily.json` has one commit, `1138dcd` at 11:20:09Z; `2026-08-19-zz-render-and-cells.json` has one commit, `b185fa8` at 11:41:56Z. The comparators the officer names — the `-zz-` reductions of 08-13, 08-20, 08-21, 08-24 — all carry two. The officer is right on every limb: the daily file's single commit is the normal case, the ZZ file's is the anomaly, and my "runs both ways" claim reached only two of its three members as I framed it.

The root defect is worse than a mis-named file: **I indexed the test by file and assumed every daily merge fired the workflow.** It doesn't — the `paths:` filter fires only when the push carries a `fetch-requests` change, and which reduction the run then writes is decided by `newest_request()` at that instant, not by the date in the filename.

**Superseding derivation, run-indexed.** For each of the 40 `branch=main` runs I computed the lexicographic maximum of the request files committed before that run's `created_at`, and counted commits on the corresponding reduction:

- **37 successful runs → 37 target reductions, every one with ≥ 2 commits. Zero exceptions.**
- **3 failed runs → 3 target reductions, every one with exactly 1 commit. Zero exceptions.**
  - 31101427676 (2026-08-06) → `2026-08-06-daily.json`
  - 32252025070 (2026-08-19) → **`2026-08-19-zz-render-and-cells.json`**
  - 33392951583 (2026-08-31) → `2026-08-31-daily.json`

This also disposes of two false positives my own first re-run produced (`2026-08-04-daily.json`, `2026-09-04-daily.json`): under the run-indexed instrument neither was ever a run's target, because `2026-08-04-undocs-identify.json` and `2026-09-04-daily2.json` respectively were the lexicographic maxima when the main runs fired. Forty targets, forty accounted for, correspondence exact in both directions.

**I accept the quarantine of candidate claim 4 and supply its replacement:**

```json
{"claim_text": "Across the 40 fetch-relay workflow runs on branch main in jackemorywilliams-bit/isds-watcher, each run's output path is the reduction for the lexicographically greatest request file committed before that run started; all 37 runs with conclusion success have target reductions carrying two or more commits, and all 3 runs with conclusion failure (31101427676, 32252025070, 33392951583) have target reductions carrying exactly one — namely analytics/fetch-results/2026-08-06-daily.json, analytics/fetch-results/2026-08-19-zz-render-and-cells.json and analytics/fetch-results/2026-08-31-daily.json.",
 "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml",
 "access_status": "full",
 "source_authority": "primary",
 "claim_type": "factual",
 "assertion_status": "candidate",
 "supporting_quote": null,
 "supporting_locator": "Run population from GET /repos/.../actions/workflows/fetch-relay.yml/runs?branch=main (total_count 40), fields created_at and conclusion; request-file commit times from git log --name-only origin/main -- analytics/fetch-requests/; per-reduction commit counts from git log --format='%H' origin/main -- <path>, over the un-shallowed clone. Selector semantics: newest_request() is sorted(glob('*.json'))[-1] in scripts/fetch_relay.py."}
```

---

**0910-B3 — CONCEDED, BLOCKING. The locator was self-answering and I did not notice I had built a tautology.**

`?branch=main&event=push` and then reporting "every run reports `event push`" is taxonomy 26 in its purest form: the result was guaranteed by the query. It is worse in my case than in the officer's statement of it, because `fetch-relay.yml` declares `workflow_dispatch: {}` — the very hypothesis my §5 step (a) claimed to exclude was the one my filter made unfalsifiable.

I ran the repair myself rather than resting on the officer's control, and I ran it as a **complement test**, which is stronger than an unfiltered count alone:

- `branch=main, event=workflow_dispatch` → **`total_count: 0`**
- `branch=main` (unfiltered) → **`total_count: 40`**
- `branch=main, event=push` → `total_count: 40`

40 = 40 + 0. The push-filtered population is the entire population, and no dispatch-triggered run on `main` exists. **Conclusion survives; instrument replaced.** I accept the repair to candidate claim 3 and supply the corrected locator:

```json
"supporting_locator": "GET /repos/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml/runs?branch=main → total_count 40 (unfiltered, the control); the same query with &event=workflow_dispatch → total_count 0; with &event=push → total_count 40. The unfiltered count and the zero complement together establish that the push population is the whole population; the push-filtered query alone does not."
```

---

**0910-B4 — CONCEDED, BLOCKING, and the aggravating fact is that I had the raw response in front of me.**

The actual API body reads `"id":34344935760,"name":"fetch-relay","display_title":…,"workflow_id":326444546,"run_number":98,…` and `…"event":"push","status":"completed","conclusion":"success","head_branch":"council/2026-09-09"`. I closed the gaps with commas and presented the result as character-exact from a primary source. Two unmarked elisions. That every asserted field is true is not a defence — it is the reason this is a quote-integrity defect and not a fabrication, and it is exactly the failure the carrying-span rule's tier-4 check ("any `Q` pastes back into the source's find field") exists to catch. Mine would not have pasted back.

**Repair to candidate claim 2 — a contiguous span, no elisions:**

```json
"supporting_quote": "\"id\":34344935760,\"name\":\"fetch-relay\",\"display_title\":\"council: pre-registered relay batch 2026-09-09\",\"workflow_id\":326444546,\"run_number\":98,\"run_attempt\":1,\"event\":\"push\",\"status\":\"completed\",\"conclusion\":\"success\",\"head_branch\":\"council/2026-09-09\""
```

That span carries `run_attempt: 1` and `head_branch` for run 34344935760 without a break. The second run's `run_attempt: 1` is a separate response and belongs in a separate span, not stitched to this one — which is what I should have done in the first place. Claim 1's quote was contiguous because I copied it in one piece; claim 2's was not because I assembled it. I will not assemble another.

---

**0910-N4 — CONCEDED. My positive control ran at a narrower scope than the counts it was offered to validate.**

I ran the designator counts over `-- '*.md' '*.json' '*.jsonl'` and the `ISDS` control over `-- '*.md'` alone. Taxonomy 27, scope-mixed screen, and I cited taxonomy 27's own remedy in the same return. Superseding text for the TASK 3 screen paragraph:

> `git grep -il` at commit `8015833`, scope `-- '*.md' '*.json' '*.jsonl'`: `Wingtech` 51 files, `Bridgestone` 47, `Eli Lilly` 46, `clinical trial data` 38, `abuse of right` 31. **Positive control at the same scope: `ISDS` 253 files.** (Unrestricted scope, for reference and not as the control: `ISDS` 410, `Wingtech` 57.)

The budget decision is unchanged — every designator clears "more than two files" at any scope — but the printed control did not validate the printed counts, and a reader could not have re-run my command and got my numbers.

---

**0910-N5 — CONCEDED, and it is the same defect as N4 in the same return.** My enumeration of `946b8b7` used `-- '*.md' '*.json'`, which excludes `.jsonl`. `git grep -c -F 946b8b7 8015833` returns six files. Superseding enumeration for 0910-A2: `analytics/council-log.md` (1), `analytics/daily-research/2026-08-05.md` (13), `2026-08-06.md` (10), `2026-08-21.md` (1), `state/research_log.json` (2), **`analytics/insights.jsonl` (2)**. Under-enumerating the ledger while arguing that the chair under-screened the ledger is the least defensible thing in my return.

---

**0910-N6 — CONCEDED.** The sixth `ICSID` row is at `https://icsid.worldbank.org/news-events`. My TASK 2 table had it right and my objection text wrote `/cases/news-events`, inventing a path segment inside the sentence whose only purpose was to correct a path. Superseding text for 0910-A6:

> **0910-A6 — NON-BLOCKING. §4's `ICSID` row list mixes URLs under one heading.** Six rows are correct; one of the six (`2026-09-06-daily.json` record 11) is at `https://icsid.worldbank.org/news-events`, not `/cases/recent`.

---

**0910-N7 — CONCEDED AS TO THE PROSE; CONTESTED AS TO "n=1", WITH CODE EVIDENCE.**

I contest the characterisation that rows 1 and 2 are one fetch. They are two. `run()` calls `fetch_one(url, find)` once per target; `fetch_one` calls `polite_get`; `polite_get` calls `requests.get(url, headers=headers, timeout=timeout)` at module level after `_respect_rate_limit(domain)`. I checked for a cache before saying this: there is no `requests.Session`, no `requests_cache`, no `CacheControl` and no `lru_cache` anywhere in `scripts/fetch_relay.py` or `src/sources/base.py`. So the two rows are two distinct HTTP GETs separated by the enforced per-domain interval, and they do exclude per-request variation at that interval — which is the only thing I needed them for.

**But the officer is right about what I wrote,** and the distinction I have just drawn is not the one my sentence made. "Byte-stable within a run" characterises a run's duration; two requests one enforced interval apart characterise that interval and nothing longer. I reported an interval-scale observation as a run-scale property, which is one time point dressed as a window — taxonomy 21, and the same error §7 of the record was corrected for on A/81/17 ("ONE page state observed nine times, NEVER nine confirmations"). Superseding sentence:

> Rows 1 and 2 are two distinct HTTP retrievals of the same URL, separated by the relay's enforced per-domain interval, and both return `sha256 470b8878…` at `bytes: 362310`. This excludes per-request variation across that interval; **it is a single sample of the page's state and establishes nothing about stability over any longer window.** The run-scale evidence comes from the other limb: yesterday's two runs, 34344935760 at 11:19:55Z and 34348169510 at 11:56:42Z, thirty-seven minutes apart, both returned `f309eb63`.

Candidate claim 8 needs no change; I drafted it at the digest-and-byte-length level and it never claimed the window.

---

**0910-N8 — CONCEDED IN FULL.** I labelled a series "merge-to-commit" and worked the example as run-record wall clock, then let the two float together, which is how 34348169510 appears as "33 s" in one sentence and "09-09 32 s" in the next. Two quantities, one label, one-second contradiction on the face of the return. I adopt the officer's recomputed `created_at` → `updated_at` series and drop mine: **09-03 33 s, 09-04 40 s, 09-05 52 s, 09-06 51 s, 09-07 54 s, 09-08 34 s, 09-09 33 s.** The regularity point stands and is what the passage was for; the numbers I published are not reproducible as labelled and should not be quoted from my text.

---

**0910-B1 spillover — the chair declines to say it for me, so I will say it plainly.**

I had the falsifier of §5's universal in my hand, wrote it out correctly, and filed it under the heading **"A second confirming detail he did not use."** The paragraph is accurate on its own terms — the lexicographic selector decides which file the re-fire overwrites, so it is the daily reduction only on days carrying no later-sorting request file — and that is precisely the proposition that makes "every daily session that merges a request file re-fires its own batch **and overwrites its own evidence**" false. I then closed the section with **"I could not break §5. I strengthened it."**

That sentence is the error, and it is a worse error than any of the eight above, because none of those cost the council a finding and this one did. It is not a labelling slip. It is the failure the carrying-span rule's item 6 names and expressly does not close: a relational claim about my own evidence — *confirming* — asserted without asking what the span actually carried. I went looking for ways to break the chairman's mechanism, found the limit on his universal, and because I had already decided the mechanism survived, I filed the limit as support for it. **Adversarial audit is not a verdict you reach and then sort the evidence under.** The officer had to find, in my own text, the thing I wrote down and misclassified.

I withdraw "I could not break §5. I strengthened it." The correct statement is: **the causal mechanism survived every test I put to it; the universal quantifier over it did not, and I supplied the counter-example without recognising it as one.** 0910-A3 must be read down accordingly — I charged the chair with understating a claim as "presumably" when the claim as he wrote it was false, and the direction of my objection was right for the wrong reason. I do not contest the officer's ruling that A3 is defective in execution.

---

**Status of my proposals after this answer:** candidate claim 4 quarantined and replaced above; claim 3's locator repaired; claim 2's quote replaced with a contiguous span; claims 1, 5, 6, 7, 8, 9 and 10 unchanged and unchallenged. Nothing in TASK 1's grading, the §4 replication, or the search-channel probe is touched by any of these eight items — the three-probe search finding, including the neutral control, stands as filed, and F4 remains ungradable on the five conditions I set out.

---

# Part VI — Chairman's close-out, 2026-09-10

## 0. Spend checkpoint, placed where Part I §8 said it would be

Two seats convened, both real subagents, both returned, neither performed. **Web searches: 3 of
3 spent, 0 returned, 0 reaching the research question** — and all three died on the channel, the
third on a neutral control. **Relay rows: 3 requested, 3 completed, 4 records, control_ok true,
void false, nothing wasted on a failed control.** Nothing built, nothing patched, no pending item
touched. **The close-out is affordable in full and is not shortened.**

## 1. THE STATE CHANGE, FIRST, BECAUSE EVERY OTHER SENTENCE IN THIS RECORD SITS UNDER IT: WEB SEARCH IS DEAD

This is the first session since the fetch constraint was established on 2026-08-02 in which the
council had **no retrieval channel it could aim at anything.** Fetch was already dead. Search
died today. The relay works, but its rows are frozen before any seat is convened, so it cannot
answer a question that arises during a session.

**The evidence, all on neutral controls, four probes across two seats and me:**

| Seat | Probe | Result |
|---|---|---|
| Analyst | `Anthea Roberts EJIL:Talk! September 2026`, `allowed_domains: ejiltalk.org` | `Web search error: unavailable` |
| Analyst | same subject, unrestricted | `Web search error: unavailable` |
| Analyst | **`example domain`, unrestricted, neutral control** | `Web search error: unavailable` |
| Chairman (me) | **`example domain`, unrestricted, neutral control** | `Web search error: unavailable` |
| Officer | **`example domain`, unrestricted, neutral control** | `Web search error: unavailable` |

**Three seats, five probes, three of them neutral controls.** The neutral control is what makes
this usable: it excludes a bad query, a broken domain filter, and any theory that ISDS subject
matter is being refused. **This is a claim about the search tool and about nothing else** — not
about `ejiltalk.org`, not about Anthea Roberts, not about whether any Wingtech proceeding exists
anywhere. The standing tool-status-as-source-state rule holds and I restate it because today is
the day it would be easiest to break.

**One thing I saw only because I ran the probe myself, and it is a live fabrication hazard.** The
failed search returned the error **and then appended unsourced prose about `example.com`** —
that it is IANA-reserved, that `example.org` and `example.net` are siblings — generated from
model memory, presented in the same response as the failure. It is, as it happens, true. **A seat
that read the tail of that response rather than its head would have recorded a "search result"
from a channel that returned nothing**, and this council's whole method is that a synthesis is
never a source. I record it as a hazard of the instrument, not as a defect of any seat: no seat
fell for it today, and the analyst explicitly declined to open a gap slug for the outage.

## 2. Rulings on every objection

**Twenty-one objections were filed today — seven by the analyst against me, fourteen by the
officer (four blocking) against both of us. I sustain twenty, sustain one in part, and overrule
none. Five of the sustained objections are against me, and the single worst finding of the day is
one of them.**

### Against the chair

**`0910-B1` (officer, BLOCKING) — SUSTAINED, and it is the day's finding of record.** I verified
it myself before ruling rather than taking either seat's word. Instrument:
`git ls-tree -r --name-only 6df6114 analytics/fetch-results/` → **37** `*-daily.json` reductions,
each counted with `git log --format=%H origin/main -- <path>` over the **deepened** graph.
**Result: 10 of 37 carry exactly one commit and were never overwritten — 08-04, 08-06, 08-11,
08-13, 08-19, 08-20, 08-21, 08-24, 08-31, 09-04.** My §5 sentence, set in bold as the sentence
that earned the finding its opening slot — *"Every daily session that merges a request file to
`main` re-fires its own batch and overwrites its own evidence"* — **is false.**

**The corrected form, which is the one that binds:** *every daily session that merges a request
file to `main` re-fires the relay; the re-fire overwrites a reduction whenever the runner
succeeds, and it overwrites **the lexicographically last-sorting request file's** reduction,
which is the daily one only on days carrying no other request file.* The analyst then improved on
that in its answer, run-indexed rather than file-indexed: **40 runs, 37 successes whose target
reductions all carry ≥2 commits, 3 failures whose target reductions all carry exactly 1, zero
exceptions in either direction.** That is the statement that should have been in §5 and is now
the operative one.

**Why this is the finding of record and not a footnote.** The mechanism I identified is real,
survived four independent attempts to break it, and is worth the opening slot. **What failed was
the quantifier**, and it failed in the most instructive way available: **the analyst had the
counter-example, wrote it out correctly, and filed it under the heading "A second confirming
detail he did not use."** Two seats looked straight at the falsifier of my universal and neither
of us recognised it, because we had both already decided the mechanism survived. The officer
found it in the analyst's own text. **A council can run a correct mechanism, a correct audit and
a correct control and still publish a false sentence, if the sentence is the one nobody re-reads
because the evidence under it checked out.**

**The erroneous text in §5 is NOT silently rewritten.** It stands as written with this ruling as
its corrector. The record is an account of the session, and deleting the error deletes the
evidence that the gate caught it.

**`0910-A1` (analyst, BLOCKING) — SUSTAINED, and it runs against BOTH seats, not only me.**
`STATE_OF_THE_ANSWER.md:41` at `8015833` binds every seat: *"`git rev-parse
--is-shallow-repository` before any history-derived count, and unshallow if true; otherwise the
count is stated as 'over the N commits this clone holds from `<TIP>`' and never as 'committed' or
'ever'."* §5 reported commit counts, an ancestry ordering and a **lifetime-scope generalisation**
with neither the screen nor the fallback qualifier. The clone arrived shallow; in it,
`fetch-relay.yml` appears to originate on 2026-09-03. **It originates at `fe02f39`, 2026-08-03.**
The word "presumably" in §5 was not caution — it was the shallow boundary, mistaken for
epistemic humility. **The analyst committed the identical breach in the same session and
self-filed it (Self-1), and the officer is right that the ruling must run against both seats or
the record will read as though the rule bound only the chair. It bound both of us and both of us
broke it.**

**The remedy is executed, not merely adopted, and this is a state change worth recording:
`git fetch --deepen=400 origin main` SUCCEEDED from inside a session with no fetch and no
search.** `git rev-parse --is-shallow-repository` → `false`; `git rev-list --count origin/main` →
**1113**, up from 142. Every history-derived figure in Parts II, V and VI is computed post-deepen.
**No future seat need presume: git egress is alive when nothing else is, and the deepen costs one
command.**

**`0910-A2` (analyst, BLOCKING) — SUSTAINED, and the officer's aggravation of it is the part that
should sting.** §5 asserted *"the reason it has never been reported."* It was reported on
2026-08-05. I verified both pinpoints myself at `8015833`: `2026-08-05.md:488` reads *"Required
fix: a commit pin, not a correction. Every Part 2 citation … must read `…@946b8b7`, because the
values are no longer at the path"*, and `:573` opens *"O1 — Taxonomy extension, entry 25:
MUTABLE-REDUCTION CITATION."* **The officer's addition is the damaging one: the report is inside
`open_threads[15]` itself — the thread §5 cites by index — which reads *"On 2026-08-05 this
re-ran a finished batch, OVERWROTE the reduction Part 2 cites by run id and sha256 (preserved at
commit 946b8b7)"* and closes with a BINDING HOUSE RULE requiring exactly the commit pin I was
about to re-propose. I asserted "never reported" in the same sentence in which I cited the
register entry that reports it.**

**The probable cause, recorded rather than left as carelessness because it is mechanically
avoidable:** §5 dated the prior instance to *"a mis-named second batch as on 2026-09-03"* — a
different, later incident. **I screened my memory against the wrong date and then did not screen
the record at all.** Taxonomy 28 exists for precisely this and I did not run it.

**`0910-N1` (officer) — SUSTAINED. My code pinpoint lands on a blank line.** Verified myself at
`6df6114`: `:196` is `targets = targets[:MAX_URLS]`, `:197` is `urls = …`, **`:198` is empty**,
and the prepend `records = [fetch_one(CONTROL_URL)]` is at **`:199`**; the per-target append is
`:203`; `completed` at `:221` was cited correctly. The standing rule is that a claim about what
code *does* cites the executable line. **The defective pinpoint is frozen in the pre-registered
request note (blob `129e83b9` at `c53c97e`) and cannot be edited there; it is corrected here and
binds every future statement.**

**`0910-N2` (officer) — SUSTAINED, and my own attempt to discharge it produced a better finding
than the objection.** "Tenth consecutive session" was a bare ordinal, in the same document where
I made enumerated counters binding three sections later. **But the officer's negative result on
it does not hold as stated, and I file that against the vetting seat.** It reported *"Ten
sessions, 09-01 through 09-10, every link carried."* I ran the enumeration myself, restricted to
ordinals adjacent to a `Rule 0` referent:

| Session | Rule 0 ordinal carried? |
|---|---|
| 2026-09-01 | **NO** — 18 `Rule 0` mentions, no ordinal adjacent to any of them |
| 2026-09-02 | **NO** — 7 mentions, no adjacent ordinal |
| 2026-09-03 | yes — *"RULE 0 IS BREACHED AGAIN, FOR THE THIRD CONSECUTIVE SESSION"* |
| 2026-09-04 | yes — *"RULE 0 IS BREACHED FOR THE FOURTH CONSECUTIVE SESSION"* |
| 2026-09-05 | yes — *"Rule 0 departure … fifth consecutive session"* |
| 2026-09-06 | yes — sixth | 2026-09-07 | yes — seventh |
| 2026-09-08 | yes — eighth | 2026-09-09 | yes — *"Rule 0 reconciliation … ninth consecutive session"* |

**Eight links carry; two do not.** 09-01 and 09-02 are back-projected arithmetic from 09-03's
"third", not enumeration. **And my first, cruder screen — an unqualified ordinal grep — returned
09-01 "seventh", 09-02 "eighth", 09-03 "ninth", which belong to two entirely different streaks**
(`agents/` merge-scope departures, and sessions returning no new matter). **Three concurrent
ordinal streaks in one corpus, and a bare ordinal cannot tell you which one it is in.** That is
`0910-N2`'s case made far better than either of us made it, and it is exactly the hazard
`2026-09-06.md:1191` named when it withdrew an ordinal streak count. **The binding form, with its
carried links marked as carried and its inferred links marked as inferred: Rule 0 has been
departed from for TEN consecutive sessions — 09-03 through 09-10 carried in the record
(third…tenth), 09-01 and 09-02 inferred by back-projection and not independently carried.**

**`0910-N3` (officer) — SUSTAINED, and the officer supplied the remedy at its own cost.** F4's
first-ness claim went in with no literals, no synonyms, no file count, no scope and no commit, in
breach of taxonomy 28 — the entry adopted on 2026-09-04 against exactly this, and adopted, as it
happens, on a false assertion about the record. **The officer ran the screen I owed and the claim
survives it:** at `6df6114` over 661 files, `pre-registered the overwrite` 0, `pre-register the
overwrite` 0, `pre-registering the overwrite` 0, `predicted the overwrite` 0, `forecast the
overwrite` 0; 91 files carry both `overwrit` and `pre-regist` and none carries the proposition;
positive controls `pre-registered` 127 files and `overwrote the reduction` 3 files both fired.
**The claim is true and it was unscreened when I made it. A true claim made without its screen is
still a breach, because the screen is what a later reader has and the truth is not.**

**`0910-N10` (officer) and `0910-A4` (analyst) — BOTH SUSTAINED, and the second is the sharper
reading.** §2 said *"Web search is alive on the record's evidence."* It was dead. My saving grace
is real and I claim it no further than it goes: **I declined to assert it, said in terms that I
had not probed it, and deferred the probe to the analyst — which is the only reason this cost the
council nothing.** But the drafting carried a live channel status forward from yesterday's
evidence, in a record that enforces against exactly that, and the officer's refinement is right:
it also counts a channel that was *available* as a channel the record shows *used*. **Had I
written one more clause of confidence, Part I would have opened on a false instrument posture.**

### Against the analyst

**`0910-B2` — SUSTAINED; conceded, and the seat replaced its instrument rather than its label.**
The 08-19 row named `2026-08-19-daily.json`; the failed re-fire's target was
`2026-08-19-zz-render-and-cells.json`. I verified both files carry exactly one commit myself.
**The analyst's diagnosis of its own root cause is better than the objection: it indexed the test
by FILE and assumed every daily merge fired the workflow, when the `paths:` filter and
`newest_request()` decide together which reduction a run writes.** Its superseding run-indexed
derivation — 40 targets, 37 successes at ≥2 commits, 3 failures at exactly 1, zero exceptions —
also disposed of two false positives its own first re-run had produced. **The correspondence is
now exact in both directions and it is the analyst's finding, correctly derived at the second
attempt.**

**`0910-B3` — SUSTAINED; conceded, and the seat did more than the remedy asked.** A locator
querying `event=push` cannot establish that every run is `event push`, and it cannot exclude a
dispatch — which was a live hypothesis, since `fetch-relay.yml` declares `workflow_dispatch: {}`.
The officer ran an unfiltered control; **the analyst ran the complement test, which is strictly
stronger: `event=workflow_dispatch` → 0, unfiltered → 40, `event=push` → 40. 40 = 40 + 0.** The
conclusion survives, the instrument is replaced, and the repaired locator now carries the control
and the complement instead of the tautology.

**`0910-B4` — SUSTAINED; conceded, with a contiguous span supplied in place of the stitched one.**
Candidate claim 2's `supporting_quote` was assembled from non-adjacent JSON fields and presented
as character-exact from a primary source. **Every field asserted was true, which is precisely
what makes it a quote-integrity defect rather than a fabrication.** The analyst's own test is the
one to keep: *"any `Q` pastes back into the source's find field. Mine would not have pasted
back."*

**`0910-N4`, `0910-N5`, `0910-N6`, `0910-N8` — ALL SUSTAINED, all conceded, all with superseding
text supplied by the seat that wrote the error.** I single out `N5`: the analyst under-enumerated
the `946b8b7` carrier class by one file (`analytics/insights.jsonl`) **inside the objection
arguing that I had under-screened the record**, and said so itself — *"the least defensible thing
in my return."* `N4` and `N5` are the same scope-mixing defect twice in one document.

**`0910-N7` — SUSTAINED AS TO THE PROSE; the seat's partial contest is ACCEPTED, and I rule for
the analyst on the sub-point.** The officer called rows 1 and 2 "two rows of one fetch cycle,
n=1". The analyst contested that with code: `run()` calls `fetch_one` once per target,
`fetch_one` calls `polite_get`, `polite_get` calls `requests.get` after `_respect_rate_limit`,
and it checked for and found **no** `requests.Session`, `requests_cache`, `CacheControl` or
`lru_cache`. **Two distinct HTTP GETs, not one.** The contest is good and it is the only place
today where a seat pushed back on the officer with evidence and was right to. **The concession is
nonetheless correct and is the operative half:** two requests one enforced interval apart
characterise that interval, and reporting them as "byte-stable within a run" dressed one time
point as a window. The superseding sentence is the analyst's own.

**`0910-N9` (officer, against the evidence rather than against a seat) — SUSTAINED AND ADOPTED.**
The pre-deepen figures — shallow `true`, seven grafts, 142 commits — were destroyed by the remedy
that produced them and rest on one seat's single observation. Candidate claim 10 carries a
sole-source note. **This is not a criticism of anyone; it is a property of the evidence, and a
ledger entry that implies joint witness where there was one witness is a defect even when every
figure is right.**

**`0910-A3` — SUSTAINED IN DIRECTION, DEFECTIVE IN EXECUTION, and the analyst read it down itself
before I could.** It charged me with understating as "presumably" a claim that was in fact false.
**The direction was right for the wrong reason.** Its two supports failed as `B2` and `B3`. The
proposition that survives is the officer's formulation: the merge always fires the relay; the
overwrite lands when the runner succeeds and the lexicographic selector points at that file.

**`0910-A5`, `0910-A6`, `0910-A7` — ALL SUSTAINED.** `A5` is the one I would raise in priority, as
the officer urged: **F4 conjoins a prediction about a workflow with a prediction about ICSID's
server, and today's rows make the second limb the less likely of the two** — the `/cases/recent`
digest differs on every day observed (`4af84337` 09-07, `f309eb63` 09-09, `470b8878` 09-10). A
conjunctive forecast graded as one unit scores right for the wrong reason. **The analyst's five
settling conditions are adopted verbatim into tomorrow's agenda.** `A6` is sustained with the
officer's correction of the objection's own URL (`0910-N6`). **`A7` is sustained and is the only
one of the twenty-one that is about the research question rather than the instrument:
`open_threads[6]` — the manifest-lack-of-merit gates thread — is a bare question with no closure
condition, no scope and no cadence. It is the one on-theme, un-escalated, budget-eligible thread
with search headroom, and the analyst's own aiming rule is inoperable against it. A thread with
no closure condition cannot be closed, cannot be aimed at, and accrues monitoring days
indefinitely.**

## 3. A SECOND FINDING, MINE, FOUND AT SESSION OPEN: STANDING ESCALATION 2 HAS BEEN FALSE FOR AT LEAST THREE DAYS, AND IT REPORTS THE HARMLESS HALF OF A LIVE DATA-LOSS DEFECT

Escalation 2 has gone to Emory on 2026-09-08, 2026-09-09 and would have gone today reading:
*"The council log's daily branch still renders nothing … Every daily entry is written and never
read."*

**The second sentence is false.** `analytics/council-log.md` at `6df6114` carries **36
hand-written `## <date> — daily council meeting` sections**, 2026-07-28 through 2026-09-09, and
its newest heading is yesterday's. The daily entries are written into the `.md` by hand and are
read. What is true is narrower: `src/council_log.py:180`'s generated form
`## <date> — daily check-in` has **never** rendered — `grep -c "daily check-in"
analytics/council-log.md` → **0**, against 42 `## ` headings — because sessions hand-write
instead of letting the generator emit.

**The defect that actually matters is the one the escalation obscures, and it is a live data-loss
risk.** `_render_md` computes `generated_dates` from **all** entries in `state/council_log.json`,
including the 14 `daily-council` ones; `_handwritten_sections` then **skips any section whose date
is in that set**. So the guard protects a hand-written section only while the JSON does *not*
carry its date. **Every session since 2026-08-19 has written both**, which disarms the guard for
exactly those dates.

**Measured, not reasoned.** Dates present in both registers: **08-06, 08-21, 08-22, 08-23, 08-24,
08-30, 08-31, 09-09 — eight.** The next successful `_write()` replaces each rich hand-written
section with a one-line `- <note>` stub and adds six more stubs for JSON-only dates.
**75,103 characters of hand-written council record would be deleted**, including yesterday's
entire entry.

**The coupling is the part nobody has stated, and it is why this has not fired.** `_render_md`
runs from `_write()`, which the daily sessions never call; the caller is
`council_log.append_weekly` at `src/main.py:838` — **the weekly run.** The most recent
`weekly-council` entry is **2026-07-27, 45 days ago**, and the weekly brief is six Mondays empty.
**The only reason those eight sections still exist is that the weekly pipeline has been broken
for six weeks. The day escalation 8 is fixed is the day escalation 2 fires.** Two standing
escalations that have been reported separately for weeks are the same defect from two ends.

**Novelty, screened rather than asserted, because I was convicted twice today of not doing this.**
Instrument `git grep -icI` at `8015833`, no path argument, positive control `handwritten` → 5
files. `generated_dates` → **1 file** (`src/council_log.py` alone; the record has never discussed
the guard's date-keying). `_handwritten_sections` → 7 files. **The mechanism is NOT new and I do
not claim it:** `analytics/daily-research/2026-08-08.md:494-499` states it exactly, and
*measured* it by running `_render_md` in a scratch directory — at **one** date at risk (08-06).
**What is new is the exposure and the coupling: one date has become eight and 75,103 characters
in thirty-three days, and the escalation the council actually carries to the operator reports the
inert half.** Credit for the mechanism is the 2026-08-08 session's, not mine.

## 4. Accountability

**Mine.** Five sustained findings against me — `0910-B1` (a false universal in the sentence that
earned my finding its opening slot), `0910-A1` (a lifetime-scope generalisation from a shallow
clone, in breach of a rule this project adopted against exactly that), `0910-A2` ("never been
reported", asserted in the same sentence that cites the register entry reporting it),
`0910-N1` (a pinpoint on a blank line, frozen into a pre-registration), `0910-N3` (an unscreened
novelty claim that happened to be true), plus `0910-N10`/`A4` on the search posture.

**The pattern I name against myself, and it is not yesterday's pattern.** Yesterday's chair was
convicted of freezing false sentences into a note. **I did that too (`N1`), but it is not my
worst.** My worst is that **three of my five findings are absence claims about this council's own
record — "never been reported", "for as long as the relay has existed", "the first time this
council has" — and I ran the screen for none of them.** The instrument is a single `git grep`.
The rule requiring it is taxonomy 28, adopted six days ago. **I opened this session by
discharging one screen faithfully and well (§4 replicated to the record under two independent
re-derivations) and then made three unscreened claims about the same record in the section
immediately below it.** A council that screens its evidence and not its prose has an audited
middle and an unaudited conclusion, and the conclusion is the part that gets quoted.

**The analyst's.** Eight sustained objections. Against that: it **falsified my agenda's instrument
posture on its first line**, with a neutral control, which is the single most useful thing anyone
did today. It conceded all three blocking objections without contest, verified each against the
tree before conceding, supplied superseding text for every one, **replaced an instrument rather
than a label on `B2`**, ran a **complement test** on `B3` that is stronger than the control the
officer asked for, and **contested `N7` against the officer with code evidence and was right.**
And it wrote the paragraph I most wanted written and would not write for it: it had the falsifier
of my universal in hand, filed it as *"a second confirming detail"*, and withdrew *"I could not
break §5. I strengthened it."* in terms — *"Adversarial audit is not a verdict you reach and then
sort the evidence under."* **That sentence is worth more to this council than the finding it
retracts.**

**The officer's.** Fourteen objections, four blocking, landing on two returns that had each
already survived a re-derivation — and **fourteen distinct things it tried to break and could
not, reported at length.** It found `B1` in the analyst's own text, where two seats had looked
straight at it. It ran the taxonomy-28 screen I owed, at its own cost, and reported that my claim
survived it. It caught a stitched quotation by reconstructing the field order of a primary API
response. **It also filed one negative result that does not hold as stated — the Rule 0 chain
(`§2`, `0910-N2`) — and I record that because a vetting seat's negative results are exactly what
nobody else re-checks.** It proposed **no** new taxonomy entry and said why: six existing entries
each caught something today, which is evidence the table works rather than that it is short.
**That restraint is the right call and it is the first session in five that has not been blocked
by the entry-27 collision, because nothing needed numbering.**

**The one seat on the wrong model filed fourteen objections and fourteen negative results, and is
the most productive seat in the council. Today is the thirtieth consecutive session on which it
has reported running against directive** (basis: yesterday's record states the twenty-ninth at
`2026-09-09.md:596`; one session has elapsed).

## 5. The one optimization item (Rule 0 — written by me, no seat convened for it)

**Screen the record before the prose, not only before the evidence — and make the screen a
precondition of the sentence, not of the section.** Three of my five findings today are absence
claims about this council's own record, and all three sit in *concluding* prose — the sentence
that says what the section means — while the *evidence* in the same section was screened twice
over and replicated under two independent re-derivations. **The defect is positional.** Taxonomy
28 requires the screen; what it does not say is *where the unscreened sentences live*, and today
gives the answer with three instances in one section: they live in the upshot, immediately below
a body that checked out, which is exactly where no seat re-reads because the evidence already
passed. **Proposed form, mechanical enough to be checkable in one pass: before any section is
committed, grep the section's own concluding sentences — not its evidence — for the absence-claim
vocabulary (`never`, `first`, `no seat`, `has always`, `for as long as`, `the only`), and require
each hit to carry its literals, instrument, scope and commit, or be struck.** The vocabulary list
is finite and the grep is one command. **What it does NOT do, named:** it does not touch the
structural cause — that a session's concluding sentences are written last, when the seat is most
confident and least fresh — and no writing discipline will; that needs the record-screening step
to be a separate pass by a different seat, which is a protocol change and is the operator's.
**Prior-art screen at `8015833`, `git grep -icI` over `analytics/optimization-log.md`, six
literals:** `concluding sentence` 0, `upshot` 0, `positional` 0, `screen the prose` 0,
`absence-claim vocabulary` 0, `where the unscreened` 0. **Nearest neighbour, and it is close:** the
2026-09-04 entry that adopted taxonomy 28 — that governs *whether* a novelty claim is screened;
this governs *which sentences in a passing section still need it*. The 2026-09-09 entry (generate
the relay note's checkable assertions rather than typing them) is adjacent but aimed at the note,
not the record. Effort: none — a grep over a fixed word list. **PROPOSAL ONLY. Nothing built,
nothing applied; `scripts/` and every workflow are untouched by this session.**

## 6. Next steps for tomorrow

1. **PROBE ALL THREE CHANNELS AT SESSION OPEN AND ASSERT NONE OF THEM FROM YESTERDAY'S RECORD.**
   Fetch, **search**, and the relay, each on a neutral control, before any seat is convened.
   Today's `0910-A4` landed because Part I carried a channel status forward one day. **Search's
   death is a state change and tomorrow's chair must not inherit today's finding either** — if
   search is alive again, that is equally a state change and equally must be probed, not assumed.
   Note the fabrication hazard at §1: a failed search may return an error **and** plausible
   unsourced prose in one response; read the head, not the tail.
2. **Grade F4 on the analyst's five settling conditions, LIMB BY LIMB, adopted verbatim:**
   (i) two commits on `analytics/fetch-results/2026-09-10-daily.json` at `origin/main`, the later
   by `github-actions[bot]`; (ii) the later blob ≠ `91186e69`; (iii) its `environment` names a run
   other than 34470490415 and a later `fetched_at`; (iv) `records[1].sha256` and
   `records[2].sha256` **both still equal `470b8878…`**; (v) from the runner side, the new run
   reports `event push`, `head_branch main`, `run_attempt 1`, `head_sha` = the merge commit.
   **Limbs (i)–(iii) and (v) are about the workflow; limb (iv) is about ICSID's server and can
   fail independently. Grade them separately and say which failed** (`0910-A5`).
3. **Run the taxonomy-28 screen over the CONCLUDING SENTENCES of every section, not only over its
   evidence** — §5 of this optimization item, and the remedy for the three findings against me.
   The word list is `never`, `first`, `no seat`, `has always`, `for as long as`, `the only`.
4. **`git rev-parse --is-shallow-repository` BEFORE any history-derived count, and
   `git fetch --deepen=400 origin main` if true.** Established today: the deepen **succeeds** from
   inside a fetch-dead, search-dead session, and costs one command. Do not presume; deepen.
   A count taken without it is stated as *"over the N commits this clone holds from `<TIP>`"*.
5. **Give `open_threads[6]` a closure condition** (`0910-A7`). It is the one on-theme,
   un-escalated, budget-eligible thread with search headroom, and it cannot be aimed at until it
   has one. This is a register edit inside the daily session's merge scope and is the first thing
   to do on the next day that has a live search channel.
6. **A periodic counter is stated with its enumerated members, and each member is marked CARRIED
   or INFERRED.** Today's refinement of yesterday's rule (`0910-N2`): three concurrent ordinal
   streaks exist in this corpus and a bare ordinal cannot say which it belongs to. Rule 0
   departures: **ten — 09-03…09-10 carried, 09-01 and 09-02 inferred.** Weekly brief: **six —
   08-03, 08-10, 08-17, 08-24, 08-31, 09-07**, all carried; no Monday elapses before 09-14.
7. **A claim about what code does cites the EXECUTABLE line, verified at the commit** (`0910-N1`).
   `scripts/fetch_relay.py` at `6df6114`: prepend `:199`, per-target append `:203`, `completed`
   `:221`. `:198` is blank.
8. **Carried unchanged:** `open_threads[0]` stays CLOSED; the Ecuador slug stays CONVERTED at zero
   budget with its closure condition unwidened and its paywalled-body synthesis QUARANTINED; the
   Einarsson family stays retired; the three escalated gaps stay at zero budget; cite the **blob**
   not the path for any reduction or digest artefact — **and §5 of Part I is this council's second
   demonstration in five weeks of why.**

## 7. Escalations to Emory

1. **WEB SEARCH IS DEAD, AND WITH FETCH ALREADY DEAD THIS SESSION HAD NO RETRIEVAL CHANNEL IT
   COULD AIM AT ANYTHING.** Full evidence at §1: five probes, three seats, three of them on
   neutral controls, all `Web search error: unavailable`. **This is a state change, not the
   standing constraint.** The relay still works but its rows are frozen before any seat is
   convened, so it cannot answer a question that arises mid-session. **On today's evidence the
   research question is not budget-limited or perimeter-limited — it is channel-limited to
   zero.** If this persists, the daily council can audit its own instrument and nothing else.
   **A related hazard, and it is new: a failed search returns the error AND plausible unsourced
   prose in the same response.** No seat fell for it today.
2. **STANDING ESCALATION 2 HAS BEEN FALSE FOR THREE DAYS AND IT REPORTS THE INERT HALF OF A LIVE
   DATA-LOSS DEFECT.** Full mechanism at §3. `analytics/council-log.md` carries **36 hand-written
   daily sections** and they *are* read; the true defect is that `_handwritten_sections` is
   **date-keyed**, so it stops protecting a section the moment the same date also exists in
   `state/council_log.json` — which every session since 2026-08-19 has caused. **Eight dates are
   armed (08-06, 08-21, 08-22, 08-23, 08-24, 08-30, 08-31, 09-09) and 75,103 characters of
   hand-written record would be replaced by one-line stubs**, yesterday's entry included. **It has
   not fired only because `_render_md` is called from the WEEKLY run (`src/main.py:838`), which
   has not succeeded since 2026-07-27 — 45 days.** **Fixing escalation 8 triggers escalation 2.**
   Credit where due: `2026-08-08.md:494-499` found and *measured* this mechanism at one date at
   risk; nobody has restated it since and the exposure has grown eightfold. `src/` is outside this
   session's merge scope. **The fix is one line — key the guard on section CONTENT or on a
   `generated: true` marker, not on the date.**
3. **THE INTEGRITY OFFICER IS ON THE WRONG MODEL AND TODAY IS DAY THIRTY** (basis:
   `2026-09-09.md:596` states the twenty-ninth; one session elapsed). Reported `claude-opus-5`
   against a directive of Claude Opus 4.8, disclosed unasked on its first line. **New today, and
   it is worse than a selector problem:** `.claude/agents/integrity-officer.md` at `8015833`
   carries `model: opus` at `:4` — a TIER — **while its own `description` at `:3` asserts in prose
   "Runs on Claude Opus 4.8." The file contradicts itself and neither half is enforced.**
   `.claude/agents/research-analyst.md:4` carries the same tier selector, so that seat's apparent
   conformance remains coincidence. One line closes it.
4. **THE TAXONOMY'S ENTRY-27 COLLISION IS THIRTY-FIVE DAYS OPEN, AND TODAY IS THE FIRST SESSION IN
   FIVE THAT IT DID NOT BLOCK — because the officer deliberately proposed nothing.** Six existing
   entries (14, 17a, 21, 26, 27, 28) each caught something today. **The seat's restraint is the
   right call and it should not be mistaken for the collision being harmless: it blocked four
   adoptions in the four preceding sessions.** One ruling closes it. `agents/` is outside this
   session's merge scope.
5. **ESCALATION 5, UNCHANGED AND UNTESTABLE TODAY.** `prompts/research_analyst.txt:158-163`
   mandates a Roberts query each session; `2026-07-30.md:220` subsumes it at zero budget *"until
   A/81/17 publishes or a Roberts item surfaces by another route"*; carrier
   `agents/Workflow Threads.md:1902`. **The analyst DID spend its first budget unit on the Roberts
   check today and the channel refused it** — so unlike yesterday, the release condition could not
   be tested at all. **Nothing whatever is asserted about whether Roberts has posted.** One line in
   either file closes the conflict; both are outside this session's merge scope.
6. **RULE 0 IS DEPARTED FROM FOR THE TENTH CONSECUTIVE SESSION AND I DECLARE IT RATHER THAN HIDE
   IT** — 09-03 through 09-10 carried in the record, 09-01 and 09-02 inferred (§2, `0910-N2`). Two
   subagents where Rule 0 permits one, because your cardinal rule against performed seats is the
   one I will not trade. Every concession that costs nothing was kept: **no analytics officer and
   no systems researcher convened**, the digest sentence is two lines copied from `meta.json`, the
   optimization item is one paragraph I wrote. **On today's evidence the gate produced four
   blocking objections, one of which — `0910-B1` — killed the chair's own headline claim, and
   neither of the other two seats found it.** The rule is still yours.
7. **THE PERIMETER IS NO LONGER THE BINDING CONSTRAINT — THE CHANNEL IS.** Yesterday's escalation 7
   asked you to rule on the relay allowlist because `unis.unvienna.org` is outside it. **That
   decision is still owed and is now second in line:** with search dead, the allowlist governs the
   only retrieval channel the council has, so every off-allowlist gap
   (`unis.unvienna.org`, `dailyjus.com`) is not merely slow to close but **unreachable by any
   means this session possesses.** The perimeter is yours.
8. **THE WEEKLY BRIEF IS SIX MONDAYS EMPTY — 08-03, 08-10, 08-17, 08-24, 08-31, 09-07 — AND THE
   WEEKLY RUN HAS NOT SUCCEEDED SINCE 2026-07-27, 45 DAYS.** No Monday has elapsed since the last
   statement of this counter (09-08 Tue, 09-09 Wed, 09-10 Thu), so it does not increment today;
   the next member would be 09-14. `Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced
   (0 matches, 0 watch-list leads)`, `validation_status_only: true`, unchanged. **Read this
   together with escalation 2: fixing it is what arms the data loss.**

## 8. Where this leaves the research question

**Unchanged, and today it is unchanged for a reason worth stating plainly rather than dressing an
instrument day as a research day: the council had no channel.** IP as a covered investment, a
regulatory or judicial measure as the challenged conduct, and a live litigated jurisdictional or
admissibility doctrine still meet in the three decided seed awards and in no new matter this
council has found.

`open_threads[5]` stands at a **67-day monitoring window** — computed as 2026-09-10 minus the
thread's earliest carrier 2026-07-05, **not** a post-cooling-off day count — with **no ICSID
registration of a *Wingtech v. Netherlands* proceeding found in accessible sources, and it is
never stated that none exists.** The matter remains a **Ring 1–2 near-miss: Ring 2 present, Ring 3
architecturally live but on the branch that does not bite, Ring 1 NOT established.** Today's relay
row put the thread's own rigid designator into the channel for the first time in 418 records and
returned a null that, under the pre-registered reading rule, licenses **only** a statement about
one literal in one rendering of one body at one instant.

**Nothing in Parts I–VI asserts anything about any dispute, docket, tribunal or claimant.** The
officer searched both returns and the candidate-claims block for any case number, party, tribunal
or matter drawn from an excerpt and found none.

**What today actually bought, stated at the strength that survived vetting:**

- **The instrument posture is materially worse than yesterday and we know it precisely.** Search
  is dead on three neutral controls across three seats. That is worth more than a guess at what
  the research question might have returned, and it was bought by a seat probing a claim its
  chairman had declined to make.
- **The daily merge is established as the recurring trigger of the relay's in-place overwrite** —
  40 runs, 37 successes whose targets carry ≥2 commits, 3 failures whose targets carry exactly 1,
  zero exceptions in either direction. **The overwrite itself was reported on 2026-08-05 and I
  wrongly called it unreported; what is new is the trigger, not the defect.**
- **My headline universal was false and the gate killed it.** Ten of 37 reductions were never
  overwritten. Two seats held the counter-example and neither recognised it; the third found it in
  the second's own text, filed under a heading calling it confirming.
- **A standing escalation has been going to you false for three days**, and the true defect under
  it is a live 75,103-character data loss, armed on eight dates, held back only by a second
  broken thing.
- **`git fetch --deepen` works when nothing else does.** One command, 142 commits to 1113. Every
  history-derived figure in this record is post-deepen, and no future session need presume.
- **Twenty-one objections, twenty sustained, none overruled, five against the chair — and the
  worst of them found by neither of the two seats that held the evidence.**

---

## 9. Procedural caveat — the Rule 0 / cardinal-rule conflict, and the close-out re-anchor applied in HALF

**The conflict, per the protocol's own Precedence section, which directs that it earn a line here
rather than be buried.** `prompts/daily_council_protocol.md` Rule 0 permits two convened agents
and makes the integrity challenge a voice inside Part V rather than a convened seat. **The
operator's cardinal rule forbids me performing any seat.** I resolved it by convening the officer
as a real subagent and placing its return *inside* Part V rather than giving it a Part of its own
— honouring Rule 0's structure while never performing a voice. **That is a departure from Rule 0's
seat count and I declare it as escalation 6 rather than harmonising it away.**

**The re-anchor, applied in half, and I name which half.** The close-out mandates
`python3 scripts/reanchor.py` as the last commit, staging `STATE_OF_THE_ANSWER.md` **and four
`agents/` notes**. I ran the script. **I commit only the `STATE_OF_THE_ANSWER.md` half and revert
the four `agents/` files, because `agents/` is outside this session's authorised merge scope**
(`analytics/`, `state/research_log.json`, `STATE_OF_THE_ANSWER.md`), and a PR touching it may not
be self-merged. **This is the same genuine conflict yesterday's chair declared and it is
unresolved: the protocol instructs the chair to do something the merge scope forbids, and every
session departs from one or the other.** `.github/workflows/reanchor.yml` runs on every PR to
`main` and is the machinery built for this path; the close-out step is the belt for the
squash-merge that does not wait for checks. **The cost is MEASURED, not guessed.** `python3 scripts/check_currency.py` at this branch's tip
reports **2 STALE of 9 claims checked** — `agents/Workflow Threads.md` (7 commits touched its
declared paths since `abeeb5b`) and `agents/Claim Map.md` (1). **The same check at the
pre-session tip `6df6114` reports 1 STALE**, `agents/Workflow Threads.md` alone. **So this
session's net cost to the currency guard is exactly one newly-stale note, `agents/Claim Map.md`,
and it is stale because I reverted the re-anchor the protocol told me to apply to it.** Both
failures are in `agents/`; **zero are in any path this PR touches.**

**The operator's call, and I am not making it
silently:** either widen the daily session's merge scope to include the tracked notes the protocol
tells it to re-anchor, or amend the close-out step to stage only what the session may merge.

---

## 10. Ledger note — this session deliberately arms a NINTH date under §3's defect, and says so

`prompts/daily_council_protocol.md` and `COUNCIL.md` both require the session to write
`state/council_log.json` **and** the readable section in `analytics/council-log.md`. §3 of this
close-out establishes that writing both is exactly what disarms `_handwritten_sections`' guard for
that date. **I have written both anyway, and I record the consequence rather than dodging it: the
armed set goes from eight dates to nine — 08-06, 08-21, 08-22, 08-23, 08-24, 08-30, 08-31, 09-09
and now 2026-09-10 — and the character count rises by the length of today's section.**

**Why I did not simply skip one register.** Suppressing a required ledger entry to keep a defect
from applying to me would leave the operator a smaller number and a quieter escalation, which is
the opposite of what §3 is for. **Every armed section remains recoverable from git; what is at
risk is the readable artefact, not the evidence.** The fix is one line in `src/council_log.py` —
key the guard on section content or on an explicit `generated: true` marker rather than on the
date — and `src/` is outside this session's merge scope.

---

*End of the 2026-09-10 daily council record.*

---

# ADDENDUM, APPENDED AFTER THE MERGE AND AFTER THE EMAIL — F4 GRADED, ALL FIVE LIMBS

**Disclosure first, because this council does not silently amend a record a reader has already
received.** The 2026-09-10 record merged to `main` at `aea5227` and `daily-update` wrote
`.sent/2026-09-10` at `f99403c`, so **the operator's emailed copy ends at Part VI §10 and does not
contain this addendum.** Nothing above is edited; this is appended, and it is appended because
F4's trigger is the merge itself — the one forecast this council has ever pre-registered that
could not be graded before the record was published. **Grading it now costs one section; leaving
it would have made tomorrow's next step 2 a re-derivation of something already settled.**

**F4, quoted verbatim from the committed request file (blob `129e83b9` at `c53c97e`):**
*"F4 - THIS REDUCTION WILL BE OVERWRITTEN IN PLACE BY TODAY'S OWN MERGE … the post-merge blob will
carry a different environment run id and a later fetched_at while the substantive sha256 stays
equal."*

**Graded limb by limb on the analyst's five settling conditions, adopted verbatim (`0910-A5`):**

| Limb | Condition | Observed | Verdict |
|---|---|---|---|
| i | two commits on the reduction at `origin/main`, later by `github-actions[bot]`, message `chore: fetch-relay results [skip ci]` | `8015833` 11:17:37Z and **`ededf19` 12:00:07Z**, both `github-actions[bot]`, both that message | **CONFIRMED** |
| ii | later blob ≠ `91186e69` | **`5560b5f`** | **CONFIRMED** |
| iii | later blob names a run ≠ 34470490415, `fetched_at` later than 11:17:37.181145Z | run **34474228028**, `fetched_at` **12:00:06.535722Z** | **CONFIRMED** |
| iv | `records[1].sha256` and `records[2].sha256` **both still** `470b8878…` | both **`470b88788b480bd0…`**; `records[2].find_matched` still `false` | **CONFIRMED** |
| v | runner-side: `event push`, `head_branch main`, `run_attempt 1`, `head_sha` = the merge commit | `event: push`, `head_branch: main`, `run_attempt: 1`, **`head_sha aea5227`** = the merge commit; `display_title: "council: daily meeting 2026-09-10 (#156)"`; `run_number: 101`, `conclusion: success`, `run_started_at 11:59:42Z` | **CONFIRMED** |

**F4 IS CONFIRMED IN ALL FIVE LIMBS — INCLUDING LIMB (iv), THE ONE BOTH VETTING SEATS FLAGGED AS
THE MOST LIKELY TO FAIL.** The officer's reasoning for that flag was sound and is worth preserving
against the outcome: `/cases/recent` had returned a different digest on every day observed
(`4af84337` 09-07, `f309eb63` 09-09, `470b8878` 09-10), so a limb predicting digest *equality*
across a 43-minute gap was the fragile one. **It held.** The forecast was conjunctive and the
conjunction survived; had it not, limbs (i)–(iii) and (v) would still have been confirmed, which
is exactly why `0910-A5` required limb-by-limb grading and why that requirement was right
independent of how the grading came out.

**What this establishes and what it does not.** It establishes that the daily merge re-fires the
relay against the session's own request file and overwrites the session's own reduction in place —
now observed prospectively, under a forecast frozen in a commit GitHub timestamps before the run
that answered it, rather than reconstructed afterwards. **It establishes nothing about ICSID's
website**: `470b8878` appearing in both blobs is a statement about two responses at two stated
times and is not a claim that any case listing is unchanged, stable, or current.

**Consequence for tomorrow: next step 2 is DISCHARGED HERE and is struck from the agenda.**
`0910-B1`'s corrected universal now has a prospective instance alongside its 40-run retrospective
derivation. **The defect itself remains unfixed and remains escalation 1's neighbour** — a one-line
`branches:` filter in `.github/workflows/fetch-relay.yml`, or an existence check in `run()`, both
outside this session's merge scope and neither on the operator's approved list.

**Also confirmed on `main` after the merge, and it closes the procedural caveat's open half:**
`.github/workflows/reanchor.yml` ran on the PR and committed `abdee03 chore(currency): re-anchor
tracked notes to HEAD`, moving the four `agents/` anchors this session was not permitted to move.
**`scripts/check_currency.py` at `origin/main` now reports 0 STALE.** The machinery reached the
path the session could not, exactly as Part VI §9 said it was built to. **The operator's decision
on the merge-scope conflict is still owed** — the session and the workflow currently cover for
each other by luck of ordering, not by design.

---

*End of addendum. Nothing above the addendum marker has been altered.*
