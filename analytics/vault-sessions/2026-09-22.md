# Archivist session — 2026-09-22

**Audited against `1cf6108`** (`main`), on a complete **1,531-commit** history. The container's
clone arrived shallow at **123** — the **tenth consecutive** session — so every sha below was
resolved only after `git fetch --unshallow`.

**The window is `f1a7850..1cf6108` — 70 commits**, three days.

**The short version.** For the second consecutive session **nothing this seat audits for contract
drift moved**: zero commits to `.claude/agents/`, `prompts/` or `src/models.py`, and zero to
`scripts/`, `tests/`, `.github/` or `src/`. The professor-facing surface moved once, and only as a
generated write. So this record is, again, the taxonomy plus carried state re-measured.

**But the taxonomy half is different this time, and it is the reason this session exists.** Entry
**41** and extension **40a** were adopted on 2026-09-20 and had not reached the vault. That is
**D15** at occurrences twelve and thirteen — and **for the first time the count is not mine**. The
integrity officer wrote the twelfth into its own memo on 09-21, **measured** the thirteenth on
09-22, and **cited extension 40a twice in a memo out of a file that did not carry it**. The
chairman escalated the same gap to you himself. Three seats spent part of three sittings
establishing what this note is supposed to tell them for free. Both are filed now.

**Two things got worse by sitting, and both are counted rather than characterised.** **D20** is at
day eight and the ungated-merge count has gone from twelve to **twenty**. **D16** produced five
new stranded branches after a window that produced none.

**Nothing new was found that needs you.** Everything on the escalation table is carried, and two
items have a measured growth rate for the first time.

---

## 1. D15 — entries 41 and 40a filed, and the seats bound by them are now counting the latency themselves

**Filed in this change set** — the first time either reached a note:

| Entry | Pattern | Adopted | Proposed / adopted |
| --- | --- | --- | --- |
| **41** | Invented limb | 2026-09-20 | `1d6e3e7` `2026-09-20.md:991` / `:1315` |
| **40a** | Unit-swapped screen record (extension of 40) | 2026-09-20 | `1d6e3e7` `2026-09-20.md:995` / `:1323` |

The canonical table's heading moved **40 → 41**; the live next free number is **42**; **nothing was
renumbered** — 41 to the tail, 40a beside its parent, both under your rule of 2026-09-13. Six
matching rows were added to [[Agent Registry]]'s adopted-method-rules table.

**Two further patterns were proposed by name and left unnumbered**, and are filed in the pending
block below the table rather than in it: *one-pass artefact read* (2026-09-20, `:1334`) and
*screen-record self-falsification* (2026-09-22, `:946`, `a814d18`), the latter adopted together
with the analyst's narrower mechanical variant of it (`:1218`, `a9bc44f`). **The pending block now
holds five patterns, three of them binding practice.** This seat does not issue numbers the council
did not issue.

**Now the part that prices the routing question, and I did not have to construct it — the record
constructed it across three consecutive days.**

- **2026-09-20** — the officer opened the table, verified the heading was current, numbered **41**
  off it correctly (`:788`). The chairman then **declined a second number in the same close-out**
  and said why: *"`agents/` is outside this session's merge scope, I am already issuing 41 into a
  table I cannot edit, and a second number issued blind into the same unwritable table is how the
  entry-27 collision happened"* (`:1334`).
- **2026-09-21** — the officer opened the same heading and found it behind **by its own previous
  day's adoption**: *"Entry 41 and extension 40a — both adopted by you yesterday and both binding
  on me today — have not reached the vault. … This is the twelfth consecutive occurrence of the
  D15 routing latency the vault itself records"* (`2026-09-21.md:753`, `224cbfb`).
- **2026-09-22** — the officer **measured** the gap instead of asserting it — *"`INVENTED LIMB`
  occurs 0 times and `40a` occurs 0 times in that file. I measured all three"* — declined a number
  for the second day running, and **cited 40a twice in the same memo, at `:816` and `:880`, out of
  a file that does not contain it** (`:942`, `:944`, `a814d18`). You have it as item 8 of his
  close-out (`67ef197`).

**What that cost, stated precisely so it is not inflated.** No objection was wrong. No ruling was
affected. The entire cost was paid in three seats' verification time and in a daily record that now
documents its own memory failing. **That is the 2026-08-03 failure in its mild form** — a note that
was real, correct, and for two days invisible — and the mild form is the one worth catching,
because it is the one that does not announce itself.

> **NEEDS YOU — the choice is unchanged and only its evidence has grown.** Give the daily council
> write access to the taxonomy table in `agents/integrity-officer.md` **alone** — a one-file scope
> widening — or accept the latency and have the council's close-out carry an explicit *"UNFILED —
> routed to archivist on `<date>`"* line. **I can execute either and neither is mine to decide.**
> What this window adds: the risk a widening would create is a blind number issued into the table,
> and **the chairman named that exact risk on 2026-09-20 as his own reason for declining one.**
> The behaviour the widening would rely on is the behaviour he demonstrated unprompted, on the day
> he had the strongest reason not to.

---

## 2. NEEDS YOU — D20: `main` is red at day eight, the drift has doubled, and twenty merges have now gone in ungated

`pipeline-guards` run **232**, head `3eb1499`, 2026-09-22 12:13Z — **failure**.

**Re-measured against the live archive this session, not restated from the last record.** I
measured before reading the chairman's figures; they agree to the digit.

| Assertion | Pinned in `tests/` | Live archive |
| --- | --- | --- |
| `runs` | 16 | **18** |
| `screened` | 492 | **520** |
| digests carrying `per_source` | 11 | **13** |
| `per_source` sum | 191 | **219** |
| their `screened` | 230 | **258** |

**Every one of the five deltas is exactly the two digests 2026-09-14 (7 screened) and 2026-09-21
(21 screened).** Single cause, two failing tests, and the fix is **five numbers** at
`tests/test_site_claims.py:155-156` and `:305-308`.

**What eight days has cost, recounted.** `git log 529e32d0..origin/main --grep='(#[0-9]'` now
returns **20** squash-merged pull requests, **#192 through #211**, plus one direct commit
(`37fd77e`). **Three days ago it was twelve.** Every one went in against a red base, so not one of
the twenty was gated by the suite.

**And this is the only thread on the list with a known growth rate.** The drift grows by one digest
**every Monday**, because the weekly digest writer changes the published figures the constants are
pinned against. On 2026-09-19 it was one digest of drift; today it is two.

> **NEEDS YOU — same ask as 2026-09-16 and 2026-09-19; only the price has changed.**
> **systems-designer or site-experience:** re-measure the five constants above in one change set.
> **Emory:** the second-order question is still the one that matters — the weekly digest is a
> scheduled writer that changes published figures *and* carries `[skip ci]`, so the commit that
> breaks the suite is the one commit the suite never runs on. Either those commits run the suite,
> or the constants are derived by the job that writes the digest. **I price neither**; `tests/`,
> `scripts/` and `.github/` are all outside this seat's paths. The chairman independently filed the
> same two asks today as his escalations 5 and 6 (`67ef197`).

---

## 3. Agent-context audit — no contract drift anywhere, and the model gap is at day thirty-eight

- `git log f1a7850..1cf6108 -- .claude/agents/ prompts/ src/models.py` returns **zero** commits
  across the 70-commit window. **No seat's contract was edited by anyone**, for the second
  consecutive session.
- All nine definitions declare `model: opus`. `scripts/check_models.py` exits **0** over 12 cards;
  `scripts/check_currency.py` exits **0** over 9 claims on arrival (10 after this record's own
  anchor); `node tools/isds-workflow-3d/validate.mjs` exits **0** — 30 cards, 10 chips, 44 edges,
  SVG fresh against its inputs-sha256, zero "Jack" tokens.
- **"Jack" discipline:** professor-facing surfaces (`README.md`, `docs/`,
  `scripts/site_templates/`, `METHODOLOGY.md`, `COUNCIL.md`, `HANDOFF.md`) are **clean — zero
  occurrences**. The two known live defects are unchanged and both outside my paths:
  `HUMAN_REVIEW.md:115` and `prompts/council_roundtable.txt:73`. (`HUMAN_REVIEW.md:173` is your
  signature as reviewer and is correct as written.)
- **The method rules each seat has adopted are, where they matter, written where that seat will
  read them — with the one structural exception this vault has recorded for thirty-nine days.**
  The four rules the council adopted on 09-21 and 09-22 are now in [[Agent Registry]]; the two
  taxonomy entries are in [[integrity-officer]], which is **the only definition that names its own
  vault note**. That is D5, below, and it is the reason §1 exists.

> **TO VERIFY — D6, eighth first-person observation, day thirty-eight.** I queried this session's
> own runtime: `configured_model`, `session_context.model` and `external_metadata.last_served_model`
> are all **`claude-opus-5`**. `.claude/agents/obsidian-archivist.md:3` and `:24` say "Runs on
> Claude Opus 4.8." **REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`.** Consistent with
> 2026-08-16, 08-25, 09-04, 09-07, 09-13, 09-16 and 09-19. `check_models.py` cannot see this and is
> not failing — it compares three declarations to each other, never a runtime. **I have again left
> the rows unchanged**, because they record your directive and rewriting them would ratify a
> substitution nobody authorised.

**The description split, re-enumerated from the files rather than carried** — the discipline of
taxonomy entry 40, which D6 owes because its whole argument is a count:

| | Count | Seats |
| --- | --- | --- |
| Description says **Opus 5** | **4** | council-chairman, research-analyst, site-experience, systems-designer |
| Description says **Opus 4.8** | **5** | analytics-officer, integrity-officer, obsidian-archivist, research-editor, systems-researcher |
| Names no model | **0** | — |

**4 / 5 / 0** — identical to the corrected 2026-09-19 figures, which is what a zero-commit window
should produce and is why I state it as a measurement rather than as "unchanged".

**D5 unchanged at day forty.** Eight of nine definitions never name their seat's own vault note;
the integrity officer's remains the sole exception. Editing `.claude/agents/` is a contract change
and is yours.

---

## 4. Where a note would have prevented a recorded failure — recorded in the note, as the mandate requires

Filed at [[integrity-officer]]'s taxonomy heading and again in its change log:

> Had this note been current on the morning of **2026-09-21**, the officer would not have had to
> write a vault-currency caveat into its memo; had it been current on **2026-09-22**, the officer
> would not have spent a measurement establishing the gap, and **its two citations of extension
> 40a would have been sourceable at their own authority instead of at a file that does not carry
> the rule.**

That is a precise, bounded, verifiable statement of what staleness cost, and it is written where
the next seat to open that file will see it.

---

## 5. Deliverable drift — the surface moved once, and only as a generated write

`git log f1a7850..1cf6108 -- README.md docs/ scripts/site_templates/ METHODOLOGY.md HANDOFF.md
COUNCIL.md views/isds-workflow-3d/workflow.json` returns **one** commit: `1eb4235`, *chore: weekly
digest + state update `[skip ci]`*. It adds `digests/2026-09-21*` and regenerates 20 pages under
`docs/`. **No prose changed and nothing was passed over.**

**D7 re-derived from the code today rather than restated**, and it has got one file worse:

> **FOR EMORY — `METHODOLOGY.md` §III, two numbers, and the isolation now widens weekly.**
> 1. `METHODOLOGY.md:33` says Bing News is polled through **eight** fixed, fingerprint-derived
>    queries. `len(bing_news.QUERIES)` is **12**.
> 2. The same sentence enumerates seven open repositories plus two added channels — **nine** —
>    while `len(all_sources())` is **10**.
>
> **Where GDELT is named today:** `README.md` **1**, `docs/` **10 files** (nine on 2026-09-19 — the
> 2026-09-21 digest page landed at `1eb4235` and carries it), `scripts/site_templates/` **1**,
> `METHODOLOGY.md` **0**. Every professor-facing surface names the tenth source except the one
> document a reviewer reads to judge the method, **and the gap widens by one file every week the
> digest writer runs.** It is a two-number edit and it is yours — this seat does not edit your
> prose. Tracked as **D7**.

**Escalated, not touched:** the flowchart manifest is the systems designer's; `README.md` and
`scripts/site_templates/` are site-experience's; `METHODOLOGY.md` is yours. Nothing in this change
set is outside `agents/` and `analytics/`.

---

## 6. Vault mechanics — clean, and clean the first time again

**Graph:** measured on the tree before this record was written — **194 notes, 402 edges, 0
orphans**, 84 managed blocks would update, **17 WARNs**. Growth since the 2026-09-19 record's own
post-write measurement (191 / 399): **+3 notes, +3 edges**. All 17 WARNs are index or session notes
exceeding the four-link cap, expected under the standing convention; none is drift.

**Broken wiki-link targets: four, across three files, all under `think-tank/`, all long-standing,
all outside my paths.** Unchanged from 2026-09-19 and 2026-09-16.

**No broken link was introduced by this change set.** I ran `build_graph.py --dry-run` after **each**
of the six note edits rather than once at the end — the habit the 2026-09-16 record earned the hard
way. Nothing to report, which is the point of saying it.

**The thread-ID collision is unchanged: three pairs.** `grep -o '^### [A-Z][0-9]*' | sort | uniq -d`
still returns **B5, C11, C12** across 75 threads. Not renumbered, same disposition and same reason.
Your ruling of 2026-09-13 on the taxonomy is the template this needs.

**C8 is still real in the code and still not firing.** `scripts/build_graph.py:198` remains
`pat.sub(block, text)` with no count argument. The managed blocks in this vault are regenerable and
were not hand-edited in this change set.

---

## 7. D18 — 1 of 3 days red, and this window's red was due on the calendar

`main` is **green** as I write: `check_currency.py` exits **0** at `1cf6108`.

Same method as the last three sessions — `main`'s tip at the end of each UTC day, every tracked
note's anchor evaluated against it, in a detached worktree.

| Day | Tip | Verdict | What staled it |
| --- | --- | --- | --- |
| 09-20 | `5df71d2` | green | tip was a re-anchor |
| 09-21 | `1eb4235` | **RED** | `chore: weekly digest + state update [skip ci]` |
| 09-22 | `1cf6108` | green | tip was a re-anchor |

The one red carries **exactly one** STALE line and it is [[Workflow Threads]] — *"2 commit(s)
touched its declared paths since `c6db37e`"* — the other four tracked notes clean. **Fourth
consecutive session in which every red resolves to that one note's `analytics` declaration meeting
a `[skip ci]` bot writer.**

**One thing worth separating out.** The last two sessions read this rate as tracking how often the
day happens to end on a re-anchor — essentially a coin flip. **This window's red is not that.** Its
writer is the **weekly digest**, which runs on a fixed weekly cadence, so one red per week is
*due*, not chance. The other three named writers stay random. I am recording the distinction rather
than the rate.

**The fix I am still NOT proposing**, restated: narrowing this note's declared paths so its own
guard stops going red would be this seat marking its own homework, which is the precise failure the
guard exists to prevent. The two real options are unchanged and both need pricing I cannot do —
drop `[skip ci]` from the bot writers, or give `reanchor.yml` a scheduled run after the day's last
automated writer. **Still systems-designer's.**

---

## 8. Orphan sweep — the 2026-09-19 zero was one clean window, not a fix

**182 remote refs** (excluding `main`); **38 are not ancestors of `origin/main`**, up from 33. The
repository squash-merges, so a squash-merged tip is never an ancestor — **I classified by diff
content, not by counting.**

**Five are new since 2026-09-19, and all five are the D16 mechanism:**

| Branch | Tip | Insertion side |
| --- | --- | --- |
| `council/daily-2026-09-20-addendum` | `0d598a6` | anchor lines + superseded generated pages |
| `council/daily-2026-09-22` | `20ff7c5` | **5 insertions, all five anchor shas** |
| `council/daily-2026-09-22-addendum` | `11273b4` | **5 insertions, all five anchor shas** |
| `vault/archivist-2026-09-19` | `fac4fe0` | anchor lines + superseded generated pages |
| `vault/archivist-2026-09-19-addendum` | `fce1334` | anchor lines + superseded generated pages |

**No substantive work is stranded in any of the five.** The last record reported zero new orphans
and said so as a fact about that window; **it is not a fix, and three days were enough to show it.**

The carried set, none of it moved:

| Branch / PR | Tip | What is stranded | Age |
| --- | --- | --- | --- |
| **PR #184** `fix/repoint-r21-citations` | `529c744` | Ruling 1's citation half, 409 insertions — §9 | **9 days** |
| `council/analyst-gaps-and-methodology` | `74958fd` | 9 files, 26 insertions; outside my paths | 11 days |
| **PR #150** `chore/reanchor-2026-09-08` | `f73ac06` | anchor edits only | **14 days** |
| **PR #110** `vault/archivist-2026-08-28` | `de73f9f` | five note edits; the 350-line record is safely on `main` | **25 days** |
| `chore/operator-marks-2026-07-27` (**F1**) | `6f9e1da` | 20 operator ledger marks | **57 days** |
| `feat/research-council` | `af00584` | `METHODOLOGY.md` + 8 June digests | 92 days |
| `claude/sweet-mccarthy-*` (×3) | — | 07-30/07-31/08-01 council records | 52–54 days |
| the rest (**24**) | — | stranded anchor edits and `.sent` markers only | — |

**PR #110 is at day twenty-five** and I again did not cherry-pick it, for the reason six prior seats
gave and which still holds: those are live statements about a repository that has moved far past
them, and re-deriving beats importing. **F1 is at day fifty-seven.**

`vault/archivist-2026-09-16` (`77c9036`) is still on `origin`, as the 2026-09-19 record predicted.
**Branch deletion still fails from this container — fourth consecutive session.**

---

## 9. PR #184 — day nine, and the guard it carries is still dormant while the condition it guards is true

Re-measured on `main` today, not taken from the branch.

PR **#184** is open at **day nine**, head unchanged at `529c744`, last updated 2026-09-13T22:49Z —
**nothing has been pushed to it and nothing has been said on it.** `src/rings.py:923` and
`scripts/check_lock.py:20` both still carry the bare *"R2.1 record"* citation on `main`, and the
record they should point to — `analytics/locked_set/VALIDATION_RECORD.md` — **is** on `main`.

**So the branch's strict-xfail forcing function — written so it fails the build the moment the
cited record can be opened — has now been dormant for nine days while exactly that condition is
true.**

The chairman noted the PR's age again on 2026-09-22 and classed it *"hygiene, not a finding"*
(escalation 9, `67ef197`). That is right about the council's own exposure and still does not reach
the dormant guard, which is why the thread stays open.

> **NEEDS YOU — land PR #184, or close it and say which of those two citations stands as written.**
> Every path in it is outside this seat's merge scope, so I cannot land it and did not try. Tracked
> as **D19**.

---

## 10. What I could NOT verify

Stated rather than omitted.

- **`scripts/check_currency.py` reported three hard `FAIL`s on arrival and zero after
  `git fetch --unshallow`** — `373cce6`, `9efafb0`, `ae42639`, *"is not a commit"*. **I screened
  before writing this up:** they are the **same three shas** as the standing caution this vault
  recorded on 2026-08-13, and PR #150's body flags the same three. **Carried, not new** — tenth
  recurrence, agent-session-only, CI unaffected (`fetch-depth: 0`). I record it because a guard
  whose shallow-clone failure mode is **indistinguishable from real drift** will cost a session
  that does not know it, and because a first-ness claim here would have been exactly the defect
  taxonomy entry 28 exists to catch.
- **Why the clone keeps arriving shallow** — tenth consecutive session, now at **123** commits
  against a real 1,531. I can show it happens; I cannot see the container provisioning.
- **`gh` is still not installed in this container**, for the fourth session running. My standing
  instructions say to close with `gh pr create` and `gh pr merge`; I used the GitHub MCP tools
  instead, which reach the same API. **Flagging it a fourth time so the instruction and the
  environment get reconciled rather than silently diverging** — the instruction is not wrong about
  what should happen, only about how.
- **I did not run the test suite locally this session.** The 2026-09-19 record established that it
  takes four rounds of dependency installation and that a third failure
  (`tests/test_one_pagers.py::test_missing_pdf_skips_case`) is a container artefact from gitignored
  `seeds/`. **I measured the five drifted constants directly against the archive instead**, which
  is the number D20 actually turns on, and read the failure from CI run 232. Saying so plainly
  rather than implying a run I did not do.
- **I did no external Obsidian/PKM research this session and am not dressing anything up as it.**
  My mandate asks for it *periodically*; the 2026-09-16 session did a bounded ecosystem scan whose
  finding was that nothing needs changing, and six days is not long enough for that to have
  changed. The session's time went to the taxonomy filing and the D20 re-measurement instead.
- **Whether D11 fires on this pull request** — I cannot know before pushing. If it does, the
  addendum is below; if there is no addendum, it did not.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D15** — taxonomy 41 and 40a adopted 09-20 and unfiled until today; occurrences twelve and thirteen. **The bound seats counted the latency themselves this window**, and a seat cited 40a twice out of a file that did not carry it | **Emory** (routing or cadence) | 15 days |
| 2 | **D20** — `main` red since 2026-09-14, now day eight; **twenty** merged PRs (#192–#211) ungated, up from twelve; five constants need re-measuring; **drift grows one digest every Monday** | systems-designer / site-experience; **Emory** for the writer question | 6 days |
| 3 | **D19** — PR #184 at day nine; `main` still carries two dangling "R2.1 record" citations, and the branch's forcing guard is dormant while its condition is true | **Emory** | 6 days |
| 4 | **D18** — 1 of 3 days red; this window's red was the weekly digest, so one red per week is due rather than chance | systems-designer / **Emory** | 9 days |
| 5 | **D7** — `METHODOLOGY.md` §III: eight queries against 12; nine sources against 10; still the only surface that does not name GDELT, now against 10 files in `docs/` | **Emory** | carried |
| 6 | **D16** — five new stranded-anchor branches after a zero window; branch deletion fails from this container for a fourth session | **Emory** (one repository setting) | carried |
| 7 | **PR #110** — 2026-08-28 session's five note edits still stranded | **Emory** | 25 days |
| 8 | **Thread-ID collision** — three pairs (B5, C11, C12), unchanged; your 2026-09-13 taxonomy ruling is the template | **Emory** to authorise; archivist to execute | 18 days |
| 9 | **D6** — seat models: requested Opus 4.8, served `claude-opus-5`; eighth first-person observation. Split re-measured at **4 / 5 / 0** | **Emory** | 38 days |
| 10 | **D5** — eight of nine definitions never name their own vault note | **Emory** (contract change) | 40 days |
| 11 | **F1** — operator-marks branch never merged (`6f9e1da`), 20 ledger marks | **Emory** | 57 days |
| 12 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |
