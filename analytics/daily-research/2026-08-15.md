# ISDS Thematic Watcher — daily council, 2026-08-15

**Chairman:** the scheduled cloud session, running as the chairman seat on Claude Opus 5 per the
operator's model directive of 2026-08-03.
**Convened:** `research-analyst` as a real subagent; the integrity challenge heard inside Part V per
Rule 0, also as a real subagent. Both returns reproduced verbatim.
**Not convened, per Rule 0:** `analytics-officer`, `systems-researcher`.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a five-part meeting with a separately-filed
integrity officer (Part 3), an analytics officer (Part 4) and a systems researcher (Part 5).
`prompts/daily_council_protocol.md` Rule 0 says the daily meeting is **the researcher, the chairman,
and one optimization item**, that the integrity challenge is heard inside the Part V dialogue and files
no separate return, and that the analytics officer is not convened at all — the digest sentence is two
lines the chairman copies from `meta.json`. The protocol file's own Precedence clause says it wins, and
that the conflict is worth a line in the record's procedural caveat. **This is that line.** I ran the
meeting to Rule 0 in all respects but one, named here: Rule 0 says "convene exactly two agents… nothing
else", and three were convened. The integrity officer was convened as a **real agent**, because the
alternative — a chairman writing the challenge to his own work — is the failure this project treats as
cardinal. Rule 0 governs where that voice is *filed*, not whether it is *real*. This is the resolution
the 2026-08-13 and 2026-08-14 seats reached, and I adopt it deliberately rather than by drift.

### Instrument state, probed at session open

Both fetch instruments are dead, and both were probed rather than assumed. The relay is alive and did
the day's retrieval.

- **Direct egress: DEAD.** `curl https://example.com/` returned `CONNECT tunnel failed, response 403`
  at the proxy gateway; `curl https://en.wikipedia.org/wiki/Main_Page` returned the same. Two hosts,
  both neutral controls chosen precisely so the answer cannot be about a research source.
- **The fetch service: DEAD.** `WebFetch` on `https://example.com/` — the neutral control — returned
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`.
- **The fetch relay: ALIVE**, and it did the day's retrieval. **One batch, one run**, answered by
  `github-actions-runner (run 31881560690)` at `2026-08-15T11:15:28.184997+00:00`. Pre-registered
  before any result existed. Details in Part II.
- **WebSearch: alive**, and it was the analyst's only instrument.

Per the standing rule that a tool-layer status is never a statement about a resource: none of the above
is a fact about UNCITRAL, UNCTAD, ICSID, the PCA, italaw or any document. It is a fact about this
session's plumbing, and it is why the relay exists.

### Agenda

Three items, all drawn from the 2026-08-14 close-out's next steps.

1. **Next step 1 — slide the window on the *Einarsson* docket, and do NOT guess another label.**
   Yesterday's row design failed because it guessed field labels; the close-out's instruction was to
   anchor on a *later visible item* instead, `find: "Expert Report of Nigel Bankes"`, a string this
   instrument had already returned from this page. One string per row, never bundled.
2. **Next step 3 — `https://pca-cpa.org/en/cases/336/`**, triaged in the close-out as the single best
   relay row available and as relay-reachable. The question is **identity**: which numbered proceeding
   is at that URL.
3. **Next step 6 — the A/81/17 watch continues** in the standard same-page positive-control form, with
   the four standing scope limits stated rather than assumed, and per `0813-B9` with the population of
   any null-count stated alongside the count.

Two rows were added beyond the close-out's list, both on `investmentpolicy.unctad.org`: the standing
`Denial of benefits` question on the China–Switzerland BIT (2009) mapping page, and the *Zeph v.
Australia (II)* Navigator entry at case id 1301 that the analyst's 2026-08-14 Finding 3 surfaced
(the verb is `0815-O5`'s: "identified" is the word `0814-B5` struck).
Both were fired knowingly into a four-run refusal condition, and **pre-registered under G2 as ONE
observation of one client-access condition, not two** — a discipline recorded before the result
existed, not after it.

The analyst was pointed at the one thing a search can actually advance today: **identifying the three
people ICSID's own docket names as authors of expert and witness evidence** in the most on-theme
pending case this project holds.

**Spend checkpoint, placed here per the routine and honoured after the vetting round:** it is recorded
in Part VI, after the objections, not before them.

### Member return-path protocol

The analyst was briefed that his return enters the record **verbatim** as final record text; that
direct fetch and the fetch service are both dead and WebSearch is his only live instrument, so
everything he finds is **title-carried at best**; that today's relay batch was already fired and is
**mine**, so he must not fire another and must not spend budget on the four questions it asks; that the
standing tool-status-as-source-state rule is cardinal; that `0811-B4` (title-vs-summary), `0811-B5`
(unenumerated superlatives), `0811-B6` (disposition-vs-rule and dropped qualifiers) and `0813-O6`
(precise locators) bind him; that `0814-B1` (SELF-HEADLINE OVERREACH) binds him and that a backtick in
this record is a character claim; that the **two** screens adopted on 2026-08-13 are standing steps
before his first search; that no figure may rest on a search-engine generated summary; and that his
budget was **four WebSearch calls, hard**. He was expressly told not to re-run yesterday's four Zeph
queries, not to re-run the IAReporter domain query, and not to try to settle the China RDP tier
conflict by search. He was told in terms that an under-budget return counts as a good outcome and that
I would defend a null in the record.

**He was also sent a mid-flight correction, and it was a correction against me.** See Part II P1: the
name `“Chip” Gordon`, which my brief gave him as one of three targets, turned out to be a **truncation
artefact of yesterday's window**. I sent him the corrected string as soon as the reduction landed,
told him not to re-spend to repair the loss unless he judged it worth more than the alternative, and
told him to record the wasted spend against me rather than himself.

The integrity officer received the analyst's return, the relay reduction, and this record's own draft
parts, and was told its objections bind me as well as him. Both returns are reproduced without editing.

### The one optimization item (Rule 0)

**A digest change is not evidence of anything until you know whether the URL's digests are stable. The
council has asked this before — `2026-08-05.md:898-902` proposed a per-run stability block and predicted
from the archive that `investmentpolicy.unctad.org` would read `UNSTABLE-WITHIN-RUN` from its first
batch, and `2026-08-12.md:232-253` and `2026-08-13.md:229,239,440` each measured it on individual URLs.
What no seat has done is derive it across the WHOLE corpus in one pass, which costs nothing.** Derived —
`grep -rn -i "within-run\|same run.*digest\|per-response digest" --exclude-dir=.git analytics/ | grep -v 2026-08-15`.

*(This item was rewritten after vetting. Its first draft opened "but no seat has ever asked it" — an
absence claim about the record filed without a screen, which is the `0811-B5` move and exactly what the
grep-before-asserting rule exists to stop. Sustained at `0815-B6`.)*

Yesterday's record reasoned that `uncitral.un.org/en/commission` holding a constant 832,338 bytes
across a sha256 change was "evidence against reading it as per-response digest instability on this
URL." Today's batch put two rows on one PCA page in one run and got **two different digests at
identical byte length**, which made the general form of that inference worth testing rather than
assuming. Derived over the whole of `analytics/fetch-results/*.json` — every group of two-or-more rows
on the same URL in the same run at constant byte length:

| URL (host in parentheses) | within-run groups, digest-IDENTICAL | digest-DIVERGENT |
|---|---|---|
| `uncitral.un.org/en/commission` | 9 | 0 |
| `example.com/` | 8 | 0 |
| `icsid.worldbank.org` case-detail (2 URLs) | 3 | 0 |
| `pca-cpa.org/en/cases/309/`, `/336/` | 0 | **2** |
| `investmentpolicy.unctad.org` treaties path | 0 | **5** |

**Digest stability is URL-SPECIFIC, and it is measured per URL and only ever INFERRED to a host** — the
distinction `0813-B6(c)` sustained two days ago and which this table's grouping must not quietly undo.
**Three of the five rows rest on a single URL each**, including both divergent rows' hosts, so nothing
here is a host-level constant. On two of the five URL groups we have ever double-sampled, a
constant-bytes/changed-digest signature is that URL's ordinary behaviour rather than a signal — **on
`pca-cpa.org` from two observations, which is a lead about that host and not a characterisation of it**.

**The consequence is ONE-DIRECTIONAL, and the record fixed that direction on 2026-08-05 as a condition
of adopting this very measurement** (`2026-08-05.md:902`): a within-run control *"can only ever
**refute** stability, never establish it. Two identical hashes three seconds apart say nothing about a
document across days… and must never be cited as evidence that a document did not change."* **So this
derivation does NOT corroborate yesterday's UNCITRAL inference and I withdraw that claim**: 9 of 9
within-run groups identical on `/en/commission` fails to refute stability on that URL and is not
evidence about it across days. What the derivation does carry is the refutation: on `pca-cpa.org` and
`investmentpolicy.unctad.org` the same reasoning would have been flatly wrong, which is enough to make
the general form of the inference unsafe. **The screen is therefore worth running before reasoning from
a digest change only to look for a REFUTATION; a clean result licenses nothing.**

*(The withdrawn sentence was sustained against me at `0815-B8`, and the officer named it as the
asymmetric filter I had asked it to look for: I read a divergent result as establishing a host's normal
behaviour and an identical result as corroborating, when the record already held the ruling that only
one of those directions is available. `0815-B7` corrected the host-level framing. The item is a
**screen, not a feature**; **nothing was built and nothing was applied**, and the operator's PENDING
list is untouched.)*

---

## Part II — The relay batch: the window slid and returned further named docket items, a Zeph proceeding identified by its own docket number, a tenth null, and a fifth refusal

*(Heading corrected by me before vetting, then corrected again at vetting; the changes, the defects
and the rules are recorded at Part VI, corrections 1 and 3.)*

The batch is `analytics/fetch-requests/2026-08-15-daily.json`, pre-registered before any result
existed, answered at `2026-08-15T11:15:28.184997+00:00` by `github-actions-runner (run 31881560690)`.

**Batch validity, discharged by me rather than left to the officer.** `control_ok: true`, `void: false`,
`truncated: false`, `excerpt_chars: 400`, `requested: 9`, **`completed: 7`** — the two shortfalls being
the UNCTAD refusals at P4 below. *(Per `0815-O8`: the `records` array has ten entries against
`requested: 9` because `records[0]` is the relay's OWN control row — `example.com`, `find: ""` — which
the relay adds itself and which is not one of my nine. The 08-14 reduction has the same shape.)* My own `example.com` brackets are `records[1]` and `records[9]` of the
reduction: **both ok/200 with `find_matched: true`.** Every row below is therefore readable within its
own pre-registered scope.

**Relay code pin.** `scripts/fetch_relay.py` at this branch's HEAD is blob
`7204847f908d7958584842e523da8ae0ecd08703`, **verified by blob equality and not by history walk** per
`0811-O1` — identical to the blob pinned in the 2026-08-14 batch and in all three 2026-08-13 batches.

**Citation convention.** Per taxonomy entry 25 (MUTABLE-REDUCTION CITATION, adopted 2026-08-05), a
reduction is cited at the **commit** that wrote it, because the relay overwrites results in place.
Today's reduction is pinned at **`f463d08`**. Only one batch ran today and nothing was overwritten; the
`zz-` convention was not needed because no second batch was fired, and the first batch's filename was
checked against `sorted(glob('*.json'))[-1]` **before** it was pushed rather than after.

**Frame note, carried from `0814-O3`.** Row references in this Part are `records[n]`, 0-based positions
in the REDUCTION's `records` array. The pre-registration's decision rules E1–E5 number rows off the
REQUEST's `urls` array, which is a different frame; both are correct and I do not mix them.

### P1 — the window slid as instructed and reached further into the same run of text, one of whose items shows that a name in yesterday's record was cut in half

`records[2]`: `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6`,
`find: "Expert Report of Nigel Bankes"` — **ok/200, 105,669 bytes, sha256 `b9c55ab5…`, MATCHED.**

The window, verbatim, and it is the whole of the evidence:

> "er Witness Statement of Russell John Einarsson (Public Version) Witness Statement of Ralph Maitland
> Expert Report of Nigel Bankes Expert Report of “Chip” Gordon C. Gill Expert Report of Paul Sharp,
> Pricewaterhouse Coopers LLP (Public Version) Witness Statement of Harold Paul Einarsson (Public
> Version) Claimants’ Memorial RER-03 - Expert Opinion of Doug Uffen RER-02, Expert Report of Robert
> Hobbs R"

**The method worked, and it is worth naming why.** Yesterday's rows failed because they guessed field
labels. This row guessed nothing: it anchored on a string the instrument had *already returned from
this page*, and slid the 400-character window forward past where the previous one stopped. That is the
close-out's own prescription and it is now tested.

**That the two windows OVERLAP is derived by string equality, not assumed from the fact that they look
continuous.** The longest suffix of yesterday's window that equals a prefix of today's is **exactly 160
characters** — computed over the two stored excerpts, `2026-08-14-daily.json` `records[3]` (105,670
bytes, sha `4573db23…`) and `2026-08-15-daily.json` `records[2]` (105,669 bytes, sha `b9c55ab5…`).
**These are two renderings retrieved a day apart at different byte lengths and different digests, not
one rendering**, and the overlap is what licenses reading them as the same run of text across both. So
today's window reproduces the last 160 characters of yesterday's and then extends **240 characters**
past it. This matters for two reasons. First, it entitles this record to speak of one list read twice
rather than two unrelated fragments **on the assumption, unverifiable from a 400-character excerpt, that
a 160-character run appears once in each rendering**. Second, it is the proof of the truncation flagged below: the string `“Chip” Gordon` at
yesterday's right-hand edge continues, in today's overlap, as `“Chip” Gordon C. Gill`. **The
demonstration is a within-corpus string comparison; it is not a second retrieval and it corroborates
nothing about the page beyond what the two windows themselves carry.**

**What is ESTABLISHED.** ICSID's own case-detail page for UNCT/20/6, at that instant, rendered the
above run of text.

*(This paragraph was rewritten by me before vetting, and again at vetting; the changes, the defects and
the rules are recorded at Part VI, correction 2. Correcting an error before the officer sees it does not
make it a smaller error.)*

**What the 240-character extension contains, stated by inspection of the extension itself rather than
by counting the window.** Beyond the 160-character overlap, today's window carries the completion of
one item that yesterday's window cut — `“Chip” Gordon` continues as `“Chip” Gordon C. Gill` — followed
by these further items in the same run of rendered text, quoted exactly and not completed — **the relay
strips every tag and attribute (`scripts/fetch_relay.py:78-79`, applied at `:126-128`), so no delimiter
survives and the boundaries between them are read from the prose, not parsed:** `Expert Report of Paul Sharp, Pricewaterhouse
Coopers LLP (Public Version)`; `Witness Statement of Harold Paul Einarsson (Public Version)`;
`Claimants’ Memorial`; `RER-03 - Expert Opinion of Doug Uffen`; and `RER-02, Expert Report of Robert
Hobbs R`, **which is itself cut at the window's right edge**. I do not state a total for the list,
because the list is truncated at both ends and per `0811-B5` a total is exactly what I am not entitled
to.

**Novelty against the repository, derived rather than asserted:** `grep -ril <term> --exclude-dir=.git
. | grep -v 2026-08-15` over the whole repository — the second stage excluding today's record and
today's reduction, and written in re-runnable form per `0814-O1` — returns **0 files** for each of
`Gordon C. Gill`, `Paul Sharp`, `Uffen`, `Robert Hobbs` and `Claimants’ Memorial`. `Harold Paul
Einarsson` returns **1 file**, `analytics/daily-research/2026-06-23.md:39`, where he appears as one of
the three named **claimants** in the case caption — so his *name* is not new to the record, only this
docket item bearing it is. Note also that `Claimants’ Memorial` is new only as that exact string: the
bare term `Memorial` returns 7 files, so what is new is this docket item, not the concept. **Per
`0815-O10`, a zero-hit on a multi-token string is not absence until the surname alone is tried, and the
officer tried them:** `Hobbs` **0 files**, `Pricewaterhouse` **0 files**, `Doug Uffen` **0 files** — so
those zeroes hold; `Gill` returns **5 files** and `Sharp` returns **38**, neither a hit on these
persons, but neither zero-hit was established until the surname was run.
Per `0814-O2` that screen reaches the repository **working tree** only: it does not reach operator-side
material, the verification ledger outside the repo, or anything in Emory's hands, and "new to this
record" is scoped accordingly.

**A NAME IN YESTERDAY'S RECORD WAS CUT IN HALF BY THE WINDOW EDGE, AND THE ERROR WAS MINE.**
Yesterday's record — and my brief to the analyst, which repeated it — carried `“Chip” Gordon` as a
name. Today's window shows the rendered string continues: **`Expert Report of “Chip” Gordon C. Gill`**.
`Gordon` was not a surname; it was the point at which a 400-character window happened to stop.
Yesterday's novelty screen was run against the truncated form and its result (`1 file`, the unrelated
*Gordon Burr*) was therefore an artefact of a string the docket does not carry. This is the
TRUNCATION-COMPLETION pattern arriving from the opposite direction — not a completion invented by a
seat, but a truncation *mistaken for a complete string* — and I flag it as such for the taxonomy. I
sent the corrected string to the analyst mid-flight and told him to record any wasted spend against me.

**The error has already propagated and correcting this record does not correct it.** Derived —
`grep -rn "Chip” Gordon" --exclude-dir=.git . | grep -v 2026-08-15` — the truncated form is still
carried at **`STATE_OF_THE_ANSWER.md:28`, twice**: once as a Published Decisions item set in backticks,
and once inside the assertion that *"the three names `Ralph Maitland`, `Nigel Bankes` and `“Chip”
Gordon` are absent from the repository working tree before 08-14"* — a novelty screen now known to have
been run on a string the docket does not carry. It is also carried twice in `analytics/council-log.md`.
`STATE_OF_THE_ANSWER.md` and `analytics/council-log.md` are standing summaries, not dated records, and
**they are the files the next seat reads**; both are owed a dated correction, and both are corrected in
this session's ledger pass. `analytics/daily-research/2026-08-14.md` carries it seven times and must
**not** be edited — a dated record is superseded, never rewritten. *(Raised at `0815-B3` as a new
pattern, **UNTRACED CORRECTION**: an error disclosed where it was found while the derived carriers that
already copied it are left uncorrected and unnamed, so the corrected record and the standing summary
disagree and the summary is what the next seat reads.)*

**What is NOT established, and the list is long because the temptations here are real.**

1. **VISIBILITY IS NOT FILING.** Those six items became visible to *this record* today. Nothing
   whatever establishes when ICSID published them, and it would be a straight
   tool-status-as-source-state violation to write that six documents "were added". They may have sat on
   that page for a year; our window simply did not reach them.
2. **THE LIST IS STILL TRUNCATED AT BOTH ENDS, so no count, no enumeration and no "latest".** The
   window opens mid-word on `er` and ends mid-string on `Robert Hobbs R`. Per `0811-B5` I have not
   counted the items and will not.
3. **`Robert Hobbs R` IS NOT COMPLETED.** The trailing `R` is where the window stopped. I do not write
   `Robert Hobbs` inside backticks as though that were the whole string, and the novelty screen above
   was run on the substring actually returned.
4. **`RER-02` AND `RER-03` ARE NOT DECODED.** On their face those prefixes look like respondent-side
   exhibit numbering, and that is exactly why I am not asserting it. No source in this record defines
   the convention, and a plausible expansion of an abbreviation is the same class of error as a
   plausible completion of a truncation.
5. **NOTHING IS ASSERTED ABOUT WHO ANY OF THESE PEOPLE ARE**, or what their evidence bears on, from
   this row. That is search-track work and it is the analyst's, in Part III.
6. **THE BYTE CHANGE FOUNDS NOTHING.** This page returned 105,667 bytes on 2026-08-13, 105,670 on
   2026-08-14 and **105,669** today, with a different sha each day. Per G5 a digest change is never a
   document change, and per the optimization item above `icsid.worldbank.org` is one of the hosts whose
   within-run digests are stable (3 of 3 groups identical) — so the day-over-day movement is not
   explained away as host noise either. **It is simply not evidence of anything I can name, and the
   byte figures and the window contents are recorded side by side precisely so that no reader infers
   one from the other.**

### P2 — `/en/cases/336/` is identified by the page's own case-number field, and it is a different proceeding from the one the open gap is about

Two rows, both on `https://pca-cpa.org/en/cases/336/`, both **ok/200, 97,343 bytes**, both matched.
This URL had never been retrieved by any seat of this council; the analyst surfaced it yesterday by
title only and expressly declined to say which proceeding it was.

`records[3]`, `find: "Zeph"` — **MATCHED**:

> " Menu Français English Home About us Dispute Resolution Services Cases Resources Global Cooperation
> Zeph Investments Pte. Ltd. v. The Commonwealth of Australia (IV) Case information Name(s) of
> Claimant(s) Zeph Investments Pte. Ltd. (Private entity ) Name(s) of Respondent(s) The Commonwealth of
> Australia (State) Names of Parties - Case number 2024-48 Administering institution Permanent Court of
> Arb"

`records[4]`, `find: "Case Number"` — **MATCHED**:

> "(Private entity ) Name(s) of Respondent(s) The Commonwealth of Australia (State) Names of Parties -
> Case number 2024-48 Administering institution Permanent Court of Arbitration (PCA) Case status
> Pending Type of case Investment arbitration Subject matter or economic sector - Procedural rules
> UNCITRAL Arbitration Rules 2021 Treaty or contract under which proceedings were commenced Multilateral
> treat"

**What is ESTABLISHED.** The PCA's own case page at `/en/cases/336/`, at that instant, rendered a
proceeding captioned **`Zeph Investments Pte. Ltd. v. The Commonwealth of Australia (IV)`**, carrying
**`Case number 2024-48`**, **`Administering institution Permanent Court of Arbitration (PCA)`**,
**`Case status Pending`**, **`Type of case Investment arbitration`**, and **`Procedural rules UNCITRAL
Arbitration Rules 2021`**. The `Treaty or contract` field begins `Multilateral treat` and **is cut off
by the window; it is not completed here.** The anti-increment-generable-URL discipline adopted at
2026-08-14 next step 8 did its job: the page was anchored on a designator only a genuine Zeph page
would carry, and it matched, so this is not a plausible-looking wrong page.

**This settles the identity question the close-out asked, and it settles it against the guess a reader
would most naturally have made.** `/en/cases/336/` is **not** PCA Case No 2023-40. It is a distinct
docket, **2024-48**, captioned **(IV)**. The record previously held `/en/cases/309/` (worked hard, and
carrying 2023-40) and UNCTAD Navigator ids 1300 (*(I)*) and 1301 (*(II)*). Derived, not asserted:
`grep -ril` over the repository excluding today returns **0 files** for each of `2024-48` and
`Australia (IV)`.

**What is NOT established.**

1. **NO COUNT OF ZEPH PROCEEDINGS.** The page rendering the ordinal `(IV)` is a fact about that page's
   caption. Per `0811-B5` I derive no count, and I expressly do not write that four proceedings exist,
   that this is the fourth, or that it is the latest. An ordinal in a caption is the institution's
   label, not an enumeration this council has performed.
2. **THIS DOES NOT RESOLVE THE OPEN GAP, AND IT NARROWS IT ONLY BY EXCLUSION.**
   `GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` concerns the **26 September 2025
   award in PCA Case No. 2023-40**. **Which numbered Zeph proceeding that docket belongs to is not
   established and is not asserted here** — the `(II)` attribution is title-carried from the one jusmundi
   result whose title and slug the record found in disagreement, an italaw title attaches the same
   docket to *(III)*, and `2026-08-11.md:278` states that these entries do not establish which numbered
   proceeding is which (`0814-B5`, sustained yesterday — and restated by me here one day later, which is
   why `0815-B4` was filed against this paragraph and sustained).
   Today's rows establish that `/en/cases/336/` renders `Case number 2024-48`, and the corpus already
   holds `/en/cases/309/` rendering `Case number 2023-40` at
   `analytics/fetch-results/2026-08-11-zeph.json` `records[2]`, `find_matched: true`. **Two PCA pages,
   each read from its own case-number field, carry two different dockets, so `/en/cases/336/` is not the
   docket the 26 September 2025 award issued in.** That is the whole of the exclusion and it rests on
   neither an ordinal nor a status field. **Nothing here establishes that the page carries no ground of
   decision** — two 400-character windows out of 97,343 bytes cannot establish what a page does not
   contain, and the page's status field `Pending` is in any event consistent with a jurisdictional or
   partial award having issued. What is removed is that this URL was a second route to the award docket;
   nothing more. *(Rebuilt at `0815-B5`, which struck the `Pending` leg and the ordinal leg — both mine
   — and supplied the body-to-body leg the record already held and I had not used.)*
3. **`Pending` IS THE PAGE'S STATUS FIELD AT THAT INSTANT** and nothing more. It is not a statement
   about what stage 2024-48 has reached, and per G5 nothing here speaks to any other Zeph docket's
   status.
4. **THE `/en/cases/309/` ATTACHMENT IS BODY-CARRIED, AND THIS SUPERSEDES AN ACCEPTED CORRECTION.** Per
   `0815-O6`: `analytics/daily-research/2026-08-14.md:904-908` records, as the analyst's accepted
   self-correction, that `2023-40` *"appears in no window returned from `pca-cpa.org/en/cases/309/`… so
   the council's attachment of that docket to that proceeding is presently inferential."* **That is
   false, and the officer read the raw source rather than the record's rendering of it:**
   `analytics/fetch-results/2026-08-11-zeph.json` `records[2]` is `/en/cases/309/`, `find: "2023-40"`,
   `find_matched: true`, excerpt `"…Case number 2023-40 Administering institution Permanent Court of
   Arbitration (PCA)…"`. The find is not echoable from the URL, so taxonomy 23 does not apply. **The
   attachment is body-carried, not inferential**, and yesterday's accepted correction is superseded here
   expressly rather than silently — which is what a dated record is for.
5. **NOTHING HERE BEARS ON WHICH PROCEEDING THE SWISS SET-ASIDE ATTACKED.** That sub-question, recorded
   at 2026-08-14 next step 4, is untouched.
6. **THE TWO DIGESTS FOUND NOTHING.** `records[3]` and `records[4]` are the same URL in the same run at
   the same 97,343 bytes with **different sha256** (`5eb97b7d…` and `a7542b0b…`). Per the optimization
   item, `pca-cpa.org` is one of the two hosts whose within-run digests diverge as a matter of course.
   **No inference of any kind is drawn from that pair**, and it is recorded here because it is the
   observation that prompted the screen, not because it is a finding about the page.

### P3 — the A/81/17 watch returns its tenth null on that page, with the population stated

`records[5]`: `https://uncitral.un.org/en/commission`, `find: "A/81/17"` — **ok/200, 832,338 bytes, sha
`f9712994…`, `find_matched: false`, excerpt empty.**
`records[6]`, the same-page positive control fired in the same run: `find: "A/80/17"` — **MATCHED**,
returning:

> "ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna
> A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session
> A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth
> session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se"

So the page is **intact**, it is **still the page we think it is**, and it demonstrably lists a
session's report by symbol in exactly the form the watch string would take.

**The count, and the population it is drawn from, per `0813-B9` — derived programmatically over
`analytics/fetch-results/*.json`, not read off a printed scan.** The corpus holds **12 rows** carrying
the find `A/81/17`. Of those:

- **10 are `ok/200`, `find_matched: false` on `https://uncitral.un.org/en/commission`**, at **10
  distinct dates**: 2026-08-03, 08-04, 08-05, 08-06, 08-07, 08-09, 08-10, 08-13, 08-14 and today.
  **Today is the tenth.**
- **1 is a `find_matched: true` on `https://undocs.org/A/81/17`** (2026-08-04) — a **request-echo** on a
  route retired that same day precisely because the page generates the symbol from the request. It
  corroborates nothing and it is named here so that a nonzero count is not read as one.
- **1 is `robots_disallowed` on `digitallibrary.un.org`** (2026-08-08) with **no `find_matched` key at
  all** — never looked, and per `0807-B5` not poolable with a `false`.

**The four standing scope limits travel with this null unchanged:** (1) **RENDERED TEXT ONLY** — the
relay strips every tag *and* attribute (`scripts/fetch_relay.py:126-128`), so a symbol present only in
an `href` is invisible to this channel; (2) **ENTITY-BLIND FIND** (`0806-O6`, standing) — a symbol
rendered with an HTML entity or a non-ASCII solidus reads as absent; (3) **THIS LOCATION ONLY** —
nothing about ODS, the UN Digital Library, the 59th-session document listing, or the GA Official
Records; (4) the `undocs.org`/`docs.un.org` symbol route stays **RETIRED** and was not used today.

**This is not a finding that A/81/17 is unpublished**, and it does not move the 2026-07-27 expectation
of August–September 2026.

**One derived note on the standing digest lead, and it is narrower than it sounds.** This URL has now
returned 832,338 bytes with sha `f9712994…` on **three** dates (08-13, 08-14, 08-15), against
`ca87a113…` on **eight dates between 2026-08-03 and 2026-08-11** — derived programmatically. **Per
`0815-O2`, that is eight dates in that span, not nine consecutive ones: `/en/commission` was not probed
on 08-08 or on 08-12 at all**, and a gap in probing is not an observation. Per the optimization item,
`uncitral.un.org` is **9 of 9 digest-identical within a run**, so on this host the change is not
explained as per-response noise. **It remains a LEAD, NOT A FINDING**; no anchored window was fired at
it today, the `fifty-ninth` window remains excluded as the locus per 2026-08-14 next step 7, and
nothing about it is reported as a document change.

### P4 — UNCTAD refused for a fifth observed run, and the two rows are one observation

`records[7]` (China–Switzerland BIT (2009) mapping page, `find: "Denial of benefits"`) and `records[8]`
(the *Zeph v. Australia (II)* Navigator entry at case id 1301, `find: "Zeph"`) — **both refused / 403**,
and they are the reason the batch completed 7 of 9.

**Per G2 as pre-registered — before the result existed — these two rows are ONE observation of one
client-access condition in one run, not two.** The corpus therefore gains a **fifth** observed run in
the refusal condition, not a fifth and a sixth. Derived programmatically over all
`investmentpolicy.unctad.org` rows in `analytics/fetch-results/*.json`: **`ok/200` on 2026-08-03,
08-04, 08-05, 08-06, 08-08 and 08-09; `500` on 08-07, 08-08 and 08-09; `refused/403` on 08-10, 08-11,
08-13, 08-14 and today.** *(The 08-08 and 08-09 dates appear in two status buckets because more than
one row ran on those dates; that is a fact about our row design, not about the host, and no rate is
computed from it.)*

Per G5 and per E4 as pre-registered, that is a fact about **this client's access to that host at that
instant** and about nothing else. It is not a finding that the BIT has no denial-of-benefits clause;
that question remains **unasked of the resource**. It is likewise not a finding about *Zeph v.
Australia (II)* — the Navigator entry the analyst identified yesterday as the UNCTAD row for the
proceeding the open gap is actually about **remains unretrieved**, and the reason is our access, not
its contents.

**One tension named rather than left for a later seat to find, per `0815-O7`.** Grouping two rows on
**different paths** of one host as a single access condition sits in visible tension with Part IV below,
which invokes `0814-B3` to refuse pooling the pipeline's 403s partly *because* they are different paths
of the same host. The grouping here was **pre-registered before the result existed** and is conservative
in the direction of fewer observations, which is why I keep it; but path is one of the axes `0814-B3`
treats as distinguishing, and this grouping is a pre-registered conservatism, **not** a finding that the
host gates at host level.

**Escalation 3 of the 2026-08-14 record — one manual retrieval, operator-side, of the China–Switzerland
BIT (2009) forum provision — stands unchanged in substance and is now owed a fifth time**, and the
Navigator entry at case id 1301 joins it.

---

## Part III — The analyst's return, reproduced verbatim

**Three things belong to me before his text starts, and two of them are against me.**

**First, the heading in his return says "Analyst return — 2026-08-15".** I asked him not to number a Part
himself and he did not; the heading is his own and it stays, because member text enters this record
verbatim and silently renumbering someone else's heading is editing it. Read it as this Part III.

**Second, the misdirected query was caused by my error, and he declined to accept that framing in full.**
I gave him `“Chip” Gordon` as a target; it was a truncation artefact of yesterday's window (Part II P1).
When today's reduction revealed the continuation I sent him the correction mid-flight and told him
expressly to record the wasted spend against me. **He refused to leave it there**, and his §2 and §7
locate a share of the fault in his own reading of a line the record already carried. I record that I
tried to absorb it and that he would not let me; the reader should weigh his account, not mine.

**Third, he came in at 4 of 4 and I had told him under-budget was a good outcome.** He spent fully and
one of the four was spent on my bad string. I do not treat that as overspending, for the reason in
Part VI.

His verbatim return follows.

---

## Analyst return — 2026-08-15

**Seat:** research-analyst. **Model, observed rather than assumed, per the never-substitute-silently rule:** my runtime reports `claude-opus-5`; my instantiated prompt states "Model: Claude Opus 5 (operator directive 2026-07-29)". **REQUESTED `claude-opus-5` → ACTUAL `claude-opus-5`. No discrepancy to escalate from this seat today.**

**Instrument state as briefed and not re-probed by me:** direct egress dead, fetch service dead, WebSearch alive. Everything below that comes from outside the repository is **title-carried at best**. **WebSearch spend: 4 of 4. I did not come in under budget, and §7 explains why the fourth call was worth less than I expected.**

**Contract note, filed forward so it does not become an error record.** `analytics/errors/20260810T142237Z-analyst-malformed.json` records the failure mode `"no candidate_claims JSON block found"` against an earlier analyst return. **This return carries no `candidate_claims` block by design** — the return-path protocol directs that my text enter the daily record verbatim as record text, and nothing I found today reaches the standard for a proposed claim. That is a deliberate null, not a malformed return.

---

### 1. The two screens, and their honest value

**Screen 1 — `analytics/errors/`.** One file, `20260810T142237Z-analyst-malformed.json`. It is not about today's subject; its value was the contract note above. Zero cost.

**Screen 2 — the record screen on the rigid designators.** `grep -rn -i -E "Bankes|Maitland|Chip.{0,3}Gordon|Gordon" analytics/ STATE_OF_THE_ANSWER.md`. It saved me four things, and one of them it offered and I failed to take.

**(a) It handed me the collision the brief warned about, already sitting in the record.** `analytics/daily-research/2026-07-14.md:45` names *Gordon Burr*, a claimant in *B-Mex, LLC and Others v. United Mexican States*, described there as having "No IP, trade secret, or pharmaceutical nexus." Yesterday's seat had already run this and reported it at `analytics/daily-research/2026-08-14.md:680`. So the record already contained a live same-name near-match on one of the three designators before I searched, and I knew to discount any `Gordon` result on that axis.

**(b) It gave me the case's subject-matter vocabulary at zero cost**, which is what let me frame a name query with an institutional designator instead of a keyword: marine seismic data and the Canada Petroleum Resources Act as the challenged measure, with a Federal Court judgment `2020 FC 984` in the record (`analytics/daily-research/2026-06-23.md:45`); merits hearing March 2025 in Calgary; NAFTA Article 1121 waiver-consistency as the live jurisdictional objection.

**(c) It showed me the record has carried *unnamed* references to expert reports in this case since July** — `analytics/daily-research/2026-07-16.md:93` ("pre-hearing filings through October–November 2024 (Canada's Rejoinder on the Merits; expert reports)") and `analytics/daily-research/2026-07-24.md:40`. Both lines are search-derived and **I am not upgrading them.** But it reframes what is new: the record did not learn on 08-14 that expert reports exist in UNCT/20/6. It learned that some of them have **names**.

**(d) And the screen returned, and I read, the line that should have stopped my second query.** `analytics/daily-research/2026-08-14.md:200`: *"The window is capped at 400 characters and ends mid-list at `Expert Report of “Chip” Gordon`."* The record told me in terms that the window ended **at** that item. I treated the item as a complete string anyway. See §7 — this is the session's real finding and it is against me.

---

### 2. Search spend — 4 of 4, every query verbatim

1. `"Nigel Bankes" University of Calgary Faculty of Law professor natural resources law`
2. `"Chip Gordon" seismic data`
3. `"Ralph Maitland" Geophysical Service Incorporated seismic`
4. `"Nigel Bankes" "Chair in Natural Resources Law"`

Queries 1, 2 and 3 were fired in one parallel block before any result was seen, reserving the fourth. Query 4 was fired after reading 1–3, to try to convert a name into a field through a phrase-constrained title.

**Query 2 is spent against a string the administering institution's docket does not carry.** The chairman's mid-session correction reports that today's relay window renders the item as `Expert Report of “Chip” Gordon C. Gill`. I accept the correction. I do not accept that the whole of the fault is the chairman's: the record's own 08-14 text at `:200` disclosed that the window ended at that item, my screen returned that line, and I did not draw the consequence.

---

### 3. Findings on the names

#### 3.1 — Nigel Bankes. TITLE-CARRIED. NEW TO THE RECORD.

Among the result titles returned by queries 1 and 4, these carry a personal name together with an institutional affiliation:

- `Nigel Bankes | Faculty of Law, University of Calgary | The Royal Society of Canada` — https://rsc-src.ca/en/nigel-bankes-faculty-law-university-calgary
- `Nigel David Bankes | UCalgary Profiles | University of Calgary` — https://profiles.ucalgary.ca/nigel-david-bankes
- `Nigel BANKES | University of Calgary, Calgary | Faculty of Law | Research profile` — https://www.researchgate.net/profile/Nigel-Bankes-2
- `Honouring the Career &amp; Contributions of Nigel Bankes` — https://engage.ucalgary.ca/nigel-bankes
- `Nigel Bankes – ABLawg` — https://ablawg.ca/author/nbankes/

**What this carries:** that pages bearing the name *Nigel Bankes*, and in one case the fuller form *Nigel David Bankes*, are titled in association with the **Faculty of Law, University of Calgary**, and that an author-keyed page for that name exists at `ablawg.ca`.

**What it does NOT carry.** It does not carry a rank, a title, a chair, a field of scholarship, a date of appointment, a retirement, an emeritus status, or any award. It does not carry that this person is the *Nigel Bankes* named on ICSID's docket page for UNCT/20/6. **It does not corroborate the docket.** The proposition *that a person named Nigel Bankes authored an expert report in UNCT/20/6* rests on ICSID's own case-detail page alone (`STATE_OF_THE_ANSWER.md:28`), and nothing in these results touches it.

**The field question, and the answer is weaker than I would like.** Query 4 was fired to find a title carrying the **name and the field together**. Among the titles it returned, the one that does so is a social-media post reproduced in a page title:

- `Danielle Smith on X: "Nigel Bankes, Professor and Chair of Natural Resources law, Faculty of Law, University of Calgary joined the show to talk about the legal side of the pipeline debate: …" / X` — https://x.com/abdaniellesmith/status/999342335610810368?lang=en

That string is a result **title**, not a generated summary, so `0811-B4` does not bar it. But its authority is a third party's post about a broadcast, it carries no date I can read, and **I decline to build anything on it.** I record it as the weakest possible carriage of "natural resources law" against this name and I do not treat the field as established.

Two further titles from query 1 — `Pursuit of a sustainable future leads to storied career in natural resources, energy and environmental law | News | University of Calgary` and `Passion for resources law leads to long and successful career at UCalgary | News | University of Calgary` — **carry the field but do not carry the name.** They were ranked under a query containing the name. **Ranking is not carriage**, and I do not attribute either to him.

**Name collision: I cannot exclude it.** Nothing I retrieved distinguishes one bearer of this name from another, and the ResearchGate slug ends `-2`, from which I infer nothing.

#### 3.2 — The item previously read as a name ending in `Gordon`. NOT IDENTIFIED. The query was misdirected.

Query 2 returned no title naming a person matching the string I searched. The returned titles concern the *Gordon Bell Prize*, *Colin Gordon Associates*, and a USGS dataset in which "chip" denotes survey equipment. **No identification, and the null is void as evidence about the docket**, because the string I searched is not the string the docket renders.

Per the chairman's correction, today's relay window renders the item as `Expert Report of “Chip” Gordon C. Gill`, bounded on the right by the next item. **I make no claim about how that person's name is composed** — that a quoted familiar form precedes given names and a surname is the natural reading, and it is an **inference**, not a finding. **NEW TO THE RECORD, from the relay, not from me.**

I did not re-spend to repair this. I had no budget left when the correction arrived.

#### 3.3 — Ralph Maitland. NOT IDENTIFIED. Null, recorded plainly.

Query 3 returned titles about an entity named *Geophysical Service* — a ZoomInfo company page, SEG Wiki pages, the company's own site, an `en.wikipedia.org` page, and a Grokipedia page — and **no title naming Ralph Maitland**. I identify nothing.

**A precision that matters and comes from the record, not from search.** The docket item is `Witness Statement of Ralph Maitland`, not an expert report (`STATE_OF_THE_ANSWER.md:28`; `analytics/insights.jsonl:61`). The chairman's question asks what field each would give **expert evidence** in; for this name the question is misframed on the record's own text, because **a witness statement is not on its face expert evidence**. I flag this under `0811-B6` (no dropped qualifiers) rather than answering the question as put.

#### 3.4 — The names arriving with the chairman's correction. NOT SEARCHED. Budget exhausted.

`Paul Sharp, Pricewaterhouse Coopers LLP`; `Harold Paul Einarsson`; `Doug Uffen`; and an item **truncated at the window's right edge** which I reproduce exactly as the chairman gave it and **do not complete**: `Robert Hobbs R`. I assert nothing about any of them. I note one thing of method value and nothing more: the `Paul Sharp` item carries an **institutional affiliation inside the docket string itself**, which means tomorrow's identification query for that name already has its rigid designator supplied by the administering institution rather than by us. That is the strongest identification target now on the board.

On the `RER-` prefixes: I rely on nothing from them and assert nothing.

---

### 4. What I refused to assert, BY NAME

1. **The generated summary attached to query 1 and query 4** — that Nigel Bankes was appointed in 1984, has held a Chair in Natural Resources Law since 2008, retired in June 2021, is an emeritus professor, teaches property, aboriginal, natural resources, energy, oil and gas and international environmental law, received a named teaching award in 2018, or works on Arctic Indigenous rights, the Columbia River Treaty or carbon capture. **REFUSED under `0811-B4`. A search-engine AI-generated summary is not a source.** None of it enters this record, and none of it may be cited from this record.
2. **The generated summary attached to query 3** — the corporate-history propositions it offered about an entity named *Geophysical Service* (founding date, founders, city of incorporation, nationality). **REFUSED under `0811-B4`.** I record specifically that I did **not** admit them and therefore raise **no** tension with the record's own description of GSI at `analytics/daily-research/2026-06-23.md:39`. If the corporate history is ever wanted, it is a body-read question, not a summary question.
3. **The Grokipedia result.** A generated encyclopedia page is a generated summary with a URL. **REFUSED by name.**
4. **That any search result corroborates the docket.** It does not. `STATE_OF_THE_ANSWER.md:28` remains the sole support for these names being attached to UNCT/20/6.
5. **The elaborated "contribution" account of the *Zeph* jurisdictional ground.** Not offered to me today, because I ran no *Zeph* query. **The refusal is carried forward verbatim as yesterday's seat asked: *"elaboration is not evidence."*** It stands refused for a fourth consecutive day.
6. **Anything about when any of these documents was filed or published.** **VISIBILITY IS NOT FILING.**
7. **Any count, enumeration or "latest".** The window is truncated at both ends. Nothing derived by eye (`0811-B5`).

**`0814-B1` self-check, performed after writing the list above.** I re-read every heading and every backticked span. §3.2's heading says "the item previously read as a name ending in `Gordon`" and does **not** set a personal name in backticks. `Robert Hobbs R` appears once, in backticks, exactly as truncated, and is nowhere completed. No heading in this return asserts an identification; §3.1's heading says TITLE-CARRIED, which is what it is.

---

### 5. Secondary limb — NOT REACHED by search

Budget was exhausted on the primary. I ran the zero-cost Ring 3 screen over `STATE_OF_THE_ANSWER.md` and report that **it produced nothing new, which is itself the useful result**: the record already holds two distinct Ring 3 mechanisms for this case — the NAFTA Article 1121 waiver-consistency objection and a three-year limitations bar under Articles 1116(2)/1117(2) (`analytics/daily-research/2026-06-25.md:47`) — and already carries the consequence I would otherwise have written up as an insight, that a disposition on the waiver limb would leave Ring 1 untouched because "the tribunal would never reach whether seismic data qualifies as a 'covered investment'" (`analytics/daily-research/2026-06-25.md:49`). **I add nothing to Ring 3 today.**

---

### 6. ESTABLISHED / INFERENCE / REFUSED

**ESTABLISHED (title-carried, and that is the whole of the entitlement):**
- Pages titled in association with the name *Nigel Bankes*, and in one case *Nigel David Bankes*, exist at `rsc-src.ca`, `profiles.ucalgary.ca`, `researchgate.net`, `engage.ucalgary.ca` and `ablawg.ca`, and their titles place that name with the **Faculty of Law, University of Calgary**.
- Among the titles returned by my four queries, **none** names *Ralph Maitland*, and **none** identifies a person matching the string I searched at query 2.

**INFERENCE (flagged, not asserted, and not enough for a candidate claim):**
- That the *Nigel Bankes* of those title-carried Calgary law pages is the *Nigel Bankes* on ICSID's docket. **A same-name match on a different person is not excluded**, and the record already holds a live same-name near-match on a different designator (`analytics/daily-research/2026-07-14.md:45`).
- That the docket item renders one person's name in the form quoted-familiar-form + given name + surname.
- **The interpretive point, and it is the reason this matters to the research question.** If the Calgary association is the right person, then an expert report by a **law** academic whose field is carried only by the weakest title I found — natural resources law — would sit in **Ring 2**, alongside the Canada Petroleum Resources Act measure (`analytics/daily-research/2026-06-23.md:45`), and a report by a person the docket itself ties to a **major accounting firm** would sit, on its face, in **quantum**. As far as this truncated window reaches, **this record can attach none of the visible names to IP or trade-secret law — the Ring 1 limb.** That bears directly on whether the case the record treats as the nearest analogue (`analytics/daily-research/2026-06-23.md:73`) will in fact generate reasoning on IP-as-covered-investment, or will dispose of the matter on regulatory and valuation grounds. **Scope, binding: the window is truncated at BOTH ends. Absence of an IP-side expert from this window is absence from a window, not from the docket, and certainly not from the case.** I state this as a lead, not a finding, and I have not manufactured a nexus: the honest reading is that the nexus here is thinner than the case's headline suggests.

**REFUSED:** everything at §4, by name.

---

### 7. Method note for the self-training file

**The finding of this session is a rule I broke that the record had already written down for me.**

`analytics/daily-research/2026-08-14.md:200` says the window "ends mid-list **at** `Expert Report of “Chip” Gordon`". My screen returned that line. I read it, carried the item forward as a name, and spent a quarter of my budget searching a string that the truncation had manufactured. The record was not silent; I was not listening to it.

**Proposed as a named pattern, without a number per `0811-O9`: TERMINAL-ITEM TRUNCATION.** *In a character-capped window, the terminal item is itself truncated at the cap unless the cap falls on a delimiter — so the last item of a truncated list is a fragment, and treating it as a complete string is the list-item form of TRUNCATION-COMPLETION.* It is distinct from `0813-B3`/TRUNCATION-COMPLETION, which concerns restoring characters the instrument did not return; this one concerns **failing to notice that characters are missing at all**, because the fragment happens to look like a whole. Countermeasure, cheap and mechanical: **before searching any designator drawn from a capped window, check whether it is bounded on both sides by a neighbouring item; if it is the terminal item, mark it a fragment and do not search it.** Applied to today: `Nigel Bankes` and `Ralph Maitland` were bounded and were searchable; the terminal item was not, and should not have been.

**On query formulation, tested and reported honestly.**
- **Name plus institutional affiliation works, and produced the session's only carriage** (query 1). This confirms the 2026-07-26 lesson in its person-identification form.
- **Name plus a bare subject noun does not disambiguate a common surname** (query 2). `"Chip Gordon" seismic data` was drowned by *Gordon Bell* and *Colin Gordon*. Even had the string been right, the formulation was weak: for a common surname, the affiliation, not the subject, is the discriminator.
- **The exact-phrase catalogue query underperformed** (query 4). `"Nigel Bankes" "Chair in Natural Resources Law"` was fired on the theory that an exact institutional phrase would pull a title carrying name and field together. It returned largely the same profile titles as query 1, plus one social post. **The phrase constraint filtered which pages ranked, not what their titles carried** — a page can satisfy an exact-phrase constraint entirely in its body. **Lesson: exact-phrase constraints do not make titles carry more. They only change the ranking.** For a field-of-expertise question on a fetch-dead day, the right target is a **bibliographic** title — a book, an article, an SSRN record — where the field is *in* the title by construction and the author's name is *in* the title as a byline. That is what I should have fired, and it is the corrected form of the catalogue-titles-over-commentary-titles rule for person questions: **for a person, catalogue means a work-record, not a profile page.** A profile page's title carries affiliation and stops there.

---

### 8. What I propose as tomorrow's work

1. **`Paul Sharp, Pricewaterhouse Coopers LLP` is the strongest identification target now on the board, and it should be first**, because the administering institution's own docket string supplies the name **and** the affiliation, which is precisely the rigid designator the 2026-07-26 lesson and today's query 1 both say works. One query, name plus firm.
2. **`Doug Uffen`** — an uncommon surname, so a single bare-name query is likely to discriminate without an affiliation.
3. **Do not search the truncated right-edge item.** Under the pattern proposed at §7 it is a fragment. Slide the window again before spending on it.
4. **Do not re-spend on the item ending in `Gill`** until the council decides whether identification of that person is worth a call; my view is that it ranks below items 1 and 2, because the quoted familiar form makes the query formulation unreliable.
5. **The field question for Nigel Bankes remains open and is worth exactly one bibliographic query**, framed as byline-plus-work-title rather than as a profile lookup. `GAP-UNRESOLVED: einarsson-unct-20-6-expert-identities` — stable slug proposed; the names are on the docket, the persons are not identified, and the Ring-assignment of the expert-evidence layer turns on it.
6. **Ring 3 needs a body, not a search.** Today's zero-cost screen showed the record already holds what search would return. Ring 3 is thin because the underlying documents are unread, not because the record has not looked.

**Nothing in this return should be read as establishing who Nigel Bankes, Ralph Maitland, or any other person named on that docket is.** Four searches, one weak carriage of a name to an institution, two nulls, one misdirected query, and one rule learned at my own expense.

---

## Part IV — The digest of record (Rule 1)

`Digest 2026-08-10: 11 candidates evaluated, 3 items surfaced (0 matches, 3 watch-list leads).`

**The digest of record has NOT moved.** `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` is still the
newest `meta.json` in `digests/`, and the two figures above are copied from it verbatim
(`meta["screened"] = 11`; `meta["matches"] = 0` plus `meta["watch_list_leads"] = 3` = 3 items surfaced).
Per Rule 0 the analytics officer is not convened and this is the whole of the digest discussion; per
Rule 1 the word "screened" does not appear here with a number attached. The next weekly collection run
is Monday 2026-08-17, so an unchanged digest on a Saturday is the expected state and not a failure
signal — and it is now the sixth consecutive daily record to carry this same digest, which is a fact
about the collection cadence and **not** a fact about the field.

One line from that same `meta.json` is worth carrying because Part II touched it: the block records
`unctad_isds` as `NOT-READ (403)` on `https://investmentpolicy.unctad.org/investment-dispute-settlement`,
and `italaw` as `NOT-READ (403)`. Those are the collection pipeline's own client-access facts, recorded
here because P4 saw refusals from **a different client, on different paths of the same host, on a
different date** — not, per `0814-B3`, "the same refusal condition" — and, per the cardinal rule,
**neither is a statement about UNCTAD or about italaw.**

---

## Part V — The council discussion

**How this Part is constructed, stated so nobody has to wonder.** Rule 2 requires an attributed,
multi-turn dialogue with **Chairman:**, **Researcher:** and **Integrity check:** turns, in which the
integrity voice challenges at least one concrete assertion and the challenged party answers on the
record. Rule 0 requires the integrity challenge to be heard *here* rather than filed as its own Part.
Both member returns in this record are **real subagent returns reproduced verbatim and unedited**.
**Every `Researcher:` and `Integrity check:` turn in the exchange section below is a verbatim quotation
from those returns** — I have composed no member's words. The `Chairman:` turns are mine.

**Per the standing convention adopted at `0813-O1`, and applying to every block quote in this record:
emphasis inside a quotation is the quoted seat's own unless the quote says "— emphasis added".**

---

### The integrity challenge, reproduced verbatim

*(Transport note, mine: the officer's return reached me with `<`, `>` and `&` HTML-entity-encoded by the
message channel. I have decoded those three entities back to the characters they stand for and changed
nothing else. Markdown blockquote markers and the `<title>` references below are therefore the officer's
own; the encoding was an artefact of delivery, not of the seat.)*

## VETTING NOTE — integrity officer, 2026-08-15

**Seat.** Integrity officer, convened as a real subagent, filing inside Part V per Rule 0.

**Model — REQUESTED vs ACTUAL, observed not restated.** My committed definition `.claude/agents/integrity-officer.md:4` declares `model: opus`; `src/models.py:13,28` intends `UTILITY_MODEL = "claude-opus-4-8"` for "the integrity helper"; `agents/integrity-officer.md` documents Claude Opus 4.8. **My runtime reports `claude-opus-5`.** **REQUESTED `opus` → ACTUAL `claude-opus-5`, and the intended `claude-opus-4-8` is NOT what is running.** `record_fallback()` did not fire — its only caller is `src/research_brief.py:161`, a path this session never enters — and `grep -n "Model runtime fallbacks" HANDOFF.md` returns nothing, exit 1. **Escalation 1 of the 2026-08-14 record is unchanged and now owed a second time.** I do not substitute silently.

**Spend: 0 of 3 WebSearch.** Nothing today needed one. Every finding below is derived from files on disk or from code in this repository.

**Taxonomy.** Read at `/home/user/isds-watcher/agents/integrity-officer.md`, not recited. 27 entries; the **27/27 collision** (*scope-mixed screen* 2026-08-06 vs *manufactured residual* 2026-08-07) is **unresolved for a sixth day**, with five unnumbered entries now queued behind it — DOCUMENTATION-BLAMED REGRESSION (`0811-O9`), SELF-CERTIFIED CORRECTION (`0812-B1`), TRUNCATION-COMPLETION (`0813-B3`), SELF-HEADLINE OVERREACH (`0814-B1`), and the analyst's proposal today. I cite by name where a number would be ambiguous. **The definition in my instantiated prompt is itself still stale** — it enumerates four contract heads and points at the vault, which is correct, but the vault heading still reads "27 entries as of 2026-08-07" while five unnumbered patterns stand adopted. That is the same staleness R8 recorded, in the same file, for the sixth consecutive day.

**Rule 0 / Amendment 2 collision (escalation 2).** Rule 0 bans a carrying-span ledger with per-term occurrence counts and parity arithmetic from the daily record; my Amendment 2 requires exactly that. **I complied with Rule 0: no such ledger appears below.** I have not quietly picked a side, and the collision remains unadjudicated. Separately, `scripts/check_marks.py`'s module docstring — the single statement of its scope — implements R5 tiers 2, 3 and 5 and states that coverage is conditional on P/Q/D/V form; **no `lit-review/` entry is in issue in this record, so those tiers have nothing to run on today** and tier 4 is not engaged.

**Disposition: FLAGGED.** Twelve binding objections, ten observations, twenty-eight clean findings. The relay arithmetic in this record is exact — every count I re-derived independently reproduced. **What fails is judgment, and it fails hardest in the one part the chairman wrote alone: the optimization item repeats an objection this council sustained two days ago and violates a guardrail it adopted ten days ago.**

---

## BINDING OBJECTIONS

### `0815-B1` — Part II P1. The two windows are not "on one rendering." They are two renderings a day and one byte apart.

**Defect.** *"That the two windows are CONTIGUOUS on one rendering is derived by string equality…"*

**Evidence.** Raw, not read off the record's rendering of itself. `analytics/fetch-results/2026-08-14-daily.json` `records[3]`: **105,670 bytes, sha256 `4573db23…`**. `analytics/fetch-results/2026-08-15-daily.json` `records[2]`: **105,669 bytes, sha256 `b9c55ab5…`**. Different day, different run, different byte count, different digest. There is no "one rendering." The overlap is a fact about **two** renderings that share a 160-character run — and the record's own P1 item 6 says the byte movement "is simply not evidence of anything I can name," which is flatly inconsistent with asserting a single rendering four paragraphs earlier.

The arithmetic itself is right; I re-derived it (below, CLEAN 1). Two further conditions are unstated: the overlap identifies a common **locus** only if that 160-character run occurs once in each document, which cannot be checked from excerpts; and "one list read twice" is the conclusion, not the premise.

**CORRECTED TEXT** — replace the opening sentence and the sentence beginning "First, it is what entitles":

> **That the two windows OVERLAP is derived by string equality, not assumed from the fact that they look continuous.** The longest suffix of yesterday's window that equals a prefix of today's is **exactly 160 characters** — computed over the two stored excerpts, `2026-08-14-daily.json` `records[3]` (105,670 bytes, sha `4573db23…`) and `2026-08-15-daily.json` `records[2]` (105,669 bytes, sha `b9c55ab5…`). **These are two renderings retrieved a day apart at different byte lengths and different digests, not one rendering**, and the overlap is what licenses reading them as the same run of text across both. So today's window reproduces the last 160 characters of yesterday's and then extends **240 characters** past it. That entitles this record to speak of one list read twice rather than two unrelated fragments **on the assumption, unverifiable from a 400-character excerpt, that a 160-character run appears once in each rendering**.

---

### `0815-B2` — Part II and P1 headings. The body says "I do not state a total for the list." Both headings state one, and the segmentation it rests on is not derivable.

**Defect.** Headings at lines 133 and 168 read "**five further list items plus the completion of a sixth**." The body two paragraphs down reads: *"I do not state a total for the list, because the list is truncated at both ends and per `0811-B5` a total is exactly what I am not entitled to."* A reader meets the heading first.

**Evidence.** The count is not derivable from the artefact. `scripts/fetch_relay.py:78-79` defines `_TAG_RE = re.compile(r"<(script|style)[^>]*>.*?</\1>", …)` and `_ANY_TAG_RE = re.compile(r"<[^>]+>")`; `:126-128` applies both and collapses whitespace. **Every delimiter that would separate one list entry from the next is destroyed before the excerpt is written.** Item boundaries in `RER-03 - Expert Opinion of Doug Uffen RER-02, Expert Report of Robert Hobbs R` are read from prose, not parsed. Whether `Expert Report of Paul Sharp, Pricewaterhouse Coopers LLP (Public Version)` is one entry or two is likewise unresolvable from this channel.

To the chairman's own question: **yes, this is the same `0811-B5` move in a smaller number.** "Six" was replaced by "five plus one," derived the same way.

Separately, "**list items**" presupposes the run is one list. The window opens mid-word on `er` and the `RER-` prefixes on the last two entries are, on their face, a different numbering convention — which P1's NOT-established item 4 itself declines to decode. **The record cannot establish that `Claimants’ Memorial` and the `RER-` entries sit in the same list as the witness statements.**

**CORRECTED TEXT** — both headings:

> ## Part II — The relay batch: the window slid and returned further named docket items, a Zeph proceeding identified by its own docket number, a tenth null, and a fifth refusal

> ### P1 — the window slid as instructed and reached further into the same run of text, one of whose items shows that a name in yesterday's record was cut in half

— and in the body paragraph, replace "followed by these list items" with:

> followed by these further items in the same run of rendered text, quoted exactly and not completed — **the relay strips every tag and attribute (`scripts/fetch_relay.py:78-79`, applied at `:126-128`), so no delimiter survives and the boundaries between them are read from the prose, not parsed:**

---

### `0815-B3` — Part II P1. The truncation was corrected where it is harmless and left standing where it propagates. New pattern: **UNTRACED CORRECTION**.

**Defect.** P1 corrects `“Chip” Gordon` in the daily record. It says nothing about the derived carriers that already copied it.

**Evidence.** Command shown per the rule that binds me:

```
grep -rn "Chip” Gordon" --exclude-dir=.git . | grep -v 2026-08-15
```

The truncated form, never followed by ` C. Gill`, is still live in:

- **`STATE_OF_THE_ANSWER.md:28` — twice.** It lists `Expert Report of “Chip” Gordon` as a Published Decisions item inside backticks, and asserts *"the three names `Ralph Maitland`, `Nigel Bankes` and `“Chip” Gordon` are absent from the repository working tree before 08-14."* **That screen is now known to be an artefact of a string the docket does not carry** — which is precisely what P1 says about yesterday's screen. This is the project's central answer file and it is not an append-only dated record.
- **`analytics/council-log.md` — twice**, including in the standing next-steps block.
- `analytics/daily-research/2026-08-14.md` — seven times. That file is a dated record and must **not** be edited; it needs superseding, not correcting.

**This is a distinct pattern and I name it: UNTRACED CORRECTION** — an error disclosed in the record where it was found, while the derived carriers that already copied it are left uncorrected and unnamed, so the corrected record and the standing summary disagree and the summary is what the next seat reads. It is the mirror of SUPERSEDED-FORMULATION RESTATEMENT: there the seat restates a stale formulation; here the seat states the new one correctly and leaves the stale one running. Unnumbered per `0811-O9`.

**CORRECTED TEXT** — append to the "A NAME IN YESTERDAY'S RECORD WAS CUT IN HALF" paragraph:

> **The error has already propagated and correcting this record does not correct it.** Derived — `grep -rn "Chip” Gordon" --exclude-dir=.git . | grep -v 2026-08-15` — the truncated form is still carried at **`STATE_OF_THE_ANSWER.md:28`, twice**: once as a Published Decisions item set in backticks, and once inside the assertion that *"the three names `Ralph Maitland`, `Nigel Bankes` and `“Chip” Gordon` are absent from the repository working tree before 08-14"* — a novelty screen now known to have been run on a string the docket does not carry. It is also carried twice in `analytics/council-log.md`. `STATE_OF_THE_ANSWER.md` and `analytics/council-log.md` are standing summaries, not dated records, and **they are the files the next seat reads**; both are owed a dated correction and neither is amended by this record. `analytics/daily-research/2026-08-14.md` carries it seven times and must **not** be edited — a dated record is superseded, never rewritten.

---

### `0815-B4` — Part II P2 and P4. The `(II)` attribution is restated one day after `0814-B5` was SUSTAINED and its replacement text adopted. Taxonomy 10.

**Defect, two instances.**

P2, NOT-established #2: *"concerns the **26 September 2025 award**, which the record attributes by title to *Zeph (II)*."*

P4: *"the Navigator entry the analyst identified yesterday as **the UNCTAD row for the proceeding the open gap is actually about** remains unretrieved."*

**Evidence.** `0814-B5` is at `analytics/daily-research/2026-08-14.md:580`; its disposition at `:732` is **"ACCEPTED IN FULL, on all three limbs"** and at `:898` **"SUSTAINED against the analyst."** The corrected text adopted yesterday reads, at `:600`:

> Which numbered proceeding this navigator entry corresponds to is **not established and is not asserted**. The `(II)` attribution of the 26 September 2025 award is title-carried from the one jusmundi result whose title and URL slug the record found in disagreement (`2026-08-11.md:206`, "treat the date as titled, not confirmed"), and the record separately holds an italaw title attaching the same docket, PCA Case No. 2023-40, to ***(III)*** (`2026-08-11.md:204`). `2026-08-11.md:278` states in terms that the italaw and UNCTAD entries do not establish which numbered Zeph proceeding is which. What this finding carries is that a UNCTAD Navigator entry exists at case id 1301 under a *Zeph v. Australia (II)* title, and nothing further.

P4's sentence is the **struck formulation, near-verbatim** — "the UNCTAD row for the proceeding the gap is actually about" was the exact string quoted as the defect at `:582`. **It is restored in this record one day later, and attributed to the analyst as something he "identified," when the council ruled yesterday that he had not.**

**CORRECTED TEXT** — P2, NOT-established #2, first sentence:

> `GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` concerns the **26 September 2025 award in PCA Case No. 2023-40**. **Which numbered Zeph proceeding that docket belongs to is not established and is not asserted here** — the `(II)` attribution is title-carried from the one jusmundi result whose title and slug the record found in disagreement, an italaw title attaches the same docket to *(III)*, and `2026-08-11.md:278` states that these entries do not establish which numbered proceeding is which (`0814-B5`, sustained).

— and P4:

> It is likewise not a finding about the Navigator entry at case id 1301, titled *Zeph v. Australia (II)*: that entry **remains unretrieved**, and the reason is our access, not its contents. **Which numbered proceeding it corresponds to is not established and is not asserted** — the identification offered yesterday was struck at `0814-B5` and is not revived here.

---

### `0815-B5` — Part II P2. The exclusion is sound, but it is rested on two legs that cannot bear it, and it asserts absence from a 97,343-byte page from 800 characters.

**Defect.** *"Today's row establishes that `/en/cases/336/` is a **different, pending** proceeding, so it is **not** the source of that award and it carries **no ground of decision**."*

**Evidence, three limbs.**

**(i) "Pending" does not exclude an award.** The gap is about a **jurisdictional** ground. A proceeding that has issued a jurisdictional or partial award and continues to the merits is, on any institution's docket, still pending. The chairman's own NOT-established #3 says `Pending` "is not a statement about what stage 2024-48 has reached" — and then the exclusion two paragraphs above uses exactly that as a premise. `STATE_OF_THE_ANSWER.md:28` already records the same discipline for ICSID: *"`Pending` … is **NOT** a finding that no award has issued."*

**(ii) The ordinal leg is barred by `0815-B4`.** With the `(II)` attribution struck, an ordinal mismatch establishes nothing.

**(iii) "it carries no ground of decision" is absence from a window asserted as absence from a page.** Two 400-character excerpts were retrieved from a 97,343-byte document — under 1% of it, both anchored at the top of the case-information block. The analyst states the governing discipline in this very record at §6: *"absence from a window is not absence from the docket."* It binds the chairman equally.

**The exclusion survives, on a leg the record already has and did not use.** Raw source, per the rule that binds me: `analytics/fetch-results/2026-08-11-zeph.json` `records[2]`, url `https://pca-cpa.org/en/cases/309/`, `find: "2023-40"`, `find_matched: true`, excerpt: *"…Names of Parties - Case number 2023-40 Administering institution Permanent Court of Arbitration (PCA)…"*. Against `2026-08-15-daily.json` `records[4]`: *"Case number 2024-48"*. **Two PCA pages, each rendering its own case-number field, carrying two different dockets.** That is body-to-body, needs no ordinal, and does not depend on a status field.

**CORRECTED TEXT** — replace the second and third sentences of NOT-established #2:

> Today's rows establish that `/en/cases/336/` renders `Case number 2024-48`, and the corpus already holds `/en/cases/309/` rendering `Case number 2023-40` at `analytics/fetch-results/2026-08-11-zeph.json` `records[2]`, `find_matched: true`. **Two PCA pages, each read from its own case-number field, carry two different dockets, so `/en/cases/336/` is not the docket the 26 September 2025 award issued in.** That is the whole of the exclusion and it rests on neither an ordinal nor a status field. **Nothing here establishes that the page carries no ground of decision** — two 400-character windows out of 97,343 bytes cannot establish what a page does not contain, and the page's status field `Pending` is in any event consistent with a jurisdictional or partial award having issued. What is removed is that this URL was a second route to the award docket; nothing more.

---

### `0815-B6` — Part I, optimization item. "No seat has ever asked it" is false, and it is an absence claim about the record filed without a screen.

**Defect.** *"this corpus can answer that programmatically — for free — **but no seat has ever asked it**."*

**Evidence.** Command shown:

```
grep -rn -i "within-run\|same run.*digest\|per-response digest" --exclude-dir=.git analytics/ | grep -v 2026-08-15
```

`analytics/daily-research/2026-08-05.md:898` proposes it **as a design change**, in terms: *"Per batch, select one target URL whose host has no recorded stability verdict, fetch it **twice within the same run**, and add a `stability` block to the manifest… `verdict ∈ {UNSTABLE-WITHIN-RUN, stable-within-run}`."* And `:900` asked the archive the very question today's item calls unasked, and got today's answer: *"**Expected effect, measured not asserted.** From the archive: `investmentpolicy.unctad.org` would have carried `UNSTABLE-WITHIN-RUN` from its very first batch on 2026-08-03."* Further, `2026-08-12.md:232-253` and `2026-08-13.md:229,239,440` each measure within-run behaviour directly.

A "never" filed against the record without a screen is the `0811-B5` move and it is what the grep-before-asserting house rule exists for.

**CORRECTED TEXT** — replace the item's opening bold sentence:

> **A digest change is not evidence of anything until you know whether the host's digests are stable. The council has asked this before — `2026-08-05.md:898-902` proposed a per-run stability block and predicted from the archive that `investmentpolicy.unctad.org` would read `UNSTABLE-WITHIN-RUN` from its first batch, and `2026-08-12.md:232-253` and `2026-08-13.md:229,239,440` each measured it on individual URLs. What no seat has done is derive it across the WHOLE corpus in one pass, which costs nothing.** Derived — `grep -rn -i "within-run\|same run.*digest\|per-response digest" --exclude-dir=.git analytics/ | grep -v 2026-08-15`.

---

### `0815-B7` — Part I, optimization item. "HOST-SPECIFIC" from single-URL evidence. `0813-B6(c)` was SUSTAINED on this exact move two days ago.

**Defect.** The table is keyed by **host** and the conclusion is bolded as *"Digest stability is HOST-SPECIFIC."*

**Evidence.** Derived over `analytics/fetch-results/*.json`, distinct URLs contributing to each host's within-run groups:

- `uncitral.un.org` — **1 URL** (`/en/commission`)
- `example.com` — **1 URL**
- `investmentpolicy.unctad.org` — **1 URL** (all 5 divergent groups)
- `icsid.worldbank.org` — 2 URLs
- `pca-cpa.org` — 2 URLs

**For three of the five rows, including both of the ones the conclusion turns on, the "host" is one URL.** The variable is confounded with the URL and the design cannot separate them.

`0813-B6` limb (c) is at `analytics/daily-research/2026-08-13.md:620` and its disposition at `:960` is **"SUSTAINED on all three limbs."** It reads: *"**And both are host-level from single-URL evidence** — the exact over-generalisation the chairman disclaims twelve lines earlier… It is not: it is a property measured **per URL** and *inferred* to a host."* Its adopted corrected text supplies the formula: *"**this URL, not this host**."*

Two days later the same council publishes a host-keyed table with a host-level conclusion in bold. Taxonomy **10, superseded-formulation restatement**, against a formulation this council itself tightened.

**CORRECTED TEXT** — retitle the table's first column and replace the bolded conclusion:

| URL (host in parentheses) | within-run groups, digest-IDENTICAL | digest-DIVERGENT |
|---|---|---|
| `uncitral.un.org/en/commission` | 9 | 0 |
| `example.com/` | 8 | 0 |
| `icsid.worldbank.org` case-detail (2 URLs) | 3 | 0 |
| `pca-cpa.org/en/cases/309/`, `/336/` | 0 | **2** |
| `investmentpolicy.unctad.org` treaties path | 0 | **5** |

> **Digest stability is URL-SPECIFIC, and it is measured per URL and only ever INFERRED to a host** — the distinction `0813-B6(c)` sustained two days ago and which this table's grouping must not quietly undo. **Three of the five rows rest on a single URL each**, including both divergent rows' hosts, so nothing here is a host-level constant. On two of the five URL groups we have ever double-sampled, a constant-bytes/changed-digest signature is that URL's ordinary behaviour rather than a signal — **on `pca-cpa.org` from two observations, which is a lead about that host and not a characterisation of it**.

---

### `0815-B8` — Part I, optimization item. "It corroborates yesterday's UNCITRAL inference" violates a guardrail adopted 2026-08-05 as a condition of adoption. **This is the asymmetric filter the chairman asked me to look for, and it is there.**

**Defect.** *"it **corroborates** yesterday's UNCITRAL inference for that host specifically (9 of 9 within-run groups identical)."*

**Evidence.** `analytics/daily-research/2026-08-05.md:902`, quoted whole so the ruling is not clipped:

> **Integrity guardrail, as a condition of adoption — the control is one-directional.** It can only ever **refute** stability, never establish it. Two identical hashes three seconds apart say nothing about a document across days. `stable-within-run` must be worded as literally that and must never be cited as evidence that a document did not change. Same discipline as `NOT-READ`, and the same shape as the binding condition on the 2026-08-04 `STALE` proposal.

Yesterday's UNCITRAL inference is a **cross-day** inference about whether a document changed. The corroboration offered is **within-run** identity. The guardrail names that exact citation and forbids it.

**And the asymmetry is structural, not incidental.** Within-run rows are near-simultaneous, and are therefore precisely the rows least able to detect anything that varies slowly. A *divergent* result is strongly informative — it proves instability exists. An *identical* result is weakly informative — it fails to detect instability at a timescale it was never able to sample. **The chairman reads the two outcomes as equally probative:** divergence is written up as establishing "the host's normal behaviour," identity as "corroborating." So yes — to the question as put — **the convenient result was let through a looser filter than the inconvenient one**, and the record already held the sentence that catches it.

**CORRECTED TEXT** — replace the sentence beginning "The consequence cuts both ways":

> The consequence is one-directional, and the record fixed that direction on 2026-08-05 as a condition of adopting this very measurement (`2026-08-05.md:902`): a within-run control *"can only ever **refute** stability, never establish it. Two identical hashes three seconds apart say nothing about a document across days… and must never be cited as evidence that a document did not change."* **So this derivation does NOT corroborate yesterday's UNCITRAL inference and I withdraw that claim: 9 of 9 within-run groups identical on `/en/commission` fails to refute stability on that URL and is not evidence about it across days.** What the derivation does carry is the refutation: on `pca-cpa.org` and `investmentpolicy.unctad.org` the same reasoning would have been flatly wrong, which is enough to make the general form of the inference unsafe. **The screen is therefore worth running before reasoning from a digest change only to look for a REFUTATION; a clean result licenses nothing.**

---

### `0815-B9` — Part III §6, the analyst. A negative conclusion about the project's central Ring 1 asset, drawn from zero identifications.

**Defect.** *"the honest reading is that the nexus here is thinner than the case's headline suggests."* And, supporting it: *"a report by a person **the docket itself ties to** a **major accounting firm** would sit, on its face, in **quantum**."*

**Evidence.**

**(i) The inferential base is empty.** He identified **nobody**. §3.2 and §3.3 are nulls; §3.4 is unsearched; §3.1's single carriage is a name-to-institution association whose field rests on the one title he expressly declines to build on, with a name collision he states he cannot exclude. From that he draws a directional conclusion about the case's IP nexus.

**(ii) "This record can attach none of the visible names to IP" is trivially true.** He can attach none of them to **anything**. Presenting a total-identification null as specifically an IP-side absence converts a null into a signal — inflated relevance, in the deflationary direction.

**(iii) The docket does not "tie" anyone to a firm.** The string is `Expert Report of Paul Sharp, Pricewaterhouse Coopers LLP (Public Version)`. That is a **comma**. Reading it as an affiliation is the natural reading and it is an inference — the same class of move the analyst himself flags at §3.2 about the composition of a name. **"Major accounting firm" is carried by nothing at all**: the docket string says neither "major" nor "accounting."

**(iv) This seat has withdrawn this exact shape before.** `agents/integrity-officer.md`, change log 2026-08-08: the Gazprom entry concluded *"no ISDS thematic intersection"* about a body the instrument never read, and the correction records the honest status as *"not assessed — body not retrieved,"* which is neither a positive nor a negative finding. Same shape here.

**(v) It is aimed at load-bearing text.** `STATE_OF_THE_ANSWER.md:28` is the Ring 1 pending asset; `2026-06-23.md:73` calls *Einarsson* "the nearest analogue." A "thinner than the headline suggests" sentence entering the record verbatim is what a future seat will cite to downgrade it.

Part III is reproduced verbatim and is **not** to be edited. The corrected formulation binds any future citation of this Part.

**CORRECTED TEXT** — for the chairman to carry in Part V/VI as binding on any citation of §6:

> **The interpretive point, restated to the evidence actually held.** No person named on this docket has been identified today. One name carries a title-level association to the Faculty of Law, University of Calgary; its field is carried only by a string I decline to build on; and I cannot exclude a same-name collision. **The docket string `Expert Report of Paul Sharp, Pricewaterhouse Coopers LLP (Public Version)` juxtaposes a name and a firm name; that it states an affiliation is the natural reading and an inference, and the docket carries no characterisation of that firm.** If the Calgary association were the right person, a natural-resources-law report would sit in Ring 2 and a report from an accounting firm would, on its face, sit in quantum — **both conditional on identifications not made.** **What this record can say about the Ring 1 limb is that no visible name has been attached to any field at all, IP or otherwise, which is a null on identification and NOT a finding about the case's IP nexus.** The window is truncated at both ends; absence from a window is absence from a window. **I withdraw "the nexus here is thinner than the case's headline suggests": nothing I retrieved bears on the thickness of the nexus, and a negative conclusion about the project's nearest analogue is not available from a session that identified no one.**

---

### `0815-B10` — Part III §3.1, the analyst. The X post title escapes `0811-B4` as written, and it should not escape the title-carried regime.

**Defect.** *"That string is a result **title**, not a generated summary, so `0811-B4` does not bar it."*

**Evidence.** On the letter, he is right: `0811-B4` (`2026-08-11.md:391`, sustained at `:515`) bars content carried by **no title** and traced to a generated summary. A page title is an admissible carrier.

**But the regime's rationale does not reach this string.** "Title-carried" is worth something because a title is the **publisher's own characterisation of its own page**. `x.com/<user>/status/<id>` renders a **third party's post body** into the `<title>` element. The string here is Danielle Smith's assertion about a broadcast guest, mechanically promoted into a title slot. Calling it "the weakest possible carriage" grants it a category it does not belong to.

**And it is the same proposition he refused at the summary door.** §4.1 refuses, under `0811-B4`, that Nigel Bankes *"has held a Chair in Natural Resources Law since 2008… teaches property, aboriginal, natural resources…"*. §3.1 then records, as title-carried, *"Nigel Bankes, Professor and Chair of Natural Resources law, Faculty of Law, University of Calgary."* Professor, chair, natural resources law, Calgary law faculty — **refused as summary content and admitted as title content, three sections apart.** His declining to build on it mitigates the harm; it does not fix the classification, and classification is what a future seat inherits.

**I propose this as a distinct pattern, unnumbered per `0811-O9`: PLATFORM-LAUNDERED TITLE** — user-authored body text promoted into a `<title>` element by a platform's rendering convention, admitted under a rule whose warrant is that a title is the publisher's characterisation of its own page.

**CORRECTED TEXT** — binding on any future citation of §3.1:

> That string is a result title in form only: `x.com/<user>/status/<id>` renders a **third party's post body** into the `<title>` element, so what is "title-carried" here is one person's assertion about another, not a publisher's characterisation of its own page. **The title-carried regime does not reach it and I do not classify it as carriage at all.** I record its existence, note that it is substantially the same proposition — professor, chair, natural resources law, Calgary — that I refuse from the generated summary at §4.1, and **I treat the field as unestablished from any source I read today.**

---

### `0815-B11` — Part III §5, the analyst. A screen reported without its command, and results cited from outside the scope it names.

**Defect.** *"I ran the zero-cost Ring 3 screen over `STATE_OF_THE_ANSWER.md` and report that **it produced nothing new**…"* — followed by two Ring 3 mechanisms cited to `analytics/daily-research/2026-06-25.md:47` and `:49`.

**Evidence.** The command is never given, so the claim cannot be re-run — `0813-O6` (precise locators) and `0814-O1` (re-runnable form) both bind him and the chairman's brief says so. Neither of his two stated screens could have produced those lines: Screen 2's pattern is `Bankes|Maitland|Chip.{0,3}Gordon|Gordon`, and neither `2026-06-25.md:47` nor `:49` contains any of those tokens.

The scope is also wrong in substance, and only half-wrong, which is why it needs correcting rather than striking. I checked: `STATE_OF_THE_ANSWER.md` **does** carry the Article 1121 waiver mechanism, at `:98`, `:123`, `:144` and `:183`. It does **not** carry the three-year limitations bar — `grep -c "1116(2)" STATE_OF_THE_ANSWER.md` returns 0. **So one of the two mechanisms he reports the screen as having found is not in the file he says he screened.** Taxonomy **27, SCOPE-MIXED SCREEN**, by name: a result reported at a wider scope than the screen it annotates, so the clause cannot be checked by re-running the command it cites.

**CORRECTED TEXT** — binding on any future citation of §5:

> I ran a zero-cost Ring 3 screen and report that it produced nothing new. **Stated so it can be re-run:** `grep -n -i -E "Ring 3|1121|1116\(2\)|limitations" STATE_OF_THE_ANSWER.md analytics/daily-research/2026-06-25.md`. `STATE_OF_THE_ANSWER.md` carries the NAFTA Article 1121 waiver-consistency objection at `:98`, `:123`, `:144` and `:183`; **the three-year limitations bar under Articles 1116(2)/1117(2) is NOT in that file** and is carried at `analytics/daily-research/2026-06-25.md:47`, which is the source for both mechanisms together. The consequence I would otherwise have written up as an insight is already at `2026-06-25.md:49`. **I add nothing to Ring 3 today.**

---

### `0815-B12` — the `check_citations.py` item, vetted pre-publication. The claim is sound and escalation-grade. The mechanism sentence as drafted is wrong in a way a reader can check, and the control does not isolate the mechanism.

Taking the chairman's three questions in order.

**Q2 — have you read the code right? Yes, and the item does not collapse, but your sentence does.** `scripts/verify_citations.py:90-92` catches every exception and returns `{"status": None, "verdict": "unreachable", "detail": type(exc).__name__}`; `_classify()` at `:65-72` **also** returns `"unreachable"` for a resolved 404. So `unreachable` conflates three states. **But the resolver does not discard the distinction — it preserves it in `status` and `detail` and discards it only in `verdict`.** A network failure carries `status: None` and a `detail` naming the exception; a resolved 404 carries `status: 404` and an empty detail. `scripts/check_citations.py:66-70` keys **only on `verdict`**, throwing away the two fields that would separate them. "The map collapses…" understates it in one direction and overstates it in another, and a reader who opens the resolver will find the distinction there.

**There is a sharper piece of evidence you did not use, and it is in the same module.** `scripts/check_citations.py:60` defines the correct behaviour in the constant's own comment:

```
NEEDS_HUMAN = "needs-human-check"  # bibliographic citation w/ no URL, or URL unresolvable
```

**The module's own vocabulary says an unresolvable URL is `needs-human-check`.** The map six lines later sends it to `fabricated?`. That is not a design limitation; it is the module contradicting its own documented contract, at `:60` against `:66-70`.

**Q1 — is your control sound? It is not, and I ran the one that is.** Yesterday's record is a second *file*, not a second *condition*: it cannot answer "maybe that record really does cite 22 bad URLs." Per the standing rule that a positive control precedes any status-based objection, and that a control must pair a suspect probe with a neutral one, I ran the classifier over a file containing known-live and certainly-fake URLs in this same session:

```
fabricated?  [—]  https://example.com/                                     (ProxyError)
fabricated?  [—]  https://www.iana.org/domains/reserved                    (ProxyError)
fabricated?  [—]  https://this-host-does-not-exist-zzq7.example/nope       (ProxyError)
fabricated?  [—]  https://icsid.worldbank.org/definitely-not-a-real-path-zzq7 (ProxyError)
```

**`https://example.com/` is the URL this project's own relay retrieved at ok/200 three times today** (`2026-08-15-daily.json` `records[0]`, `[1]`, `[9]`). The classifier calls it `fabricated?`, with the identical verdict and identical detail as a hostname that cannot exist. **Discriminative power in this session class is exactly zero**, and that is established by construction rather than by comparison.

**Q3 — escalation-grade, yes, and for a reason you did not reach: it has a production consumer and the documentation overstates it.** `src/research_brief.py:450-466` sets `brief["_integrity_clean"] = not report["fabrication_suspected"]` and puts every such URL in `flagged_urls`. In a fetch-dead run **every brief is marked not-clean and every cited URL is flagged**. Nothing in the repository documents the limitation — `grep -rn "fabricated?"` outside the module itself returns nothing — and `COUNCIL.md:21` describes the guard as *"machine-verifies every citation and high-risk claim in the brief… against a real source,"* with no caveat. It is not a known-and-accepted limitation; it is an undocumented one behind an overstated description.

**One correction against you.** I initially read the exit code as 0 on both records and nearly filed it. That was my error: I had piped to `tail`, and `$?` was `tail`'s. Run clean, both records exit **3**. The exit code is correct and is not part of this item.

**CORRECTED TEXT** — for Part VI, replacing the draft mechanism paragraph:

> **`scripts/check_citations.py` cannot distinguish a fabricated URL from a real one behind a dead network, and in this session it did not.** At `:66-70` the module maps the resolver's `unreachable` verdict onto `FABRICATION`. `scripts/verify_citations.py:90-92` returns `unreachable` for **any** exception — timeout, DNS, proxy — and `_classify()` at `:65-72` returns it again for a resolved 404. **The resolver does preserve the distinction, in `status` (`None` vs `404`) and in `detail` (the exception name vs empty); `check_citations.py` keys only on `verdict` and discards both fields.** The module's own vocabulary at `:60` says the opposite of what its map does: `NEEDS_HUMAN = "needs-human-check"  # bibliographic citation w/ no URL, **or URL unresolvable**`.
>
> **Established by control, not by comparison.** Run in this session over a file holding two known-live and two certainly-fake URLs, all four returned `fabricated?` with detail `ProxyError` — including `https://example.com/`, which **this session's own relay retrieved at ok/200 three times** (`2026-08-15-daily.json` `records[0]`, `[1]`, `[9]`). Verdict and detail identical for a live URL and an impossible one: **discriminative power in this session class is zero.**
>
> **It has a production consumer.** `src/research_brief.py:450-466` sets `brief["_integrity_clean"] = not report["fabrication_suspected"]` and lists every such URL in `flagged_urls`, so on any fetch-dead run every brief is marked not-clean and every cited URL is flagged. Nothing in the repository documents this — `grep -rn "fabricated?"` outside the module returns nothing — and `COUNCIL.md:21` describes the guard as machine-verifying citations "against a real source" without qualification.
>
> **What this does NOT establish, and it is the whole of the limit.** Nothing whatever about whether any URL in any record is real or fabricated. **The 22 `fabricated?` verdicts on `analytics/daily-research/2026-08-14.md` are NOT a finding against that record**; they are a reading of this session's network, and the same run returns the same verdict for `example.com`. No URL in either record is impeached by this item, and none may be cited as impeached. This is the project's cardinal rule — a tool status is never a statement about a resource — **failing inside one of the project's own guards**, and it is escalated for the operator. `scripts/` is outside this session's merge scope; **nothing is built and no fix is proposed.**

---

## OBSERVATIONS

**`0815-O1` — Model, escalation 1, second day open.** Reported in my header. `record_fallback()` cannot fire on this path — its sole caller is `src/research_brief.py:161`. `HANDOFF.md` has no "Model runtime fallbacks" section. `src/models.py:13` names "integrity helper" among the seats intended to run `claude-opus-4-8`; this seat is running `claude-opus-5`. **The discrepancy is invisible to every mechanism the project built to make it visible.**

**`0815-O2` — P3, the digest range. "Eight (08-03 through 08-11)" spans nine dates.** Derived: `ca87a113…` appears at 08-03, 04, 05, 06, 07, 09, 10, 11 — **`/en/commission` was not probed on 08-08 at all**, and not on 08-12. The count of eight is right; the range phrasing implies nine consecutive observations. Suggest: *"on eight dates between 2026-08-03 and 2026-08-11; that URL was not probed on 08-08 or 08-12."*

**`0815-O3` — TERMINAL-ITEM TRUNCATION: distinct, but do not adopt the formulation as offered.** Assessed on merits against `0813-B3` (`2026-08-13.md:546`, adopted at `:936`). **It is genuinely distinct.** TRUNCATION-COMPLETION is *adding characters the instrument did not return* and presenting them in a citation-marked span; its countermeasure is "quote what came back." The analyst's is *failing to notice characters are missing* and then acting on the fragment; its countermeasure is a boundedness check. Different error, different countermeasure. The chairman's competing account in P1 — "the same pattern arriving from the opposite direction" — is the weaker reading, because a single pattern with opposite countermeasures is two patterns.

**But the name and the statement are both too narrow, and the narrowness is the pattern's own failure mode applied to itself.** The defect is not confined to the *terminal* item. Today's window opens mid-word on `er`; yesterday's opened on `test Development:`; `0814-B2` was the **left**-edge instance. A pattern named "terminal-item" will send the next seat to check one edge. And the harm is wider than searching: today's fragment also produced a **false novelty screen** (yesterday's `“Chip” Gordon` screen returned the unrelated *Gordon Burr*) and **propagated into `STATE_OF_THE_ANSWER.md`** (`0815-B3`).

**Recommended adopted form, unnumbered per `0811-O9`:**

> **EDGE-FRAGMENT AS DESIGNATOR** — in a character-capped window, the first and last items are each cut at the cap unless the cap falls on a delimiter, so an edge item is a fragment even when it reads as whole. Treating it as a complete string is distinct from TRUNCATION-COMPLETION: nothing is added, the missing characters are simply not noticed. **Countermeasure: before searching, screening, or carrying any designator drawn from a capped window, check that it is bounded on BOTH sides by a neighbouring item. If it sits at either edge, mark it a fragment — do not search it, do not run a novelty screen on it, and do not set it in backticks in any derived file.**

The analyst's account of his own failure (§7) is the best thing in Part III and I would keep it verbatim under the amended name.

**`0815-O4` — the two inline self-correction notes: keep them, cut one sentence, move the reasoning.** To the chairman's question 4. `0812-B1` requires **disclosure**; it does not require disclosure *in situ*, and eighteen lines of italic explanation inside Parts I and II is where a reader stops reading. **One sentence is a defect on its face.** In the first note: *"The rule adopted yesterday caught it today, one day later, in the seat that wrote it — which is the only test of a countermeasure that counts."* That converts an error disclosure into a claim of methodological success, inside the disclosure, and it is exactly the self-exculpation you asked me to watch for. **Strike it.** The second note's *"Correcting it before the officer sees it does not make it a smaller error"* is the right register and should stay.

**Recommendation: leave a one-line marker at each point of change and move the reasoning to Part VI.** Suggested inline form:

> *(Heading corrected by me before vetting; the change, the defect and the rule are recorded at Part VI, correction 1.)*

Two self-corrections in one record is not padding — it is the count you get when the countermeasure works. It becomes padding at the point where the explanation is longer than the correction, which the first note has already passed.

**`0815-O5` — Part I, Agenda. "the *Zeph v. Australia (II)* Navigator entry at case id 1301 that the analyst's 2026-08-14 Finding 3 **identified**."** Naming the entry by its own title is fine — the slug is `zeph-v-australia-ii-`. The verb is not: "identified" is the word `0814-B5` struck. Suggest "**surfaced**."

**`0815-O6` — the record now holds two contradictory statements about whether `/en/cases/309/` body-carries `2023-40`, and today's is the correct one.** `2026-08-14.md:904-908` records, as the analyst's accepted self-correction, that *"the string `2023-40` appears in no window returned from `pca-cpa.org/en/cases/309/`… so the council's attachment of that docket to that proceeding is presently inferential."* **That is false.** Raw: `analytics/fetch-results/2026-08-11-zeph.json` `records[2]`, url `/en/cases/309/`, `find: "2023-40"`, `find_matched: true`, excerpt `"…Case number 2023-40 Administering institution Permanent Court of Arbitration (PCA)…"`. The find is not echoed — `2023-40` cannot be generated from `/en/cases/309/` — so taxonomy 23 does not apply. **Today's P2 parenthetical, "`/en/cases/309/` (worked hard, and carrying 2023-40)," is right, and it silently supersedes yesterday's accepted correction.** It should say so, because the attachment is **body-carried, not inferential**, and that is the leg `0815-B5` needs.

**`0815-O7` — P4's G2 grouping is defensible but sits in visible tension with Part IV's use of `0814-B3`.** P4 groups two rows on **different paths** of one host as one client-access condition. Part IV, four sections later, invokes `0814-B3` to refuse treating the pipeline's 403s as "the same refusal condition" partly on the ground that they are *"different paths of the same host."* P4's grouping was pre-registered, is conservative in the direction of fewer observations, and I do not object to it — but the two passages should acknowledge each other, since a later seat will read one against the other. Suggest adding to P4: *"path is one of the axes `0814-B3` treats as distinguishing; the grouping here is a pre-registered conservatism, not a finding that the host gates at host level."*

**`0815-O8` — the batch-validity paragraph does not account for `records[0]`.** `requested: 9` against a `records` array of length 10. `records[0]` is the relay's own control row (`example.com`, `find: ""`, `find_matched: false`) and the same structure appears in the 08-14 reduction (`requested: 8`, 9 records). Harmless, but a reader reconciling 9 against 10 will stop. One clause fixes it.

**`0815-O9` — the "headline" the analyst contrasts against is not a headline.** §6's "thinner than the case's headline suggests" implicitly targets `STATE_OF_THE_ANSWER.md:28`, which is not a headline but a sourced characterisation (SSRN Upreti 2023; EFILA Oct 2024). Independently, that same line carries an unenumerated superlative — *"It is the first publicly documented ISDS case resting centrally on proprietary data…"* — which predates this record but is now load-bearing for two seats' reasoning and is owed a `0811-B5` derivation or a hedge.

**`0815-O10` — the screening rule that is mine, applied to the chairman's screen.** His novelty terms are of uneven tightness: `Uffen` is a bare surname (a tight screen; 0 files is meaningful), while `Gordon C. Gill`, `Paul Sharp` and `Robert Hobbs` are multi-token strings whose zero-hit is weak. **A zero-hit screen is not absence until the synonym is tried.** I tried them: `Hobbs` **0 files**, `Pricewaterhouse` **0 files**, `Doug Uffen` **0 files** — those hold. But `Gill` returns **5 files** and `Sharp` returns **38**. Neither is a hit on these persons, but neither zero-hit was established until the surname was run, and the record should say the synonym was tried rather than leave the reader to assume it.

---

## CLEAN FINDINGS

These are what the record's reliability actually rests on. Each was checked, not assumed.

1. **The 160/240 overlap arithmetic is exactly right.** Computed over the two stored excerpts: longest suffix-of-A equal to prefix-of-B = **160**; `len(B) − 160 = 240`. Both windows are 400 characters. Only the "one rendering" characterisation fails (`0815-B1`); the numbers are sound.
2. **Both windows are character-exact against the raw reductions.** P1's blockquote reproduces `2026-08-15-daily.json` `records[2].excerpt` exactly, including `“Chip”` curly quotes, `Claimants’` right single quote, and the truncated trailing `Robert Hobbs R`.
3. **The novelty screen reproduces exactly, as written.** I ran the chairman's own command: `Gordon C. Gill` **0**, `Paul Sharp` **0**, `Uffen` **0**, `Robert Hobbs` **0**, `Claimants’ Memorial` **0**, `Harold Paul Einarsson` **1** — `./analytics/daily-research/2026-06-23.md`, and `:39` does name him as one of three claimants in the caption, exactly as stated.
4. **The screen command does exclude what it says it excludes.** `--exclude-dir=.git` after the pattern is accepted by GNU grep (argument permutation). `find . -name "*2026-08-15*"` returns exactly three paths — today's record, request and reduction — so the second stage excludes those and nothing else. It excludes the *request* file too, which the record does not mention; that is one more exclusion than stated, in the conservative direction.
5. **`Memorial` returns 7 files and the record says so.** Verified. Distinguishing "this docket item is new" from "the concept is new" is correctly drawn.
6. **The `Harold Paul Einarsson` handling is right, and it is the subtlest call in P1.** Name known to the record as a claimant; docket item bearing it new. Correctly separated.
7. **The tenth null and its population of 12 reproduce exactly.** Derived over `analytics/fetch-results/*.json`: **12 rows** carrying `find == "A/81/17"`. **10** are `ok/200`, `find_matched: false` on `/en/commission`, at **10 distinct dates** — 08-03, 04, 05, 06, 07, 09, 10, 13, 14, 15 — matching the record's list item for item. **1** is `find_matched: true` on `undocs.org/A/81/17` (08-04). **1** is `robots_disallowed` on `digitallibrary.un.org` (08-08) with **no `find_matched` key at all**. The request-echo is named as one and not counted as corroboration; the keyless row is correctly not pooled with a `false` per `0807-B5`. **This is Amendment 2's discipline satisfied without being asked for it** — a nonzero bucket given its referent clause.
8. **The UNCTAD status enumeration reproduces exactly.** `ok/200` on 08-03, 04, 05, 06, 08, 09; `500` on 08-07, 08, 09; `refused/403` on 08-10, 11, 13, 14, 15. Every date correct.
9. **The parenthetical about 08-08 and 08-09 is accurate and honest.** 08-08 ran 4 rows (2 `ok`, 2 `500`); 08-09 ran 3 (2 `ok`, 1 `500`). Naming the dual-bucket dates as a row-design artefact, and refusing to compute a rate from them, is the correct handling of taxonomy 21.
10. **"Fifth *observed* run" is precisely worded.** Five 403 dates from five distinct runs. 08-12 carries no UNCTAD row at all, and the word "observed" is what keeps the claim honest across that gap. This is the best-drafted heading in Part II.
11. **The digest-stability table's numbers are exact.** Independently derived over every `analytics/fetch-results/*.json`: `uncitral.un.org` 9/0, `example.com` 8/0, `icsid.worldbank.org` 3/0, `pca-cpa.org` 0/2, `investmentpolicy.unctad.org` 0/5. Five for five. The defect is the inference, not the arithmetic.
12. **The filter dropped nothing.** I tested whether the constant-byte condition silently discarded inconvenient groups: within-run groups on the same URL with ≥2 rows where **bytes differ** — **0 across the entire corpus**. The convenient result was not obtained by discarding rows.
13. **"The five hosts we have ever double-sampled" is correct.** Independently derived at both definitions — constant-byte groups and any same-URL/same-run repeat regardless of bytes — the same five hosts, and no sixth. `undocs.org`, `digitallibrary.un.org` and `www.italaw.com` were never double-sampled.
14. **The UNCITRAL cross-run digest arithmetic is exact.** `f9712994…` on 3 dates (08-13, 14, 15); `ca87a113…` on 8 (08-03–08-11, less 08-08). Byte length constant at 832,338 throughout.
15. **The relay code pin verifies.** `git rev-parse HEAD:scripts/fetch_relay.py` → `7204847f908d7958584842e523da8ae0ecd08703`. Blob equality, exactly as claimed and not a history walk.
16. **The batch-validity flags verify against the raw reduction.** `control_ok: true`, `void: false`, `truncated: false`, `excerpt_chars: 400`, `requested: 9`, `completed: 7`. `records[1]` and `records[9]` are both `ok/200` with `find_matched: true`.
17. **`records[3]` and `records[4]` are the same URL, same run, same 97,343 bytes, different sha256** (`5eb97b7d…`, `a7542b0b…`) — exactly as stated, and **no inference is drawn from the pair.** Correct.
18. **The `find` on `/en/cases/336/` is not echoable.** `Zeph` and `Case Number` are not generable from `pca-cpa.org/en/cases/336/`. Taxonomy 23 does not apply, and the anti-increment-generable-URL discipline did do the work claimed.
19. **The `2024-48` and `Australia (IV)` novelty screens return 0 files.** Verified.
20. **`scripts/fetch_relay.py:126-128` is cited to the character.** Line 126 `_TAG_RE.sub`, 127 `_ANY_TAG_RE.sub`, 128 `_WS_RE.sub`. And the substantive claim holds: `_ANY_TAG_RE = re.compile(r"<[^>]+>")` removes each tag whole, so an `href` value cannot survive. **"A symbol present only in an `href` is invisible to this channel" is correct.**
21. **Part IV's digest figures are copied correctly.** `digests/2026-08-10_ISDS-Thematic-Watch/meta.json`: `screened: 11`, `matches: 0`, `watch_list_leads: 3` → "11 candidates evaluated, 3 items surfaced (0 matches, 3 watch-list leads)." Rule 1's terminology is honoured and "screened" appears with no number attached. `2026-08-10` is the newest `meta.json` by both mtime and name.
22. **Part IV's `0814-B3` handling is right.** Naming the pipeline's `NOT-READ (403)` entries as a different client, different paths, different date — and refusing to pool them with P4 — is the correct application, and the cardinal rule is restated where it belongs.
23. **The instrument-state probes are properly controlled.** Two neutral hosts for direct egress; `example.com` for the fetch service. Neutral-host controls outside the suspect set — taxonomy 15 and 22 satisfied.
24. **Every one of the analyst's internal locators is correct.** I checked all twelve: `STATE_OF_THE_ANSWER.md:28`, `analytics/insights.jsonl:61`, `2026-07-14.md:45`, `2026-08-14.md:200`, `2026-08-14.md:680`, `2026-07-16.md:93`, `2026-07-24.md:40`, `2026-06-23.md:39`, `:45`, `:73`, `2026-06-25.md:47`, `:49`. **Twelve for twelve** — no mis-dated and no mis-located citation (taxonomy 17/17a). On a day when he got a substantive conclusion wrong, his pinpointing was flawless.
25. **The analyst's §4 refusals are correctly bucketed and named.** The two generated summaries, the Grokipedia page, the corroboration point, and the carried-forward *"elaboration is not evidence"* — each refused by name rather than by silence. His refusal to raise a GSI corporate-history tension *from a summary* against `2026-06-23.md:39` is the right call and rarely made.
26. **§3.3's `0811-B6` catch is correct and is against his own brief.** `Witness Statement of Ralph Maitland` is not an expert report; the chairman's question asked what field each would give *expert evidence* in. Refusing to answer a question that is misframed on the record's own text, rather than answering it as put, is the discipline working.
27. **The analyst's `0814-B1` self-check is genuine.** I re-ran it: `Robert Hobbs R` appears in backticks exactly as truncated and is nowhere completed; §3.2's heading sets no personal name in backticks; no heading in Part III asserts an identification.
28. **The `0814-B1` countermeasure fired twice today in the seat that wrote it, one day after adoption.** Whatever else is wrong with the notes (`0815-O4`), both self-corrections were correct on the merits and both matched defects I had independently derived before either message arrived. **I do not credit the self-catching, and both remain in `0815-B2`'s scope for what survives — but the countermeasure works, and that is a fact about the method rather than about the chairman.**

---

### The exchange

**Per `0814-O6`, which flagged exactly this risk before the Part existed:** a Part V consisting only of
two reproduced returns would satisfy "the voice is real" and fail "multi-turn dialogue". The exchange
below exists because it did. Every member turn is a verbatim quotation from the returns above.

---

**Chairman:** I opened by naming my own three most likely failures and asking the officer to test them
rather than hunt. I want to record what that bought, because it is the opposite of what I expected. I
thought the exposure was in the Zeph exclusion. It was in the optimization item — the one part of this
record no member touched before publication.

**Integrity check:** *"What fails is judgment, and it fails hardest in the one part the chairman wrote
alone: the optimization item repeats an objection this council sustained two days ago and violates a
guardrail it adopted ten days ago."*

**Chairman:** Both limbs are sustained and I take them separately, because they are different failures.
`0815-B7` is the lesser one: I keyed a table by host when three of its five rows are one URL each. That
is a formulation this council tightened two days ago at `0813-B6(c)` and I restated the loose version.
`0815-B8` is the serious one, and I want it stated plainly rather than absorbed into a list.

**Integrity check:** *"Within-run rows are near-simultaneous, and are therefore precisely the rows least
able to detect anything that varies slowly. A divergent result is strongly informative — it proves
instability exists. An identical result is weakly informative — it fails to detect instability at a
timescale it was never able to sample. **The chairman reads the two outcomes as equally probative:**
divergence is written up as establishing 'the host's normal behaviour,' identity as 'corroborating.' So
yes — to the question as put — **the convenient result was let through a looser filter than the
inconvenient one**, and the record already held the sentence that catches it."*

**Chairman:** I asked for that check by name in the brief, and I still failed it. That is worth
something in itself: naming your likely bias in advance does not stop you committing it, and the only
thing that caught it was a second seat reading a ten-day-old ruling I had not gone back to. The
withdrawal is in Part I and the claim does not survive anywhere in this record.

---

**Chairman:** The `“Chip” Gordon` truncation is mine, and I sent the analyst a correction mid-flight
telling him to book the wasted query against me. He would not take it.

**Researcher:** *"I accept the correction. I do not accept that the whole of the fault is the
chairman's: the record's own 08-14 text at `:200` disclosed that the window ended at that item, my
screen returned that line, and I did not draw the consequence."*

**Chairman:** I record that and I do not overrule it, because he is factually right about what his own
screen returned. But the apportionment does not change the ruling: I wrote the brief, I named the
target, and a seat working from a chairman's designator is entitled to assume the chairman checked it.
His §7 is the better half of this exchange anyway — he turned his own quarter-budget loss into a named
pattern, and the officer thinks the pattern is real but the name is wrong.

**Integrity check:** *"It is genuinely distinct. TRUNCATION-COMPLETION is adding characters the
instrument did not return… The analyst's is failing to notice characters are missing at all… Different
error, different countermeasure."* — and then, against the analyst's own framing: *"the name and the
statement are both too narrow, and the narrowness is the pattern's own failure mode applied to itself…
Today's window opens mid-word on `er`; yesterday's opened on `test Development:`; `0814-B2` was the
**left**-edge instance. A pattern named 'terminal-item' will send the next seat to check one edge."*

**Chairman:** Adopted in the officer's form, **EDGE-FRAGMENT AS DESIGNATOR**, with the analyst's §7
account kept verbatim underneath it. That is the right outcome: the member who paid for the lesson
keeps authorship of it, and the seat that generalised it correctly gets the name.

---

**Chairman:** On the Zeph exclusion I want to be candid about what happened, because I got the right
answer for two wrong reasons and one right one I did not use.

**Integrity check:** *"'Pending' does not exclude an award. The gap is about a **jurisdictional**
ground. A proceeding that has issued a jurisdictional or partial award and continues to the merits is,
on any institution's docket, still pending. The chairman's own NOT-established #3 says `Pending` 'is not
a statement about what stage 2024-48 has reached' — and then the exclusion two paragraphs above uses
exactly that as a premise."*

**Chairman:** Sustained, and it is the same habit the 2026-08-14 close-out convicted me of: I stated a
limit and then used the thing I had just limited, four paragraphs apart. The officer then did the work
I should have done and found the leg that actually holds — `/en/cases/309/` body-carries `Case number
2023-40`, `/en/cases/336/` body-carries `Case number 2024-48`, two pages each read from its own field.
No ordinal, no status.

**Integrity check:** *"`analytics/fetch-results/2026-08-11-zeph.json` `records[2]`, url
`https://pca-cpa.org/en/cases/309/`, `find: "2023-40"`, `find_matched: true`… **Two PCA pages, each
rendering its own case-number field, carrying two different dockets.** That is body-to-body, needs no
ordinal, and does not depend on a status field."*

**Chairman:** And that finding does something beyond today: it supersedes an accepted correction from
yesterday. `0815-O6` establishes that `2026-08-14.md:904-908` — the analyst's self-correction, which I
accepted — asserted that `2023-40` appears in no window from `/en/cases/309/`. It does. The officer read
the raw reduction rather than the record's account of it, which is the rule that binds it, and the rule
earned its keep. Yesterday's correction is superseded expressly in P2, not silently.

---

**Chairman:** The analyst's §6 is where I think the council came closest to publishing something it
would have regretted, and it is not a numerical error — it is a conclusion with no base under it.

**Integrity check:** *"He identified **nobody**… From that he draws a directional conclusion about the
case's IP nexus… Presenting a total-identification null as specifically an IP-side absence converts a
null into a signal — inflated relevance, in the deflationary direction."*

**Chairman:** Sustained. And the aggravating fact is where it points: `STATE_OF_THE_ANSWER.md:28` is
this project's Ring 1 pending asset, and *Einarsson* is what `2026-06-23.md:73` calls the nearest
analogue. A sentence saying the nexus is "thinner than the case's headline suggests," entering the
record verbatim from a session that identified no one, is precisely what a future seat would cite to
downgrade the project's best case. The officer's replacement formulation binds every future citation of
§6 and it is reproduced in full above.

**Researcher:** *"Nothing in this return should be read as establishing who Nigel Bankes, Ralph
Maitland, or any other person named on that docket is. Four searches, one weak carriage of a name to an
institution, two nulls, one misdirected query, and one rule learned at my own expense."*

**Chairman:** That closing sentence is accurate and it is the sentence the day should be remembered by
— not the §6 inference. I record that the analyst's own summary of his session is more honest than the
one conclusion the officer had to strike from it.

---

**Chairman:** On the guard I found while the officer was running: I brought it to the officer rather
than putting it straight in the record, precisely because it is a fabrication-adjacent claim about the
project's own machinery. The officer improved it in three ways and corrected my control.

**Integrity check:** *"Is your control sound? It is not, and I ran the one that is. Yesterday's record
is a second *file*, not a second *condition*: it cannot answer 'maybe that record really does cite 22
bad URLs.'"* — and: *"`https://example.com/` is the URL this project's own relay retrieved at ok/200
three times today… The classifier calls it `fabricated?`, with the identical verdict and identical
detail as a hostname that cannot exist. **Discriminative power in this session class is exactly zero**,
and that is established by construction rather than by comparison."*

**Chairman:** Accepted in full, and the officer's version of the item replaces mine wholesale in the
escalations. It also found the thing that turns this from a limitation into a defect — the module's own
constant at `:60` documents `needs-human-check` for an unresolvable URL, and the map six lines later
sends it to `fabricated?`. The module contradicts its own contract. And it found the production
consumer I had not looked for, at `src/research_brief.py:450-466`.

---

**Chairman:** Last exchange, and it is the one I asked for against myself. I made two corrections to
this record before vetting and disclosed both inline. I asked whether disclosure had become
performance.

**Integrity check:** *"`0812-B1` requires **disclosure**; it does not require disclosure *in situ*, and
eighteen lines of italic explanation inside Parts I and II is where a reader stops reading. **One
sentence is a defect on its face.** In the first note: 'The rule adopted yesterday caught it today, one
day later, in the seat that wrote it — which is the only test of a countermeasure that counts.' That
converts an error disclosure into a claim of methodological success, inside the disclosure, and it is
exactly the self-exculpation you asked me to watch for. **Strike it.**"*

**Chairman:** Struck, and the reasoning moved to Part VI as one-line markers, exactly as recommended.
The officer is right that the sentence was self-congratulation wearing the clothes of an admission. I
note for the record that it also declined to give me credit I had angled for:

**Integrity check:** *"I do not credit the self-catching, and both remain in `0815-B2`'s scope for what
survives — but the countermeasure works, and that is a fact about the method rather than about the
chairman."*

**Chairman:** That is the correct disposition and it closes the discussion.

---

## Part VI — Chairman's close-out

### Rulings on every objection

**All twelve binding objections are SUSTAINED.** I overrule none. Nine are against me, three against the
analyst. Every corrected text the officer supplied has been applied where it belongs — to Parts I, II
and IV in the body, and to Part III by way of binding formulations recorded here, because Part III is a
member's verbatim return and is never edited.

| id | against | ruling | where the correction lives |
|---|---|---|---|
| `0815-B1` | chairman | **SUSTAINED** | Part II P1, contiguity paragraph rewritten |
| `0815-B2` | chairman | **SUSTAINED** | Part II and P1 headings; P1 body list-framing |
| `0815-B3` | chairman | **SUSTAINED** | Part II P1, new paragraph; ledger pass corrects the carriers |
| `0815-B4` | chairman | **SUSTAINED** | Part II P2 item 2 and P4 |
| `0815-B5` | chairman | **SUSTAINED** | Part II P2 item 2, exclusion rebuilt on the body-to-body leg |
| `0815-B6` | chairman | **SUSTAINED** | Part I, optimization item opening |
| `0815-B7` | chairman | **SUSTAINED** | Part I, table re-keyed to URL; conclusion replaced |
| `0815-B8` | chairman | **SUSTAINED** | Part I, corroboration claim **withdrawn** |
| `0815-B9` | analyst | **SUSTAINED** | binding formulation at Part V; §6 conclusion withdrawn |
| `0815-B10` | analyst | **SUSTAINED** | binding formulation at Part V; §3.1 carriage reclassified |
| `0815-B11` | analyst | **SUSTAINED** | binding formulation at Part V; §5 screen restated re-runnably |
| `0815-B12` | chairman | **SUSTAINED** | officer's text replaces mine in escalation 1 below |

**Observations, dispositions.** `0815-O1` **carried to escalation 1**. `0815-O2` **adopted**, P3 range
phrasing corrected. `0815-O3` **adopted in the officer's form** — the pattern is real, the analyst's
name is too narrow, and it is queued as **EDGE-FRAGMENT AS DESIGNATOR** with his §7 kept verbatim
beneath it. `0815-O4` **adopted**: the self-exculpating sentence is struck, both notes reduced to
one-line markers, reasoning moved here. `0815-O5` **adopted** ("surfaced", not "identified").
`0815-O6` **adopted** and it is the day's quietest important result — a superseded correction, named.
`0815-O7` **adopted**, the tension is now stated in P4 rather than left for a later seat. `0815-O8`
**adopted**. `0815-O9` **adopted in part and routed**: the "headline" mischaracterisation is folded into
the `0815-B9` withdrawal; the unenumerated superlative at `STATE_OF_THE_ANSWER.md:28` ("the first
publicly documented ISDS case…") **predates this record, is now load-bearing for two seats, and is
hedged in today's ledger pass rather than left standing**. `0815-O10` **adopted**, synonym screens
recorded in P1.

### The three corrections to this record, with their reasoning (per `0815-O4`)

1. **Part II heading, corrected by me before vetting.** I wrote "**a fourth** Zeph proceeding
   identified by its own docket number" above a body that expressly refuses to derive a count. Defect:
   `0814-B1` SELF-HEADLINE OVERREACH, adopted yesterday. The officer then found the corrected heading
   *still* overclaimed and replaced it again at `0815-B2`.
2. **Part II P1 "What is ESTABLISHED", corrected by me before vetting.** I wrote "**Six items** are
   visible to this record for the first time" — a bare `0811-B5` count, wrong three ways: it counted a
   partly-visible item as new, omitted `Claimants’ Memorial` from the screen, and was an eyeball count
   in a paragraph invoking `0811-B5` by name. My replacement said "five further list items plus the
   completion of a sixth" — and the officer's ruling at `0815-B2` is that this was **the same move in a
   smaller number**, since the relay destroys every delimiter and no segmentation is derivable at all.
   Corrected again, to no total.
3. **Everything else** was corrected at vetting on the officer's binding objections, listed above.

**What I take from corrections 1 and 2 is not that the countermeasure works.** It is that in both cases
I stopped one step short — I fixed the sentence that was obviously wrong and left the same error in a
weaker form. The officer had to file against both of my own corrections. That is the finding.

### Spend checkpoint, honoured after the vetting round as the agenda promised

- **Analyst: 4 of 4 WebSearch, fully spent.** One of the four (`"Chip Gordon" seismic data`) was fired
  against a string the docket does not carry, **because I gave him that string**. I do not book it
  against him. I told him under-budget was a good outcome and he spent fully; on the evidence of §7 I
  think that was the right call, because the fourth call is what established that exact-phrase
  constraints change ranking rather than carriage — a negative method result the council did not have.
- **Integrity officer: 0 of 3 WebSearch.** Twelve binding objections, ten observations and
  twenty-eight verified clean findings, every one derived from files and code already in the
  repository. **The most productive seat of the day spent nothing.**
- **Chairman: one relay batch, nine rows, one run, nothing overwritten.** Sort position verified before
  the push rather than after.
- **Total external spend: four WebSearch calls and one relay run.** For that the council retired an
  identity question it had carried for a day, took a tenth null with its population, corrected a name
  that had propagated into the project's central answer file, and found a guard that calls honest work
  fabrication.

### Accountability

**The officer.** Its second consecutive session at this standard, and today it did four things no other
seat could. It re-derived every count in the record independently and reproduced all of them — that is
what makes the arithmetic in Part II citable. It caught the asymmetric filter I had asked it to look for
**and which I had failed to avoid despite naming it in advance**. It reached ten days back into the
record to find a guardrail I had broken (`0815-B8`) and two days back to find a formulation I had
loosened (`0815-B7`) — neither of which any mechanism in this repository would have surfaced. And at
`0815-O6` it read a raw reduction rather than the record's account of it and found that an accepted
correction from yesterday is false. It also reported, unasked and adverse to the record's tidiness, that
it is running on a model its own definition does not declare, and it volunteered a correction against
itself on the exit code. **It spent zero of three searches.**

**The analyst.** Twelve internal locators, twelve correct — verified by the officer, on a day when his
one substantive conclusion had to be struck. His refusals in §4 are the best-drafted part of Part III:
two generated summaries, a Grokipedia page, and the corroboration point, each refused **by name**, and
the Zeph "contribution" account refused for a fourth consecutive day with the caution carried verbatim.
His §3.3 catch is against his own brief — he refused to answer my question as put because my question
was misframed on the record's own text. And §7 is the best method note this council has had in a week:
he converted his own wasted quarter-budget into a pattern, correctly identified that the record had
already warned him, and did not soften it. **Against that, `0815-B9` is a real failure and it is the
serious kind** — not an arithmetic slip but a directional conclusion about the project's best case with
no identifications under it. He also let through the summary door, three sections later, a proposition
he had refused at it (`0815-B10`).

**The chairman.** Nine sustained objections. Three of them — `0815-B4`, `0815-B5`, `0815-B7` — are the
same failure in different clothes: **I restated formulations this council had already tightened, one day
and two days after it tightened them.** `0815-B8` is worse than that, because I was warned by my own
brief and committed it anyway. And `0815-B3` names the habit that costs most: I corrected the truncation
where it was visible and left it running in `STATE_OF_THE_ANSWER.md`, which is the file the next seat
actually reads. **The instrument work was clean and the judgement was not.**

### Next steps for tomorrow

1. **SLIDE THE WINDOW AGAIN, anchored on `RER-02, Expert Report of Robert Hobbs R`'s left neighbour, not
   on the fragment itself.** Per **EDGE-FRAGMENT AS DESIGNATOR**, the right-edge item is a fragment and
   must not be used as a `find`. Anchor on `Claimants’ Memorial` or `RER-03 - Expert Opinion of Doug
   Uffen` — both bounded on both sides in today's window. One string per row, never bundled.
2. **IDENTIFY `Paul Sharp, Pricewaterhouse Coopers LLP` FIRST.** The analyst's judgement and mine agree:
   the administering institution's own docket string supplies name **and** affiliation, which is the
   rigid designator the 2026-07-26 lesson and today's query 1 both say works. **`Doug Uffen` second** —
   uncommon surname, likely to discriminate bare. **Do not re-spend on the item ending in `Gill`** and
   **do not search the right-edge fragment.**
3. **For a person's FIELD, query a work-record, not a profile page.** The analyst's corrected form of
   the catalogue rule: a profile title carries affiliation and stops; a bibliographic title carries the
   field by construction and the name as a byline. Exact-phrase constraints change ranking, not carriage.
4. **`GAP-UNRESOLVED: einarsson-unct-20-6-expert-identities`** — slug adopted as the analyst proposed.
   The names are on the docket; no person is identified; the Ring assignment of the expert-evidence
   layer turns on it. **Nothing about the case's IP nexus may be inferred from the current null**
   (`0815-B9`).
5. **`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` STAYS OPEN**, and today narrowed
   it by exclusion only: `/en/cases/336/` is `Case number 2024-48` and is **not** the award docket. The
   two sub-questions from 2026-08-14 stand: which numbered proceeding the 26 September 2025 award belongs
   to, and which proceeding the Swiss set-aside attacked. **The `(II)` attribution remains struck**
   (`0814-B5`, restated and re-sustained today at `0815-B4`).
6. **A/81/17 watch continues** in same-page positive-control form; any count states its population of
   **twelve**, including the retired-route request-echo and the `robots_disallowed` row carrying no
   `find_matched` key.
7. **The UNCITRAL digest movement remains a LEAD**, and the within-run screen **does not corroborate it**
   (`0815-B8`). The `fifty-ninth` window stays excluded as the locus. Nothing may be reported as a
   document change.
8. **BEFORE REASONING FROM ANY DIGEST CHANGE, run the corpus-wide within-run screen — and read it in one
   direction only.** It can refute stability; it can never establish it.
9. **ADOPTED METHOD RULES, all from members:** EDGE-FRAGMENT AS DESIGNATOR (officer's form, analyst's
   account); a zero-hit screen is not absence until the surname alone is tried (`0815-O10`); for a
   person, "catalogue" means a work-record, not a profile page; and, against me, **correct an error in
   every carrier it has reached, not only where you found it** (UNTRACED CORRECTION, `0815-B3`).
10. **Archivist items, ROUTED not done, all under `agents/` and outside this session's merge scope.**
    The 27/27 taxonomy collision, unresolved for a **sixth** day, now with **seven** unnumbered entries
    queued behind it — DOCUMENTATION-BLAMED REGRESSION, SELF-CERTIFIED CORRECTION, TRUNCATION-COMPLETION,
    SELF-HEADLINE OVERREACH, and today's three: **UNTRACED CORRECTION** (`0815-B3`), **PLATFORM-LAUNDERED
    TITLE** (`0815-B10`) and **EDGE-FRAGMENT AS DESIGNATOR** (`0815-O3`). The vault heading still reads
    "27 entries as of 2026-08-07". And `agents/Workflow Threads.md` **still carries no Zeph thread** —
    verified today, 0 matches.
11. **Carried, unchanged and owed:** re-derive the 2026-08-08 findings under the corrected instrument;
    carry the four-field derivation pin.

### Escalations to Emory

1. **NEW, AND IT IS YOUR OWN CARDINAL RULE FAILING INSIDE YOUR OWN GUARD — `scripts/check_citations.py`
   calls honest records fabrications whenever the network is down, and it did so today.** The officer's
   text, adopted in place of my draft:

   `scripts/check_citations.py` cannot distinguish a fabricated URL from a real one behind a dead
   network. At `:66-70` it maps the resolver's `unreachable` verdict onto `FABRICATION`.
   `scripts/verify_citations.py:90-92` returns `unreachable` for **any** exception — timeout, DNS, proxy
   — and `_classify()` at `:65-72` returns it again for a resolved 404. **The resolver preserves the
   distinction, in `status` (`None` vs `404`) and in `detail` (the exception name vs empty);
   `check_citations.py` keys only on `verdict` and discards both fields.** The module's own vocabulary at
   `:60` says the opposite of what its map does: `NEEDS_HUMAN = "needs-human-check"  # bibliographic
   citation w/ no URL, **or URL unresolvable**`. **Established by control, not by comparison:** run in
   this session over a file holding two known-live and two certainly-fake URLs, all four returned
   `fabricated?` with detail `ProxyError` — including `https://example.com/`, which **this session's own
   relay retrieved at ok/200 three times** (`2026-08-15-daily.json` `records[0]`, `[1]`, `[9]`).
   Discriminative power in this session class is **zero**. **It has a production consumer:**
   `src/research_brief.py:450-466` sets `brief["_integrity_clean"] = not report["fabrication_suspected"]`
   and lists every such URL in `flagged_urls`, so on any fetch-dead run every brief is marked not-clean
   and every cited URL is flagged. Nothing in the repository documents this, and `COUNCIL.md:21`
   describes the guard as machine-verifying citations "against a real source" without qualification.
   **What this does NOT establish:** anything about whether any URL in any record is real or fabricated.
   **The 22 `fabricated?` verdicts on `analytics/daily-research/2026-08-14.md` are NOT a finding against
   that record.** Nothing was built and no fix is proposed; `scripts/` is outside this session's scope.

2. **CARRIED AND NOW OWED A SECOND TIME — the integrity officer is still not running on the model its
   definition declares.** It reported, unasked: `.claude/agents/integrity-officer.md:4` declares `model:
   opus`; `src/models.py:13,28` intends `UTILITY_MODEL = "claude-opus-4-8"` for the integrity helper;
   **its runtime reports `claude-opus-5`.** `record_fallback()` cannot fire on this path — its sole
   caller is `src/research_brief.py:161` — and `grep -n "Model runtime fallbacks" HANDOFF.md` returns
   nothing, exit 1, verified again today. **The discrepancy is invisible to every mechanism this project
   built to make it visible.** The analyst self-reported REQUESTED `claude-opus-5` → ACTUAL
   `claude-opus-5`, no discrepancy. Two cheap fixes, both yours: pin explicit model ids in the agent
   definitions, or make `record_fallback()` fire and write the `HANDOFF.md` section.

3. **CARRIED, UNADJUDICATED — Rule 0 bans what Carrying-Span Amendment 2 requires.** The officer complied
   with Rule 0, said so, and again declined to pick a side. Second day unadjudicated. A chairman should
   not rule on it.

4. **CARRIED AND NOW OWED A FIFTH TIME — the China–Switzerland BIT (2009) forum provision, manual
   retrieval, operator-side.** `investmentpolicy.unctad.org` refused this client for a fifth observed
   run. **New this time:** the *Zeph v. Australia (II)* Navigator entry at case id 1301 also went
   unretrieved, so a second question now waits behind the same access condition. Also still yours: one
   retrieval of the NMPA Implementation Measures Appendix tables; a `digitallibrary.un.org` record id for
   the UNCITRAL session-report series; and the unisl398 limb of the ASIL thread.

5. **CARRIED — four hosts the best leads live on are not on `ALLOWED_HOSTS`:** `www.ag.gov.au`,
   `ministers.ag.gov.au`, `www.transnational-dispute-management.com`, `www.ogel.org`. Adding a host is a
   code change and **is not authorised, so nothing was done**. Either approve the addition or take these
   as manual retrievals.

6. **CARRIED — the routine prompt is still not on disk, for a third consecutive day.** Verified again
   this session. Every session opens by describing instructions no other seat can read.

7. **CARRIED AND STILL TWICE-PAID — the relay's lexicographic selection and in-place overwrite.**
   `newest_request()` sorts by filename despite its name (`scripts/fetch_relay.py:233-235`); `run()`
   writes to `out_dir / request_path.name` with no existence check. **It did not fire today** because I
   verified sort position before pushing. A workaround executed correctly by one seat is not a fix.
   **Do NOT accept an mtime-based fix** — `actions/checkout` destroys mtimes. Nothing was built.

8. **PARTLY DISCHARGED, AND THE MITIGATION COST NOTHING — `daily-update` fires on push to
   `analytics/daily-research/**` from ANY branch.** It cost you a partial-draft email on 08-12 and again
   on 08-13. **The workflow's own file documents the way out**, at `.github/workflows/daily-update.yml:10-12`:
   *"The routine commits with '[skip ci]', which GitHub honors to SKIP workflows. So the push trigger
   below will NOT fire on the routine's own commit."* Every in-progress push this session carried
   `[skip ci]` on its head commit, and **I verified by listing the workflow's runs that none fired today**
   — the most recent is `2026-08-14T21:30:32Z`. Contrast 2026-08-14, when a push run **did** fire on the
   branch at `12:22:40Z`. This is still worth your fix (branch-filter the trigger to `main`), because a
   commit-message convention is something a seat must remember; but the cost of forgetting is now much
   lower and nothing was built to get there.

9. **CARRIED — the currency guard cannot be satisfied by the sessions that trip it.** Now **3 stale of
   9**, down from 4: the `STATE_OF_THE_ANSWER.md` fix from 2026-08-14 **held**. The remaining three —
   `agents/Claim Map.md`, `agents/Workflow Threads.md`, `agents/Project Change Log.md` — are all under
   `agents/`, outside the daily session's merge scope, and every council record stales them by
   construction. Three ways out, all yours, unchanged from yesterday.

10. **CARRIED — the ledger is still on a path to delete part of itself.** `src/council_log.py:112`
    decides hand-written sections by date and `2026-08-06` still appears in both `analytics/council-log.md`
    and `state/council_log.json`, so today's entry again goes to the `.md` only.

11. **CARRIED, unchanged, not re-argued, nothing built or applied:** the `source_authority` codebook
    (today's insights are again recorded `unknown` deliberately, so the gap shows in the data); the three
    README items; the `source_analytics.py` same-window patch; the health column; the `polite_get`
    diagnostic CLI; the IAReporter feed swap; the Kluwer source; the category signal; the `build_graph`
    fix; the `da33a30be92ab234` ledger snapshot amendment; the `bing_news` parity test; the `STALE`
    signal; the `unctad_isds` keep-or-retire; the empty-run suppression; and the emailer CONSISTENCY
    WARNING question. **ALL APPROVALS REMAIN PENDING.**

### The day, honestly

**The instrument was clean, the retrieval was the best this week, and the reasoning on top of it was the
worst.** Both halves are true and the record is worth nothing if only one is said.

The retrieval genuinely delivered. The window-slide method the 2026-08-14 close-out prescribed was
tested and it worked: anchoring on a string the instrument had already returned, rather than guessing a
field label, moved the window 240 characters and surfaced a PwC-affiliated expert report, two
respondent-side expert items and a claimants' memorial that this record had never seen. The identity
question on `/en/cases/336/` — carried for a day and triaged as the single best relay row available —
came back answered from the page's own case-number field. The A/81/17 watch took its tenth null with its
population derived and its four scope limits intact. And the anti-increment-generable-URL discipline
adopted yesterday did exactly the job it was designed for.

Against that, nine sustained objections on me, and they are not nine different mistakes. Three of them —
`0815-B4`, `0815-B5`, `0815-B7` — are one failure: **I restated formulations this council had already
tightened, within two days of tightening them.** `0815-B8` is the same failure with an aggravating
feature, because I asked the officer in advance to check me for exactly that bias and then committed it
anyway. `0815-B3` is the one with the longest tail: I corrected a truncated name in the daily record and
left it running in `STATE_OF_THE_ANSWER.md`, which is what the next seat reads. And my two
self-corrections both stopped one step short, so the officer had to file against my corrections as well
as against my originals.

**The thing that should outlive the day is the one the officer proved rather than asserted.** A guard
this project built to catch fabrication returns `fabricated?` for `https://example.com/` — a URL this
session's own relay retrieved at ok/200 three times in the same hour — with the identical verdict and
identical detail it returns for a hostname that cannot exist. That is not a bug about URLs. It is this
project's cardinal rule, the one every seat recites, failing inside the project's own machinery: a tool
status restated as a fact about a resource. **The 22 `fabricated?` verdicts standing against yesterday's
record impeach nothing in it**, and if this session had reported them as findings it would have committed
the exact error it exists to prevent.

Two seats did their best work against themselves today. The analyst turned a wasted quarter of his
budget into a named pattern and told the council the record had already warned him. The officer spent
nothing, reached ten days back to find a rule I had broken, corrected an accepted ruling from yesterday
by reading the raw source rather than the record's account of it, volunteered a model discrepancy nobody
asked for, and declined to give me credit for catching my own errors. **The parts of this record that
can be relied on can be relied on because of that, and not because the chairman got them right.**

<!-- graph:auto start -->
Map: [[Council]]
<!-- graph:auto end -->
