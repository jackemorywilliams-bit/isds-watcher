# Vault session — 2026-08-05 (council: the Ring 3 definition and the Apotex holdout item)

**Seat:** Obsidian archivist, sitting as continuity gate inside the live council session.
**Audited against `eac8ed9`** (`main`, clean tree at session start).
**Not done, by instruction:** no commit, no push, no PR, no branch change. No edit to
`prompts/`, `fingerprint.yaml`, `METHODOLOGY.md` or `scripts/` — those belong to other seats
this session. Everything below is either a measurement of the tree as it stands or an edit to
`agents/Claim Map.md`, which this seat owns.

The council is ruling on two things. This record exists so that both rulings are priced
before they are made, because `agents/Claim Map.md` was written for exactly this: *a claim is
not changed until every file listed against it is changed in the same change set.*

---

## Part 0 — What this session changed, in one place

| File | Change |
|---|---|
| `agents/Claim Map.md` | **Added C13** — "What Ring 3 *is*: a doctrinal dimension, or a disposition" |
| `agents/Claim Map.md` | **Added C14** — "The holdout's composition, and the Apotex item's identity" |
| `agents/Claim Map.md` | **C1 corrected: DIVERGENT → CLOSED.** The 20-vs-12 / 0.95-vs-0.92 split was closed at `7959777` on 2026-08-04, the same day the row was written calling it open |
| `agents/Claim Map.md` | **C7 corrected** — the map said "ten flowchart cards, ten `.claude/agents/*.md`, twelve `agents/*.md`". Measured: **twelve** model-bearing cards, **nine** definitions, **nine** seat notes among thirteen files |
| `agents/Claim Map.md` | **C12 corrected: open → CLOSED.** The row asked whether `scripts/check_models.py` covers the two formerly undeclared seats. It does; all nine now declare `model:` |
| `agents/Claim Map.md` | Snapshot anchor moved to `eac8ed9` for the new and corrected rows; the `c9050e6` anchor retained for C2–C6, C8–C11 |

Nothing else in the vault was touched.

---

## Part A — Ring 3: the complete blast radius, before the ruling

Two incompatible tests are in the tree at once.

- **Definition A — DIMENSION.** Ring 3 is engaged when jurisdiction/admissibility doctrines
  are *live and litigated*, whatever the outcome.
  (`think-tank/methodology/ring3-reconciliation.md:55-59`)
- **Definition B — DISPOSITION.** Ring 3 requires that the tribunal *disposed of the case* at
  the threshold, without reaching the merits.
  (`prompts/research_analyst.txt:18-20`)

Full tables are now carried permanently at `agents/Claim Map.md`, **C13**. What follows is the
ruling-relevant summary: which files become false under which choice.

### A.1 — Would become FALSE if Definition A is adopted (the B statements)

| file:line | What it currently says |
|---|---|
| `prompts/research_analyst.txt:18-20` | "the tribunal **disposes of the case** at the JURISDICTIONAL / ADMISSIBILITY stage … **without reaching the merits**" |
| `prompts/research_analyst.txt:113` | "attacked through a measure, and **disposed of at the jurisdictional gate**" |
| `prompts/council_chairman.txt:8-9` | "(3) **disposal at the jurisdictional/admissibility stage**" |
| `.claude/agents/council-chairman.md:31-32` | "(3) **disposal at the jurisdictional/admissibility gate**" |
| `prompts/council_calibration.md:20-23` | "…and **disposal at the jurisdictional/admissibility stage** — not surface keyword matches" |
| `prompts/systems_researcher.txt:18-19` | "Ring 3 **jurisdictional/admissibility disposal**" |
| `prompts/research_editor.txt:28` | "or for the **jurisdictional/admissibility gate**" (weak) |
| `prompts/classifier.txt:12-14` | "jurisdictional / admissibility doctrines **decide the case**" |
| `moc/Research Question.md:3-5` | "…and **disposed of at the jurisdictional/admissibility gate?**" |
| `METHODOLOGY.md:10` → `docs/methodology.html:96` | "a rule of law applied at the jurisdictional or admissibility level **determines that the case cannot proceed**" |
| `STATE_OF_THE_ANSWER.md:4` | "…and **disposed of at the jurisdictional or admissibility stage**" |
| `scripts/site_templates/index.html.j2:13` → `docs/index.html:45` | "**the case turns on** jurisdiction and admissibility" |
| `scripts/site_templates/base.html.j2:162` | "the threshold **on which the seed cases were decided**" |
| `working/02c-framework-rings.original.txt:7` | "**The case is decided at the threshold, not on the merits.** … each case **died on** jurisdiction and admissibility" |
| `working/one-pagers/philip-morris-v-australia.md:18`, `:22`, `:30` | "Ring 3 (**jurisdictional disposal**)"; "**is disposed of** at the jurisdictional gate" |

**Count: 15 locations, 2 of them generated.** Of these, three are *not* merely re-wordable:
`moc/Research Question.md:5`, `METHODOLOGY.md:10` and `STATE_OF_THE_ANSWER.md:4` state the
project's **research question**, not the ring. See the escalation in Part F.

### A.2 — Would become FALSE if Definition B is adopted (the A statements)

| file:line | What it currently says |
|---|---|
| `think-tank/methodology/ring3-reconciliation.md:48`, `:56`, `:59`, `:115`, `:133`, `:139` | The **decided council note** of 2026-06-29 (`8909390`): justifying a dimension by a disposition statistic is "a **category mistake**"; the test is "recurring and **live** … **not whether it was the disposition**"; "the instrument **keys on doctrinal engagement**"; and a standing rule — "**Never justify a fingerprint dimension by a disposition statistic again**" |
| `scripts/site_templates/index.html.j2:74-77` → `docs/index.html:100-105` | "the threshold **dimension** anchored by *Philip Morris*, **dispositive there** and a recurring doctrinal **risk** across the broader seed pattern" |
| `scripts/site_templates/base.html.j2:169` → `docs/index.html:392` | "a **live** jurisdiction-and-admissibility **question**" |
| `prompts/council_roundtable.txt:19-23` | "a rule … that **can** dispose of the case BEFORE the merits. Seeds: Philip Morris (disposed on Ring 3), Eli Lilly (merits), **Bridgestone (Ring 3 present, decided on merits)**" |
| `prompts/classifier.txt:26-28`, `:60-61`, `:149`, `:172-186` | Bridgestone assigned "**Rings 1 + 2 + 3**" though decided on the merits; few-shot Example 2 fires Ring 3 on a *contested, undecided* standing objection and scores HIGH |
| `fingerprint.yaml:81-102`, `:110-115`, `:162-172` | Seventeen doctrinal phrases, **zero outcome terms**; ring PRESENT at keyword subtotal ≥ 12; the Bridgestone few-shot scores HIGH on a "jurisdiction **fight**" |
| `src/classify.py:33-37`, `:155-263` | `VALID_RINGS`, `keyword_score` |
| `README.md:8-9`, `:31-33` | "where threshold questions … **may be in play**" |
| `working/one-pagers/bridgestone-v-panama.md:18`, `:22`, `:24` | "**it cuts against reading jurisdictional dismissals as the primary filter**"; "a jurisdictional dismissal is best read as **only the outermost of several available filters**" |
| `working/one-pagers/eli-lilly-v-canada.md:22`, `:26`, `:34` | "**No jurisdictional disposal occurred**"; "the **jurisdictional-exit pattern is not universal**" |
| `src/config.py:68-72` (`THEME_ONE_LINER`, every digest footer); `templates/digest.html.j2:104-106` | "…and **jurisdictional/admissibility doctrines**" |
| `METHODOLOGY.md:19` | "a rule applied at the jurisdictional, admissibility, **or merits** stage … each award presents a **unique disposition** of the same structure" |

**Count: 12 locations, 3 of them generated, and one of them is a decided council note with a
standing rule in it.**

### A.3 — Mixed: the paragraph that is currently doing the reconciling

`METHODOLOGY.md:26` → `docs/methodology.html:105`. Heading says "Potentially Dispositive
**Dimension**" (A). Defining sentence says "the ground on which a case **may be denied
disposition prior to having its merits addressed**" (B-shaped). Closing sentence says the
preliminary questions "**may** act as dispositive issues — and did in Philip Morris — **even
though all three seeds were not decided based on these issues**" (A). The seed facts here are
**correct**. This is the adopted 2026-06-29 fix, and it is *not* the memo's own recommended
language at `ring3-reconciliation.md:133`, which was cleanly A. **Under either ruling this
paragraph moves**, because it currently states both.

### A.4 — The single largest surface, and it is false under BOTH definitions

`scripts/site_templates/base.html.j2:162`:

> `'threshold on which the seed cases were decided.',`

Plural. Only **one** of the three seeds was decided at that threshold; Eli Lilly and
Bridgestone were decided on the merits, verified with sources at
`think-tank/methodology/ring3-reconciliation.md:16-22`. This is the exact sentence the
2026-06-29 correction was convened to remove from `METHODOLOGY.md` — and it survived, because
it is a string inside a JavaScript object literal inside the **base** template, which no prose
sweep reaches and which the June correction did not think to check.

`base.html.j2` is inherited by all six page templates
(`index`, `methodology`, `how_it_works`, `backtest`, `digest_index`, `digest`), so the
sentence renders on **sixteen published pages**:

| file:line |
|---|
| `docs/index.html:385` |
| `docs/methodology.html:260` |
| `docs/how-it-works.html:350` |
| `docs/backtest.html:603` |
| `docs/digests/index.html:450` |
| `docs/digests/2026-06-09.html`, `2026-06-10`, `2026-06-15`, `2026-06-16`, `2026-06-22`, `2026-06-29`, `2026-07-06`, `2026-07-13`, `2026-07-20`, `2026-07-27`, `2026-08-03` (eleven files) |

Verified: `grep -rl "the seed cases were decided" docs/ | wc -l` → **16**.

**`docs/index.html` contradicts itself roughly 280 lines apart**: the Ring III card at `:100-105`
carries the corrected Definition-A text; the Venn hover readout at `:383-385` carries the
false disposition claim. And `base.html.j2` contradicts itself **seven lines apart** — B at
`:162`, A at `:169`.

`docs/` is never the fix (map §"generation rule"). The edit is `base.html.j2:160-162`, then a
rebuild, and `scripts/check_site_sync.py` proves it.

### A.5 — What the machine can actually implement

`grep -niE "disposit|outcome|dismiss|merits|holding" src/classify.py src/config.py` returns
**nothing**. `fingerprint.yaml:81-102` is seventeen doctrinal phrases with no outcome term.
`fingerprint.yaml:111` defines ring presence as a keyword subtotal ≥ 12.

**Definition B is not implementable by the deterministic layer.** It could only ever be
applied by `prompts/classifier.txt` and by prose. Adopting A costs the fifteen edits in A.1
and moves no scoring artifact; adopting B falsifies the twelve statements in A.2 *and* leaves
the code unable to do what the prose would then promise.

---

## Part B — The Apotex item: the complete blast radius

### B.1 — What the defect actually is, measured

`scripts/holdout_set.json:17-21`:

```
"id": "apotex_v_us", "label": 1, "prov": "partial",
"text": "Apotex Holdings v. United States. Apotex asserted its FDA abbreviated new drug
applications as a covered investment. The tribunal held that Apotex does not qualify as an
investor who has made an investment in the United States; the regulatory rights merely
supported cross-border sales and export business and were not a covered investment,
disposing of the case on the definition of investor and jurisdiction."
```

The caption names the **2014** *Apotex Holdings* award (ICSID ARB(AF)/12/1, the FDA
**import-alert** dispute, cited correctly at `lit-review/kim-memo.md:72-80`). The facts and
holding recited — **ANDAs** as the asserted investment, and the not-an-investor /
not-a-covered-investment ruling — belong to the **2013** *Apotex Inc.* award. Both the facts
*and* the holding are 2013; **only the caption is 2014**.

The defect is already **disclosed** in the published methodology, `METHODOLOGY.md:52` →
`docs/methodology.html:118` — "the item captioned Apotex v. United States recites the
jurisdictional holding of the 2013 Apotex Inc. award rather than that of the 2014 Apotex
Holdings award its caption names." Those are the only two lines in the repo stating it.

### B.2 — Measured this session, and it decides the cost question

Scored against `src/classify.py` at `eac8ed9`:

| variant | score |
|---|---|
| **current** — "Apotex Holdings v. United States. …" | **8** |
| caption → "Apotex Inc. v. United States" | **8** |
| caption → "Apotex Inc. v. United States (UNCT/10/2, Award on Jurisdiction and Admissibility, 14 June 2013)" | **8** |

Full holdout, current: `loewen_v_us` 56, `mondev_v_us` 62, `pm_v_uruguay` 73,
`apotex_v_us` **8**. The item matches `ip_as_investment` and `jurisdictional_admissibility`
as **sub-floor brushes only** — both subtotals below `PRESENT_FLOOR = 12` — so the scorer's
only false negative registers **no ring at all**.

**A caption-only repair is metrics-neutral and guard-neutral.** Score stays 8, the item stays
the false negative, 1.00 / 0.75 / 0.95 / 0.86 all hold, `check_claims.py` Facts 6–12 stay
green, `tests/test_check_claims.py:53-55` stays green.

### B.3 — Files restating the holdout's composition or metrics

| file:line | What it says |
|---|---|
| `scripts/holdout_set.json` | 20 items; 4 `label: 1`; `apotex_v_us` `prov: "partial"` |
| `scripts/backtest_corpus.json:30` | `apotex_v_us` in `holdout_positive_ids` (4 positives, **16** negatives) |
| `scripts/backtest_corpus.json:52` | `miss_reasons` — "Apotex turns on a **negative jurisdictional finding**…" |
| `scripts/backtest_corpus.json:57` | `display_names` — "**Apotex Holdings v. United States**" — **a second, independent copy of the wrong caption**, and the one the site renders |
| `METHODOLOGY.md:52` → `docs/methodology.html:118` | 20 / four positives / sixteen negatives / the caption-mismatch sentence / "the Apotex item as **partial**" / 1.00 ×2 / 0.75 / 0.95 / 0.86 ×2 |
| `METHODOLOGY.md:67` → `docs/methodology.html:126` | "— **twenty items, only four positives** —" |
| `METHODOLOGY.md:21` → `docs/methodology.html:102` | "The **Apotex v. United States** case defines the outer limit of what would be considered an 'investment'" |
| `docs/backtest.html:155`, `:161`, `:175-183`, `:202-205` | 20 cases; TP 3 / FN 1 / FP 0 / TN 16; 1.00 / 0.75 / 0.95 / 0.86 |
| `docs/backtest.html:267-275`, `:459` | The published row: caption, score **8**, band **LOW**, "**false negative**", miss-reason paragraph |
| `scripts/site_templates/backtest.html.j2:44`, `:113-114`, `:120`, `:134-142`, `:161-172`, `:211-214` | The template that generates all of the above — all values are Jinja expressions, none hard-coded |
| `think-tank/methodology/ring3-reconciliation.md:94` | "the holdout's lone false negative, **Apotex Holdings v. United States**, is precisely a case **decided on a Ring-3 disposition**" — **false as written** |
| `think-tank/methodology/ring3-reconciliation.md:101-108`, `:117`, `:141` | The pasted harness run and the argument built on it |
| `analytics/fingerprint-gap-report.md:23`, `:104`, `:112`, `:212-215`, `:243` | "the one on-theme item the live holdout misses"; the E4 probe; before/after metrics table |
| `analytics/fingerprint_probes.json:30` | Probe `E4_einarsson_negative_space` — "The **Apotex / Hela Schwarz negative-space shape**" |
| `lit-review/kim-memo.md:72-80` | The **only citation** in the repo: ARB(AF)/12/1, Award (Aug. 25, 2014), import-alert dispute |
| `working/FINGERPRINT_DRIFT.md:41`, `:67` | "the **Apotex outer limit**"; "backtest F1 (illustrative 0.86) must be re-run" |
| `STATE_OF_THE_ANSWER.md:8` | "Apotex outer limit" |
| `prompts/systems_researcher.txt:32`, `.claude/agents/systems-researcher.md:21`, `agents/systems-researcher.md:28` | The seat's standing weak-point list: "the deterministic scorer's **'not-a-covered-investment' miss (Apotex)**" |
| `agents/Claim Map.md` C1, C9, C14 | The map's own rows |

### B.4 — Tests and guards, and exactly which declared facts need re-pointing

`scripts/check_claims.py` holds **13 facts against 29 declared restatements** (verified green
at `eac8ed9`). It is **fail-closed on a pattern that matches nothing** —
`check_claims.py:356-361` for authorities, `:371-378` for mirrors: *"nothing in {path} matches
the declared pattern — the claim was rewritten or removed, and the registry must be re-pointed
by a person rather than pass on an empty search."*

**No declared fact names Ring 3, `jurisdictional_admissibility`, or Apotex.** A pure Ring-3
narrative correction trips nothing. What trips is the *numbers* and the *exact substrings*.

**Nine of the twenty-nine mirrors point into `METHODOLOGY.md:52` — one physical line:**

| Fact (`check_claims.py`) | Declared pattern | Would need re-pointing if… |
|---|---|---|
| `holdout items` (`:195-201`) | `holdout of ([a-z]+) items` on `:52`; `[—-] ([a-z]+) items, only` on `:67` | the count changes, **or the em-dash on `:67` is replaced by a comma** |
| `holdout on-theme positives` (`:202-208`) | `([A-Za-z]+) of the twenty were on-theme positives`; `items, only ([a-z]+) positives` | the label set changes, or the phrase "of the twenty" is reworded |
| `holdout off-theme negatives` (`:209-214`) | `compared against ([a-z]+) other awards` | the negatives change or the clause is reworded |
| `holdout precision` (`:215-220`) | `precision of ([0-9.]+)` — **two matches, both on `:52`** | either sentence is reworded |
| `holdout recall` (`:221-226`) | `recall of ([0-9.]+)` | the item's score crosses 40 |
| `holdout accuracy` (`:227-232`) | `accuracy of ([0-9.]+)` | as above |
| `holdout F1` (`:233-238`) | `F1 of ([0-9.]+)` — **two matches, both on `:52`** | as above |

Also hard-asserted, independently of the registry:
`tests/test_check_claims.py:53-55` — `assert a["n"] == b["n"] == 20`, `n_pos == 4`,
`n_neg == 16`. And `tests/test_one_pagers.py:74-77` **forbids** creating
`seeds/Apotex_v_USA.pdf` or `working/one-pagers/apotex-v-usa.md`.

CI wiring: `.github/workflows/claims-consistency.yml:63` (`test_check_claims.py`), `:68`
(`eval_holdout.py`), `:71` (`backtest.py`), `:74` (`check_claims.py`). Its trigger paths
include `scripts/holdout_set.json` and `scripts/backtest_corpus.json`, so the repair fires it.
`.github/workflows/weekly.yml:69` runs `eval_holdout.py --fail-under-precision 0.9
--fail-under-recall 0.6`. `.github/workflows/site-sync.yml:41` catches a forgotten rebuild.

### B.5 — The fork, priced

| | **Caption-only** (fix the caption to the 2013 *Apotex Inc.* award, whose holding the text already recites) | **Substitution** (replace the text with the real 2014 *Apotex Holdings* award) |
|---|---|---|
| Scored text | unchanged (measured: 8) | changes |
| Metrics | unchanged | `recall`/`accuracy`/`f1` may move |
| `check_claims.py` Facts 6–12 | green | 10–12 at risk |
| `tests/test_check_claims.py:53-55` | green | green *unless* the count or label set moves |
| Files to change | `holdout_set.json:20`, `backtest_corpus.json:57` **and `:52`**, `METHODOLOGY.md:52` candor clause, `ring3-reconciliation.md:94`, `docs/` rebuild | all of the left column **plus** every metric literal in B.3 |
| Effect on the Ring 3 ruling | **none — preserves it** | **destroys the empirical prop under Definition A** (see Part D.1) |

---

## Part C — What was added to the claim map

**C13 — What Ring 3 *is*: a doctrinal dimension, or a disposition.** Four tables (B
statements, A statements, the mixed paragraph, and the third sense in Part E.1), the
asymmetry finding from A.5, the sixteen-page `base.html.j2` surface, and a
new-divergence warning on the Bridgestone-seeded Ring 3 weights (Part D.2).

**C14 — The holdout's composition, and the Apotex item's identity.** Twenty-one rows
covering the item, both of its captions, the metrics, the guards, the tests, the memos and
the vault notes; the measured score result; and the fork above.

**Three existing rows corrected in the same pass**, because a stale row licenses a partial fix:

- **C1 — CLOSED.** The row was written on 2026-08-04 recording a 20-vs-12 / 0.95-vs-0.92
  divergence. That divergence was closed **the same day** at `7959777`.
  Measured at `eac8ed9`: `backtest_corpus.json` now carries 4 positives, **16** negatives and
  20 `display_names`, and `docs/backtest.html` renders "20 cases", TN 16, accuracy **0.95**.
  The row is corrected in place, not deleted, so the correction stays legible — and the row's
  hazard is now **inverted**: the agreement is mechanical, so the Apotex repair is the thing
  most likely to break it.
- **C7 — counts corrected.** "Ten flowchart cards / ten definitions / twelve vault notes" →
  **twelve** model-bearing cards (nine seats plus `ai-check`, `daily-researcher`, `minutes`,
  which are stages), **nine** `.claude/agents/*.md`, **nine** seat notes among thirteen
  `agents/*.md` files. No file carried the number ten.
- **C12 — CLOSED, having been tested.** The row asked whether `scripts/check_models.py`
  (`c25ea64`) covers the two seats that declared no `model:` key. All nine now declare one;
  `python scripts/check_models.py` reports *"Checked 12 flowchart cards … ok — every card
  names a configured model, backed by a declared `model:` key."*

---

## Part D — Changes that would create a NEW divergence

**D.1 — The two decisions are coupled, and only in one direction.**
`think-tank/methodology/ring3-reconciliation.md` §3.3 rests Definition A partly on an
empirical claim about **this exact holdout item**: *"the one jurisdiction-disposed holdout
case is the miss … even within the out-of-sample test, 'disposed of on jurisdiction' does not
track 'scores high'."* That argument requires the item to be the **2013** award, which was
disposed on the definition of investor. **Substituting the 2014 *Apotex Holdings* award
removes the prop from under the Ring 3 ruling in the same change set that makes it.** The
caption-only repair preserves it. Rule the Ring 3 question first, or rule the Apotex fork in
the knowledge that one branch of it reaches back.

**D.2 — Adopting Definition B would orphan a quarter of Ring 3's own weight.** Of the ring's
100 points, **26** are seeded from Bridgestone at `fingerprint.yaml:94-100` — `shell
subsidiary` (6), `definition of investor` (5), `abusive tactics` (4), `standing of licensor vs
licensee` (4), `standing to claim denial of justice when not a party` (4), `exhaustion of
local remedies` (3). Bridgestone's jurisdiction was **upheld**. Under B, a quarter of Ring 3
would be drawn from a case that does not engage Ring 3.

**D.3 — Fixing `METHODOLOGY.md:26`'s heading without its defining sentence changes nothing.**
The heading already says "Dimension". The sentence a reader takes as the definition is "the
ground on which a case may be denied disposition prior to having its merits addressed". Both
move, or neither.

**D.4 — And `METHODOLOGY.md:26` contains a second claim that Definition A makes false.** It
says Ring 3 "**did not form part of the reasoning in Eli Lilly**". But
`working/one-pagers/eli-lilly-v-canada.md:22` records that a Ring 3 gate *was* raised —
Canada's time-bar objection — and rejected. Under **A**, a raised-and-rejected threshold
objection is exactly a live Ring 3 engagement, so the sentence writes out an engagement its
own definition would count. Under **B** the sentence is fine. **Adopting A without touching
this clause creates a new contradiction inside the paragraph being fixed.**

**D.5 — Rewriting `METHODOLOGY.md:52` to remove the caption-mismatch candor sentence will
fail the build unless nine substrings survive verbatim.** That one physical line carries the
candor clause *and* all nine declared mirrors. The guard is fail-closed on an empty match, so
the failure is loud rather than silent — but it is a failure, and the em-dash in
`[—-] ([a-z]+) items, only` (`check_claims.py:200`) is part of a pattern on `:67`.

**D.6 — Fixing `holdout_set.json` alone leaves the wrong caption on the public site.**
`scripts/backtest_corpus.json:57` is a **separate** copy of "Apotex Holdings v. United
States", and it — not `holdout_set.json` — is what `docs/backtest.html:267` renders.
`backtest_corpus.json:52` carries a third restatement in prose. Three files, one change set.

**D.7 — Whatever is decided, `scripts/site_templates/base.html.j2:162` must move anyway.** It
is false today under **both** definitions, and it is live on sixteen published pages. Deciding
Ring 3 and leaving that string is the largest partial fix available this session.

---

## Part E — What neither the chairman nor the analyst is looking at

**E.1 — There is a THIRD Ring 3 in daily use, and it has different contents from the
classifier's.** The research layer has grown a "**Ring 3 taxonomy**", now at **five
mechanisms**: abuse-of-right/critical-date, administrative-review prerequisite,
fork-in-the-road, MFN-forum-access, and first-generation-BIT ISDS scope limitation. It is
carried in living memory and in the record:

| file:line |
|---|
| `analytics/insights.jsonl` — **17 lines** using "Ring 3" in this sense, incl. "Ring 3 taxonomy now complete: (1) Philip Morris abuse-of-rights/critical-date …", "Ring 3 taxonomy update from four to five mechanisms" |
| `STATE_OF_THE_ANSWER.md:114-124` — the five-mechanism table; `:124` "must navigate two Ring 3 gates" |
| `state/research_log.json:111`, `:171`, `:201`, `:211` — four session headlines |
| `analytics/optimization-log.md:34` — a live proposal to add `mfn_dispute_resolution_scope` as "a dedicated Ring 3 classifier tag in `fingerprint.yaml`" |
| `analytics/council-log.md:50` — "completing the cross-BIT pre-ICSID typology" |

**Not one of the four newer mechanisms appears in `fingerprint.yaml:81-102`.** Only `denial of
benefits` (weight 2) is even adjacent. So the research layer's Ring 3 and the classifier's
Ring 3 have diverged in *content*, not just in definition — and the ruling before the council
is about the definition only. `analytics/optimization-log.md:34` is the seam: it proposes
importing one of the research-layer mechanisms into the fingerprint, which would be the first
time the two vocabularies were reconciled. **This is escalated in Part F.**

**E.2 — `prompts/classifier.txt` contradicts itself, and it is the component that assigns the
ring in production.** `:12-14` says the doctrines "**decide the case**" (B). `:26-28` assigns
Bridgestone "Rings 1 + 2 + 3" though it was decided on the merits (A); `:60-61` defines the
ring as a class of doctrines (A); `:149` fires Ring 3 on a *contested, undecided* standing
objection and scores it HIGH (A); `:172-186` frame Ring-3 absence as "no jurisdictional
**objection**" (A). One line against four.

**E.3 — `prompts/council_calibration.md:20-23` is the highest-leverage B statement and is easy
to miss.** It is injected into every analyst run at `prompts/research_analyst.txt:7` via
`{{CALIBRATION}}` and binds every seat. A sweep of `prompts/research_analyst.txt` alone will
not find it.

**E.4 — `prompts/council_roundtable.txt:19-23` is the only prompt carrying the corrected seed
scorecard** — "Philip Morris (disposed on Ring 3), Eli Lilly (merits), **Bridgestone (Ring 3
present, decided on merits)**". If A wins, that is the model wording the other prompts should
be brought to; it already exists and was not written this session.

**E.5 — The claim map itself drifted within one day, and that is the finding that matters most
for how this session's output should be used.** C1 was written DIVERGENT on 2026-08-04 and
closed by `7959777` on 2026-08-04. C7's three counts were never right. C12 carried an open
escalation that `c25ea64` had already resolved. All four are corrected above. The map's
maintenance contract already says *"a row whose file list is stale is worse than no row,
because it licenses a partial fix"* — this session is the first evidence that the map does
that to itself, and the mitigation is the anchor line: **re-run `git log <anchor>..HEAD -- …`
before trusting any row**, which this session did and which is why the three were caught.

**E.6 — Minor, and outside this seat: the drift-checker's own prose has drifted.**
`scripts/check_claims.py:31` says "It checks fourteen facts" and `:143` says "fourteen facts";
the registry holds **13** and the tool prints "Checked 13 self-descriptive facts". A
fourteenth was retired in place at `:246-263` and neither line was updated. Not a claim
divergence in the map's sense — a file describing itself wrongly. **`scripts/` is not this
seat's to edit; recorded for the systems designer.**

**E.7 — A CI path-filter gap, same origin.** `.github/workflows/claims-consistency.yml`
includes `scripts/check_claims.py` and `tests/test_check_claims.py` in its **pull_request**
path list (`:30-31`) but **not** in its **push** list (`:36-47`). A direct push to `main` that
edits only the guard does not re-run the guard. Recorded for the systems designer.

---

## Part F — Escalations

**NEEDS YOU: the Ring 3 ruling reaches your research question, not just the ring.**
Three of the fifteen Definition-B statements are not ring definitions — they state the
project's **research question**, and two of them are professor-facing:

- `moc/Research Question.md:3-5` — "…asserted as a covered investment, challenged through a
  state or judicial measure, and **disposed of at the jurisdictional/admissibility gate?**"
- `METHODOLOGY.md:10` (Part I) → `docs/methodology.html:96` — "a rule of law applied at the
  jurisdictional or admissibility level **determines that the case cannot proceed**"
- `STATE_OF_THE_ANSWER.md:4` — "…and **disposed of at the jurisdictional or admissibility
  stage**"

If Definition A is adopted, the honest consequence is that the project watches cases where
those doctrines are **live**, not only cases **disposed of** on them — which is a wider
question than the one Dr. Benavides has been shown. The council can rule on what Ring 3 means
inside the instrument. **Whether your stated research question widens with it is yours.**

**NEEDS YOU: the Apotex fork changes what the published metrics measure, or it does not.**
The measurement is in B.2 and the fork is priced in B.5. A caption-only repair costs nothing
and preserves the Ring 3 reasoning. A substitution changes the item the site reports as the
scorer's one miss, may move the published accuracy, and removes the empirical support the
2026-06-29 memo gave to Definition A. **Which repair you want is a call about what the holdout
is for**, and the council should not make it silently.

**NEEDS YOU: the research layer's five-mechanism "Ring 3 taxonomy" and the fingerprint's
Ring 3 are not the same thing** (Part E.1). Three ways out, and they are not equivalent:
(a) extend `fingerprint.yaml`'s Ring 3 with the four newer mechanisms — which is a scoring
change and re-opens the holdout; (b) keep them separate and give the research-layer concept
its own name so "Ring 3" means one thing; (c) declare them deliberately different levels of
description and say so once, in `METHODOLOGY.md`. `analytics/optimization-log.md:34` already
proposes (a) for one mechanism and has sat "proposed" since 2026-07-02.

**ACTION — chairman / site-experience, after the ruling:**
`scripts/site_templates/base.html.j2:160-162` is false today on sixteen published pages
(Part A.4) and must change under either definition. `docs/` is generated; the fix is the
template plus a rebuild.

**ACTION — systems designer:** `scripts/check_claims.py:31` and `:143` say "fourteen facts"
against a registry of 13 (E.6); `.github/workflows/claims-consistency.yml`'s push path list
omits the guard's own files (E.7).

---

## Could not verify

- **Whether the 2013 award's correct short caption and docket should read *Apotex Inc. v.
  United States*, UNCT/10/2, or another form.** No network access was used this session and
  the repo carries a citation only for the **2014** case (`lit-review/kim-memo.md:74`,
  ARB(AF)/12/1). The score measurements in B.2 are caption-insensitive, so the conclusion
  holds whichever form is chosen — but **the citation itself must be sourced by the analyst
  or integrity seat before it is written into `holdout_set.json`.** It is not in the repo to
  be copied.
- **Whether `docs/digests/*.html` line numbers for the `base.html.j2` string are stable.**
  Only the five non-digest pages were line-located (A.4); the eleven digest pages were
  confirmed by content match, not by line.
- **Who rebuilt the site mid-session.** The tree was clean at session start. Partway through,
  all sixteen `docs/*.html` files became modified — **one line each, the build stamp only**
  (`Site built 2026-08-05 13:45 UTC · f565407` → `14:03 UTC · eac8ed9`), which is precisely
  what `scripts/check_site_sync.py:33-45` exempts. Another seat in the live session ran
  `scripts/build_site.py`; this seat did not, and left the change in place. **Every `docs/`
  line number cited above was re-verified after that rebuild and is unchanged.**
- **Whether any archived digest annotation applied a *disposition* test to a live item.** The
  four annotations sampled (`digests/2026-06-16`, `2026-06-22`, `2026-07-20`, `2026-08-03`)
  all use engagement grammar — "raise jurisdictional/admissibility doctrines". The remaining
  seven runs were not read.

<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->
