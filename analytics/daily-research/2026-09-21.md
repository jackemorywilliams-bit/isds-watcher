# ISDS Thematic Watcher — daily council meeting, 2026-09-21

**Chair:** council chairman (this session, `claude-opus-5` per the operator's model directive of
2026-08-03). **Convened seats:** `research-analyst` and `integrity-officer` — REAL subagents spawned
with the Agent tool against their committed definitions in `.claude/agents/`. No seat is performed.
Where a member's text appears it is filed VERBATIM and is marked as such.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

Each member is spawned with the Agent tool against its committed definition, is given its own bounded
task list, and returns a finished memo as its final message. I paste that memo into this record
unaltered, inside a fenced block, under the seat's own heading. **I write no member's contribution.**
If a spawn fails or a member does not return, that seat is recorded as PROCEDURAL FAILURE for the day
and the failure is stated both in my close-out and in my final summary — it is never covered by my
writing the part myself. Members do not commit; they hand me file bodies and I commit them, so every
artefact in the day's diff has one author of record.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

I inherited no channel status. I re-probed, as the convening instruction requires, and both probes
were on neutral controls rather than on research hosts, so that nothing in this section can be misread
as a statement about a source.

| Probe | Target (neutral control) | Result |
|---|---|---|
| `curl` egress | `https://example.com` | `curl: (56) CONNECT tunnel failed, response 403` |
| `curl` egress | `https://en.wikipedia.org/wiki/Main_Page` | `curl: (56) CONNECT tunnel failed, response 403` |
| fetch service | `https://example.com` | `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by the network egress proxy."}` |
| `WebSearch` | ISDS/IP query, 9 result rows returned | **ALIVE** |

**No state change: this session has no fetch of its own, for the twentieth consecutive session.** One
detail is worth a line because it is a change in the *failure message* and not in the *fact*: the
fetch service now answers a neutral control with a typed `EGRESS_BLOCKED` error naming the proxy,
where earlier sessions recorded an undifferentiated 403 on every URL. That is a clearer diagnosis of
the same closed door, and it is a fact about this container, **not** about any resource on the
internet. The standing rule holds without amendment: **a tool-layer outcome is never a claim about a
resource.**

**The day's retrieval instruments are therefore two, and both are alive.** (a) `WebSearch`, native to
this session, which returns engine titles and snippets and **never a document body**. (b) The
**fetch-relay** — `.github/workflows/fetch-relay.yml`, adopted after the 2026-08-03 records this
session's convening instruction points at — which fires on a pushed request file under
`analytics/fetch-requests/`, runs `scripts/fetch_relay.py` on a GitHub-hosted runner that does have
network, and commits a reduction (outcome, status, `content_type`, `bytes`, `sha256`, and a
400-character excerpt) back to `analytics/fetch-results/`. Git is the one network capability this
container is proven to have. **No fetch-dependent item is stranded today**, and I do not record one as
such.

### 1.3 Duplicate guard

`git show origin/main:analytics/daily-research/2026-09-21.md` returns *"path ... does not exist in
'origin/main'"*. No record for today had been convened manually. This meeting is not a duplicate.

### 1.4 What this session inherits, and the agenda it sets

The 2026-09-20 close-out left one gap marker open with a mechanical closure condition, and that
condition is the whole of today's priority. The marker is
**`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested`** — whether *any* host on
`ALLOWED_HOSTS` serves NAFTA Chapter 11 in a content type from which `excerpt_of` can produce text is
**untested**, because the one candidate tried, `investmentpolicy.unctad.org`, refused the relay seven
times out of seven. **Its closure condition, fixed in advance and not re-negotiable today: one `ok`
row with a non-empty excerpt at any allowlisted locator.** It matters because it is the channel
underneath **escalation 2**, which has been an ask of the operator for ten days: does NAFTA Article
1110 — the expropriation article of a seed-award treaty — carry an IP-specific internal limitation at
a paragraph (7)? If the council can read its own seed treaty's expropriation chapter, that escalation
stops being an ask of Emory.

Yesterday's next step 2 forbids re-firing at `investmentpolicy.unctad.org` as a batch, and the reason
is evidential rather than dispositional: fifteen `ok` rows all dated 2026-08-09 or earlier, twelve
`refused`/403 from 2026-08-10 through 2026-09-17, and seven more refusals yesterday across two path
shapes never previously tried — **a six-week regime indifferent to path across all five shapes so far
attempted.** That prohibition is binding today. It also *defines* today's search problem, and the
definition is the useful part: the analyst must find the chapter at an allowlisted host that is **not**
that one, or report honestly that no such locator can be identified.

**The agenda, in three bounded tasks, with budgets I set here:**

1. **TASK 1 — the NAFTA channel question (primary).** Up to **six** `WebSearch` calls to identify a
   locator for NAFTA Chapter 11 / Article 1110 served in HTML or plain text at an allowlisted host
   other than `investmentpolicy.unctad.org`. Docket-page-before-document-hunt applies. Every call is
   recorded in the **pre-filed** skeleton at `analytics/search-results/2026-09-21.json`.
2. **TASK 2 — design the relay batch.** At most **nine** requested rows, pre-registered per
   **outcome class** (`ok` / `refused` / `robots_disallowed` / `no_contact`) and never per document,
   with the pairing discipline (`find:""` companion on any row that could produce a null) and the
   closing-bracket control. I fire it; the analyst does not.
3. **TASK 3 — the record's substance.** Candidate claims with marks, method and error notes, and an
   honest statement of what today does and does not establish — including the null case.

### 1.5 Spend checkpoint (placed here, taken after the vetting round at §4)

The checkpoint is a real stop, not a ritual: after the integrity officer's return I decide whether the
relay batch is fired at all. It is fired only if TASK 1 produced a locator at an allowlisted host that
was **identified by a source rather than guessed** (METHODOLOGY Part VIII) and survived vetting. If it
did not, the honest close is a null day with the gap marker re-carried open, and I will write it that
way rather than fire a batch to have something to report.

---

## 2. Research analyst — filed verbatim

Spawned with the Agent tool against `.claude/agents/research-analyst.md`. It returned a finished memo
as its final message. **Everything between the two rules below is the analyst's text, unaltered. I
wrote none of it.** Its two artefacts arrived in the working tree uncommitted, as the return-path
protocol requires; I validated both as JSON before filing (7 calls / 90 rows in the search record;
9 urls and a 13,166-character `note` in the request) and I commit them under my own hand.

---

# RESEARCH ANALYST — 2026-09-21

## 0. What this session had, and what it did with it

No fetch of its own — I inherited your probe and did not re-run it; that is a fact about this container and not about any resource. `WebSearch` alive. Seven invocations of it, six returning rows. Two files written to the working tree, **uncommitted**, for you to commit or discard:

- `/home/user/isds-watcher/analytics/search-results/2026-09-21.json` (41,676 bytes — your pre-filed skeleton, filled)
- `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json` (9 rows, note 13,166 chars)

All figures below carry their commit. **HEAD is `256cb6e`. `analytics/fetch-results/` holds 70 files at `256cb6e`** — the same count you recorded at `798c0f7`, so no result file has landed since; it was 69 at `8b05d3c`. `analytics/insights.jsonl` is 198 lines at `256cb6e`.

**Extension 40a discharge, stated expressly because I restate figures nearby: the `0919-N2` render double-count is UNCHANGED.** I re-measured it myself rather than inheriting it: `grep -c "Article 1110"` returns **1 line** in each of `METHODOLOGY.md` and `docs/methodology.html` at `256cb6e`, and the single `METHODOLOGY.md` line carries the string **twice** ("under NAFTA Article 1110" and "either Article 1105 or Article 1110"). **1 line / 2 occurrences each. The correction stands.**

---

## 1. TASK 1 — the search. Result: a candidate, and something worse than a candidate found in our own record.

### 1.1 The two mandatory search parameters cannot both be passed. This is not an excuse; it is an instrument fact, and it re-scopes a sustained objection.

My first invocation was your instruction executed literally — `allowed_domains: ["www.italaw.com","italaw.com"]` **and** `blocked_domains: ["dailyjus.com","unis.unvienna.org"]`. It returned, verbatim:

> `Error: Cannot specify both allowed_domains and blocked_domains in the same request`

Zero rows. **Your two binding instructions — "use `allowed_domains` to restrict to candidate allowlisted hosts" and "`blocked_domains` on EVERY call, MECHANICALLY" — are not jointly satisfiable at the tool layer.**

**The resolution I adopted, stated so the officer may rule against it.** On every call I passed a single-host or small `allowed_domains` list containing **neither** prohibited host, which excludes them by construction rather than by parameter; and I then **screened all 90 returned rows** for both. **`dailyjus.com`: 0 rows. `unis.unvienna.org`: 0 rows.** The residual risk is named, not waved away: `analytics/insights.jsonl:198` records that `allowed_domains` did not constrain one multi-host result set on 2026-09-20, so exclusion-by-construction is not a guarantee — which is exactly why I report the row screen instead of promising the parameter.

**And this re-scopes `0920-B6`.** I opened `analytics/search-results/2026-09-20.json` and read the filters on all seven calls. **S1, S2, S3, S4 and S6 all passed `allowed_domains` — so on five of the seven calls, passing `blocked_domains` was mechanically impossible.** Only **S5** (no filter, passed nothing) and **S7** (no `allowed_domains`, passed `["dailyjus.com"]` — one of the two) were calls where compliance was available. **The honest denominator for yesterday's "0 of 7" is 2, not 7.** I state this against the interest of tidiness and in favour of my predecessor in this seat: the sustained objection is correct as to S5 and S7 and **over-scoped as to S1–S4 and S6**, and neither the officer nor the chair could have known that, because nobody had tried to pass both.

Budget treatment, declared: I count the erroring invocation as an invocation made but **not** as one of the six searches you budgeted, because it returned zero rows. If the officer counts it, the session used 7 of 6 and I accept that finding without contest.

### 1.2 `allowed_domains` admits subdomains. One consequence bites this project directly.

Three observations this session, all in the filed artefact:

- S1 passed exactly `["unctad.org"]` → **4 of 10 rows are at `investmentpolicy.unctad.org`**, a different host.
- S3 passed exactly `["italaw.com"]` → **10 of 10 rows are at `www.italaw.com`**.
- S4 passed `pca-cpa.org` → one row at **`files.pca-cpa.org`**.

**The consequence that matters: `files.pca-cpa.org` is NOT on `ALLOWED_HOSTS` while `pca-cpa.org` is.** A search restricted to an allowlisted host can hand back a locator the relay may not fetch, and it did so today. I screened all 90 rows against `ALLOWED_HOSTS` as read at `256cb6e`: **89 in set, 1 out** (that PCA row). No batch row was proposed for it.

**This does NOT retire yesterday's leak finding, and per 40a I say so expressly: `analytics/insights.jsonl:198` is UNCHANGED.** The four out-of-set rows it records — `cdn.clinicaltrials.gov` ×2, `image-ppubs.uspto.gov`, `www.ncbi.nlm.nih.gov` — are not subdomains of any value in that call's list, so suffix-matching does not explain them. Today refines that finding; it does not absorb it.

### 1.3 The six calls, and what they bought

| call | `allowed_domains` | rows | what it bought |
|---|---|---|---|
| S1 | `unctad.org` | 10 | 6 rows at the bare host `unctad.org` (never contacted by this relay, zero rows ever) — **all six `/system/files/official-document/*.pdf`**. No NAFTA-chapter candidate in a readable type. |
| S2 | `digitallibrary.un.org` | 40 | Four internal result blocks in one invocation. Records **about** NAFTA (a 1997 study, ECLAC papers); **no treaty-text record of NAFTA at all.** Null. |
| S3 | `italaw.com` | 10 | Ten PDFs. **Result 1 is the finding at §1.4.** |
| S4 | UNCITRAL / undocs / docs.un / un.org / ICSID / PCA | 10 | Ten PDFs — nine ICSID party-exhibit PDFs, one out-of-allowlist PCA file. **Null for every UN-family host and for ICSID and PCA.** |
| S5 | `www.italaw.com` | 10 | **Docket-page-before-document-hunt, and it worked.** Two HTML-shaped landing pages. |
| S6 | `www.italaw.com` | 10 | `/investment-treaties/nafta` returned again; three `/cases/NNN` HTML pages. |

Host tally over all 90 rows: `digitallibrary.un.org` 40, `www.italaw.com` 30, `icsid.worldbank.org` 9, `unctad.org` 6, `investmentpolicy.unctad.org` 4, `files.pca-cpa.org` 1.

**Every engine summary is quarantined in its own field and used nowhere.** S4's is the dangerous one — narrative prose purporting to state what Article 1110(2) requires. **No treaty text was retrieved by this session and none is stated anywhere in this memo.**

### 1.4 What Task 1 found

**Two HTML-shaped locators at an allowlisted host, both identified by a source and neither guessed:**

- `https://www.italaw.com/investment-treaties/nafta` — engine title *"NAFTA - Investment treaties Profile & Case History"* (S5 result 6; returned again at S6 result 1)
- `https://www.italaw.com/investment-treaties` — engine title *"Investment Treaties | italaw"* (S5 result 1)

**Every caveat I can put on that, I put on it.** S5 and S6 are **one engine twice, not two sources** — same session, same seat, same single-host filter (`0920-B5`, sustained). An engine's indexed title is the **engine's**, not the document's. Nothing establishes that either URL serves `text/html`, and **nothing whatever establishes that either page carries one word of Chapter Eleven** — "Profile & Case History" is, on its face, a case list. The engine's prose asserting that the index page provides *"Full text of Investment Treaties"* is the engine's and is relied on nowhere; today's batch **tests** it rather than assuming it.

**And a third locator, which cannot close the gap but which I think is the day's sharpest finding.** S3 result 1 returned `https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf` under the engine title:

> `NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble`

I then scanned this project's own committed artefact for that string. **The identical eleven-word title is carried by three distinct URLs across two hosts:** `investmentpolicy.unctad.org/.../treaty-files/2412/download` (2026-09-20 S1 r4), `www.italaw.com/.../laws/italaw6187(6).pdf` (2026-09-20 S3 r9), and `italaw6187(14).pdf` (today). **Escalation 2 currently sends Emory to UNCTAD `2412` on the strength of exactly that title. The title does not individuate the file.** That does not make `2412` the wrong place to look; it means the title is not what makes it the right one. It is `0920-N5`'s lesson in a second currency: adjacency in a database is not identity, and **an engine title is not a file identity either.**

### 1.5 The thing I found in our own record, and it is against this council

While checking whether `/investment-treaties/<slug>` was a new path shape here, it turned out **not to be new at all.**

`https://www.italaw.com/investment-treaties/china-germany-bit-2003` has been in this repository since **2026-06-26**, at three loci at `256cb6e`: `STATE_OF_THE_ANSWER.md` (the China–Germany BIT (2003) entry, cited by text and commit per `0918-B7`), `analytics/daily-research/2026-06-26.md:41`, and `analytics/insights.jsonl:10`.

And on 2026-08-05 a seat of this council wrote (`analytics/daily-research/2026-08-05.md:205`):

> "`www.italaw.com` is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT — `STATE_OF_THE_ANSWER.md:40` cites `https://www.italaw.com/investment-treaties/china-germany-bit-2003`."

That seat then **correctly** declined to construct a `china-switzerland-bit-2009` sibling by slug analogy, because that is discovery. **It did not fire the URL the record already held. Nobody has, in the 47 days since** — `grep` over `analytics/fetch-requests/` and `analytics/fetch-results/` returns zero rows at that URL at `256cb6e`.

**Stated so it is not overread, and this is the `0920-B7` species of error I am trying not to repeat: no seat of this council has ever RETRIEVED that page.** The 2026-06-26 entry that carries the URL describes the direct treaty PDF as *"not directly readable (403)"*. What the record holds is a **locator**; the characterisation of it as a treaty-text page is a seat's, not a retrieval. I do not assert that italaw serves treaty text. I assert that we have had a testable locator of the target shape for 87 days and never sent one row at it.

**That row is now row 6 of the batch, and it is the row I would keep if I could keep only one** — because it is the only URL of the target path shape whose existence is attested by our own committed record rather than by a search engine's index, and it separates **slug** from **shape** inside one run.

---

## 2. TASK 2 — the batch. Nine rows, ready to fire.

Complete body at `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json`. All nine pass `scripts.fetch_relay.validate()` (I ran it). 9 requested + 1 prepended = 10 ≤ `MAX_URLS` 12, no truncation. Eight substantive rows at one host at 3.0s `MIN_INTERVAL_SECONDS` ≈ 24s of politeness; the runner has time.

```json
{
  "urls": [
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": ""},
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": "1110"},
    {"url": "https://www.italaw.com/investment-treaties/nafta", "find": "Chapter Eleven"},
    {"url": "https://www.italaw.com/investment-treaties", "find": ""},
    {"url": "https://www.italaw.com/investment-treaties", "find": "NAFTA"},
    {"url": "https://www.italaw.com/investment-treaties/china-germany-bit-2003", "find": ""},
    {"url": "https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf", "find": ""},
    {"url": "https://www.italaw.com/sites/default/files/laws/italaw6187(6).pdf", "find": ""},
    {"url": "https://example.com/", "find": "Example Domain"}
  ]
}
```

The `note` field is 13,166 characters and is on disk in full, ready to write verbatim. **Its substance, which I will not let be summarised away:**

- **My prior, stated before any row returns and against my own interest.** At `www.italaw.com`, across all 70 committed reductions at `256cb6e`, this relay holds **12 rows: 4 `ok`/200, every one `application/pdf` with excerpt length 0, every one under `/sites/default/files/case-documents/`; and 8 `refused`/403** covering the **bare site root** `https://www.italaw.com/` (2026-08-03), `/cases/8154` (×3), `/cases/documents/10912` (×2) and `/cases/11573` (×2, 2026-09-12). **Eight of eight non-PDF rows ever fired at this host have been refused, the site root among them. I EXPECT ROWS 1–6 TO REFUSE AND ROWS 7–8 TO ANSWER.** If that is how it falls, **this batch does not close the gap and I will say so.** The one thing that makes rows 1–6 worth firing rather than zero rows: `/investment-treaties/` is a shape never tried here, and italaw's refusals are already known to be **path-discriminating within a single run, client held constant** — `analytics/insights.jsonl:73` (2026-08-18), which I cite rather than re-deriving, so a host-wide reading is not available to any seat.
- **Per-outcome-class pre-registration, never per document.** Four groups plus the bracket; for each, what `ok`+html, `ok`+non-text, `ok`+empty-find, `refused`, `robots_disallowed` and `no_contact` each license and — more of the note than not — what they do not. `content_type` and `bytes` are recorded whatever the body is; **a refusal has no body to type, and its nulls mean that and nothing else.** The `robots_disallowed`/`refused` distinction is carried to `src/sources/base.py:339` and `analytics/insights.jsonl:167`.
- **The Group D pair is pre-registered to discriminate either way:** if `italaw6187(6)` and `(14)` differ in bytes or sha256, two distinct files carry one identical engine title; if they are identical, one byte-stream sits at two paths. **On either branch the title does not individuate, and on neither branch is any file identified as NAFTA.**
- **Pairing discipline, checkable:** rows 2 and 3 are companioned by row 1; row 5 by row 4; rows 6–8 already carry `find:""`; row 9's companion is the runner's own prepended control.
- **The closing bracket** is row 9, `find: "Example Domain"`, distinct from the prepended control: the prepended row fires with no find and proves **reachability only**; row 9 proves **the find path executed**. If row 9 is `ok` with `find_matched` false, every empty excerpt in the batch is uninterpretable and the batch is void on that axis whatever else it says.
- **`control_ok: false` voids every row, including the ones that look like answers.**
- **Not discovery, and I expect to be checked on it.** Every URL was identified by a source before it was requested, with call and result index given in the note. No URL was constructed by slug analogy, pattern or guess; `china-switzerland-bit-2009`, the URL the 2026-08-05 seat rightly refused to build, is not in this batch.
- **Concentration risk, declared:** all eight substantive rows are at one host. That is not a design preference — it is the honest consequence of six searches finding no non-PDF NAFTA Chapter Eleven candidate at any other allowlisted host. **If italaw refuses everything, this batch returns no information about any resource, and I pre-register that as a real and reasonably likely outcome.**

**I declined the one permitted UNCTAD `treaty-files/2412` diagnostic row.** Your steer plus my own arithmetic: 23 refusals against 15 `ok` all dated 2026-08-09 or earlier, a regime that has not moved in six weeks. The slot went to row 6, which tests a shape nobody has tested rather than re-testing a host everybody has.

### 2.1 A defect in the gap's closure condition, recorded before firing and NOT re-negotiated

You fixed it as **"one `ok` row with a non-empty excerpt at any allowlisted locator."** Read literally, **that condition is already satisfied by 134 `ok` rows with non-empty excerpts at `https://example.com/`**, which is on `ALLOWED_HOSTS` at `scripts/fetch_relay.py:67-68` (marked *"Neutral control only — never a research source"*). So the literal reading cannot be the intended one.

**I do not amend your condition.** I record, before firing, the reading I act on — the one the slug itself requires: *an `ok` row with a non-empty excerpt at an allowlisted locator **that serves NAFTA Chapter Eleven***. Under that reading **only rows 1–3 could close it**, and even they close it only if the page turns out to carry the chapter's text rather than a case history — **which this batch cannot determine.** Rows 4–5 name an index, row 6 a different treaty, rows 7–8 are PDF-suffixed, row 9 is a control. This is here so that whoever grades the batch grades it against a stated reading rather than picking one afterwards.

---

## 3. TASK 3 — substance

### 3.1 What today establishes, and what it does not

**Establishes:** that `WebSearch` cannot honour both of this council's mandatory filter parameters at once, and that the sustained `0920-B6` denominator is 2 and not 7; that `allowed_domains` admits subdomains, one of which today was off `ALLOWED_HOSTS`; that one engine title sits on three distinct locators across two hosts; that a testable locator of the target path shape has been in this repository since 2026-06-26 and has never been fired.

**Does not establish, and nothing today moves an inch toward:** anything about what NAFTA Article 1110 says, whether a paragraph (7) exists, or whether any IP-specific internal limitation is carried anywhere. **No treaty text has been read by any seat of this council.** `GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread` stands untouched, and **escalation 2 remains an ask of Emory whatever these nine rows do.**

**The theme itself is quiet at the three-ring intersection for an eleventh consecutive session, and I did not spend a query on it.** My whole six-call budget went to the channel question at your direction. **I therefore report no search for new investor–State matter at the intersection of IP as a covered investment, a regulatory or judicial measure, and a live jurisdictional doctrine, and I make no claim about whether one exists.** Honest form: **not searched today**, which is weaker than "not found in accessible sources" and I will not upgrade it.

### 3.2 Errors reported against myself

1. **I drafted the pre-registration with a wrong pinpoint** — `scripts/fetch_relay.py:122-123` for the content-type guard, which is at **`:124-125`**. Caught by reading the file rather than by being told, and corrected **before** the file was fired, which is the only window in which a pre-registration may be corrected at all.
2. **I drafted a day-count of "forty-eight days" for 2026-08-05 → 2026-09-21. It is 47.** Corrected before firing. I computed it instead of feeling it, which is the only reason I caught it.
3. **My first draft of the closure-condition paragraph said rows 1–6 could close the gap. Row 6 names a different treaty and could not close a NAFTA gap on any reading.** Corrected before firing.
4. **My first batch draft spent a row on `https://www.italaw.com/cases/715` as a negative control and did not contain the China–Germany row at all** — because I had not yet screened our own record for the path shape I was about to call new. I found it on a novelty check I nearly skipped. **This is `0920-N5` again in my own hands: I read a search return against one proposition and did not read the repository against the same one.** The cure is not a name; it is that the screen ran. It ran late.
5. **Unspent budget, declared:** six budgeted searches, six used, plus one invocation that errored. Nothing held back, nothing exceeded except as at §1.1.

### 3.3 Carrying-span marks

**Entry 1**
- **P** — `WebSearch` refuses `allowed_domains` and `blocked_domains` in the same request.
- **Q** — `Error: Cannot specify both allowed_domains and blocked_domains in the same request` [the tool's own return this session, recorded verbatim at `analytics/search-results/2026-09-21.json`, call `S0`, field `error_verbatim`]
- **D** — the instrument's own error return, 2026-09-21. Not a litigated matter; the artefact is the retrieval tool this session runs on. Stage: instrument. Outcome on the point cited: the invocation returned zero rows.
- **V** — checked 2026-09-21. **Rigid designators present and strong:** the parameter names `allowed_domains` and `blocked_domains`, which no field can paraphrase away. **Weakness declared: the source is my own return, filed in the same memo, on one invocation; I did not re-run it, so I cannot say the refusal is deterministic rather than conditional.** **Independent corroboration that is not mine:** `analytics/search-results/2026-09-20.json` shows `blocked_domains: null` on **all five** calls that passed `allowed_domains` (S1–S4, S6) and non-null only on the one call that passed none (S7) — a pattern consistent with the exclusivity and recorded by a different seat before the exclusivity was known. No exit taken.

**Entry 2**
- **P** — `allowed_domains` admits subdomains of the value passed, and one such row today is at a host not on `ALLOWED_HOSTS`.
- **Q** — `"allowed_domains": ["unctad.org"]` against `"host": "investmentpolicy.unctad.org"`; and `"url": "https://files.pca-cpa.org/pcadocs/..."` under a list containing `pca-cpa.org` [`analytics/search-results/2026-09-21.json`, calls S1 and S4]
- **D** — my own search record, 2026-09-21. **Outcome on the point cited: it establishes what the engine returned under those filters, and nothing about any resource.**
- **V** — checked 2026-09-21 by a programmatic host-derivation over all 90 rows. Rigid designators: `unctad.org`, `investmentpolicy.unctad.org`, `files.pca-cpa.org`. Three observations (S1, S3, S4), **not independent** — one engine, one session. **Referent clause for the finding this does not retire:** `analytics/insights.jsonl:198`'s four out-of-set rows are not subdomains of any value in that call's list, so suffix-matching does not explain them and **that finding is unchanged.** No exit taken.

**Entry 3**
- **P** — One identical eleven-word engine title is carried by three distinct URLs across two hosts.
- **Q** — `"title": "NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble"` at `.../treaty-files/2412/download` and at `.../laws/italaw6187(6).pdf` [`analytics/search-results/2026-09-20.json`, S1 result 4 and S3 result 9], and at `.../laws/italaw6187(14).pdf` [`analytics/search-results/2026-09-21.json`, S3 result 1]
- **D** — two committed search artefacts of this project, read at `256cb6e`. **Outcome: establishes what one engine returned on two days. It establishes nothing about the three files, and in particular does not establish that any of them is NAFTA.**
- **V** — checked 2026-09-21 by a scan of the 2026-09-20 artefact for titles containing `NORTH AMERICAN`, returning **4 rows**. **Referent clause for the two that are not carriers:** S3 r5 and S3 r8 carry the different string `IN THE ARBITRATION UNDER CHAPTER 11 OF THE NORTH AMERICAN FREE TRADE AGREEMENT` at case-document URLs. The exact-string carriers are 2 in that file and 1 in today's — three in total. No exit taken.

**Entry 4**
- **P** — `https://www.italaw.com/investment-treaties/china-germany-bit-2003` has been in this repository's committed record since 2026-06-26, and no relay row has ever been fired at it.
- **Q** — `[(italaw)](https://www.italaw.com/investment-treaties/china-germany-bit-2003)` [`STATE_OF_THE_ANSWER.md`, the China–Germany BIT (2003) entry, at `256cb6e` — cited by text and commit, not by line, per `0918-B7`], and the same URL in the `sources` array of [`analytics/insights.jsonl:10`, dated 2026-06-26].
- **D** — this repository at `256cb6e`, read 2026-09-21.
- **V** — checked 2026-09-21. **First limb:** the URL screens to carriers at `STATE_OF_THE_ANSWER.md`, `analytics/daily-research/2026-06-26.md:41` and `analytics/insights.jsonl:10`; a fourth locus, `analytics/daily-research/2026-08-05.md:205`, restates it. **Referent clause for the nearby nonzero that is not a carrier:** `analytics/daily-research/2026-08-05.md:364` names a *`china-switzerland`* URL of the same shape and names it **as rejected**, being a constructed URL; it is a different string and supports nothing here. **Second limb (the absence):** `grep -rc "china-germany-bit-2003" analytics/fetch-requests/ analytics/fetch-results/` returns a hit only in **today's own request file**, which I wrote. **Verdict on the absence limb is weaker for want of a rigid designator — an absence has none, and I write that down as the rule requires; it is stated as a fact about this repository only.** No exit taken.

### 3.4 Gap markers

`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested — Whether any host on ALLOWED_HOSTS serves NAFTA Chapter Eleven in a content type from which excerpt_of can produce text remains UNRESOLVED at the moment I file. Today's six searches produced HTML-shaped candidates at www.italaw.com at engine-title strength only; nothing was retrieved. The batch at analytics/fetch-requests/2026-09-21-daily.json tests it, and my pre-registered prior is that rows 1-6 refuse.`

`GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread — Unchanged and untouched by this session. No treaty body has been read by any seat.`

**I open no third slug.** The closure-condition ambiguity at §2.1 is a defect in a marker's wording, not a persistent gap, and opening a slug for it would inflate the escalation counter against something one sentence from you fixes.

### 3.5 Insights — dedup screen printed FIRST

Screened against `analytics/insights.jsonl` (198 lines at `256cb6e`) and `STATE_OF_THE_ANSWER.md`:

| term | insights.jsonl | STATE_OF_THE_ANSWER.md |
|---|---|---|
| `mutually exclusive` | 0 | 0 |
| `blocked_domains` | 1 | 1 |
| `allowed_domains` | 2 | 1 |
| `subdomain` | 0 | 0 |
| `suffix` | 1 | 0 |
| `engine title` | 0 | 0 |
| `indexed title` | 0 | 2 |
| `italaw6187` | 0 | 0 |
| `/laws/` | 0 | 0 |
| `investment-treaties` | 4 | 4 |
| `path-sensitive` | 0 | 0 |
| `path shape` | 2 | 0 |
| `non-PDF` | 0 | — |
| `one ok row` | 0 | — |

**Referent clauses for every nonzero I rely on.** `allowed_domains` / `blocked_domains` — lines 196 and 198, both 2026-09-20; **:198 is the leak finding and I am not re-logging it**, and **:196 is the UNCTAD locator finding**. `suffix` — line 121, a Tethyan `ARB/12/1` post-award block, unrelated. `path shape` — line 82 (Hela Schwarz award access) and line 197 (the UNCTAD `treaty-files` path); neither is italaw. `investment-treaties` (4/4) — **all four are UNCTAD IIA-database URL segments** for the China–Germany, China–Netherlands and China–Switzerland BITs and the Wingtech entry; **none is italaw's `/investment-treaties/` path**, which is why my Entry 4 carriers are the `sources` array of `:10` and not these.

**One thing the screen caught that I would otherwise have logged wrongly.** `analytics/insights.jsonl:73` (2026-08-18) **already holds** that italaw's refusals are path-discriminating within a single run, client held constant, with the byte-level evidence. **I do not re-log it and I cite it instead.** Re-verification is not corroboration (taxonomy 16).

**Four insights clear the screen. I log four, and no fifth.**

```json
{"date": "2026-09-21", "thread_id": "INSTRUMENT-websearch-allowed-and-blocked-domains-are-mutually-exclusive", "ring": "Ring 1 / instrument", "insight": "THE COUNCIL'S TWO MANDATORY SEARCH PARAMETERS CANNOT BOTH BE PASSED, AND THAT RE-SCOPES A SUSTAINED OBJECTION AGAINST A PRIOR SEAT. WebSearch returns, verbatim, 'Error: Cannot specify both allowed_domains and blocked_domains in the same request' and yields zero rows. The 2026-09-20 close-out next step 6 required blocked_domains ['dailyjus.com','unis.unvienna.org'] on EVERY call, and the same brief required allowed_domains to scope calls to candidate allowlisted hosts. THEY ARE NOT JOINTLY SATISFIABLE. CONSEQUENCE FOR 0920-B6, WHICH WAS SUSTAINED ON A DENOMINATOR OF SEVEN: reading the filters recorded in analytics/search-results/2026-09-20.json, calls S1, S2, S3, S4 and S6 all passed allowed_domains, so on FIVE OF THE SEVEN CALLS COMPLIANCE WAS MECHANICALLY IMPOSSIBLE. Only S5 (no filters at all) and S7 (blocked_domains ['dailyjus.com'], one of the two required hosts) were calls where compliance was available. THE HONEST DENOMINATOR IS 2, NOT 7 - the objection is correct as to S5 and S7 and over-scoped as to the rest, and nobody could have known because nobody had tried to pass both. THE SUBSTITUTE PRACTICE ADOPTED TODAY AND OFFERED FOR ADOPTION: pass a single-host or small allowed_domains list containing NEITHER prohibited host, which excludes them by construction, and then SCREEN every returned row for both and report the count. Today: 90 rows, dailyjus.com 0, unis.unvienna.org 0. THE RESIDUAL RISK IS NAMED AND NOT WAVED AWAY: analytics/insights.jsonl:198 records that allowed_domains did not constrain one multi-host result set, so exclusion-by-construction is not a guarantee, which is why the screen is reported rather than promised. WEAKNESS: one invocation, not re-run; whether the refusal is deterministic or conditional is untested.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json"], "confidence": 0.9}
{"date": "2026-09-21", "thread_id": "INSTRUMENT-websearch-allowed-domains-admits-subdomains-including-non-allowlisted-hosts", "ring": "Ring 1 / instrument", "insight": "allowed_domains MATCHES SUBDOMAINS, AND TODAY IT HANDED THIS COUNCIL A LOCATOR ITS OWN RELAY MAY NOT FETCH. Three observations, all in analytics/search-results/2026-09-21.json: call S1 passed exactly ['unctad.org'] and 4 of 10 returned rows are at investmentpolicy.unctad.org; call S3 passed exactly ['italaw.com'] and 10 of 10 rows are at www.italaw.com; call S4 passed 'pca-cpa.org' and returned https://files.pca-cpa.org/pcadocs/... THE CONSEQUENCE THAT BITES: files.pca-cpa.org is NOT on scripts/fetch_relay.py ALLOWED_HOSTS while pca-cpa.org IS, so a search a seat believes it has restricted to an allowlisted host can return a locator validate() will reject. A screen of all 90 rows against ALLOWED_HOSTS at commit 256cb6e returns 89 in set and 1 out, and no batch row was proposed for the one. SECOND CONSEQUENCE, FOR HOW WE READ OUR OWN SCOPING: a single-host allowed_domains call has NOT searched only that host, so 'this host carries nothing on X' is not available from such a call - which is the same bar analytics/insights.jsonl:198 set by a different route. THIS DOES NOT RETIRE THAT FINDING AND IT IS UNCHANGED: the four out-of-set rows it records (cdn.clinicaltrials.gov x2, image-ppubs.uspto.gov, www.ncbi.nlm.nih.gov) are not subdomains of any value in that call's list, so suffix-matching does not explain them. Today refines it; it does not absorb it.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py"], "confidence": 0.85}
{"date": "2026-09-21", "thread_id": "METHOD-an-engine-title-does-not-individuate-a-file", "ring": "Ring 1 / instrument; bears on escalation 2", "insight": "ONE ENGINE TITLE, THREE DISTINCT LOCATORS, TWO HOSTS - AND ESCALATION 2 IS CURRENTLY BANKED ON THAT TITLE. The exact eleven-word string 'NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble' is carried by https://investmentpolicy.unctad.org/international-investment-agreements/treaty-files/2412/download (analytics/search-results/2026-09-20.json, S1 result 4), by https://www.italaw.com/sites/default/files/laws/italaw6187(6).pdf (same file, S3 result 9), and by https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf (analytics/search-results/2026-09-21.json, S3 result 1). The 2026-09-20 close-out next step 3 sends the operator to 2412 on the strength of that title, on 0920-N5's reasoning that it is 'the only returned UNCTAD row whose engine title names the agreement in full'. THAT REMAINS TRUE OF THE UNCTAD ROWS AND IS NO LONGER TRUE OF THE CORPUS: the title is shared, so it cannot be what makes 2412 the right place to look. THIS IS 0920-N5'S LESSON IN A SECOND CURRENCY - adjacency in a database is not identity, and an engine title is not a file identity either. WHAT THIS IS NOT: it does not make 2412 the wrong locator, it does not identify any of the three files, and no body was retrieved by any seat. The 2026-09-21 batch tests the two italaw members of the set against each other on content_type, bytes and sha256; on either branch - differing or identical - the title fails to individuate. SCREEN: a scan of the 2026-09-20 artefact for titles containing 'NORTH AMERICAN' returns 4 rows; the two that are not carriers hold the different string 'IN THE ARBITRATION UNDER CHAPTER 11 OF THE NORTH AMERICAN FREE TRADE AGREEMENT' at case-document URLs.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json"], "confidence": 0.9}
{"date": "2026-09-21", "thread_id": "INSTRUMENT-italaw-investment-treaties-path-shape-held-in-our-own-record-since-2026-06-26-and-never-fired", "ring": "Ring 1 / instrument; Ring 3 treaty access", "insight": "THIS COUNCIL HAS HELD A TESTABLE LOCATOR OF THE TARGET PATH SHAPE FOR EIGHTY-SEVEN DAYS AND HAS NEVER SENT ONE ROW AT IT. https://www.italaw.com/investment-treaties/china-germany-bit-2003 sits in this repository's committed record since 2026-06-26 at three loci at commit 256cb6e - the China-Germany BIT (2003) entry in STATE_OF_THE_ANSWER.md, analytics/daily-research/2026-06-26.md:41, and the sources array of analytics/insights.jsonl:10 - and a grep over analytics/fetch-requests/ and analytics/fetch-results/ returns ZERO rows at it. On 2026-08-05 a seat wrote 'www.italaw.com is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT' (analytics/daily-research/2026-08-05.md:205) and then correctly declined to CONSTRUCT a china-switzerland sibling by slug analogy, because that is discovery - AND DID NOT FIRE THE URL THE RECORD ALREADY HELD. Nobody has in the 47 days since. STATED SO IT IS NOT OVERREAD, BECAUSE THIS IS THE 0920-B7 SPECIES OF ERROR: NO SEAT HAS EVER RETRIEVED THAT PAGE. The 2026-06-26 entry describes the direct treaty PDF as 'not directly readable (403)'. What the record holds is a LOCATOR; calling it a treaty-text page is a seat's characterisation, not a retrieval, and nothing here asserts what italaw serves. SEPARATE AND WEAKER LIMB, AT ENGINE-TITLE STRENGTH ONLY: six bounded searches today returned two HTML-shaped locators of the same shape at the same allowlisted host - https://www.italaw.com/investment-treaties/nafta (S5 r6, returned again S6 r1) and https://www.italaw.com/investment-treaties (S5 r1). S5 and S6 are ONE ENGINE TWICE, NOT TWO SOURCES (0920-B5). An engine's indexed title is the engine's. 'Profile & Case History' is on its face a case list, and NOTHING ESTABLISHES THAT EITHER PAGE CARRIES ONE WORD OF CHAPTER ELEVEN. MY PRIOR, PRE-REGISTERED BEFORE FIRING: these refuse - 8 of 8 non-PDF rows ever fired at www.italaw.com have been refused, the bare site root among them, across 12 rows at 70 committed reductions at 256cb6e. THE CITABLE PRIOR FINDING I AM NOT RE-LOGGING: italaw's path discrimination within a single run, client held constant, is already at analytics/insights.jsonl:73 (2026-08-18).", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/STATE_OF_THE_ANSWER.md", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/insights.jsonl", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/daily-research/2026-08-05.md", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json"], "confidence": 0.85}
```

### 3.6 `candidate_claims`

```json
{"candidate_claims": [
  {"claim_text": "The WebSearch tool available to this session rejects any request that specifies both allowed_domains and blocked_domains, returning an error and zero results.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "Error: Cannot specify both allowed_domains and blocked_domains in the same request",
   "supporting_locator": "analytics/search-results/2026-09-21.json, call S0, field error_verbatim; one invocation, 2026-09-21, not re-run"},

  {"claim_text": "On the 2026-09-20 search record, all five calls that passed allowed_domains carry blocked_domains null, and the only call carrying a non-null blocked_domains passed no allowed_domains.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"allowed_domains\": [\"investmentpolicy.unctad.org\"], \"blocked_domains\": null",
   "supporting_locator": "analytics/search-results/2026-09-20.json, calls S1-S7, read at 256cb6e"},

  {"claim_text": "A WebSearch call passing allowed_domains exactly ['unctad.org'] returned four of ten result rows at the host investmentpolicy.unctad.org.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"allowed_domains\": [\"unctad.org\"]",
   "supporting_locator": "analytics/search-results/2026-09-21.json, call S1; host derivation over all 90 rows run programmatically 2026-09-21"},

  {"claim_text": "The host files.pca-cpa.org is not a member of ALLOWED_HOSTS in scripts/fetch_relay.py, while pca-cpa.org and www.pca-cpa.org are.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"pca-cpa.org\",\n    \"www.pca-cpa.org\",",
   "supporting_locator": "scripts/fetch_relay.py:64-65, read in the working tree at 256cb6e"},

  {"claim_text": "The string 'NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble' appears as the engine-supplied title of three distinct URLs across two hosts in this project's two committed search artefacts.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"title\": \"NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble\"",
   "supporting_locator": "analytics/search-results/2026-09-20.json S1 result 4 and S3 result 9; analytics/search-results/2026-09-21.json S3 result 1. THIS IS A CLAIM ABOUT ENGINE TITLES AND NOT ABOUT THE THREE FILES, NONE OF WHICH HAS BEEN RETRIEVED BY ANY SEAT."},

  {"claim_text": "This repository has carried the URL https://www.italaw.com/investment-treaties/china-germany-bit-2003 since 2026-06-26, and no relay request or result file records any row fired at it.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/insights.jsonl",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"https://www.italaw.com/investment-treaties/china-germany-bit-2003\"",
   "supporting_locator": "analytics/insights.jsonl:10 (2026-06-26); also the China-Germany BIT (2003) entry of STATE_OF_THE_ANSWER.md and analytics/daily-research/2026-06-26.md:41, all at 256cb6e. Absence limb: grep over analytics/fetch-requests/ and analytics/fetch-results/ at 256cb6e returns no prior row."},

  {"claim_text": "Across all 70 committed reductions in analytics/fetch-results/ at commit 256cb6e, www.italaw.com has returned four outcome-'ok' rows, every one content_type application/pdf with an excerpt of length zero, and eight outcome-'refused' rows at status 403, every one at a non-PDF path including the bare site root.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/fetch-results/2026-09-12-daily.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"outcome\": \"refused\", \"status\": 403",
   "supporting_locator": "Enumerated by a json.load walk over all 70 committed result files on 2026-09-21 at 256cb6e; rows at 2026-08-03-control.json, 2026-08-17/18/19 and 2026-09-12-daily.json. Independently re-derives the figure recorded as 0920-N3 at a different commit."},

  {"claim_text": "Six bounded searches restricted to allowlisted hosts on 2026-09-21 returned no locator for NAFTA Chapter Eleven in any non-PDF shape at any of unctad.org, digitallibrary.un.org, uncitral.un.org, undocs.org, docs.un.org, www.un.org, icsid.worldbank.org or pca-cpa.org.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "\"result_row_count\": 90",
   "supporting_locator": "analytics/search-results/2026-09-21.json, calls S1, S2 and S4. THIS IS A STATEMENT ABOUT WHAT ONE ENGINE RETURNED TO SIX QUERIES IN ONE SESSION. It is 'not found in accessible sources' and never 'does not exist', and per the same file's own suffix-matching record a domain-scoped call has not searched only the named hosts."},

  {"claim_text": "The gap-closure condition as fixed on 2026-09-20 - one ok row with a non-empty excerpt at any allowlisted locator - is, read literally, already satisfied by 134 ok rows with non-empty excerpts at https://example.com/, which is a member of ALLOWED_HOSTS.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/scripts/fetch_relay.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "# Neutral control only — never a research source.\n    \"example.com\",",
   "supporting_locator": "scripts/fetch_relay.py:67-68 at 256cb6e; the 134 figure by a json.load walk over all 70 committed result files, 2026-09-21. Offered as a defect in the condition's wording, not as a closure."}
]}
```

### 3.7 Method note (self-training mandate)

**The formulation that worked was the one that asked the site for its own map, not for the document.** S5's query — `italaw.com browse investment treaties and laws index page` — named **navigation vocabulary** ("browse", "index page") and returned, at results 1 and 6, the only two HTML-shaped candidates of the entire session. The four calls that named the doctrine or the treaty (S1, S2, S3, S4) returned **70 rows and not one non-PDF candidate between them.** That extends 2026-09-20's lesson ("when the target is a database record, query the database's vocabulary") one step further and in a more useful direction: **when you need a page rather than a file, query the site's navigation, not its subject matter.** Docket-page-before-document-hunt is not only a rule about what to request — **it is a rule about how to phrase the query.**

Second note, and it is the more transferable one. **The single most valuable locator in today's batch came from no search at all.** It came from a `grep` I ran to check whether a path shape was new, and it had been sitting in our own record for 87 days. **Before spending a search budget on whether a host serves a shape, screen the repository for that shape.** Yesterday's method note said to check our own fetch-results before scoping a search to a host; today's is the same instruction pointed one directory further out — **check the record, not just the reductions.**

---

## 4. For your part

1. **Fire the batch.** `/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json` is final and validated. **Do not edit the note.** My prior is on the record: rows 1–6 refuse, rows 7–8 answer, and if that is how it falls **the gap does not close and I will say so in those words.**
2. **Commit the search artefact.** `/home/user/isds-watcher/analytics/search-results/2026-09-21.json` — your skeleton, filled per call as the calls returned, no block edited after its call. **A declared departure you should rule on: I did not paste its 90 result rows into this memo.** The header, the three screens, the parameter-conflict record and every call's purpose, query, filters, independence statement and quarantined summary are above or in the file; the file on disk is the complete, unedited body and is committable as-is. I judged 41KB of JSON inside the daily record worse for the record than a pointer to the file the skeleton was created to be. **If you want it inline, say so and I am wrong, not inconvenienced.**
3. **`0920-B6` needs your ruling, not mine.** I have given you the evidence that its denominator is 2 and not 7. **I do not propose that it be withdrawn** — S5 and S7 were breaches and the practice it protects is sound. I propose the finding be **re-scoped on the record** and that the carried instruction at next step 6 be **rewritten** into a form a seat can actually execute: *pass an `allowed_domains` list containing neither prohibited host, and screen and report every returned row.*
4. **Escalation 2 should be amended before it goes to Emory again, and this is the one concrete thing today buys him.** It currently points at UNCTAD `treaty-files/2412` on the strength of an engine title now known to sit on three locators across two hosts. **Add the italaw siblings and say what each is worth:** `https://www.italaw.com/sites/default/files/laws/italaw6187(14).pdf` and `italaw6187(6).pdf` carry the identical title at a host that has answered this relay 200 four times out of four on PDF paths, against UNCTAD's 23 refusals since 10 August. **He has a browser and a PDF reader; we have neither.** The question is unchanged and still costs him two minutes: **does Article 1110 contain an IP-specific internal limitation at a paragraph (7)?**
5. **One sentence from you fixes the closure condition** (§2.1). I did not re-negotiate it and I will not.
6. **Prohibitions:** all carried, none touched. Zero budget spent on the EU Ring-2 object, the Ecuador slug, the Grosse Ruse-Khan slug (**no twelfth row**), A/81/17, the monitored-author check, the Vadi citation, the ARB/24/16 docket, `dailyjus.com` and `unis.unvienna.org` (**0 rows at either across all 90**). The struck Achmea ground is not restated; the Spentech passes stay quarantined; no thread is referred to by array index; the 2026-08-25 asymmetric licence is carried unreleased and nothing today bears on it.

---

*End of the research analyst's verbatim return.*

---

## 3. Integrity officer — filed verbatim

Spawned with the Agent tool against `.claude/agents/integrity-officer.md`. **Everything from the rule
below to the end of this Part is the officer's text, unaltered. I wrote none of it, and I have edited
nothing — including the parts that are objections against me.** It files five blocking objections
(`0921-B1`–`B5`), ten non-blocking (`0921-N1`–`N10`), one verification (`0921-V1`) and one ruling
(`0921-R1`), and it recommends the batch be fired. **Procedural caveat, disclosed by the seat itself
and carried to §7: it reports being served by `claude-opus-5` where its committed definition names
`claude-opus-4-8`. That is a model discrepancy and it is recorded, not smoothed over.**

---

# VETTING NOTE — INTEGRITY OFFICER, 2026-09-21

**Seat:** security / integrity officer. **Model actually served:** `claude-opus-5` (my committed definition at `/home/user/isds-watcher/.claude/agents/integrity-officer.md` says `claude-opus-4-8`; the gap is the one the vault records at `/home/user/isds-watcher/agents/integrity-officer.md` and I disclose it again rather than let it go unsaid).

**Bottom line, up front.** This is the strongest analyst return I have vetted in this seat. Its central new claim is correct and I reproduced it myself. Every arithmetic figure in it — and I re-derived all of them, inheriting none — is exact. Its two limbs at §1.5 both hold, including under a screen the analyst did not run. **No row of the batch should be stopped, and I recommend you fire it.** The defects I found are five blocking ones, all of them in the *record* rather than in the *batch*, and all curable by rewriting sentences rather than by withdrawing findings. The substance of the day survives every one of them.

Two things before the objections, because they bear on what you do next.

---

## 0. Two facts about the repository that you and the analyst both need

### 0.1 The branch moved under me. What I vetted is byte-identical to what is now on the tip.

You convened me against HEAD `bdaebfa`. HEAD is now `2b54dfc`. `bdaebfa` and `56fdd8f` are **siblings**, both children of `256cb6e`; the day's work was re-committed as `56fdd8f` (memo + search record) plus `2b54dfc` (the batch). `bdaebfa` is no longer reachable from the branch tip.

I checked whether this invalidates my work. It does not: `git diff --stat bdaebfa 2b54dfc` is **empty**, and the three artefact blobs are identical object-for-object:

| artefact | `bdaebfa` vs `2b54dfc` |
|---|---|
| `analytics/daily-research/2026-09-21.md` | SAME |
| `analytics/search-results/2026-09-21.json` | SAME |
| `analytics/fetch-requests/2026-09-21-daily.json` | SAME |

Everything below stands at the live tip. Two consequences for you:

- **Your premise that the batch has not been fired is correct, and I verified it properly.** `origin/council/daily-2026-09-21` is at `56fdd8f`, which contains **only** `analytics/daily-research/2026-09-21.md` and `analytics/search-results/2026-09-21.json`. `git cat-file -e 56fdd8f:analytics/fetch-requests/2026-09-21-daily.json` returns *"exists on disk, but not in '56fdd8f'"*. The request file exists only in `2b54dfc`, which is unpushed. The relay trigger is `on: push / paths: analytics/fetch-requests/**` (`.github/workflows/fetch-relay.yml:15-18`). **No row has gone out. The checkpoint is real.**
- Every commit the analyst cites — `256cb6e`, `798c0f7`, `8b05d3c` — is an ancestor of the tip and resolves. Only `bdaebfa`, which is *your* framing and not the analyst's, is orphaned. That is `0921-N10` below.

### 0.2 One of the three facts you re-measured is an artefact of this clone, and I am telling you because you told me to

You wrote: *"the entire 2026-09-14 digest entered `main` in a single commit `fe3e9d6` whose message is `chore(currency): re-anchor tracked notes to HEAD [skip ci]`."*

The message, the author (`github-actions[bot]`), the date (2026-09-15 12:13:37 UTC) and the presence of the whole 2026-09-14 digest in that commit are all exactly as you say. I confirmed each.

**But the inference does not follow, because this is a shallow clone and `fe3e9d6` is its boundary.** `.git/shallow` exists and lists four commits, `fe3e9d63d0fa5e2bccf1ffa12b75363b06ab8160` among them. `git rev-list --parents -n 1 fe3e9d6` returns the commit with **no parent**. Git therefore has nothing to diff `fe3e9d6` against, so **every file in the repository appears as an addition in it** — 728 lines of `--stat`, the whole tree.

The demonstration that settles it: `fe3e9d6` also "adds" `analytics/daily-research/2026-06-26.md`, `analytics/daily-research/2026-07-31.md` and `analytics/daily-research/2026-08-03.md`. Those did not enter `main` on 2026-09-15. `git log --oneline -- analytics/daily-research/2026-06-26.md` returns exactly one commit, `fe3e9d6`, for the same reason.

So: **"the 2026-09-14 digest entered main in a single commit" is not available from this clone.** It is equally consistent with a truncated history — which the commit message, *re-anchor tracked notes to HEAD*, announces on its face. If the escalation you have in mind turns on that observation, it turns on a shallow-clone artefact and needs re-measuring against unshallowed history (`git fetch --unshallow`) before it is carried further. This is my own standing rule applied to you: a grep establishes absence from the repository, never from the project — and here, absence of history from a clone is not absence of history.

**On `pipeline-guards / suite` run #226 I say nothing either way.** I did not establish a positive control on any GitHub instrument this session, and my first standing rule forbids me to speak to a tool-layer outcome I have not controlled. I neither confirm nor contradict it. Nothing in today's memo depends on it.

---

## 1. THE POSITIVE CONTROL YOU ASKED FOR — §1.1 reproduced, and strengthened

I have `WebSearch`. I ran it four times.

**Invocation 1** — `query: "NAFTA Chapter Eleven Article 1110 expropriation text"`, `allowed_domains: ["www.italaw.com","italaw.com"]`, `blocked_domains: ["dailyjus.com","unis.unvienna.org"]`. Returned, verbatim:

> `Error: Cannot specify both allowed_domains and blocked_domains in the same request`

**Invocation 2 (positive control, instrument alive)** — `query: "italaw investment treaties index page"`, `allowed_domains: ["www.italaw.com"]` only. Returned **9 rows**, including `https://www.italaw.com/investment-treaties` under the title `Investment Treaties | italaw` and `https://www.italaw.com/investment-treaties/nafta` under the title `NAFTA - Investment treaties Profile & Case History`.

**Invocation 3 (positive control, other parameter alive)** — `query: "ICSID annulment committee decision 2026"`, `blocked_domains: ["dailyjus.com","unis.unvienna.org"]` only. Returned **10 rows**.

**Invocation 4 (determinism test — the gap the analyst named)** — a *different* query and a *different* domain pair: `query: "bilateral investment treaty intellectual property expropriation"`, `allowed_domains: ["icsid.worldbank.org"]`, `blocked_domains: ["example.com"]`. Returned, verbatim, the identical string:

> `Error: Cannot specify both allowed_domains and blocked_domains in the same request`

**Ruling. `0921-V1` — the claim at §1.1 is VERIFIED, and verified more strongly than the analyst could state it.** The refusal fires on two different queries with two disjoint domain pairs, while either parameter alone returns rows. It is a schema-level mutual exclusion, not a condition of the domains passed. **The analyst's declared weakness in Entry 1's `V` mark — *"one invocation, not re-run; whether the refusal is deterministic or conditional is untested"* — is now discharged, by a second seat, and should be struck and replaced with this control rather than carried forward.** The analyst named the gap instead of hiding it, and that is why it took me one call to close.

Collateral: invocation 2 independently corroborates, from a different seat and a different invocation, that the engine serves `https://www.italaw.com/investment-treaties/nafta` under the title the analyst reports. That is still an engine title and still not a document, and I assert nothing about what those pages contain.

---

## 2. THE `0920-B6` RE-SCOPING — I read all seven calls. The analyst's reading of the facts is exactly right. Its conclusion is not.

I extracted `analytics/search-results/2026-09-20.json` at `256cb6e` and read `allowed_domains` and `blocked_domains` off every call:

| call | `allowed_domains` | `blocked_domains` | rows |
|---|---|---|---|
| S1 | `["investmentpolicy.unctad.org"]` | `null` | 10 |
| S2 | `["investmentpolicy.unctad.org"]` | `null` | 10 |
| S3 | `["italaw.com","www.italaw.com"]` | `null` | 10 |
| S4 | `["investmentpolicy.unctad.org"]` | `null` | 10 |
| S5 | `null` | `null` | 9 |
| S6 | six-host list | `null` | 9 |
| S7 | `null` | `["dailyjus.com"]` | 9 |

**Every factual particular the analyst states is correct.** Five calls (S1–S4, S6) passed `allowed_domains`; S5 passed neither; S7 passed `blocked_domains` short by `unis.unvienna.org`. 67 rows total. I confirm it line by line.

**Now the adversarial half you asked for, and it matters.** This is a seat re-scoping, in its own favour, an objection my seat filed and you sustained against its predecessor. The facts survive. The conclusion does not, for one reason the analyst's own memo refutes three pages earlier.

Next step 8 of 2026-09-16 is a **prohibition on two hosts**. `blocked_domains` is one *means* of honouring it, not the prohibition itself. On 2026-09-20 compliance with the prohibition was available on all seven calls — and **the analyst proves it, today, by doing it**: pass an `allowed_domains` list containing neither host, then screen every returned row and report the count. That is §1.1's own substitute practice. It works, it worked today (90 rows, `dailyjus.com` 0, `unis.unvienna.org` 0 — I re-derived both), and it was available on 2026-09-20.

So the honest statement is not that compliance was *mechanically impossible* on five of seven calls. It is that **the parameter named in the instruction was unavailable once `allowed_domains` was chosen, and the seat neither noticed nor reported that, and did not reach for the substitute.** "The honest denominator is 2, not 7" converts an excuse into an acquittal. The count of calls that did not carry the prohibition remains 0 of 7; what changes is culpability on five of them, which is your ruling to make and not the analyst's to bank.

Filed as `0921-B5` below. To be clear about the direction: **the re-scoping is a creditable act and the evidence for it is sound.** The analyst expressly declines to propose withdrawal and hands you the ruling (§4 item 3). My objection is to one sentence in a permanent ledger entry, not to the exercise.

---

## 3. EVERY FIGURE, RE-DERIVED. NONE INHERITED.

I re-derived all of them from the repository at the commits named, by `git ls-tree`, `git show` and a `json.load` walk over all 70 reductions. I inherited nothing from the memo.

| figure as stated | my re-derivation | verdict |
|---|---|---|
| `analytics/fetch-results/` holds **70** files at `256cb6e` | 70 | **EXACT** |
| 70 also at `798c0f7`; **69** at `8b05d3c` | 70; 69 | **EXACT** |
| `analytics/insights.jsonl` **198** lines at `256cb6e` | 198 | **EXACT** |
| italaw: **12** rows total | 12 | **EXACT** |
| — **4** `ok`/200, every one `application/pdf`, excerpt length **0** | 4, all `application/pdf`, all len 0 | **EXACT** |
| — **8** `refused`/403 incl. the **bare site root** | 8; root `https://www.italaw.com/` (2026-08-03-control.json), `/cases/8154` ×3, `/cases/documents/10912` ×2, `/cases/11573` ×2 | **EXACT, including the ×3/×2/×2 breakdown** |
| **134** `ok` rows with non-empty excerpts at `https://example.com/` | 134 example.com rows; all 134 `ok`; all 134 non-empty; all 134 at exactly that URL | **EXACT** |
| **89 in set, 1 out** against `ALLOWED_HOSTS` | 89 / 1; the one is S4's `https://files.pca-cpa.org/pcadocs/...RA-49 - NAFTA FTC, Notes of Interpretation (July 31, 2001).pdf` | **EXACT** |
| 7 calls / **90** rows | 7 / 90 | **EXACT** |
| host tally 40/30/9/6/4/1 | `digitallibrary.un.org` 40, `www.italaw.com` 30, `icsid.worldbank.org` 9, `unctad.org` 6, `investmentpolicy.unctad.org` 4, `files.pca-cpa.org` 1 | **EXACT** |
| `dailyjus.com` 0 rows, `unis.unvienna.org` 0 rows | 0, 0 | **EXACT** |
| `"NORTH AMERICAN"` title scan | see below | **EXACT — and your restatement of it is not** |
| **47** days 2026-08-05 → 2026-09-21 | 47 | **EXACT** |
| **87** days 2026-06-26 → 2026-09-21 | 87 | **EXACT** |
| S1: 6 rows at bare `unctad.org`, **all six** `/system/files/official-document/*.pdf` | 6; all six | **EXACT** |
| S3: ten PDFs, 10/10 at `www.italaw.com` | 10/10 | **EXACT** |
| S4: ten PDFs, **nine** ICSID, one PCA out-of-allowlist | 9 ICSID + 1 `files.pca-cpa.org`, all `.pdf` | **EXACT** |
| all nine batch rows pass `validate()` | all nine return `None`; negative controls `files.pca-cpa.org` and `dailyjus.com` both return `'host_not_allowed'` | **EXACT, with discrimination proved** |
| `MAX_URLS` 12; `MIN_INTERVAL_SECONDS` 3.0 | `scripts/fetch_relay.py:76`; `src/sources/base.py:28` | **EXACT** |
| self-reported error 1: guard is at `:124-125`, not `:122-123` | the content-type guard is at `scripts/fetch_relay.py:124-125` | **the correction is correct** |
| self-reported error 2: 47, not 48 | 47 | **the correction is correct** |

**On the `"NORTH AMERICAN"` scan, I must correct your brief rather than the memo.** You asked me to check *"the 'NORTH AMERICAN' title scan returning 4 rows of which 3 are exact-string carriers."* That is not what the memo says, and the memo is right. The memo (Entry 3 `V`) says the scan **of the 2026-09-20 artefact** returns 4 rows, and that the exact-string carriers are **2 in that file and 1 in today's — three in total**. My scan:

- 2026-09-20: 4 rows with `NORTH AMERICAN` in the title. **2 exact** (S1 r4 `…/treaty-files/2412/download`; S3 r9 `…/laws/italaw6187(6).pdf`). **2 not exact** (S3 r5 `italaw9114.pdf`, S3 r8 `italaw9111_0.pdf`), both carrying `IN THE ARBITRATION UNDER CHAPTER 11 OF THE NORTH AMERICAN FREE TRADE AGREEMENT`.
- 2026-09-21: 1 row, exact (S3 r1 `…/laws/italaw6187(14).pdf`).
- **Exact carriers: 3 in total.** The analyst's referent clause for the two non-carriers is correct and names them by the right indices.

Your restatement conflates "4 rows in one file" with "3 carriers across two files". The memo does not.

**Every result index the memo cites resolves exactly**: 2026-09-20 S1 r4, S3 r5, S3 r8, S3 r9; 2026-09-21 S3 r1, S5 r1, S5 r6, S6 r1. Eight for eight.

---

## 4. §1.5 — BOTH LIMBS HOLD, and the absence limb is stronger than the memo claims for it

**First limb.** I screened the whole tree at `256cb6e` with `git grep` and **no path argument** (the entry-29 countermeasure). `china-germany-bit-2003` occurs in exactly four files: `STATE_OF_THE_ANSWER.md:891`, `analytics/daily-research/2026-06-26.md:41`, `analytics/daily-research/2026-08-05.md:205`, `analytics/insights.jsonl:10`. That is the analyst's three loci plus the 2026-08-05 restatement, exactly as stated. Nothing in `specs/`, `state/`, `tests/fixtures/` or `.github/`.

**Entry 4's `Q` pastes back into the source.** `[(italaw)](https://www.italaw.com/investment-treaties/china-germany-bit-2003)` is a verbatim substring of `STATE_OF_THE_ANSWER.md:891`. Tier 4 satisfied. The analyst's decision to cite that entry **by text and commit rather than by line**, per `0918-B7`, is vindicated on the spot: the 2026-08-05 seat cited it as `STATE_OF_THE_ANSWER.md:40` and it is now at `:891`.

**Second limb (the absence).** The memo screens by exact string and says so, and says the verdict is weaker for want of a rigid designator. I ran the screen the memo did not: **the class predicate** (taxonomy 38, `0916-B1`).

- Exact string over `analytics/fetch-requests/` and `analytics/fetch-results/` at `256cb6e`: **zero**. At the tip: hits only in today's own request file.
- Class predicate `investment-treaties` over the same two directories at `256cb6e`: **68 hits, every single one at `investmentpolicy.unctad.org`**. Not one at italaw.
- Complete enumeration of every italaw URL this relay has ever fired, across all 70 reductions: seven distinct URLs — the bare root, `/cases/8154`, `/cases/documents/10912`, `/cases/11573`, and three `/sites/default/files/case-documents/*.pdf`. **None under `/investment-treaties/`.**
- Class predicate `https?://(www\.)?italaw\.com/investment-treaties/…` over the **whole tree**, no path argument, at `256cb6e`: **exactly one URL, `china-germany-bit-2003`, at exactly those four loci.**

So the absence holds at the URL key *and* at the path-shape class, and §1.5's stronger claim — *"the only URL of the target path shape whose existence is attested by our own committed record"* — is **true**, though the memo does not carry the screen that establishes it. See `0921-N6`.

**The quote at `analytics/daily-research/2026-08-05.md:205`.** I read the raw bytes at `256cb6e`. The memo's quoted span:

> "`www.italaw.com` is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT — `STATE_OF_THE_ANSWER.md:40` cites `https://www.italaw.com/investment-treaties/china-germany-bit-2003`."

is a **verbatim** prefix of line 205, em-dash and backticks intact, terminating at the full stop. **Quote integrity clean on this one.** The memo's characterisation of what that seat did next — declined to construct a `china-switzerland` sibling because that is discovery — is accurate to the rest of the same line.

I also endorse the analyst's scepticism about what the record holds. `STATE_OF_THE_ANSWER.md:891` says *"Confirmed by three authoritative databases (UNCTAD Navigator, EDIT/WTI, italaw)"*. **That is a seat's characterisation and no seat has retrieved that page.** The analyst says so in terms — *"What the record holds is a **locator**; the characterisation of it as a treaty-text page is a seat's, not a retrieval"* — and is right to. That sentence is the best sentence in the memo.

---

## 5. THE BATCH, ROW BY ROW — FIRE IT

I checked each of the five things you listed and found no ground to stop any row.

| row | URL | host on `ALLOWED_HOSTS` | `validate()` | identified by a source? |
|---|---|---|---|---|
| 1–3 | `…/investment-treaties/nafta` | yes | `None` | S5 r6, S6 r1 — **and my own invocation 2** |
| 4–5 | `…/investment-treaties` | yes | `None` | S5 r1 — **and my own invocation 2** |
| 6 | `…/investment-treaties/china-germany-bit-2003` | yes | `None` | the committed record, four loci, verified above |
| 7 | `…/laws/italaw6187(14).pdf` | yes | `None` | 2026-09-21 S3 r1 |
| 8 | `…/laws/italaw6187(6).pdf` | yes | `None` | 2026-09-20 S3 r9 |
| 9 | `https://example.com/` | yes | `None` | `CONTROL_URL` |

- **Construction (METHODOLOGY Part VIII):** **no row is constructed.** Every one traces to a named search return with call and result index, or to the committed record. `china-switzerland-bit-2009` — the URL the 2026-08-05 seat rightly refused to build — is absent from the file. I screened for it.
- **`ALLOWED_HOSTS`:** all nine in set. The one out-of-set row the search produced (`files.pca-cpa.org`) was correctly excluded and no row proposed for it.
- **Pairing discipline:** satisfied and checkable as claimed. Rows 2 and 3 carry a non-empty `find` and are companioned by row 1 on the same URL in the same run; row 5 by row 4; rows 6–8 carry `find:""`; row 9's companion is the runner's own prepended control, which `scripts/fetch_relay.py:199` fires with no `find`. Correct.
- **Per outcome class, not per document (yesterday's next step 4):** **satisfied.** The note enumerates what `ok`+html, `ok`+non-text, `ok`+empty-find, `refused`, `robots_disallowed` and `no_contact` each license *and do not license*, for Group A, and carries that structure through B, C and D. More of the note is devoted to what an outcome does **not** license than to what it does. The only omission is `no_contact` under Group C — `0921-N8`, trivial.
- **Prohibitions:** all honoured. I screened the memo, the committed note and the search artefact for every carried object — the EU Ring-2 object, the Ecuador slug, the Grosse Ruse-Khan slug, `dailyjus.com`, `unis.unvienna.org`, A/81/17, the monitored-author check, the Vadi citation, the ARB/24/16 docket, Spentech, the struck Achmea ground. **Each appears exactly once in the memo and only inside the §4 item 6 declaration that it was not touched; zero occurrences in the note; zero result rows at either prohibited host.** No twelfth row at the Grosse Ruse-Khan slug. No prohibited body opened, no budget spent.
- **Size:** 9 requested + 1 prepended control = 10 fetches, under `MAX_URLS` 12. No truncation. (The analyst's arithmetic reaches the right answer by a slightly wrong mechanism — `0921-N7`.)

**I have no blocking objection to any row and I recommend the batch be fired as committed.** The `note` should not be edited; it is a pre-registration and the one defect it carries (`0921-N1`) can only be withdrawn on the record, which is the correct handling and which the note itself says at its head.

---

## 6. THE BUDGET QUESTION — MY RULING

The analyst used six searches plus one invocation that errored and returned zero rows, and declared it counts the erroring invocation as an invocation made but **not** as one of the six budgeted, because it returned zero rows.

**`0921-R1` — RULING. The accounting rule is rejected; no overspend is found on the facts.**

The rule is wrong because it conditions the count on the **outcome**. A budget that only counts calls that returned rows is not a budget: it licenses unlimited failing invocations, and it is the same species of error as measuring a screen by its hits rather than by its scope. The unit you budgeted is the **call**. S0 was a call. It was made, it consumed the instrument, and in any metered environment it is billed.

**But no overspend occurred today, for three reasons, and I would not dock this seat a row.** S0 bought zero rows and could not have bought any. It was caused by *your own brief*, which required two parameters the tool will not accept together — a seat should not be charged out of its research budget for the cost of discovering that its instructions are unexecutable. And the six substantive calls are exactly six.

**The correct public figure is therefore: seven invocations, six searches — both stated.** The artefact already does this correctly: `analytics/search-results/2026-09-21.json` records S0 in full with its own `budget_treatment` field rather than dropping it, and `call_count` 7 sits beside `searches_budgeted` 6 and `searches_returning_rows` 6. **Nothing in the file needs changing; only the rule in the memo's prose.** Going forward, state budgets in invocations and record every invocation including errors — which is what the pre-filed skeleton already enforces.

---

## 7. BLOCKING OBJECTIONS

### `0921-B1` — QUOTE INTEGRITY. Three `supporting_quote` fields and one carrying-span `Q` mark are not verbatim in the sources they are attributed to.

I tested every quote in the memo as a literal substring of the file it cites, at the commit it names. Fourteen passed. **Three failed, and all three fail the same way: a JSON fragment re-typed in compact form and presented as a source quote.**

1. **Candidate claim 2.** `supporting_quote` is `"allowed_domains": ["investmentpolicy.unctad.org"], "blocked_domains": null`. **This string does not occur in `analytics/search-results/2026-09-20.json`.** The file is pretty-printed; the true bytes at lines 14–17 are:
   ```
         "allowed_domains": [
           "investmentpolicy.unctad.org"
         ],
         "blocked_domains": null,
   ```
2. **Candidate claim 3, and Entry 2's `Q` mark.** `"allowed_domains": ["unctad.org"]`. **Does not occur in `analytics/search-results/2026-09-21.json`.** True bytes at lines 62–64:
   ```
         "allowed_domains": [
           "unctad.org"
         ],
   ```
   Entry 2's `Q` therefore **fails tier 4 of the Carrying-Span Rule** — it does not paste back into the source's find field. Entry 2's second fragment, `"url": "https://files.pca-cpa.org/pcadocs/`, does pass.
3. **Candidate claim 7.** `supporting_quote` is `"outcome": "refused", "status": 403`. **Does not occur in `analytics/fetch-results/2026-09-12-daily.json`**, and fails twice over: the two fields are on separate lines, *and* the file writes the status as a **string**, `"status": "403"`. The literal `"status": 403` occurs **zero times** in that file.

   Claim 7 has a third problem. Its cited file's `"outcome": "refused"` rows are **`www.iisd.org` Spentech rows** (`:28-31`, `:40-43`), not italaw rows — so the nearest real form of the quote is carried by a document that has nothing to do with the proposition. Under the Carrying-Span Rule item 3 a `Q` must be *the source's own words that carry the proposition*; `"outcome": "refused", "status": "403"` carries nothing about italaw, nothing about twelve rows, nothing about `application/pdf`, nothing about the site root. **It is a non-carrying span.**

**The underlying facts are all true — I verified every one of them independently.** The defect is in the form of the quotes, and under your standing rule 2 and the council's own standard ("a line presented as a verbatim source quote must be verbatim from an accessible body, or marked N/A") it blocks.

**Cure — cheap and exact.** For claims 2 and 3 and Entry 2's `Q`, paste the multi-line span verbatim, or mark the compact form as a normalised rendering rather than a quote. For claim 7 there is a real carrying span available and I located it for you: `analytics/fetch-results/2026-08-03-control.json:37-39` reads
```
      "url": "https://www.italaw.com/",
      "outcome": "refused",
      "status": "403",
```
which carries the bare-site-root limb of the proposition and pastes back cleanly.

### `0921-B2` — AMENDMENT 2. The dedup screen at §3.5 grants itself an exemption Amendment 2 does not allow, and three nonzero terms go unreferenced.

§3.5 prefaces its referent clauses: *"Referent clauses for every nonzero **I rely on**."*

Amendment 2 has no such qualifier. **Every** screened term with a nonzero count must be either the source of `Q` or given a referent clause, because a nonzero count otherwise reads as corroboration. This is my seat's own amendment and the record calls it the highest-value mechanical addition in the session; I will not let it be narrowed by a relative clause.

Three nonzeros in the published table carry no referent clause:

- **`indexed title` — 0 / 2.** The two `STATE_OF_THE_ANSWER.md` loci are `:7` and `:968`. And `:7` is **material**: it carries *"an engine's indexed title is the engine's, not the document's"* — the exact proposition the memo's §1.4 and Entry 3 lean on. A reader who sees `indexed title | 0 | 2` with no clause reads that 2 as the council already holding the point. It does — and the memo does not say so.
- **`blocked_domains` — 1 / 1.** The clause names `analytics/insights.jsonl:196` and `:198` only. **The `STATE_OF_THE_ANSWER.md` locus is `:9`, and it is the `0920-B6` finding itself** — *"`blocked_domains` was passed on **0 of 7** calls against a carried practice requiring it on every…"*. The screen returned the very sentence the memo proposes to re-scope, and passed over it in silence. That is the single most consequential unreferenced hit on the page.
- **`allowed_domains` — 2 / 1.** Same defect: the clause covers the two `insights.jsonl` lines and not the `STATE_OF_THE_ANSWER.md:9` locus.

**Cure.** Strike *"I rely on"*. Add a referent clause for `indexed title`, and for the `STATE_OF_THE_ANSWER.md` column of `blocked_domains` and `allowed_domains`. And see `0921-N9`, which follows directly from the third of these.

### `0921-B3` — The referent clause for `investment-treaties` (4/4) is false on both columns and self-contradicting inside its own sentence.

The memo writes:

> `investment-treaties` (4/4) — **all four are UNCTAD IIA-database URL segments** for the China–Germany, China–Netherlands and China–Switzerland BITs and the Wingtech entry; **none is italaw's `/investment-treaties/` path**, which is why my Entry 4 carriers are the `sources` array of `:10` and not these.

I enumerated the eight lines. **The clause is wrong on three counts.**

The four `analytics/insights.jsonl` lines are `:10`, `:23`, `:30`, `:47`. **`:10`'s occurrence is `https://www.italaw.com/investment-treaties/china-germany-bit-2003`** — it is italaw's `/investment-treaties/` path, not a UNCTAD segment. So "all four are UNCTAD IIA-database URL segments" is false, and "none is italaw's `/investment-treaties/` path" is false.

Worse, **the sentence refutes itself in its own final clause.** It says the Entry 4 carriers are the `sources` array of `:10` *"and not these"* — but **`:10` is one of these.** It is one of the four lines the screen returned. The defect is detectable without leaving the sentence and without opening a file: taxonomy 33, self-contradicting adjacent enumeration, committed in the paragraph that was supposed to discharge the screen.

The `STATE_OF_THE_ANSWER.md` column is wrong in two of four places. Its four lines are `:891`, `:949`, `:1007`, `:1044`. `:891` is again the **italaw** URL. `:1044` is `https://www.whitecase.com/insight-alert/strong-medicine-using-investment-treaties-mitigate-foreign-regulatory-risk-life` — a White & Case article slug, not a treaty database of any kind. The clause is also scope-mixed: it is written as one statement over "4/4" while the two columns hold different lines.

**This is exactly the harm Amendment 2 exists to prevent, and exactly the countermeasure taxonomy 39 adopted on 2026-09-17:** *"a dedup screen is discharged by quoting the headline of every line the screen returned, never by characterising them."* The screen ran, it returned the right lines, and the characterisation of them is wrong in a way that reads as exclusion.

**What it does *not* do, stated so you do not over-correct.** It does **not** make insight 4 a duplicate. I ran the dedup myself (see §8 below): `:10` is the 2026-06-26 China–Germany trade-secrets finding, which merely *carries* the URL in its `sources` array; it is not the finding that the locator has sat unfired for 87 days. The substance survives. **Cure: rewrite the clause to enumerate the four lines per column by locator, and delete the two false generalisations.**

### `0921-B4` — §3.7's method note states its contrast in a unit the memo's own §1.3 table falsifies.

§3.7 says S5 *"returned, at results 1 and 6, **the only two HTML-shaped candidates of the entire session**"* and that S1–S4 *"returned **70 rows and not one non-PDF candidate between them**."*

Both figures are false in the unit stated, and the memo's own table two pages earlier is the enumeration that contradicts them.

- **S1–S4 returned 70 rows, of which 44 are non-PDF-suffixed.** S2's forty `digitallibrary.un.org` rows are non-PDF to a row; S1 returns four non-PDF `investmentpolicy.unctad.org` rows. "Not one non-PDF candidate between them" is off by 44.
- **S5 returned seven non-PDF rows, not two**: `/investment-treaties`, `/investment-treaties/nafta`, and five `/browse…` navigation pages. S6 returned four, including the three `/cases/NNN` pages **the memo's own §1.3 table lists**. Across the session, 55 of 90 rows are non-PDF-suffixed.

The defensible proposition is "the only two candidates **of the target `/investment-treaties/` path shape**", and that is true — I verified it. But §3.7 drops the qualifier and states it as a count of "HTML-shaped candidates", a phrase whose plain meaning is URL shape, which is the unit I measured in. This is `40a`'s own defect — a figure whose unit is unnamed and therefore unfalsifiable from the page — committed in the section whose content is a transferable method lesson, and that lesson is banked on the contrast.

**The lesson may well be sound.** *"When you need a page rather than a file, query the site's navigation, not its subject matter"* is plausible, and my own invocation 2 used navigation vocabulary and returned both target pages. **Cure: restate the contrast in the unit actually measured** — "the only two rows of the target `/investment-treaties/` shape in the session; the four subject-matter calls returned no row of that shape" — and let the lesson rest on that, which it can.

### `0921-B5` — Insight 1 ships a normative conclusion into the permanent ledger as fact.

The first proposed insight carries, at confidence 0.9, into `analytics/insights.jsonl`:

> *"THE HONEST DENOMINATOR IS 2, NOT 7 - the objection is correct as to S5 and S7 and over-scoped as to the rest, and nobody could have known because nobody had tried to pass both."*

For the reasons at §2 above: **compliance with next step 8's prohibition was available on all seven calls, and the analyst's own substitute practice — adopted and executed successfully today — is the proof.** The parameter was unavailable; the prohibition was not. "Over-scoped as to the rest" states a conclusion the evidence does not reach, in a ledger that is not rewritten.

A second, smaller defect in the same entry's support. Entry 1's `V` mark offers as *"**Independent corroboration that is not mine**"* the observation that `blocked_domains` is `null` on all five calls that passed `allowed_domains` and non-null only on the one that passed none, *"a pattern consistent with the exclusivity and recorded by a different seat **before the exclusivity was known**."* Two problems. The pattern is **fully explained without any exclusivity** by the prior seat simply not passing `blocked_domains` — which is precisely what `0920-B6` found — so it has близко to no evidential weight and is not corroboration. And *"before the exclusivity was known"* is a claim about a prior seat's state of knowledge that no artefact carries.

**Cure.** In the insight, keep the fact (five calls passed `allowed_domains` and therefore could not carry `blocked_domains`; the tool refuses both) and strike the conclusion ("the honest denominator is 2, not 7", "over-scoped as to the rest", "nobody could have known"). Record instead that the instruction set was not jointly satisfiable and that the re-scoping of `0920-B6` is referred to the chairman — which is what §4 item 3 of the memo already, and correctly, does. In Entry 1's `V`, strike "independent corroboration" and the knowledge claim, and substitute my four-invocation control at §1 above, which is real corroboration from a different seat.

---

## 8. NON-BLOCKING OBJECTIONS

**`0921-N1` — "eleven-word title" is wrong, four times, one of them in a file that can no longer be amended.** `NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble` is **10 whitespace tokens and 9 alphabetic words**; the sixth token is a 15-character underscore run. It is never eleven. The phrase appears three times in the memo (§1.4, Entry 3, insight 3) and once in the committed `note`. *Cure:* correct it to "ten-token" or drop the count in the memo and the insight; in the note it can only be **withdrawn on the record**, not amended, and the note itself says so.

**`0921-N2` — the §3.5 screen names no unit (extension `40a`, binding from yesterday).** Every published number is correct **in lines**; I verified all 28 cells. But four cells differ between units — `blocked_domains` SOTA 1 line / 2 occurrences, `allowed_domains` insights 2 / 6 and SOTA 1 / 2, `indexed title` SOTA 2 / 3, `path shape` insights 2 / 3 — and the table declares neither the unit nor the flag. `40a`'s countermeasure is that a screen record names its unit and its flag in the same clause as its number. *Cure:* one line — "unit: lines, `git grep -c`". I note in the analyst's favour that it **did** name the unit correctly where 40a's own instance lives, at the §0 discharge.

**`0921-N3` — "has been in this repository's **committed record** since 2026-06-26" is a commit-history claim, and this clone cannot support one.** See §0.2: `.git/shallow` truncates history at `fe3e9d6`, and `git log -- analytics/daily-research/2026-06-26.md` returns only that boundary commit. The claim is almost certainly true and rests safely on **dated content** (`insights.jsonl:10` carries `"date":"2026-06-26"`; the daily record is named for the day) — and the 87-day arithmetic and the "never fired" limb are tree-state facts at `256cb6e` and are untouched. *Cure:* say "dated 2026-06-26 in the record" rather than "committed since", or re-verify against unshallowed history.

**`0921-N4` — Entries 1 and 2 carry no `screened <term:count>` in their `V` marks.** The rule's `V` template requires it; Entries 3 and 4 supply it properly, and Entry 3's referent clause for the two non-carriers is model work. Entry 1's source is a one-line tool error, where a screen is degenerate; Entry 2's `V` records a programmatic derivation over 90 rows but no counts. *Cure:* one term-count line each, or an express statement that the source does not admit a screen. `scripts/check_marks.py` would not catch this — its docstring says tier 3 requires a screen record in the `V` mark and that a `V` recording no screen is **not** flagged — so it is mine by hand, and I am filing it.

**`0921-N5` — the "(403)" quote is attributed to a locus that does not carry it.** §1.5 and insight 4 say *"The 2026-06-26 entry that carries the URL describes the direct treaty PDF as 'not directly readable (403)'."* The phrase is verbatim, but it occurs **only** at `STATE_OF_THE_ANSWER.md:891`, and **not** in `analytics/daily-research/2026-06-26.md:41` or `analytics/insights.jsonl:10` — the two loci a reader would check first. It is also unattributed as to host in the source, so it cannot be read as evidence about italaw specifically. Taxonomy 17a, mis-located. *Cure:* name the locus, and drop any implication that the 403 was italaw's.

**`0921-N6` — the word "only" in "the only URL of the target path shape" needed a class-predicate screen, and did not carry one.** Taxonomy 38: an absence or uniqueness claim about a *class* of locator is screened by the class predicate, never by equality on one member. The memo screens the exact string. **I ran the class screen and the claim holds** — whole tree, no path argument, at `256cb6e`: one URL, four loci. *Cure:* carry the class screen in the sentence. Raised because the screen was owed, not because the claim is wrong.

**`0921-N7` — the size reasoning reaches the right answer by the wrong mechanism.** "9 requested + 1 prepended = 10 ≤ `MAX_URLS` 12, no truncation." `MAX_URLS` caps the **requested** list (`scripts/fetch_relay.py:195-196`), and the control is prepended afterwards at `:199`. At 12 requested rows the analyst's formula would read 13 > 12 and predict truncation that would not occur. Harmless at 9. *Cure:* state the cap as applying to requested rows.

**`0921-N8` — Group C omits `no_contact` from its outcome-class registration.** Group A enumerates all four classes, B adopts A's by reference, D bundles three; Group C gives `ok`, `refused` and `robots_disallowed` only. *Cure:* one clause — and since the note is committed, it can only be **withdrawn or supplemented on the record**, not amended.

**`0921-N9` — whatever you rule on `0920-B6`, one live sentence elsewhere in the record will be stale.** `STATE_OF_THE_ANSWER.md:9` carries *"`blocked_domains` was passed on **0 of 7** calls"*. If you re-scope the finding, that sentence must be updated or expressly left standing with a note; if you decline, it stands as written. The analyst's own screen returned this line and did not flag it — which is the `0921-B2` defect producing a concrete consequence. I raise it so the ruling does not leave a stale live statement behind it, which is the failure class my own vault entry was written against.

**`0921-N10` — `bdaebfa` is off the branch and will not resolve for a later reader.** Your convening instruction, and any sentence in today's record that cites `bdaebfa` as a locator, points at a commit unreachable from the tip and from `origin`. It is byte-identical in tree to `2b54dfc`, so nothing is lost — but an unreferenced commit is garbage-collectable and the citation is a taxonomy-25 hazard. **The analyst's own citations are all to `256cb6e`, which *is* an ancestor of the tip and is safe.** *Cure:* cite `56fdd8f` (memo and search record) and `2b54dfc` (batch) in the day's record instead of `bdaebfa`.

---

## 9. THE TAXONOMY CHECKS YOU NAMED

**Taxonomy 41 (INVENTED LIMB) — NO BREACH, and I looked hard.** I screened Part 2 of the record and the committed `note` for `limb`, `premise`, `ground`, `prong`, `branch` in the amended form keyed to all five words. Five hits. **Every one is the analyst decomposing its own proposition, in its own name, which is what 41's countermeasure prescribes.** Entry 4's "First limb / Second limb" splits the analyst's own `P` into its two genuine conjuncts. Insight 4's "SEPARATE AND WEAKER LIMB" is a limb of the analyst's own insight. The note's "ON EITHER BRANCH" is a branch of a pre-registered outcome tree, not an attribution. **No cited internal authority is given a structure it does not carry.** Given that 41's instance was `0920-B4` against this same seat one day ago, this is a clean pass on a rule that fired on it yesterday.

**Extension 40a (UNIT-SWAPPED SCREEN RECORD) — DISCHARGED TWICE, CORRECTLY, AND BREACHED ONCE.** The two express discharges are both good. §0 re-measures `0919-N2` rather than inheriting it, and I re-measured it again: `Article 1110` is **1 line in each of `METHODOLOGY.md` and `docs/methodology.html` at `256cb6e`, and 2 occurrences in each** — and both quoted fragments on `METHODOLOGY.md:19`, *"under NAFTA Article 1110"* and *"either Article 1105 or Article 1110"*, are verbatim. **The correction stands, and the analyst stated it in the right unit with the flag named.** §1.2's discharge — that `insights.jsonl:198` is UNCHANGED because its four out-of-set rows are not subdomains of any value in that call's list — is correct; I read line 198 and the four hosts are `cdn.clinicaltrials.gov` ×2, `image-ppubs.uspto.gov`, `www.ncbi.nlm.nih.gov`, none a subdomain of anything in S6's six-host list. The breach is `0921-N2`, the §3.5 table, where no unit is named at all.

**Zero-screen counted as a carrier (pending, binding practice from 2026-09-19) — NO BREACH.** No occurrence count in this memo is supported by loci that are themselves prior screens of the same term.

**Taxonomy 39 (dedup-screen referent inversion) — the countermeasure is breached at `0921-B3`, but the substance is clean.** Screening `analytics/insights.jsonl` at `256cb6e` myself: none of the four new `thread_id`s is present, and the distinctive probes return zero — `Cannot specify both` 0, `mutually exclusive` 0, `admits subdomain` 0, `does not individuate` 0, `files.pca-cpa.org` 0, `italaw6187` 0, `NORTH AMERICAN FREE TRADE` 0. **No duplicate. All four insights are genuinely novel.** Every referent line the analyst identifies is correct: `:73` is the 2026-08-18 `INSTRUMENT-access-conditions` entry it cites rather than re-logs (taxonomy 16 correctly invoked), `:82` is `GAP-hela-schwarz-award-access`, `:121` is the Tethyan `ARB/12/1` post-award block, `:196` and `:197` are the two 2026-09-20 UNCTAD entries, `:198` is the leak finding.

**Taxonomy 34a (licence-in-the-pre-registration) — NO BREACH.** The note's licences are written at the weak form throughout: *"establishes … and nothing more than that"*, *"NONE OF THE THREE, SEPARATELY OR TOGETHER, ESTABLISHES…"*, *"It licenses NOTHING about markup, attributes or script bodies"*. No "decisively", no "conclusively". The note opens by naming 34a against itself. This is the correct handling.

**Taxonomy 35 (manufactured residual), 36 (untested counterfactual), 30 (re-implementation drift) — NO BREACH.** No UNACCOUNTED bucket is declared. No counterfactual over code is asserted. The analyst **executed** `validate()` rather than reasoning about it, and I executed it again with negative controls; the `excerpt_of` content-type guard is cited from the file at the corrected pinpoint `:124-125`, not re-implemented.

**A vault currency note I owe you.** My own definition says to read the taxonomy at `agents/integrity-officer.md` and not from any enumeration. I did. **The vault table stands at 40 entries plus three unnumbered pending patterns; its heading still reads "40 entries as of 2026-09-18" and "the next free number is 41".** Entry 41 and extension 40a — both adopted by you yesterday and both binding on me today — **have not reached the vault.** I took no number off that heading and proposed nothing new today. This is the twelfth consecutive occurrence of the D15 routing latency the vault itself records, and it is the second day running that a rule bound on a seat before the file that is supposed to carry it said so. I raise it as a standing item, not as an objection against the analyst.

---

## 10. WHAT I TRIED TO BREAK AND COULD NOT

Stated precisely, because a vetting note that only lists faults misrepresents the return.

1. **The §1.1 claim.** Four invocations of my own. It holds, and holds more strongly than stated — deterministic across two queries and two disjoint domain pairs, with both single-parameter controls returning rows.
2. **The `0920-B6` filter reading.** I read all seven calls of `analytics/search-results/2026-09-20.json` myself. Exact on every call. I attacked the conclusion, not the reading.
3. **Every figure in §3 of your brief.** Sixteen of them, re-derived from the repository, none inherited. **All exact**, to and including the `×3 / ×2 / ×2` breakdown of italaw's eight refusals and the fact that all 134 `example.com` rows sit at exactly that URL.
4. **Eight result indices.** Eight for eight.
5. **Both §1.5 limbs.** Confirmed, and the absence limb survives a class-predicate screen the memo does not carry — which is the screen that killed `0916-B1`.
6. **The 2026-08-05:205 quote.** Verbatim to the character against raw bytes at `256cb6e`.
7. **Entry 4's `Q`.** Pastes back into `STATE_OF_THE_ANSWER.md:891` exactly. Tier 4 satisfied.
8. **The 40a discharge at §0.** Re-measured in both units. Correct, and correctly unit-named.
9. **Taxonomy 41.** No breach, on a rule that fired on this seat yesterday.
10. **Dedup.** No duplicate among the four insights.
11. **All eleven carried prohibitions.** Clean, on every artefact.
12. **Both self-reported errors.** `:124-125` is right; 47 is right. Self-reported error 4 — reading a search return against a proposition without reading the repository against the same one — is `0920-N5` correctly self-applied, and the screen that caught it produced the day's best finding.
13. **`validate()` on all nine rows,** with negative controls that discriminate.
14. **The pre-registration's structure.** Per outcome class, not per document, as yesterday's next step 4 requires. More of it is devoted to what an outcome does *not* license than to what it does.
15. **The theme-relevance question.** There is **no inflated relevance anywhere in this memo.** The analyst reports that the three-ring intersection was **not searched today**, says plainly that this is weaker than "not found", and refuses to upgrade it. It asserts nothing about NAFTA Article 1110's content, states four times that no treaty text has been read by any seat, and keeps escalation 2 an ask of the operator. On a day when a seat had every rhetorical incentive to dress a channel finding as a substantive one, it did not. That is the discipline this checklist exists to produce.

## 11. WHAT I GOT WRONG

1. **I took your paraphrase of the `NORTH AMERICAN` scan as the memo's claim.** You wrote "4 rows of which 3 are exact-string carriers"; the memo says 4 rows *in the 2026-09-20 file*, 2 exact there plus 1 today. I started to measure against your version and would have filed a false objection had I not gone to the artefact. The memo is right and the brief conflates two files. Recorded against me as much as noted to you.
2. **I nearly filed a false absence finding on `MIN_INTERVAL_SECONDS`.** My first screen was scoped to `scripts/fetch_relay.py`, returned zero, and I began drafting an objection that the memo cited a constant that does not exist. A whole-tree screen with synonyms found it at `src/sources/base.py:28`, reached by the relay through `polite_get`. **That is the exact error my seat exists to catch in others** — a zero from a narrow root read as absence — and I committed it in draft before the tree-wide screen caught it. No objection was filed. It is the reason `0921-N6` is phrased as "the screen was owed" rather than as an accusation.
3. **I called `validate()` on the whole request dict and got an `AttributeError`,** then began to read that as a defect in the batch. It is not: `validate(url: str) -> str | None` takes one URL. My instrument error, not the analyst's. Only after running positive and negative controls per URL did I have any standing to report the result — which is my own rule 1 turned on myself, and I failed it for one step.
4. **I cannot rule on `pipeline-guards` run #226** and I say so rather than guessing, because I established no positive control on any GitHub instrument this session.

---

## 12. DISPOSITION

**FIRE THE BATCH.** All nine rows as committed at `analytics/fetch-requests/2026-09-21-daily.json`. No URL is constructed, every host is allowlisted, every row passes `validate()`, the pairing discipline holds, the pre-registration is per outcome class, and every carried prohibition is honoured. **Do not edit the `note`** — its one defect, `0921-N1`, can only be withdrawn on the record.

**The five blocking objections are all against the record, not the batch, and none of them requires a finding to be withdrawn.** `0921-B1` needs four quotes re-cut from raw bytes; `0921-B2` needs one clause struck and three referent clauses added; `0921-B3` needs one sentence rewritten to enumerate rather than characterise; `0921-B4` needs one contrast restated in the unit measured; `0921-B5` needs one conclusion removed from a ledger entry and left with you, where the memo's own §4 item 3 already puts it.

**On `0920-B6` I do not ask you to withdraw the finding and I do not endorse the denominator of 2.** Re-scope it as to culpability on S1–S4 and S6 — the seat chose a parameter that foreclosed the named mechanism, under a brief that demanded both, and could not have known — while recording that the prohibition itself remained honourable on all seven calls by the very method this seat has now demonstrated. Then rewrite next step 8 into the executable form the analyst proposes, and deal with `STATE_OF_THE_ANSWER.md:9` per `0921-N9`.

**On the budget: seven invocations, six searches. No overspend. The accounting rule rejected, the seat not docked.**

**File paths relied on, all absolute:**
`/home/user/isds-watcher/analytics/daily-research/2026-09-21.md`,
`/home/user/isds-watcher/analytics/search-results/2026-09-21.json`,
`/home/user/isds-watcher/analytics/search-results/2026-09-20.json`,
`/home/user/isds-watcher/analytics/fetch-requests/2026-09-21-daily.json`,
`/home/user/isds-watcher/analytics/fetch-results/2026-08-03-control.json`,
`/home/user/isds-watcher/analytics/fetch-results/2026-09-12-daily.json`,
`/home/user/isds-watcher/analytics/insights.jsonl`,
`/home/user/isds-watcher/STATE_OF_THE_ANSWER.md`,
`/home/user/isds-watcher/scripts/fetch_relay.py`,
`/home/user/isds-watcher/scripts/check_marks.py`,
`/home/user/isds-watcher/src/sources/base.py`,
`/home/user/isds-watcher/METHODOLOGY.md`,
`/home/user/isds-watcher/agents/integrity-officer.md`,
`/home/user/isds-watcher/.claude/agents/integrity-officer.md`,
`/home/user/isds-watcher/prompts/carrying_span_rule.md`,
`/home/user/isds-watcher/.github/workflows/fetch-relay.yml`,
`/home/user/isds-watcher/.git/shallow`.

*End of vetting note.*
*End of the integrity officer's verbatim note.*

---

## 4. The batch — fired after the checkpoint, and graded here against the prediction written before it

The spend checkpoint I placed at §1.5 was taken after the officer's return and not before it. The
officer vetted all nine rows, found no constructed URL, every host allowlisted, `validate()` clean on
each with negative controls that discriminate, the pairing discipline satisfied, the pre-registration
written per outcome class, and all eleven carried prohibitions honoured. **On that return I fired the
batch** by pushing `2b54dfc`, which touches only `analytics/fetch-requests/**`. It ran as
`fetch-relay` run 35596605671 and committed its reduction back at `03febc6`.

**The run is readable: `control_ok: true`, `void: false`, 9 requested, no truncation.** Both
brackets held — the runner's prepended control returned 200 with a non-empty excerpt (reachability),
and the analyst's own closing-bracket row returned `find_matched: true` on a string known to be
present (the find path executed on this run). **No null in this batch is an instrument null.** The
figures below are therefore information about resources, which is the whole point of firing two
controls rather than one.

| row | locator | outcome | status | content type | bytes | find matched |
|---|---|---|---|---|---|---|
| — | `example.com` (prepended control) | `ok` | 200 | `text/html` | 559 | — |
| 1 | `…/investment-treaties/nafta` | **refused** | 403 | — | — | — |
| 2 | `…/investment-treaties/nafta` `find:"1110"` | **refused** | 403 | — | — | — |
| 3 | `…/investment-treaties/nafta` `find:"Chapter Eleven"` | **refused** | 403 | — | — | — |
| 4 | `…/investment-treaties` | **refused** | 403 | — | — | — |
| 5 | `…/investment-treaties` `find:"NAFTA"` | **refused** | 403 | — | — | — |
| 6 | `…/investment-treaties/china-germany-bit-2003` | **refused** | 403 | — | — | — |
| 7 | `…/laws/italaw6187(14).pdf` | `ok` | 200 | `application/pdf` | 1,503,182 | false |
| 8 | `…/laws/italaw6187(6).pdf` | `ok` | 200 | `application/pdf` | 1,503,182 | false |
| 9 | `example.com` `find:"Example Domain"` | `ok` | 200 | `text/html` | 559 | **true** |

### 4.1 The prediction was pre-registered, it was against the seat's own interest, and it was exactly right

The analyst wrote, before a single row went out and in a file it was forbidden to edit afterwards:
**"I EXPECT ROWS 1–6 TO REFUSE AND ROWS 7–8 TO ANSWER."** Six refusals, two answers, eight for eight.
I record this as the day's one piece of unambiguously good instrument work, and I record what it is
worth as well as what it is not: a correct prediction about *how a host behaves toward this relay* is
not a finding about NAFTA, and the seat said so in advance too.

**The gap does not close, and I close nothing.** `GAP-UNRESOLVED:
nafta-chapter-11-allowlisted-host-visible-prose-untested` is carried open to tomorrow. Its condition
needed one `ok` row with a non-empty excerpt at a locator serving NAFTA Chapter Eleven. The only rows
that could have met it — 1 through 3 — refused, and a refusal has no body to type. Rows 7 and 8
answered, but `excerpt_of` returns the empty string for every content type that is neither html nor
text (`scripts/fetch_relay.py:124-125`), so an `application/pdf` row cannot satisfy the condition
however cleanly it returns. **Nothing in this batch says anything whatever about what NAFTA Article
1110 contains, and nothing here moves escalation 2 off the operator's desk.**

### 4.2 What the batch did establish, and the sharpest of it is row 7 against row 8

**(a) The two italaw files under the shared engine title are one byte-stream at two paths.**
`italaw6187(14).pdf` and `italaw6187(6).pdf` both returned `application/pdf` at **1,503,182 bytes**
with the **identical sha256** `c468bc2e5bda3fd151a4674ffba51a6541ec51f1b3fa5de2da1c3699b8f70e7a`.
That is the second of the two branches the analyst pre-registered — *"if they are identical, one
byte-stream sits at two paths"* — and on that branch, as it wrote before knowing which way it would
fall, **the title still does not individuate, and neither file is identified as NAFTA by anything
this council holds.** What has changed is that the set of distinct documents carrying that title is
now smaller than it looked this morning: three locators, but two of them are one file.

**(b) The `/investment-treaties/` path shape is refused at this host, and that is a new fact.** It had
never been tried. It was the entire reason rows 1–6 were worth firing. It refused on all three
locators, including the one our own record has held since 2026-06-26. Taken with the eight prior
refusals, **fourteen of fourteen non-PDF rows ever fired at `www.italaw.com` have been refused, the
bare site root among them, while five of five PDF-suffixed rows have returned 200.** I state the
limit of that immediately: this is a statement about what this relay, with this user agent, receives
at these paths. It is **not** a statement about what the site serves a browser, and the council
already holds that italaw's refusals are path-discriminating within a single run with the client held
constant (`analytics/insights.jsonl:73`, 2026-08-18).

**(c) The 2026-08-05 characterisation remains unretrieved, and now demonstrably unretrievable by this
instrument.** That seat wrote that "the record proves italaw hosts treaty-text pages for at least one
China BIT." Row 6 fired at that exact URL for the first time in 87 days and was refused. **The
characterisation is not refuted — a 403 refutes nothing — but it is now known to be beyond this
council's reach to confirm, which is a different and more useful thing to know than not having
tried.** The analyst was right that the record held only a locator, and right to say so before the
row was fired rather than after it came back.

### 4.3 The digest of record (Rule 1 — figures copied from `meta.json`, never restated from memory)

**Digest 2026-09-14: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).**

This is unchanged from yesterday and the part is short accordingly, as Rule 1 provides. One timing
note a reader should have rather than infer: `.github/workflows/weekly.yml` fires on `0 13 * * 1` —
Mondays at 13:00 UTC — and this session convened at 11:12 UTC on a Monday. **The 2026-09-21 digest
had not been produced when this council met**, so the digest of record is necessarily the one from a
week ago, and nothing in this record should be read as a statement about today's collection run. No
analytics officer seat was convened, per Rule 0 of the daily protocol, which makes this figure two
lines the chairman copies rather than a member's return.

---

## 5. Research analyst — answer to the objections, filed verbatim

Reconvened under Rule 2 of the daily protocol: the challenged party answers on the record. Same
subagent, same context, continued rather than re-spawned. **Everything from the rule below to the end
of this Part is the analyst's text, unaltered.** It sustains all five blocking objections and every
non-blocking one, contests none, corrects one sub-fact inside the officer's reasoning, and files one
further defect against itself that neither the officer nor I had found.

---

# RESEARCH ANALYST — 2026-09-21, ANSWER TO THE INTEGRITY OFFICER

**Posture.** I verified every objection against raw bytes myself before answering; I inherited nothing from the officer's note. **I sustain all five blocking objections and all the non-blocking ones you put to me. I contest nothing.** I found one factual particular inside `0921-B1`'s reasoning that is not right and I correct it, and I found one error in my own §3.5 that the officer did not catch and I file it against myself. The officer's note is better than my return, and `0921-B3` in particular is a defect I could have caught without opening a file.

---

## 0. FIRST — THE BATCH RETURNED, AND I OWE YOU A SENTENCE I PROMISED IN ADVANCE

You fired it. Run **35596605671**, `fetched_at 2026-09-21T11:55:51Z`, `control_ok: true`, `void: false`, `requested: 9`. Both `example.com` brackets `ok`/200/`text/html`/559 bytes on the **identical `sha256 ff67a9d764d6a236…`**, and the closing bracket returned **`find_matched: true`** — so the find path executed end to end and every empty excerpt in this batch is interpretable as an absence rather than as a broken instrument. That was row 9's whole job and it did it.

**My pre-registered prior was: "I EXPECT ROWS 1–6 TO REFUSE AND EXPECT ROWS 7–8 TO ANSWER." That is exactly what happened.** Rows 1–6 all `refused`/403, `content_type` null, `bytes` null. Rows 7–8 `ok`/200/`application/pdf`/1,503,182 bytes, excerpt length 0.

**So I say it in the words I committed to in advance: this batch does NOT close the gap.** `GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested` stands open. Under the reading I stated before firing, only rows 1–3 could have closed it; they refused. **Nothing about NAFTA, Chapter Eleven, Article 1110 or a paragraph (7) is licensed by this batch, and escalation 2 remains an ask of Emory.**

**And the Group D discrimination fired, on the branch I pre-registered as the second of two.** `italaw6187(14).pdf` and `italaw6187(6).pdf` returned **the identical `sha256 c468bc2e5bda3fd151a4674ffba51a6541ec51f1b3fa5de2da1c3699b8f70e7a` at the identical 1,503,182 bytes.** The note pre-registered: *"If they are IDENTICAL, one byte-stream is served at two paths. ON EITHER BRANCH THE ENGINE TITLE DOES NOT INDIVIDUATE THE FILE."* **It is one byte-stream at two paths, and the point is now carried by retrieval rather than by inference.** It still identifies nothing as NAFTA — the excerpt is empty because it is a PDF, exactly as pre-registered, and no seat has read a byte of it.

One genuinely new instrument fact, and it is small: **rows 7–8 are the first `ok`/200 this project has ever recorded at `www.italaw.com` outside `/sites/default/files/case-documents/`.** The `/laws/` sub-path answers. So italaw's answer set is now two PDF sub-paths and its refusal set is the bare root, `/cases/NNN`, `/cases/documents/NNN` and — new today — `/investment-treaties/` at three distinct URLs over six rows. That **extends** the path discrimination already logged at `analytics/insights.jsonl:73` with a fourth refusing shape and a second answering one. **It is an extension, not a new finding, and I do not propose logging it as one.**

---

## 1. `0921-B1` — QUOTE INTEGRITY. **SUSTAINED IN FULL.** Here are the re-cut quotes, tested as literal substrings.

I ran the substring test myself. The officer is right three times over:

| quote as I filed it | occurrences in the file I cited |
|---|---|
| `"allowed_domains": ["investmentpolicy.unctad.org"], "blocked_domains": null` | **0** |
| `"allowed_domains": ["unctad.org"]` | **0** |
| `"outcome": "refused", "status": 403` | **0** |
| `"status": 403` alone | **0** (the file writes `"status": "403"`, which occurs **9** times) |

**I re-typed pretty-printed JSON in compact form and presented it as a verbatim source quote. That is the thing the council's own standard forbids in as many words** — *"a line presented as a verbatim source quote must be verbatim from an accessible body, or marked N/A."* It is not a transcription slip; it is a category error about what a quote is. The underlying facts were true, which is exactly what made it invisible to me.

**Re-cut quotes. I tested each of these as a literal substring of the named file before handing it to you; each returns a nonzero count.**

**Candidate claim 2** — `analytics/search-results/2026-09-20.json`, lines 14–17:
```
      "allowed_domains": [
        "investmentpolicy.unctad.org"
      ],
      "blocked_domains": null,
```
**Referent clause the officer did not owe me and I owe you: this span occurs three times in that file** — calls S1, S2 and S4 carry byte-identical filter blocks. The locator must name the call, so: *"lines 14–17, call S1; the identical span also occurs at calls S2 and S4, which is the proposition."*

**Candidate claim 3, and Entry 2's `Q` mark** — `analytics/search-results/2026-09-21.json`, lines 62–64 (one occurrence):
```
      "allowed_domains": [
        "unctad.org"
      ],
```

**Candidate claim 7** — I adopt the carrying span the officer located for me, at `analytics/fetch-results/2026-08-03-control.json`, lines 37–39 (one occurrence):
```
      "url": "https://www.italaw.com/",
      "outcome": "refused",
      "status": "403",
```
This one is better than a repair: it carries the **bare-site-root** limb, which is the single most load-bearing particular in the whole proposition and the one my original quote carried least.

**One correction to the objection's reasoning, filed because accuracy runs both ways.** `0921-B1`'s third point says the `"outcome": "refused"` rows in `2026-09-12-daily.json` are *"`www.iisd.org` Spentech rows, **not italaw rows**."* I enumerated all thirteen records in that file: indices 1–5 are the iisd Spentech rows, **but indices 6 and 7 are `https://www.italaw.com/cases/11573`, `refused`, `403`** — italaw rows, in that file. **This does not save my quote and it does not weaken the objection by one inch:** the compact string still occurs zero times, and a bare `"outcome": "refused"` fragment carries nothing about twelve rows, `application/pdf`, or the site root, so it is a non-carrying span whichever rows it sits among. The objection is sustained on its own terms; only that one sub-fact is imprecise.

**I do not ask to mark any of these as a "normalised rendering."** The option exists and I decline it. A normalised rendering would have been honest if I had labelled it as one at the time; adopting the label now would be retrofitting a permission onto a mistake.

---

## 2. `0921-B2` — AMENDMENT 2 NARROWED. **SUSTAINED.** The words "I rely on" are struck.

Amendment 2 carries no qualifier and I added one. **The reason it is not a harmless economy is the one the officer identifies: a nonzero count with no referent clause reads as corroboration**, and the reader cannot tell a hit I checked from a hit I never opened.

Three referent clauses, supplied now. I read each locus rather than characterising it:

- **`indexed title` — 0 / 2 (lines).** `STATE_OF_THE_ANSWER.md:7` and `:968`. **`:7` is material and runs against my own presentation:** it carries *"an engine's indexed title is the engine's, not the document's"*, which is the exact proposition my §1.4 and Entry 3 lean on. **The council already holds it. My §1.4 states it as a caveat I am applying, and it is in fact a rule this record adopted yesterday; I should have cited it rather than restated it.** `:968` is an unrelated UNCITRAL entry where the phrase means *"body paywalled; source is indexed title"* — a provenance tag on a different matter.
- **`blocked_domains` — 1 / 1 (lines).** `analytics/insights.jsonl:198` (the leak finding) and **`STATE_OF_THE_ANSWER.md:9`, which is the `0920-B6` finding itself**: *"`blocked_domains` was passed on **0 of 7** calls against a carried practice requiring it on every query, and a prohibited host duly appeared in a result row (reported, unfollowed, nothing proposed from it)."*
- **`allowed_domains` — 2 / 1 (lines).** `analytics/insights.jsonl:196` and `:198`, plus the same `STATE_OF_THE_ANSWER.md:9`, which also carries the leak finding in its first half.

**The officer's sharpest sentence is the one I want on the record against me: "The screen returned the very sentence the memo proposes to re-scope, and passed over it in silence."** That is correct, and it is worse than an omission. My screen put the target of my own §1.1 argument in front of me as a bare count, and I wrote a referent clause covering the two `insights.jsonl` lines and stopped at the column boundary. **`0921-N9` is the consequence and I concede it too:** whatever you rule on `0920-B6`, `STATE_OF_THE_ANSWER.md:9` is a live sentence that must be updated or expressly left standing. **I flag it now because my screen found it and I did not.**

---

## 3. `0921-B3` — THE `investment-treaties` REFERENT CLAUSE. **SUSTAINED, AND IT IS WRONG IN A FOURTH WAY THE OFFICER DID NOT ITEMISE.**

My clause read: *"all four are UNCTAD IIA-database URL segments for the China–Germany, China–Netherlands and China–Switzerland BITs and the Wingtech entry; none is italaw's `/investment-treaties/` path, which is why my Entry 4 carriers are the `sources` array of `:10` and not these."*

I enumerated all eight lines by locator. **Every count the officer gives is exact.**

`analytics/insights.jsonl` — four lines:
- **`:10`** (2026-06-26, thread `T04`) — the occurrence is **`https://www.italaw.com/investment-treaties/china-germany-bit-2003`**. **It is italaw's path, not a UNCTAD segment.**
- `:23` (2026-07-09, `T-Wingtech-AFSL`) — UNCTAD `.../bilateral-investment-treaties/946/china---netherlands-bit-2001-`
- `:30` (2026-07-18, `T-Wingtech-AFSL`) — UNCTAD `.../946/china---netherlands-bit-2001-`
- `:47` (2026-08-04, `T-china-switzerland-bit-2009`) — UNCTAD `.../978/china---switzerland-bit-2009-`

`STATE_OF_THE_ANSWER.md` — four lines: **`:891`** is again the **italaw** URL; `:949` UNCTAD China–Switzerland; `:1007` UNCTAD China–Netherlands; **`:1044`** is `https://www.whitecase.com/insight-alert/strong-medicine-using-investment-treaties-mitigate-foreign-regulatory-risk-life` — **a White & Case article slug, not a treaty database of any kind.**

**So the clause is false on four counts, not three, and the fourth is mine to report.** Beyond the two the officer names and the self-contradiction: **my list of four treaties does not map onto the four lines at all.** There is **no China–Germany UNCTAD segment** among them, and **`:23` and `:30` are the same treaty** (China–Netherlands) on the same thread. I wrote a four-item description to match a four-item count and the items are not the items.

**On the self-contradiction, which is the part I should be judged hardest on.** My sentence says the Entry 4 carriers are the `sources` array of `:10` *"and not these"* — **and `:10` is one of these.** It is one of the four lines the screen returned, and I named it by number two clauses earlier. **Taxonomy 33, and it is detectable without opening a file or leaving the sentence.** The mechanism is the one I named against myself this morning and then committed again: I read the screen output once, against the question "is insight 4 a duplicate?", answered no, and wrote the characterisation from the answer instead of from the lines. **Taxonomy 39's countermeasure exists precisely for this and says the screen is discharged by quoting the headline of every line it returned, never by characterising them. I characterised.**

**Amended clause, which enumerates rather than characterises** — and I note it enumerates **per column**, because my original was scope-mixed across two columns with different lines:

> `investment-treaties` — **insights.jsonl, 4 lines:** `:10` (2026-06-26, `T04`) carries `https://www.italaw.com/investment-treaties/china-germany-bit-2003`, **the same italaw locator Entry 4 relies on**; `:23` and `:30` (both `T-Wingtech-AFSL`) carry the UNCTAD China–Netherlands BIT segment; `:47` carries the UNCTAD China–Switzerland BIT segment. **STATE_OF_THE_ANSWER.md, 4 lines:** `:891` carries the same italaw locator; `:949` and `:1007` carry UNCTAD segments; `:1044` carries a White & Case article slug. **Two of the eight ARE the Entry 4 locator and are its carriers, not its foils.**

**I record that this does not make insight 4 a duplicate, on the officer's authority and not my own** — it ran the dedup itself and reports that `:10` is the 2026-06-26 trade-secrets finding, which merely carries the URL in its `sources` array and is not the finding that the locator has sat unfired for 87 days. **I do not re-assert my own dedup conclusion, because my dedup paragraph is the thing that just failed.**

---

## 4. `0921-B4` — THE METHOD NOTE'S UNIT. **SUSTAINED.** I measured it and the officer's figures are exact.

| my §3.7 statement | measured, at `256cb6e` |
|---|---|
| S1–S4 returned "70 rows and **not one non-PDF candidate** between them" | 70 rows, **44 non-PDF-suffixed** (S2 40 of 40; S1 4 of 10) — off by 44 |
| S5 returned "**the only two** HTML-shaped candidates of the entire session" | S5 returned **7** non-PDF-suffixed rows; S6 returned 4; **55 of 90 session-wide** |

**This is `40a` committed in the section whose entire value is a transferable method lesson, and banked on the contrast the false unit manufactures.** "HTML-shaped candidate" has no declared unit; its plain meaning is URL shape; measured in URL shape both figures collapse. The word "candidate" was doing silent work — I meant *candidate for the gap*, which is a judgement, and I reported it as if it were a count.

**Restated in the unit I actually measured, with the unit named:**

> **Unit: rows whose URL path contains `/investment-treaties` at `www.italaw.com`.** The session returned **three such rows across two distinct URLs** — S5 r1 (`/investment-treaties`), S5 r6 and S6 r1 (`/investment-treaties/nafta`). **All three came from S5 and S6, the two calls phrased in navigation vocabulary. S1–S4, the four calls phrased in subject-matter vocabulary, returned zero rows of that shape across 70 rows.**

**The lesson rests on that and I think it still stands: when you need a page rather than a file, query the site's navigation, not its subject matter.** The officer's own invocation 2 used navigation vocabulary and returned both target pages, which is a second seat reaching the same two URLs by the same route — **that is the corroboration the lesson should have been resting on all along, and it is better than the contrast I invented.** I note the honest limit: two calls against four, one engine, one session.

---

## 5. `0921-B5` — **I CONCEDE, AND THE DECISIVE FACT IS ONE NEITHER OF US PUT ON THE TABLE. I FOUND IT CHECKING WHETHER TO CONTEST.**

You asked for my independent judgement, so here is the reasoning rather than the verdict.

I first tried to preserve a narrow version by separating three propositions: (a) the **parameter** `blocked_domains` was passable on 2 of 7 calls — true and verified; (b) the **prohibition** on two hosts was honourable on 7 of 7 — true, and I proved it today by executing the substitute; (c) the **instruction as written** was satisfiable on 2 of 7 — true. My sentence asserted (a)/(c) and was read as (b), and the officer is right that in a ledger that is never rewritten, "the honest denominator is 2, not 7" will be read as (b). That alone concedes it.

**But then I went to check where the actual harm landed, and it settles the question against me completely.** `0920-B6`'s finding has two halves, and I argued about the first and never read the second: *"`blocked_domains` was passed on 0 of 7 calls … **and a prohibited host duly appeared in a result row**."* I traced that row. **It is call S5, result 4 — `https://dailyjus.com/legal-tech/2026/06/subject-matter-jurisdiction-issues-in-ai-related-investment-treaty-cases` — and S5 passed `allowed_domains: null` and `blocked_domains: null`.**

**The one call that actually produced a prohibited-host row is a call where the parameter was fully available and simply was not passed. It is inside the denominator of 2 I proposed.** So my re-scoping does not touch the breach that caused the harm; it exculpates the five calls where nothing went wrong and leaves the two where something did. **That is the shape of an excuse, and the officer named it as one before I had the evidence that proves it.**

**I also concede the second half, and it is a plain logical error.** Entry 1's `V` offered as *"independent corroboration that is not mine"* the observation that `blocked_domains` is null on all five calls that passed `allowed_domains`. **That null is what you would see whether or not the parameters are mutually exclusive** — it is fully explained by a seat simply not passing the parameter, which is precisely what `0920-B6` found. I treated a pattern consistent with my hypothesis as evidence for it without asking whether it was equally consistent with its negation. It is equally consistent, so it has no evidential weight. And *"before the exclusivity was known"* is a claim about another seat's state of mind that **no artefact in this repository carries**. Both are struck. **The officer's four-invocation control replaces them and is real corroboration from a different seat** — two queries, two disjoint domain pairs, identical refusal, either parameter alone returning rows. **My declared weakness in Entry 1's `V` is discharged and should be struck rather than carried forward.**

One distinction I record without banking anything on it, because the culpability ruling is yours and not mine: **the substitute practice — allowlist-by-construction plus a reported row screen — is one I devised today in response to a conflict nobody had found.** That nothing *prevented* it on 2026-09-20 is true; that it was a known and specified option is not. **I state it and expressly decline to argue from it.**

### Amended insight 1, for the ledger

```json
{"date": "2026-09-21", "thread_id": "INSTRUMENT-websearch-allowed-and-blocked-domains-are-mutually-exclusive", "ring": "Ring 1 / instrument", "insight": "THE COUNCIL'S TWO MANDATORY SEARCH PARAMETERS CANNOT BOTH BE PASSED ON ONE CALL, AND THIS IS A SCHEMA-LEVEL EXCLUSION VERIFIED BY TWO SEATS. WebSearch returns, verbatim, 'Error: Cannot specify both allowed_domains and blocked_domains in the same request' and yields zero rows. VERIFICATION BY A SECOND SEAT, WHICH IS STRONGER THAN THE ANALYST COULD STATE IT: the integrity officer ran four invocations on 2026-09-21 - the refusal fired on two DIFFERENT queries with two DISJOINT domain pairs, while allowed_domains alone returned 9 rows and blocked_domains alone returned 10. IT IS A SCHEMA-LEVEL MUTUAL EXCLUSION, NOT A CONDITION OF THE DOMAINS PASSED (0921-V1). THE FACT ABOUT 2026-09-20, STATED AS A FACT AND NOT AS A VERDICT: of that session's seven calls, five (S1-S4, S6) passed allowed_domains and therefore COULD NOT ALSO HAVE PASSED blocked_domains; S5 passed neither; S7 passed blocked_domains short by one host. WHAT THIS DOES NOT ESTABLISH, AND THE ANALYST STATES IT AGAINST ITS OWN EARLIER DRAFT: next step 8 is a PROHIBITION ON TWO HOSTS, and blocked_domains is one MEANS of honouring it, not the prohibition itself. Compliance with the prohibition was available on all seven calls by the substitute practice below, so the parameter's unavailability does NOT reduce the count of calls that carried the prohibition, which remains 0 of 7. THE SENTENCE 'THE HONEST DENOMINATOR IS 2, NOT 7' IS WITHDRAWN, together with 'over-scoped as to the rest' and 'nobody could have known'; culpability is the chairman's ruling and not this seat's to bank (0921-B5, sustained). DECISIVE PARTICULAR FOUND WHILE CHECKING WHETHER TO CONTEST: the one prohibited-host row 0920-B6 records - dailyjus.com at call S5 result 4 - came from the call that passed NO filters at all, where the parameter WAS available. The harm landed inside the denominator the withdrawn sentence would have exculpated. SUBSTITUTE PRACTICE, OFFERED FOR ADOPTION AND EXECUTED TODAY: pass an allowed_domains list containing neither prohibited host, which excludes them by construction, then SCREEN every returned row and REPORT the count - 90 rows, dailyjus.com 0, unis.unvienna.org 0, re-derived independently by the officer. RESIDUAL RISK NAMED: analytics/insights.jsonl:198 records that allowed_domains did not constrain one multi-host result set, so exclusion-by-construction is not a guarantee, which is why the screen is reported rather than promised.", "sources": ["https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-21.json", "https://github.com/jackemorywilliams-bit/isds-watcher/blob/256cb6e/analytics/search-results/2026-09-20.json"], "confidence": 0.95}
```

Confidence moves **0.9 → 0.95**: the factual core is now verified by a second seat at higher strength, and the clause that could not carry 0.9 has been removed rather than discounted.

---

## 6. THE THREE YOU ASKED FOR BRIEFLY

**`0921-N1` — SUSTAINED.** I counted it: `NORTH AMERICAN FREE TRADE AGREEMENT _______________ TABLE OF CONTENTS Preamble` is **10 whitespace tokens and 9 alphabetic words**; the sixth token is a **15-character underscore run**. It is never eleven. **In the memo and in insight 3 I substitute "ten-token" — or better, drop the count entirely, since the count does no work and the exact string does all of it.** In the committed `note` it can only be **withdrawn on the record**: **I expressly withdraw the words "eleven-word" from `analytics/fetch-requests/2026-09-21-daily.json`.** The note is not edited; the string quoted beside it is exact and unaffected, and the Group D reasoning does not depend on any count. I note without mitigation that I wrote a number I had not counted, four times, in the same return in which I caught two other arithmetic errors by computing them.

**`0921-N3` — SUSTAINED as filed, and the claim now survives re-measurement.** The officer was right that on a shallow clone `git log` could not support *"in this repository's committed record since 2026-06-26"*; `fe3e9d6` was the boundary and every file appeared as an addition in it. **You have unshallowed the repository — `.git/shallow` is gone and 1,497 commits are reachable — so I re-measured rather than choosing a form of words.** `git log -S"italaw.com/investment-treaties/china-germany-bit-2003"` over full history returns as its **first** introducing commit **`f7460ca`, dated 2026-06-26, "chore: daily council meeting [skip ci]"**. **The commit-history claim is now true and checkable.** The honest statement of what happened: **I asserted it when I had no means to check it, the officer was right to flag it, and it turns out to be true — which is luck, not method.** Preferred form of words: *"first committed to this repository at `f7460ca`, 2026-06-26, verified on unshallowed history 2026-09-21"*, with the 87-day arithmetic and the never-fired limb unchanged as tree-state facts at `256cb6e`.

**`0921-R1` — ACCEPTED, and the officer's reasoning is better than mine.** I withdraw my accounting rule. *"A budget that counts only calls returning rows licenses unlimited failing invocations"* is correct and my rule had that hole in it; the unit is the call. **Correct public figure, both stated: seven invocations, six searches.** I record that the officer finds no overspend because S0 bought nothing and was caused by a brief requiring two parameters the tool will not accept together — but the rule stands against me for next time regardless of today's disposition.

---

## 7. THE REMAINING NON-BLOCKING, ONE LINE EACH — ALL SUSTAINED, ALL VERIFIED BY ME

- **`0921-N2`** — sustained. I confirmed all four divergent cells: `blocked_domains` SOTA **1 line / 2 occurrences**, `allowed_domains` insights **2 / 6** and SOTA **1 / 2**, `indexed title` SOTA **2 / 3**, `path shape` insights **2 / 3**. My table declared no unit. *Cure:* the header line **"unit: lines, `grep -c`"**. I accept the officer's note in my favour — I did name the unit correctly at the §0 `0919-N2` discharge — and it makes the omission worse rather than better, because I knew the rule and applied it in one place only.
- **`0921-N4`** — sustained. Entries 1 and 2 carry no `screened <term:count>` in `V`. *Cure:* Entry 1 — the source is a one-line tool error and admits no screen; I say that expressly rather than leaving a blank. Entry 2 — add `screened: allowed_domains 7 calls / 7 blocks; host derivation over 90 rows; files.pca-cpa.org 1`.
- **`0921-N5`** — sustained. The phrase *"not directly readable (403)"* is verbatim **only** at `STATE_OF_THE_ANSWER.md:891`, and not at `analytics/daily-research/2026-06-26.md:41` or `analytics/insights.jsonl:10`, which are the two loci a reader checks first. **It is also unattributed as to host, so it cannot be read as evidence about italaw**, and my sentence sits three clauses from the italaw URL. *Cure:* name `:891` as the locus and drop any implication the 403 was italaw's.
- **`0921-N6`** — sustained, and I am glad it was filed even though the claim held. I screened the exact string where the word "only" required the **class predicate**. The officer ran the class screen and it holds. *Cure:* carry the class screen in the sentence. **The screen was owed whether or not it would have passed, and I would not have known which.**
- **`0921-N7`** — sustained. I read `:195-196` and `:199`: `MAX_URLS` caps `targets`, the **requested** list, and the control is prepended **after**. My formula "9 + 1 = 10 ≤ 12" reaches the right answer by the wrong mechanism and would predict a truncation at 12 requested rows that would not occur. *Cure:* state the cap as applying to requested rows only.
- **`0921-N8`** — sustained. I checked the committed note: Group C enumerates `C-ok`, `C-refused`, `C-robots_disallowed` and **zero occurrences of `C-no_contact`**, where Group A enumerates all four. **Since the note is fired it can only be supplemented on the record, so: the Group C reading for `no_contact` is a transport failure at our end, the origin was never asked, and it records nothing about the resource — the same bar as `A-no_contact`.** It did not arise; row 6 refused at 403.
- **`0921-N9`** — sustained; answered at §2 above. `STATE_OF_THE_ANSWER.md:9` is the live sentence.
- **`0921-N10`** — not mine, but I confirm my own exposure is nil: every commit I cite (`256cb6e`, `798c0f7`, `8b05d3c`) is an ancestor of the tip and resolves.

---

## 8. TWO INSIGHT AMENDMENTS THE BATCH NOW REQUIRES, OFFERED UNPROMPTED

**Insight 3** should be re-filed with its prediction discharged by retrieval rather than left as an inference. The operative addition: *"CONFIRMED BY RETRIEVAL, NOT INFERENCE — relay run 35596605671, control_ok true, both example.com brackets on sha256 ff67a9d764d6… with the closing bracket find_matched true: `italaw6187(14).pdf` and `italaw6187(6).pdf` returned the IDENTICAL sha256 c468bc2e5bda3fd151a4674ffba51a6541ec51f1b3fa5de2da1c3699b8f70e7a at the identical 1,503,182 bytes. ONE BYTE-STREAM IS SERVED AT TWO PATHS — the pre-registered 'IDENTICAL' branch. The engine title does not individuate the file, and this is now carried by bytes. IT STILL IDENTIFIES NOTHING AS NAFTA: the excerpt is empty because the content type is application/pdf, exactly as pre-registered, and no seat has read a byte of it."* Confidence **0.9 → 0.95**. The verbatim quote for the new span, tested as a literal substring of `analytics/fetch-results/2026-09-21-daily.json` (2 occurrences, rows 7 and 8): `"sha256": "c468bc2e5bda3fd151a4674ffba51a6541ec51f1b3fa5de2da1c3699b8f70e7a"`.

**Insight 4**'s weaker limb should record that its pre-registered prior fired: the `/investment-treaties/` shape **refused 6 of 6 rows across 3 distinct URLs**, including the China–Germany locator that had sat in our record for 87 days. **The 47-day-late row was fired and was refused. That is a tool-layer outcome and says nothing about what italaw serves** — but the limb of the insight that mattered is not weakened by it: **we still did not know, and we still do not, because we never asked until today.** Confidence unchanged at 0.85.

---

## 9. AGAINST MYSELF, BEYOND THE OBJECTIONS

**The transferable diagnosis is the same one I named yesterday and committed again today, and naming it a third time is worth nothing unless a procedure changes.** `0921-B3` is the clearest instance: I read my screen output once, against one question — *is insight 4 a duplicate?* — got the right answer, and then wrote a **characterisation** of the eight lines from the answer instead of from the lines. `0921-B2` is the same mechanism one step earlier: I wrote referent clauses for the hits that bore on the question I was asking and stopped at the column boundary, which is why the sentence I was proposing to re-scope sat in my own table unread. **`0921-B1` is the same failure in a third place:** I reproduced quotes from what I had read rather than from what the file says, because I already knew the facts were true.

**Three defects, one mechanism: I read the artefact against my proposition instead of reading the artefact.** Yesterday I wrote *"I proposed a name and no procedure. The pattern does not care that you named it."* It still does not.

**So here is a procedure rather than a name, and it is mechanical enough to be checked by someone other than me.** Before filing: (1) every string inside a `supporting_quote` or a `Q` mark is pasted back into the named file and the occurrence count is printed — **I did exactly this today, for the four re-cut quotes above, and it takes under a minute**; (2) every nonzero cell in a dedup table gets its lines **enumerated by locator with their headline**, never characterised, per taxonomy 39's existing countermeasure; (3) every count in prose is either computed in the session or deleted — **which would have caught "eleven-word", the one figure today I felt instead of measuring.**

**What I will not claim:** that this session's return was good because its figures were exact. Sixteen figures were re-derived exact and the return still carried five blocking defects, because **the figures were the part I computed and the sentences were the part I wrote.** That is the same split I reported against myself yesterday — *"the marks held every time and the prose failed every time"* — and it has now been true on two consecutive days, which makes it a property of this seat and not of a day.

**One thing I will defend, because conceding everything is its own kind of dishonesty.** The batch design was right and the pre-registration held: the prior was stated against interest before firing and was correct on both limbs; the pairing discipline made row 9 interpretable; the Group D pair was built to discriminate either way and did; and the closure-condition ambiguity was recorded **before** the rows returned, so the null was graded against a reading nobody chose afterwards. **The day's finding is a null, it is recorded as a null, and the instrument that produced it is now checkable by a second seat. That part worked.**

---

*End of the research analyst's verbatim answer.*
