# Vault session — 2026-08-08 (reconciling the vault with the master-prompt repair)

**Seat:** Obsidian archivist, sixth deployment.
**Audited against `2686422`** plus the **uncommitted working tree** of the master-prompt repair
session, on branch `fix/restore-council-label`. Said in those words throughout: most of what
this record describes is not committed, so it is cited by file path and branch, never by a hash
it does not have.
**Not done, by instruction:** no commit, no push, no PR, no branch change. No edit to
`lit-review/`, `working/`, `METHODOLOGY.md`, `README.md`, `src/`, `scripts/`, `tests/`,
`digests/`, `docs/`, or the day's analytics artifacts — those are other seats' finished work.
Everything below is either a measurement of the tree as it stands or an edit to a note this
seat owns.

Live checks run this session, all against the working tree:
`pytest` **414 passed / 5 xfailed** · `scripts/check_models.py` **exit 0, 12 cards** ·
`scripts/check_claims.py` **exit 0, 13 facts / 29 restatements** ·
`scripts/build_graph.py --dry-run` **122 notes, 236 edges, 0 orphans, 7 WARNs, 12 planned
edits** before the edits below, **123 / 251 / 0 / 8 / 13** after (write mode not run — see §4) ·
`scripts/check_currency.py` **3 failures → 1**.

---

## 0 — What this session changed, in one place

| File | Change |
|---|---|
| `agents/Claim Map.md` | Snapshot anchor moved to `2686422` + branch. **Every row re-pointed.** **C15 added.** C1, C2, C4, C5, C6, C8, C12, C13, C14 recorded closed or converged, tables retained. Maintenance contract gains rules 6 and 7 |
| `agents/Agent Registry.md` | Re-audited: **no roster change**, measured not assumed. Expected-WARN set restated (seven before this session's edits, **eight** after, both given). Managed-block backlog restated at **thirteen**. `Project Machinery` broken link recorded gone. Four adopted method rules added. Zero-cost escalation recorded |
| `agents/Workflow Threads.md` | Snapshot refreshed. **B5–B8 and C11–C14 added. D1 rewritten.** Nothing closed on a report's authority |
| `agents/Project Change Log.md` | **2026-08-08 section added. 2026-08-07 section added — it was missing entirely.** Open drift: one item struck as closed three days late, one added, one extended |
| `agents/systems-designer.md` | Machine row **7 → 9** (four days stale). Zero-cost constraint corrected and escalated. Full 2026-08-08 build entry |
| `agents/research-analyst.md` | Vanda closure on slip-op-only citations; two claim classes kept withdrawn; two Kim readings corrected |
| `agents/integrity-officer.md` | The 5-of-7 fail-closed rejection rate; the ring-at-25 substantive review; what was *not* converted |
| `agents/council-chairman.md` | A ruling from that seat overcounted by one, corrected from the files; session continuity |
| `agents/site-experience.md` | Four queued items; one of them found already closed and recorded on the file's authority |
| `agents/obsidian-archivist.md` | Own change-log entry and the audit slice below |
| `moc/Workflow.md` | Instrument map added; the fill suspension flagged at the hub |
| `moc/Evidence Ledger.md` | Locked set, retrospective audit, and two standing cautions on quoting numbers |
| `HANDOFF.md` | Pointer to this record and to the threads register, plus the one item the day's checkpoint does not carry (C15) |

Nothing else in the vault was touched.

**Two guards went from red to green as a side effect, which is the useful way to know the work
landed.** `scripts/check_currency.py` reported **three** failures at the start of this session
— [[Claim Map]] STALE against `c9050e6` by 24 commits, [[Project Change Log]] STALE against
`eac8ed9` by 8, and `STATE_OF_THE_ANSWER.md` with no anchor at all. Both index notes are now
anchored to `2686422` + branch and pass. **One failure remains and it is deliberately left:**
see §5 and [[Workflow Threads]] D3.

**And one defect this seat created and caught in the same pass, recorded because the lesson
generalises.** A first draft of the WARN table in [[Agent Registry]] wrote wiki-link syntax
inside a backtick code span in order to name what was being counted. The next
`build_graph --dry-run` reported a **new broken link** from that note to a note named `…`:
`scripts/build_graph.py` scans link syntax **inside inline code spans**, stripping fenced
blocks but not backticks. **You cannot quote link syntax anywhere in this vault without minting
a broken link.** Rewritten in words; recorded in the registry as a sibling of the open C8
defect.

---

## 1 — The audit slice: seat notes against the tree, not the definitions

**The currency query passed and three seat notes were wrong anyway.** That is the finding, and
it is against this seat's method rather than against the notes.

`git log 373cce6..HEAD -- .claude/agents/ prompts/` returns **no commit**; `git status --short`
shows **no modified file** under either path. No agent's model, prompt binding or contract
changed on 2026-08-07 or 2026-08-08 — a genuine, useful result, and the reason the roster
needed no edit. But:

- **[[systems-designer]] placed its own flowchart box at machine row 7.** It has been at
  **row 9** since manifest v2.2, dated 2026-08-03 in `workflow.json`'s own `meta.note`, when
  the fetch relay took rows 7–8. [[Agent Registry]] recorded the move on 2026-08-04. **The
  roster and the seat note therefore disagreed about where the seat sits for four days**, and
  the maintenance rule written to prevent exactly that binds only when a *definition* changes.
  A manifest is not a definition.
- **The same note bound the seat to a zero-cost constraint** that the seat's own code reading
  disproved the same morning.
- **[[Claim Map]] carried C13 as an open definitional split for two days after the council
  ruled it**, under a heading that invited a reader to think the question was live.

None of the three lives in `.claude/agents/` or `prompts/`. The query cannot see them by
construction. [[Workflow Threads]] **D1** is rewritten around the corrected statement — *vault
notes lag the tree, not the definitions* — with a second per-session query specified: for each
seat note, re-read the non-definition artifacts it asserts facts about (its manifest row, the
guards it claims exist, the constraints it claims to be bound by).

**Own drift fixed.** The registry claimed four `build_graph` WARNs and one broken link from
this seat's note to a nonexistent `Project Machinery`. Re-measured: **seven** WARNs before this
session's edits and **eight** after, and the broken link is **gone**. Four of the eight now
name per-agent notes, which retires the 2026-07-31 rule that any per-agent WARN is drift; it is
replaced by a named expected set with both the pre- and post-edit counts, so the ninth WARN
will be legible as new rather than lost in a known-noisy report. Both figures are recorded
because a single post-edit measurement would attribute this seat's own additions to the rest of
the vault.

---

## 2 — The one new defect of the day, and it is large

**[[Claim Map]] C15 — the fill-toward-six rule is suspended in code and stated as operative in
eight other places.**

`FILL_FLOOR_SUSPENDED` defaults ON (`src/config.py:31-39`). Items scoring 25–39 no longer
surface; a cycle with nothing at or above 40 sends a status note whose single authoritative
wording is `src/render.py:59`, asserted verbatim by `tests/test_pipeline.py:951`.

**The numbers did not move.** `RELEVANCE_FLOOR` is still 25 and `MIN_DIGEST_ITEMS` still 6, so
`scripts/check_claims.py` passes, every numeric sweep passes, and every sentence describing
*behaviour* is wrong. There is no mechanical check for this class.

Still stating the old behaviour: `METHODOLOGY.md:49` · `README.md:79-86` ·
`HANDOFF.md:99-101` · `fingerprint.yaml:4-6` · `scripts/site_templates/index.html.j2:186`
(**the public homepage**) · the `quality-bar` card in `views/isds-workflow-3d/workflow.json`
(twice over — it is also still citing the wrong file for the threshold) · `src/main.py:497-500`
(a code comment naming a "never-empty rule" and quoting text the same function no longer
emits).

**The sharpest instance is inside one file.** `METHODOLOGY.md:49` says the digest is filled
toward six. `METHODOLOGY.md:67` says that rule is suspended. Eighteen lines apart, in the
document that goes to Dr. Benavides. This is the same shape as the C6 defect the external
reviewer found in August — a contradiction a reader finds without leaving the page — and it was
created by an addition that was otherwise scrupulous about saying what it had and had not done.

Owners are split three ways (Emory, systems-designer, site-experience), which is why it is
recorded as a coordinated change set rather than assigned. **There is no partial version of
this fix that is not a relocation of the contradiction.**

---

## 3 — What was verified closed, and what was refused

**Closed, each re-read in the file rather than taken from a report:** the Ring 3
dimension/disposition split (thirteen of fifteen Definition-B statements gone; the two
survivors are true of their own subject matter); the Apotex caption, repaired at `373cce6` by
the metrics-neutral option and disclosed by *amendment* rather than rewrite, which is what
preserved the nine declared mirrors pointing into `METHODOLOGY.md:52`; the homepage's "never
empty" promise; the digest index's "fell from 78 to 13 as deduplication matured"; the
`fingerprint.yaml` 30-vs-18 subtotal conflict; the `HUMAN_REVIEW.md` intra-file contradiction
and the site's universal safeguard claim (`33861fd`); and the `digest.html.j2:120` overclaim,
which the day's handoff still lists as open and which the file shows repaired.

**Refused.** Nothing PENDING or externally gated was converted to verified. The eleven open
rows of `analytics/locked_set/RETRIEVAL_LEDGER.md` are recorded as QUEUED or BLOCKED exactly as
the ledger states them; the *Saluka* and *Bovine Hides* "extracted and verified" claims stay
withdrawn; the Vanda closure is recorded on **slip-op-only** citations with reporter page pins
and appellate status expressly excluded and the Federal Circuit docket still QUEUED. One
correction I made mid-pass and am recording because it is the same failure in miniature: I
wrote that `METHODOLOGY.md:71` had introduced a false "no truncated stems" claim, then checked
`fingerprint.yaml` and found **zero** trailing-hyphen phrases. The sentence is true and the
row was stale. The claim was withdrawn before it reached the map.

---

## 4 — Could not do, and why

- **`build_graph` write mode was not run.** Twelve notes await a managed block — one more than
  on 2026-08-04, and now including **`agents/Claim Map.md`**, which has been in the vault since
  2026-08-04 and has never carried one, so the map is invisible to the graph. Four of the twelve
  planned files are this session's out-of-bounds substantive work, and the script is whole-vault
  with no path filter. **A managed block is never hand-written**, so no lawful partial run
  exists. The full list is recorded in [[Agent Registry]] so it is not lost a third time.
- **`.claude/agents/systems-designer.md:17`** still binds the seat to a zero-cost constraint.
  Contract edit; Emory's.
- **The `quality-bar` card** is wrong about a file *and* about behaviour. Manifest edit;
  systems-designer on Emory's go-ahead; never a hand-edit of the generated chart.
- **`docs/` lags four separate claims** — the Apotex caption (source correct since
  2026-08-06, wrong on the live site for two days), the uncorrected Gazprom annotation, the
  §VI.B and §IX additions, and the eight archive corrections. `scripts/check_site_sync.py`
  correctly reports the gap. Expected while the session is uncommitted; **it becomes drift the
  moment the session is committed without a rebuild.**

---

## 5 — For Emory

**AWAITING EMORY — the merge-or-skip decision expires on a cron.** The whole repair session is
uncommitted and `.github/workflows/weekly.yml` fires `0 13 * * 1`. Merge, and Monday is the
first run under the suspended fill — on the archive's evidence a status-only cycle, so
Dr. Benavides receives the new wording with no prior notice. Skip, and Monday publishes under
the old fill rule, sending sub-40 items in the very week the project concluded it should stop.
**A third option is not in the handoff note:** merge, and disable Monday's run for one week, so
the first status-only email is sent deliberately rather than by cron. Recorded, not
recommended. ([[Workflow Threads]] C12.)

**ACTION — three fail-closed guards are not wired to CI.** `check_telemetry_privacy.py`,
`check_seen_integrity.py` and `telemetry_query.py` are untracked files with no workflow entry.
The privacy guard is the only thing standing between the telemetry stream and article text, and
it currently runs when someone remembers. Wiring changes what fails a PR, so it needs your word.
([[Workflow Threads]] C11 / B8.)

**NEEDS YOU — a seat definition binds a constraint the repository has disproved.**
`.claude/agents/systems-designer.md:17` says "zero-cost". `README.md:3-9` now says low-cost,
cents per run. Either strike the word or replace it with the bound you intend. I corrected the
vault note and left the definition alone, because editing `.claude/agents/` is a contract change.

**TO VERIFY — the eight-file fill divergence needs one coordinated pass, and three of the eight
are yours.** `METHODOLOGY.md:49` and `README.md:79-86` are the two that reach a reader.
[[Claim Map]] C15 lists all eight with quoted text and owners.

**TO VERIFY — the living-memory file's own timestamp is two days behind its own content.**
`STATE_OF_THE_ANSWER.md:10` reads "Last updated: 2026-08-06"; `:5` was materially rewritten
today with the Kim corrections. It also carries no `Audited against <sha>` anchor, so it is the
last remaining `check_currency` failure. I did not restamp it: it is the research analyst's
canonical record, it was being edited while I worked, and an archivist asserting a currency it
did not establish is the failure mode this seat is supposed to police. ([[Workflow Threads]] D3.)

**FOR EMORY — the cheapest measurement available is thirteen codings.**
`analytics/retrospective-audit-2026-08-08.md` §6 labels the 13 distinct published items from
their source pages. It moves no holdout label, so it cannot disturb C1, and it measures the real
production distribution — which the 20-item holdout, evaluated at a threshold that has never
fired in 347 screenings, does not. Ten of the 13 are paywalled, so expect ~3–4 informative
labels and report them with that limit stated, or not at all.

---

## 6 — Could not verify

- **Who did what.** The repository does not carry per-seat authorship for the day's artifacts;
  `working/benavides-comment-replies-2026-08-08.md` is written in the first person and names no
  seat. Attribution in the seat notes follows the session brief and cites the **artifact path**,
  not an authorship claim the files support.
- **Whether the nine site-named sources map one-to-one onto the nine modules in
  `src/sources/`.** Both counts are nine and both were read; the correspondence was not tested.
  Recorded at [[Claim Map]] C11 as partially verified rather than closed.
- **The five archived entries whose classifier identity is formally ambiguous.** Unrecoverable,
  by the audit's own statement. Not inferred here.
