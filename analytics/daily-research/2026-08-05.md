# Daily Council Meeting — 2026-08-05

**Research question:** When and how is intellectual property — especially trade secrets and pharmaceutical clinical trial data — asserted as a covered investment under an investment treaty, challenged through a governmental regulatory or judicial measure, and disposed of at the jurisdictional or admissibility stage?

**Procedural caveats (chairman).** (1) This session runs delegated in the scheduled cloud environment: I direct, and every member section below is what that member's real subagent returned, relayed verbatim. No member's voice is performed. (2) Model directive: per the operator's directive of 2026-08-03, the chairman and the research analyst run on Claude Opus 5 (both top seats moved from Fable 5 when the Fable credit balance was exhausted); the integrity, analytics and systems seats are spawned from their committed definitions under the harness's `opus` alias, which the operator's directive intends as Claude Opus 4.8. As recorded on 2026-08-03 and 2026-08-04, the exact version that alias resolves to in this cloud harness is not independently verifiable in-session; per the never-substitute-silently rule that uncertainty is recorded here rather than papered over. (3) No record for 2026-08-05 existed on `main` when this session opened, so the duplicate guard did not fire and this is the day's only meeting.

---

## Part 1 — Chairman's Agenda

### The instrument check, before the agenda

Per the delegation rule adopted 2026-07-31 and amended 2026-08-03 — *probe the instruments before writing the agenda; agenda scope follows the instruments actually alive* — I ran both probes at session open, before setting any task.

**Direct fetch: dead, both layers, unchanged for the fourth consecutive session.**

- Proxy egress (curl), neutral control hosts only: `https://example.com/` and `https://www.iana.org/` both returned `curl: (56) CONNECT tunnel failed, response 403`. The gateway refused the tunnel; no origin was contacted.
- Fetch service, neutral control: `WebFetch` on `https://example.com/` returned HTTP 403, response body not retrieved.

Both probes used neutral controls, not research hosts. Neither result is — and neither may be read as — information about any research resource. This is the standing tool-status-as-source-state rule.

**Indirect retrieval: alive, and it is the day's working instrument.** The fetch relay merged 2026-08-03 (`.github/workflows/fetch-relay.yml` + `scripts/fetch_relay.py`) is the committed relay mechanism this session's brief tells me to look for, so no fetch-dependent item is stranded today and none is written up as stranded. I fired today's batch at session open, before writing this agenda, so it would run while the council worked: `analytics/fetch-requests/2026-08-05-daily.json`, four rows.

It returned before Part 2 was seated. **Run 31001060670, `fetched_at` 2026-08-05T11:20:54Z, `control_ok: true`, `void: false`, 4 requested / 4 completed, `truncated: false`.** The three relay disciplines adopted 2026-08-04 are binding on every reading of it: every batch carries the neutral control and a failed control voids the batch entire; every requested URL gets a row, and `no_contact` is never recorded as an origin answer; the relay is a retrieval channel for what the record already identified, never a discovery channel (METHODOLOGY Part VIII), which its host allowlist enforces.

To that list today adds a fourth discipline, which I state in the agenda because the batch that just landed demonstrates it and the analyst must work to it. The 2026-08-04 rule was *never choose as `find` a string the target page can generate from your own request.* Today's `Mapped by` row shows the adjacent failure: `excerpt_of` in `scripts/fetch_relay.py` returns the window around the **first** case-insensitive match in the tag-stripped body, so a find string that also occurs in page boilerplate returns the boilerplate and not the structural label that was wanted. That is not a null, and it must not be recorded as one. It is a **mis-anchored row** — the question was not put to the page. I want that distinction drawn explicitly in the memo.

### The item I am not letting pass a fifth day

`agents/Workflow Threads.md` and `state/research_log.json` both carry the ASIL ILIB reconciliation, opened 2026-08-01, whose recorded next action is to reconcile "from the officer's saved raw HTML." I greped `analytics/` for that artefact before writing this agenda and did not find it. If it is genuinely not in the repository, then the thread's stated method has been un-executable since the session that closed on 2026-08-01, and `asil.org` is not on the relay allowlist, so it is not relay-reachable either. That would make it the same failure this council has been fighting all week in yet another costume: **a next action that names an artefact which never reached the record.** I am not asserting it — I have been wrong about the record's own contents twice this week and the countermeasure I adopted yesterday was to stop doing exactly that. So it goes to the analyst as a question to verify, not to me as a finding to announce.

### Tasks and budgets

**Task 1 — A/81/17, day 5 (analyst; relay-sourced; small).** Report the day-5 result in the standard same-page positive-control form, from the batch above. The `undocs.org`/`docs.un.org` symbol route was retired 2026-08-04 and is not used; do not revive it. Standing rule: **no search-synthesis figures** — a search result set is evidence about an index, never about whether a document exists. Note for your reading, not as my conclusion: the Commission Sessions page digest in today's batch appears to repeat the value the record already carries from 08-03 and 08-04. Say precisely what that does and does not add.

**Task 2 — Mapper attribution and vintage for UNCTAD treaty 978 (analyst; relay-sourced; primary).** This was next step 2 from yesterday, at low priority, and today's row did not answer it for the reason given above. Two things are wanted, in this order: (a) state plainly what the row does and does not establish, and classify it correctly as mis-anchored rather than null; (b) name the anchor that would actually retrieve the per-treaty attribution line, reasoning from `scripts/fetch_relay.py` and from what the record holds about the sibling treaties' attribution format ("Xiamen/La Plata, 03-2013"; "Laval/Paraiba, 10-2016"). If you name a row I will fire a second batch this session. **This calibrates the 2026-08-04 cell; it cannot change it, and it must not be written as though it could.**

**Task 3 — `china-switzerland-bit-2009-forum-provision-text`: is it relay-reachable at all? (analyst; record-and-code only; zero search).** This residual opened yesterday and is unassigned. Before any budget is spent hunting the treaty text, settle the prior question from the allowlist in `scripts/fetch_relay.py` and the reduction the relay actually produces: can an allowlisted host deliver this instrument's text through this channel, and what would the reduction look like if the text is a PDF? A reasoned "no, and here is why" is a full answer to this task and I will take it as one. Do not speculatively search.

**Task 4 — ASIL ILIB reconciliation: executable or not? (analyst; record-only; bounded).** Verify the question in the section above from the repository's own contents. If the artefact is absent, say so with the grep you ran, and state what would make the thread executable rather than leaving it to be re-inherited a fifth time. This is record hygiene, and record hygiene is in scope on a day whose retrieval questions are small.

**Zero budget, unchanged and not re-argued:** the China–France Protocol full text; the `first-application tracker` (its first cycle is late August, and it was explicitly not searched on adoption day); any ledger amendment; and every pending operator approval — the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix. **Nothing on that list is built or applied today.** Approvals are pending, not granted.

### Member return-path protocol

Each member is spawned as a real subagent from its committed definition in `.claude/agents/`. A member's returned text enters this record **verbatim**, under its own heading; I do not write, summarise, or improve any member's contribution. If a spawn fails or a member returns nothing usable, that seat is recorded as **PROCEDURAL FAILURE** for the day and the record says so — the seat is never performed.

### Spend checkpoint

A checkpoint falls **after the vetting round (Part 3)**: Parts 1–3 are committed and pushed before the analytics seat is convened, so the session's substantive work is never more than one commit from safe. This discipline saved the 08-03 record and it holds today.

### Order of business

Part 2 analyst → Part 3 integrity officer vets the analyst's return (positive control before any HTTP-status objection; raw source for any quote claim; **grep the record before asserting something is absent from it**) → **spend checkpoint** → Part 4 analytics officer, short if the digest of record is unchanged, which on my reading it is → Part 5 systems researcher only if its queue has ungated work → Part 6 my close-out: rulings on every objection, accountability, next steps, escalations to Emory.
