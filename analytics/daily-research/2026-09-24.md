# ISDS Thematic Watcher — daily council meeting, 2026-09-24

**Chair:** council chairman (this session, `claude-opus-5` per the operator's model directive of
2026-08-03). **Convened seat:** `research-analyst`, and it is the only one — a REAL subagent spawned
with the Agent tool against its committed definition in `.claude/agents/`. No seat is performed.
Member text is filed VERBATIM and marked as such.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

The analyst is spawned with the Agent tool against its committed definition, is given a bounded task
list, and returns a finished memo as its final message. I paste that memo into this record unaltered,
inside a fenced block, under its own heading. **I write no member's contribution.** If the spawn fails
or the member does not return, that seat is recorded as PROCEDURAL FAILURE for the day, stated in my
close-out and in my final summary, and it is never covered by my writing the part myself. The member
does not commit; it hands me file bodies and I commit them, so every artefact in the day's diff has one
author of record.

### 1.2 Seat count — Rule 0 is followed literally today, for the first time in twenty-one sessions

My convening instruction names four seats to spawn: `research-analyst`, `integrity-officer`,
`analytics-officer`, and `systems-researcher`. **`prompts/daily_council_protocol.md` Rule 0 overrides
it**, which is what that file's own Precedence section requires, and which requires this sentence.

- **`analytics-officer` is NOT convened.** The digest line at §1.4 is copied by me from `meta.json`.
- **`systems-researcher` is NOT convened.** The one optimization item is one short paragraph of mine.
- **`integrity-officer` is NOT convened, and that is today's change.** For twenty consecutive sessions
  this council convened it anyway and declared a Rule 0 departure on the chairman's own authority.
  Yesterday my predecessor put that question to the officer itself, sustained its answer in full, and
  ruled that the twentieth was the last such departure (2026-09-23 §6.5). **I am bound by that ruling
  and I do not reopen it.** Reopening it would be a chairman restoring, on his own authority, a
  departure his own seat had just given up — in the direction that suits the chair. The integrity
  challenge is discharged today by me, in my own name, at Part 4. Under Rule 0 that is not performing
  a member: Rule 0's operative move is that the integrity officer **is not a member of the daily
  meeting**, so there is no member's voice to perform.
- **Escalation 11 stays open to Emory** — amend Rule 0 or keep it — and nothing I did today settles it.
  The cost recorded yesterday runs the other way and stands unaltered: the officer's return on
  2026-09-23 caught four wrong numbers of the chairman's under a correct mechanism, and that session's
  chairman did not believe his own self-challenge would have found them. **Today's record is the first
  evidence in the other direction and it is a sample of one.** I say that here rather than at the end,
  because a reader deciding escalation 11 should know that today's compliance is not a demonstration
  that the rule is right.

The operator's verbatim correction behind Rule 0: *"the daily council meetings are meant to be the
researcher + the chairman + one optimization suggestion or whatever."*

**The sections Rule 0 bans by name are gone from this record**, per yesterday's §6.5(3): no
instrument-probe table, no "Priority focus" section, no "Budgets" section, no carrying-span ledger with
per-term occurrence counts, no "Verify / be skeptical" section, no "Repository facts" section. The
analyst still receives bounded budgets — it is the *section* that is banned, not the discipline — and
what it was asked to do is stated in prose at §1.5.

### 1.3 Channel state — probed at session open, on neutral controls, nothing inherited

I inherited no channel status and re-probed rather than re-derived. **Both probes fired at
`https://example.com`, a neutral control, never at a research host,** so nothing in this section can be
read as a statement about a source. This is the standing tool-status-as-source-state rule and it binds
the whole record.

Direct `curl` returned `curl: (56) CONNECT tunnel failed, response 403`; the fetch service returned
`{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`. The proxy's own status endpoint corroborates
the first at the gateway rather than at the client, logging `connect_rejected` — *"gateway answered 403
to CONNECT (policy denial or upstream failure)"* — at `2026-09-24T11:10:24.024Z`. **NO STATE CHANGE.**
Direct fetch is dead on both channels for this session class, as it has been since 2026-08-02. What that
means is narrow and I state it plainly: *this session* cannot reach those hosts. It means nothing
whatever about whether any host is serving anything.

**Three channels are alive and no item may be called stranded on fetch-layer grounds.** The web-search
track works. The committed relay works: `.github/workflows/fetch-relay.yml` fires on any push touching
`analytics/fetch-requests/**`, runs `scripts/fetch_relay.py` on a GitHub runner that does have egress,
and commits a reduction — status, final URL, content-type, byte length, sha256, timestamp, user-agent
and one excerpt capped at 400 characters — back to `analytics/fetch-results/`; the document itself never
travels, and that path is inside this session's merge scope. And the GitHub API is reachable from this
session class, as yesterday's §6.6 established against a chairman who had written it off without probing
it.

**The clone arrived shallow and I unshallowed it before reading any history**, per the rule adopted
yesterday (§6.12(6)): `git rev-parse --is-shallow-repository` returned `true` at 120 commits, and `1562`
after `git fetch --unshallow`. Every statement in this record that rests on `git log` rests on the full
history.

### 1.4 Digest of record — UNCHANGED (Rule 1 protocol sentence, copied from `meta.json`)

`Digest 2026-09-21: 21 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied verbatim from `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`: `"screened": 21`,
`"matches": 0`, `"watch_list_leads": 0`. **This is the same digest of record the council met on for the
third day running.** `weekly.yml` fires Mondays at 13:00 UTC; the 2026-09-21 run is the newest and today
is Thursday, so nothing new was due. The digest is not evidence of a quiet field: its own
`health_warnings` carries a SOURCE DEGRADATION WARNING for `gdelt` (five consecutive zero runs), and the
council's escalation 7 — a falsified reason in `specs/iisd_itn.yaml:24` — is still open.

### 1.5 What this session inherits, and the work I set

Yesterday's close-out retired a plan. The gap `nafta-chapter-11-allowlisted-host-visible-prose-untested`
was shown to be closable by no batch this council can fire at any budget, because the nineteen hosts the
relay is permitted to read are arbitral institutions and document repositories while the institutions
that publish treaty text as web pages are trade ministries and secretariats. Yesterday's next steps put
that in terms: **do not propose gap-closing rows, and do not spend a session hunting locators for it.**
That instruction frees this session's entire budget, and the honest question is what to spend it on.

I spend it on the research question, which has not moved for twelve consecutive sessions and which
yesterday's record states was not searched for at all. Three inherited facts set the work:

1. **Open thread 11 carries the only standing observation in the whole thread set that is
   simultaneously on the research question, at an allowlisted host, and reachable by a live
   instrument.** Its closure condition, adopted 2026-09-11, asks whether a codified manifest-lack-of-merit
   gate has ever disposed of a claim on the ground of whether an IP right is a covered investment — which
   is Ring 3 doctrine crossed with Ring 1 subject-matter, i.e. the research question itself rather than a
   fact about our own plumbing. Its standing observation is one relay row at
   `https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit`, against
   an officer-verified 2026-07-30 baseline. **It last fired on 2026-09-13. That is eleven days, and the
   thread's own adopted cadence is one row per cycle.** Re-establishing it is the first thing I asked for.
2. **The council has been auditing its instrument and not its own cadence.** Several open threads carry
   adopted standing observations with stated cadences — thread 11's per-cycle row, thread 13's weekly
   relay row on the Huawei ARB/22/2 docket, thread 10's monthly Wingtech watch. Nothing in this repository
   checks whether any of them fired. A lapsed standing observation is indistinguishable, in the record,
   from a null result — and the difference matters, because one is evidence about the world and the other
   is evidence about us. That reconciliation is pure file work at zero fetch cost and I asked for it second.
3. **The search track is alive and has not been pointed at the research question in twelve sessions.**
   Every search budget in that span went to locator discovery for the NAFTA gap. With that plan retired,
   the budget goes back to the question, bounded and with the standing prohibitions intact.

I asked the analyst for four things, in that order of priority, and told it to leave budget unspent
rather than manufacture a result: re-establish thread 11's standing observation and pre-register a single
relay row for it; reconcile every standing observation in the open-thread set against when it last
actually fired; spend at most four bounded web searches on the research question, recorded to the
committed `analytics/search-results/` artefact; and answer one drafting question of mine. The standing
prohibitions were restated to it in full, including that no seat may describe NAFTA Article 1110's
internal structure, that no locator hunt for the retired gap is to be undertaken, and that the carried
zero-budget items are released by an operator ruling and by nothing else.

### 1.6 Duplicate guard

`analytics/daily-research/2026-09-24.md` did not exist at session open. I checked the working tree and
`git log origin/main` for that path; the newest council record on `main` is `2026-09-23.md`, merged as
#214 with a post-merge addendum as #215. No manual session had convened today. This is the day's only
meeting and it is not a duplicate.

### 1.5a AMENDMENT TO §1.5(1), MADE AFTER THE ANALYST'S RETURN AND MARKED AS ONE

§1.5(1) above says the thread-11 standing observation *"last fired on 2026-09-13. That is eleven days,
and the thread's own adopted cadence is one row per cycle."* **The eleven days are exact. The
implication a reader would draw from them — that the observation is overdue — is wrong, and the
analyst demonstrated it against my own agenda.** The only place this repository ever fixes the unit
for this tracker is `analytics/optimization-log.md:65`, where the adoption of 2026-07-31 reads *"monthly
cadence,"* corroborated at `agents/Workflow Threads.md:1558`. Eleven days against a monthly cycle is
not a lapse. **I leave the sentence standing rather than editing it away**, because a Part 1 that
quietly matched the Part 3 finding would hide that the day's agenda was built on an unexamined premise.
My ruling is at §3.1 and §6.2.

---

## 2. Research analyst — filed VERBATIM

The text below is the analyst's return exactly as it was handed to me. I have altered nothing in it,
added nothing to it and removed nothing from it. Its headings are its own and its section numbering is
its own; where it says "you" it is addressing me.

---

# RESEARCH ANALYST — RETURN FOR THE DAILY COUNCIL OF 2026-09-24

## 0. What I read, what I spent, what I did not do

**Read in full:** `prompts/research_analyst.txt`, `prompts/council_calibration.md`, `prompts/daily_council_protocol.md`, `prompts/carrying_span_rule.md`; `analytics/daily-research/2026-09-24.md` (Part 1, all of it); `analytics/daily-research/2026-09-23.md` §6.12 and §6.13; open threads 0, 2, 10, 11, 12, 13, 25, 29 of `state/research_log.json` (raw JSON values, not prints); `analytics/optimization-log.md`; the relevant sections of `analytics/daily-research/2026-08-20.md`, `2026-08-21.md`, `2026-08-22.md`, `2026-08-23.md`, `2026-09-11.md`, `2026-09-12.md`, `2026-09-13.md`; `scripts/fetch_relay.py:45-160`; `src/classify.py:145-185, 845-865`; `analytics/search-results/2026-09-20.json` and `2026-09-22.json` for the artefact schema; `analytics/insights.jsonl` in full by machine screen, and entries 3, 81, 90, 164, 166, 199 and 200 in full text.

**Spent:** 4 of 4 permitted WebSearch calls (B3). 0 relay rows fired — I propose one and fire none; you commit. 0 searches on B1, B2 or B4, as instructed.

**Did not do, deliberately:** I did not re-probe any channel and I state nothing about any host's availability. I hunted no locators for `nafta-chapter-11-allowlisted-host-visible-prose-untested` and propose no gap-closing row for it. I wrote nothing about NAFTA Article 1110's internal structure. I cite no inventory figure at all. I touched none of the zero-budget carried items. I wrote no `daily-council` stub and committed nothing. I confirmed `git rev-parse --is-shallow-repository` returns `false` at 1563 commits **before** every statement below that rests on history.

---

## B1 — THREAD 11'S STANDING OBSERVATION

### (a) What this council already holds at that URL — stated, not re-derived

**Method first.** I parsed every file in `analytics/fetch-results/` as JSON and read its `records` list (not `results` — see my errors at §5), then matched rows on **exact URL equality**, not substring. The result:

**43 rows, on 8 dates, all `ok`/200/`text/html`, one page state per date:**

| date | rows | bytes | sha256 (first 16) |
|---|---|---|---|
| 2026-08-19 | 5 | 94,996 | `66a879e013bd8cbf` |
| 2026-08-20 | 6 | 94,995 | `29bd0911889f8b40` |
| 2026-08-21 | 4 | 94,994 | `ebcebcae85945237` |
| 2026-08-22 | 2 | 94,998 | `1f026c81313f76af` |
| 2026-08-23 | 2 | 94,996 | `cbe0d9f1161e209c` |
| 2026-08-24 | 1 | 94,997 | `e162b94509d72d96` |
| 2026-09-12 | 21 | 96,161 | `64eb3f52bfc496cf` |
| 2026-09-13 | 2 | 96,161 | `e1ceead703fcce31` |

**What the 2026-09-12 and 2026-09-13 reductions establish** (I restate rather than re-derive; all of this is already in the record and none of it is mine):

1. ICSID's own register carries a row for *Spentech Engineering Limited v. United Arab Emirates* (ICSID Case No. ARB/24/16), document designation `Award`, date `July 28, 2025`, fourth-column value `Yes`. Two independently centred windows on one page state carry the identical span. (`analytics/daily-research/2026-09-12.md`, blobs `77dc9816` records[10] and `b29bfab` records[1].)
2. The table's four columns are `Case name` · `decision or Award (language(s))` · `date of decision or Award` · `Finding that Claim is Manifestly Without Legal Merit`, established from two literals returning the header at different offsets. (Same record, blob `65549c7` records[2] and records[3].)
3. The page is byte-stable **within** a day across at least 21 fetches in three runs, and its digest changes **between** days at constant byte length — which excludes a per-request or per-session nonce. (`analytics/daily-research/2026-09-13.md:605`.)
4. The `Spentech` window is character-for-character identical across 2026-09-12 and 2026-09-13, so the change between those two page states lies outside that 400-character region. Two windows exclude 0.83% of a 96,161-byte body; nothing stronger was claimed and nothing stronger is available. (`0913-N9`.)

**What they do NOT establish, and the record says so against itself:**

- **The instrument is not named for the Spentech row.** Neighbouring rows name their gate in the document-type cell; the Spentech cell reads only `Award`. Thread 11's closure condition requires the instrument named. It is not named. (`2026-09-12.md`, §(b) point 1.)
- **The ground is not established at all.** `Intellectual` returned `find_matched: false` against a live 200 — and that null is worth nothing, because a register of case names, document designations and dates cannot carry a ground. Grounds live in award bodies. (Same, point 2; and `0913-B3`, which records that exactly one IP literal was ever screened and no synonym tried.)
- **The 2026-09-12 statement that a gate "produced a disposition" is WITHDRAWN** (`0913-B4`). The ARB/24/16 docket records the Rule 41(1) objection **made and contested across at least four rounds** and nowhere states how it was decided. The substitute form, binding: *the gate was invoked and briefed, on a primary source, for the first time in this thread's history.*

**The 2026-07-30 baseline.** `analytics/daily-research/2026-07-30.md:35`: the integrity officer read the table in vetting — *"60+ decisions, 2008–2025, none IP-, trade-secret-, pharmaceutical-, or data-related"* — so the null rests on the authoritative list, not only on search absence. `:37` records the confidence discipline: *"absence is 'not found in accessible sources,' never 'no application exists.'"* A withdrawn FY2025 statistic is barred from that baseline by name.

**One thing I add, and I mark it an INFERENCE where it is one.** On 2026-08-20 the register's terminal row in the reduced stream was *Eurohold Bulgaria AD and Euroins Insurance Group AD v. Romania* (ARB/24/18), and the stream ran straight from its date-and-value into page navigation: `…( English ) September 30, 2025 No In this section Overview Decisions on Manifest La` (`analytics/fetch-results/2026-08-20-daily.json`, records[3], find `ARB/24/`). On 2026-09-12 the same anchor is followed by three further case rows: `…September 30, 2025 No Spentech Engineering Limited v. United Arab Emirates (ICSID Case No. ARB/24/16) Award July 28, 2025 Yes Ricardo Filomeno Duarte Ventura Leitão Machado v. Republic of Angola (ICSID Case No. ARB/24/8) Decision on the Respondent's Rule 41 Objection May 29, 2025 - Samvel Karapetyan v. The French Republi` (blob `77dc9816`, records[10]). **Two observations, each self-contained on its own page state.** I do **not** join them into a claim that ICSID "appended three rows", and I do not characterise the table's ordering or insertion policy — `0820-E4` withdrew exactly that kind of sentence and the withdrawal binds me. What survives is narrow and operational: **the region immediately after the previously-terminal row is where this project's observation of the register now ends, and it is unobserved to its right.**

### (b) PRE-REGISTRATION — written before any row fires

**URL:** `https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit` (host on `ALLOWED_HOSTS`, `scripts/fetch_relay.py:49`).

**`find`: `ARB/25/26`.**

**Why that string and not another.** Three reasons, in order of weight.

1. `excerpt_of` returns `body[idx-100 : idx+300]` (`scripts/fetch_relay.py:134`; the match sits at the 25% point, not the centre, established 2026-09-09 against the docstring). **Three quarters of the window falls to the right of the match.** `ARB/25/26` is the case identifier at the extreme right edge of the last window this project holds — the 2026-09-12 `ARB/24/16` window was cut mid-phrase at `Decision on the Respondent's Rule 41(1) `. Anchoring there spends 300 of 400 characters on ground this council has never seen.
2. **Re-firing `Spentech` would, by construction, produce no new observation of any region.** Same URL, same literal, same first-occurrence region yields the same window — which is precisely the defect `analytics/daily-research/2026-09-13.md:248` names against that session's own row 6. Repeating it would repeat a named failure.
3. `ARB/25/26` is pure ASCII, contains no space, and cannot occur in page chrome: a rigid designator in the carrying-span sense.

**Rejected: an IP literal** (`patent`, `trade secret`, `clinical trial data`). `0913-B3` is right that only one IP literal was ever screened here and no synonym tried. But the 2026-09-12 seat is also right that the null is worth nothing on this surface, and the 2026-08-20 ruling is right that a party-name register carries party names and not subjects. Firing one would spend the council's only substantive row on a predictably worthless null. I would rather leave that defect standing and named than pretend to cure it.

**Predictions, capable of being wrong.**
- **P1** — `ok`, 200, `text/html`, `find_matched: true`. HIGH, ~85%.
- **P2** — the window's opening characters reproduce, character-for-character, the right-edge span of the 2026-09-12 `ARB/24/16` window: `Samvel Karapetyan v. The French Republic (ICSID Case No. ARB/25/26) Decision on the Respondent's Rule 41(1) `. MODERATE, ~65%. This is a **cross-page-state comparison**, and this council's own rule is that nothing transfers between page states — so it is a prediction and never an inference.
- **P3** — conditional on P1, at least 250 characters of the returned window lie beyond the right edge of every window this project has ever held at this URL. HIGH, ~90%.
- **P4** — `bytes != 96161` or `sha256 != e1ceead703fcce31…`. HIGH. **Registered so it is on the record, not so it can be read as movement:** `analytics/insights.jsonl:90` (2026-08-21) establishes that a digest change on this URL is not by itself evidence that the table's contents changed. **The evidential weight of this row is in the excerpt, not in the digest**, and I say so now so that nobody can say it afterwards.

**Named outcome classes, including the ones that mean the instrument failed rather than the source answered.**
- **(A)** `ok`/200/`text/html`/`find_matched: true` — the row worked.
- **(B)** `ok`/200/`find_matched: false` — a real absence from the rendering **or** an undecoded entity or U+200B inside the literal. **Not separable by this row** (G6/N7, proven live on this host).
- **(C)** `ok` but status ≠ 200, or `final_url` ≠ `url` — read `final_url` before any excerpt (G9); the row is about wherever it landed.
- **(D)** `refused` / 403 / 202 / timeout / `no_contact` — **INSTRUMENT FAILURE.** A fact about this runner, this user agent, this path, this timestamp, and nothing whatever about ICSID.
- **(E)** `host_not_allowed` / `scheme_not_https` / `private_address` — a defect in my row, caught by our own `validate()`; nothing about the source.
- **(F)** `control_ok: false` — the batch is **VOID** and no row may be read as information about its resource.
- **(G)** the workflow does not fire, or no reduction lands in `analytics/fetch-results/` — **the standing observation lapses again**, which is a fact about this council and not about the table.

**D, E, F and G are the instrument-failure classes.** None may be written as a finding about ICSID.

### (c) What an `ok` row WOULD and WOULD NOT license — in terms

**Would license:** that this council reached the page; the page's `bytes` and `sha256` at that instant; the returned 400-character window as a span **on that page state**; and, if new case rows appear in it, that ICSID's own register carries those case names, document designations and dates.

**Would NOT license — and this is the whole point.** **It would not close thread 11, on any outcome.** The closure condition adopted 2026-09-11 requires the **body of a primary document** — a tribunal decision or award, **or an ICSID/PCA docket entry naming that document** — carrying **both** (i) the gate, named, and (ii) a stated ground that is whether an IP right is a covered investment. **A table of decisions is a docket-summary surface.** `G16` (`analytics/daily-research/2026-08-25.md`) bars any such row from founding an ordinal, a count, a disposition or a holding; `G10` bars reading a document designation as the tribunal's characterisation. The fourth column is headed *"Finding that Claim is Manifestly Without Legal Merit"* — a register's own designation, not a tribunal's stated ground. Nor would an `ok` row open the **third bucket**, which needs some indication that IP was asserted; and a pharmaceutical-sounding party name would be a party name, not a subject.

**The honest description of this row: it is a change-detector on the population, sited where change would first become visible. It is not, and cannot become, a closure instrument.**

### File body — `analytics/fetch-requests/2026-09-24-daily.json`

One substantive row; the runner prepends its own control. **Full body printed at §7 below** (it is long, and I have kept it in one place so you can paste it without hunting).

---

## B2 — CADENCE RECONCILIATION

### (a) Method, stated before any number

**Enumeration.** Every element of `state/research_log.json.open_threads` (37 entries, all of them strings) screened case-insensitively for `per cycle|each cycle|weekly|monthly|daily|each session|every session|cadence|standing watch|standing observation|recurring`; then `analytics/optimization-log.md`, `agents/Workflow Threads.md`, `HUMAN_REVIEW.md` and `prompts/research_analyst.txt` screened for the same terms plus adoption status.

**Firing measurement, relay track.** Every `analytics/fetch-results/*.json` parsed; rows read from the `records` key; matched on **exact URL equality**. A firing's date is the **result filename's leading ten characters**, never `fetched_at` — `fetched_at` is written once per file after every fetch in the batch completes, so it cannot date a row (gate `G4`, 2026-08-21: *"rows are ordered, not timed"*). The command, so you can re-run it:

```
python3 - <<'EOF'
import json,glob,os,collections
byurl=collections.defaultdict(lambda: collections.Counter())
for f in sorted(glob.glob('analytics/fetch-results/*.json')):
    d=json.load(open(f)); date=os.path.basename(f)[:10]
    for r in d.get('records',[]): byurl[str(r.get('url',''))][date]+=1
EOF
```

**THIS COUNT IS CRUDE, and here is exactly how.** A row's presence proves the runner attempted that URL on that date. It does **not** prove the seat that wrote it intended it as *that thread's* standing observation. I had to separate intents by hand, by reading each row's `find` string — and it mattered: three of thread 10's five relay dates turned out to be rows for a different thread entirely. **What would make it not crude: a `thread_id` field on each request row.** There is none. That is a concrete, near-free instrument proposal and I offer it as this seat's contribution to the optimization item.

**Search track — a hard limit.** `analytics/search-results/` contains **exactly three files** (2026-09-20, -21, -22); the artefact schema was established 2026-09-20. **Every search firing before 2026-09-20 is not auditable from a machine-readable artefact** and can only be read out of the daily records' prose, which is a weaker evidential class. I mark it wherever I rely on it.

### (b) The numbers

**This council has exactly THREE adopted, cadenced standing observations.** (`analytics/optimization-log.md` carries 34 entries at `_Status: proposed._` and 5 at adopted; of the five adopted, **only one** carries a recurring cadence. Several *proposed* entries recite monthly or weekly cadences; none is adopted, so none has a cadence to lapse. I name this so the count is not confused with a count of adopted items.)

---

**1 · Thread 11 — ICSID *Decisions on Manifest Lack of Legal Merit* register (relay).**
URL as above. **Last actually fired: 2026-09-13 — 11 days ago.** Search limb last fired 2026-09-11 (that session's queries 3, 4 and 9) — 13 days ago, read from prose, not from an artefact.

**The cadence is stated twice and the two statements use different units.**
- `analytics/optimization-log.md:65` — *"adopted 2026-07-31 … monthly cadence, analyst executes, next cycle late August 2026."* Corroborated at `agents/Workflow Threads.md:1558`: *"First monthly cycle, **late August 2026**."*
- The closure condition, 2026-09-11 — *"STANDING OBSERVATION, checkable in one relay row per cycle."*

**"Cycle" is not defined anywhere in `prompts/`, `METHODOLOGY.md` or `COUNCIL.md`** — the grep returns nothing. The only place this project ever fixed the unit for *this* tracker is the optimization log, where **a cycle is a month**.

**Verdict: CURRENT.** Eleven days against a monthly cycle is not a lapse. The largest gap in the series is 2026-08-24 → 2026-09-12, **18 days**, still inside a month.

**AND THIS CORRECTS PART 1 OF TODAY'S RECORD.** §1.5(1) writes: *"It last fired on 2026-09-13. That is eleven days, and the thread's own adopted cadence is one row per cycle."* The eleven days are exact. The implication that the observation is therefore overdue is **not supported by any definition of "cycle" this repository contains**, and under the only definition it does contain, the row is not due for another two and a half weeks. **I still recommend firing it, on the merits set out at B1 — but the agenda should not rest on a lapse that cannot be demonstrated.**

---

**2 · Thread 13 — Huawei v Sweden, ICSID ARB/22/2 case-detail page (relay).**
URL: `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/22/2`.
Adopted cadence, verbatim from the thread: *"ADOPTED 2026-08-06: this watch becomes a STANDING WEEKLY RELAY ROW (three rows, one page, two same-page controls); next row 2026-08-13."*
**Fired: 7 rows on 5 dates — 2026-08-06 (3 rows), 2026-09-04 (1), 2026-09-05 (1), 2026-09-06 (1), 2026-09-18 (1). Last fired 2026-09-18 — 6 days ago.**

**Verdict: CURRENT BY ONE DAY** (weekly → next due 2026-09-25). **And the cadence has been honoured almost never since adoption:**
- The named next row, **2026-08-13, never fired.**
- **2026-08-06 → 2026-09-04 is 29 days** — four consecutive scheduled weekly rows missed.
- **2026-09-06 → 2026-09-18 is 12 days** — a fifth miss.
- The adopted **shape** — three rows on one page with two same-page controls — has been honoured on **exactly one date**, 2026-08-06. Every later date carries a single row, so the two same-page controls did not fire on 09-04, 09-05, 09-06 or 09-18.
- **I can find no record suspending it** across the 29-day gap. This is the one adopted observation in the set that has lapsed silently.

**Correction to your list, and it is a real one.** You described thread 13's weekly row as being on the Huawei ARB/22/2 page. That is correct for the 2026-08-06 adoption. But **the thread's own 2026-08-30 update is captioned "(ARB/12/1 Tethyan, not Huawei)"**, and the bulk of this thread's relay work went to **ARB/12/1** — 31 rows on ten consecutive dates, 2026-08-26 to 2026-09-04, and then nothing. **ARB/12/1 last fired 2026-09-04 — 20 days ago — and no cadence was ever adopted for it.** The thread also names Hela Schwarz and Einarsson: **ARB/17/19 last fired 2026-08-24 (31 days ago)**; **UNCT/20/6 last fired 2026-09-07 (17 days ago)**. Neither carries an adopted cadence either. **Three of this thread's four dockets are being watched at no stated cadence at all**, which is not a lapse but is the condition that makes one undetectable.

---

**3 · Thread 10 — Wingtech / Nexperia v Netherlands, ICSID pending-cases watch.**
Adopted cadence: **monthly** — *"Downgraded July 28, 2026 … check monthly"*, corroborated at `agents/Workflow Threads.md:3050`.
Instrument: mixed. It ran as a **search** on 2026-09-08 (recorded in open thread 25) and as **relay rows** on `/cases/recent` and `/cases/pending`.
**Last actually fired on a Wingtech-discriminating literal: 2026-09-10** — `find: "Wingtech"` on `https://icsid.worldbank.org/cases/recent`, `find_matched: false`. **14 days ago.** A weaker proxy, `find: "Netherlands"`, fired 2026-09-09 on both URLs.
**The 2026-09-17 rows on those two URLs are NOT this watch** — their `find` strings are `Subject of Dispute`, `Subject`, `ARB/`, `ICSID`, written for the ICSID subject-of-dispute threads. I separated them by reading the `find` string; a count by URL alone would have scored this watch as current to 2026-09-17 and been wrong by a week.
**Verdict: CURRENT** (14 days against a monthly cadence).

**Two stale pointers found in passing, both of the class this council already banned.** Open thread 25 pins the schedule at `agents/Workflow Threads.md:1901`; **that line today is B8 text about pipeline guards**, and the Wingtech schedule now sits at **line 3050**. And thread 25 addresses thread 10 as `open_threads[5]`, while `Workflow Threads.md:3050` cites it as `open thread 1`. Both are positional, both are wrong at HEAD, and both are the index-addressing defect ruled against on 2026-09-12.

---

**4 · The monitored-author check (Anthea Roberts, `ejiltalk.org/author/aroberts/`).**
Adopted cadence: **each session, one query, before any topical search** (`prompts/research_analyst.txt:158-170`).
**Last actually fired: 2026-09-13** — `analytics/daily-research/2026-09-13.md:389`, query `Anthea Roberts EJIL:Talk September 2026`. **11 days ago.**
**Verdict: NOT LAPSED — SUSPENDED.** It sits at zero search budget under a carried prohibition first recorded at 2026-09-18 §7.5 item 8, restated at `analytics/daily-research/2026-09-19.md:400` (*"Anthea Roberts' author page was not checked and nothing is asserted about it"*), and restated to me today.

---

**5 and 6 · Two more suspended, for completeness.** The **A/81/17 watch** (open thread 12) is a recurring null series of 15 rows, carried at zero budget. **The EU pharma-package RDP watch** (open thread 2) was *"DEMOTED 2026-09-19 to a standing watch at ZERO SEARCH BUDGET"* and **no cadence was ever adopted for it**, so it is **UNAUDITABLE as to cadence** — not because I cannot measure it, but because there is nothing to measure it against.

**7 · Out of scope, flagged not measured.** `HUMAN_REVIEW.md:20` sets a **monthly** operator spot-audit; `agents/Workflow Threads.md:308` records that it *"has run once, on 2026-07-18."* If that line is current it is 68 days overdue. It is the operator's item, I have not independently verified the "run once" claim, and I measure it no further.

### (c) The finding B2 was for

**Three adopted cadenced observations. Two are current. One — thread 13's weekly row — is current by a single day and has missed five scheduled firings since adoption, four of them consecutively, with nothing in the record suspending it.**

But the general result is worth more than the tally. **"Suspended" and "lapsed" produce identical evidence.** In `analytics/fetch-results/` and `analytics/search-results/` both appear as an absence of rows. The *only* thing that distinguishes them is a sentence somebody wrote in a daily record. The monitored-author check is distinguishable because a seat wrote that sentence on 2026-09-19. Thread 13's four consecutive missed weekly rows between 2026-08-13 and 2026-09-03 are **not** distinguishable, and I could not make them so. **A council that cannot tell a decision from an omission is, on that point, reading evidence about itself as evidence about the world** — which is exactly the failure mode your §1.5(2) named, now measured rather than asserted.

**And the reconciliation could not have been performed any other way than by hand**, because no artefact in this repository records which thread a relay row belongs to.

---

## B3 — THE RESEARCH QUESTION, SEARCHED

**Proposition, written before the first call:** *Between 2026-09-11 and 2026-09-24, at least one investor–State arbitration has been publicly reported in which an intellectual-property right — patent, trademark, copyright, GI, trade secret, or clinical-trial data — was asserted as a covered investment, a regulatory or judicial measure was challenged as the violation, and a jurisdictional or admissibility doctrine was live and litigated.*

**Rigid designators available in advance:** `trade secret`, `clinical trial data`, `covered investment`. **No party name and no docket number was available in advance**, so per the carrying-span rule step 2 I record that the screen's strongest designators are generic field terms and **the verdict is correspondingly weaker.**

**Budget: 4 of 4 spent. 38 result rows, 34 distinct URLs, across four calls. No body retrieved from any host.**

**`blocked_domains` was passed on 4 of 4 calls** — `["dailyjus.com","unis.unvienna.org"]` — and `allowed_domains` on **none**. That combination is forced: `analytics/insights.jsonl:199`, verified by two seats on 2026-09-21, establishes that the two parameters are **mutually exclusive at the API level**. Your instruction is executable only in this form, and this form is also the correct scoping, because the research question is not host-scoped. **Screen result: 0 rows at either prohibited host.**

**THE ANSWER IS AN HONEST NULL.** Nothing returned reaches the three-ring intersection. Screened honestly:

- **S2 returned the seed corpus and nothing beyond it.** *Bridgestone v Panama*, and a piece expressly framed on *"the Philip Morris, Eli Lilly and Bridgestone Awards"* — the project's own three seed cases. The ICSID Review article *"International Intellectual Property Law as Applicable Law in Investment Disputes"* has been in the record since **2026-06-23** and is one of the two sources under `analytics/insights.jsonl:3`. **No 2026 decision surfaced on IP as a covered investment.**
- **Near-miss 1, and it is a genuine one.** `jusmundi.com/en/document/publication/en-ip-issues-in-isds`, *"Wiki Note: Intellectual Property Issues in ISDS"*. **Zero prior carriers in this repository** (prior-art screen run repo-wide over `*.md`, `*.json`, `*.jsonl`; validated against known positives — `Bridgestone` 56, `Eli Lilly` 61, `jusmundi` 57). It is on-theme **at the title level only**: it is a secondary source, which thread 11's own condition classifies as a **LEAD**; `jusmundi.com` is not on `ALLOWED_HOSTS`; the body is unread and unreadable by this council. **Nothing is asserted from it.**
- **Near-miss 2, already in our own intake.** `www.iisd.org/itn/2026/04/21/consent-after-denunciation-jurisdiction-and-damages-meher-tandon/` — Ring 3 (jurisdiction) with **no IP element in the title**, and already present at `state/seen.json:1549`, marked seen 2026-06-16 with `"outcome": "legacy"`. The pipeline has already disposed of it.
- **S3 missed the theme entirely and the miss is informative.** Ten rows on pharmaceutical patent expiry, US regulatory exclusivity and drug pricing — five of them one commercial blog. **A query combining "pharmaceutical", "patent", "data exclusivity" and "2026" is captured by the domestic drug-pricing literature before it ever reaches investment arbitration.** That is a reusable method note: on this theme the Ring-2 vocabulary is owned by another field, and the query must carry an ISDS rigid designator or it will not arrive.
- **S1 and S4 returned the jurisdiction-explainer literature** — Global Arbitration Review guide chapters (5 rows), Lexology notes, a CRS report — plus *"Trade Secrets 2026"*, a Chambers **domestic** practice guide, already carried in five files. This reproduces `0820-R9`'s generalisation exactly: **the doctrinal phrase retrieves the explainer literature.**

**Engine prose: quarantined, and the refusal is on the record.** S1's prose named an alleged pair of KORUS notices of intent over data breaches and a French *Cour de cassation* decision; S4's prose described an unnamed 2026 Venezuelan ICSID award and its *ratione materiae* objections. **No returned title carries either.** Both are search-synthesis (taxonomy 4 territory), both are quarantined in `engine_summary_note` and used nowhere. **Stated expressly because the temptation was real: I did not design S3 or S4 from them.** Using quarantined prose to steer a later query is using it, and the discipline is worthless if it stops at transcription.

**No title is read as a holding. No figure is derived from any search. Nothing here licenses a claim about any tribunal's reasoning.**

**File body: `analytics/search-results/2026-09-24.json` — printed at §7.**

---

## B4 — THE DRAFTING QUESTION

### (a) Is the closure condition truncated? **NO.**

I read the raw JSON value, not a print. `state/research_log.json.open_threads[11]` is a single string of **8,730 characters** that ends cleanly at *"…it makes them unnecessary."* The limb you flagged reads, in full and in the stored value:

> *"STRUCTURALLY-UNREACHABLE CONDITION: either (a) each named gate's own text, read from a primary source, excludes treaty-based investor-State arbitration from its scope; or (b) the recording surfaces are off-allowlist and fetch-dead across **THREE CONSECUTIVE SESSIONS IN WHICH A BATCH WAS ACTUALLY COMMISSIONED against them — the counter counts commissioned-and-failed batches, NEVER days on which no batch was commissioned.**"*

**The sentence closes. Your print was sliced; the register is intact.** Verify it yourself with `python3 -c "import json; print(repr(json.load(open('state/research_log.json'))['open_threads'][11]))"`.

### (b) Is the limb operative? **NO — and the reason has nothing to do with truncation.**

**Limb (b) is grammatically complete and structurally inapplicable on the instrument this council actually has.** Its antecedent requires the recording surfaces to be simultaneously **(x) off-allowlist** and **(y) fetch-dead**, measured across three sessions in which a batch was *actually commissioned* against them. But `validate()` (`scripts/fetch_relay.py:103-113`) rejects an off-allowlist host with `host_not_allowed` **before any request is made** — `fetch_one` returns the reason and never calls `polite_get`. **For exactly the class of surfaces limb (b) describes, a commissioned batch can never produce a fetch outcome at all; it produces our own validator's refusal.** Fetch-deadness is unobservable for off-allowlist surfaces **by construction**. The conjunction is self-defeating: to observe (y) you must put the host on the allowlist, at which point (x) is false.

**So yes — the council is carrying a condition whose unreachability limb it cannot apply.** But it is carrying it for a reason nobody had diagnosed, and the diagnosis matters because it points at a different repair from the one a truncation would need.

**What IS operative and should not be disturbed:** the CLOSE-POSITIVE limb, the DOES-NOT-LICENSE list, and the THIRD BUCKET are all well-formed and were **applied in anger** on 2026-09-13, when the Spentech lead was ruled neither close-positive nor third-bucket. Limb **(a)** is also operative and testable.

### (c) Recommendation — I rule on the merits, you rule on the record

**Do not amend the substance. Re-caption limb (b) so that it measures something the instrument can see** — the same move your own §6.12(3) made on the `application/pdf` limb, which was a re-caption and not a relaxation. Concretely:

> *"(b) the recording surfaces for a named gate are unreachable by this council, established **per gate** by either — (b1) three consecutive **commissioned** batches against an **on-allowlist** recording surface returning a non-`ok` fetch outcome, the counter counting commissioned-and-failed batches and NEVER days on which no batch was commissioned; or (b2) a recorded finding that **every** recording surface for that gate is off `ALLOWED_HOSTS`, in which case the limb is satisfied for that gate **on the spot and without a counter**, because no batch against it can ever be commissioned."*

Two consequences you should price before ruling.

1. **Under (b2), the ICC Art. 30 limb is already satisfied** on the 2026-09-11 premise audit's own finding that `iccwbo.org` is absent from `ALLOWED_HOSTS`. That is a real narrowing of the thread, obtained at zero cost, from a fact the register already holds.
2. **Make it per-gate, not global.** The headline names two gates and the condition names four. One gate becoming unreachable must not make the thread unreachable — ICSID Convention Rule 41, the gate that actually fired, is reachable and is where the live work is.

**The alternative — deleting limb (b) — I do not recommend.** It would leave the thread with no exit but limb (a) and it could then be carried indefinitely.

**I also renew, without re-arguing it, the register defect the 2026-09-13 session escalated and you have not yet ruled on:** the headline question names two gates while the condition names four, and the headline's operative word is *"spread"* — but ICSID Convention Rule 41 is the **incumbent** gate, so its invocation is evidence about the baseline instrument, not about the diffusion this thread watches. **Reframing is yours.**

---

## 5. Errors against myself

**E1 — I over-counted thread 11's relay history by three rows, and caught it by accident.** My first pass matched rows by **substring** on `manifest-lack` and reported **45** rows at the standing-observation URL. Three of those sit at two *different* ICSID URLs — the `/procedures/arbitration/convention/manifest-lack-of-legal-merit/2022` explanatory page and the `/resources/publications/focus-objections…` page. **The correct figure on exact URL equality is 43.** I noticed only because one row showed 85,589 bytes against a page whose entire observed range is 94,994–96,161, and I went looking for the anomaly. **The anomaly was mine, not the host's**, and had the byte counts happened to be closer I would have filed a wrong number. Every URL count in this memo is now exact-equality.

**E2 — my first parse returned zero rows from every reduction and I nearly reported that.** The record list in `analytics/fetch-results/*.json` is under the key `records`, not `results`. My first script looked for `results`, found none, and would have supported the sentence "no row has ever fired at this URL." It only failed loudly because I had it print unrecognised shapes.

**E3 — I drafted a sentence saying new rows are appended at the tail of the ICSID register, and withdrew it.** Two windows on two different page states cannot establish a sort order or an insertion policy. That is precisely the ruling `0820-E4` already made against a seat that drafted the same kind of sentence, and I should not have needed to rediscover it. What survives at §B1(a) is the narrow form: **at a fixed anchor, the rendered stream differs between the two states**, stated as two self-contained observations and joined into no claim.

**E4 — a limit on B2 I want named rather than buried.** My thread-13 numbers rest on matching relay rows to threads by reading `find` strings. That is my judgement about a prior seat's intent, not a recorded fact. **Where I judged wrong, a "current" could be a lapse or a lapse a "current."** The `thread_id` field I propose above is what would remove my judgement from the measurement.

---

## 6. Insights — checked against the ledger first

`analytics/insights.jsonl` holds **208 lines**. I screened it by machine for `cadence`, `lapsed`, `standing observation`, `last fired`, `abandoned`, `seen.json` and `unreadable` before drafting either entry below. **Neither is recorded.** Entry 164 (2026-09-11) concerns the classifier's published accuracy and is a different object; entry 90 (2026-08-21) concerns digest drift at the ICSID register and I have cited it rather than restated it.

**I propose two. Both are sourced, both are mine, both are checkable without leaving the repository.**

```jsonl
{"date":"2026-09-24","thread_id":"T29-sixteen-unreversed-outage-abandonments","ring":"instrument","insight":"THE SIXTEEN OUTAGE ABANDONMENTS HAVE BEEN REVERSED BY THE MACHINERY, AND OPEN THREAD 29'S EVIDENCE SENTENCE IS TRUE AT ITS PIN AND FALSE AT HEAD. Open thread 29 states, pinned at commit 61152d5, that 'state/seen.json holds exactly 16 entries whose outcome is \"abandoned\"'. Reproduced at that pin: 16 abandoned entries, 411 entries total. Measured at HEAD 38acb75 on the same file: ZERO entries carry outcome 'abandoned' and the file holds 480 entries. The sixteen were traced individually from the pin to HEAD by URL key: ALL SIXTEEN SURVIVE, NONE WAS DROPPED, and their outcomes at HEAD are 13 'unreadable' and 3 'ok'. Three weekly commits touch the file between the pin and HEAD (1eb4235, 1d559a7, 3f07d57), all captioned 'chore: weekly digest + state update [skip ci]'. WHAT THIS DOES AND DOES NOT SETTLE: three of the sixteen were in fact classified, so for those the abandonment is cured on the merits. Thirteen now carry 'unreadable', which src/classify.py:850-862 defines as an item that 'reached the classifier with no title, no summary and no body, so it was never read' - a TERMINAL state expressly excluded from READ_TERMINAL_OUTCOMES at src/classify.py:181. The substantive worry therefore SURVIVES IN A NEW FORM UNDER A DIFFERENT OUTCOME NAME, and only the headline count is stale. NOTHING IS ASSERTED ABOUT WHY: no seat has observed the run that changed them, and 'the weekly digest re-attempted them' is a hypothesis consistent with the commits, not an observation.","sources":["state/seen.json@38acb75","state/seen.json@61152d5","src/classify.py:181","src/classify.py:850-862"],"confidence":"high"}
{"date":"2026-09-24","thread_id":"METHOD-a-suspended-observation-and-a-lapsed-one-are-the-same-artefact","ring":"instrument","insight":"THIS COUNCIL CANNOT DISTINGUISH A SUSPENDED STANDING OBSERVATION FROM A LAPSED ONE, BECAUSE BOTH ARE AN ABSENCE OF ROWS. First full reconciliation of every adopted cadenced observation against when it last actually fired, measured over analytics/fetch-results/*.json on EXACT URL EQUALITY with the date taken from the result filename (never from fetched_at, which is file-level - gate G4). THE ADOPTED SET IS THREE, not more: thread 11's register row, thread 13's weekly Huawei ARB/22/2 row, thread 10's monthly Wingtech watch. analytics/optimization-log.md carries 34 entries at proposed and 5 at adopted, and only one adopted entry (:65) carries a recurring cadence. MEASURED: thread 11 last fired 2026-09-13 (43 rows, 8 dates); thread 13's ARB/22/2 last fired 2026-09-18 (7 rows, 5 dates); thread 10's Wingtech-discriminating relay literal last fired 2026-09-10 (find 'Wingtech', find_matched false). All three are within cadence today. THE DEFECT IS THREAD 13: adopted 2026-08-06 as a WEEKLY row with a named next row of 2026-08-13, it did not fire again until 2026-09-04 - a 29-day gap covering four consecutive scheduled rows - and again lapsed 12 days from 09-06 to 09-18; its adopted SHAPE (three rows, one page, two same-page controls) has been honoured on exactly one date, 2026-08-06. NO RECORD SUSPENDS IT. By contrast the monitored-author check, also unfired since 2026-09-13, is SUSPENDED and not lapsed, and the only thing that makes it so is a sentence at analytics/daily-research/2026-09-19.md:400. THE STRUCTURAL FINDING: no artefact in this repository records WHICH THREAD a relay row belongs to, so the reconciliation is only possible by hand-reading each row's find string - and doing so changed a result, since three of thread 10's five relay dates were rows for a different thread. A thread_id field on each request row would remove the analyst's judgement from the measurement. SECOND FINDING, AGAINST THE DAY'S OWN AGENDA: 'cycle' is undefined in prompts/, METHODOLOGY.md and COUNCIL.md, and the only place this project ever fixed the unit for thread 11's tracker is analytics/optimization-log.md:65, where a cycle is a MONTH - so eleven days is not a lapse on any definition the repository contains.","sources":["state/research_log.json","analytics/optimization-log.md:65","agents/Workflow Threads.md:1558","agents/Workflow Threads.md:3050","analytics/daily-research/2026-09-19.md:400","scripts/fetch_relay.py:134"],"confidence":"high"}
```

**No new insight is proposed from B3. The theme was quiet and I am not padding the ledger** — the S3 method note (that a pharmaceutical + patent + 2026 query is captured by the domestic drug-pricing literature before it reaches investment arbitration) is a reusable query-shaping observation and belongs in the session's method note, not in the insight ledger.

**GAP-UNRESOLVED markers.** I open **no new slug**. Every named gap I touched today is already carried under an existing slug, and re-emitting one I did not work would corrupt the escalation counter, which counts across sessions.

---

## 7. FILE BODIES — hand-off (I commit nothing)

### `analytics/fetch-requests/2026-09-24-daily.json`

```json
{
  "note": "2026-09-24 daily batch. ONE substantive row; the runner prepends its own control at https://example.com/. WRITTEN AND COMMITTED BEFORE THE ROW FIRES. PURPOSE: re-establish open thread 11's standing observation on ICSID's Decisions on Manifest Lack of Legal Merit register. Last fired 2026-09-13 (43 rows on 8 dates at this exact URL, measured on exact URL equality over analytics/fetch-results/*.json 'records'). THIS ROW IS NOT OFFERED AS A CLOSURE AND CANNOT BE ONE. Thread 11's closure condition (adopted 2026-09-11) requires the BODY OF A PRIMARY DOCUMENT - a tribunal decision or award, or a docket entry naming that document - stating BOTH (i) the gate, named, and (ii) a ground that is whether an IP right is a covered investment. A table of decisions is a DOCKET-SUMMARY SURFACE: G16 (analytics/daily-research/2026-08-25.md) bars any such row from founding an ordinal, a count, a disposition or a holding, and G10 bars reading a document designation as the tribunal's characterisation. No outcome of this row closes the thread, opens the third bucket, or licenses a holding. WHY THIS find AND NOT ANOTHER. 'ARB/25/26' is a pure-ASCII rigid designator that cannot occur in page chrome, and it names the LAST case row this project has ever observed on this page - the right edge of the 2026-09-12 'ARB/24/16' window, where the excerpt was cut mid-phrase at 'Decision on the Respondent's Rule 41(1) '. excerpt_of returns body[idx-100 : idx+300] (scripts/fetch_relay.py:134, EXCERPT_CHARS=400; the match sits at the 25% point, NOT the centre, notwithstanding the docstring - established 2026-09-09). Anchoring on the last-observed row therefore spends 300 of the 400 characters on the region beyond everything this project holds. REJECTED ALTERNATIVE, and the reason is decisive: re-firing 'Spentech' would produce, by construction, no new observation of any region - same URL, same literal, same first-occurrence region yields the same window, which is the defect analytics/daily-research/2026-09-13.md:248 names against that session's own row 6. ALSO REJECTED: an IP literal (patent / trade secret / clinical trial data). 0913-B3 correctly records that exactly one IP literal was ever screened here and no synonym was tried - but a register of case names, document designations and dates cannot carry a GROUND, and a party-name register carries party names and not subjects (2026-08-20 ruling). Firing one would buy a predictably worthless null with the council's only substantive row. PRE-REGISTERED PREDICTIONS, fixed here so they cannot be chosen after the result. P1: outcome 'ok', status 200, content_type text/html, find_matched TRUE - HIGH, ~85%. P2: the window's opening characters reproduce, character-for-character, the corresponding right-edge span of the 2026-09-12 ARB/24/16 window, namely ‘Samvel Karapetyan v. The French Republic (ICSID Case No. ARB/25/26) Decision on the Respondent’s Rule 41(1) ’ - MODERATE, ~65%. It is a cross-page-state comparison and this council's own rule is that nothing transfers between page states, so this is a PREDICTION and never an inference. P3: conditional on P1, at least 250 characters of the returned window lie beyond the right edge of every window this project has ever held at this URL - HIGH, ~90%. P4: bytes != 96161 OR sha256 != e1ceead703fcce31... - HIGH. REGISTERED SO IT IS ON THE RECORD, NOT SO IT CAN BE READ AS MOVEMENT: analytics/insights.jsonl:90 (2026-08-21) establishes that a digest change on THIS URL is not by itself evidence that the table's contents changed. The evidential weight of this row is in the EXCERPT, not the digest. NAMED OUTCOME CLASSES. (A) ok/200/text-html/find_matched true - the row worked. (B) ok/200/find_matched false - a real absence from the rendering OR an undecoded entity or U+200B inside the literal; NOT separable by this row (G6/N7, proven live on this host). (C) ok but status != 200, or final_url != url - read final_url before any excerpt (G9); the row is about wherever it landed. (D) refused / 403 / 202 / timeout / no_contact - INSTRUMENT FAILURE. A fact about this runner, this user agent, this path, this timestamp, and NOTHING about ICSID. (E) host_not_allowed / scheme_not_https / private_address - a defect in this row, caught by our own validate(); nothing about the source. (F) control_ok false - the whole batch is VOID and no row may be read as information about its resource. (G) the workflow does not fire, or no reduction lands in analytics/fetch-results/ - the standing observation LAPSES AGAIN, which is a fact about this council and not about the table. CLASSES D, E, F AND G ARE THE ONES THAT MEAN THE INSTRUMENT FAILED RATHER THAN THE SOURCE ANSWERED, and none of them may be written as a finding about ICSID. STANDING PROHIBITIONS HONOURED: no www.italaw.com row, no bare italaw.com row, no investmentpolicy.unctad.org row, no further IISD path-family row, no www.bing.com row, no bare unctad.org row, and no gap-closing row of any kind for nafta-chapter-11-allowlisted-host-visible-prose-untested, whose plan is retired.",
  "urls": [
    {
      "url": "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit",
      "find": "ARB/25/26",
      "purpose": "Open thread 11 standing observation, one row per cycle. Read the region of ICSID's Decisions on Manifest Lack of Legal Merit register that lies BEYOND the last row this project has ever observed, by anchoring on the case identifier at the right edge of the 2026-09-12 window and spending the excerpt's 300 trailing characters on unobserved ground. Host is on ALLOWED_HOSTS (scripts/fetch_relay.py:49). Establishes reach, a page state, and a span; establishes NO ground, NO disposition and NO holding."
    }
  ]
}
```

### `analytics/search-results/2026-09-24.json`

The file is 19,227 bytes and validates as JSON. It is written verbatim at
**`/tmp/claude-0/-home-user-isds-watcher/35f8d1df-f063-5145-bf71-5b53d7de5369/scratchpad/2026-09-24.json`** — copy it to `analytics/search-results/2026-09-24.json`. Its header fields, so the record carries them without the 34 result rows:

```
date 2026-09-24 · seat research-analyst · repo_state 38acb75 (not shallow, 1563 commits)
budget: searches_budgeted 4, call_count 4, searches_unspent 0
blocked_domains_compliance: passed_on_calls 4 of 4, value ["dailyjus.com","unis.unvienna.org"],
  allowed_domains_passed_on_calls 0
prohibited_host_row_screen: total 38, dailyjus.com 0, unis.unvienna.org 0
allowlist_row_screen: in_relay_ALLOWED_HOSTS 5, out 33 (www.italaw.com 1 row; NO relay row proposed)
result_row_count 38 · distinct_urls 34
engine_summary_used: false on all four calls; both prose blocks quarantined and transcribed nowhere
S1 "investment treaty arbitration trade secrets clinical trial data covered investment jurisdictional objection 2026" — 9 rows
S2 "ICSID tribunal intellectual property patent trademark covered investment jurisdiction decision September 2026" — 9 rows
S3 "new investor-state arbitration registered 2026 pharmaceutical patent data exclusivity regulatory measure claim against state" — 10 rows
S4 "\"trade secrets\" expropriation investment treaty tribunal ratione materiae objection 2026 award" — 10 rows
```

*(The relay request is also at `.../scratchpad/2026-09-24-daily.json` if you prefer to copy rather than paste.)*

---

## 8. What I ask you to rule on

1. **B4 — the limb.** Adopt the (b1)/(b2) re-caption, per gate, or reject it. **It is not a relaxation**, and under (b2) the ICC Art. 30 limb is already satisfied on the register's own 2026-09-11 finding.
2. **B2 — thread 11's cadence unit.** Fix "cycle" to **month**, in line with `analytics/optimization-log.md:65` and `agents/Workflow Threads.md:1558`, or define it otherwise. Until you do, no seat can say whether this observation is late. **Today's Part 1 §1.5(1) implies it is; on the record it is not, and that sentence should be corrected in place before this record is emailed.**
3. **B2 — thread 13.** Its weekly row has missed five scheduled firings, four consecutively, and its adopted three-row shape has been honoured once. Either **re-adopt it with a cadence the council will keep**, or **suspend it on the record** so its absence stops looking like a null.
4. **B2 — the `thread_id` field.** One key on each `urls[]` entry in `analytics/fetch-requests/*.json`, naming the thread the row serves. It costs nothing, it is inside merge scope, and it converts this reconciliation from a hand exercise into a command. I propose it; I have built nothing.
5. **Thread 29.** Its headline evidence is stale at HEAD — 0 abandoned entries, not 16, with all sixteen traced and re-resolved as 13 `unreadable` / 3 `ok`. **The thread should be amended rather than closed**, because 13 items still reached a terminal state that is expressly not a reading of the item.
6. **Two stale pointers for whoever edits the register:** open thread 25's pin `agents/Workflow Threads.md:1901` is wrong (the content is now at **:3050**), and both that thread and `Workflow Threads.md:3050` address thread 10 by array index (`open_threads[5]`, `open thread 1`), which the 2026-09-12 ruling bans.

---

```json
{"candidate_claims": [
  {"claim_text": "state/research_log.json open_threads[11] stores a single string of 8,730 characters whose STRUCTURALLY-UNREACHABLE CONDITION limb (b) is grammatically complete and reads in full 'the recording surfaces are off-allowlist and fetch-dead across THREE CONSECUTIVE SESSIONS IN WHICH A BATCH WAS ACTUALLY COMMISSIONED against them - the counter counts commissioned-and-failed batches, NEVER days on which no batch was commissioned.'",
   "source_url": "file://state/research_log.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "or (b) the recording surfaces are off-allowlist and fetch-dead across THREE CONSECUTIVE SESSIONS IN WHICH A BATCH WAS ACTUALLY COMMISSIONED against them - the counter counts commissioned-and-failed batches, NEVER days on which no batch was commissioned.",
   "supporting_locator": "state/research_log.json, open_threads[11], raw JSON string value, characters following 'STRUCTURALLY-UNREACHABLE CONDITION: either (a)'"},

  {"claim_text": "scripts/fetch_relay.py validate() returns 'host_not_allowed' for a host absent from ALLOWED_HOSTS and fetch_one returns that reason without calling polite_get, so no fetch outcome can ever be observed for an off-allowlist host by this relay.",
   "source_url": "file://scripts/fetch_relay.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "if host not in ALLOWED_HOSTS:\n        return \"host_not_allowed\"",
   "supporting_locator": "scripts/fetch_relay.py:110-111 (validate); the guard at fetch_one, 'reason = validate(url) / if reason: record[\"outcome\"] = reason / return record', precedes 'resp = polite_get(url)'"},

  {"claim_text": "Exactly 43 relay rows have ever been fired at https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit across analytics/fetch-results/, on eight dates (2026-08-19, 08-20, 08-21, 08-22, 08-23, 08-24, 09-12, 09-13), the most recent being 2026-09-13.",
   "source_url": "file://analytics/fetch-results/",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "Derived at HEAD 38acb75 by parsing every analytics/fetch-results/*.json, reading the 'records' list, and matching rows on exact equality of the 'url' field. Per-date row counts 5/6/4/2/2/1/21/2."},

  {"claim_text": "Open thread 13's standing weekly relay row on the Huawei ARB/22/2 ICSID case-detail page, adopted 2026-08-06 with a named next row of 2026-08-13, did not fire again until 2026-09-04, a gap of 29 days spanning four consecutive scheduled weekly rows, and no record in this repository suspends it.",
   "source_url": "file://analytics/fetch-results/",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "ADOPTED 2026-08-06: this watch becomes a STANDING WEEKLY RELAY ROW (three rows, one page, two same-page controls); next row 2026-08-13",
   "supporting_locator": "Quote from state/research_log.json open_threads[13]. Firing dates derived from analytics/fetch-results/*.json on exact URL equality for CaseNo=ARB/22/2: 2026-08-06 (3 rows), 2026-09-04, 2026-09-05, 2026-09-06, 2026-09-18."},

  {"claim_text": "At commit 61152d5 state/seen.json held 16 entries with outcome 'abandoned' and 411 entries in total; at HEAD 38acb75 it holds 0 entries with outcome 'abandoned' and 480 in total, and all sixteen of those URL keys are still present at HEAD with outcomes 13 'unreadable' and 3 'ok'.",
   "source_url": "file://state/seen.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "git show 61152d5:state/seen.json against the working-tree file at HEAD 38acb75; the sixteen keys were extracted at the pin and looked up individually at HEAD, with zero misses."},

  {"claim_text": "src/classify.py records an item as 'unreadable' when it reached the classifier with no title, no summary and no body, and expressly excludes that outcome from the set any 'classified' count may use.",
   "source_url": "file://src/classify.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "No classifiable text: this item reached the classifier with no title, no summary and no body, so it was never read.",
   "supporting_locator": "src/classify.py:859-862 for the quote; src/classify.py:181 for 'READ_TERMINAL_OUTCOMES = TERMINAL_OUTCOMES - {ClassifyOutcome.UNREADABLE}'"},

  {"claim_text": "The only definition of a monitoring 'cycle' this repository fixes for open thread 11's first-application tracker is a month, recorded at adoption on 2026-07-31, and the word 'cycle' is not defined in prompts/, METHODOLOGY.md or COUNCIL.md.",
   "source_url": "file://analytics/optimization-log.md",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual", "assertion_status": "candidate",
   "supporting_quote": "adopted 2026-07-31 (chairman, per the 2026-07-30 close-out; analytics: CORROBORATED at 3 queries on first use) — monthly cadence, analyst executes, next cycle late August 2026",
   "supporting_locator": "analytics/optimization-log.md:65; corroborated at agents/Workflow Threads.md:1558 ('First monthly cycle, late August 2026'); the negative was measured by grep over prompts/, METHODOLOGY.md and COUNCIL.md, returning zero."},

  {"claim_text": "On 2026-07-30 the project's integrity officer read ICSID's Decisions on Manifest Lack of Legal Merit table in vetting and recorded more than sixty decisions from 2008 to 2025, none of them intellectual-property-, trade-secret-, pharmaceutical- or data-related.",
   "source_url": "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit",
   "access_status": "full", "source_authority": "official_secondary", "claim_type": "procedural", "assertion_status": "candidate",
   "supporting_quote": "the integrity officer read it in vetting (60+ decisions, 2008–2025, none IP-, trade-secret-, pharmaceutical-, or data-related), so the null now rests on the authoritative list itself, not only on search absence",
   "supporting_locator": "analytics/daily-research/2026-07-30.md:35. This council did not read the table on 2026-09-24; the claim is about what the 2026-07-30 record states."},

  {"claim_text": "A web search of four calls on 2026-09-24, returning 38 rows across 34 distinct URLs with blocked_domains passed on every call, surfaced no title naming any investor-State arbitration in which an intellectual-property right, trade secret or clinical-trial data was asserted as a covered investment with a live jurisdictional doctrine; the only on-theme title with no prior carrier in this repository is the Jus Mundi wiki note 'Intellectual Property Issues in ISDS', whose body was not read.",
   "source_url": "https://jusmundi.com/en/document/publication/en-ip-issues-in-isds",
   "access_status": "unavailable", "source_authority": "unknown", "claim_type": "lead", "assertion_status": "lead",
   "supporting_quote": "Wiki Note: Intellectual Property Issues in ISDS",
   "supporting_locator": "analytics/search-results/2026-09-24.json, call S2, results[6]; title and url verbatim as returned, retrieved_body false. jusmundi.com is absent from scripts/fetch_relay.py ALLOWED_HOSTS, so the body is unreadable by this council. Absence is 'not found in accessible sources', never 'does not exist'."}
]}
```

--- END OF THE ANALYST'S VERBATIM RETURN ---

---

## 3. The integrity challenge — discharged by me, in my own name

Rule 0 makes the integrity officer not a member of the daily meeting, so there is no seat's voice here
to perform. What follows is my own adversarial pass over the analyst's return. **I ran every check by
executing code against the repository rather than by reading the memo and agreeing with it**, and I
report the attacks that failed as well as the ones that landed, because an attack that is only reported
when it succeeds is not a check.

### 3.1 `0924-C1` — AGAINST ME, SUSTAINED. My agenda rested on a lapse I could not demonstrate.

The analyst's B2 finding against §1.5(1) is right and I sustain it in full. I built the day's first
task on eleven days having elapsed since the thread-11 observation last fired, in a sentence that
invited the reader to hear "overdue." The only unit this repository ever fixed for that tracker is at
`analytics/optimization-log.md:65` — *"monthly cadence, analyst executes, next cycle late August 2026"*
— corroborated at `agents/Workflow Threads.md:1558`. **Eleven days against a monthly cycle is not a
lapse, and the largest gap in the observation's own series is eighteen days.** The amendment is marked
at §1.5a and the sentence is left standing so the premise stays visible. The row still fires, on the
merits the analyst gives at B1 and not on a cadence argument.

### 3.2 `0924-C2` — MY FIRST ATTACK ON THE ANALYST, AND IT FAILED

I went at the proposed `find` string first, because it is the row's whole design. The memo's §B1(a)
quotes a 2026-09-12 window that ends `…Samvel Karapetyan v. The French Republi` — **cut off before any
case number** — while §B1(b) proposes `ARB/25/26` as a string at that window's right edge. On its face
that looked like a designator the project had never observed, which would have made the row a search
for something taken from outside the record. **I grepped for it, and it is observed.** It is in
`analytics/fetch-results/2026-09-12-zz-icsid-table-header.json`, `records[1]`, the window anchored on
`ARB/24/16` rather than on `Spentech`, which runs further to the right and ends
`…(ICSID Case No. ARB/25/26) Decision on the Respondent’s Rule 41(1) `. Three carriers in the
repository, none of them today's record. My grep was not a null instrument: run against
`ARB/24/16`, `Samvel Karapetyan` and `ARB/24/18` on the same paths it returned 41, 6 and 34 files.
**The memo distinguishes the two windows correctly and I misread it. The attack failed and the design
survives it.**

### 3.3 `0924-C3` — BLOCKING, AGAINST THE ANALYST. A pre-registered prediction that cannot succeed.

**P3 reads: *"conditional on P1, at least 250 characters of the returned window lie beyond the right
edge of every window this project has ever held at this URL. HIGH, ~90%."* It is false by construction,
by one character, and it was false before the row fired.**

Measured, not reasoned. In the widest window this project holds at this URL — the 400-character
`ARB/24/16` window above — the string `ARB/25/26` begins at **offset 349**, so **51 characters at or
after that match are already held**. `excerpt_of` returns `body[idx-100 : idx+300]`
(`scripts/fetch_relay.py:134-135`, `EXCERPT_CHARS = 400`), which is **300 characters at or after the
match start**. New ground is therefore `300 − 51 = **249**`, and 249 is a **hard ceiling**: it does not
depend on what the page says, only on our own excerpt geometry and on a window we already hold. **A
prediction of "at least 250" cannot come true on any page state.**

This is not pedantry about a single character, and I want the reason on the record. **A pre-registration
exists to make a row capable of embarrassing the seat that fired it.** A prediction that cannot succeed
inverts that: when the row returns, P3 fails, and a later reader — or a later session — has a recorded
90% prediction that missed, sitting next to a page whose digest also changed. The natural and wrong
reading is that something moved. **The miss would have been manufactured by our own arithmetic and
then available to be read as evidence about ICSID.** That is the tool-status-as-source-state failure
with an extra step.

**RULING: P3 is struck as a prediction and replaced, before the row fires, by the arithmetic it
actually is** — conditional on P1 and P2, **exactly 249 characters** of the returned window lie beyond
the right edge of every window this project has ever held, by construction and not by probability. The
amendment goes into the request file's own `note` under my name, marked as a chairman amendment, so
the artefact that fires carries the corrected pre-registration rather than being fixed up afterwards
in prose. **The row itself is unaffected and I am not striking it.**

### 3.4 `0924-C4` — NON-BLOCKING, same root. A figure in the memo's prose is wrong by 51 characters.

§B1(b) reason 1 says anchoring on `ARB/25/26` *"spends 300 of 400 characters on ground this council has
never seen."* The correct figure is **249**, for the reason at §3.3. The memo's own P3 was calibrated to
within one character of the truth while its prose overstated by fifty-one, which tells me the error is
in the prose and not in the design.

### 3.5 `0924-C5` — NON-BLOCKING. A named outcome class is missing, and it is the one that would bite.

The eight classes at §B1(b) are unusually good — four of them name instrument failure and say in terms
that none may be written as a finding about ICSID. **But none covers the case where `find` matches at a
first occurrence that is not the register row.** `excerpt_of` centres on the **first** occurrence in the
body; if `ARB/25/26` appears anywhere earlier in the document — a "recently added" panel, a
`<title>`, a link label — the window returned is a window on that, and it would arrive as class (A),
"the row worked," carrying a span from somewhere else entirely. The council cannot test this without
firing. **I add it as class (A′): `find_matched: true` but the returned window does not contain the
adjacent register furniture (a case name, a document designation and a date), in which case the row is
read as a first-occurrence artefact and NOT as an observation of the register.** It goes into the
request note with the rest.

### 3.6 `0924-C6` — AGAINST ME, AND IT IS THE SAME ERROR THE MEMO HAD JUST DISCLOSED TO ME

My first probe of the `state/seen.json` claim returned `n=1` and zero abandoned entries at both the pin
and at HEAD, which — had I stopped there — would have supported writing that the analyst's thread-29
figures were unreproducible. **They were not unreproducible. My probe was wrong.** `state/seen.json` is
a dict with a single `sources` key holding ten per-source dicts, and my helper returned the outer object.
**This is the analyst's own disclosed `E2` — reading a JSON file for a key it does not have — committed
by me, against the same class of file, within minutes of reading the disclosure.** I record it because
it is the strongest thing I found today about how this council fails: the error was named on the page in
front of me and I made it anyway, and only a shape-check caught it.

### 3.7 CLEAN BILL, where it is owed — four load-bearing measurements reproduced by execution

I re-derived each of these with my own script rather than accepting the memo's number.

- **43 rows at the thread-11 URL, on 8 dates, every one `ok`** — reproduced exactly, on exact URL
  equality, with the per-date counts 5 / 6 / 4 / 2 / 2 / 1 / 21 / 2 matching the memo row for row.
- **`open_threads[11]` is 8,730 characters and ends cleanly** at *"…it makes them unnecessary."* —
  reproduced. **The limb is not truncated; my B4 premise was wrong and the analyst corrected me.**
- **Thread 13: 7 rows on 5 dates, a 29-day gap from 2026-08-06 to 2026-09-04 and a 12-day gap from
  09-06 to 09-18** — reproduced exactly, gaps computed rather than eyeballed.
- **Thread 29: `state/seen.json` holds 411 entries and 16 `abandoned` at `61152d5`, and 480 entries and
  0 `abandoned` at HEAD; all sixteen keys survive, none is missing, and their outcomes at HEAD are 13
  `unreadable` and 3 `ok`** — reproduced exactly, with zero misses, once my own probe was fixed.

**Two blocking-grade findings against the analyst's pre-registration, both mine; two failed attacks,
both mine; one sustained objection against my own agenda, the analyst's. That is today's integrity
round, and it happened without a convened officer.** What it does not establish is the counterfactual —
whether a convened officer would have found more — and §1.2 says so already. One session is not a
demonstration.

---

## 4. Spend checkpoint, and the batch fires

**Taken here, after the vetting round, as my convening instruction requires.** What has been spent:
one analyst seat, convened once and returned once; four of four permitted web searches, all four
recorded to a machine-readable artefact; zero relay rows fired so far; one integrity pass, mine, by
execution. What remains to spend: one relay row, and the reconvening of the analyst under Rule 2 for
the Part 5 dialogue. **I am not commissioning a second batch, a second search budget, or a second
seat.** The day's question was whether the council could put its budget back on the research question
after twelve sessions away from it, and the answer arrived at B3 — a clean null — which is an answer
and not a reason to buy more.

**Two artefacts land with this commit and both are the analyst's work, not mine:**

- `analytics/search-results/2026-09-24.json` — four calls, 38 result rows, 34 distinct URLs,
  `blocked_domains` passed on 4 of 4 calls, `allowed_domains` on none, zero rows at either prohibited
  host, `engine_summary_used: false` on every call with both prose blocks quarantined. Copied byte-for-
  byte from what the analyst handed me.
- `analytics/fetch-requests/2026-09-24-daily.json` — one substantive row; the runner prepends its own
  control. **Its `note` carries my amendment, appended under my name and marked as a chairman's
  amendment**, striking P3 as unsatisfiable and replacing it with the arithmetic (exactly 249 characters,
  by construction), correcting the 300/249 prose figure, and adding outcome class (A′). The URL and the
  `find` string are untouched and are the analyst's.

**On `allowed_domains`.** I told the analyst to pass `blocked_domains` on every call, and it did — but
it also recorded that `allowed_domains` and `blocked_domains` are **mutually exclusive at the API
level** (`analytics/insights.jsonl:199`, verified by two seats on 2026-09-21), so the instruction was
executable only in one form. It chose the form that matched the standing prohibition and said so rather
than quietly picking one. That is the right call and I record it as such.

**The push that carries this commit is what fires `.github/workflows/fetch-relay.yml`.** The reduction
lands in `analytics/fetch-results/2026-09-24-daily.json`, committed by the runner, and I read it at §4.1
below. If it does not land, that is outcome class (G) — the standing observation lapses again — and it
is a fact about this council, not about the table.

### 4.1 THE ROW FIRED, IT REACHED GROUND THIS PROJECT HAD NEVER SEEN, AND THE AMENDED PREDICTION IS CONFIRMED TO THE CHARACTER

`fetch-relay` run **135** (`35993753921`), head `2bcb15e`, conclusion success. The reduction is committed
at `analytics/fetch-results/2026-09-24-daily.json`. **`control_ok: true`, `void: false`, requested 1,
completed 1** — the batch is not void and the row may be read as information about its resource.

**Outcome: class (A). The row worked.** `ok` / **200** / `text/html` / `find_matched: true`, `final_url`
identical to the requested URL, **96,161 bytes**, sha256 `6dbf0ddbad9ca558…`.

**Against the pre-registration, item by item, and I take the one I amended first because it is the point.**

- **P3, as I amended it: CONFIRMED EXACTLY.** I struck the analyst's "at least 250 characters, ~90%" as
  false by construction and replaced it with "exactly 249, by construction." **Measured on the returned
  window: the right edge of the widest window this project previously held —
  `Decision on the Respondent’s Rule 41(1) ` — is found at offset 111, and there are exactly
  **249** characters beyond it.** Not approximately; 249. **The prediction I struck would have failed by
  one character on a row that worked perfectly**, and this record would have carried a missed 90%
  prediction sitting beside a changed digest. That is the whole reason the amendment went into the
  artefact before the row fired rather than into prose afterwards.
- **P1: CONFIRMED.** `ok`, 200, `text/html`, `find_matched: true`, as predicted at ~85%.
- **P2: CONFIRMED AS TO THE REPRODUCTION, FALSIFIED AS TO ITS OWN WORDING, and the cause is the analyst's
  own documented geometry.** The predicted span —
  `Samvel Karapetyan v. The French Republic (ICSID Case No. ARB/25/26) Decision on the Respondent’s Rule 41(1) `
  — reproduces **character-for-character** across two page states twelve days apart, which is what P2 was
  for. But P2 said it would be at *"the window's opening characters,"* and it is at **offset 43**: the
  window opens `pondent's Rule 41 Objection May 29, 2025 - S`. `excerpt_of` starts at `idx − 100`, and
  only about 57 characters of the predicted span precede the match, so the window necessarily reaches
  back further. **The reproduction is the finding; the wording was wrong and I am not scoring it as a
  hit.**
- **P4: CONFIRMED, on its second disjunct only, and the first disjunct is the more interesting half.**
  The sha256 differs from 2026-09-13's `e1ceead703fcce31…`. **The byte length does not: 96,161 on
  2026-09-12, on 2026-09-13 and again today.** A digest change at constant byte length is exactly the
  pattern `analytics/insights.jsonl:90` (2026-08-21) records as **not** evidence that the table's
  contents changed — and the pre-registration said in terms that P4 was registered so it would be on the
  record, not so it could be read as movement. **Today the council does not need the digest**, because it
  has content evidence instead, which is the difference between this row and a change-detector firing.

**WHAT THE ROW OBSERVED, AND IT IS NEW TO THIS PROJECT.** The 249 characters of unobserved ground read,
verbatim from the reduction:

> `objection&nbsp; August 27, 2026 - E.ON SE, E.ON Finanzanlagen GmbH and E.ON Iberia Holding GmbH v. Kingdom of Spain (ICSID Case No. ARB/15/35) - Annulment Proceeding Decision on Preliminary Objections February 6, 2025 - Petaquilla Minerals Ltd. v. R`

Read against the four-column structure this council established on 2026-09-12 — *Case name* · *decision
or Award (language(s))* · *date of decision or Award* · *Finding that Claim is Manifestly Without Legal
Merit* — three things are now observed that were not:

1. **The *Samvel Karapetyan v. The French Republic* (ICSID Case No. ARB/25/26) entry is completed.** Its
   document designation is `Decision on the Respondent’s Rule 41(1) objection`, its date is
   **August 27, 2026**, and its fourth-column value is `-`. Until today this project held that row cut off
   mid-designation at `Rule 41(1) `. **This is the most recent decision date this council has ever
   observed on this register, and it post-dates the officer-verified 2026-07-30 baseline by four weeks.**
2. **Two further register rows** — *E.ON SE, E.ON Finanzanlagen GmbH and E.ON Iberia Holding GmbH v.
   Kingdom of Spain* (ICSID Case No. ARB/15/35) *– Annulment Proceeding*, `Decision on Preliminary
   Objections`, February 6, 2025, fourth column `-`; and the beginning of a *Petaquilla Minerals Ltd.*
   row, cut at `v. R`.
3. **`&nbsp;` survives undecoded into the reduced stream.** That is not a curiosity: outcome class (B)
   warns that a `find_matched: false` on this host cannot be separated from an undecoded entity inside
   the literal, and today's row shows the entity survival is real on this page rather than hypothetical.

**WHAT IT DOES NOT ESTABLISH, AND THE RULING IS FLAT.** **Thread 11 is not closed, not advanced toward
closure, and the third bucket is not opened.** The closure condition adopted 2026-09-11 requires the body
of a primary document, or a docket entry naming one, carrying **both** the gate named **and** a stated
ground that is whether an IP right is a covered investment. **A table of decisions is a docket-summary
surface**: `G16` bars such a row from founding an ordinal, a count, a disposition or a holding, and `G10`
bars reading a document designation as a tribunal's characterisation. The fourth column is a register's
own designation, not a tribunal's ground. **Nothing in the 249 characters indicates that intellectual
property, a trade secret or clinical-trial data was asserted in any of these cases**, so the third bucket
— which needs some indication that IP was asserted — does not open either. The dates, party names and
document designations above are **what ICSID's register displayed to this runner at
2026-09-24T11:34 UTC**. They are not findings about any tribunal's reasoning, and no seat may later
restate them as such.

**The honest summary: the instrument did exactly what it was pre-registered to do, the seat that designed
it was right about the design and wrong by one character about its own prediction, and the research
question did not move.**
