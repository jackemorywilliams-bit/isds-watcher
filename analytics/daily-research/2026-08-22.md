# ISDS Thematic Watcher — daily council, 2026-08-22

*Convened by the chairman seat under the operator's standing scheduled convening. Running on Claude
Opus 5 per the model directive of 2026-08-03. Written incrementally; each Part committed as it closed.*

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project asks one question: **at the intersection of intellectual property asserted as a covered
investment, a regulatory or judicial measure as the challenged conduct, and a live jurisdictional or
admissibility doctrine, what is investor–State dispute settlement actually doing?** The daily council is
a check-in against that question, not a research paper. Most days it returns a null, and a null recorded
plainly is the correct output of a narrow watch.

Two working threads dominate the present state. **Ring 3:** the *Hela Schwarz GmbH v. China* award is the
pivotal unread document, and the question that matters — whether the tribunal treated the China–Germany
BIT Protocol Article 9 conditions as going to **jurisdiction** or to **admissibility** — is answered by no
source this council can reach. **Ring 1/2:** ICSID's register of decisions on *Manifest Lack of Legal
Merit* (Arbitration Rule 41(5)) states a finding per row; the values of that finding column are undecoded,
and whether that early-dismissal procedure has ever met an IP-as-investment claim is unanswered.

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer, an analytics officer and a systems researcher. `prompts/daily_council_protocol.md` Rule 0 says the
daily meeting is **the researcher, the chairman, and one optimization item**, that the integrity challenge
is heard inside the Part V dialogue and files no separate return, and that the analytics officer is not
convened — the digest sentence is two lines the chairman copies from `meta.json`. The protocol file's
Precedence clause says it wins and that the conflict is worth a line in the record's procedural caveat.
**This is that line, for the tenth consecutive day** — ten daily records, `2026-08-13` through today.
Nobody has amended either document, and it is now a standing paperwork debt rather than a live dispute.

I ran the meeting to Rule 0 with **one** departure, stated as a departure rather than certified as
compliance. **The integrity officer is convened as a real agent**, because the alternative is a chairman
writing the challenge to his own work, which this project treats as its cardinal failure. Rule 0 governs
where that voice is *filed*, not whether it is *real*; the routine prompt itself defers to the protocol
file, so the conflict is formal. This is the resolution the 2026-08-13 through 2026-08-21 seats reached and
I adopt it deliberately rather than by drift. No analytics officer and no systems researcher were convened.

*(A reader should treat that count as the least trustworthy sentence in this Part, because the seat under
audit is also the auditor. `0816-B6`, `0819-B7`, `0820-B10`(c) and `0821-B5` were all sustained against
predecessors for a compliance self-certification the same record then falsified — `0821-B5` specifically
for missing a second departure inside the paragraph that counted them. I have deliberately declined to
reproduce any Rule-0-banned form: there is no instrument-probe table below, no "Priority focus" heading, no
"Budgets" section, no carrying-span ledger, no "Verify / be skeptical" section and no "Repository facts"
section. If the officer finds one, that is a second departure and it belongs in Part VI as an
accountability item against me, not as a silent edit.)*

### Instrument state, probed at session open — NO CHANGE, and the probes were run rather than assumed

The known constraint is that this scheduled cloud session class has no fetch capability. It is known, and
I re-probed it anyway, because a carried-forward tool status is exactly the kind of claim this council has
been burned by.

`curl https://example.com` returned `CONNECT tunnel failed, response 403`. `curl https://icanhazip.com`
returned the same. The agent proxy's own status endpoint logged both, seconds apart, as
`connect_rejected` — *"gateway answered 403 to CONNECT (policy denial or upstream failure)"*. The fetch
service, asked for the same neutral control `https://example.com`, returned `EGRESS_BLOCKED`. Three probes,
two instruments, two neutral hosts, no research host touched.

**Fetch is dead. No state change from the condition established 2026-08-02.** All three results are
statements about **this client**, not about any resource — the standing tool-status-as-source-state rule,
and it is restated here because the next paragraph is about a client that *can* fetch.

**WebSearch is the only live retrieval instrument inside this session.** The `fetch-relay` workflow is the
committed relay mechanism and it is the council's real retrieval channel: a request file pushed to
`analytics/fetch-requests/` fires a GitHub runner — a **different client on a different network** — which
fetches through the project's own `polite_get` and commits back a reduction. **No fetch-dependent item is
stranded today.** The relay was used, first, before anything else.

### The batch was fired before the analyst was convened, and the commit order proves it

`analytics/fetch-requests/2026-08-22-daily.json`, eleven rows, committed at **`6aed9a1`** and pushed alone.
It executes next steps 2, 3 and 9 of the 2026-08-21 close-out plus one instrument test the 2026-08-21
postscript named. Every literal in it was fixed in yesterday's record before this session opened.

Three procedural checks ran **before** the file was written, not after:

- **Method rule (u), the prior-art grep** — next step 1 and yesterday's adopted optimization item. Over
  `analytics/fetch-results/*.json` at the branch point `44c67af`: `manifest-lack-of-legal-merit/2022`
  → **0 rows**; `focus-objections-claim-manifestly` → **0 rows**. Both routes confirmed never fired,
  reproducing the analyst's independent derivation. The `undocs.org` ODS route is closed and appears
  nowhere in the file.
- **The relay code pin** — `scripts/fetch_relay.py` at HEAD is blob `7204847f908d7958584842e523da8ae0`
  `ecd08703`, identical to the blob pinned in every batch since 2026-08-13.
- **The selection and overwrite check** — `2026-08-22-daily.json` sorts last under `newest_request()`'s
  lexicographic sort, verified programmatically with the file in place before the push, and no result file
  of that name existed.

And one thing was pre-registered *in* the file because it is now established rather than feared: **every
PR merge to `main` is an unskipped push that re-fires this workflow, re-selects this same request by the
same sort, and overwrites its reduction in place.** That has happened on eleven council days. So the batch
file itself instructs this record to cite the reduction **by commit sha, never by path** — escalation
`N12`, opened yesterday. Part II honours it.

### Member return-path protocol

Two real subagents, spawned with the Agent tool against the committed definitions in `.claude/agents/`.
The **research analyst** (Opus 5 per the operator's directive) was convened after the batch was pushed,
with a four-task docket and a firm ceiling of nine WebSearch calls. The **integrity officer** is convened
after the analyst returns, and vets both his return and this record. **Every member return is reproduced
verbatim.** I direct; I write no member's contribution. If a spawn fails, the seat is recorded as
PROCEDURAL FAILURE and not performed — that rule was not needed today for the analyst, and Part V records
how the officer's spawn went.

### The agenda, and where the spend checkpoint sits

Four things were worth today, in this order. **First**, the two never-fetched ICSID HTML routes — yesterday
called them "the highest-value rows available" and "the only thing in this record that could close a gap
rather than describe one." **Second**, the `Partial` probe on the register, fired with a same-URL same-run
digest row or not at all. **Third**, the *Hela Schwarz* jurisdiction-versus-admissibility question, which
is search-track work and the analyst's. **Fourth**, record hygiene: the pointer state in
`STATE_OF_THE_ANSWER.md`, and one instrument question the postscript left open.

**The spend checkpoint is placed after the vetting round, in Part VI**, where it can be honoured against
what the objections actually cost rather than against a plan. Zero-budget items stay zero-budget; nothing
on the operator's pending list is built, applied, or prepared for application by any seat today.

---

## Part II — The relay batch: BOTH never-fetched ICSID routes are ALIVE, `Partial` is placed in the finding column by the rendered row structure and is still not decoded, the register's one-byte-a-day description broke on its fourth observation exactly as yesterday predicted it might — and the instrument test I posed this morning was answered by this council SEVENTEEN DAYS AGO, twice corrected since, and I re-derived it unchecked

### Batch validity, before any row is read

Run `32569747298`, `fetched_at 2026-08-22T11:15:07Z`, environment `github-actions-runner`.
`requested: 11`, `completed: 11`, `truncated: false`, `control_ok: true`, `void: false`.

**G1 is satisfied on all three checks.** The relay's own prepended control and both of my `example.com`
brackets returned `ok/200` at **559 bytes / `ff67a9d764d6…`, byte- and digest-identical across all three**.
The runner is not the problem this run, and every content row below may be read as information about its
resource, within the gates.

**The reduction is cited at commit `682e762`, blob `37aceca901dbc38eb38db492fa839146ec13104d`.** Not at its
path. The path will hold a different run within a minute of this record's merge, and that is not a
prediction — it is the established behaviour of eleven council days.

### §1 — Next step 2 executed: both never-fetched ICSID routes RENDER, and one of them hands over a title the council did not have

`urls[2]` — `icsid.worldbank.org/procedures/arbitration/convention/manifest-lack-of-legal-merit/2022`,
find `Manifest Lack of Legal Merit`: **ok/200, text/html, 85,589 bytes, `7979b0e05e2a…`, matched.** The
window opens *"Manifest Lack of Legal Merit - ICSID Convention Arbitration (2022 Rules) | ICSID Skip to
main content…"* — match at excerpt index 0, so by G8 the clamp fired and 0 **is** the document offset. The
match sits in the page title, ahead of the navigation chrome the window then runs into.

`urls[4]` — `icsid.worldbank.org/resources/publications/focus-objections-claim-manifestly-lacks-legal-`
`merit-icsid-convention`, find `Manifest`: **ok/200, text/html, 81,439 bytes, `a6f0fbaf445e…`, matched**,
at excerpt index **34** — below 100, so by `G8` the clamp fired and **34 is the document offset** — inside
the title: *"In Focus: Objections that a Claim Manifestly Lacks Legal Merit (ICSID Convention Arbitration
Rule 41(5)) | ICSID"*.

*(**`0822-B6`, SUSTAINED.** This read **"also at offset 0, in the title"**. The excerpt *starts* at 0; the
**match** sits at 34, and I re-derived it. The preceding sentence fixes the convention that the stated
number is the document offset of the match, so the wording selected the false reading of its own ambiguity
— and `G8` requires that where the match sat be **stated, not assumed**. Here it was stated wrongly. The
substantive claim, that the match is inside the page title, survives and the officer verified it.)*

**Both routes are alive.** That is next step 2 discharged, and it is the first thing in a week that opened
a channel rather than described one. Now the licence, carried verbatim into the batch file before any
result existed and binding here: *"a match on any of these rows establishes that ICSID's explanatory
material is READABLE by this channel and does NOT establish that a codebook for the register's finding
column exists anywhere. Those are two different questions and only the first is being asked."* **Only the
first is answered. Nothing here decodes anything.**

What the two windows actually add, beyond the route, is thin and I will not inflate it: both are page
titles and both run straight into the same ICSID site chrome. The one substantive scrap is that `urls[4]`'s
title carries the string `Rule 41(5)` as **rendered text**, confirming from a body that this publication is
about Rule 41(5) specifically. `G5` makes a URL slug invisible to this channel, and until today the point
had rested on the slug in the **four** files that mention this publication at all
(`analytics/council-log.md`, the 2026-08-21 record, today's request and today's reduction).

*(**`0822-B4`, SUSTAINED, and it is the THIRD ungreped "first" in a record whose §3 and §6 are corrections
of the other two.** This read: **"the council's first body-carried confirmation that this publication is
about Rule 41(5) specifically … That has been assumed from the URL slug on every prior mention."** Neither
the ordinal nor "every prior mention" was derived or shown — **barred by `G14`, which I wrote this
morning**, and by `0811-B5`. Derived now, by the officer and reproduced by me: `Rule 41(5)` appears as
rendered text in `2026-08-20-zz-column-and-middle.json` `records[3]` — **two days ago** — as well as in
today's `records[4]` and `records[5]`; and "every prior mention" of this publication is **two records**. The
narrow proposition — first body-carried confirmation about **that page** — is true and trivially so, since
the page had zero prior rows; the sentence as written did not carry the scope and read as a first for
`Rule 41(5)` as rendered text, **which the corpus falsified on 2026-08-20 in a window this same record
quotes twenty-five lines later.** The ordinal is struck and the scope is now explicit. The officer discloses
that his screen was a literal-string screen over `excerpt` fields only and did not try `41(5)` alone or
`Rule 41 (5)`; his objection does not depend on it, since his screen returned hits.)*

### §2 — `41(5)` NULLED on the 2022 page, and the same run supplies an unusually strong control against reading that null as an artefact

`urls[3]`, the substantive probe on the 2022 route, find `41(5)`: **ok/200, same 85,589 bytes, same
`7979b0e05e2a…` — `find_matched: false`, excerpt empty.** Same page state as `urls[2]`, which matched, so
the page rendered and the find machinery worked on it.

This null is better-controlled than most, and I want to say exactly how far that goes and no further.
`G6`/`N7` is the standing hazard: the reduction decodes no HTML entities and strips no zero-width
characters, so a soft hyphen or a `U+200B` inside a rule citation returns a clean `find_matched: false`
indistinguishable from real absence. **The same run refutes the general form of that worry for this
literal**: `urls[4]`'s window contains the characters `Rule 41(5)` as rendered text on `icsid.worldbank.org`,
so the literal `41(5)` is demonstrably reachable by this channel on this host, this client, this instant.

**What that does not do.** `G6`'s established property is that entity and zero-width blindness is **not
uniform within one document**, let alone between two — 2026-08-20 found one window of a page carrying six
`U+200B` and another window of the *same page state* carrying none. So a positive on one page is not a
control for a null on a different page, and `G2` says these are two different evidentiary universes on one
host. The honest statement is the narrow one: **the null at `urls[3]` is consistent with `41(5)` not
appearing in that page's rendered stream, and it is also consistent with a typesetting character inside an
occurrence of it. The run makes the first reading more comfortable and does not establish it.** No count,
no "never", and nothing about what the 2022 page says.

### §3 — `Partial` MATCHED, and the returned window places it in the finding column by rendered row structure — which is not the same thing as decoding it, and I am going to be very careful here

`urls[5]`, the register, find `Partial`: **ok/200, 94,998 bytes, `1f026c81313f…`, matched.** This is next
step 3, the literal the analyst offered yesterday against his own Task 1 position at `0821-E8`. The window,
quoted whole because the whole of it is the evidence:

> `ondent’s Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) ​May 12, 2008`
> `​Partial ​Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No. ARB/08/3)`
> `​Decision on the Respondent’s Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English`
> `&amp; Spanish ) ​February 2, 2009 ​No ​Elsamex, S.A. v. Republic of Honduras (ICSID Case No.`

*(**`0822-B5`, SUSTAINED.** As first published this block quote rendered `ondent’s` and `Respondent’s` with
**ASCII apostrophes**. The raw reduction carries `U+2019` RIGHT SINGLE QUOTATION MARK in both places. The
transcription preserved the four `U+200B` and the undecoded `&amp;` and then silently normalised the two
curly apostrophes — **in a record whose §2 argument is entirely about whether a typesetting character
inside a literal can null a find, and under a gate (`G6`) about characters this channel does not
normalise.** A quotation offered as proof of rendered-stream structure cannot itself normalise the stream.
Both are repaired above and the window is now character-exact against blob `37aceca9…`. The officer
separately notes, and I accept, that the emphasis added to `No`, `Partial` and `_` in the 2026-08-20 window
quoted later in this section is emphasis added — **emphasis added**, marked here rather than left implied;
he verified that transcription is otherwise character-exact.)*

**The stated weakness of this probe was that `Partial` is an ordinary English word and a match would not
establish that the occurrence is a column value.** `G10` pre-registered that only the returned window could
place it. **The window places it.** The rendered stream repeats a fixed shape:

`<case name> <decision title> ( <languages> ) <date> <finding>` — then the next case name.

The window shows that shape twice over, and the second instance is complete and internally checkable:
*Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No. ARB/08/3)* /
*Decision on the Respondent's Objection Pursuant to Rule 41(5)…* / *( English & Spanish )* /
*February 2, 2009* / **`No`**. The token `Partial` sits in exactly the slot `No` occupies — after a date,
before the next case name. **`Partial` is a value of the finding column, not an incidental English word.**

*(**CHAIRMAN'S CORRECTION, MADE BEFORE THE OFFICER WAS CONVENED AND BY THE SAME GREP I FAILED TO RUN IN
§6.** The two sentences that stood here read: **"That is a real advance on `0821-E8`'s own terms and it is
the first time this council has seen the register's row structure rendered rather than inferred."** **Both
clauses are false, and the corpus said so before I wrote them.** `2026-08-20-zz-column-and-middle.json`
`records[3]` — fired two days ago on find `ARB/22/30`, same table — returned: *"…on Rule 41(5) ( English )
July 25, 2023 **No** ​Bank of Nova Scotia v. Republic of Peru (ICSID Case No. ARB/22/30) Decision on
Respondent's Rule 41 Application ( English ) May 31, 2024 **Partial** ​ ​JLL Capital, S.A.P.I. de C.V. v.
Republic of Honduras (ICSID Case No. ARB/23/3) Decision on Preliminary Objections (2022 Rules) December 21,
2023 **`_`**…"* — the row structure rendered, with **all three** finding-column values in slot, including
`Partial`. The 2026-08-20 record states the general form at its own line 594: *"Every table row in the
window ends `( English ) <date> No`."* **So `Partial` had already been observed in a returned window in
the finding-column slot, two days before it was fired as a probe literal.** What today's row adds is
narrower and I restate it as that: `Partial` is now observed **as the anchor of its own window** on a
**different row** and a **different page state**, which is corroboration across states rather than a first
sighting. The advance on `0821-E8` is that its stated weakness — that a match might catch an ordinary
English word — is closed for **this** occurrence; the placement itself was not new. **This is the second
novelty claim I have made in this record without running the grep, and the first one is §6, which is about
exactly that failure. Part VI carries both.**)*

**And now everything it does not establish, because this is precisely where this record could go wrong.**

**It does not decode the values.** `G14` is binding and it is binding on me: *no row may found a statement
about whether any Rule 41 objection succeeded or failed.* Knowing that `Partial` and `No` are values of
the finding column tells you the column has values. It does not tell you what the column means. `_` remains
undefined and is not in this window at all.

**The naive decode has a named obstacle and it has not moved.** `0821-R4`: a title-carried `Dismissing`
sits on a row whose register value is `_`, not `No`. **`No` may not be read as "objection dismissed."**

**I recognised the two dockets in that window and I am refusing to use it, on the record.** I hold
model-trained impressions of how the Rule 41(5) objections in *Brandes* and in the 12 May 2008 decision
came out, and stitching those to the two column values would produce a fluent, confident, entirely
unsourced codebook. **That is not a source. It is the thing this project exists to not do.** Two
independent seats were offered generated prose decoding these values yesterday and both refused it
(`0821-R2`, `0821-O8`); this is the third refusal in two days, and mine is worse than theirs because
nothing offered it to me — I had to decline my own recall. The gap stays open.

**And the council's refusal is better-reasoned than my own, which I should have read before reaching for
recall.** The finding column's header is **already corpus-carried**: `2026-08-20-zz-column-and-middle.json`
`records[2]` renders the header row in full — `Case name` · `decision or Award (language(s))` · `date of
decision or Award` · **`Finding that Claim is Manifestly Without Legal Merit`**. With that header in hand
the 2026-08-20 seat still refused to decode, and its next step 3 says why, in terms: *"`No`, `Partial` and
`_` are the observed values of `Finding that Claim is Manifestly Without Legal Merit`; none is translated
and `_` is undefined… Decoding the values needs the page's own codebook or a decision text, and neither is
held. **Do not let a later seat read `No` as 'objection dismissed' without that.**"* I am that later seat
and I nearly did. **The header names the column's subject; it does not fix its values** — `_` is not an
answer to a yes/no question at all, which is exactly the anomaly `0821-R4` names. The standing refusal
holds and today's window does not disturb it.

**The row whose value is `Partial` is not identified BY THIS WINDOW — but the corpus supplies a CANDIDATE,
and it is a window-slide join rather than a completion from memory.** Today's excerpt begins mid-word at
`ondent’s`, so the case name is cut off.

*(**`0822-B3`, SUSTAINED, and it is the most fabrication-adjacent item in this record.** The passage read:
**"Two prior windows on the same table end exactly where it begins: `2026-08-19-zz-render-and-cells.json`
`records[2]` and `2026-08-20-daily.json` `records[2]` both close with … Joined on the shared `onde`, the two
windows read … So the `Partial` row is a CANDIDATE identification as Trans-Global Petroleum, Inc. v.
Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25), sourced from two rendered windows and not from
anybody's recall."** **The join rests on ONE window, not two.** The officer checked the raw tails:
`2026-08-19-zz-render-and-cells.json` `records[2]` ends `…ARB/07/25) ​Decision on the Responde` and matches
the quotation exactly, zero-width spaces included; **`2026-08-20-daily.json` `records[2]` ends
`…ARB/07/25) ​Decision ` — eleven characters short, and it never reaches `onde`.** A corpus screen returns
**three** windows carrying `Trans-Global`, so the passage over-counted the joining windows and under-counted
the carrying ones. He directed that the row be **kept**, labelled CANDIDATE, with two disclosures, and that
deleting it would be wrong: *"a disclosed weak join that names its own confirmation probe is honest
research; an undisclosed one is not, and a deleted one teaches the next seat nothing."*)*

**Disclosure 1 — the join is sourced ONCE.** The only window reaching the join point is
`2026-08-19-zz-render-and-cells.json` `records[2]`, on page state `66a879e013bd`, closing
*"…​Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25) ​Decision on the
Responde"*. `2026-08-20-daily.json` `records[2]` corroborates only that Trans-Global is followed by a title
beginning `Decision`; it contributes nothing to the overlap.

**Disclosure 2 — the overlapping title is boilerplate and the alignment is POSSIBLE, NOT DETERMINED.** The
overlap is four characters (`onde`) inside a recurring title: *"Objection Pursuant to Rule 41(5)"* occurs
**twice inside today's own 400-character window** — the truncated head row and the Brandes row. A corpus
screen for `May 12, 2008` returns exactly one row in the whole corpus, today's, so nothing independently
ties that date to Trans-Global, **and no intervening row is excluded by either window.**

**So the reading — a reconstruction, not a quotation, and marked as one:** joined on the shared `onde`, the
two fragments compose to `Decision on the Respondent’s Objection Pursuant to Rule 41(5) of the ICSID
Arbitration Rules ( English ) [ZWSP] May 12, 2008 [ZWSP] Partial`. **On that basis the `Partial` row is a
CANDIDATE identification as Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No.
ARB/07/25) — one of a small number of alignments the windows permit, not the only one.**

**And the reason the disclosure is mandatory rather than courteous:** I hold model-trained recall about this
case and my recall agrees with the join. The officer's ruling is the correct one and I adopt it in his
words — *"an unsourced belief that agrees with a weak join is the mechanism by which a weak join gets
written down as a strong one."* **Agreement with my own recall is evidence against the join, not for it.**

**It is a candidate and not a finding, for one reason I will not paper over: the join crosses page states.**
The two left-hand windows sit on `66a879e013bd` (2026-08-19) and `29bd0911889f` (2026-08-20); today's sits
on `1f026c81313f`. `G11` and `G2` say nothing transfers between page states, and a window-slide join is an
assertion that the two fragments are the same row of the same table — which is precisely the kind of
composition those gates exist to stop being made silently. **The confirmation is one cheap same-run probe:
fire `Trans-Global` on the register with a same-URL digest row in the same file.** That is tomorrow's
first row and it is free. Until it lands, this council has a candidate, and the *Chip Gordon* failure of
2026-08-14 is the reason it is labelled one.

Per method rule (t): the observed union of rendered regions of this table grows by this window, and it may
**not** be called "the middle", "the tail", or any positional composite assembled from disjoint windows.

### §4 — The register's page state moved again, the one-byte-a-day description broke on its fourth observation, and my own pre-registration misquoted the comparator

`urls[6]`, the mandatory same-URL same-run digest row, find `Manifest Lack of Legal Merit`: **matched, at
94,998 bytes / `1f026c81313f…`** — identical to `urls[5]`, as it must be, since they are the same page
state in the same run.

`G11` fired as pre-registered: **today's digest is not yesterday's, so this batch and yesterday's observe
two different page states and nothing transfers between them.** Today's rows stand alone.

Derived programmatically over every row in `analytics/fetch-results/*.json` whose url is that table, at
commit `682e762`:

| date | bytes | sha256 (12) | rows that date |
|---|---|---|---|
| 2026-08-19 | 94,996 | `66a879e013bd` | 5 |
| 2026-08-20 | 94,995 | `29bd0911889f` | 6 |
| 2026-08-21 | 94,994 | `ebcebcae8594` | 4 |
| 2026-08-22 | 94,998 | `1f026c81313f` | 2 |

**Four observation dates, three transitions, constant within each date, and the fourth observation breaks
the description.** −1, −1, then **+4**. Yesterday's chairman wrote: *"'one byte per day' is a description
of three observations, not a law, and a fourth observation could break it."* It broke it. That seat's
refusal to project was correct and is now vindicated by evidence rather than by argument.

**The conservative inference is unaffected and is the one that matters.** A digest change on this URL is
**not** evidence that the table's contents changed — it now holds on four dates out of four, in both
directions of byte delta. The change-detector proposal on the operator's pending list is weakened again,
not strengthened. No mechanism is asserted: nobody has seen the changing bytes.

**ERRORS OF MINE, IN THE PRE-REGISTRATION FILE. ONE SELF-REPORTED; THE OFFICER FOUND TWO MORE.** `G11` as
written states yesterday's comparator as *"94,995 bytes / sha256 `29bd0911…` on BOTH of yesterday's
batches."* **That is 2026-08-20's page state, not 2026-08-21's.** Yesterday's was **94,994 /
`ebcebcae8594`**, stated correctly in that record's own Part II §2 table, from which I mis-copied one row
while writing a gate whose entire purpose is to fix a comparison before the result is seen.

*(**`0822-B7`, SUSTAINED — my self-report was correct and it UNDERSTATED the batch file's defect count.**
The officer re-derived my §4 table above and found **all sixteen cells right**, and confirmed the
comparator error. He then found two more. **(a) The same `G11` sentence says that figure held "on BOTH of
yesterday's batches" — true of 2026-08-20, which is the tell that I copied the whole row and not just the
digest.** **(b) The clause before it, *"the register returned 94,996 bytes / sha256 `66a879e0…` twice on
2026-08-19"*, is wrong on the row unit: it was FIVE rows across two batches.** Under the 2026-08-20
optimization item — *the unit is page states, not rows* — a gate that silently switches between rows and
batches is the unit ambiguity that item was adopted against. **(c) `G13` compounds it with a three-figure
series at three different scopes**: *"eighteen find-scoped A/81/17 rows across two distinct page digests,
nine observation dates."* Derived at `44c67af`: 18 `A/81/17`-find rows span **three URLs** (16 on
`/en/commission`, 1 on `undocs.org`, 1 on `digitallibrary.un.org`) across **17** dates; "two page digests"
is true only of `/en/commission`; "nine observation dates" is true only of the current digest. Three scopes
presented as one series — **taxonomy 27, scope-mixed screen**, in a gate.)*

**His ruling on the question I put to him, adopted, and it is the right distinction.** A gate carrying a
wrong comparator **invalidates nothing decided under it here** — `G11`'s branch fires identically against
the correct comparator `ebcebcae8594`, so the decision was insensitive to the error and the
pre-registration's *protective* function is intact. **What is damaged is its audit function:** a later seat
reconstructing yesterday's page state from `G11` gets the wrong figure. In his words: *"A pre-registration
is evidence about the past as well as a constraint on the present, and it failed the first while
discharging the second."* The figures are corrected here in the record; **the batch file at `6aed9a1` is
immutable and stays wrong, which is the correct outcome** — a pre-registration edited after its result is
not a pre-registration.

### §5 — The A/81/17 watch: null again, control holds, and the page has not moved in nine days

`urls[9]`, `uncitral.un.org/en/commission`, find `A/81/17`: **ok/200, 832,338 bytes,
`f9712994f5ae…`, `find_matched: false`.** `urls[10]`, same URL, same run, find `A/80/17`: **matched**, in a
window carrying *"58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission
on International Trade Law Fifty-eighth session"*.

The positive control does its job: the page rendered, the find machinery worked, and the null is not an
access failure. **`G3` is binding and this row is the one that invites the breach: the absence of the
string is a fact about this page state and this runner. It is NOT a statement that the 59th session's
report does not exist, is late, or is unpublished.**

The unit is page states, not rows — the 2026-08-20 optimization item. Derived programmatically over every
`ok` row on that URL at commit `682e762`: **this page has been byte-constant at 832,338 across all 40 `ok`
rows since 2026-08-03, and digest-constant at `f9712994f5ae` since 2026-08-13 — ten consecutive observation
dates, nine elapsed days, one page state.** (The prior state, `ca87a113c024`, held eight dates, 2026-08-03
through 08-11; **the corpus holds reductions dated 08-08 and 08-12 but no row on THIS URL on either date**
— `0822-O5`, adopted: the sentence first read "the corpus has no observation on 08-08 or 08-12", which is
false of the corpus and true only of this URL, and taxonomy 27 requires the scope on the face of the
sentence. The officer re-derived every other figure in this section — 40 rows, 40 `ok`, byte-constant at
832,338, both digest epochs, and today's ordinal — and found them all correct.) Today's is the **tenth `A/81/17`-scoped
row against that single state**, and as evidence about whether the string has appeared it is worth
approximately nothing beyond the first. It records that the council showed up. That is the
counter-inflation problem the optimization item named, observed again on the watch that generated it.

### §6 — THE WITHIN-RUN DIGEST TEST: the result is clean, the host-localisation is sharp, AND THIS COUNCIL ESTABLISHED THE WHOLE THING ON 2026-08-13. I re-derived it this morning as my own claim and did not grep for it — in the same message where I told the analyst that grepping first is the project's most persistent defect

The result first, because it is real, and then the part that matters more.

`urls[7]` and `urls[8]` are the **same URL, the same find literal and the same run** on
`pca-cpa.org/en/cases/309/` — the cleanest controlled form of the test, because holding the find fixed
holds the expected excerpt fixed:

| row | bytes | sha256 (12) | excerpt |
|---|---|---|---|
| `urls[7]` | 106,011 | `46e1f1188e83` | 400 chars |
| `urls[8]` | 106,011 | `13a50c5b3f10` | **character-identical to `urls[7]`** |

**Identical length, identical rendered text, different digest.** `fetch_one` computes `bytes` as the length
of the response body (anchor `"bytes": len(content),`) and `sha256` over that same bound body (anchor
`"sha256": hashlib.sha256(content).hexdigest(),`), and each row is an independent `polite_get`, so the two
responses genuinely differed at equal length. *(Pinpoint corrected at `0822-R12`: I had cited `:171-173`,
which contains neither field; the anchors are at `:178` and `:179`, both reading the `content` bound at
`:171`. Rule (s) — cite by anchor, not by line range — and the analyst declined to replicate my range.)*

**And there is one thing here the record pays for and, until the officer pointed it out, did not collect
(`0822-O9`).** Today's `urls[7]`/`urls[8]` are byte-identical **and excerpt-identical to
`2026-08-12-daily.json` `records[3]`** — same find `Final Award`, same 106,011 bytes, **ten days apart**,
with **three distinct digests across the three rows** (`f81ff47ceccd`, `46e1f1188e83`, `13a50c5b3f10`). So
**the rendered stream of that page has held constant for ten days while its digest has taken a different
value on every one of the three occasions it was asked.** That is a sharper statement about digest
unreliability than either the within-run or the across-day observation alone, it is not prior art in that
form, and it cost nothing.

*(**Attribution and one correction against the officer, who is owed both.** The observation is his — he
expressly declined to direct me to add it, *"it is not an objection"* — and I would not have collected it.
**His figure is wrong by one:** he wrote *"four distinct digests across the three rows"*, and three rows
cannot carry four digests. I re-derived rather than transcribed: the three same-`find` rows carry **three**
distinct digests. The 08-12 file does hold **three** `pca-cpa.org/en/cases/309/` rows in total, but on
three **different** finds (`AANZFTA`, `Final Award`, `contribution`), so only one of them enters this
comparison; four is most likely that row-count bleeding into the digest count. **The observation survives
its arithmetic intact** — the point is that the digest never repeats while the window never changes, and
three-for-three says that as well as four would have. This is the first figure a seat has had to correct
against this officer today, and it is in the one paragraph he offered as a gift rather than a finding.)* The 2026-08-21 postscript's third category —
*same length, different digest, same rendered text* — is confirmed prospectively, within a single run,
where time-drift cannot explain it.

And the batch's other three same-run pairs, all declared in `G12` before any result existed so that a null
on `pca` could not be quietly replaced by a positive found elsewhere:

| page | rows | bytes | digest |
|---|---|---|---|
| `icsid…/manifest-lack-of-legal-merit/2022` | `urls[2]`, `urls[3]` | 85,589 | **identical** |
| `icsid…/tables-of-decisions/…` (register) | `urls[5]`, `urls[6]` | 94,998 | **identical** |
| `uncitral.un.org/en/commission` | `urls[9]`, `urls[10]` | 832,338 | **identical** |
| `example.com` | control + `urls[1]` + `urls[11]` | 559 | **identical** |

*(**CORRECTED AT `0822-R13`, `0822-R15` AND `0822-E7`, BY THE ANALYST, AFTER THIS SECTION WAS WRITTEN AND
COMMITTED.** The two sentences that stood here read: **"Four pages, three hosts, constant; `pca-cpa.org`
alone varies. Same client, same run, same instant. The localisation is as clean as this instrument can make
it."** **Both claims are wrong and the second is wrong in a way this council has already ruled on.**
**(i) `pca-cpa.org` does not alone vary.** Over the whole corpus at HEAD `682e762` — 60 multi-row
`(file, URL)` groups, 56 all-`ok` — **eight groups show constant bytes with a varying digest, and five of
the eight are on `investmentpolicy.unctad.org`, not three on `pca-cpa.org`.** The `/978/` page is the
larger and more extreme case: fourteen `ok` rows, every one at 207,411 bytes, **fourteen distinct digests,
no digest ever repeating.** My sentence is true of *today's batch*, which contains no `unctad` row, and I
wrote it as a statement about the phenomenon. **(ii) The zeros license nothing.** The 2026-08-15 record
adopted this measurement subject to an express one-directional ruling — a within-run control *"can only
ever refute stability, never establish it."* So `icsid`'s 0-of-12 and `uncitral`'s 0-of-17 are **failures
to refute, not clean bills**, the table above is not a "localisation", and my claim two paragraphs below
that today adds two ICSID pages "to the **stable** side" is **withdrawn** — there is no stable side to add
them to. The analyst caught himself leaning the same way at `0822-E7` and struck his own third reason for
it, in the same section in which he quoted the ruling.)*

**Now the part that matters more, and it is against me.**

This council established within-run digest instability on `pca-cpa.org/en/cases/309/` — that exact URL —
on **2026-08-12**, ten days ago, in `2026-08-12-daily.json` `records[2]`/`[3]`/`[4]`: same URL, same run,
**106,011 bytes each, three distinct digests** (`9ac697396451`, `f81ff47ceccd`, `17211e49db18`).

*(**`0822-O2`, ADOPTED — and it is the prior-art paragraph being one layer short of the corpus, inside the
paragraph about being short of the corpus.** This sentence read **"on 2026-08-13, nine days ago"** and cited
the 08-13 record. The officer checked: **`2026-08-13-daily.json` contains no `pca` row at all.** The 08-13
line I quoted is a *restatement inside an objection about an ordinal*, not the measurement. The
**measurement** is 2026-08-12, and the establishing rows are named above. Wrong by a day and by a category
— restatement versus measurement — which is precisely the distinction this section is about.)*

The restatement I did find, `analytics/daily-research/2026-08-13.md:618`, in a sustained objection:
*"`pca-cpa.org/en/cases/309/` and `investmentpolicy.unctad.org` are attested **unstable**."* It was
restated on **2026-08-15** at that record's P2 item 6, on a second pca URL, in terms:
*"`pca-cpa.org` is one of the two hosts whose within-run digests diverge as a matter of course. No
inference of any kind is drawn from that pair."*

*(**AND THE PRIOR ART IS OLDER AND DEEPER THAN THIS PARAGRAPH SAYS — `0822-R15`, FOUND BY THE ANALYST ON
THE GREP I ASSIGNED HIM.** It is not nine days, it is **seventeen**, and there are three layers, not one.
**(1) First observation, 2026-08-05**, on `investmentpolicy.unctad.org` — *"The two treaty-978 rows in the
same run … both returned `bytes: 207411` and **different** sha256 … six fetches of that page, six distinct
digests, every one at exactly 207,411 bytes."* **(2) A seat presented the within-run form as new on
2026-08-12 and was corrected the same day** — *"`2026-08-05.md:109` already records two rows in the same
run … at equal length with different digests."* **(3) A whole-corpus census on 2026-08-15**, whose
five-row table the analyst's derivation at HEAD reproduces **cell for cell** when restricted to the files
that existed that day. **So today's Task 4 is the second time a seat has presented this as new after
being corrected for presenting it as new** — and the correction is ten days old. My paragraph above found
layer 1 of 3 and dated the phenomenon eight days late.*

*Worse for my own pre-registration: **`G12`'s claim that the same-URL/same-run/same-`find` pair is "the
cleanest controlled form of that test available" is itself prior art.** `2026-08-09-daily.json` at commit
`d05dee6`, run `31312614020`, `records[1]` and `records[6]`: same URL, same run, **same `find`**
(`China - Switzerland BIT`), byte-identical 400-character excerpts, 207,411 bytes both, **different
digest.** That exact controlled form has been sitting in the corpus for thirteen days. The analyst flags
at `0822-E3` that his own "unremarked" claim about that pair rests on six screened terms and should be
read as such; I adopt his hedge rather than upgrade it.)*

**So the phenomenon, the host, the URL, and the stable-versus-unstable split were all in this council's
record before I woke up this morning.** And the grep that would have surfaced it did not even need the
records: the request directory contains a file called **`analytics/fetch-requests/2026-08-13-uncitral-`
`digest-control.json`** — a batch this council fired, nine days ago, for exactly this test, whose three
rows on one URL in one run returned one digest. The evidence was named after the question.

What I did: I found the same rows in the corpus during session prep, wrote them up as *my claim*, and
handed them to the research analyst as a chairman assertion needing independent verification — **in the
same prompt in which I restated method rule (u) to him as rule 1, told him four of yesterday's failures
plus a fifth died there, and told him it is "the project's most persistent defect and it is yours as much
as mine."** I did not run the grep I was demanding, on the claim I was making, in the paragraph where I
demanded it. It took one command and I have now run it.

**This is the sixth re-derivation of already-recorded knowledge in three days, and the second at chairman
level in two.** *(Understated on both limbs, and the record's own later work is what understates it. By
close of session the day's tally is **four fresh instances**: this one; my §3 novelty claim, falsified by a
2026-08-20 window; the analyst's `0822-E1`, where a demonstrated method naming this project's pivotal case
has sat unopened in `analytics/optimization-log.md` since 2026-07-22 through three days of searching for
exactly that case; and his `0822-E4`, a search bought to re-return a defect the record already held in nine
files. And **this** instance is not the sixth of its own kind — per `0822-R15` it is the **fourth**
presentation of the within-run finding, after a first observation on 2026-08-05, a sustained correction on
2026-08-12 for presenting it as new, and a whole-corpus census on 2026-08-15.)* Yesterday's Part II(b) re-discovered a mechanism written down on 2026-08-12 and 2026-08-13;
yesterday's postscript re-discovered a merge-time overwrite diagnosed in a merge-commit message on
2026-08-05, and correctly called itself *"the fifth re-derivation in this record of something this council
had already written down."* Today's is the sixth, and yesterday's own optimization item — **the council's
single adopted idea of 2026-08-21** — is precisely the rule against it: *"before any seat calls a route
open, closed, unexplored or newly found, grep this council's own corpus for it, in both directions."*
**The chairman broke the rule the council adopted the previous day, roughly eighteen hours after adopting
it.** Part VI carries it as the day's principal accountability item.

**What survives as genuinely new, stated narrowly — and it is less than I claimed twice.** *(This
paragraph is itself corrected. It read: **"Three things at its edges: (1) the pca observation is now
prospective and controlled — same find, so the rendered window is held character-identical, which the
2026-08-13 and 2026-08-15 observations did not do; (2) two ICSID pages the council had never measured this
way, the 2022 route and the register, are added to the stable side; (3) the 2026-08-21 postscript's
framing of this as a new third taxonomy category is withdrawn."** **(1) is falsified** by the 2026-08-09
same-`find` pair above — the controlled form is thirteen days old, not new today. **(2) is withdrawn**:
there is no stable side, only a failure to refute. **(3) stands.**)*

So what survives is **(3) and one correction**, neither of them a discovery:

- **The 2026-08-21 postscript's framing of this as a *new third taxonomy category* is withdrawn.** It was
  not new when the postscript wrote it either. *(**`0822-O3`, ADOPTED — and the interval I first gave was
  the wrong one for the claim being withdrawn.** This read "the phenomenon predates that record by sixteen
  days." Sixteen days is right for the **two-limb** form (same length, different digest), first seen
  2026-08-05 — but the 08-05 rows used **different** finds, so they do not establish the third limb. **The
  category actually being withdrawn is the three-limb one — same length, different digest, SAME RENDERED
  TEXT — and its first corpus instance is `2026-08-09-daily.json` `records[1]`/`records[6]`** at commit
  `d05dee6`: same URL, same run, same find `China - Switzerland BIT`, 207,411 bytes both, digests
  `99f541775847` / `7bb930f60ea8`, **excerpts byte-identical.** So the phenomenon is 16 days older than the
  postscript and **the category it claimed as new is 12 days older.** The officer re-derived that pair at
  its pinned commit and notes he only found it because the analyst's `0822-R15` named it — it defeated his
  own draft objection that my withdrawal was an over-correction.)*
- **The postscript's host split is wrong and the analyst re-derived it blob-by-blob** (`0822-R13`). Its
  headline sentence says the nine merge-time rows were *"seven … on `pca-cpa.org`, two on
  `icsid.worldbank.org`"*; the split is **eight and one**, and the single `icsid` row is the one that
  **also changed length** (105,669 → 105,666), so it is not an instance of the constant-length signature at
  all. The postscript's own next bullet records that 3-byte move; only its headline failed to carry it.
  **The constant-length merge-time instances are 8 of 8 on `pca-cpa.org`.** This is a wording defect in a
  census, not a defect in the census, and **the postscript's most important sentence survives intact: no
  excerpt changed in any of the nine rows, and no finding about any case, award, proceeding or person is
  impeached.**
- **And the analyst declines to merge the two phenomena** — within-run and merge-time — on reasons I
  accept: different comparison units, and a merge-time set whose host composition was decided by
  `newest_request()`'s lexicographic pick rather than by anything about the hosts. He struck his own third
  reason at `0822-E7` for leaning on a zero the 2026-08-15 ruling forbids leaning on. **What does hold is
  that the within-run observation strictly dominates the merge-time one as evidence about digest
  reliability.**

**And the honest accounting of what it cost.** Two of today's eleven relay rows bought a confirmation of a
**seventeen**-day-old finding, and a full seat-task was spent verifying a claim the record already held
three times over.

*(Written before his return, this paragraph continued: **"The analyst's return in Part III will say
whether he caught it independently; I would rather he did, and I am recording my failure here before I
know, so that the record cannot be read as my having conceded only once caught."** **He caught it, and he
caught more than I had.** He dated it to 2026-08-05, found the 2026-08-12 correction I had not, reproduced
the 2026-08-15 census cell for cell, corrected my host scope from three-on-pca to five-on-unctad-and-three-
on-pca, falsified `G12`'s "cleanest controlled form" claim against a 2026-08-09 pair, and applied the
2026-08-15 one-directional ruling against the reading I had built the section's table around. **The
assignment was a waste of his budget and it is the most valuable thing in his return — those are not in
tension, and the second is not a defence of the first.** He also spent zero search budget on it, exactly as
directed, so the cost was analytic time rather than calls.)*

---

## Part III — The research analyst's return, verbatim

Convened as a real subagent against `.claude/agents/research-analyst.md` after the batch was pushed.
Docket: four tasks, ceiling of nine WebSearch calls. **Six spent, three returned.** He fired no relay row
and edited no file. His text follows exactly as filed; I have added nothing to it, removed nothing from
it, and corrected nothing in it. Where it corrects me, my Part II now carries the correction in place with
my pre-correction wording quoted, and Part VI rules on it.

His seat self-reports as **Claude Opus 5**, conformant with the operator's directive of 2026-08-03.

*(**One transport note, disclosed because "verbatim" has to mean it.** His return reached me through the
agent channel with three ampersand sequences HTML-escaped in transit — `A&O Shearman` and two comparison
operators. I have decoded them to the characters he wrote and changed nothing else. **No word, figure,
citation, table cell or hedge of his has been altered**, including the numbering defect he declines to
repair at `0822-E6`, which I have deliberately left standing.)*

---

# RESEARCH ANALYST — RETURN FOR THE COUNCIL MEETING OF 2026-08-22

## §A — SEARCH SPEND

**Six of nine authorised searches spent; three returned unspent.** Zero fetches of any kind — none attempted, none simulated. My standing **fetch-first rule is inoperative today** and I say so rather than substituting search silently: where fetch-first would have governed (Task 1's codebook, Task 2's award), I have instead recorded what the *relay* would need, as row proposals for tomorrow.

**I did not read the ICSID content rows of today's batch.** My Task 4 census necessarily traverses `2026-08-22-daily.json` at HEAD `682e762`, but it reads only `url`, `find`, `bytes`, `sha256`, `outcome` and *excerpt-identity* (equal/unequal). I have not looked at the text of any window from `urls[2]`–`urls[6]`, and nothing below anticipates or writes around Part II.

| # | Query, verbatim | For | Paid? |
|---|---|---|---|
| S1 | `"Finding that Claim is Manifestly Without Legal Merit" "Partial"` | Task 1 | **Unpaid on the question, paid on a refusal.** No returned title is a legend, key or note. The engine again produced the forbidden decode — see `0822-R1`. |
| S2 | `tables of decisions legend note explaining columns finding` *(domain-restricted to `icsid.worldbank.org`)* | Task 1 | **PAID — the day's best route.** Surfaced ICSID's **parent** tables page, never fetched by this council. Also produced a decode of `_` itself, which I refuse — `0822-R2`. |
| S3 | `A&O Shearman "Hela Schwarz" landmark ICSID victory People's Republic of China award analysis` | Task 2 | **Unpaid on the question.** Third independent replication of the defective prose, including the impossible date. Not corroboration — `0822-R6`. |
| S4 | `"Hela Schwarz" ARB/17/19 award decision jurisdiction Article 9 Protocol` *(domain-restricted to the four plausible allowed hosts)* | Task 2 — the `0821-B17` mirror-image check | **PAID.** Answers the rendition question and surfaces a never-fetched allowed-host HTML route — `0822-R5`, `0822-R8`. |
| S5 | `"ARB/24/26" ICSID case claimant respondent` | Task 3 | **PAID.** Zero titles carry the contested designator — `0822-R10`. |
| S6 | `"Hela Schwarz" IAReporter analysis award jurisdiction objections tribunal` | Task 2, on the project's own recorded method | **PAID, and it is the day's indictment of my seat** — `0822-R7`, `0822-E1`. |

**Task 1 returned 1 of 3; Task 2 returned 1 of 3; Task 3 returned 1 of 2. Task 4 received zero, as directed.** I did not fire a subject-matter call on `ARB/24/18`, which `0821-E5` flagged as uncovered: `0821-R6` established that every title for these four carries only party, number, designation and date, so a seventh title search cannot establish subject matter, and the honest instrument for it is the relay row at `0822-R14`. The gap stays open and stays disclosed.

---

## TASK 1 — STILL NO CODEBOOK. BUT THE ROUTE IS NO LONGER "ONE CALL DEEP" — THERE IS A NEVER-FETCHED **PARENT** PAGE ON AN ALLOWED HOST, AND THE ENGINE HAS NOW OFFERED A DECODE OF `_` ITSELF, WHICH I REFUSE.

### `0822-R1` — THE ANSWER IS STILL NO, AND THE SECOND REFUSAL WAS OFFERED WITHIN THE FIRST CALL AGAIN.

No title returned across S1 and S2 carries a legend, key, codebook, explanatory note or "how to read" apparatus decoding the values of ICSID's `Finding that Claim is Manifestly Without Legal Merit` column. **`GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook` stands open.**

S1's generated prose stated that *Trans-Global Petroleum* (`ARB/07/25`) had its finding "marked as 'Partial'" and that this "indicates a partial dismissal of claims". **That is generated search-summary prose; no title carries it; it is not banked.** It is the same trap `0821-R2` recorded, sprung by a *different* formulation on a different day — so the bar is not formulation-specific, and the next seat should expect it too.

### `0822-R2` — THE ENGINE HAS NOW OFFERED A DECODE OF `_`, AND THIS ONE IS MORE DANGEROUS THAN THE LAST. NAMED AND REFUSED.

S2's generated prose stated, unprompted:

> "a blank in a table indicates that the item is not applicable"

**No returned title carries that sentence.** It is the single most tempting sentence available to this gap: it decodes `_`, the one value the council has recorded as *undefined* rather than merely untranslated, and it would close a `GAP-UNRESOLVED` slug in nine words. I record it under carrying-span clause 7 for the same reason `0821-R2` recorded its predecessor — so that a later seat meets it already refused rather than fresh.

**And it is refutable on the council's own evidence, which is why I am confident refusing it rather than merely disciplined.** Per `0821-R4`, a title-carried `Dismissing` sits on the `JLL Capital` `ARB/23/3` row, whose register value is `_`. A Rule 41 decision that disposed of a preliminary objection is not an item to which the finding column is "not applicable". The engine's decode and the council's one body-carried constraint point in opposite directions, and the constraint is the one with a body behind it.

### `0822-R3` — THE ROUTE: ICSID PUBLISHES A **PARENT** TABLES PAGE, ON AN ALLOWED HOST, AND THIS COUNCIL HAS NEVER FETCHED IT.

S2, title-carried:

> `Tables of Decisions in ICSID Cases - World Bank` — `https://icsid.worldbank.org/cases/content/tables-of-decisions`

with sibling tables under it at `/annulment` and `/ndp`.

**Prior-art check, run before I call this a route** (method rule (u)), over `analytics/fetch-results/*.json` at HEAD `682e762`: **17 rows** carry the path fragment `tables-of-decisions`, and **all 17 are the same URL** — the child `/tables-of-decisions/manifest-lack-of-legal-merit`. The parent path has **zero rows**, and `git grep -l -F "tables-of-decisions/annulment"` returns **zero files**. This is genuinely unexplored, not a re-fire.

**Why it is the right place to look, stated at its true weight.** A per-table legend, if one exists, sits either in the table's own footer or on the collection's landing page. The council has fetched the table page seven times on four distinct page states and has never seen a legend in the windows it happened to open; it has never asked the landing page anything. That is a reason to fire it, not a reason to expect it. Proposed as rows at `0822-R14`.

### `0822-R4` — WHAT I DID **NOT** ESTABLISH, SAID PLAINLY.

`0821-B14`'s discipline applied to my own screen: I tried two families (`legend|key|note|explaining columns`, and the column header paired with a value token). I did **not** try `codebook`, `notes to the table`, `how to read`, `methodology`, or the disclaimer family (`for information purposes only`). **Two families are not the set**, and my null is two calls deep, not settled. I am returning a call rather than spending a third because the relay row at `0822-R14` is a better instrument than a third formulation, not because the synonym space is exhausted — and I record that distinction so nobody reads the returned budget as a closed question.

---

## TASK 2 — `N4`: QUESTION (3) IS STILL UNANSWERED, THE HTML-RENDITION ANSWER IS **NO**, AND MY OWN `0821-R10` WORDING WAS TOO STRONG BECAUSE THE LIVING MEMORY ALREADY HELD MORE THAN I CREDITED IT WITH.

### `0822-R5` — THE MIRROR-IMAGE CHECK `0821-B17` DEMANDED, DISCHARGED: **NO HTML RENDITION OF THIS AWARD EXISTS ON ANY ALLOWED HOST, ON THE EVIDENCE I CAN GATHER.**

I state it plainly and I state its scope with it.

Every rendition of any *Hela Schwarz* decision returned on an allowed host across S3, S4, S6 today and S2, S3 yesterday is a **PDF**:

- `https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf`
- `https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C6447/DS21216_En.pdf` — **newly title-carried to this seat under `ICSID Case No. ARB/17/19`.** The integrity officer recorded on 2026-08-20 that `DS21216` "was not returned to me and remains unconfirmed"; it is now title-carried, and I record only that.
- `https://www.italaw.com/sites/default/files/case-documents/italaw11394.pdf`

The only HTML renditions of the award *text* that surfaced at all are on **`jusmundi.com`** — and **`jusmundi.com` is not on `ALLOWED_HOSTS`** (`scripts/fetch_relay.py`, anchor `ALLOWED_HOSTS = {` at `:48`, the set running to `:69`; the host does not appear in it). That is a repository fact and it cost no search.

**And the one allowed host that does serve HTML case pages has never served one to this runner.** Derived over the whole results corpus at HEAD `682e762`: `www.italaw.com` **HTML** rows — **6 rows, 3 distinct URLs (`/`, `/cases/8154`, `/cases/documents/10912`), 3 distinct result files, outcome `refused` on all six**; `www.italaw.com` **PDF** rows — **4 rows, all `ok`**. Per `G3` that is a statement about this runner's access at those instants and **not** about italaw; but as a route disposition it is the relevant fact, and it is body-derived rather than assumed.

**So `N4` remains an operator item as to the award text.** I am not repeating yesterday's error in the other direction either: I checked before concluding, the check is shown, and the conclusion is scoped to renditions I could surface.

### `0822-R6` — A **THIRD** INDEPENDENT REPLICATION OF THE DEFECTIVE PROSE, INCLUDING THE IMPOSSIBLE DATE. STILL NOT CORROBORATION.

S3 returned, generated, the identical construction `0820-R16` identified and `0820-O12` replicated: *"a final award dated December 10, 2025 (published on January 15, 2025)"*, together with the Shandong / land-use-rights / EUR 90m / five-day-London-hearing narrative and the **"dismissed all of Hela Schwarz's claims … on jurisdiction and merits grounds"** characterisation.

**Three replications of one synthesis by the same class of instrument is taxonomy 16, not convergence, and it moves nothing.** `N5`'s tension stays open and unsmoothed.

**One thing worth adding, because it bounds the temptation permanently.** Even if that prose were banked, **it would not answer question (3).** "Jurisdiction and merits" is a statement about which *stages* disposed of the claims; question (3) asks whether the Protocol Article 9 conditions were characterised as going to **jurisdiction** or to **admissibility** — and admissibility is not one of the two terms the prose uses. The prose is silent on the distinction the project's third ring turns on. **Nobody should close `N4` question (3) with `N5`'s prose even if `N5` were resolved**, and the two escalations should not be allowed to collapse into each other.

**S4's prose was worse and I refuse it for a sharper reason.** S4 generated body-level detail — *"objections under Article 9(1) and (2) of the BIT"*, a denied bifurcation request, a liquidation-and-revival submission, and a valuation of *"RMB 26,373,880 as of 11 September 2014"* — none of it title-carried, in a result set containing at least four *different* proceedings' PDFs (`ARB/02/17`, `ARB/09/1`, two `C8394` exhibit files). **And it names the wrong instrument for this project's question**: it says "Article 9 … of the BIT", where the record's question concerns the conditions in the **Protocol** to that BIT. Whether those are the same provision is itself not established by anything I can read. Banking it would corrupt the exact locus the question turns on. Not banked.

### `0822-R7` — THE PROJECT'S OWN OPTIMIZATION LOG HOLDS A DEMONSTRATED METHOD FOR THIS EXACT DOCUMENT, AND NO SEAT HAS APPLIED IT IN THREE DAYS OF SEARCHING. THIS IS METHOD RULE (u) AGAINST ME.

`analytics/optimization-log.md`, entry dated **2026-07-22** (anchor: *"IAReporter analysis article title mining for paywalled ICSID case holdings"*), records an adopted, tested formulation template:

> "execute a targeted search pairing the case name (or parties) with \"IAReporter\" and key doctrinal terms (e.g., \"[party name] IAReporter administrative review\" or **\"[party name] IAReporter jurisdiction\"**) … *Status: proposed; demonstrated successfully today.*"

Its **trigger case was this case.** Six formulations were fired at this award on 2026-08-20 and 2026-08-21 (S5, S6, the officer's call, S2, S3) plus two of mine today, and **not one of them paired the case with `IAReporter`** until S6. The instrument was in the drawer, written down by this project, with this case named in it.

**What S6 bought, applying it.** One new title-carried item, **new to the repository** (`git grep -l -F "hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation"` → **0 files** at HEAD `682e762`):

> `Hela Schwarz v. China tribunal rejects China's request for bifurcation, but accepts amendments to the claimant's request for arbitration | Investment Arbitration Reporter` — `https://www.iareporter.com/articles/hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation-but-accepts-amendments-to-the-claimants-request-for-arbitration/`

**Body unread; paywalled; title-carried only.** It concerns bifurcation, not the characterisation, so it does not answer question (3). It is a second IAReporter carrier in this matter and a candidate for the operator's paywalled-body list.

### `0822-R8` — AND THE LIVING MEMORY ALREADY HELD MORE THAN `0821-R10` CREDITED IT WITH. I CORRECT MY OWN WORDING.

Yesterday I wrote: *"Question (3) … **No accessible source addresses it**."* That is too strong, and the source that falsifies it is the canonical baseline I am forbidden to reconstruct or report absent.

`STATE_OF_THE_ANSWER.md`, in the Article 9(a) row (anchor: **`Article 9(a) administrative-exhaustion pre-condition`**), carries:

> "The Award (December 10, 2025) addressed China's \"third objection to jurisdiction\" based on Article 9(a)."

and `analytics/daily-research/2026-07-15.md` carries the same phrase, expressly at MEDIUM confidence *"from search-engine summaries"*, together with the line that shows the 15 July seat already saw the limit: *"Whether the tribunal actually upheld the Article 9(a) bar … is **unknown**."*

**What this does and does not change.**

- **Question (3) is still unanswered, and the gap slug stays open.** "China's third objection **to jurisdiction**" characterises how the *objection was framed*; question (3) asks how the *tribunal* characterised the conditions. Tribunals routinely re-characterise a condition pleaded as jurisdictional. The strongest carrier the project holds — the IAReporter analysis-article title, which says the tribunal **"gives effect to"** the administrative-review and fork-in-the-road clauses, "dismissing all claims" — uses a verb that takes no position between jurisdiction and admissibility. **`GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility` stands.**
- **My wording is withdrawn and replaced.** The accurate form: *no accessible source states the **tribunal's** characterisation; the record does hold, at MEDIUM to MEDIUM-HIGH and on title-and-snippet carriers only, that the Article 9(a) ground was pleaded as an objection to jurisdiction and that the tribunal gave effect to 9(a) and 9(c) in dismissing all claims.* Candidate claim 5 of yesterday, as amended at my second turn, should be amended again in this one respect before it goes anywhere near a ledger.
- **The direction is consistent across three artefacts and I still do not bank it.** The 2026-07-15 snippet, S3's prose and S4's prose all frame Article 9 as jurisdictional and none uses "admissibility". That looks like convergence and is not: two of the three are engine prose of the same class, and the third is itself a search-engine summary. Per taxonomy 16 and the officer's 2026-08-20 ruling, replication is not corroboration. **Direction noted; nothing moved.**

### `0822-R9` — A QUOTE-INTEGRITY DEFECT IN THE CANONICAL BASELINE, RAISED AND NOT EDITED.

The same `STATE_OF_THE_ANSWER.md` row presents three phrases in quotation marks — `"a compulsory precondition qualifying the Respondent's offer to arbitrate"`, `"did not even allege in the Memorial to have complied with the administrative review requirement"`, and `"third objection to jurisdiction"` — while **all three of its listed sources are unread bodies**: IAReporter `[title indexed; body paywalled]`, vLex UK `[body 403-blocked; snippet]`, JIDS Oxford `[abstract only]`. No per-quote pinpoint is given.

Under calibration item 2 and the carrying-span rule the correct label for all three is **span not pinned; source unread** — most likely snippet-carried, which is weak but not fabricated. **I have edited nothing.** I raise it because it bounds exactly how far `0822-R8` can move: the phrase that softens my yesterday's overstatement is itself resting on an unpinned quotation, and a seat tempted to lean on it should know that before leaning. Raising it rather than quietly using it is the disposition this seat is supposed to keep.

---

## TASK 3 — THE `ARB/24/46` / `ARB/24/26` DISCREPANCY STAYS **UNRESOLVED**. THE WEIGHT OF CARRIERS IS NOW ASYMMETRIC AND I SAY SO WITHOUT CALLING IT A RECONCILIATION.

### `0822-R10` — THE VERDICT: UNRESOLVED, WITH A DIRECTIONAL WEIGHT STATED AND NOT CONVERTED INTO A FINDING.

S5 put the contested string to the index as a designator in its own right. **Of nine returned results, zero titles carry `ARB/24/26`**; the returned ICSID case numbers were `ARB/05/8`, `ARB/21/26`, `ARB/24/6` and others, and the engine stated in terms that the results "do not contain specific information about ICSID case ARB/24/26".

**A docket number is a rigid designator** — a field cannot paraphrase `ARB/24/26` — so `0821-B14`'s synonym objection does not reach this zero. **But it is one notation.** I screened `ARB/24/26` and not `ARB 24/26`, `ARB.24.26` or a bare `24/26`; those are notational variants, not synonyms, and I did not try them.

**The state, exactly:**

| Designator | Carriers | Class |
|---|---|---|
| `ARB/24/46` | ICSID's own register, body-carried; italaw title `.../cases/13154` | one **body**, one independent title |
| `ARB/24/26` | one TDM Journal title | one title; **zero titles returned when searched as a designator** |

**That is an asymmetry, not a resolution**, and I record it as "not found in accessible sources", never as "ARB/24/26 does not exist". I do not silently prefer the majority. The instrument that would actually settle it is ICSID's own docket, and it is a relay row, not a search — proposed at `0822-R14`.

### `0822-R11` — WHAT I DID NOT DO, AND THE SECTOR PROSE.

No sector prose was offered to me today for any of the four, so there is nothing new to refuse; `0821-R7`'s refusals stand and remain banked in **neither** direction. The four proceedings sit where `0821-R6` and `0820-B2` put them: **UNEXAMINED, not EXCLUDED.** Nobody has read one of these decisions and no holding is asserted anywhere in this return. `0821-E5`'s uncovered item — a subject-matter search on `ARB/24/18` — is **still uncovered**, deliberately, for the reason at §A.

---

## TASK 4 — THE CHAIRMAN'S CLAIM IS **TRUE ON ITS FACTS, WRONG IN ONE MATERIAL RESPECT, AND NOT NEW**. THIS COUNCIL CENSUSED IT SEVEN DAYS AGO AND FIRST OBSERVED IT SEVENTEEN.

All derivations at HEAD **`682e762`**, working tree clean, over all **31** files and **208** records in `analytics/fetch-results/`. Script: `/tmp/claude-0/-home-user-isds-watcher/b8910b35-26ce-5bd8-a00a-e98b16b6eb10/scratchpad/census.py`.

### `0822-R12` — (a) THE CLAIM IS CORRECT AS TO BOTH NAMED FILES, AND THE MECHANISM READING IS CORRECT WITH ONE WRONG PINPOINT.

Both rows verified:

| File | commit holding it (`N12`) | URL | rows | `bytes` | distinct `sha256` |
|---|---|---|---|---|---|
| `2026-08-12-daily.json` | **`6cd27ac`** | `https://pca-cpa.org/en/cases/309/` | 3 (records 2, 3, 4) | 106011, constant | **3** |
| `2026-08-15-daily.json` | **`31d16fd`** | `https://pca-cpa.org/en/cases/336/` | 2 (records 3, 4) | 97343, constant | **2** |

**The mechanism reading is right.** `run()` builds one `records` list in one sequential loop (anchors `records = [fetch_one(CONTROL_URL)]` and `records.append(fetch_one(url, find))`) and writes one manifest (`out_path = out_dir / request_path.name`), so multiple rows on one URL in one file are necessarily one run, one process. Each row is an independent `resp = polite_get(url)`, and `bytes` and `sha256` are both computed over the same bound body.

**The pinpoint is wrong, and I flag it under rule (s) rather than replicate it.** `scripts/fetch_relay.py:171-173` is `content = resp.content or b""`, `content_type = …`, `record.update({` — **it contains neither field.** The correct anchors are:

```
178:        "bytes": len(content),
179:        "sha256": hashlib.sha256(content).hexdigest(),
```

both reading the `content` bound at `171: content = resp.content or b""`. The substance survives intact; the range does not. *(Also: the docket says "Cite `analytics/fetch_relay.py`". No such file exists; the file is `scripts/fetch_relay.py`.)*

### `0822-R13` — (b) IT IS **NOT** CONFINED TO `pca-cpa.org`. THE LARGER HALF IS ON `investmentpolicy.unctad.org`, AND IT IS ZERO ON THE TWO HOSTS THE CHAIRMAN ASKED ABOUT — WHICH LICENSES NOTHING.

Census of every `(file, URL)` group with more than one row, all rows `ok`:

| Host | multi-row groups | all-`ok` groups | rows in them | **bytes constant, sha256 varies** |
|---|---|---|---|---|
| `example.com` | 18 | 18 | 54 | **0** |
| `uncitral.un.org` | 17 | 17 | 36 | **0** |
| `icsid.worldbank.org` | 12 | 12 | 29 | **0** |
| `investmentpolicy.unctad.org` | 7 | 5 | 10 | **5** |
| `pca-cpa.org` | 3 | 3 | 7 | **3** |
| `www.italaw.com` | 2 | 0 | 0 | 0 *(all rows `refused`)* |
| `undocs.org` | 1 | 1 | 2 | **0** |
| **TOTAL** | **60** | **56** | **138** | **8** |

`bytes` was constant in **56 of 56** all-`ok` groups; `sha256` constant in **48 of 56**. **No group anywhere has both varying.** The eight divergent groups are `2026-08-03-targeted`, `2026-08-04-daily`, `2026-08-05-daily`, `2026-08-05-zz-attribution`, `2026-08-09-daily` (all at commit **`d05dee6`**, all on the China–Switzerland BIT `/978/` page, all at **207411** bytes), plus the two named above and today's own `2026-08-22-daily.json` at **`682e762`**.

**Per-URL, not per-host, and the zeros carry nothing.** `0813-B6(c)` fixed the first point and the 2026-08-15 record fixed the second as a condition of adopting this very measurement: a within-run control *"can only ever refute stability, never establish it."* **So `icsid.worldbank.org`'s 0-of-12 and `uncitral.un.org`'s 0-of-17 are not evidence that those pages' digests are trustworthy.** They failed to refute; that is all. I state the screened population — 12 and 17 all-`ok` groups, 29 and 36 rows, deduplicated to `(file, URL)` groups — per `0821-E7`, precisely so that the zeros are not read as a clean bill.

**On the `/978/` page the signature is total:** 14 `ok` rows across the corpus, **every one at 207411 bytes, 14 distinct digests, no digest ever repeating.**

**The two phenomena are TWO, and I decline to merge them.** Re-deriving the postscript's merge-time census blob-by-blob for its four digest-changing files:

| File | last commit | rows changed | hosts | excerpt changed? |
|---|---|---|---|---|
| `2026-08-11-zeph.json` | `0a67756` | 2 | pca ×2 | no |
| `2026-08-12-daily.json` | `6cd27ac` | 4 | pca ×4 | no |
| `2026-08-15-daily.json` | `31d16fd` | 2 | pca ×2 | no |
| `2026-08-16-daily.json` | `de3ed3c` | 2 | **icsid ×1**, pca ×1 | no |

**A correction to the postscript, on its own headline sentence.** It reads: *"Nine of the nine affected rows returned a DIFFERENT `sha256` at an IDENTICAL byte length … seven of them on `pca-cpa.org`, two on `icsid.worldbank.org`."* The split is **eight `pca-cpa.org` and one `icsid.worldbank.org`**, not seven and two — and **the single icsid row is the one that also changed length**, `105,669 → 105,666` (`find: "RER-03 - Expert Opinion of Doug Uffen"`). So it is **not** an instance of the constant-length signature at all, and the constant-length merge-time instances are **8 of 8 on `pca-cpa.org`**. The postscript's own next bullet already recorded the 3-byte move; the headline sentence did not carry it forward.

**Why they are two observations and not one:**

1. **Different comparison units.** Within-run compares two responses seconds apart in one process (bounded below by `MIN_INTERVAL_SECONDS = 3.0` per domain, and by nothing above that the record can establish — `0805`'s "minutes apart" was struck for exactly this). Merge-time compares two responses roughly a day apart, from different runner sessions.
2. **The merge-time set is not a sample of hosts.** Which files got overwritten was decided by `newest_request()`'s lexicographic pick at merge time, not by anything about the hosts. `investmentpolicy.unctad.org`'s divergent groups all sit in files dated 2026-08-03 to 2026-08-09, before the overwrite window opens on 2026-08-10 — so its **absence** from the merge-time set is an artefact of file selection and carries no information.
3. **On the one host where the two sets could have disagreed, they do.** `icsid.worldbank.org` shows **zero** within-run divergent groups over 12 groups, and its single merge-time change was a length change. Treating them as one phenomenon would require the merged account to predict a length change it does not predict.

**But one relation between them does hold, and it is the useful one.** The within-run observation **strictly dominates** the merge-time one as evidence about digest reliability: if two responses seconds apart differ in digest at equal length, then nothing can be inferred from two responses a day apart differing on that same URL. **On `pca-cpa.org` the merge-time observation adds no information the within-run data did not already carry** — which is the substance of `0822-R15`.

### `0822-R14` *(sic — see `0822-R16` for the row proposals; this number is used at `0822-R16` below)*

> **CHAIRMAN'S NOTE, INSERTED BESIDE THE ANALYST'S TEXT AND NOT INTO IT — `0822-B14`, SUSTAINED.** The
> stub above points to the wrong section. **The row proposals are at `0822-R17`** ("RELAY ROWS PROPOSED FOR
> TOMORROW"); `0822-R16` is "(d) WHAT IT LICENSES". **Four live cross-references run through this stub** —
> §A, `0822-R3`, `0822-R10` and `0822-R11` all forward-reference `0822-R14` for the row proposals — and
> each therefore lands a reader one section short of the target. The officer endorsed leaving the **number**
> standing, on the analyst's own reasoning at `0822-E6`: renumbering is how a later citation comes to
> resolve to the wrong finding, and this council has the `0821-R13`(4) dead-escalation precedent for
> exactly that. But *"leave the number"* and *"leave the misdirection"* are different things. **The stub's
> internal pointer should read `0822-R17`.** I have not edited the analyst's line — a verbatim return stays
> verbatim — so the correction lives here, in my voice, and this note is the operative pointer.

### `0822-R15` — (c) THE PRIOR-ART CHECK, AND IT IS THE MOST VALUABLE THING IN THIS SECTION. **THIS COUNCIL WROTE IT DOWN THREE TIMES, AND THE THIRD TIME WAS A WHOLE-CORPUS CENSUS.**

The chairman asked me to find this rather than let it surface in a week. It is there, in three layers:

**1. First observation — 2026-08-05.** `analytics/daily-research/2026-08-05.md`, anchor *"The two treaty-978 rows in the same run"*:

> "The two treaty-978 rows in **the same run, minutes apart**, both returned `bytes: 207411` and **different** sha256 (`c7afb6fd734d…` and `636c65c4033e…`). Across all three runs there are now **six fetches of that page, six distinct digests, every one at exactly 207,411 bytes**."

*(The words "minutes apart" in that sentence were themselves struck later the same day and should not be carried forward.)*

**2. The same mistake, already caught once — 2026-08-12.** `analytics/daily-research/2026-08-12.md`, anchor *"Today's batch is not the record's first same-run demonstration"*:

> "`analytics/daily-research/2026-08-05.md:109` already records two rows in **the same run, minutes apart**, at equal length with different digests — so 'today's batch demonstrated it three times' was not the first same-run demonstration."

**A seat presented the within-run form as new on 2026-08-12; the officer caught it that day.** Today's Task 4 is the same presentation, ten days later.

**3. The whole-corpus census — 2026-08-15, and it is the same census.** `analytics/daily-research/2026-08-15.md`, anchor *"every group of two-or-more rows on the same URL in the same run at constant byte length"*, reports:

| URL (host) | digest-IDENTICAL | digest-DIVERGENT |
|---|---|---|
| `uncitral.un.org/en/commission` | 9 | 0 |
| `example.com/` | 8 | 0 |
| `icsid.worldbank.org` case-detail (2 URLs) | 3 | 0 |
| `pca-cpa.org/en/cases/309/`, `/336/` | 0 | **2** |
| `investmentpolicy.unctad.org` treaties path | 0 | **5** |

**My census at HEAD reproduces that table exactly and extends it by seven days.** Restricting my figures to files existing on 2026-08-15 gives uncitral 9, example.com 8, icsid 3, pca 2, unctad 5 — identical in every cell. At HEAD the same cells read 17, 18, 12, 3 and 5, the pca increment being today's own batch. **The derivation is reproducible and stable, and it was already answered.**

**And the controlled form is prior art too, which bears on today's pre-registration.** Gate `G12` of `analytics/fetch-requests/2026-08-22-daily.json` describes `urls[7]`/`urls[8]` — same URL, same run, **same `find`** — as *"the cleanest controlled form of that test available."* That exact form has been sitting in the corpus since **2026-08-09**, on a different host, unnoticed:

```
2026-08-09-daily.json  (commit d05dee6)   run 31312614020
  records[1]  find='China - Switzerland BIT'  bytes=207411  sha=99f5417758472b2d…
  records[6]  find='China - Switzerland BIT'  bytes=207411  sha=7bb930f60ea8e647…
  excerpts byte-identical: True
```

Same URL, same run, same `find`, identical 400-character window, identical length, **different digest.** The 2026-08-15 census counted groups and did not isolate same-`find` pairs, so this specific pair is, as far as my grep reaches, unremarked — but the phenomenon it demonstrates is the one the record has held since 2026-08-05.

**The honest verdict on (a)–(c): the claim is true, its host scope is wrong, and it is a fourth re-derivation of something this council established on 2026-08-05, was corrected about on 2026-08-12, and censused corpus-wide on 2026-08-15.** That outcome is worth more to this council than a new finding, and it is the one I was asked to look for.

### `0822-R16` — (d) WHAT IT LICENSES. **NOTHING SUBSTANTIVE IS TOUCHED, AND I CHECKED RATHER THAN ASSUMED.**

**What it costs the digest.** On `investmentpolicy.unctad.org`'s `/978/` page and on `pca-cpa.org/en/cases/309/` and `/336/`, `sha256` is **worthless as a change signal**, in both directions and even at constant byte length: inequality shows nothing, and equality has never been observed on `/978/` at all. Change detection against those URLs must run on `outcome`, `status`, `bytes` and `find_matched` — which is next step 7 of the 2026-08-10 record, already adopted, and this adds no new host to it. **It costs the other hosts nothing and buys them nothing.**

**What it touches: no excerpt, no quoted window, no substantive finding.** Two independent screens:

1. **Within-run.** 21 same-`(URL, find)` groups with ≥2 `ok` rows across the corpus; **0 with a divergent excerpt.**
2. **Corpus-wide, across files.** 14 distinct `(URL, find)` keys have ≥2 `ok` rows, **130 rows** in them; **9 of the 14 keys show a varying digest; 0 of the 14 show a varying excerpt.** Including `Manifest Lack of Legal Merit` on the ICSID register — 7 rows, 7 files, **4 distinct digests, 1 excerpt** — so even the register's day-by-day one-byte movement never moved that window.

Add the field-by-field merge-time diff at `0822-R13`: **excerpt unchanged in all nine rows.**

**So: no excerpt in the corpus has ever differed between two rows that asked the same URL the same question.** That is the population — 14 keys, 130 rows, deduplicated by `(URL, find)`, scoped to `outcome == "ok"` — and it is the correct population for this negative, stated per `0821-E7`.

**What it emphatically does not license.** No mechanism. Nobody has seen the varying element; a rotating token, a nonce and a constant-width timestamp would each serve, and per the 2026-08-06 ruling this class of pattern is reported as *most consistent with* a constant-width per-response varying field and is **not diagnosed**. It says nothing about any case, award, proceeding, treaty or person; it is an instrument property of three URLs.

---

## `0822-R17` — RELAY ROWS PROPOSED FOR TOMORROW (`0821-R9` FORM). LICENCES FIXED HERE, BEFORE ANY RESULT EXISTS. I HAVE FIRED NOTHING.

Prior-art check run first, at HEAD `682e762`, over `analytics/fetch-results/*.json`: `cases/content/tables-of-decisions` (parent, exact) → **0 rows**; `CaseNo=ARB/17/19` → **0 rows** and **0 files repo-wide**; `CaseNo=ARB/24/26` and `CaseNo=ARB/24/46` → **0 rows**. All four routes are unfired. All are on `icsid.worldbank.org` (`ALLOWED_HOSTS`, `:49`), which has returned HTML this channel reads on every batch since 2026-08-19, and the `case-detail` form specifically has **16 `ok` rows across three case numbers** (`ARB/22/2` ×3, `UNCT/20/6` ×13).

| # | URL | `find` | What a match licenses; what a null licenses |
|---|---|---|---|
| 1 | `https://icsid.worldbank.org/cases/content/tables-of-decisions` | `Manifest Lack of Legal Merit` | **Render check on a never-fetched page.** A match licenses only that this page renders to this channel and that the find machinery worked on it, plus whatever the window shows. **Not** a control for the child table page or for any other row. |
| 2 | same URL | `not applicable` | **The `_` probe, fired against my own refusal at `0822-R2`.** A match licenses **only** the content of the returned window — it does **not** confirm the engine's prose, and only the window can place the string as a legend rather than prose. Weakness stated first: it contains a space, so per `G7` the relay can manufacture one and never remove one, making this a weaker test than a space-free literal. **If it nulls, the row banks nothing**: not-rendering, real absence and `G6`'s zero-width hazard are not separable by this row and I pre-register that I will not try to separate them afterwards. |
| 3 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/17/19` | `Status of Proceeding` | **Near-positive control**, body-observed on the `ARB/22/2` case-detail page. Lands a window in the metadata block. |
| 4 | same URL | `Decision on Jurisdiction` | **`N4`'s docket probe, at its true and small scope.** A match licenses **only** that ICSID's docket lists a document it designates that way. **A document designation is not the tribunal's characterisation and CANNOT answer question (3).** If a future seat cites this row for question (3), that citation is wrong and this sentence is why. |
| 5 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/24/26` | `Status of Proceeding` | **The digit discriminator, from ICSID's own body.** `find` is deliberately **not** `ICSID Case No.` and not the case number: a case-detail page may echo its own query parameter, and a `find` the page can generate from my request URL reports a property of the request (relay method rule 1 / taxonomy 23, this seat's own catch). **Branches pre-registered:** if the window carries party names other than ENCORE/Türkiye, the TDM title's designator resolves to a different proceeding and the discrepancy is closed **from a body**; if it carries ENCORE, the two designators both resolve to this matter and the discrepancy deepens; **if the row nulls or 404s, it banks nothing in either direction** — `G3`, a status is this runner's access at that instant. |
| 6 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/24/46` | `Status of Proceeding` | Row 5's pair, same run, same `find`, so each is the other's context. **Neither is a control for the other** — they are different pages, which is `0821-B17`'s whole lesson. |

**Pre-registered nulls-of-design, licensing nothing, across all six:** `G3` — a refusal or 404 is this runner's access and never a statement about ICSID, a publication, or a proceeding; `G6`/`N7` — an undecoded entity or zero-width character inside any literal returns a clean `find_matched: false` indistinguishable from real absence; `G5` — anything living in an attribute is invisible; and **the cardinal one carried forward verbatim from yesterday: a match on rows 1–2 establishes that ICSID's explanatory material is readable by this channel and does NOT establish that a codebook for the register's finding column exists anywhere.**

---

## `0822-R18` — METHOD NOTE (self-training mandate): THE FAILING FORMULATION IS THE ONE I INVENT WHEN THE PROJECT ALREADY OWNS ONE.

`0821-R15` refined the rule at the level of query syntax — quote the party name, not the `ICSID Case No.` phrase. **Today shows the syntax was never the binding constraint.** S6 succeeded because it applied a template `analytics/optimization-log.md` has held since 2026-07-22, naming this exact case as its trigger; eight formulations across three days did not, because no seat opened the log. `0821-E3` diagnosed this as "I design the batch before I finish reading my own prior return" — that was too narrow. **The rule I actually need: the prior-art check under method rule (u) is not only over the daily records and the results corpus; it is over `analytics/optimization-log.md` and `analytics/insights.jsonl`, which is where this project stores methods that worked.** Cost of learning it: two days of Task-3 searching that a one-line grep would have redirected.

Second, cheaper note: **domain-restricting a search is a distinct instrument, not a filter.** S2 restricted to `icsid.worldbank.org` and surfaced a parent page eight unrestricted calls across four days never returned. It should be tried whenever the target is an institution's own apparatus rather than commentary about it.

---

## § CANDIDATE CLAIMS

```json
[
  {
    "claim_text": "No source title-carried to this council decodes the values of ICSID's 'Finding that Claim is Manifestly Without Legal Merit' column; the observed values No, Partial and _ remain untranslated and _ remains undefined. ICSID publishes a parent 'Tables of Decisions in ICSID Cases' page at https://icsid.worldbank.org/cases/content/tables-of-decisions which this council has never fetched.",
    "confidence": "medium-high",
    "sources": [
      "https://icsid.worldbank.org/cases/content/tables-of-decisions",
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"
    ],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM, scoped to this instrument, TWO CALLS DEEP AND NOT SETTLED. WebSearch S1 ('\"Finding that Claim is Manifestly Without Legal Merit\" \"Partial\"') and S2 ('tables of decisions legend note explaining columns finding', domain-restricted to icsid.worldbank.org) of 2026-08-22 returned 9 and 10 results; no returned title is a legend, key, codebook, explanatory note or how-to-read apparatus for the column. SYNONYM FAMILIES NOT TRIED: codebook, notes to the table, how to read, methodology, 'for information purposes only' — per 0821-B14 a zero on two families is not a zero on the set. The parent-page novelty is derived programmatically at HEAD 682e762: 17 rows in analytics/fetch-results/ carry 'tables-of-decisions', all 17 on the child URL /manifest-lack-of-legal-merit; the parent has 0 rows and 'tables-of-decisions/annulment' returns 0 files repo-wide. GENERATED PROSE DECODING BOTH 'Partial' AND '_' IS REFUSED AT 0822-R1 AND 0822-R2 AND IS NOT BANKED. Per the cross-BIT consistency rule this is 'not found in accessible sources', NOT 'does not exist'. GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook."
  },
  {
    "claim_text": "No HTML rendition of the Hela Schwarz GmbH v. People's Republic of China award (ICSID Case No. ARB/17/19, 10 December 2025) has been identified on any host in the relay's ALLOWED_HOSTS; every rendition surfaced on an allowed host is a PDF, and the sole HTML rendition of the award text surfaced anywhere is on jusmundi.com, which is not on ALLOWED_HOSTS.",
    "confidence": "medium-high",
    "sources": [
      "https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf",
      "https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C6447/DS21216_En.pdf",
      "https://www.italaw.com/sites/default/files/case-documents/italaw11394.pdf"
    ],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM about a ROUTE, not about a document. Screened over WebSearch S3, S4 (domain-restricted to italaw.com, icsid.worldbank.org, icsidfiles.worldbank.org, investmentpolicy.unctad.org) and S6 of 2026-08-22 plus S2 and S3 of 2026-08-21 — five formulations, two days. Every award-bearing rendition returned on an allowed host ends in .pdf; excerpt_of returns '' for a PDF (scripts/fetch_relay.py, anchors 'if \"html\" not in content_type and \"text\" not in content_type:' / 'return \"\"'). jusmundi.com is absent from ALLOWED_HOSTS (anchor 'ALLOWED_HOSTS = {' at :48, set closing :69). ALL BODIES UNREAD. Derived at HEAD 682e762: www.italaw.com HTML rows in analytics/fetch-results/ number 6, across 3 distinct URLs and 3 result files, outcome 'refused' on all six; its PDF rows number 4, all 'ok'. PER G3 THE REFUSALS ARE THIS RUNNER'S ACCESS AT THOSE INSTANTS AND ARE NOT A STATEMENT ABOUT ITALAW. An HTML page on an allowed host that HAS never been fetched and IS newly title-carried is the ICSID case-detail page https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/17/19 (0 rows in the corpus, 0 files repo-wide); it is a DOCKET page, not the award, and cannot answer the characterisation question."
  },
  {
    "claim_text": "No accessible source states whether the Hela Schwarz tribunal characterised the China-Germany BIT Protocol Article 9 conditions as going to jurisdiction or to admissibility. The project record does hold, on title and search-engine-snippet carriers only, that the Article 9(a) ground was pleaded as an objection to jurisdiction and that the tribunal gave effect to Article 9(a) and 9(c) in dismissing all claims.",
    "confidence": "medium-high",
    "sources": [
      "https://www.iareporter.com/articles/analysis-icsid-tribunal-hearing-spice-investors-claim-against-china-gives-effect-to-idiosyncratic-administrative-review-and-fork-in-the-road-clause-found-in-bit-protocol-dismissing-all-clai/",
      "https://www.iareporter.com/articles/hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation-but-accepts-amendments-to-the-claimants-request-for-arbitration/"
    ],
    "supporting_quote": null,
    "supporting_locator": "AMENDS AND REPLACES candidate claim 5 of 2026-08-21 as amended at that seat's second turn. THE AMENDMENT IS AGAINST MY OWN PRIOR WORDING: 0821-R10's 'No accessible source addresses it' was too strong. STATE_OF_THE_ANSWER.md (anchor: 'Article 9(a) administrative-exhaustion pre-condition') and analytics/daily-research/2026-07-15.md both carry the phrase 'third objection to jurisdiction' for the Article 9(a) ground; the 2026-07-15 record labels it MEDIUM confidence 'from search-engine summaries' and states in terms that whether the tribunal upheld the 9(a) bar 'is unknown'. THAT CHARACTERISES HOW THE OBJECTION WAS PLEADED, NOT HOW THE TRIBUNAL CHARACTERISED THE CONDITION, AND SO DOES NOT ANSWER THE QUESTION. The strongest carrier the project holds, the IAReporter analysis-article title, uses the verb 'gives effect to', which takes no position between jurisdiction and admissibility. QUOTE-INTEGRITY CAVEAT TRAVELLING WITH THIS CLAIM (0822-R9): all three sources listed on that STATE_OF_THE_ANSWER.md row are unread bodies — IAReporter paywalled, vLex 403-blocked, JIDS abstract-only — and its three quoted phrases carry no pinpoint; under the carrying-span rule they are 'span not pinned; source unread'. Generated prose asserting 'jurisdiction and merits grounds' was replicated a THIRD time today (S3) including the impossible construction 'a final award dated December 10, 2025 (published on January 15, 2025)'; per taxonomy 16 replication by the same class of instrument is NOT corroboration and it is NOT banked, and it would not answer this question even if it were, since it never uses the word admissibility. The second IAReporter title is NEW to the repository (git grep -l -F 'hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation' → 0 files at HEAD 682e762), body unread, and concerns bifurcation. GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility."
  },
  {
    "claim_text": "The discrepancy between ICSID Case No. ARB/24/46 (ICSID register body; italaw title) and ARB/24/26 (TDM Journal title) for ENCORE Investment Group Limited v. Republic of Türkiye is UNRESOLVED; a search treating ARB/24/26 as a designator in its own right returned zero titles carrying that string.",
    "confidence": "medium",
    "sources": [
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit",
      "https://www.italaw.com/cases/13154"
    ],
    "supporting_quote": "ENCORE Investment Group Limited (Malta) v. Republic of Türkiye (ICSID Case No. ARB/24/46) Decision on the Respondent Rule 41 Application ( English ) October 7, 2025 No",
    "supporting_locator": "Quote is the ICSID register half, body-carried in the window reproduced at analytics/daily-research/2026-08-20.md Part II section 1 (records[3], find 'ARB/24/'). WebSearch S5 of 2026-08-22, '\"ARB/24/26\" ICSID case claimant respondent', returned 9 results; ZERO returned titles carry ARB/24/26, and the engine stated the results 'do not contain specific information about ICSID case ARB/24/26'. A DOCKET NUMBER IS A RIGID DESIGNATOR so 0821-B14's synonym objection does not reach this zero; LIMITATION STATED: one notation only, and ARB 24/26, ARB.24.26 and a bare 24/26 were not screened. THIS IS 'NOT FOUND IN ACCESSIBLE SOURCES', NOT 'DOES NOT EXIST', and it is NOT a reconciliation: the carriers are asymmetric (one body plus one independent title for /46; one title and zero designator hits for /26) and the discrepancy STAYS UNRESOLVED. The trailing register token 'No' is reproduced as rendered and IS NOT TRANSLATED. NOBODY HAS READ THIS DECISION AND NO HOLDING IS ASSERTED. Relay rows that would settle it from ICSID's own body are proposed at 0822-R17 rows 5-6 and were NOT fired by me."
  },
  {
    "claim_text": "Within a single relay run, multiple rows on the same URL have returned identical bytes with different sha256, in 8 of 56 all-ok multi-row (file,URL) groups across the fetch-results corpus: 5 on investmentpolicy.unctad.org and 3 on pca-cpa.org, and 0 on icsid.worldbank.org, uncitral.un.org, undocs.org or example.com. No excerpt has ever differed between two rows asking the same URL the same question.",
    "confidence": "high",
    "sources": ["scripts/fetch_relay.py"],
    "supporting_quote": "The two treaty-978 rows in the same run, minutes apart, both returned `bytes: 207411` and **different** sha256",
    "supporting_locator": "NOT A NEW FINDING — PRIOR ART, and the quote is that prior art: analytics/daily-research/2026-08-05.md, anchor 'The two treaty-978 rows in the same run'. (The phrase 'minutes apart' in that sentence was struck later the same day and must not be carried forward.) The council was corrected for re-presenting this as new on 2026-08-12 (anchor 'Today's batch is not the record's first same-run demonstration') and ran the WHOLE-CORPUS census on 2026-08-15 (anchor 'every group of two-or-more rows on the same URL in the same run at constant byte length'), whose five-row table my derivation reproduces cell-for-cell when restricted to files existing on that date. CENSUS DERIVED PROGRAMMATICALLY at HEAD 682e762 over 31 files / 208 records / 186 ok rows: 60 multi-row (file,URL) groups, 56 all-ok, bytes constant in 56/56, sha256 constant in 48/56, zero groups with both varying. Divergent groups and their commit pins per N12: 2026-08-03-targeted, 2026-08-04-daily, 2026-08-05-daily, 2026-08-05-zz-attribution, 2026-08-09-daily (all d05dee6, all /978/, all 207411 bytes); 2026-08-12-daily (6cd27ac, pca 309, 106011); 2026-08-15-daily (31d16fd, pca 336, 97343); 2026-08-22-daily (682e762, pca 309, 106011). Mechanism anchors: 'content = resp.content or b\"\"' (:171), '\"bytes\": len(content),' (:178), '\"sha256\": hashlib.sha256(content).hexdigest(),' (:179) — NOTE the range :171-173 contains NEITHER field. THE ZEROS LICENSE NOTHING: per the 2026-08-15 ruling a within-run control 'can only ever refute stability, never establish it', so 0/12 on icsid and 0/17 on uncitral are not a clean bill. NO MECHANISM IS DIAGNOSED; per the 2026-08-06 ruling this is reported as most consistent with a constant-width per-response varying field. Excerpt-invariance screened over 14 distinct (URL,find) keys with >=2 ok rows, 130 rows: 9 keys show a varying digest, 0 show a varying excerpt."
  },
  {
    "claim_text": "The 2026-08-21 postscript's merge-time census misstates the host split of its nine digest-changing rows as seven pca-cpa.org and two icsid.worldbank.org; the split is eight and one, and the single icsid.worldbank.org row also changed byte length (105,669 to 105,666), so it is not an instance of the constant-length signature at all.",
    "confidence": "high",
    "sources": ["analytics/daily-research/2026-08-21.md"],
    "supporting_quote": "Nine of the nine affected rows returned a DIFFERENT `sha256` at an IDENTICAL byte length with an IDENTICAL rendered excerpt — seven of them on `pca-cpa.org`, two on `icsid.worldbank.org` case-detail (one of which also moved 3 bytes).",
    "supporting_locator": "Quote from the 2026-08-21 POSTSCRIPT, section 'An instrument finding that fell out of the census, and it is new'. Re-derived blob-by-blob (first commit vs last commit touching each file) for the four files the postscript names: 2026-08-11-zeph (cc3730b→0a67756) 2 rows, both pca-cpa.org; 2026-08-12-daily (4af5f11→6cd27ac) 4 rows, all pca-cpa.org; 2026-08-15-daily (f463d08→31d16fd) 2 rows, both pca-cpa.org; 2026-08-16-daily (1ec3e9c→de3ed3c) 2 rows, one icsid.worldbank.org (find 'RER-03 - Expert Opinion of Doug Uffen', 105669→105666) and one pca-cpa.org. Total 8 pca + 1 icsid = 9. THE POSTSCRIPT'S NEXT BULLET ALREADY RECORDS THE 3-BYTE MOVE; only its headline sentence fails to carry it forward, so this is a wording defect, not a derivation defect. CONSEQUENCE: the constant-length merge-time instances are 8 of 8 on pca-cpa.org. NO EXCERPT CHANGED IN ANY OF THE NINE ROWS, confirming the postscript's most important sentence; no finding about any case, award, proceeding or person is impeached."
  }
]
```

---

## § OBJECTIONS AND OBSERVATIONS AGAINST MY OWN WORK

**`0822-E1` — THE PRIOR-ART FAILURE AT `0822-R7` IS MINE THREE DAYS RUNNING, AND I FOUND IT ONLY BECAUSE I WAS ORDERED TO RUN A DIFFERENT GREP.** `analytics/optimization-log.md` has held, since 2026-07-22, a demonstrated method naming *this case* as its trigger. I did not open that file on 2026-08-20 or 2026-08-21, and I opened it today only while chasing the living memory's IAReporter citation for Task 2 — not as a prior-art step. Had I not been reading the Article 9(a) row for an unrelated reason, S6 would never have been designed and I would have filed a third day's null on a question the project had a tool for. **The check that saves me is the one I run for a different purpose, which means it is luck, not method**, and `0822-R18` is my attempt to convert it.

**`0822-E2` — MY TASK 4 CENSUS TRAVERSES A FILE WHOSE INTERPRETATION IS NOT MINE, AND ONE OF ITS ROWS IS THE CHAIRMAN'S OWN PRE-REGISTERED EXPERIMENT.** `2026-08-22-daily.json` records[7]/[8] are `G12`'s pre-registered pair, and they fall inside my category-A count. I have reported them as census rows — bytes constant at 106011, two distinct digests, excerpts byte-identical — because excluding today's file from a corpus census would be a silent scope cut, and because `G12` fixed both branches in advance so no reading of mine can be chosen after the fact. **But the licence attaching to that pair is Part II's to state, not mine**, and if the chairman considers I have pre-empted him, the two rows can be struck from my count and it falls from 8 to 7 without disturbing anything else. I flag it rather than let it pass unremarked.

**`0822-E3` — `0822-R15`'s "as far as my grep reaches" is doing real work and I want it visible.** I claim the 2026-08-09 same-`find` pair is unremarked in the record. I screened `within-run`, `same run`, `same-run`, and the byte counts `106011`, `106,011`, `97343`, `97,343`, `207411`, `207,411`. I did **not** screen for a seat describing the same observation without any of those strings — e.g. by digest prefix, or by run number `31312614020`. **A grep establishes absence from the repository under the terms screened, never absence from the project**, and given that today's whole Task 4 is a lesson about re-derivation, my own novelty claim is the last one that should get a pass. Treat it as "not found under six screened terms".

**`0822-E4` — I SPENT S3 ON A COMMENTARY WHOSE TITLE THE RECORD HAS HELD IN NINE FILES SINCE JULY, AND IT RETURNED WHAT THE RECORD SAID IT WOULD.** `git grep -l -F "landmark-icsid-victory-for-the-peoples-republic"` returns **9 files** at HEAD `682e762`; `aoshearman` returns **9**. The 2026-08-20 record already named A&O Shearman as a source that "emphasised the 9(c) court-withdrawal ground" and already recorded the impossible date. **I bought a third replication of a known defect for one call.** This is `0821-E3`'s pattern in its fourth instance: I designed S3 from the *gap* rather than from the *record*, and the record already contained the answer to what S3 would return. Two of my six calls today (S3, and arguably S1) were purchasable for free.

**`0822-E5` — WHAT I DID NOT CHECK, NAMED SO COVERAGE IS NOT READ AS SILENCE.** (i) No subject-matter search on `ARB/24/18` or `ARB/22/30`; my Task 3 verdict for those two still rests entirely on the 2026-08-20/21 title harvests, collected to answer a different question — `0821-E5`'s admission, uncorrected. (ii) Four synonym families for Task 1 untried (`0822-R4`). (iii) One notation only for `ARB/24/26` (`0822-R10`). (iv) **I did not verify that ICSID case-detail pages carry any field that would place a proceeding in a subject-matter ring**; `0821-R9` screened every stored case-detail excerpt for `Economic Sector|Subject Matter|Sector|Industry|Applicable Treaty|Convention` and got **zero hits**, and nothing today changes that, so rows 3–6 of `0822-R17` are probes into a metadata block whose contents I am partly guessing at. (v) I have not read any window from today's batch's ICSID rows, by design, so nothing I say about Task 1's route accounts for what those rows may already show.

**`0822-E6` — A DEFECT IN MY OWN NUMBERING, LEFT VISIBLE RATHER THAN SILENTLY REPAIRED.** `0822-R14` is a stub pointing forward to `0822-R16`/`0822-R17`; I drafted the row-proposal table before the Task 4 sub-answers and did not renumber when they grew. **Renumbering after the fact is how a citation to `0822-R14` in a later record comes to resolve to the wrong finding** — the same failure class as `0821-R13`(4)'s dead escalation number and `0821-R12`'s self-falsifying anchor. The stub stays; the chairman may collapse it, but if he does, he should note the collapse rather than close the gap silently.

**`0822-E7` — THE OBSERVATION I MOST EXPECT TO BE WRONG ABOUT.** At `0822-R13` I assert that the within-run and merge-time phenomena are two and decline to merge them, and my third reason leans on `icsid.worldbank.org` showing zero within-run divergence over twelve groups. **By the 2026-08-15 one-directional ruling I quote three paragraphs earlier, that zero refutes nothing** — so it cannot bear the weight of "the two accounts disagree on this host". The honest reduction of my third reason is narrower: *the merge-time set contains an icsid row that changed length, which the constant-length account does not predict; whether icsid also varies within a run is unmeasured, not measured-negative.* Reasons 1 and 2 stand unaffected and the refusal to merge survives on them. **I caught myself applying the asymmetric filter the officer named at `0815-B8`, in the same section where I quoted the ruling against it.**

---

*Return closed. Six of nine searches spent, three returned. Zero fetches attempted; the relay was not fired by me and no second batch is proposed for today. Nothing edited, nothing built, nothing applied — `STATE_OF_THE_ANSWER.md`, the pre-registration file and the operator's pending list are untouched by this seat. Two gap slugs stand open, one of my own findings from yesterday is withdrawn and re-worded against me, and the day's central assignment resolved to prior art this council wrote down on 2026-08-05, was corrected about on 2026-08-12, and censused on 2026-08-15.*

---

## Part IV — The digest of record (Rule 1)

`Digest 2026-08-17: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied verbatim from `digests/2026-08-17_ISDS-Thematic-Watch/meta.json` — `screened: 7`, `matches: 0`,
`watch_list_leads: 0` — read from the file, not restated from memory, and pinned to blob `e1d1ef76`
at commit `feb1b94`. Rule 1's fixed terminology is used and the banned bare word does not appear.

**The digest of record has not moved, for the fourth consecutive daily record.** It is the same digest the
2026-08-19, 08-20 and 08-21 records carried. The collection run is weekly and fires on Mondays; today is
Saturday, so **the next collection is Monday 2026-08-24** and no digest movement was possible today.

Rule 0 keeps this short when the digest is unchanged, and it is unchanged, so this is short. Two things
are worth one line each because they bear on how the next digest should be read.

**`meta.json` still carries no collection-run identifier** (escalation `N8`). The 2026-08-17 file was
rewritten three times across two hours and twenty-three minutes with `screened` going 13 → 17 → 7, and
nothing in the file distinguishes those three states. Today's reading is safe only because it is pinned to
a blob and because that file settled five days ago. **Monday's collection will produce a file with the same
defect**, and any seat reading it inside the rewrite window will read a number that does not survive.

**Source health, unchanged and not re-litigated:** the archived `source_health` block records `italaw` and
`unctad_isds` as `RECOVERED (Internet Archive)` each after one `403` refusal, `iareporter_headlines` as
`HEADLINE-ONLY`, `gdelt` as `NOT-READ (429)`, and `iisd_itn` as `QUIET (feed live; newest item
2026-04-21)`. Zero of the seven candidates reached a match. **Per the standing rule these are statements
about that run's access, not about the sources.** The health-column proposal that would surface them
routinely remains on the operator's pending list and is not built.


---


## Part V(a) — The integrity officer's vetting note, turn 1, verbatim

Convened as a real subagent against `.claude/agents/integrity-officer.md`. **This is the one departure from
Rule 0 declared in Part I**: Rule 0 files the integrity challenge inside the Part V dialogue rather than as
a separately convened seat, and I convened it as a real agent anyway, because the alternative is a chairman
writing the challenge to his own work. **Today it earned itself seven times over, and five of the seven are
against sentences I wrote with confidence rather than the three I had already flagged as doubtful.**

Turn 1 vetted Parts I and II, the pre-registered batch and its reduction, and my two edits to
`STATE_OF_THE_ANSWER.md`. It spent **zero** WebSearch calls. Turn 2, on the analyst's return, follows at
Part V(b). His text is reproduced exactly as filed.

---

**MODEL, REPORTED AS REQUIRED.** This seat is running on **`claude-opus-5`**. `.claude/agents/integrity-officer.md` declares `claude-opus-4-8` and the operator's directive of 2026-08-03 governs; this is the tenth consecutive seat to report the gap, escalation **`N2`**, carried as a procedural caveat and never substituted silently. Vetting performed at HEAD **`fdc5833`** (the record moved from `95a2f31` to `fdc5833` mid-vetting; §6 findings below are filed against the text as it now stands, not the superseded text). **Zero WebSearch calls spent** — every claim in scope is corpus-internal or repository-internal and a network call would have added nothing.

---

## BINDING OBJECTIONS

### `0822-B1` — CHAIRMAN. `STATE_OF_THE_ANSWER.md` at `d017add`: the `[403-blocked]` count is falsified by the commit that wrote it. It is **seven**, not six — a fourth-generation instance of the exact defect the sentence is inside.

Offending text, added at `d017add` and live at HEAD:

> **The blocked-access label itself is not a usable anchor — its bracketed form occurs six times.**

Derivation, over the file blob before and after the edit:

```
git show d017add^:STATE_OF_THE_ANSWER.md | count "[403-blocked]"  ->  6
git show d017add :STATE_OF_THE_ANSWER.md | count "[403-blocked]"  ->  7
git show HEAD    :STATE_OF_THE_ANSWER.md | count "[403-blocked]"  ->  7
```

The seventh occurrence is inside the same commit's own new block: *"**The `[403-blocked]` amendment is still NOT applied and stays on the operator's pending list**"*. **Six was correct when computed and false when published.** That is verbatim the mechanism the surrounding paragraph diagnoses — *"The pointer was correct when it was computed and was falsified by the act of publishing it"* — and the paragraph is the one that removes the third-generation instance. It is now the fourth generation: line number (08-19) → quoted anchor (08-20) → line number again (08-21) → **occurrence count (08-22)**. Rule (s) banned the *kind* of pointer; nothing yet bans a **self-referential quantity**, which is the same failure wearing a number instead of a location.

**Required fix:** the count must be re-derived after the edit that carries it, or replaced by a non-quantitative statement (*"the bracketed form is not unique in this file"*). This is a defect in the cumulative record, not the daily one, so it does not decay tomorrow — it will be read by every future seat.

Two further stale figures in the same block, same class, both mine to name rather than to repair:
- *"line 117 of this file today is a table row on Protocol Article 9(b)"* — true of the **pre-edit** file (line 117 = `| 2 | 9(b) | Three months must have elapsed since referral | …`); at `d017add` line 117 is **blank**. A present-tense line-number assertion, inside the sentence that bans line numbers, stale on commit.
- The instruction adds one occurrence each of `Award PDF` (6→7) and `italaw` (22→23) to the file, so a plain grep for either token now lands on the instruction first.

### `0822-B2` — CHAIRMAN. The novelty claim retracted in §3 is **still live at HEAD** in `STATE_OF_THE_ANSWER.md`, which is the durable record.

`d017add` writes into the cumulative file:

> **The register's rendered row structure was observed for the first time and places `Partial` in the finding column**

§3 of the daily record falsifies exactly that proposition — *"Both clauses are false, and the corpus said so before I wrote them"* — against `2026-08-20-zz-column-and-middle.json` `records[3]`. `d017add` (11:27:20) precedes the correction commit `95a2f31`, so the sequence is explicable; the state is not. Verified at HEAD:

```
git show HEAD:STATE_OF_THE_ANSWER.md | grep -c "rendered row structure was observed for the first time"  -> 1
```

The daily record self-corrects and is read once; the cumulative record does not and is read every session. **A correction that lands only in the retracted file's neighbour is not a correction.** The `STATE_OF_THE_ANSWER.md` sentence must carry the same retraction, in place, with the pre-correction wording quoted — the standard this council applies to itself everywhere else.

*(On the fourth item you asked me to test: I checked `d017add` for a smuggled decode and there is none. The block states `No`, `Partial` and `_` "remain UNDECODED", `_` "undefined", "a placement, not a decode", and restates `0821-R4`. The refusal is clean in both files. That limb passes.)*

### `0822-B3` — CHAIRMAN. §3, the Trans-Global window-slide join: **one of the two cited left-hand windows does not reach the join point, and the overlap string is demonstrably not unique in the document.** The CANDIDATE label survives; the sourcing sentence does not.

Offending text:

> Two prior windows on the same table end exactly where it begins: `2026-08-19-zz-render-and-cells.json` `records[2]` and `2026-08-20-daily.json` `records[2]` **both close with** *"…​Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25) ​Decision on the Responde"*.

Raw tails at `682e762`:

- `2026-08-19-zz-render-and-cells.json` `records[2]` ends `…ARB/07/25) ​Decision on the Responde` — **matches the quotation exactly, ZWSPs included.**
- `2026-08-20-daily.json` `records[2]` ends `…ARB/07/25) ​Decision ` — **it stops eleven characters short and never reaches `onde`.**

So the join rests on **one** window, not two. The 08-20 window corroborates that Trans-Global is followed by a title beginning `Decision`; it contributes nothing to the overlap. Additionally, a corpus screen returns **three** prior windows carrying `Trans-Global` (08-19-zz `records[2]`, 08-20-daily `records[2]`, 08-20-zz `records[2]`), so the record both over-counts the joining windows and under-counts the carrying ones.

**And the join string is not unique.** The overlap is four characters (`onde`) inside a boilerplate title. In today's own 400-character window, `Objection Pursuant to Rule 41(5)` occurs **twice** — the truncated head row and the Brandes row. A corpus screen for `May 12, 2008` returns **one** row in the entire corpus, today's, so nothing independently ties that date to Trans-Global. The composition therefore asserts that a non-unique left fragment on page state `66a879e013bd` continues into a non-unique right fragment on page state `1f026c81313f`.

**Ruling on the hedge.** "CANDIDATE" is the right label and the row should stay — a textual alignment is a legitimate observation and the pre-registered one-row confirmation probe is the right disposal. But the label alone is not sufficient disclosure, because a reader who accepts the sourcing sentence as written believes the join is corroborated twice when it is sourced once. Two disclosures are required in place:

1. **One** window reaches the join point (`2026-08-19-zz-render-and-cells.json` `records[2]`, digest `66a879e013bd`); the 08-20 window ends at `Decision ` and is corroboration of the case name's position only.
2. The overlapping title is boilerplate recurring at least twice inside today's own window, so the alignment is **possible, not determined**, and no intervening row is excluded by either window.

With those two clauses the CANDIDATE stands. Without them it does not, and your disclosed model-trained recall agreeing with the result is precisely why the disclosure is mandatory rather than optional: an unsourced belief that agrees with a weak join is the mechanism by which a weak join gets written down as a strong one. **You were right to distrust it. It does not fully stand on the windows alone; it stands as one of a small number of alignments the windows permit.**

### `0822-B4` — CHAIRMAN. §1 carries a **third** ungreped "first" — in the same record whose §3 and §6 are corrections of the first two, and barred by your own `G14`.

Offending text:

> The one substantive scrap is that `urls[4]`'s title is **the council's first body-carried confirmation** that this publication is about **Rule 41(5)** specifically — the string `Rule 41(5)` sits inside the rendered title. That has been assumed from the URL slug on **every prior mention**.

`G14`, which you wrote this morning: *"No row here may found an ordinal, a count, a 'first', a 'never', or any superlative; any count must be derived programmatically and shown."* Neither the "first" nor the "every prior mention" is derived or shown. Derived now:

```
Rule 41(5) as RENDERED TEXT in fetch-results excerpts at 682e762:
  2026-08-20-zz-column-and-middle.json records[3]  find 'ARB/22/30'   <-- TWO DAYS AGO
  2026-08-22-daily.json records[4], records[5]

'focus-objections-claim-manifestly' anywhere at 682e762: 4 files
  (council-log.md; 2026-08-21.md; today's request; today's reduction)
```

The narrow proposition — first body-carried confirmation about **that publication** — is true, and trivially so, since the page had zero prior rows. The sentence as written does not carry that scope and reads as a first for `Rule 41(5)` as rendered text, which the corpus falsified on 2026-08-20 in a window **this same record quotes twenty-five lines later**. "Every prior mention" is two records. Restate with the scope explicit and the derivation shown, or strike the ordinal.

### `0822-B5` — CHAIRMAN. §3's block quote is **not character-exact**. Two `U+2019` are transcribed as ASCII apostrophes in the window the record calls "the evidence."

Offending text, quoted in the record as the whole of the evidence:

> `ondent's Objection Pursuant to Rule 41(5) …`
> `…​Decision on the Respondent's Objection Pursuant to Rule 41(5) …`

The raw reduction at blob `37aceca901dbc38eb38db492fa839146ec13104d` carries `ondent’s` and `Respondent’s` — RIGHT SINGLE QUOTATION MARK, both times. The record preserves the four `U+200B` and the undecoded `&amp;` and then silently ASCII-ifies the two curly apostrophes. Character fidelity is load-bearing in this record specifically: §2's whole argument is about whether a typesetting character inside a literal can null a find, and `G6` is a gate about characters the channel does not normalise. A quotation offered as proof of rendered-stream structure cannot itself normalise the stream.

Same class, same section: the composed join is presented inside quotation marks —

> the two windows read *"Decision on the Respondent's Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) May 12, 2008 Partial"*

— with the ASCII apostrophe again and with the two `U+200B` (before `May 12, 2008` and before `Partial`) silently dropped. That string is a **reconstruction**, not a quotation from any source; it must be marked as one, or the elisions marked.

*(Emphasis is also added inside the 08-20 `records[3]` quotation at lines 216–219 — `**No**`, `**Partial**`, `` **`_`** `` — without "emphasis added". Noted, not sustained; the underlying transcription of that window is character-exact, ZWSPs and straight apostrophe included, and I checked it.)*

### `0822-B6` — CHAIRMAN. §1 states `urls[4]`'s match at "offset 0". Under the reading the preceding sentence fixes, the document offset is **34**.

Offending text:

> `a6f0fbaf445e…`, matched**, also at offset 0, in the title

The prior sentence establishes the convention: *"match at excerpt index 0, so by G8 the clamp fired and 0 **is** the document offset."* For `records[4]`, `excerpt.find('Manifest')` = **34**, so `start = max(0, 34-100) = 0` — the clamp fired and the document offset of the match is **34**. The sentence is ambiguous between a true statement (the excerpt *starts* at 0) and a false one (the match *sits* at 0), and the sentence before it selects the false reading. `G8` requires that "where the match sat must be **STATED, not assumed**"; here it is stated wrongly. The substantive claim — the match is inside the page title — survives, and I verified it does.

### `0822-B7` — CHAIRMAN. Your `G11` self-report is correct but **understates the batch file's defect count**. `G11` carries a second wrong figure and `G13` carries a scope-mixed triple. The gate defects invalidate nothing decided under them.

Your self-report is confirmed on its own terms. Derived over every register row at `682e762`:

| date | bytes | sha256 (12) | rows | batches |
|---|---|---|---|---|
| 2026-08-19 | 94,996 | `66a879e013bd` | **5** | 2 |
| 2026-08-20 | 94,995 | `29bd0911889f` | **6** | 2 |
| 2026-08-21 | 94,994 | `ebcebcae8594` | **4** | 2 |
| 2026-08-22 | 94,998 | `1f026c81313f` | **2** | 1 |

**Your §4 table is exactly right in all sixteen cells**, and `G11`'s comparator `94,995 / 29bd0911…` is indeed 2026-08-20's state, not 2026-08-21's. But the same sentence also says that figure held *"on BOTH of yesterday's batches"* — which is true of **08-20**, and is the tell that the whole row was copied, not just the digest. And the clause before it, *"the register returned 94,996 bytes / sha256 `66a879e0…` **twice** on 2026-08-19,"* is wrong on the row unit: **five** rows across two batches. Under the 2026-08-20 optimization item — *the unit is page states, not rows* — a gate that silently switches between rows and batches is the unit ambiguity that item was adopted against.

`G13` compounds it with a three-figure series at three different scopes: *"eighteen find-scoped A/81/17 rows across two distinct page digests, nine observation dates."* Derived at `44c67af`: **18** `A/81/17`-find rows spanning **three URLs** (16 on `/en/commission`, 1 on `undocs.org`, 1 on `digitallibrary.un.org`) across **17** distinct dates; **two page digests** is true only of `/en/commission`; **nine observation dates** is true only of the current digest. Three scopes, one sentence, presented as one series — taxonomy entry **27, scope-mixed screen**.

**Ruling on the question you asked.** A gate carrying a wrong comparator invalidates nothing that was decided under it **here**, and I say so on the merits rather than as an accommodation: `G11`'s branch is *"IF `urls[6]` returns a digest other than `29bd0911…`"*, today's is `1f026c81313f`, and the branch fires identically against the correct comparator `ebcebcae8594`. The pre-registration's protective function — fixing the decision before the result — is intact, because the decision was insensitive to the error. What is damaged is the gate's **audit** function: a later seat reconstructing yesterday's page state from `G11` gets the wrong figure. **A pre-registration is evidence about the past as well as a constraint on the present, and it failed the first while discharging the second.** Correct the figures in place in the record's §4; the batch file at `6aed9a1` is immutable and stays wrong, which is the correct outcome.

---

## OBSERVATIONS

**`0822-O1` — Your §6 suspicion about double-counting a host is unfounded, but the retracted claim was wrong for a reason your retraction does not reach.** You asked whether the two ICSID pages were newly on the stable side given 2026-08-13 already attested an ICSID case-detail page stable. You did not double-count: `2026-08-13.md:618` attests `icsid.worldbank.org/cases/case-database/case-detail`, a different URL, and the 08-13 ruling itself insists the unit is the URL, not the host — *"a **second URL** now has within-run digest stability attested"*. Your framing ("two ICSID **pages**") was correct on that axis. The claim was nonetheless false as to the **register**, which has same-run multi-row digest constancy on **three prior dates**: 08-19 (3 rows, `66a879e013bd`), 08-20 (3+3 rows, `29bd0911889f`), 08-21 (2+2 rows, `ebcebcae8594`). Only the 2022 route was genuinely unmeasured. HEAD now withdraws the claim on a different ground ("there is no stable side, only a failure to refute"), which is correct and sufficient to remove it — but it leaves on the record the impression that the measurement was new. It was not. Recording the independent ground so a later seat does not resurrect the claim if the "stable side" framing is ever rehabilitated.

**`0822-O2` — §6's prior-art dating is still one layer short of the corpus, in the paragraph about being short of the corpus.** The record states within-run digest instability on `pca-cpa.org/en/cases/309/` was *"established … on 2026-08-13."* `2026-08-13-daily.json` contains **no pca row at all**; the line you quote is a restatement inside an objection about an ordinal. The establishing rows are `2026-08-12-daily.json` `records[2]`/`[3]`/`[4]` — same URL, same run, 106,011 bytes each, **three distinct digests** (`9ac697396451`, `f81ff47ceccd`, `17211e49db18`). HEAD's `0822-R15` insertion pushes the *phenomenon* back to 2026-08-05 on unctad, which is right, but the sentence naming 08-13 as the establishing date for **this URL** is still standing and is still wrong by a day and by a category (restatement vs. measurement).

**`0822-O3` — §6 claim (3) survives, and I verified the evidence that saves it, but "sixteen days" is the wrong interval for what is being withdrawn.** The withdrawn category is the **three-limb** one: same length, different digest, *same rendered text*. The 2026-08-05 unctad rows used **different** finds (`Mapped by`, `ISDS forum options`), so they establish the two-limb form only. The three-limb form is first in the corpus at `2026-08-09-daily.json` `records[1]`/`records[6]` — same URL, same run, **same find** `China - Switzerland BIT`, 207,411 bytes both, digests `99f541775847` / `7bb930f60ea8`, **excerpts byte-identical**. I re-derived that pair at the pinned commit `d05dee6` (run `31312614020`) and it matches the record cell for cell. So: the phenomenon is 16 days older than the postscript; **the category the postscript claimed as new is 12 days older**. State the interval that belongs to the claim being withdrawn.

**`0822-O4` — §2's hedging is correct and I could not break it.** You asked me to test it and it holds. `urls[3]`'s null sits on the same page state as `urls[2]`'s match (85,589 / `7979b0e05e2a`), so page-render and find-machinery are controlled; `urls[4]`'s window does carry the literal `Rule 41(5)` as rendered text, verified character-exact; and the record then declines to transfer it, citing `G6`'s within-document non-uniformity and `G2`. The closing formulation — *"consistent with `41(5)` not appearing in that page's rendered stream, and … also consistent with a typesetting character inside an occurrence of it. The run makes the first reading more comfortable and does not establish it"* — is the correct disposition and I would not amend a word of it. One reinforcement available free and not used: `excerpt_of` lowercases both sides (`body.lower().find(find.lower())`, `scripts/fetch_relay.py:132`), so case is not a candidate explanation for the null, which narrows the residual to entity/zero-width/absence.

**`0822-O5` — §5 is right in every derived figure, and one scope clause is loose.** Re-derived over every row on `uncitral.un.org/en/commission` at `682e762`: **40 rows, 40 `ok`, byte-constant at 832,338 across all of them**; digest `f9712994f5ae` on **ten** dates 08-13→08-22; prior state `ca87a113c024` on **eight** dates 08-03→08-11; today's is the **tenth** `A/81/17`-scoped row against the current state (7 rows on the prior state, 10 on this one). Every number in §5 is correct. The loose clause is *"the corpus has no observation on 08-08 or 08-12"* — the corpus **has** reductions dated 08-08 and 08-12; what it lacks is a row on **this URL** on those dates. Taxonomy 27 asks for the scope on the face of the sentence.

**`0822-O6` — Rule 0: I greped all six banned forms and found none. The self-certification of one departure is, this time, correct.** Counted in the record at HEAD, each of the six strings occurs exactly **once** — in the Part I sentence disclaiming it. Heading census returns no "Priority focus", "Budgets", "Verify / be skeptical" or "Repository facts" section. On the two forms that need substance rather than string matching:
- **Instrument-probe table.** The banned form is `2026-08-07.md:39`, `| Instrument | Probe | Result |` over `curl` / WebFetch / WebSearch / relay. Today's Part I renders that content as **prose**. Part II's three tables are the register page-state census, the `urls[7]`/`urls[8]` digest pair, and the four-page same-run digest census — all censuses of **research resources**, not of the session's own tooling. **None is the banned form.**
- **Carrying-span ledger with per-term occurrence counts and parity arithmetic.** The banned form is `2026-08-07.md:477-483`, the `| Entry | Screened? | Citation changed? |` table closing on `M = 4 / N = 3`. Nothing of that shape is in the record. Part III's `§ CANDIDATE CLAIMS` is the analyst's standard JSON return.

Two departures were sustained against your predecessors on exactly this certification (`0819-B7`, `0820-B10`(c), `0821-B5`). **This one holds.** Confirmed independently: `6aed9a1` touched one file only, so "pushed alone" is exact; the precedence-conflict line appears in all ten daily records 08-13→08-22, so "tenth consecutive day" is exact; the relay blob pin `7204847f908d7958584842e523da8ae0ecd08703` is carried in every request file since 08-12 (understated by one day, in the safe direction); the eleven-council-day overwrite figure re-derives to exactly **11 files on 11 distinct dates** with more than one commit touching them.

**`0822-O7` — the `d017add` header-date repair is SOUND and I clear it without qualification.** `scripts/check_currency.py`'s check 2 is *"`**Last updated:** YYYY-MM-DD` — must not be older than the last commit that touched the file"*; the pre-edit file read 2026-08-19 while `e3ac94d` (2026-08-21) was the last commit touching it, so the hard `FAIL` claim is correct and the regression attribution to the 08-21 session is correct. The preserved 2026-08-19 entry is **byte-identical** to the superseded line minus its `**Last updated:**` prefix — I diffed it, not eyeballed it. Nothing dated was rewritten and no pending amendment was applied. The 08-22 block's substantive figures are all verified: the reduction commit, the both-routes-rendered claim, the 10-rows-against-1-page-state pair, and the digest-of-record. **Edit (a) passes.** Edit (b) is sustained against at `0822-B1` and `0822-B2` and is otherwise sound — see the near-miss below.

**`0822-O8` — batch validity confirmed independently.** `requested: 11`, `completed: 11`, `truncated: false`, `void: false`, `control_ok: true`; the relay's prepended control and both brackets return `ok/200`, `text/html`, **559 bytes / `ff67a9d764d6…`, identical across all three**; no row's `final_url` differs from its request, so no redirect is in play on any content row. `G1` is satisfied on all three checks as stated. The reduction is at blob `37aceca901dbc38eb38db492fa839146ec13104d`, which I confirmed is what `682e762` holds at that path — the `N12` citation discipline is correctly observed throughout Part II and I found no path-only citation to a reduction anywhere in Parts I or II.

**`0822-O9` — a small correction in your favour on §6's cost accounting.** *"Two of today's eleven relay rows bought a confirmation of a seventeen-day-old finding"* understates what those two rows are worth in one respect: `records[7]` and `records[8]` are byte-identical **and excerpt-identical to `2026-08-12-daily.json` `records[3]`** (same find, same 106,011 bytes, ten days apart, four distinct digests across the three rows). The rendered stream of that page has now held constant across ten days while its digest has moved four times. That is a stronger statement about digest unreliability than either observation alone, it is not prior art in that form, and it costs nothing to state. I am not directing you to add it — it is not an objection — but the record currently pays for it and does not collect it.

---

## COVERAGE STATEMENT

**Checked, by re-derivation at the commits named and not by acceptance:** the reduction blob identity and every field of all twelve records; all seven non-empty excerpts against the raw JSON character by character, including `U+200B`, `U+2019` and the undecoded `&amp;`; match indices for all seven and the `G8` clamp arithmetic on each; the relay's find semantics and excerpt mechanics read in source (`scripts/fetch_relay.py:115-136, 171-180`) and its blob pin recomputed; the register page-state census (4 dates, 17 rows, all cells); the `uncitral.un.org/en/commission` census (40 rows, both digest epochs, the `A/81/17`-scoped subset and today's ordinal); the prior-art greps for both ICSID routes at `44c67af` (0 and 0, reproduced); the corpus-wide multi-row group census (60 groups, 56 all-ok, 8 constant-byte/varying-digest, 5 unctad / 3 pca) and the `/978/` census (14 rows, 207,411, 14 digests) as adopted into Part II; the 2026-08-09 same-find pair at its pinned commit `d05dee6`; every pca-cpa.org row in the corpus; the Trans-Global join across all three carrying windows plus a corpus screen for `May 12, 2008` and `ARB/07/25`; `2026-08-13.md:618`, `2026-08-15.md:384`, `2026-08-20.md:594`, `2026-08-21.md:2069-2075` read in place; `d017add` diffed in both directions with occurrence counts pre and post; `scripts/check_currency.py`'s docstring; all six Rule-0 banned forms by string and by substance against the 2026-08-07 originals; the merge-overwrite census; the precedence-conflict day count.

**Not reached this turn:** Part III (the analyst's return) — I read it only where Part II adopts its findings, and I have **not** vetted `0822-R1` through `0822-R18`, the six candidate claims, the carrying-span R5 tiers over them, or `0822-E1`–`0822-E7`; that is turn 2 and I do not treat anything in it as vetted by this note. Parts IV, V and VI, which do not yet exist in final form. `scripts/check_marks.py` was not run — its docstring governs its scope and the `lit-review/` entries are unchanged today, so tier 4 remains by hand and undone. `prompts/carrying_span_rule.md` Amendment 2 (nonzero-referent parity) is **not** exercised by Parts I–II, which contain no screened-term counts of that form; it becomes live on the analyst's return.

**Not knowable from this session:** nothing in this note rests on an HTTP status observed by me. I made no network call. Every "absent" in this note is *absent from this repository at the commit named, under the terms screened* — never absent from the project and never absent from the world.

---

## NEAR-MISSES, DISCLOSED

1. **I nearly filed a fourth-generation self-destroying-anchor objection against `d017add`'s replacement pointer, and it does not hold.** The new instruction — *"search this file for the sole markdown link whose label pairs the word `italaw` with the words `Award PDF`"* — puts both literal tokens into the file, which looks like the 08-20 defect exactly. But the qualifier is structural, and it survives: a regex over markdown links returns **exactly one** link whose label contains both tokens, before and after the edit. The instruction describes a *link*, and the prose instances are not links. It is saved by the qualifier, not by the rule as written (*"Describe the anchor; never quote it"*), and I record that it is one qualifier away from failing.
2. **I nearly filed that §6's withdrawal of the third taxonomy category was an over-correction.** My reasoning was that the 08-05 and 08-12 prior art lacked the *same rendered text* limb because the finds differed — which is true of both. The 2026-08-09 same-find pair defeats me, and I found it only because HEAD's `0822-R15` insertion named it. **The withdrawal stands and my objection died on verification.** Filed here rather than dropped, because a suspicion I could not prove is worth exactly one line and no more.
3. **My `Rule 41(5)` screen at `0822-B4` is a literal-string screen over `excerpt` fields only.** I did not try `41(5)` alone, `Rule 41 (5)`, or `Rule 41(5)` outside excerpts. A zero-hit screen is not absence until the synonym is tried; my screen returned hits, so the objection does not depend on it, but the "first" it falsifies is falsified only under the terms screened.
4. **I nearly counted Part II's tables as the banned instrument-probe form** and stopped only after reading `2026-08-07.md:39` to see what the operator actually banned. The ban is on a table of the *session's own tooling*; had I not looked, I would have filed a false second departure against a correct certification — the same shape of error as the 2026-07-31 403.
5. **Part I's "spend checkpoint" sentence is one sentence from a "Budgets" section** and is currently on the right side of the line because it defers rather than tabulates. If Part VI renders it as a table of allocations, that is a second departure and it will be mine to file next turn.

---

## DISPOSITION

**FLAGGED — seven binding objections, nine observations.** None of the seven touches a substantive proposition about a case, an award, a proceeding, a treaty, a person or a document's existence; the record asserts no holding, decodes nothing, and the refusals at `G14`, `0821-R4` and §3 are intact in both the daily record and `STATE_OF_THE_ANSWER.md`. **The three failures you self-reported are all real and all correctly characterised.** They are not, however, the whole of it, and you asked for a fourth: there are three further ungreped or misgreped novelty claims in this record — `0822-B4` (§1's "first"), `0822-O1` (the register was already measured) and `0822-O2` (the pca establishing date is 08-12, not 08-13) — plus, in the cumulative file, the retracted §3 claim still standing at HEAD (`0822-B2`) and a count that falsified itself on commit (`0822-B1`). **The pattern is not that you failed to grep once. It is that the prior-art grep is being run against the claim you are worried about and not against the claims you are confident in**, and every one of today's misses is in a sentence written with confidence.

The most consequential item is `0822-B1`+`0822-B2` together, because both live in `STATE_OF_THE_ANSWER.md`, which does not self-correct tomorrow. The most fabrication-adjacent item is `0822-B3`, and your instinct about it was right: it does not stand on two windows, it stands on one, and the overlap is boilerplate. **Keep it, labelled CANDIDATE, with the two disclosures.** Do not remove it — a disclosed weak join that names its own confirmation probe is honest research; an undisclosed one is not, and a deleted one teaches the next seat nothing.

Sustained objections are to be applied in place with the pre-correction wording quoted, per the record's own standard. Nothing in this note authorises any change to `.claude/agents/`, `src/`, or the operator's pending list, and I have made no edit to any file.

---

## Part V(b) — The integrity officer's vetting note, turn 2, verbatim

The same seat, resumed, vetting the analyst's return. **Zero WebSearch calls across both turns.** Eight
further binding objections and eleven observations. Reproduced exactly as filed.

---

## BINDING OBJECTIONS *(continuing from turn 1; `0822-B1`–`0822-B7` stand unaffected by the analyst's return)*

### `0822-B8` — ANALYST. `0822-R13` and candidate claim 6: the merge-time census is **ten rows, nine `pca-cpa.org` and one `icsid.worldbank.org`**. His own table in the same section says so; his conclusion says nine and "8 of 8".

Offending text (`0822-R13`, and `claim_text` of candidate claim 6, which is ledger-facing):

> The split is **eight `pca-cpa.org` and one `icsid.worldbank.org`**, not seven and two … the constant-length merge-time instances are **8 of 8 on `pca-cpa.org`**.

Re-derived by his own stated method — first commit versus last commit touching each of the four files the postscript names:

```
2026-08-11-zeph.json   cc3730b -> 0a67756   2 rows changed   pca x2      excerpt same
2026-08-12-daily.json  4af5f11 -> 6cd27ac   4 rows changed   pca x4      excerpt same
2026-08-15-daily.json  f463d08 -> 31d16fd   2 rows changed   pca x2      excerpt same
2026-08-16-daily.json  1ec3e9c -> de3ed3c   2 rows changed   icsid x1 (105669->105666), pca x1
TOTAL digest-changed rows: 10   |  pca 9, icsid 1
constant-length subset:     9   |  pca 9, icsid 0
```

**His own table in `0822-R13` sums to 2 + 4 + 2 + 2 = 10 and to pca ×9 + icsid ×1**, and the sentence three lines below it reports nine and eight. He corrected the postscript's *host split* and silently inherited the postscript's *total*, then propagated the inherited total into a candidate claim. The corrected figures are: the postscript's population is **ten** rows, not nine; the split is **nine and one**, not eight and one and not seven and two; and the constant-length instances are **9 of 9 on `pca-cpa.org`**, not 8 of 8.

**His substantive conclusions survive every one of these corrections and are strengthened by them** — the icsid row is still the length-changer, still not an instance of the constant-length signature, and **no excerpt changed in any of the ten rows**, which I verified field-by-field. But candidate claim 6's `claim_text` currently carries a count that its own supporting derivation contradicts, and that is the one place a wrong number does durable damage.

### `0822-B9` — ANALYST. `0822-R16` and candidate claim 5: the excerpt-invariance population does not reproduce — **127 rows and 8 of 14, not 130 and 9 of 14** — and three of the fourteen keys were incapable of exhibiting the variation the screen looked for.

Offending text, identical in `0822-R16` and in candidate claim 5's `supporting_locator`:

> 14 distinct `(URL, find)` keys have ≥2 `ok` rows, **130 rows** in them; **9 of the 14 keys show a varying digest**; 0 of the 14 show a varying excerpt.

Re-derived at `682e762`, and then re-derived under five further grouping variants (percent-decoded URLs, trailing-slash normalisation, and with non-`ok` rows included) to give the figures every chance to fall out of a defensible scope:

```
keys with >=2 ok rows: 14 | rows: 127 | vary-digest: 8 | vary-excerpt: 0
  (all rows on those keys, incl. non-ok: 129 — still not 130)
variants: unquote -> 15/129/8/0 ; unquote+slash -> 15/129/8/0
          incl. non-ok -> 17/137/9/2   (the 2 are refused-vs-ok, i.e. '' vs text)
```

No scope produces 14 keys with 130 rows and 9 varying digests. The triple that is pinned by his own key count is **14 / 127 / 8**.

**And the population is partly tautological — taxonomy 26.** Three of the fourteen keys carry an **empty excerpt in every row** and therefore cannot show a varying excerpt under any circumstances:

```
uncitral.un.org/en/commission          find='A/81/17'    17 rows, excerpt '' throughout
icsid…/tables-of-decisions/…           find='ARB/23/28'   2 rows, excerpt '' throughout
www.italaw.com/…/italaw…pdf            find=''            2 rows, excerpt '' throughout
```

That is 21 of the 127 rows, including the single largest non-`example.com` key. The screen's *discriminating* set — keys with non-empty excerpts **and** a moving digest, the only rows in which "the digest moved and the window did not" can actually be observed — is **7 keys / 37 rows**:

```
uncitral/en/commission 'A/80/17' 16 | icsid register 'Manifest Lack of Legal Merit' 7
pca-cpa.org/309/ 'Final Award' 3 | unctad /978/ 'China - Switzerland BIT' 3
uncitral 'fifty-ninth' 3 | uncitral '' 3 | unctad /978/ 'ISDS forum options' 2
```

**The negative survives — 0 varying excerpts under every `ok`-scoped variant I tried — and I confirm it.** What does not survive is the population, and `0821-E7`, which he correctly invokes twice elsewhere, exists precisely because the population is the load-bearing half of a negative. Restate as: *14 keys / 127 rows screened, of which 7 keys / 37 rows could discriminate; 0 varying excerpts in either set.* Stated that way the finding is smaller and honest, and the register cell (7 rows, 7 files, 4 digests, **1 excerpt**) — which I re-derived and which is exactly right — remains the most valuable observation in the section.

### `0822-B10` — ANALYST. `0822-R15` and candidate claim 5: two quote-integrity defects in the return's most valuable item.

**(a) A paraphrase presented as a verbatim block quotation.** He renders the 2026-08-12 correction as:

> "`analytics/daily-research/2026-08-05.md:109` already records two rows in **the same run, minutes apart**, at equal length with different digests — so 'today's batch demonstrated it three times' was not the first same-run demonstration."

The file says, at `analytics/daily-research/2026-08-12.md:531-534`:

> Today's batch is not the record's first same-run demonstration of this phenomenon either. `analytics/daily-research/2026-08-05.md:109` already records two **treaty-978** rows in **the same run, minutes apart**, both at `bytes: 207411` with different sha256. My original text said "today's batch demonstrated it three times **on one page**" as though the same-run form were new. It is not.

Four departures: the identifying qualifier `treaty-978` is dropped; `both at bytes: 207411 with different sha256` is paraphrased to "at equal length with different digests"; `on one page` is dropped from the inner quotation; and two sentences are spliced and re-ordered into one with a manufactured "— so" connective. Under calibration item 2 a line presented as a verbatim source quote must be verbatim. The 2026-08-05 quotation in the same item **is** verbatim and I verified it character for character, which makes the contrast the point: he can do this and did it once and not twice.

**(b) Candidate claim 5's `supporting_quote` carries text this council struck.** The field reads:

> "The two treaty-978 rows in **the same run, minutes apart**, both returned `bytes: 207411` and **different** sha256"

`0805-B4` was **SUSTAINED** the same day — *"'minutes apart' is not in the record. Overreach on a temporal fact… Strike"* (`2026-08-05.md:549-550`, `:612`, `:950`). He discloses the strike in the `supporting_locator` and that disclosure is exactly right in prose — but `supporting_quote` is the field a downstream consumer reads, and a struck phrase sitting inside it with the correction one field away is **amendment-stripping in miniature (taxonomy 24)**: the artefact is authentic, the attribution correct, the date right, and the amendment that conditioned it is not in the object that travels. Elide the struck words inside the quote (`the same run […]`) and keep the locator note.

### `0822-B11` — ANALYST. `0822-R7`'s "six formulations" is **five**, and `0822-R18`'s "eight formulations across three days" is **seven**.

Offending text:

> **Six** formulations were fired at this award on 2026-08-20 and 2026-08-21 (S5, S6, the officer's call, S2, S3) plus two of mine today

The parenthetical enumerates five, and five is the correct number. Enumerated from the records:

```
2026-08-20 analyst  S5  "Hela Schwarz" China ICSID ARB/17/19 award 10 December 2025 …
2026-08-20 analyst  S6  Hela Schwarz v China 2026 commentary award …
2026-08-20 officer  Call 1 (Hela Schwarz)   [Call 2 = Eurohold; third call unspent — 2026-08-20.md:1319-1323]
2026-08-21 analyst  S2  "Hela Schwarz" "ARB/17/19" decision on jurisdiction italaw
2026-08-21 analyst  S3  "Hela Schwarz GmbH v. People's Republic of China" award analysis …
2026-08-21 officer  X1 = ENCORE/italaw, X2 = JLL Capital, X3 = ICSID "In Focus"  — none on this award
                                                                        TOTAL = 5
+ 2026-08-22 S3, S4 (S6 is the one that did pair IAReporter)            TOTAL = 7
```

**The substance is untouched and is the day's best finding**: no formulation paired the case with `IAReporter` until S6, and the optimization-log entry naming this exact case sat unopened. I verified that entry independently — `analytics/optimization-log.md`, dated **2026-07-22**, anchor resolves once, template verbatim, `_Status: proposed; demonstrated successfully today._`, and *"Triggered by today's Hela Schwarz 9(a) finding"*. But this seat sustained `0822-B4` against the chairman this morning for an ordinal without a derivation, and the same rule binds the seat that found the failure. Fix both figures to five and seven.

### `0822-B12` — ANALYST. `0822-R10` and candidate claim 4: the asymmetry table omits the one datum in the council's own record that neutralises the zero it rests on.

Offending text:

> | `ARB/24/26` | one TDM Journal title | one title; **zero titles returned when searched as a designator** |

The record holds that **the same engine returned zero titles for `ARB/24/46` — the designator that IS body-carried by ICSID's own register — twenty-four hours earlier**:

> `2026-08-21.md:701` — S4: `"ICSID Case No. ARB/24/46" ENCORE decision Rule 41` … **UNPAID, and its failure is today's method note.** Zero returned titles carry `ARB/24/46`.
> `2026-08-21.md:703` — S6: `"ENCORE" Türkiye ICSID ARB/24/46 Investment Group Limited case details` … **PAID.**

So a designator-quoted formulation returning zero titles has already been shown, *in this very matter, on this very engine, one day ago*, to be a property of the formulation and not of the designator. Today's S5 (`"ARB/24/26" ICSID case claimant respondent`) is closer in form to the failing S4 than to the succeeding S6. His stated limitation — *"But it is one notation"* — is the wrong limitation; the binding one is that the instrument has a demonstrated false-negative on exactly this class of query in exactly this matter. This is selective-flag reporting (taxonomy 9): the favourable half of the 08-21 pair is absent from the row that most needs it.

**His verdict does not change** — UNRESOLVED is right, and his refusal to prefer the majority is right. What must change is the third column: the `ARB/24/26` zero carries essentially no weight, and the honest asymmetry is *one body-carried designator against one title-carried designator*, with the search result contributing nothing on either side.

### `0822-B13` — ANALYST. `0822-R17` rows 5 and 6 go into tomorrow's batch with **no `final_url` branch pre-registered**, and without one both rows are uninterpretable in the failure mode they are most likely to hit.

Rows 5 and 6 probe `…/case-detail?CaseNo=ARB/24/26` and `…?CaseNo=ARB/24/46`. He correctly refuses an echoed find (taxonomy 23, his own catch) and pre-registers three branches on the window's *party names*. The gap: an unknown or malformed `CaseNo` on a database-backed page commonly **redirects or normalises** to a search page or a default record. The relay records `final_url` (`scripts/fetch_relay.py:176`) and it is the only field that would reveal it — and no gate mentions it. Under that failure the page renders `ok/200`, the find matches, and the window carries party names belonging to **whatever the redirect landed on**, which would be read straight into branch 1 or branch 2 as a body-carried answer.

**Required, before the batch is written:** a pre-registered gate reading, in substance — *if `final_url` differs from the requested URL on either row, that row is uninterpretable as to the requested designator and banks nothing in either direction; the branches below apply only where `final_url` is unchanged.* This costs nothing, and the whole value of `0822-R17` is that its licences are fixed before any result exists.

*(Rows 1–4 I have no objection to. Row 3's premise checks out: `Status of Proceeding` is body-present in the `ARB/22/2` case-detail excerpt of `2026-08-06-daily.json`. The prior-art greps all reproduce: parent `tables-of-decisions` **0 rows**; `CaseNo=ARB/17/19` **0 rows**, **0 files**; `CaseNo=ARB/24/26` and `CaseNo=ARB/24/46` **0 rows**; case-detail **16 `ok` rows**.)*

### `0822-B14` — ANALYST and CHAIRMAN. `0822-E6`: leaving the stub was the right principle, but **the stub points to the wrong section**, and four live cross-references run through it.

The stub, left standing at the analyst's request and reproduced verbatim by the chairman:

> ### `0822-R14` *(sic — see `0822-R16` for the row proposals; this number is used at `0822-R16` below)*

The row proposals are at **`0822-R17`** ("RELAY ROWS PROPOSED FOR TOMORROW"). `0822-R16` is "(d) WHAT IT LICENSES". Four places in the return forward-reference `0822-R14` for the row proposals — §A ("the honest instrument for it is the relay row at `0822-R14`"), `0822-R3` ("Proposed as rows at `0822-R14`"), `0822-R10` ("proposed at `0822-R14`") and `0822-R11` — so every one of them now resolves to a stub that sends the reader one section short of the target.

**Ruling: leaving the number standing is correct and I endorse it.** `0822-E6`'s reasoning is right — renumbering is how a later citation to `0822-R14` comes to resolve to the wrong finding, and this council has the `0821-R13`(4) dead-escalation precedent for exactly that. But "leave the number" and "leave the misdirection" are different things. The repair is one token — `R16` → `R17` in the stub — and it changes no number anybody cites. **Apply it, and note the correction in the stub rather than silently.** This is a chairman edit into a verbatim return, so it must be marked as such: the correction belongs beside the stub in the chairman's own voice, not inside the analyst's text.

### `0822-B15` — CHAIRMAN. `0822-R9` is correct on its facts, raising-without-editing was the right disposition **for his seat**, and the repair must be made today by yours — and the row carries a heavier defect than the one he found.

I verified the row. All three phrases are in quotation marks in `STATE_OF_THE_ANSWER.md`:

> China argued this constitutes **"a compulsory precondition qualifying the Respondent's offer to arbitrate."** The Claimant in Hela Schwarz **"did not even allege in the Memorial to have complied with the administrative review requirement."** The Award (December 10, 2025) addressed China's **"third objection to jurisdiction"** based on Article 9(a).

and all three listed sources are unread bodies on their own face: IAReporter `[title indexed; body paywalled]`, vLex UK `[body 403-blocked; snippet]`, JIDS Oxford `[abstract only]`. No per-quote pinpoint. **`0822-R9` is right**, and raising rather than editing is correct for a seat that edits no files — his close-out confirms he touched nothing.

**It should be repaired today, and by you.** The cost is one dated annotation on that row — *span not pinned; source unread* — and you are already required by `0822-B2` to touch that file this session. Do not delete the quotation marks: the vLex source is expressly labelled `[snippet]`, so the phrases are probably real text the project cannot pin, and stripping the marks would destroy information. Label them.

**And the row carries a defect he did not name, which is worse than the one he did.** The same row reads:

> **The tribunal's disposition on the Article 9(a) objection is now confirmed at MEDIUM-HIGH confidence (July 22, 2026):** IAReporter's indexed analysis article … confirms that 9(a) was also **applied and sustained**.

The title it rests on says the tribunal *"gives effect to idiosyncratic administrative review and fork-in-the-road clause found in BIT Protocol, dismissing all claims"*. **"Sustained" is not in the title, and neither is "applied."** That is taxonomy 4, title-as-holding, sitting in the canonical baseline and labelled *confirmed* — and it is the precise substitution the analyst himself refuses two sections earlier when he notes that "gives effect to" *"takes no position between jurisdiction and admissibility"*. He is right about the verb and then did not follow it into the sentence that misuses it. **Both go on the same dated annotation.** The honest form is: *the title carries "gives effect to … dismissing all claims"; whether the 9(a) objection was sustained, and on what characterisation, is not carried by any source this project has read.*

---

## OBSERVATIONS

**`0822-O10` — `0822-R13`'s census reproduces cell for cell, and I audited his derivation rather than carrying his figures.** At `682e762`, over **31 files / 208 records / 186 `ok` rows**: 60 multi-row `(file, URL)` groups, 56 all-`ok`, `bytes` constant **56/56**, `sha256` constant **48/56**, 8 divergent. Per host, every cell of his table: `example.com` 18/18/54/0; `uncitral.un.org` 17/17/36/0; `icsid.worldbank.org` 12/12/29/0; `investmentpolicy.unctad.org` 7/5/10/**5**; `pca-cpa.org` 3/3/7/**3**; `www.italaw.com` 2/0/0/0; `undocs.org` 1/1/2/0 — rows summing to 138. The `/978/` figure is exact: **14 `ok` rows, every one at 207,411 bytes, 14 distinct digests, none repeating.** This is the most reliable derivation in the record from either seat.

**`0822-O11` — `0822-R15`'s reproduction claim is TRUE, and it is more robust than he says.** Restricting my census to files dated on or before 2026-08-15 returns, against the 2026-08-15 table at that record's `:119-127`: `uncitral.un.org/en/commission` **9 / 0**; `example.com/` **8 / 0**; `icsid.worldbank.org` case-detail **3 / 0**; `pca-cpa.org` **0 / 2**; `investmentpolicy.unctad.org` **0 / 5** — identical in all ten cells. His HEAD extension (17, 18, 12, 3, 5) is also exact and the pca increment is indeed today's batch. What he does not say and should: **four of the files in that restricted set were overwritten in place after 2026-08-15** (`N12`), changing digest *values* — and the group-level classification survived unchanged. A census that reproduces across the very overwrites that impeach reduction paths is a stronger result than one that merely reproduces.

**`0822-O12` — `0822-R12`'s pinpoint correction against the chairman is right on both halves, and I checked it because nobody else had.** `scripts/fetch_relay.py:171-173` is `content = resp.content or b""` / `content_type = …` / `record.update({` — neither field. `:178` is `"bytes": len(content),` and `:179` is `"sha256": hashlib.sha256(content).hexdigest(),`, both over the `content` bound at `:171`. His collateral note is also right: there is no `analytics/fetch_relay.py`. His mechanism anchors all resolve — `records = [fetch_one(CONTROL_URL)]` `:199`, `out_path = out_dir / request_path.name` `:228`, `newest_request` `:233-235`, `ALLOWED_HOSTS = {` `:48` closing `:69` with `jusmundi` absent, `MIN_INTERVAL_SECONDS = 3.0` at `src/sources/base.py:28`. **One mechanism fact neither seat states and both rely on:** `bytes`/`sha256` are computed over `resp.content` (raw bytes) while `excerpt` is computed over `resp.text` (decoded) and truncated to 400 characters. That asymmetry is the whole reason a digest can move while a window does not, and stating it costs nothing and diagnoses nothing.

**`0822-O13` — `0822-R5`'s repository derivation reproduces exactly; the negative is correctly scoped and I would not strengthen it.** `www.italaw.com` non-PDF rows: **6 rows, 3 distinct URLs** (`/`, `/cases/8154`, `/cases/documents/10912`), **3 result files, `refused` on all six**. PDF rows: **4, all `ok`**. He then declines to convert the refusals into a statement about italaw, per `G3`, and confines the conclusion to "renditions I could surface". That is the correction to `0821-B17` done properly and in the right direction — he did not over-correct into a negative his evidence cannot carry, which is what you asked me to test. Two scope clauses are load-bearing and are present: *on any allowed host*, and *on the evidence I can gather*. The claim is about a **route**, not a document, and it says so.

**`0822-O14` — two of the six anchors he supplies do not literally resolve.** `analytics/daily-research/2026-08-05.md` — the anchor *"The two treaty-978 rows in the same run"* fails, because the source reads `rows in **the same run, minutes apart**` and the emphasis markers fall inside the span. `analytics/daily-research/2026-08-15.md` — *"every group of two-or-more rows on the same URL in the same run at constant byte length"* fails, because the source wraps between `rows` and `on the same URL`. Both are recoverable by an obvious relaxation and both point at the right place, so nothing is lost today. But this council has now spent four generations on pointers that fail (`0820-R18`, `0821-R12`, and today's `0822-B1`), and an anchor is only better than a line number if it resolves. **Rule (s) needs one clause: an anchor must be a span that survives whitespace collapse and emphasis-marker removal, and must be verified to resolve before it is written.** I checked all six of his; the other four resolve exactly once each, including the optimization-log anchor and the `STATE_OF_THE_ANSWER.md` Article 9(a) anchor.

**`0822-O15` — `0822-R17` row 2 fires a literal whose provenance is the refused decode itself, and the row does not say so.** Every other literal this council has fired was corpus-sourced; `not applicable` came from the engine's generated prose at `0822-R2`. His licence correctly says a match "does not confirm the engine's prose", which is the important half. The missing half is on the face of the row: **this find is engine-sourced, not corpus-sourced**, so a match is a match on a string the instrument that produced the suspect claim also produced. That is not taxonomy 23 — the page cannot generate it from the request — but it is its neighbour, and the provenance belongs in the row so that tomorrow's seat meets it stated rather than has to reconstruct it.

**`0822-O16` — `0822-R10`'s search returned two designators one character from the contested one, and the record does not say so.** He lists the returned case numbers as *"`ARB/05/8`, `ARB/21/26`, `ARB/24/6` and others"*. `ARB/21/26` and `ARB/24/6` each differ from `ARB/24/26` by a single character. That is the datum most likely to tempt a later seat into adopting the transcription-error hypothesis, and disclosing it makes his refusal to adopt it stronger, not weaker — a refusal that has not met the tempting fact is not yet a refusal. (The "and others" enumerates three of nine; naming three of a class of nine and closing with "and others" is the shape of taxonomy 14, though nothing here turns on the unnamed six.)

**`0822-O17` — a small count in `0822-R17`'s preamble.** *"the `case-detail` form specifically has **16 `ok` rows across three case numbers** (`ARB/22/2` ×3, `UNCT/20/6` ×13)"* — 16 is right and the split is right, but it is **two** case numbers across **three** URL strings (`CaseNo=ARB/22/2` ×3, `CaseNo=UNCT%2F20%2F6` ×12, `CaseNo=UNCT/20/6` ×1). His own parenthetical names two. The 2026-08-21 officer recorded the same figure correctly as "×13 across two URL encodings".

**`0822-O18` — carrying-span R5 and Amendment 2, applied to all six candidate claims.** *Amendment 2 passes.* Every screened term with a nonzero count carries a referent clause: claim 1's `9 and 10 results` → "no returned title is a legend…"; claim 1's `17 rows` → "all 17 on the child URL"; claim 2's `6 rows / 3 URLs / 3 files` → "`refused` on all six" and `4` → "all `ok`"; claim 3's third replication → "not banked"; claim 5's whole census → the express non-licence. **Claim 5's treatment of its zeros is model practice and I want it on the record as such**: `0/12` on icsid and `0/17` on uncitral are stated *with* the 2026-08-15 one-directional ruling attached — *"a within-run control can only ever refute stability, never establish it"* — so the zeros cannot be read as corroboration. That is precisely what Amendment 2 exists to force, arrived at without being asked. *One tier defect:* claims 1, 2 and 3 carry `supporting_quote: null` where this project's convention for a negative claim is an explicit `Q — N/A` **with the reason** (`2026-08-21.md:1260`: *"`Q` — N/A, no carrying span exists in any source I can open"*). A bare `null` is an absent field, not a reasoned N/A, and the two are indistinguishable to a downstream reader. Claim 4's `Q` carries a quotation pair and a pinpoint and I verified the quote **character-exact** against `2026-08-20-daily.json` `records[3]` — including `Türkiye` and the trailing register token `No`, which he correctly reproduces as rendered and does not translate.

**`0822-O19` — `0822-R8`'s two carriers verify verbatim, and one of them has a superseded predicate he does not flag.** `STATE_OF_THE_ANSWER.md`: *"The Award (December 10, 2025) addressed China's \"third objection to jurisdiction\" based on Article 9(a)"* ✓. `analytics/daily-research/2026-07-15.md:50`: *"MEDIUM confidence, from search-engine summaries"* ✓ and *"Whether the tribunal actually *upheld* the Article 9(a) bar — as distinct from the 9(c) bar — is **unknown**"* ✓, with his ellipsis honest. The withdrawal of his own `0821-R10` wording is correct and correctly bounded — *pleaded as* an objection to jurisdiction is not *characterised by the tribunal as* jurisdictional, and he says so in terms. The unflagged item: the same 07-15 sentence continues *"the Award PDF remains 403-blocked across all retrieval routes (italaw, icsidfiles, jusmundi)"*, which 2026-08-19 falsified — the italaw award PDF returned `ok/200`, 1,272,557 bytes, and there are four `ok` italaw PDF rows in the corpus. Nothing he draws from that record depends on it, but taxonomy 10 asks that a seat quoting a dated record note where that record has since been overtaken.

**`0822-O20` — `0822-E1`, `0822-E4` and `0822-E7` all verify, and `0822-E7` is the best self-correction either seat filed today.** `0822-E4`: `git grep -l -F "landmark-icsid-victory-for-the-peoples-republic"` → **9 files**; `aoshearman` → **9**. Both exact. `0822-R7`'s new title: `hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation` → **0 files**, genuinely new. `0822-E7` catches him leaning on `icsid`'s 0-of-12 three paragraphs after quoting the ruling that forbids leaning on it, strikes his own third reason, and keeps the conclusion on the two that survive — that is the asymmetric-filter catch made against himself, unprompted, and it is what saved the chairman's Part II §6 from carrying a "stable side" that does not exist.

---

## COVERAGE STATEMENT

**Checked by independent re-derivation, not by acceptance:** the full `(file, URL)` group census at `682e762` (31 files / 208 records / 186 `ok` rows / 60 / 56 / 8) and its per-host breakdown; the same census restricted to files dated ≤ 2026-08-15 against the 2026-08-15 table cell by cell; the `/978/` census; the `(URL, find)` key census under six grouping variants, plus the empty-excerpt and discriminating-set partitions; the merge-time census blob-by-blob for all four named files, first commit versus last, row by row, with excerpt comparison on every changed row; `scripts/fetch_relay.py` lines 48–69, 76, 115–136, 171–182, 199, 215, 227–235 read in source; `src/sources/base.py:28`; all four `0822-R17` prior-art greps; the `case-detail` row census by URL; the italaw HTML/PDF census; the `tables-of-decisions` parent/child census; the optimization-log entry, its date, its anchor and its trigger; all six anchors tested for literal resolution; every search formulation on this award across 2026-08-20, 08-21 and 08-22 enumerated from the three records including both officers' calls; candidate claim 4's `supporting_quote` character-exact against the raw reduction; candidate claim 6's `supporting_quote` against the 08-21 postscript modulo emphasis markers; the 2026-08-05, 2026-08-12 and 2026-08-15 prior-art passages read in place; the `STATE_OF_THE_ANSWER.md` Article 9(a) row and `2026-07-15.md:50` read in full; `0822-E4`'s two git greps; carrying-span R5 and Amendment 2 applied to all six candidate claims.

**Not reached, and named so coverage is not read as silence:** I cannot verify that Part III is **verbatim**. I have no copy of the analyst's return as filed, only the chairman's reproduction of it; the transport note about three HTML-escaped ampersands is unverifiable by me in either direction, and the chairman's assurance that he "added nothing, removed nothing, corrected nothing" is accepted on his word, not checked. I did not examine the content of any WebSearch result — six calls, three engines' worth of returned prose, none of it in the record — so every statement about *what a search returned* rests on the analyst's report of it; where a returned title matters (the IAReporter bifurcation article, the parent tables page, the three PDF URLs) I could confirm only that it is new to or absent from this repository, never that the engine returned it. I did not re-run `scripts/check_marks.py` (no `lit-review/` entry changed today and all 33 remain in the legacy form, so tier 4 stays by hand and undone). I have not vetted Parts IV, V or VI, which are unwritten or unfinished, and the optimization item and spend checkpoint are therefore unexamined. **Zero WebSearch calls spent across both turns** — nothing in either docket turned on a fact outside this repository, and I decline to spend budget to confirm what I cannot then pin.

**Not knowable from this session:** as in turn 1, nothing in this note rests on an HTTP status observed by me; I made no network call. Every "absent" is *absent from this repository at the commit named, under the terms screened* — never from the project and never from the world.

---

## NEAR-MISSES, DISCLOSED

1. **I nearly filed that `0822-R15`'s "reproduces that table exactly" was unverifiable and probably loose.** It is exact in all ten cells. My prior was that a census reproduced across four merge-time overwrites would drift; it did not, and I was wrong to expect it to.
2. **I nearly filed against `0822-R16`'s "0 varying excerpts" as unsupported.** It holds under every `ok`-scoped variant I could construct. The defect is the population, not the negative, and `0822-B9` is written to say only the smaller thing I can prove.
3. **I nearly sustained `0822-B8` at "nine rows, eight and one" — i.e. I nearly reproduced his error.** My first pass compared each file's last two commits rather than first-versus-last, which drops `2026-08-11-zeph.json`'s earlier overwrite. His stated method (first versus last) is the correct one and it is the one that yields ten. I record this because the objection I am filing is one I very nearly got wrong in the same direction he did.
4. **My `0822-B12` rests on the 2026-08-21 record's characterisation of its own S4 result, which I did not independently verify** — I cannot see that search either. If S4's zero was a different kind of zero, the objection weakens. I file it because the record is the council's own and he is bound by it exactly as I am.
5. **I did not screen notational variants of `ARB/24/26` in the repository** (`ARB 24/26`, `ARB.24.26`, bare `24/26`) before endorsing his UNRESOLVED. His own limitation statement covers the search; neither of us has covered the repository. A zero-hit screen is not absence until the synonym is tried, and this one has not been tried at all.

---

## DISPOSITION

**FLAGGED — eight binding objections, eleven observations.** Turn 1's seven stand unaffected; nothing in the analyst's return touches them, and `0822-R15`'s prior-art layering independently corroborates `0822-O2` and `0822-O3`.

**This is the stronger of the two returns, and the pattern in its defects is the opposite of the chairman's.** Every one of the chairman's failures today was a claim asserted more confidently than a grep would allow. Every one of the analyst's is an **arithmetic slip inside a derivation whose method is sound and whose conclusion survives** — 10 read as 9, 127 as 130, 8 as 9, five as six, seven as eight. Not one of his substantive findings falls: the prior art really is seventeen days deep with two prior corrections; the host scope really was wrong; the zeros really do license nothing; the pinpoint correction against the chairman is right; no excerpt in this corpus has ever moved; the optimization-log entry really did sit unopened. **Five wrong integers in one return is nonetheless a finding about the seat**, because this council's whole method is that a derived number is the thing a later seat inherits and cannot check, and because the return itself is an indictment of a chairman who did not check his numbers.

The item I would put first if only one can be applied is `0822-B15`'s second limb — the *"applied and sustained"* in `STATE_OF_THE_ANSWER.md`. It is the only defect found across both turns that puts a **holding** into the canonical record on a carrier that does not contain it, it has been sitting there since 2026-07-22 labelled *confirmed*, and both seats read that row today and neither saw it. Everything else on today's list is a count, a pointer or a scope clause. That one is the thing this project exists to not do.

`0822-B14` requires a chairman edit inside a verbatim return; make it in your own voice, beside the stub, not inside his text. `0822-B13` must land **before** tomorrow's batch is written, not after. `0822-B15`'s repair should go on the same dated annotation you are already required to add to that file by `0822-B1` and `0822-B2`.

I have made no edit to any file across either turn.

---

## Part VI — Chairman's close-out

### The first thing in it is not a ruling

**This council spent its best work today re-finding things it had already written down, and it did so on
the day after adopting the rule against exactly that.** Four fresh re-derivations, two of them mine, one of
them a claim I handed to another seat for verification without running the one-line check I was
simultaneously lecturing him about. The officer's diagnosis is sharper than my confession and I adopt it
verbatim as the finding of the day:

> **The pattern is not that you failed to grep once. It is that the prior-art grep is being run against the
> claim you are worried about and not against the claims you are confident in**, and every one of today's
> misses is in a sentence written with confidence.

That is a better description of the failure than anything I wrote about it, and it is the reason today's
optimization item proposes a **field** rather than another rule.

**And the single most consequential thing found today was found by neither of the seats who went looking
for it.** `0822-B15`'s second limb: `STATE_OF_THE_ANSWER.md` has carried, since **2026-07-22**, labelled
*confirmed* at MEDIUM-HIGH, the assertion that the *Hela Schwarz* tribunal's Article 9(a) objection was
*"applied and sustained"* — on the strength of a title that says the tribunal *"gives effect to"* those
clauses and says nothing about sustaining anything. **A holding, in the canonical record, on a carrier that
does not contain it, for thirty-one days.** The analyst established two sections earlier in the same
session that *"gives effect to"* takes no position on the very distinction at issue, and then did not follow
his own verb into the sentence that misuses it; I read that row twice today and did not see it either. The
officer found it while verifying somebody else's objection. **It is repaired.** Everything else on today's
list is a count, a pointer or a scope clause; that one is the thing this project exists to not do.

### Rulings on every objection

**Fifteen binding objections. All fifteen SUSTAINED. None overruled, none partially sustained.** Seven
against me in turn 1, seven against the analyst in turn 2, one (`0822-B14`) against both. Every one is
applied in place with the pre-correction wording quoted.

**Against the chairman — `0822-B1` to `0822-B7`, all sustained, all applied.**

- **`0822-B1`** — the `[403-blocked]` count. **Sustained.** Six was correct when computed and false when
  published, because the commit carrying the sentence added the seventh occurrence. I verified 6 → 7
  independently. Replaced with a non-quantitative statement, which cannot falsify itself. **This is the
  fourth generation of one defect** — line number, self-naming anchor, line number again, and now a
  self-referential quantity — and it is in the cumulative file, so it does not decay tomorrow.
- **`0822-B2`** — the retracted novelty claim still live in `STATE_OF_THE_ANSWER.md`. **Sustained, and it is
  the one I am most embarrassed by**, because I wrote the retraction and put it in the file that gets read
  once instead of the file that gets read every session. *"A correction that lands only in the retracted
  file's neighbour is not a correction."* Applied in place.
- **`0822-B3`** — the Trans-Global join. **Sustained.** The join rests on **one** window, not two: the
  2026-08-20 window stops eleven characters short and never reaches the overlap. The overlap is four
  characters of boilerplate that recurs twice inside today's own window. **Kept as CANDIDATE with both
  disclosures, on his direction and his reasoning** — a disclosed weak join that names its own confirmation
  probe is honest research; a deleted one teaches the next seat nothing. My recall agreeing with the join is
  recorded as evidence against it.
- **`0822-B4`** — the third ungreped "first", in §1. **Sustained**, and barred by a gate I wrote this
  morning. `Rule 41(5)` was in a returned window on 2026-08-20 — one this same record quotes twenty-five
  lines later. Ordinal struck, scope made explicit.
- **`0822-B5`** — two `U+2019` transcribed as ASCII in the window the record calls "the evidence".
  **Sustained.** Repaired to character-exact; the composed join is now marked as a reconstruction with its
  elisions shown; emphasis added is marked as added.
- **`0822-B6`** — `urls[4]`'s match stated at offset 0 when it is at 34. **Sustained**, re-derived, applied.
- **`0822-B7`** — my `G11` self-report understated the batch file's defects. **Sustained.** Two further
  gate errors: a row/batch unit switch inside `G11`, and a three-scope series in `G13`. **His ruling on my
  question is adopted whole and is the useful distinction:** the wrong comparator invalidated nothing
  decided under it, because the branch fires identically against the correct figure — the pre-registration's
  *protective* function held — but its **audit** function failed. *"A pre-registration is evidence about the
  past as well as a constraint on the present."* Corrected here; **the batch file at `6aed9a1` stays wrong
  and is not edited**, because a pre-registration amended after its result is not a pre-registration.

**Against the analyst — `0822-B8` to `0822-B13`, all sustained. Corrected figures recorded here rather
than in his text, which stays verbatim.**

- **`0822-B8`** — the merge-time census. **Sustained.** The population is **ten rows, nine `pca-cpa.org`
  and one `icsid.worldbank.org`**; the constant-length instances are **9 of 9 on `pca-cpa.org`**. His own
  table sums to ten and he reported nine — he corrected the postscript's host split and silently inherited
  its total, then carried the inherited total into a ledger-facing claim. **His conclusions survive and are
  strengthened**: the icsid row is still the length-changer, still not an instance of the constant-length
  signature, and no excerpt changed in any of the ten.
- **`0822-B9`** — the excerpt-invariance population. **Sustained.** The correct triple is **14 keys / 127
  rows / 8 varying digests**, not 14 / 130 / 9, and no grouping variant produces his figures. **And the
  population is partly tautological:** three of the fourteen keys carry an empty excerpt in every row and
  could not have shown variation — 21 of the 127 rows. **The negative itself survives every variant.** The
  honest form, adopted: *14 keys / 127 rows screened, of which 7 keys / 37 rows could discriminate; zero
  varying excerpts in either set.* Smaller and true.
- **`0822-B10`** — two quote-integrity defects. **Sustained on both limbs.** (a) The 2026-08-12 correction
  is rendered as a block quotation and is a paraphrase — four departures including a dropped identifying
  qualifier and a manufactured connective. (b) Candidate claim 5's `supporting_quote` carries *"minutes
  apart"*, **struck by `0805-B4` the day it was written**, with the disclosure one field away in the
  locator. That is amendment-stripping in miniature: the field that travels does not carry the amendment
  that conditioned it. **The elision is directed for any downstream use of that claim.**
- **`0822-B11`** — "six formulations" is five; "eight across three days" is seven. **Sustained.** The
  substance — that not one formulation paired the case with `IAReporter` until today's S6, while the
  optimization log has named this exact case since 2026-07-22 — is untouched and is the day's best finding.
  The officer verified that log entry independently.
- **`0822-B12`** — the `ARB/24/26` asymmetry omits the datum that neutralises it. **Sustained, and this is
  the most substantive of the seven against him.** The same engine returned **zero titles for `ARB/24/46`**
  — the designator ICSID's own register body-carries — twenty-four hours earlier. So a designator-quoted
  zero is already demonstrated, in this matter on this engine, to be a property of the *formulation*. **The
  verdict UNRESOLVED does not change; the weight does.** The honest asymmetry is one body-carried designator
  against one title-carried designator, **with the search contributing nothing on either side.**
- **`0822-B13`** — rows 5 and 6 of tomorrow's batch have no `final_url` branch. **Sustained, and it lands
  before the batch is written, not after** — carried into next step 3 below as a binding condition. A
  database-backed page that redirects or normalises an unknown `CaseNo` returns `ok/200` with party names
  belonging to whatever it landed on, and both of his pre-registered branches would read that as a
  body-carried answer.

**Against both — `0822-B14`. Sustained.** The stub points to `0822-R16`; the row proposals are at
`0822-R17`, and four live cross-references run through it. **Leaving the number standing is right** — the
analyst's reasoning at `0822-E6` is correct and this council has the dead-escalation precedent for
renumbering. Leaving the *misdirection* is not. Corrected **beside** his text in my own voice, per the
officer's direction; his line is untouched.

**Observations adopted rather than merely noted:** `0822-O2` (the pca establishing date is **2026-08-12**,
and what I cited from 08-13 was a restatement inside an objection, not a measurement — wrong by a day and
by a category, in the paragraph about being short of the corpus); `0822-O3` (the withdrawn category is 12
days old, not 16 — the 08-05 rows used different finds and establish only the two-limb form); `0822-O5`
(scope clause); `0822-O9` (collected, **with its own digest count corrected against the officer: three rows
carry three digests, not four** — the first figure a seat has had to correct against him today, and it is
in the one paragraph he offered as a gift rather than a finding); `0822-O14` (rule (s) needs a clause: an
anchor must survive whitespace collapse and emphasis-marker removal, and must be **verified to resolve**
before it is written — two of the analyst's six do not); `0822-O15` (row 2's literal is **engine-sourced,
not corpus-sourced**, and the row must say so); `0822-O16` (the search returned two designators one
character from the contested one — disclosing it makes the refusal stronger); `0822-O12`'s unstated
mechanism fact, which both seats relied on and neither wrote down: **`bytes` and `sha256` are computed over
`resp.content`, raw bytes, while `excerpt` is computed over `resp.text`, decoded and truncated — that
asymmetry is the whole reason a digest can move while a window does not.**

**One departure I am recording against myself rather than being caught on: the analyst was not resumed for
a second turn.** Yesterday's council resumed him to answer the objections against him; today's seven —
`0822-B8` to `0822-B13` and `0822-B14` — are answered by me, from the officer's derivations, and not by
him. **He has not conceded them and has not had the chance to contest them.** Every one is arithmetic
inside a derivation the officer independently reproduced, and I judged the corrected integers safe to
record without his concurrence; that judgement is mine and it is a real reduction in the adversarial
standard this council normally applies. It should not become practice.

### The one optimization item (Rule 0)

*(Written in full into `analytics/optimization-log.md`; summarised here.)* **The prior-art check works when
it is a required field and fails when it is a rule, and today's record is a controlled experiment proving
it.** Yesterday's adopted item was the prior-art rule; it was in force for eighteen hours and this session
produced four fresh re-derivations anyway. But the same session made **two** novelty claims that carried a
written receipt — the two never-fetched ICSID routes, greped and recorded in the request file's required
`prior_art_check_run_before_this_file_was_written` field **before the file was written** — and **both were
correct**, independently reproduced by the analyst. **Four unreceipted novelty claims, four wrong; two
receipted, two right**, from the same seat in the same hour. The proposal, in two parts: **(1) scope** (the
analyst's half, `0822-R18`) — the prior-art corpus includes `analytics/optimization-log.md` and
`analytics/insights.jsonl`, where this project stores methods that already worked; **(2) mechanism** — any
sentence asserting novelty must carry an **inline receipt naming the terms screened and the counts
returned**, in the shape the request file already requires, and a novelty claim without one is downgraded
to *"not found under terms not stated."* Deduped against all prior entries; nearest neighbour is the
2026-08-21 entry, which governs **routes and hosts at batch-firing time** — and all four of today's failures
were claims about a **finding, a method or a source, in prose, after the batch had fired**, outside its
trigger by construction. **Proposed only. Nothing built, nothing applied.**

### Spend checkpoint, honoured after the vetting round as Part I said it would be placed

**Stated as prose, not as a table of allocations** — the officer's near-miss 5 warned that a tabulated
version would be a second Rule 0 departure, and he was right to warn.

**Analyst:** nine WebSearch calls authorised, **six spent, three returned unspent**, zero fetches attempted.
Of the six, he judges two purchasable for free from the record (`0822-E4`, and arguably S1). **Officer:**
three calls authorised across two turns, **zero spent**, on the ground that nothing in either docket turned
on a fact outside this repository. **Chairman:** one relay batch, eleven rows, fired once; no second batch;
zero searches. **Relay:** one run, `32569747298`, twelve records, all `ok`.

**What the spend bought, honestly.** Two rows opened both never-fetched ICSID routes — the day's real
acquisition. Two rows bought a seventeen-day-old finding. Two rows are the standing `A/81/17` watch and were
worth approximately nothing beyond the first ever taken. One analyst task was spent verifying a claim the
record already held three times over — **a waste of his budget that produced the most valuable section of
his return**, and those are not in tension. **The council's cheapest instrument today was `git grep`, and
its most expensive mistake was not running it.**

### Accountability

**Chairman — the worst-performing seat today, and not narrowly.** Seven sustained objections, four
re-derivations of council knowledge (two mine), three novelty claims made without the check I was
demanding of another seat in the same message, a pre-registration file carrying three defective figures, a
quotation that normalised the very characters the record's argument is about, and a correction I landed in
the daily file while leaving the claim standing in the cumulative one. **What I did right is thinner and I
will not inflate it:** I fired the batch before convening anyone so the pre-registration is provable by
commit order; I ran the prior-art greps on the two route claims and recorded them, which is the only reason
those two were right; I self-reported three failures before the officer was convened; and I repaired the
`STATE_OF_THE_ANSWER.md` header `FAIL` rather than explaining it away.

**Research analyst — the strongest substantive return this seat has filed, with a systematic arithmetic
defect running through it.** Six sustained objections, **every one an integer inside a derivation whose
method is sound and whose conclusion survives**: 10 read as 9, 127 as 130, 8 as 9, five as six, seven as
eight. Not one substantive finding falls. Against that: he discharged the `0821-B17` mirror-image check in
the right direction, refused two engine-generated decodes and **refuted** one on the council's own evidence,
withdrew his own prior wording as too strong, spent zero of his Task 4 budget as directed, disclosed five
things he did not check, and filed `0822-E7` — catching himself leaning on a zero the 2026-08-15 ruling
forbids, three paragraphs after quoting that ruling. **`0822-E7` is what saved my Part II §6 from carrying
a "stable side" that does not exist.** The officer's judgement is right: five wrong integers is a finding
about the seat, because a derived number is what a later seat inherits and cannot check.

**Integrity officer — the seat that carried this record, for the second consecutive day.** Fifteen binding
objections, **zero WebSearch calls across two turns**, every count re-derived rather than accepted, every
quoted window checked character-exact including zero-width and curly-quote characters, all six Rule 0
banned forms greped by string **and** by substance against the 2026-08-07 originals before clearing my
self-certification, five near-misses disclosed including one where he nearly reproduced the analyst's own
error in the same direction. **He found the `"applied and sustained"` holding that two seats read past.** He
also declined to strengthen findings he could have — clearing my §2 hedging with *"I would not amend a
word of it"*, and offering `0822-O9` as a gift rather than an objection. **One correction against him: that
gift's digest count was wrong by one**, and it is the only error either seat found in his work today.

**Model conformance.** Analyst and chairman on **Claude Opus 5**, conformant with the directive of
2026-08-03. The officer self-reports **`claude-opus-5`** while `.claude/agents/integrity-officer.md`
declares `claude-opus-4-8` — the **tenth** consecutive self-reported discrepancy, escalation `N2`, recorded
as a procedural caveat and never substituted silently.

**Rule 0 compliance: one departure, and this time the certification held.** The officer greped all six
banned forms and found none; he independently confirmed that Part II's three tables are censuses of
research resources, not of the session's own tooling, and that the banned instrument-probe form is a table
of the session's instruments. He nearly filed a false second departure and stopped only after reading the
2026-08-07 original to see what the operator actually banned. **Three predecessors were sustained against
on this same certification; today's holds.**

### Next steps for tomorrow

1. **THE PRIOR-ART CHECK IS ITEM 1, IT IS PROCEDURE NOT RESEARCH, AND ITS CORPUS IS WIDER THAN ANY SEAT
   HAS BEEN TREATING IT** (method rule (u), as extended by `0822-R18` and today's optimization item).
   Before a batch is written and before any sentence claims novelty:
   `git grep` `analytics/fetch-results/`, `analytics/daily-research/`, **`analytics/optimization-log.md`**
   and **`analytics/insights.jsonl`**. All four of today's re-derivations die in one of those four places,
   and two of them die in the two that were not previously part of the check.
2. **THE `Trans-Global` CONFIRMATION IS THE FIRST ROW AND IT IS FREE.** Today's `Partial` window truncates
   its row's case name; **ONE** earlier window ends where it begins and joins, across page states, to
   *Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25)* — the
   2026-08-19 window; the 2026-08-20 one stops eleven characters short (`0822-B3`). **That is a CANDIDATE,
   not a finding**, on ONE window, across page states, over a four-character overlap inside a boilerplate
   title that recurs twice in today's own window. One same-run pair settles it: find `Trans-Global` on the register, with a same-URL digest
   row in the same file. **If it nulls, the candidate is withdrawn, not weakened.**
3. **THE ANALYST'S SIX ROWS AT `0822-R17` ARE ADOPTED WHOLE, LICENCES VERBATIM.** The parent tables page
   (`…/cases/content/tables-of-decisions`, 0 rows in the corpus, 0 files repo-wide for its `/annulment`
   sibling) with finds `Manifest Lack of Legal Merit` and `not applicable`; and four `case-detail` rows on
   `ARB/17/19`, `ARB/24/26` and `ARB/24/46`. **FOUR constraints travel with them and all are binding.** (a) **`0822-B13`, SUSTAINED, and it must land
   BEFORE the batch is written, not after: rows 5 and 6 need a pre-registered `final_url` gate.** A
   database-backed page commonly redirects or normalises an unknown `CaseNo`; under that failure the page
   returns `ok/200`, the find matches, and the window carries party names belonging to whatever it landed
   on — which both pre-registered branches would read as a body-carried answer. The gate, in substance:
   *if `final_url` differs from the requested URL on either row, that row is uninterpretable as to the
   requested designator and banks nothing in either direction; the branches apply only where `final_url`
   is unchanged.* (b) **`0822-O15`: row 2's literal `not applicable` is ENGINE-SOURCED, not corpus-sourced**
   — every other literal this council has fired came from a body — so the row must say so on its face: a
   match is a match on a string the same instrument that produced the suspect decode also produced. (c) The
   `not applicable` row is fired **against** his own refusal at `0822-R2` and a match does **not** confirm
   the engine's prose — only the window can place the string as a legend. (d) Row 4's licence in his own
   words: *"A document designation is not the tribunal's characterisation and CANNOT answer question (3).
   If a future seat cites this row for question (3), that citation is wrong and this sentence is why."*
4. **THE FINDING-COLUMN CODEBOOK REMAINS UNRESOLVED AND THE REFUSALS ARE NOW THREE DEEP.**
   `GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook`, **two calls deep with four synonym families
   untried** (`codebook`, `notes to the table`, `how to read`, `methodology`, and the disclaimer family) —
   the analyst states this against himself at `0822-R4`. The column's header **is** corpus-carried
   (`Finding that Claim is Manifestly Without Legal Merit`, since 2026-08-20) and **it does not decode the
   values.** `0821-R4`'s obstacle stands: a title-carried `Dismissing` sits on a `_` row. **`No` may not be
   read as "objection dismissed", and `_` may not be read as "not applicable"** — the engine offered that
   second decode today, unprompted, and the analyst refused it at `0822-R2` **and refuted it** on the
   council's own body-carried evidence. Three refusals in two days by three seats.
5. **BOTH NEVER-FETCHED ICSID ROUTES ARE NOW FIRED AND ALIVE, AND THAT CLOSES NEXT STEP 2 OF 2026-08-21.**
   `…/manifest-lack-of-legal-merit/2022` (85,589 bytes, `7979b0e05e2a…`) and
   `…/focus-objections-claim-manifestly-lacks-legal-merit-icsid-convention` (81,439 bytes,
   `a6f0fbaf445e…`), both `ok/200`, both matched at document offset 0. **What that establishes is that
   ICSID's explanatory material is readable by this channel. It does not establish that a codebook
   exists**, and no seat may cite these rows for the codebook question. `41(5)` nulled on the 2022 page.
6. **THE REGISTER'S BYTE PATTERN BROKE ON ITS FOURTH OBSERVATION** — 94,996 → 94,995 → 94,994 → **94,998**.
   Yesterday's seat wrote that a fourth observation could break the "one byte per day" description; it
   did. **The conservative conclusion is strengthened, not weakened: a digest change on this URL is not
   evidence that the table's contents changed**, now on four dates out of four and in both directions of
   delta. The change-detector proposal on the operator's pending list is weakened again. No mechanism is
   asserted; nobody has seen the changing bytes.
7. **THE WITHIN-RUN DIGEST QUESTION IS CLOSED AS RESEARCH AND OPEN ONLY AS A DISCIPLINE.** Established
   2026-08-05, corrected 2026-08-12, censused 2026-08-15, and re-derived twice since. **Do not census it a
   third time.** The operative facts: 8 of 56 all-`ok` multi-row groups diverge, **5 on
   `investmentpolicy.unctad.org` and 3 on `pca-cpa.org`**; `/978/` shows 14 `ok` rows, 14 distinct digests,
   all at 207,411 bytes. **The zeros on `icsid` and `uncitral` license nothing** — a within-run control can
   only refute stability, never establish it (2026-08-15). **No excerpt in the corpus has ever differed
   between two rows asking the same URL the same question** — **14 keys / 127 rows screened, of which only
   7 keys / 37 rows could discriminate** (`0822-B9`: three keys carry an empty excerpt in every row and
   could never have shown variation), **zero varying excerpts in either set.** So no quoted window and no
   substantive finding anywhere in this project is impeached by any of it. **And the merge-time population
   is TEN rows, nine pca and one icsid, with the constant-length instances 9 of 9 on `pca-cpa.org`**
   (`0822-B8`), not the nine-and-eight the analyst reported or the seven-and-two the postscript did.
8. **`N4` IS AN OPERATOR ITEM AS TO THE AWARD TEXT, AND THE CHECK THAT ESTABLISHES THAT WAS RUN THIS TIME.**
   Every *Hela Schwarz* rendition on an allowed host is a PDF and the reduction decodes none; the only HTML
   rendition of the award text is on `jusmundi.com`, which is **not** on `ALLOWED_HOSTS`. That is the
   mirror-image check `0821-B17` sustained against the analyst yesterday, discharged today in the correct
   direction — he checked before concluding and showed the check. **Question (3) — jurisdiction or
   admissibility — is still unanswered**, and his `0822-R8` **withdraws his own yesterday's wording**
   ("no accessible source addresses it") as too strong: the record does hold, on title-and-snippet carriers
   only, that the Article 9(a) ground was *pleaded* as an objection to jurisdiction. **How the objection was
   pleaded is not how the tribunal characterised the condition.** `GAP-UNRESOLVED:
   hela-schwarz-art9-jurisdiction-or-admissibility` stands.
9. **`ARB/24/46` vs `ARB/24/26` STAYS UNRESOLVED, WITH THE ASYMMETRY STATED AND NOT CONVERTED.** One body
   plus one independent title for `/46`; one TDM title for `/26`, and zero titles returned when `/26` was
   searched as a designator in its own right — **and that zero carries essentially no weight** (`0822-B12`):
   the same engine returned zero titles for `ARB/24/46`, the designator ICSID's own register body-carries,
   twenty-four hours earlier, so a designator-quoted zero is already demonstrated in this matter to be a
   property of the formulation. **The honest asymmetry is one body-carried designator against one
   title-carried designator, with the search contributing nothing on either side.** Also: one notation
   screened only — `ARB 24/26`, `ARB.24.26` and a bare `24/26` were not, in the search OR the repository. Recorded as "not found in accessible sources", never as "does not exist". The
   four register-carried proceedings (`ARB/24/46`, `ARB/24/18`, `ARB/22/30`, `ARB/23/3`) remain
   **UNEXAMINED, not EXCLUDED**; nobody has read one of these decisions and no holding is asserted anywhere
   in this record. `ARB/24/18` and `ARB/22/30` have had **no** subject-matter search at all — the analyst
   discloses this at `0822-E5`(i) rather than let coverage read as silence.
10. **THE A/81/17 WATCH IS A PAIR, NOT A COUNT** — 10 `A/81/17`-scoped rows against **1** page state
    (`f9712994f5ae`, byte-constant at 832,338, ten consecutive dates 2026-08-13 → 2026-08-22). The
    `A/80/17` control matched, so the null is not an access failure. **`G3` is binding: the string's
    absence is a fact about this page state and this runner, not a statement that the 59th session's
    report does not exist or is late.** The lag baseline stays **UNAVAILABLE** (`N10`): the repository
    cannot supply it, the ODS route is closed, and `web.archive.org`'s CDX endpoint is not on
    `ALLOWED_HOSTS`.
11. **RECORD HYGIENE, DONE TODAY AND NOT LEFT FOR TOMORROW.** `STATE_OF_THE_ANSWER.md`'s `Last updated:`
    header read 2026-08-19 while the file's last commit was 2026-08-21 — a hard `FAIL` from
    `scripts/check_currency.py`, regressed since the 2026-08-19 seat fixed the same thing. Repaired. Its
    pointer-correction passage still carried **a line number, stale, in the sentence adopting the rule
    against line numbers** — third generation of the same defect. Removed and replaced by a description
    that cannot make its own target non-unique. **The `[403-blocked]` amendment is NOT applied and stays
    on the operator's pending list.** The audit anchor remains `STALE` by construction (escalation 7 of
    2026-08-17, unfixed): a session that writes both the file and the ledgers cannot satisfy the guard.
12. **ARCHIVIST ITEMS, ROUTED AND NOT DONE** — all under `agents/`, outside this session's merge scope, and
    failing identically on `origin/main`: `agents/Workflow Threads.md` cites three non-commits
    (`373cce6`, `9efafb0`, `ae42639`) and is stale against 59 commits; `agents/Claim Map.md` stale against
    3; `agents/Project Change Log.md` stale against a commit this council did not create. **A thirteenth
    day for the taxonomy collision.**
### Escalations to Emory

**Numbered `N1`–`N13` as of the 2026-08-22 record.** Per method rule (w), **cite these by number *and* by
this date** — the list is re-issued daily and a bare `N`-number silently resolves to the wrong item in
tomorrow's. **Every carried item below was re-verified programmatically this session, not copied forward**,
and where a figure moved I say so.

**N1. THE PATH-WATCHING TRIGGER. FIFTH SESSION, AND TODAY IT DID NOT FIRE — WHICH IS NOT THE SAME AS
FIXED.** The mechanism, as corrected by yesterday's postscript: **any path-watching workflow fired by any
unskipped push, and every PR merge to `main` is such a push.** `daily-update.yml` watches
`analytics/daily-research/**`; `fetch-relay.yml` watches `analytics/fetch-requests/**`; GitHub evaluates
`[skip ci]` on the **head commit of the push**. **Verified this session: zero `daily-update` runs exist on
branch `council/2026-08-22`.** It held for a structural reason rather than luck — the day's one unskipped
push (`6aed9a1`) touched only `analytics/fetch-requests/`, which `daily-update` does not watch, and every
push that touched the record carried `[skip ci]` at its head. **That is still a discipline, not a fix**,
and it does not survive the merge: **merging this PR will fire both workflows**, which is why the reduction
is cited by commit throughout and why today's operator email will, correctly, carry the finished record.
**The one-line fix is yours and only yours: branch-filter the `daily-update` push trigger to `main`.**
`.github/` has been outside the merge scope of all five sessions that have paid for this.

**N2. CARRIED AND OWED A NINTH TIME — pinned model ids.** Re-read at HEAD:
`.claude/agents/{research-analyst,integrity-officer,council-chairman}.md` **all three declare
`model: opus`**, a tier alias that pins nothing. `.claude/agents/integrity-officer.md` states "Claude Opus
4.8" in its prose. **The analyst and I ran on Opus 5 and are conformant with your directive of
2026-08-03.** The officer's self-report is recorded in Part V(a) in its own words and any discrepancy is a
procedural caveat, never a silent substitution.

**N3. CARRIED — the relay's `status` field still has two types, and the counts moved today exactly as the
defect predicts.** `scripts/fetch_relay.py` writes `resp.status_code`, an **int**, on contact, and
`entry.get("detail") or None`, a **string**, on failure. Re-derived at HEAD over the whole corpus:
**186 int, 19 str, 3 None** — the int count up 12 from yesterday's 174, which is exactly today's twelve
records. `status == 403` returns **0** rows; `status == '403'` returns **15**. Raised 2026-08-08, escalated
2026-08-10, then **fell off this list and stayed off for eleven days** before biting a seat mid-session.
Two things for you: the schema defect, and the fact that **items leave this list silently**.

**N4. CARRIED — the *Hela Schwarz* award is still the pivotal unread document, and the route question is
now answered in the negative.** Every rendition on an allowed host is a **PDF** and the reduction decodes
none; the only HTML rendition of the award text is on **`jusmundi.com`, which is not on `ALLOWED_HOSTS`**.
`icsidfiles.worldbank.org/…/C6447/DS11402_En.pdf` remains title-carried and never retrieved, and
`…/C6447/DS21216_En.pdf` is **newly title-carried** under `ARB/17/19`. **This is an operator-side retrieval
of a retrievable document.** Question (3) — did the tribunal characterise the Protocol Article 9 conditions
as going to **jurisdiction** or to **admissibility**? — is unanswered by any accessible source.

**N5. CARRIED AND NARROWED — the "jurisdiction and merits" tension, plus a warning against closing it the
easy way.** The prose asserting "jurisdiction and merits grounds" was replicated a **third** time today,
including the impossible construction *"a final award dated December 10, 2025 (published on January 15,
2025)"*. **Three replications by one class of instrument is not corroboration and it is not banked.** And
the analyst's bound, which matters more than the tension: **even if that prose were banked it would not
answer `N4`'s question (3)**, because it never uses the word *admissibility*. `N4` and `N5` must not be
allowed to collapse into each other.

**N6. CARRIED AND UNFIXED — three defects in `scripts/fetch_relay.py`, all re-verified at HEAD.**
(a) `excerpt_of`'s docstring says the window is *"centred"* on the match; the code computes the **quarter**
point. (b) `newest_request()` sorts **lexicographically** despite its name. (c) `run()` writes to
`out_dir / request_path.name` at **`:228` with no existence check**, which is why a merge overwrites a
reduction in place. Worked around correctly again today by verifying sort position before pushing.
**A workaround executed correctly is still not a fix.**

**N7. CARRIED — the reduction decodes no HTML entities and strips no zero-width characters.** Re-verified:
**zero occurrences of `html.unescape` in the file**, and `_WS_RE = re.compile(r"\s+")` does not match
`U+200B`. It is **not uniform within one document**. This bounds **every null in this record**, including
today's `41(5)` and today's `A/81/17`.

**N8. CARRIED — `meta.json` still carries no collection-run identifier.** Re-read at HEAD; its keys are
`date, screened, matches, held_for_review, validation_status_only, watch_list_leads, accepted, per_source,
accepted_by_source, source_health, health_warnings`. **No run id.** The 2026-08-17 file was rewritten three
times over two hours and twenty-three minutes with `screened` going 13 → 17 → 7. Today's reading is safe
only because it is pinned to a blob and that file settled five days ago. **Next collection: Monday
2026-08-24, and it will have the same defect.**

**N9. OWED A TWELFTH TIME — the operator-side retrieval of the China–Switzerland BIT (2009) forum
provision.** `investmentpolicy.unctad.org/978/`, established at eight refusals over six dates with one
access transition. **No relay row was fired**; a twelfth observation of an established access condition
changes no decision this council faces. *(That page is separately the worst case of the digest-instability
finding — 14 `ok` rows, 14 distinct digests, all at 207,411 bytes.)*

**N10. CARRIED — the A/81/17 lag baseline is UNAVAILABLE and both non-archive routes are closed.** The
repository cannot supply it (the corpus opens 2026-06-23, consistent with any lag in [0, 377] days); the
`undocs.org` / `docs.un.org` ODS route is **closed**, established non-discriminating 2026-08-04 against an
`A/81/99` control and reconfirmed 2026-08-21. **The only remaining route is `web.archive.org`'s CDX
endpoint, which is not on `ALLOWED_HOSTS` and is therefore operator-side.**

**N11. THE PENDING LIST IS UNTOUCHED AND UNBUILT, FOR THE FIFTH CONSECUTIVE SESSION.** The
`source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap,
the Kluwer source, the category signal, the `build_graph` fix, the sha256 change-detector, and the
`[403-blocked]` ledger amendment all remain **pending, not granted**. **Nothing was built, applied, or
prepared for application by any seat today.** Today's evidence bears on one of them and bears **against**
it: the change-detector is weakened by the register's fourth-observation break and by the corpus-wide
digest-instability census.

**N12. CARRIED AND HONOURED — reductions cited by council records are not immutable.** Eleven have been
overwritten in place at merge time. **Every citation in this record to `analytics/fetch-results/` carries
its commit sha**, and today's own reduction is pinned at `682e762` / blob `37aceca9…` in the batch file
itself, before the result existed. **This record predicts its own reduction will be overwritten within a
minute of merge**, and that is a description of established behaviour, not a worry. The fix is the
existence check at `N6`(c).

**N13. NEW — THE PRIOR-ART FAILURE IS NOW STRUCTURAL, IT HAS SURVIVED BEING NAMED AS THE FIX FOR ITSELF,
AND IT IS COSTING REAL RESEARCH CAPACITY.** This is not a request for a code change; it is the thing I most
want you to see. In three days this council has produced **ten** documented re-derivations of knowledge it
had already written down — six by yesterday's count, four more today. **Yesterday's single adopted
improvement was the rule against exactly this, and it was broken by the chairman roughly eighteen hours
after adoption**, twice, in the record that adopted it. The analyst independently broke it three days
running on a *different* corpus — `analytics/optimization-log.md`, which has held since 2026-07-22 a
demonstrated search method **naming this project's pivotal case as its trigger**, unopened through three
days of searching for that case. **The cost is measurable in this session alone:** two relay rows, one full
seat-task, and two of six search calls, spent re-deriving what the record held. **What today shows about
the cure** is in the optimization item above and it is the one encouraging thing here: the two novelty
claims that carried a *required written receipt* were both correct, and the four that did not were all
wrong. **The rule fails; the field works.** Nothing has been built or applied — the proposal is yours to
grant or refuse.

### Where this leaves the research question

**No new ISDS development on the theme surfaced today, and the digest of record is unchanged for the fourth
consecutive record.** That is the honest headline and it is a null.

What moved is a **route**, and it is the first thing in a week that opened a channel rather than described
one. ICSID's two explanatory pages on Arbitration Rule 41(5) — the 2022 procedures page and the *In Focus*
publication — **rendered to this council for the first time**, on an allowed host, both `ok/200`, both
matched. Yesterday's close-out called these "the highest-value rows available" and "the only thing in this
record that could close a gap rather than describe one." They are fired, they work, and **they did not close
the gap.** ICSID's explanatory material is readable by this channel; **no codebook was found**, and a match
on those pages was pre-registered as establishing the first and not the second.

So the standing question — **has ICSID's early-dismissal procedure ever been applied to a claim where
intellectual property, trade secrets or clinical-trial data is the asserted investment?** — is **not
answered.** The register that would make it answerable states a finding per row; **the values of that
column are still undecoded and `_` is still undefined**, and today the refusals went from two to three: a
search engine offered a decode of `_` itself — *"a blank in a table indicates that the item is not
applicable"* — and the analyst not only refused it but **refuted** it from the council's own body-carried
evidence, since a title-carried `Dismissing` sits on a `_` row. **The column's header has been in the corpus
since 2026-08-20 and it does not decode the values.** Four proceedings are named from that register and
every one sits at *unexamined*; **nobody has read a single one of those decisions.**

On the Ring-3 side the picture got **worse in a way that matters more than any of today's instrument work.**
The *Hela Schwarz* award remains the pivotal unread document, and today's route check answers the question
that has been open since `0821-B17`: **there is no HTML rendition of that award on any allowed host** —
every rendition is a PDF this channel cannot decode, and the only HTML rendition anywhere sits on a host
not on `ALLOWED_HOSTS`. It is an operator-side retrieval and it will stay one. Question (3) — whether the
tribunal treated the Protocol Article 9 conditions as going to **jurisdiction** or to **admissibility** —
is unanswered.

**And this council was carrying an answer to it that it had invented.** For thirty-one days
`STATE_OF_THE_ANSWER.md` said, labelled *confirmed*, that Article 9(a) was *"applied and sustained"* — on a
title that says only that the tribunal *"gives effect to"* those clauses. **That is the third ring's central
question, answered in the canonical record by a verb nobody wrote.** It is repaired today. The gap it was
papering over is now visible, and a visible gap is the better state.

**The instrument is in good order and the method is not.** Fetch is dead and the relay is alive and did
real work; the digest is a poor change signal on three URLs and an unmeasured one everywhere else; no
excerpt in this corpus has ever moved, so nothing about any case, award or proceeding is impeached by any
of it. What is not in good order is that ten documented re-derivations in three days is a research council
spending its capacity re-finding its own notes, and that today's single largest acquisition — two open
routes — came from the one part of the session that was forced to write down its prior-art check first.

---

*Record closed 2026-08-22 by the chairman seat. **Two real subagents convened**, the officer resumed for a
second turn; all three member returns reproduced verbatim, with one transport note disclosed. **Fifteen
binding objections, all sustained, none overruled** — seven against the chairman, seven against the analyst,
one against both — every one applied in place with its pre-correction wording quoted. **No fabrication was
found anywhere in this record.** One holding that had stood in the canonical record for thirty-one days on
a carrier that does not contain it was found and repaired. The optimization item is proposed and not built;
**every item on the operator's pending list is untouched, for the fifth consecutive session.** One departure
recorded against the chairman: the analyst was not resumed to answer the objections against him.*
