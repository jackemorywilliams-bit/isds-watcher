---
aliases: [Vault Session 2026-08-28]
tags: [vault-session, archivist]
hub: Evidence Ledger
---
# Vault session — 2026-08-28 (twelfth archivist deployment)

**Audited against `b7d973d` (`main`, clean tree), on a complete 846-commit history.** The
container's clone arrived shallow for the fourth consecutive session — 186 commits — and every
ancestry, orphan and guard result below was re-derived after `git fetch --unshallow`. Cadence
held: three days since 2026-08-25, as scheduled.

**The headline is good news, and it is the first time this seat has been able to write that.**
The currency guard is green on `main` for the first time in ten days. It was not this seat that
fixed it, and the fix is narrower than it looks — but the seventeen-run red streak that three
sessions escalated is over.

The rest of this record is mostly small corrections to my own surfaces, plus one finding I did
not expect: a rule the project treats as binding on two seats cites a file that has never
existed in this repository.

---

## The good news, stated precisely

`pipeline-guards` is **green on `main`**. The red streak ran **seventeen consecutive
push-triggered runs** — run #34 (`b34f01e`, 2026-08-18T12:10Z) through run #59 (`05cde8f`,
2026-08-27T12:23Z). It ended at run #61 (`6950040`, 2026-08-27T19:20Z, PR #108) and held at run
#62 (`26fafd2`, 2026-08-28T12:13Z). Locally, `check_currency.py` now reports **9 claims, 1
failed** — it was 4 failed on 2026-08-25.

What fixed it: `scripts/reanchor.py` (`a8bccbb`), its CI wiring (`93a69f7`,
`.github/workflows/reanchor.yml`), and the council close-out step added to
`prompts/daily_council_protocol.md`. Escalation 7 — the anchor moved by hand and got wrong every
generation — was retired at `2e87700`. That is a real repair and it deserves saying plainly.

**One qualification, because it will matter later.** This did not fix what C17 actually
diagnosed. `_is_maintenance` was widened only to the `.sent/` markers. I called it directly at
`b7d973d`: `check_currency._is_maintenance("cb762ed")` — the commit that landed my own last
session — still returns **False**, because that commit touches `HANDOFF.md`,
`agents/obsidian-archivist.md` and `analytics/vault-sessions/2026-08-25.md`, none of them tracked.
Every archivist session still makes two notes stale against its own commit. The anchors are now
simply moved forward again by machinery before anyone sees red. The guard is green because the
symptom is swept up automatically, not because the false positive is gone — so a session that
skips the close-out, or a PR opened between a session commit and the next re-anchor, reproduces
it. C17 refined, not closed.

---

## NEEDS YOU — 1. A rule binding on two seats cites a file that has never existed

`agents/Agent Registry.md` records, as adopted and **binding** on the integrity officer and the
research editor, the rule that humanizer output is fail-closed — a rewriting pass run with every
quotation, citation and pinpoint sentinel-masked, rejected outright on fabricated quotation,
flipped negation, hallucinated rule or sentinel destruction.

Its only citation is `working/benavides-comment-replies-2026-08-08.md:5-6`.

**That file does not exist and never has.** Not in the working tree; `git log --all` returns no
commit touching the path; `git rev-list --all --objects` returns no object with that name; and
`origin/fix/restore-council-label` — the branch every neighbouring citation of that vintage
names, still present on the remote — does not carry it. Every one of the twelve other paths cited
"(uncommitted)" alongside it landed at `667772c` on 2026-08-11. This one did not land with them
and is reachable from nothing.

**I have not deleted the rule**, and I want to be clear about why. The rule is corroborated
independently — the fail-closed behaviour is live in the pipeline — so this is a sourcing defect,
not a false rule. But this note's standing discipline is that an entry that cannot be sourced is
not written, and this one has been unsourceable the whole time it has been read as binding. It is
the same shape as **F2**, the lost 2026-08-03 standing-rules record: a rule outliving its
evidence. Marked at the registry row and opened as [[Workflow Threads]] **D10** so the next
session inherits the question rather than a silent gap.

**What I need from you:** either the artifact exists somewhere outside the repository (a session
transcript), or the rule should be re-adopted on a dated line that cites itself. I can do the
second in a minute once you say so; I cannot manufacture the first.

---

## NEEDS YOU — 2. The site still tells your professor there are nine sources. Day eleven.

This is the fourth consecutive session I have escalated this, and it is the only item on my list
that a reader outside this project can see.

| Cell | Reads | State |
|---|---|---|
| `scripts/site_templates/how_it_works.html.j2:3` | "the **nine** public sources" | nine |
| `scripts/site_templates/how_it_works.html.j2:27` | "the first band is the **nine** public sources checked" | nine |
| `docs/how-it-works.html:7` (meta description) | "the **nine** public sources" | nine |
| `docs/how-it-works.html:58` | "the first band is the **nine** public sources checked" | nine |
| `docs/how-it-works.html:96` (the chart on that same page) | "THE **10** SOURCES", ten chips | ten |
| `README.md:56` | "the **ten** sources" | ten |
| `src/sources.all_sources()` | returns **10** | ten |

`git log ad66a96..HEAD -- scripts/site_templates/` returns **no commit**; the template has not
been touched since 2026-08-07, ten days before GDELT joined the fleet at `44550ca` (2026-08-17).
On one page, the prose says nine and the chart directly below it says ten.

It is a two-sentence edit in one `.j2` file. It is **site-experience's** file, not mine, which is
why I have escalated it four times instead of fixing it — but if you would rather I just make the
edit, say so and I will.

---

## FOR EMORY — 3. Two contract surfaces still bind seats to "zero-cost", twenty days on

`grep -rni "zero-cost\|zero cost" .claude/agents/ README.md` returns exactly three hits at
`b7d973d`, the same three as 2026-08-22:

- `.claude/agents/site-experience.md:20` — "Zero-cost, no new services."
- `.claude/agents/systems-designer.md:17` — "the zero-cost constraint"
- `README.md:6` — the line recording that the claim was false.

The public correction landed 2026-08-08 and has never reached the contracts. Two seats are told a
cost constraint the project has already established it does not meet, in the file each reads
first. `.claude/agents/` is a contract surface, so this is yours. **D8**, day six.

---

## FOR EMORY — 4. `METHODOLOGY.md:33`, unchanged since I opened D7

`git log ad66a96..HEAD -- METHODOLOGY.md` returns no commit. The paragraph still enumerates
**nine** collection channels and still omits **GDELT** (`src/sources/gdelt.py`, added at
`44550ca`). The same sentence still says Bing News is polled through "**eight** fixed,
fingerprint-derived queries"; `len(QUERIES)` in `src/sources/bing_news.py` is **12**.

Both numbers read from the code today, not from a commit message. It is a two-number edit. It is
your document and your prose, which is why I have not touched it. **D7**, day six.

---

## TO VERIFY — 5. The commit that fixed the currency guard also created a fresh instance of D5

D5 is the thread that says adopted rules get written where the seat bound by them never reads.
`93a69f7` added a close-out rule addressed to whoever merges the daily meeting branch — *"Before
the meeting branch merges to `main`, run the currency re-anchor as the LAST commit"* — into
`prompts/daily_council_protocol.md`.

**The council chairman's definition does not enumerate that file.**
`.claude/agents/council-chairman.md` lists its canonical training explicitly and exhaustively:
`prompts/council_chairman.txt`, `prompts/council_calibration.md`, `prompts/council_reconvene.txt`,
and the live state it stewards. `grep -rn "daily_council_protocol"
.claude/agents/council-chairman.md agents/council-chairman.md` returns nothing.

Exactly one definition of nine names that file — `.claude/agents/analytics-officer.md:15`, which
claims a *different* rule in it (digest terminology) as that seat's to enforce. So a merge-time
procedural rule now lives in a file read by the analytics officer and not by the chairman who
runs the meeting and performs the merge.

**Testable at the next daily meeting:** if the close-out happens, it will be because the session
was told to somewhere else, not because the seat read it here. The remedy is one line added to
`.claude/agents/council-chairman.md`. That is a contract edit, so it is yours.

Day fifteen on D5 overall: still **eight of nine** definitions never naming their own vault note.

---

## What I fixed, each citing a hash

**Thirteen "(uncommitted)" citations audited across my own surfaces; twelve were stale by
seventeen days.** Every path cited "(uncommitted, `fix/restore-council-label`)" in
[[Agent Registry]] and [[Workflow Threads]] landed on `main` at `667772c` (2026-08-11) — the
locked-set files, `instrument-map-2026-08-08.md`, `retrospective-audit-2026-08-08.md`,
`state-space-resolution-2026-08-09.md`, `check_lock.py`, `src/config.py`, the v2 and triage
prompts — with `.github/workflows/pipeline-guards.yml` last touched at `e8931ac` (2026-08-17).
Corrected in place with the landing hash at `agents/Agent Registry.md:430,431,433` and
`agents/Workflow Threads.md` C13, C14, B6 and B7. The dated observations that were true when
written are preserved as history; only the live claims changed.

**The thirteenth is the D10 file above** — which is exactly why I resolved these one at a time. A
blanket find-and-replace over "(uncommitted)" would have asserted that a file which has never
existed had landed at `667772c`, converting a stale-but-honest annotation into a false one.

**Three thread IDs are ambiguous, and one citation had already gone astray.** `B5`, `C11` and
`C12` each head two unrelated threads in [[Workflow Threads]]; [[Claim Map]] runs a third `C11`.
`agents/Project Change Log.md:55` carries the bare pointer "**C11** the site's nine-vs-ten", which
resolves to *neither* Workflow Threads thread — the real target is [[Claim Map]] C11, as `:72`
says correctly. A disambiguation table now heads the threads note, with the rule: cite an ID
together with the note that holds it. **Deliberately not renumbered** — those IDs are cited across
the change log, the daily records and the seat notes, and renumbering would silently break every
inbound citation. Also recorded: `C15` and `C16` have never existed, so a search returning nothing
for them is correct.

**C13's substance re-verified while I was in it.** `analytics/locked_set/RETRIEVAL_LEDGER.md` reads
thirteen data rows: 2 RETRIEVED (both *Vanda* Fed. Cl. slip opinions), 3 BLOCKED, 8 QUEUED. The
counts in the thread were right; only the "(uncommitted)" was wrong.

**D9 sharpened by measurement rather than repeated.** I ran the check the guard's docstring
promises but its code does not: resolving every backticked 7–40 hex token across the five tracked
notes and `HANDOFF.md` returns **35 unresolvable**. Reading every one in context, **all 35 are
correctly written** — each is framed as a blob ("blob `f3dbbf6`"), a ledger-claim id, an Actions
run id, or an orphan the note itself declares an orphan ("`3d31de8` … was never pushed";
"`6f9e1da` … is not an ancestor of `origin/main`"). **Zero unannotated orphan citations remain**;
`80ad250`, the one exception, was annotated on 2026-08-25.

That changes the recommendation. A naive bare-sha check would fire 35 false positives on a clean
tree and be switched off within a week. The docstring's own wording — a sha "cited **as closing or
landing** something" — is the correct specification, and the vault's existing lexical conventions
are what distinguish citation modality. **Implement it against modality, not against every hex
token.** Recorded in D9 for the systems designer.

**The prescribed close-out pushes this seat out of its own merge scope, found by running it.**
`scripts/reanchor.py` moves the anchor on all five tracked notes, one of which is
`STATE_OF_THE_ANSWER.md` — not in my authorized record set (`analytics/`, `agents/`, `moc/`,
`HANDOFF.md`). Run as written it would have made this PR ineligible for self-merge. I restored the
answer file and committed only the four `agents/` anchors; `check_currency.py` then reported
**9 claims, 0 failed**, so that file's anchor never needed moving — it was already current. A
`--only` flag on `reanchor.py` removes the conflict; until then this seat runs the script and
reverts one file by hand, which is a manual step inside the script that exists to end manual
anchor moves. Recorded in C17 for the systems designer.

**Worse: the CI half of that machinery pushes the out-of-scope file back, unasked.**
`.github/workflows/reanchor.yml` runs on every PR to `main` and commits the result to the PR
branch. On this session's own PR #110 it pushed `5f74cde` (`github-actions[bot]`,
"chore(currency): re-anchor tracked notes to HEAD"), whose five changed files include
`STATE_OF_THE_ANSWER.md`. **An archivist PR that is inside the record-path merge scope when opened
is moved outside it by machinery, silently.** A session that checks scope once — before pushing —
would then either self-merge a file it is not authorized to merge, or find its own PR blocked for
a reason it did not create. I have adopted the rule of re-checking scope *after* the workflow
fires, and recorded it in C17. The durable fix is the same `--only` flag, applied in the workflow
as well as in the script. **This is yours or the systems designer's to decide**, because it is two
pieces of machinery disagreeing about what this seat is allowed to merge.

**And the close-out has a second sharp edge, which I hit on my first attempt at this PR.** The
re-anchor commit earns its maintenance exemption **only if it touches nothing but the five tracked
notes**. My first attempt bundled one edit to this session record into the same commit; that one
non-tracked file made `_is_maintenance` return `False`, and CI came back `9 currency claims
checked, 2 failed` — [[Project Change Log]] and [[Workflow Threads]], each stale against the
re-anchor commit itself. **That is C17 firing exactly as this record predicted two sections
above**, and it was my error, not the guard's: the protocol at `prompts/daily_council_protocol.md`
does say the close-out "touches nothing but the tracked notes". I rebuilt the branch as two
commits — everything substantive first, then a re-anchor touching only the four `agents/` notes —
and the check went green. Worth stating because the failure mode is silent locally: running
`check_currency.py` *before* making the close-out commit reports 0 failed, and the commit you make
next is what breaks it.

**One more one-line exemption is now evidenced.** Today's single currency failure is
[[Workflow Threads]], stale by exactly one commit: `b7d973d`, whose only changed file is
`analytics/fetch-results/2026-08-28-daily.json` — a machine-written retrieval artifact, the same
class as the `.sent/` markers `a8bccbb` already exempted. Adding `analytics/fetch-results/` to
`_MARKER_PREFIXES` clears it.

---

## Orphan check — 7 of 100 branches, one new, and nothing substantive is stranded

`git merge-base --is-ancestor <tip> origin/main` over all 100 remote branches returns **seven**
orphans, up from six of 95 on 2026-08-25. The new one is **`origin/council/2026-08-26`**.

I checked it at content level rather than by sha, because squash-merges make branch tips
non-ancestors routinely. Its only unlanded commit is `a0d2600`, and the only file that commit
touches is `analytics/daily-research/.sent/2026-08-26` — a send marker whose equivalent **is
present on `main`**. Identical in shape to `origin/council/2026-08-13`, the known orphan from the
same class. The 2026-08-26 council record itself landed via `5b7f7a2` (PR #105) and `58cb26c`
(PR #106).

**No research content is stranded on any branch.** This is the failure this session exists to
catch — the 2026-07-31 audit that sat unmerged for three days — so I state the negative result as
a measurement rather than as reassurance: I tested every branch, and the two council orphans
carry markers only.

The other five are the long-known set: `origin/chore/operator-marks-2026-07-27` (**F1**, day
thirty-two, `6f9e1da`), three `claude/sweet-mccarthy-*` cloud-run branches, and
`origin/feat/research-council` from 2026-06-22.

---

## Guards, run today at `b7d973d`

`check_models.py` exit 0 over 12 flowchart cards. `check_lock`, `check_headline_lane`,
`check_claims`, `check_seen_integrity`, `check_telemetry_privacy` all exit 0.
`node tools/isds-workflow-3d/validate.mjs` exit 0 — 30 cards, 10 chips, 44 edges, SVG fresh
against its inputs hash, zero "Jack" tokens. `build_graph.py --dry-run`: 151 notes, 326 edges, 0
orphans, **42** managed blocks would update, and 3 broken wikilinks — all three under
`think-tank/`, none in the record surfaces.

`src/models.py` is unchanged: `CHAIRMAN_MODEL` and `HEAVY_MODEL` at `claude-opus-5`,
`UTILITY_MODEL` and `FALLBACK_MODEL` at `claude-opus-4-8`, `DIGEST_CLASSIFIER_MODEL` at
`claude-haiku-4-5-20251001`. No model drift exists.

---

## What I could not verify, stated plainly

- **Whether the fail-closed-humanizer rule's wording is still what the two seats operate under.**
  I verified only that its citation is unresolvable. The rule's *content* I could not check
  against any surviving primary record, because there is none. That is the whole of D10.
- **Whether the council close-out in `93a69f7` is actually being performed.** The guard being
  green is consistent with it working and equally consistent with `reanchor.yml` catching
  everything on the PR path. I could not separate the two from the commit record alone; the next
  daily meeting will show it.
- **Whether `analytics/fetch-results/` should be exempted or whether some note legitimately
  reasons about it.** I checked that today's failure is caused by nothing else. I did not audit
  every note's declared paths for a dependency on that directory, so the one-line fix is proposed,
  not asserted as safe.
- **Anything about the three `claude/sweet-mccarthy-*` orphans beyond their tips.** They are
  daily-council commits from 2026-07-30, 07-31 and 08-01. Previous sessions recorded them as
  superseded; I confirmed they are still non-ancestors but did not re-diff their contents against
  `main` this session.
- **No external Obsidian/PKM research was done this session.** The self-training mandate allows it
  periodically; I spent the budget on the agent-context audit and the citation sweep instead, and
  I am recording that rather than dressing an internal finding as outside practice.

---

## NEEDS YOU — 6. This PR is NOT self-merged, and the reason is the machinery above

**PR #110 is left open deliberately.** All four checks are green on my head — `currency`,
`guards`, `models`, `reanchor` — and every path I authored is inside my record-path merge scope
(`agents/`, `analytics/`). But `.github/workflows/reanchor.yml` then pushed **`d8667fe`**
(`github-actions[bot]`, "chore(currency): re-anchor tracked notes to HEAD") onto my branch, and
that commit's one changed file is **`STATE_OF_THE_ANSWER.md`** — outside the set I am authorized
to self-merge.

The commit is trivial in content: a single anchor sha moved from `a4e9997` to `ccd47e9`, nothing
else. I am not treating that as a reason to merge anyway. My authority is stated as a path test,
not a judgement call, and the whole value of a path test is that the seat bound by it does not get
to decide which violations are small. So: **branch pushed, PR open, not merged, and you have the
last word.**

I could not resolve it myself. Reverting that file and pushing only makes the workflow fire again
and re-add it — the loop is the defect, not an accident of timing.

**What I need from you — any one of these ends it:**
1. Merge PR #110 yourself (the content is green and in scope apart from the bot's anchor line); or
2. Tell me `STATE_OF_THE_ANSWER.md` anchor-only changes count as inside my scope, and I will merge
   it next session; or
3. Have the systems designer add a scope flag to `reanchor.py` **and** to `reanchor.yml`, which
   fixes it permanently for every future archivist session.

Until one of those happens, **every archivist PR from now on will hit this**, because the workflow
runs on every PR to `main`. That is the part worth your attention — not this one PR.

## Escalation summary

| # | Item | Owner | Age |
|---|---|---|---|
| 1 | **D10** — binding rule cites a file that has never existed | **Emory** + integrity officer | new today |
| 2 | **Claim Map C11** — public site says nine sources, instrument runs ten | **site-experience** | 11 days |
| 3 | **D8** — "zero-cost" on two contract surfaces | **Emory** | 6 days |
| 4 | **D7** — `METHODOLOGY.md:33` nine channels, "eight" queries vs 12 | **Emory** | 6 days |
| 5 | **D5** — 8 of 9 definitions never name their own note; new instance from `93a69f7` | **Emory** | 15 days |
| 6 | **D9** — `check_currency.py` check 3 unimplemented; now specified exactly | **systems-designer** | 3 days |
| 7 | **C17** — `_is_maintenance` gap; symptom masked, cause live | **systems-designer** | 12 days |
| 8 | **F1** — operator-marks branch never merged (`6f9e1da`) | **Emory** | 32 days |
| 9 | **PR #110 left open** — `reanchor.yml` pushes `STATE_OF_THE_ANSWER.md` onto every archivist PR, outside this seat's self-merge scope | **Emory** / systems-designer | new today |

<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->
