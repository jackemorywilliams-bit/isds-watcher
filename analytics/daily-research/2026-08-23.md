# ISDS Thematic Watcher — daily council, 2026-08-23

*Convened by the chairman seat under the operator's standing scheduled convening. Running on Claude
Opus 5 per the model directive of 2026-08-03. Written incrementally; each Part committed as it closed.*

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project asks one question: **at the intersection of intellectual property asserted as a covered
investment, a regulatory or judicial measure as the challenged conduct, and a live jurisdictional or
admissibility doctrine, what is investor–State dispute settlement actually doing?** The daily council is
a check-in against that question, not a research paper. Most days it returns a null, and a null recorded
plainly is the correct output of a narrow watch.

Two working threads dominate the present state. **Ring 3:** the *Hela Schwarz GmbH v. China* award is the
pivotal unread document, and the question that matters — whether the tribunal treated the China–Germany
BIT Protocol Article 9 conditions as going to **jurisdiction** or to **admissibility** — has been answered
by no source this council can reach. **Ring 1/2:** ICSID's register of decisions on *Manifest Lack of Legal
Merit* (Arbitration Rule 41(5)) states a finding per row; the values of that finding column are undecoded,
and whether that early-dismissal procedure has ever met an IP-as-investment claim is unanswered.

**Today is not a null day.** For the first time, ICSID's own case-detail page for `ARB/17/19` was read by
this council's retrieval channel, and three other never-fetched routes returned with it. What that does and
does not buy is Part II's and Part III's work, not Part I's, and I have deliberately written nothing here
that anticipates it.

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer, an analytics officer and a systems researcher. `prompts/daily_council_protocol.md` Rule 0 says the
daily meeting is **the researcher, the chairman, and one optimization item**, that the integrity challenge
is heard inside the Part V dialogue and files no separate return, and that the analytics officer is not
convened at all — the Rule 1 digest sentence being two lines the chairman copies from `meta.json`. That
file's Precedence clause says it wins over any conflicting routine wording and that **the conflict itself is
worth a line in the record's procedural caveat.** This is that line.

**Accordingly, and consistent with every session since 2026-08-07:** two seats are convened as real
subagents — the **research analyst** (Part II) and the **integrity officer**, whose vetting note is heard
*inside* Part V rather than filed as a Part of its own. The **analytics officer** and the **systems
researcher** are **not convened**, per Rule 0. The digest sentence at Part IV is mine, copied from
`meta.json`. The single optimization item at Part VI is mine and is one paragraph.

**The real-agents rule is the operator's cardinal rule and it is honoured absolutely.** No seat's words in
this record are mine. Every member contribution below is the verbatim return of a real subagent spawned
with the Agent tool against its committed definition in `.claude/agents/`. Where a seat cannot be spawned,
the record says PROCEDURAL FAILURE and I do not perform the seat.

### The member return-path protocol

Each convened seat is spawned with the Agent tool against its committed definition, receives a written
brief with bounded budgets and the binding rules of its seat, and returns markdown that is dropped into
this record **verbatim and unedited.** I direct; I do not write. Where I disagree with a returned figure,
the member's text stays as filed and my correction is recorded **beside** it, in my own voice, in Part VI —
that is the disposition `0822-B14` settled and it is applied again today. The integrity officer's objections
are **binding on me**, not advisory.

### The egress probe, run at session open rather than assumed

The standing convening requires this to be re-probed and not inherited from the prior record. It was.

> **`0823-B7`, SUSTAINED, APPLIED IN PLACE.** This passage carried a **three-row probe table** (columns
> `Probe` / `Target` / `Result`, rows: curl→`example.com`, curl→`en.wikipedia.org/wiki/Main_Page`, fetch
> service→`example.com`). The officer read the 2026-08-07 original before ruling and the receipt decides it:
> a tabulated session-open probe appears in **exactly three** daily records — `2026-08-06.md:39`,
> `2026-08-07.md:39` (**the record that caused the ban**), and today. **Fifteen consecutive daily records
> between the ban and today did without one.** Renaming the column from `Instrument` to `Probe` does not
> change what the object is, and it bought nothing — the prose below already carried the whole finding. His
> sharpest point is that my own Part I applies exactly this reasoning to the spend section three paragraphs
> later and I did not apply it here. **The table is dropped; the finding is unchanged and is stated in one
> sentence.**

**`curl` to `https://example.com` and to `https://en.wikipedia.org/wiki/Main_Page` both returned
`CONNECT tunnel failed, response 403` at the proxy gateway, and the fetch service returned `EGRESS_BLOCKED`
on the same neutral control.** The officer re-ran the first two independently from inside this container and
added a third — `https://icsid.worldbank.org`, the same error — and records at `0823-O8` that **two neutral
hosts and one research host returning the identical proxy-gateway error is what makes this a fact about the
client.** He did not re-run the fetch-service probe and vouches for it only as consistent with the class.

**No state change from 2026-08-02 onward.** This session has no network of its own. **All three probes are
facts about the CLIENT and none is a fact about any resource** — the standing tool-status-as-source-state
rule, which is the rule this council breaks most easily and guards hardest. `example.com` is not down; this
session cannot reach it. The relay runner reached the same host, `ok/200`, minutes later.

**WebSearch is alive** and is the analyst's only self-directed instrument today. **The relay is alive**, and
it is not a workaround — it is a committed instrument of the repository (`.github/workflows/fetch-relay.yml`,
`scripts/fetch_relay.py`), and it is the reason no fetch-dependent item is stranded today. **The standing
instruction to declare fetch-dependent work stranded does not apply while the relay runs**, and it did.

### The batch was fired before any seat was convened, and commit order proves it

`analytics/fetch-requests/2026-08-23-daily.json` was written, committed and pushed at commit **`2b89919`**,
**before** the research analyst was spawned. That ordering is not a claim in this record — it is a property
of the branch's history, independently checkable by anyone reading it, and it is why every licence and every
pre-registered branch in that file is a pre-registration rather than a rationalisation. Sixteen gates
`G1`–`G16` travel with it.

The batch executes **next step 3** of the 2026-08-22 close-out — the analyst's six rows at `0822-R17`,
**adopted whole with their licences verbatim**, together with all four binding constraints that travel with
them. **`0822-B13` landed BEFORE the file was written, not after**, as that close-out required: `G9` is the
pre-registered `final_url` gate, and it is the constraint the council owed him. It also executes **next step
2** (the `Trans-Global` confirmation row and its mandatory same-run digest pair) and the standing `A/81/17`
watch (**next step 10**).

The prior-art check required by method rule (u), as extended to four corpora by `0822-R18` and yesterday's
adopted optimization item, was run **before** the file was written and is recorded inside it with its terms
and counts shown, in the inline-receipt form that item proposed. Seven routes and literals were screened;
all seven returned zero rows. I make no novelty claim beyond what those counts carry.

The relay run is **`32635974946`**, and its reduction is committed at **`d341cae`**. Per the batch's own
`overwrite_and_selection_check`, and because escalations **N6-as-upgraded** and **N12** are live and unfixed,
**this record cites that reduction by commit SHA and never by path**: merging this branch to `main` is an
unskipped push that re-fires the workflow, re-selects the same request file by the same lexicographic sort,
and overwrites the reduction in place.

> **`0823-B1`, SUSTAINED, APPLIED IN PLACE, AND THE OFFICER'S RULING ADOPTED WHOLE.** This sentence ended
> *"That has now happened on **twelve council days**."* **I did not derive that figure.** I took yesterday's
> *"eleven council days"* (`2026-08-22.md:95`, verified verbatim by him) and added one, checking neither.
> My own first correction — **seven files** — was also wrong, because it was computed on a shallow clone
> (Part III). On the full 758-commit history the officer independently derives **21 of 32 reduction files
> touched by more than one commit; 26 commits in excess of one per file; 18 distinct dates.**
>
> **His ruling is that the claim is not repairable as a count at all, and I adopt it rather than pick the
> framing that flatters my error.** Three mechanical reasons: the relay's `git diff --staged --quiet &&
> exit 0` means a byte-identical re-fire commits nothing, so 21 is a floor with no ceiling; "files touched
> twice" is **not** "merge-time overwrites" — the 2026-08-03 and 08-04 multiples (3×, 5×) are same-day
> re-fires — and the honest unit is not derivable from the artefact; and **the merge that publishes this
> record is itself an instance, so any number the record carries is falsified at the moment it carries it.**
>
> **The disposition, which replaces the number:** *this has happened to the majority of the reductions this
> project holds, it is happening to today's on the merge that publishes this record, and the count is not
> derivable from the artefact.*
>
> **And the figure is not only here.** It is in the **pre-registration blob `7a1b0e62` at `2b89919`**, field
> `overwrite_and_selection_check`, which also carries *"twelve council days"* — **so that file breaches its
> own `G16`** (*"any git-history count must carry its commit and exclusions"*) **in the same file that
> declares it.** Per his binding direction the pre-registration is **NOT edited**: amending it would destroy
> the pre-registration property the whole day rests on. It stays wrong, on the record, corrected here.

### The spend checkpoint is placed after the vetting round

Per the standing shape, the day's spend is accounted **after** the integrity officer has vetted the
analyst's return, not before — so that the accounting can say what the spend actually bought rather than
what it was expected to buy. It is at Part VI, stated as prose. It is **not** a "Budgets" section and it is
not a table of allocations; a tabulated version would be a Rule 0 departure and the officer's near-miss 5
of 2026-08-22 warned about exactly that.

### The agenda, seeded from yesterday's Part VI

1. **The prior-art check is item 1 and it is procedure, not research** (method rule (u), four corpora).
   **Done before the batch was written**, with terms and counts shown.
2. **The `Trans-Global` confirmation** (next step 2). Fired. A null is a **withdrawal**, not a weakening.
3. **The analyst's six rows at `0822-R17`, adopted whole** (next step 3), with the `final_url` gate landed
   ahead of the file.
4. **The finding-column codebook** (next step 4), where four synonym families remain untried and three
   engine decodes have been refused by three seats.
5. **`ARB/24/18` and `ARB/22/30` have had no subject-matter search at all** — the analyst disclosed this
   against his own coverage at `0822-E5`(i) and it is the largest uncovered gap on the register.
6. **`N4` question (3)**, on a strictly bounded budget and only if the analyst can say in advance what a
   call could buy.
7. **Record hygiene, done today and not deferred**, and the reconciliations that cost nothing.

**What is NOT on the agenda, and why.** The within-run digest question is **closed as research** by next
step 7 and a third census is forbidden. **The operator's pending list is pending, not granted**: the
`source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the `IAReporter` feed swap,
the Kluwer source, the category signal, the `build_graph` fix and every ledger amendment are **not built and
not applied** by this session, and the `[403-blocked]` amendment stays on that list. Nothing outside
`analytics/`, `state/research_log.json` and `STATE_OF_THE_ANSWER.md` is touched.

---

## Part II — The research analyst's return, verbatim

*Spawned with the Agent tool against `.claude/agents/research-analyst.md` (committed definition; `model: opus`).
A real subagent, not a voice. Budget: eight WebSearch calls, allocated by task, with the standing
instruction that returning calls unspent is a legitimate outcome. **Filed unedited. Where I disagree with a
figure below, his text stays as filed and my correction is recorded in Part VI, in my own voice.***

**One transport note, recorded because today's optimization item is about exactly this class of thing.**
The return arrived through the agent transport with HTML entities escaped at the transport layer —
blockquote markers as `&gt;`, ampersands as `&amp;`, angle brackets as `&lt;`. I have **un-escaped those
transport artefacts and nothing else**, so that the markdown renders as he wrote it and so that the
strings he is quoting from the reduction — `&nbsp;`, `<title>`, `12 > 12` — appear as the characters they
are rather than as doubly-escaped noise. **No word, figure, emphasis or ordering is changed.** I say it
here rather than leave a reader to wonder why a verbatim section needed touching at all, and I note the
irony without excusing it: a display layer altered the evidence between the seat and the record on the
same day the council is adopting a rule about display layers.

---

# RESEARCH ANALYST — RETURN FOR THE COUNCIL MEETING OF 2026-08-23

## §A — WHAT THIS RETURN ESTABLISHES AND WHAT IT DOES NOT

**This is not a null day, and the thing that moved is a body, not a route.** One pre-registered relay row settled a candidate this council has carried since 2026-08-19 on a cross-page-state join: `Trans-Global` matched on the register and the returned window carries the case name, the Rule 41(5) designation, the date **and** the finding-column value contiguously, in one window, on one page state. **`G13`'s first branch fires.** A second row put ICSID's own docket behind the *Hela Schwarz* award date for the first time. A third pair closed the `ARB/24/46` / `ARB/24/26` discrepancy **from a body**, in the direction the register already pointed. Two subject-matter searches gave the first accessible characterisation of `ARB/24/18` and `ARB/22/30` — and neither reaches Ring 1.

**What it does not establish.** The finding column is still undecoded and `_` is still undefined; four synonym families are now tried and the answer is still no. **Question (3) of `N4` — jurisdiction or admissibility — is unanswered, and today's most tempting prose was the fourth engine rendition to describe the Article 9 conditions without ever taking a position on the distinction.** Nobody on this council has read any of the four register-carried decisions, and no holding is asserted anywhere below.

**Instrument state, stated rather than assumed.** My **fetch-first rule is inoperative today**; egress is dead and I have substituted nothing silently. **Zero fetches attempted, zero simulated.** I fired no relay row. Every retrieval below is the chairman's pre-registered batch, reduction at commit **`d341cae`** (cited by sha, never by path, per `N12`), request file at `2b89919`, my derivations at HEAD **`3cd149c`** over **32 files / 221 records** in `analytics/fetch-results/`.

---

## §B — SEARCH SPEND

**Eight authorised. Six allocated (Task 1 ×3, Task 2 ×2, Task 3 ×1), two unallocated. Five spent, three returned** — one from Task 1's allocation and both unallocated calls. Task 4 received zero, as directed.

| # | Query, verbatim | For | Paid? — and *on what* |
|---|---|---|---|
| S1 | `tables of decisions how to read notes to the table methodology codebook finding column` *(domain-restricted to `icsid.worldbank.org`)* | Task 1 | **Unpaid on the question, and unpaid on a refusal too — the engine offered no decode today.** Four untried families discharged in one call. `0823-R8`. |
| S2 | `ICSID "tables of decisions" disclaimer "for information purposes only" secretariat prepared table published decisions` | Task 1 — disclaimer family | **Unpaid.** No disclaimer, legend or note returned; the engine expressly reported it could not locate the string. `0823-R8`. |
| S3 | `"Eurohold" "Euroins" Romania ICSID arbitration claim insurance regulator measure` | Task 2 — `ARB/24/18` | **PAID, and smaller than it looked.** Five new title carriers give the first subject-matter characterisation; the *designator* titles were already held from 2026-08-20. `0823-R9`, `0823-E2`. |
| S4 | `"Bank of Nova Scotia" Peru ICSID arbitration claim tax measure Rule 41 objection` | Task 2 — `ARB/22/30` | **PAID thinly, on a route and a refusal.** Three never-recorded `icsidfiles.worldbank.org` PDF paths; every title was already held; the substantive prose is refused. `0823-R10`. |
| S5 | `"Hela Schwarz" IAReporter admissibility jurisdiction Protocol administrative review award analysis` | Task 3 — `N4` question (3) | **PAID on two title carriers and a fourth refusal; UNPAID on the question.** `0823-R11`. |

**Returned unspent:** Task 1's third call — the remaining instrument for that gap is a relay row landing a window on the parent page's introductory prose, not a sixth formulation, and I say so at `0823-R8` rather than let the returned call read as a closed question. Both unallocated calls returned: nothing in Task 4 turned on a fact outside this repository.

---

## §C — ITEM 1, PROCEDURE NOT RESEARCH: THE PRIOR-ART CHECK, WITH INLINE RECEIPTS

Run at HEAD `3cd149c` over all four corpora (`analytics/fetch-results/`, `analytics/daily-research/`, `analytics/optimization-log.md`, `analytics/insights.jsonl`), **against the claims I am confident in as well as the ones I was worried about** — that was the officer's diagnosis and I have applied it to myself.

| Term screened | Count | Where it lands |
|---|---|---|
| excerpts containing **both** `Trans-Global` and `Partial` | **1 of 221 records** — today's `records[8]` | `0823-R3`. Three prior excerpts carry `Trans-Global` (`2026-08-19-zz` `[2]`, `2026-08-20-daily` `[2]`, `2026-08-20-zz` `[2]`); **none carries `Partial`** |
| `Trans-Global` (repo-wide, `git grep -l -F`) | **12 files** | the party name is *not* new; the single-window adjacency is |
| `The Tribunal renders its award` | **0 files** | `0823-R5` |
| `Outcome of Proceeding` | **0 files** | `0823-R5` — a docket field label new to this project |
| `Concluded` | 5 files, **all PCA/Zeph** (`2026-08-11.md`, `2026-08-16.md`, `council-log.md`, `2026-08-11-zeph.json`, `research_log.json`) | no prior ICSID `Status of Proceeding: Concluded` |
| `Hughes Hubbard` · `Savings Deposit Insurance Fund` · `Global Law Office` | **0 / 0 / 0 files** | `0823-R6`, `0823-R7` |
| `cbe0d9f1161e209c` | **0 files** | `0823-R4` |
| `romania-insider` · `xprimm` · `seenews` · `dgkv` · `arbitrationmonitor` · `Financial Supervisory Authority` · `Euroins Romania` | **0 files each** | `0823-R9` |
| `italaw.com/cases/11988` · `italaw.com/cases/10055` · `180777` | **2 / 1 / 1 files** — all in `2026-08-20.md` | **NOT new.** `0823-E2` |
| `DS19799` · `DS19936` · `C10899` · `arbitration.org/award/1228` | **0 files each** | `0823-R10` |
| `arbitration-cases/hela-schwarz-v-china` · `procedural-order-no-3-decision-on-the-respondents-request-for-bifurcation` | **0 / 0 files** | `0823-R11` |
| route: parent `tables-of-decisions` with find `updated` · register with find `updated` · `CaseNo=ARB/24/18` · `CaseNo=ARB/22/30` · `CaseNo=ARB/17/19` + `Procedural Order No. 6` | **0 rows each**; `CaseNo=ARB/24/18` and `CaseNo=ARB/22/30` also **0 files repo-wide** | `0823-R15` |
| finds `Subject Matter` · `updated` · `Sector` fired anywhere in the corpus | **0 / 0 / 0** | `0823-R15` |

**Nothing below claims novelty without a count in this table or beside it.** Where a term returns nonzero I say what the hits are, and `0823-E2` records the one place where the check cost me a claim I had already half-written.

---

## TASK 4 — THE PRE-REGISTERED BATCH, READ AGAINST ITS GATES

### `0823-R1` — `G1` AND `G9` FIRST, BEFORE ANY BRANCH.

`control_ok: true`, `void: false`, `requested: 12`, `completed: 12`, `truncated: false`. Both my brackets — `records[0]`/`records[1]` and `records[12]` — and the relay's own prepended control returned `ok/200`, 559 bytes, digest `ff67a9d764d6a236` on all three. `truncated: false` is the pinned relay code's `12 > 12 = False`, exactly as the batch file predicted. **The batch is valid as to every content row.**

**`G9`, the `final_url` gate, applied before any branch and it does not trip.** All four `case-detail` rows return `final_url` **byte-identical** to the requested URL, including the unencoded `/` inside each `CaseNo` value — no redirect, no normalisation, no landing on a search or index page:

```
records[4] final_url = …/case-detail?CaseNo=ARB/17/19   == requested
records[5] final_url = …/case-detail?CaseNo=ARB/17/19   == requested
records[6] final_url = …/case-detail?CaseNo=ARB/24/26   == requested
records[7] final_url = …/case-detail?CaseNo=ARB/24/46   == requested
```

**So `G10` and `G12`'s branches are live rather than void.** This gate landed because `0822-B13` was sustained against me and written into the request file before the file existed; had it not, the failure mode it names would have been indistinguishable from a body-carried answer. **The objection against me is the reason today's four rows can be read at all**, and I record that in preference to defending myself.

**Where every match sat (`G8`, stated and not assumed).** `records[2]`, `[4]`, `[6]`, `[7]`, `[8]`, `[11]` all have their match at **excerpt-internal index exactly 100** — the clamp did **not** fire, so each document offset is **at least 100** and **no offset measurement is licensed for any of them**. `records[9]` sits at index **13**: the clamp fired, so **13 is the document offset** — the register page's `<title>` region. The controls sit at 0.

### `0823-R2` — `G13` FIRST BRANCH FIRES. THE `Trans-Global` CANDIDATE IS CONFIRMED FROM A SINGLE PAGE STATE, AND THE CROSS-PAGE-STATE JOIN IS NO LONGER LOAD-BEARING.

`records[8]`, find `Trans-Global`, `ok/200`, 94,996 bytes, digest `cbe0d9f1161e209c`, `find_matched: true`. **The excerpt, character-exact and reproduced whole** (the `​` are literal **U+200B**, general category Cf; the apostrophe in `Respondent’s` is **U+2019**; `&nbsp;` is an **undecoded entity**, reproduced as the field carries it):

> `he preliminary objections filed by the Respondents on Annulment (English) ​June 25, 2014 ​_ &nbsp; ​Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25) ​Decision on the Respondent’s Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) ​May 12, 2008 ​Partial ​Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No.`

**Derived from the raw JSON, not from any rendering of it** — character offsets within the 400-character field:

```
'June 25, 2014'  75      'Trans-Global'  100     'ARB/07/25'  176
'May 12, 2008'  294      'Partial'      308      'Brandes'    317      len(excerpt) = 400
```

**`Partial` sits at 308, after `May 12, 2008` at 294 and before the next case name at 317.** The window does **not** truncate before the value; it truncates 83 characters later, mid-token, inside the *Brandes* row's case number.

**Why `Partial` is Trans-Global's value and not *Brandes*'s — disambiguated from a body, not assumed.** The header row is corpus-carried (`2026-08-20-zz-column-and-middle.json` `records[2]`): `​​Case name decision or Award (language(s)​) date of decision or Award​ ​​ ​Finding that Claim is Manifestly Without Legal Merit`, and in that same window the header is followed **immediately** by the first case name — `​Ioan Micula, Viorel Micula and others v. Romania (ICSID Case No. ARB/05/20) - Annulment Proceeding` — with **no value between them**. If the finding preceded the case name, a value would sit there. It does not. **Finding is the last column of its own row, so the token after a date belongs to the row that date belongs to.** The same order is independently body-carried in the ENCORE cell this council quoted on 2026-08-20 (`…(ICSID Case No. ARB/24/46) Decision on the Respondent Rule 41 Application ( English ) October 7, 2025 No`).

**On the chairman's question, which is the part he said he was least sure of:** the `_` at offset 90 belongs to the **preceding** row — the *Micula* annulment row, whose date `June 25, 2014` sits at 75. Same rule, same window, and the 2026-08-19 and 2026-08-20 windows show that row's head.

**I derived this before his correction reached me and from the same raw field**, so this is corroboration of his corrected reading and not adoption of it. The reading he withdrew — that the window truncates at `May 12` — is falsified by the field: `Partial` is present at 308.

**What is confirmed, at its exact weight.** The register's `Partial` value belongs to *Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25)*, on a decision the register designates `Decision on the Respondent’s Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules`, dated `May 12, 2008` — **all four fields in one contiguous window, one page state, one row.** `0822-B3` reduced the prior support to one window joined across page states over a four-character overlap in recurring boilerplate; **that join is retired, not repaired.** Receipt: exactly **1 of 221 corpus records** carries both strings in one excerpt, and it is this one.

**What is emphatically NOT confirmed.** `Partial` is **not decoded**. `G16` binds: no row here founds a statement about whether any Rule 41 objection succeeded or failed, and **nobody on this council has read this decision.**

### `0823-R3` — `G14` FIRES: A DIFFERENT PAGE STATE FROM YESTERDAY'S. AND THE BYTE LENGTH HAS NOW **RECURRED** AT A DIFFERENT DIGEST, WHICH IS NEW.

`records[9]`, the mandatory same-URL same-run digest pair, returns **94,996 bytes / `cbe0d9f1161e209c`**. Yesterday's state was **94,998 / `1f026c81313f76af`**. **The digest differs, so this batch and yesterday's observe two different page states, nothing transfers between them, and today's register rows stand alone** — which is exactly why `0823-R2` matters: it does not need yesterday's window.

Derived programmatically over every corpus row on that URL (19 rows, 8 result files):

| observation date | bytes | sha256 (first 16) |
|---|---|---|
| 2026-08-19 | 94,996 | `66a879e013bd8cbf` |
| 2026-08-20 | 94,995 | `29bd0911889f8b40` |
| 2026-08-21 | 94,994 | `ebcebcae85945237` |
| 2026-08-22 | 94,998 | `1f026c81313f76af` |
| **2026-08-23** | **94,996** | **`cbe0d9f1161e209c`** |

**Five distinct `(bytes, sha256)` pairs across five dates — and 94,996 now appears twice, on two different digests five days apart.** Receipt: `cbe0d9f1161e209c` → **0 files** repo-wide before today. **What this licenses:** equal byte length on this URL is **not** evidence of an equal page state, which is a stronger form of the caution the record already carries about the digest. **What it does not license:** no mechanism, no trend, no "one byte a day" — that description broke on its fourth observation and a fifth observation is a fifth observation. Nobody has seen the changing bytes.

### `0823-R4` — `G12`: THE `ARB/24/46` / `ARB/24/26` DISCREPANCY IS CLOSED **FROM A BODY**, IN THE DIRECTION THE REGISTER POINTED — AND THE WINDOWS CARRY COUNSEL, NOT PARTY NAMES.

Both pages returned `ok/200` with `final_url` unchanged. They are **different resources with different content in every observed field**:

| | `records[6]` — `CaseNo=ARB/24/26` | `records[7]` — `CaseNo=ARB/24/46` |
|---|---|---|
| bytes / sha256 | 86,762 / `7ff2ac39ef2a43cf` | 88,032 / `ebc0d19a27f26ca8` |
| representative block in window | `y, Panama` · `Hughes Hubbard & Reed, Washington D.C., U.S.A.` | `avings Deposit Insurance Fund, Istanbul, Türkiye` · `King & Spalding, London, U.K. and New York, U.S.A.` |
| Language(s) of Proceeding | `English,Spanish` | *(not in window)* |
| Status of Proceeding | `Pending` | `Pending` |
| Latest Development | `June 18, 2025 - The proceeding is suspended pursuant to Arbitration Rule 54(1).` | `July 29, 2026 - The Claimant files observations on the request to address the objections to jurisdiction as a preliminary question.` |

**The chairman's warning 3 is right and I adopt it rather than argue with it: neither window carries a party name.** Both carry a representative-and-location block; `y, Panama` is a firm or entity name cut at the window's left edge and **is not completed**. So the reading is not "the window names other parties"; it is:

**`ARB/24/26` resolves, on ICSID's own docket, to a proceeding with a Panama-located representative, English-and-Spanish proceedings and a Rule 54(1) suspension of 18 June 2025 — a docket that differs from `ARB/24/46`'s in every field observed.** The TDM Journal title pairing *ENCORE Investment Group Limited v Republic of Türkiye* with `ARB/24/26` is therefore **not corroborated by ICSID's own docket**, and the register's `ARB/24/46` — body-carried since 2026-08-20 — stands. **The discrepancy is closed from a body in that direction.**

**Three limits travel with it.** (i) The identification rests on two 400-character windows and on the two pages being distinct resources; it does **not** rest on a party name, because neither window has one. (ii) `Panama` and `Türkiye` were not fired as finds, so their presence is **window-scoped**, not page-scoped. (iii) This says nothing about TDM as a source and nothing about what either decision holds. A `find` on a party name would settle limb (i) and is proposed at `0823-R15`.

**And it removes `0822-B12`'s weight problem entirely.** That objection was sustained against me because a designator-quoted search zero is a property of the formulation. **Today's answer comes from ICSID's body, so the search contributes nothing on either side and does not need to.**

### `0823-R5` — `ARB/17/19`: ICSID'S OWN DOCKET NOW CARRIES THE *HELA SCHWARZ* AWARD DATE. FIRST BODY CARRIER IN THIS PROJECT.

`records[4]`, `ok/200`, 94,976 bytes, `6c66b441ffc51968`, `final_url` unchanged. Excerpt, character-exact (`People’s` carries **U+2019**; zero U+200B in this window):

> ` of China Global Law Office, Beijing, People’s Republic of China Language(s) of Proceeding: English Status of Proceeding: Concluded Outcome of Proceeding: December 10, 2025 - The Tribunal renders its award. (a) Original Proceeding Published Decisions Procedural Order No. 1 Procedural Order No. 2 Procedural Order No. 3 Procedural Order No. 4 Procedural Order No. 5 Procedural Order No. 6 Procedural `

**What is newly body-carried.** `Status of Proceeding: Concluded`; the field label `Outcome of Proceeding:` (as against `Latest Development:` on the two pending dockets in the same run); and **`December 10, 2025 - The Tribunal renders its award.`** Receipts: `The Tribunal renders its award` → **0 files**; `Outcome of Proceeding` → **0 files**; all five prior `Concluded` hits are the PCA *Zeph* docket. **Until today every carrier for this award's date in this project was a title or a search-engine summary.** It is now ICSID's own docket.

**Procedural Orders Nos. 1–6 are body-carried for this matter for the first time. The list is ended by the 400-character cap, not by the docket** — the field truncates mid-token at `Procedural ` — so **no count, no enumeration and no "latest"** may be drawn, and `0815-B2` binds as it always has. **Visibility is not filing.**

**The `Decision on Jurisdiction` null, and what it does not license.** `records[5]`, same URL, same run, same file, `find_matched: false` with `records[4]`'s positive control matching on the same page state — so the page rendered and the find machinery worked. `excerpt_of` computes `idx = body.lower().find(find.lower())` over the whole tag-stripped body, so this is a **page-scoped, case-insensitive** absence.

**It is bounded on three sides and none of them is optional.** `G3`: it is this runner's view of this page state, **not** a statement that no such decision exists. `G6`/`N7`: a single U+200B or an undecoded entity inside the phrase returns a clean `false` indistinguishable from real absence — and this host demonstrably inserts U+200B, seven of them in `records[8]`'s window, though zero in this page's window. `G7`: the literal carries two spaces, and the relay can manufacture a space and never remove one, so it is a weaker test than a space-free literal.

**And the reading the chairman warned about is the one I refuse.** This null does **not** license "the objections were joined to the merits", or any other statement about how the tribunal handled them. **What would establish that:** the award text, or a document designation retrieved from a window rather than inferred from a null. `G10`'s licence is binding and is the sentence I wrote yesterday against my own future self: *a document designation is not the tribunal's characterisation and cannot answer question (3).*

### `0823-R6` — `ARB/24/46` IS A **LIVE** RING-3 PROCEEDING ON ICSID'S OWN DOCKET, DATED TWENTY-FIVE DAYS AGO.

From `records[7]`, character-exact:

> `Latest Development: July 29, 2026 - The Claimant files observations on the request to address the objections to jurisdiction as a preliminary question.`

**Body-carried, from ICSID's docket, on a proceeding this council already holds a Rule 41 decision date for.** *Objections to jurisdiction*, and a *request to address them as a preliminary question*, is the shape of this project's third ring, and it is live — the entry is dated 29 July 2026.

**And it goes no further than that, for a reason I state rather than hedge.** Ring 1 is **unknown** for this proceeding: no accessible carrier states what the asserted investment is. `G16` binds — no holding, no disposition, and the register value `No` on this matter's Rule 41 row is **still not translated**. A live jurisdictional objection with an unknown investment is a Ring-3 datum with no Ring-1 anchor, which under the scoring grammar is **not a match and not close to one**. I record it because the docket route now reaches it, not because it reaches the theme.

### `0823-R7` — `G11`: THE `_` PROBE NULLED AND BANKS NOTHING; THE PARENT PAGE RENDERS AND CARRIES THE PROSE A LEGEND WOULD LIVE IN.

`records[3]`, find `not applicable`, `find_matched: false`. **Per the branch I pre-registered yesterday and am bound by: this row banks nothing.** Not-rendering, real absence and `G6`'s hazard are not separable by it, and I said in advance that no seat would try to separate them afterwards. I do not try.

`records[2]` matched and shows the page renders:

> `are regularly updated and provide links to published decisions. The tables consist of: Decisions on Manifest Lack of Legal Merit Decisions on Disqualification Decisions on Provisional Measures Decisions on Non-Disputing Party Participation Decisions on Place of Arbitration in Additional Facility Cases Decisions on Annulment &nbsp; &nbsp; In this section Overview Decisions on Manifest Lack of Legal`

**Six sibling tables body-carried.** And **the cardinal licence carried verbatim from the request file: this establishes that ICSID's explanatory material is readable by this channel and does NOT establish that a codebook for the finding column exists anywhere.**

**The `&nbsp;` entities are visible in the field**, which is `N7` observed rather than inferred — prior art, not a finding: `2026-08-19-zz` and both 2026-08-20 register windows already carry them.

**What the window shows me for tomorrow:** the match sat at document offset ≥ 100 and the sentence **begins before the window's left edge** — `are regularly updated…` is mid-sentence. **A legend or disclaimer, if one exists, sits in the text this window cuts off.** That is a relay row, not a search, and it is `0823-R15` row 3.

### `0823-R8` — `G15`: THE `A/81/17` WATCH, AS A PAIR.

`records[10]` null, `records[11]` control matched, same URL, same run, same file. Derived over every corpus row carrying an `A/81/17` find (**20 rows total**: 18 on `uncitral.un.org/en/commission`, 1 request-echo `true` on the retired `undocs.org` route, 1 `robots_disallowed` carrying no `find_matched` key):

**11 `A/81/17`-scoped rows on the commission page against 1 distinct page state** — `f9712994f5ae`, byte-constant at **832,338**, eleven consecutive observation dates **2026-08-13 → 2026-08-23**. The earlier state `ca87a113c024` accounts for 7 rows, 2026-08-03 → 2026-08-10, also at 832,338 bytes.

**`G3` restated because this row invites the breach:** the string's absence is a fact about this page state and this runner. **It is not a statement that the 59th session's report does not exist or is late**, and the `A/80/17` control's purpose is only to distinguish a page that did not render from a string that is not on it. The lag baseline stays **UNAVAILABLE** (`N10`).

---

## TASK 1 — THE FINDING-COLUMN CODEBOOK: STILL NO. FOUR FAMILIES NOW TRIED, AND THE ENGINE DID NOT OFFER A DECODE TODAY.

### `0823-R9` — `GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook` STANDS OPEN.

S1 discharged the four families I named against myself at `0822-R4` — `codebook`, `notes to the table`, `how to read`, `methodology` — in one domain-restricted call. S2 fired the disclaimer family. **No returned title across either call is a legend, key, codebook, explanatory note, disclaimer or how-to-read apparatus for the column.** S2's engine reported in terms that it could not locate the disclaimer string and referred the question back to the page itself.

**The prediction in my docket did not materialise, and I record that as readily as I would record its opposite.** I was told to expect the engine to offer a decode again and to name and refuse it. **It offered none.** S1's closing prose speculated that methodology "would likely be found as supplementary documentation on that page" — a speculation about where an answer might be, not an answer, and not a decode. Nothing to refuse today.

**The three standing refusals are undisturbed and I restate them because they are the operative constraint:** `No` may not be read as "objection dismissed"; `_` may not be read as "not applicable"; and `_` remains **undefined**, not merely untranslated.

**A datum against my own refusal, disclosed because a refusal that has not met the tempting fact is not yet a refusal.** Today's `records[8]` window body-carries a second `_` row: the *Ioan Micula and others v. Romania* (`ARB/05/20`) **Annulment Proceeding** row, on a `Decision on the applicable arbitration rules and on the preliminary objections filed by the Respondents on Annulment`, dated `June 25, 2014`. **An annulment-proceeding row is the most favourable case the engine's "not applicable" decode could have**, and it now sits in the record on a body. It does not rescue the decode: `0821-R4`'s obstacle is a *different* row — a title-carried `Dismissing` on the `JLL Capital` `ARB/23/3` row, whose register value is also `_` — and one decode must cover both. **Two body-carried `_` rows, pointing opposite ways, is why the value stays undefined.** Disclosing the favourable one makes the refusal stronger, not weaker.

**Two calls deep on four families is not a settled null.** The families I have *not* tried are the ones an institutional page would use in a footer rather than a heading, and no formulation reaches a footer. **The instrument for that is a relay row, and I return the third call rather than spend it** — stated so that nobody reads the returned budget as a closed question.

---

## TASK 2 — `ARB/24/18` AND `ARB/22/30`: THE FIRST SUBJECT-MATTER CHARACTERISATION EITHER HAS EVER HAD, AND **NEITHER REACHES RING 1**.

**Calibration stated before the spend, as directed.** `0821-R6` established that every title for these four carries only party, number, designation and date. **What a null would have established:** that no accessible title characterises the subject matter — **not** that the proceeding is off-theme, and **not** that it lacks an IP nexus. **What a hit could buy:** a title naming the measure or the sector, which places the proceeding relative to Rings 1 and 2 at title weight and never at holding weight. Both calls hit; the calibration held in the direction I did not predict.

### `0823-R10` — `ARB/24/18` (*Eurohold Bulgaria AD and Euroins Insurance Group AD v. Romania*): TITLE-CARRIED AS AN **INSURANCE-LICENCE REVOCATION** CLAIM. RING 1 ABSENT.

Five title carriers, **none of them previously in this repository** (receipts in §C):

> `ICSID rejects Romania's call to terminate Eurohold's EUR 500 mln arbitration request | Romania Insider`
> `Eurohold and EIG launched half billion euro arbitration proceedings against Romania - Re/Insurance groups -` *(xprimm.com)*
> `ICSID rejects Romania’s request to halt Euroins Romania case | Southeast Europe Legal News | SeeNews`
> `DGKV and Pinsent Mason officially launch €500M arbitration proceedings on behalf of Eurohold and EIG against the Government of Romania`
> `Eurohold and EIG v. Romania (I), Decision on the Respondent's Rule 41 and Rule 48 Objections, 30 Sept 2025` *(jusmundi.com)*

**What the titles carry:** a re/insurance-sector claimant group; a quantum of roughly €500 million; a Romanian State respondent; a procedural fact that a request to terminate or halt the arbitration was rejected; a proceeding numeral `(I)`; and a decision designated on **Rule 41 *and* Rule 48** objections, dated 30 September 2025 — which matches the register's date for this row exactly and matches the register's own designation `Decision on Rule 41 and 48 Objections` body-carried on 2026-08-20.

**Ring assessment, stated honestly and not inflated.** **Ring 1 — IP as a covered investment — is ABSENT on every accessible title**: the asserted investment is an insurance business and its Romanian subsidiary, and no title mentions any intellectual property, trade secret, know-how or clinical-data element. **Ring 2** is present in shape only — a financial regulator's measure — and **Ring 3** is not characterised by any title. **This proceeding does not reach the theme**, and under the scoring grammar it cannot: a claim with no Ring 1 is not a match at any score. That is a relevance statement about accessible titles, **not** a finding about the decision, which nobody has read.

**Refused and not banked:** the engine's account of the ASF licence revocation of 17 March 2023, the capital-deficit figure, the Bucharest Tribunal's insolvency confirmation, the ~30% third-party motor market share, and Romania's jurisdictional/admissibility arguments. **No returned title carries any of it.** I record it under carrying-span clause 7 so the next seat meets it already refused, and I note that it would not change the Ring-1 verdict even if banked.

### `0823-R11` — `ARB/22/30` (*Bank of Nova Scotia v. Republic of Peru*): NO NEW TITLE, THREE NEW ALLOWED-HOST DOCUMENT ROUTES, AND THE MOST TEMPTING PROSE OF THE DAY.

**Every title S4 returned was already in this record from 2026-08-20** — italaw `/cases/10055`, the jusmundi decision title, both TDM titles (English and Spanish), and the italaw PDF `180777.pdf`. Receipts in §C. **I bought no title.**

**What I did buy is a route.** Three `icsidfiles.worldbank.org` paths under case folder `C10899` — `DS19799_En.pdf`, `DS19800_En.pdf`, `DS19936_En.pdf` — return **0 files** repo-wide. `icsidfiles.worldbank.org` is on `ALLOWED_HOSTS`, but **the reduction returns `""` for any `content_type` without `html` or `text` in it**, so a PDF is unreadable by this channel. **These are operator-side retrievals of retrievable documents**, and they belong on the same list as the *Hela Schwarz* award.

**The refusal, and why this one matters more than most.** The engine produced a detailed account of Peru's four Rule 41 grounds, of a challenged **2021 Constitutional Court decision**, and of the tribunal's disposition, complete with quoted-looking phrases. **No returned title carries one word of it, and it is not banked.** I flag it at length for one reason: **if it were true, this would be a Ring-2/Ring-3 structural analogue to this project's seed corpus with Ring 1 absent** — a judicial measure challenged, and the definition of "the investment" litigated at the early-dismissal stage. That is exactly the shape a seat would most want to be true, which is exactly why generated prose must not supply it. **The instrument that could establish it is a PDF on an allowed host that this council cannot decode.** Ring 1 is absent on every accessible carrier either way: the claimant is a bank and no carrier names an IP asset.

**Both proceedings move from UNEXAMINED-with-nothing to UNEXAMINED-with-title-carried-subject-matter. Neither becomes EXAMINED, and neither becomes EXCLUDED.** `ARB/23/3` (*JLL Capital v. Honduras*) still has no subject-matter search at all.

---

## TASK 3 — `N4` QUESTION (3): STILL UNANSWERED, AND THE CALL WAS WORTH FIRING.

### `0823-R12` — WHAT I SAID IN ADVANCE IT COULD BUY, AND WHAT IT BOUGHT.

**Stated before firing:** IAReporter's article titles are long doctrinal sentences that routinely carry the disposition; a title pairing this matter with the word **admissibility** would be the first carrier this project holds that takes a position on the distinction. **A null would establish only that no such title surfaced under this formulation** — never that the tribunal did not so characterise the conditions.

**No returned title uses the word *admissibility* in connection with this matter.** `GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility` **stands open.** The one IAReporter title returned for this matter is the bifurcation article already recorded at `0822-R7`, and the strongest carrier this project holds still uses the verb **"gives effect to"**, which takes no position between jurisdiction and admissibility.

**Two title-carried items, both new to the repository (receipts in §C):**

> `Hela Schwarz v. China | Investment Arbitration Reporter` — `https://www.iareporter.com/arbitration-cases/hela-schwarz-v-china/`
> `Hela Schwarz v. China, Procedural Order No.3 (Decision on the Respondent’s Request for Bifurcation), 17 Dec 2018` — jusmundi

The second **dates and designates PO No. 3**, and today's ICSID docket body-carries `Procedural Order No. 3` in this matter's Published Decisions list — so a title-carried designation and a body-carried listing now meet on the same document. **Body unread in both cases; the IAReporter host remains headline-only.**

**The fourth refusal, and it is the most dangerous formulation yet because it contains both words.** The engine generated, unprompted, party-position prose including `"informal and non-mandatory wording of Article 9(2) of the BIT"` and that the cooling-off requirement `"does not constitute a formal pre-condition to the jurisdiction of the Tribunal and/or the admissibility of the claim"`. **No returned title carries it. Not banked.** And **it would not answer question (3) even if it were banked**, on two independent grounds: (i) it reports the **Claimant's** assertion, and question (3) asks how the **tribunal** characterised the conditions; (ii) it names **Article 9(2) of the BIT**, where the project's question concerns the conditions in the **Protocol** to that BIT.

**A method observation worth carrying forward.** Across four sessions the engine's prose on this matter has named the instrument four different ways — "Article 9(a) of the Protocol", "Article 9(1) and (2) of the BIT" (`0822-R6`), and now "Article 9(a) of the Protocol" and "Article 9(2) of the BIT" **in the same answer**. **The generated prose does not hold the BIT/Protocol distinction stable**, and that distinction is the exact locus of this project's third-ring question. That is a reason to refuse this class of prose on this matter permanently, not merely to decline to bank it today.

---

## `0823-R13` — THE SIX OBJECTIONS SUSTAINED AGAINST ME YESTERDAY: I CONTEST NONE OF THEM, AND HERE IS THE DERIVATION FOR THE ONE I CAN CHECK.

The chairman recorded against himself that I was never resumed. I answer now.

**`0822-B8` — CONCEDED, and my own table convicts me.** The four rows I published sum as `2 + 4 + 2 + 2 = 10`, and by host `pca ×2 + pca ×4 + pca ×2 + (icsid ×1 + pca ×1) = pca 9, icsid 1`. I wrote "eight and one" and "8 of 8". **The correct figures are ten rows, nine and one, and 9 of 9 constant-length on `pca-cpa.org`.** I corrected the postscript's host split and silently inherited its total. My conclusions are untouched, and they are strengthened by the correction.

**`0822-B9` — ACCEPTED, NOT VERIFIED, AND I MARK THE DIFFERENCE.** The corrected triple is **14 keys / 127 rows / 8 varying digests**, with the honest restatement *14 keys / 127 rows screened, of which 7 keys / 37 rows could discriminate; zero varying excerpts in either set.* **I have not reproduced that derivation**, because doing so is precisely the third census next step 7 forbids and my docket forbids again today. **So it stands on the officer's derivation and not on mine, and I say so rather than let a number I did not check pass as one I did.** Every correction runs in the direction of a smaller claim, which is the direction that needs no defence.

**`0822-B10` — CONCEDED on both limbs.** (a) My rendering of the 2026-08-12 correction was a paraphrase inside block-quotation marks with a dropped qualifier and a manufactured connective. (b) `supporting_quote` carried `minutes apart`, struck by `0805-B4` the day it was written. **Any downstream use of that claim must elide it as `the same run […]`.** I have not reused the quote in this return.

**`0822-B11` — CONCEDED.** Five formulations before today, seven that did not pair the case with `IAReporter`. My parenthetical enumerated five while my sentence said six.

**`0822-B12` — CONCEDED, and superseded by evidence today.** The `ARB/24/26` search zero carried essentially no weight; **`0823-R4` replaces it with ICSID's own body**, which is what the objection said the instrument should be.

**`0822-B13` — CONCEDED, and it is the objection that made today's four rows readable.** See `0823-R1`.

**`0822-B14` — CONCEDED.** The stub's internal pointer should read `0822-R17`; the *number* `0822-R14` stays dead, for the reason I gave and the officer endorsed.

**Five wrong integers in one return is a finding about my seat and I do not soften it.** Every derived number in today's return is computed programmatically and its derivation shown; where I could not compute one, I have said whose it is.

---

## `0823-R14` — METHOD NOTE (self-training mandate)

**Today's lesson is the inverse of yesterday's, and it is worth more.** Yesterday's failure was searching without opening the record. **Today's near-failure was reading the evidence through a rendering of it.** The chairman stated a conclusion about what `records[8]`'s window does *not* contain from a console view that had truncated the field at ~300 of 400 characters — the same class of error as the 2026-08-14 `“Chip” Gordon` truncation, where a window edge became a property of a source. I avoided it only because I parsed the raw JSON with `json.load` and computed character offsets rather than reading a printed excerpt.

**The rule I take from it, and it generalises past today:** for any claim of the form *"the window does not contain X"*, the operative check is `len(field)` and `field.find(X)`, **not** a visual reading — because every display in this pipeline truncates and none of them says so. That is the same discipline as `G8`'s clamp rule, applied to the reader instead of the instrument.

**Second, cheaper note, confirming `0822-R18`'s domain-restriction point at a new weight.** S1 was domain-restricted and discharged **four** synonym families in one call where four unrestricted formulations across three days discharged two. **Domain restriction is not a filter on a query; it is a different instrument**, and on institutional apparatus it is roughly four times as efficient per call in this sample of one day.

---

## `0823-R15` — RELAY ROWS PROPOSED FOR TOMORROW (`0821-R9` FORM). LICENCES FIXED HERE, BEFORE ANY RESULT EXISTS. I HAVE FIRED NOTHING.

**Prior-art check run first, per route, at HEAD `3cd149c` over 32 files / 221 records** — counts in §C and repeated per row below.

| # | URL | `find` | Prior art | What a match licenses; what a null licenses |
|---|---|---|---|---|
| 1 | `…/case-database/case-detail?CaseNo=ARB/24/18` | `Status of Proceeding` | **0 rows**; `CaseNo=ARB/24/18` **0 files repo-wide** | The `Eurohold` docket, never fetched. A match lands a window in the metadata block and licenses **only what the window shows** — status, latest development, representative block. `G9`'s `final_url` gate applies in full: if `final_url` differs in any respect the row is uninterpretable as to the designator. **It cannot establish subject matter and cannot establish a holding.** |
| 2 | `…/case-database/case-detail?CaseNo=ARB/22/30` | `Status of Proceeding` | **0 rows**; `CaseNo=ARB/22/30` **0 files repo-wide** | Row 1's pair for the *Bank of Nova Scotia* docket. Same licence, same `G9` gate. **Neither is a control for the other** — different pages, `0821-B17`. Near-positive on the strength of **4 of 4** `ok` matches on this literal across four distinct case-detail pages today, which is a series and not a single observation. |
| 3 | `https://icsid.worldbank.org/cases/content/tables-of-decisions` | `updated` | **0 rows** for this URL+find; the literal `updated` has **never** been fired anywhere in the corpus | **The codebook probe done properly.** Today's window on this page begins mid-sentence at `are regularly updated…`, so the introductory paragraph — where an institutional disclaimer or legend would sit — lies **before** the window's left edge. `updated` is **space-free**, so `G7`'s asymmetry does not weaken it, and it lands the window ~100 characters earlier. **A match licenses only the returned window; it does not establish that a codebook exists.** If it nulls, the row banks nothing — `G6` and non-rendering are not separable. |
| 4 | `…/tables-of-decisions/manifest-lack-of-legal-merit` | `updated` | **0 rows** for this URL+find | Row 3 on the child table, for the same reason: a per-table legend sits in the table's own head or footer, and every window this council has opened on this page has landed in the row body or the `<title>`. Same licence, same nulls-of-design. |
| 5 | `…/case-database/case-detail?CaseNo=ARB/17/19` | `Procedural Order No. 6` | **0 rows** | **Window-slide on the *Hela Schwarz* Published Decisions list**, whose end today is the 400-character cap and not the docket. A match extends the visible list rightward and licenses **only the strings in the window**. **It cannot answer question (3)**, and `G10`'s licence binds it verbatim: *a document designation is not the tribunal's characterisation.* Weaker than a space-free literal per `G7` (three spaces). |
| 6 | `…/case-database/case-detail?CaseNo=ARB/17/19` | `Status of Proceeding` | 1 row (today, `d341cae`) | **Mandatory same-URL, same-run digest pair for row 5**, per `0821-E8`. Without it tomorrow's window cannot be compared to today's page state. Pre-registered branch: **if the digest differs from `6c66b441ffc51968`, the two runs observe different page states and nothing transfers between them.** |
| 7 | `…/case-database/case-detail?CaseNo=ARB/24/26` | `Panama` | **0 rows**; the literal has never been fired | **Closes the one limb `0823-R4` leaves open.** Today's identification rests on a representative block, not a party name. `Panama` is **space-free**, so `G7` does not weaken it. **A match licenses only that the string is page-carried and whatever the window shows; it does not name a party.** If it nulls, the row banks nothing — the token was window-observed and a page-scoped null cannot unobserve it, only fail to confirm it. |

**Pre-registered nulls-of-design, licensing nothing, across all seven.** `G3` — a refusal, 404 or no_contact is this runner's access at that instant and never a statement about ICSID, a proceeding or a document's existence. `G6`/`N7` — an undecoded entity or a U+200B inside any literal returns a clean `find_matched: false` indistinguishable from real absence, **and today's `records[8]` proves the hazard is live on this host rather than theoretical**. `G5` — anything living in an attribute or an `href` is invisible. `G16` — **no row here may found an ordinal, a count, a disposition or a holding**, and the four register-carried proceedings remain **UNEXAMINED, not EXCLUDED**.

---

## § CANDIDATE CLAIMS

```json
[
  {
    "claim_text": "ICSID's Decisions on Manifest Lack of Legal Merit register carries the finding-column value 'Partial' on the row for Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25), on a decision the register designates 'Decision on the Respondent's Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules', dated May 12, 2008 — all four fields contiguous in a single returned window on a single page state. The value 'Partial' is NOT decoded and no holding is asserted.",
    "confidence": "high",
    "supporting_quote": "​Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25) ​Decision on the Respondent’s Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) ​May 12, 2008 ​Partial ​Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No.",
    "supporting_locator": "BODY-CARRIED, ONE PAGE STATE, ONE WINDOW. Relay reduction at commit d341cae, records[8]; URL https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit; ok/200, 94,996 bytes, sha256 cbe0d9f1161e209c, final_url identical to requested, control_ok true, void false, requested 12 completed 12 truncated false. QUOTE IS A TAIL EXCERPT of the 400-character field beginning at offset 100 of that field; the leading characters 'he preliminary objections filed by the Respondents on Annulment (English) ​June 25, 2014 ​_ &nbsp; ' are ELIDED HERE and belong to the PRECEDING row (Ioan Micula and others v. Romania, ARB/05/20, Annulment Proceeding). The ​ characters are literal U+200B (category Cf); the apostrophe in Respondent’s is U+2019; both reproduced as the field carries them. CHARACTER OFFSETS WITHIN THE FIELD, derived programmatically: 'Trans-Global' 100, 'ARB/07/25' 176, 'May 12, 2008' 294, 'Partial' 308, 'Brandes' 317, len 400 — so 'Partial' sits between this row's date and the NEXT case name. COLUMN ORDER IS INDEPENDENTLY BODY-CARRIED: the header row (2026-08-20-zz-column-and-middle.json records[2]) reads 'Case name decision or Award (language(s)) date of decision or Award  Finding that Claim is Manifestly Without Legal Merit' and is followed IMMEDIATELY by the first case name with no value between them, so the finding is the last column of its own row. PER G8 the match sat at excerpt-internal index exactly 100, so the clamp did NOT fire and the document offset is AT LEAST 100 — this is not an offset measurement. THIS SUPERSEDES the cross-page-state window-slide join reduced by 0822-B3 to one window over a four-character overlap; that join is retired, not repaired. PRIOR ART, TERMS AND COUNTS: exactly 1 of 221 corpus records carries both 'Trans-Global' and 'Partial' in one excerpt (this one); three prior excerpts carry 'Trans-Global' and none carries 'Partial'; git grep -l -F 'Trans-Global' = 12 files repo-wide, so the party name is not new and the single-window adjacency is. THE VALUE 'Partial' IS NOT TRANSLATED AND '_' REMAINS UNDEFINED (G16); NOBODY HAS READ THIS DECISION AND NO HOLDING IS ASSERTED."
  },
  {
    "claim_text": "ICSID's own case-detail page for ICSID Case No. ARB/17/19 (Hela Schwarz GmbH v. People's Republic of China) states 'Status of Proceeding: Concluded' and 'Outcome of Proceeding: December 10, 2025 - The Tribunal renders its award.' This is the first carrier for that award date in this project that is a body rather than a title or a search-engine summary.",
    "confidence": "high",
    "supporting_quote": "Language(s) of Proceeding: English Status of Proceeding: Concluded Outcome of Proceeding: December 10, 2025 - The Tribunal renders its award. (a) Original Proceeding Published Decisions Procedural Order No. 1 Procedural Order No. 2 Procedural Order No. 3 Procedural Order No. 4 Procedural Order No. 5 Procedural Order No. 6 Procedural",
    "supporting_locator": "BODY-CARRIED. Relay reduction at commit d341cae, records[4]; URL https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/17/19; ok/200, 94,976 bytes, sha256 6c66b441ffc51968; final_url BYTE-IDENTICAL to the requested URL, so the pre-registered G9 gate does not trip and the row is interpretable as to this designator. QUOTE IS AN INTERIOR EXCERPT of the 400-character field; the leading ' of China Global Law Office, Beijing, People’s Republic of China ' is ELIDED HERE and the field's own left edge is a mid-token cut of a representative block. THE TRAILING 'Procedural ' IS THE 400-CHARACTER CAP, NOT THE END OF THE DOCKET — no count, no enumeration and no 'latest' may be drawn from the Published Decisions list (0815-B2), and VISIBILITY IS NOT FILING. Procedural Orders Nos. 1-6 are body-carried for this matter for the first time; nothing establishes when ICSID published any of them. PER G8 the match sat at excerpt index exactly 100, clamp did not fire, document offset AT LEAST 100 — not a measurement. PRIOR-ART RECEIPTS at HEAD 3cd149c: git grep -l -F 'The Tribunal renders its award' = 0 files; 'Outcome of Proceeding' = 0 files; 'Concluded' = 5 files, ALL of which are the PCA Zeph docket (2026-08-11.md, 2026-08-16.md, council-log.md, 2026-08-11-zeph.json, state/research_log.json); 'Global Law Office' = 0 files; the route CaseNo=ARB/17/19 had 0 rows in the corpus and 3 files repo-wide, all of which are yesterday's proposal, today's request file and today's reduction. THIS DATES THE RENDERING OF THE AWARD AND NOTHING ELSE: it states no ground, no disposition, and no characterisation, and it does not corroborate the engine-generated 'published on January 15, 2025' parenthetical, which remains refused (N5). GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility IS NOT TOUCHED BY THIS ROW."
  },
  {
    "claim_text": "On ICSID's own docket the designator ARB/24/26 resolves to a proceeding whose case-detail page differs from ARB/24/46's in every observed field — different byte length, different digest, different Language(s) of Proceeding, different Latest Development, and a Panama-located representative rather than a Türkiye-located one. The TDM Journal title pairing ENCORE Investment Group Limited v Republic of Türkiye with ARB/24/26 is therefore not corroborated by ICSID's docket, and the register's body-carried ARB/24/46 for that matter stands.",
    "confidence": "medium-high",
    "supporting_quote": "y, Panama Hughes Hubbard & Reed, Washington D.C., U.S.A. Language(s) of Proceeding: English,Spanish Status of Proceeding: Pending Latest Development: June 18, 2025 - The proceeding is suspended pursuant to Arbitration Rule 54(1).&nbsp;",
    "supporting_locator": "BODY-CARRIED, TWO PAGES, ONE RUN. Relay reduction at commit d341cae: records[6] (CaseNo=ARB/24/26, ok/200, 86,762 bytes, sha 7ff2ac39ef2a43cf) and records[7] (CaseNo=ARB/24/46, ok/200, 88,032 bytes, sha ebc0d19a27f26ca8). BOTH final_url values are BYTE-IDENTICAL to their requested URLs, so G9 does not trip and G12's pre-registered branches are live. THE DECISIVE LIMITATION, STATED FIRST: NEITHER WINDOW CARRIES A PARTY NAME. Both carry a representative-and-location block; 'y, Panama' is an entity or firm name CUT AT THE WINDOW'S LEFT EDGE AND NOT COMPLETED, and it must never be used as a find. records[7]'s counterpart block reads 'avings Deposit Insurance Fund, Istanbul, Türkiye King & Spalding, London, U.K. and New York, U.S.A.' — also left-cut. The tokens 'Panama' and 'Türkiye' were NOT fired as finds, so their presence is WINDOW-SCOPED, not page-scoped. THE IDENTIFICATION THEREFORE RESTS ON (i) the two URLs being distinct resources with distinct content in every observed field and (ii) the location tokens, NOT on a party name. PER G8 both matches sat at excerpt index exactly 100 — clamp did not fire, document offsets AT LEAST 100, not measurements. THE SEARCH CONTRIBUTES NOTHING ON EITHER SIDE: 0822-B12 was SUSTAINED against my 2026-08-22 reliance on a designator-quoted search zero, because the same engine returned zero titles for ARB/24/46 — the designator ICSID's register body-carries — twenty-four hours earlier. That objection is conceded and this claim does not use the search. PRIOR-ART RECEIPTS at HEAD 3cd149c: 'Hughes Hubbard' 0 files, 'Savings Deposit Insurance Fund' 0 files; CaseNo=ARB/24/26 and CaseNo=ARB/24/46 each 0 rows in the corpus before today. THIS IS A STATEMENT ABOUT WHICH DOCKET A DESIGNATOR RESOLVES TO ON ICSID'S OWN SITE. It is NOT a statement about TDM as a source, NOT a statement that ARB/24/26 'does not exist', and NOT a holding: nobody has read either decision. A find on 'Panama' would close limb (i) and is proposed as row 7 of 0823-R15."
  },
  {
    "claim_text": "ICSID's case-detail page for ICSID Case No. ARB/24/46 records a Latest Development of July 29, 2026 — 'The Claimant files observations on the request to address the objections to jurisdiction as a preliminary question' — so a jurisdictional-objection question is live and litigated in that proceeding. Nothing accessible establishes what the asserted investment is, so Ring 1 is unknown and the proceeding does not reach this project's theme on the evidence available.",
    "confidence": "medium-high",
    "supporting_quote": "Status of Proceeding: Pending Latest Development: July 29, 2026 - The Claimant files observations on the request to address the objections to jurisdiction as a preliminary question.&nbsp;",
    "supporting_locator": "BODY-CARRIED. Relay reduction at commit d341cae, records[7]; URL https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/24/46; ok/200, 88,032 bytes, sha ebc0d19a27f26ca8, final_url byte-identical to requested (G9 does not trip). '&nbsp;' is an UNDECODED HTML ENTITY reproduced as the field carries it (N7: no html.unescape anywhere in scripts/fetch_relay.py). RING ASSESSMENT, STATED AT ITS TRUE WEIGHT AND NOT INFLATED: this is a RING 3 datum — a live jurisdictional objection proposed to be heard as a preliminary question — with NO RING 1 ANCHOR, because no accessible carrier states what the asserted investment in this proceeding is. Under the scoring grammar a claim with no Ring 1 cannot be a match at any score, so this is recorded as a docket fact and NOT as a thematic lead. The register value 'No' on this matter's Rule 41 row REMAINS UNTRANSLATED and may not be read as 'objection dismissed' (0821-R4, three standing refusals). NO HOLDING, NO DISPOSITION AND NO OUTCOME IS ASSERTED; nobody has read the Rule 41 decision of October 7, 2025 or anything else in this matter. G16 binds. The proceeding remains UNEXAMINED, NOT EXCLUDED."
  },
  {
    "claim_text": "No source accessible to this council decodes the values of ICSID's 'Finding that Claim is Manifestly Without Legal Merit' column; No, Partial and _ remain untranslated and _ remains undefined. Four previously untried synonym families — codebook, notes to the table, how to read, methodology — plus the disclaimer family have now been screened and returned no legend, key, note or how-to-read apparatus.",
    "confidence": "medium-high",
    "supporting_quote": null,
    "supporting_locator": "Q — N/A, AND THE REASON: this is a NEGATIVE claim scoped to this instrument, and no carrying span exists in any source I can open, because the proposition is that no such source was returned. SCREENED: WebSearch S1 of 2026-08-23, 'tables of decisions how to read notes to the table methodology codebook finding column', domain-restricted to icsid.worldbank.org, 10 results; WebSearch S2, 'ICSID \"tables of decisions\" disclaimer \"for information purposes only\" secretariat prepared table published decisions', three result sets. NO RETURNED TITLE IS A LEGEND, KEY, CODEBOOK, EXPLANATORY NOTE, DISCLAIMER OR HOW-TO-READ APPARATUS. This supersedes the 2026-08-22 statement that my null was two calls deep with four families untried (0822-R4): the four named families are now tried. THE ENGINE OFFERED NO DECODE TODAY — the prediction in my docket that it would did not materialise, and I record that as readily as its opposite. THE RELAY LIMB: records[3] of the reduction at commit d341cae fired the literal 'not applicable' at the parent tables page and returned find_matched false; PER THE BRANCH I PRE-REGISTERED AT 0822-R17 ROW 2 AND AM BOUND BY, THAT ROW BANKS NOTHING — non-rendering, real absence and G6's zero-width hazard are not separable by it, and I do not try to separate them. records[2] matched on the same URL in the same run, so the page renders and the find machinery worked; PER THE CARDINAL LICENCE that establishes only that ICSID's explanatory material is readable by this channel and does NOT establish that a codebook exists anywhere. A DATUM AGAINST MY OWN REFUSAL, DISCLOSED: today's records[8] window body-carries a SECOND '_' row — the Ioan Micula and others v. Romania (ARB/05/20) ANNULMENT PROCEEDING row, dated June 25, 2014 — which is the most favourable case the engine's refused 'not applicable' decode could have. It does not rescue the decode, because 0821-R4's obstacle is a different row: a title-carried 'Dismissing' on the JLL Capital ARB/23/3 row, whose register value is also '_', and one decode must cover both. TWO BODY-CARRIED '_' ROWS POINTING OPPOSITE WAYS IS WHY THE VALUE STAYS UNDEFINED. Per the cross-BIT consistency rule this is 'not found in accessible sources', NEVER 'does not exist'. GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook."
  },
  {
    "claim_text": "ICSID Case No. ARB/24/18 (Eurohold Bulgaria AD and Euroins Insurance Group AD v. Romania) is title-carried as a claim by a re/insurance group against Romania of roughly EUR 500 million; no accessible title names any intellectual property, trade secret, know-how or clinical-data element, so Ring 1 is absent on the evidence available and the proceeding does not reach this project's theme.",
    "confidence": "medium",
    "supporting_quote": "Eurohold Bulgaria AD and Euroins Insurance Group AD v. Romania, ICSID Case No. ARB/24/18 | italaw",
    "supporting_locator": "TITLE-CARRIED ONLY; ALL BODIES UNREAD. The quoted title is from https://www.italaw.com/cases/11988 and IS PRIOR ART, not new: it entered this record on 2026-08-20 (analytics/daily-research/2026-08-20.md, and git grep -l -F 'italaw.com/cases/11988' = 2 files at HEAD 3cd149c). WHAT IS NEW ARE FIVE SUBJECT-MATTER TITLE CARRIERS from WebSearch S3 of 2026-08-23, '\"Eurohold\" \"Euroins\" Romania ICSID arbitration claim insurance regulator measure', each returning 0 files repo-wide before today (receipts: romania-insider 0, xprimm 0, seenews 0, dgkv 0, arbitrationmonitor 0, 'Financial Supervisory Authority' 0, 'Euroins Romania' 0): 'ICSID rejects Romania's call to terminate Eurohold's EUR 500 mln arbitration request | Romania Insider'; 'Eurohold and EIG launched half billion euro arbitration proceedings against Romania - Re/Insurance groups -'; 'ICSID rejects Romania’s request to halt Euroins Romania case | Southeast Europe Legal News | SeeNews'; 'DGKV and Pinsent Mason officially launch €500M arbitration proceedings on behalf of Eurohold and EIG against the Government of Romania'; and a jusmundi title 'Eurohold and EIG v. Romania (I), Decision on the Respondent's Rule 41 and Rule 48 Objections, 30 Sept 2025', whose date matches the register's body-carried 'September 30, 2025' and whose designation matches the register's body-carried 'Decision on Rule 41 and 48 Objections' (2026-08-20 window). REFUSED AND NOT BANKED, recorded so a later seat meets it already refused: generated search prose describing an ASF licence revocation of 17 March 2023, a capital-deficit figure, a Bucharest Tribunal insolvency confirmation, a ~30% motor-insurance market share, and Romania's jurisdictional and admissibility arguments. NO RETURNED TITLE CARRIES ANY OF IT, and it would not change the Ring 1 verdict if banked. THE RING-1 ABSENCE IS A STATEMENT ABOUT ACCESSIBLE TITLES, NOT ABOUT THE DECISION: nobody has read it, no holding is asserted, and the proceeding is UNEXAMINED, NOT EXCLUDED. The register value 'No' on this row remains untranslated."
  },
  {
    "claim_text": "The Hela Schwarz tribunal's characterisation of the China-Germany BIT Protocol Article 9 conditions — jurisdiction or admissibility — remains unstated by any accessible source, and a search pairing the matter with IAReporter and the word admissibility returned no title using that word in connection with this matter.",
    "confidence": "medium-high",
    "supporting_quote": null,
    "supporting_locator": "Q — N/A, AND THE REASON: no accessible source contains a carrying span for the tribunal's characterisation; the strongest carrier this project holds is an IAReporter article title using the verb 'gives effect to', which takes no position between jurisdiction and admissibility. CARRIES FORWARD the 2026-08-22 claim as amended at 0822-R8 and adds one screen: WebSearch S5 of 2026-08-23, '\"Hela Schwarz\" IAReporter admissibility jurisdiction Protocol administrative review award analysis', 8 results; the only IAReporter article title returned for this matter is the bifurcation article already recorded at 0822-R7. TWO NEW TITLE CARRIERS, both 0 files repo-wide before today: 'Hela Schwarz v. China | Investment Arbitration Reporter' (https://www.iareporter.com/arbitration-cases/hela-schwarz-v-china/) and a jusmundi title 'Hela Schwarz v. China, Procedural Order No.3 (Decision on the Respondent’s Request for Bifurcation), 17 Dec 2018', which meets the body-carried 'Procedural Order No. 3' in today's ICSID Published Decisions list for this matter. BODIES UNREAD; IAReporter is headline-only. FOURTH REFUSAL, RECORDED SO IT IS MET ALREADY REFUSED: S5's generated prose quoted party-position language including 'informal and non-mandatory wording of Article 9(2) of the BIT' and that a cooling-off requirement 'does not constitute a formal pre-condition to the jurisdiction of the Tribunal and/or the admissibility of the claim'. NO RETURNED TITLE CARRIES IT AND IT IS NOT BANKED. IT WOULD NOT ANSWER THE QUESTION EVEN IF BANKED, ON TWO INDEPENDENT GROUNDS: it reports the CLAIMANT's assertion, not the tribunal's characterisation; and it names Article 9(2) OF THE BIT, where the question concerns the conditions in the PROTOCOL to that BIT. ACROSS FOUR SESSIONS THE GENERATED PROSE HAS NAMED THE INSTRUMENT FOUR DIFFERENT WAYS and does not hold the BIT/Protocol distinction stable — the exact locus of the question. THE ROUTE REMAINS OPERATOR-SIDE: every rendition of this award on an ALLOWED_HOSTS host is a PDF and excerpt_of returns '' for a non-text content_type; the only HTML rendition of the award text is on jusmundi.com, which is not on ALLOWED_HOSTS. TODAY'S records[5] NULL ON 'Decision on Jurisdiction' DOES NOT BEAR ON THIS QUESTION IN EITHER DIRECTION (G3, G10). GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility."
  }
]
```

---

## § OBJECTIONS AND OBSERVATIONS AGAINST MY OWN WORK

**`0823-E1` — THE CLAIM I MOST EXPECT TO BE CHALLENGED, AND ITS WEAKEST JOINT NAMED.** `0823-R2`'s column-order argument is the load-bearing step, not the character offsets. The offsets prove `Partial` sits between this row's date and the next case name; **they do not by themselves prove `Partial` is not the *Brandes* row's value carried before its case name.** I close that with the header window — header, then first case name, with no value between — which is a body-carried observation from a *different page state* five days ago. **So one link in a single-page-state argument reaches across page states after all**, and if that header window were misread the argument weakens. It is corroborated by the ENCORE cell's name-title-date-value order, body-carried on a third page state, which is why I state it at high confidence rather than lower. **A seat wanting to break this should attack the header window, not the offsets.**

**`0823-E2` — I DESIGNED S4 FROM THE GAP AND NOT FROM THE RECORD, AND THE RECORD ALREADY HELD ITS ANSWER. FOURTH INSTANCE OF `0821-E3`'S PATTERN, THIRD SESSION RUNNING.** Every title S4 returned for *Bank of Nova Scotia* — italaw `/cases/10055`, the jusmundi title, both TDM titles, `180777.pdf` — is recorded verbatim in `analytics/daily-research/2026-08-20.md`. **A three-second `git grep` would have told me that before I spent the call**, and I ran that grep afterwards, as a receipt, which is the wrong order. The call was not worthless — three `icsidfiles` paths are new — but I did not buy what I went to buy. **This is the officer's diagnosis exactly: I ran the prior-art check on the claims I was worried about and not on the call I was confident in.**

**`0823-E3` — WHAT I DID NOT CHECK, NAMED SO COVERAGE IS NOT READ AS SILENCE.** (i) **`ARB/23/3` (*JLL Capital v. Honduras*) still has no subject-matter search at all** — I had two Task 2 calls and three register-carried proceedings without coverage, and I spent both on the other two. The gap is smaller than yesterday's and it is not closed. (ii) I did **not** screen notational variants of `ARB/24/26` (`ARB 24/26`, `ARB.24.26`, bare `24/26`) in the repository — the officer's near-miss 5 of 2026-08-22 named this and neither of us has done it; today's body evidence makes it less important, not unnecessary. (iii) I did **not** verify that any ICSID case-detail page carries a subject-matter or sector field: `0821-R9`'s screen was over **stored excerpts**, i.e. window-scoped, and a page-scoped `find` would be a strictly stronger test — which is why `Subject Matter` appears nowhere in my proposed rows as a claim and only as an untried literal. (iv) I did not read any window from the batch beyond the twelve content rows, and I have not examined `state/`, `src/` or the digest pipeline at all. (v) **I did not reproduce the officer's `0822-B9` derivation** and say so at `0823-R13` rather than let acceptance read as verification.

**`0823-E4` — A NUMBER I AM CARRYING FROM ANOTHER SEAT AND CANNOT CHECK WITHOUT BREACHING A CLOSE-OUT.** `0823-R13` accepts **14 keys / 127 rows / 8 varying digests**, and 7 keys / 37 rows discriminating. **That triple is the officer's and not mine.** Next step 7 and my own docket forbid the census that would confirm it. **A derived number a seat accepts without deriving is exactly the object this council says a later seat inherits and cannot check** — so it is marked, here, as inherited.

**`0823-E5` — MY RING ASSESSMENTS FOR `ARB/24/18` AND `ARB/22/30` ARE NEGATIVE CLAIMS FROM TITLES, AND NEGATIVE CLAIMS FROM TITLES ARE THE FAILURE THIS PROJECT DIAGNOSED IN ITS OWN DIGEST.** The 2026-08-11 record found twelve of seventeen digest article files asserting doctrinal absences about bodies they stated were never read. **I have written two Ring-1 absences from titles today.** I believe they are correctly scoped — each says *no accessible title names an IP element*, never *the proceeding involves no IP* — but the distinction lives entirely in a clause, and a clause is what gets dropped when a claim travels. **If either sentence is ever restated without "on accessible titles", it becomes the defect this project exists to not commit.** I would rather flag that now than have it flagged later.

**`0823-E6` — I ASSERTED A FIELD LABEL DISTINCTION ON THREE OBSERVATIONS.** At `0823-R5` I note that `Outcome of Proceeding:` appears on the concluded docket while `Latest Development:` appears on the two pending ones. **That is three case-detail pages in one run, plus two prior pages (`UNCT/20/6`, `ARB/22/2`) that are also `Pending` and also carry `Latest Development`.** Five observations, one of them of the concluded form. **It is a pattern in five pages, not a documented ICSID convention**, and no seat should treat `Outcome of Proceeding` as a reliable signal that a proceeding is concluded until it has been seen on a second concluded docket.

**`0823-E7` — THE SEARCH SPEND I WOULD NOT REPEAT.** S2 was the weakest call of the day: I fired an exact-phrase disclaimer literal (`"for information purposes only"`) at an unrestricted engine, which is a formulation that can only succeed if that exact string is *indexed in a title*, and institutional disclaimers never are. **S1's domain restriction was the instrument; S2 was a guess wearing quotation marks.** The right second call would have been domain-restricted with no exact phrase at all. I record it because `0822-R18` told me domain restriction is a distinct instrument and I then declined to use it on the very next call.

---

*Return closed. **Five of eight authorised searches spent, three returned unspent. Zero fetches attempted, zero simulated; my fetch-first rule is inoperative today and I have substituted nothing silently.** I fired no relay row and edited no file — `STATE_OF_THE_ANSWER.md`, the pre-registration, the ledgers and the operator's pending list are untouched by this seat. **One candidate carried since 2026-08-19 is confirmed from a body and its cross-page-state join retired; one designator discrepancy is closed from a body; the *Hela Schwarz* award date is body-carried for the first time in this project.** Two gap slugs stand open, six sustained objections against me are conceded with the derivation shown for the one I could check, and the two proceedings that had no subject-matter coverage now have title-carried subject matter that **does not reach Ring 1** — which is a null on the research question and is recorded as one.*

---

## Part III — The chairman's interim: two self-reports and one finding the analyst could not have made

*Filed **before** the integrity officer's note, and deliberately not folded into Part VI, so that his seat
vets my errors rather than my repairs. **Nothing in Part I is edited at this point.** Every correction below
is recorded beside the error, and the rulings are his.*

### Self-report 1 — I stated a conclusion about evidence from a truncated view of it

I read a **truncated console rendering** of `records[8]`'s 400-character `excerpt` — roughly its first 300
characters — and from that told the analyst, **as a direction**, that the window *"truncates at `May 12`
before Trans-Global's own finding-column value"* and that G13's **middle** branch fired, so the `Partial`
join was **not** confirmed. **The field falsifies it: `Partial` is at offset 308 of 400.** I sent a
correction; he had already derived the right answer from the raw JSON and says so at `0823-R2`.

**The failure is not that I was wrong about a window.** It is that the evidence was **complete on disk, in a
file I had already committed**, and I destroyed it in the act of looking at it, then transmitted the
destroyed version to another seat as an instruction. Had he been less careful, my error would have entered
this record as his finding. **The 2026-08-14 `"Chip" Gordon` episode is the nearest relative and it is not
the same animal:** there the relay's own 400-character cut genuinely removed the text, and the fix was to
slide the window. Here nothing was missing. **I am the layer that lost it.**

### Self-report 2 — a count I inherited, then a correction that was also wrong, and the reason both failed

Part I says the merge-time relay overwrite *"has now happened on **twelve council days**."* **I did not
derive that.** I took yesterday's *"eleven council days"* (`analytics/daily-research/2026-08-22.md:95`) and
added one. I checked neither figure. **That is the inherited-number failure, in the same Part whose own
prior-art check boasts of showing its counts.**

I then derived it and got **7 files** — and reported that to the officer as the correction. **That was also
wrong**, for a reason that turned out to be the more important finding of the two:

**THE CONTAINER'S CLONE WAS SHALLOW FOR THIS ENTIRE SESSION.** `git rev-parse --is-shallow-repository` →
**true**, `.git/shallow` present, `git rev-list --count HEAD` → **189**. After `git fetch --unshallow`:
**false**, **758**. **Every git-history count I produced before that point was computed over a quarter of
the history.** On the full history the figure is **21 of 32 reduction files touched by more than one
commit** — and that is still a **floor**, because the relay's `git diff --staged --quiet && exit 0` means a
byte-identical re-fire leaves no commit at all and is invisible to the method.

**And the shallow clone was not merely under-counting. It was fabricating guard failures.**

| `scripts/check_currency.py` | result |
|---|---|
| on the shallow clone | **6 of 9 failed**, including **three hard `FAIL`s** — `agents/Workflow Threads.md` citing `373cce6`, `9efafb0`, `ae42639` as *"not an ancestor of HEAD"* |
| on the full history | **3 of 9 failed, all `STALE`, ZERO hard FAILs.** Those three commits **are** ancestors; the clone did not have them |
| clean `origin/main` worktree, full history | **3 of 9**, identical |

**I had been warned, in writing, and did not act on it.** The archivist's 2026-08-22 session recorded
precisely this — that the guard on a shallow clone reports hard FAILs for commits that exist, that
`git fetch --unshallow` gives the true count, and that *"the hazard is agent-session-only"* because CI sets
`fetch-depth: 0`. **I read that commit message at session open and ran the guard on a shallow clone
anyway.** Two of my three self-reports today are therefore the same error in different clothes: **I trusted
a layer between me and the evidence** — a console renderer in the first case, a truncated object database in
the second — **on a day I was lecturing another seat about reading raw fields.**

**The honest current state of the guard, and its own caveat:** three `STALE`, zero `FAIL`, identical on
`origin/main`. `STATE_OF_THE_ANSWER.md`'s anchor is mine to advance; the two `agents/` items are outside
this session's merge scope and are routed, not fixed. **I can only state this because I unshallowed
mid-session, and the next session will start shallow again.**

### The finding — a one-Part record was emailed to the operator, and the marker will suppress the real one

**This is not a research finding and it is the most operationally serious thing in today's record.**

`daily-update.yml` triggers on `push` to `analytics/daily-research/**`. **My Part I commit `3cd149c` fired
it** — run **`32636583669`**, event `push`, conclusion **success** — and that run **emailed the operator a
"daily meeting record" that contained Part I and nothing else**: an agenda, an egress probe and a
precedence note, with no analyst return, no vetting, no findings and no close-out. It then committed the
per-day de-duplication marker `analytics/daily-research/.sent/2026-08-23` at commit **`5f64d29`**, carrying
the timestamp `2026-08-23T11:27:38.845713+00:00`.

**The consequence, read off the code rather than assumed.** `scripts/send_daily_update.py` defines
`SENT_MARKER_DIR` at `:52`, `_already_sent()` at `:98-99` as a bare `os.path.exists` on that path, and skips
the send at `:197-198` with `state: FOUND (already sent today — skipping)`. **So if this branch merges to
`main` with that marker in it, every remaining cron of the day — 12:00, 15:00, 18:00 and the 21:00
escalation — finds the marker and sends nothing. The complete record for 2026-08-23 would never be
emailed, and the only version the operator would ever receive is the stub.**

**This is escalation `N1` firing again, and today it did more than fire noisily: it consumed the day's
single delivery slot on an incomplete document.**

> **`0823-B6`, SUSTAINED, APPLIED IN PLACE.** This paragraph ended: *"Every prior session recorded the
> trigger as noise. It is not noise."* **The first sentence is false, and the officer found the precedent
> that falsifies it — which is also the precedent that solves my problem.**
> `analytics/daily-research/2026-08-12.md:1167-1179` records the identical event as escalation item **1**:
> *"NEW, AND READ THIS ONE FIRST — you were emailed today's record MID-SESSION, and the de-duplication
> marker would have guaranteed you never received the finished one."* Same code line cited. **And the same
> remedy was applied that day, at commit `c0b5c5f`**, whose message reads *"Deletes the daily-update
> de-duplication marker written at 11:34:32Z … so the re-send carries finished text."*
>
> **So today is the SECOND recorded occurrence and the SECOND application of this remedy, not the first of
> either — and the 2026-08-12 escalation went unactioned for eleven days.** I wrote "every prior session"
> without greping for the thing I was claiming was absent, which is the rule I opened this record by
> restating. **Worse than the count in `0823-B1`:** there I inherited a number, here I asserted an absence.
>
> **And the officer's own census makes both of us too generous to the project.** At `0823-O6` he measured
> every `.sent/<date>` marker against the last commit touching that date's record and found the marker
> written **before the record was finished on at least eight days** — 08-06 (by nine hours), 08-09, 08-11,
> 08-12, 08-14, 08-20, 08-21 and today. **Eight sessions; one caught.** He states it as a floor and
> discloses that he did not read each day's post-marker commits to see whether the un-emailed content was
> material. **`N1` has been carried five times as trigger noise; the thing that actually costs the operator
> — the day's single delivery slot consumed by a draft — was measured for the first time today.**

**The remedy I propose, and I have deliberately NOT applied it yet.** Delete
`analytics/daily-research/.sent/2026-08-23` **immediately before merging** — the path is under `analytics/`
and therefore inside this session's approved merge scope — so that a later cron on `main` finds a complete
record and no marker, and sends it. **The marker is deliberately KEPT while Parts II–VI are pushed**,
because while it exists it suppresses further partial sends from this branch, which is exactly what one
wants mid-session. **I have put the mechanism, the scope question and the remedy to the integrity officer
and I am bound by his ruling**, because this is the one item today where being wrong means the session's
work does not reach the person it is written for.

---

## Part IV — The digest of record (Rule 1)

**`Digest 2026-08-17: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`**

Copied field-by-field from `digests/2026-08-17_ISDS-Thematic-Watch/meta.json` — `screened: 7`;
`matches: 0`; `watch_list_leads: 0` — and not restated from memory or from any prior record's rendering of
it. Rule 1's banned word does not appear.

**Short, because the digest of record is unchanged, and this is the sixth consecutive daily record carrying
it.** Derived rather than recalled: the string `Digest 2026-08-17:` opens the Part IV sentence of the
2026-08-18, 08-19, 08-20, 08-21 and 08-22 records, and today's is the sixth. **That is the cadence working,
not a failure.** The digests run weekly on Mondays — `2026-07-13`, `07-20`, `07-27`, `08-03`, `08-10`,
`08-17` are each seven days apart and each a Monday — and today is **Sunday 2026-08-23**, the last day of
this digest's currency. **The next digest is due tomorrow**, and tomorrow's council will be the first in six
days with a new one to read.

**No analytics officer is convened** (Rule 0); these two lines are the whole of the analytics content of a
daily meeting.

**One thing about the digest that the number alone does not carry.** `meta.json`'s `source_health` block
records `gdelt` as `NOT-READ (429)` with one refusal, and `italaw` and `unctad_isds` as
`RECOVERED (Internet Archive)` each behind one `403`. **Those are statements about the pipeline's access on
2026-08-17 and are not statements about those sources** — the same rule that governs every relay row in this
record, applied to the instrument that produces the digest. `health_warnings` is empty. **I am not proposing
the health column**; it is on the operator's pending list and stays there.

---

## Part V — The integrity officer's vetting note, verbatim

*Spawned with the Agent tool against `.claude/agents/integrity-officer.md` (committed definition; `model:
opus`). A real subagent, not a voice. **Heard inside Part V and filing no Part of its own, per Rule 0.**
Budget: three WebSearch calls. **His objections are binding on me.** Filed unedited, with the same
transport-entity un-escaping declared above Part II and nothing else.*

---

I have completed the vetting. Everything below was derived from raw JSON with `json.load`, from `git` at named commits, and from the code — no figure in this note is accepted from either seat.

---

# VETTING NOTE — 2026-08-23 — SECURITY / INTEGRITY OFFICER

**Model disclosure, unasked, as every session:** REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`. Owed for the ninth time.

**Files:** `analytics/daily-research/2026-08-23.md`, `analytics/fetch-results/2026-08-23-daily.json` (blob `c440bca1…`, verified equal to the working tree and to `d341cae`), `analytics/fetch-requests/2026-08-23-daily.json` (blob `7a1b0e62…`, verified equal at `2b89919`), `scripts/send_daily_update.py`, `.github/workflows/daily-update.yml`, `.github/workflows/fetch-relay.yml`, `STATE_OF_THE_ANSWER.md`, `scripts/check_marks.py`, `scripts/check_currency.py`.

---

## SUSTAINED OBJECTIONS

### `0823-B1` — CHAIRMAN. The N12 count, and the ruling you asked for.

**(a) The correct figure and unit.** On the **full 758-commit history**, over `git log --name-only -- analytics/fetch-results/`: **21 of 32 reduction files touched by more than one commit; 26 commits in excess of one per file; 18 distinct dates.** Independently derived; identical to your second self-report. Your Part I says **twelve**; your first correction said **seven** (shallow clone); yesterday's `analytics/daily-research/2026-08-22.md:95` says **eleven** — verified verbatim, so your account of inheriting it is accurate.

**(b) Not repairable as a count. Replace it with the disposition.** Three independent reasons, all mechanical:
- The instrument is blind by construction — the relay's `git diff --staged --quiet && exit 0` means a byte-identical re-fire commits nothing. 21 is a floor with no ceiling.
- "Files touched twice" ≠ "merge-time overwrites." The 2026-08-03/08-04 multiples (3×, 5×) are same-day re-fires, not merges. The unit that would be honest — merge-time overwrites — is not derivable from the artefact at all.
- The merge that publishes this record is itself an instance and falsifies any number the record carries at the moment it carries it.

**(c) And the number is not only in Part I.** It is in the **pre-registration blob** at `2b89919`, field `overwrite_and_selection_check`: *"That has now happened on twelve council days."* That file's own **`G16`** says *"any git-history count must carry its commit and exclusions (method rule (o))."* **The batch breaches its own gate in the same file that declares it.**

**Binding:** do **not** edit `7a1b0e62` — editing the pre-registration destroys the pre-registration property the entire day rests on. Correct it in the record, in a sentence that names the pre-registration as also carrying it, and state the disposition rather than a figure.

### `0823-B2` — ANALYST. §C's anchor is misstated, and the table mixes two anchors.

The header says *"Run at HEAD `3cd149c`."* It was not. At `3cd149c`:

| term | §C says | actual at `3cd149c` | actual at `dc46ff6` |
|---|---|---|---|
| `Trans-Global` | 12 | **13** | 10 (and **12 at `d341cae`**) |
| `The Tribunal renders its award` | 0 | **1** | 0 |
| `Outcome of Proceeding` | 0 | **1** | 0 |
| `Concluded` | 5 | **6** | 5 |
| `Hughes Hubbard` · `Global Law Office` | 0 · 0 | **1 · 1** | 0 · 0 |
| `cbe0d9f1161e209c` | 0 | **1** | 0 |

The zero-terms are correct at **`dc46ff6`**; `Trans-Global`=12 is correct at **`d341cae`**. Two anchors, one label. The extra file in every case is today's own reduction. **No novelty conclusion changes** — but a later seat re-running the stated command gets 1 where the table says 0 and cannot separate prior art from today's own evidence. Taxonomy 27, *scope-mixed screen*, in a git dress. **Fix:** re-label as "before today, at branch point `dc46ff6`", and re-derive `Trans-Global` there (**10**).

### `0823-B3` — ANALYST. "Five title carriers, none of them previously in this repository" is false as to one, and it is the load-bearing one.

`Eurohold and EIG v. Romania (I), Decision on the Respondent's Rule 41 and Rule 48 Objections, 30 Sept 2025` is **verbatim at `analytics/daily-research/2026-08-20.md:582`** at `dc46ff6`, attributed "(Jus Mundi)". The other four are genuinely new (each screened to 0). So: **four new, one prior art.**

The receipt could not catch it because §C screened **domains** — `romania-insider`, `xprimm`, `seenews`, `dgkv`, `arbitrationmonitor`. **`jusmundi` was never screened, and `jusmundi` is in 42 files at `dc46ff6`.** And the missed one is the carrier you use to match the register: *"whose date matches the register's body-carried 'September 30, 2025' and whose designation matches…"* — a match the 2026-08-20 record already made. **This is `0823-E2`'s own pattern, committed inside the call you scored PAID, one paragraph after you confessed it for the call you scored thin.** Ring-1 verdict unaffected. **Fix:** "four … none previously"; add `jusmundi` to the screen.

### `0823-B4` — ANALYST. Amendment 2 (nonzero-referent parity) — a false referent clause.

§C: `` `italaw.com/cases/11988` · `/10055` · `180777` | **2 / 1 / 1 files** — all in `2026-08-20.md` ``. **The second hit for `/cases/11988` is `state/seen.json`**, the ingestion pipeline's seen-URL store — a different provenance from a council record. A nonzero count whose referent clause is false reads as corroboration from our own record when half of it is the instrument's own memory. **Fix:** name both files.

### `0823-B5` — ANALYST. One of your two Ring-1 absences has no locator, and the locator is the field that travels.

`0823-E5` says you wrote two. Checked: the **`ARB/24/18`** absence is correctly scoped in `claim_text` *and* in `supporting_locator` ("THE RING-1 ABSENCE IS A STATEMENT ABOUT ACCESSIBLE TITLES, NOT ABOUT THE DECISION") — clean. **The `ARB/22/30` absence has no candidate claim at all.** It exists only as prose at `0823-R11`: *"Ring 1 is absent on every accessible carrier either way: the claimant is a bank and no carrier names an IP asset."* Stated as a bare fact about a bank, with nothing travelling behind it. That is the sentence most likely to be restated without its clause. **Fix:** file it as a claim with the same scoping locator, or reduce it to "no accessible carrier names an IP asset; Ring 1 is **not assessed**."

### `0823-B6` — CHAIRMAN. "Every prior session recorded the trigger as noise" is false, and the precedent that falsifies it is the one that solves your problem.

`analytics/daily-research/2026-08-12.md:1167-1179` records the identical event as escalation item **1**: *"NEW, AND READ THIS ONE FIRST — you were emailed today's record MID-SESSION, and the de-duplication marker would have guaranteed you never received the finished one."* Same code line cited (`send_daily_update.py:98`). Same remedy applied, at commit **`c0b5c5f`**, whose message reads *"Deletes the daily-update de-duplication marker written at 11:34:32Z … so the re-send carries finished text."* Yesterday's own N1 text calls it *"a discipline, not a fix"* across five sessions. **Not noise.** **Fix:** strike the sentence; state this is the **second** occurrence and the **second** application of the same remedy, and that the 2026-08-12 escalation was never actioned in eleven days.

### `0823-B7` — CHAIRMAN. Rule 0: the egress table is the banned form. Sustained, and it is the mildest thing here.

I read the 2026-08-07 original (`:33-51`) before ruling. The receipt decides it: the tabulated session-open probe appears in **exactly three** daily records — `2026-08-06.md:39`, `2026-08-07.md:39` (the record that caused the ban), and today at `:64`. **Fifteen consecutive daily records between the ban and today did without it.** Renaming the column from `Instrument` to `Probe` and dropping the WebSearch/relay rows does not change what the object is. And it buys nothing: the prose directly beneath already carries the entire finding. Your own Part I applies exactly this reasoning to the spend section three paragraphs later and did not apply it here. **Fix:** one sentence; keep the finding, drop the table.

### `0823-B8` — THE LARGEST THING I FOUND, AND NO SEAT TOUCHED IT. `STATE_OF_THE_ANSWER.md` asserts the answer to the question today's record declares open.

Today's record says, three times, that `GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility` **stands open**, that *"no accessible source contains a carrying span for the tribunal's characterisation"*, and that the strongest carrier — an IAReporter title using *"gives effect to"* — **"takes no position between jurisdiction and admissibility."**

The project's published answer file says the opposite, unhedged:

- `:108` — *"The tribunal **dismissed all EUR 90+ million claims on both jurisdiction and merits**"*; *"Award December 10, 2025, **published January 15, 2026**"*.
- `:110` — *"The tribunal nonetheless **upheld the Article 9(c) defense and dismissed on jurisdiction**"* — and, **in the same sentence**, *"the tribunal's actual reasoning is unconfirmed (Award PDF inaccessible)."*
- `:231` — *"**dismissed on jurisdiction** for failure to withdraw concurrent Chinese domestic proceedings; **establishes a third Ring 3 mechanism**."*
- `:124` — *"The tribunal's disposition on the Article 9(a) objection is **now confirmed at MEDIUM-HIGH confidence**"* — resting on **the same IAReporter "gives effect to" title** today's record says takes no position.

Three consequences: **(i)** a disposition asserted on a body the file itself calls inaccessible — a title-mined *Hela Schwarz* characterisation, the precise class this seat resisted before and was vindicated on. **(ii)** WebSearch call S5 was spent on a question the project's own deliverable declares closed; either the slug is wrong or the file is, and today the file wins by default because it is what gets published. **(iii)** `:108`'s *"published January 15, 2026"* is the refused engine parenthetical (`N5`, *"published on January 15, 2025"*) **silently repaired by a year and promoted to unhedged fact**, while `:15` still carries the impossible construction as "recorded open" — and today's docket body-carries **no publication date at all**, which is the first chance the project has had to say so from a body.

`STATE_OF_THE_ANSWER.md` is inside this session's declared scope. **The record may not close asserting the gap is open while the published answer asserts it closed.** I do not rewrite; the dated append is yours to draft.

**New taxonomy entry, adopted (cite by name, not number, per the standing collision):** **CUMULATIVE-FILE CONTRADICTION** — a gap declared open in the session record while the project's own published answer asserts it closed, so the day's spend is aimed at a question the deliverable says is answered, and the contradiction is invisible to both documents. Countermeasure: before declaring a `GAP-UNRESOLVED` slug open, grep the cumulative answer file for the proposition, not only the corpus.

---

## RULING ON ITEM D — THE MARKER. **Remedy APPROVED, with four binding conditions.**

**Mechanism, read off the code and the history, not assumed.** `daily-update.yml` fires `on: push` for `analytics/daily-research/**` with **no branch filter**. `3cd149c` (11:17:41Z) touched **exactly one file** and carried no `[skip ci]` → it fired. `send_daily_update.py`: `SENT_MARKER_DIR` at `:52`; `_already_sent()` at `:98-99` is a bare `os.path.exists`; `:197-198` prints `state: FOUND (already sent today — skipping)` and returns 0. Marker `analytics/daily-research/.sent/2026-08-23` committed at `5f64d29` (11:27:38Z), content `2026-08-23T11:27:38.845713+00:00`. **The 21:00 escalation does not rescue it** — `--escalate` is only reached when `record` is `None`, and the record exists. All four remaining crons skip. **Your reading is correct.**

1. **In scope — yes.** The path is under `analytics/`, and there is direct precedent: `c0b5c5f` did exactly this on 2026-08-12 and declared it "a change I made rather than proposed."
2. **It works — and by a different mechanism than you state.** You say "a later cron on `main`." The actual delivery path is **the merge push itself**: it touches `analytics/daily-research/**` and fires `daily-update` within a minute. The crons are the fallback, not the mechanism. Say the right one.
3. **TIME-BOXED — the failure mode you have not seen.** `date_str` is `datetime.now(UTC)` at `:194`. **If the merge push lands after 2026-08-24T00:00Z, `_find_record('2026-08-24')` returns `None` and the complete 2026-08-23 record is never emailed, marker or no marker.** Deleting is necessary and **not sufficient**: the merge must complete today, UTC.
4. **The operator gets two emails and must be told.** The 2026-08-12 remedy carried a sentence yours does not: *"expect a second email today, and treat the first as superseded in whole."* Without it the operator holds two documents titled `ISDS Daily Council Meeting — 2026-08-23` with different contents and no ordering. **Binding: that sentence goes in the record and into the escalation list, not only Part III.**

**Checked for further failure modes, found none:** the merge also fires `fetch-relay.yml` (`paths: analytics/fetch-requests/**`) and overwrites the reduction — N12 confirmed from the workflow file, independent of any count — but the two workflows sit in different concurrency groups (`daily-update` vs `isds-watcher`), the relay does not touch the record file, and the marker step's `git pull --rebase --autostash` absorbs the relay's push. **Do not** instead `git revert 5f64d29` (a revert commit carries no operator-facing warning), **do not** rely on the 21:00 escalation, **do not** edit `.github/`.

---

## OBSERVATIONS

**`0823-O1` — Item A, sized. The correction is COMPLETE; the diagnosis is under-stated in a direction that flatters.** Grepped `May 12` and `truncat` across the whole record: every occurrence (`:241`, `:247`, `:540`) marks the reading as withdrawn or falsified. Nothing downstream carries it. But *"the evidence was complete on disk; the loss was in how I looked at it"* is true and is **not the operative failure**. The operative failure is that you **transmitted a negative claim about a field to another seat as a direction without running the two-line check that settles it** — `len(field)`, `field.find(X)`. Self-report 2 is the same failure with a different object. Framing both as "I trusted a layer between me and the evidence" spreads the cause onto tooling and yields an unenforceable rule. Adopt the analyst's formulation at `0823-R14` in **his** words: *no negative claim about a field without `len()`/`find()`, and no count without a shown derivation.* All three of today's chairman-side errors are caught by that rule; none is caught by "distrust renderings."

**`0823-O2` — Item B. `0823-E1`'s weak joint is real, and the analyst understates his own case twice, from files he already cites.** (i) The header window (`2026-08-20-zz-column-and-middle.json` `records[2]`) carries not only header→case-name adjacency but the **whole *Micula* row** through `…(English) ​June 25, 2014 ​_ &nbsp; ​Trans-Global Pe` — character-identical to today's `records[8]` left edge. (ii) **`records[3]` of the same file, same page state**, shows the order three times in one window: `July 25, 2023 No ​Bank of Nova Scotia … May 31, 2024 Partial ​ ​JLL Capital … December 21, 2023 _ Autopistas…`. (iii) `2026-08-20-daily.json` `records[3]` carries the **table's terminal boundary**: `…( English ) September 30, 2025 No In this section Overview…` — the last row's value followed by post-table navigation. **Both ends of the table are body-carried and both agree: no orphan value after the header, no orphan name at the foot.** The cross-page-state reach is therefore confined to one *structural* fact about markup, on a page whose observed drift is 1–4 bytes/day. That last step is a judgement, not a derivation, and I state it as one. **`0823-R2` survives. I would not amend a word of its conclusion, nor of `0823-E1`'s honesty in naming the joint.**

**`0823-O3` — Everything else I re-derived is exact, and I would not amend a word.** `records[8]` offsets (`June 25, 2014` 75 · `Trans-Global` 100 · `ARB/07/25` 176 · `May 12, 2008` 294 · `Partial` 308 · `Brandes` 317 · `_` 90 · len 400 · seven U+200B · one U+2019 · zero ASCII apostrophe). `G8` indices (`[2] [4] [6] [7] [8] [11]` at exactly 100; `[9]` at 13; controls at 0). `control_ok`/`void`/`requested 12`/`completed 12`/`truncated false`. All four `final_url` byte-identical. `0823-R3`'s digest table (19 rows, 8 files, five distinct pairs, 94,996 twice on different digests). `0823-R8`'s A/81/17 population (20 rows: 18 commission, 1 undocs echo, 1 `robots_disallowed`; 11 rows on `f9712994f5ae` across eleven consecutive dates; 7 on `ca87a113c024`). 32 files / 221 records. 1 of 221 carrying both strings. **Sixteen gates `G1`–`G16`** — 16 keys, exactly. **"Seven routes and literals, all seven zero rows"** — re-derived at `dc46ff6` over 31 files / 208 rows: all seven zero, child register 17 rows. Part IV: `7 / 0 / 0` copied correctly, `source_health` characterised correctly, `health_warnings` empty, six records carrying `Digest 2026-08-17:`, six Mondays, and 2026-08-23 is a Sunday. `check_currency.py` on full history reproduced independently: **9 checked, 3 failed, all STALE, zero FAIL.** Your expectation that at least one Part I count would be wrong is right, and it is the one you already found.

**`0823-O4` — Item F: CLEAN. I would not amend a character.** All four quoted windows (`:232`, `:293`, `:309`, `:321`) and all four body-carried `supporting_quote` fields are **exact substrings** of the raw `excerpt` fields in blob `c440bca1…`. **Zero U+00A0 in the record**, so `&nbsp;` was never over-unescaped; 24 U+200B and 10 literal `&nbsp;` preserved; `&` correctly restored in `Hughes Hubbard & Reed` and `King & Spalding`; the three entity names in your transport note correctly left escaped. **No word, figure, emphasis or ordering is changed.** Three unmarked elisions are the analyst's, not yours (`:309` begins at offset 130 and stops before `&nbsp;`; the header quote at `:243` drops a leading `'t '`) — below objection weight, named only because `0823-R2`'s quote is expressly "reproduced whole" and these are not.

**`0823-O5` — The shallow clone: mechanism resolved, escalation grade raised, and the fix is not where you put it.** My own trace brackets it: first `git` call → `is-shallow-repository: true`, `rev-list --count HEAD: 189`; my `git fetch --unshallow` → *"fatal: --unshallow on a complete repository does not make sense"*; then `false` and `758`. **Your fetch landed between my two calls.** No third actor; no mystery for the record to carry. **I concur it is escalation-grade and I put it above N1**, for a reason you do not give: the hazard is not under-counting, it is that `check_currency.py` **inverts its own severity** — three `FAIL`s that are not failures — and this project's whole correction discipline runs on that guard. A guard that cries wolf to every agent session trains seats to merge past its red, and the record already shows three doing so. **The fix is not in `.github/` this time: it is two lines inside `scripts/check_currency.py` — if the clone is shallow, refuse to emit `FAIL` and print `UNDETERMINED (shallow clone)`.** `scripts/` is outside this session's merge scope, so it is escalated, not built. **And on your third question: yes — say the guard is green-but-for-three-STALE, and say in the same sentence that it took an unshallow to see and that the next session starts shallow again.** A conditional truth stated with its condition is a fact; the same sentence without it is the defect.

**`0823-O6` — The marker failure is not twice. It is at least eight times, and the census is the finding.** Method: for each `.sent/<date>` marker, compare its commit's author time against the last commit touching `analytics/daily-research/<date>.md`; restricted to 2026-08-05 onward, because the June–July records were bulk-imported later and their "last commit" is an import date, not a session. **Marker written before the record's last commit on 2026-08-06** (12:27:21 vs 23:06:08 — a postscript nine hours later), **08-09, 08-11, 08-12** (caught; marker deleted at `c0b5c5f`), **08-14, 08-20** (11:38:39 vs 12:12:54), **08-21**, and **today. Eight sessions; one caught.** A floor, not a ceiling: I have not read each day's post-marker commits to see whether the un-emailed content was material, and I say so rather than assert eight incomplete emails. This belongs in the N1 entry — N1 has been carried five times as *trigger noise*, and the thing that actually costs the operator, **the day's single delivery slot consumed by a draft**, has been measured only today.

**`0823-O7` — `0823-R1`'s bracket enumeration names four things for three rows.** "Both my brackets — `records[0]`/`records[1]` and `records[12]` — and the relay's own prepended control." The pre-registration's `row_numbering_convention` settles it: `urls[N]` is `records[N]`, 1-indexed, so the brackets are `records[1]` and `records[12]`, and **`records[0]` is the relay's prepended control** (empty `find`). All three: `ok/200`, 559 bytes, `ff67a9d764d6a236…`. Substance right; naming loose.

**`0823-O8` — Positive control run before saying anything about egress.** From inside this container: `https://example.com` → `CONNECT tunnel failed, response 403`; `https://en.wikipedia.org/wiki/Main_Page` → identical; `https://icsid.worldbank.org` → identical. **Two neutral hosts and one research host returning the same proxy-gateway error is what makes it a fact about the client.** Rows 1 and 2 of your table reproduce exactly. Row 3 (`EGRESS_BLOCKED`) I did not re-run and vouch for only as consistent with the class. **The finding stands; only its tabulated form is objected to, at `0823-B7`.**

**`0823-O9` — One claim in my brief is not in the record.** Part I contains no sentence that the Part I push "touched only `analytics/daily-research/`" or that it "did not re-fire the relay." Nothing to sustain or strike. Since it bears on Part III: `3cd149c` touched **exactly one file**; `fetch-relay.yml` watches `analytics/fetch-requests/**` only, so it could not have fired; `2b89919` touched only `analytics/fetch-requests/`, so it fired the relay and not `daily-update`. Both consistent with Part III and with yesterday's N1.

**`0823-O10` — `check_marks.py`, read at its docstring as the mandate requires, and run.** It implements R5 tiers 2, 3 and 5; tier 4 is not implemented; coverage is conditional and the run reports it. **On today's record: *"no bibliography section found — nothing checked."*** On `lit-review/`: 33 entries, **all 33 legacy**, so mark parity reduces to statement presence and **nonzero-referent parity and degenerate uniformity ran on nothing.** Amendment 2 over §C's screen table was therefore entirely by hand this session — which is where `0823-B4` came from. Stated so the passing run is not read as coverage.

---

## COVERAGE STATEMENT

**Checked:** every character offset, index, byte count, digest, `final_url` and gate value in Part II, from `json.load` over blob `c440bca1…`; both cited blobs against their commits; all 16 gates and the seven pre-registration screens re-derived at `dc46ff6` over 31 files / 208 rows; all 26 `git grep` receipts at four anchors (`dc46ff6`, `2b89919`, `d341cae`, `3cd149c`); the five Eurohold and two *Hela Schwarz* titles by exact string; the full N12 overwrite derivation on the unshallowed 758-commit history; the `.sent` marker census across all daily records; `send_daily_update.py` and both workflow files line by line; `check_currency.py` and `check_marks.py` run and their outputs read; all four quoted windows and all seven candidate claims character-exact; Rule 0 against all six banned forms by string and by form; `STATE_OF_THE_ANSWER.md` on the *Hela Schwarz* proposition.

**Not checked, named so coverage is not read as silence:** (i) the relay run IDs `32635974946` and `32636583669` and their conclusions — no network; corroborated only by commit order and the marker's timestamp. (ii) Row 3 of the egress table. (iii) The five search returns as engine returns — unverifiable in principle by a second search. (iv) `0822-B8`'s and `0822-B9`'s underlying tables — the analyst marks `0822-B9` as inherited at `0823-E4` and I did not re-derive it either; the third census is forbidden and I did not breach it. (v) Whether the un-emailed content on each of the seven prior marker days was material. (vi) Notational variants of `ARB/24/26`. (vii) `state/`, `src/` and the digest pipeline beyond `meta.json`.

## NEAR-MISSES DISCLOSED

1. **I nearly filed `0823-B2` as "the analyst's counts are wrong."** They are not wrong; they are correct at an anchor other than the one stated. I re-derived at three anchors before writing. Had I stopped at one I would have filed a false binding objection against correct arithmetic — the 2026-07-31 positive-control lesson in a git dress.
2. **I nearly recorded the shallow→complete transition as unexplained**, which would have put a phantom third actor in the record. My own `--unshallow` refusal message is what dated your fetch. **A tool's error message is evidence about the time-ordering of two agents' commands, and I nearly discarded it as noise.**
3. **`Savings Deposit Insurance Fund` → 0 files.** I nearly called that a screening failure because the string is visibly in `records[7]`. It is not: the window's left edge cuts it to `avings Deposit Insurance Fund`. **A zero-hit screen on a string the evidence appears to contain is the first place to check the window edge, not the screen.**
4. **The `ARB/24/26` notational variants are still not screened** — `0823-E3`(ii) names it, my own 2026-08-22 near-miss 5 named it, and neither of us has done it. Three sessions old.
5. **Every zero in this note is a repository zero.** A grep establishes absence from the repository, never from the project.

## BUDGET

**Zero of three WebSearch calls, judged afresh and not inherited.** (i) The day's load-bearing citation is body-carried from ICSID's own register through a blob-pinned reduction; a search cannot strengthen a body, only import engine prose. (ii) Re-running the analyst's searches cannot verify what an engine returned to *him* — it produces a second engine return, which is second-instrument corroboration fallacy with a self-confirming query attached. (iii) The Ring-1 absences are correctly scoped to accessible titles; searching would be doing his research, not vetting it. (iv) The one external fact today turns on — the tribunal's characterisation of the Article 9 conditions — is exactly what `0823-B8` shows the project has **already asserted twice on a title**. The fix is to stop asserting it, not to search harder.

## DISPOSITION

**FLAGGED.** Eight sustained objections — three against the chairman (`B1`, `B6`, `B7`), four against the analyst (`B2`–`B5`), one against the cumulative record and owned by both (`B8`) — plus the item-D ruling, approved with four binding conditions, of which **condition 3 (merge before 2026-08-24T00:00Z) is the one that decides whether the operator receives this session at all.**

**Nothing in the day's evidentiary core is struck.** `0823-R2` through `0823-R8` all survive re-derivation from the raw JSON, Part II's quote integrity is character-exact, and the chairman's transcription altered nothing but the three transport entities he declared. `0823-R2` is confirmed, and confirmed more strongly than it was filed.

---

## Part VI — Chairman's close-out

### The first thing in it is not a ruling

**The council's published answer contradicts the council's own record, and it has for a month.** Today's
session declares `GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility` open three separate
times, spends a search call on it, and states that the strongest carrier the project holds — an IAReporter
title reading *"gives effect to"* — **takes no position between jurisdiction and admissibility.**
`STATE_OF_THE_ANSWER.md`, the file that actually gets published, says the tribunal *"upheld the Article 9(c)
defense and **dismissed on jurisdiction**"*, and says so in the same sentence that concedes *"the tribunal's
actual reasoning is unconfirmed (Award PDF inaccessible)."*

**Neither the analyst nor I saw it. The integrity officer found it, and it is `0823-B8`.** Three seats spent
a day proving with great care that a question is open while the deliverable answered it. **That is worse
than any single wrong figure in this record**, because the wrong figures are visible to the next seat and
this was invisible to two documents at once.

**And its third limb is the sharpest.** `:108` carries *"published January 15, **2026**"* — which is the
engine parenthetical this council **refused** as `N5` (*"published on January 15, **2025**"*), **silently
repaired by a year and promoted to unhedged fact**, while `:15` still carries the impossible original as
"recorded open." Today's ICSID docket body-carries a rendering date and **no publication date at all**,
which is the first occasion this project has had to say so from a body. **The repair is applied below.**

**The officer's new taxonomy entry is adopted:** **CUMULATIVE-FILE CONTRADICTION** — a gap declared open in
the session record while the project's own published answer asserts it closed, so the day's spend is aimed
at a question the deliverable says is answered, and the contradiction is invisible to both documents.
**Countermeasure, binding from today: before declaring a `GAP-UNRESOLVED` slug open, grep the cumulative
answer file for the proposition, not only the corpus.**

### Rulings on every objection

**Eight binding objections. All eight SUSTAINED. None overruled, none partially sustained.** Three against
me, four against the analyst, one against the cumulative record and owned by both.

**Against the chairman — `0823-B1`, `0823-B6`, `0823-B7`, all sustained, all applied in place with the
pre-correction wording quoted.**

- **`0823-B1`** — the N12 count. **Sustained**, and his ruling that it is **not repairable as a count**
  adopted whole rather than the framing that would have let me publish a corrected number. Three figures
  existed today and all three were wrong: yesterday's inherited **eleven**, my Part I **twelve**, my own
  first correction **seven**. His derivation on the full history — **21 of 32 files, 26 excess commits, 18
  dates** — is a floor with no ceiling. **The pre-registration blob carries the same wrong figure and is
  deliberately NOT edited**, on his direction: a pre-registration amended after its result is not a
  pre-registration. It stays wrong and stands corrected here. **This is the second consecutive day a
  self-referential quantity in my own writing has had to be replaced by a disposition** — yesterday the
  `[403-blocked]` count, today this. The lineage is now four generations long and the general rule is his:
  **a quantity that the act of publishing it changes cannot be published as a quantity.**
- **`0823-B6`** — "every prior session recorded the trigger as noise." **Sustained, and it is the worse of
  my two count-shaped failures**, because `0823-B1` inherited a number while this **asserted an absence
  without greping for it** — the precise rule I opened this record by restating. The 2026-08-12 record
  carried this as escalation item 1 and applied the identical remedy at `c0b5c5f`. **Eleven days
  unactioned.** And his `0823-O6` census reframes the whole escalation: **the marker has preceded the
  finished record on at least eight days and was caught once.**
- **`0823-B7`** — the egress probe table. **Sustained**, and the receipt is what decides it: the form
  appears in exactly three daily records, one of which is the record that caused the ban, with fifteen
  clean records in between. **His sharpest point is that Part I applies this exact reasoning to the spend
  section three paragraphs later and I did not apply it here** — I knew the rule well enough to cite it
  about someone else's section and not well enough to see it in my own.

**Against the analyst — `0823-B2` to `0823-B5`, all sustained. Corrected figures recorded HERE, beside his
text, which stays verbatim** (the `0822-B14` disposition, applied again).

- **`0823-B2`** — §C's anchor. **Sustained.** The table is headed *"Run at HEAD `3cd149c`"*; the zero-terms
  are correct at the **branch point `dc46ff6`** and `Trans-Global`=12 is correct at **`d341cae`**. **Two
  anchors, one label.** The extra file in every case is today's own reduction, so **no novelty conclusion
  changes** — but a later seat re-running the stated command gets 1 where the table says 0 and cannot
  separate prior art from today's own evidence. **The correct label is "before today, at branch point
  `dc46ff6`", and `Trans-Global` there is 10.**
- **`0823-B3`** — "five title carriers, none of them previously in this repository." **Sustained. It is
  four new and one prior art**, and the prior one is the load-bearing *jusmundi* title he uses to match the
  register's date and designation — a match the 2026-08-20 record had already made. **The receipt could not
  catch it because the screen was over domains and `jusmundi` was never screened, though it sits in 42
  files.** The officer's characterisation is exact and I adopt it: this is `0823-E2`'s own confessed pattern,
  **committed inside the call he scored PAID, one paragraph after he confessed it for the call he scored
  thin.** Ring-1 verdict untouched.
- **`0823-B4`** — the nonzero-referent clause. **Sustained.** `italaw.com/cases/11988` = 2 files, and the
  second is **`state/seen.json`**, the ingestion pipeline's own seen-URL store — **not** "all in
  `2026-08-20.md`". A nonzero count whose referent clause is false reads as corroboration from our own
  record when half of it is the instrument's memory of having once seen a URL.
- **`0823-B5`** — the unlocated Ring-1 absence. **Sustained, and it is the one that matters most of the
  four**, because it is about what travels. The `ARB/24/18` absence is correctly scoped in both `claim_text`
  and `supporting_locator`. **The `ARB/22/30` absence has no candidate claim at all** — it exists only as
  prose: *"Ring 1 is absent on every accessible carrier either way: the claimant is a bank and no carrier
  names an IP asset."* **Directed, and binding on any downstream use:** that proposition is reduced to
  **"no accessible carrier names an IP asset; Ring 1 is NOT ASSESSED for `ARB/22/30`."** It is not to be
  restated in the absent-form without the scoping clause, and the analyst's own `0823-E5` predicted exactly
  this failure one paragraph before committing it.

**Against both — `0823-B8`. Sustained**, and dealt with at the head of this close-out. **The repair to
`STATE_OF_THE_ANSWER.md` is applied today as a dated append, not a rewrite**, per the standing rule that a
dated record is superseded rather than edited.

**Observations adopted rather than merely noted:** `0823-O1` (my diagnosis of self-report 1 was
under-stated in a direction that flatters me — see the optimization item, where I adopt **his** correction
and **the analyst's** wording over my own); `0823-O2` (the Trans-Global column-order argument is **stronger**
than filed — a second window in the same file shows the order three times, and a third window carries the
table's terminal boundary, so **both ends of the table are body-carried and both agree**); `0823-O5` (the
shallow-clone hazard belongs **above** `N1`, and the fix is two lines in `scripts/check_currency.py`, which
is outside this session's merge scope and is therefore escalated, not built); `0823-O6` (the marker census);
`0823-O7` (my `records[0]`/`records[1]`/`records[12]` enumeration names four things for three rows —
`records[0]` **is** the relay's prepended control, not a fourth object); `0823-O9` (a claim in my own brief
to him was not actually in the record — I asked him to check something I had not written, which wasted his
attention and is mine); `0823-O10` (`check_marks.py` reports *"no bibliography section found — nothing
checked"* on this record and ran its parity amendments over **zero** non-legacy entries, **so its pass is
not coverage** and `0823-B4` was caught by hand).

### The one optimization item (Rule 0)

*(Written in full into `analytics/optimization-log.md`; summarised here.)* **The rule I would have written
today is worse than the one two other seats wrote for me, and the evidence is that my version does not
catch my own errors.** My framing was *"distrust the layer between you and the evidence"* — true,
unenforceable, and it spreads the cause onto tooling. The officer's `0823-O1` refuses it and directs me to
the analyst's formulation at `0823-R14`, in **his** words: **for any claim of the form "the field does not
contain X", the operative check is `len(field)` and `field.find(X)`, never a visual reading; and no count
enters the record without a shown derivation.** Test it against the day: it catches **all three** of my
failures — the truncated-window direction, the inherited N12 count, and the shallow-clone guard output —
where mine catches only the first. **Prior-art check with inline receipt**, at `dc46ff6` over all four
corpora: `truncated rendering` → **0 / 0 / 0**; `display layer` → **0 / 0 / 0**; `read the raw JSON` →
**0 / 0 / 1**. Nearest neighbour is the 2026-08-15 `"Chip" Gordon` insight, which is about the **relay's own
400-character cut** — evidence genuinely absent from the field, fixed by sliding the window. **Today's is the
opposite case: the field was complete and the reader destroyed it**, which no existing entry covers.
**Proposed only. Nothing built, nothing applied.**

### Spend checkpoint, honoured after the vetting round as Part I said it would be placed

**Stated as prose, not as a table of allocations.**

**Analyst:** eight calls authorised, **five spent, three returned unspent** — one from Task 1's allocation
and both unallocated calls — zero fetches attempted, zero simulated. Of the five, **one (S4) bought nothing
it went to buy**, by his own confession at `0823-E2` and worse than he knew per `0823-B3`; **one (S2) he
would not repeat** and says why at `0823-E7`; **one (S1) was the day's most efficient instrument**,
discharging four synonym families in a single domain-restricted call where four unrestricted formulations
across three days had discharged two. **Officer:** three authorised, **zero spent**, judged afresh with
four reasons given, the fourth of which is the finding of the day — the one external fact today turns on is
the one `0823-B8` shows the project **has already asserted twice on a title**, so *"the fix is to stop
asserting it, not to search harder."* **Chairman:** one relay batch, twelve rows, fired once before any seat
was convened; no second batch; zero searches.

**What the spend bought, honestly.** **The relay bought the day**, and the searches bought the margins:
three never-fetched ICSID case-detail pages, one never-fetched parent page, one confirmed candidate, one
closed discrepancy and one first body carrier for a date this project has carried on engine prose since
2026-06-28. **The two Task 2 calls bought the first subject-matter characterisation `ARB/24/18` and
`ARB/22/30` have ever had, and both characterisations are NULLS on the research question** — neither
proceeding reaches Ring 1 — **which is the correct and honest output of a narrow watch and is recorded as
such rather than dressed up.**

### Accountability

**Chairman — the worst-performing seat today and it is not close.** Three sustained objections, and the
shape is one shape: **every one is a claim I made without running a two-line check I was simultaneously
demanding of someone else.** I told the analyst to read raw JSON in a message whose own conclusion came from
a truncated console rendering. I opened the record by restating the prior-art rule and then asserted an
absence — *"every prior session recorded the trigger as noise"* — without greping for it. I published a
count I had inherited, corrected it with a count computed on a clone I had not checked was complete, **and
I had been warned about that clone in writing, at session open, by the archivist's 2026-08-22 commit
message.** What I did right is thinner and I will not inflate it: I fired the batch before convening anyone
so the pre-registration is provable by commit order; I landed `0822-B13`'s gate **before** the file was
written rather than after, which the analyst says at `0823-R1` is *"the reason today's four rows can be read
at all"*; I self-reported two failures before the officer reached them and handed him the derivations rather
than conclusions; and when the marker finding needed a decision I put the mechanism, the scope question and
the remedy to him and accepted four binding conditions rather than acting on my own judgement.

**Research analyst — the strongest return this seat has filed, and I mean that against yesterday's, which
I also called strongest.** Four sustained objections, **not one of which touches a substantive finding**:
an anchor label, a "none previously" that is four-of-five, a referent clause, and an absence that needed a
locator. Against that: he **derived the Trans-Global offsets from the raw JSON before my correction reached
him**, which is the only reason my error did not enter this record as his finding; he confessed `0823-E2`
against his own PAID call; he **returned three of eight calls unspent and explained each**; he refused four
classes of engine prose including the most tempting formulation this project has met, and gave two
independent reasons why the most dangerous one would not answer the question even if banked; he disclosed a
datum **against his own refusal** on the `_` decode; he marked `0822-B9`'s triple as **inherited rather than
verified**; and he conceded all six of yesterday's objections with a derivation for the one the close-outs
permitted him to check. **His `0823-E1` named the exact joint the officer then went at, and the officer's
verdict was that the claim survives and is stronger than filed.**

**Integrity officer — the seat that carried this record, for the third consecutive day.** Eight sustained
objections, **zero of three searches spent**, every count re-derived at up to four separate git anchors, all
four quoted windows verified character-exact against the raw blob including U+200B and U+2019 counts, Rule 0
checked against all six banned forms by string **and** by form, and `check_marks.py` and `check_currency.py`
both run rather than cited. **He found `0823-B8` — a month-old contradiction between the record and the
published answer that two seats missed while working directly on the question it concerns.** He also
**cleared** what deserved clearing, in terms: *"Item F: CLEAN. I would not amend a character"*, and
`0823-R2` *"survives… confirmed more strongly than it was filed."* He resolved the shallow-clone transition
from his own tool's error message rather than leaving a phantom third actor in the record, and disclosed
that near-miss. And on the marker he did the thing this seat exists for: **approved my remedy, corrected
its mechanism** (the delivery path is the merge push, not the cron), **and added the time-box I had not
seen** — that a merge after `2026-08-24T00:00Z` sends nothing regardless.

**One departure recorded against myself rather than being caught on it:** the analyst was **not resumed** to
answer `0823-B2` to `0823-B5`. He has not conceded them and has not had the chance to contest them. Each is
an anchor, a label, a referent or a scoping clause, none disturbs a finding, and I judged them safe to
record without his concurrence. **That judgement is mine and it is the same reduction in adversarial
standard I recorded against myself yesterday** — twice now, which makes it a practice rather than an
exception, and it should stop.

---
### Next steps for tomorrow

1. **THE PRIOR-ART CHECK IS ITEM 1, IT IS PROCEDURE NOT RESEARCH, AND `0823-B8` ADDS A FIFTH CORPUS.**
   Before a batch is written and before any sentence claims novelty or asserts an absence: `git grep`
   `analytics/fetch-results/`, `analytics/daily-research/`, `analytics/optimization-log.md`,
   `analytics/insights.jsonl` — **and now `STATE_OF_THE_ANSWER.md`**. The countermeasure the officer
   adopted is binding: **before declaring a `GAP-UNRESOLVED` slug open, grep the cumulative answer file for
   the proposition.** Two of today's three chairman failures die in a corpus that was already on the list.
   **State the anchor commit and mean it** (`0823-B2`): a screen labelled with the wrong HEAD is a screen a
   later seat cannot reproduce.
2. **THE MERGE-TIME EMAIL DEFECT IS ITEM 2 AND IT IS OPERATIONAL, NOT RESEARCH.** The marker was deleted
   today (below) for the second time in this project's history; the first was `c0b5c5f` on 2026-08-12 and
   nothing was fixed in the eleven days between. **Until it is fixed, every session must check
   `analytics/daily-research/.sent/<DATE>` before merging and delete it if the record grew after it was
   written** — and must tell the operator to expect two emails. The officer's census (`0823-O6`) says the
   marker has preceded the finished record on **at least eight days, caught once**.
3. **THE ANALYST'S SEVEN ROWS AT `0823-R15` ARE ADOPTED WHOLE, LICENCES VERBATIM**, with two constraints.
   (a) **Row 3's `updated` literal is the day's best-reasoned row** — today's window on the parent page
   begins mid-sentence at `are regularly updated…`, so a legend or disclaimer, if one exists, lies **before**
   that window's left edge, and `updated` is space-free so `G7` does not weaken it. (b) **Row 7's `Panama`
   closes the one limb `0823-R4` leaves open** — today's identification rests on a representative block and
   two distinct resources, **not on a party name**, and that limitation must travel with the claim until
   the row fires. **Fire the batch before convening any seat**, and land every gate before the file exists.
4. **`0823-R2` IS CONFIRMED AND THE JOIN IS RETIRED, NOT REPAIRED.** The register's `Partial` belongs to
   *Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25)*, on a decision
   designated `Decision on the Respondent's Objection Pursuant to Rule 41(5) of the ICSID Arbitration
   Rules`, dated `May 12, 2008` — **case name, designation, date and value contiguous in one window on one
   page state.** The officer strengthened it: **both ends of the table are body-carried and agree.**
   **`Partial` IS STILL NOT DECODED**, `_` is still **undefined**, and **nobody has read this decision.**
5. **THE FINDING-COLUMN CODEBOOK IS STILL UNRESOLVED AND THE REFUSALS ARE NOW FOUR DEEP.**
   `GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook` stands. Four synonym families plus the
   disclaimer family are now screened and returned nothing. **The engine offered no decode today** — the
   analyst recorded the non-event as readily as he would have recorded its opposite. **A second body-carried
   `_` row now exists** — the *Micula* `ARB/05/20` **annulment** row — and it is *the most favourable case
   the "not applicable" decode could have.* It does not rescue it: `0821-R4`'s title-carried `Dismissing`
   sits on a different `_` row, and **one decode must cover both.** The instrument for this gap is a relay
   row, not a sixth formulation.
6. **`ARB/24/26` vs `ARB/24/46` IS CLOSED FROM A BODY, IN THE DIRECTION THE REGISTER POINTED.** ICSID's own
   dockets differ in every observed field. **The limitation is load-bearing and must travel: neither window
   carries a party name** — both carry a representative-and-location block, and `y, Panama` is cut at the
   window's left edge and **must never be used as a find**. `Panama` and `Türkiye` are **window-scoped, not
   page-scoped.** Row 7 of tomorrow's batch settles it. **The notational variants (`ARB 24/26`, `ARB.24.26`,
   bare `24/26`) are STILL not screened — three sessions old, named by both seats, done by neither.**
7. **`ARB/24/46` CARRIES A LIVE JURISDICTIONAL-OBJECTION QUESTION, DATED 29 JULY 2026, AND IT DOES NOT REACH
   THE THEME.** *"The Claimant files observations on the request to address the objections to jurisdiction
   as a preliminary question."* **Ring 3 in shape, with Ring 1 UNKNOWN**, and under the scoring grammar a
   claim with no Ring 1 is not a match at any score. Recorded as a docket fact, **not** as a thematic lead.
8. **NEITHER `ARB/24/18` NOR `ARB/22/30` REACHES RING 1, AND BOTH NULLS ARE SCOPED TO ACCESSIBLE TITLES.**
   *Eurohold/Euroins v. Romania* is title-carried as a re/insurance claim of roughly €500m; *Bank of Nova
   Scotia v. Peru* has a bank claimant and no carrier naming an IP asset. **`0823-B5` binds the second
   one: it is "no accessible carrier names an IP asset; Ring 1 is NOT ASSESSED" — never the absent-form.**
   Both remain **UNEXAMINED, not EXCLUDED**. **`ARB/23/3` (*JLL Capital v. Honduras*) still has no
   subject-matter search at all** — the last of the four, and the gap the analyst discloses against himself.
9. **`N4` QUESTION (3) IS UNANSWERED AND THE PUBLISHED ANSWER MUST STOP SAYING OTHERWISE.** The award date
   is now body-carried by ICSID's docket — `Outcome of Proceeding: December 10, 2025 - The Tribunal renders
   its award` — **and that dates the rendering and nothing else.** It states no ground, no disposition and
   no characterisation, and the docket carries **no publication date at all**. The `Decision on Jurisdiction`
   null does **not** bear on the question in either direction (`G3`, `G10`). **The engine prose has now named
   the instrument four different ways across four sessions and does not hold the BIT/Protocol distinction
   stable — the exact locus of the question — which is a reason to refuse that class of prose on this matter
   permanently.** `STATE_OF_THE_ANSWER.md` is repaired today; **check it again tomorrow before spending
   anything on this question.**
10. **THE REGISTER'S BYTE LENGTH HAS NOW RECURRED AT A DIFFERENT DIGEST.** 94,996 appears on 2026-08-19
    (`66a879e0…`) and 2026-08-23 (`cbe0d9f1…`). **Equal byte length on this URL is not evidence of an equal
    page state** — a stronger form of the caution the record already carried about the digest. Five dates,
    five distinct `(bytes, sha256)` pairs. **No mechanism, no trend; nobody has seen the changing bytes**,
    and the "one byte a day" description died on its fourth observation. **This is an ACROSS-run comparison
    and is NOT the within-run question, which next step 7 of 2026-08-22 closed as research. Do not census
    it a third time.**
11. **THE `A/81/17` WATCH IS A PAIR, NOT A COUNT** — 11 scoped rows against **1** page state
    (`f9712994f5ae`, byte-constant at 832,338, **eleven consecutive dates** 2026-08-13 → 2026-08-23), with
    the earlier state `ca87a113c024` accounting for 7 rows across 2026-08-03 → 08-10. The `A/80/17` control
    matched, so the null is not an access failure. **`G3` is binding: this is a fact about this page state
    and this runner, and is NOT a statement that the 59th session's report does not exist or is late.** The
    lag baseline stays **UNAVAILABLE** (`N10`).
12. **RECORD HYGIENE, DONE TODAY.** `STATE_OF_THE_ANSWER.md` repaired for `0823-B8` by dated append and its
    audit anchor advanced. **The guard is green but for three `STALE` and zero hard `FAIL` — and that
    sentence is only true because this session unshallowed the clone mid-run, and the next session will
    start shallow again**, which is the whole of escalation `N14`. Two of the three `STALE` are `agents/`
    items outside this session's merge scope, failing identically on `origin/main`, **routed for the
    fourteenth day**.
13. **ARCHIVIST ITEMS, ROUTED AND NOT DONE** — `agents/Workflow Threads.md` and `agents/Project Change
    Log.md` are `STALE` on `origin/main` as well as here. **The three hard `FAIL`s previously reported
    against `agents/Workflow Threads.md` DO NOT EXIST**: `373cce6`, `9efafb0` and `ae42639` **are**
    ancestors, and the failures were shallow-clone artefacts. **Any prior session that recorded those three
    FAILs recorded an artefact of its own container.**

### Escalations to Emory

**Numbered `N1`–`N14` as of the 2026-08-23 record.** Per method rule (w), **cite these by number *and* by
this date** — the list is re-issued daily and a bare `N`-number silently resolves to the wrong item
tomorrow. Every carried item was re-verified this session rather than copied forward.

**N1. THE PATH-WATCHING TRIGGER — AND THE THING IT ACTUALLY COSTS YOU, MEASURED FOR THE FIRST TIME.
YOU WILL RECEIVE TWO EMAILS TODAY. THE FIRST IS AN AGENDA. TREAT IT AS SUPERSEDED IN WHOLE.**
`daily-update.yml` fires on any unskipped push touching `analytics/daily-research/**`, on **any branch**.
My Part I commit `3cd149c` fired run `32636583669`, which **emailed you a "daily meeting record" containing
Part I and nothing else** — an agenda, an egress probe and a precedence note, with no analyst return, no
vetting, no findings and no close-out — and then wrote the per-day de-duplication marker at `5f64d29`.
`send_daily_update.py:98-99` and `:197-198` mean that marker would have **suppressed the complete record for
the rest of the day**, and the 21:00 escalation does not rescue it because `--escalate` is reached only when
no record exists. **The marker was deleted before merge** (in scope; precedent `c0b5c5f`, 2026-08-12), so the
merge push itself re-sends the finished record. **This is the second occurrence and the second application
of the same remedy; the 2026-08-12 escalation went unactioned for eleven days.** And the officer's census
at `0823-O6` says the marker has preceded the finished record on **at least eight days and was caught
once** — 08-06 (by nine hours), 08-09, 08-11, 08-12, 08-14, 08-20, 08-21 and today. **The fix is one
condition — do not write the marker, or re-send, when the record's mtime is later than the marker's — and
it lives in `scripts/` or `.github/`, both outside this council's merge scope.** Five sessions recorded this
as trigger noise. It is not noise; it is your delivery slot.

**N14. NEW, AND THE OFFICER PLACES IT ABOVE `N1`: THE CURRENCY GUARD REPORTS FAILURES THAT DO NOT EXIST TO
EVERY AGENT SESSION.** This container's clone arrived **shallow** — 189 commits against a true 758 — and on
it `scripts/check_currency.py` reported **6 of 9 failed including three hard `FAIL`s** naming `373cce6`,
`9efafb0` and `ae42639` as "not an ancestor of HEAD". After `git fetch --unshallow`: **3 of 9, all `STALE`,
zero `FAIL`.** Those commits are ancestors. **The guard inverts its own severity on a shallow clone, and
this project's entire correction discipline runs on that guard.** CI is unaffected (`fetch-depth: 0`), so
the hazard is agent-session-only — **which is worse, not better, because it is invisible to CI and visible
only to the seats being trained by it.** Three prior sessions have merged past this guard's red with a
correct written reason; a guard that cries wolf teaches exactly that. **The officer's fix is two lines in
`scripts/check_currency.py`: if the clone is shallow, refuse to emit `FAIL` and print `UNDETERMINED (shallow
clone)`.** `scripts/` is outside this session's merge scope. **Escalated, not built.** The archivist recorded
this hazard on 2026-08-22 and I read the warning at session open and ran the guard anyway.

**N4. CARRIED, AND NARROWED BY A BODY FOR THE FIRST TIME.** The *Hela Schwarz* award is still the pivotal
unread document. **ICSID's own docket now carries `Status of Proceeding: Concluded` and `Outcome of
Proceeding: December 10, 2025 - The Tribunal renders its award`** — the first body carrier in this project
for a date it has carried on engine prose since 2026-06-28. **That dates the rendering and nothing else.**
**Question (3) — jurisdiction or admissibility — is UNANSWERED.** Every rendition on an allowed host is a
PDF the reduction cannot decode; the only HTML award text is on `jusmundi.com`, not on `ALLOWED_HOSTS`.
**This remains an operator-side retrieval.**

**N5. CARRIED AND SHARPENED — and part of it was silently promoted into your published answer.**
`STATE_OF_THE_ANSWER.md:108` carried *"published January 15, **2026**"*: the engine parenthetical this
council **refused** (*"published on January 15, 2025"*), **repaired by a year and promoted to unhedged
fact**, while `:15` still carries the impossible original as open. **Today's docket carries no publication
date at all.** Repaired today.

**N15. NEW — THE PAYWALLED / UNDECODABLE BODY LIST HAS GROWN AND IT IS THE CHEAPEST THING YOU CAN DO.**
Every item is a document on a host this channel is allowed to reach and cannot read, because the reduction
returns `""` for any `content_type` without `html` or `text`: the *Hela Schwarz* award PDFs
(`icsidfiles.worldbank.org/.../C6447/DS11402_En.pdf`, `.../DS21216_En.pdf`, `italaw11394.pdf`); and **three
newly surfaced `icsidfiles.worldbank.org` paths under case folder `C10899`** for *Bank of Nova Scotia v.
Peru* — `DS19799_En.pdf`, `DS19800_En.pdf`, `DS19936_En.pdf`, none previously in this repository. Plus two
IAReporter articles, title-indexed and body-paywalled. **A single operator-side retrieval of the Hela
Schwarz award closes the project's oldest open question.**

**N2. RE-DIAGNOSED, NOT MERELY OWED A TENTH TIME.** The archivist has reported for ten days that the
integrity officer self-reports `claude-opus-5` against a definition "declaring `claude-opus-4-8`", with no
commit on `.claude/agents/` in between. **The mechanism is that the definition never pins a version.**
`.claude/agents/integrity-officer.md` frontmatter carries `model: opus` — **a family alias** — while the
string "Claude Opus 4.8" appears only inside its `description:` prose. `research-analyst.md` and
`council-chairman.md` carry the identical `model: opus`. **No committed definition in this repository pins a
model version for any seat**, and a seat resolving to Opus 5 is what a family alias does. **The ask is not
"stop the drift"; it is "decide whether to pin, and if so pin it in `model:`."** The officer disclosed the
discrepancy again unasked today, for the ninth time by his count.

**N6 / N12. CARRIED AND UNFIXED — and the count is withdrawn rather than corrected.** The relay re-fires on
every unskipped push touching `analytics/fetch-requests/**`, re-selects the newest request file by a
**lexicographic** sort (`scripts/fetch_relay.py:233-235`, whose function name says "newest"), and overwrites
its reduction in place with no existence check (`:227-229`). **This happens to the majority of the reductions
this project holds, it is happening to today's on the merge that publishes this record, and the count is not
derivable from the artefact** — the relay commits nothing when a re-fire is byte-identical, so every figure
is a floor. Three wrong numbers were published on this point in two days (**eleven**, **twelve**, **seven**);
`0823-B1` rules that it must be a disposition and not a quantity. **Records must cite reductions by commit
SHA and never by path**, which this one does.

**N7. RE-VERIFIED BY TODAY'S OWN BATCH, which is the strongest available form.** `html.unescape` appears
**0** times in `scripts/fetch_relay.py`; `_WS_RE = re.compile(r"\s+")` at `:80` does not match U+200B
(category Cf). Today's `records[8]` carries **seven literal U+200B** and an undecoded **`&nbsp;`**. The
hazard is in the very field this record quotes, not in theory.

**N8. CARRIED — `meta.json` still has no collection-run identifier.** Re-read at HEAD: 11 keys, none a run
id.

**N9. OWED A THIRTEENTH TIME** — operator-side retrieval of the China–Switzerland BIT (2009) forum
provisions. **N10. CARRIED** — the `A/81/17` lag baseline is UNAVAILABLE; the ODS route is closed and
`web.archive.org`'s CDX endpoint is not on `ALLOWED_HOSTS`. **N11. THE PENDING LIST IS UNTOUCHED AND
UNBUILT, FOR THE SIXTH CONSECUTIVE SESSION** — the `source_analytics.py` patch, the health column, the
`polite_get` diagnostic CLI, the `IAReporter` feed swap, the Kluwer source, the category signal, the
`build_graph` fix, every ledger amendment, and the `[403-blocked]` amendment. **Nothing was built and
nothing was applied by this session.** **N13. CARRIED** — the prior-art failure is structural; today it
took a fifth corpus (`STATE_OF_THE_ANSWER.md`) and an inline-receipt mechanism, and **still** produced two
chairman failures that die inside the existing four.

**N16. NEW — `pipeline-guards` HAS BEEN RED ON `main` SINCE 2026-08-17 AND NOTHING THIS COUNCIL CAN DO WILL
FIX IT.** Last green `a9c86b8`; every push-triggered run since has failed on the currency job. The archivist
diagnosed the cause on 2026-08-22: `_is_maintenance` (`check_currency.py:93-105`) tests
`files <= set(TRACKED)`, and a real session also writes a session record and seat notes, **so the exemption
can never fire**. Combined with **N14** the guard now has two independent defects, both in one file, both
outside this council's merge scope, and both routed to the systems designer. **This PR will merge with that
check red, as the four before it did, for a reason that is written down and correct — and the officer's
point stands: that is exactly how a guard stops gating anything.**

### Where this leaves the research question

**On the question itself: a null, and a real one.** Nothing surfaced today reaches the intersection of IP
asserted as a covered investment, a regulatory or judicial measure, and a live jurisdictional or
admissibility doctrine. `ARB/24/18` is a re/insurance claim; `ARB/22/30` has a bank claimant and no
accessible carrier naming an IP asset; `ARB/24/46` has a live jurisdictional objection and **Ring 1 unknown**.
The digest of record is unchanged for a sixth day. **Two proceedings moved from UNEXAMINED-with-nothing to
UNEXAMINED-with-title-carried-subject-matter, and both moves were nulls on the theme. That is the correct
output of a narrow watch and it is not dressed up as anything else.**

**On the instrument: the best day this council has had.** Four never-fetched ICSID routes returned; a
candidate carried since 2026-08-19 on a four-character cross-page-state overlap is **confirmed inside a
single window** and its join retired; a designator discrepancy open since 2026-08-21 is **closed from a
body**; and the *Hela Schwarz* award date is **body-carried for the first time**. Every one of those came
through the relay, on rows pre-registered with their licences before any result existed, read against
branches nobody could tune afterwards. **`0822-B13` — an objection sustained against the analyst yesterday
and landed in the request file before it was written — is why four of those rows are readable at all.**

**And the thing to carry into tomorrow is none of that.** It is that **the project's published answer has
been asserting, for a month, the answer to the question this council spends its days proving is open** —
and that it took a third seat, spending zero of its budget and re-deriving everything, to notice. **The
research was sound. The deliverable was not, and the deliverable is what gets read.**

---

*Record closed 2026-08-23. Two convened seats, both real subagents, both returns verbatim. Eight binding
objections, all sustained, all applied. One optimization item, proposed only. Nothing built; nothing on the
operator's pending list touched.*
