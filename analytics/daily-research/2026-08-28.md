# ISDS Thematic Watcher — daily council, 2026-08-28

**Chairman:** council chairman seat, this scheduled cloud session, Claude Opus 5 per the operator's model
directive of 2026-08-03.
**Convened as real subagents:** research analyst; integrity officer.
**Not convened, per Rule 0:** analytics officer; systems researcher.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project watches for a narrow intersection: **intellectual property — including trade secrets and
clinical-trial data — asserted as a covered investment**, against **a regulatory or judicial measure** as the
disputed conduct, with **a live, litigated jurisdictional or admissibility doctrine**. It is deliberately
narrow, and a genuinely quiet day is an honest result rather than a failure. The daily council is a check-in:
one researcher, one chairman, one optimization item, and a binding integrity challenge heard inside the
discussion.

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer, an analytics officer and a systems researcher. `prompts/daily_council_protocol.md` Rule 0 says the
daily meeting is **the researcher, the chairman, and one optimization item**, that the integrity challenge is
heard inside the Part V dialogue rather than filed as its own Part, and that the analytics officer and systems
researcher are not convened. **Rule 0's own Precedence clause wins, and the conflict is disclosed here as that
clause requires.** The analytics officer and the systems researcher are **not convened**; the digest sentence
below is two lines I copy from `meta.json`, and the optimization item is one paragraph written by me.

**ONE DEPARTURE FROM RULE 0, DISCLOSED BY NAME.** The integrity officer **is** separately convened as a real
subagent. Rule 0 says the integrity challenge is not a separately convened seat; the operator's REAL AGENTS
rule says a council member is never a voice the chairman performs, and calls performing one this project's
cardinal failure. **Those cannot both be honoured, and I resolve it the way this council has resolved it since
2026-08-26: the officer is real, and his text enters the record verbatim inside the Part V dialogue rather than
as a Part of his own.** That keeps Rule 0's purpose — size — while never performing a voice.

### Egress state, re-probed at session open rather than inherited

The standing constraint is that this session class has no fetch capability. **I re-probed it rather than assume
it**, on two instruments:

- `curl` to the neutral host `https://example.com` → `CONNECT tunnel failed, response 403` at the proxy
  gateway. Same result for `https://www.iana.org`.
- The fetch service on the same neutral control `example.com` → `EGRESS_BLOCKED`.

**No state change from 2026-08-02 onward; this is the twenty-seventh consecutive day.** Both probes are of the
**client**, not of any resource — the standing tool-status-as-source-state rule — and neither is a statement
about any host's policy.

**The committed `fetch-relay` is ALIVE and did the day's retrieval.** It is a different client on a different
network, and its rows are the only retrieval this council has today. Fetch-dependent work is therefore **not**
stranded and is not routed operator-side.

### The batch fired before any seat was convened, and commit order proves it

One pre-registered batch, **eight targets, four under the cap of twelve**, written and pushed at commit
`1f593e7` at **11:23:23Z** — **before the research analyst or the integrity officer existed for this session.**
The relay ran as GitHub Actions run `33166965836` and its reduction was committed at `a7fe770`, blob
`1fb76e2aee36c96d27933e0b38078bce0b472fdf`, `fetched_at` **11:23:59Z**. **The reduction is cited by SHA and
never by path**, which this batch's own `overwrite_and_selection_check` pre-registers and which a chairman was
sustained against on `0827-B3` for failing.

The batch executes **next step 1 of the 2026-08-27 close-out verbatim**: the analyst's `0827-R1`–`R5` with his
licences as amended at vetting, plus the standing `A/81/17` watch pair and the `G18` tail. **`0827-R6`
(`ogel.org`) is CUT** — uncontrolled on a host this project has never fired, off the primary route, and behind
the docket-page-before-document-hunt rule — and **the four freed slots stay unused, as a decision, not as an
oversight.**

`G21` (grade the match, pre-register the null) and `G22` (page-state inequality does not localise change) bind
the pre-registration as adopted yesterday. `G6` goes in **corrected** per close-out decision 1: its `0824-O13`
provenance stated the opposite of its source, and the corrected form is **class**-level — the same class of
table/list region on this host, on a different page and a different page state. **The null-residue statement
carries FOUR sources, not two** (close-out decision 2): `G5` attribute-only, `G6` zero-width or entity,
`<script>`/`<style>` block deletion contents-included, and non-textual content type. `G23` carries close-out
decision 3's case-insensitivity finding as a gate, because it cuts both ways.

### Budgets I set for the analyst

Web search **at most three queries, and zero is an acceptable and often correct answer**; zero budget on the
escalated and zero-budget gaps (`hela-schwarz-art9-jurisdiction-or-admissibility` N4,
`icsid-2022-arbitration-rules-rule-41-48-provision-text` N15, `icsid-mllm-finding-column-value-codebook`). No
search-synthesis figures. Docket page before document hunt. At most **four** rows proposed for tomorrow.

### Spend checkpoint, placed here per the routine's own instruction

The checkpoint falls after the vetting round and is reported in Part V: search spend by seat, with unspent
budget and the reason it was returned unspent.

### The digest of record, in the fixed terminology Rule 1 requires

`Digest 2026-08-24: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied from `digests/2026-08-24_ISDS-Thematic-Watch/meta.json`, not restated from memory. **Unchanged since
2026-08-24**, and `validation_status_only` is still `true` in that file, so the weekly interpretive brief
remains suppressed. Nothing further is owed here, and Rule 1 says to keep it short when the digest has not
moved.

### What I did myself before convening anyone

**Next step 6 of the 2026-08-27 close-out — the proposition-by-proposition screen of the answer file's
seed-case dispositions against `working/one-pagers/`.** Yesterday I reported this **partially** done and said
so plainly; the marker census and the quoted-phrase carrier screen had run and the proposition screen had not.
**It ran today, and it is reported in Part V with its scope stated exactly.** It is a screen of the three seed
cases' disposition-bearing propositions in sections I–IV against the three operator-verified one-pagers. It is
**not** a screen of every proposition in the file against every source, and I do not report one as run.

---

## Part II — Return of the research analyst (verbatim)

> **Chairman's transport note, and it is the only thing I have touched in this text.** The member's return
> reached me with HTML entity encoding applied by the transport layer (`&amp;`, `&lt;`, `&gt;`). I have decoded
> those three entities back to `&`, `<` and `>`. **That restores the member's characters; it does not alter a
> word.** Nothing else in this Part is mine.

# PART II — RETURN OF THE RESEARCH ANALYST
## Daily council, 2026-08-28. Filed by the research analyst seat.

**Procedural, before anything substantive.** Egress: none in this session; carried from the pre-registration's own probe at session open (`CONNECT tunnel failed, response 403` at the proxy gateway on the neutral control, `EGRESS_BLOCKED` from the fetch service on the same control). Twenty-seventh consecutive day. Every access statement below is a fact about a client at an instant, never about a resource. **The corrected no-body-read rule, in the mandated form:** *no seat on this council has read any body since 2026-08-02, when egress closed; two primary documents were read in full on 2026-07-30 and independently verified on 2026-07-31.* Model: Claude Opus 5 per the operator directive. Today's reduction is cited **by blob** — `1fb76e2aee36c96d27933e0b38078bce0b472fdf`, at results commit `a7fe770` — and never by path, for the reason Part I.F below now demonstrates rather than predicts. All corpus derivations are run over the working tree at branch HEAD `a7fe770`: **38 result files, 287 records**, with exclusions stated at each use.

---

# MY OWN FAILURES FIRST

**1. The slide distance was my estimate and it was wrong, in the direction my seat has been named for.** On 2026-08-27 I wrote that the (b) block's status and outcome lines "fall just beyond the window's right edge" *by the (a) block's own layout*. Today's slide bought 228 further characters and did not reach them; what sits there is the (b) block's Party Representatives list. The derivation I actually ran was an offset arithmetic on one block; what I wrote up was a prediction about a second block's layout. That is `PUBLICATION-SIZE DRIFT` again — sound at the scope run, written one scope larger — and it cost the council a row.

**2. I proposed `Discontinued` without running the corpus check that was available to me.** A screen I ran today, and could have run before proposing the row: the substring `discontinu` (case-insensitive) occurs in **zero** of the 287 stored excerpts across all 38 result files. This project has never once observed ICSID's case-detail template rendering that vocabulary. The row was therefore not merely title-seeded (`0827-B11`); it was seeded by a title *and* unsupported by any prior observation that the target string is one this host emits. That check is cheap, programmatic, and belongs in every future find-literal proposal. It is in mine below.

---

# PART I — WHAT THE REDUCTION ESTABLISHES, AND WHAT IT DOES NOT

**Frame integrity.** `control_ok: true`, `void: false`, requested 8, completed 8, `truncated: false`; every row `ok`/`200`, every `content_type` `text/html`, and every `final_url` equal to its `url`, so **G9 does not bite on any row** and no row is uninterpretable as to its designator. The **G18 bracket agrees**: head `records[0]` and tail `records[8]` are both 559 bytes at sha256 `ff67a9d764d6a236…`, so every content row sits inside a verified bracket and `control_ok` is not head-anchored. The head control's `find` is the empty string, so its `find_matched: false` is an artefact of an empty literal and carries no signal; the tail's match is occurrence-only and is pre-registerable under G21(a).

## A. `records[1]` — the `Discontinued` null, graded under G21(c)

`find_matched: false`, empty excerpt, on ARB/12/1 at page state sha256 `619d80f79f660d5c…`, 176,048 bytes. This branch was pre-registered as a null branch, correctly, because added text cannot remove an occurrence and chrome cannot manufacture a false null.

**What it establishes.** Under G23 the search is `body.lower().find(find.lower())`, so the null establishes that **no case variant** of the token `Discontinued` occurs in the reduced body of that page state. Under G17 as rescoped, that scope is **reduced-body**, wider than window-scoped and narrower than page-scoped; it is not a page-scoped absence and I do not assert one.

**The four residues, all four stated as G21's residue clause requires.** The null is jointly consistent with (i) **G5**, attribute-only rendering, since the reducer strips tags and attributes before searching; (ii) **G6** in its corrected class-level form, a zero-width character or undecoded entity inside the literal; (iii) **`<script>`/`<style>` block deletion**, `_TAG_RE` stripping those blocks contents-included before general tag-stripping; and (iv) **non-textual content type**, where `excerpt_of` returns empty and `find_matched` is false regardless of the literal. Residue (iv) is *excluded on this row* — the row's `content_type` is `text/html` — and residues (i), (ii) and (iii) remain live and are not bounded.

**What it does not establish, and this is most of it.** G3 with force: this is a statement about one runner's reduction of one page state, never about the proceeding. G16 binds absolutely — no row in this batch may found a disposition. And the two weakenings I owe the record: the literal was **seeded by an unretrieved third-party listing title and by nothing in this repository**, so a null bears on the title not at all; and per my failure (2), this council has never observed the token in ICSID's docket vocabulary at all, so the null is equally consistent with "ICSID does not use this word here" as with any statement about the annulment. **The row's yield is approximately nothing, and the fault is in its design, which was mine.**

## B. `records[2]` — the window slide, graded at read time under G21(b)

`find_matched: true`, 400-character window, same page state. **Where it landed, stated rather than assumed:** the excerpt carries `WALLGREN-LINDHOLM` at offset 100, and the 100 characters of left context read `istrative Council) President: Joongi KIM (Korean) Members: Bernardo SEPÚLVEDA AMOR (Mexican) Carita `. Under G21(b) a match establishes occurrence and never location, so the anchor's identity has to be argued, not assumed.

**The arithmetic came out exactly as designed.** The literal sat at offset 328 in 2026-08-27 `records[5]`; the predicted left-edge displacement was 228 characters; the observed displacement is **228 characters exactly**, with the two windows sharing a **172-character verbatim overlap** (byte-identical, verified by string comparison, not by eye). The reducer's documented left edge — `match_index - 100` — holds.

**What is new, and it is 228 characters of it:** ` & Plimpton, New York, NY, U.S.A. and London, U.K. Feisal Hussain Naqvi, Lahore, Pakistan Bhandari Naqvi Riaz, Lahore, Pakistan Respondent(s): Office of the Attorney General for Pakistan GST, Washington, DC, U.S.A. Linklaters, L` (offsets 172–400; the right edge cuts a name mid-token). Under G16 that is a list of party representatives on a docket page and nothing else.

**Did the pre-registered mis-anchoring risk materialise? No — and the evidence for that is cross-state, which is weaker than same-state, and I say so.** The 172-character overlap with yesterday's window, whose own anchor was the literal `Date of Constitution of ad hoc Committee`, places this occurrence of the name inside the annulment block's Composition list. But that comparison crosses two page states (`588f94b5…`/176,062 → `619d80f7…`/176,048), and under **G13** it is expressly not the strong form. Within **today's** run the chain does not close: `records[3]`'s window terminates at `…March 25, 2020 Compositio` and `records[2]`'s begins at `istrative Council)`, leaving roughly sixty unobserved characters between them. So: the risk did not materialise on the evidence available, and today's run alone does not exclude it. I do not compose the two windows into a single span.

**What the slide did not reach, which is the point of the row.** No `Status of Proceeding` and no `Outcome of Proceeding` line for the (b) block appears anywhere in the observed material. **The annulment outcome remains unobserved.** One template datum from a different docket, offered as an expectation and not as a finding: on ARB/10/7 (2026-08-25 reduction, `records[8]`) the status and outcome lines of a block sit *after* that block's Party Representatives and immediately before the next block's heading — `Language(s) of Proceeding: English,Spanish Status of Proceeding: Concluded Outcome of Proceeding: July 8, 2016 … (b) Rectification Proceeding`. That is a cross-docket layout observation, not G13's strong form, and it says only that the target lines lie somewhere further right than 228 characters.

## C. `records[4]` — the replication next step 4 required

**The page state is different.** 2026-08-27 read ARB/23/3 at sha256 `ac081002159b9110…`, 94,391 bytes; today's is sha256 `1a1137654bca29a4…`, **94,393 bytes**. So the replication ran where it was supposed to run: on a fresh state, not a re-read of the same one. Incidentally this makes **four** distinct ARB/23/3 page states observed at exactly 94,393 bytes (`46ccad7f…`, `9237e1ae…`, `e0c3d269…`, `1a113765…`), where the pre-registration recorded three; **G20** gains a fourth member on this docket and a second docket besides, ARB/12/1 having now been seen at 176,048 bytes under two different digests (`bf5a70b2…` and today's `619d80f7…`).

**The result: null again**, empty excerpt, `text/html`, with the same four residues stated and residue (iv) again excluded on content type, and with G23's consequence carried — no case variant of `Published` occurs in the reduced body of `1a113765…`.

**What a second null buys.** The absence is now **state-independent across two distinct page states** rather than resting on one observation of one token on one state, which is precisely what next step 4 demanded before anything is built on it. **What it does not buy:** it does not narrow residues (i), (ii) or (iii) at all — a zero-width character or an attribute-only rendering would reproduce across states as readily as a real absence — and under **G22** the digest difference between the two states **does not localise the change** and is not evidence that the region searched changed or did not change. Two states, two nulls, same three live residues.

## D. The two controls, and G22 demonstrated twice in one run

Both controls fired: `records[3]` and `records[5]`, `find_matched: true`, carrying `Status of Proceeding: Concluded Outcome of Proceeding: July 12, 2019 - The Tribunal renders its award. (b) Annulment Proceeding` at offset 100 and `Status of Proceeding: Pending Latest Development: February 11, 2026` at offset 100 respectively. Their job is done: neither null above is an access failure or a reduction failure.

They also carry the day's cleanest instrument result. **On both dockets the page state changed and the window did not.** `records[3]`'s excerpt is **byte-identical** to 2026-08-27 `records[6]` across a 14-byte page change and a digest change; `records[5]`'s excerpt is **byte-identical** to 2026-08-27 `records[3]` across a 2-byte page change and a digest change. Derived over the tree: the ARB/23/3 `Status of Proceeding` series is now **6 rows across 5 distinct page states with exactly 1 distinct window**, and the ARB/12/1 series **3 rows, 3 distinct page states, 1 distinct window**. That is G22's corollary made concrete — a digest series is not a change-detection series for a scoped region unless the window itself is compared — and it is the converse of G20, not a weakening of it.

## E. The standing `A/81/17` watch

`records[6]` null, `records[7]` matched at offset 100 with `A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session`. **The digest pair holds today**, which matters more than usual for the reason in Part I.F.

**Corrected population, carried forward per next step 3 and re-derived here rather than incremented by memory.** Over the tree at `a7fe770`: **25 `A/81/17` find-scoped rows including today**, of which **23 are on the commission page and all 23 are null**; the two excluded rows are the 2026-08-08 `robots_disallowed` no-contact on `digitallibrary.un.org` and the 2026-08-21 `undocs.org` row, neither of which is an origin answer. Yesterday's corrected figures were 24/22 including 2026-08-27 and 23/21 at the anchor; today's are **25/23 including today**, and the definition travels with the figure. **The unit is page states, not rows:** those 23 nulls sit on **four** distinct page states — `832338/ca87a113…` (7 rows, 2026-08-03 to 08-10), `832338/f9712994…` (14 rows, 08-13 to 08-26), `7309/2a1d5ebc…` (1 row, 08-27), and `832678/bd63b2d4…` (1 row, today). As evidence, twenty-three observations are worth four. Of the 23, **19 carry a same-run `A/80/17` match**; three have no pair row at all; one — 2026-08-27 as the tree now holds it — has a pair row that also nulled and is therefore **uncontrolled**. Today's is the nineteenth controlled null and the first on a page state new to this project.

**G3, restated because this row invites the breach.** None of the above is a statement that the 59th session's report does not exist, is late, has not been issued, or has not been symbol-assigned. It is a statement about a string in a reduced body, on four page states, as seen by one runner. A further datum in the same key: the `A/80/17` window is byte-identical across **all 21 of its matches on three distinct page states**, including across today's 340-byte page change — again G22, and again no localisation.

## F. **The escalated overwrite defect fired on 2026-08-27, and it changed a row the council relied on**

This is the finding of the day and it was not on the agenda. Escalations N6-as-upgraded and N12 predict that every unskipped push re-selects the same request file by `sorted(glob(...))[-1]` and overwrites its reduction in place. **It happened.** `analytics/fetch-results/2026-08-27-daily.json` carries **two** commits: `7e755e2` (fetched `2026-08-27T11:26:05Z`) and `057185a` (fetched `12:23:49Z`). The 2026-08-27 council read the first. The path now holds the second, and the two **differ**:

- at `7e755e2`: `records[7]` `A/81/17` null and `records[8]` `A/80/17` **matched**, both at 832,338 bytes / `f9712994…`;
- at `057185a`: both null, at **7,309 bytes** / `2a1d5ebc…`.

Yesterday's Part I.E — *"`records[8]` `A/80/17` → **matched**"* — and its candidate claim 6, whose locator names `f9712994f5aec4c9` at 832,338 bytes, are **correct at the blob that council read and not reproducible at the path**. Nothing in yesterday's record is falsified; it is rendered uncheckable by the citation form it used, which is exactly what `0827-B3` was sustained on and exactly why today's pre-registration binds this record to sha citation. I ran the census over all 38 files: **ten carry two commits, eight are record-identical, and two changed** — 2026-08-27 as above, and 2026-08-22, whose `records[7]` and `records[8]` (`Final Award`) hold the same 106,011 bytes under different sha256 in each version, which is a G20 pattern arriving inside a single day. I did not re-open the 2026-08-22 record and I do not report its reliance either way.

**Two consequences I carry rather than bury.** First, every path-derived population figure in this record, including my 25/23 above, is a **lower bound on runs**: the tree omits the 11:26:05Z run of 2026-08-27 entirely. Second, **today's reduction is subject to the same defect** — blob `1fb76e2a…` at `a7fe770` is what I read, and a later push to this branch can replace what sits at the path.

---

# PART II — GAP-UNRESOLVED MARKERS

Each slug re-derived **individually** against `STATE_OF_THE_ANSWER.md`, `working/one-pagers/` and `lit-review/` before being declared open, per rule 7. `working/one-pagers/` returned **zero** carriers for every slug below, a fifth consecutive day, which continues to make the seven-place rule a five-place rule in practice.

`GAP-UNRESOLVED: tethyan-copper-arb-12-1-annulment-outcome — STANDS, AND MOVED LESS THAN I FORECAST. Two rows fired at it. The slide worked mechanically — 228 characters exposed exactly as computed, 172-character verbatim overlap — and bought only the (b) block's Party Representatives: Claimant(s) Debevoise & Plimpton (New York and London), Feisal Hussain Naqvi (Lahore), Bhandari Naqvi Riaz (Lahore); Respondent(s) the Office of the Attorney General for Pakistan, GST (Washington DC) and Linklaters (right edge cuts the entry). G16 binds: docket statements only, no disposition and no holding. THE OUTCOME REMAINS UNOBSERVED and my 2026-08-27 estimate that the status lines fell "just beyond the window's right edge" is WITHDRAWN as an over-stated cross-block layout inference. The `Discontinued` null adds nothing: the literal was title-seeded, and this project has never observed the token in any of 287 stored excerpts, so the null is as consistent with a vocabulary mismatch as with anything about the proceeding. The OGEL/TDM title remains a G10 lead, unretrieved, and NOTHING today corroborates or contradicts it. The locator is the corrected FULL pincite ¶¶ 1283, 1288, 1327–1330, 1331–1333, and those paragraphs are missing by REDACTION from the publicly accessible italaw text, per this project's own auditor's note at lit-review/ferguson-memo.md — NO RELAY ROW, TODAY OR EVER, CAN ANSWER WHAT THEY HOLD. The finality question and the span question come apart and only the first is on the relay's route.`

`GAP-UNRESOLVED: icsid-arb-23-3-no-published-decisions-on-docket — STANDS, AND IS NOW REPLICATED. Second null on a second page state (1a113765… / 94,393 bytes, against ac081002… / 94,391), so the absence is state-independent across two states and next step 4 is discharged. The residue does NOT narrow: G5, G6 and script/style block deletion all reproduce across states as readily as a real absence, and under G22 the digest difference localises nothing. G23 carried: no case variant occurs in either reduced body. The honest form remains "not found in this surface", never "does not exist".`

`GAP-UNRESOLVED: icsid-subject-of-dispute-vocabulary-enumeration — STANDS, UNTOUCHED. No row fired at it, no search went near it. I screened ARB/10/7 as a candidate row and rejected it: its Subject of Dispute field is already captured (2026-08-25, "Tobacco industry"), so a row would be a re-fire of an established positive. Eleven observations remain eleven observations and not an enumeration.`

`GAP-UNRESOLVED: philip-morris-uruguay-arb-10-7-pleaded-covered-investment — STANDS, UNTOUCHED. My retraction of the "trademark-and-plain-packaging" description as unsourced is carried unsoftened. Noted only: the docket's Subject of Dispute field records an industry, not a pleaded investment, so that field cannot answer this gap and I do not propose a row that pretends otherwise.`

`GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility — STANDS, UNTOUCHED, ESCALATED (N4). Zero search budget spent, per the escalated-gap rule. No row went near it. Carrier confirmed at STATE_OF_THE_ANSWER.md:124; I ran no disposition screen today and report none as run.`

`GAP-UNRESOLVED: icsid-2022-arbitration-rules-rule-41-48-provision-text — DOES NOT MOVE. N15-class operator-side retrieval, zero budget, nothing retrieved, nothing learned about the text.`

`GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook — UNTOUCHED. Zero budget. `No`, `Partial` and `_` remain untranslated and `_` undefined.`

**In daily tracking, TWO markers and not three, per decision 7.** `landreau-1922-holding-and-pinpoint` — carrier confirmed at `lit-review/ferguson-memo.md:289`, open, and unmoved: the award is located at the RIAA URL and **located is not read**; no pinpoint and no holding are asserted. `philip-morris-uruguay-2013-jurisdiction-para-185` — carrier confirmed at `lit-review/kim-memo.md:92`, open, and unmoved: whether ¶ 185 of the 2 July 2013 Decision on Jurisdiction is the tribunal's own statement or a recital of argument cannot be determined from any source in hand. **`vanda-cfc-2024-2025-takings-record` is CLOSED** — `lit-review/kim-memo.md:206`, *"gap closed 2026-08-08"* — twenty days ago, and I do not revive it.

**No slug is closed today and none is proposed for closure.**

---

# PART III — SEARCH SPEND

Authorised 3. **Spent 1, returned 2 unspent.**

**Search 1 — SPENT. The monitored-author standing check, which `prompts/research_analyst.txt` requires each session.** Query: `"Anthea Roberts" ejiltalk.org author aroberts new post 2026`, with `allowed_domains: ejiltalk.org`. Result set: the author page, the `arobertsandrbraddock` co-author page, four pagination shells, and three post titles. Two of the three titles are already in this record. **One is not:** `UNCITRAL and ISDS Reform (Hybrid): Islands of Persuasion` returns **zero** carriers on a repo-wide grep excluding `.git`. Under **G10** that establishes that a title exists on a listing page and nothing more: **it carries no date, it is not evidence of an August 2026 post, and no body was read** — the direct route to ejiltalk.org is WAF-403 to identified non-browser clients and this project does not masquerade as a browser. **What I refused:** the engine's generated biography and its assertion that "the most recent posts referenced in the results are from 2023 and earlier." That is search-synthesis prose, it is not a source, and none of it is carried in any form, including hedged. Result: **one undated title added as a lead; no post established as new; standing watch held.**

**Searches 2 and 3 — RETURNED UNSPENT, with reasons.** The Tethyan annulment was searched yesterday in the document-query form and returned titles; a repeat is not a second observation, and the follow-up it produced is a *retrieval* problem on uncontrolled listing hosts, not a search problem. `hela-schwarz-art9-jurisdiction-or-admissibility` (N4), `icsid-2022-arbitration-rules-rule-41-48-provision-text` (N15) and `icsid-mllm-finding-column-value-codebook` are zero-budget by standing rule. The remaining gaps are enumeration problems no search can close. A search cannot tell me what ICSID's own template renders, which is the one question today's rows raised; only the relay can, and it fired.

**Method note for the self-training record.** Yesterday's month-scoped author query (`Anthea Roberts EJIL:Talk August 2026`) returned **one** post title. Today's **domain-scoped** author query with `allowed_domains: ejiltalk.org` returned **three**, one of them absent from this repository. Same author, same instrument, one day apart: **domain-scoping the author check beat month-scoping it, and the month token was the weak half.** This refines rather than replaces the 2026-07-26 lesson — name the artefact, not the place — by adding that where no title is yet in hand, *constrain the place and drop the date* rather than constrain the date. I recommend the monitored-author protocol record the month-scoped listing query as the weakest of the three forms.

---

# PART IV — ROWS FOR TOMORROW (`0828-R…`)

Cap 12 including the relay's head control and the G18 tail. I propose **four**, which with the standing `A/81/17` watch pair, the head control and the tail comes to eight — **four under the cap, and the four freed slots stay unused.** All prior art derived programmatically over the tree at `a7fe770`, 38 files / 287 records, with the command form stated at each row. **No presence-branch is pre-registered on a match anywhere below.**

**`0828-R1` — the slide continued. The day's best row.**
URL `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1`, find **`Linklaters`**.
*Prior art, programmatic:* iterating every `records[]` of every `analytics/fetch-results/*.json`, `Linklaters` appears **as a find literal 0 times** and **in a stored excerpt exactly once** — today's `records[2]`, this docket. First firing; not a re-fire of an established negative.
*Rationale:* it is the **rightmost** distinctive token in today's newly exposed tail (excerpt offset 387), so it yields the maximum available slide of **287 characters** — 100 more than any alternative I screened (`GST, Washington` 259, `Office of the Attorney General for Pakistan` 215, `Bhandari Naqvi Riaz` 162). It is also a **single token of ten characters**, which is G7's corrected form applied: minimum contiguous span, fewest markup positions that can break it. On the ARB/10/7 template datum the target status line lies after the block's representative list, so a 287-character slide is a plausible but **unpromised** reach; I state that as an estimate, having got the last one wrong.
*Licence:* G16 absolutely — whatever returns is a docket statement and a date, never a disposition, never an outcome, never a holding. G10 if any document designation appears.
*Branch, graded under G21(b) and NOT pre-registered on the match:* (i) if the window's 100-character left context reads `…Office of the Attorney General for Pakistan GST, Washington, DC, U.S.A. ` — text already observed today at a known offset — the anchor is **self-verified** as the occurrence we can already see, and the row is read for its right-hand 287 characters; (ii) if the left context differs, the row **mis-anchored** on an earlier occurrence and is **a probe, not a finding**, and is reported as uninformative. I have no basis to exclude an earlier `Linklaters` in the (a) block's respondent representatives — that part of the (a) block has never been inside a window — and I do not pretend to. *Exposure declared in advance per G21(d): this council's ICSID chrome holdings are one 400-character capture, which is not a chrome census, so a chrome landing is live and unbounded.*
*Null branch, pre-registered under G21(c):* the token is absent from the reduced body in every case variant per G23, with all four residues stated — and it would **contradict** today's `records[2]` window, which makes it an **instrument alarm**, not a docket finding.

**`0828-R2` — mandatory control.** Same URL, find `Status of Proceeding`. Same-run, same-URL digest pair for R1 per `0821-E8`, and the fourth point in this docket's window-constancy series (3 rows / 3 page states / 1 window to date). **No branch pre-registered:** a digest difference is expected, licence-free, and under G22 localises nothing.

**`0828-R3` — the ARB/23/3 third state.** URL `…CaseNo=ARB/23/3`, find `Published`. *Prior art:* two firings, both null, on two distinct page states (`ac081002…`, `1a113765…`). *Rationale:* a third state converts "state-independent across two" into "across three" and no more; it narrows no residue. **Null branch pre-registered under G21(c)** with all four residues and G23 carried. **Match → the two-state reading is withdrawn same-day and the change is the finding**, with no location claim pre-registered on it.

**`0828-R4` — mandatory control, travelling with R3.** Same URL, find `Status of Proceeding`. R3 and R4 travel together or not at all: a replication without its digest pair would be compared against an unknown state, which `0825-R10` forbids, and would look controlled while not being controlled.

**What I would cut first if you want slots back: R3 and R4, together, and I would cut them without argument.** They are the lowest-yield pair on the sheet — a third null narrows nothing that the second did not already fix, and the gap they serve is the one gap that moved this week. R1 and R2 are the only rows aimed at an unobserved fact, and R2 cannot be cut without decontrolling R1. **What I would not propose at any price:** any row on `undocs.org`/`docs.un.org` (route marked spent, decision 4), any row on `ogel.org` or `transnational-dispute-management.com` (uncontrolled, zero baseline in 287 records, off the primary route), and any row aimed at the Tethyan award paragraphs, which are **redacted, not inaccessible**, and which no relay row can ever answer.

---

# PART V — CANDIDATE CLAIMS

I propose; the operator's ledger decides. Any status below is ignored by design.

```json
{"candidate_claims": [
  {"claim_text": "ICSID's case-detail page for ICSID Case No. ARB/12/1, as retrieved on 2026-08-28, lists as party representatives in the block containing the ad hoc Committee's composition: for the Claimant(s), Debevoise & Plimpton (New York, NY, U.S.A. and London, U.K.), Feisal Hussain Naqvi (Lahore, Pakistan) and Bhandari Naqvi Riaz (Lahore, Pakistan); and for the Respondent(s), the Office of the Attorney General for Pakistan, GST (Washington, DC, U.S.A.) and Linklaters, the last entry truncated by the window's right edge.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Party Representatives Claimant(s): Debevoise & Plimpton, New York, NY, U.S.A. and London, U.K. Feisal Hussain Naqvi, Lahore, Pakistan Bhandari Naqvi Riaz, Lahore, Pakistan Respondent(s): Office of the Attorney General for Pakistan GST, Washington, DC, U.S.A. Linklaters, L",
   "supporting_locator": "2026-08-28 relay reduction, blob 1fb76e2aee36c96d27933e0b38078bce0b472fdf at commit a7fe770, records[2].excerpt offsets 131-400; page state sha256 619d80f79f660d5c, 176048 bytes. Block attribution rests on a 172-character verbatim overlap with the 2026-08-27 window anchored on 'Date of Constitution of ad hoc Committee' at a DIFFERENT page state (588f94b5c8547e9d), and is therefore expressly NOT G13's strong form."},

  {"claim_text": "The ICSID case-detail page for ARB/12/1 states no status and no outcome for the annulment proceeding within the 400 contiguous characters observed on 2026-08-28, which run from the ad hoc Committee's composition through that block's respondent representatives.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Carita WALLGREN-LINDHOLM (Finnish) Party Representatives Claimant(s): Debevoise & Plimpton",
   "supporting_locator": "2026-08-28 relay reduction, blob 1fb76e2a…, records[2].excerpt, offset 93; the observed span terminates at the right edge shown and no claim is made about text beyond it"},

  {"claim_text": "The token 'Discontinued' does not occur, in any case variant, in the tag-stripped reduced body of the ICSID case-detail page for ARB/12/1 at page state sha256 619d80f79f660d5c as retrieved on 2026-08-28; this is an instrument observation scoped to that reduction and is not a statement about the proceeding, the annulment, or ICSID's status vocabulary.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "2026-08-28 relay reduction, blob 1fb76e2a…, records[1], find_matched false with empty excerpt, content_type text/html; same-run same-URL control at records[3] find_matched true. No carrying span exists for a null and none is offered. Residues G5, G6 and script/style block deletion remain live; non-textual content type is excluded on this row."},

  {"claim_text": "The token 'Published' does not occur, in any case variant, in the tag-stripped reduced body of the ICSID case-detail page for ARB/23/3 at either of two distinct page states — sha256 ac081002159b9110 (94,391 bytes, 2026-08-27) and sha256 1a1137654bca29a4 (94,393 bytes, 2026-08-28) — making the absence state-independent across two states; this is an instrument observation and is not a statement about the docket.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "2026-08-28 relay reduction, blob 1fb76e2a…, records[4]; and 2026-08-27 relay reduction at blob-bearing commit 7e755e2, records[2]. Both content_type text/html; both paired with a same-run same-URL 'Status of Proceeding' control that matched."},

  {"claim_text": "On the ICSID case-detail pages for ARB/12/1 and ARB/23/3, the 400-character window anchored on the literal 'Status of Proceeding' is byte-identical across page states whose sha256 and byte counts differ — 3 rows across 3 page states yielding 1 distinct window for ARB/12/1, and 6 rows across 5 page states yielding 1 distinct window for ARB/23/3.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "Language(s) of Proceeding: Spanish Status of Proceeding: Pending Latest Development: February 11, 2026",
   "supporting_locator": "Derived programmatically over analytics/fetch-results/*.json at branch HEAD a7fe770 (38 files, 287 records) by grouping identical excerpt strings; ARB/23/3 states 46ccad7f, 9237e1ae, e0c3d269, ac081002, 1a113765; ARB/12/1 states bf5a70b2, 588f94b5, 619d80f7. Path-derived and therefore a lower bound on runs, per the overwrite finding."},

  {"claim_text": "The UNCITRAL commission page at uncitral.un.org/en/commission carries the text 'A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session' as at 2026-08-28, on a page state (sha256 bd63b2d4f9f5, 832,678 bytes) not previously observed by this project, while the string 'A/81/17' returns no match in the reduced body of that same state.",
   "source_url": "https://uncitral.un.org/en/commission",
   "access_status": "partial",
   "source_authority": "official_secondary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session",
   "supporting_locator": "2026-08-28 relay reduction, blob 1fb76e2a…, records[7].excerpt offset 100 and records[6] find_matched false. The A/81/17 null is a fact about this reduction and this runner and is NOT a statement that the 59th session's report does not exist, is late, or has not been issued."},

  {"claim_text": "The result file analytics/fetch-results/2026-08-27-daily.json was written twice and its content changed between the two writes: at commit 7e755e2 (fetched 2026-08-27T11:26:05Z) records[8] 'A/80/17' matched on a 832,338-byte page state sha256 f9712994, and at commit 057185a (fetched 12:23:49Z) the same row returned no match on a 7,309-byte state sha256 2a1d5ebc. The 2026-08-27 council record cites the first; the repository path now holds the second.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "chore: fetch-relay results [skip ci]",
   "supporting_locator": "git log --format='%h %cI' -- analytics/fetch-results/2026-08-27-daily.json returns exactly two commits, 7e755e2 (2026-08-27T11:26:05+00:00) and 057185a (2026-08-27T12:23:50+00:00); records compared field-by-field at both blobs. Census over all 38 result files at HEAD a7fe770: ten carry two commits, eight are record-identical, two differ (2026-08-27 as stated, and 2026-08-22 records[7] and [8], same 106,011 bytes under different sha256 in each version). This confirms escalations N6-as-upgraded and N12 as live, and does not falsify the 2026-08-27 record, which is correct at the blob it read."},

  {"claim_text": "A post titled 'UNCITRAL and ISDS Reform (Hybrid): Islands of Persuasion' is listed on ejiltalk.org under Anthea Roberts' authorship in the result set of a domain-scoped search run 2026-08-28; the listing page was not retrieved, no body was read, and the title carries no date.",
   "source_url": "https://www.ejiltalk.org/uncitral-and-isds-reform-hybrid-islands-of-persuasion/",
   "access_status": "unavailable",
   "source_authority": "media",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": null,
   "supporting_locator": "Search result title returned 2026-08-28, query '\"Anthea Roberts\" ejiltalk.org author aroberts new post 2026' with allowed_domains ejiltalk.org. The page itself was not opened, so no substring of the source is available and none is offered. Under G10 this establishes that a title exists on a listing page and nothing about the post's date, content, or novelty. A repo-wide grep excluding .git returns zero prior carriers of this title."}
]}
```

---

# PART VI — ONE OPTIMIZATION OBSERVATION

**Concrete, component-specific, and it falls out of Part I.F.** `scripts/fetch_relay.py`'s `run()` writes to `out_dir/request_path.name` with **no existence check**, and `newest_request()` re-selects the same file on every unskipped push. The council's standing countermeasure is a *documentation* rule — cite by sha — which preserves the evidence but does not preserve the *observation*: the 11:26:05Z UNCITRAL row of 2026-08-27 is recoverable only because git happens to keep it, and every population figure derived from the path silently under-counts runs. **The one-line change that would fix the class, entirely within this council's existing "no new components" posture:** have `run()` write to `out_dir/<request_stem>.<fetched_at compact timestamp>.json` and, if a plain `<request_stem>.json` is wanted for convenience, write it as a *second* copy — so a re-fire adds a file instead of replacing one, and no reduction this council has cited can ever cease to exist at its own name. **Prior art, screened by me at `a7fe770` over the full optimization log:** the 2026-08-18 entry reads `scripts/fetch_relay.py:130-135` directly and catches the `excerpt_of` docstring error; the 2026-08-27 entry proposes enumerating a code path's failure modes before relying on an inference class. **Neither touches write-path collision, and no entry in the log does — I screened for `newest_request`, `out_dir`, `overwrite` and `existence`.** The increment over 2026-08-27's entry is that this is not an enumeration discipline but a two-token change to one line, and the defect it fixes has now **fired twice, observably, on days the council relied on the affected rows.** I note the standing rule that no code change is made by this council; this is filed as an observation for the operator's pending list, alongside N6-as-upgraded and N12, which it would close. *Status: proposed.*

---

*Filed 2026-08-28 by the research analyst. One of three searches spent, two returned unspent with reasons stated; one engine's generated prose refused in full and recorded as refused. Every corpus figure derived programmatically over the working tree at branch HEAD `a7fe770` — 38 result files, 287 records — with exclusions named at each use and the path-derivation's lower-bound status stated once and carried. Every quoted window verified as an exact substring at a stated offset against blob `1fb76e2aee36c96d27933e0b38078bce0b472fdf` before this text was written; the reduction is cited by blob and never by path. **I report my own two failures first: an over-stated layout forecast of the same shape my seat was named for yesterday, and a find literal I proposed without running a corpus check I could have run and did run today.** Two gates were exercised rather than restated — G21's grading at read time on the one match that needed it, and G22 demonstrated twice on two dockets in a single run. One gap materially advanced, one replicated, none closed, no proceeding moved. The day's largest finding was not on the agenda: an escalated defect that had been described for weeks has now been observed changing a row this council relied on. Nobody read a body.*
