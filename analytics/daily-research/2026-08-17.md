# ISDS Thematic Watcher — daily council, 2026-08-17

**Chairman:** the scheduled cloud session, running as the chairman seat on Claude Opus 5 per the
operator's model directive of 2026-08-03.
**Convened:** `research-analyst` as a real subagent; the integrity challenge heard inside Part V per
Rule 0, also as a real subagent. Both returns reproduced verbatim.
**Not convened, per Rule 0:** `analytics-officer`, `systems-researcher`.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed
integrity officer (Part 3), an analytics officer (Part 4) and a systems researcher (Part 5).
`prompts/daily_council_protocol.md` Rule 0 says the daily meeting is **the researcher, the chairman,
and one optimization item**, that the integrity challenge is heard inside the Part V dialogue and files
no separate return, and that the analytics officer is not convened at all — the digest sentence is two
lines the chairman copies from `meta.json`. The protocol file's own Precedence clause says it wins and
that the conflict is worth a line in the record's procedural caveat. **This is that line**, for the
fifth consecutive day, because nobody has amended either document.

I ran the meeting to Rule 0 with **one** departure, and I state it here rather than certifying
compliance: Rule 0 says "convene exactly two agents… nothing else", and **three were convened** — the
analyst, the integrity officer, and me. The integrity officer was convened as a **real agent** because
the alternative is a chairman writing the challenge to his own work, which this project treats as its
cardinal failure. Rule 0 governs where that voice is *filed*, not whether it is *real*. This is the
resolution the 2026-08-13 through 2026-08-16 seats reached and I adopt it deliberately rather than by
drift. The routine prompt itself defers to this file ("`prompts/daily_council_protocol.md` governs"),
so the conflict is formal rather than live.

*(I have deliberately not written the sentence "I complied in all respects but one" without then
listing what follows it. `0816-B6` was sustained against my predecessor for exactly that: a compliance
self-certification falsified twelve lines below by the record itself. Today's optimization item is one
paragraph, so the second departure that produced `0816-B6` does not arise. A reader should still treat
this paragraph as the least trustworthy in the record, because it is the one place where the seat under
audit is also the auditor.)*

### Instrument state, probed at session open

Both fetch instruments are dead and both were **probed on a neutral control** rather than assumed. The
relay is alive and did the day's retrieval.

- **Direct egress: DEAD.** `curl https://example.com/` returned `curl: (56) CONNECT tunnel failed,
  response 403`. A neutral control host, chosen precisely so the answer cannot be about a research
  source.
- **The fetch service: DEAD.** `WebFetch` on `https://example.com/` — the same neutral control —
  returned `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`.
- **The fetch relay: ALIVE**, and it did the day's retrieval. **One batch, one run**, answered by
  `github-actions-runner (run 32025443591)` at `2026-08-17T11:33:17.275667+00:00`. Pre-registered and
  pushed before any result existed. Details in Part II.
- **WebSearch: alive**, and it was the analyst's only live *external* instrument. The repository
  filesystem is also an instrument, and as on 2026-08-16 it carried real findings today.

Per the standing rule that a tool-layer status is never a statement about a resource: none of the above
is a fact about UNCITRAL, UNCTAD, ICSID, italaw, the PCA or any document. It is a fact about this
session's plumbing, and it is why the relay exists.

### Agenda

Set from Part 6 of the 2026-08-16 record, and scoped to the instruments actually alive.

1. **Slide the *Einarsson* (UNCT/20/6) window again**, anchored on a both-sides-bounded ASCII
   designator, never on the right-edge fragment. Next step 1 of the 2026-08-16 close-out.
2. **Fire the italaw PDF row** that the 2026-08-16 close-out adopted, with its refusal history
   pre-registered as one observation of this client's access. Next step 3.
3. **Take the A/81/17 watch with a paired same-page positive control in the same batch** — the
   discipline adopted yesterday as next step 4, in response to two of eleven prior nulls resting on a
   control that fired in a different file on a different day.
4. **Analyst, task 1:** the free repository operation adopted from his own revised proposal — screen
   every committed relay excerpt for `Pricewaterhouse` and read the adjacency, settling I4 in one
   direction from windows already held, with no fetch at all.
5. **Analyst, task 2:** re-derive the A/81/17 null population programmatically, with its definition
   travelling with the figure, and verify which of those rows carry a paired control in their own file.
6. **Analyst, task 3, budget-permitting:** `Nigel Bankes`'s field. **No further WebSearch on
   `Paul Sharp` or `Doug Uffen`** — titles are exhausted and the residual is identity linkage, which a
   title cannot carry.

**Spend checkpoint** is placed after the vetting round, in Part VI, and is honoured there.

### Member return-path protocol

Both members are real subagents spawned with the repo's committed definitions in `.claude/agents/`.
Each files a written return; **I do not write, summarise or paraphrase any member's contribution** —
member text enters this record verbatim, inside a fenced block, and my own voice appears only in Part I,
Part II, Part IV and Part VI. Where I disagree with a member I rule against it on the record in Part VI
rather than editing what it said. If a spawn had failed, the seat would have been recorded as
PROCEDURAL FAILURE for the day and not performed.

### The one optimization item (Rule 0)

**A novelty screen must be scoped to the resource the designator belongs to, whenever the designator is
resource-relative rather than rigid — and this council ran an unscoped one today and got four false
"seen before" verdicts out of eight.** Screening today's *Einarsson* window against every committed
relay excerpt reported `Procedural Order No. 2`, `No. 3`, `No. 4` and `No. 5` as already in the record.
They are not. The prior carriers are `2026-08-06-daily.json` `records[4]` and `records[5]`, and both are
`icsid.worldbank.org/…?CaseNo=ARB/22/2` — **a different proceeding**. "Procedural Order No. 4" names a
document only relative to a case; across two cases it is a homonym, and a bare substring screen cannot
tell the two apart. Re-run scoped to rows whose URL is the UNCT/20/6 page — **six distinct non-empty
windows of 400 characters, 2,400 characters, deduplicated by exact string; canaries
`RWS-01 - Witness Statement of Bharat Dixit` and `Doug Uffen`, both live** — none of the eight strings
appears in a prior window of this case. This extends the adopted vocabulary rule (f) — *a screen must
carry the rigid designator the record cannot paraphrase away* — with its missing converse: **when the
designator is not rigid, rigidity must be supplied by scoping the corpus, not by choosing a better
string**, because no string will do.

**CORRECTED AFTER VETTING, and the corrections are the more useful half of this item.** Three defects,
all sustained, all mine:

1. **`0817-B2`.** The limit I first wrote — *"scoping by URL is only correct where one URL is one
   resource"* — is true but is not the limit today's evidence exposed. Scoping did not remove the error;
   **it changed its sign.** The unscoped screen gave four false negatives; the scoped re-run then gave
   **false positives**, because the scope excluded the rest of the repository, where two of the eight
   sit in the right case (`0817-B1`). The real rule is: **a screen establishes novelty only within the
   corpus screened, so a claim of novelty *to the record* requires a second, repository-wide pass. Both
   passes and both scoping keys are reported, or neither figure is.** URL-scoping is the right
   instrument for the homonym problem and the wrong one for a novelty claim; one command cannot answer
   both.
2. **`0817-O1`.** I wrote "2,420 characters". The true figure is **2,400** — six windows of 400. The
   extra 20 was the four-character separator my own join inserted five times, i.e. **an artefact of my
   measuring instrument reported as a property of the corpus.** I also failed to name the canaries or
   the dedup rule, in the same paragraph in which I made naming the scoping key a rule.
3. **The scoping key was itself encoding-dependent, and this is the finding I would keep if I could
   keep only one.** My key was the literal `CaseNo=UNCT%2F20%2F6`. It **silently dropped a row**:
   `2026-08-13-zz-einarsson-docket.json` `records[5]` names the same case with a **literal slash**,
   `CaseNo=UNCT/20/6` — three percent-encoded rows and one unencoded row, in one file, written by one
   runner. Re-run with both encodings the verdict is unchanged, but that is luck, not method. **A
   scoping key is a string too, and one resource can be named by more than one URL string.**

---

## Part II — The relay batch: the window slid past the carried fragment and reached eight items new to this case, italaw answered for the first time in the corpus's two attempts, and the A/81/17 watch took its twelfth null with its control matching in the same file

**Batch integrity, checked against the rows rather than against intent.** `control_ok: true`,
`void: false`, `requested: 6`, `completed: 6`, `truncated: false`. My own brackets are `urls[1]` and
`urls[6]`, which land at `records[1]` and `records[6]` of the result file — the relay prepends its own
control row at `records[0]`, and **this batch's request states that off-by-one explicitly**, because
`0816-B7` was sustained when the previous batch's gate text named row numbers that did not exist in the
file it would be read against. All three checks are green: `records[0]` ok/200, `records[1]` ok/200,
`records[6]` ok/200. **G1 is satisfied, and it is satisfied against the rows I have just named.**

### P1 — the window slid, the carried fragment was completed by character equality, and eight items appeared that the record has never held for this case

The 2026-08-16 window ended on the fragment `Counter-Memorial on Jurisdiction, Merits and Dama`. The
close-out forbade anchoring on it and named two lawful anchors; this batch took the one that slides
furthest, `RWS-02 - Witness Statement of Trevor Bennett`, verified at session open to be bounded on both
sides in the 2026-08-16 window and to be pure ASCII. It matched.

**The fragment is completed, and by character equality rather than by eye.** The **49**-character tail
of yesterday's window is a substring of today's, occurring at index 145 and continuing as
`Counter-Memorial on Jurisdiction, Merits and Damages Pr`. The trailing `Dama` was the first four
characters of `Damages`. That is a deduction from two windows, not an inference about a docket.
*(`0817-B4`: my draft said "48-character". The quoted tail is 49. The substantive claim is unaffected
and the officer verified it independently — but it was the numeral inside the one sentence whose entire
claim is mechanical precision, which is why it is corrected here and not waved through.)*

Today's window, `records[2]`, in full:

> `Carl Makrides RER-04, Expert Report of The Brattle Group RWS-01 - Witness Statement of Bharat Dixit RWS-02 - Witness Statement of Trevor Bennett Counter-Memorial on Jurisdiction, Merits and Damages Procedural Order No. 2 Procedural Order No. 2 - Annex A Procedural Order No. 2 - Annex B Procedural Order No. 3 Procedural Order No. 3 - Annex A Procedural Order No. 4 Procedural Order No. 5 Procedural `

**Eight STRINGS are added by this window. SIX are new to this record for UNCT/20/6; two are not, and my
draft said all eight were.** The corrected form, per `0817-B1`, sustained:

- **NEW to this record for this case — six**, each returning **0 files repo-wide** before today:
  `Procedural Order No. 2`, `No. 2 - Annex A`, `No. 2 - Annex B`, `No. 3`, `No. 3 - Annex A`, `No. 5`.
  *(Screened repo-wide, not merely against relay excerpts; the four bare `Procedural Order No. N` forms
  that hit `2026-08-06` are `CaseNo=ARB/22/2`, `Huawei v. Sweden`, a different matter.)*
- **NOT new — `Procedural Order No. 4`.** It has been in this record for this case since
  `2026-08-13.md:290` and `:726`, where it entered as a generated-summary item and was **refused as
  unassertible**. It entered four days ago and my screen did not see it because my screen looked only at
  relay excerpts.
- **NOT new as an item — `Counter-Memorial on Jurisdiction, Merits and Damages`.** The record has held
  it since 2026-08-16 in truncated form. What today adds is its **completion**, not its appearance —
  and the screen returned zero precisely because **a completed designator cannot match its own truncated
  form.** *(As a **string** it is genuinely novel: `Merits and Damages` returns zero hits repo-wide
  before today, which is the half the analyst got wrong in the other direction at `0817-B5`.)*

**And "eight items" was the wrong noun.** The relay strips every tag and attribute, so **no delimiter
survives and no segmentation is derivable** (`0815-B2`). Calling these eight *items* is a reading by
eye. They are eight **strings**, and the record says so.

**What this does NOT establish, stated before anyone asks.** A window is not a docket. Per `0811-B5` no
count, no enumeration, no "latest" and no "complete" may be drawn from a list still truncated at both
ends — and this one is truncated at both ends, ending on the fragment `Procedural `. **Nothing here
touches the IP or pharmaceutical nexus of this case in either direction**, per `0815-B9`. A procedural
order's *name* is not its *content*, and per `0811-B6` a disposition is not a rule and a document's name
is neither. **Nobody was identified by this row.**

**A hazard for tomorrow's anchor, derived from the corpus rather than guessed.** On 2026-08-06 the same
host rendered the *same* procedural-order designators **twice on one page** — `records[4]` shows a bare
list (`Published Decisions Procedural Order No. 1 Procedural Order No. 2 …`) and `records[5]` shows a
dated list (`Procedural Order No. 2 (September 26, 2022) English (Original) …`). The relay centres its
window on the **first** case-insensitive match. An anchor on a bare `Procedural Order No. N` is
therefore ambiguous by construction on this host, and a seat that takes one must say which rendering it
believes it landed in.

### P2 — italaw answered, and the honest form of that fact is narrower than "italaw now works"

`records[3]`: `outcome: ok`, `status: 200`, `content_type: application/pdf`, `bytes: 1587866`,
`sha256: 8de4c3db96fd6e0f13c86342422ca0ca9e757aeca897e6e8e1db3782da2f3de2`.

**The G3 discrimination fires in the affirmative direction.** The batch pre-registered that a
`text/html` content-type with a 200 would mean we received a page *about* the document rather than the
document. We received `application/pdf`. The 1.59 MB is the object's byte length, not a landing page's.

**The whole italaw history of this relay, derived programmatically over `analytics/fetch-results/*.json`
and shown rather than recalled — it is two rows:**

| file | row | url | outcome |
|---|---|---|---|
| `2026-08-03-control.json` | `records[2]` | `https://www.italaw.com/` | `refused` / 403 |
| `2026-08-17-daily.json` | `records[3]` | `https://www.italaw.com/sites/default/files/case-documents/italaw1827069.pdf` | `ok` / 200 |

**And this is where I decline the headline the record was set up to write.** The 2026-08-16 close-out
told this session to expect a refusal and to treat it as one observation of client access. It did not
refuse. But **the two observations are not a before-and-after on the same resource**: 2026-08-03 was the
site **root**, today was a **case-documents PDF path**, fourteen days apart. Two variables moved at once
— the path and the date — and two observations cannot separate them. So the corpus supports
*"this client retrieved this PDF from this path at this instant"* and **does not** support *"italaw
stopped refusing us"*, *"italaw's 403 was path-specific"*, or any statement about what the weekly
collector will see.

**One further observation, which sharpens the confound without resolving it, and which I went and read
rather than recalled.** The `italaw` entry in `digests/2026-08-10_ISDS-Thematic-Watch/meta.json`
`source_health` records `status: "NOT-READ (403)"`, `refusal_count: 1`, and its single logged refusal
URL is **`https://www.italaw.com/`** — *the site root again*. So across two clients on two networks
there are now three observations of this host: **two against the root, both refused; one against a deep
file path, ok/200.** That is *consistent* with path-specific refusal, and it is **not evidence for it**:
n=1 on the file path, a fourteen-day gap, and two different clients mean path and time and client are
all still confounded. Per G2 the collector's observation is a **different client on a different network**
and corroborates nothing about *our* access. I record the pattern because a later seat will otherwise
re-derive it; I refuse the inference because one row cannot carry it. **The way to settle it is one root
row and one file-path row in the same batch from the same client** — proposed to tomorrow, not asserted
today.

**What this row cannot do, exactly as pre-registered at E2 before the result existed.** Per G3 a find on
a PDF is void by construction on this channel — `excerpt_of` returns empty for any content-type
containing neither `html` nor `text` (`scripts/fetch_relay.py:124-125`), so `find_matched` is
structurally false whatever the document contains (`:181-182`). **The find was therefore left empty on
purpose**, so that no seat could later mistake a structural false for a searched-for absence. The
identity question this row was proposed to serve — whether the `Paul Sharp` of that document's title is
the `Paul Sharp` of UNCT/20/6 — **is untouched, and this row must not be reported as having advanced
it.** What the council now holds that it did not hold yesterday is that the object exists as retrievable
by this client, its length, and its digest. **Nobody was identified.**

### P3 — the A/81/17 watch takes another null, with its positive control matching in the same file

`records[4]`: `find: "A/81/17"`, `find_matched: false`. `records[5]`: `find: "A/80/17"`,
`find_matched: true`, on the **same URL in the same run**, returning a window naming
`A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session`.

The control matched, so the page is the page we think it is and the null is a **null rather than a
void**. Its honest form, in the wording adopted yesterday: **the string `A/81/17` was not matched
against the tag-stripped, space-collapsed rendering of `https://uncitral.un.org/en/commission` at
`2026-08-17T11:33:17Z`, with a same-shaped positive control matching in the same batch.** THIS LOCATION
ONLY — nothing about ODS, the UN Digital Library, the 59th-session document listing, or the GA Official
Records; the `undocs.org`/`docs.un.org` symbol route stays **RETIRED** and was not used. Per G5 this is
**not** a finding that A/81/17 has or has not been issued.

The population count and its definition are the analyst's task 2 and are reported in his return at
Part III, derived programmatically there rather than asserted here.

### P4 — what was deliberately not fired, recorded so a later seat does not read the absence as an oversight

**(a) `investmentpolicy.unctad.org` was not fired**, and the carried figure that justifies not firing it
was **re-derived programmatically rather than copied forward.** **The carried claim of "six observed
runs" is CORRECT**, and correct because it is stated as the **403** condition specifically: the dates
are exactly `2026-08-10, 08-11, 08-13, 08-14, 08-15, 08-16` — **six**, one batch each.

**CORRECTED, `0817-B3` SUSTAINED — and this is the sharpest objection filed against me today.** My
draft asserted "an earlier and different refusal condition, `status: 500` on 08-07, 08-08 and 08-09",
and built a UNCTAD-versus-italaw contrast on it. **There is no earlier refusal condition.** Every `500`
in the corpus is on the **fabricated treaty id `/999999/`** — a pre-registered *negative control* — and
the canonical resource `/978/china---switzerland-bit-2009-` returned **ok/200 in the very same batches**
on 08-08 and 08-09. This council had already ruled on it at `2026-08-09.md:220`: *"id 999999's error is
not a window in the host's availability."* **I read a deliberately invalid URL's error as a fact about
the host's access, which is the precise thing this instrument's controls exist to prevent, and I did it
while writing a Part whose governing gate is G5.**

**The corrected form, resource-scoped per `0817-O2`:** on `/978/china---switzerland-bit-2009-` the
corpus holds **20 rows — ok/200 on 08-03, 08-04, 08-05, 08-08, 08-09 and refused/403 on 08-10, 08-11,
08-13, 08-14, 08-15, 08-16: eight refusals, all 403, over six dates, and exactly ONE access transition,
between 08-09 and 08-10, on one client.** My "28 rows / 13 refusals / 9 dates" mixed three populations —
the treaty resource, four fabricated-id controls, an IIA index page, a `zzz---zzz-bit-1900-` slug
control and a Zeph case page — and a mixed population is not an access history.

**The contrast with italaw survives, and it survives better in the corrected form:** UNCTAD has one
observed transition on one resource with one client; italaw has **two root observations, both refusals,
and one file-path observation, ok** — which is not a transition at all. That is why the italaw inference
is refused in P2 and the UNCTAD condition is treated as established here.

A seventh observation of an established condition changes no decision the council faces. What is owed is
the **operator-side** manual retrieval of the China–Switzerland BIT (2009) forum provision, and it is
owed a seventh time whether or not a row fires here. **(b) `italaw.com/cases/8154`** was withdrawn by the analyst himself on 2026-08-16 and the
withdrawal adopted; it is not silently dropped. **(c) `pca-cpa.org/en/cases/309/`** was not fired: the
Zeph docket is OFF-THEME, a scoped Ring-3 comparator only, and its open gap is an ordinal-mapping
question that a further Documents-list window is not shaped to answer.

---

## Part III — The analyst's return, reproduced verbatim

`research-analyst`, spawned as a real subagent from `.claude/agents/research-analyst.md`. Budget set by
me at four WebSearch calls and no fetch of any kind; the relay is fired by the chairman and not by him.
His text is reproduced below **exactly as returned**, including the two findings that correct this
council rather than extend it, and including his objection to a bar I placed on his own budget.

*(Chairman's note on two characters, so a later seat does not read them as evidence: the return channel
delivered `&lt;&lt;` in his heredoc and `&amp;` in one quoted TDM title as HTML entities. Those are
transport artefacts of the return path, not the member's characters and not the sources'. I reproduce
them unaltered rather than silently repairing a member's text.)*

---

## RESEARCH ANALYST — RETURN FOR 2026-08-17

**Search spend: 4 of 4 WebSearch calls. Verbatim queries, in order fired:**

1. `"Nigel Bankes"`
2. `"Expert Report of Nigel Bankes"`
3. `"Expert Report of The Brattle Group" Einarsson`
4. `"UNCT/20/6"`

No fetch of any kind was attempted. I fired nothing at the relay.

---

### 1. THE FREE REPOSITORY OPERATION — RUN, AND IT RETURNS A NULL ON THE DIRECTION THAT WOULD HAVE SETTLED I4

**Command (re-run after today's result file landed mid-session — see §2 note):**

```
grep -o -i -E ".{60}Pricewaterhouse.{25}" analytics/fetch-results/*.json
```

**Output — one occurrence in the entire committed corpus:**

```
analytics/fetch-results/2026-08-15-daily.json:u201cChip” Gordon C. Gill Expert Report of
Paul Sharp, Pricewaterhouse Coopers LLP (Public Vers
```

Programmatic adjacency tally over all 23 result files, case-insensitive: **ADJACENT (`PricewaterhouseCoopers`) = 0; SPACED (`Pricewaterhouse Coopers`) = 1.** The one carrier is `2026-08-15-daily.json`, the `icsid.worldbank.org/…CaseNo=UNCT%2F20%2F6` row, `find: "Expert Report of Nigel Bankes"`, `ok/200`, 105,669 bytes, `sha256 b9c55ab5…`, `find_matched: true`.

**Which direction the evidence runs, stated exactly.** Under the insert-only asymmetry (`scripts/fetch_relay.py:126-128`), an **adjacency** in an excerpt is reliable — nothing in the transform deletes a space or joins separated characters, so adjacent characters in an excerpt were adjacent in the source. **The corpus holds zero adjacencies.** A **space** in an excerpt is the ambiguous rendering: it is consistent with a literal space on ICSID's page, with a stripped tag between the two tokens, or with a newline. The corpus holds exactly one occurrence and it is of that ambiguous kind.

**Therefore: the operation CANNOT run in either direction.** It cannot settle I4 affirmatively (no adjacency exists to be read), and a spaced rendering cannot refute the spurious-space hypothesis either. **`I4` remains an inference, unchanged, and the free operation has been exhausted — it will not become productive by being re-run on more excerpts of the same page.** Only a fresh source outside the relay can move it. This is a null and I report it as a null; the reasoning that motivated the operation was sound and the corpus simply does not contain the observation it needed.

**Note against a temptation.** The generated summary attached to query 3 spells the firm **`PricewaterhouseCoopers`, adjacent.** That carries nothing for I4: `E7`'s scope is expressly limited to relay-excerpt strings and `find_matched` values, and WebSearch text is outside it; and under `0811-B4` summary text is not evidence at all. **Refused as bearing on I4.**

---

### 2. THE A/81/17 NULL POPULATION, RE-DERIVED — AND A CORRECTION TO THE RECORD ON THE PAIRED CONTROLS

**Procedural fact that changes how the figure must be stated.** When this session opened, the newest result file was `2026-08-16-daily.json`. **Today's result file landed mid-session** — `analytics/fetch-results/2026-08-17-daily.json`, committed at `1631c40`, `fetched_at 2026-08-17T11:33:17Z`, `control_ok: true`, `void: false`, 6 requested / 6 completed. My first two screens predate it and were re-run.

**Command:**

```python
python3 - &lt;&lt;'PY'
import json,glob,os
by_find=[]; by_url=[]
for f in sorted(glob.glob('analytics/fetch-results/*.json')):
    d=json.load(open(f,encoding='utf-8')); recs=d.get('records',[])
    ctrl80=[r for r in recs if 'A/80/17' in (r.get('find') or '')]
    for r in recs:
        if 'A/81/17' in (r.get('find') or ''):
            by_find.append((f,r.get('find_matched'),r.get('outcome'),
                [(c.get('find'),c.get('find_matched'),c.get('url')==r.get('url')) for c in ctrl80]))
print(len(by_find))
PY
```

**Figures, each with its definition attached (per `0813-B9`):**

- **Population = 13**, on the governing definition **rows carrying an `A/81/17` find**, **as of before today's result landed** — i.e. over `2026-08-03-targeted.json` … `2026-08-16-daily.json`. This reproduces `0816-O2` exactly: 11 `find_matched: false` at 11 distinct dates (08-03, 08-04, 08-05, 08-06, 08-07, 08-09, 08-10, 08-13, 08-14, 08-15, 08-16), 1 `find_matched: true` (the `undocs.org/A/81/17` request-echo on the retired route, `2026-08-04-daily.json records[3]`), 1 `robots_disallowed` with no `find_matched` key (`2026-08-08-daily.json records[1]`). 11+1+1 = 13.
- **Population = 14 with today's row included** — `2026-08-17-daily.json`, `uncitral.un.org/en/commission`, `find: "A/81/17"`, `ok/200`, `find_matched: false`. **The twelfth null.** Its `A/80/17` positive control is in the same file, on the same URL, `find_matched: true`.
- **14 by URL, before today** = the 13 by-find **plus** `2026-08-04-undocs-control.json records[2]`, `https://undocs.org/en/A/81/17`, `find: "Fifty-ninth"` — outside the population only because of how the definition is worded. This is `0816-O2`'s arithmetic and it is exact.
- **A third figure, named here so it is not mistaken for a fourth definition: 3** = rows whose **URL string literally contains** `A/81/17` or `A%2F81%2F17` (08-04-daily `records[3]`, 08-04-undocs-control `records[2]`, 08-08-daily `records[1]`). Two of those three are already inside the by-find population.

**PAIRED SAME-PAGE POSITIVE CONTROLS — verified against the files, and the record's figure of two is WRONG under the record's own test.**

The record (2026-08-16 next-step 4; escalation N3) states that **two** of the eleven nulls — 08-03 and 08-10 — carry no `A/80/17` row in the same file. **Derived from the files, it is three: 08-03, 08-10 and 08-13.** `2026-08-13-daily.json` carries five rows besides its `A/81/17` null and none of them is an `A/80/17` row. The eight controlled nulls (08-04, 08-05, 08-06, 08-07, 08-09, 08-14, 08-15, 08-16, and now 08-17) each carry an `A/80/17` row **on the same URL, in the same file, `find_matched: true`.**

**And the three uncontrolled files are not uncontrolled in the same way**, which matters because the discipline adopted yesterday is stated two different ways in two adjacent paragraphs:

| File | `A/80/17` row in same file? | ANY matching control on the SAME URL? | Same-*shaped* control? |
|---|---|---|---|
| `2026-08-03-targeted.json` | **NO** | **YES** — `records[4]`, same `uncitral.un.org/en/commission`, `find: "fifty-ninth"`, matched | **NO** (`fifty-ninth` is not a UN document symbol) |
| `2026-08-10-daily.json` | **NO** | **NO** — the only matched row is `digitallibrary.un.org/robots.txt`, a different host | NO |
| `2026-08-13-daily.json` | **NO** | **NO** — the only matched rows are two `example.com` brackets | NO |

**The honest form: under the test as the record words it in next-step 4 ("no `A/80/17` row in the same file") the answer is three, not two. Under the test as next-step 5 words it ("a same-shaped positive control matching in the same batch") the answer is also three — 08-03 fails that one too, because `fifty-ninth` is not the same shape. The two formulations agree; the record's count of two does not.** 08-08 carries an `A/80/17` row but it is `robots_disallowed` with `find_matched: null`, so it is not a matching control either — immaterial, since 08-08's `A/81/17` row is the `robots_disallowed` entry and not one of the eleven nulls.

**One further repository fact, free, bearing on a pre-registration.** `2026-08-17-daily.json` records `https://www.italaw.com/sites/default/files/case-documents/italaw1827069.pdf` as **`ok`, status 200, `application/pdf`, 1,587,866 bytes, `sha256 8de4c3db…`**. The pre-registered expectation was that a refusal would be one observation of this client's access; **it did not refuse.** The 2026-08-03 `403` therefore stands as one observation on one date and is not a standing condition. Per the pre-registration a PDF row returns existence and byte length only, and that is all I take from it. Today's ICSID window also completed the 08-16 right-edge fragment: the docket item reads in full **`Counter-Memorial on Jurisdiction, Merits and Damages`**, followed by `Procedural Order No. 2`, `No. 2 - Annex A`, `No. 2 - Annex B`, `No. 3`, `No. 3 - Annex A`, `No. 4`, `No. 5`, then truncation. Novelty-screened excluding today's artefacts: `Procedural Order No. 5` and `Jurisdiction, Merits and Damages` return hits at `2026-08-06.md:315,319` and `2026-08-06-daily.json records[4],[5]` — **referent clause: those are `CaseNo=ARB/22/2` (Huawei v. Sweden), a different matter**, so the strings are not novel to the corpus but the *Einarsson* occurrences are.

---

### 3. NIGEL BANKES — AND A METHOD RESULT LARGER THAN THE FIELD QUESTION

I did **not** search `Paul Sharp` or `Doug Uffen`. The bar was honoured.

**3.1 — Query 1, `"Nigel Bankes"`, the bare exact phrase. TITLE-CARRIED, and it CONFIRMS RULE (b) AS A CEILING WHILE FAILING THE TASK.** Ten results. Titles: `Nigel Bankes` (policyoptions.irpp.org/authors/nigel-bankes/) — new host, name-only template; `Nigel Bankes` (scholar.google.com/citations?user=2fqSsLYAAAAJ) — new host, name-only; `Nigel Bankes at University of Calgary | Rate My Professors`; `Nigel Bankes - Calgary, Alberta, Canada` (LinkedIn — the role/company slots of that template rendered as a **location**, not a role); plus the five hosts already at `2026-08-15.md:540-544`. **NOT ONE of the ten titles carries a field.** The bare phrase is the ceiling and the ceiling is below the target: **for this name no top-ranking host fills a role-or-field slot.** Rule (a) TITLE-TEMPLATE TARGETING did not fail — it correctly predicted there was nothing to aim at.

**3.2 — Query 2, `"Expert Report of Nigel Bankes"`. TITLE-CARRIED. THE FIRST NAME-TO-MATTER LINKAGE IN THIS THREAD.**

> `Einarsson and GSI v. Canada, Expert Report of Nigel Bankes, 30 Aug 2022`
> — https://jusmundi.com/en/document/other/en-theodore-david-einarsson-harold-paul-einarsson-and-russell-john-einarsson-geophysical-service-incorporated-v-government-of-canada-expert-report-of-nigel-bankes-tuesday-30th-august-2022

**What it carries:** a host titles a document `Expert Report of Nigel Bankes` and, **in the same title string**, attaches it to the matter `Einarsson and GSI v. Canada` and to the date **30 Aug 2022**. jusmundi's template has a matter slot and a date slot. The URL slug independently carries the three claimant forenames and `government-of-canada`.

**What it does NOT carry:** that this Nigel Bankes is the Calgary-law Nigel Bankes of `2026-08-15.md:540-544`. **The join between name→matter and name→institution is a same-name inference and I do not make it.** It does not carry his field, a rank, or what the report is about.

**Also title-carried, and NEW by screen (0 files before today):** `EXPERT REPORT OF NIGEL BANKES` at `https://www.italaw.com/sites/default/files/case-documents/italaw171339.pdf` — a distinct italaw file id. **That title does not name the matter**, so on its own it is a same-title risk; it is corroborative only alongside the jusmundi title.

**Narrow gain against a specific prior denial:** `2026-08-15.md:548` records that the then-known title set "does not carry a rank, a title, a chair". Today's set includes `Address by Professor Nigel Bankes | Request PDF` (researchgate) — the token **`Professor`** is now title-carried in association with the name. It carries a rank token and nothing more: no field, no institution, no date, no link to the docket. And `Nigel Bankes, Author at Energy Regulation Quarterly` carries a **publication name**, which is a field *proxy* and is **not** a statement of field. **The field question stands OPEN.**

**3.3 — Query 3, `"Expert Report of The Brattle Group" Einarsson`. THE RICHEST TITLE THIS RECORD HAS SEEN, AND TWO PERSONS NAMED.**

> `Theodore D. Einarsson and others v Canada ICSID Case No. UNCT/20/6 - NAFTA - Expert Report of The Brattle Group - 16 January 2023 - Legal &amp; Regulatory docs. | TDM Journal`
> — https://www.transnational-dispute-management.com/legal-and-regulatory-detail.asp?key=33851

One title string carrying **five slots**: parties, **case number `ICSID Case No. UNCT/20/6`**, treaty **`NAFTA`**, document, and date **16 January 2023**. Screened: `transnational-dispute-management` returns 15 files, all of them either Huawei `ARB/22/2` Procedural Order No. 8, the Zeph item at `key=37053`, or the `ALLOWED_HOSTS` escalation — **`key=33851` is new**.

> `Einarsson and GSI v. Canada, Expert Report of Darrell Chodorow and Alexis Maniatis, 16 Jan 2023` — jusmundi

**`Chodorow` → 0 files in the repository. `Maniatis` → 0 files.** Two persons are now title-carried into this matter with a document and a date. **The gap `einarsson-unct-20-6-expert-identities` records ZERO persons identified; that is no longer accurate as to name-to-matter carriage, though it remains accurate that no person has been *identified* (linked to a biography, a firm, or a field from a body).**

> `RER-04 Expert Report of the Brattle Group | italaw` — https://www.italaw.com/cases/documents/10912

`cases/documents/10912` → 0 files: new. **This is second-host corroboration that the prefix `RER-04` is part of the item's designation on italaw as well as in our relay excerpt** — the prefix is not an artefact of our transform. It does **not** decode what `RER-` means.

**The 16 January 2023 date appears in both the TDM title (attached to `The Brattle Group`) and the jusmundi title (attached to `Chodorow and Maniatis`). That the two titles describe the same document is an INFERENCE and I do not assert it.**

**3.4 — Query 4, `"UNCT/20/6"` bare. NULL, AND THE NULL IS A METHOD FINDING.** Eight results; the top hit is `Carlos Sastre and others v. Mexico, ICSID Case No. UNCT/20/**2**`, the rest are UN Country Team documents. **The case number was not reachable as a bare exact phrase even though query 3 proved the string is indexed verbatim in a TDM title.** `UNCT` collides with "UN Country Team" and the number tokens are common. **This qualifies adopted rule (b):** the bare exact phrase is floor and ceiling only where the phrase is *lexically* distinctive. A designator that is rigid **in law** — a docket number — can be non-distinctive **to the index**. The party surname `Einarsson` outperformed the case number as a retrieval key.

**3.5 — DS20215: a title that is new, on a URL that is forty-five days old.** Query 3 returned `119035102 v1 IN THE MATTER OF AN ARBITRATION UNDER CHAPTER ELEVEN OF THE` at `https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C10678/DS20215_En.pdf`. **Applying rule (g) — I named the seat most likely to have found it and read it: `2026-08-06.md` was not it; `2026-07-03.md:184` was.** That record already cites this exact URL, labelled `(ICSID pleadings, DS20215)`, and `2026-07-04.md:103` / `2026-07-05.md:116` cite `DS20900_En.pdf` under the same `C10678` path. **The URL is not new and I withdraw any claim that it is.** What *is* new is the **host-generated title** — the record previously held only an author-supplied label. The path token `OnlineAwards` is a directory name and I do **not** read it as saying the document is an award.

---

### 4. METHOD NOTE

**The formulation that produced today's only substantive findings is: SEARCH THE DOCUMENT STRING, NOT THE PERSON.** `"Expert Report of Nigel Bankes"` did in one call what eight profile-oriented queries across 08-15 and 08-16 could not. The reason is template shape: **hosts that index case documents (jusmundi, italaw, TDM) have title templates with a MATTER slot and a DATE slot, which are exactly the slots an identity-linkage gap needs.** Profile hosts have neither. This does not supersede rule (a); it supplies the missing half — rule (a) says look for a template with the slot you need, and today identifies *which family of hosts* has the matter-and-date slots.

**Consequence I am obliged to report, against the instruction I was given.** The bar on `Paul Sharp` and `Doug Uffen` rests on the stated ground that "the residual is identity linkage, which a title cannot carry." **Today falsifies that premise: a title did carry identity linkage.** The document-string forms — `"Expert Report of Paul Sharp"`, `"Expert Opinion of Doug Uffen"` — were never fired; only person-oriented forms were. **I honoured the bar and fired neither.** I propose it be lifted for the document-string form only, and that is the chairman's call, not mine.

**Novelty-screen discipline applied to myself.** I never piped a screen into `head`. I excluded every `2026-08-17` date-stamped artefact from the novelty pass and ran a separate all-files pass first so that a pattern matching nothing anywhere would be caught as a dud rather than banked as a null — that pass caught `Chodorow`, `Maniatis`, `cases/documents/10912` and `italaw171339` at 0-everywhere, which is why I verified each against today's search output before calling it new. **Amendment 2 referent clauses for the nonzero screened terms:** `jusmundi` 26 files — Zeph, Hela Schwarz, Factory at Chorzów and a June 23 *Einarsson* judgment citation, none concerning Bankes or Brattle; `icsidfiles` 18 — Huawei/Hela pathfinding and the `ALLOWED_HOSTS` list; `C10678` 5 — already mapped to `UNCT/20/6` at `2026-07-05.md:116`; `arbitration.org` 11 — `award/785` and `award/1058`, and the record twice flags that host as having given inconsistent characterisations (`2026-07-16.md:77`, `2026-07-17.md:22`), so I give its `award/910` title no weight; `Einarsson and GSI` 1 — `2026-06-23.md:45`, a jusmundi citation to the 2020 FC 984 judgment under the same slug family, **which means jusmundi was already an established carrier for this matter and my linkage finding is a new document on a known host, not a new host.**

---

### 5. WHAT I REFUSED TO ASSERT, BY NAME

1. **That Nigel Bankes's field is natural resources law, or that he is an emeritus professor, or that he held a chair.** Query 1's and query 2's generated summaries both say so. Both are summary text about pages I did not open. **Refused entire under `0811-B4`** — and refused for the second consecutive day on the same proposition, which is the correct outcome, not a stalemate.
2. **That the Nigel Bankes on ICSID's `UNCT/20/6` docket is the Nigel Bankes of the Calgary law pages.** Same-name join. Not carried by any title.
3. **That `italaw171339.pdf` is the *Einarsson* Bankes report.** Its title names no matter.
4. **That the Brattle report was prepared by Maniatis and Chodorow, that it responds to Paul Sharp, that Brattle was engaged by Canada, or that Sharp is of PricewaterhouseCoopers LLP.** All of it is query 3's summary. **The same refusal the 08-16 seat made; the summary reappeared and I refuse it again.**
5. **That the TDM and jusmundi 16 January 2023 titles describe the same document.**
6. **That `DS20215_En.pdf` is an award, or is the *Einarsson* award.** `OnlineAwards` is a directory name.
7. **That anything follows about the *Einarsson* case's IP or pharmaceutical nexus** from the expert-identity position, **in either direction** (`0815-B9`).
8. **That the adjacent spelling `PricewaterhouseCoopers` in a search summary bears on I4.**
9. **Any figure assembled from search snippets.** None appears above.

---

### 6. LIMBS NOT REACHED

- **Nigel Bankes's field — STILL OPEN.** Two calls spent on the name; zero titles with a field slot. This is now a **twice-failed** target and I do not recommend a third call on it.
- **I4 — closed as to the free operation, open as to the fact.** No fresh source touched it.
- **`RER-`/`RWS-` prefix decoding — UNTOUCHED.** italaw independently renders `RER-04`, which corroborates the token but decodes nothing.
- **Identity linkage for `Ralph Maitland`, `"Chip" Gordon C. Gill`, `Robert Hobbs`, `Carl Makrides`, `Bharat Dixit`, `Trevor Bennett`** — the document-string method is now shown to work and was applied to **two** of the docket's items only.
- **`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground`** — zero budget spent; untouched.
- **The China–Switzerland BIT (2009) forum provision** — escalated, zero budget, correctly untouched.
- **The weekly digest rollover** — the chairman's item; I did not read `meta.json` and assert nothing about the digest of record.

---

### 7. LEDGER

**ESTABLISHED**

- **`0817-E1`** — The committed relay corpus contains exactly **one** occurrence of `Pricewaterhouse` (2026-08-15, ICSID `UNCT/20/6` row) and it is **spaced**; **zero adjacencies** exist. *Repository-derived, BODY-CARRIED from a committed artefact.*
- **`0817-E2`** — A/81/17 population = **13** before today's result landed, **14** after, on the definition *rows carrying an `A/81/17` find*; 14-before / 15-after if the by-URL row is admitted; 3 rows carry the symbol in the URL string. *Programmatically derived; reproduces `0816-O2` exactly.*
- **`0817-E3`** — **THREE**, not two, of the eleven pre-today nulls lack an `A/80/17` row in their own file: **08-03, 08-10, 08-13.** Under the "same-shaped control in the same batch" wording the answer is also three, because 08-03's same-URL control `fifty-ninth` is not the same shape. **The record's figure of two is corrected.** *BODY-CARRIED from the result files.*
- **`0817-E4`** — jusmundi titles a document `Expert Report of Nigel Bankes` in `Einarsson and GSI v. Canada`, dated **30 Aug 2022**. *TITLE-CARRIED.*
- **`0817-E5`** — TDM titles a document with `ICSID Case No. UNCT/20/6`, `NAFTA`, `Expert Report of The Brattle Group`, `16 January 2023` in one string. *TITLE-CARRIED.* New to the corpus by screen.
- **`0817-E6`** — jusmundi titles a document `Expert Report of Darrell Chodorow and Alexis Maniatis, 16 Jan 2023` in the same matter. Both surnames screen to **0 files**. *TITLE-CARRIED, NEW.*
- **`0817-E7`** — italaw independently renders the item as `RER-04 Expert Report of the Brattle Group`. *TITLE-CARRIED; corroborates the `RER-04` prefix from a second host.*
- **`0817-E8`** — `italaw1827069.pdf` returned **ok/200, application/pdf, 1,587,866 bytes** on 2026-08-17. The 08-03 `403` was one observation on one date, not a standing condition. *BODY-CARRIED from `2026-08-17-daily.json`.*
- **`0817-E9`** — `icsidfiles.worldbank.org` and `www.italaw.com` **are** on `ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`); `jusmundi.com`, `www.transnational-dispute-management.com` and `arbitration.org` are **not**. *BODY-CARRIED.*

**INFERENCE**

- **`0817-I1`** — I4 (the space in `Pricewaterhouse Coopers LLP` is a transform artefact) remains **an inference and is now known to be unreachable from the existing corpus**.
- **`0817-I2`** — That the two 16 January 2023 titles describe one document. *Not asserted.*
- **`0817-I3`** — That the docket's Nigel Bankes is the Calgary-law Nigel Bankes. *Not asserted.*
- **`0817-I4`** — METHOD: document-string queries reach matter-and-date title templates that person-name queries cannot. **n = 2 successes, 1 method-null (query 4).** Proposed as a rule, not banked as one.

**REFUSED** — `0817-R1` through `0817-R9`: the nine items enumerated by name in §5.

---

### 8. WHAT I PROPOSE AS TOMORROW'S WORK

1. **Relay rows, proposed for the chairman's batch — I fire nothing.** All on `ALLOWED_HOSTS`, all previously unfired: **(a)** `https://www.italaw.com/cases/documents/10912`, find `RER-04` — an HTML document page, so it returns an excerpt, and it is the cheapest test of whether italaw's document pages carry a date and a filing party. **(b)** `https://www.italaw.com/sites/default/files/case-documents/italaw171339.pdf`, no find — existence and byte length only; establishes the Bankes report exists as a file on the same host as the Brattle report. **(c)** `https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C10678/DS20215_En.pdf`, no find — existence and byte length; the record has cited this URL since 2026-07-03 and **has never observed it**. Each PDF row returns existence and byte length only; pre-register that.
2. **Slide the *Einarsson* window on `Procedural Order No. 5`** — bounded on both sides in today's window, pure ASCII. It is the furthest-right bounded designator today's excerpt provides. Do **not** anchor on the trailing fragment `Procedural`.
3. **Correct the paired-control figure from two to three (08-03, 08-10, 08-13)** in `STATE_OF_THE_ANSWER.md` and wherever the 08-16 close-out's wording propagated. **This is a correction to a named weakness in a standing watch and it must be traced to a commit sha, not certified in a sentence** — `0816-B1` fired on exactly that.
4. **Rule on the `Paul Sharp` / `Doug Uffen` bar.** Its stated ground is falsified; the document-string forms are untried. Chairman's call.
5. **Do not spend further budget on Nigel Bankes's field.** Twice failed, ten titles screened, no host with the slot. Record it as a bounded negative rather than re-firing.
6. **`GAP-UNRESOLVED: einarsson-unct-20-6-expert-identities` stays OPEN, with its annotation amended:** two persons — `Darrell Chodorow`, `Alexis Maniatis` — are now **title-carried into the matter**; **zero persons are identified**; `RER-`/`RWS-` remain undecoded; and per `0815-B9` nothing about the case's IP or pharmaceutical nexus follows from any of it.

---

## Part IV — The digest of record (Rule 1)

`Digest 2026-08-10: 11 candidates evaluated, 3 items surfaced (0 matches, 3 watch-list leads).`

Copied field-by-field from `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` at
`screened: 11`, `matches: 0`, `watch_list_leads: 3`, read from the file in this session and **not
restated from memory or carried forward from yesterday's record.**

**This is the SEVENTH consecutive record to carry the 2026-08-10 digest, and the 2026-08-16 close-out
predicted it would be the day it moved. It did not, and the prediction was wrong for a reason worth
recording rather than quietly dropping.** Next step 2 of that close-out reads "TOMORROW IS MONDAY AND
THE WEEKLY COLLECTION RUNS AT 13:00 UTC… For the first time in six records the digest of record will
move." The cron is correctly cited — `.github/workflows/weekly.yml:5` is `cron: "0 13 * * 1"`, Mondays
13:00 UTC. **What the prediction missed is that this session opens at ~11:30 UTC**, roughly ninety
minutes *before* the collector runs. The digest of record at the time this Part was written is
`2026-08-10`, the newest directory under `digests/` is `2026-08-10_ISDS-Thematic-Watch`, and no
2026-08-17 digest exists to copy from. **A seat cannot copy numbers out of a file that will not exist
for another hour, and it must not pre-write them.**

So the honest form is: **the digest of record has not moved as of this record, and it is expected to
move later today, after this session has closed.** Tomorrow's seat inherits the actual rollover and must
copy the Rule 1 sentence from the **new** `meta.json` rather than from this record.

**No per-source ratio is constructed here, per `0816-B8`.** The 2026-08-10 file's
`source_health[].count` values sum to 12 against `screened: 11`; the two fields do not reconcile, and
nothing in the file documents the mapping between them. Any figure built by dividing one into the other
would be a fabrication of precision, and none appears above.

**One `source_health` field is quoted because Part II depends on it, and only that one.** The `italaw`
entry reads `status: "NOT-READ (403)"`, `count: 0`, `refusal_count: 1`, with its single logged refusal
URL `https://www.italaw.com/`. That is the fact Part II P2 uses, and Part II P2 states in terms that it
is a **different client on a different network** and corroborates nothing about this council's own
access.

---

## Part V — The council discussion

Rule 0 files the integrity challenge here rather than as a separate Part. It was produced by a **real
subagent** (`.claude/agents/integrity-officer.md`), not performed by me, and it is reproduced verbatim.
It cost no external budget: the officer fired nothing at the relay and spent no search call.

**It filed ten binding objections. Four of the six against the chairman are against the arithmetic and
the reasoning of the two Parts I wrote before either member had reported, and one of them — `0817-B3` —
is the sharpest catch of the day.**

### The integrity challenge, reproduced verbatim

## VETTING NOTE — integrity officer, 2026-08-17

Protocol: every figure, quotation, line citation and screen in Parts I–III was re-derived by me from `analytics/fetch-results/*.json`, `scripts/fetch_relay.py`, the dated records and `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` before being challenged or cleared; I ran each member's own command rather than reading their report of it (`0816-O9`); I filed no HTTP-status objection and therefore needed no positive control; the taxonomy was read from `/home/user/isds-watcher/agents/integrity-officer.md` (27 entries, collision at 27 unresolved) and not recited from my definition.

---

## BINDING OBJECTIONS

### `0817-B1` — CHAIRMAN. Part II P1 ("Eight items new to this record for UNCT/20/6", line 139) and Part I optimization item ("all eight items are new to the record", line 101).

**Wrong.** The scoped screen was run over **committed relay excerpts only**. The record is the repository, not the relay corpus. Screened repo-wide, excluding today's artefacts, two of the eight fail:

- **`Procedural Order No. 4` is already in this record for this case.** `analytics/daily-research/2026-08-13.md:290` — *"Procedural Order No. 4 was issued on 9 Feb 2024 regarding consequences for the proceedings of the passing of Claimant Theodore David Einarsson"* — and `:726`, `:813`. It entered as a generated-summary item refused as unassertible, but it entered, four days ago, named to this matter.
- **`Counter-Memorial on Jurisdiction, Merits and Damages` is not a new item.** P1 says so itself two paragraphs earlier: the record has held it since 2026-08-16 as the truncated fragment `Counter-Memorial on Jurisdiction, Merits and Dama` (`analytics/fetch-results/2026-08-16-daily.json` `records[2]`; `analytics/daily-research/2026-08-16.md:215`). What today adds is its completion, not its appearance. The screen returned zero because a completed designator cannot match its own truncated form — the string was new, the item was not.

Beyond those two, the record has carried since `2026-07-01.md:76` — and at `2026-07-02.md:84`, `2026-07-05.md:114`, `2026-07-09.md:93,178`, `2026-07-10.md:93`, `2026-07-14.md:74`, `2026-07-25.md:36`, `2026-07-26.md:34`, `2026-07-28.md:77`, `2026-07-29.md:87`, `2026-08-12.md:386`, and `STATE_OF_THE_ANSWER.md:32` — the proposition that **Procedural Order No. 9 is the latest indexed document in UNCT/20/6**, and `2026-06-23.md:49` carries a jusmundi Einarsson **Procedural Order No. 10**. A record holding "No. 9 is the latest" is not a record to which the existence of lower-numbered orders in this case is news.

**Second limb.** The numeral "eight" is a segmentation claim over a delimiter-free window. `0815-B2` is standing (the relay strips every tag and attribute, so no delimiter survives and no segmentation is derivable), and P1 restates `0811-B5` — *"no count, no enumeration… may be drawn from a list still truncated at both ends"* — nine lines below giving a count and an enumeration of exactly that list.

**Corrected form.** *"Of the eight strings this window adds, six are new to this record for UNCT/20/6: `Procedural Order No. 2`, `No. 2 - Annex A`, `No. 2 - Annex B`, `No. 3`, `No. 3 - Annex A`, `No. 5` (each 0 files repo-wide before today). `Procedural Order No. 4` is not new — `2026-08-13.md:290` and `:726` name it for this matter. `Counter-Memorial on Jurisdiction, Merits and Damages` is not new as an item; it is the completion of the fragment carried since 2026-08-16. Segmentation into 'items' is by eye and is not derivable from a delimiter-free window (`0815-B2`); state them as strings."*

### `0817-B2` — CHAIRMAN. Part I optimization item, "Its honest limit" (lines 106–109).

The stated limit — *"scoping by URL is only correct where one URL is one resource"* — is true, is not the operative limit today, and is not the one the day's own evidence exposed.

The unscoped screen produced four **false negatives** (verified: `Procedural Order No. 2`, `No. 3`, `No. 4`, `No. 5` each hit `2026-08-06-daily.json` `records[4]` and `records[5]`, both `CaseNo=ARB/22/2`; the other four items hit nothing). The scoped re-run then produced **false positives**, for the reason `0817-B1` sets out: the scope excluded the rest of the repository, where two of the eight sit in the right case. Scoping did not remove the error; it changed its sign. The rule's real limit is the one my canonical training states and this record does not: **a screen establishes novelty only within the corpus screened; a novelty claim about "the record" must be screened against the record.** URL-scoping is the correct instrument for the homonym problem and the wrong instrument for a novelty claim, and the two must not be answered by one command.

**Corrected form.** Keep the rule — when the designator is not rigid, rigidity is supplied by scoping the corpus. Add: *"and a URL-scoped screen answers only 'novel within these windows of this URL'. Any claim of novelty **to the record** requires a second, repository-wide pass, because scoping converts the homonym false-positive into a false-negative of the opposite kind. Both passes and both scoping keys are reported, or neither figure is."*

### `0817-B3` — CHAIRMAN. Part II P4(a), lines 232–236.

*"there is an earlier and different refusal condition, `status: 500` on `2026-08-07`, `08-08` and `08-09`"* — and the contrast built on it, *"on UNCTAD the corpus does hold a genuine before-and-after on one client."*

**There is no earlier refusal condition.** Every `500` in the corpus is on a **fabricated treaty id**, `/999999/`, fired as a pre-registered negative control:

| file | row | path tail | outcome |
|---|---|---|---|
| `2026-08-07-daily.json` | `[3]` | `/999999/china---switzerland-bit-2009-` | refused / 500 |
| `2026-08-08-daily.json` | `[6]` | `/999999/china---switzerland-bit-2009-` | refused / 500 |
| `2026-08-08-daily.json` | `[7]` | `/999999/zzz---zzz-bit-1900-` | refused / 500 |
| `2026-08-09-daily.json` | `[4]` | `/999999/china---switzerland-bit-2009-` | refused / 500 |

The canonical resource `/978/china---switzerland-bit-2009-` returned **ok/200 in the same batches** on 08-08 (`records[4]`) and 08-09 (`records[1]` and `records[6]`). This council already ruled on it: `analytics/daily-research/2026-08-09.md:220` — *"`investmentpolicy.unctad.org` was answering before row 4 and after it; **id 999999's error is not a window in the host's availability.**"* Reading those rows as a host access condition is a superseded-formulation restatement (taxonomy 10) laid over a control-inside-the-suspect-set (taxonomy 15), and it will read to the next seat as UNCTAD having refused this client since 08-07.

**Corrected form.** *"On the canonical resource `/978/china---switzerland-bit-2009-` the corpus holds ok/200 on 08-03, 08-04, 08-05, 08-08 and 08-09 and refused/403 on 08-10, 08-11, 08-13, 08-14, 08-15 and 08-16 — **one** access transition, between 08-09 and 08-10, on one client. The `500`s of 08-07 to 08-09 are not part of it: every one is on the fabricated id `/999999/`, a negative control, and `0809` ruled that its error is not a window in the host's availability. That single transition is what italaw lacks, and it is enough to carry the contrast."*

The 403 figure survives untouched and I confirm it: six dates, `2026-08-10, 08-11, 08-13, 08-14, 08-15, 08-16`, one batch each — the carried "six observed runs" is correct.

### `0817-B4` — CHAIRMAN. Part II P1, line 130.

*"The **48-character** tail of yesterday's window is a substring of today's."* The tail quoted, `Counter-Memorial on Jurisdiction, Merits and Dama`, is **49 characters**. The 48-character tail is `ounter-Memorial on Jurisdiction, Merits and Dama`.

The substantive claim is **sound and I verify it**: the 49-character tail occurs in today's window at index 145, continuing `…Damages Proc`; the anchor `RWS-02 - Witness Statement of Trevor Bennett` is bounded on both sides in `2026-08-16-daily.json` `records[2]` and is pure ASCII; both windows are 400 characters. Only the numeral is wrong — but it is the numeral in the sentence whose whole claim is mechanical precision, and `0816-B3` fired on this seat's window-to-window arithmetic. **Corrected form: "49-character".**

### `0817-B5` — ANALYST. §2, final paragraph (record line 341).

*"Novelty-screened excluding today's artefacts: `Procedural Order No. 5` **and `Jurisdiction, Merits and Damages`** return hits at `2026-08-06.md:315,319` and `2026-08-06-daily.json records[4],[5]` — **referent clause: those are `CaseNo=ARB/22/2` (Huawei v. Sweden), a different matter**."*

**`Jurisdiction, Merits and Damages` returns zero hits.** Repo-wide, excluding today's artefacts, `grep -rn -F "Merits and Damages"` = **0**. The four cited locations carry `Procedural Order No. 5`; none carries the other string. I read `2026-08-06.md:310–322` directly to be sure.

The direction is the dangerous one and it is the one his own §4 discipline is written against: a genuinely novel string is demoted to "not novel to the corpus", and the referent clause — which under Amendment 2 exists to stop a nonzero count reading as corroboration — is here **borrowed from a co-screened term for a term with no hits at all**. That is a pattern the taxonomy does not yet hold; I propose it at `0817-O7`.

**Corrected form.** *"`Procedural Order No. 5` returns hits at `2026-08-06.md:315,319` and `2026-08-06-daily.json records[4],[5]`; referent clause: those are `CaseNo=ARB/22/2` (Huawei v. Sweden), a different matter. `Jurisdiction, Merits and Damages` returns **zero** hits repo-wide before today and is novel as a string."*

### `0817-B6` — ANALYST. `0817-E8` (record line 434) and §2, final paragraph.

*"The 08-03 `403` was one observation on one date, **not a standing condition**."*

**The analyst is the looser of the two seats, and the chairman's Part II P2 is right.** The word doing the damage is *therefore*: he derives the non-standing of the 08-03 refusal **from today's 200**, and today's 200 is on a different URL path fourteen days later. A standing refusal on `https://www.italaw.com/` is entirely consistent with an ok/200 on `…/sites/default/files/case-documents/italaw1827069.pdf`; nothing in the corpus separates them. P2 refuses exactly this at lines 178–182 and is correct to: *"the two observations are not a before-and-after on the same resource… two variables moved at once."* The third observation cuts against him — `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` `source_health[3]` logs a **second root refusal** (`status: "NOT-READ (403)"`, `refusal_count: 1`, url `https://www.italaw.com/`), which I read in the file. Two of two root observations are refusals.

Second limb: E8 is filed as **ESTABLISHED, "*BODY-CARRIED from `2026-08-17-daily.json`*"**. The bytes, content-type and digest are body-carried and correct. "Not a standing condition" is a negative claim about a different URL and is carried by nothing in that file. An ESTABLISHED entry may not contain it.

**Corrected form for E8.** *"`italaw1827069.pdf` returned ok/200, `application/pdf`, 1,587,866 bytes, sha256 `8de4c3db…` on 2026-08-17. BODY-CARRIED. The 2026-08-03 `403` was on the site **root**; this row is on a case-documents file path fourteen days later, so it neither confirms nor retires that refusal, and the corpus now holds two root observations, both refusals, from two clients. Whether the 403 is standing, path-specific or expired is **untested** and needs one root row and one file-path row in the same batch from the same client."*

### `0817-B7` — ANALYST. §3.2 heading and `0817-E4` (record lines 351–356).

Two limbs.

**(a) "THE FIRST NAME-TO-MATTER LINKAGE IN THIS THREAD" is false.** The record has held name-to-matter for Nigel Bankes since **2026-08-15**, from a stronger carrier: `analytics/fetch-results/2026-08-15-daily.json` `records[2]`, `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6`, `find: "Expert Report of Nigel Bankes"`, `ok/200`, 105,669 bytes, sha256 `b9c55ab5…`, `find_matched: true` — the administering institution's own docket, body-carried, matched. `2026-08-15.md:548` says so in terms: *"The proposition that a person named Nigel Bankes authored an expert report in UNCT/20/6 rests on ICSID's own case-detail page alone."* What jusmundi's title adds is a **date** (30 Aug 2022) and a second host, and the date is the genuinely new element.

**(b) "The URL slug **independently** carries the three claimant forenames and `government-of-canada`" — strike "independently".** The slug is the same host's derivation of the same title string (it also carries `tuesday-30th-august-2022`, the same date). A title-derived slug is one observation rendered twice, not a second carrier. Taxonomy 16, second-instrument corroboration fallacy.

**Corrected form.** *"E4 — jusmundi titles a document `Expert Report of Nigel Bankes` in `Einarsson and GSI v. Canada`, dated 30 Aug 2022. TITLE-CARRIED. **The name-to-matter link is not new** — ICSID's own docket carried it body-matched at `2026-08-15-daily.json records[2]`. New is the **date** and a second host. The URL slug is the same host's rendering of the same title and is not independent carriage."*

### `0817-B8` — ANALYST. `0817-E7` and §3.3 (record lines 375–377, 433); §5 does not cover it.

E7 banks *"italaw independently renders the item as `RER-04 Expert Report of the Brattle Group`… second-host corroboration."* **That title names no matter.** It is the identical shape to `EXPERT REPORT OF NIGEL BANKES` at `italaw171339.pdf`, which he correctly caveats at §3.2 — *"That title does not name the matter, so on its own it is a same-title risk"* — and refuses at §5.3. No parallel caveat and no parallel refusal is given for `cases/documents/10912`, and the risk is higher, not lower: `RER-04` is a case-relative exhibit designator, and italaw hosts many cases. **The §5 refusal list therefore does not cover what §3.3 asserts** — which is the specific test the chairman asked me to run.

**Corrected form.** Either add to §5: *"That `www.italaw.com/cases/documents/10912` is the UNCT/20/6 item our relay excerpt renders as `RER-04, Expert Report of The Brattle Group`. Its title names no matter; `RER-04` is case-relative."* — or downgrade E7 from ESTABLISHED corroboration to an inference. His own §8.1(a) proposal (fire that URL with find `RER-04`) is the right way to settle it and should stand.

### `0817-B9` — ANALYST. §4 "Consequence I am obliged to report" and `0817-I4` / §8.4 (record lines 391, 442, 453).

*"The bar… rests on the stated ground that 'the residual is identity linkage, which a title cannot carry.' **Today falsifies that premise: a title did carry identity linkage.**"*

**He is wrong, and the bar's stated ground survives.** The term is used in two senses one section apart. The record's sense is fixed at `state/research_log.json:8` — *"Residual is IDENTITY LINKAGE, which no page title can carry — it is a **BODY** question"* — and means the join from a docket name to a **person** (a biography, a firm, a field). What today's titles carried is name-to-**matter**. He says so himself, three times: §3.2 (*"The join between name→matter and name→institution is a same-name inference and I do not make it"*), §5.2, and §8.6 (*"**zero persons are identified**"*). Nothing today touched name-to-person for anyone.

For Paul Sharp and Doug Uffen the point is sharper still: name-to-matter is **already held body-carried** from ICSID's own docket (`2026-08-15-daily.json records[2]`; `2026-08-16-daily.json records[2]`, find `RER-03 - Expert Opinion of Doug Uffen`, matched). A document-string query would return what the record already has, from a weaker carrier.

**Corrected form for I4.** *"METHOD: document-string queries reach case-document hosts whose title templates carry a matter slot and a **date** slot; profile hosts have neither. n = 2, 1 method-null. It does **not** bear on identity linkage in the record's sense (name→person, a body question), and the bar's stated ground is unfalsified."* My recommendation on the ruling is at `0817-O6`.

### `0817-B10` — ANALYST. §1, the block labelled "**Output — one occurrence in the entire committed corpus:**" (record lines 284–287).

I ran his command verbatim. Actual output:

```
analytics/fetch-results/2026-08-15-daily.json:u201cChip” Gordon C. Gill Expert Report of Paul Sharp, Pricewaterhouse Coopers LLP (Public Vers
```

The record renders `Chip”`. The `”` escape has been decoded; the `u201c` on the other side has not. A block presented as command output must reproduce from the command. The **substance is exactly right** and I confirm it independently — one occurrence, spaced. **Corrected form:** restore `”`, or bring the block under the chairman's transport-artefact note at record lines 255–257 so no seat later reads a curly quote as a character on ICSID's page.

---

## OBSERVATIONS

**`0817-O1` — CHAIRMAN, Part I, "6 prior windows, 2,420 characters, both canaries live".** Six reproduces; 2,420 does not. Rows on the UNCT/20/6 URL before today: **8**, of which one (`2026-08-14-daily.json records[2]`) has an empty excerpt and two are byte-identical, giving **7 non-empty / 6 distinct**, and **2,400** characters (400 × 6), or 2,800 counting all non-empty rows. 2,420 is reachable only by joining six windows with a 4-character separator. The dedup rule is unstated and the two canaries are unnamed — against this seat's own new rule in the same paragraph that the scoping key be named whenever a novelty count is reported. Recommend: *"six distinct non-empty windows of 400 characters, 2,400 characters, deduplicated by digest; canaries `X` and `Y` live."*

**`0817-O2` — CHAIRMAN, Part II P4(a), "28 rows… 13 of them refusals… 9 distinct dates".** All three reproduce exactly under the reading that "9 distinct dates" attaches to the refusals (08-07 through 08-16 less 08-12; the corpus-wide figure for all 28 rows is **13** distinct dates, the same numeral as the refusal count — a collision worth avoiding in one sentence). But the 28 mixes three populations: the canonical treaty resource, four fabricated-id control rows, one IIA index page, one `zzz---zzz-bit-1900-` slug control, and one Zeph case page (`2026-08-15-daily.json records[8]`). Resource-scoped, the figures are: 20 rows on `/978/china---switzerland-bit-2009-`, 8 refusals, all 403, on 6 dates. Recommend the resource-scoped form; see `0817-B3`.

**`0817-O3` — ANALYST, §8.3.** The correction is directed at `STATE_OF_THE_ANSWER.md`, which does not carry the figure — I grepped it. The actual carriers of "two (08-03, 08-10)" are `analytics/council-log.md:14` and `state/research_log.json:8` (both standing summaries, both correctable), plus `analytics/daily-research/2026-08-16.md:1151, :1257, :1492, :1580`, which are dated records and must be superseded, never rewritten (`2026-08-15.md:763`). Naming the wrong file is how `0816-B1` happened.

**`0817-O4` — a gain neither seat claimed, and it is larger than the one they did.** Today's window **discharges a standing refusal**. On 2026-08-13 the council refused `Procedural Order No. 4` in this matter as generated-summary-only and unassertible (`2026-08-13.md:290, :726, :813`). ICSID's own docket now carries the string, body-carried, matched, control-bracketed. The record can assert the item's existence (not its date, not its content, not "latest"). This is the day's strongest Einarsson result and it was invisible to a screen scoped to relay excerpts — which is `0817-B2` in one concrete instance.

**`0817-O5` — ANALYST, §3.1.** "Ten results… **NOT ONE of the ten titles carries a field**" is supported by nine: four new hosts named, plus the five at `2026-08-15.md:540-544` (verified: five). §3.2 then cites `Nigel Bankes, Author at Energy Regulation Quarterly`, a sixth host not in that five. An absolute over a set one member of which is not shown. Non-binding — I cannot re-run his search — but it bears on §6's recommendation to spend no further budget, which I otherwise think is right.

**`0817-O6` — my recommendation on the ruling the chairman asked for.** Refuse the analyst's ground (`0817-B9`) and consider lifting the bar on a different one, if the chairman wants it lifted. The real value of a document-string query on a case-document host is not title carriage — it is that it returns a **file id on an `ALLOWED_HOSTS` host**, i.e. a fetchable body. `state/research_log.json:8` says the residual "is a BODY question, and the named target is …italaw1827069.pdf (UNREAD)"; `E9` is correct that `www.italaw.com` is on `ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`, verified, and jusmundi / TDM / arbitration.org are correctly absent). `"Expert Report of Paul Sharp"` is worth one call **if and only if** it is framed as locating a relay-fetchable file, and the return is banked as a URL and nothing else. That is a different justification from the one offered and the chairman should rule on it as such.

**`0817-O7` — taxonomy extensions proposed** (vault stands at 27 entries; collision at 27 unresolved, so I propose by name, never by number, per the table's own instruction):

- **Phantom-hit referent** — a screened term reported as returning hits, annotated with a referent clause borrowed from a co-screened term that actually carries them, when the term's own count is zero. Inverse of Amendment 2: Amendment 2 catches a nonzero count with no referent; this catches a referent with no count, and it converts a novel string into a stale one silently. Instance: `0817-B5`.
- **Fragment-evades-screen** — a novelty screen on a completed designator returns zero against a corpus that holds the same item truncated, so completion is banked as first appearance. Distinct from truncation-completion (`0813-B3`) and terminal-item truncation (proposed 2026-08-15): both concern the string, this concerns the screen. Countermeasure: before banking a completed designator as novel, screen its longest carried prefix. Instance: `0817-B1`.
- **Negative-control-as-condition** — a status returned to a deliberately invalid resource read as an access condition of the host or resource, when a same-batch row on the real resource answered 200. Countermeasure: before folding any non-ok row into an access history, check whether the row's URL was fabricated. Instance: `0817-B3`.

---

## CLEAN FINDINGS

Audited and sound. Each was re-derived, not read.

1. **`0817-E3` — SUSTAINED, and it is correct.** I re-derived the population from `analytics/fetch-results/*.json` without reading his arithmetic. A/81/17 by-find rows: 14 total, 13 before today (11 `find_matched: false` at 08-03, 08-04, 08-05, 08-06, 08-07, 08-09, 08-10, 08-13, 08-14, 08-15, 08-16; one `true` — the `undocs.org/A/81/17` request-echo at `2026-08-04-daily.json records[3]`; one `robots_disallowed` with the key absent at `2026-08-08-daily.json records[1]`). Files carrying an A/80/17 row: 08-04, 08-05, 08-06, 08-07, 08-08, **08-13-uncitral-digest-control**, 08-14, 08-15, 08-16, 08-17. Cross-referenced: **three** of the eleven nulls sit in files with no A/80/17 row — `2026-08-03-targeted.json`, `2026-08-10-daily.json`, `2026-08-13-daily.json`. The record's carried figure of two (`analytics/council-log.md:14`; `2026-08-16.md:1151, :1257, :1492, :1580`; `state/research_log.json:8`) is wrong; **three is right**. His three-row table is right in every cell: 08-03 has a same-URL control (`records[4]`, `find: "fifty-ninth"`, matched) that is not the same shape; 08-10's only matched row is `digitallibrary.un.org/robots.txt`; 08-13's only matched rows are two `example.com` brackets. 08-13's A/80/17 control does exist — same date, same URL, matched — but in `2026-08-13-uncitral-digest-control.json`, a **different file and a different batch six minutes later** (11:26:29Z vs 11:32:13Z), which is precisely the failure mode next-step 4 names. The 08-08 note is right and immaterial. This is a correction to a standing watch and it should be adopted.
2. **`0817-E2`** — 13 before / 14 after on the by-find definition; 14 by-URL before today; 3 rows carrying the symbol in the URL string. All four figures reproduce exactly, with their definitions attached.
3. **`0817-E1` — sound, and it survives a trap he did not name.** His command reproduces one occurrence; tallies reproduce (adjacent 0, spaced 1); the carrier row's bytes (105,669) and sha256 (`b9c55ab5…`) are exact. My canonical trap says a zero-hit screen is not substantive absence until the synonym is tried, so I tried them: `PwC`, `Price Waterhouse` and bare `Coopers` across all 23 result files return nothing but the same single occurrence. The null holds. His refusal of the search summary's adjacent spelling (§1 note, §5.8) is correct.
4. **`0817-E9`** — verified line by line. `icsidfiles.worldbank.org` and `www.italaw.com` are in `ALLOWED_HOSTS` at `scripts/fetch_relay.py:48-69`; `jusmundi.com`, `www.transnational-dispute-management.com` and `arbitration.org` return no match anywhere in that file.
5. **Every `scripts/fetch_relay.py` line citation in Part II P2 is exact.** `:124-125` is the non-html/text early return; `:126-128` is the tag-to-space, whitespace-collapse insert-only transform; `:181-182` is `find_matched`. The G3 argument built on them — that a find on a PDF is structurally false and was therefore left empty on purpose — is correct, and `records[3]` does carry `find: ""`.
6. **Batch integrity, Part II opening — green against the rows named.** `control_ok: true`, `void: false`, `requested: 6`, `completed: 6`, `truncated: false`; `records[0]`, `[1]` and `[6]` all ok/200 on `example.com` at one digest; `records[1]` and `[6]` are the chairman's brackets and they bracket. The `records[0]` off-by-one is stated in the request note, which cures `0816-B7`. Pre-registration precedes result in commit order: `d041f67` then `1631c40`.
7. **Part II P2's italaw table is complete and exact.** Two rows in the entire corpus, and I derived them independently: `2026-08-03-control.json records[2]`, root, refused/403; `2026-08-17-daily.json records[3]`, the PDF path, ok/200, `application/pdf`, 1,587,866 bytes, sha256 `8de4c3db96fd6e0f13c86342422ca0ca9e757aeca897e6e8e1db3782da2f3de2`. The `meta.json` third observation is exactly as described (`source_health[3]`: `"NOT-READ (403)"`, `refusal_count: 1`, single url `https://www.italaw.com/`). **P2's refusal of the inference is the best-calibrated passage in the record** and it is what `0817-B6` measures the analyst against.
8. **Part II P3.** `records[4]` `find_matched: false`, `records[5]` `find_matched: true`, same URL, same run, same digest (`f9712994…`), and the A/80/17 window is quoted **character-exact** — I string-matched it. Null rather than void, correctly. Twelfth null, correct.
9. **Every verbatim quotation I could test is character-exact.** Today's `records[2]` excerpt appears in the record byte-for-byte. The 2026-08-06 hazard fragments are exact and correctly attributed: the bare list is in `records[4]` only, the dated list in `records[5]` only, neither in the other. The hazard itself — that the relay centres on the first case-insensitive match, so a bare `Procedural Order No. N` anchor is ambiguous by construction on this host — is a real and useful finding, and it is not tautological.
10. **The analyst's Amendment 2 discipline is the strongest I have audited on this seat.** Every referent-clause count reproduces exactly against the working tree: `transnational-dispute-management` 15, `jusmundi` 26, `icsidfiles` 18, `C10678` 5, `arbitration.org` 11, `Einarsson and GSI` 1; and every zero he banks is a true zero — `Chodorow` 0, `Maniatis` 0, `cases/documents/10912` 0, `italaw171339` 0, `key=33851` 0. His self-imposed all-files pre-pass against dud patterns is the right instrument and he ran it. `0817-B5` is the single failure in an otherwise clean set.
11. **§3.5 (DS20215) — exemplary.** `2026-07-03.md:184` is the only prior carrier of that URL; `2026-07-04.md:103` and `2026-07-05.md:116` carry `DS20900_En.pdf` under the same `C10678` path. All three verified. His withdrawal of the novelty claim, and his refusal to read `OnlineAwards` as saying "award", are both right. §8.1(c) is also right that the corpus has **never observed** that URL — zero `icsidfiles` rows exist in any result file.
12. **§3.4 — the method finding is real.** `UNCT/20/6` colliding with "UN Country Team" is a genuine qualification of rule (b), and the distinction he draws — rigid in law, non-distinctive to an index — is sound and worth keeping.
13. **Part I's precedence paragraph.** Rule 0 is quoted correctly against `prompts/daily_council_protocol.md`; the one departure is stated and its ground is the right one. I checked the record against Rule 0's banned-artefact list and find no second departure to add: the instrument-state block is a four-item list, not a probe table; there is no "Priority focus" section, no budgets section, and no carrying-span ledger with per-term occurrence counts *and* parity arithmetic. The `0816-B6` shape does not recur.
14. **Relevance discipline (calibration items 3 and 4) is clean on both seats.** `0815-B9` is honoured everywhere it applies — P1 line 146, §5.7, §8.6 — and nothing in this record inflates the Einarsson expert-identity thread toward the IP/pharmaceutical nexus in either direction. No fabricated case, award, tribunal, holding or URL anywhere in Parts I–III: every URL I could test resolves to a committed artefact or an `ALLOWED_HOSTS` entry, and the four unfetched hosts are correctly flagged as title-carried only. `0811-B4` is applied consistently to every generated summary, including the one that would have been most convenient.

---

## ESCALATIONS TO THE OPERATOR (Emory)

**E-1 — Model, REQUESTED vs ACTUAL, this seat, 2026-08-17. DISCREPANCY.**

| Date | Seat | REQUESTED | ACTUAL | Source |
|---|---|---|---|---|
| 2026-08-17 | integrity-officer | `claude-opus-4-8` | `claude-opus-5` | this vetting note; runtime self-report |

`.claude/agents/integrity-officer.md` frontmatter declares `model: opus` — a **tier alias, not a pinned id** — and its prose documents this seat twice as Claude Opus 4.8 (line 3, line 40). My runtime reports `claude-opus-5`. This is the **fifth consecutive observation** of the same shape on this seat and it extends the `HANDOFF.md` table at lines 197–205 unchanged in character: every seat documented as 4.8 that has been convened reports the discrepancy; every seat documented as Opus 5 reports parity. That remains consistent with the tier alias resolving to the platform's current Opus and inconsistent with per-seat misconfiguration. `scripts/check_models.py` cannot see this and is right to exit 0. Only you can decide whether the fix is pinning the id or correcting the prose — and note that a fifth identical observation is not new information about the cause. It is new information only about how long a known defect has gone unfixed.

**E-2 — Taxonomy entry 27 is still a collision**, eleven days on. Two distinct patterns (*scope-mixed screen*, 2026-08-06; *manufactured residual*, 2026-08-07) hold the same number, and the vault table says renumbering is not the archivist's to do because it changes what two council rulings say. I have again cited by name and not by number, and I am proposing three further entries at `0817-O7` that cannot be numbered until you settle it. Escalated at `[[Workflow Threads]] C11`; unresolved.

**E-3 — My own definition is stale in the way it warns against.** `.claude/agents/integrity-officer.md` line 40 states "Model: Claude Opus 4.8" and line 3 repeats it. That is the enumeration-goes-stale-silently failure the same file names at lines 48–55. The taxonomy pointer is correct and I worked from the vault; the model line is not, and it is the input to E-1.

**E-4 — Nothing was built, applied or amended.** I made no edit to any file, none to `scripts/`, and I did not touch the record. Every operator approval remains PENDING: the `source_analytics.py` patch, the health column, the `polite_get` CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, and any ledger amendment. The two `fetch_relay.py` defects the batch note names — lexicographic `newest_request()` selection and in-place reduction overwrite with no existence check — are unchanged and remain yours; the chairman correctly worked around them by naming rather than patching.

---
