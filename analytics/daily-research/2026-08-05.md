# Daily Council Meeting — 2026-08-05

**Research question:** When and how is intellectual property — especially trade secrets and pharmaceutical clinical trial data — asserted as a covered investment under an investment treaty, challenged through a governmental regulatory or judicial measure, and disposed of at the jurisdictional or admissibility stage?

**Procedural caveats (chairman).** (1) This session runs delegated in the scheduled cloud environment: I direct, and every member section below is what that member's real subagent returned, relayed verbatim. No member's voice is performed. (2) Model directive: per the operator's directive of 2026-08-03, the chairman and the research analyst run on Claude Opus 5 (both top seats moved from Fable 5 when the Fable credit balance was exhausted); the integrity, analytics and systems seats are spawned from their committed definitions under the harness's `opus` alias, which the operator's directive intends as Claude Opus 4.8. As recorded on 2026-08-03 and 2026-08-04, the exact version that alias resolves to in this cloud harness is not independently verifiable in-session; per the never-substitute-silently rule that uncertainty is recorded here rather than papered over. (3) No record for 2026-08-05 existed on `main` when this session opened, so the duplicate guard did not fire and this is the day's only meeting.

---

## Part 1 — Chairman's Agenda

### The instrument check, before the agenda

Per the delegation rule adopted 2026-07-31 and amended 2026-08-03 — *probe the instruments before writing the agenda; agenda scope follows the instruments actually alive* — I ran both probes at session open, before setting any task.

**Direct fetch: dead, both layers, unchanged for the fourth consecutive session.**

- Proxy egress (curl), neutral control hosts only: `https://example.com/` and `https://www.iana.org/` both returned `curl: (56) CONNECT tunnel failed, response 403`. The gateway refused the tunnel; no origin was contacted.
- Fetch service, neutral control: `WebFetch` on `https://example.com/` returned HTTP 403, response body not retrieved.

Both probes used neutral controls, not research hosts. Neither result is — and neither may be read as — information about any research resource. This is the standing tool-status-as-source-state rule.

**Indirect retrieval: alive, and it is the day's working instrument.** The fetch relay merged 2026-08-03 (`.github/workflows/fetch-relay.yml` + `scripts/fetch_relay.py`) is the committed relay mechanism this session's brief tells me to look for, so no fetch-dependent item is stranded today and none is written up as stranded. I fired today's batch at session open, before writing this agenda, so it would run while the council worked: `analytics/fetch-requests/2026-08-05-daily.json`, four rows.

It returned before Part 2 was seated. **Run 31001060670, `fetched_at` 2026-08-05T11:20:54Z, `control_ok: true`, `void: false`, 4 requested / 4 completed, `truncated: false`.** The three relay disciplines adopted 2026-08-04 are binding on every reading of it: every batch carries the neutral control and a failed control voids the batch entire; every requested URL gets a row, and `no_contact` is never recorded as an origin answer; the relay is a retrieval channel for what the record already identified, never a discovery channel (METHODOLOGY Part VIII), which its host allowlist enforces.

To that list today adds a fourth discipline, which I state in the agenda because the batch that just landed demonstrates it and the analyst must work to it. The 2026-08-04 rule was *never choose as `find` a string the target page can generate from your own request.* Today's `Mapped by` row shows the adjacent failure: `excerpt_of` in `scripts/fetch_relay.py` returns the window around the **first** case-insensitive match in the tag-stripped body, so a find string that also occurs in page boilerplate returns the boilerplate and not the structural label that was wanted. That is not a null, and it must not be recorded as one. It is a **mis-anchored row** — the question was not put to the page. I want that distinction drawn explicitly in the memo.

### The item I am not letting pass a fifth day

> **CORRECTION, entered by the chairman at close-out per sustained objection 0805-B1.** The first clause of the paragraph below is **wrong**. `agents/Workflow Threads.md` does **not** carry the ASIL ILIB reconciliation; only `state/research_log.json:10` does, plus the archival next-step line at `analytics/council-log.md:36`. The analyst established this at Part 2 §4.2, and the integrity officer confirmed it independently at nine spellings across two greps. The paragraph is left standing rather than silently rewritten, because a chairman's error is worth more on the record than a clean page — and because this is the third consecutive day a Part 1 premise has drawn a sustained objection. **Part 2 §4.2 supersedes the first clause below.** Note that the error is not incidental to the finding: the thread's absence from the owner-register is precisely *why* it has been re-inherited four times, so the sentence I got wrong was the sentence that mattered.

`agents/Workflow Threads.md` and `state/research_log.json` both carry the ASIL ILIB reconciliation, opened 2026-08-01, whose recorded next action is to reconcile "from the officer's saved raw HTML." I greped `analytics/` for that artefact before writing this agenda and did not find it. If it is genuinely not in the repository, then the thread's stated method has been un-executable since the session that closed on 2026-08-01, and `asil.org` is not on the relay allowlist, so it is not relay-reachable either. That would make it the same failure this council has been fighting all week in yet another costume: **a next action that names an artefact which never reached the record.** I am not asserting it — I have been wrong about the record's own contents twice this week and the countermeasure I adopted yesterday was to stop doing exactly that. So it goes to the analyst as a question to verify, not to me as a finding to announce.

### Tasks and budgets

**Task 1 — A/81/17, day 5 (analyst; relay-sourced; small).** Report the day-5 result in the standard same-page positive-control form, from the batch above. The `undocs.org`/`docs.un.org` symbol route was retired 2026-08-04 and is not used; do not revive it. Standing rule: **no search-synthesis figures** — a search result set is evidence about an index, never about whether a document exists. Note for your reading, not as my conclusion: the Commission Sessions page digest in today's batch appears to repeat the value the record already carries from 08-03 and 08-04. Say precisely what that does and does not add.

**Task 2 — Mapper attribution and vintage for UNCTAD treaty 978 (analyst; relay-sourced; primary).** This was next step 2 from yesterday, at low priority, and today's row did not answer it for the reason given above. Two things are wanted, in this order: (a) state plainly what the row does and does not establish, and classify it correctly as mis-anchored rather than null; (b) name the anchor that would actually retrieve the per-treaty attribution line, reasoning from `scripts/fetch_relay.py` and from what the record holds about the sibling treaties' attribution format ("Xiamen/La Plata, 03-2013"; "Laval/Paraiba, 10-2016"). If you name a row I will fire a second batch this session. **This calibrates the 2026-08-04 cell; it cannot change it, and it must not be written as though it could.**

**Task 3 — `china-switzerland-bit-2009-forum-provision-text`: is it relay-reachable at all? (analyst; record-and-code only; zero search).** This residual opened yesterday and is unassigned. Before any budget is spent hunting the treaty text, settle the prior question from the allowlist in `scripts/fetch_relay.py` and the reduction the relay actually produces: can an allowlisted host deliver this instrument's text through this channel, and what would the reduction look like if the text is a PDF? A reasoned "no, and here is why" is a full answer to this task and I will take it as one. Do not speculatively search.

**Task 4 — ASIL ILIB reconciliation: executable or not? (analyst; record-only; bounded).** Verify the question in the section above from the repository's own contents. If the artefact is absent, say so with the grep you ran, and state what would make the thread executable rather than leaving it to be re-inherited a fifth time. This is record hygiene, and record hygiene is in scope on a day whose retrieval questions are small.

**Zero budget, unchanged and not re-argued:** the China–France Protocol full text; the `first-application tracker` (its first cycle is late August, and it was explicitly not searched on adoption day); any ledger amendment; and every pending operator approval — the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix. **Nothing on that list is built or applied today.** Approvals are pending, not granted.

### Member return-path protocol

Each member is spawned as a real subagent from its committed definition in `.claude/agents/`. A member's returned text enters this record **verbatim**, under its own heading; I do not write, summarise, or improve any member's contribution. If a spawn fails or a member returns nothing usable, that seat is recorded as **PROCEDURAL FAILURE** for the day and the record says so — the seat is never performed.

### Spend checkpoint

A checkpoint falls **after the vetting round (Part 3)**: Parts 1–3 are committed and pushed before the analytics seat is convened, so the session's substantive work is never more than one commit from safe. This discipline saved the 08-03 record and it holds today.

### Order of business

Part 2 analyst → Part 3 integrity officer vets the analyst's return (positive control before any HTTP-status objection; raw source for any quote claim; **grep the record before asserting something is absent from it**) → **spend checkpoint** → Part 4 analytics officer, short if the digest of record is unchanged, which on my reading it is → Part 5 systems researcher only if its queue has ungated work → Part 6 my close-out: rulings on every objection, accountability, next steps, escalations to Emory.

---

## Part 2 — Research Analyst

*Spawned as a real subagent from `.claude/agents/research-analyst.md` (Claude Opus 5 per the operator's directive). The memo below is the analyst's return, verbatim and unedited. It corrects my Part 1 on one point of fact — see its §4.2 — and I have not touched the correction. — Chairman*

# RESEARCH ANALYST — MEMO OF 2026-08-05

*Council seating of 2026-08-05 (verified `date -u +%F` → `2026-08-05`). Branch `council/daily-2026-08-05` at `042229d`. Nothing committed, nothing edited, no pending approval touched.*

---

## METHOD NOTE (first, because it governs how everything below may be read)

**Instruments used: zero web searches, zero direct fetches, zero new relay rows.** Every retrieval result here comes from three reductions already in the repository: `analytics/fetch-results/2026-08-05-daily.json` (run 31001060670, `fetched_at` 2026-08-05T11:20:54Z, `control_ok: true`, `void: false`, 4/4 completed, `truncated: false`), `2026-08-04-daily.json` (run 30904314141), and `2026-08-03-targeted.json` (run 30858039847). I did not re-derive the direct-fetch death; the chairman probed it at session open on neutral controls, and the tool-status-as-source-state rule bars reading either failure as information about any research resource.

**This is a deliberate zero-search day, and it is the correct spend.** Tasks 1 and 2 are questions about what a specific page's rendered text contains at a timestamp — a search index cannot answer them, and a search result set is evidence about an index, never about whether a document exists. Tasks 3 and 4 are expressly zero-search. I fired nothing speculatively.

**Carrying-span posture.** The rule (`prompts/carrying_span_rule.md`, adopted 2026-08-03 as amended) binds this return per R7. Load-bearing propositions carry P/Q/D/V marks in the ledger at the end; the remainder carry inline quote-plus-pinpoint. The rule fired and **changed five propositions** in this memo — the count and the changes are in that section, per R8. Where a proposition has no rigid designator I say so.

---

## TASK 1 — A/81/17, day 5 of the publication window

### 1.1 The null, in the standard same-page positive-control form

**A/81/17 is not present in the rendered text of the UNCITRAL Commission Sessions page (`https://uncitral.un.org/en/commission`) as retrieved 2026-08-05T11:20:54Z: `outcome: "ok"`, `status: 200`, `final_url` identical to the requested URL (no redirect), `find: "A/81/17"` → `find_matched: false`, `excerpt: ""`.**

**Same-page positive control, same batch, same page state:** `find: "A/80/17"` → `find_matched: true`, excerpt:

> "ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se"

That control excerpt is **byte-identical (400 chars, exact string equality) to the 2026-08-04 control excerpt** — the B8 form of confirmation, which rests on excerpt-window byte identity across runs rather than on hash arithmetic. It shows how a published Commission report appears at this location, and the 59th-session slot lacks it.

Confidence **HIGH** as to the null at that location and timestamp. Day 5 of an August–September window; a null is the expected result and is recorded without inflation. **No gap slug** — a document inside its expected publication window is a monitoring thread, not an access gap (house rule, `2026-08-01.md:461`).

The `undocs.org`/`docs.un.org` symbol route was not used. It was retired 2026-08-04 as carrying no existence information at the relay's reduction level, and I did not revive it.

### 1.2 The repeated digest — precisely what it adds and what it does not

The Commission Sessions page has now been fetched **six times across three runner sessions** — 2026-08-03T22:17Z (×2), 2026-08-04T11:20Z (×2), 2026-08-05T11:20Z (×2) — and every one returned **`bytes: 832338`** and **`sha256: ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706`**.

**What the repeated digest ADDS, and it is a real gain the daily nulls alone cannot give:** the page is unchanged, byte for byte, across the whole interval 2026-08-03T22:17Z → 2026-08-05T11:20Z, roughly 37 hours. **Nothing was posted to, edited on, or removed from that page in that interval.** That is a statement about a *span*, not about three instants, and it is strictly stronger than the conjunction of the three nulls.

**What it does NOT add, in four parts, none of which should be allowed to soften:**

1. **It is not three independent observations of A/81/17's absence.** It is one page state observed three times. The day-5 null adds no new evidence about A/81/17 beyond what the day-3 null already carried; what it adds is interval coverage. Recording days 3, 4 and 5 as three confirmations would be triple-counting one observation.
2. **A repeated digest cannot distinguish "nothing was posted" from "the page is not the posting surface."** It is evidence about this page only. It says nothing about whether A/81/17 exists at ODS, at the 59th-session document listing, or in the General Assembly Official Records, and nothing about whether UNCITRAL posts a report here promptly on issue. The null's scope is the location, not the document.
3. **It is about rendered text, not raw HTML — the standing qualification, carried.** `_ANY_TAG_RE = re.compile(r"<[^>]+>")` (`scripts/fetch_relay.py:79`) strips every tag *including its attributes*, so a symbol occurring only inside an `href` is invisible to the relay. The 2026-08-01 officer's null was verified against raw HTML (`2026-08-01.md:374`); these are not the same strength and must not be recorded as equivalent. This is not a live worry — the site's own pattern puts the symbol in the visible link text, as the A/80/17 control demonstrates — but the difference is real.
4. **A stable digest is not proof that the relay would notice a change.** That has to be argued from elsewhere, and it can be: see below.

**The instrument is demonstrably capable of returning a changed digest from a live host, so the Commission page's stability is a property of that page and not an artefact of the relay.** Today's batch contains the proof in a stronger form than the record previously had. The two treaty-978 rows in **the same run, minutes apart**, both returned `bytes: 207411` and **different** sha256 (`c7afb6fd734d…` and `636c65c4033e…`). Across all three runs there are now **six fetches of that page, six distinct digests, every one at exactly 207,411 bytes**. Per ruling B8 I state the reading at the permitted strength and no higher: **this is most consistent with a constant-width per-response varying field**; "nonce" is a hypothesis, not a diagnosis, and nothing in this memo rests on it.

### 1.3 Operative statement for the record

**A/81/17 — day 5, 2026-08-05: not found in accessible sources.** Verified absent from the rendered text of the canonical posting location with a same-page positive control whose excerpt window is byte-identical to yesterday's; the page itself is byte-identical to its 2026-08-03T22:17Z state, so nothing was posted there in the intervening ~37 hours. The three standing questions ride unchanged: the adopted Supplementary Provisions numbering (whether SP 4 = early dismissal carries into the adopted text), the TPF SP10/SP11 inconsistency, and whether Draft Provision 19's ex officio dismissal power survived.

---

## TASK 2 — Mapper attribution and vintage, UNCTAD treaty 978

### 2.1 (a) What today's `Mapped by` row establishes — and the chairman's reading, tested against the code

**The chairman is right, and the code confirms it exactly.** I tested the reading three ways.

*The code.* `excerpt_of` at `scripts/fetch_relay.py:130-135`:

```
    if find:
        idx = body.lower().find(find.lower())
        if idx == -1:
            return ""
        start = max(0, idx - EXCERPT_CHARS // 4)
        return body[start:start + EXCERPT_CHARS]
```

`str.find` returns the **lowest** index of the substring. So the window is centred on the first case-insensitive occurrence in the tag-stripped, whitespace-collapsed body, with `EXCERPT_CHARS // 4` = 100 characters of left context and 300 of right (`EXCERPT_CHARS = 400`, `:42`).

*The arithmetic on today's actual row.* The returned excerpt is 400 characters. Its first 100 characters are `"ative between UNCTAD and universities worldwide to map the content of IIAs. Individual treaties are "` — and `excerpt[100:]` begins `"mapped by law students…"`. Because `start = idx − 100`, the match therefore sits at excerpt offset exactly 100. **The matched occurrence of "Mapped by" is the one inside "…Individual treaties are mapped by law students from participating universities…".** That is arithmetic on the returned reduction, not inference.

*The classification.* **MIS-ANCHORED, not null. The chairman's classification is correct and I do not dissent.** The distinction is not cosmetic: a null asserts that the page was asked a question and did not answer it; a mis-anchored row asserts that the question was never put. Recording this as a null would enter a false negative about UNCTAD's page into the record.

**One precision the chairman's formulation does not carry, and it matters.** Part 1 says boilerplate "preempted the structural label." That phrasing presupposes the structural label is there to be preempted. **It is not established that a per-treaty attribution line appears on this page at all.** The row establishes only that the *first* occurrence of "mapped by" in the rendered body is disclaimer prose. It is silent as between (i) an attribution line further down that the first-match rule skipped, and (ii) no attribution line on this page. Mis-anchoring is agnostic between those two, and the record should say so. So:

**What the row establishes:** (1) The treaty-978 page returned `outcome: "ok"`, `status: 200`, `bytes: 207411` at 2026-08-05T11:20:54Z with no redirect. (2) Its rendered text contains the IIA Mapping Project's disclaimer paragraph, verbatim: *"Individual treaties are mapped by law students from participating universities, under the supervision of their professors and with the overall guidance and coordination of UNCTAD. The mapping results included in the IIA Mapping Project database serve a purely informative purpose."* That is a small genuine gain — it moves this page's *character* as an IIA Mapping Project page from inference to retrieved text, and it puts the student-mapped/non-authoritative caveat class on the record from the source itself rather than by transfer from `HUMAN_REVIEW.md:198`.

**What it does not establish:** the mapper attribution for treaty 978; the mapping vintage for treaty 978; whether either is rendered on this page. **The identification caveat is untouched** — no retrieved excerpt names China or Switzerland, so B7 stands in full: identification is URL-slug-level, resting on UNCTAD's own id-to-slug assignment alone.

**And the generalisable defect, which is the day's transferable instrument lesson.** Yesterday's rule was *never choose as `find` a string the page can generate from your own request*. Today's row is a second species of the same genus, and both are now nameable as one thing: **`find` selects a position, not a proposition.** A `find_matched: true` is a statement about where a string first occurs, and there are at least two ways for that position to be uninformative — **request-echo** (the first occurrence is generated from your request, as on `undocs.org`) and **boilerplate-preemption** (the first occurrence is generic page prose, as here). Both produce `find_matched: true` and both answer a question nobody asked. The unified rule: **choose `find` so that its FIRST occurrence in the tag-stripped body is the occurrence you want; if you cannot guarantee that, you have not asked the question.**

### 2.2 (b) The anchor that would actually retrieve it — and why the obvious ones fail

This is where the carrying-span screen did most of its work today; I record the discards because the reasoning is the deliverable.

**The record's evidence about the attribution's rendered form.** The operator-verified ledger snapshot for the sibling China–France treaty, `analytics/verification_ledger.jsonl:54` and `:55` (claim `da33a30be92ab234…`, `operator_verified` 2026-07-27, `quote_ok: true`, `scope_ok: false`), reads in full:

> `"source_snapshot": "Relationship between forums: 'Fork in the road' | Alternatives to arbitration: None | ISDS forum options: Domestic courts of the host State: Yes; ICSID: Yes; UNCITRAL: Yes | Umbrella clause: Inconclusive | Mapped by Laval University (Canada); Federal University of Paraiba (Brazil), 10-2016"`

Three things follow, and one that does not.

**(i) The label carries no colon.** Every other element in that snapshot is rendered `label: value` — "Relationship between forums:", "Alternatives to arbitration:", "Umbrella clause:". The attribution is not: it reads "Mapped by Laval University (Canada)…". The operator preserved colons everywhere else, so their absence here is evidence, not transcription noise. **DISCARDED PROPOSAL 1: `find: "Mapped by:"`.** It was my first candidate — the minimal disambiguator, since "Mapped by:" cannot match "are mapped by law students". The snapshot kills it: on this evidence the page renders no colon, so the anchor would return an empty excerpt and a `find_matched: false` that means nothing at all. A worse outcome than today's row, because it would look like a null.

**(ii) The value format is `<University> (<Country>); <University> (<Country>), MM-YYYY`.** The record's shorthand "Xiamen/La Plata, 03-2013" (`HUMAN_REVIEW.md:198`) and "Laval/Paraiba, 10-2016" (`STATE_OF_THE_ANSWER.md:134`) are compressions of that long form, not the rendered string. Anyone anchoring on the shorthand would be anchoring on text that is not on the page.

**(iii) `find` is a literal substring search, not a regex** — `body.lower().find(find.lower())`, `:131`. **DISCARDED PROPOSAL 2: a pattern anchor on the vintage format (`", MM-YYYY"`, the `/` separator, article numbering).** Not expressible. This is worth recording as a code fact because it is the most natural idea a future seat will have.

**(iv) What the snapshot does NOT establish: page position or adjacency.** This is the finding that changed my primary recommendation, and it is new to the record. Compare the snapshot's element order — Relationship between forums → Alternatives to arbitration → ISDS forum options → Umbrella clause → Mapped by — against the order the relay actually retrieved from the treaty-978 page today: *"…Type of consent to arbitration Provides express or implied consent Forums **ISDS forum options** Domestic courts of the host State Yes ICSID Yes UNCITRAL Yes Other forums No **Relationship between forums** Preserving right to arbitration after domestic court proceedings Other specific ISDS features…"*. **On the page, "ISDS forum options" precedes "Relationship between forums"; in the snapshot the order is reversed.** So the ledger snapshot is a **curated transcription of selected elements, not a capture of the page's rendered order**, and it cannot be used to infer that "Umbrella clause" — or anything else — sits adjacent to "Mapped by" on the page. **DISCARDED PROPOSAL 3:** anchoring on "Umbrella clause" *as an established neighbour*. It survives only in the weaker form below. (This observation is independently relevant to standing escalation 6, the `da33a30be92ab234` snapshot amendment: the snapshot's `scope_ok: false` is doing more work than "provision-level characterisation" — it is also not a positional record of the source page.)

**(v) DISCARDED PROPOSAL 4: a party-name anchor (`find: "Switzerland"`) to upgrade the slug-level identification.** Barred by the 2026-08-04 rule. The requested URL's slug contains `china---switzerland-bit-2009-`, so a server-side template could render the party names from the request path; the record cannot exclude that, and the `undocs.org` row is the proof that this failure mode is real on live hosts. I do not name it.

**What actually survives — and it is honest that it is a probe, not an anchor.** There is **no find string derivable from the record that is guaranteed to retrieve the attribution**, and that is a property of the instrument rather than of the page: `str.find` returns only the first occurrence, and the reduction reports no offset, so a later occurrence of a string whose earlier occurrence is boilerplate is **unreachable by construction**. Given that, the two rows I name are chosen to be assumption-free and to fail informatively:

| # | url | `find` | why, and what a miss would mean |
|---|---|---|---|
| A | `https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009-` | `""` (empty) | With an empty `find`, `excerpt_of` returns `body[:EXCERPT_CHARS]` (`:136`) — the page head, zero assumptions about layout. Establishes whether the attribution and/or the treaty title sit at the top of the rendered body. It is positional, so it cannot mislead: it is a read, not a question. Caveat stated in advance: the script's own docstring notes "the opening of a page is almost always navigation chrome" (`:121-122`), so this may return chrome and nothing else. `find_matched` will be `false` by construction (`bool(find)` is `False`, `:181`) and **must not be read as a null**, exactly as the control row's `false` must not. |
| B | same URL | `Umbrella clause` | A mapped element the record independently attests for this database (`verification_ledger.jsonl:54`), and a term the disclaimer paragraph does not contain — so it is not preempted by the boilerplate. 300 characters of right context comfortably covers a ~105-character attribution string *if* the attribution follows it. **Adjacency is explicitly NOT established** (see (iv)); this row is a probe, not a targeted retrieval. It cannot be wasted: a miss on the attribution still returns treaty 978's umbrella-clause value, which the record does not hold. |

I would fire A and B together or not at all; separately they answer less than the pair does. **Low priority, and I say so plainly: this is confidence calibration on a cell that is already recorded, and it cannot change the cell.**

**The real fix, PROPOSED ONLY and explicitly not built.** Add two integers to each relay record: `match_offset` (the `idx` already computed at `:131`) and `body_chars` (`len(body)` after stripping). Neither is content — a position is not a quotation — so the no-third-party-body rule at `:10-13` is untouched. With `match_offset`, today's row would have been **self-diagnosing**: a small offset against a 207 KB page would have said "you matched near the top, in the chrome/disclaimer region" without any human noticing the boilerplate by eye. And a page-walk would become deterministic instead of guesswork. Routed to the systems seat and to Emory as an analysis item; **I built nothing and I claim no authority to.**

### 2.3 The third dated retrieval of the forum block — what it is worth

Today's batch re-ran `find: "ISDS forum options"` on treaty 978. It returned `find_matched: true` with an excerpt that is **byte-identical (exact string equality, 400 characters) to the 2026-08-04 excerpt for the same anchor**. I also checked the other pairing: the 2026-08-03 `relationship between forums` excerpt and the 2026-08-04 `Relationship between forums` excerpt are likewise byte-identical.

**Per B8, the confirmation rests on that byte identity, not on hash arithmetic.** So what the third retrieval is worth, stated at exactly its strength:

- **It is a genuine third dated retrieval** — a third run id, a third UTC date, a passing neutral control, and a same-page anchor whose window contains the cell. The cell value "Preserving right to arbitration after domestic court proceedings" has now been returned from that page on 2026-08-03, 2026-08-04 and 2026-08-05, with the window byte-identical across the last two.
- **It does not raise the evidentiary tier and I am not proposing that it should.** The cell remains mapping-level, from a provision-level, student-mapped, informational, non-authoritative database; not ledger-grade (no claim id, no operator verification, no preserved `source_snapshot`); identification still URL-slug-level per B7; **and the three treaties' labels are still reported side by side and NOT ranked**, because UNCTAD's codebook is not in the record. Nothing in Task 2 touches the 2026-08-04 forum-relationship cell, and nothing here is written as though it could.
- **What it does buy is durability of the retrieval, not weight of the source.** Three dated retrievals with identical windows is a strong answer to "did the relay read it right"; it is no answer at all to "is the mapping right about the treaty". Those are different questions and only the treaty text answers the second — which is Task 3.

---

## TASK 3 — `china-switzerland-bit-2009-forum-provision-text`: is it relay-reachable at all?

Record-and-code only; zero search fired. **The answer is NO, and it is a structural no, not a "not yet".**

### 3.1 The allowlist, applied

`ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`) is 18 research hosts plus `example.com` as the neutral control. Three of the 18 could conceivably carry a BIT text: `investmentpolicy.unctad.org`, `unctad.org`, and `www.italaw.com`/`italaw.com`.

**But the record holds no URL for this instrument's text on any of them.** `STATE_OF_THE_ANSWER.md:42` reads in full:

> **China-Switzerland BIT (2009) — "know-how" basis:** Explicitly includes "know-how" in its investment definition (alongside copyrights, patents, trade marks, trade names). Confirmed from treaty text. Swiss pharmaceutical investors (Roche, Novartis, Lonza) hold the most direct exposure under this BIT. Sources: [(UNCTAD Navigator)](https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/978/china---switzerland-bit-2009-); [(EDIT/WTI)](https://edit.wti.org/document/show/2e5072e0-92b8-49fa-a32b-4ca1611085bb)

Two locators. The first is the UNCTAD mapping landing page — allowlisted, and already fully exploited by the relay; it is a mapping page, not the treaty text. The second is `edit.wti.org`, **which is not on the allowlist**. So the one non-UNCTAD locator the record actually carries for this treaty is on a host the relay may not fetch. (The line's "Confirmed from treaty text" refers to the Ring 1 know-how element, not to the forum provision, and the record holds no text at all for the forum provision of this treaty — `STATE_OF_THE_ANSWER.md:132` carries only the *administrative-exhaustion* Protocol fragment: `| China-Switzerland (2009) | **Permissive** | Protocol: "may require an investor ... to exhaust domestic administrative review procedures" | MEDIUM-HIGH |`.)

`www.italaw.com` is allowlisted, and the record proves italaw hosts treaty-text pages for at least one China BIT — `STATE_OF_THE_ANSWER.md:40` cites `https://www.italaw.com/investment-treaties/china-germany-bit-2003`. **But no italaw URL for the China–Switzerland BIT is in the record.** Constructing one by slug analogy is **discovery**, which the relay is expressly built to refuse: "The relay retrieves what the sources or the record already identified — it is not a discovery channel (METHODOLOGY Part VIII)" (`:44-47`). I record for the chairman that the *cost* of such a guess would be bounded and legible — `polite_get` returns `None` on any non-200 (`src/sources/base.py:273-276`), which the relay carries through as `outcome: "refused"` with the status code in `status` (`fetch_relay.py:163-169`), so a 404 comes back as a clean refusal and not as a void — but bounded cost is not authorisation, and I am not proposing it.

### 3.2 The relay cannot bootstrap its way to a document URL — and this is the decisive point

Even holding the UNCTAD landing page, **the relay can never learn the treaty-text link target from it.** `_ANY_TAG_RE = re.compile(r"<[^>]+>")` (`:79`) removes every tag *including its attributes* before any matching or excerpting happens (`:126-128`). An `href` is an attribute. **The relay is therefore structurally incapable of reporting a hyperlink target from any page it fetches.** It can only ever reach URLs a human already put in the record. The allowlist is a policy expression of that; tag-stripping makes it a property of the instrument.

### 3.3 What the reduction would look like if the text is a PDF — and the trap that must be pre-empted

`excerpt_of` begins (`:124`):

```
    if "html" not in content_type and "text" not in content_type:
        return ""
```

A PDF returns `content_type: "application/pdf"`, which contains neither substring, so **`excerpt` is `""`**. And because `find_matched` is `bool(find) and bool(excerpt_of(...))` (`:181-182` — the B6 correction, applied), **`find_matched` is `false`**.

So the reduction of a treaty-text PDF would be: `outcome: "ok"`, `status: 200`, `final_url`, `content_type: "application/pdf"`, `bytes`, `sha256`, `excerpt: ""`, `find_matched: false`. **That is existence plus a fingerprint. It is not one character of text.**

**The trap, stated before anyone ever fires such a row:** on a PDF row, `find_matched: false` is **guaranteed by the content-type gate alone** and carries **zero** information about whether the string is in the document. A future session reading a PDF row's `find_matched: false` as "the phrase is not in the treaty" would be committing a false negative of exactly the class the `undocs.org` row nearly produced. This is the PDF analogue of the echo defect and it should enter the record under instrument/method now, not after it bites.

### 3.4 And even for HTML, the channel cannot deliver a provision

`EXCERPT_CHARS = 400` (`:42`), of which 100 are left context. The relay's own docstring states the design: *"What travels is the REDUCTION, never the document (council standing rule, 2026-08-03)… No third-party body is written to any file, branch, artifact, or log. The repository is public; a commit is publication."* (`:10-13`). Reading a provision through it would require a chain of anchored rows, each anchored on a string you must already know is in the document — and you cannot know it without having read the document. `MAX_URLS = 12` (`:76`) caps a chained walk at roughly 12 × 300 ≈ 3,600 newly-seen characters per batch under ideal conditions, and there are no ideal conditions, because `find` is literal-only and the reduction reports no offset.

### 3.5 Disposition

**The residual `china-switzerland-bit-2009-forum-provision-text` is not relay-work, and no relay budget should be spent on it.** The relay cannot deliver this instrument's text — not because of a defect, but because delivering documents is the one thing it was deliberately built not to do. A reasoned "no" is the whole of my answer to Task 3 and I do not dress it up as anything else.

**Two things the relay *could* honestly do, if and only if the operator supplies a locator** (chairman's call, not mine; I propose no spend):

1. If Emory or anyone puts a treaty-text URL **on an allowlisted host** into the record, one relay row would confirm that the named file exists at the named locator, is reachable under the project's own identifying User-Agent with robots.txt honoured, and record its byte length and sha256. That is a **provenance fixture** — precisely the preserved-snapshot discipline the July 31 lesson asks for and that the China–France ledger row was faulted for lacking. It is not a reading of the treaty and must never be written as one.
2. Nothing else.

**Where the residual should actually live.** It is either (a) an operator manual retrieval with a preserved `source_snapshot` — the same shape as downgraded escalation 2, and the two could be executed in one pass; or (b) a search-track item against an accessible secondary that reproduces the forum provision verbatim; or (c) it stays open, honestly, as an unread instrument. I recommend the chairman pick one rather than let it inherit forward unassigned, because an unassigned residual with no reachable method is the exact failure Task 4 is about.

---

## TASK 4 — ASIL ILIB reconciliation: executable or not?

Record-only. **The chairman's suspicion is correct on the substance and wrong on one register, and the second half matters more than it looks.**

### 4.1 The greps I ran, and what they returned

```
grep -n -i -E "asil|ilib|tosun|unisl|reconcil" "agents/Workflow Threads.md"     → no output, exit 1
git ls-files | grep -i -E "asil|ilib|tosun"                                     → no match
find . -path ./.git -prune -o -iname "*asil*" -print                            → no match
find /tmp -iname "asil_target.html"                                             → no match
git log --all --oneline --name-only | grep -i asil                              → two commit SUBJECT lines only; no file paths
grep -rn "Tosun" . --exclude-dir=.git                                           → 3 hits, all prose
grep -rn "unisl398" . --exclude-dir=.git                                        → 4 hits, all prose
grep -rn "asil\.org" . --exclude-dir=.git                                       → 11 hits, all prose
```

### 4.2 Finding 1 — `agents/Workflow Threads.md` does NOT carry this thread

Part 1 states, at `analytics/daily-research/2026-08-05.md:30`: *"`agents/Workflow Threads.md` and `state/research_log.json` both carry the ASIL ILIB reconciliation, opened 2026-08-01, whose recorded next action is to reconcile 'from the officer's saved raw HTML.'"* The first half is **incorrect**. The grep above returns nothing on any of five spellings. `Workflow Threads.md` runs A1–A7, B1–B3, C1–C9, D1, E, F0–F3 — there is no ASIL entry anywhere in it.

**Only `state/research_log.json:10` carries the thread** (plus `analytics/council-log.md:36`, which is the 08-01 session's next-step line, an archival record rather than a live register).

**This is not pedantry, and it is the mechanism of the re-inheritance.** `Workflow Threads.md` is the register that assigns an **owner**. Its own preamble, `:9-10`:

> "where that is recorded → who owns the next action. One thread, one chain, no branching. If
> a thread is not here, either it is closed or it could not be sourced — and an entry that
> cannot be sourced is not written."

The ASIL thread is neither closed nor unsourceable — it is sourced twice over. It is simply **missing from the one register that names owners**. That is why it has been re-inherited four times with nobody accountable for it: `research_log.json` records open threads but assigns no owner, and the register that assigns owners has never seen this one.

### 4.3 Finding 2 — the artefact is not absent from the *record*; it is absent from the *repository*

My proposition began as "the artefact is absent from the record." Screening the rigid designator `asil_target.html` returned a hit, and the proposition had to be narrowed. Its **locator** is in the record, at `analytics/daily-research/2026-08-01.md:363`, whose whole line reads:

> `| 2 | `https://asil.org/ilib/uncitral-finalizes-advisory-centre-statute-and-adopts-supplementary-isds-provisions/` | curl `-L`, browser UA (Chrome 126/macOS) | **200**, 140,430 b — saved at `.../scratchpad/asil_target.html` |`

Row 1 of the same table gives the elided directory unabbreviated:

> `| 1 | `https://uncitral.un.org/en/commission` | curl `-L`, project UA `isds-watcher/1.0 (+https://github.com/jackemorywilliams-bit/isds-watcher)` | **200**, 832,338 b, `text/html` — raw HTML saved at `/private/tmp/claude-501/-Users-emorywilliams-isds-watcher/eebda73d-3b6a-41d2-8205-616eca97a4e8/scratchpad/uncitral_commission_proj.html` |`

So the file was written to a **per-session scratchpad on the operator's local macOS machine**, under a session UUID. It was never committed, exists on no branch, and is not present in this environment (the `find /tmp` above is on a different machine and a different session id). **The record preserved the receipt and lost the document.**

### 4.4 Finding 3 — `asil.org` is not relay-reachable, and neither is the other half of the thread

`asil.org` does not appear in `ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`); the relay would return `outcome: "host_not_allowed"` at `validate()`, `:111-112`, without contacting the origin. And the thread's *other* limb is in the same position: the unisl398 half turns on `https://unis.unvienna.org/unis/en/pressrels/2026/unisl398.html`, and **`unis.unvienna.org` is not on the allowlist either**.

### 4.5 Verdict

**The thread's stated next action — "reconcile from the officer's saved raw HTML" — has been un-executable since the moment the 2026-08-01 session closed, and it is un-executable by every instrument this session class possesses.** Direct fetch is dead; the relay bars both hosts; the artefact is on a machine we cannot reach. This is the week's recurring failure in its fourth costume: **a next action naming an artefact that never reached the record.** The chairman's countermeasure of not asserting it himself was right, and the assertion he declined to make is confirmed.

### 4.6 What would make it executable

Three things, cheapest first, and the cheapest is not a retrieval at all.

**(1) Split the thread, because its two halves have different fates.** As recorded at `2026-08-01.md:404`, the SP subject-matter delta is *"ASIL's SP subject-matter list also adds 'burden and standard of proof' and 'damages and compensation' and omits bifurcation and interim measures, against the record's Day-12 enumeration."* That delta is **already fully stated in the record** — reading the ASIL page again would add nothing to it. What it cannot do is *resolve*, because resolving a secondary bylined list against the primary requires the adopted Supplementary Provisions text, which is what A/81/17 is being watched for. **Recommendation: fold the SP-list delta into the A/81/17 thread (A1) as one of the questions that ride on publication, alongside the SP numbering and the TPF SP10/SP11 inconsistency.** It becomes executable on the day A/81/17 publishes and needs no ASIL retrieval ever.

**(2) The unisl398 limb is a retrieval and needs an operator or an allowlist decision.** `unis.unvienna.org` is off-allowlist, so it is either Emory's manual action or an allowlist amendment. **I propose neither and build nothing** — an allowlist change is a code change to `scripts/fetch_relay.py` and every such approval is PENDING, not granted. I note only that the record's entire adoption confirmation rests on unisl400 (`STATE_OF_THE_ANSWER.md:105`), so a UNIS release the record does not carry, cited by a third party as the announcement, is a live question about the record's own foundation rather than a housekeeping item.

**(3) The record hygiene that stops a fifth re-inheritance regardless of either.** Strike *"reconcile from the officer's saved raw HTML"* from `state/research_log.json:10` and replace it with a method that exists, and give the thread an entry with a named owner in `agents/Workflow Threads.md`. **A next action that cannot be executed is worse than an open question, because it reads as a plan and defers the seat that reads it.** (Both are ripple edits to the chairman's own registers; I have edited nothing.)

### 4.7 Adjacent finding — the same divergence, in the other direction

Reported because it is the same mechanism and a future seat will hit it, not to inflate the day. `Workflow Threads.md` is **stale against the 2026-08-04 close-out**, by its own maintenance rule at `:434-436`: *"It is refreshed when the council's close-out changes a thread's state — which means the close-out, not this note, is the source of truth, and a disagreement between them is a defect in this note."*

- **A4** (`:79`) still reads *"Opened 2026-07-31 as an ordinary gap. The fork-in-the-road vs. no-U-turn characterization for the China–Switzerland BIT (2009) is missing…"* — but the 08-04 Part 6 closed that gap at mapping level, dissolved the fork-in-the-road/no-U-turn binary as a false dichotomy, and opened `china-switzerland-bit-2009-forum-provision-text` in its place.
- **A1** (`:38`) still reads *"The publication window **opens 2026-08-01**"* — four days into the window, and the standing null and its qualifications are not there.

The note's snapshot line (`:19`) is *"**Snapshot taken:** 2026-08-04, at `b76f6c3` (merge of PR #49)."* I checked the ancestry: `51bb7a2` (08-04 Part 6, 11:56Z) **is** an ancestor of `b76f6c3` (14:12Z), so the close-out was available to that pass. The staleness is not an accident of timing; it is policy, stated at `:26`: *"The A- and C-series states below are carried from the 2026-07-31 pass except where a section says otherwise."*

**One mechanism, two symptoms:** `research_log.json` carries a thread the owner-register lacks (ASIL), and the owner-register carries states `research_log.json` has superseded (A4, A1). Neither register is wrong about itself; they have drifted from each other, and the drift is invisible from inside either one. That is exactly the *duplicated-knowledge* pattern the systems seat diagnosed in `src/` on 2026-08-04 — six hand-maintained copies of facts owned elsewhere — now visible in the vault registers rather than in code.

---

## RELAY ROWS I NAME (consolidated, for the chairman's second batch)

Two rows, one page, low priority, **calibration only**. I am content if the chairman declines them; nothing in the record turns on them.

| # | url | `find` | why |
|---|---|---|---|
| A | `https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009-` | `""` | Empty find returns `body[:400]` (`fetch_relay.py:136`) — the page head, assumption-free. Establishes whether the attribution or the treaty title sit at the top. `find_matched` will be `false` by construction (`:181`) and must not be read as a null. May return only navigation chrome; that is a legitimate outcome. |
| B | same | `Umbrella clause` | A mapped element the record attests for this database (`verification_ledger.jsonl:54`) and a term the disclaimer paragraph does not contain, so it is not boilerplate-preempted. 300 chars of right context would carry a ~105-char attribution **if** it follows. Adjacency is NOT established — see §2.2(iv). A miss still returns treaty 978's umbrella-clause value, which the record lacks. |

**Not named, and why:** any `undocs.org` row (route retired 2026-08-04); any party-name anchor (URL-echo risk, §2.2(v)); any treaty-text row (Task 3 — no allowlisted locator in the record, and the reduction could not carry text if there were); anything on `asil.org` or `unis.unvienna.org` (off-allowlist).

---

## CARRYING-SPAN LEDGER

Marks for the load-bearing propositions. The rule fired and changed **five** propositions today; per R8 the number is **N = 5 changed / M = 9 screened**, and per the officer's inversion a nonzero N is the point of the exercise.

**Entry 1.**
**P** — A/81/17 is not present in the rendered text of the UNCITRAL Commission Sessions page as retrieved 2026-08-05T11:20:54Z.
**Q** — `"find": "A/81/17" … "find_matched": false`, with same-page control `"find": "A/80/17" … "find_matched": true`, excerpt *"…58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session…"* [`analytics/fetch-results/2026-08-05-daily.json`, records 2 and 3]
**D** — Relay reduction, run 31001060670, fetched 2026-08-05T11:20:54Z; `control_ok: true`, `void: false`, 4/4 completed. Not a case; no stage or disposition applies.
**V** — checked 2026-08-05 in the repository; screened `A/81/17` (rigid designator) 1 occurrence, in the request row itself, matched false; `A/80/17` 1, matched true and is the source of Q; `uncitral.un.org/en/commission` 2, both the requested and final URL of the same rows. No exit taken.

**Entry 2.**
**P** — The Commission Sessions page was byte-identical across 2026-08-03T22:17Z, 2026-08-04T11:20Z and 2026-08-05T11:20Z.
**Q** — `"bytes": 832338, "sha256": "ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706"` in all six fetches [`2026-08-03-targeted.json` records 4–5; `2026-08-04-daily.json` records 2–3; `2026-08-05-daily.json` records 2–3]
**D** — Three relay reductions, runs 30858039847 / 30904314141 / 31001060670. **This proposition has no rigid designator** — it is a claim about a hash and a byte count, which any field can paraphrase into a different encoding. Recorded per step 2; the verdict is weaker for it, and I state the interval claim only, not any claim about the site's posting behaviour.
**V** — checked 2026-08-05 by exact string comparison of all six `sha256` and `bytes` fields; screened `832338` 6, all six the source of Q; `ca87a113` 6, likewise. No exit.

**Entry 3.**
**P** — Today's `Mapped by` row matched the IIA Mapping Project's disclaimer prose, not a per-treaty attribution line.
**Q** — excerpt = *"ative between UNCTAD and universities worldwide to map the content of IIAs. Individual treaties are mapped by law students from participating universities…"*, with `excerpt[100:]` beginning at the match because `start = max(0, idx - EXCERPT_CHARS // 4)` [`2026-08-05-daily.json` record 4; `scripts/fetch_relay.py:134`]
**D** — Relay reduction of 2026-08-05 plus the committed source of `scripts/fetch_relay.py` at `042229d`.
**V** — checked 2026-08-05; screened `Mapped by` (rigid designator, the literal label) 1 occurrence in the excerpt, the source of Q; `excerpt_of` 4 occurrences in `fetch_relay.py` (definition at `:116`, two calls at `:180` and `:182`, one docstring mention) — the calls are the `excerpt` and `find_matched` assignments and are the subject of §2.1's classification, not the source of Q; `find` many, throughout the request/record plumbing. No exit taken.

**Entry 4.**
**P** — The China–France ledger `source_snapshot`'s element order does not match the treaty-978 page's rendered order, so it cannot establish adjacency.
**Q** — snapshot: *"…| ISDS forum options: … | Umbrella clause: Inconclusive | Mapped by Laval University (Canada); Federal University of Paraiba (Brazil), 10-2016"* with "Relationship between forums" **first** [`analytics/verification_ledger.jsonl:54`]; page: *"…Forums ISDS forum options Domestic courts of the host State Yes ICSID Yes UNCITRAL Yes Other forums No Relationship between forums Preserving right…"* [`2026-08-05-daily.json` record 5]
**D** — Ledger entries `:54-55`, claim `da33a30be92ab234…`, `operator_verified` 2026-07-27, `quote_ok: true`, `scope_ok: false`; and the relay reduction of 2026-08-05. Disposition on the point cited: the snapshot is operator-verified as to quote, expressly **not** as to scope.
**V** — checked 2026-08-05; screened `da33a30be92ab234` (rigid designator) 2 occurrences, both the ledger rows that are the source of Q; `Mapped by` 3 occurrences outside the relay files — two in the ledger snapshot (source of Q) and one in `2026-08-05.md:26`, which is the chairman's agenda language about the anchor, not about page order; `Umbrella clause` 2, both the ledger rows. **Exit taken: (ii) — proposition narrowed.** The original proposition was "'Umbrella clause' is the neighbouring structural label that would retrieve the attribution"; nothing in the record carries page-order adjacency, so it is narrowed to "a probe, adjacency not established."

**Entry 5.**
**P** — The artefact named by the ASIL thread's next action is not in the repository.
**Q** — *"| 2 | `https://asil.org/ilib/…` | curl `-L`, browser UA (Chrome 126/macOS) | **200**, 140,430 b — saved at `.../scratchpad/asil_target.html` |"* [`analytics/daily-research/2026-08-01.md:363`], read against `git ls-files` and `find`, both empty.
**D** — Council record of 2026-08-01, Part 3 (integrity officer's fetch table), and the working tree at `042229d`.
**V** — checked 2026-08-05; screened `asil_target.html` (rigid designator) 1 occurrence in the record and 0 in the filesystem; `Tosun` 3, all prose in `research_log.json:10` and `2026-08-01.md:390` / `:439`, none an artefact; `unisl398` 4, all prose; `asil.org` 11, all prose. **Exit taken: (ii) — proposition narrowed** from "absent from the record" to "its locator is in the record; the file is not in the repository."

**Rejects (item 7), with reasons.** `find: "Mapped by:"` — rejected, the ledger snapshot shows no colon (§2.2(i)). A regex/pattern anchor on the vintage format — rejected, `find` is literal-only (§2.2(iii)). `find: "Umbrella clause"` **as an established neighbour** — rejected in that form; retained as a probe (§2.2(iv)). `find: "Switzerland"` for identification — rejected, URL-echo risk barred by the 2026-08-04 rule (§2.2(v)). A constructed `italaw.com/investment-treaties/china-switzerland-bit-2009` URL — rejected, discovery, barred by `fetch_relay.py:44-47` (§3.1). `undocs.org` in any form — rejected, retired 2026-08-04. `edit.wti.org` (the record's own second locator for treaty 978) — rejected, off-allowlist. Four search queries that would have "confirmed" the treaty text — not formulated, per the zero-search scope.

---

## SELF-TRAINING / METHOD NOTE

**The day's transferable lesson, for the method file.** The 2026-07-26 lesson was *author-name+title beats keyword search*. The 2026-08-04 lesson was *never choose as `find` a string the page can generate from your request*. Today's subsumes both: **`find` selects a position, not a proposition.** Request-echo and boilerplate-preemption are two ways for the first occurrence to be the wrong occurrence, and they produce identical-looking `find_matched: true` rows. The operative discipline is a pre-flight question with a yes/no answer — *is the first occurrence of this string in the tag-stripped body the occurrence I want?* — and where the honest answer is "I cannot know," the row must be written up as a **probe**, with what a miss would mean stated in advance, and never as a question the page answered.

**And the corollary that cost me two proposals today: the reduction reports no offset, so a mis-anchored row cannot diagnose itself.** Adding `match_offset` and `body_chars` would convert this whole class of failure from a matter of a careful reader noticing boilerplate by eye into an integer anyone can check. Proposed to the systems seat; not built.

**Query formulations that produced findings today (all record/code, no index):** the productive move on Task 4 was grepping the **register that assigns owners** separately from the register that lists threads, rather than grepping "the record" as one thing — the divergence is invisible to a repo-wide grep because both registers return hits. On Task 2 the productive move was arithmetic on the excerpt's own offsets (`excerpt[100:]` against `start = idx − 100`) rather than reading the excerpt for meaning; that turned a plausible reading into a proof. Both are cheap and both generalise.

---

## INSIGHT DISPOSITION

Three genuinely new, sourced insights. The A/81/17 thread produced no insight and is recorded as standing watch. The treaty-978 forum cell produced no new insight — three dated retrievals of a recorded value is durability, not discovery, and I am not logging it as one.

1. **Instrument/method — `T-instrument-access-method`.** `find` selects a *position*, not a *proposition*: request-echo and boilerplate-preemption are one defect with two faces, both yielding an uninformative `find_matched: true`. Proved by arithmetic on today's `Mapped by` excerpt against `scripts/fetch_relay.py:130-135`. Carries the missing-offset corollary and the propose-only `match_offset`/`body_chars` fix. (§2.1, §2.2, method note)
2. **Instrument/method — `T-instrument-access-method`.** The relay is structurally incapable of delivering a document, in three independent ways: it cannot learn a link target (tag-stripping removes `href`s, `:79`), it returns no text at all for a PDF (`:124`, so `excerpt: ""` and `find_matched: false` by construction), and its 400-character cap plus literal-only `find` makes a chained read circular. **Pre-emptive rule to enter the record now: a PDF row's `find_matched: false` is guaranteed by content-type and is never evidence of textual absence.** (§3)
3. **Record hygiene — `T-record-registers`.** The vault's two thread registers have drifted apart and neither can see it from inside: `state/research_log.json:10` carries an ASIL thread absent from `agents/Workflow Threads.md`, whose preamble at `:9-10` asserts every open thread is in it; and `Workflow Threads.md` A4 (`:79`) and A1 (`:38`) carry states the 2026-08-04 close-out superseded, though `51bb7a2` is an ancestor of the `b76f6c3` the note snapshots at. One mechanism — duplicated knowledge with no derivation — two symptoms: an owner-less thread and a stale closed one. This is the `src/` pattern the systems seat diagnosed on 2026-08-04, now in the vault. (§4.2, §4.7)

**No insight, standing watch:** A/81/17 (day 5, null, as expected); the China–Switzerland forum-relationship cell (recorded 2026-08-04, unchanged, not re-argued, not ranked); `china-france-bit-2007-protocol-text` (zero budget, untouched, no fresh failure to count); the first-application tracker (zero budget, first cycle late August).

---

## `candidate_claims`

```json
[
  {
    "claim_text": "A/81/17 is not present in the rendered text of the UNCITRAL Commission Sessions page (https://uncitral.un.org/en/commission) as retrieved 2026-08-05T11:20:54Z (HTTP 200, no redirect, find_matched false), on day 5 of the expected August-September 2026 publication window. The same-page positive control in the same batch, find 'A/80/17', matched, and its 400-character excerpt window is byte-identical to the 2026-08-04 control excerpt.",
    "source_locator": "https://uncitral.un.org/en/commission ; reduction at analytics/fetch-results/2026-08-05-daily.json (run 31001060670, fetched 2026-08-05T11:20:54Z, control_ok true, void false, 4/4 completed), records 2 and 3",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_primary",
    "access_status": "partial",
    "supporting_quote": "ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se",
    "note": "NULL, in the standard same-page positive-control form. Scope limits, all binding: (1) this is about RENDERED TEXT, not raw HTML — scripts/fetch_relay.py:79 strips every tag including attributes, so a symbol occurring only inside an href is invisible to the relay, which makes this null weaker in kind than the 2026-08-01 officer's raw-HTML null (2026-08-01.md:374); (2) the null is about THIS LOCATION only and carries no information about ODS or any other posting surface; (3) the undocs.org/docs.un.org symbol route was RETIRED 2026-08-04 and was not used. No gap slug: a document inside its expected publication window is a monitoring thread, not an access gap (house rule, 2026-08-01.md:461)."
  },
  {
    "claim_text": "The UNCITRAL Commission Sessions page was byte-identical at three timestamps spanning roughly 37 hours — 2026-08-03T22:17:09Z, 2026-08-04T11:20:27Z and 2026-08-05T11:20:54Z — returning bytes 832338 and sha256 ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706 in all six fetches across the three runs. Nothing was posted to, edited on, or removed from that page in that interval.",
    "source_locator": "analytics/fetch-results/2026-08-03-targeted.json (records 4-5), 2026-08-04-daily.json (records 2-3), 2026-08-05-daily.json (records 2-3)",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_primary",
    "access_status": "partial",
    "supporting_quote": "\"bytes\": 832338, \"sha256\": \"ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706\"",
    "note": "INTERVAL claim, stronger than the conjunction of the three daily nulls; but the three nulls are ONE page state observed three times, NOT three independent observations, and must not be counted as three confirmations. This proposition has no rigid designator (it is a claim about a hash and a byte count) — recorded per carrying-span step 2, verdict correspondingly weaker. The relay is demonstrably capable of returning differing digests from a live host: the treaty-978 page returned six distinct sha256 at a constant 207,411 bytes across the same three runs, two of them minutes apart within run 31001060670. Per ruling B8 that pattern is reported as MOST CONSISTENT WITH a constant-width per-response varying field; 'nonce' is not asserted as a diagnosis and nothing here rests on it."
  },
  {
    "claim_text": "The relay row of 2026-08-05 requesting find 'Mapped by' on the UNCTAD treaty-978 page (China - Switzerland BIT (2009)) is MIS-ANCHORED, not null: excerpt_of returns the window around the FIRST case-insensitive match in the tag-stripped body (scripts/fetch_relay.py:131, str.find), and the returned excerpt's match sits at offset 100 — inside the IIA Mapping Project's disclaimer prose 'Individual treaties are mapped by law students…' — because start = idx minus EXCERPT_CHARS//4 = idx minus 100 (:134). The row therefore establishes that the page's rendered text contains the IIA Mapping Project disclaimer paragraph, and establishes nothing whatever about the mapper attribution or mapping vintage for treaty 978.",
    "source_locator": "https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009- ; reduction at analytics/fetch-results/2026-08-05-daily.json record 4 ; code at scripts/fetch_relay.py:116-136 and :181-182",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_secondary",
    "access_status": "partial",
    "supporting_quote": "ative between UNCTAD and universities worldwide to map the content of IIAs. Individual treaties are mapped by law students from participating universities, under the supervision of their professors and with the overall guidance and coordination of UNCTAD. The mapping results included in the IIA Mapping Project database serve a purely informative purpose. The mapping of treaty provisions is not exh",
    "note": "CLASSIFICATION claim. Mis-anchored is agnostic as between (i) an attribution line further down the page that the first-match rule skipped and (ii) no attribution line on this page at all — the row cannot distinguish them, and it must not be written as though the label was 'preempted', which presupposes its existence. find_matched is bool(find) and bool(excerpt_of(...)) (:181-182, the B6 correction), so a TRUE match here carries no information about the question asked — the same defect class as the undocs.org request-echo row of 2026-08-04. This row CALIBRATES nothing and CHANGES nothing about the 2026-08-04 forum-relationship cell. The one small gain: the disclaimer's presence in retrieved text puts the student-mapped / informational / non-authoritative caveat class on the record from the source itself rather than by transfer from HUMAN_REVIEW.md:198. The treaty identification caveat (B7, URL-slug-level) is untouched: no retrieved excerpt names China or Switzerland."
  },
  {
    "claim_text": "The UNCTAD IIA Mapping cell for treaty 978 recording 'Relationship between forums' as 'Preserving right to arbitration after domestic court proceedings' has now been returned from that page on three separate dates — 2026-08-03T22:17:09Z (run 30858039847), 2026-08-04T11:20:27Z (run 30904314141) and 2026-08-05T11:20:54Z (run 31001060670) — each with a passing neutral control, and the 400-character excerpt window anchored on 'ISDS forum options' is byte-identical between the 2026-08-04 and 2026-08-05 runs.",
    "source_locator": "https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009- ; reductions at analytics/fetch-results/2026-08-03-targeted.json, 2026-08-04-daily.json, 2026-08-05-daily.json",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_secondary",
    "access_status": "partial",
    "supporting_quote": "or prudential measures No Type of consent to arbitration Provides express or implied consent Forums ISDS forum options Domestic courts of the host State Yes ICSID Yes UNCITRAL Yes Other forums No Relationship between forums Preserving right to arbitration after domestic court proceedings Other specific ISDS features Limitation period for submission of claims No Provisional measures No Consolidatio",
    "note": "DURABILITY OF RETRIEVAL ONLY — this does NOT raise the evidentiary tier and is not proposed as doing so. Per ruling B8 the confirmation rests on the excerpt window being byte-identical across runs, not on hash arithmetic. Every 2026-08-04 caveat stands unchanged and in full: mapping level only; provision-level, student-mapped, informational, non-authoritative; NOT ledger-grade (no claim id, no operator verification, no preserved source_snapshot); mapper attribution and vintage for 978 still unknown; treaty identification URL-slug-level per B7; and the three treaties' forum labels are reported side by side and NOT RANKED, because UNCTAD's codebook is not in the record. The treaty and Protocol texts remain unread — GAP: china-switzerland-bit-2009-forum-provision-text."
  },
  {
    "claim_text": "The China-France ledger source_snapshot (claim da33a30be92ab234…, operator_verified 2026-07-27, quote_ok true, scope_ok false) lists mapped elements in an order that does not match the order in which the UNCTAD treaty page renders them: the snapshot places 'Relationship between forums' before 'ISDS forum options', while the treaty-978 page's retrieved rendered text places 'ISDS forum options' before 'Relationship between forums'. The snapshot is therefore a curated transcription of selected elements, not a positional capture of the source page, and cannot be used to infer that any two elements are adjacent on the page.",
    "source_locator": "analytics/verification_ledger.jsonl:54-55 ; compared against analytics/fetch-results/2026-08-05-daily.json record 5",
    "source_locator_type": "file",
    "source_authority": "operator_verified_ledger",
    "access_status": "full",
    "supporting_quote": "Relationship between forums: 'Fork in the road' | Alternatives to arbitration: None | ISDS forum options: Domestic courts of the host State: Yes; ICSID: Yes; UNCITRAL: Yes | Umbrella clause: Inconclusive | Mapped by Laval University (Canada); Federal University of Paraiba (Brazil), 10-2016",
    "note": "METHOD claim about the record's own artefact, not about either treaty. It does NOT impeach the snapshot's element VALUES, which are operator_verified with quote_ok true; it bounds what may be inferred from their ORDER, which scope_ok: false already flagged as unverified. Two consequences: (1) no relay anchor may be justified on the strength of snapshot adjacency — my 'Umbrella clause' proposal is therefore named as a PROBE, not a targeted retrieval; (2) it bears on standing escalation 6 (the da33a30be92ab234 snapshot amendment), since a positional capture and a value list are different artefacts and only the latter exists. Two further code facts recorded with it: the label renders WITHOUT a colon ('Mapped by Laval University…', unlike every colon-bearing element in the same snapshot), which defeats a 'Mapped by:' anchor; and find is a literal substring search (body.lower().find, :131), not a regex, so no pattern anchor on the vintage format is expressible."
  },
  {
    "claim_text": "The artefact named as the ASIL ILIB reconciliation thread's next action — 'the officer's saved raw HTML', i.e. asil_target.html — is not present in the isds-watcher repository, on any branch, or in this environment; only its locator survives, in the 2026-08-01 council record, which shows it was written to a per-session scratchpad on the operator's local machine and never committed. asil.org and unis.unvienna.org are both absent from the relay's ALLOWED_HOSTS. The thread's stated method has therefore been un-executable since the 2026-08-01 session closed and is un-executable by every instrument this session class possesses.",
    "source_locator": "analytics/daily-research/2026-08-01.md:363 (and :364 for the unelided scratchpad directory at row 1); state/research_log.json:10; scripts/fetch_relay.py:48-69",
    "source_locator_type": "file",
    "source_authority": "council_record",
    "access_status": "full",
    "supporting_quote": "| 2 | `https://asil.org/ilib/uncitral-finalizes-advisory-centre-statute-and-adopts-supplementary-isds-provisions/` | curl `-L`, browser UA (Chrome 126/macOS) | **200**, 140,430 b — saved at `.../scratchpad/asil_target.html` |",
    "note": "RECORD-STATE claim, verified by grep and named in full in the memo: `git ls-files | grep -i -E \"asil|ilib|tosun\"` no match; `find . -path ./.git -prune -o -iname \"*asil*\" -print` no match; `find /tmp -iname \"asil_target.html\"` no match; `git log --all --oneline --name-only | grep -i asil` returns commit SUBJECT lines only, no file paths. TWO CORRECTIONS TO PART 1 OF THE 2026-08-05 AGENDA: (a) `agents/Workflow Threads.md` does NOT carry this thread — `grep -n -i -E \"asil|ilib|tosun|unisl|reconcil\"` on that file returns nothing, exit 1 — so only state/research_log.json:10 (plus the archival next-step line at analytics/council-log.md:36) carries it; since Workflow Threads.md is the register that assigns owners, the thread has no owner, which is the mechanism of its four re-inheritances; (b) the artefact is NOT absent from the record — its locator is in the record and the file is absent from the repository, which is a different and more precise fact. Absence is stated as 'not found in the repository by the greps named', never as 'does not exist'."
  }
]
```

---

**Files read this session, none edited, nothing committed:** `/home/user/isds-watcher/analytics/daily-research/2026-08-05.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-04.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-01.md` (Parts 3–6), `/home/user/isds-watcher/analytics/fetch-results/2026-08-05-daily.json`, `/home/user/isds-watcher/analytics/fetch-results/2026-08-04-daily.json`, `/home/user/isds-watcher/analytics/fetch-results/2026-08-03-targeted.json`, `/home/user/isds-watcher/analytics/fetch-requests/2026-08-05-daily.json`, `/home/user/isds-watcher/scripts/fetch_relay.py`, `/home/user/isds-watcher/src/sources/base.py` (`polite_get`), `/home/user/isds-watcher/state/research_log.json`, `/home/user/isds-watcher/agents/Workflow Threads.md`, `/home/user/isds-watcher/STATE_OF_THE_ANSWER.md` (lines 40–42, 64, 66, 84, 86, 105, 130–134, 177), `/home/user/isds-watcher/analytics/verification_ledger.jsonl` (`:54-55`), `/home/user/isds-watcher/analytics/council-log.md`, `/home/user/isds-watcher/HUMAN_REVIEW.md:198`, `/home/user/isds-watcher/prompts/carrying_span_rule.md`, `/home/user/isds-watcher/prompts/council_calibration.md`.

---

## Part 3 — Integrity Officer

*Spawned as a real subagent from `.claude/agents/integrity-officer.md`. The vetting note below is the officer's return, verbatim and unedited, including its two rulings on the mid-vetting addendum I sent it and its finding against my own Part 1. My rulings on every objection are in Part 6. — Chairman*

## PART 3 — INTEGRITY OFFICER · VETTING NOTE OF 2026-08-05

*Date verified: `date -u +%F` → `2026-08-05`. Branch `council/daily-2026-08-05`. Nothing committed, nothing edited, no approval touched. Taxonomy read from the vault at `/home/user/isds-watcher/agents/integrity-officer.md` (24 entries as of 2026-08-04), not recited from the definition.*

**Scope worked.** Part 1 and Part 2 in full. I verified 38 of the analyst's ~40 file:line citations by opening each cited line; re-ran every grep in §4.1 myself plus five synonym variants; checked the offset arithmetic against the reduction and the code; counted every fetch in all eight result files by URL rather than by the memo's enumeration; ran the ancestry check; and read the raw JSON for both of today's batches. No HTTP-status objection appears below, so no positive control was required for one; where I rule on identification I say what control would settle it.

---

### FIRST — the chairman's mid-vetting addendum

**A1 · The commit-pin question. RULED: commit-pin, not correction. The analyst is not at fault on any cited value.**

I verified this independently rather than on your account. `git diff 946b8b7 -- analytics/fetch-results/2026-08-05-daily.json` changes exactly four values and nothing else:

- `fetched_at` `11:20:54.533726Z` → `11:41:05.453460Z`
- `environment` run `31001060670` → run `31002416607`
- record 4 `sha256` `c7afb6fd734d…` → `e1402bb4dce8…`
- record 5 `sha256` `636c65c4033e…` → `ef4317697797…`

Every other byte is identical. Specifically confirmed: **both Commission Sessions rows (1 and 2) are byte-identical objects across the two versions** — `bytes: 832338`, `sha256: ca87a113c024605c…`, excerpts, `find_matched` — so every `ca87a113…` claim in Part 2 checks out against *either* version. And **both treaty-978 excerpts are byte-identical across the two runs**, so §2.1's arithmetic and §2.3's window are unaffected.

Re-run against the pinned blob `git show 946b8b7:analytics/fetch-results/2026-08-05-daily.json`:
- `Mapped by` excerpt: 400 chars; `"mapped by"` occurs **once**, at offset **exactly 100**; first 100 chars are exactly as §2.1 quotes them. **The offset arithmetic is correct.** Since `start = max(0, idx − 100)` and the match sits at excerpt offset 100, the `max()` did not clamp and the matched occurrence is the disclaimer prose. The mis-anchored classification holds, and it is proof, not inference, as the memo claims.
- `A/80/17` control pair 08-05 vs 08-04: **exact string equality, 400 chars.** The memo's blockquote is character-for-character the excerpt. Quote integrity clean.
- `ISDS forum options` pair 08-05 vs 08-04: **exact string equality, 400 chars.**
- Bonus pair the memo also asserts: 08-03 `relationship between forums` vs 08-04 `Relationship between forums`: **exact string equality.**

All three B8-form claims verified. **Required fix: a commit pin, not a correction.** Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path. See O4 — this is a new taxonomy entry.

**A2 · The B7 identification ruling. RULED: the page head does NOT discharge the B7 caveat. It discharges a weaker sibling proposition, and B7 stands in narrowed wording.**

I read the raw row myself at `/home/user/isds-watcher/analytics/fetch-results/2026-08-05-zz-attribution.json` (run 31002699165, 11:45:19Z, `control_ok: true`, 2/2). Your quotation of both rows is verbatim; I found no drift.

- **What it adds.** For the first time the *page's own rendered text* asserts the parties. Before today, "China–Switzerland" was known only from the string we sent. That moves identification from *we-asked-for-a-slug* to *the-page-returned-a-title*. Real, and worth recording.
- **Why it does not discharge B7.** `China - Switzerland BIT (2009)` is a plausible mechanical inverse of `china---switzerland-bit-2009-`, and **no control separates a title read from UNCTAD's record for id 978 from a title rendered off the requested path.** Your §2.2(v) instinct is right in genus. I note in the analyst's favour that `BIT` (uppercased) and `(2009)` (parenthesised) are *not* naively recoverable from the slug, which is a weak argument for independence — weak is not established, and this seat does not convert weak into discharged.
- **The distinction that must be drawn, because someone will over-apply §3.1.** Row A used an **empty `find`**, so the echoed-find defect (taxonomy 23) does not arise in its own terms — `excerpt_of` returned `body[:400]` unconditionally, and the row is positional. The residual worry is a *different* mechanism: server-side templating of the title from the path. Naming it correctly matters.

**What would discharge it — one row, allowlisted, assumption-free, and I state what makes it negative (taxonomy 22).**

> **Row C.** `https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/zzz---zzz-bit-1900-`, `find: ""`.
> The slug `zzz---zzz-bit-1900-` cannot correspond to any real treaty; that is what makes it negative. **If the head still reads "China - Switzerland BIT (2009)", the title is keyed to the id, our string contributed nothing, and B7 is discharged to database-record level.** If it returns "Zzz - Zzz BIT (1900)", or `outcome: "refused"` with a 404, the title is slug-derived or slug-validated and **B7 stands in full**. Both outcomes are informative; the row cannot be wasted. Optional Row D — `.../977/china---switzerland-bit-2009-` — separates "slug ignored" from "slug must match id".

**This is a control, not discovery.** §3.1 correctly refuses to *construct a research locator*; a deliberately-false URL retrieves no research resource and tests the instrument. Record the distinction or §3.1 will be cited to block the very test that settles B7.

**Until Row C fires, B7's wording changes from "identification is URL-slug-level only" to "identification is URL-slug-level, with a page-rendered title not yet separable from the requested slug."** Even discharged, it would reach *UNCTAD's record for id 978*, never the treaty instrument.

**A3 · The two new rows — three overreach flags, pre-emptive.**
1. **Umbrella clause "Yes" may not be ranked.** Treaty 978 maps `Yes`; China–France maps `Inconclusive` (ledger `:54`). Those may be reported side by side and compared for **identity/difference only**. UNCTAD's codebook is not in the record, so `Yes` is **not** "stronger than" `Inconclusive`. Taxonomy 19, and the three-way treaty comparison is exactly where this will be tempted.
2. **The adjacency finding is bounded by the window.** Row B establishes that within 300 characters of right context the next element is "Entry and sojourn of personnel". It does **not** establish that no attribution line appears further down. Write it as *"does not immediately follow within the retrieved window"*, never as *"does not follow"* — that is §2.1's own lesson applied to the row that vindicated it.
3. Row A's `find_matched: false` is false **by construction** (`bool(find)`, `:181`) and is not a null. The request note says so; the record must too.

---

### BINDING OBJECTIONS

**B1 — Part 1 is wrong on the record; §4.2 is right, and the correction is sustained.**
Part 1 (`2026-08-05.md:30`) asserts that `agents/Workflow Threads.md` carries the ASIL ILIB reconciliation. It does not. I ran it myself, and I did not stop at the analyst's five spellings — a zero-hit screen is not substantive absence until the synonym is tried:
```
grep -n -i -E "asil|ilib|tosun|unisl|reconcil" "agents/Workflow Threads.md"          → exit 1
grep -n -i -E "\bASIL\b|American Society|International Law in Brief|raw HTML" same   → exit 1
```
Zero on both. **The analyst's §4.2 is correct and the chairman's Part 1 is in error.** This is the third consecutive day a Part 1 premise has drawn a sustained objection. `state/research_log.json:10` and `analytics/council-log.md:36` both verified as the only carriers, verbatim as quoted. **Fix: Part 1 must be corrected in place, or carry an inline note that §4.2 supersedes it.** The analyst's mechanism finding — the owner-register never saw the thread, hence four re-inheritances — is sound and I endorse it.

**B2 — Amendment-2 failure: the V-mark screen counts are understated in three of five entries, and the missed occurrences are substantive. This is the day's principal objection.**
Amendment 2 is this seat's own and the record calls it the highest-value check in the set: every screened term with a nonzero count must be the source of `Q` **or** carry a referent clause, because a nonzero count otherwise reads as corroboration. An *undercounted* screen is worse — it silently removes occurrences from the referent obligation.

| Entry | Term | Memo says | Repo holds (excl. the memo's own text) | Missed, and it matters |
|---|---|---|---|---|
| 4 | `Mapped by` | 3 outside relay files | **8 occurrences on 6 lines** | `2026-08-04.md:520` (**the prior officer's own ruling on this exact anchor**), `:759` (the chairman's 08-04 next step commissioning this task), `:286` (the analyst's own 08-04 proposal), `council-log.md:13` |
| 4 | `da33a30be92ab234` | 2, both ledger rows | **13+ lines** | `Workflow Threads.md:200` — *"### C1 · Ledger snapshot amendment for `da33a30be92ab234`"*, i.e. **standing escalation 6 itself**, in the very register §4.7 audits; also `WT.md:64`, `Project Change Log.md:155`, `SOTA:44/:134/:179/:184`, `research_log.json:8` |
| 5 | `asil_target.html` (declared rigid designator) | 1 in the record | **2** | `2026-08-01.md:479` — the 08-01 close-out's task assignment, *"read the officer's saved raw `asil_target.html`"*, which is the origin of the un-executable next action |
| 5 | `unisl398` | 4 | **6** | — |
| 5 | `asil.org` | 11 | **13** | — |
| 2 | `832338` / `ca87a113` | 6 each, all the source of Q | **7 each at memo time** (now 9) | `2026-08-03-queued.json` — see B3 |

`Tosun` = 3 is **exactly right**, and I say so.

In fairness, and because Rule 4 binds me: I read the four missed `Mapped by` lines and **none contradicts the memo**. `2026-08-04.md:520` confirms "Mapped by" as the literal label and does not rule on the colon. The defect is the count, not the conclusion — but a screen the author cannot be checked against is not a screen. Also note two of the five V marks scope their screens repo-wide ("3 occurrences outside the relay files") and the others do not scope at all; an unscoped screen reads as repo-wide.
**Fix: re-run all five V-mark screens at repo scope, restate the counts, and give every nonzero occurrence that is not the source of `Q` a referent clause. `Workflow Threads.md:200` in particular must be surfaced — §2.2(iv) invokes escalation 6 while the screen that should have found it returned 2.**

**B3 — Silent class truncation (taxonomy 14): both fetch classes are enumerated short, and the interval claim is understated.**
I counted by URL across all eight result files rather than by the memo's list.

- **Commission Sessions page.** Memo: *"fetched six times across three runner sessions"*, *"the whole interval 2026-08-03T22:17Z → 2026-08-05T11:20Z, roughly 37 hours."* At the time the memo was written the record held **seven fetches across four runner sessions**, the fourth being `2026-08-03-queued.json` (run 30841165880, **2026-08-03T18:25:23Z**) — same `bytes: 832338`, same `sha256: ca87a113c024605c…`. **The true interval was 40 h 55 m, not ~37 h.** As the record now stands it is **nine fetches, five runs, one digest, one byte length, 41 h 16 m.**
- **Treaty-978 page.** Memo: *"six fetches, six distinct digests, every one at exactly 207,411 bytes."* At memo time: **seven fetches, seven distinct digests**, the seventh being `2026-08-03-queued.json` (`929d9e70ae1e…`). Now: **eleven fetches, eleven distinct sha256, all at exactly 207,411 bytes.**

Direction matters and I record it: **every omission understates the analyst's own case.** The word "whole" in "the whole interval" is nonetheless false, and `candidate_claims` entry 2 would enter the pipeline with "in all six fetches across the three runs." **Fix: re-enumerate both classes at URL level (taxonomy 14's countermeasure), correct the interval, and pin `@946b8b7`.** Note the eleven-distinct-digests figure strengthens the constant-width-varying-field reading materially; per B8 it still may not be called a diagnosis.

**B4 — "minutes apart" is not in the record. Overreach on a temporal fact.**
§1.2: *"The two treaty-978 rows in **the same run, minutes apart**…"*. The reduction carries **one** `fetched_at` per manifest, computed in `run()` after the fetch loop (`scripts/fetch_relay.py:215`); there is **no per-record timestamp**. The only interval the code guarantees is `MIN_INTERVAL_SECONDS = 3.0` (`/home/user/isds-watcher/src/sources/base.py:28`), and the two rows are consecutive same-domain fetches with nothing between them. **Seconds, not minutes, and the record establishes neither.** This is the live edge of taxonomy 21 — *count time points, not rows* — and here a time separation was supplied that no row carries. **Fix: strike "minutes apart"; write "consecutively within one run, separated by at least the 3.0-second per-domain interval and by no recorded amount more."** The genuinely multi-time-point evidence now exists and should be used instead: 11:20:54Z and 11:41:05Z are **separate runner sessions twenty minutes apart** and returned different digests.

**B5 — §4.6(2) overreaches on the record's foundation and drops the adverse qualification from the line it cites.**
Memo: *"the record's **entire** adoption confirmation rests on unisl400 (`STATE_OF_THE_ANSWER.md:105`)."* I read `:105` in full. Two problems:
1. **"Entire" is wrong.** `:105` records adoption confirmed and then **upgraded** to HIGH on 2026-07-29 by unisl400, over *"Prior sourcing: IAReporter headline…"*, and it names **unisl399** as well. unisl400 carries the upgrade, not the whole confirmation.
2. **The omitted flag, which is the more serious half.** The same line states that unisl400's *"body [is] 403-blocked; title confirmed from multiple independent search-engine returns."* The memo cites the favourable half and drops the adverse half from one sentence — **selective-flag reporting (taxonomy 9)**, and the record's HIGH rests on a **title**, not a body. Under this seat's standing user-agent-gating finding, a "403-blocked" characterisation is an access artefact until a same-host positive control under a varied UA is run; I state that conditionally, because the finding was made on `uncitral.un.org` and absolutizing it is taxonomy 13.
**Fix: hedge to "the July-29 upgrade to HIGH rests on unisl400", and carry the body-403 / title-only qualification with it.** The analyst's substantive point survives and is arguably stronger for the correction.

**B6 — §3.2 absolutizes a true structural finding (taxonomy 13).**
*"The relay is therefore structurally incapable of reporting a hyperlink target from any page it fetches."* Verified: `_ANY_TAG_RE = re.compile(r"<[^>]+>")` (`:79`) substitutes the whole tag, attributes included, at `:127`, before any matching. So an `href` **target** never survives. But a URL printed as **visible body text** does survive tag-stripping and is excerptable, and `final_url` is a network-learned URL the reduction does report. **Fix: narrow to "cannot report a URL that appears only inside an attribute."** Insight 2 restates the absolute form and needs the same narrowing. **Task 3's structural "no" is unaffected and I sustain it** — I checked all three legs independently: tag-stripping (`:79`, `:126-128`) ✓; the content-type gate `if "html" not in content_type and "text" not in content_type: return ""` (`:124-125`) with `find_matched = bool(find) and bool(excerpt_of(…))` (`:181-182`) ✓; `EXCERPT_CHARS = 400` (`:42`) and `MAX_URLS = 12` (`:76`) ✓. The "no" rests on the record holding no allowlisted locator, which is independent of leg 1's wording.

**B7 — Four mis-located internal citations (taxonomy 17a). The other 34 verified exact.**
- `analytics/daily-research/2026-08-01.md:364` cited in `candidate_claims` "for the unelided scratchpad directory **at row 1**". **Row 1 is at `:362`.** `:364` is row 3, the `https://asil.org/ilib/` same-host control. The §4.3 blockquote of row 1 is verbatim-correct against `:362`; only the locator is wrong. → **`:362`**
- `scripts/fetch_relay.py:121-122` for *"the opening of a page is almost always navigation chrome."* The phrase spans **`:120-121`**. → **`:120-121`**
- `agents/Workflow Threads.md:9-10` — the blockquote's third clause, *"cannot be sourced is not written"*, is on **`:11`**. → **`:9-11`**
- `agents/Workflow Threads.md:26` — *"pass except where a section says otherwise"* is on **`:27`**. → **`:26-27`**

Verified exact and requiring no change: `fetch_relay.py` `:42`, `:44-47`, `:48-69`, `:76`, `:79`, `:111-112`, `:116-136`, `:124`, `:126-128`, `:130-135`, `:131`, `:134`, `:136`, `:163-169`, `:181-182`, `:10-13`; `src/sources/base.py:273-276`; `verification_ledger.jsonl:54` and `:55` (the `source_snapshot` blockquote is **character-for-character** on both lines; `quote_ok: true` / `scope_ok: false` / `operator_verified` are fields of `:55`); `STATE_OF_THE_ANSWER.md:40`, `:42` (quoted "in full" — confirmed the whole line), `:105`, `:132`, `:134`; `Workflow Threads.md:19`, `:38`, `:79`, `:434-436`; `state/research_log.json:10`; `2026-08-01.md:363`, `:374`, `:390`, `:404`, `:439`, `:461`; `HUMAN_REVIEW.md:198`; `council-log.md:36`. The memo's 1-indexed record convention is used consistently in all seven places it appears; no objection.

---

### OBSERVATIONS AND CORRECTIONS

**O1 — Taxonomy extension, entry 25: MUTABLE-REDUCTION CITATION.** *Citing a retrieval reduction by its path when the path is overwritten in place by a later run, so the cited run id and digests silently cease to be retrievable at the locator a reader is given.* Caught live today, against today's own memo: `analytics/fetch-results/2026-08-05-daily.json` was rewritten at 11:41:05Z by run 31002416607 after Part 2 was committed. The memo's `c7afb6fd…`/`636c65c4…` are correct and are now findable only at `946b8b7`. **Countermeasure, two parts: (a) every citation to a reduction carries its commit sha; (b) the relay should refuse to overwrite an existing result file — write `<request>-<run_id>.json`, or fail. A reduction is evidence; evidence files must be append-only, as the verification ledger already is.** I will add this to the vault taxonomy as entry 25 and date it 2026-08-05. Note the memo's own §4.3 lesson — *the record preserved the receipt and lost the document* — recurred within the same session, one layer up.

**O2 — `newest_request()` is lexicographic, and the `zz-` rename buries the finding in a data file.** `sorted(p for p in directory.glob("*.json"))[-1]` (`:233-235`) sorts by name despite the function's name. Confirmed as the cause. The defect is currently recorded only in the `note` field of `analytics/fetch-requests/2026-08-05-zz-attribution.json` — a request payload, not the record. **Recommend it enter Part 6 and the systems queue explicitly**; a defect documented only inside the artefact that works around it is the same class as `Workflow Threads.md` A1/A4. I propose nothing be built: `fetch_relay.py` is code, and code changes are pending approval, not granted.

**O3 — §3.3's PDF argument has a live positive demonstration in the record that the analyst did not use.** `analytics/fetch-results/2026-08-03-control.json` record 1: `https://uncitral.un.org/sites/default/files/2026-06/sps.pdf`, `outcome: "ok"`, `status: 200`, `bytes: 2104033`, **`excerpt: ""`**. That converts §3.3 from a reading of the code into a behaviour the instrument has already exhibited. **Recommend the memo cite it.** Strengthening, not a defect.

**O4 — §2.2(i) credits the snapshot's punctuation while §2.2(iv) impeaches its order, and transfers across treaties.** *"The snapshot kills it"* is stated more strongly than a curated transcription supports: if the artefact is not positional, its punctuation fidelity is not established either, and `scope_ok: false` covers both. Separately, the snapshot is the **China–France** record and the inference is about the **China–Switzerland** page's rendering — same database, plausibly same template, but the transfer is unflagged. **Fix: hedge (i) to "on the best evidence available the label renders without a colon, and the snapshot is not a positional capture, so this is a reason to prefer another anchor rather than a proof."** The **discard itself is conservative and correct** and I do not disturb it. The prior officer's `2026-08-04.md:520` confirms "Mapped by" as the literal label and does **not** rule on the colon — no contradiction.

**O5 — §3.1's two locators are two different URL strings.** `STATE_OF_THE_ANSWER.md:42` carries `…/treaties/**bit**/978/china---switzerland-bit-2009-`; the relay has been fetching `…/treaties/**bilateral-investment-treaties**/978/…`, which matches `STATE_OF_THE_ANSWER.md:86`. So the record does carry the relay's form and there is **no discovery problem** — but "the first is the UNCTAD mapping landing page — already fully exploited by the relay" treats two distinct strings as one page without evidence of a redirect (`final_url` shows none). **Fix: cite `:86` for the fetched URL.** Live because B7 is a URL-string question.

**O6 — Objection-label collision across days.** Part 2 cites `B6`, `B7`, `B8` as standing rulings of 2026-08-04 while today's objections are also `B1…B7`. **Recommend today's be recorded as `0805-B1`…`0805-B7`,** or the standing rulings be renamed. This will otherwise mis-resolve within a week.

**O7 — The `.sent` marker, for your parallel investigation; one thing you may not have.** Confirmed: `analytics/daily-research/.sent/2026-08-05` was written by `5649d1f` at **11:22:38Z**, i.e. after Part 1 (`042229d`) and before Part 2 (`9b6868f`) — so the emailed record contained the agenda only. The marker is **date-addressed**, not content-addressed (`.github/workflows/daily-update.yml:73-84`), so no later completion of the day's record can ever be sent. **Countermeasure: key the marker to a hash of the record file, so a changed record re-sends and an unchanged one does not.** That is a small change to the workflow and is a pending approval, not a grant. I spent no further budget here.

---

### CLEAN, AND SAID PLAINLY

- **Task 1 §1.1 and §1.3 are clean.** The null is in the correct same-page positive-control form; the control excerpt is verbatim; the four "what it does not add" limits are correct, complete, and unusually well-drawn — in particular *"It is not three independent observations"* is the right reading and survives the corrected count of four (now five) runs.
- **Task 2 §2.1 is clean and its arithmetic is proof.** I re-derived it independently against the pinned blob. The correction of Part 1's *"preempted"* presupposition is right, and the mis-anchored/null distinction should enter the method record as the analyst frames it.
- **Task 3 is a correct structural "no"**, subject only to B6's wording. All three legs verified in code.
- **§4.7 verified in full.** `git merge-base --is-ancestor 51bb7a2 b76f6c3` → **true**. `51bb7a2` is 2026-08-04 11:56:09Z; `b76f6c3` is 14:12:41Z. Both timestamps as stated. `WT.md:19`, `:26`, `:38`, `:79`, `:434-436` all carry what is claimed. The staleness finding stands.
- **Zero-budget list honoured, verified from git, not from the memo's assurance.** `git diff --name-only c9050e6..HEAD` touches only the day's record, the two request files, the two result files, and the `.sent` marker. Part 2's commit added 397 lines to one file and nothing else. No ledger amendment, no `source_analytics.py`, no `build_graph`, no allowlist edit, no approval consumed. **Nothing was built or applied.**
- **No ranking anywhere in Part 2.** §2.3 states the non-ranking rule explicitly and `candidate_claims` 4 repeats it. Taxonomy 19 clean.
- **No fabrication risk in either Part.** Every case, page, author, URL and quotation in Parts 1–2 resolves to a repository artefact or a retrieved reduction I opened. `Enes Y. Tosun` is carried by the record at `2026-08-01.md:390`/`:439`, correctly marked bylined-secondary and not imported as fact. No plausible-but-unconfirmed citation appears.
- **No inflated relevance.** A zero-search day on a quiet theme, correctly spent, correctly not padded. §1.3 and the insight disposition both decline to log durability as discovery. Insight 3 is genuine.
- **Carrying-span mechanical tiers.** Five entries, five verification statements; `P`/`Q`/`D`/`V` present and non-empty in all five; every `Q` carries a quotation pair and a pinpoint; `D` marks name the disposition on the point cited, including Entry 4's correct *"operator-verified as to quote, expressly not as to scope"*. Two exits taken and both properly declared. `scripts/check_marks.py` was not run: per its docstring the strict tiers do not exercise a memo in this form, and tier 4 is always by hand. **Tier 2, 4 and 5 pass by hand; tier 3 — Amendment 2 — fails, at B2.** One gap: §1.2's *"six distinct sha256 at a constant 207,411 bytes"* is load-bearing for the interval claim's value and carries **no P/Q/D/V entry**; it appears only inside a `candidate_claims` note. **Fix: give it an entry, with the corrected count.**

**Two traps, discharged in my own findings.** Every zero-hit screen I relied on was retried with synonyms (B1: five spellings plus four more). Every absence I state is absence **from this repository at `042229d`/HEAD**, never from the project — and B2/O1 are both instances of why that distinction earns its keep.

---

### DISPOSITION

| # | Objection | Class | Disposition |
|---|---|---|---|
| **0805-B1** | Part 1 wrong that `Workflow Threads.md` carries the ASIL thread; §4.2 correct | Mis-stated record | **SUSTAINED against Part 1.** Correct Part 1 or note the supersession inline |
| **0805-B2** | Amendment-2 screen counts understated in Entries 2, 4, 5 | Tier-3 / taxonomy 14 | **SUSTAINED.** Re-run all five screens at repo scope; referent-clause every nonzero |
| **0805-B3** | Both fetch classes enumerated short; interval understated | Taxonomy 14 | **SUSTAINED.** Re-enumerate by URL; correct to 7/4 at memo time, 9/5 and 11 now |
| **0805-B4** | "minutes apart" not carried by the reduction | Overreach / taxonomy 21 | **SUSTAINED.** Strike; substitute the ≥3.0 s floor and the real 20-minute inter-run gap |
| **0805-B5** | "entire adoption confirmation rests on unisl400"; body-403 flag dropped | Overreach + taxonomy 9 | **SUSTAINED.** Hedge to "the July-29 upgrade", carry the title-only qualification |
| **0805-B6** | "incapable … from any page it fetches" absolutized | Taxonomy 13 | **SUSTAINED as to wording only.** Task 3's "no" stands |
| **0805-B7** | Four mis-located citations (`08-01.md:364`, `relay:121-122`, `WT:9-10`, `WT:26`) | Taxonomy 17a | **SUSTAINED.** Repoint to `:362`, `:120-121`, `:9-11`, `:26-27` |
| **0805-O1** | Mutable-reduction citation — taxonomy entry 25 | New pattern | **ADOPTED.** Commit-pin every reduction citation; relay must not overwrite |
| **0805-O2** | `newest_request()` lexicographic; workaround buried in a request note | Record hygiene | Recommend to Part 6 and the systems queue; build nothing |
| **0805-O3** | Live PDF-gate demonstration unused | Strengthening | Recommend citing `2026-08-03-control.json` record 1 |
| **0805-O4** | §2.2(i) colon inference over-stated; cross-treaty transfer | Hedge | Hedge (i); discard itself stands |
| **0805-O5** | Two different 978 URL strings treated as one | Precision | Cite `SOTA:86` for the fetched URL |
| **0805-O6** | B-label collision across days | Record hygiene | Prefix today's `0805-` |
| **0805-O7** | `.sent` marker is date-addressed | Chairman's matter | Content-addressed marker; pending approval |
| **A1** | Commit-pin question | Ruling | **Commit-pin, not correction.** Analyst at fault on nothing |
| **A2** | Does the page head discharge B7? | Ruling | **NO.** B7 stands, narrowed. Row C discharges it |
| **A3** | New rows | Pre-emptive | No ranking of `Yes` vs `Inconclusive`; adjacency bounded to the window |

### OVERALL VERDICT

**Parts 1–2 may NOT enter the record as written. They may enter with the seven sustained corrections at 0805-B1 through 0805-B7 applied, plus the A1 commit-pin and the A2 narrowing of B7.**

None of the seven touches a conclusion. Every substantive finding in Part 2 survives: the null, the mis-anchored classification and its arithmetic, the structural "no" on Task 3, the ASIL un-executability, the register-drift diagnosis, and the three byte-identity confirmations — all of which I verified independently against raw sources. Four of the seven objections **understate** the analyst's own case rather than inflate it, which is the honest direction for an error to run and is worth recording as such. This is a strong memo. Its one systemic defect is that the screens meant to make it checkable by a non-author were not run at the scope they claim — and that defect is the one this seat exists to catch.

**Part 1 has now drawn a sustained objection on three consecutive days (08-03 B3(a), 08-04 B1, 08-05 0805-B1), each time on a premise about the project's own record asserted without the grep.** I record it as a pattern, not an incident. The chairman's own countermeasure — putting it to the analyst as a question rather than announcing it — is what kept it recoverable today, and it worked; the remaining gap is that the erroneous premise was still written as fact in the sentence before the question.

**Files of record for this note:** `/home/user/isds-watcher/analytics/daily-research/2026-08-05.md`; `/home/user/isds-watcher/analytics/fetch-results/2026-08-05-daily.json` (**cite as `@946b8b7`**); `/home/user/isds-watcher/analytics/fetch-results/2026-08-05-zz-attribution.json`; `/home/user/isds-watcher/analytics/fetch-results/2026-08-03-queued.json`; `/home/user/isds-watcher/analytics/fetch-results/2026-08-03-control.json`; `/home/user/isds-watcher/scripts/fetch_relay.py`; `/home/user/isds-watcher/src/sources/base.py`; `/home/user/isds-watcher/agents/Workflow Threads.md`; `/home/user/isds-watcher/agents/integrity-officer.md` (taxonomy — entry 25 to be added).

---

## Part 4 — Analytics Officer

*Spawned as a real subagent from `.claude/agents/analytics-officer.md`. The entry below is the officer's return, verbatim and unedited. The digest of record is unchanged since its last seating, so its brief was deliberately short; the section it wrote on delivery risk was commissioned by me after today's emailer finding and is new work. — Chairman*

## ANALYTICS OFFICER — 2026-08-05

Digest 2026-08-03: 13 candidates evaluated, 1 item surfaced (0 matches, 1 watch-list lead).

Numbers copied this session from `/home/user/isds-watcher/digests/2026-08-03_ISDS-Thematic-Watch/meta.json`.

### 1. The digest of record is unchanged — verified, not taken on trust

`digests/` holds nothing newer than `2026-08-03_ISDS-Thematic-Watch`. `git log` on the meta path returns exactly one commit, `bdf8c79` (2026-08-03, "chore: weekly digest + state update"), and `git status --porcelain` on `digests/` is clean. The artefact has not been amended since my 2026-08-04 seating. My section is short because there is nothing new to count, which is the correct outcome, not a gap.

### 2. Per-source receptivity

`per_source` is byte-identical to what I reported yesterday — same file, same commit, untouched working tree. `analytics/source-receptivity.md` last moved at `8705f7a` (2026-08-03) and is clean. No table re-derived. Nothing in the longitudinal picture has moved because no run has occurred.

### 3. Resolution of my 2026-08-04 findings

**(a) `health_warnings: []` is a cold-start artefact — CONFIRMED, with one refinement against myself.** `git log -- state/source_health.json` returns exactly one commit (`bdf8c79`, 2026-08-03), so on that run the guard loaded an empty file, no streak could exceed 1, and `DEGRADED_AFTER = 3` was unreachable. The empty array says nothing about degradation. **But my 08-04 statement was too broad.** `build_warnings()` emits two independent warning classes, and the second one *was* honestly earned: `collection_anomaly()` requires all-but-at-most-one active source at zero, and the run had four of seven non-zero (`icsid` 1, `iareporter_headlines` 12, `unctad_isds` 25, `pca_press` 1). So `health_warnings: []` is a cold-start artefact with respect to DEGRADED and a genuine negative with respect to COLLECTION ANOMALY. Earliest possible DEGRADED remains the 2026-08-17 run.

**(b) `bing_news` invisible to the guard — CONFIRMED, and now sharper.** `ACTIVE_SOURCES` in `/home/user/isds-watcher/src/source_health.py` lists seven names; `bing_news` is not among them, while `HANDOFF.md:115` documents it live-verified 2026-07-29 and in place of the retired `google_news_rss`. New detail from data I read this session: `state/source_health.json` records `bing_news` at `zero_streak: 1`, `last_nonzero: null`. `update_streaks()` tracks every source in the counts dict, but `apply_to_source_health()` short-circuits at `if name not in ACTIVE_SOURCES: continue`. So the streak accumulates correctly and forever and can never produce a flag. The evidence is being collected and the alarm is disconnected — that is worse than "not tracked", and it argues for the parity test against the source registry rather than adding a string.

**(c) The 2026-08-17 date — I confirmed the arithmetic myself, and it holds, with one condition the record must carry.** Chain, each link read this session: `DEGRADED_AFTER = 3`; `state/source_health.json` has exactly one commit, so the guard's first live run was 2026-08-03 and `italaw` sits at `zero_streak: 1`; `weekly.yml` cron is `0 13 * * 1`; 2026-08-03, 08-10 and 08-17 are all Mondays (checked, not assumed), giving streaks 1 / 2 / 3; `apply_to_source_health()` fires at `streak >= threshold`. **2026-08-17 is correct.** `_EXEMPT_STATUSES = {"DISABLED", "FAILED"}` still omits `NOT-READ` (emitted at `src/main.py:153`), so nothing exempts it from the overwrite.

The condition: this requires `italaw` to return zero raw on **both** 08-10 and 08-17. `HANDOFF.md:110` records the Cloudflare managed challenge as unresolved, so zero is the expectation — but if the challenge lifts on either date the streak resets and the date moves. **2026-08-17 is the earliest possible and the expected date; it is not guaranteed.** I'd ask that the operator's list carry it that way rather than as a flat certainty.

A second branch worth recording: the collision as framed also assumes italaw's status *is* `NOT-READ` on 08-17, i.e. that HTTP was attempted and nothing read. If it degrades instead to a plain zero-count `RETURNED`, the overwrite still occurs and the warning line — "Their fetchers likely no longer match the live site" — is still emitted against a source we know is being challenged rather than broken. The false causal claim survives either branch. The defect is robust; only the word "overwrite" is branch-dependent.

**(d) `unctad_isds` — UNCHANGED, and it will never self-report.** From the files read this session: `source_health` count 25, `per_source` 0, no entry in `accepted_by_source`. `state/source_health.json` shows `zero_streak: 0`, `last_nonzero: "2026-08-03"` — the guard reads it as perfectly healthy, because the guard keys on raw count and raw count is 25. That is the whole blind spot in one line: **no automated signal in the instrument today will ever raise this source.** It surfaced from a hand audit and it will only ever surface from a hand audit, or from the proposed `STALE` signal. The keep-or-retire question is unresolved and remains PENDING.

### 4. Is there an analogous silent-delivery-failure risk on the digest side?

Short answer: **the specific 08-05 failure cannot occur there — but there is one genuine analogue, and it is a silent non-delivery rather than a truncation.**

**Why the dedup class is structurally absent.** The `.sent` marker mechanism exists in exactly two places: `scripts/send_daily_update.py:52,99,105` and `scripts/send_vault_log.py:39,55,61`. Neither the weekly digest path (`weekly.yml` → `src/main.py`) nor the Monday packet (`human-review.yml` → `scripts/send_human_review.py`) writes or reads a marker; both send unconditionally, once per invocation. The architectural reason is sound: the daily emailer is fired by commit activity and must defend against repeat firing, so it needs a marker and inherits the marker's first-write-wins semantics. The digest is sent *in-process by the pipeline that produces it*, exactly once per run, so it needs no dedup and has none.

**Why staleness is also absent on the weekly digest.** `send_digest(html, subject, cfg)` at `src/main.py:293` is passed the same in-memory `html` object rendered at `:262` and written to disk at `:265`. There is no read-back from the filesystem, so email and archived artefact cannot diverge. Failure is loud: a false return sets `email_status = "failed"`, `main()` returns 1, the step fails, and `weekly.yml`'s "Notify on failure" step emails the operator. `render.write_digest_folder` (`src/render.py:149-157`) additionally refuses to let an empty re-run overwrite a populated same-date record. This path is well defended.

**The one real analogue — `src/main.py:277-282`.** On `empty_run` (`stats["total_candidates"] == 0`) the digest email is suppressed, the run exits 0, and nothing tells the operator. That suppression is correct for its designed case — a same-day re-run where every candidate deduped. But `total_candidates` is counted *after* collection, and every fetcher is wrapped in a try/except that degrades to `[]`. A total collection failure therefore also yields `total_candidates == 0`, and the condition as written cannot distinguish the two. In that case: green run, no digest email, no failure alert — and the `COLLECTION ANOMALY` warning that `source_health.collection_anomaly()` exists precisely to raise is written into `stats["health_warnings"]` and archived into a `meta.json` the operator never receives. **The instrument's loudest alarm is routed exclusively through the one channel this condition switches off.** That is the same shape as the truncation bug: a delivery path where "nothing happened" and "nothing got through" are indistinguishable at the inbox.

Honest severity: this has not fired and is remote today — it needs all nine sources at zero raw in one run, and `iareporter_headlines` (12) and `unctad_isds` (25) have returned volume in every archived run. But the margin is thinning, not widening: `iisd_itn`, `google_alerts`, `italaw` and `bing_news` are all already at zero, and `unctad_isds`'s 25 is furniture, not fresh collection.

**PROPOSAL ONLY — nothing built, nothing changed, PENDING operator approval.** Condition the empty-run suppression on *why* the count is zero rather than on the count alone: suppress only when `health_warnings` is empty and no documented-active source is sitting on a zero-streak; otherwise send. It touches no collection code and adds no state.

**Two lesser observations, offered as observations, not defects.** First, in `weekly.yml` the email is sent inside the `Run watcher` step, while `scripts/validate_archive.py` — which the workflow describes as failing the run on counts that disagree across meta.json, article files and email footer — runs two steps later. It is therefore a detector on an already-delivered artefact, never a gate. That may well be deliberate (deliver and flag beats withhold), so I flag it rather than call it wrong. Second, in `scripts/send_human_review.py`, `_latest_weekly_council()` has a real freshness check with three loud states (added after the 2026-07-20 regression) — but `age > max_age_days` with `max_age_days=7` means an entry from the *previous* Monday is exactly 7 days old and passes as fresh; and `_latest_record()` takes `sorted(names)[-1]` with no age check at all, so an arbitrarily old daily-research note can render as "Latest daily research note". Both cases print the source date in the heading, so the packet is stale-but-labelled rather than stale-and-silent. That is the tolerable end of this class and I do not propose changing it.

### 5. Standing

Every approval remains PENDING and none was treated as granted: the `NOT-READ` exemption fix, the `bing_news` parity test, the `STALE` signal, the `unctad_isds` keep-or-retire decision, the ledger snapshot amendment, `source_analytics.py` sign-off, and the emailer CONSISTENCY WARNING question. Nothing was built, applied, or committed; no file was modified this session.

**Files of record for this entry (absolute):**
- `/home/user/isds-watcher/digests/2026-08-03_ISDS-Thematic-Watch/meta.json`
- `/home/user/isds-watcher/state/source_health.json`
- `/home/user/isds-watcher/src/source_health.py`
- `/home/user/isds-watcher/src/main.py`
- `/home/user/isds-watcher/src/render.py`
- `/home/user/isds-watcher/scripts/send_human_review.py`
- `/home/user/isds-watcher/scripts/send_archived.py`
- `/home/user/isds-watcher/.github/workflows/weekly.yml`
- `/home/user/isds-watcher/.github/workflows/human-review.yml`
- `/home/user/isds-watcher/.github/workflows/send-digest.yml`
- `/home/user/isds-watcher/HANDOFF.md`

---

## Part 5 — Systems Researcher

*Spawned as a real subagent from `.claude/agents/systems-researcher.md`, on a propose-only brief. Seated today because its queue acquired genuinely ungated analysis work: two instrument defects this session surfaced by tripping over them. The entry below is the seat's return, verbatim and unedited. It corrects the chairman's stated merge plan in §5 and I have adopted its correction — see Part 6. — Chairman*

## Part 5 — Systems Researcher

Date verified: `date -u +%F` → `2026-08-05`. Branch `council/daily-2026-08-05`, 9 commits ahead of `origin/main`, not merged. Nothing built, nothing applied, no file changed.

---

### 1. Both defects confirmed. You are right on the mechanism of each; you are wrong on one consequence, and you have understated a third defect that sits underneath Defect 1.

**Defect 1 — confirmed at the line.** `scripts/fetch_relay.py:233-235` is `sorted(p for p in directory.glob("*.json"))[-1]` — lexicographic on `Path`, despite the name `newest_request`. `run()` at line 228-229 is `out_path = out_dir / request_path.name` with an unconditional `write_text`. No existence check anywhere. Your reconstruction of today's sequence matches the artifacts: run `31001060670` wrote `2026-08-05-daily.json` at 11:20:54Z (commit `946b8b7`); run `31002416607` overwrote the same path at 11:41:05Z (commit `5e3677f`).

**Update since you wrote the brief:** the `zz-` rename workaround landed and worked. `analytics/fetch-results/2026-08-05-zz-attribution.json` now exists (run `31002699165`, `fetched_at` 11:45:19Z, `control_ok: true`, `void: false`, `completed 2/2`). The attribution batch is served. That also confirms the diagnosis experimentally: the only thing that changed was the sort key of the filename.

**Defect 2 — confirmed, and the root cause is one you did not name.** The `on: push` block at `.github/workflows/daily-update.yml:19-21` has **no `branches:` filter**. That is why every council part-commit on a feature branch fires the emailer. And the workflow's own header comment, lines 9-11, says:

> `*   The routine commits with "[skip ci]", which GitHub honors to SKIP workflows.`
> `    So the push trigger below will NOT fire on the routine's own commit. We keep`
> `    on: push for any non-skip push (and manual pushes), but we do NOT rely on it.`

That comment was **true when written and was silently falsified by a protocol change elsewhere.** Through 2026-07-29 the record was one commit, `chore: daily council meeting [skip ci]` — the push trigger genuinely never fired, and the 12:00/15:00/18:00 crons on `main` were the real delivery path, exactly as documented. From 2026-07-31 the council switched to named part-commits (`council: daily meeting <date> (part N)`) **without** `[skip ci]`. The trigger the workflow explicitly says it does not rely on became the sole delivery path, firing at Part 1. Nothing in the repository noticed, because the workflow's documentation of its own behaviour was never re-checked against the new commit convention.

---

### 2. Defect 1 — your four questions, answered

**(a) Was 2026-08-04 luck? Yes, and I can put a number on it.**

Every request file was committed exactly once (verified: `git log --diff-filter=A` and full log per file agree — no request was ever edited). So firing order = commit order:

| fired | request | lexicographic rank |
|---|---|---|
| 18:22:46Z 08-03 | `2026-08-03-control.json` | 0 |
| 18:24:57Z 08-03 | `2026-08-03-queued.json` | 1 |
| 18:27:36Z 08-03 | `2026-08-03-targeted.json` | 2 |
| 11:19:04Z 08-04 | `2026-08-04-daily.json` | 3 |
| 11:35:10Z 08-04 | `2026-08-04-undocs-control.json` | 4 |
| 11:51:07Z 08-04 | `2026-08-04-undocs-identify.json` | 5 |

Firing order and lexicographic order are **identical for all six**. `control < queued < targeted`; `daily < undocs-control < undocs-identify`. Cross-day is never at risk — the `YYYY-MM-DD` prefix guarantees a new day sorts last. The exposure is purely *within* a day, among same-day siblings. Two independent days each drew one of `3! = 6` orderings and each drew the ascending one: **1/36**. The record's account of 2026-08-04 is correct; the mechanism that made it correct was a coincidence of vocabulary, and that is worth stating in the record because it means the instrument has never once been *tested* — it has been *lucky* six times and unlucky the seventh.

**(b) Prior silent overwrites: the signature you named finds nothing; a different signature finds four.**

I paired all 8 request files against all 8 result files. **No request has ever lacked a result.** Your proposed signature (request with no matching result) is clean across the whole archive. That line of inquiry comes up empty and I am not going to dress it up.

But the archive does record overwrites — I checked every commit touching every result file, not just the current contents. Four of eight result files were written more than once:

| result file | writes | run IDs / times |
|---|---|---|
| `2026-08-03-control.json` | 2 | 18:23:14Z, 18:24:52Z |
| `2026-08-03-targeted.json` | 3 | 18:28:14Z, 18:31:21Z, 22:17:09Z |
| `2026-08-04-undocs-identify.json` | 5 | 11:52:34Z → 15:57:05Z |
| `2026-08-05-daily.json` | 2 | 11:20:54Z, 11:41:05Z |

Only the last is a *selection* failure. The other three are re-fires of what was correctly the newest request. But they are all *data-loss* events, because —

**(c) The defect underneath the defect: the relay's reductions are not reproducible, and exactly one host is responsible.**

I diffed every version of every re-written result, row by row. Result:

| host | observations across re-runs | distinct sha256 |
|---|---|---|
| `investmentpolicy.unctad.org` | **12** | **12** |
| `uncitral.un.org` (`/en/commission`, `sps.pdf`) | 12 | 2 (one per URL) |
| `undocs.org` (`/en/A/80/17`, `/en/A/81/99`) | 10, spanning 11:52:34Z → 15:57:05Z (4h 05m) | 2 (one per URL) |
| `example.com` (control) | 12 | 1 |

Twelve UNCTAD re-fetches produced twelve different hashes. Zero repeats. Every other host was byte-identical across up to five runs and up to four hours. This is a clean measurement with a within-project negative control, and it settles the 2026-08-04 B8 objection ("the nonce conclusion is stated as established and it is not") in the affirmative — but only for the narrow proposition it actually supports:

- **Established:** `sha256` is not a stable content identifier for `investmentpolicy.unctad.org`. Any record that cites an UNCTAD IIA sha256 as a re-verifiable fingerprint, or infers from two differing UNCTAD hashes that the *page changed*, is reading a value that changes on every request.
- **Not established, and must not be written as if it were:** the *mechanism* (per-request nonce vs. genuine content churn vs. CDN variant) — I measured the symptom, not the cause. Nor does host stability *in these observations* establish that `undocs.org` or `uncitral.un.org` hashes are stable identifiers in general; it establishes only that they were stable here.

This is why the overwrite is not a bookkeeping nuisance. For an UNCTAD row, a re-run does not reproduce the prior reduction — it destroys it.

**(d) Is "cite the commit" sufficient repair? Necessary, nearly sufficient, and it has one failure mode you should rule out today.**

For *retrievability*: yes, fully. Git blobs are immutable and content-addressed; `946b8b7:analytics/fetch-results/2026-08-05-daily.json` is a permanent locator for the run-`31001060670` reduction. Two gaps remain:

1. **The path lies and does not know it.** `analytics/fetch-results/2026-08-05-daily.json` now holds a different document with the same name, the same `"request"` field, and different `sha256` values on two of four rows. A reader who resolves the *path* rather than the *commit* gets the wrong artifact with nothing warning them. The repair belongs at the path as well as in the citation: the record should carry an explicit line that the working-tree file at that path was overwritten by run `31002416607` at 11:41:05Z, and that the cited reduction is the blob at `946b8b7`.
2. **The cited blob's survival depends on how this branch is merged.** `946b8b7` is reachable today only from `council/daily-2026-08-05`. A normal merge commit keeps it reachable from `main` forever. A **squash merge** does not — the original commits become unreachable and are eventually garbage-collected, and the record's repair citation dies with them. **Do not squash-merge this branch.** That is a concrete, dated instruction, and it is the strongest reason I would *also* want fix (2) below rather than relying on citation discipline alone.

**(e) Smallest correct fix — and the "obvious" option is the one that is actively wrong.**

- **mtime-based selection — reject.** `git` does not preserve mtimes. `actions/checkout@v4` writes every file at checkout time, so on a runner all eight request files have near-identical mtimes in an order that reflects nothing. This would replace a deterministic-but-wrong selector with a non-deterministic one — strictly worse, and worse in the way that is hardest to debug. I want this named in the record so it is not proposed again next week.
- **Process every request lacking a matching result — adopt.** Selection stops depending on names entirely. Verified property that makes this safe: **it is a strict no-op against today's repository** — all 8 requests already have results, so landing it changes no archived artifact and fetches nothing. Cost: an intentional re-fire of an existing request now needs `--request` explicitly (the record did re-fire three times, so this is a real behaviour change, though arguably the correct one). Guardrail, as a condition of adoption: the predicate must be *"no file at that path"* and nothing about the file's contents — a `void` result is a result, and auto-retrying VOIDs would re-hit hosts on every push.
- **Refuse to overwrite an existing result — adopt, alongside.** Necessary but *not* sufficient on its own: had only this landed, today's relay would have selected the daily batch, refused to write, and the attribution batch still would not have run. Precedent for the ergonomics: `actions/upload-artifact@v4` — "Artifacts created by upload-artifact@v4 are immutable"; "uploading to the same artifact via multiple jobs is _not_ supported with `v4`"; replacement requires an explicit `overwrite: true` (verified by fetch 2026-08-05, https://github.com/actions/upload-artifact). Overwriting a completed reduction should be an explicit act, never the default.
- **Run-stamped / content-addressed output name — reject for now.** It makes reductions append-only, which is the right long-run shape, but it orphans every existing `analytics/fetch-results/<name>.json` citation in the record unless the plain name is also kept as a pointer — which reintroduces the mutable path. Higher blast radius than the problem justifies today.

**My choice: the two adoptions together (~8 lines in `scripts/fetch_relay.py`).** Selection becomes name-independent and results become write-once. The decisive argument is that the combination is provably a no-op against the current archive, so it can land without touching a single committed reduction. **Proposed only.**

---

### 3. Defect 2 — the day-by-day answer, from run logs rather than inference

Method: I pulled all 187 `daily-update` runs from the Actions API, matched each `.sent/<DATE>` marker timestamp to the run that wrote it, and diffed `analytics/daily-research/<DATE>.md` **at that run's `head_sha`** against the final record. The `head_sha` is exactly the tree that was emailed, so this is a measurement, not an estimate.

One artifact to discount first: ~25 days show a uniform 76-character delta. It is not truncation — it is the vault graph block appended later by the mapping script:

```
<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->
```

Discounting that, the record:

| period | days | delivery |
|---|---|---|
| 2026-06-29 | 1 | **complete** (single-commit day) |
| 2026-06-30 → 2026-07-08 | 10 | **not determinable by this method** — see below |
| 2026-07-09 → 2026-07-29 | 21 | **complete** (single `[skip ci]` commit; push trigger never fired; cron delivered) |
| **2026-07-30** | 1 | **NEVER DELIVERED** |
| 2026-07-31 | 1 | **complete** — sent 16:39:38Z, last part 16:37:03Z, 39/39 headings |
| **2026-08-01** | 1 | **NEVER DELIVERED** |
| 2026-08-02 | 1 | **complete** — 36/36 headings; five parts arrived in one push at 11:33:40Z |
| **2026-08-03** | 1 | **TRUNCATED after Part 3** |
| **2026-08-04** | 1 | **TRUNCATED after Part 1** |
| **2026-08-05** | 1 | **TRUNCATED after Part 1** |

**The three truncations, with the cut point:**

- **2026-08-03** — sent 11:33:02Z at `688bc77` (Part 3). **11 of 26 headings; 26,913 of 65,272 chars missing (41%).** Cut away: Part 4 (Analytics Officer, all four sections), Part 5 (Systems Researcher), **Part 6 (Chairman's Rulings and Close-Out) in its entirety** — rulings on binding objections, hedges, gap-marker dispositions, accountability, next steps, escalations to the principal.
- **2026-08-04** — sent 11:21:04Z at `8756859` (Part 1). **8 of 71 headings; 128,782 of 138,428 chars missing (93%).** The operator received the chairman's agenda and nothing else. Cut away: the entire analyst memo (Tasks 1–3, including §1.5 on the differing sha256 and §1.6 the gap-marker disposition), the integrity officer's **eight binding objections B1–B8**, the analytics officer's part, and the close-out.
- **2026-08-05 (today)** — sent 11:22:37Z at `042229d` (Part 1). **8 of 38 headings so far; 63,708 of 73,127 chars missing (87%)**, and the record is still growing. Everything from Part 2 onward — including the Task 3 relay-reachability finding and the Task 4 ASIL reconciliation verdict — is currently unsent.

**The two never-delivered days — verified verbatim from the runner logs, not inferred from marker absence:**

- **2026-07-30**, job `91018358184`, 22:12:41Z:
  ```
  state:      ESCALATION
  subject:    ISDS Daily Council Meeting — 2026-07-30 — ALERT: no record by end of day
  record:     NOT found
  sent:       True
  ```
  The record (40,860 chars) was first committed the next day, `20c573c` 2026-07-31T15:18:19Z. It was never mailed, on that day or any other. The operator received an alert saying it did not exist.
- **2026-08-01**, job `91422980587`, 22:02:46Z: same three lines, same `record: NOT found`, `sent: True`. The record (80,098 chars, Parts 1–6) was committed 22:53:38Z–23:10:57Z — **51 minutes after the alert declaring it missing.** The branch `council/daily-2026-08-01` was pushed on 2026-08-03 (run `30819207000`), by which time `date_str` was `2026-08-03` and that day's marker already existed, so it skipped. Never mailed.

**Cannot be determined, and why:** for **2026-06-30 through 2026-07-08** (10 days), `analytics/daily-research/<DATE>.md` did not exist at the sending run's `head_sha` — all ten files were backfilled together on 2026-07-18 (`4af4d80`, "feat(verify): append-only ledger + state machine"). Those emails were built from `_find_record`'s `analytics/council-log.md` fallback (`scripts/send_daily_update.py:88-95`). Whether the operator received a complete day is a real question, but it is a *different* question from part-truncation and the file comparison cannot answer it. I did not resolve it and I am not going to guess.

**The headline:** over the last seven council days, a complete record reached the operator **twice** — 2026-07-31 and 2026-08-02 — and **both times by accident**, because those sessions happened to push all their parts in a single push rather than one push per part. Three days were truncated; two were never delivered at all.

---

### 4. Defect 2 — smallest correct fix

- **`[skip ci]` on intermediate part-commits (protocol, no code) — reject as the primary fix.** It works, but it is a discipline that must be remembered on every part of every day, and it fails *silently and identically to the present bug* the first time it is forgotten. It also skips every other workflow on those pushes. This is the same class of mechanism that produced the defect: correct-by-convention, unenforced, undetected when violated.
- **Narrow the push trigger to `main` — adopt.** One line under `on.push`:
  `branches: [main]`
  This *restores the design the workflow's own comment already describes*: cron on `main` is the delivery path, push is a fallback for non-skip pushes. Consequences, honestly:
  - The email moves from ~11:22Z to whenever the branch merges, or the next cron. Historically the merge to `main` was same-day and fast (08-04: 12:00:33Z; 08-03: 13:42–13:47Z), so latency is roughly one to two hours, and the 12/15/18 crons catch it regardless.
  - **It converts the 07-30 / 08-01 silent-loss class into a loud alert.** If a day's branch is never merged, the cron on `main` finds no record and the 21:00 escalation fires — which is exactly the right behaviour, and exactly what should have happened on 2026-08-01 rather than an alert followed by 51 minutes of unmailed work.
  - It needs no change to the marker at all. "One email per day" stays correct once only one push per day can trigger.
  - Residual gap: a branch merged after 21:00Z draws a spurious ALERT, then the merge push sends the real record. Self-correcting, minor.
- **Marker records which commit was sent, re-send when the record grows — reject.** More code, and it needs a growth threshold and a quiet period to avoid mailing six times a day. That tuning is where it goes wrong, and it trades a one-line fix for a policy.

**My choice: `branches: [main]`, one line.** Smallest possible diff, requires no discipline from the session, restores the documented intent, and turns the worst failure mode (silent total loss) into an alert. **It changes when the operator receives mail and it is a workflow change, so it needs operator sign-off. Proposed only.**

---

### 5. Your marker-deletion plan — the outcome is right, the mechanism is not, and it re-arms the defect

> "my plan is to delete today's marker in the session's final commit so the merge to `main` re-fires the emailer against the complete record"

**That is not what will happen.** Because `on: push` has no branch filter, **the commit that deletes the marker will itself fire `daily-update` on `council/daily-2026-08-05` the moment it is pushed.** That run checks out the branch, finds no marker, finds the record *as of that commit*, sends it, then re-creates the marker and pushes it back to the branch. Therefore:

1. The email fires at **branch-push time, not at merge time**.
2. The marker is **re-created on the branch**, so the later merge-to-`main` push finds `.sent/2026-08-05` present and **skips**.
3. Anything committed after that push — the chairman's Part 6 close-out, or content added while resolving the merge — **is truncated away exactly as before, and this time there is no marker left to delete.** The plan silently re-arms the defect one level up.

Two further failure modes you have not named:

4. **Concurrency cancellation on merge day.** `daily-update` sets `group: daily-update, cancel-in-progress: false`, which protects the *in-progress* run but not *pending* ones. Per GitHub's own docs (`github/docs`, `content/actions/concepts/workflows-and-actions/concurrency.md`, fetched 2026-08-05 at `https://raw.githubusercontent.com/github/docs/main/content/actions/concepts/workflows-and-actions/concurrency.md`): *"by default only one run can be pending in a concurrency group—any additional pending runs cancel the previous one."* Every run to date has concluded `success`, so this has not bitten yet — but merge day is precisely when the marker-delete push, the merge push, and a 12:00Z cron cluster within minutes.
5. **Marker push race.** The branch run does `git pull --rebase --autostash origin <branch> && git push`. If you are committing to the same branch at that moment, your next push is rejected until you pull.

**What to do instead, to get exactly your stated intent:** put `[skip ci]` in the *message of the commit that deletes the marker*. GitHub then fires nothing on that branch push, the marker stays deleted, and the **merge commit to `main`** — which carries no skip token — is what fires the emailer, against the complete merged record. I verified the preconditions today: `origin/main` currently has **neither** `analytics/daily-research/2026-08-05.md` **nor** `.sent/2026-08-05`, so the merge run takes the FOUND-and-not-yet-sent path. And per §2(d): **merge, do not squash** — `946b8b7` must stay reachable or the record's own repair citation stops resolving.

---

### 6. Approval status — against my own interest

**No standing approval covers anything in this entry.** Not the `fetch_relay.py` selection/overwrite change, not the `branches: [main]` line, not the new idea in §7. Every item listed in today's brief as pending remains pending and I have not touched any of it. I wrote to no file in `src/`, `scripts/`, `.github/`, or `prompts/`, and I have not modified `analytics/optimization-log.md` — the idea below is proposed for the chairman to log or decline, not logged by me.

---

### 7. One new idea — per-host reduction-stability control in the relay batch

**Component:** `scripts/fetch_relay.py::run()`, lines 199-225.

**The gap.** Every batch carries exactly one control row, `CONTROL_URL = "https://example.com/"`, and it answers exactly one question: *is the runner able to reach anything*. The relay emits nothing that answers the second question the record actually leans on every day — *does this host's `sha256` mean anything*. The record has been treating `sha256` as a re-verifiable fingerprint for all nine hosts, and §2(c) shows that for one of them it is a value that changes on every request.

**The change.** Per batch, select one target URL whose host has no recorded stability verdict, fetch it **twice within the same run**, and add a `stability` block to the manifest: `{host, sha_a, sha_b, verdict}` with `verdict ∈ {UNSTABLE-WITHIN-RUN, stable-within-run}`. Persist the per-host verdict in a small state file so any given host is double-fetched **at most once, ever**.

**Expected effect, measured not asserted.** From the archive: `investmentpolicy.unctad.org` would have carried `UNSTABLE-WITHIN-RUN` from its very first batch on 2026-08-03. The 2026-08-04 record would not have needed the contested §1.5 inference that drew objection B8 — the instrument would have answered it in the manifest, on day one, instead of the council reasoning toward it across three sessions from n=2.

**Integrity guardrail, as a condition of adoption — the control is one-directional.** It can only ever **refute** stability, never establish it. Two identical hashes three seconds apart say nothing about a document across days. `stable-within-run` must be worded as literally that and must never be cited as evidence that a document did not change. Same discipline as `NOT-READ`, and the same shape as the binding condition on the 2026-08-04 `STALE` proposal.

**Cost.** One extra request per newly-seen host, ever. No new host is contacted — it re-fetches a URL already in the batch. Compliance is inherited unchanged because it goes through `polite_get`, and the second fetch is already spaced ≥ 3.0 s by `MIN_INTERVAL_SECONDS` (`src/sources/base.py:28`). ~20 lines, no new dependency. Free-tier and open-source constraints untouched.

**Dedup — checked against all 28 entries.** **Nearest neighbour: 2026-08-03, "Egress-triage protocol for delegated sessions."** That entry pairs a suspect-host probe with a *neutral-host* probe to establish whether the **instrument** is alive. This one re-fetches the *same* host to establish whether that host's **fingerprint is meaningful**. Different control, different question (instrument liveness vs. reduction semantics), different failure caught (mistaking a proxy denial for an origin answer vs. mistaking a nonce for a content change). Also distinct from 2026-07-31 (health column) and 2026-08-04 (`STALE`), which both concern source *yield over time* in the collection pipeline — neither touches the relay. **Nothing in the log touches the relay at all.**

**Effort:** low. **Status: PROPOSED ONLY** — not built, not applied, awaiting operator sign-off.

---

### 8. Self-training note

Two things to record against my own history.

**A pattern in my own output I should name before someone else does.** This is my third seated session. I have now proposed: a health column (2026-07-31), a `STALE` signal (2026-08-04), and a stability control (today). All three are observability instruments; the first two are still `proposed` and unactioned. If the operator's approval queue is the bottleneck — and with ten items pending it plainly is — then a third pending observability proposal has low marginal value. **I would put both defect fixes in §2(e) and §4 ahead of my own new idea in the queue**, and I would not object if the chairman declines to log §7 at all this week. The idea is cheap and it will keep.

**A recalibration on what this seat is for.** My contract says one excellent sourced suggestion beats five vague ones. Today the highest-value thing I produced was not a suggestion — it was two measurements: the 12-observation sha-drift table with its negative control, which converts a contested inference (B8) into a settled narrow finding; and the 36-day delivery table, which converts "some emails were probably truncated" into five named days, two of which were never delivered at all and neither of which anyone had noticed. Both were fully determinable from committed data and runner logs the whole time. The lesson I am taking forward: **when a question is determinable, measuring it beats proposing an instrument to measure it later.** I should check for that condition first each session.

**One correction to my own §2(b) instinct.** I went looking for your signature — a request with no matching result — expected to find prior incidents, and found none. That line came up empty and I have said so rather than reframing it into a finding. The overwrites I did find required a different query than the one I was asked to run.

---

**Files read this session (absolute paths):** `/home/user/isds-watcher/scripts/fetch_relay.py`, `/home/user/isds-watcher/scripts/send_daily_update.py`, `/home/user/isds-watcher/.github/workflows/daily-update.yml`, `/home/user/isds-watcher/.github/workflows/fetch-relay.yml`, `/home/user/isds-watcher/analytics/optimization-log.md`, `/home/user/isds-watcher/src/sources/base.py` (grep only), plus every file under `/home/user/isds-watcher/analytics/fetch-requests/`, `/home/user/isds-watcher/analytics/fetch-results/`, and `/home/user/isds-watcher/analytics/daily-research/.sent/`, and their full git histories.

---

## Part 6 — Chairman's Close-Out

Written in this session, after every member had returned and after I had verified the contested lines myself.

### The day in one paragraph

The council set out to do three small things — a day-5 null, a low-priority confidence calibration, and two record-hygiene questions — and instead found that **the instrument has been failing to deliver its own output for a week, and nobody noticed because the failure is silent at both ends.** Over the last seven council days a complete meeting record reached Emory **twice**, and the systems seat established that both times were accidents of commit timing. Three days were truncated; two were never delivered at all. The 2026-08-04 record — the analyst memo, eight sustained objections, the entire close-out — reached him as an agenda and nothing else, 93% missing. That is the same failure this project has been fighting since the two lost weeks, in the one costume nobody had checked: not work that never reached `main`, but work that reached `main` and never reached the reader. The research tasks themselves came in as expected: A/81/17 day-5 null with its control, the attribution question unanswered and correctly classified as mis-anchored rather than null, the treaty-text residual ruled structurally out of the relay's reach, and the ASIL thread ruled un-executable since the day it was opened.

### The instrument state

Direct fetch is dead, both layers, on neutral controls — fourth consecutive session, unchanged, and not a fact about any research resource. The relay is alive and did real work: three batches today (runs 31001060670, 31002416607, 31002699165), every one with a passing neutral control, none void. But the relay also **damaged today's own record while the record was being written**, and that is the more important instrument fact of the day. Both defects are in Part 5 with measurements.

### Rulings on the vetting round

**Every binding objection is SUSTAINED. There is no overrule today.** I verified 0805-B1, B3, B4 and B6 against the files myself before ruling, per the discipline this council adopted after I overruled the officer's B3(b) on 2026-08-03. Per 0805-O6, which I adopt, today's objections are recorded with the `0805-` prefix so they cannot collide with the standing `B1–B8` rulings of 2026-08-04.

**0805-B1 — SUSTAINED, and the fault is mine.** `agents/Workflow Threads.md` does not carry the ASIL thread. I asserted that it did, in the sentence immediately before the sentence in which I said I was not asserting it. **Part 1 is corrected in place, above, and the erroneous paragraph is left standing under the correction rather than rewritten.** The officer is right to record this as a pattern rather than an incident: three consecutive days, each time a premise about the project's own record asserted without the grep. See Accountability.

**0805-B2 — SUSTAINED, and it is the day's principal objection.** The analyst's five carrying-span V-mark screens are understated in three of five entries, and the misses are substantive — most sharply `Workflow Threads.md:200`, which is standing escalation 6 itself, missed by a screen that returned 2 in the very section that invokes escalation 6. The officer's own fairness note is adopted with it: none of the missed occurrences contradicts the memo, so **the defect is the checkability, not the conclusion.** That distinction is the whole point of Amendment 2 — a screen a non-author cannot check is not a screen. The corrected counts enter the record via this ruling; the analyst's memo stands verbatim, as every member's does, and is read subject to it.

**0805-B3 — SUSTAINED.** Both fetch classes were enumerated short. The Commission Sessions page had been fetched **seven** times across **four** runner sessions at the time the memo was written, not six across three, the missing one being `2026-08-03-queued.json` at 18:25:23Z; the true interval was **40 h 55 m**, not "roughly 37 hours", and the word "whole" was therefore false. The treaty-978 page stood at seven fetches and seven distinct digests, not six and six. **As the record closes: nine Commission fetches across five runs at one digest and one byte length spanning 41 h 16 m; eleven treaty-978 fetches at eleven distinct sha256, every one at exactly 207,411 bytes.** I record the officer's observation that every omission understated the analyst's own case — that is the honest direction for an error to run, and it is not nothing.

**0805-B4 — SUSTAINED.** "Minutes apart" is struck. The reduction carries one `fetched_at` per manifest and no per-record timestamp; the only interval the code guarantees is the 3.0-second per-domain floor. The corrected form is the officer's: *consecutively within one run, separated by at least 3.0 seconds and by no recorded amount more.* The genuinely multi-time-point evidence exists and is stronger — 11:20:54Z and 11:41:05Z are separate runner sessions twenty minutes apart, and they returned different digests.

**0805-B5 — SUSTAINED, and the second half is the serious half.** "Entire" is struck: `STATE_OF_THE_ANSWER.md:105` names unisl399 and prior IAReporter sourcing, so unisl400 carries the **July-29 upgrade to HIGH**, not the whole confirmation. And the memo cited the favourable half of that line while dropping the adverse half — unisl400's **body is 403-blocked and the title alone was confirmed**. That is selective-flag reporting, and it means the record's HIGH on UNCITRAL adoption rests on a title. The officer's conditional framing of the 403 is adopted too: a 403 characterisation is an access artefact until a same-host positive control under a varied UA is run, and that finding was made on `uncitral.un.org` — absolutising it to `unis.unvienna.org` would be the very error the officer names.

**0805-B6 — SUSTAINED as to wording only.** "Structurally incapable of reporting a hyperlink target from any page it fetches" is narrowed to **"cannot report a URL that appears only inside an attribute"** — a URL printed as visible body text survives tag-stripping, and `final_url` is a network-learned URL the reduction does report. Insight 2 carries the same narrowing. **Task 3's structural "no" is unaffected and I sustain it in full**, on the officer's independent check of all three legs.

**0805-B7 — SUSTAINED.** Four citations repoint: `2026-08-01.md:364` → **`:362`**; `fetch_relay.py:121-122` → **`:120-121`**; `Workflow Threads.md:9-10` → **`:9-11`**; `Workflow Threads.md:26` → **`:26-27`**. The other 34 verified exact, which is a better citation rate than the seat posted yesterday.

**0805-O1 through O7 — all accepted.** O1 is a genuinely new epistemic pattern and I adopt it as binding house rule, below. O3 is adopted: `analytics/fetch-results/2026-08-03-control.json` record 1 is the live demonstration of the PDF gate — `sps.pdf`, HTTP 200, 2,104,033 bytes, `excerpt: ""` — and it converts Task 3 §3.3 from a reading of the code into a behaviour the instrument has already exhibited. O4's hedge on the colon inference is adopted and the discard itself stands. O5 is adopted: the fetched URL is the form at `STATE_OF_THE_ANSWER.md:86`, not `:42`. O2, O6 and O7 are adopted as stated.

**A1 — the officer's ruling is adopted: commit-pin, not correction.** The analyst is at fault on nothing. Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` is to be read as **`@946b8b7`**, and the systems seat's stronger form is adopted with it: **the working-tree file at that path was overwritten by run 31002416607 at 2026-08-05T11:41:05Z and now holds a different document under the same name.** A reader resolving the path rather than the commit gets the wrong artefact with nothing to warn them. That sentence belongs in the record and it is here.

**A2 — the officer's ruling is adopted: the page head does NOT discharge B7.** The retrieved title "China - Switzerland BIT (2009)" is a plausible mechanical inverse of the requested slug, and no control separates a title keyed to UNCTAD's record for id 978 from a title templated off the path. **B7's wording changes to: identification is URL-slug-level, with a page-rendered title not yet separable from the requested slug.** I authorise the officer's Row C for tomorrow — a deliberately false slug at the same id — and I adopt its accompanying distinction as binding, because it will otherwise be argued the wrong way: **a deliberately false URL retrieves no research resource and is therefore a control, not discovery.** §3.1's refusal to construct a research locator is untouched by it.

**A3 — adopted in full, pre-emptively.** Treaty 978's umbrella clause maps **Yes** and China–France maps **Inconclusive**; those may be compared for identity and difference and **may not be ranked**, because UNCTAD's codebook is still not in the record. The adjacency finding is bounded to the retrieved window — "does not immediately follow within 300 characters of right context", never "does not follow". And Row A's `find_matched: false` is false by construction and is not a null.

### The two instrument defects, and what I am doing about each

**Defect 1 — the relay selects lexicographically and overwrites in place.** `newest_request()` sorts by filename, not mtime, despite its name; `run()` writes to `out_dir / request_path.name` with no existence check. Today that re-ran a completed batch, overwrote its reduction, and left the authorised batch unfetched. The systems seat established that the six prior batches ran correctly by **coincidence of vocabulary** — firing order matched lexicographic order all six times, a 1-in-36 draw across two days — so the instrument has never been tested, only lucky. And underneath it sits a defect neither I nor the analyst had reached: **twelve re-fetches of `investmentpolicy.unctad.org` produced twelve distinct sha256, against a within-project negative control of every other host returning byte-identical digests across up to five runs and four hours.** That settles yesterday's B8 in the affirmative for the narrow proposition it supports — an UNCTAD sha256 is not a stable content identifier — and expressly not for the mechanism, which remains unmeasured. It also means the overwrite is not bookkeeping: for an UNCTAD row a re-run does not reproduce the prior reduction, it destroys it. **I applied a filename workaround (`zz-` prefix) to get the authorised batch served, and I record plainly that a workaround is not a fix.** Both proposed fixes go to Emory below. The seat's rejection of mtime-based selection is adopted into the record so it is not re-proposed: `actions/checkout` writes every file at checkout time, so mtimes on a runner carry no information.

**Defect 2 — the emailer sends at the first part-commit and de-duplicates the rest away.** The root cause is one I had not named and the systems seat did: `on: push` in `daily-update.yml` has **no `branches:` filter**, so every council part-commit on a feature branch fires it. Worse, the workflow's own header comment asserts it does not rely on the push trigger because the routine commits with `[skip ci]` — **true when written, and silently falsified when the council switched to named part-commits on 2026-07-31.** A component's documentation of its own behaviour went stale against a protocol change elsewhere and nothing in the repository could notice. That is the same mechanism the analyst diagnosed in the vault registers this morning (Part 2 §4.7) and the systems seat diagnosed in `src/` yesterday: duplicated knowledge with no derivation. Three files, three costumes, one disease.

**On my own plan, which was wrong.** I stated I would delete today's `.sent` marker in the final commit so that the merge to `main` would re-fire the emailer against the complete record. The systems seat showed that this **re-arms the defect one level up**: the marker-deleting push would itself fire the emailer on the branch, send whatever existed at that instant, and re-create the marker — truncating the close-out away with no marker left to delete. **I adopt its correction in full and it is what this session does:** the marker-deleting commit carries `[skip ci]`, so nothing fires on the branch, and the **merge commit to `main`** — which carries no skip token — is what mails the complete record. And per its §2(d): **this branch is merged, never squashed**, because `946b8b7` must stay reachable or the A1 repair citation stops resolving. I have verified the precondition it named: `origin/main` currently holds neither today's record nor today's marker.

### New binding house rules, adopted today

1. **Mutable-reduction citation (officer taxonomy 25).** Every citation to a relay reduction carries the commit sha that holds it. A reduction is evidence; a path that can be rewritten is not a citation.
2. **`find` selects a position, not a proposition** (analyst, Part 2 §2.1). Request-echo and boilerplate-preemption are one defect with two faces. Before firing a row, answer: *is the first occurrence of this string in the tag-stripped body the occurrence I want?* Where the honest answer is "I cannot know", the row is written up as a **probe**, with what a miss would mean stated in advance.
3. **A PDF row's `find_matched: false` is guaranteed by the content-type gate and is never evidence of textual absence.** Demonstrated, not merely derived, at `2026-08-03-control.json` record 1.
4. **A mis-anchored row is not a null.** A null says the page was asked and did not answer; a mis-anchored row says the question was never put. Recording the second as the first enters a false negative about a live source.
5. **A deliberately false URL is a control, not discovery.**
6. **Intermediate council part-commits carry `[skip ci]`; the final commit does not.** This is a stopgap I am adopting *because* the systems seat is right that it fails silently when forgotten — it is the only lever inside this session's authority, and the durable fix is Emory's to sign off.

### Gap-marker dispositions

- **A/81/17 — day 5, null, no gap slug.** Monitoring thread inside its expected window, per the house rule. The three standing questions ride unchanged. Recorded with its two qualifications: rendered text not raw HTML, and days 3/4/5 are one page state observed repeatedly, not independent confirmations.
- **Treaty-978 mapper attribution and vintage — NOT retrieved, and the row that sought it is classified MIS-ANCHORED, not null.** The probe rows returned the page head and the umbrella-clause value instead. The attribution question stays open at low priority; the confidence calibration the record wanted for treaty 978 against its two siblings is still not available.
- **`china-switzerland-bit-2009-forum-provision-text` — RULED NOT RELAY-WORK, and I am assigning it rather than letting it inherit forward unassigned.** The analyst's structural "no" is sustained: the relay cannot learn a link target from an attribute, returns no text for a PDF, and cannot chain-read a provision through a 400-character cap with a literal-only `find`. Of the three homes it offered, **I pick (a): it goes to Emory as a manual retrieval with a preserved `source_snapshot`, executed in one pass with the already-downgraded escalation 2.** The seat's warning is right and is why I am choosing now: an unassigned residual with no reachable method is exactly the ASIL failure, and I am not opening a second instance of it on the day we diagnosed the first.
- **ASIL ILIB reconciliation — RULED UN-EXECUTABLE AS FRAMED, and split.** The named artefact `asil_target.html` was written to a per-session scratchpad on the operator's local machine and never committed; `asil.org` and `unis.unvienna.org` are both off-allowlist. **The SP-list delta limb folds into the A/81/17 thread (A1)** — the delta is already fully stated in the record and only the adopted Supplementary Provisions text can resolve it, so it becomes executable on publication and needs no ASIL retrieval ever. **The unisl398 limb goes to Emory**, because it bears on the record's own foundation. And the thread gets a named owner in the register that assigns owners, which is the hygiene that stops a fifth re-inheritance.
- **`china-france-bit-2007-protocol-text`** — untouched, zero budget as assigned; standing open, no fresh failure to count.
- **New, instrument/method:** the relay's lexicographic selection and in-place overwrite; the UNCTAD sha256 instability finding; and the emailer's first-commit truncation. All three logged under instrument, never under any research thread.

### Accountability

- **Chairman (Opus 5).** The session's value came from two decisions that were mine and were right: probing the instruments before writing the agenda, and putting the ASIL question to the analyst as a question rather than announcing it as a finding. The second is what kept 0805-B1 recoverable. Against that stands the objection itself, and it is not a small one: **three consecutive days on which a Part 1 premise about the project's own record was asserted without the grep** (08-03 B3(a), 08-04 B1, 08-05 0805-B1). Yesterday's countermeasure — *quote the whole line before ruling on what it says* — did not fire today because I was not quoting a line; I was characterising a file I had not opened. So the rule is generalised and it is the fourth iteration of the same lesson at my own desk: **before Part 1 asserts anything about the repository, run the command and paste the output.** Not "quote the line" — *run the command*. Two further faults are mine today. I named the second relay batch in a way that silently cancelled it, and I did not check the selection logic before committing a file whose name I chose freely. And my merge plan was wrong in a way that would have truncated this very close-out away — caught only because I put it to the systems seat as a question I could be wrong about. That is now three times in one session that the recoverable outcome came from asking rather than asserting, which is the pattern I should be extracting rather than the individual errors. I also deviated from my own stated order of business: I convened Parts 4 and 5 in parallel with the vetting round rather than after the checkpoint. Parts 1–2 were committed and pushed before either was seated, so the checkpoint's purpose was honoured, but the stated order was not and I record the deviation rather than quietly reordering the agenda to match what I did.
- **Research analyst (Opus 5).** A correctly-reasoned zero-search day on questions no index could answer, and the seat said so as a deliberate spend rather than an omission. Three things stand out. The offset arithmetic in §2.1 is the best single passage of the day: it converted a plausible reading of an excerpt into a proof by computing against the excerpt's own offsets, and the officer re-derived it independently and confirmed it. The Task 3 structural "no" is a complete answer that refuses to dress itself up — and it correctly identified that the relay's inability to deliver a document is a design property, not a defect. And §4.2 corrected the chairman on a matter of fact, plainly, which is what the seat is for. Against that, 0805-B2 is a real and systemic defect: the carrying-span screens — the very apparatus that makes the memo checkable by someone other than its author — were not run at the scope they claim, in three of five entries, and one miss was standing escalation 6 sitting in the register the memo was auditing. The seat's citation discipline was otherwise excellent (34 of 38 exact). The pattern I noted yesterday — impeccable retrieval discipline, looser application discipline — did **not** recur today; the overreaches this time (0805-B4, 0805-B5) were both in the direction of understating its own case, except B5's dropped 403 flag, which is the one that matters and which the seat had policed correctly in another section of the same memo.
- **Integrity officer (Opus 4.8).** The second consecutive exemplary seating and I would rate it above yesterday's. It verified 38 of ~40 citations by opening each line; it re-ran every grep the analyst reported plus four more spellings rather than accepting a zero-hit screen; it counted every fetch by URL across eight files rather than trusting an enumeration, and caught two short counts in a direction that helped the party it was checking. It ruled on two questions I sent it mid-vetting and got both right, including refusing to convert a weak independence argument into a discharge — and then named the exact control that would settle it, with the distinction (control vs. discovery) that the ruling needs to survive contact with §3.1. Taxonomy entry 25 is a genuinely new pattern caught live against the session's own memo. Its Rule 4 compliance is worth naming: it read the four occurrences its own objection had found and reported that none contradicted the memo, which weakened its own objection and was the honest thing to do.
- **Systems researcher (Opus 4.8).** The strongest single contribution of the day and it changed what this session did. It confirmed both defects at the line, then found a third underneath one of them that nobody had asked about. It answered a question I posed with a proposed signature, found that signature clean across the whole archive, said so plainly instead of reframing an empty result into a finding, and then found four real overwrites with a different query. Its 12-observation sha-drift table with a within-project negative control converts a contested inference the council spent three sessions circling into a settled narrow finding, correctly bounded to the symptom and expressly not to the mechanism. Its 36-day delivery table replaces "some emails were probably truncated" with five named days, cut points, byte counts, and two days verified from runner logs as never delivered at all. It rejected the obvious fix (mtime) with a reason that will stop it recurring, chose fixes on the argument that they are provably a no-op against the current archive, and corrected my merge plan with a specific failure mode plus two more I had not seen. And it argued **against its own interest twice**: it claimed no standing approval covers anything it proposed, and it recommended its own new idea be deprioritised behind the two defect fixes, naming its own three-session pattern of proposing observability instruments that then sit unactioned. Its self-training note — *when a question is determinable, measuring it beats proposing an instrument to measure it later* — is the best methodological line any seat has produced this week.
- **Analytics officer (Opus 4.8).** Fifth consecutive exemplary seating. Rule 1 sentence with numbers copied from the file; the unchanged digest confirmed from `git log` rather than taken on my word; per-source correctly not re-derived in one line. It resolved all four of its prior findings and **corrected itself on one**, narrowing its own 08-04 statement: `health_warnings: []` is a cold-start artefact with respect to DEGRADED and a *genuine negative* with respect to COLLECTION ANOMALY. It confirmed the 2026-08-17 date by re-deriving every link in the chain and then added the condition the operator's list needed — earliest possible and expected, not guaranteed, because a lifted Cloudflare challenge resets the streak — plus the observation that the false causal claim survives either branch. On the question I sent it, it found the digest path structurally safe and then found a real analogue anyway: the empty-run suppression at `src/main.py:277-282` cannot distinguish "everything deduped" from "collection failed entirely", so the instrument's loudest alarm is routed exclusively through the one channel that condition switches off. It priced that honestly as remote-but-thinning rather than inflating it.
- **Procedural.** All four seated members were real subagents spawned from their committed definitions in `.claude/agents/`; no member's voice was performed; every member section is verbatim. Zero spawn failures, zero transport faults. Spend checkpoint honoured — Parts 1–4 committed and pushed before Part 5 was written up. Nothing built, nothing applied, no pending approval touched: the only files this session wrote are the day's record, two relay request files, and the `.sent` marker deletion.

### Where this leaves the research question

No closer on substance today, and I will not dress a null as progress. A/81/17 is still unpublished on day 5 of its window, and it is the document three standing questions ride on. The China–Switzerland forum cell is where 2026-08-04 left it — mapping-level, unranked, with the treaty text unread and now formally routed to the operator because the relay provably cannot reach it. What today bought is not an answer but the **reliability of the apparatus that carries answers**: a record that reaches `main` but not the reader is worth nothing, and for a week that is what this council has been producing. Five of the last seven days' findings — including yesterday's eight sustained objections and the entire China–Switzerland gap closure — were written, verified, committed, and never read. Fixing that is worth more to the research question this week than any single retrieval would have been.

### Next steps for tomorrow

1. **A/81/17, day 6 (analyst, one relay row + same-page control).** Unchanged form. Do not use `undocs.org` — retired 2026-08-04.
2. **Row C, the B7 discharge control (analyst, one relay row).** `…/bilateral-investment-treaties/978/zzz---zzz-bit-1900-`, `find: ""`. If the head still reads "China - Switzerland BIT (2009)", the title is keyed to the id and B7 discharges to database-record level; if it returns a slug-derived title or a 404, B7 stands in full. Authorised today; state in advance what makes it negative.
3. **Name the request file so it sorts last, and check `analytics/fetch-results/` for a file of that name before firing.** Until the relay is fixed, this is the operating discipline and it is on the chairman, not the analyst.
4. **Re-run the five carrying-span V-mark screens at repo scope (analyst)**, restate the counts, and give every nonzero occurrence that is not the source of `Q` a referent clause — 0805-B2's countermeasure, applied to tomorrow's memo, not retrospectively to yesterday's.
5. **Register hygiene (chairman, and it is mine to do).** Give the ASIL thread an owner in `agents/Workflow Threads.md`; strike the un-executable "reconcile from the officer's saved raw HTML" from `state/research_log.json`; refresh A1 and A4, which are stale against the 08-04 close-out.
6. **Chairman's delegation template, amended — fourth iteration:** before Part 1 asserts anything about the repository, **run the command and paste the output.**

### Escalations to Emory

1. **NEW, and it is the most important thing on this list: you have not been reading this council's work for a week.** Over the last seven council days a complete record reached you **twice** (07-31, 08-02), and the systems seat established both were accidents of commit timing. **2026-08-03 was cut after Part 3 (41% missing, including the entire close-out). 2026-08-04 was cut after Part 1 (93% missing — you got the agenda; the analyst memo, eight sustained objections and the close-out never went). 2026-08-05 was cut after Part 1. 2026-07-30 and 2026-08-01 were never delivered at all** — on both you received an end-of-day ALERT saying no record existed, and on 08-01 the record was committed 51 minutes after that alert and never mailed. Nothing is lost: every record is on `main` and readable. **The three truncated days are worth reading now** — 08-04 in particular, which closed the China–Switzerland gap. Today's record will reach you complete, because the merge commit is what mails it.
2. **NEW, and it needs your sign-off: `daily-update.yml` needs `branches: [main]` under `on.push`.** One line. It restores the delivery design the workflow's own comment already describes, needs no discipline from the session, and converts the worst failure mode — silent total loss, as on 07-30 and 08-01 — into a loud end-of-day alert. It changes when you receive mail (from ~11:22Z to merge time, historically one to two hours later, with the 12/15/18 crons as backstop), which is why it is yours and not mine. The council is using `[skip ci]` on intermediate part-commits as a stopgap from today; the systems seat is right that a convention which fails silently when forgotten is the same class of mechanism that caused this.
3. **NEW, and it corrupted a citation in today's own record: the relay picks its batch lexicographically and overwrites results in place.** `newest_request()` sorts by filename despite its name, and `run()` writes to `out_dir/<request name>` with no existence check. Today that re-ran a finished batch, **overwrote the reduction Part 2 cites by run id and sha256**, and left the authorised batch unfetched until I renamed it. The proposed fix is ~8 lines: process every request that lacks a matching result, and refuse to overwrite an existing result. It is **provably a no-op against the current archive** — all eight requests already have results — so it can land without touching a single committed reduction. Do not accept an mtime-based fix; `actions/checkout` destroys mtimes. **Related and separate: twelve re-fetches of `investmentpolicy.unctad.org` produced twelve distinct sha256, while every other host was byte-identical across up to five runs.** An UNCTAD sha256 is not a re-verifiable fingerprint, and no record should treat it as one. The mechanism is unmeasured and must not be asserted.
4. **CARRIED FORWARD, dated, and now qualified: the source-health guard will publish a false statement on 2026-08-17.** `NOT-READ` is absent from `_EXEMPT_STATUSES`, so after three zero runs the guard overwrites it with `DEGRADED` and emits a causal claim about a refused source that the standing rule forbids. The analytics seat re-derived every link of the date chain and confirms 2026-08-17 — with one qualification the list should carry: it is the **earliest possible and expected** date, not a guarantee, because a lifted Cloudflare challenge on `italaw` would reset the streak. The false causal claim survives either branch of the failure. **Still the only item on this list with a deadline, and it is twelve days out.**
5. **RE-ROUTED TO YOU, and it is the only research item here: the China–Switzerland BIT (2009) forum provision text.** The relay provably cannot deliver it — no allowlisted locator exists in the record, and the channel returns no text for a PDF and cannot learn a link target from an attribute. This is now a manual retrieval, and it should be executed **in one pass with the downgraded escalation on treaty 978's mapper attribution and vintage**, capturing a preserved `source_snapshot`. That would settle whether the mapped preservation is unconditional or withdrawal-conditioned, and calibrate the cell to the level the two sibling treaties already have.
6. **ALSO YOURS, small: the unisl398 limb of the ASIL thread.** `unis.unvienna.org` is off-allowlist and I am proposing no allowlist change. It matters because the record's confirmation of UNCITRAL adoption rests on unisl400 — whose **body is 403-blocked, with only the title confirmed** — and a third party cites a UNIS release the record does not carry as the announcement. That is a question about the record's own foundation, not housekeeping.
7. **STANDING, unchanged, not re-argued:** the `bing_news` parity test (the seat found its streak is being tracked and the alarm is simply disconnected — worse than untracked); the `STALE` signal; the `unctad_isds` keep-or-retire decision (no automated signal in the instrument today will ever raise it); the empty-run suppression fix at `src/main.py:277-282`; the `da33a30be92ab234` ledger snapshot amendment — to which today adds that the snapshot is not a positional capture of the source page, which `scope_ok: false` was already flagging more broadly than anyone had read it; the `source_analytics.py` sign-off; the health column; the `polite_get` diagnostic CLI; the IAReporter feed swap; the Kluwer source; the category signal; the `build_graph` fix; the emailer CONSISTENCY WARNING question. **All approvals remain PENDING, not granted. Nothing was built or applied today.**

### Self-training note (chairman)

The rule I adopted yesterday was *quote the whole line before ruling on what it says*. It did not fire today, and the reason is instructive: I was not misquoting a line, I was characterising a file I had never opened. The countermeasure was pitched one level too specific, so the same failure walked around it. That is the third time this week a chairman's premise about the repository has been wrong, and the fourth version of the same rule. So this one is pitched at the mechanism instead of the symptom: **before Part 1 asserts anything about the repository, run the command and paste the output into the agenda.** A pasted grep cannot be wrong about what it found.

But the more useful lesson today is not about the error. Three times this session the recoverable outcome came from putting something to a member as a question I might be wrong about rather than as a finding: the ASIL premise (wrong, caught), the B7 identification question (I would have over-read the page head; the officer refused it), and the merge plan (wrong in a way that would have truncated this close-out away). The instrument-probing rule protects against being wrong about the environment; the quote-the-line rule protects against being wrong about the record; **neither protects against being wrong about a plan, and the only thing that did was asking.** A chairman who directs rather than performs has one structural advantage over every member — he can be corrected by four independent seats — and it is only an advantage if he actually puts his own reasoning in front of them. Today it worked three times out of three. That is the practice worth keeping, not the rule.

<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->
