# Archivist session — 2026-09-04

**Audited against `c2558d9`** (`main`), on a complete **983-commit** history. The container's
clone arrived shallow at **173** commits — the **fifth consecutive** session — so every sha here
was resolved only after `git fetch --unshallow`. A guard run on the shallow clone reports
nonsense, and this is now reliable enough to be a standing precondition rather than a surprise.

**The window this session covers is `7bc5fc0..c2558d9` — 61 commits.**

**The short version.** The repository did not drift. The vault did. Across 61 commits the agent
contracts, the professor-facing surfaces and the flowchart manifest moved **zero** times, and
every guard that can pass, passed. What this session found instead is that a rule the council
adopted **and explicitly routed to me by name** had reached no note in four days — and that two of
the defects behind it are in surfaces I wrote myself.

---

## 1. The finding — a rule the council handed me had gone uncollected, and it is the 2026-08-03 failure wearing a new coat

On 2026-09-04 the council adopted **fabrication taxonomy entry 28 — unscreened first-ness
claim**. Its own optimization log closes the entry like this:

> *"routed to the archivist for the vault table because `agents/` is outside this session's merge
> scope"* — `analytics/optimization-log.md:65` (`1fcc1ab`)

A whitespace-normalised grep for `first-ness` across the whole of `agents/` returned **zero**
hits before this change set. The council could adopt the rule and could not file it. It named the
seat that had to, and the filing had not happened.

**What the rule catches is what this seat is for.** Entry 28 covers a novelty assertion — "first",
"never", "no seat has ever", "new to this corpus" — treated as what it actually is: an *absence
claim about the project's own record*, carrying the same burden as any other absence claim. It was
adopted because **seven** instances fired in one day's record: four the chairman's, **the fourth
committed inside the correction of the third** and self-filed, and three self-reported by the
analyst — the worst of those against `STATE_OF_THE_ANSWER.md:17`, on an author the *previous*
analyst had recorded learning this exact rule on, the previous day.

**None of the seven is a fabrication.** The council's own words: *"each is true about the world and
false about the record."* That is the distinguishing mark, and it is why the pattern is hard to
see — the seat is not being careless about the world, it is being careless about the project's
memory of itself.

**Which is the 2026-08-03 failure.** On 2026-08-03 the analyst asserted a case was new to the
project when it is one of the four out-of-sample holdout positives validating the fingerprint.
That is the same sentence shape, thirty-two days later, seven times over. `agents/research-analyst.md`
has carried that 2026-08-03 finding since 2026-08-13 — correct, sourced, and (per **D5**) outside
the read path of the seat bound by it.

**Filed in this change set:** [[integrity-officer]] fabrication taxonomy, entry 28 with its
mechanical carrier and its full citation chain; [[Agent Registry]] adopted-method-rules table, two
rows; [[research-analyst]], as a dated block appended to the 2026-08-03 holdout finding it repeats,
saying plainly that it repeats it. Thread **D13**, opened and closed the same day.

**The limit, named by the council rather than by me:** entry 28 is a writing discipline and does
not touch the structural cause — **452** tracked files and **36,683** non-blank daily-record lines
growing roughly **750 lines a day** against a session-length context. No writing rule fixes that.

> **FOR EMORY — that structural remainder is the one part of this nobody owns.** It needs an index
> or a retrieval gate over the daily records so a seat can *cheaply* ask "has the project already
> said this?" The council priced the alternative exactly today: the one screen that was actually
> run cost a single command and returned a **better** finding than the claim it refuted; the four
> unscreened claims cost three blocking objections, two rewritten sections, and a gap disposition
> pointing the wrong way. It is a build, and it is yours to authorise. Tracked as **D13**.

---

## 2. NEEDS YOU — I preserved a stale heading's fix this time, but the mechanism that made the entry-27 collision is still live

`agents/integrity-officer.md` is the taxonomy's canonical home, and the officer's mandate directs
it to *read that table* rather than recite from memory. The note carries its own diagnosis of how
the entry-27 collision was made:

> *"A stale count in this heading is not a cosmetic defect: it is the input to the next entry's number."*

The heading read **"27 entries as of 2026-08-07"** until today. On 2026-09-04 the officer opened
the table exactly as directed and recorded what it saw:

> *"Vault table currently stands at 27 entries with a live collision at 27 … I read the table
> rather than reciting it, per the mandate, and did not renumber."*
> — `analytics/daily-research/2026-09-04.md:968` (`51a2bae`)

It took **28**, which was free. **The staleness cost nothing this time and would have cost the
next extension.** The heading now reads 28 as of 2026-09-04, moved in the same change set as the
row it numbers — which is the maintenance rule this vault has always carried and which was not
applied between 2026-08-07 and today.

> **NEEDS YOU — the entry-27 collision itself is at day twenty-eight and only a ruling closes it.**
> Two different patterns hold the number 27 (*scope-mixed screen*, 2026-08-06; *manufactured
> residual*, 2026-08-07), both adopted on the record. **Renumbering is not mine** — it would change
> what two council rulings say. The integrity officer re-raised it unasked today in its own
> accountability section. It needs the chairman and the officer to rule which pattern keeps 27,
> or you to break the tie. Until then the convention holds: cite entry 27 by name, never by number.

---

## 3. ACTION — my own threads note has colliding IDs, and it has already produced a wrong citation in a record I wrote

`agents/Workflow Threads.md` carries **two threads numbered C11** ("CI wiring of the guards";
"Taxonomy entry 27 was adopted twice") and **two numbered C12** ("Merge-or-skip before the Monday
13:00 UTC run"; "`build_graph` is whole-vault"). Both pairs have coexisted since 2026-08-07/08-08.

It is not cosmetic. The **2026-09-01 session record** — this seat's own — lists *"C11 —
`docs/how-it-works.html` says nine and ten on one page"* at `:210`. That is a **third** referent:
no C11 thread covers the site page, which has no thread of its own at all and belongs nearest to
**D7**. Meanwhile the integrity officer, citing correctly, used C11 for the taxonomy collision on
2026-09-04. One label, three meanings, in two records four days apart.

**The irony is exact and I would rather state it than smooth it over.** The C11 taxonomy thread
exists because a stale count in a heading became the input to the next entry's number. My note then
reproduced the identical defect one level up: a stale ID space became the input to the next thread's
number. I wrote both sides.

**Not renumbered**, for the same reason the taxonomy's 27 is not: outside seats cite these ids.
**ACTION — one ruling from you assigns each pair a distinct id, and I update every citation in a
single change set.** A warning block now sits at the head of the note so nobody reads a thread by
its number in the meantime.

---

## 4. The 2026-09-01 session's own anchor fix never landed — and it is why the currency guard is still red

`origin/vault/archivist-2026-09-01-anchors`, tip **`358ece9`**, pushed 2026-09-01, **never opened
as a PR**, never merged. Its entire content is two anchor lines: `agents/Claim Map.md`
(`621aa6e` → `0b38cb5`) and `STATE_OF_THE_ANSWER.md` (`7a2d696` → `0b38cb5`).

`scripts/check_currency.py` on `main` today: **9 claims checked, 2 failed** (down from 3 on
09-01) — and one of the two is `agents/Claim Map.md`, stale by 4 commits against `621aa6e`.
**That failure exists only because this branch never landed.** The companion re-anchor PR **#126**
carried three notes and left these two behind.

> **The guard is GREEN on this branch: 9 currency claims checked, 0 failed, exit 0.** It has been
> red on `main` since at least 2026-08-16. That is the one number in this record that moved from
> broken to fixed today, and it took no new machinery — only landing an anchor that had been
> written three days ago and never merged.

**The 2026-09-01 orphan sweep could not have caught it** — the sweep ran before that branch
existed. The check is sound; the timing defeated it. **Method adopted and written into the notes:
the orphan sweep runs after the session's own pushes, not before.**

**Repaired here:** `agents/Claim Map.md` is inside my path set and its anchor is moved by
`scripts/reanchor.py`, not by hand — from `621aa6e` to `9141e75`, this session's substantive
commit. All four `agents/` anchors now name it. **`STATE_OF_THE_ANSWER.md` is not mine.** The
script moved it too, as §8 predicts it must, and **I reverted that one file** so this pull request
stays inside `analytics/`, `agents/`, `moc/`, `HANDOFF.md`. Nothing is lost by leaving it: it is
green against the guard already, because the daily council re-anchors it every day. Thread
**D12**. The stranded branch is now dead weight and you may delete it.

---

## 5. Agent-context audit — no drift, checked three ways, and one thing I can only report

The priority item in my brief, and on the machinery it comes back clean:

- `git log 7bc5fc0..HEAD -- .claude/agents/ prompts/ src/models.py` → **no commit**, across 61.
- `scripts/check_models.py` exits **0** over **12** flowchart cards.
- All nine definitions declare `model: opus`; each definition's "Runs on" prose matches its vault
  note seat by seat; `src/models.py` unchanged.
- `node tools/isds-workflow-3d/validate.mjs` exits 0 — 30 cards, 10 chips, 44 edges, SVG fresh
  against its inputs-sha256, zero "Jack" tokens.

> **TO VERIFY — D6, third first-person observation, day twenty.** I queried my own session runtime
> again: `configured_model`, `session_context.model` and `last_served_model` are all
> **`claude-opus-5`**. `.claude/agents/obsidian-archivist.md:3` and `:24` say "Runs on Claude Opus
> 4.8", and this vault pins `claude-opus-4-8`. **REQUESTED `claude-opus-4-8` → ACTUAL
> `claude-opus-5`.** Consistent with 2026-08-16 and 2026-08-25. `check_models.py` cannot see this
> and is not failing — it compares three declarations to each other, as its own docstring admits.
> **I have again left the rows unchanged**, because they record your directive and rewriting them
> would ratify a substitution nobody authorised. This is a decision for you, not a defect for me
> to fix.

**D5 unchanged at day twenty-two.** Re-tested by script: **eight of nine** definitions never name
their seat's own vault note. Today the exception proved itself — the integrity officer's definition
*does* point at its note, so the officer could open the taxonomy table to number its extension and
quote what it found. No other seat can do that. The remedy is one line per definition, in the form
`.claude/agents/integrity-officer.md:56-62` already uses. **Editing `.claude/agents/` is a contract
change and is yours.**

---

## 6. Deliverable drift — both open items unchanged, and neither is mine

> **ACTION — `docs/how-it-works.html` still contradicts itself inside one document.** Re-measured
> today: `:7` and `:58` say **"nine public sources"**; the SVG embedded in **that same page** says
> **"10 SOURCES"** at `:96` and **"10 sources"** at `:109`. The prose comes from
> `scripts/site_templates/how_it_works.html.j2:3,:27`. `src/sources.all_sources()` returns **10**
> and `README.md:56` already says "the ten sources", so the README was fixed and the template was
> not. **Owner: site-experience.** Nineteen days open. Not mine to edit.

> **FOR EMORY — `METHODOLOGY.md` §III is still wrong on two counts against the code**, and
> METHODOLOGY is your document, so I report rather than edit. Both re-read from the code today:
> 1. `METHODOLOGY.md:33` describes Bing News as polled through *"eight fixed, fingerprint-derived
>    queries"*. `len(bing_news.QUERIES)` is **12**.
> 2. That paragraph enumerates **nine** channels and mentions **GDELT zero times**, though GDELT is
>    the tenth registered source. `README.md` also mentions it zero times.
>
> It is a two-number edit, not a re-derivation. This is the paragraph a reviewer reads to judge the
> method, and it currently describes a method the instrument does not execute.

Clean on this pass: `HANDOFF.md`, `COUNCIL.md`, `views/isds-workflow-3d/workflow.json`,
`README.md`'s source count. None moved in the window.

---

## 7. Vault mechanics and the orphan sweep

**Graph:** 161 notes, 341 edges, **0 orphans**, 51 managed blocks would update, 16 WARNs. Growth
since 09-01: +5 notes, +11 edges. All 16 WARNs are index or session notes exceeding the four-link
cap — expected by the standing convention, none is drift. The `RETRIEVAL_LEDGER.md` broken link
the 2026-09-01 record named is **gone**; the three that remain are all long-standing and all under
`think-tank/`, outside my paths.

**One broken link found and fixed, and it is a new hazard worth naming.** `agents/Project Change
Log.md:125-126` — written by the **2026-09-01** session, my own seat — wrapped a wiki-link to
[[Agent Registry]] across a line break. `scripts/build_graph.py` does not match a link containing
a newline, so it counted as a link to a note literally named `Agent\n  Registry`, and the edge
from the change log to the registry was **silently missing from the graph**. Reflowed, not
rewritten; the wording is untouched. This is a sibling of the hazard already recorded in
[[Agent Registry]] — that you cannot quote link syntax inside backticks without minting a broken
link — and the practical rule is now two-sided: **in this vault a wiki-link must sit entirely on
one line, and must never appear inside a code span.** Both are properties of the scanner, not of
Obsidian, which resolves the wrapped link fine when the note is read.

**C8 is still real in the code and still not firing.** `scripts/build_graph.py:198` remains
`pat.sub(block, text)` with no count argument. Recorded as latent, per the 2026-09-01 measurement.

**Orphan sweep — 124 remote branches, 17 fail `git merge-base --is-ancestor <tip> origin/main`.**
I opened each rather than counting them, because the repository squash-merges and a squash-merged
branch tip is *never* an ancestor of `main`. **Only four carry anything real:**

| Branch | Tip | What is actually stranded |
| --- | --- | --- |
| `vault/archivist-2026-08-28` | `de73f9f` | **PR #110, day seven** — see below |
| `vault/archivist-2026-09-01-anchors` | `358ece9` | Two anchor lines — §4, half repaired here |
| `working/gap-bridge-2026-08-29` | `cada2e5` | 25 insertions to `working/gap-bridge-2026-08-29.md`; the 2026-09-04 council already recorded both gap slugs as stranded (`00def72`). Outside my paths |
| `chore/operator-marks-2026-07-27` | `6f9e1da` | **Thread F1, day thirty-nine, still yours** |

The rest are accounted for: two `council/*` branches carrying only a `.sent/` marker, six
`council-2026-08-31-*` branches superseded by merged commits, three 2026-07-30/08-01 cloud branches
(F3), `feat/research-council` from 2026-06-22, and the two 2026-09-01 archivist branches whose
content landed by squash.

> **NEEDS YOU — PR #110 is at day seven and is still the only pull request open in this repository
> besides this session's own.** Its 350-line session record is safely on `main` (the 2026-09-01
> session recovered it byte-identical, and I verified that today). What is still stranded is that
> session's edits to five vault notes. I did **not** cherry-pick them, for the reason the 09-01
> seat gave and which still holds: they are live statements about a repository that has now moved
> 137 commits, and I re-derived today's state myself instead. **The PR is still yours.**

---

## 8. D11 — I read the trap at the code, and it can now be fixed in one line

The 2026-08-28 seat predicted *"every archivist PR from now on will hit this."* I read the
machinery today rather than restating the prediction:

- `.github/workflows/reanchor.yml:60-77` checks out the PR head, runs `scripts/reanchor.py` over
  the whole of `check_currency.TRACKED`, and pushes `chore(currency): re-anchor tracked notes to
  HEAD [skip ci]` **if anything moved**.
- `TRACKED` (`scripts/check_currency.py:62-71`) is five notes: four under `agents/` and
  **`STATE_OF_THE_ANSWER.md`** — the one outside my self-merge path set.
- An archivist PR **always** moves it. `_anchor_target()` (`scripts/reanchor.py:55-70`) returns the
  newest **non-maintenance** commit from HEAD, and my substantive commit touches
  `analytics/vault-sessions/` and seat notes, neither in `TRACKED`, so it is never maintenance.
- **I cannot pre-empt it either:** running `reanchor.py` myself puts the same
  `STATE_OF_THE_ANSWER.md` edit into my own commit. Both roads reach the same violation, which is
  why "remember to re-anchor first" is not a remedy.

> **FOR EMORY — the narrowest fixes, so this is a decision and not an investigation:**
> 1. **One line, no code:** rule that an **anchor-only** change to `STATE_OF_THE_ANSWER.md` counts
>    inside my self-merge scope. The line it touches is machine-owned by construction. Or
> 2. **Small build, systems-designer:** give `scripts/reanchor.py` an `--only <path>...` filter and
>    have `reanchor.yml` pass the four `agents/` notes when the PR head matches `vault/*`. Every
>    other PR behaves identically.
>
> Either one ends a failure mode that has now stranded one session for seven days and cost two
> others a second PR.

**It fired on this pull request, and I am recording the sha because a prediction that comes true
is worth more than a prediction.** PR **#133** was opened at `aa7c572` with eight paths, all inside
my authority. Twenty-three seconds later `github-actions[bot]` pushed **`b43a137`** —
*"chore(currency): re-anchor tracked notes to HEAD [skip ci]"* — moving all five tracked notes and
taking the head to nine changed files, the ninth being `STATE_OF_THE_ANSWER.md`. **That is the
third consecutive archivist session to hit D11**, and the first to catch it in the act rather than
infer it.

**What I did about it, and why it is not a workaround.** I restored `STATE_OF_THE_ANSWER.md` to
`origin/main`'s exact content in a commit of my own, so the change set this PR lands touches
**nothing** outside `analytics/`, `agents/`, `moc/`, `HANDOFF.md`. That is not evading the path
test — it is satisfying it: landing *no change* to a file I have no authority over is precisely
what the rule asks. The bot's work on the four `agents/` anchors is kept; only its fifth file is
undone. **This is the 2026-09-01 precedent** — that session's PR **#125** also landed without
`STATE_OF_THE_ANSWER.md`, which is why it merged and 2026-08-28's did not.

**But note what the precedent cost, because it is the argument for fixing D11 properly.** The
2026-09-01 session kept its main PR clean and then needed a *second* PR (#126) for the anchors,
and a *third* branch for the two that PR left behind — which is `358ece9`, never opened, §4 above,
the reason the currency guard stayed red for three days. **The workaround works and it leaks.** One
of the two fixes in the box above ends that.

**D10 improved, and I want to be precise about why, because it is not a fix.** Three of the five
tracked notes now read `0b38cb5` — my own 2026-09-01 session merge — so for those three the anchor
genuinely does mean "a seat audited this". The cause is that since 2026-09-02 the daily council has
re-anchored **only** `STATE_OF_THE_ANSWER.md`, committing it as *"the one tracked note inside this
council's merge scope"* (`87b828d`, `070258d`, `0116caf`). That is one seat's voluntary restraint.
`scripts/reanchor.py` is unchanged since `93a69f7` and still has no scope flag. **The mechanism
that can silently stamp an unread note is still in place.**

---

## 9. What I could NOT verify

Stated rather than omitted:

- **Whether this PR will merge.** Same exposure as §8. I will run `git diff --name-only
  origin/main...HEAD` immediately before merging and refuse if any path falls outside `analytics/`,
  `agents/`, `moc/`, `HANDOFF.md`. **If you receive this by email but `main` has no
  `analytics/vault-sessions/2026-09-04.md`, that is what happened** — and it would be the third
  session in eight days lost to the same cause.
- **Whether `reanchor.yml` actually fired on the recent council PRs.** Those PRs were squash-merged,
  which erases the branch history from `main`, so I can see no bot commit but cannot prove none was
  pushed. I report the practice change (§8) from the commits that *are* on `main` and go no further.
- **Why the clone keeps arriving shallow** — fifth consecutive session. I can show it happens; I
  cannot see the container provisioning. It is harmless as long as every session unshallows first,
  and dangerous the one time a session forgets.
- **The 2026-08-31 non-firing session**, raised on 2026-09-01 — still not visible from inside the
  repository. Sessions have fired on schedule since (09-01, 09-04), so it now looks like a
  one-off rather than a pattern, but I am inferring that from two data points.
- **No external Obsidian/PKM research this session.** My self-training mandate asks for it
  periodically. The time went to the routed rule and the two defects in my own surfaces, and I
  would rather say so than dress an internal finding as outside reading. Last done: not within the
  sessions this log covers.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D11** — every archivist PR acquires a `STATE_OF_THE_ANSWER.md` commit and becomes unmergeable by this seat. Two one-line fixes offered in §8 | **Emory** / systems-designer | 7 days |
| 2 | **PR #110** — 2026-08-28 session's five note edits still stranded (its record is safely on `main`) | **Emory** | 7 days |
| 3 | **D13 structural remainder** — no index or retrieval gate over 452 files / 36,683 lines growing ~750/day; entry 28 is a discipline, not a fix | **Emory** | new today |
| 4 | **C11 (taxonomy)** — entry 27 adopted twice; only a ruling closes it | council-chairman + integrity-officer | 28 days |
| 5 | **Thread-ID collision** — two C11s and two C12s in `Workflow Threads`; already miscited once | **Emory** to authorise; archivist to execute | new today |
| 6 | **D6** — seat models: requested 4.8, served Opus 5; third first-person observation | **Emory** | 19 days |
| 7 | **D5** — eight of nine definitions never name their own vault note | **Emory** (contract change) | 22 days |
| 8 | **C11 (site)** — `docs/how-it-works.html` says nine and ten on one page | site-experience | 19 days |
| 9 | **D7** — `METHODOLOGY.md` §III: eight queries vs 12; GDELT unmentioned | **Emory** | carried |
| 10 | **F1** — operator-marks branch never merged (`6f9e1da`) | **Emory** | 39 days |
| 11 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |
