# Seeds are private source material — never commit
seeds/

# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Env / secrets
.env
.env.local

# OS
.DS_Store

# Local Claude settings
.claude/*
!.claude/agents/

# Humanization workflow scratch (keep term-preservation.md tracked)
working/*.txt

# Obsidian vault scratch (operator-local; never tracked)
.obsidian/
*.canvas
*.base
Untitled*

# 3D workflow view toolchain — deps are installed locally, never committed
tools/isds-workflow-3d/node_modules/
