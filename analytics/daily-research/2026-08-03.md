# Daily Council Meeting — 2026-08-03

**Research question:** When and how is intellectual property — especially trade secrets and pharmaceutical clinical trial data — asserted as a covered investment under an investment treaty, challenged through a governmental regulatory or judicial measure, and disposed of at the jurisdictional or admissibility stage?

**Procedural caveats (chairman).** (1) The scheduled routine did not fire on 2026-08-01 or 2026-08-02: no records exist for those dates, the gap is recorded here plainly, and nothing is backfilled — the A/81/17 daily check therefore resumes today after a two-day lapse, three days into the publication window. (2) This session runs delegated in the scheduled cloud environment: the chairman (Claude Fable 5) directs, and every member section below is what that member's real subagent returned, relayed verbatim; no member's voice is performed. (3) Model caveat: the chairman and research analyst run on Claude Fable 5 per the operator's directive; the other seats are spawned from their committed definitions under the harness's `opus` alias, which the operator's directive intends as Claude Opus 4.8 — the exact version the alias resolves to in this cloud harness is not independently verifiable in-session, and per the never-substitute-silently rule that uncertainty is recorded here rather than papered over. (4) Today is Monday: the weekly GitHub Actions run (and the first live execution of the source-health guard, per the 2026-07-31 Part 5 note) is expected today. At session open the newest archived digest on main remains 2026-07-27; the analytics officer will check mid-session whether the 2026-08-03 run has landed.

---

## Part 1 — Chairman's Agenda

**Chairman presiding.** Day 24 post-close of the UNCITRAL 59th session; day 3 of the A/81/17 publication window (Aug–Sep 2026). Living memory present and canonical (STATE_OF_THE_ANSWER.md last updated 2026-07-31; research_log.json seq 36; the 2026-07-31 record complete through Part 6, its next steps seeding this agenda).

**Session protocol.** Members are real subagents spawned from the committed definitions in `.claude/agents/`; each member's return path is its final report to the chairman as launcher, entered into this record verbatim. A SPEND CHECKPOINT sits immediately after the vetting round: the record through Part 3 is committed before any further work, so a mid-session termination never loses the close-out. The record is written incrementally, one commit per part.

**Priority focus (analyst budgets bounded; delegation applies the latest-dated-refinement rule adopted 2026-07-31):**

1. **A/81/17 check — the day's first question.** Window open since August 1; the last check (07-31) was null. Budget: one search query; if the report is indexed or published, fetch it (browser user-agent where the CDN gates; the ODS symbol route is canonical). On publication it is the immediate lead: adopted SP numbering; the TPF SP10/SP11 inconsistency; whether DP 19's ex officio dismissal power survived. If still unindexed: null recorded plainly, no search-synthesis specifics carried.
2. **UA-controlled 403 re-audit — `uncitral.un.org` only** (2026-07-31 next step 2). Enumerate the record's standing "403-blocked" characterizations on that host; run the integrity officer's Observation 4 protocol — positive control first (a resource known retrievable, e.g. `2026-06/sps.pdf`), then one browser-UA re-test per item; reclassify each as retrievable / origin-absent / genuinely gated. Budget: 1 positive control + up to 6 re-tests; if an item proves retrievable, read the single highest-value one in full. Language discipline: these are "403-returning paths," never "blocked paths" — a 403 from this host carries no existence information.
3. **China–Switzerland BIT (2009) forum-relationship** (fork-in-the-road vs. no-U-turn; gap marker `china-switzerland-forum-relationship`). One bounded attempt, fetch-first: the UNCTAD IIA Mapping page for the treaty, then at most one search query. If inaccessible, it becomes a candidate for the July 23 operator IIA-mapping protocol — not an escalation.

Total analyst budget: ≤ 10 fetches/queries across the three tasks.

**Verify / be skeptical.** Any China–France BIT statement carries both ledger flags — operator-verified as to quotation (`quote_ok: true`); scope not certified (`scope_ok: false`) — plus the B2 caveat (the no-prerequisite and FET elements live only in the operator's chat-supplied table, not the preserved `source_snapshot`). Any Hela Schwarz safe-harbor restatement uses the July 11 tightening (STATE line 66: formal withdrawal from Chinese domestic proceedings *before they conclude*, not merely none pending at ICSID filing; "curable" stays hedged). No search-synthesis figures; absence is "not found in accessible sources," never "does not exist."

**Standing zero-budget (unchanged):** all pending operator approvals — the source_analytics.py patch, the health column, the polite_get diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the build_graph fix, any ledger amendment — are PENDING, not granted; nothing is built or applied. Escalated gaps zero-budget. Wingtech (monthly cycle, late August), IISD ITN / EJIL:Talk polling, Roberts (subsumed), first-application tracker (monthly, late August): no work today.

**Systems researcher — not seated today.** Its entire carried queue is gated on pending operator sign-off (the source_analytics.py patch application and the health-column proposal); with no ungated work, the seat does not convene. Recorded here, not as a procedural failure — the queue rule is "seated only if its queue has work."

**Live threads:** (1) A/81/17 (window open — the immediate lead on publication). (2) The uncitral.un.org 403 re-audit (retrospective record hygiene with possible new primary reads). (3) China–Switzerland forum-relationship (completes the three-way treaty-selection comparison opened 2026-07-31).

---

## Part 2 — Research Analyst's Findings (research-analyst subagent, Claude Fable 5)

*The analyst's return, delivered as the subagent's final report to the chairman as launcher,
is reproduced verbatim below — top-level memo heading demoted for record nesting, text
otherwise unedited. Vetting disposition in Part 3; any amendments are recorded there and in
the close-out, never silently applied here.*

### Research Analyst Memo — 2026-08-03 session

**Budget spent: 7 of ≤10 total actions.** (1) WebSearch A/81/17; (2) curl undocs.org/A/81/17 [browser UA]; (3) curl positive control `2026-06/sps.pdf` [browser UA]; (4) WebFetch same positive control; (5) WebFetch UNCTAD China–Switzerland BIT treaty page; (6) curl same page [browser UA]; (7) WebSearch China–Switzerland forum-relationship. Zero-budget items honored in full: escalated gaps, Wingtech/Nexperia, IISD ITN/EJIL:Talk polling, Roberts check (subsumed), first-application tracker, all pending operator approvals. No repository file edited. Browser UA used wherever curl was employed: `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36`.

**Session-defining instrument fact, stated up front because every task ran into it:** this session's environment cannot reach the audit hosts at all. The agent proxy gateway rejected CONNECT to `undocs.org:443`, `uncitral.un.org:443`, and `investmentpolicy.unctad.org:443` (`connect_rejected — "gateway answered 403 to CONNECT (policy denial or upstream failure)"`, confirmed at the proxy's own status endpoint, `$HTTPS_PROXY/__agentproxy/status`, read directly). These 403s are emitted by the sandbox proxy **before any origin contact** — they carry no information about any resource, and are a different artifact class from the CDN UA-gating in Observation 4. WebFetch (which routes outside the sandbox proxy) reached uncitral.un.org but was CDN-gated as a non-browser client. Consequence: the 2026-07-31 officer protocol is executable only from an environment with direct egress (it worked from the operator-machine session of 2026-07-31; it cannot run from today's). Confidence: HIGH for each observed denial (proxy status endpoint + this session's recorded curl/WebFetch outputs).

#### Task 1 — A/81/17: null (day 3 of window); symbol route untestable from this environment

One query (`A/81/17 UNCITRAL report fifty-ninth session Supplement No. 17`). **Not indexed as of 2026-08-03.** Results returned only prior-session reports and general pages: [A/80/17 (58th session)](https://uncitral.un.org/sites/uncitral.un.org/files/2511964e_uncitral_report_advance_0.pdf), [A/79/17 (57th session) advance copy](https://uncitral.un.org/sites/uncitral.un.org/files/a_79_17_commission_session_report_2024_website_version_-_advance_copy.pdf), [A/68/17](https://unctad.org/meetings/en/sessionaldocuments/a68d17_en.pdf), and the [UNCITRAL Commission sessions page](https://uncitral.un.org/en/commission). Per the B4 discipline, no search-synthesis specifics about the 59th session are carried from the result text. What I actually read: the result listing only; no body fetched.

Because the undocs.org symbol route cannot be tested by search (it is a resolver, not an indexed page), I attempted it directly under the fetch-first rule: **curl with browser UA → proxy CONNECT rejected.** This is an environment-layer denial, not an origin answer — it says nothing about whether A/81/17 exists at `undocs.org/A/81/17`. The one incremental method fact worth recording: the two predecessor reports' URL patterns above suggest UNCITRAL publishes the advance copy on its own site under a non-symbol filename (`2511964e_...`, `a_79_17_..._advance_copy.pdf`) — meaning when A/81/17 lands, the accessible artifact will likely be an oddly-named advance-copy PDF on uncitral.un.org, a host this environment cannot reach with curl and WebFetch cannot pass the CDN gate on. **Proposal for the chairman (not an escalation by me): the A/81/17 daily check remains a search-track task, but the *retrieval* on publication should be pre-routed to an environment with direct egress or to the operator, or it will bounce off both gates on day one.** The a)-c) reading targets (adopted SP numbering; TPF SP10/SP11 inconsistency; DP 19 ex officio survival) stand unchanged and untouched — no body was read, so nothing is stated about any of them. Null recorded plainly. Confidence: HIGH for the null-in-index (my own result set); no existence claim either way.

#### Task 2 — UA-controlled 403 re-audit: enumeration complete; audit NOT executable this session; zero re-tests spent, by protocol

**Enumeration (the record's standing "403-blocked" characterizations on uncitral.un.org, from STATE_OF_THE_ANSWER.md and analytics/daily-research/, excluding the two already-resolved sps.pdf paths):**

| # | Item | URL path | Recorded at | Value |
|---|------|----------|-------------|-------|
| 1 | A/CN.9/1246 advance copy (draft Protocol text) | `sites/default/files/2026-04/a_cn9_1246_advance_copy.pdf` | 06-30, 07-01, 07-07, 07-16, 07-18; STATE l.97 | Highest — underlies multiple MEDIUM snippet findings (temporal scope; Mauritius mechanism) |
| 2 | Guide to the DPs (Secretariat negotiating-history guide) | `sites/default/files/media-documents/uncitral/en/guide_to_the_dps.pdf` | 07-10; STATE l.103 | High — bears on DP-level history incl. DP 19 |
| 3 | A/CN.9/1270 (ICSID Secretariat comments on 1246) | `sites/default/files/2026-06/2609264e.pdf` | 07-19, 07-25 | Medium-high |
| 4 | WP.253 (draft provisions initial draft; DP renumbering) | `sites/uncitral.un.org/files/media-documents/uncitral/en/wp.253_draft_provisions_initial_draft_for_website.pdf` | 06-25; STATE l.97 sources | Medium-high |
| 5 | A/CN.9/1265 (Advisory Centre, ver. 9 Jan) | `sites/uncitral.un.org/files/media-documents/uncitral/en/acn.9.1265_ver._9_jan_for_webpage.pdf` | 07-03 | Medium |
| 6 | 59th-session agenda, calendar format (16 April version) | `sites/default/files/2026-04/agenda_for_59th_session_of_the_commission-calendar_format_16_april_website_version.pdf` | 07-03 | Low (superseded by session close) |

(The Day 11 "updated agenda PDFs (July 1 / July 3)" carry no recorded direct URLs — only the media page `uncitral.un.org/en/media/13010` — and so are not re-testable as enumerated items. The Indico 403 and UN ODS 403 are other hosts, out of today's scope.)

**Protocol outcome.** Positive control first, exactly as directed: curl + browser UA against the known-retrievable `2026-06/sps.pdf` (officer-verified 200 / 2,104,033 bytes on 2026-07-31) → **HTTP 000, `CONNECT tunnel failed, response 403`** — a proxy-gateway rejection logged at the proxy status endpoint. Alternate instrument on the same target: WebFetch → **HTTP 403** from the CDN (non-browser client class). The positive control therefore failed on both available instruments, for instrument reasons. Under the officer's own discipline — the whole point of the positive control — I ran **zero** of the six re-tests: every result would have been a proxy artifact indistinguishable from any origin state, and recording six fake 403s against the enumerated items would be exactly the "tool-status-as-source-state" failure the taxonomy names. **Classification of all six items: UNTESTABLE THIS SESSION — no origin information obtained; all standing "403-blocked" characterizations remain exactly as recorded, now with the July 31 caveat that they carry no existence information.** The re-audit is not dead: it is environment-bound. It was proven executable from the operator-machine environment on 2026-07-31 (three controlled results: 200 / 404 / 200). Proposal for the chairman: re-queue Task 2 for the next session running in a direct-egress environment, or fold the six URLs into the operator's next manual pass — items 1 and 2 alone would collapse several MEDIUM snippet-based findings into read-or-refuted. Budget saved: 6 actions, of which 3 were re-spent on Task 3 and Task 1's symbol-route attempt.

#### Task 3 — China–Switzerland BIT (2009) forum relationship: fetch track blocked; one query yields an unverifiable but structurally suggestive lead; gap persists

Fetch-first, as directed: WebFetch of the [UNCTAD IIA Navigator treaty page](https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/978/china---switzerland-bit-2009-) → **403** (origin/CDN); curl + browser UA → **proxy CONNECT rejected** (environment layer). The mapped-content view was never reached; nothing on that page is stated.

The one permitted query returned a result set ([ICLG Switzerland investor-state report](https://iclg.com/practice-areas/investor-state-arbitration-laws-and-regulations/switzerland); [Springer chapter, "The Interplay Between Investor-State Arbitration and Domestic Courts in the Existing IIA Framework"](https://link.springer.com/chapter/10.1007/978-3-030-44164-7_3); [Prager Dreifuss note on a Swiss Federal Tribunal judgment](https://www.prager-dreifuss.com/en/news/jurisdictional-hurdles-in-investment-disputes-in-china-%E2%80%93-a-recent-judgment-of-the-swiss-federal-tribunal-1124); [NILR, "China's Stance on ISDS"](https://link.springer.com/article/10.1007/s40802-020-00182-3)) and a **search-engine synthesis** presenting the following as the Switzerland–China BIT's provision text: *"[a] dispute that has been submitted, in accordance with paragraph (2), to a competent court of the Contracting Party concerned, may only be submitted to international arbitration after withdrawal by the investor of the case from the domestic court."*

Handling, per the fetch-first correction and B4: **this quotation is search-engine synthesis with no established attribution to any body I read — it enters as a LEAD only, confidence LOW-MEDIUM, and is not a finding.** Two observations about the lead, both conditional on the text being genuine: (a) under the record's July 8 definitions, a clause permitting arbitration *after withdrawal* from the domestic court is the **withdrawal-cure (no-U-turn) structure**, not a pure fork-in-the-road — yet the same synthesis labels it "fork-in-the-road," reproducing precisely the label-vs-structure ambiguity the record already carries for the China–Germany clause (UNCTAD mapping "no U-turn" vs. IAReporter "fork-in-the-road" — same provenance class, shared weakness, disclosed per the standing rule); (b) if the operator's mapping query were to confirm a withdrawal-cure relationship, the three-way comparison would read: China–Germany (mandatory 9(a) referral + 9(b) three months + 9(c) withdrawal-cure, where the operative safe harbor is the **July 11 tightening — formal withdrawal from Chinese domestic proceedings before they conclude**, cure pathway *appearing available on the provision text only*, tribunal reasoning unconfirmed, Award PDF unread), China–France (no mapped prerequisite but a mapped fork-in-the-road — operator-verified as to quotation, `quote_ok: true`; scope NOT certified, `scope_ok: false`; no-prerequisite and FET elements existing only in the operator's chat-supplied table, outside the preserved `source_snapshot`, so the record cannot self-verify them), and China–Switzerland (permissive "may require" exhaustion + a possibly curable forum election). **I do not assert that comparison — it is stated here only to show what the operator's one mapping query would complete.** The bounded attempt is exhausted; the item is a clean candidate for the July 23 operator IIA-mapping protocol. I am not escalating it; the chairman decides. The cross-BIT consistency rule is not triggered: no provision finding was established today.

#### Candidate claims (the operator's ledger decides)

```json
[
  {
    "claim_text": "As of 2026-08-03, a web search for 'A/81/17 UNCITRAL report fifty-ninth session Supplement No. 17' returns no indexed copy of A/81/17; the result set contains only prior-session reports (A/80/17, A/79/17 advance copy, A/68/17) and general UNCITRAL pages.",
    "source_locator": "WebSearch result set, 2026-08-03 session (URLs: uncitral.un.org/sites/uncitral.un.org/files/2511964e_uncitral_report_advance_0.pdf; uncitral.un.org/sites/uncitral.un.org/files/a_79_17_commission_session_report_2024_website_version_-_advance_copy.pdf)",
    "source_locator_type": "search-result-listing",
    "source_authority": "unknown",
    "access_status": "partial",
    "note": "Null-in-index claim only; carries no claim about existence or non-existence of A/81/17. Day 3 of the Aug-Sep publication window."
  },
  {
    "claim_text": "On 2026-08-03 the session environment's agent proxy gateway rejected CONNECT to undocs.org:443, uncitral.un.org:443, and investmentpolicy.unctad.org:443 ('gateway answered 403 to CONNECT'), so no curl request from this session could contact those origins; independently, WebFetch received HTTP 403 from uncitral.un.org on the known-retrievable positive control sites/default/files/2026-06/sps.pdf (officer-verified 200, 2,104,033 bytes, 2026-07-31 under a browser UA).",
    "source_locator": "Agent proxy status endpoint ($HTTPS_PROXY/__agentproxy/status), recentRelayFailures entries of 2026-08-03T11:17Z; session curl/WebFetch outputs",
    "source_locator_type": "session-instrument-log",
    "source_authority": "primary",
    "access_status": "full",
    "note": "Instrument/procedural claim. Consequence: the 2026-07-31 Observation 4 re-audit protocol is executable only from a direct-egress environment; all six enumerated uncitral.un.org items remain UNTESTABLE from this environment and their recorded '403-blocked' labels carry no existence information."
  },
  {
    "claim_text": "LEAD ONLY (not a finding): a search-engine synthesis of 2026-08-03 attributes to the China-Switzerland BIT (2009) the provision text '[a] dispute that has been submitted, in accordance with paragraph (2), to a competent court of the Contracting Party concerned, may only be submitted to international arbitration after withdrawal by the investor of the case from the domestic court' — which, if genuine, is a withdrawal-cure (no-U-turn) structure rather than a pure fork-in-the-road.",
    "source_locator": "Search-engine synthesis over result set including link.springer.com/chapter/10.1007/978-3-030-44164-7_3 and iclg.com/practice-areas/investor-state-arbitration-laws-and-regulations/switzerland; attribution to a specific body NOT established; no body read",
    "source_locator_type": "search-synthesis",
    "source_authority": "unknown",
    "access_status": "unavailable",
    "note": "Proposed strictly as a lead for the operator IIA-mapping protocol query on treaty 978; must not be asserted as the treaty's text or mapping. The synthesis's own 'fork-in-the-road' label conflicts with the withdrawal-cure structure of the text it quotes."
  }
]
```

#### Gap-marker proposals (chairman writes the record)

- `GAP-UNRESOLVED: china-switzerland-forum-relationship` — fork-in-the-road vs. no-U-turn mapping for the China–Switzerland BIT (2009); this session's bounded attempt failed (treaty page 403/proxy-blocked at both instruments; one query yielded only an unattributable synthesis lead). Candidate for the operator IIA-mapping protocol — proposed, not escalated by me.
- `china-france-bit-2007-protocol-text` — not worked today (zero budget assigned); no fresh failure to count. Standing open; listed in the docket below, no marker emitted.
- New proposal for the chairman's consideration (task-class, not a provision gap, so no slug emitted): the six-item uncitral.un.org re-audit and the A/81/17 retrieval-on-publication both require a direct-egress environment or an operator pass; routing decision is the chairman's.

#### Standing docket status

- **A/81/17:** not indexed (day 3 of window); daily one-query check continues; retrieval-on-publication needs egress routing (above). Reading targets unchanged: adopted SP numbering; TPF SP10/SP11 inconsistency; DP 19 ex officio survival.
- **uncitral.un.org 403 re-audit:** enumerated (6 items); execution environment-blocked; zero origin information obtained; re-queue for direct-egress session or operator pass.
- **China–Switzerland forum relationship:** unresolved; one unverified lead on file; operator IIA-mapping candidate.
- **China–France BIT (2007):** closed 2026-07-31 on ledger `da33a30be92ab234`; restated today only with both flags (quote_ok true / scope_ok false) and the B2 snapshot caveat; Protocol text still unread (ordinary).
- **Huawei ARB/22/2; Einarsson UNCT/20/6:** not tasked, not searched.
- **Wingtech/Nexperia, first-application tracker (both late August), IISD ITN / EJIL:Talk polling, Roberts check, all escalated gaps and operator approvals:** zero budget, honored.
- **New insight assessment:** one genuinely new, sourced instrument insight (session-environment egress denial as a distinct artifact class from CDN UA-gating, with its positive-control detection method); no new substantive legal finding — the theme's substance today is honestly null.

#### Session method note (self-training mandate)

The positive-control-first rule paid for itself in the inverse direction from July 31: there it exposed a false objection, here it exposed a dead instrument before I spent six actions recording proxy artifacts as origin states. Extension adopted: **before any HTTP-status audit, verify the session's egress path to the target host (one positive control), and on any `HTTP:000` / CONNECT failure, read the proxy status endpoint — a free, local diagnostic that cleanly separates proxy-policy denial from origin answers.** Three artifact layers are now distinguishable in this project's instruments, each needing its own language: proxy CONNECT rejection (says nothing about the origin), CDN UA-gating 403 (says nothing about resource existence), and origin answers (200/404 — the only layer that is evidence). Query-formulation note: the exact-symbol query for A/81/17 is performing correctly (it found both predecessor reports' real URLs, incidentally revealing the advance-copy filename pattern that will matter on publication day); the Task 3 quoted-element query (`"relationship between forums" OR "fork in the road" OR "U-turn"` + treaty name) produced a provision-text-shaped snippet where the July sessions' generic keyword queries produced nothing — consistent with the 2026-07-26 lesson that specific-string queries beat keyword queries, while re-confirming that what such queries return without a fetchable body is a lead, never a finding.

---

## Part 3 — Security / Integrity Officer's Vetting Note (integrity-officer subagent, Claude Opus 4.8)

*The officer's return, delivered as the subagent's final report to the chairman as launcher,
reproduced verbatim — its own Part-3 heading absorbed into this one, text otherwise unedited.
The chairman's rulings on the objections are in Part 6.*

Independent work performed: direct read of the agent proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`, `http://127.0.0.1:46377`); my own controlled curl probes of all three audit hosts **plus two neutral control hosts**; six independent WebFetch probes **including three neutral controls**; independent reproduction of the Task 1 search; full record checks of the six-item enumeration table against `/home/user/isds-watcher/STATE_OF_THE_ANSWER.md` and `/home/user/isds-watcher/analytics/daily-research/`; verification of every internal-authority citation the memo makes. No repository file edited; worktree clean.

### Verified clean

- **The proxy-layer finding is real and independently reproduced.** I read the same status endpoint: the three `connect_rejected` entries (`undocs.org:443` 11:17:22Z, `uncitral.un.org:443` 11:17:42Z, `investmentpolicy.unctad.org:443` 11:18:49Z, detail string `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`) are recorded exactly as the memo states. My own probes reproduced `HTTP/1.1 403 Forbidden` to `CONNECT` for all three, logged at 11:23:38–39Z. Confidence HIGH.
- **Spending zero of the six re-tests was the correct call and I endorse it without qualification.** Recording six proxy artifacts against the enumerated items would have been textbook tool-status-as-source-state.
- **Task 1 null independently reproduced.** Same query string, no A/81/17 in my result set. All four cited URLs appear in mine (`2511964e_uncitral_report_advance_0.pdf`; `a_79_17_..._advance_copy.pdf`; the unctad.org A/68/17 PDF; `uncitral.un.org/en/commission`). **B4 discipline verified**: my own synthesis contained a garbled assertion ("aim to finalize the Legal Remedy in International Advisory Centre Statute at its fifty-ninth session"); the memo carried no synthesis specifics. Exactly right.
- **Enumeration items 1 and 2 are accurate** as to every recorded-at date and STATE line (item 1: 06-30, 07-01, 07-07, 07-16, 07-18 + `STATE:97`; item 2: 07-10 ×2 + `STATE:103`). Exclusion of the two `sps.pdf` paths is proper (200-read; 404-closed under the 07-31 B4 ruling).
- **Latest-dated-refinement rule honored on all three watched restatements.** *Hela Schwarz*: the July 11 tightening ("formal withdrawal from Chinese domestic proceedings before they conclude") with the B3 hedge ("cure pathway appearing available on the provision text only, tribunal reasoning unconfirmed, Award PDF unread") — verbatim compliant. China–France: both flags plus the B2 snapshot caveat — B1 and B2 rulings honored in full. China–Germany: H4's shared-provenance disclosure carried unprompted.
- **The Task 3 LEAD labeling is honest and consistently applied** across body, candidate claim (`search-synthesis` / `unknown` / `unavailable`), gap-marker proposal, and docket. The comparison is expressly not asserted.
- **"Permissive 'may require' exhaustion" for China–Switzerland is sourced** — `STATE_OF_THE_ANSWER.md:130`, MEDIUM-HIGH. Not invented for the comparison.
- **Zero-budget compliance confirmed**: `git status --porcelain` empty; the only commits touching the tree are the chairman's two record commits. Headline budget 7 of ≤10 matches the enumerated action list.
- No fabricated case, docket number, party, date, or URL anywhere in the memo.

### BINDING OBJECTIONS

**B1 — TOOL-STATUS-AS-SOURCE-STATE, in the memo that names it. The WebFetch 403 is not evidence of CDN UA-gating, and the second instrument was never shown alive.** The memo states: "WebFetch (which routes outside the sandbox proxy) **reached uncitral.un.org** but was **CDN-gated as a non-browser client**"; Task 2: "WebFetch → HTTP 403 **from the CDN** (non-browser client class)"; Task 3: "WebFetch ... → **403 (origin/CDN)**". Control: **WebFetch returned HTTP 403 for 6 of 6 URLs I attempted this session, including `https://example.com/`, `https://www.wikipedia.org/`, and `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/22/2`** — hosts with no UA-gate, one of them read successfully by this project on 2026-07-31. The 403 is an instrument-layer artifact. Nothing establishes that any request reached any origin, and the attribution to Observation 4's UA-gating mechanism is contradicted by the neutral controls. Required: strike "reached uncitral.un.org," "CDN-gated as a non-browser client," "from the CDN," "(origin/CDN)," and the unverifiable "which routes outside the sandbox proxy"; restate as *WebFetch returned 403 for every URL attempted this session including neutral controls — instrument-layer, no origin information*. This governs the session-defining paragraph, Task 2's protocol outcome, Task 3's fetch line, candidate claim 2, and the method note. The Task 2 conclusion (UNTESTABLE; zero information obtained) survives intact and is strengthened.

**B2 — ENUMERATION MIS-SCOPED. The table is presented as "the record's standing '403-blocked' characterizations on uncitral.un.org"; three of six items never carried one, and three that do are missing.** Over-inclusive: **item 4 (WP.253)** — `2026-06-25.md:85` is a plain source cite, and `STATE:97` lists it in a sources block with no 403 tag; **item 5 (A/CN.9/1265)** — `2026-07-03.md:164` cites it, and the record says the content was "recovered from search engine summary ... **not from directly-read document text**," which is unread, not 403; **item 6 (agenda PDF)** — `2026-07-03.md:172`, plain source cite. Under-inclusive: **item 3 omits its earliest 403 record**, `2026-07-05.md:84–86` ("[PDF 403-blocked; document title and content confirmed from search engine summary...]"); and two uncitral.un.org URLs carrying standing 403 characterizations are absent — **`uncitral.un.org/en/media/13010`** (`2026-07-09.md:85` "[PDF 403-blocked; existence confirmed by UNCITRAL website listing]"; `STATE:102`) and **`uncitral.un.org/en/commission`** (`2026-07-11.md:39`, "the UNCITRAL commission page were both 403-blocked"), the latter appearing in the analyst's own Task 1 result set today. The memo's stated ground for excluding the Day 11 items — "no recorded direct URLs" — is inaccurate: the 403 tag attaches to `/en/media/13010`, which is a testable URL. (A third, genuinely ambiguous item: `2026-07-10.md:42` attributes a 403 to a Rabat "workshop PDF" whose URL is not recorded.) Required: the table must not be adopted under its present caption, and the conclusion "all standing '403-blocked' characterizations remain exactly as recorded" must not attach a 403 provenance to items 4–6 that the record never gave them. Re-scope or correct — I do not rewrite.

**B3 — MIS-DATED INTERNAL AUTHORITY, twice. The memo cites the project's own record by date for propositions those dates do not contain.**
(a) "under **the record's July 8 definitions**, a clause permitting arbitration after withdrawal ... is the withdrawal-cure (no-U-turn) structure." `2026-07-08.md:64` says the opposite — it lists three *distinct* categories: "a pure FITR (irrevocable), a **no-U-turn (once domestic claim filed, no return)**, or a **withdrawal-permitting clause with a cure pathway**." The equation the memo's whole lead-analysis turns on exists only at `STATE_OF_THE_ANSWER.md:84` (July 11): "This is the no-U-turn provision. Cure pathway: withdrawal under Chinese civil procedure." Required: cite `STATE:84` (July 11), not July 8 — and see Observation 5, because the record is internally inconsistent here and the memo silently resolves it.
(b) "consistent with **the 2026-07-26 lesson** that specific-string queries beat keyword queries." No such lesson is in `2026-07-26.md` (thirteen searches, no query-formulation finding). The record's dated technique is **2026-07-01** ("targeted quoted-phrase search"), and it was **WEAKENED on 2026-07-30** (`2026-07-30.md:157`) and re-sequenced to fallback-after-attempted-fetch. Required: correct the date and restate in its post-07-30 form.
Both are memory-file reconstruction turned on the project's own log — the pattern this seat exists to catch.

### REQUIRED HEDGES

**H1 — "This session's environment cannot reach the audit hosts at all" implies a host-targeted policy; it is a blanket denial.** My controls: `example.com:443` and `www.un.org:443` are `connect_rejected` identically (logged 11:23:38.358Z, 11:23:39.685Z). There is **no curl egress to any external host** in this environment. Hedge accordingly — and note for the chairman's routing decision that the remedy is not a three-host allowlist.

**H2 — The method note's "three artifact layers" is four.** Insert *fetch-service blanket 403* between proxy CONNECT rejection and CDN UA-gating. The CDN layer was **not observed at all this session** (B1); it remains a 2026-07-31 finding only.

**H3 — Candidate claim 2 field fidelity.** `source_authority: primary` / `access_status: full` are earned for the proxy half (I read the same endpoint), not for the WebFetch sentence, which must be corrected per B1 before this claim reaches the ledger.

**H4 — Candidate claim 1 composition.** `source_locator` names two URLs; `claim_text` names three prior-session reports (the A/68/17 unctad.org URL is dropped). My independent reproduction also returned items outside the stated composition ("only prior-session reports ... and general UNCITRAL pages") — a cisg-online.org commentary PDF, an arbitrationindia.com copy of A/CN.9/876, and the un.org Sixth Committee page. Hedge the composition to *the analyst's 2026-08-03 result set*; result sets are session-specific.

**H5 — "the accessible artifact will likely be an oddly-named advance-copy PDF on uncitral.un.org."** A two-instance pattern inference. Keep "likely," and record that the ODS/undocs symbol route remains untested, not excluded.

**H6 — "one genuinely new, sourced instrument insight."** Narrow to the proxy-layer half. The second-instrument half is withdrawn under B1.

### Officer's observations to the record

1. **Budget arithmetic.** The headline (7 of ≤10) is correct and matches the action list. The internal split — "Budget saved: 6 actions, of which 3 were re-spent" — is off by one in each term: Task 2 spent 2 (curl + WebFetch on the control), so 5 were saved, and the re-spends outside Task 2 were 2 (Task 1's undocs curl; Task 3's second-instrument curl). Both formulations reconcile to 3 unspent. Non-material; recorded for precision.
2. **The proxy status read is correctly uncounted** — 127.0.0.1, inside `no_proxy`, zero external cost. Endorsed as standing method, and it is now mine as well.
3. **The layers split cleanly by tool: WebSearch works this session; every fetch instrument does not.** Any routing decision should say so rather than "no egress."
4. **The failure is not theme-specific.** My `icsid.worldbank.org` control also 403s, so a Huawei docket re-check would have died the same way today. Relevant to what the chairman re-queues.
5. **For the chairman — a real record defect surfaced by B3(a).** The project record carries two incompatible taxonomies of the forum-relationship clause: three categories at `2026-07-08.md:64` (no-U-turn ≠ withdrawal-cure) versus two at `STATE:84` (no-U-turn = cure pathway). The China–Switzerland lead's entire "structurally suggestive" force depends on which governs. Worth a reconciliation line or a marker; it is not the analyst's error to fix, but it should not be inherited silently.

### Taxonomy extension (self-training mandate)

Running taxonomy — *unsourced precision, inverted dispositions, snippet-as-fact, title-as-holding, memory-file reconstruction, image-embedded primary text, tool-status-as-source-state, summarizer-render-as-full-access, selective-flag reporting, superseded-formulation restatement* — extended with three:

- **Control-inside-the-suspect-set** (new). A positive control drawn from the same host or instrument class as the item under test, so it cannot separate the suspected artifact from a general instrument failure. Today's control (`sps.pdf` on `uncitral.un.org`) was correctly chosen to test *retrievability* but could not test *attribution*, and the memo drew an attribution conclusion from it. Countermeasure: **every positive control now pairs a suspect-host probe with a neutral-host probe on the same instrument** (`example.com`, `wikipedia.org`).
- **Second-instrument corroboration fallacy** (new, tool-status family). Treating a second tool's identical failure as independent corroboration of a mechanism, when the second tool was never shown functional. Countermeasure: prove the instrument alive before reading its failures as evidence.
- **Mis-dated internal-authority citation** (extension of memory-file reconstruction). Citing the project's own record by date for a proposition that date does not contain — B3(a) and B3(b). Countermeasure: grep the cited date before citing it; the latest-dated-refinement rule requires knowing which date actually holds the proposition.

### DISPOSITION: **FLAGGED**

Three binding objections, six required hedges. The memo's **judgment** was right everywhere it mattered: refusing the six re-tests, recording the null plainly, labeling the China–Switzerland text a LEAD and holding it there, and carrying the China–France and *Hela Schwarz* rulings without slippage. The failures concentrate in one place — **it read its own instruments' failures as facts about the world** (B1), and then cited the project's record from memory rather than from the record (B3). Nothing here impeaches the day's substantive result, which is honestly null. The proxy finding survives, verified twice over; the CDN half of it does not.

---

## Part 4 — Analytics Officer: Source Yield and Digest Relation (analytics-officer subagent, Claude Opus 4.8)

*The officer's return, delivered as the subagent's final report to the chairman as launcher,
reproduced verbatim — one relay transport-escape normalized (`&lt;=` → `<=`), text otherwise
unedited.*

**Digest 2026-07-27: 10 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).**

Copied this session from `origin/main:digests/2026-07-27_ISDS-Thematic-Watch/meta.json` (`screened` 10, `matches` 0, `watch_list_leads` 0), read via `git show` after fetch — not from memory, not from the working tree.

### 1. Weekly-run check: the 2026-08-03 run has NOT landed; digest of record unchanged

`git fetch origin main` completed **2026-08-03T11:32:54Z**; `git ls-tree origin/main digests/ --name-only` lists ten archived run directories ending at `2026-07-27_ISDS-Thematic-Watch`. **No `2026-08-03` digest exists on `origin/main` as of 11:32:54Z.** This is not lateness: `.github/workflows/weekly.yml:5` sets the cron at `0 13 * * 1` — Mondays 13:00 UTC — so my check ran roughly 87 minutes *before* the scheduled fire time. Nothing is inferable from its absence.

**Source-health guard, first live execution — not yet observed.** `git ls-tree origin/main state/ --name-only` returns exactly `council_log.json`, `research_log.json`, `seen.json`. **`state/source_health.json` does not exist on `origin/main`.** The guard's artifact path is `HEALTH_PATH = "state/source_health.json"` (`/home/user/isds-watcher/src/source_health.py:30`), and `weekly.yml:90` does `git add state/ digests/ docs/ briefs/ analytics/`, so if the 13:00Z run executes the guard the file will appear on main in the same commit as the digest. Its absence now is consistent with the run not having fired, and carries no information about whether the guard works.

Consequently the 2026-07-27 figures remain the numbers of record for the eleventh straight day of council sessions. Copied fresh this session, unchanged: `per_source` — `iareporter_headlines` 10; `iisd_itn`, `google_alerts`, `gmail_scholar`, `google_news_rss`, `italaw`, `icsid`, `unctad_isds`, `pca_press` all 0. `accepted_by_source` empty. `source_health` — `iisd_itn` RETURNED 0, `google_alerts` RETURNED 0, `gmail_scholar` RETURNED 0, `google_news_rss` DISABLED 0, `italaw` RETURNED 0, `icsid` RETURNED 0, `iareporter_headlines` HEADLINE-ONLY 10, `unctad_isds` RETURNED 25, `pca_press` RETURNED 0.

**The longitudinal picture stands as written and is not re-derived here:** `analytics/daily-research/2026-07-30.md` Part 4 §§1–4 — 13 items ever surfaced across 10 archived runs from exactly two sources (`iareporter_headlines` 9, `italaw` 4), 0 matches in 10 of 10 runs, seven sources never having surfaced anything, and the zero-streak list. IAReporter's headline-only ceiling is the standing caveat: it supplies the candidate volume and is structurally incapable of reaching a match. `analytics/source-receptivity.md` still carries the mixed-window artifacts (`italaw` 200%, `iareporter_headlines` 17%); the `scripts/source_analytics.py` patch is PENDING operator sign-off and I touched nothing.

### 2. Today's council items against the collection surface

Zero of today's items came through the collection surface. Per my own Parts 4 of 07-30 and 07-31, that is the **third consecutive session record** with no item from the digest (the routine did not fire 08-01 or 08-02, so no records exist for those days). What is genuinely new today, and only this:

- **The blocked environment is not the collection environment.** Today's null is instrument-layer: per the officer's B1/H1 controls, curl has no egress to *any* external host and WebFetch returned 403 for 6 of 6 URLs including neutral controls. That is a fact about this delegated cloud session. The weekly collection runs in a GitHub Actions runner — a different environment I have not probed and make no claim about. **Nothing in today's session is evidence about the instrument's collection health, in either direction**, and the record should not later be read as if a blocked analyst session implied a blocked digest run. If the 13:00Z run lands with normal per-source counts, that is not a contradiction of today's finding.
- **The near-miss host is implicated a second time.** Task 3's fetch target was `investmentpolicy.unctad.org` — the host `unctad_isds` already polls weekly (`src/sources/unctad_isds.py:29` polls the ISDS Navigator; the IIA Mapping Navigator is polled by nothing), which is the near-miss I flagged in 07-31 Part 4 §3 when the China–France reconciliation arrived through the operator channel. Today it appears again, this time as an unreachable fetch. n=2 across three sessions is a datum for Emory's `unctad_isds` keep-or-retire decision, not a trend, and I am not calling it one.
- **`uncitral.un.org`** (Tasks 1–2) — unchanged from 07-30 §2: no digest source polls that host in any form. **A/81/17** — search track, no digest surface by design. **The China–Switzerland lead** — search-synthesis, LOW-MEDIUM, unverified; no collection-surface relation and no yield implication.

### 3. Optimization-proposal status pass

**No open item's status has changed since 2026-07-31.** All pending operator approvals — the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, the ledger amendment — remain PENDING, not granted. The first-application tracker remains ADOPTED (monthly, next cycle late August). The 07-01 quoted-phrase re-sequencing and the 06-30 ODS-route correction received their log correction lines in the 07-31 systems return (Part 5 §3); nothing has moved since.

### 4. New for the analytics record — two, both from reading the guard's code against the 07-27 counts

Not run-observations; derived by reading `/home/user/isds-watcher/src/source_health.py` against the recorded 2026-07-27 `source_health` block. Both are falsifiable by today's 13:00Z run, and both are flagged to the chairman, not acted on (zero budget; I edited nothing).

1. **The guard starts cold, so its `zero_streak` numbers will disagree with my ledger for two runs.** `load()` treats a missing file as `{"sources": {}}` and `update_streaks()` increments from 0, so today's run records `zero_streak: 1` for sources returning zero raw items — *not* the historical streaks in my 07-30 §4 list (`iisd_itn` 0 in 4 of 4 health-recorded runs, `gmail_scholar` the same). With `DEGRADED_AFTER = 3`, **the earliest any source can be flagged `DEGRADED` is the run of 2026-08-17** (08-03, 08-10, 08-17). A future record must not read the guard's "1 zero run" as refuting the ledger's "4 of 4"; they measure different windows, and that is a Rule-1-class consistency hazard in the making.
2. **`unctad_isds`'s static 25 masks both the anomaly detector and its own degradation.** `update_streaks()` and `collection_anomaly()` key on the raw pre-dedup `count`, and `unctad_isds` has recorded `count: 25` in every health-recorded run while contributing 0 fresh and 0 surfaced. So its `zero_streak` can never increment — it is permanently exempt from `DEGRADED` — and it permanently counts as one of the "nonzero" active sources. Applying `collection_anomaly()` to the 2026-07-27 block as recorded: of the seven `ACTIVE_SOURCES`, `iareporter_headlines` (10) and `unctad_isds` (25) are nonzero, so `nonzero = 2` and the predicate `nonzero <= 1` is **False** — the detector would **not** have fired on the very run its own docstring names as the motivating example ("An anomaly like the 2026-07-27 run — every active feed but one empty"). This is the concrete downstream cost of the fact I recorded on 07-30 §1: the feed "looks healthy in the header and contributes nothing." Whether the fix is excluding `unctad_isds` from `ACTIVE_SOURCES`, keying the guard on fresh rather than raw counts, or retiring the source, is the systems seat's and Emory's call — I am not proposing one, and no `scripts/` or `src/` file was touched.

**Rule 1 compliance:** today's is the third consecutive council record to open with the protocol sentence (07-30, 07-31, 08-03). The open question from 07-30 §6 — whether the operator saw the emailer's CONSISTENCY WARNING on the 07-21 through 07-29 sends — remains unanswered and can only be closed by Emory.

**For the chairman, since my seat will not be seated again before the run lands:** when the 2026-08-03 digest appears, the three things to copy fresh are `meta["screened"]` / `matches` / `watch_list_leads` for the Rule 1 sentence, the `source_health` statuses (checking whether any read `DEGRADED (N zero runs)` — none should, per finding 1), and whether `state/source_health.json` was committed alongside it.

Files read this session (no repository file edited): `/home/user/isds-watcher/prompts/council_roundtable.txt`, `/home/user/isds-watcher/prompts/daily_council_protocol.md`, `/home/user/isds-watcher/prompts/council_calibration.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-03.md`, `/home/user/isds-watcher/analytics/daily-research/2026-07-31.md` (Part 4), `/home/user/isds-watcher/analytics/daily-research/2026-07-30.md` (Part 4), `/home/user/isds-watcher/src/source_health.py`, `/home/user/isds-watcher/.github/workflows/weekly.yml`, and `origin/main:digests/2026-07-27_ISDS-Thematic-Watch/meta.json` via `git show`.

---

## Part 5 — Systems Researcher

**Not seated this session (chairman's Part 1 ruling).** The seat's entire carried queue is gated on pending operator sign-off — the `source_analytics.py` same-window patch application (2026-07-31 escalation 2) and the health-column proposal sequenced behind it — and the seat convenes only when its queue has ungated work. Not a procedural failure. Two items now await its next seating, both contingent: (a) on operator sign-off, the patch + tests + same-commit report regeneration; (b) the analytics officer's Part 4 §4 finding 2 (the `unctad_isds` static-25 anomaly-detector mask), which the officer expressly routed to the systems seat and Emory for the fix decision.

---

## Part 6 — Chairman's Rulings and Close-Out

*Written in-session, before the final commit. The analyst was not re-convened for a response
round: the objections concern framing and citation rather than research rework, the day's
substantive result is null either way, and spend discipline argued against a second relay
cycle. The analyst's Part 2 stands verbatim; the rulings below are the record's operative
statement wherever the two differ. One officer objection is overruled on grep evidence —
a first for this record — with the verification reproduced below.*

### Rulings on the binding objections

**B1 — ACCEPTED.** The operative statement everywhere the record touches this session's fetch instruments: *WebFetch returned HTTP 403 for every URL attempted this session, including neutral control hosts (the officer's controls: example.com, wikipedia.org, icsid.worldbank.org) — an instrument-layer artifact carrying no origin information.* Struck from the operative record: "reached uncitral.un.org," "CDN-gated as a non-browser client," "from the CDN," "(origin/CDN)," and "which routes outside the sandbox proxy." No CDN behavior was observed this session; the UA-gating finding remains a 2026-07-31 finding only. The analyst committed the exact taxonomy failure — tool-status-as-source-state — that its own memo names and correctly avoided on the curl side; noted in accountability. The Task 2 conclusion (UNTESTABLE; zero origin information) survives and is strengthened.

**B2 — ACCEPTED.** The Part 2 enumeration table is not adopted under its caption. Operatively it is *"uncitral.un.org document paths of research value recorded in the project record"* — of which only items 1–3 carry recorded 403 characterizations (item 3's earliest at 2026-07-05), while items 4–6 are unread-document citations with no 403 provenance, and the record's actual standing 403 items also include `uncitral.un.org/en/media/13010` (2026-07-09; STATE:102) and `uncitral.un.org/en/commission` (2026-07-11). **The corrected audit queue for the next direct-egress session or operator pass is therefore eight testable URLs** — enumeration items 1–6 plus the two under-inclusions — under the officer's paired-control protocol (suspect-host probe + neutral-host probe on the same instrument), with the Rabat workshop PDF (2026-07-10; no URL recorded) noted as untestable. Because zero re-tests ran, the mis-scope damaged nothing downstream; the corrected list is the queued deliverable.

**B3(a) — ACCEPTED; chairman verified both texts this session.** `2026-07-08.md` ("Analytical note") lists three distinct categories — pure FITR (irrevocable), no-U-turn (once filed, no return), and withdrawal-permitting with a cure pathway — while `STATE_OF_THE_ANSWER.md:84` (July 11) states "This is the no-U-turn provision. Cure pathway: withdrawal under Chinese civil procedure," equating no-U-turn with the cure structure. The memo's citation of "the record's July 8 definitions" for the equation is corrected to STATE:84 (July 11). The officer's Observation 5 is confirmed real: the record carries two incompatible taxonomies of the forum-relationship clause. **New ordinary gap marker: `GAP-UNRESOLVED: forum-clause-taxonomy-reconciliation`** — reconcile the 07-08 three-category taxonomy with STATE:64/84's two-category usage, from the record's own texts; assigned to the analyst as a zero-fetch, record-only task at its next seating. Until reconciled, the China–Switzerland lead's "withdrawal-cure (no-U-turn)" characterization inherits the ambiguity and is read as *withdrawal-permitting structure* without committing to a label.

**B3(b) — OVERRULED, on grep evidence.** The officer objected that "No such lesson is in `2026-07-26.md` (thirteen searches, no query-formulation finding)." The record refutes this: `2026-07-26.md:102` reads "Today's lesson: the July 25 session ran Search 2 on the keyword ... but 'Lifelong Learning' was found today only when the search was reformulated with Roberts' name and article title as search terms" — a dated query-formulation lesson, which the analyst's committed definition (`.claude/agents/research-analyst.md:36`) encodes as "the 2026-07-26 lesson: author-name+title beat keyword search." The analyst's citation was accurate to its date. Two residuals from the objection are retained as hedges: the memo's paraphrase ("specific-string queries beat keyword queries") generalizes beyond the recorded instance and should name it; and any application touching quoted-phrase snippets carries the 2026-07-30 re-sequencing caveat — which the memo's method note in fact honored ("a lead, never a finding"). The officer's own new countermeasure — grep the cited date before citing it — applies with equal force to objections: **an objection is a claim like any other, and where it asserts absence from the record, the chairman verifies before ruling.** This overrule is the precedent.

### Hedges and candidate claims

**H1–H6 accepted** (H1: the denial is blanket — no curl egress to any external host, not a three-host policy; H2: the artifact-layer count is four — proxy CONNECT rejection, fetch-service blanket 403, CDN UA-gating [2026-07-31 finding, not observed today], origin answer; H3–H4 applied below; H5: the advance-copy filename pattern stays "likely," ODS/undocs symbol route untested, not excluded; H6: the day's new insight is the proxy-layer half only). Operative candidate claims, amended before any ledger submission:

1. *(per H4)* "As of 2026-08-03, the research analyst's web search for 'A/81/17 UNCITRAL report fifty-ninth session Supplement No. 17' returned no indexed copy of A/81/17; **the analyst's result set** contained prior-session reports (A/80/17, A/79/17 advance copy, A/68/17) and general UNCITRAL pages. Result sets are session-specific; the officer's independent reproduction confirmed the null while returning a partially different set." Null-in-index only; no existence claim either way.
2. *(per B1/H3)* "On 2026-08-03 the session environment's agent proxy gateway rejected CONNECT to undocs.org:443, uncitral.un.org:443, and investmentpolicy.unctad.org:443 ('gateway answered 403 to CONNECT'), and to neutral controls example.com:443 and www.un.org:443 — a blanket curl-egress denial, not a host-targeted policy; independently, WebFetch returned HTTP 403 for every URL attempted this session including neutral control hosts — an instrument-layer artifact. No origin was shown to have been contacted by either instrument; no origin information was obtained about any resource." Source: proxy status endpoint (read independently by analyst and officer) + both members' recorded probe logs; primary as to the instruments themselves.
3. The China–Switzerland LEAD claim enters **unchanged** — its labeling was verified clean at every occurrence — subject to the B3(a) reading note above (withdrawal-permitting structure; label deferred to the taxonomy reconciliation).

### Gap-marker dispositions (the record's operative markers)

- **STANDING (opened 2026-07-31):** `china-switzerland-forum-relationship` — today's bounded attempt failed at both instruments (environment-layer); one LOW-MEDIUM unattributable synthesis lead on file (withdrawal-permitting-shaped provision text). Now a clean candidate for the July 23 operator IIA-mapping protocol — routed to Emory below (escalation 1), with the lead as the query's precise target, not as evidence.
- **OPENED (ordinary):** `forum-clause-taxonomy-reconciliation` — per the B3(a) ruling; analyst, zero-fetch, next seating.
- **STANDING:** `china-france-bit-2007-protocol-text` — untouched today, zero budget, as assigned.
- **Task-class queue (not a gap slug):** the eight-URL uncitral.un.org audit (B2 ruling) and the A/81/17 retrieval-on-publication — both require a direct-egress environment or an operator pass; routing is escalation 2.

### Status

An honest triple-null day on substance — A/81/17 not yet indexed (day 3 of the window), the uncitral.un.org re-audit untestable from this environment, the China–Switzerland gap still open — and a productive day on instrument knowledge, which is where the value concentrated. The day's lead finding: **this scheduled cloud session class has no fetch capability at all** — curl egress is denied at the proxy gateway for every external host, and the fetch service 403s every URL including neutral controls; only the search track works. The analyst's positive-control discipline caught this before it could contaminate the record with six fake audit results, and the officer's neutral-host controls then corrected the analyst's residual mis-attribution of the second instrument's failure. The vetting layer also surfaced a real record defect (the forum-clause taxonomy inconsistency) — and, for the first time, filed an objection the record itself refutes, which the chairman caught by applying the officer's own grep-before-citing countermeasure to the objection. The verification machinery is now demonstrably symmetric: it checks the checker. The analytics seat contributed the day's most consequential forward-looking instrument finding — `unctad_isds`'s static raw count of 25 permanently exempts it from the DEGRADED escalation and would have prevented the collection-anomaly detector from firing on the very run its docstring cites as the motivating example — strengthening the standing keep-or-retire datum with code-level specificity. Delegation machinery: all three seated members returned clean; zero transport faults; spend checkpoint honored (Parts 1–3 committed and pushed before Part 4); the systems seat correctly not convened on a fully gated queue.

### Accountability

- **Chairman (Fable 5):** The latest-dated-refinement delegation rule adopted 2026-07-31 worked — the analyst carried all three watched restatements without slippage, eliminating the B3-class regression it was designed for. Two defects are mine: my Part 1 Task 2 language ("the record's standing '403-blocked' characterizations") handed the analyst the mis-scoped caption B2 then caught; and I set a fetch-heavy agenda in an environment I had not probed — one neutral-host egress check before writing Part 1 would have re-scoped the day toward search-track work and saved the budget spent discovering the denial. Both enter my delegation template (below).
- **Research analyst (Fable 5):** Judgment right everywhere it mattered: refusing all six re-tests when the positive control failed, the plainly recorded triple null, the LEAD discipline held at every occurrence, and full compliance on the watched restatements. Against that: B1 — it committed the taxonomy failure its own memo names, reading a second instrument's failure as a fact about the CDN without ever showing the instrument alive — and one mis-dated internal citation (B3(a)). One objection against it overruled (B3(b): its citation was accurate). The 07-31 watch item (yield pressure degrading citation discipline) gets a third data point in B3(a), though today's variant is mis-attribution rather than fabrication risk; the watch continues.
- **Integrity officer (Opus 4.8):** The strongest single catch of the session — the neutral-host controls that falsified the CDN attribution (B1) are exactly the discipline the seat exists for, and Observation 5 surfaced a genuine record defect nobody had noticed. Three sound taxonomy additions. Against that: its first refuted binding objection (B3(b)) — asserting a lesson absent from a record file that contains it at line 102 — an instance of the very memory-file-reconstruction pattern the objection alleged. The seat's standard remains the project's spine; the lesson is that it, too, is subject to it.
- **Analytics officer (Opus 4.8):** Exemplary null-day discipline for the third consecutive seating — Rule 1 sentence with fresh-copied numbers, the weekly-run absence correctly diagnosed as pre-fire-time rather than lateness, the explicit firewall between this session's instrument failure and the collection environment's health — plus two genuinely new, code-derived, falsifiable findings (the guard's cold-start window; the `unctad_isds` anomaly-detector mask), both properly routed and neither acted on. The seat's best session to date.
- **Systems researcher (Opus 4.8):** Not seated (queue fully gated); no assessment. Two items await its next seating.

### Next steps

1. **A/81/17 (analyst, daily):** continue the one-query check — the search track works from this environment. On indexing, do NOT attempt retrieval from a no-egress session: route retrieval per escalation 2. The advance-copy filename pattern (memo Task 1) is the noted find-path.
2. **Forum-clause taxonomy reconciliation (analyst, next seating, zero-fetch):** write the reconciliation of `2026-07-08.md`'s three-category taxonomy with `STATE:64/84`'s two-category usage from the record's own texts; until then the China–Switzerland lead reads "withdrawal-permitting structure," label deferred.
3. **Eight-URL uncitral.un.org audit (queued, environment-gated):** per the B2 ruling, for the next direct-egress session or operator pass, under the paired-control protocol.
4. **China–Switzerland forum-relationship:** no further search spend; awaiting the operator's one IIA-mapping query (escalation 1).
5. **Next session's Part 4:** when the 2026-08-03 digest lands, copy the three fresh number sets per the analytics officer's handoff note (Rule 1 values; `source_health` statuses — no DEGRADED expected per the cold-start finding; whether `state/source_health.json` was committed).
6. **Chairman's delegation template (adopted today):** (a) before setting any fetch-dependent agenda in a delegated environment, run one neutral-host egress probe and read the proxy status endpoint — agenda scope follows the instruments actually alive; (b) an officer objection asserting absence from the record is verified by grep before ruling.

### Escalations to the principal (Emory)

1. **One mapping query closes the three-way comparison.** The China–Switzerland BIT (2009) forum-relationship element (fork-in-the-road vs. no-U-turn) is the last piece of the treaty-selection comparison your China–France query completed the exhaustion axis for. Same protocol as July 27: UNCTAD IIA Mapping, treaty 978 ("relationship between forums" element), marked into the ledger — ideally with the full snapshot preserved, per the July 31 snapshot lesson. Today's session produced an unverified LOW-MEDIUM lead suggesting a withdrawal-permitting clause; treat it as the query's target, not as evidence.
2. **Routing decision — this session class cannot fetch.** Today's controlled finding: the scheduled cloud environment denies all curl egress at its proxy gateway and its fetch service returns 403 for every URL including neutral controls; only web search works. Fetch-dependent work — the eight-URL uncitral.un.org audit (list in Part 6, B2 ruling), A/81/17 retrieval the day it publishes, any ICSID docket re-check — needs either your machine's delegated sessions (proven working 2026-07-31) or your manual pass. Decide where fetch tasks should live; the council will scope cloud-session agendas to the search track until then.
3. **Standing, unchanged, not re-argued:** the ledger snapshot amendment for `da33a30be92ab234`; the `source_analytics.py` sign-off (patch + tests verified 2026-07-31); the source-architecture decision (`unctad_isds` keep-or-retire — **strengthened today**: the analytics officer showed its static raw count of 25 permanently exempts it from DEGRADED and would have kept the collection-anomaly detector from firing on the 2026-07-27 run its own docstring cites as the motivating example; `google_news_rss`); the emailer CONSISTENCY WARNING question (07-21 through 07-29 sends). All other approvals remain PENDING; nothing was built or applied today.

### Self-training note (chairman)

Applied from 2026-07-31 and held: the latest-dated-refinement delegation rule eliminated the regression class it targeted (the officer verified all three watched restatements compliant), and incremental writing with the spend checkpoint left the record never more than one commit from safe. Today's two adopted rules are in next step 6: probe the instruments before writing the agenda — the day's largest avoidable cost was directing a fetch-heavy agenda into a fetch-dead environment — and verify officer-asserted absences by grep before ruling, because today the record's verification machinery produced its first false objection and the fix was the officer's own countermeasure applied one level up. The scheduled-routine gap (08-01, 08-02 did not fire) is recorded in the procedural caveats; the cadence question stays with the operator's standing decisions.
