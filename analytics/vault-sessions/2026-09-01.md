# Archivist session — 2026-09-01

**Audited against `7bc5fc0`** (`main`), on a complete **922-commit** history. The container's
clone arrived shallow at **156** commits — the fourth recurrence — so every sha in this record
was resolved only after `git fetch --unshallow`. A guard run on the shallow clone would have
reported nonsense.

**The window this session actually covers is `ad66a96..7bc5fc0` — 118 commits.** Not the window
the notes' own anchor line implies. Why that distinction is the session's main finding is §2.

---

## 1. NEEDS YOU — the 2026-08-28 archivist session never landed, and I could not land it for you

This is the failure this seat exists to catch, and it happened again.

- Branch `origin/vault/archivist-2026-08-28`, tip **`de73f9f`**, PR **#110**, open since
  2026-08-28 — the **only** open PR in the repository.
- `git merge-base --is-ancestor de73f9f origin/main` → **false**.
- It carries **676 insertions** across seven files, including a **350-line** session record,
  `analytics/vault-sessions/2026-08-28.md`, which **did not exist on `main` at all**.

That is 2026-07-31 repeating exactly: a note that was real, correct, and invisible. On top of
it, **no session ran on 2026-08-31** — no branch, no record, no commit. Against a three-day
cadence, the vault's *landed* memory had been seven days stale since `cb762ed` (2026-08-25).

**The 08-28 seat did nothing wrong. It was trapped, and it said so in advance.**
`.github/workflows/reanchor.yml` runs on every PR to `main` and pushes a commit whose one
changed file is `STATE_OF_THE_ANSWER.md` — outside this seat's self-merge path set
(`analytics/`, `agents/`, `moc/`, `HANDOFF.md`). On PR #110 that commit was **`d8667fe`**. The
seat refused to self-merge on the path test, recorded why in **`4135fd8`**, and wrote: *"every
archivist PR from now on will hit this."* It was right. Four days passed and nothing moved.

**What I did about it.** I recovered `analytics/vault-sessions/2026-08-28.md` onto `main`
verbatim from `de73f9f` in this change set. The path is inside my scope, and
`.github/workflows/vault-log.yml` emails any record with no marker in `.sent/` — so recovering
the file also **delivers the email you never received**. Expect two vault emails today: that
four-day-old record, and this one. It is not a duplicate and it is not a resend.

**What I deliberately did not do.** I did not cherry-pick the 08-28 branch's edits to the four
core notes. Those are live statements about a repository that has moved 118 commits since; I
re-derived today's state myself instead of importing four-day-old claims. **PR #110 is still
open and still yours.**

**FOR EMORY — any one of these ends this permanently, and it is thread D11:**

1. Merge PR #110 yourself; or
2. Rule that anchor-only `STATE_OF_THE_ANSWER.md` changes count inside my self-merge scope; or
3. Have the systems designer add a scope flag to `scripts/reanchor.py` **and**
   `.github/workflows/reanchor.yml`.

Until one happens, this recurs every session — including, possibly, this one. See §6.

---

## 2. NEEDS YOU — the currency anchor stopped meaning "audited", and the staleness query now reports clean by construction

Every note carries `*Audited against <sha>*`, and my standing method is to run
`git log <anchor>..HEAD -- <paths>` so staleness is a **query** rather than a judgement call.
That convention is now broken, and it broke quietly.

Since **`2e87700`** (2026-08-27) the anchor is machine-owned. `reanchor.yml` fires on every PR
to `main`, and the daily-council close-out calls `scripts/reanchor.py` directly. The sha
therefore advances on merges by seats that never read the note. It moved **nine times between
2026-08-27 and 2026-08-31** — `56b0b7b`, `91b72b6`, `d287a32`, `e8b0f71`, `08380af`, `e104a72`,
`a200b10`, `5decc23`, `c12901e`, `8c3034f` — a window in which **no archivist session ran**.

The four core notes today say *"Audited against `621aa6e`"*, a 2026-08-31 commit. They were
last actually audited **2026-08-25**, against `ad66a96`. The sentence is false in the one word
that carries its meaning.

**The operational consequence, measured rather than argued:**

| Query run 2026-09-01 | Commits returned |
| --- | --- |
| `git log 621aa6e..HEAD -- .claude/agents/ prompts/ src/models.py` (machine anchor) | **0** |
| `git log ad66a96..HEAD -- .claude/agents/ prompts/ src/models.py` (true last audit) | **1** |
| `git rev-list --count ad66a96..HEAD` (true window, all paths) | **118** |

A note can now be a week stale and read green. That is the 2026-08-03 shape of failure — a
record that is wrong but looks current — rebuilt at the guard level.

**In fairness, and stated because omitting it would be its own kind of dishonesty: this time it
concealed nothing.** Across the true 118-commit window the contract paths moved exactly once
(`93a69f7`, the re-anchor CI change itself), and `README.md`, `METHODOLOGY.md`, `COUNCIL.md`
and `views/isds-workflow-3d/workflow.json` moved **zero** times. No real drift hid behind the
false anchor. The defect is that it *would* hide it, and no seat would be told.

`reanchor.py` is good engineering for the problem it was built for — a session cannot name a
commit later than itself, which was escalation 7 for two years. The fix simply took a word that
meant "a seat read this and confirmed it" and gave it to a bot. **Wanted: the note distinguishes
a machine re-anchor from a seat's audit.** Owner: **systems-designer**. Thread **D10**. I have
put a warning block at the head of [[Agent Registry]] so the next session is not misled the way
this one nearly was.

**ACTION — `scripts/check_currency.py` is red on `main` right now:** 9 claims checked, **3
failed** — `STATE_OF_THE_ANSWER.md` (1 commit past anchor `7a2d696`), `agents/Claim Map.md`
(1), `agents/Workflow Threads.md` (**14**). The machine anchors do not keep it green, because
council merges touch declared paths *after* the re-anchor lands. `reanchor.yml` documents that
limit honestly at its own lines 9-18; the limit is simply not covered.

---

## 3. Agent-context audit — no model drift, and I checked it three independent ways

This is the priority item in my standing brief, and it comes back clean. Stated plainly rather
than padded:

- `scripts/check_models.py` exits **0** over **12** flowchart cards.
- All nine `.claude/agents/` definitions declare `model: opus`.
- Each definition's "Runs on" prose matches its vault note **seat by seat**: chairman,
  research-analyst, site-experience and systems-designer on Claude Opus 5; analytics-officer,
  integrity-officer, obsidian-archivist, research-editor and systems-researcher on Claude Opus
  4.8.
- `src/models.py` unchanged — `CHAIRMAN_MODEL` and `HEAVY_MODEL` `claude-opus-5`,
  `UTILITY_MODEL` and `FALLBACK_MODEL` `claude-opus-4-8`, `DIGEST_CLASSIFIER_MODEL`
  `claude-haiku-4-5-20251001`.

**D5 carried, unchanged and still the most consequential context gap.** Eight of the nine
definitions never name their own vault note, so the method rules each seat has adopted are not
in the path that seat reads. Only `.claude/agents/integrity-officer.md:56-62` points at its own
note — a remedy adopted for one seat after council R8 and never generalised. Editing
`.claude/agents/` is a contract change and is yours, so this stays escalated rather than fixed.

---

## 4. Deliverable drift — what the project tells a human about itself

**ACTION — the professor-facing page contradicts itself inside one document.** This is C11, and
it is worse than "the site says nine":

- `docs/how-it-works.html:7` and `:58` — "nine public sources"
- the SVG embedded in **that same page**, `:96` and `:109` — "10 SOURCES" / "10 sources"

A reader sees both numbers on one screen. The prose comes from
`scripts/site_templates/how_it_works.html.j2:3,:27`. `README.md:56` already says "ten" and
`src/sources.all_sources()` returns **10**, so the README was fixed and the template was not.
**Owner: site-experience.** Not mine to edit.

**FOR EMORY — `METHODOLOGY.md` §III is wrong on two counts against the code**, and METHODOLOGY
is your document, so I am reporting rather than editing:

1. It describes Bing News as polled through *"eight fixed, fingerprint-derived queries"*.
   `len(bing_news.QUERIES)` is **12**.
2. It mentions **GDELT zero times**, though GDELT is the tenth registered source (joined at
   `44550ca`). `README.md` also mentions it zero times. The channel list in §III enumerates
   nine, which is why the "nine sources" figure keeps regenerating downstream.

Clean on this pass: `HANDOFF.md` (untouched since my own `cb762ed`), `COUNCIL.md`,
`views/isds-workflow-3d/workflow.json`. The flowchart validator passes — 30 cards, 10 chips,
44 edges, SVG fresh against its inputs-sha256, zero "Jack" tokens.

---

## 5. Vault mechanics — one thread refined *downward*, because the evidence said so

**C8 is real in the code and not firing today.** `scripts/build_graph.py:198` is still
`pat.sub(block, text)` with no count argument, so the block-replacement hazard stands as
written. But I ran a **full** build in a scratch copy of `7bc5fc0` rather than trusting the
dry-run: **47 files changed, 170 insertions, 6 deletions, and not one note lost net content.**
`analytics/daily-research/2026-08-05.md` carries two managed blocks (lines 807-809 and
1030-1032), but both are well-formed pairs, so it is a cosmetic duplicate — not the 92-line
loss of the original incident. I am recording the thread as **latent, not active**. It should
still be fixed; it is not currently costing anything.

**Graph state:** 156 notes, 330 edges, **0 orphans**, 47 managed blocks would update. One
broken wikilink, to `analytics/locked_set/RETRIEVAL_LEDGER.md`.

**Orphan check — 15 of 115 remote branches unmerged**, up from six at the last audit. Besides
`vault/archivist-2026-08-28` (§1): `chore/operator-marks-2026-07-27` (`6f9e1da`) — **thread F1,
day thirty-six, still yours**; six `council-2026-08-31-*` branches whose content is superseded
by the merged `c12901e` and `8c3034f`; three 2026-07-30/08-01 `claude/sweet-mccarthy-*` cloud
branches (F3); `feat/research-council` (`af00584`, 2026-06-22); and `council/2026-08-26`, which
I checked rather than assumed — **it carries only `.sent/2026-08-26`, and
`analytics/daily-research/2026-08-26.md` is on `main`, so nothing substantive is stranded
there.**

---

## 6. What I could not verify, and what may go wrong with this record

Stated rather than omitted:

- **Whether this PR will merge.** D11 applies to me exactly as it applied to 08-28: if
  `reanchor.yml` pushes a `STATE_OF_THE_ANSWER.md` commit onto my branch before I merge, I am
  forbidden to self-merge and this record strands too. I will verify paths immediately before
  merging and report the outcome in my closing message either way. **If you receive this by
  email but the repository has no `analytics/vault-sessions/2026-09-01.md` on `main`, that is
  what happened** — and it is the second consecutive session lost to the same cause.
- **Why the 2026-08-31 session did not fire.** I can show it left no trace; I cannot see the
  scheduler, so I cannot say whether it failed, was skipped, or never triggered. **TO VERIFY —
  worth a look at the routine's run history**, because a silently non-firing session is
  indistinguishable, from inside the repo, from a healthy quiet one.
- **`agents/Project Machinery.md` does not exist.** My standing prompt names it conditionally;
  recorded so the next session does not hunt for it.
- **No external Obsidian/PKM research this session.** My self-training mandate asks for it
  periodically; I spent the time on the anchor and stranding findings instead, and I would
  rather say that than dress an internal finding as outside reading.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D11** — every archivist PR is unmergeable by the archivist; PR #110 stranded four days | **Emory** / systems-designer | 4 days |
| 2 | **D10** — the currency anchor no longer means "audited"; staleness query suppressed | **systems-designer** / Emory | new today |
| 3 | `check_currency.py` red on `main` — 9 claims, 3 failed | systems-designer | live |
| 4 | **D5** — eight of nine definitions never name their own vault note | **Emory** (contract change) | 19 days |
| 5 | **C11** — `docs/how-it-works.html` says nine and ten on one page | site-experience | 16 days |
| 6 | **D7** — `METHODOLOGY.md` §III: eight queries vs 12; GDELT unmentioned | **Emory** | carried |
| 7 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |
| 8 | **F1** — operator-marks branch never merged (`6f9e1da`) | **Emory** | 36 days |
| 9 | 2026-08-31 session left no trace; scheduler not visible from the repo | **Emory** | new today |
