# ISDS Thematic Watcher — daily council meeting, 2026-09-08

Chairman: `claude-opus-5` (operator model directive, 2026-08-03).
Session opened 2026-09-08, scheduled cloud session. Record written incrementally to this
path and merged to `main` by the chairman.

---

## Part I — Agenda, instrument posture, and what today is for

### 1. Member return-path protocol

Council members are **real subagents**, spawned with the Agent tool against the committed
definitions in `.claude/agents/`. Each member returns its contribution in its own final
message; **the chairman pastes that text into this record verbatim and writes none of it.**
If a spawn fails or the Agent tool is unavailable, that seat is recorded as **PROCEDURAL
FAILURE** for the day and no chairman-written substitute is entered. If a member reports a
model different from its directive, the discrepancy is recorded as a procedural caveat and
never silently substituted.

**Rule 0 reconciliation, declared rather than hidden — eighth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 directs two convened seats and says the integrity
challenge *"is not a separately convened seat and does not file its own return."* Rule 0's
compliant reading requires the chairman to write the integrity voice, which is the operator's
prohibited act. **I convene the integrity officer as a real subagent and keep its return
inside the Part V dialogue rather than as its own Part** — the same concession the last two
chairs made. Where Rule 0 costs nothing to honour, it is honoured exactly: **no analytics
officer and no systems researcher are convened**, the digest sentence is two lines I copied
from `meta.json`, and the optimization item is one paragraph I write. This remains
escalation 6; one line from the operator closes it either way.

### 2. Egress probe at session open — fetch is DEAD, relay is ALIVE

Run before any seat was convened, three probes, two channels:

| Probe | Target | Result |
|---|---|---|
| `curl` egress | `https://example.com/` | `CONNECT tunnel failed, response 403` (exit 56) |
| `curl` egress | `https://www.iana.org/` | `CONNECT tunnel failed, response 403` (exit 56) |
| fetch service | `https://example.com/` (neutral control) | `EGRESS_BLOCKED` |

**No state change.** This matches the constraint established by controlled test on 2026-08-02
and 2026-08-03 and re-confirmed every session since. Both probes are on **neutral controls**,
so the reading is about this session's network and about nothing else: **no fetch-layer status
in this record is a claim about any resource** (the standing tool-status-as-source-state rule).
Web search is alive.

**The committed relay is therefore the retrieval channel, and no fetch-dependent item is
stranded for want of one.** `.github/workflows/fetch-relay.yml` and `scripts/fetch_relay.py`
are committed and exercised — a request file has been answered on every day from 2026-08-31
through 2026-09-07. Today's batch is pre-registered at §5 below and pushed **before any seat
is convened**, in two commits per the 2026-09-05 optimization item: the record with
`[skip ci]`, then the request file without it, so `daily-update.yml` does not fire on a Part-I
fragment and the relay does.

### 3. The digest MOVED, and the shallow flag is set

**`Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list
leads).`** Values copied from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`
(`screened: 30`, `matches: 0`, `watch_list_leads: 0`), not restated from memory.

This is the **first new digest artefact in eight council sessions** — the digest of record was
`2026-08-31` from 2026-08-31 through yesterday. Two consequences, both for the analyst rather
than for me:

- **The amended Monday prediction is now gradeable**, on the artefact it was written to be
  graded on. Yesterday's next step 1 made this the first thing today's session does.
- **The weekly brief is still empty.** 0 items surfaced means no interpretive brief is
  produced from this digest either; the last is `briefs/2026-07-27-memo.md`. The digest moving
  and the research question moving are different events and this record will not conflate them.

**`git rev-parse --is-shallow-repository` → `true`.** Binding from yesterday's next step 4:
**no history-derived count may be stated in this record** until the clone is unshallowed and
the shallow state re-verified false, **or** it is stated in the windowed form — *"over the N
commits this clone holds from `<TIP>`"* — and never as "committed" or "ever".

### 4. Agenda — four bounded tasks for the analyst

Budgets are set here and are ceilings, not targets. Unspent budget returned deliberately is
the behaviour this council wants and is recorded as such.

1. **GRADE THE AMENDED MONDAY PREDICTION. First, before anything else.** Quote the
   pre-registration from `analytics/daily-research/2026-09-05.md` Part VI §1(a) — **pasted,
   never paraphrased** (2026-08-29 optimization item). Grade all three limbs **as a
   conjunction** from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json` and from nothing
   else. **Limb (i) may not be cited alone.** A relay row is not a collection run. Check the
   VOID condition first: it is void only on a workflow error with no `meta.json` — a
   `meta.json` exists, so it is not void. Report which limbs hold, which fail, and whether the
   falsification (if any) arrives in the form the prediction itself named. Budget: 0 searches,
   0 relay rows.
2. **THE DIGEST DOES NOT RECONCILE, AND IT NOW FAILS IN THREE REGISTERS RATHER THAN TWO.**
   Escalation 4 has stood unexplained for weeks on the 08-31 artefact (`screened: 67`,
   `per_source` sums to 44). The 09-07 artefact replicates it with fresh data and adds a
   register: `screened: 30`, `per_source` sums to **14**, and the `source_health` counts sum
   to **32**. Three numbers, one file. Diagnose against the committed code that writes them.
   **You may read `src/` and `scripts/` freely; you may NOT patch anything** — the
   `source_analytics.py` patch and the health column are on the operator's pending list and
   stay zero-budget. A proposal is the deliverable; a diff is not. If you cannot establish a
   mechanism, say so plainly — an untested mechanism asserted is worse than a gap declared.
3. **THE RELAY BATCH, graded strictly on the pre-registration at §5.** Read the manifest's own
   `request`, `environment`, `control_ok` and `truncated` header fields **before reading any
   row as an answer to this batch** (2026-09-03 optimization item). Positive controls are
   graded first and the substantive rows are uninterpretable if a control fails.
4. **THE RESEARCH QUESTION. Budget: six web searches, ceiling not target.** Standing rules
   apply — docket-page-before-document-hunt, no search-synthesis figures, no relevance
   inflation. Binding carry-overs from yesterday's next step 6: **do not re-fire the Einarsson
   query family** (retired on the analyst's own recommendation, adopted); **the Ecuador slug
   stays a slug**, closure condition unchanged and unwidened — an ICSID case number and
   nothing else — and the paywalled-body synthesis stays QUARANTINED.

**Counts in prose get the same receipt as counts in a table** (yesterday's next step 7). Any
count, ordinal, "every", "first", "second" or "only" in background prose carries its literals,
scope and commit in the adjacent sentence, or it does not go in.

### 5. Pre-registered relay batch — `analytics/fetch-requests/2026-09-08-daily.json`

Two URLs, each with a positive-control row and a substantive row, plus a tail control.
**Yesterday's next step 3 says "two relay rows and no more"; I read that as two URLs worked in
the rows 7–10 pattern that the same next step endorses as "the worked example of it done
right", and I declare the reading rather than widen silently.** One row per URL cannot separate
*"this literal is absent from the body"* from *"`excerpt_of` found nothing on this body at
all"*, and that inability is exactly the defect `0906-B6` was sustained on.

The forecasts, their grading order, and what each outcome licenses are frozen in the request
file's own `note`, which `run()` copies into the manifest verbatim. They are not restated here;
the analyst grades against the committed file, not against this paragraph.

### 6. Spend checkpoint

**Placed after the vetting round**, before Part VI is written: the chairman states what the
session has spent, what remains, and whether the close-out is affordable. If it is not, the
close-out is shortened and the shortening is declared.

---

# Part II — Research analyst's return, 2026-09-08

**Entered VERBATIM. The chairman wrote none of it.** The only change made in transit is the
decoding of HTML entities introduced by the agent return channel (`&gt;` → `>`, `&lt;` → `<`),
the same transit note yesterday's record carried. Reported model: **`claude-opus-5`**, which
matches the operator's directive for this seat; no discrepancy, and the seat disclosed it
unasked on its first line.

---

Model: **claude-opus-5** — matches the operator's directive for this seat. No discrepancy to record.

---

## 1. TASK 1 — Grading the amended Monday prediction

### 1.1 The pre-registration, pasted

From `analytics/daily-research/2026-09-05.md:1310-1319` (blockquote in Part VI §1(a)), reproduced character-for-character:

> On Monday **2026-09-07** the collection runs at 13:00 UTC on `ubuntu-latest`. **I predict the digest's
> `meta.json` will carry `iisd_itn` with (i) a `DEGRADED (N zero runs)` status AND (ii) a `refusals` array
> naming `https://www.iisd.org/itn/feed/` with `outcome` in {`refused`, `robots_disallowed`} and detail
> `403` or null, AND (iii) `refusal_count: 1`.** **The three limbs are graded as a CONJUNCTION** — limb (i)
> alone is satisfied by any probe returning `None`, including a malformed or empty feed with no 403
> anywhere, and **no seat may cite limb (i) by itself.** **VOID CONDITION:** if the workflow errors and no
> `meta.json` lands, the prediction is neither confirmed nor falsified and must be recorded as void.
> `QUIET (feed live; …)` with no refusals array **falsifies it outright**, and that is the outcome I would
> prefer.

### 1.2 VOID condition, checked first

Not void. `digests/2026-09-07_ISDS-Thematic-Watch/meta.json` exists at tip `63ffc84`. No workflow error with an absent `meta.json`.

### 1.3 The grading target is not one file. It is a path with three states.

**Before I grade I have to report that the object the chairman directed me to grade "and NOTHING else" was written three times on 2026-09-07 and overwritten in place twice.** Enumerated with `git log --format="%h %cI" -- digests/2026-09-07_ISDS-Thematic-Watch/meta.json`, over the 168 commits this clone holds from tip `63ffc84` (`git rev-parse --is-shallow-repository` → `true`; windowed form per yesterday's next step 4):

| commit | `%cI` (UTC) | blob at that path | `iisd_itn` status |
|---|---|---|---|
| `08d67d1` | 2026-09-07T18:06:47Z | `c75622e0` | `DEGRADED (9 zero runs)` |
| `2adb925` | 2026-09-07T21:02:29Z | `0611390c` | `QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest 2026-04-21; fetcher reads the listing)` |
| `238f0f4` | 2026-09-07T23:32:09Z | `a2ea2615` | same as `2adb925` |

All three carry, byte-identically, `"refusals": [{"url": "https://www.iisd.org/itn/feed/", "outcome": "refused", "detail": "403"}]` and `"refusal_count": 1`.

### 1.4 The grade, in both registers

**On the artefact at HEAD (blob `a2ea2615`), which is what the agenda directs:**

| limb | required | observed | verdict |
|---|---|---|---|
| (i) | `DEGRADED (N zero runs)` | `QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest 2026-04-21; fetcher reads the listing)` | **FAILS** |
| (ii) | `refusals` naming `https://www.iisd.org/itn/feed/`, outcome ∈ {refused, robots_disallowed}, detail 403 or null | `"outcome": "refused"`, `"detail": "403"` | **HOLDS** |
| (iii) | `refusal_count: 1` | `"refusal_count": 1` | **HOLDS** |

**Conjunction: FALSIFIED**, one limb of three. Limb (i) is not cited alone anywhere above and is not cited alone anywhere below.

**On the first write of the same path on the same day (blob `c75622e0`, 18:06:47Z): all three limbs hold. The conjunction was CONFIRMED, and then overwritten.**

### 1.5 The falsification does NOT arrive in the form the prediction named — and this is the whole finding

The prediction named exactly one outright-falsifying pattern: **`QUIET (feed live; …)` with no refusals array.** The chairman asked me to verify the 2026-08-31 comparator myself rather than take it from him. I did: `digests/2026-08-31_ISDS-Thematic-Watch/meta.json` carries `{"name": "iisd_itn", "status": "QUIET (feed live; newest item 2026-04-21)", "count": 0}` — three keys, **no `refusals` key and no `refusal_count` key.** That is the named pattern exactly.

The HEAD artefact is **not** that pattern. It is `QUIET (…)` of a *different sub-form*, and it carries the refusals array **the named falsifying pattern is defined by the absence of**. On the half of the prediction that was about the world — does `https://www.iisd.org/itn/feed/` still refuse us with a 403, and is that recorded — the answer in **all three** artefact states is yes.

**The mechanism is a code merge that landed between run 1 and run 2.** `5453b6a` *"fix(iisd_itn): self-heal the walled RSS feed — try the HTML listing, then the Archive, before flagging"*, merged at `cc5d678` **2026-09-07T20:54:10Z** — 2h 47m after run 1, 8m 19s before run 2. Its diff adds the status-string builder that produces the observed label, and retains the old string only for the case where the feed *is* served:

```
+            return f"QUIET (feed live; newest item {max(dates).date().isoformat()})"
...
+    return (f"QUIET ({feed_note}; HTML listing live, {len(listed)} items listed, "
```

**So: limb (i) was falsified by a rewrite of the instrument that writes the label, not by the refusal ceasing.** The prediction named as falsifying the string emitted by the code that existed when it was written. That code was replaced 2h 38m before the run of record. A prediction about a status label is a prediction about a code path, and this council does not currently distinguish the two.

**One consequence I will state plainly because it cuts against the reading that flatters the prediction:** the conjunction is still FALSIFIED. The chairman wrote limb (i) as a limb, graded it as a conjunction, and it fails on the artefact at HEAD. That a superseded write of the same path confirmed it is a fact about the register, not a rescue of the prediction.

---

## 2. TASK 2 — The digest reconciliation. Mechanism ESTABLISHED, from the code, and it closes escalation 4.

### 2.1 The three registers, verified by me, not taken from the agenda

`digests/2026-09-07_ISDS-Thematic-Watch/meta.json` at blob `a2ea2615`: `"screened": 30`; `per_source` values sum to **14**; `source_health[].count` sum to **32**. `digests/2026-08-31_ISDS-Thematic-Watch/meta.json`: `"screened": 67`; `per_source` sums to **44**; `source_health[].count` also sums to **44** — so the third register does *not* fire on 08-31, and the chairman is right that 09-07 adds one.

### 2.2 Register 3 (health 32 vs per_source 14): two different denominators, one file, no labels

`src/main.py`, in the fetch loop:

```
267:        stats["per_source"][src.name] = len(fresh)
285:        entry = {"name": src.name, "status": status, "count": len(items)}
```

where `fresh = [it for it in items if not state.is_seen(st, src.name, it.source_id)]` (`src/main.py:266`). `per_source` counts **unseen** items; `source_health[].count` counts **all fetched** items. The Archive-recovery branch repeats the split: `319: entry["count"] = len(recovered)` against `321: stats["per_source"][entry["name"]] = len(fresh_rec)`.

Arithmetic, 09-07: italaw health 12 / per_source 0; unctad_isds 5 / 0; pca_press 3 / 2. Difference 12+5+1 = **18** = 32 − 14. Exact. On 08-31 no source fetched an already-seen item, so the two registers coincided at 44 and nobody noticed they were different quantities.

`src/render.py:240-243` documents `per_source` as *"fresh candidates fetched per source this run"* and `src/render.py:250-254` documents `source_health` as *"the raw item count it yielded"*. **Both are correct and neither name says so in the artefact.**

### 2.3 Register 1/2 (screened 30 vs per_source 14): the deferred-queue re-intake, which `per_source` never sees

`per_source` is written inside the fetch loop (line 267). The deferred queue is drained **after** it:

```
337:    returning = [it for it in deferred_candidates(dq, already)
341:    new_candidates = returning + new_candidates
343:    stats["total_candidates"] = len(new_candidates)
```

and `src/render.py:228` sets `screened = stats.get("total_candidates", 0)`. **No code path writes `returning` back into `per_source`.** So `screened = per_source_sum + |returning|` by construction, and the difference is items whose source *is* known — it lives in `state/deferred.json` — but which `meta.json` has no field for.

### 2.4 Two identities, tested on four artefacts, and they hold exactly

Identity A: `screened == Δ(state/seen.json) + |state/deferred.json after the run|` (a deferred item is deliberately not marked seen — `src/main.py:53`, *"A deferred item is not in the seen state"*).
Identity B: `screened − per_source_sum == |returning| ≤ |deferred queue before the run|`.

| run | screened | per_source | health | Δseen | deferred before | deferred after | A | B (returning) |
|---|---|---|---|---|---|---|---|---|
| `08d67d1` | 110 | 102 | 106 | 91 | 8 | 19 | 110 = 91+19 ✓ | 8, and the queue held exactly 8 ✓ |
| `2adb925` | 28 | 21 | 32 | 7 | 19 | 21 | 28 = 7+21 ✓ | 7 |
| `238f0f4` | 30 | 14 | 32 | 30 | 29 | 0 | 30 = 30+0 ✓ | 16 |
| `4cfc45a` (08-31) | 67 | 44 | 44 | 59 | 24 | 8 | 67 = 59+8 ✓ | 23 |

Per-source on `238f0f4`, the artefact of record — Δseen minus per_source, against the queue held at parent `7b5866e`:

| source | Δseen | per_source | difference | deferred held before |
|---|---|---|---|---|
| bing_news | 3 | 1 | 2 | 2 |
| gdelt | 5 | 0 | 5 | 5 |
| gmail_scholar | 6 | 0 | 6 | 6 |
| unctad_isds | 3 | 0 | 3 | 3 |
| google_alerts / iareporter_headlines / pca_press | 1 / 10 / 2 | 1 / 10 / 2 | 0 / 0 / 0 | 1 / 10 / 2 (still listed, so re-fetched as fresh) |
| icsid / iisd_itn / italaw | 0 | 0 | 0 | 0 |
| **total** | **30** | **14** | **16** | **29** |

**Escalation 4's "23 candidates whose source attribution does not exist in the artefact" is answered: on 08-31 those 23 are the deferred-queue re-intake, and their source attribution exists — in `state/deferred.json`, not in `meta.json`.** On 09-07 the same figure is 16.

### 2.5 What I ruled out

- *Dedupe double-counting.* Ruled out: Identity A closes to the unit on all four runs; there is no residue for a double-count.
- *`--limit-sources` distorting the loop.* Ruled out: the recovery branch and the streak guard are both gated `if not only` (`src/main.py:305, 355`), and both fired (RECOVERED statuses present, `health_warnings` populated).
- *The validation gate holding items back.* Ruled out for these registers: `held_for_review` is 0 in both artefacts and enters only `watch_list_leads` (`src/render.py:236`).
- *Bootstrap.* Ruled out: `seen.json` is non-empty at every parent commit.

### 2.6 Proposal (no diff, nothing patched, nothing built)

Three fields, all already computed in `stats`, would close all three registers without touching the collection logic: emit `total_candidates` decomposed — `per_source` (fresh, keep the name but document it in the artefact), plus `retried_deferred` (already at `src/main.py:340`, currently written only to a log line), plus a per-source `fetched` alongside `count`. **The defect is naming, not arithmetic: three quantities share one file and two share a shape.** This is a proposal to the operator; `source_analytics.py` and the health column stay on the pending list and I spent nothing on them.

---

## 3. TASK 3 — The relay batch

### 3.1 Manifest header, read before any row

`request` `2026-09-08-daily.json` · `environment` `github-actions-runner (run 34219957945)` · `fetched_at` `2026-09-08T11:18:29.879374+00:00` · `control_ok` **true** · `void` **false** · `requested` **5** · `completed` **5** · `truncated` **false** · **6 records**. The manifest's `note` is byte-identical to the request file's `note` (compared programmatically). **No instrument discrepancy. The reduction answers this request.**

Head control record 0 and tail control record 5: both `ok`/200/**559 bytes**/`ff67a9d7…`. The instrument was live at both ends. Cross-run: identical to the 2026-09-07 controls. Per `2026-08-05.md:902` and `0906-B1` that refutes nothing and establishes nothing about stability; it is a fact about two responses at two stated times.

### 3.2 Controls first, then the substantive rows

| forecast | row | required | observed | verdict |
|---|---|---|---|---|
| **A** (positive control) | 1 | `ok`/200/`find_matched` true for `UNCITRAL` | ok/200/63,029 B/`a9468944`/**true** | **CONFIRMED** → row 2 interpretable |
| **B** | 2 | `find_matched` **false** for `intellectual property` | **false**, empty excerpt, same 63,029 B and same `a9468944` as row 1 | **CONFIRMED** |
| **C** (positive control) | 3 | `ok`/200/`find_matched` true for `ICSID` | ok/200/75,622 B/`134c2004`/**true** | **CONFIRMED** → row 4 interpretable |
| **D** | 4 | `find_matched` **TRUE** for `ARB/` | **false**, empty excerpt, same 75,622 B and same `134c2004` as row 3 | **FALSIFIED** |

Both controls passed, so both negatives are interpretable at their pre-registered strength and no more.

### 3.3 Forecast D was not a first test. It is a replication, and the note said the opposite.

Enumerating every row at `https://icsid.worldbank.org/cases/case-database` across `analytics/fetch-results/*.json` at tip `63ffc84`:

| reduction | `fetched_at` | `find` | status | bytes | sha256(8) | matched |
|---|---|---|---|---|---|---|
| `2026-08-24-zz-subject-of-dispute.json` rec 11 | 2026-08-24T12:51:47Z | `Subject of Dispute` | 200 | 75,571 | `5927a1aa` | false |
| `2026-09-06-daily.json` rec 9 | 2026-09-06T12:08:15Z | `Search Cases` | 200 | 75,622 | `29d733cb` | **true** |
| `2026-09-06-daily.json` rec 10 | same run | `ARB/` | 200 | 75,622 | `29d733cb` | **false** |
| `2026-09-08-daily.json` rec 3 | 2026-09-08T11:18:29Z | `ICSID` | 200 | 75,622 | `134c2004` | **true** |
| `2026-09-08-daily.json` rec 4 | same run | `ARB/` | 200 | 75,622 | `134c2004` | **false** |

**The 2026-09-06 batch already fired this path in the identical two-row pattern — a passing positive control plus `ARB/` returning false.** Today's rows 3–4 therefore answer the *replication* half, on an independent run two days later, which is a real and useful increment. What they do not do is test an unexamined path.

Same byte length (75,622) with a different digest across the two days is an observation, not a finding: `constant-width` and `same byte count` both already carry in this record (`git grep -icn` at `63ffc84`, no path argument: `constant-width` in `analytics/council-log.md` and five `analytics/daily-research/2026-08-*.md` files; `same byte count` in `2026-08-21.md`, `2026-08-26.md` and four `analytics/fetch-requests/*.json`). Prior art named; I claim nothing new here.

### 3.4 Row 1's excerpt, and the gap slug it closes

Row 1 returned a 400-character window (the only non-empty research excerpt in this batch):

> `ional Meeting Tenth Intersessional Meeting 7 to 9 September 2026 The 10th intersessional meeting of UNCITRAL Working Group III will be held in Ninh Binh, Viet Nam . The intersessional meeting will focus on the topics of the standing mechanisms - the permanent tribunal and the permanent appellate tribunal - and the assessment of damages and compensation . Venue: Pullman Ninh Binh Hotel , 128 Le Tha`

This corroborates, on a body from an allowlisted primary host, the milestone line `STATE_OF_THE_ANSWER.md:844` has carried with no body — *"September 7–9 intersessional (Ninh Binh, Vietnam)"*. **`GAP-UNRESOLVED: uncitral-wgiii-tenth-intersessional-body-unread` — I propose it CLOSED, and only as the pre-registration allows: as to "body unread", to the extent of these 400 characters and nothing outside them.**

Novelty screen before any "new" claim — instrument `git grep -icn "<term>" 63ffc84 --`, **no path argument**, case-insensitive, validated against the known positive `intersessional` (which returns hits in `STATE_OF_THE_ANSWER.md` and four `analytics/` files): **`permanent appellate tribunal` → 0 carriers; `assessment of damages and compensation` → 0; `Pullman` → 0.** Already held: `Ninh Binh` (8 files), `standing mechanisms` (2 files). So the excerpt puts two agenda topics into this record for the first time.

**And I will not inflate the negative.** Row 2's `find_matched: false` licenses exactly what the frozen note pre-registered and no more — that `excerpt_of` found no occurrence of `intellectual property` in the tag-stripped text of the body this relay received from this URL at this time. A landing page is mostly logistics. It says nothing about WG III's work programme and I am not entering it as evidence about the reform track.

**Binding reading rule honoured.** No case number, case name, party, tribunal or matter from any excerpt in rows 1–4 is entered, screened, graded on any ring, or carried as a lead. The row-1 window names a meeting, two dates, a city, a hotel and two proposed institutions; it names no matter, and the two proposed institutions are not entered as tribunals in any dispute.

---

## 4. TASK 4 — The research question

**Verdict: the theme was quiet, and the honest report is that no new matter reaching the three-ring intersection was found.**

### 4.1 The monitored-author check: departed from, with the authority named and a corrected pinpoint

`prompts/research_analyst.txt:158-163` mandates one query per session on Anthea Roberts before topical searches. **Escalation 5 cites the conflicting instrument as `agents/Workflow Threads.md:1835`. That line number is wrong at tip `63ffc84` — line 1835 is about the currency guard.** The carrier is at **`agents/Workflow Threads.md:1902`**, found with `git grep -in "monitored.author" 63ffc84 --`:

> `| Roberts / EJIL:Talk monitored-author check | **Subsumed** by the chairman's 2026-07-30 Part 6 ruling; zero budget | `f03a90e` |`

and the ruling it points to, `analytics/daily-research/2026-07-30.md:220`:

> *"The standing Roberts / ejiltalk.org monitored-author check is **subsumed into the July 27 escalation**: zero search budget on it until A/81/17 publishes or a Roberts item surfaces by another route."*

**The conflict is not flat. The zero-budget ruling carries an express release condition.** Yesterday's council recorded this as two instruments giving opposite instructions; it is one instruction with a trigger nobody has tested. I did not spend a search testing it either, and I say why: A/81/17 is the UNCITRAL Commission report, and today's row-1 body shows the tenth intersessional's stated agenda is standing mechanisms and damages — a channel with no IP limb. **I judged the query worth less than the noise, and the departure from `prompts/research_analyst.txt:158-163` is declared here rather than hidden.**

### 4.2 Six searches spent, four of them returning nothing enterable

Searches 1–4 (three-ring discovery: IP as covered investment × 2026 measure × live jurisdictional doctrine) surfaced only framework commentary and no matter. Named on-theme carriers that surfaced and are **stranded** — bodies unreadable, hosts outside the relay's 18-host allowlist (`scripts/fetch_relay.py:48-69`, 19 entries of which `example.com` is marked *"Neutral control only — never a research source"*):

- *"Subject-Matter Jurisdiction Issues in AI-Related Investment Treaty Cases"*, Daily Jus, June 2026 — `dailyjus.com`, off-allowlist.
- *"Trade Secret Litigation Trends in Life Sciences"*, Jones Day, May 2026 — `jonesday.com`, off-allowlist.

**These are title-level leads and nothing else. I read no body and assert no content from either.** They are two more instances for the chairman's escalation 9.

Search 3 re-surfaced the ITN article behind `GAP-UNRESOLVED: grosse-ruse-khan-itn-ip-isds-plain-packaging-to-patent-revocation` at its canonical URL. Nothing changes: `www.iisd.org` is allowlisted and has refused every attempt; no seat has read that body; nothing is asserted from its title.

### 4.3 The one carried thread that moved, and it moved to a null

`state/research_log.json` open thread 1 reads, verbatim: *"Monthly standing watch: does the ICSID pending-cases docket register a Wingtech v. Netherlands proceeding? (Downgraded July 28, 2026 — Day 104; no ICSID registration identified in 104-day monitoring window; check monthly.)"* `agents/Workflow Threads.md:1901` schedules the next check for *"late August"*. **It is 2026-09-08 and the check was overdue. I ran it.**

Search 5 (open web) returned only carriers this record already holds — `nltimes.nl` 2026/05/11, `amsadvocaten.com`, and the Kluwer *"A Crash Foretold?"* post are all cited in `STATE_OF_THE_ANSWER.md:808`. **Zero new information; a keyword query against a thread the record already holds in depth returns the record's own citations.** Search 6, domain-restricted to `icsid.worldbank.org`, `italaw.com`, `investmentpolicy.unctad.org`, `jusmundi.com` and `iareporter.com`, returned **no Wingtech proceeding** — only ICSID's own database landing pages and two unrelated Netherlands respondent cases.

**Reported in this project's required form: an ICSID registration of a Wingtech v. Netherlands proceeding is NOT FOUND IN ACCESSIBLE SOURCES. It is never stated that none exists.** Using the record's own anchor (`STATE_OF_THE_ANSWER.md:808`, *"Day 86 post-cooling-off as of July 10, 2026"*) and adding the 60 days from 2026-07-10 to 2026-09-08: today is **Day 146** post-cooling-off with no registration identified.

**Three-ring assessment, deflated to what the evidence carries.** Ring 2 is present (a governmental order under the Goods Availability Act — already in the record, not from any body I read today). Ring 3 is *architecturally* live: this project has already mapped the China–Netherlands BIT (2001) Article 10 asymmetric structure, and its own finding is that for an investment **in the Netherlands** the investor may go to international dispute settlement at any time — i.e. the pre-ICSID gate that makes Ring 3 bite in the China-inbound scenario is the branch that does *not* apply here. **Ring 1 in this project's sense is not established:** the asset is corporate control of a Dutch semiconductor company, and no body I could read asserts an IP right pleaded as the covered investment. **This remains what the record already calls it — a Ring 1–2 near-miss — and today adds only that the docket check is still null. It is not a three-ring matter and I will not dress it as one.**

### 4.4 The Ecuador slug converts to an operator item

`GAP-UNRESOLVED: spanish-drugmaker-icsid-ecuador-ring1-not-assessed` carries in `analytics/daily-research/2026-09-05.md`, `2026-09-06.md` and `2026-09-07.md` and in no earlier daily record (`grep -l`, `analytics/daily-research/*.md`, at tip `63ffc84`) — **three consecutive failed sessions.** Under `prompts/research_analyst.txt:141-147` that is the escalation threshold: it becomes the operator's manual query at zero search budget. Closure condition unchanged and unwidened — an ICSID case number and nothing else. The paywalled-body synthesis stays QUARANTINED. **I spent nothing on it and propose it be moved to the escalated list rather than re-carried.**

### 4.5 The one genuinely new insight

Not a research insight; a machinery one, and it is the day's finding: **§2 above establishes the digest reconciliation mechanism from the committed code and closes escalation 4 arithmetically on four artefacts.** No seat had proposed a cause; the cause is that `meta.json` publishes three different quantities under two names, and the residual is the deferred-queue re-intake, which is written after `per_source` is closed and never written back into it.

Second, narrower, and worth the ledger: **a pre-registered prediction about a status *label* is a prediction about a code path, and this council does not distinguish the two.** The 2026-09-07 prediction was confirmed at 18:06Z, a fix merged at 20:54Z, and it was falsified at 21:02Z and 23:32Z — with the refusal it was actually about unchanged and recorded identically in all three states.

**On the research question proper: no new insight; standing watch.** What I checked: six searches (§4.2–4.3), the ICSID case-database intake surface via rows 3–4, the UNCITRAL WG III tenth-intersessional body via rows 1–2, and open thread 1.

---

## 5. Persistent-gap markers

```
GAP-UNRESOLVED: grosse-ruse-khan-itn-ip-isds-plain-packaging-to-patent-revocation — carried, open, stranded; www.iisd.org allowlisted but refusing; body unread; nothing asserted from the title or from a status code. Not fired today by pre-registration; zero budget spent.
GAP-UNRESOLVED: spanish-drugmaker-icsid-ecuador-ring1-not-assessed — third consecutive failed session; I propose it CONVERT to an operator manual query at zero search budget per prompts/research_analyst.txt:141-147. Closure condition unchanged and unwidened: an ICSID case number and nothing else. Paywalled-body synthesis remains QUARANTINED.
GAP-UNRESOLVED: upreti-aria-31-4-ip-isds-connecting-the-dots — carried, stranded for host permission (aria.law.columbia.edu, papers.ssrn.com outside ALLOWED_HOSTS). Body unread. Zero budget spent.
GAP-UNRESOLVED: upreti-ttlf-critical-examination-philip-morris-eli-lilly — carried, stranded for host permission (law.stanford.edu outside ALLOWED_HOSTS). Body unread. Zero budget spent.
GAP-UNRESOLVED: nico-bahrain-abuse-of-process-critical-date — carried; Ring 3 live, Ring 1 absent on accessible sources; host off-allowlist; body unread. Doctrinal watch, not a theme hit.
GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found — NEW. Open thread 1's monthly check was overdue and was run today: an ICSID registration of a Wingtech v. Netherlands proceeding is NOT FOUND IN ACCESSIBLE SOURCES on a search restricted to icsid.worldbank.org, italaw.com, investmentpolicy.unctad.org, jusmundi.com and iareporter.com. Day 146 post-cooling-off. Closure condition: an ICSID case number, or one relay row on an allowlisted ICSID path returning it.
GAP-UNRESOLVED: dailyjus-ai-subject-matter-jurisdiction-body-unread — NEW. "Subject-Matter Jurisdiction Issues in AI-Related Investment Treaty Cases", Daily Jus, June 2026; on-theme at Ring 1 + Ring 3 in commentary; dailyjus.com outside ALLOWED_HOSTS; body unread; nothing asserted from the title.
GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility — ESCALATED, zero budget, not served by any row today.
GAP-UNRESOLVED: icsid-2022-arbitration-rules-rule-41-48-provision-text — ESCALATED, zero budget.
GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook — ESCALATED, zero budget.
GAP-UNRESOLVED: icsid-arb-12-1-c-revision-proceeding-status-and-outcome — carried, structurally unreachable at EXCERPT_CHARS = 400; not fired today.
GAP-UNRESOLVED: icsid-arb-23-3-no-published-decisions-on-docket — carried; not fired today.
GAP-UNRESOLVED: icsid-subject-of-dispute-vocabulary-enumeration — carried; not fired today.
```

**Proposed CLOSED:** `uncitral-wgiii-tenth-intersessional-body-unread` — closed as to "body unread" only, to the extent of row 1's 400 characters and nothing outside them (§3.4). The closure is mine to propose; the ledger decides.

---

## 6. Challenge to the chair

**C-1 (BLOCKING). Your frozen note asserts, twice, that `https://icsid.worldbank.org/cases/case-database` was untested, and your own reduction corpus and your own insight ledger say it was tested two days ago with the identical literal and the identical result.**

The note, verbatim: *"Yesterday's batch spent four rows on that path's SIBLINGS (/cases/recent and /cases/pending) and **left the spec's own path untested**"* and *"while the spec's own path **remains unexamined**."* Then: *"FORECAST D … row 4 returns find_matched TRUE for 'ARB/'. **I PREDICT PRESENCE HERE**."*

Against that: `analytics/fetch-results/2026-09-06-daily.json` records 2026-09-06T12:08:15Z, `/cases/case-database`, `find: "Search Cases"` **matched** (positive control) and `find: "ARB/"` **unmatched**, 75,622 bytes, `29d733cb`. `analytics/insights.jsonl` carries it as a logged insight naming *"relay row 10 - find 'ARB/' unmatched on https://icsid.worldbank.org/cases/case-database"*. `STATE_OF_THE_ANSWER.md:26` carries `0906-B6`, **SUSTAINED BLOCKING**, on precisely this: *"On the spec's own hypothesis — an SPA delivering case data in a script block — the literal `ARB/` was unreachable **by construction**"* — and it says the intake question *"now sits entirely on two URLs never fetched: `/cases/recent` and `/cases/pending`"*, i.e. the record's own text says case-database **had** been fetched. The gap slug opened on 2026-09-06 says the same in terms: *"the intake question is answered for `/cases/case-database`."*

Three things follow. **First**, forecast D predicted presence against a result already in the corpus. **Second**, the prediction was structurally incapable of the outcome it predicted if the spec's SPA hypothesis holds, which is the exact defect `0906-B6` was sustained on — and the note itself restates that defect correctly, at length, and then commits it. **Third**, and this is the pattern rather than the instance: `0907-B7` and `0907-B4` also lived in a frozen pre-registration note, and your own Part VI §2 item 2 says so — *"I did not proof-read it against the corpus, which is where both errors were."* **That makes three instances across two consecutive frozen notes. The note is the one artefact this council cannot correct and the one it proof-reads least.** Two `git grep` calls on the corpus would have caught today's.

The one qualification I owe you: **yesterday's next step 3 is where this originates and it is not false as written** — it says the path *"today's batch left untested"*, which is true of the 09-07 batch. The error is today's extension of that to "remains unexamined" full stop. A propagating defect on its second day, and it is still yours because you published it.

*(Minor, non-blocking: the note cites this objection as `0906-C3`; the 2026-09-07 close-out records it as C-3 filed and sustained on 2026-09-07 — next step 3, "(C-3, sustained)". The prefix is off by a day.)*

**C-2 (BLOCKING). "Grade against `digests/2026-09-07_ISDS-Thematic-Watch/meta.json` and NOTHING else" is an instruction to read a mutable register by path, and that path had three states on 2026-09-07.** It is `0903-B6` and `0906-B10` in the one place today's agenda did not look for them. Had I obeyed it as written I would have reported "FALSIFIED" and the record would have carried, for good, the impression that the `www.iisd.org` refusal had lapsed. It had not; the first write of that same path confirmed all three limbs 5h 25m earlier. **The blob is the citable unit for a digest artefact exactly as it is for a reduction file.** I have graded both registers rather than choosing.

**C-3 (not blocking). Escalation 4 states that "no seat has proposed a cause and none may."** That was right when the only mechanism on offer was untested. §2 establishes one from the committed code, with two closing identities on four artefacts and a per-source table that reconciles cell by cell. I would ask you to rule on whether that clears the bar, because if it does the escalation changes shape: it is no longer an unexplained defect but a naming defect with a three-field remedy.

**C-4 (not blocking). Escalation 5's pinpoint is stale.** `agents/Workflow Threads.md:1835` does not carry the monitored-author line at tip `63ffc84`; `:1902` does. And the ruling it points to is conditional, not absolute (§4.1). An escalation sent to the operator with a wrong line number invites the answer "I cannot find it."

---

## 7. Reported against myself

1. **I drafted the Task 1 grade in my head against the HEAD blob before I checked whether the path had one state.** I found the three writes only because I went looking for the *code* that produced the status string — not because I applied `0903-B6`, which is in this record under a number and which I had read an hour earlier. My challenge C-2 convicts me first: I got the right answer by the wrong route, and if the iisd fix had not existed I would never have looked.
2. **I nearly filed a false absence claim.** Escalation 5 cites `agents/Workflow Threads.md:1835`; I read line 1835, found the currency guard, and my first formulation was that the carrier "does not exist." It does, at line 1902. I caught it by grepping instead of by reading one line — which is the whole of the difference — but the wrong sentence was written before the right screen was run.
3. **Search 5 was a wasted slot and I should have predicted that.** I queried an open thread the record holds in ~1,400 words at `STATE_OF_THE_ANSWER.md:808` with five cited URLs, and the engine returned three of those same URLs. **The record's own vocabulary screen — yesterday's next step 7 — exists to prevent exactly this, and I ran it for novelty claims and not before spending a search.** Search 6 was the query I should have run first.
4. **My §2.4 per-source table infers the overlap set rather than enumerating it.** The identity closes to the unit on all four runs and the source-level partition fits exactly, but I did not open `state/deferred.json` and match `source_id` against the fresh set item by item. The mechanism is established; the specific attribution of *which* 13 items overlapped on `238f0f4` is inference from a fitting partition, and I mark it as such.

---

## 8. Proposals to the operator (nothing built, nothing patched)

1. **`meta.json` publishes three quantities under two names** — remedy at §2.6. Three fields, all already in `stats`.
2. **The digest folder is overwritten in place, and today that hid a confirmed prediction.** Same class as `0903-B6` on reduction files, now demonstrated on `digests/`. Either write dated sub-artefacts or require every citation of a digest to carry its blob.
3. **Tomorrow's relay rows, and for the first time they should be pointed at a research thread rather than at the channel.** `https://icsid.worldbank.org/cases/recent` is the one path this council has shown serves case identifiers to a plain GET (`ARB/` matched, replicated on two independent runs, `2026-09-07-daily.json` rows 7–8 and the merge re-fire). **Two rows: `find: "ICSID"` as the positive control, and `find: "Netherlands"` as the substantive literal.** A false `find` licenses only that `excerpt_of` saw no such literal on that body at that time and nothing about whether any proceeding is registered; a true `find` returns a 400-character window that is a lead and not a docket. This is the closure route for `wingtech-netherlands-icsid-registration-not-found` and for open thread 1, which is now six weeks past its scheduled check.
4. **The relay allowlist is the binding constraint on the research question, not the search budget.** Today added two stranded on-theme carriers (`dailyjus.com`, `jonesday.com`) to a list the record already runs to at least five inside one week. Six searches produced no readable body outside the 18 allowlisted hosts.

---

## 9. Budget spent

- **Searches: 6 of 6.** Returned unspent: **0** — and I record the reason honestly, since the chairman said he would credit restraint: I did not return budget, I *misspent* one slot (search 5, §7 item 3). Searches 1–4 were topical discovery and returned no matter; search 5 returned the record's own citations; search 6 was the only one that answered a question the record had actually posed.
- **Monitored-author query: 0**, departed from with the authority named and the pinpoint corrected (§4.1).
- **Escalated gaps: 0 searches**, per standing rule.
- **Relay rows read: 6 of 6** (4 research + 2 controls), plus 5 prior rows on `/cases/case-database` and 12 prior rows on `icsid.worldbank.org` paths read from committed reductions for the replication table. **No relay row requested.**
- **Task 1: 0 searches, 0 relay rows**, as budgeted.
- **Nothing patched, built, or applied.** `source_analytics.py`, the health column and every other pending item untouched.

---

## 10. Method note (self-training file)

**What worked.** Reading the *committed code that writes the field* before theorising about the field. Escalation 4 stood for weeks against seats that reasoned about the numbers; it fell in under an hour to `git grep` for the assignment statements plus two arithmetic identities tested on four artefacts. **The generalisation: when an artefact's numbers disagree, find the two assignment statements before you find the story.**

**What worked second.** Enumerating every prior row at a URL across `analytics/fetch-results/*.json` before grading a forecast about that URL. That is what caught C-1, and it cost one Python loop.

**What failed.** Firing a keyword search at a thread the record already holds in depth. **New rule for myself: before spending a search on any named matter, `git grep -icn` the matter's rigid designator across the tree; if the record holds it in more than two files, the search must be aimed at the thread's stated closure condition, not at the matter.** Search 5 hit the matter and returned nothing; search 6 hit the closure condition and returned a usable null.

---

## 11. Candidate claims

The analyst's candidate-claim JSON block is entered verbatim below; it is the input to the
ledger and no seat may edit it in transit.

```json
{"candidate_claims": [
  {"claim_text": "The file digests/2026-09-07_ISDS-Thematic-Watch/meta.json was written three times on 2026-09-07 in the commits this clone holds from tip 63ffc84 - at 18:06:47Z (blob c75622e0), 21:02:29Z (blob 0611390c) and 23:32:09Z (blob a2ea2615) - and the first of those three carries iisd_itn with status 'DEGRADED (9 zero runs)', a refusals array naming https://www.iisd.org/itn/feed/ with outcome 'refused' and detail '403', and refusal_count 1, thereby satisfying all three limbs of the amended prediction, while the third carries the same refusals array and refusal_count under a status of 'QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest 2026-04-21; fetcher reads the listing)'.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/08d67d1/digests/2026-09-07_ISDS-Thematic-Watch/meta.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"status\": \"DEGRADED (9 zero runs)\",",
   "supporting_locator": "Blob c75622e0, source_health[0]. The clone is shallow (git rev-parse --is-shallow-repository -> true), so 'three' is scoped to the 168 commits this clone holds from tip 63ffc84 and is not a whole-history count. The three blobs are cited rather than the path because the path was overwritten in place twice."},

  {"claim_text": "Commit 5453b6a, 'fix(iisd_itn): self-heal the walled RSS feed', was merged at cc5d678 on 2026-09-07T20:54:10Z - after the 18:06:47Z digest run and 8 minutes 19 seconds before the 21:02:29Z digest run - and adds the status-string builder that emits the QUIET label observed in the second and third writes, retaining the string 'QUIET (feed live; newest item ...)' only for the branch where the RSS feed is served.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/commit/5453b6a",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "return (f\"QUIET ({feed_note}; HTML listing live, {len(listed)} items listed, \"",
   "supporting_locator": "Diff of 5453b6a, added line in the iisd_itn source-health probe; merge timestamp from git log -1 --format=%cI cc5d678. This is a claim about the code path that writes the label, not about the state of www.iisd.org."},

  {"claim_text": "digests/2026-08-31_ISDS-Thematic-Watch/meta.json carries iisd_itn as {\"name\": \"iisd_itn\", \"status\": \"QUIET (feed live; newest item 2026-04-21)\", \"count\": 0} with no refusals key and no refusal_count key, which is the pattern the 2026-09-05 amended prediction named as falsifying it outright.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/4cfc45a/digests/2026-08-31_ISDS-Thematic-Watch/meta.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"status\": \"QUIET (feed live; newest item 2026-04-21)\",",
   "supporting_locator": "source_health[0], three keys only. Verified by the analyst directly rather than taken from the chairman's Part I."},

  {"claim_text": "In src/main.py the field per_source is assigned len(fresh) - items not already in the seen state - at line 267, while the source_health entry's count is assigned len(items), the gross fetch, at line 285; and the deferred-queue re-intake is appended to new_candidates at line 341 and counted into total_candidates at line 343, after per_source has been written, with no code path writing it back, so that meta.json's screened field (src/render.py:228) equals the per_source sum plus the deferred re-intake by construction.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/63ffc84/src/main.py",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "stats[\"per_source\"][src.name] = len(fresh)",
   "supporting_locator": "src/main.py:267, 285, 319-321, 337-343; src/render.py:228, 240-243, 250-254. Q is the line at 267; the companion line at 285 reads 'entry = {\"name\": src.name, \"status\": status, \"count\": len(items)}'."},

  {"claim_text": "Across four digest artefacts the identity screened == (increase in state/seen.json entries across the run) + (entries in state/deferred.json after the run) holds exactly: 110 == 91 + 19 at 08d67d1; 28 == 7 + 21 at 2adb925; 30 == 30 + 0 at 238f0f4; and 67 == 59 + 8 at 4cfc45a, and in every one of the four the difference screened minus the per_source sum is less than or equal to the number of entries the deferred queue held before the run.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/238f0f4/state/deferred.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "Computed from state/seen.json and state/deferred.json at each run commit and its parent, and from the per_source and screened fields of the meta.json blob written by that commit. Q is N/A: a cross-blob identity has no single carrying span. The per-source attribution of WHICH deferred entries overlapped the fresh set on 238f0f4 is inference from a fitting partition, not an item-by-item match, and is marked as such in the memo."},

  {"claim_text": "In the 2026-09-08 relay run (run 34219957945, control_ok true, void false, requested 5, completed 5, truncated false, 6 records), https://icsid.worldbank.org/cases/case-database returned status 200 at 75622 bytes with find_matched true for the literal 'ICSID' and find_matched false for the literal 'ARB/', on a byte- and digest-identical body (134c2004) across both rows; and the same URL had already returned find_matched false for 'ARB/' at 75622 bytes (digest 29d733cb) on 2026-09-06 alongside a passing 'Search Cases' control in the same run.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/975f5d2/analytics/fetch-results/2026-09-08-daily.json",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"find\": \"ARB/\"",
   "supporting_locator": "records[3] and records[4] of the 2026-09-08 reduction; records[9] and records[10] of analytics/fetch-results/2026-09-06-daily.json. This is a statement about four fetches and about excerpt_of's script-stripped, tag-stripped extraction (scripts/fetch_relay.py:78, 116-136), NOT about what the page shows a browser, not about any internal JSON API, and not about any case. No case identifier is entered, screened or carried."},

  {"claim_text": "The body served to the relay at https://uncitral.un.org/en/tenth-intersessional-meeting on 2026-09-08T11:18:29Z (status 200, 63029 bytes, sha256 a9468944) contains, in tag-stripped text, the statement that the 10th intersessional meeting of UNCITRAL Working Group III will be held 7 to 9 September 2026 in Ninh Binh, Viet Nam and will focus on the standing mechanisms - the permanent tribunal and the permanent appellate tribunal - and the assessment of damages and compensation; and the literal 'intellectual property' returned find_matched false on that same body in the same run.",
   "source_url": "https://uncitral.un.org/en/tenth-intersessional-meeting",
   "access_status": "partial", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "The 10th intersessional meeting of UNCITRAL Working Group III will be held in Ninh Binh, Viet Nam . The intersessional meeting will focus on the topics of the standing mechanisms - the permanent tribunal and the permanent appellate tribunal - and the assessment of damages and compensation .",
   "supporting_locator": "Row 1 excerpt (400 characters, window centred on the first occurrence of 'UNCITRAL'), analytics/fetch-results/2026-09-08-daily.json records[1]; row 2 is records[2]. The excerpt establishes what its own 400 characters contain and nothing about what lies outside them. The negative on 'intellectual property' is a statement about excerpt_of's search of the returned body, NOT about UNCITRAL Working Group III's work programme."},

  {"claim_text": "A search restricted to icsid.worldbank.org, italaw.com, investmentpolicy.unctad.org, jusmundi.com and iareporter.com for a registered or pending Wingtech v. Netherlands ICSID proceeding returned no such case, only ICSID's own database landing pages and two unrelated Netherlands respondent cases; an ICSID registration is therefore not found in accessible sources as of 2026-09-08, which is Day 146 post-cooling-off on this project's own anchor of Day 86 as of 2026-07-10.",
   "source_url": "https://icsid.worldbank.org/cases/pending",
   "access_status": "unavailable", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "lead",
   "supporting_quote": null,
   "supporting_locator": "Domain-restricted web search, 2026-09-08; no body was retrieved (fetch is dead this session and this row was not in the pre-registered relay batch). Q is null because the proposition is an absence and no document carries it. Recorded per this project's standing form as NOT FOUND IN ACCESSIBLE SOURCES and never as 'no such case exists'. The Day 146 figure is arithmetic on STATE_OF_THE_ANSWER.md:808's own 'Day 86 post-cooling-off as of July 10, 2026'."},

  {"claim_text": "Daily Jus published an article titled 'Subject-Matter Jurisdiction Issues in AI-Related Investment Treaty Cases' dated June 2026, and Jones Day published one titled 'Trade Secret Litigation Trends in Life Sciences' dated May 2026; neither body was retrieved, and dailyjus.com and jonesday.com are both outside the relay's ALLOWED_HOSTS.",
   "source_url": "https://dailyjus.com/legal-tech/2026/06/subject-matter-jurisdiction-issues-in-ai-related-investment-treaty-cases",
   "access_status": "unavailable", "source_authority": "media", "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": "Subject-Matter Jurisdiction Issues in AI-Related Investment Treaty Cases",
   "supporting_locator": "Search-return page titles only. No content of either body is asserted. The search engine composed characterisations of both articles' contents; that synthesis is refused in full and none of it is entered. Both are recorded as stranded carriers under the chairman's escalation 9. ALLOWED_HOSTS enumerated at scripts/fetch_relay.py:48-69, 19 entries of which example.com is labelled 'Neutral control only - never a research source'."},

  {"claim_text": "At commit 63ffc84 the file agents/Workflow Threads.md carries at line 1902 the row '| Roberts / EJIL:Talk monitored-author check | Subsumed by the chairman's 2026-07-30 Part 6 ruling; zero budget | f03a90e |', not at line 1835 as the 2026-09-07 escalation 5 states; and the ruling it references, at analytics/daily-research/2026-07-30.md:220, makes the zero budget conditional - 'until A/81/17 publishes or a Roberts item surfaces by another route'.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/63ffc84/agents/Workflow%20Threads.md",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "| Roberts / EJIL:Talk monitored-author check | **Subsumed** by the chairman's 2026-07-30 Part 6 ruling; zero budget | `f03a90e` |",
   "supporting_locator": "agents/Workflow Threads.md:1902 at 63ffc84, located with git grep -in 'monitored.author' 63ffc84 -- (no path argument); the ruling at analytics/daily-research/2026-07-30.md:220. Line 1835 at this commit concerns the currency guard and does not carry the monitored-author row."},

  {"claim_text": "At commit 63ffc84 the strings 'permanent appellate tribunal', 'assessment of damages and compensation' and 'Pullman' each return zero carriers in a case-insensitive whole-tree screen, while 'Ninh Binh' returns carriers in eight files and 'standing mechanisms' in two.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/tree/63ffc84",
   "access_status": "full", "source_authority": "primary", "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "Instrument: git grep -icn \"<term>\" 63ffc84 -- , no path argument, case-insensitive. scripts/wsgrep_at.py does not exist and was not used. The zeros were validated against a known positive, 'intersessional', which returns carriers in STATE_OF_THE_ANSWER.md and four analytics/ files at the same commit. Q is null: a screen result has no carrying span."}
]}
```

**Files an operator or a later seat will want, all absolute:**
`/home/user/isds-watcher/analytics/fetch-results/2026-09-08-daily.json`, `/home/user/isds-watcher/analytics/fetch-requests/2026-09-08-daily.json`, `/home/user/isds-watcher/digests/2026-09-07_ISDS-Thematic-Watch/meta.json` (blob `a2ea2615`; the confirming state is blob `c75622e0`, reachable only by `git show 08d67d1:` that path), `/home/user/isds-watcher/src/main.py` (lines 267, 285, 319-321, 337-343), `/home/user/isds-watcher/src/render.py` (lines 228, 240-243, 250-266), `/home/user/isds-watcher/scripts/fetch_relay.py` (lines 48-69, 78, 116-136, 195-197), `/home/user/isds-watcher/state/deferred.json`, `/home/user/isds-watcher/state/seen.json`.

---

# Part V — The council discussion, 2026-09-08

## Spend checkpoint, taken after the vetting round as Part I §6 promised

Two seats convened, both real subagents, both returned. Analyst: 6 of 6 searches, 6 of 6
relay rows read, nothing patched. Integrity officer: 0 searches, read-only, nothing patched.
The relay batch cost one workflow run (`34219957945`), 5 requested rows, all `ok`. The
close-out is affordable and is written in full; nothing below is shortened for budget.

**One thing the checkpoint has to record because it is a cost this council keeps paying and
never prices: the officer's vetting was longer and harder than the memo it vetted, and it
found six blocking defects in a memo whose every figure it could not fault.** That is the gate
working, and it is also the reason Rule 0's compliant reading — the chairman writing the
integrity voice — is a rule I will not follow.

## The integrity officer's return, entered VERBATIM

**The chairman wrote none of it.** The only change made in transit is the decoding of HTML
entities introduced by the agent return channel (`&lt;` → `<`, `&gt;` → `>`). Reported model:
**`claude-opus-5` against a directive of Claude Opus 4.8** — escalation 1, day twenty-eight,
disclosed unasked on its first line, as every holder of this seat since 2026-08-12. **No seat
was substituted silently.**

---

**Model: `claude-opus-5`. The operator's directive for this seat is Claude Opus 4.8. REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`. Disclosed unasked; escalation 1, day twenty-eight. Cause believed structural (`model: opus` in `.claude/agents/integrity-officer.md:4` selects a tier, not a version). Never substituted silently.**

Instruments: `git grep -icn "<term>" 63ffc84 --` (no path argument, case-insensitive), `git ls-tree -r`, `git rev-list --count`, `git show <commit>:<path>`, and Python over the committed JSON blobs. `scripts/wsgrep_at.py` does not exist; I built nothing and used git. I did not fetch — curl egress and the fetch service are dead (Part I §2) and no objection below rests on a fetch outcome. Vault taxonomy read at `/home/user/isds-watcher/agents/integrity-officer.md` (29 entries as of 2026-09-07; entry-27 collision still open, day 32; cited by name below, never by number).

---

## BLOCKING

### `0908-B1` — §4.5, §10 and §2's framing: the mechanism was in this record on 2026-08-18. The day's headline is an unscreened first-ness claim.

**Claim.** §4.5: *"§2 above establishes the digest reconciliation mechanism from the committed code and closes escalation 4… **No seat had proposed a cause**; the cause is that `meta.json` publishes three different quantities under two names, and the residual is the deferred-queue re-intake, which is written after `per_source` is closed and never written back into it."* §10: *"Escalation 4 stood for weeks against seats that reasoned about the numbers; it fell in under an hour to `git grep` for the assignment statements."*

**Evidence.** `/home/user/isds-watcher/analytics/daily-research/2026-08-18.md:664-670`, verbatim:

> At `src/main.py:298-311` the re-intake block sets `new_candidates = returning + new_candidates` before `total_candidates` is taken at `:312`, and items in `returning` are **never** added to `per_source`, which is written only at `:236` and `:290`. So `screened ≥ sum(per_source)`, with equality **if and only if the deferred queue contributed nothing that run** — and a third party cannot check that condition from the artefact, because `stats["retried_deferred"]` is written at `:311` and **never rendered into `meta.json`**. I verified all of this myself.

and at `:679-680`: *"`source_health[].count` and `screened` are not the same quantity and were never meant to reconcile."*

That is §2.3's mechanism, §2.2's mechanism, and §2.6's remedy, all three, from the assignment statements, three weeks ago, under the then-current line numbers. The memo cites none of it. The 08-18 seat also recorded that the equality held on **all eight** digests carrying a `per_source` field (2026-06-29 → 2026-08-17) and predicted the exact break condition; 08-31 and 09-07 are that predicted break, and the memo grades them as an unexplained defect.

**Verdict. SUSTAINED, BLOCKING.** Taxonomy: **unscreened first-ness claim** (entry 28) — the mandatory carrier is literals, synonyms, file count, scope and commit, and §4.5 and §10 carry none. Aggravating: §10 "What worked second" says the seat enumerates prior art before grading a claim, and §3.3 shows it doing so. It ran that discipline on the peripheral claim and not on the load-bearing one.

**This does not touch the arithmetic.** I re-derived every figure in §2 independently and it is exact (see the close). What must change is the framing, in four places:
1. §4.5 — delete *"No seat had proposed a cause"*; replace with the citation to `2026-08-18.md:664-670` and state today's increment: the two closing identities on four artefacts, the per-source partition, and the fact that 08-18's predicted break condition has now fired twice.
2. §10 — *"seats that reasoned about the numbers"* is false; a seat reasoned about the code, at the assignment statements, on 2026-08-18, and reached the same result. The stated generalisation was executed three weeks before the memo claims to have discovered it.
3. §2.6 — credit `2026-08-18.md:668-669` for *"`stats["retried_deferred"]` … never rendered into `meta.json`"*.
4. **C-3 must be re-put to the chair in narrowed form:** not *"no seat proposed a cause, and now one is established"* but *"the cause proposed on 2026-08-18 was expressly not asserted, and is now closed arithmetically on four artefacts."* The chair should rule on that question, not the one asked.

### `0908-B2` — §3.4's novelty screen: two counts wrong, the control mis-described, and the first-ness claim refuted by the synonym screen. Candidate claim 11 is false as written.

**Claim.** §3.4: *"`permanent appellate tribunal` → 0 carriers; `assessment of damages and compensation` → 0; `Pullman` → 0. Already held: `Ninh Binh` (8 files), `standing mechanisms` (2 files). **So the excerpt puts two agenda topics into this record for the first time.**"* CC11 asserts the same, with the locator certifying the control *"'intersessional', which returns carriers in STATE_OF_THE_ANSWER.md and four analytics/ files at the same commit."*

**Evidence**, instrument exactly as the memo names it, `git grep -icn "<term>" 63ffc84 --`:

| term | memo | actual |
|---|---|---|
| `permanent appellate tribunal` | 0 | **0 ✓** |
| `assessment of damages and compensation` | 0 | **0 ✓** |
| `Pullman` | 0 | **0 ✓** |
| `Ninh Binh` | 8 files | **13 files** |
| `standing mechanisms` | 2 files | **2 ✓** |
| `intersessional` (the control) | STATE_OF_THE_ANSWER.md + 4 analytics files (= 5) | **17 files** |

No scope reproduces 8: `analytics/daily-research/` gives 9, `analytics/` gives 10, `*.md` gives 11, case-sensitive gives 13.

**Synonym screen — the standing rule (*a zero-hit screen is not absence until the synonym is tried*), which the memo did not run:**

- `appellate mechanism` → **4 files**, including `STATE_OF_THE_ANSWER.md` and `analytics/daily-research/2026-09-07.md`
- `appellate tribunal` → 1 file (`2026-06-26.md`)
- `damages and compensation` → **2 files** (`2026-08-01.md`, `2026-08-05.md`)
- `standing mechanism` (singular) → **5 files**, including `analytics/optimization-log.md`

**Verdict. SUSTAINED, BLOCKING.** The three exact-literal zeros are real. The inference drawn from them is not: *"the excerpt puts two agenda topics into this record for the first time"* is a **topic** claim screened only on exact literals, and both topics are already carried — one of them in yesterday's own record. Taxonomy: entry 28, plus the zero-hit/synonym rule.

**Also an Amendment 2 breach, and it is mine to police:** `Ninh Binh` (nonzero) and `standing mechanisms` (nonzero) are given a bare count with no referent clause, so a nonzero count sits in the sentence reading as corroboration and cannot be checked by re-running the command cited.

**Must change:** correct 8 → 13 and the control to 17 files in §3.4 and in CC11's `claim_text` and `supporting_locator`; delete *"puts two agenda topics into this record for the first time"* and replace with the exact-literal form (*"these three literals are new; the topics are not — `appellate mechanism` in 4 files including `STATE_OF_THE_ANSWER.md` and `2026-09-07.md`, `damages and compensation` in 2"*); add referent clauses for both nonzero counts.

### `0908-B3` — §3.3's stated scope is falsified by its own table, and its prior-art clause miscounts on the instrument it names.

**Claim (a).** §3.3: *"Enumerating every row at `https://icsid.worldbank.org/cases/case-database` across `analytics/fetch-results/*.json` **at tip `63ffc84`**"* — followed by a five-row table whose last two rows are `2026-09-08-daily.json` recs 3 and 4.

**Evidence.** `git ls-tree -r 63ffc84 --name-only | grep fetch-results/2026-09-08` → **absent**. That file first exists at `975f5d2`. The stated enumeration cannot produce the stated table. Taxonomy: **scope-mixed screen**.

**Claim (b).** §3.3: *"`git grep -icn` at `63ffc84`, no path argument: `constant-width` in `analytics/council-log.md` and **five** `analytics/daily-research/2026-08-*.md` files; `same byte count` in `2026-08-21.md`, `2026-08-26.md` and **four** `analytics/fetch-requests/*.json`."*

**Evidence**, same instrument, same commit:
- `constant-width` → 14 files: `council-log.md`, **nine** `2026-08-*` daily records (08-04, 08-05, 08-06, 08-08, 08-09, 08-12, 08-13, 08-22, 08-26), plus `2026-09-06.md`, `2026-09-07.md`, `analytics/fetch-requests/2026-08-13-uncitral-digest-control.json`, `analytics/insights.jsonl`.
- `same byte count` → 10 files: `2026-08-21.md`, `2026-08-26.md`, **seven** `analytics/fetch-requests/*.json` (08-26 … 09-01), plus `analytics/insights.jsonl`.

**Verdict. SUSTAINED, BLOCKING.** Two undercounts and two omitted classes under a receipt that names a whole-tree instrument. Taxonomy: **silent class truncation** into **scope-mixed screen**. With `0908-B2` this is four wrong screen counts in one memo, in the same direction each time — the direction that shortens the prior art. That is a pattern, not a slip, and the chair should treat it as one.

**Must change:** re-scope §3.3's enumeration to `975f5d2` (the substance is correct there — I reproduced all five rows exactly, and note that the discrimination matters: a substring screen on `case-database` returns **133** rows because of the `…/case-detail?CaseNo=` children, and the exact-URL screen is what makes 5 right); correct both prior-art enumerations.

### `0908-B4` — §4.3 and §8 item 3 cite the wrong open thread, and the proposal is aimed at a closure condition it does not satisfy.

**Claim.** §4.3: *"`state/research_log.json` **open thread 1** reads, verbatim: 'Monthly standing watch: does the ICSID pending-cases docket register a Wingtech v. Netherlands proceeding? …'"* §8 item 3: *"This is the closure route for `wingtech-netherlands-icsid-registration-not-found` and for **open thread 1**, which is now **six weeks past its scheduled check**."*

**Evidence.** `state/research_log.json` `open_threads` is a 19-element list. The quoted text is **`open_threads[5]`** and is verbatim-exact — I checked it character-for-character against the file. `open_threads[0]` is a different thread entirely:

> `GAP-UNRESOLVED: icsid-recent-pending-listing-untested-as-intake` — … **CLOSURE CONDITION: one relay row at each URL, and nothing else.**

`open_threads[1]` is the ARB/12/1 revision proceeding. There is no reading of "1" that reaches index 5.

**Consequence, and why this is blocking rather than a pinpoint slip.** §8 item 3 proposes **two rows on `/cases/recent` only** (`ICSID` control + `Netherlands`) and offers them as *"the closure route … for open thread 1."* The actual open thread 1 requires **one row at each of `/cases/recent` and `/cases/pending`**. As written, the proposal is graded against a thread it cannot close and leaves the real thread 1 unserved — while `/cases/pending` is precisely the URL `STATE_OF_THE_ANSWER.md:26` says the ICSID intake question *"now sits entirely on."*

**Also.** *"six weeks past its scheduled check"* — `agents/Workflow Threads.md:1901` schedules it for *"late August"*; today is 2026-09-08, i.e. one to two weeks past. Six weeks is the interval since the 2026-07-28 downgrade, which is not what the sentence says. Taxonomy: **mis-located internal-authority citation**, extended form.

**Must change:** "open thread 1" → "open thread 6 (`open_threads[5]`)" in §4.3, §8 item 3 and the gap-marker block; §8 item 3 either adds the `/cases/pending` row or drops the claim to close open thread 1; "six weeks past its scheduled check" → "one to two weeks past its scheduled late-August check; six weeks since the 2026-07-28 downgrade."

### `0908-B5` — the windowed form is stated against a tip at which it is false. Two carriers.

**Claim.** §1.3: *"over the **168 commits** this clone holds from tip `63ffc84`."* CC1 `supporting_locator`: *"'three' is scoped to the 168 commits this clone holds from tip 63ffc84."*

**Evidence.** `git rev-parse --is-shallow-repository` → `true` (confirmed). `git rev-list --count 63ffc84` → **167**. `git rev-list --count 975f5d2` → **168**. `git rev-list --count ffdb6fb` → 169.

**Verdict. SUSTAINED, BLOCKING (mechanical, one-token fix).** The windowed form was adopted (Part I §3, from yesterday's next step 4) precisely so a shallow-clone denominator can be re-run by a third party. This one fails when re-run. The count is right for `975f5d2` and the tip is right for the enumeration; they are paired wrongly. **Must change:** state `167` against `63ffc84`, or `168` against `975f5d2`, in both carriers.

### `0908-B6` — Part I §3, against the chair: "the first new digest artefact in eight council sessions" is an unscreened ordinal, and it is seven on the chairman's own definition.

**Claim.** Part I §3: *"This is the **first new digest artefact in eight council sessions** — the digest of record was `2026-08-31` from 2026-08-31 through yesterday."*

**Evidence.** No literals, no scope, no commit — in direct breach of the rule the same Part imposes eleven lines later at `:111-113`: *"Any count, ordinal, 'every', 'first', 'second' or 'only' in background prose carries its literals, scope and commit in the adjacent sentence, **or it does not go in**."* This is the class yesterday's next step 7 was adopted to stop, in background prose, in the document that adopts it.

And on the chairman's own stated definition it is **seven**, not eight. `git ls-tree -r 63ffc84 --name-only`: the `2026-08-31` digest `meta.json` landed at `4cfc45a`, **2026-08-31T19:29:12Z**, while the 2026-08-31 council record was committed at `7ac387b` **2026-08-31T12:48:51Z** (and `806d1f5` at 12:46:32Z) — six hours and forty minutes *earlier*. During the 08-31 session the digest of record was `2026-08-24`. The clause *"the digest of record was 2026-08-31 from 2026-08-31"* is therefore false at its near end, and the sessions for which 08-31 was the digest of record are 09-01 through 09-07 — **seven**.

**Verdict. SUSTAINED, BLOCKING against Part I.** Taxonomy: entry 28. The analyst did not catch it. **Must change:** either carry the receipt (`analytics/daily-research/*.md` at `63ffc84`, sessions 09-01 → 09-07, seven, with the 08-31 landing time) and say seven, or strike the clause.

---

## NON-BLOCKING

**`0908-N1` — CC9 (Daily Jus / Jones Day): unverifiable URL and search-return dates asserted as publication dates.** The `source_url` `https://dailyjus.com/legal-tech/2026/06/subject-matter-jurisdiction-issues-in-ai-related-investment-treaty-cases` is a slug-shaped URL for a jurisdiction article filed under `/legal-tech/`; nothing in the memo says whether it is the engine's returned href or a reconstruction. I cannot fetch (Part I §2), so I cannot resolve it, and I will not treat my inability as evidence about the resource. **This is the seat's standing fabrication-risk flag: a plausible-but-unconfirmed URL.** The analyst must certify the href is the search return character-for-character, or drop the URL to null. Likewise *"dated June 2026"* / *"May 2026"* are search-return display values and must read as such, not as publication dates (calibration rule 2). The memo's *"title-level leads and nothing else. I read no body"* is correct and stands.

**`0908-N2` — CC8 field labels overstate the evidence.** `source_authority: "primary"` with `source_url: https://icsid.worldbank.org/cases/pending` on a claim whose only evidence is a domain-restricted web search, on a page that was not retrieved. The `supporting_locator` is honest (*"no body was retrieved"*); the fields are not, and the gate reads the fields (`src/integrity_gate.py:111-114`). `source_authority` should be `unknown`. All eleven claims otherwise use valid vocabulary (`CLAIM_TYPES`/`ASSERTION_STATUSES`/`ACCESS_STATUSES`/`AUTHORITIES`, `src/integrity_gate.py:36-39`) — I checked each.

**`0908-N3` — pinpoint drift, four instances, none substantive.** §1.1 cites `2026-09-05.md:1310-1319`; the blockquote is **1311–1319** (1310 is blank). §2.5 cites `src/main.py:305`; `if not only:` is at **306** (355 is right). §2.6 cites `src/main.py:340` for `retried_deferred`; the assignment is at **342** (340 is `carried = …`). §2.4 and CC4 cite `src/render.py:250-254` for *"the raw item count it yielded"*; that phrase is at **248**. Fix the pinpoints; the propositions all verify.

**`0908-N4` — §2.4 overstates what Identity B tested.** *"Two identities, tested on four artefacts, and they hold exactly."* Identity B's equality half is definitional, and §2.3 says so itself (*"`screened = per_source_sum + |returning|` **by construction**"*); the `returning` column in the table **is** `screened − per_source_sum` in all four rows, so the equality cannot fail. Only the `≤ |deferred before|` bound was tested (8≤8, 7≤19, 16≤29, 23≤24 — all hold). **Must change:** say that B's equality is derived and its bound is the test. Identity A is a genuine test and does discriminate against the double-count hypothesis §2.5 uses it to rule out, so that ruling-out stands.

---

## RULINGS ON THE ANALYST'S CHALLENGES TO THE CHAIR

**C-1 — SUSTAINED, verified independently and wider than filed.** Exact-URL enumeration across every committed reduction reproduces the analyst's five-row table cell for cell (subject to `0908-B3`'s scope correction). `analytics/fetch-results/2026-09-06-daily.json` recs 9 and 10: `Search Cases` matched / `ARB/` unmatched, 200, 75,622 B, `29d733cb`, `control_ok: true`, head and tail controls both `ok` — exactly as the analyst states. `analytics/insights.jsonl:143` and `STATE_OF_THE_ANSWER.md:26` verify verbatim, including *"unreachable **by construction**"* and *"it now sits entirely on two URLs never fetched."* The frozen note's three quoted strings verify verbatim from `analytics/fetch-requests/2026-09-08-daily.json`'s `note`. **The wider finding:** the 2026-09-06 request note ran this very screen and recorded the result — *"THE RELAY HAS FIRED AT THAT URL EXACTLY ONCE, AND I FOUND IT BY SCREENING RATHER THAN BY REMEMBERING — I had drafted the sentence 'no seat has ever fired the relay at that URL' and my own entry-28 screen falsified it before this file was pushed."* Today's note is therefore not an unscreened claim; it is a **regression against a screen this council had already run, recorded, and published two days earlier.** That is worse than the analyst filed it.

**C-2 — SUSTAINED.** Verified end to end: three commits (`08d67d1` 18:06:47Z, `2adb925` 21:02:29Z, `238f0f4` 23:32:09Z), three distinct blobs (`c75622e0…`, `0611390c…`, `a2ea2615…`), ancestry linear. The first carries `"status": "DEGRADED (9 zero runs)"` **and** the refusals array **and** `refusal_count: 1` — all three limbs. All three blobs carry the refusals array and `refusal_count: 1` byte-identically. "Grade against this path and NOTHING else" was an instruction to read a mutable register by path.

**C-3 — must be re-put in the narrowed form set out in `0908-B1` before the chair rules.**

**C-4 — SUSTAINED, and wider, and the widening is against my own seat.** `agents/Workflow Threads.md:1835` carries the currency guard, and `:1902` carries the monitored-author row, verbatim as the analyst quotes it; `analytics/daily-research/2026-07-30.md:220` carries the ruling verbatim including its release condition. I then checked drift: `:1835` was the currency-guard line at **all six** of the most recent commits touching that file (`4173537`, `09dafc9`, `a694fc5`, `eeb8ff1`, `fb2546c`, `4eb3fc9`), spanning all of 2026-09-07. **Yesterday's integrity officer certified at `analytics/daily-research/2026-09-07.md:793` that *"`research_analyst.txt:158-163` and `agents/Workflow Threads.md:1835` do conflict, verbatim."* That certification was false when it was written.** Escalation 5 has therefore been carried to the operator on a pinpoint this seat affirmatively verified and got wrong. That goes in the accountability line.

---

## THE CHAIRMAN'S REGISTER FINDING — reproduced exactly, and your instrument is the wrong unit for the reason that matters

Re-derived at `975f5d2` with Python over `git show 975f5d2:state/council_log.json` and `:analytics/council-log.md`.

**Your arithmetic is exactly right.** 17 distinct dates in the JSON; 40 distinct dates in the markdown; 12 in both; JSON-only = `2026-08-19`, `08-20`, `09-03`, `09-04`, `09-05` (5); markdown-only = 28, including `2026-09-06` and `2026-09-07`. Every figure reproduces.

**Two corrections, one of which strengthens your conclusion and one of which narrows it.**

1. **The unit.** A `^## <date>` grep is nearly right but discards the discriminator. The headings are `## <date> — <label>`, and the renderer at `src/council_log.py:142-181` emits exactly two labels: `— weekly council (issue #N)` and `— daily check-in`. **There are zero `— daily check-in` headings in the file.** Every one of the 41 headings is `— weekly council (issue #N)` (6) or `— daily council meeting` (35), and the latter is not a form the renderer can produce. So the register holds **18 JSON entries / 41 markdown headings**, both duplicating `2026-06-29`, and the date-only grep merges those two genuine weekly sessions into one.
2. **"12 in both" overstates render fidelity by half.** Only the **6 weekly** JSON entries actually render. All **12 daily-type** JSON entries are unrendered — including the seven (`08-06`, `08-21`, `08-22`, `08-23`, `08-24`, `08-30`, `08-31`) whose dates appear in the markdown only because a *handwritten* `— daily council meeting` section happens to sit on the same date. The true JSON→markdown render rate is 6 of 18, not 12 of 17.
3. **Narrowing, in the other direction.** The 28 markdown-only dates are **not a defect**: `src/council_log.py:112-140` (`_handwritten_sections`) deliberately preserves hand-written blocks across a rewrite, and its docstring says so. `COUNCIL.md:110`'s *"rendered to"* is not simply false — a renderer exists and runs. The defect is narrower and sharper than "the two registers have diverged": **the renderer has a daily branch that has never produced a single line in this file**, so every daily JSON entry is write-only. That is what should go to the operator, and it is a code finding, not a bookkeeping one.

You do not have it wrong. You have it right at a unit that hides the mechanism.

---

## WHAT I VERIFIED AND COULD NOT FAULT

- **§1.1's pre-registration paste is exact.** `diff` of `2026-09-05.md:1311-1319` against `2026-09-08.md:156-164` is **byte-identical**, raw and whitespace-normalised. Only the line range in the citation is off (`0908-N3`).
- **§1.3 in full.** Three commits, three blob SHAs, three `%cI` timestamps, and the first blob's `DEGRADED (9 zero runs)` with the refusals array and `refusal_count: 1` — every element reproduces. The day's headline is correct.
- **§1.5's causal claim.** `5453b6a` merged at `cc5d678` **2026-09-07T20:54:10Z** (= 16:54:10-04:00) ✓; 2h 47m 23s after run 1 ✓; 8m 19s before run 2 ✓; 2h 37m 59s before run 3 ✓. Both quoted added lines are verbatim in the diff, and the diff does exactly what the memo says — it removes the old top-level `QUIET (feed live; …)` return and re-adds it inside the feed-served branch only.
- **§1.5's 08-31 comparator.** `4cfc45a`: `{"name": "iisd_itn", "status": "QUIET (feed live; newest item 2026-04-21)", "count": 0}` — three keys, no `refusals`, no `refusal_count`. The named falsifying pattern, exactly.
- **§2 arithmetic, all of it, independently.** 09-07 at `a2ea2615`: `screened` 30 / `per_source` 14 / health 32. 08-31 at `4cfc45a`: 67 / 44 / 44. The per-source split (italaw 12/0, unctad_isds 5/0, pca_press 3/2 → 18 = 32−14) reproduces. Every `src/main.py` and `src/render.py` line quote is verbatim at `63ffc84`. `retried_deferred` is confirmed absent from `meta.json`'s key list.
- **Identity A on all four runs**, from `state/seen.json` and `state/deferred.json` at each commit and its parent: 110 = 91+19; 28 = 7+21; 30 = 30+0; 67 = 59+8. Exact. Deferred-before counts 8 / 19 / 29 / 24 all reproduce.
- **§2.4's per-source table reproduces cell by cell** against `7b5866e` → `238f0f4`: bing_news 3, gdelt 5, gmail_scholar 6, unctad_isds 3, google_alerts 1, iareporter 10, pca_press 2; total Δseen 30; deferred-before 2/5/6/3/1/10/2, total 29.
- **§7 item 4's self-reported weakness is stronger than the analyst left it, and I closed it.** The analyst declined the item-by-item match. I ran it: **all 29 deferred `(source, source_id)` keys at `7b5866e` are in the 30 newly-seen keys at `238f0f4`** — the queue drained completely, exactly one item was genuinely new, and the fitting partition is now item-verified rather than inferred. The 13/16 split resolves uniquely from that. **The inference the analyst flagged against itself is correct.**
- **§3.1 and §3.2 against the committed reduction.** `request`, `environment` (run 34219957945), `fetched_at`, `control_ok: true`, `void: false`, `requested: 5`, `completed: 5`, `truncated: false`, 6 records — all verify. The manifest `note` is byte-identical to the request file's `note` (9,975 bytes each). Both controls `outcome: ok`, 200, 559 B, `ff67a9d7…`. Rows 1–4 verify on status, bytes, digest and `find_matched`. **I nearly filed a false objection here:** the records carry no `ok` key, so my first read showed `ok = None` for every row and I drafted a quote-integrity flag. The schema check showed `outcome: "ok"` on all six and the memo's `ok/200` shorthand is exact. Reported against myself, per the positive-control discipline that exists for precisely this.
- **§3.4's excerpt is byte-identical to `records[1].excerpt`** in the committed reduction, all 400 characters. `STATE_OF_THE_ANSWER.md:844` carries *"September 7–9 intersessional (Ninh Binh, Vietnam)"* as stated.
- **§4.1's authorities.** `Workflow Threads.md:1835` is the currency guard; `:1902` and `:1901` carry what the memo says, verbatim, including markdown emphasis; `2026-07-30.md:220` carries the ruling verbatim with its conditional release.
- **§4.3's Day 146.** `STATE_OF_THE_ANSWER.md:808` carries *"Day 86 post-cooling-off as of July 10, 2026"* ✓. 2026-07-10 → 2026-09-08 is 60 days ✓; 86+60 = 146 ✓; cross-checked against the same file's independent *"Day 104"* anchor at 2026-07-28 (April 15 + 104) — both resolve to an April 15 origin and agree. The **NOT FOUND IN ACCESSIBLE SOURCES** form is used correctly and the memo never states that no such case exists.
- **§4.2's allowlist.** `scripts/fetch_relay.py:48-69` is the exact span; 19 entries; `example.com` carries *"Neutral control only — never a research source."* 18 research hosts ✓. `_TAG_RE` at `:78` ✓ (CC6).
- **§4.4's Ecuador screen** at its own declared scope: the slug carries in `2026-09-05.md`, `2026-09-06.md`, `2026-09-07.md` and no earlier daily record ✓. Scope correctly declared; no scope-mix.
- **§3.4's binding reading rule is honoured** — no case number, party, tribunal or matter is entered from any excerpt. §3.3's refusal to convert the byte-length coincidence into a finding, and §3.4's refusal to read row 2's negative as evidence about WG III's work programme, are both correct and both are the discipline this council most often loses. §4.3's three-ring deflation is honest: Ring 1 correctly refused, the near-miss correctly left a near-miss.

**On the taxonomy, one honest positive:** `.claude/agents/integrity-officer.md` carries no enumeration of patterns today, so the R8 failure mode named in this seat's mandate — a stale list in the definition — is not live in that file. The vault table is current to 2026-09-07 at 29 entries. The entry-27 collision is day 32 and remains the chair's and this seat's to settle.

---

## TAXONOMY EXTENSION — one new pattern, proposed by name

**WINDOWED-FORM TIP MISMATCH.** A count stated in the mandated windowed form — *"the N commits this clone holds from tip `<X>`"* — where N is the count at a **different** tip, so the very form adopted to make a shallow-clone denominator third-party-checkable is the thing that fails when checked. Distinguished from the shallow-clone defect it was written to cure (an unqualified whole-history denominator): here the qualification is present, correctly worded, and internally inconsistent, which is harder to see because the sentence displays compliance. **Countermeasure:** the tip named in the sentence is the tip the count was taken at; `git rev-list --count <tip>` is re-run against the literal string in the sentence before it ships. **Instance:** `0908-B5`, two carriers (`§1.3`, CC1 `supporting_locator`), 168 against `63ffc84` where `63ffc84` returns 167 and 168 is `975f5d2`.

Off the table I actually read (`agents/integrity-officer.md:114`, *"29 entries as of 2026-09-07"*), the next free number is **30**. I do not renumber and I do not adopt; I propose it by name, per the vault's standing instruction, and the chair adopts or does not.

---

## DISPOSITION

**FLAGGED. Six blocking objections, four non-blocking, all with receipts.** Four blocking are against the analyst (`B1`–`B5`, less `B5` which is mechanical), one against the chair (`B6`), and the widening of `C-4` is against this seat.

The memo's substantive work is sound and I could not fault a single figure in it: §2's mechanism closes, §1.3's headline is right, §1.5's causal chain verifies to the second, C-1 and C-2 are correct and I have made C-1 wider. **What fails is the memo's account of its own novelty.** Four screen counts are wrong in the same direction, a three-week-old record already held the mechanism the memo calls the day's finding, and a first-ness claim survives only because the synonym was not tried. That is one failure mode, not four: **this memo screened everything except itself.**

Files: `/home/user/isds-watcher/analytics/daily-research/2026-09-08.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-18.md` (lines 664–680, the prior mechanism), `/home/user/isds-watcher/state/research_log.json` (`open_threads[0]`, `[5]`), `/home/user/isds-watcher/src/council_log.py` (lines 112–181, the unfired daily render branch), `/home/user/isds-watcher/analytics/fetch-results/2026-09-08-daily.json`, `/home/user/isds-watcher/analytics/fetch-results/2026-09-06-daily.json`, `/home/user/isds-watcher/agents/integrity-officer.md`.

---
