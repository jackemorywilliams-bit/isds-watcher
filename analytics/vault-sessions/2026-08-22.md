---
aliases: [Vault Session 2026-08-22]
tags: [vault-session, archivist]
hub: Evidence Ledger
---
# Vault session — 2026-08-22 (tenth archivist deployment)

**Audited against `c4f6825` (`main`, clean tree), on a complete 748-commit history.** The
container's clone arrived shallow again — 196 commits — and every ancestry and guard result
below was re-derived after `git fetch --unshallow`. §6 explains why that mattered more this time
than last.

The headline is on your public website, and a reader can see it without scrolling: the page says
nine sources directly above a chart that says ten. It has been live for five days. The finding
against me is next to it — my own claim map had already recorded that row as fixed, on the
strength of a commit message rather than a reading of the files.

---

## NEEDS YOU — 1. The site tells Dr. Benavides there are nine sources, above a chart saying ten

**What a reader sees on one screen.** On `docs/how-it-works.html`:

| Line | What it says |
|---|---|
| `:58` | "Read it top to bottom. The first band is the **nine public sources** checked" |
| `:96` | the inlined chart, whose banner renders "**WHERE WE LOOK — THE 10 SOURCES, CHECKED EVERY RUN**", with ten chips |
| `:7` | meta description: "the **nine** public sources" |

Thirty-eight lines apart, on the same page, the prose and the picture it introduces contradict
each other. The meta description is what a search result and a link preview show.

**Which half is right: the chart.** `src/sources.all_sources()` returns **10** instances —
`iisd_itn`, `google_alerts`, `gmail_scholar`, `italaw`, `icsid`, `iareporter_headlines`,
`unctad_isds`, `pca_press`, `bing_news`, `gdelt`. `node tools/isds-workflow-3d/validate.mjs`
exits 0 reporting "10 source chips, 30 cards, 44 edges". `README.md:56` says ten.

**Where it comes from, and why it is a two-string fix.** The chart banner is generated from
`views/isds-workflow-3d/workflow.json` and moved to ten automatically. The two "nine" strings are
hand-written English in the Jinja template — `scripts/site_templates/how_it_works.html.j2:3` and
`:27` — and `docs/how-it-works.html` is the faithful build of them. Nothing needs re-deriving;
two sentences need editing and the site needs rebuilding.

**Live since `44550ca` (2026-08-17) — five days.** That commit's own message states "Every
nine-sources surface moves to ten in the same change ... README, rebuilt view.js/SVG/site."
Two surfaces did not move.

**Why no guard caught it.** `scripts/check_site_sync.py` compares the built `docs/` against the
templates. Here they agree perfectly — and both are wrong. The guard is working as designed; the
class of defect is outside it.

**Owner: [[site-experience]]**, which owns `scripts/site_templates/` and `docs/`. Escalated, not
edited — I do not write to either path. Recorded as [[Claim Map]] **C11**.

## TO VERIFY — 2. This is my failure before it is the site's, and it is the same failure twice

[[Claim Map]] exists for exactly this: one row per claim the project makes about itself, listing
every file that states it. **C11 is the source-count row. It was never re-read.**

Worse, on 2026-08-17 I wrote into that map's snapshot-anchor block: *"C12's source-count row is
satisfied by that same commit (ten stated once, everywhere)."* That sentence is wrong twice.

1. **Wrong row.** The source-count row is **C11**. C12 is Model assignments.
2. **Wrong fact.** The row was not satisfied, and the words "ten stated once, everywhere" came
   from `44550ca`'s commit message — not from opening the four files C11 names.

So the map recorded a fix that had not happened, and the row that would have caught the site went
on saying "nine" in every cell while the anchor above it said the matter was closed. **A note was
real, correct in form, and false in substance** — the 2026-08-03 failure mode in a new costume.

**The rule this deployment adopts:** *a commit message is a claim about a change, never a reading
of the files after it; a claim-map row is closed only by re-reading every cell it names.* It is
the 2026-08-16 rule — "a guard's exit 0 licenses only the claim the guard actually tests" — in a
second form, and both are now in `agents/obsidian-archivist.md` and the change log.

**Fixed here.** The erroneous sentence is struck in place with its original wording quoted, C11
is re-read cell by cell at `c4f6825`, and the superseded 2026-08-08 table is folded into a
collapsed block rather than deleted.

## FOR EMORY — 3. `METHODOLOGY.md:33` describes a nine-source fleet and an eight-query Bing lane

Your methodology memo enumerates the collection channels one by one — ICSID, UNCTAD, italaw,
IISD ITN, IAReporter, PCA press, Bing News, plus your Google Alerts and Google Scholar feeds.
That is **nine**, and **GDELT is not among them**. It landed at `44550ca` (2026-08-17) as
`src/sources/gdelt.py`.

The same sentence says Bing News is "polled through **eight** fixed, fingerprint-derived
queries". `src/sources/bing_news.py:23` defines `QUERIES` with **12** entries — `44550ca` moved
the intent of three retired Google Alerts queries and an italaw-outage backstop into that lane.

Both numbers read from the code at `c4f6825`, not from the commit message this time.

This matters more than a count because `:33` is the paragraph that grounds the memo's
methodological claim — triangulation across independent collections, and the "lead-generation
floor" framing. An undisclosed tenth channel is a described method that does not match the
executed method, in the document a reviewer reads to judge the method.

**Yours** — `METHODOLOGY.md` is your own prose and I do not edit it. Opened as
[[Workflow Threads]] **D7** with both numbers and their code locations, so it is a two-number
edit rather than a re-derivation.

## ACTION — 4. D6 is at day ten. Ten consecutive days, one two-line fix

Your integrity officer has now reported the model gap against itself on **ten consecutive days**,
unbroken since 2026-08-12 — six of them since my last session:

| Date | Where | What it says |
|---|---|---|
| 2026-08-17 | `analytics/daily-research/2026-08-17.md:719` | "DISCREPANCY" |
| 2026-08-18 | `2026-08-18.md` | same |
| 2026-08-19 | `2026-08-19.md:1148` | "SEVENTH consecutive day" |
| 2026-08-20 | `2026-08-20.md:1113` | eighth; also raised as escalation `N2` |
| 2026-08-21 | `2026-08-21.md:1024` | ninth |
| 2026-08-22 | `2026-08-22.md:1080` | tenth |

**I re-observed it from this seat today**, six days after the first time: the runtime reports
`session_context.model` = `claude-opus-5` and `last_served_model` = `claude-opus-5`, against
`agents/obsidian-archivist.md`, which pins `claude-opus-4-8`.

`git log d997c32..HEAD -- .claude/agents/ src/models.py` returns **no commit**. Nothing has
changed. `scripts/check_models.py` still exits 0 over twelve cards, which — per the rule above —
licenses only the claim that three declarations agree with one another.

The officer's own 2026-08-19 wording is the right reading and I will not improve on it: a seventh
identical observation "carries no information about the cause; it is information about how long a
two-line fix has gone unmade."

**Yours**, because `.claude/agents/` is a contract surface. Either correct the five 4.8-documented
seats to Opus 5, or pin `claude-opus-4-8` explicitly so your directive binds. I have again left
the Model lines themselves unchanged, for the reason given on 2026-08-16: rewriting them would
ratify a substitution nobody authorised.

## ACTION — 5. F1 is at day twenty-six, with the same three counts a fourth time

Re-replayed from both blobs by `event` key, not carried forward from any report:

| | `claim_created` | `verification_changed` (marks) | distinct ids |
|---|---|---|---|
| `origin/main` | 37 | **21** | 37 |
| `origin/chore/operator-marks-2026-07-27` | 40 | **38** | 40 |

`main`'s ledger is still last written by `8891c21` (**2026-07-27**) — twenty-six days with no
commit touching it. The branch still fails
`git merge-base --is-ancestor origin/chore/operator-marks-2026-07-27 origin/main`.

Identical figures recorded on 2026-08-04, 2026-08-13, 2026-08-16 and now here. Seventeen marks
and three claims you made in the 2026-07-27 sweep have never reached `main`, so the integrity
gate resolves three claims you ruled on as `unverified`, and the adverse `scope_ok: false` half
is stranded with the favourable half.

**Yours alone** — the ledger is append-only and operator-owned. Either merge the branch or re-run
the marks.

## 6. Orphan check — nothing new stranded, and a guard hazard worth more than the result

**Six branches fail `git merge-base --is-ancestor <tip> origin/main`** — the same six as
2026-08-13 and 2026-08-16, with no new ones. I tested every one of the 89 remote branches, and
this time also tested whether each orphan's *content* had reached `main` by another route:

| Branch | Content | Reached `main`? |
|---|---|---|
| `council/2026-08-13` | a `.sent` marker | yes — `analytics/daily-research/.sent/2026-08-13` present |
| `claude/sweet-mccarthy-8mouy6` | daily research 2026-07-30 | yes — file present |
| `claude/sweet-mccarthy-i95s3k` | daily research 2026-07-31 | yes — file present |
| `claude/sweet-mccarthy-d5kgmw` | daily research 2026-08-01 | yes — file present |
| `feat/research-council` | a METHODOLOGY heading + built site | yes — heading present |
| `chore/operator-marks-2026-07-27` | 17 operator marks, 3 claims | **NO — this is F1, above** |

All eight `vault/*` branches are confirmed ancestors of `origin/main`. **The failure this
standing session exists to catch has not recurred; my own memory is landing.** One branch still
carries content the record needs, and it is the one that has carried it for twenty-six days.

**The hazard, which is the part worth keeping.** Run against the shallow clone this container
started with, `scripts/check_currency.py` reported **7** failures — including three hard `FAIL`s:
`373cce6`, `9efafb0` and `ae42639` "is not a commit". **All three commits exist.** The shallow
clone simply could not see them. After `git fetch --unshallow` the same guard reports **4**
failures and **zero** `FAIL`s.

A fail-closed guard fabricating three defects is bad; the direction is worse. A session that
trusted the shallow reading would have spent itself chasing three phantom missing commits while
the four real stale anchors sat behind them. CI is unaffected —
`.github/workflows/pipeline-guards.yml:172` sets `fetch-depth: 0` for exactly this job — so the
hazard is agent-session-only. **Unshallow before running any guard that resolves a sha.** Written
into [[Workflow Threads]]' snapshot block. This extends the 2026-08-16 caution, which covered
`git log` and not the guards.

## 7. Agent-context audit — no contract moved, and D5 is unchanged after nine days

- `git log d997c32..HEAD -- .claude/agents/ prompts/ src/models.py` returns **no commit**. No
  seat's model, prompt binding or contract changed in this window.
- `scripts/check_models.py` exits 0 over twelve cards. `src/models.py` unchanged:
  `CHAIRMAN_MODEL`/`HEAVY_MODEL` `claude-opus-5`, `UTILITY_MODEL`/`FALLBACK_MODEL`
  `claude-opus-4-8`, `DIGEST_CLASSIFIER_MODEL` `claude-haiku-4-5-20251001`.
- **D5 re-tested, unchanged after nine days.** Eight of nine definitions still never name their
  seat's own vault note; [[integrity-officer]] remains the one that does. The adopted method
  rules each seat is bound by still sit where that seat does not read.
- Guards run clean: `check_lock`, `check_headline_lane`, `check_claims`, `check_seen_integrity`,
  `check_telemetry_privacy` all exit 0. `validate.mjs` exits 0 at 30 cards / 10 chips / 44 edges.

**D8 opened — a second "Zero-cost" binding, missed by my last two sessions.**
`.claude/agents/site-experience.md:20` reads "Zero-cost, no new services." I had been carrying
only `.claude/agents/systems-designer.md:17` since 2026-08-16. A `grep -rn` over
`.claude/agents/` and `README.md` returns exactly three hits: those two bindings, and
`README.md:6`, which records that the claim was corrected on 2026-08-08 because the classifier
makes paid model calls. **Two seats are told a cost constraint the project has already
established it does not meet.** Contract surface — yours; carried, not fixed.

## 8. Deliverable drift and vault hygiene — what I fixed

- **`HANDOFF.md` sources table did not carry GDELT**, five days after the module landed, under a
  heading reading "current reality". Added the `gdelt` row citing `src/sources/gdelt.py` and
  `44550ca`; corrected the `bing_news` row to record 12 queries rather than the retired 8; added
  a measured count line naming all ten instances `all_sources()` returns and saying plainly which
  two the table's inherited build-time-scout scope omits.
- **A broken wikilink in my own note.** `agents/obsidian-archivist.md:706` wrapped
  `[[Workflow Threads]]` across a line break, so Obsidian resolved nothing and
  `build_graph.py --dry-run` listed this note under "links to nonexistent notes". Rewrapped; the
  dry run no longer lists it.
- **Currency: 4 failed → 2.** [[Agent Registry]], [[Claim Map]], [[Workflow Threads]] and
  [[Project Change Log]] restamped to `c4f6825` with their declared paths.
- **`agents/Workflow Threads.md` header snapshot refreshed** from its 2026-08-13 reading. Two of
  its counts were superseded by measurement: the validator now reports **10** source chips, not
  9; `build_graph.py --dry-run` now plans **34** managed-block updates, not 27. The 2026-08-13
  figures are left standing as the dated reading they were.

## 9. What I could NOT verify, stated rather than omitted

- **`STATE_OF_THE_ANSWER.md` is the one currency failure I left**, three commits stale against
  `e170ba5`. It is [[research-analyst]]'s living memory, and the 2026-08-08 decision not to
  restamp another seat's file still governs. Deliberate, not missed.
- **The test suite was not run.** No `src/`, `tests/` or `scripts/` commit landed in this window
  beyond `44550ca`'s, so the 2026-08-13 clean-clone reading (562 passed / 1 environmental failure
  / 3 skipped / 5 xfailed) is carried forward rather than re-measured. It remains a fact about
  one machine, and `tests/test_gdelt.py` is untested by me.
- **`scripts/check_site_sync.py` — not run**, sixth consecutive session. Per **B9** it rebuilds
  `docs/` in place before diffing, so running it is a write. Site/`docs/` sync stays unverified —
  and §1 is a defect it could not have caught anyway.
- **`scripts/build_graph.py` write mode — not run, fifteenth consecutive session.** `--dry-run`
  plans managed-block edits to **34** notes, up from 27, and some fall outside this seat's merge
  set. Still needs a `--paths` flag or your own run. See **C12**.
- **I cannot see which model actually served each token of this session.** The runtime reports a
  configured and a last-served model; that is what I recorded, and it is not the same as
  instrumenting the calls.
- **Whether all ten sources are currently returning** is unverified, and C11 says so. The flat
  count conceals concentration either way: `analytics/instrument-map-2026-08-08.md` §1 records 10
  of 14 published entries coming from `iareporter_headlines` alone.
- **No external PKM research this session.** The self-training mandate's research half was not
  performed, and I will not dress an internal finding as external practice. The audit half was
  performed and is §1–§8.

## TO VERIFY — 10. This session's own cadence slipped, and nothing reported it

This is a standing **every-3-days** session. The previous record is
`analytics/vault-sessions/2026-08-16.md` (`5138312`). Today is 2026-08-22 — **six days**.
`git log --all -- 'analytics/vault-sessions/2026-08-19*'` finds nothing on any branch, so the
2026-08-19 session did not run and did not partially run.

The emailer is not at fault and is correctly designed: `analytics/vault-sessions/README.md`
states it "runs daily and asks only 'is anything unsent?', so ... a session that never ran sends
nothing rather than a false all-clear." That is the right behaviour — but the consequence is that
**a skipped session is silent**. You would have learned of it only by noticing an email that
never came.

Everything in §1–§5 above sat unexamined for those three extra days, including a public-facing
contradiction that was already live. Worth a decision from you: either accept the silence, or add
a check that notices a missing record. I am not building one unasked.

## 11. What changed, by file

- **`agents/Claim Map.md`** — C11 re-read cell by cell and marked DIVERGENT with the site
  contradiction quoted from both lines; the 2026-08-17 anchor sentence struck in place with its
  original wording preserved and both its errors named; superseded 2026-08-08 table folded into a
  collapsed block; anchor moved to `c4f6825`.
- **`agents/Workflow Threads.md`** — header snapshot refreshed to `c4f6825` with the shallow-clone
  guard hazard; **D7** opened (METHODOLOGY source and query counts); **D8** opened (second
  "Zero-cost" binding); **D6** re-measured at day ten with all six intervening citations; **F1**
  re-measured at day twenty-six.
- **`agents/Agent Registry.md`** — 2026-08-22 change-log entry, anchor moved to `c4f6825`; D5, D6
  and D8 recorded. Roster rows and Model lines unchanged, deliberately.
- **`agents/Project Change Log.md`** — 2026-08-22 entry, every line citing a hash or path; anchor
  moved to `c4f6825`.
- **`agents/obsidian-archivist.md`** — second runtime observation of the model gap recorded at the
  Model block; tenth-deployment change-log entry; the adopted rule about commit messages; broken
  wikilink at `:706` repaired.
- **`HANDOFF.md`** — `gdelt` row added, `bing_news` row corrected to 12 queries, measured
  ten-instance count line added under the sources heading.
- **`analytics/vault-sessions/2026-08-22.md`** — this record.

Nothing outside `analytics/`, `agents/`, `moc/` and `HANDOFF.md` was written. `METHODOLOGY.md`,
`.claude/agents/`, `scripts/site_templates/` and `docs/` were read and escalated, never edited.
