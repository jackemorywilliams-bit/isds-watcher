# ISDS Thematic Watcher — daily council meeting, 2026-09-09

Chairman: `claude-opus-5` (operator model directive, 2026-08-03).
Session opened 2026-09-09, scheduled cloud session. Record written incrementally to this
path and merged to `main` by the chairman.

---

## Part I — Agenda, instrument posture, and what today is for

### 1. Member return-path protocol

Council members are **real subagents**, spawned with the Agent tool against the committed
definitions in `.claude/agents/`. Each member returns its contribution in its own final
message; **the chairman pastes that text into this record verbatim and writes none of it.**
If a spawn fails or the Agent tool is unavailable, that seat is recorded as **PROCEDURAL
FAILURE** for the day and no chairman-written substitute is entered. If a member reports a
model different from its directive, the discrepancy is recorded as a procedural caveat and
never silently substituted.

**Rule 0 reconciliation, declared rather than hidden — ninth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 directs two convened seats and says the integrity
challenge *"is not a separately convened seat and does not file its own return."* Rule 0's
compliant reading requires the chairman to write the integrity voice, which is the operator's
prohibited act. **I convene the integrity officer as a real subagent and keep its return
inside the Part V dialogue rather than as its own Part** — the concession every chair since
2026-09-01 has made. Where Rule 0 costs nothing to honour, it is honoured exactly: **no
analytics officer and no systems researcher are convened**, the digest sentence is two lines
I copied from `meta.json`, and the optimization item is one paragraph I write.

**Today that concession costs even less than usual, and I say why rather than assert it.**
The digest of record is **unchanged** (§3): the newest artefact under `digests/` is still
`2026-09-07_ISDS-Thematic-Watch`, as it was yesterday. My own convening instruction directs
the analytics seat be "short if the digest of record is unchanged"; Rule 0 directs it not be
convened at all. On an unchanged digest the two instruments converge on the same output, so
nothing is lost by taking the narrower one. The systems researcher's queue is **gated**: every
optimization item it would work — `source_analytics.py`, the health column, the `polite_get`
diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the
`build_graph` fix — is on the operator's pending list and stays zero-budget. A seat convened
against a fully gated queue produces a paragraph saying so, and I can say so myself in one
line: **I have, here.** This remains escalation 6; one line from the operator closes it either
way.

### 2. Egress probe at session open — fetch is DEAD, relay is ALIVE

Run before any seat was convened, three probes, two channels:

| Probe | Target | Result |
|---|---|---|
| `curl` egress | `https://example.com/` | `CONNECT tunnel failed, response 403` (exit 56) |
| `curl` egress | `https://example.org/` | `CONNECT tunnel failed, response 403` (exit 56) |
| fetch service | `https://example.com/` (neutral control) | `EGRESS_BLOCKED` |

**No state change.** This matches the constraint established by controlled test on 2026-08-02
and 2026-08-03 and re-confirmed every session since. All three probes are on **neutral
controls**, so the reading is about this session's network and about nothing else: **no
fetch-layer status in this record is a claim about any resource** (the standing
tool-status-as-source-state rule). Web search is alive.

**The committed relay is therefore the retrieval channel, and no fetch-dependent item is
stranded for want of one.** `.github/workflows/fetch-relay.yml` and `scripts/fetch_relay.py`
are committed and exercised — a request file has been answered on every day from 2026-08-31
through 2026-09-08. Today's batch is pre-registered at §5 below and pushed **before any seat
is convened**, in two commits per the 2026-09-05 optimization item: the record with
`[skip ci]`, then the request file without it, so `daily-update.yml` does not fire on a Part-I
fragment and the relay does.

### 3. The digest did NOT move, and the shallow flag is set

**`Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list
leads).`** Values copied from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`
(`screened: 30`, `matches: 0`, `watch_list_leads: 0`, `validation_status_only: true`), not
restated from memory.

**This is the same artefact yesterday's record graded, and it is the digest of record again
today** — enumerated with `ls -d digests/*/ | sort | tail -1` → `digests/2026-09-07_ISDS-Thematic-Watch/`.
Nothing collected between yesterday's session and this one. Two consequences:

- **The weekly brief is now seven Mondays empty and today does not change that.** 0 items
  surfaced means no interpretive brief is produced; the last is `briefs/2026-07-27-memo.md`.
  > **CORRECTION — `0909-B3`, SUSTAINED against the chair; see Part VI §1. "Seven" is WRONG.
  > It is SIX: 08-03, 08-10, 08-17, 08-24, 08-31, 09-07.** Yesterday's record incremented this
  > counter on a Tuesday and I inherited it without checking, in the same document in which I
  > made "compute every count with an aggregating command" binding on all seats including me.
  > The erroneous sentence is left standing rather than rewritten, because deleting it would
  > delete the evidence that the gate caught it.
- **Yesterday's grading of the amended Monday prediction is final and is not re-graded.** It
  is FALSIFIED on the conjunction at the artefact of record and CONFIRMED on the first of that
  path's three writes. No seat re-opens it today.

**`git rev-parse --is-shallow-repository` → `true`.** Binding, carried forward: **no
history-derived count may be stated in this record** unless the clone is unshallowed and the
shallow state re-verified false, **or** it is stated in the windowed form — *"over the N
commits this clone holds from `<TIP>`"* — and never as "committed" or "ever". Session tip at
open: **`ec4ca67`**.

### 4. Agenda — three bounded tasks for the analyst

Budgets are set here and are ceilings, not targets. Unspent budget returned deliberately is
the behaviour this council wants and is recorded as such. **Yesterday's close-out produced
seven next steps; three of them (nos. 3, 4, 7) bind me rather than the analyst and are
discharged in this Part rather than delegated.**

1. **THE RELAY BATCH, graded strictly on the pre-registration frozen in the request file's own
   `note` (§5).** Read the manifest's own `request`, `environment`, `control_ok`, `requested`,
   `completed` and `truncated` header fields **before reading any row as an answer to this
   batch** (2026-09-03 optimization item). Positive controls are graded first; the substantive
   rows are uninterpretable if a control fails. **This batch serves `open_threads[5]` and
   nothing else** — yesterday's next step 2, adopted unchanged. Budget: 0 searches.
2. **THE WINGTECH THREAD, and this is the day's substantive question.** `open_threads[5]` is a
   monthly standing watch whose check was overdue by six weeks when yesterday's analyst ran it
   and returned a null; `GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found` was
   opened on that null. Today's rows 1–4 are the relay's attempt at the same question through a
   different channel. **Grade the thread, not the rows**: does the record's position on
   `open_threads[5]` change, and if not, say so plainly. Yesterday established the anchor —
   Day 146 post-cooling-off as of 2026-09-08 — so today is **Day 147**, and I state the
   arithmetic with its receipt so the analyst need not re-derive it and no seat carries it on
   trust: `STATE_OF_THE_ANSWER.md:808` anchors **Day 86 at 2026-07-10**; the elapsed interval
   2026-07-10 → 2026-09-09 is 21 + 31 + 9 = **61 days**; 86 + 61 = **Day 147**. The anchor is a
   day-number attached to a date, not a start date, and adding elapsed days to the wrong operand
   is the way this arithmetic goes wrong. **Verify it before you use it; if it is wrong, say so
   and the correction is mine.**
   > **CORRECTION — `C-1`, SUSTAINED against the chair; see Part VI §1. The ARITHMETIC is right
   > (Day 147) and the PINPOINT is stale.** The Day-86 anchor sits at **`STATE_OF_THE_ANSWER.md:827`**
   > at today's tip `ec4ca67`, not `:808`; it sat at `:808` at yesterday's tip `63ffc84`, and
   > commit `6de0617` — yesterday's own close-out — shifted it 19 lines. Per `0909-N1`, line 808
   > at today's tip is **blank**; the ICC Article 30 paragraph is at `:807`. The correction is
   > mine, as I said it would be. Binding from today: **a line pinpoint into a mutable tracked
   > file carries the commit at which it resolves.**
3. **THE RESEARCH QUESTION. Budget: five web searches, ceiling not target.** One fewer than
   yesterday, deliberately: yesterday's analyst spent six and reported one of them
   (**search 5**) as misspent against a thread the record already held in ~1,400 words, and
   proposed its own remedy — *"before spending a search on any named matter, `git grep -icn`
   the matter's rigid designator across the tree; if the record holds it in more than two
   files, the search must be aimed at the thread's stated closure condition, not at the
   matter."* **I adopt that rule as binding on this seat today and cut the budget by the slot
   it would have saved.** Standing rules apply — docket-page-before-document-hunt, no
   search-synthesis figures, no relevance inflation.

**Binding carry-overs from yesterday's next steps, restated so no seat has to reconstruct
them:**

- **`open_threads[0]` is CLOSED. Do not re-carry it** (next step 1).
- **The Ecuador slug is CONVERTED, not re-carried** — it is an operator manual query at zero
  search budget; closure condition unchanged and unwidened, an ICSID case number and nothing
  else; the paywalled-body synthesis stays QUARANTINED. **Do not re-fire the Einarsson query
  family** (retired 2026-09-07) (next step 6).
- **NO PREDICTION ON A STATUS LABEL without naming the code path that writes it** (next step 5).
- **Before any citation of a digest artefact: cite the BLOB, not the path** (next step 7,
  `C-2`). `digests/` is overwritten in place.
- **Every count is computed by an aggregating command, never read off a display** (next step 4).
  `| wc -l`, `grep -c`, `git rev-list --count <literal tip>`. This binds all seats including me.

### 5. Pre-registered relay batch — `analytics/fetch-requests/2026-09-09-daily.json`

Four research rows and a tail control, exactly as yesterday's next step 2 directs: two URLs
worked in the two-row pattern (positive control + substantive literal) that this council has
adopted as the only shape that can separate *"this literal is absent from this body"* from
*"`excerpt_of` found nothing on this body at all."*

The forecasts, their grading order, and what each outcome licenses are frozen in the request
file's own `note`, which `run()` copies into the manifest verbatim. They are not restated here;
the analyst grades against the committed file, not against this paragraph.

### 6. Yesterday's next step 3, discharged HERE and by me — the corpus screen on my own note

Yesterday's `C-1` was sustained against a previous chair for asserting in a frozen note that a
URL was "untested" when this council's own reduction corpus held the identical literal at the
identical URL with the identical result, two days earlier. The remedy adopted was a screen, and
it is the chairman's to run, not the analyst's. **I ran it before writing the note and I record
its output here rather than merely certifying that I ran it.**

Instrument: a Python enumeration over **every** record in **every** file matching
`analytics/fetch-results/*.json` at session tip `ec4ca67` — **412 rows across 51 reductions**,
counted by the enumeration itself and not read off any display.

| screened assertion in today's note | exact-URL / exact-literal match in corpus | verdict |
|---|---|---|
| `https://icsid.worldbank.org/cases/recent` has prior rows | 2 rows, `2026-09-07-daily.json` rec 7–8: `ICSID` matched, `ARB/` matched, both 362,084 B / `4af84337` | **NOT untested — and today's note says so** |
| `https://icsid.worldbank.org/cases/pending` has prior rows | 2 rows, `2026-09-07-daily.json` rec 9–10: `ICSID` matched, `ARB/` **un**matched, both 74,431 B / `ecbd32d6` | **NOT untested — and today's note says so** |
| the literal `Netherlands` has never been fired at any URL | **0 rows of 412** carry a `find` containing `etherland` | **untested, and the note may say so** |

**So the honest form of today's novelty claim is narrow and I hold the note to it: the two URLs
are not new, the `ICSID` control on each is a deliberate re-fire, and the only untested thing
in this batch is the substantive literal.** That is a smaller claim than "these paths are
unexamined" and it is the true one.

### 7. Spend checkpoint

**Placed after the vetting round**, before Part VI is written: the chairman states what the
session has spent, what remains, and whether the close-out is affordable. If it is not, the
close-out is shortened and the shortening is declared.

---

# Part II — Research analyst's return, 2026-09-09

**Entered VERBATIM. The chairman wrote none of it.** The only change made in transit is the
decoding of HTML entities introduced by the agent return channel (`&gt;` → `>`, `&lt;` → `<`),
the same transit note the last two records carried. Reported model: **`claude-opus-5`**, which
matches the operator's directive for this seat; no discrepancy, and the seat disclosed it
unasked on its first line.

---

Model: **claude-opus-5** — matches the operator's directive for this seat. No discrepancy to record.

---

## TASK 1 — The relay batch, graded

### 1.1 Manifest header, read before any row was read as an answer

`analytics/fetch-results/2026-09-09-daily.json`, at branch tip `d34b9ec` (`chore: fetch-relay results [skip ci]`), pulled from `origin/council/2026-09-09`:

| field | value |
|---|---|
| `request` | `2026-09-09-daily.json` |
| `environment` | `github-actions-runner (run 34344935760)` |
| `fetched_at` | `2026-09-09T11:19:55.176614+00:00` |
| `control_url` / `control_ok` | `https://example.com/` / **true** |
| `void` | **false** |
| `requested` / `completed` / `truncated` | **5** / **5** / **false** |
| `excerpt_chars` | 400 |
| records | **6** |

The note's own discrepancy test — *"A reduction with 'truncated': true, or with any 'requested' count other than 5, or with fewer than 6 records, is an instrument discrepancy"* — is passed on all three limbs. **The manifest `note` is byte-identical to `analytics/fetch-requests/2026-09-09-daily.json`'s `note`**, compared programmatically (`d['note']==req['note']` → `True`, both 10,483 chars). **The reduction answers this request. No instrument discrepancy.**

Head control (record 0) and tail control (record 5): both `ok`/200/**559 bytes**/`ff67a9d7…`, record 5 with `find_matched: true` on `Example Domain`. The instrument was live at both ends of the run. Per `0906-B1` this establishes nothing about stability across days; it is a fact about two responses at one stated time.

### 1.2 The grade, controls first

| forecast | row | pre-registered requirement | observed | verdict |
|---|---|---|---|---|
| **A** (positive control, graded first) | 1 | `ok`/200/`find_matched` true for `ICSID` on `/cases/recent` | ok/200/**362,084 B**/`f309eb63…`/**true** | **CONFIRMED** → row 2 interpretable |
| **B** | 2 | `find_matched` **FALSE** for `Netherlands` | **TRUE**, non-empty 400-char window, same 362,084 B and same `f309eb63…` as row 1 | **FALSIFIED** |
| **C** (positive control, graded first for its pair) | 3 | `ok`/200/`find_matched` true for `ICSID` on `/cases/pending` | ok/200/**74,430 B**/`712196ea…`/**true** | **CONFIRMED** → row 4 interpretable |
| **D** | 4 | `find_matched` **FALSE** for `Netherlands` | **false**, empty excerpt, same 74,430 B and same `712196ea…` as row 3 | **CONFIRMED** |

Both controls passed. Both substantive rows are interpretable at their pre-registered strength and no more.

### 1.3 Forecast B is falsified, and the chairman pre-registered that this was the higher-value outcome

Row 2's window, quoted as returned (`records[2].excerpt`):

> `thers v. Swiss Confederation (ICSID Case No. ARB/26/4) January 15, 2026 Shell PLC v. Kingdom of the Netherlands (ICSID Case No. ARB/26/2) January 7, 2026 Hiroshi Osumi v. Swiss Confederation (ICSID Case No. ARB/26/1) January 6, 2026 Ntega Holding Burundi S.A. v. Republic of Burundi (ICSID Case No. ARB(AF)/26/1) December 22, 2025 Nimba Investment LLC and others v. Republic of Guinea (ICSID Case No.`

**The binding reading rule in the frozen note is honoured in full.** No case number, case name, party, claimant, tribunal or matter in that window is entered into this record as a discovered matter, screened, graded on any ring, or carried as a lead today. The note itself anticipated this outcome and pre-committed the treatment (*"that matter is a LEAD for a future session's docket check and is not screened today"*). I quote the window because it is the evidence of the falsification, not because I am reading it as research.

**What the falsification licenses, exactly.** `excerpt_of` (`scripts/fetch_relay.py:130-135`) centres the window on the **first** case-insensitive match; `find_matched` is `bool(find) and bool(excerpt_of(...))` (`:180-181`). So row 2 establishes that the tag-stripped, whitespace-collapsed rendering of the body this relay received from `https://icsid.worldbank.org/cases/recent` at `2026-09-09T11:19:55Z` **contains at least one occurrence of the literal `Netherlands`, and that occurrence sits inside a run of per-case rows carrying party names, ICSID case numbers and dates**. It licenses nothing about how many such occurrences exist, nothing about what lies outside those 400 characters, and nothing about the docket.

### 1.4 Forecast D confirmed, and I hold the chair to his own pre-registration

The chairman pre-registered row 4 as *"the weaker of the two"* and its confirmation as *"close to UNINFORMATIVE ABOUT THE DOCKET"*, on the ground that `ARB/` was UNMATCHED on `/cases/pending` at 74,431 bytes on 2026-09-07 while it MATCHED on `/cases/recent` at 362,084 bytes. **Today's result is exactly the predicted asymmetry and I spend it as worth nothing.** `Netherlands` matched on the 362,084-byte body and did not match on the 74,430-byte body, in the same run, with both controls passing. The natural reading — that `/cases/pending` does not serve per-case rows into text `excerpt_of` can see — is now supported by two literals on two days rather than one, but **a literal unreachable by construction produces an absence that proves nothing**, and row 4's confirmation is not evidence about any register. It is not cited below for anything.

One observation, framed as the standing rule requires: **`/cases/pending` returned 74,430 bytes / `712196ea…` today against 74,431 bytes / `ecbd32d6…` on 2026-09-07 (`analytics/fetch-results/2026-09-07-daily.json` records 9–10, `fetched_at 2026-09-07T12:18:11Z`).** That is a statement about **two responses**, not about the page. It is not a finding and I draw nothing from it.

### 1.5 Audit of the chairman's §6 corpus screen — SUSTAINED, in full

I re-ran the screen myself rather than accepting it. Instrument: a Python enumeration over every record in every file matching `analytics/fetch-results/*.json` **at the session tip `ec4ca67` the chairman named**, read through `git show` (not the working tree), counted by the enumeration.

- **51 reductions, 412 rows.** Both figures reproduce his exactly.
- `https://icsid.worldbank.org/cases/recent`: **2 rows**, `2026-09-07-daily.json` records **7** (`ICSID`, matched) and **8** (`ARB/`, matched), both 362,084 B / `4af84337`. Reproduces exactly.
- `https://icsid.worldbank.org/cases/pending`: **2 rows**, `2026-09-07-daily.json` records **9** (`ICSID`, matched) and **10** (`ARB/`, **un**matched), both 74,431 B / `ecbd32d6`. Reproduces exactly.
- Rows whose `find` contains `etherland`: **0 of 412.** Reproduces exactly.

I also ran two checks he did not: **0 of 412 rows carry `Netherlands` in an *excerpt*** either, and **0 of 412 rows touch `Wingtech` in `find` or `excerpt`**. So the narrow novelty claim he held his note to — *"the only untested thing in this batch is the substantive literal"* — is true in the stronger form as well. **The remedy adopted after yesterday's `C-1` worked. I could not break it.**

---

## TASK 2 — The Wingtech thread, graded

### 2.1 The day count: the arithmetic is RIGHT, the receipt is WRONG

`86 + 61 = 147` is correct. 2026-07-10 → 2026-09-09 is 61 days (`(date(2026,9,9)-date(2026,7,10)).days` → `61`). **Today is Day 147 post-cooling-off**, and this is consistent with yesterday's Day 146 (`86 + 60`) and with the independent check that 2026-04-15 + 147 days = 2026-09-09.

**But `STATE_OF_THE_ANSWER.md:808` does not anchor it.** At session tip `ec4ca67` and at branch tip `d34b9ec`, line 808 is the ICC Arbitration Rules **Article 30 "Early Determination"** paragraph and carries no day anchor at all. The string `Day 86 post-cooling-off as of July 10, 2026` sits at **`:827`** (and, quoted inside yesterday's council-update block, at `:19`). This is Challenge C-1 below; the correction is the chairman's, not mine, and I state the corrected pinpoint here so no seat carries the stale one forward: **the anchor is `STATE_OF_THE_ANSWER.md:827` at `d34b9ec`.**

### 2.2 Does the record's position on `open_threads[5]` change? NO.

`open_threads[5]` asks whether the ICSID **pending-cases** docket registers a Wingtech v. Netherlands proceeding. The closure condition on `GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found` (research_log thread 20, verbatim) is *"an ICSID case number, or one relay row on an allowlisted ICSID path returning it."*

- **Limb 1 (an ICSID case number): not met.** No search was spent on it today, by direction.
- **Limb 2 (one relay row on an allowlisted ICSID path returning it): NOT MET, and it was never capable of being met by this batch.** Rows 1–4 fired the literals `ICSID` and `Netherlands`. **Neither is a rigid designator for this thread.** `Netherlands` is a respondent-State name shared across an unknown number of matters; it cannot discriminate a Wingtech proceeding from any other Netherlands-respondent proceeding, and `excerpt_of` returns one 400-character window centred on the *first* match, so it cannot enumerate even the Netherlands-respondent set. The rigid designator this thread needs is **`Wingtech`**, and **no row in the corpus has ever carried it** (0 of 412 at `ec4ca67`; 0 of 418 at `d34b9ec`).

**The thread's position is unchanged. Recorded in this project's required form: an ICSID registration of a Wingtech v. Netherlands proceeding is NOT FOUND IN ACCESSIBLE SOURCES. It is never stated that none exists.** Day 147 post-cooling-off with no registration identified. The gap slug stays open, unamended.

### 2.3 What today *does* add to the thread — one thing, and it is about the instrument

The falsification of Forecast B converts a channel assumption into a channel fact: **`/cases/recent` demonstrably serves party names *and* ICSID case numbers into the text `excerpt_of` can search**, on an allowlisted primary host, in the same run in which `/cases/pending` demonstrably does not. Before today the council knew only that `ARB/` matched there (2026-09-07); it did not know that a **respondent-State name** was reachable. That is the difference between "the page carries case identifiers" and "the page carries the kind of literal a docket check actually asks about."

**The operational consequence, stated as a proposal and not acted on:** the second limb of this thread's closure condition is testable, on one row, by firing `find: "Wingtech"` at `https://icsid.worldbank.org/cases/recent` with the standard paired positive control on the same URL in the same batch. That is the shape the record already requires (`0816`/`0818` paired-control rule) and it is the first formulation that puts the thread's own rigid designator into the channel. It would still be a window onto a register, not the register — a null would license only *"`excerpt_of` found no occurrence of `Wingtech` in the tag-stripped text of the body this relay received from this URL at this time."* **I do not propose that a null there closes the gap.**

### 2.4 Three-ring assessment, deflated

Unchanged from the record and not re-argued: **Ring 2** present (the Dutch Goods Availability Act order); **Ring 3** architecturally live but on the branch that does *not* bite (China–Netherlands BIT (2001) Art. 10: for an investment *in the Netherlands* the investor may go to international dispute settlement at any time); **Ring 1 NOT established** — the asset is corporate control of a Dutch semiconductor company, and no body any seat has read pleads an IP right as the covered investment. **A Ring 1–2 near-miss. Not a three-ring matter, and I will not dress it as one.**

---

## TASK 3 — The research question

**Verdict: the theme was quiet. No new matter reaching the three-ring intersection was found. Four searches spent, one returned.**

### 3.1 The closure-condition rule, applied before every search

The chairman adopted yesterday's seat's rule as binding: `git grep -icn` the rigid designator; if the record holds it in more than two files, the search must aim at the thread's **stated closure condition**, not at the matter. Screen run at `d34b9ec` before any search (`git grep -l -i -F`, whole tree, `| wc -l`):

| designator | files | consequence |
|---|---|---|
| `A/81/17` | 125 | search must target thread 7's closure condition, not the document generally |
| `Wingtech` | 54 | **0 searches spent** (chairman's direction; yesterday's query not re-fired) |
| `Nexperia` | 48 | 0 spent |
| `trade secret` | 104 | search must not be aimed at the matter |
| `Anthea Roberts` | 32 | see §3.3 |

**Every designator I might have searched exceeds the threshold. The rule bound all four searches, and I say below what closure condition each was aimed at.**

### 3.2 Search 1 — thread 7's closure condition (has A/81/17 issued?) — NULL, and it doubles as the release test

Aimed at thread 7's operative precondition: A/81/17 must exist before it can confirm or fail to confirm the A/CN.9/1246 threshold. Query: `"A/81/17" UNCITRAL Commission report fifty-ninth session 2026 Official Records Supplement No. 17`.

**The returned result set contains no A/81/17 document URL.** The only Commission-report PDF returned is the prior year's: `https://uncitral.un.org/sites/uncitral.un.org/files/2511964e_uncitral_report_advance_0.pdf`, titled *"A/80/17 United Nations Report of the United Nations"*. I state this as a fact about the result set and nothing more. **The search tool's own prose asserted the report is "forthcoming"; that is search synthesis, I read no body carrying it, and I do not assert it.** Per `G5` nothing here founds any claim that A/81/17 has or has not been issued.

One title-level lead, recorded and not relied on: `https://unis.unvienna.org/unis/en/pressrels/2026/unisl400.html`, *"UNCITRAL advances dispute settlement and digital trade work at its 59th session"*. `unis.unvienna.org` is **not** in `ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`), so the body is unreachable by the only retrieval channel this session has. Title only; no content asserted. New gap slug below.

### 3.3 The monitored-author check — NOT departed from, because the release condition tested negative

`prompts/research_analyst.txt:158-163` mandates one Roberts query per session. `analytics/daily-research/2026-07-30.md:220` subsumes it at zero budget **with an express release condition**: *"until A/81/17 publishes or a Roberts item surfaces by another route."* Yesterday's seat declared a judgment-call departure. **I did better than a judgment call: search 1 tested the release condition and returned no A/81/17 document, and no Roberts item surfaced by another route in any of today's four result sets.** The zero-budget ruling therefore still governs on its own terms and I spend nothing on it. This is not a departure from the prompt; it is the prompt's instruction yielding to a live, tested carve-out.

### 3.4 Search 2 — thread 6's closure condition ("any disposition touching IP-as-investment") — NULL, and it produced no new carrier at all

Query: `ICSID Rule 41 manifest lack of legal merit decision 2026 intellectual property patent trademark covered investment dismissed`. The result set is framework material — ICSID's own procedure pages, a Jus Mundi wiki note, two Lexology notes. **No 2026 disposition applying any codified manifest-lack-of-merit gate to an IP-as-investment claim was returned.** Thread 6 is unmoved and stays open.

**And the honest negative about the search itself: every on-theme URL it returned is already in this tree.** Screened at `d34b9ec`: `manifest-lack-of-legal-merit/2022` (6+ files including `STATE_OF_THE_ANSWER.md`), the ICSID *In Focus* Rule 41(5) publication (5 files), the tables-of-decisions page (fired repeatedly). The tables-of-decisions page has already been worked with `ARB/` (matched, `2026-08-19-zz-render-and-cells.json` rec 2), `ICSID Case No.` (matched), `Bridgestone` (**un**matched) and `Philip Morris` (**un**matched). **I therefore withdraw the proposal I had drafted to fire that page again: the record already holds the diagnostic I was about to propose, and proposing it would have been exactly the misspend yesterday's rule was written to stop.**

### 3.5 Search 3 — three-ring discovery, Ring 1 substantive — NULL, and a named query defect

Query: `2026 investment treaty arbitration trade secrets confidential clinical data asserted as protected investment tribunal jurisdiction objection award`. **Every returned carrier is about trade secrets as a *procedural confidentiality* problem** — redaction, protective orders, closed hearings, the UNCITRAL Transparency Rules — **not about a trade secret pleaded as a covered investment.** No matter, no award, no tribunal.

This is a query defect worth naming rather than a quiet week: in ISDS retrieval, `trade secrets` is a **false friend**. It sits in two disjoint literatures, and the procedural one is far larger and dominates recall. `procedural confidentiality`, `confidentiality literature` and `false friend` each return **0 files** in `analytics/insights.jsonl` and `STATE_OF_THE_ANSWER.md` at `d34b9ec`, so this is not already recorded. It goes in the method note (§10), not the insight ledger — it is about how to search, not about the law.

### 3.6 Search 4 — 2026 Ring-1 registrations — NULL on the matter, and the day's one new finding

Query: `ICSID case registered 2026 intellectual property rights patent expropriation claimant investment treaty new arbitration`. **No 2026-registered ICSID matter involving IP was returned.** The substantive result set is historical framework (an OECD 2010 working paper, a 2022 student-forum piece, GAR guide chapters) plus the *Eli Lilly* seed case. Ring 1 is quiet and I report it as quiet.

**What the result set did carry is new, and it is about the retrieval channel, which is this project's binding constraint.** Two URL *patterns* on the allowlisted host `icsid.worldbank.org` appear in the returns, and **each is held in 0 files across this clone's tree at `d34b9ec`** (`git grep -l -i -F -e '<pattern>' d34b9ec -- | wc -l`, whole tree, no path argument):

1. **`https://icsid.worldbank.org/apps/ICSIDWEB/cases/pages/casedetail.aspx?CaseNo=UNCT%2F14%2F2`** — a **legacy, case-number-parameterised case-detail route**, returned here for *Eli Lilly v. Canada*, one of the three seed awards. `apps/ICSIDWEB` → **0 files**; `casedetail.aspx` → **0 files**.
2. **`https://icsid.worldbank.org/sites/default/files/parties_publications/…`** — **party-filed exhibits and legal authorities served from the allowlisted host itself**, not from `icsidfiles.worldbank.org`. `parties_publications` → **0 files**.

Both are certified as the search returned them, character-for-character. **I fetched neither and assert nothing about what either returns** — status, redirect behaviour and content are all unknown, and `.aspx` legacy routes commonly redirect. Two caveats travel with them so no row is wasted: (a) the second pattern serves **PDFs**, and `excerpt_of` returns `""` for any content type lacking `html`/`text` (`scripts/fetch_relay.py:124-125`), so such a row yields `bytes` + `sha256` and an empty excerpt **by construction** — this is already-held knowledge in this record (`application/pdf` in 12+ files at `d34b9ec`) and I claim no novelty for it; (b) pattern 1 is **parameterised by a case number you must already hold**, so it is a second surface for thread 8's standing dockets (Huawei ARB/22/2, Hela Schwarz ARB/17/19), **and it cannot serve `open_threads[5]`, whose whole difficulty is that no case number is held.**

### 3.7 The one new insight for the ledger

Not a doctrinal insight; a channel one, and it is the day's finding. **The council's ICSID retrieval surface is wider than the record knows, and today it widened in two independent registers at once**: (i) `/cases/recent` was shown to serve *respondent-State names* alongside case numbers into `excerpt_of`-visible text, while `/cases/pending` — the path whose name matches `open_threads[5]`'s question — was shown, on a second literal and a second day, not to; and (ii) two allowlisted-host ICSID URL patterns that this clone's tree has never held surfaced incidentally, one of them a case-number-parameterised legacy detail route for a seed case. **The practical upshot for the research question is narrow but real: the constraint on closing `open_threads[5]` has never been the channel's reach; it is that no query, on any channel, has yet been aimed at the thread's own rigid designator.**

---

## Persistent-gap markers

`GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found` — an ICSID registration of a *Wingtech v. Netherlands* proceeding is NOT FOUND IN ACCESSIBLE SOURCES; Day 147 post-cooling-off. Closure condition **unchanged and unwidened**: an ICSID case number, or one relay row on an allowlisted ICSID path returning it. **PROPOSED next action (a proposal, not a grant, and it does not amend the closure condition):** one relay row `{"url": "https://icsid.worldbank.org/cases/recent", "find": "Wingtech"}` with a paired same-URL positive control in the same batch. A null there would license only a statement about the fetch and would **not** close the gap.

`GAP-UNRESOLVED: grosse-ruse-khan-itn-ip-isds-plain-packaging-to-patent-revocation` — carried unchanged. `www.iisd.org` is allowlisted and has refused every attempt; no seat has read that body; nothing asserted from its title. **Not fired today, by direction.**

`GAP-UNRESOLVED: dailyjus-ai-subject-matter-jurisdiction-body-unread` — carried unchanged; `dailyjus.com` is off-allowlist. **0 budget spent.**

`GAP-UNRESOLVED: uncitral-59th-session-press-release-body-unread` — **NEW, opened today.** `https://unis.unvienna.org/unis/en/pressrels/2026/unisl400.html`, title *"UNCITRAL advances dispute settlement and digital trade work at its 59th session"*, certified as the search return. Host is not in `ALLOWED_HOSTS` (`scripts/fetch_relay.py:48-69`), so the body is unreachable by the relay and unreachable by fetch (dead this session). **Title only; no content asserted; it is a lead and not a finding.** Bears on thread 7's four riding questions. Closure condition: the body read from an accessible source, or the host added to the allowlist by the operator.

*Not emitted as slugs, per direction:* `spanish-drugmaker-icsid-ecuador-ring1-not-assessed` (CONVERTED to an operator manual query; 0 budget spent; paywalled-body synthesis stays QUARANTINED) and the three escalated gaps (`hela-schwarz-art9-jurisdiction-or-admissibility`, `icsid-2022-arbitration-rules-rule-41-48-provision-text`, `icsid-mllm-finding-column-value-codebook`) — **0 budget spent on all four, and I hypothesize nothing about any of their contents.** I note only, because it arose incidentally in a search aimed elsewhere and cost nothing: `https://icsid.worldbank.org/procedures/arbitration/convention/manifest-lack-of-legal-merit/2022` is on the allowlist **and has already been fired twice** (`2026-08-22-daily.json` records 2–3), so the escalated Rule 41/48 provision-text gap is not for want of that page.

---

## Challenge to the chair

### C-1 — BLOCKING. Part I §4 freezes a stale pinpoint into the record, and it is the receipt, not the arithmetic, that fails.

Part I §4 states: *"`STATE_OF_THE_ANSWER.md:808` anchors **Day 86 at 2026-07-10**"*, and adds *"I state the arithmetic with its receipt so the analyst need not re-derive it and no seat carries it on trust."*

**At the session tip the chairman himself declared (`ec4ca67`), and at branch tip `d34b9ec`, line 808 of `STATE_OF_THE_ANSWER.md` is the ICC Arbitration Rules Article 30 "Early Determination" paragraph. It contains no day anchor.** The anchor is at **`:827`**. Verified with `git show <commit>:STATE_OF_THE_ANSWER.md | grep -n`, three commits compared:

| commit | line of `Day 86 post-cooling-off as of July 10, 2026` |
|---|---|
| `63ffc84` (yesterday's session tip) | **808** |
| `ec4ca67` (today's session tip) | **827** |
| `d34b9ec` (branch tip) | **827** |

**The commit that moved it is `6de0617`, 2026-09-08T12:06:29Z, *"council: daily meeting 2026-09-08 (ledgers + state of the answer) [skip ci]"* — yesterday's own close-out.** Its parent still carries the anchor at 808; it carries it at 827. The file gained 19 lines above the anchor.

This is precisely the shape of yesterday's sustained `C-1`: **a chair froze into a pre-registration a sentence about the record that the record itself falsifies.** It is worse in one respect and better in another. Worse: the chairman instructed me to *verify* it, and the verification he invited is the one that breaks it, so the instrument caught it — but only because a seat looked. Better: the arithmetic is right (`86 + 61 = 147`), so nothing downstream is numerically wrong.

**The defect has already propagated.** `state/research_log.json` open thread 20 — written by the same close-out — reads *"Day 146 post-cooling-off on this project's own anchor (STATE_OF_THE_ANSWER.md:808, 'Day 86 as of July 10, 2026')"*. The stale pinpoint is now in the ledger, not just in prose. **Remedy sought, blocking:** correct Part I §4 to `:827` before Part I is merged, correct thread 20's parenthetical in the same commit, and adopt as a standing rule that **a line-number pinpoint into a mutable tracked file carries the commit at which it resolves**, exactly as the record already requires for `digests/` blobs and for reduction counts (`0903-B2`, `0906-B10`). A pinpoint without a commit into a file this council appends to daily has a half-life of about one day, and this is the demonstration.

### C-2 — NON-BLOCKING but sustained on its face. The frozen note asserts a monitoring window the record does not support.

The manifest note, frozen verbatim into `2026-09-09-daily.json`, states: *"the thread's own question has returned null on every channel tried for 147 days."*

**147 days is the post-cooling-off count, not the count of days on which a channel was tried.** The cooling-off expired approximately 2026-04-15 (`STATE_OF_THE_ANSWER.md:827`), but the earliest daily record in this tree carrying `Wingtech` is `analytics/daily-research/2026-07-05.md` (enumerated with `git grep -l -i -F -e Wingtech d34b9ec --`, 54 files, earliest daily by filename), and `STATE_OF_THE_ANSWER.md:827` itself dates the finding *"July 5, 2026"* and calls it *"the first confirmed Ring 1–2 ISDS dispute notice … since the project began monitoring."* **2026-07-05 → 2026-09-09 is 66 days.** For roughly the first 81 days of the 147, this project had no Wingtech thread and tried no channel against it. The sentence claims a monitoring effort that did not occur.

Non-blocking because it changes no grade in this batch and no seat relied on it. Sustained because **it is a false sentence frozen into evidence**, which is the category yesterday's council ruled cannot be edited out afterwards. Remedy: in tomorrow's note, either *"Day 147 post-cooling-off; null on every channel tried since 2026-07-05, 66 days"* or drop the duration clause.

### C-3 — NON-BLOCKING, and it is a challenge to the batch's design rather than to a sentence.

Rows 1–4 were commissioned to serve `open_threads[5]` "and nothing else", and the chairman's convening instruction says row 4 was "fired anyway because next step 2 directs it." **Fair enough as obedience; but the batch as designed could not have closed the thread on either row, and that was knowable in advance from the note's own instrument semantics.** `Netherlands` is not a rigid designator for a Wingtech proceeding, and `excerpt_of` returns one window centred on the *first* match, so even a match cannot enumerate. **Four rows were spent on a thread whose own rigid designator — `Wingtech` — was never put into the channel and has never appeared in any of the 418 rows this clone holds.** The corpus screen the chairman ran would have surfaced this if it had been run against the *thread's* designator rather than against the *batch's* literals: he screened `etherland` (0 of 412) and did not screen `Wingtech` (also 0). Remedy proposed, not built: extend the §6 corpus screen to include the **rigid designator of every thread the batch claims to serve**, not only the literals the batch happens to fire.

### C-4 — NON-BLOCKING. Two audits I ran to break Part I and could not.

Reported because an adversarial seat that only reports hits is not adversarial. (a) The §6 corpus screen reproduces **exactly** — 51 reductions, 412 rows, the four prior rows with their bytes and digests, 0 `etherland` rows — and it survives two checks he did not run (0 `Netherlands` excerpts, 0 `Wingtech` rows). (b) The manifest `note` is **byte-identical** to the request file's `note`, so the pre-registration is intact and `run()` copied it faithfully. **Part I §6's central claim is sound and I say so.**

---

## Reported against myself

1. **I drafted a proposal and had to withdraw it after the screen, not before.** I read the *In Focus* Rule 41(5) PDF URL out of search 2's returns, drafted a proposal to fire the tables-of-decisions page with `ARB/` to body-carry the "40 proceedings / 7 fully upheld / 26 dismissed / 4 partially upheld" figures the record flags as never retrieved — **and only then screened the corpus and found the page had already been fired eight times with `ARB/`, `ICSID Case No.`, `Bridgestone` and `Philip Morris` between 2026-08-19 and 2026-08-24.** The rule the chairman made binding on me today says screen *before* spending. I applied it to my searches but not to my proposals. **A proposal that duplicates the record is the same misspend as a search that does; I nearly filed one, and only the discipline of writing the receipt caught it.**

2. **My §3.5 "false friend" observation is weaker than its phrasing wants to be.** It rests on **one** result set from **one** query formulation. I did not run a discriminating control (e.g. the same query with the procedural vocabulary excluded) to show the effect is the vocabulary rather than the topic's genuine emptiness. I have put it in the method note rather than the insight ledger for exactly that reason, and a future seat should treat it as a hypothesis with n=1.

3. **I quoted row 2's excerpt in full.** The binding rule forbids *entering* the matters it names; it does not forbid quoting the window as evidence. But quoting a window that names three case numbers and four parties puts those literals into the permanent record, and `Shell PLC`, `ARB/26/2` and `ARB/26/4` now each occur in **2** files at merge time rather than 1. **I judged the falsification unprovable without the quote and I still think that is right, but a chair who wanted to argue I widened the record by transcription would have a point worth hearing.**

4. **I returned budget I might have spent well.** Search 5 is unspent (§9). The strongest use I could see for it was aimed at a matter the closure-condition rule forbade or a gap at zero budget. I am reasonably confident that is right; I am not certain, and a more imaginative seat might have found a fifth question that satisfied the rule.

---

## Proposals to the operator (nothing built, nothing patched, no diff)

1. **Correct the two stale `:808` pinpoints** (Part I §4 and `state/research_log.json` thread 20) to `:827`, and adopt: **a line pinpoint into a mutable tracked file carries the commit at which it resolves.** The rule already exists for `digests/` blobs and for reduction counts; it is not yet stated for `STATE_OF_THE_ANSWER.md`, which is the file this council pins most often and appends to daily.
2. **Fire the thread's rigid designator.** One row, `{"url": "https://icsid.worldbank.org/cases/recent", "find": "Wingtech"}`, paired with a same-URL positive control in the same batch. It is the only formulation in 418 rows that puts `open_threads[5]`'s own designator into the channel. **A null does not close the gap and I do not propose that it should.**
3. **Extend the §6 corpus screen** to cover the rigid designator of every thread a batch claims to serve, not only the literals the batch fires. Today's batch would have failed that screen in advance, cheaply.
4. **Two allowlisted-host ICSID URL patterns are unexamined and are worth one diagnostic row each, at the chair's discretion and not today**: the legacy `apps/ICSIDWEB/cases/pages/casedetail.aspx?CaseNo=<encoded>` route (0 files at `d34b9ec`; a second surface for thread 8's standing dockets, useless for `open_threads[5]`), and `sites/default/files/parties_publications/…` (0 files; **PDF — empty excerpt by construction, `scripts/fetch_relay.py:124-125` — so it can return a `bytes`/`sha256` existence check and nothing more**).
5. **Consider adding `unis.unvienna.org` to `ALLOWED_HOSTS`.** It is the UN Information Service, a first-party UN host for Commission press releases, and it is currently the only accessible-looking carrier for the 59th-session outcome that thread 7 rides on. **This is a request for a decision, not a change; I have patched nothing.**
6. *Untouched and confirmed untouched:* the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, and any ledger amendment. **Zero budget spent on all of them. I read `src/` and `scripts/` and wrote to neither.**

---

## Budget spent, itemised

| task | ceiling | spent | detail |
|---|---|---|---|
| Task 1 (relay batch) | 0 searches | **0** | Graded from the committed reduction and the committed request file only. |
| Task 2 (Wingtech thread) | 0 searches | **0** | Yesterday's domain-restricted query **not** re-fired. Day count verified arithmetically, offline. |
| Task 3 (research question) | 5 searches | **4** | S1 A/81/17 issuance (thread 7 closure condition; doubles as the Roberts release test) — null. S2 codified manifest-lack-of-merit gate × IP-as-investment (thread 6 closure condition) — null, and every on-theme URL already in tree. S3 Ring-1 substantive discovery — null; false-friend defect named. S4 2026 ICSID registrations × IP — null on the matter, two new allowlisted URL patterns as byproduct. |
| **Total** | **5** | **4** | **1 returned unspent.** |

**Why search 5 was returned rather than spent.** Every remaining question I could formulate fell into one of three forbidden or unproductive classes: (a) aimed at a matter whose designator exceeds the two-file threshold with no untried closure condition (`Wingtech`, `Nexperia`, `trade secret`); (b) an escalated gap or a converted operator query at zero budget (Ecuador, Hela Schwarz Art. 9, the 2022 Rules provision text, the MLLM codebook); (c) a re-fire of a family already retired (Einarsson) or already null 24 hours ago. **A fifth search would have been spent to show I had spent it.** The chairman said returned budget is credited; I am taking him at his word and inviting him to hold me to the reasoning rather than the number.

Relay rows: **0 requested by me.** Commits: **0** — I committed nothing; the chairman commits.

---

## Method note (self-training)

**What produced findings today, ranked.** (1) **Reading the instrument's source before reading its output.** `excerpt_of` centring the window on the *first* match (`scripts/fetch_relay.py:130-135`) is what makes row 2's match un-enumerable, and that single line is the difference between "the docket lists a Netherlands matter" (unsupported) and "the body contains at least one occurrence" (supported). Every grade in Task 1 turns on code I read today rather than on semantics I carried. (2) **Auditing the chair's receipt rather than his arithmetic.** `86 + 61 = 147` was right and checkable in one line; the pinpoint was wrong and took four `git show | grep -n` calls across three commits. **The error was in the part that looked least like a claim.** Generalisation: in this record, arithmetic is usually right and *provenance* is usually where the rot is, because arithmetic gets re-derived and pinpoints get copied. (3) **Screening the corpus at the *thread's* designator, not the *batch's* literals.** That is what produced C-3 and the Task 2 proposal, and it cost one Python loop.

**What produced nothing, and why.** Keyword discovery searches aimed at the three-ring intersection (S2, S3, S4) returned zero matters across three formulations. This is now a stable pattern and I state it as one: **generic three-ring keyword search has a very low yield in this project, and its failure mode is retrieving the adjacent literature rather than nothing.** S3's `trade secrets` returned the procedural-confidentiality corpus; S4's `intellectual property` returned the 2010–2022 framework corpus. **Both are false friends of the ring they name, and both are recall-dominant over the substantive question.** The 2026-07-26 lesson (author-name + title beat keyword search) generalises here: **the productive queries in this project name a rigid designator — a case number, a document symbol, a party, an author — and the unproductive ones name a doctrine.** Today's four searches split exactly on that line: S1 named `A/81/17` and returned a clean, gradeable null with a usable adjacent carrier; S2–S4 named doctrines and returned libraries.

**Method commitment for tomorrow's seat, carried forward:** before opening a search, write the proposition and identify its rigid designator (the carrying-span rule's screening step). **If the proposition has no rigid designator, that is itself the finding — the verdict will be weaker and the search is likely to retrieve the adjacent literature.** Today, three of four searches had no rigid designator and three of four returned nothing enterable.

**Weekly primary-source study (self-training mandate):** this week's depth item was `scripts/fetch_relay.py` read end-to-end rather than a doctrinal source — specifically `excerpt_of` (`:116-136`), `fetch_one` (`:139-182`) and `ALLOWED_HOSTS` (`:48-69`). I flag this as a **substitution**, not a discharge: the mandate asks for one primary award or doctrinal source per week, and I studied an instrument instead because every grade today depended on it. **Next session should take a doctrinal source to keep the three-ring judgment sharpening.**

---

## Candidate claims

The analyst's candidate-claim JSON block is entered verbatim below; it is the input to the
ledger and no seat may edit it in transit.

```json
{"candidate_claims": [
  {"claim_text": "In the fetch-relay reduction analytics/fetch-results/2026-09-09-daily.json (run 34344935760, fetched_at 2026-09-09T11:19:55.176614+00:00), the row fetching https://icsid.worldbank.org/cases/recent with find 'Netherlands' returned outcome 'ok', status 200, 362084 bytes, sha256 f309eb63dded9667bac6e59deafacc33d303bf9e6d0b424aff56d7b329c30261, and find_matched true.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/analytics/fetch-results/2026-09-09-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "thers v. Swiss Confederation (ICSID Case No. ARB/26/4) January 15, 2026 Shell PLC v. Kingdom of the Netherlands (ICSID Case No. ARB/26/2) January 7, 2026 Hiroshi Osumi v. Swiss Confederation (ICSID Case No. ARB/26/1) January 6, 2026",
   "supporting_locator": "records[2].excerpt, reduction at commit d34b9ec"},

  {"claim_text": "In the same reduction and the same run, the row fetching https://icsid.worldbank.org/cases/pending with find 'Netherlands' returned outcome 'ok', status 200, 74430 bytes, sha256 712196eac55f92d270525dce8ef162ac66ebc57cea6852578e8786c0073171ba, find_matched false and an empty excerpt, while its paired positive control on the same URL with find 'ICSID' returned find_matched true.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/analytics/fetch-results/2026-09-09-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "\"url\": \"https://icsid.worldbank.org/cases/pending\", \"find\": \"Netherlands\"",
   "supporting_locator": "records[3] and records[4], reduction at commit d34b9ec"},

  {"claim_text": "scripts/fetch_relay.py computes each excerpt as a 400-character window centred on the FIRST case-insensitive occurrence of the sought literal in the tag-stripped, whitespace-collapsed body, so a find_matched:true row establishes at least one occurrence and cannot enumerate occurrences.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/scripts/fetch_relay.py",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "With ``find``, the window is centred on the first case-insensitive match so the excerpt answers the question that was asked",
   "supporting_locator": "scripts/fetch_relay.py:119-121 (docstring of excerpt_of); implementation at :130-135"},

  {"claim_text": "scripts/fetch_relay.py's excerpt_of returns an empty string for any response whose content-type contains neither 'html' nor 'text', so a PDF fetched through the relay yields a byte length and sha256 but no excerpt and find_matched false by construction.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/scripts/fetch_relay.py",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "if \"html\" not in content_type and \"text\" not in content_type:\n        return \"\"",
   "supporting_locator": "scripts/fetch_relay.py:124-125"},

  {"claim_text": "At commit ec4ca67 the corpus analytics/fetch-results/*.json comprises 51 reduction files and 412 records; zero of those records carry a 'find' value containing the substring 'etherland', and zero carry 'Wingtech' in either 'find' or 'excerpt'.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/tree/ec4ca67/analytics/fetch-results",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "enumeration over git ls-tree -r --name-only ec4ca67 analytics/fetch-results/ with git show per file; counted by the enumeration, not read off a display"},

  {"claim_text": "The string 'Day 86 post-cooling-off as of July 10, 2026' sits at STATE_OF_THE_ANSWER.md line 827 at commits ec4ca67 and d34b9ec, and at line 808 at commit 63ffc84; the 19-line shift was introduced by commit 6de0617 (2026-09-08T12:06:29Z), so the pinpoint 'STATE_OF_THE_ANSWER.md:808' is stale at today's session tip.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/STATE_OF_THE_ANSWER.md",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "The six-month cooling-off period expired approximately April 15, 2026; formal ICSID arbitration filing has not been confirmed (Day 86 post-cooling-off as of July 10, 2026).",
   "supporting_locator": "STATE_OF_THE_ANSWER.md:827 at d34b9ec; same string at :808 at 63ffc84; git show <commit>:STATE_OF_THE_ANSWER.md | grep -n"},

  {"claim_text": "Adding the 61 elapsed days from 2026-07-10 to 2026-09-09 to the project's Day 86 anchor gives Day 147 post-cooling-off as of 2026-09-09, consistent with a cooling-off expiry of approximately 2026-04-15.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/STATE_OF_THE_ANSWER.md",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "Day 86 post-cooling-off as of July 10, 2026",
   "supporting_locator": "STATE_OF_THE_ANSWER.md:827 at d34b9ec"},

  {"claim_text": "No ICSID registration of a Wingtech v. Kingdom of the Netherlands proceeding is found in accessible sources as of 2026-09-09 (Day 147 post-cooling-off); it is not stated that none exists.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/state/research_log.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "RECORDED IN THIS PROJECT'S REQUIRED FORM: an ICSID registration is NOT FOUND IN ACCESSIBLE SOURCES. It is never stated that none exists.",
   "supporting_locator": "state/research_log.json open_threads[20] at d34b9ec"},

  {"claim_text": "A web search for the UNCITRAL Commission report symbol A/81/17 returned no A/81/17 document URL; the only Commission-report PDF in the result set is the prior year's A/80/17 advance report at uncitral.un.org.",
   "source_url": "https://uncitral.un.org/sites/uncitral.un.org/files/2511964e_uncitral_report_advance_0.pdf",
   "access_status": "unavailable",
   "source_authority": "primary",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": "A/80/17 United Nations Report of the United Nations",
   "supporting_locator": "search-result title, WebSearch 2026-09-09; body not fetched (egress dead, no relay row fired)"},

  {"claim_text": "A UN Information Service press release titled 'UNCITRAL advances dispute settlement and digital trade work at its 59th session' exists at https://unis.unvienna.org/unis/en/pressrels/2026/unisl400.html; its host is not in scripts/fetch_relay.py ALLOWED_HOSTS and its body has not been read.",
   "source_url": "https://unis.unvienna.org/unis/en/pressrels/2026/unisl400.html",
   "access_status": "unavailable",
   "source_authority": "official_secondary",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": "UNCITRAL advances dispute settlement and digital trade work at its 59th session",
   "supporting_locator": "search-result title, WebSearch 2026-09-09; URL certified character-for-character as returned; body unread"},

  {"claim_text": "ICSID serves a legacy case-detail route parameterised by case number at https://icsid.worldbank.org/apps/ICSIDWEB/cases/pages/casedetail.aspx?CaseNo=<urlencoded>, returned by search for Eli Lilly v. Canada (UNCT/14/2); the substrings 'apps/ICSIDWEB' and 'casedetail.aspx' occur in zero files of this repository at commit d34b9ec.",
   "source_url": "https://icsid.worldbank.org/apps/ICSIDWEB/cases/pages/casedetail.aspx?CaseNo=UNCT%2F14%2F2",
   "access_status": "unavailable",
   "source_authority": "primary",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": "International Centre for Settlement of Investment Disputes",
   "supporting_locator": "search-result title and URL, WebSearch 2026-09-09; URL not fetched, status and redirect behaviour unknown; repository counts from git grep -l -i -F at d34b9ec piped to wc -l"},

  {"claim_text": "ICSID serves party-filed exhibits and legal authorities from the allowlisted host icsid.worldbank.org under the path prefix /sites/default/files/parties_publications/, a prefix occurring in zero files of this repository at commit d34b9ec.",
   "source_url": "https://icsid.worldbank.org/sites/default/files/parties_publications/C8394/Claimants'%20documents/CL%20-%20Exhibits/CL-0279.pdf",
   "access_status": "unavailable",
   "source_authority": "primary",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": "\"Indirect Expropriation\" and the \"Right to Regulate\" in ...",
   "supporting_locator": "search-result title and URL, WebSearch 2026-09-09; PDF not fetched; repository count from git grep -l -i -F -e 'parties_publications' d34b9ec -- | wc -l -> 0"},

  {"claim_text": "The manifest note in analytics/fetch-results/2026-09-09-daily.json is byte-identical to the note in analytics/fetch-requests/2026-09-09-daily.json (10,483 characters each), confirming run() copied the pre-registration verbatim.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/d34b9ec/analytics/fetch-results/2026-09-09-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "programmatic comparison of the two files' 'note' fields at commit d34b9ec"}
]}
```

---

# Part V — The council discussion, 2026-09-09

**Chairman:** Two seats convened, both real subagents, both returning their own text. The
analyst's return is Part II above, entered verbatim. The integrity officer's vetting return
follows, entered verbatim, and its objections are binding on me. **Reported model:
`claude-opus-5` against a directive of Claude Opus 4.8 — a discrepancy, disclosed unasked on
its first line, and recorded as a procedural caveat rather than silently substituted. This is
the twenty-ninth consecutive session in which this seat has reported it** (escalation 3).

---

## The integrity officer's vetting return, entered VERBATIM

**The chairman wrote none of it.** The only change made in transit is the decoding of HTML
entities introduced by the agent return channel.

---

**Model: `claude-opus-5`.** The directive for this seat is **Claude Opus 4.8**. I am not running on it. `.claude/agents/integrity-officer.md:4` declares `model: opus`, which selects a TIER, not a VERSION; the description line on the same file says "Runs on Claude Opus 4.8" and is not enforced by anything. This is the **29th consecutive** disclosure. Recorded as a procedural caveat; never silently substituted.

**Procedural caveats before the note.** (1) Rule 0 conflict is properly declared at Part I §1 and I add nothing to it. (2) The branch is **ahead of `origin/council/2026-09-09` by one commit** — `46008e3` is local-only. The analyst graded at `d34b9ec`; the record lives at `46008e3`. Every count below names its commit. (3) Egress is dead; I fetched nothing and no objection below depends on a fetch.

---

## Objections

**`0909-B1` — BLOCKING. §3.7's closing sentence is false, and the analyst states it correctly twice elsewhere.**
Challenged: *"the constraint on closing `open_threads[5]` … is that no query, **on any channel**, has yet been aimed at the thread's own rigid designator."*
Instrument: `git show d34b9ec:analytics/daily-research/2026-09-08.md`, line 529 — yesterday's own candidate claim reads *"A search restricted to icsid.worldbank.org, italaw.com, investmentpolicy.unctad.org, jusmundi.com and iareporter.com **for a registered or pending Wingtech v. Netherlands ICSID proceeding** returned no such case."* Corroborated by `state/research_log.json` `open_threads[20]` at `d34b9ec` (read as JSON, not by grep). A query was aimed at `Wingtech` on the search channel **one day ago** — a fact Part II itself records three times (§3.1 "yesterday's query not re-fired", §2.2, budget table). The true claim is the relay-corpus one, which the analyst states correctly at §2.3 ("into the channel") and proposal 2 ("in 418 rows"). Remedy: mark the sentence SUPERSEDED in the close-out; corrected form — *"no **relay row** has yet carried the thread's rigid designator (0 of 418 rows at `d34b9ec`); the search channel fired it on 2026-09-08 and returned null."*

**`0909-B2` — BLOCKING. §3.7(i) spends evidence §1.4 promised not to spend.**
§1.4: *"row 4's confirmation is not evidence about any register. **It is not cited below for anything.**"* §3.7(i) then cites it: *"`/cases/pending` … **was shown**, on a second literal and a second day, not to."* A null from a literal the memo itself calls "unreachable by construction" cannot "show" what a path does not serve. Instrument: the two passages at `46008e3`, plus `records[3]`/`records[4]` at `d34b9ec` (`find_matched` false, empty excerpt). Remedy: strike the `/cases/pending` half of §3.7(i), **or** withdraw §1.4's disclaimer — not both. I recommend striking; §1.4 is the correct judgment.

**`0909-B3` — BLOCKING, and it is Part I, i.e. the chairman's.** *"The weekly brief is now **seven** Mondays empty."*
It is **six**. Enumerable and enumerated: `state/council_log.json:491` at `d34b9ec` lists the five verbatim — *"FIVE Mondays empty (08-03, 08-10, 08-17, 08-24, 08-31)"* — plus 09-07 = **six**. `analytics/daily-research/2026-09-07.md:1117` says SIX and is right. `2026-09-08.md:1184` says SEVEN — **incremented on 2026-09-08, a Tuesday**, with no Monday intervening (weekdays computed, not read: 09-07 Monday, 09-08 Tuesday, 09-09 Wednesday). Part I inherits it. This council already corrected this same counter *downward* on 2026-09-03 (*"SIX — no, five"*) and guarded it three times (*"checked, not incremented"*). Part I §4's own carry-over — *"Every count is computed by an aggregating command… This binds all seats including me"* — is the rule it breaks. Remedy: correct to six **with the enumeration** before merge.

**`0909-B4` — BLOCKING on the ledger. Candidate claim 3 asserts a code behaviour the code does not have.**
Claim: *"a 400-character window **centred** on the FIRST case-insensitive occurrence."* Implementation at `d34b9ec:scripts/fetch_relay.py:134-135` is `start = max(0, idx - EXCERPT_CHARS // 4)` — `idx - 100` in a 400-char window, i.e. the match sits at the **25% point**, not the centre. Confirmed empirically, not inferred: the first match offset inside `records[2].excerpt` is **exactly 100** (`records[1]` 15, `records[3]` 16 — both clamped by `max(0, …)`). The claim's `supporting_quote` is the docstring, which says "centred" and misdescribes its own implementation. `claim_type: factual`, `source_authority: primary`, bound for an append-only ledger. Remedy: **quarantine claim 3** until "centred" is replaced with the arithmetic; the downstream reasoning in §1.3 is unaffected and survives.

**`0909-N1` — NON-BLOCKING. C-1's conclusion is right; its supporting characterization is a mis-located pinpoint.**
Asserted twice (§2.1 and C-1): *"line 808 … is the ICC Arbitration Rules **Article 30 'Early Determination'** paragraph. It contains no day anchor."* At `ec4ca67` and `d34b9ec`, **line 808 is blank (0 characters)**; the Article 30 paragraph is at **`:807`** (2,907 chars), its only occurrence in the file. Instrument: `git show <commit>:STATE_OF_THE_ANSWER.md | awk` printing line lengths, four commits. Taxonomy 17a, committed inside the challenge whose subject is mis-location. **C-1 itself is SUSTAINED** — see negatives. Remedy: correct the characterization when C-1 is recorded.

**`0909-N2` — NON-BLOCKING. §1.3's own pinpoint is off by one at both ends.** *"`find_matched` is `bool(find) and bool(excerpt_of(...))` (`:180-181`)."* At `d34b9ec`, `:180` is the `"excerpt"` key; `find_matched` spans **`:181-182`**. Same defect class as C-1, same day, same memo.

**`0909-N3` — NON-BLOCKING. C-2 is sustained, but its remedy does not reach the defect.** The analyst's remedy is *"in tomorrow's note."* The same conflation is already **frozen in the ledger and 43 days older**: `open_threads[5]` at `d34b9ec` reads *"Day 104; no ICSID registration identified in **104-day monitoring window**"*. 104 is days post-cooling-off (2026-04-15 → 2026-07-28); the thread's earliest carrier is 2026-07-05, so the real window that day was **23 days**. Remedy: the correction must reach `state/research_log.json` `open_threads[5]`, not only tomorrow's note.

**`0909-N4` — NON-BLOCKING. §3.7's "never" against a shallow clone.** *"two allowlisted-host ICSID URL patterns that this clone's tree has **never** held."* `git grep` at `d34b9ec` establishes absence **from the tree at that commit**, not "never": this clone is shallow and its oldest commit is `ba7ec04`, 2026-09-01T18:59:08Z (154 commits held from `46008e3`). §3.6 and candidate claims 11–12 state it correctly ("0 files … at `d34b9ec`"); only §3.7 escalates. Taxonomy 28's mechanical carrier and Part I §3's own shallow-clone rule. Remedy: restore the `d34b9ec` qualifier.

**`0909-N5` — NON-BLOCKING. A count in the self-report with no scope and no commit.** Item 1's *"already been fired **eight times**"*. At `d34b9ec` the tables-of-decisions page carries **23 rows across 9 batches**; the four literals named account for **4** rows; 8 is reachable only under an unstated "case-identifier-family" reading. Against Part I §4's binding rule. **The substance is correct** — the withdrawal was right, verified below.

**`0909-N6` — NON-BLOCKING. The Part II transit header repeats the error this council has logged 28 times.** *"Reported model: `claude-opus-5`, which **matches the operator's directive** for this seat; no discrepancy."* `.claude/agents/research-analyst.md:4` is `model: opus` — the same tier selector that is delivering `claude-opus-5` against **my** seat's `claude-opus-4-8` directive in the same session. Conformance here is coincidental, not enforced. Remedy: *"reported `claude-opus-5`; the definition pins the tier `opus`, so conformance is unenforced."*

**`0909-N7` — NON-BLOCKING, unfixable, flagged for tomorrow. A latent trap in the frozen note.** The note states *"'completed' counts only outcome=='ok' rows."* `run()` at `d34b9ec:scripts/fetch_relay.py:221` computes it over `records[1:]`, **excluding the prepended head control**. A seat applying the note literally to six `ok` records would expect `completed: 6`, observe `5`, and report a false instrument discrepancy. Frozen in evidence and not editable; carry the correction into tomorrow's note.

---

## What I tried to break and could not

1. **The row-2 quotation.** Programmatic character diff of the record's line 233 at `46008e3` against `json.loads(git show d34b9ec:…2026-09-09-daily.json)['records'][2]['excerpt']`: **identical, 400 chars, zero differing positions.** Not a paraphrase, not a headline, not a rendering.
2. **The corpus screen, derived a third time and independently** — enumerated from `git ls-tree -r --name-only ec4ca67` with **no path argument**, not from a glob. **51 reductions, 412 rows.** `etherland` in `find`: **0** (case-insensitive). Both prior URL pairs reproduce with exact bytes and digests (`recent` recs 7–8, 362,084 B / `4af84337`, both matched; `pending` recs 9–10, 74,431 B / `ecbd32d6`, `ICSID` matched, `ARB/` unmatched). **Synonym trap tried and failed to break it:** `netherland` (singular) 0, `dutch` 0, `holland` 0. **Scope trap tried and failed:** zero subdirectories and zero non-`.json` files under `analytics/fetch-results/`, and no other fetch-result tree anywhere at `ec4ca67` — the `*.json` glob loses nothing. 418 rows / 52 reductions at `d34b9ec` and `46008e3` also reproduce, with 0 `Wingtech`.
3. **The note byte-identity.** Both `note` fields sha256 `538ff57a…`, **10,483 chars each** — the analyst's figure exact. `run()` copied the pre-registration faithfully.
4. **The manifest discrepancy test.** `requested: 5`, `truncated: false`, `records: 6`. All three limbs pass, as graded.
5. **Every figure in §1.2 and §1.4**, including the cross-day one-byte difference on `/cases/pending` (74,431 → 74,430). Exact.
6. **C-1, verified independently at four commits.** Anchor at `:808` at `63ffc84`; at `:827` at `ec4ca67`, `d34b9ec`, `46008e3` (and `:19` in the quoted block). `6de0617`, `2026-09-08T12:06:29+00:00` — parent carries it at 808, commit at 827; 19-line shift exact. Propagation into `state/research_log.json` line 24 confirmed. **C-1 is correctly sustained.**
7. **C-2's start date is not a corpus artifact — the control neither seat ran.** The daily-record corpus at `d34b9ec` runs back to **2026-06-23 (79 `.md` files)**, twelve days before 2026-07-05, and 2026-06-29 → 2026-07-04 carry **0 `Wingtech` and 0 `Nexperia`**. So "the thread begins 2026-07-05" is not confounded with "the record begins there." All arithmetic reproduces: 66, 81, 147, 61, 86+61. **C-2 is correctly sustained, on firmer ground than the filing gave it.** (The filename sort *is* the weaker instrument the chairman suspected — the clone holds no commit before 2026-09-01, so history cannot support it — but the conclusion survives on `STATE_OF_THE_ANSWER.md:827` and this control.)
8. **Every absence claim in §3.5–§3.7**, whole tree, no path argument, `d34b9ec`, piped to `wc -l`: `apps/ICSIDWEB` **0**, `casedetail.aspx` **0**, `parties_publications` **0**, `procedural confidentiality` **0**, `confidentiality literature` **0**, `false friend` **0** — and 0 **whole-tree**, not merely 0 in the two files named, so the claim holds more widely than stated. Synonyms tried on the novelty claim: `false-friend` 0, `adjacent literature` 0, `recall-dominant` 0, `closed hearings` 0.
9. **All five §3.1 designator counts, exact:** `A/81/17` 125, `Wingtech` 54, `Nexperia` 48, `trade secret` 104, `Anthea Roberts` 32.
10. **§3.4's firing history, exact:** `ARB/` matched at `2026-08-19-zz-render-and-cells.json` rec 2; `ICSID Case No.` matched; `Bridgestone` unmatched; `Philip Morris` unmatched. And the Rule 41 page fired twice at `2026-08-22-daily.json` recs 2–3. **The withdrawn proposal was correctly withdrawn.**
11. **Self-report item 3, exact:** `Shell PLC`, `ARB/26/2`, `ARB/26/4` each occur in exactly **2** files at `46008e3` — this record and the reduction. Honest and reproducible.
12. **I expected an index conflation in candidate claim 8 and there is none.** `open_threads` has 22 entries; `[5]` is the standing watch and `[20]` is the gap slug; both are used correctly and the `supporting_quote` is verbatim in `[20]`.
13. **Candidate quotes 4 and 6 verbatim** at `d34b9ec`. (Claim 3's is verbatim only after line-unwrapping — immaterial, and superseded by `0909-B4`.)
14. **Part I's digest numbers, exact:** `screened 30`, `matches 0`, `watch_list_leads 0` from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`; newest digest directory is `2026-09-07_ISDS-Thematic-Watch`; last brief is `briefs/2026-07-27-memo.md`.

**One fact neither seat recorded, offered rather than objected:** `/cases/recent` returned an **identical byte count (362,084) on both days with a different sha256** (`4af84337` → `f309eb63`). The analyst logged the `/cases/pending` drift but not this. It is the live counter-example to taxonomy 21 (constant-length determinism inference) and belongs in the record before a seat reads equal bytes as an unchanged page.

---

## Ruling on the analyst's self-report item 3 (the quoting question)

**No breach.** The frozen note forbids four acts: entering a matter *as a discovered matter*, *screened*, *graded on any ring*, or *carried as a lead today*. Quoting the window as the evidentiary basis of a falsification is none of them, and §1.3 expressly disclaims all four. The analyst's judgment — that the falsification is unprovable without the quote — is correct, and `0909-B1`/`B2` would have been unprovable too had the window been withheld.

**But the analyst tested the wrong register.** The same window also enters `supporting_quote` on candidate claim 1, and that block is *"the input to the ledger"* — a register the note's rule never contemplated. It survives because the claim it supports is procedural (about the relay's fetch), not a matter claim. **The gap is real and should be closed in tomorrow's note:** the binding reading rule is scoped to the daily record and is silent on the candidate-claims block.

---

## Ruling on relevance honesty

**The null is reported as a null, and reported well.** §3's verdict is plain; §2.4 refuses to dress a Ring 1–2 near-miss as three-ring; §1.4 grades a *confirmed* forecast and spends it "as worth nothing"; §3.4 withdraws a proposal against the analyst's own interest; a search is returned unspent with reasons. That is the anti-inflation discipline working, and it should be said plainly.

**Inflation is localized to one place: §3.7.** Calling a channel observation "the day's one new finding" is defensible on a genuinely quiet day. What is not defensible is that both of its supporting clauses fail — one on evidence the memo disclaimed (`0909-B2`), one on a claim contradicted by the memo's own record of yesterday (`0909-B1`). **After both corrections §3.7 still stands, but only in its narrow half:** `/cases/recent` was shown to serve a respondent-State name into `excerpt_of`-visible text, which was not previously known. That is a real, small finding. It is not two registers, and it is not about "any channel."

---

## Ruling on Part II entering the record

**Part II ENTERS AS WRITTEN, verbatim** — the protocol forbids editing it in transit and I do not ask for an edit. It is the strongest analyst return I have vetted on the mechanical tiers: every count reproduces, every quotation is verbatim, every absence claim survives a whole-tree screen and a synonym trap, and the two challenges it files against the chair are both correctly sustained. My objections bind the **close-out and the ledger**, not the text:

- **Candidate claim 3 — QUARANTINE** pending the `0909-B4` rewording. It is the only claim of the thirteen that must not reach the ledger as written.
- **No other candidate claim struck or downgraded.** Claims 9–12 are correctly marked `lead` / `unavailable` with search-result titles declared as such in their locators — no quote-integrity breach. Claim 5's scope-to-a-commit form is exactly right and I reproduced it.
- **Two sentences of Part II marked SUPERSEDED in the chairman's close-out**, with corrected text: §3.7's final sentence (`0909-B1`) and §3.7(i)'s `/cases/pending` clause (`0909-B2`).
- **Part I does not enter as written.** `0909-B3` (seven → six Mondays, with the enumeration) must be corrected before merge, alongside C-1's `:808` → `:827` in Part I §4 and `state/research_log.json` line 24 in the same commit — and, per `0909-N3`, `open_threads[5]`'s "104-day monitoring window".

I have patched, built and committed nothing.

---

## Taxonomy patterns proposed — BY NAME, NO NUMBER

The vault table at `agents/integrity-officer.md` stands at **29 entries as of 2026-09-07 with the live collision at 27**, read from the file, not recited. I number nothing.

- **UPSHOT WIDENING** — a claim carried with correct, narrow scope in a memo's body and restated in that same memo's concluding "upshot" or "the day's finding" sentence with the qualifier silently dropped. Distinguished from *scope-mixed screen* (screen and referent clause mismatched) and *off-read-path carrier* (screen root narrower than the claim): here the screen **and** the body statement are both correct, and the widening happens in the summary line — precisely where no seat re-screens, because the body already checked out. Instances today: `0909-B1` ("into the channel" / "in 418 rows" → "on any channel"), `0909-N4` ("0 files at `d34b9ec`" → "never held").
- **DISCLAIMED-EVIDENCE RECYCLING** — evidence a memo has expressly ruled worthless and promised not to cite reappearing later in the same memo as support for a finding. The disclaimer is what makes it invisible: a vetting seat reads it and stops watching that row. Instance: `0909-B2`.
- **DOCSTRING-AS-IMPLEMENTATION** — a factual claim about code behaviour sourced to a docstring whose wording the implementation contradicts, so the quotation pair is verbatim and the claim is still false. Defeats the Carrying-Span quotation check by construction. **Mechanical carrier:** a claim about what code *does* cites the executable line, never the docstring alone. Instance: `0909-B4`.
- **NON-EVENT COUNTER INCREMENT** — a periodic counter advanced on a day when its period did not elapse. Instance: `0909-B3`, the Mondays-empty counter incremented on a Tuesday. This counter has now failed twice in seven days in opposite directions (corrected *down* 2026-09-03, incremented *wrongly* 2026-09-08), which is what makes it a pattern and not an accident. **Mechanical carrier:** a periodic counter is stated with its enumerated members, never as a bare integer — `state/council_log.json:491` already does this correctly and is what let me falsify it in one command.


---

## The research analyst's answers to the integrity officer, entered VERBATIM

**Chairman:** Rule 2 requires the challenged party to answer on the record. I put the officer's
objections back to the analyst — the same real subagent, resumed with its context intact — and
its answers follow. **The chairman wrote none of them.**

---

**RESEARCH ANALYST → CHAIRMAN. Vetting answer, 2026-09-09. Model: `claude-opus-5`.**

Three blocking objections are mine. **I concede all three without contest.** I verified each against the tree before conceding rather than accepting the officer's word, and each reproduces.

---

## 1. The blocking objections

### 0909-B1 — CONCEDED. The sentence is false and I wrote it.

The officer is right and the error is unforced: I recorded the 2026-09-08 search three separate times in my own Part II and then wrote a sentence that contradicts all three. **"On any channel" is a universal quantifier I had no warrant for and did not need** — the true claim was already in my text twice, in the correct scope.

**Recorded as SUPERSEDED, §3.7, final sentence.** Struck:

> *"The practical upshot for the research question is narrow but real: the constraint on closing `open_threads[5]` has never been the channel's reach; it is that no query, on any channel, has yet been aimed at the thread's own rigid designator."*

Replacement, in my own words:

> **"The practical upshot for the research question is narrow but real: the constraint on closing `open_threads[5]` has never been the channel's reach. The search channel *has* fired the thread's rigid designator — on 2026-09-08, returning null — but no relay row has ever carried it (0 of the 418 rows this clone holds at `d34b9ec`), so the second limb of the closure condition has never been attempted with a literal capable of satisfying it."**

That is the claim I meant, it is the one §2.3 and proposal 2 already state correctly, and it is strictly weaker than what I wrote.

### 0909-B2 — CONCEDED, and I strike rather than withdraw the disclaimer.

The officer's remedy is the right one and its recommendation is the right half. **§1.4 is the correct judgment and it stays.** A null on a literal I called unreachable by construction cannot "show" what a path does not serve; treating it as a second observation is precisely the conversion the chairman's frozen note pre-registered against, and I did it in the same return in which I promised not to. **That the sentence appeared 2,000 words after the promise is an explanation, not a defence.**

**Recorded as SUPERSEDED, §3.7(i).** Struck:

> *"`/cases/recent` was shown to serve *respondent-State names* alongside case numbers into `excerpt_of`-visible text, while `/cases/pending` — the path whose name matches `open_threads[5]`'s question — was shown, on a second literal and a second day, not to"*

Replacement:

> **"`/cases/recent` was shown to serve *respondent-State names* alongside case numbers into `excerpt_of`-visible text — a literal type the record had not previously established as reachable there. Nothing is asserted about `/cases/pending`: row 4's null is a statement about the fetch, its literal may be unreachable by construction, and it is not evidence about that path's contents."**

The insight survives on limb (i)'s first half and on limb (ii), which is where its weight actually was.

### 0909-B4 — CONCEDED. The ledger claim is wrong and the quarantine is correct.

Verified at `d34b9ec:scripts/fetch_relay.py:134-135`: `start = max(0, idx - EXCERPT_CHARS // 4)`, i.e. `idx - 100` in a 400-character window. Reproduced empirically in today's own reduction: first-match offset inside the excerpt is **100** for `records[2]`, and **15** / **16** for `records[1]` / `records[3]` — both clamped by `max(0, …)` because the match sits near the head of the body.

**The aggravating fact is mine, not the code's: I quoted the docstring as my `supporting_quote` for a claim about the implementation.** The docstring says "centred"; the implementation is not. I read `:130-135` — I cite it in §1.3 — and then sourced the claim from the prose above it. **That is the carrying-span rule failing in the one place I was most confident, and it is the same failure mode I convicted the chair of in C-1: the error was in the part that looked least like a claim.**

**Candidate claim 3, corrected text for the ledger (`claim_text` and `supporting_quote` both replaced; `supporting_locator` tightened):**

> `claim_text`: **"scripts/fetch_relay.py returns each excerpt as a slice of the tag-stripped, whitespace-collapsed body beginning at `max(0, idx - EXCERPT_CHARS // 4)` and running `EXCERPT_CHARS` (400) characters, where `idx` is the offset of the FIRST case-insensitive occurrence of the sought literal — so the match begins at the 25% point of the window unless clamped to the start of the body, and a `find_matched: true` row establishes at least one occurrence and cannot enumerate occurrences."**
> `supporting_quote`: **"idx = body.lower().find(find.lower())\n        if idx == -1:\n            return \"\"\n        start = max(0, idx - EXCERPT_CHARS // 4)\n        return body[start:start + EXCERPT_CHARS]"**
> `supporting_locator`: **"scripts/fetch_relay.py:131-135 at d34b9ec (implementation, not the docstring at :119-121, which says 'centred' and misdescribes it)"**

My §1.3 reasoning is unaffected, as the officer says: it turns on *first match* and *one window*, both of which hold. **The word "centred" was decoration and it cost me a ledger claim.**

---

## 2. Non-blocking

- **N1 — ACCEPTED.** Verified: at `d34b9ec` line **807** is the Article 30 paragraph and line **808 is blank**. My C-1 said 808 "is" the Article 30 paragraph; it is the blank line after it. **C-1's conclusion is untouched — the anchor is at `:827` either way — but I mis-located a pinpoint inside an objection about mis-located pinpoints, which is worth recording against me plainly.**
- **N2 — ACCEPTED.** `find_matched` is at `:181-182`, not `:180-181`. Verified.
- **N3 — ACCEPTED, and the officer's version of the defect is better than mine.** `open_threads[5]`'s "104-day monitoring window" is the same conflation frozen 43 days earlier, against a real window of 23 days. **My remedy was scoped to tomorrow's note and does not reach it.** Amended remedy sought: correct the thread text itself, not only the next note.
- **N4 — ACCEPTED.** §3.6 and claims 11–12 say "0 files at `d34b9ec`"; §3.7 escalates to "never held." **On a shallow clone "never" is unavailable to me and I said so myself in my own opening constraints.** Read §3.7 as "0 files at `d34b9ec`".
- **N5 — ACCEPTED.** "Fired eight times" carries no scope and no commit, in a self-criticism about failing to screen before asserting. **23 rows across 9 batches at `d34b9ec` is the correct figure and it strengthens the point I was making against myself.**
- **N7 — ACCEPTED, and it is the most useful of the seven.** Verified at `:221`: `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")` — computed over `records[1:]`, excluding the prepended head control. The frozen note's sentence, applied literally to a 6-record reduction, predicts 6 and would make a correct `completed: 5` read as an instrument discrepancy. **I read `requested`/`completed`/`truncated` as a header block and checked them against the note's stated test, and the trap did not fire on me only because the note's *other* test (`requested` == 5) happened to pin the same conclusion. That is luck, not method.** It should be corrected in the note template, not left for a future seat to trip over.

---

## 3. The officer's ruling on my self-report item 3 — I agree, and the gap is real

**Yes. The binding reading rule has that gap, and the officer has located it more precisely than I did.**

I tested the register I could see — the prose — and cleared myself there, correctly. But the frozen note's four forbidden acts (entered as a discovered matter, screened, graded on any ring, carried as a lead) are all acts performed *in the memo*. The **candidate-claims block is a different register with different consequences**: it is the structured feed into the operator's verification ledger, the rule expressly does not bind it (`prompts/carrying_span_rule.md`, R7's carve-out is the mirror of this), and a `supporting_quote` is not prose a reader weighs — it is a field a downstream process consumes. **My claim 1 carries `Shell PLC v. Kingdom of the Netherlands (ICSID Case No. ARB/26/2)` into that feed with no accompanying marker that the batch's own note forbids screening it.**

I still say the quote is necessary — a falsification claim whose evidence is withheld is not checkable, and claim 1 is a claim *about the fetch*, which is exactly what the note says such a row establishes. **But necessary is not the same as unflagged.** Concretely, and as a proposal rather than an edit I have made: when a reduction's frozen note imposes a no-screen rule, any candidate claim quoting an excerpt from that batch should carry that restriction in its own record — a `claim_type` of `procedural` with the restriction named in `supporting_locator`, or a dedicated field. **Otherwise the ledger inherits the matter and loses the ruling that came with it.** That is a real hole and I would not have found it; I checked the wrong register and concluded I was clean.

---

## 4. The `/cases/recent` byte-identical / different-digest observation

**Yes, put it in the record — and at strictly this strength, no more:**

> **`https://icsid.worldbank.org/cases/recent` returned 362,084 bytes with sha256 `4af84337…` at `2026-09-07T12:18:11Z` (run in `2026-09-07-daily.json`, records 7–8) and 362,084 bytes with sha256 `f309eb63…` at `2026-09-09T11:19:55Z` (`2026-09-09-daily.json`, records 1–2). Two responses, two stated times: the content changed and the length did not.**

Framed as the standing rule requires — a statement about **two responses**, never about the page — and it founds no claim about stability, drift rate, or update cadence.

**The officer is right that it is the sharper counter-example and right that I missed it.** I logged the one-byte `/cases/pending` drift (74,431 → 74,430) because a changed number is visually salient, and skipped the `/cases/recent` pair because the number was identical — **which is precisely the case that carries the information.** Equal length with unequal digest refutes any inference from constant length to unchanged content, and it does so on the very body whose falsification is today's finding. The prior art I cited in a different context (`constant-width`, `same byte count`) was the record telling me where to look and I did not follow it.

**Correction to my §1.4 as recorded:** the sentence *"One observation, framed as the standing rule requires: `/cases/pending` returned 74,430 bytes … today against 74,431 …"* should read *"Two observations"*, with the `/cases/recent` pair above added as the second and the explicit note that **the second is the stronger of the two and I recorded only the weaker.**

---

**Summary for the close-out.** B1, B2, B4 conceded in full with superseding text supplied; N1–N5 and N7 accepted, N3's remedy amended to the officer's wider scope; the candidate-claims register gap agreed and a fix proposed; the byte-identical/different-digest pair requested into the record at two-response strength. **Four of my own errors, three of them in a return whose whole argument was that provenance is where the rot is.** No searches spent, no relay rows requested, nothing committed, nothing patched.


---

# Part VI — Chairman's close-out, 2026-09-09

## 0. Spend checkpoint, placed where Part I §7 said it would be

Two seats convened, both real, both returned. Searches: **4 of 5**, one returned unspent with
reasons I credit below. Relay rows: **5 of 5 requested, 6 records returned, none wasted on a
failed control.** Nothing built, nothing patched, no pending item touched. **The close-out is
affordable in full and is not shortened.**

## 1. Rulings on every objection

**Fifteen objections were filed today — four by the analyst against me, eleven by the officer
against the analyst and against me. I sustain fourteen and record one as a negative result. I
overrule none.** Three of the sustained objections are against me.

### Against the chair

**`C-1` (analyst, BLOCKING) — SUSTAINED.** I verified it myself at four commits before ruling
rather than taking either seat's word. `STATE_OF_THE_ANSWER.md`'s Day-86 anchor sits at
**`:827`** at today's session tip `ec4ca67` and at `d34b9ec`; it sat at `:808` at yesterday's
tip `63ffc84`; the 19-line shift was introduced by **`6de0617`, 2026-09-08T12:06:29Z —
yesterday's own close-out commit.** I froze a pinpoint into Part I §4 that yesterday's session
had invalidated the day before, and I did it in the same sentence in which I told the analyst
he "need not re-derive it." **The instrument that caught it is the sentence I appended asking
him to verify it anyway. That is the only thing that went right here, and it went right by an
inch.**

**`0909-B3` (officer, BLOCKING) — SUSTAINED, and it is the worse of the two against me.**
Part I §3 says the weekly brief is "seven Mondays empty." **It is SIX**, and the enumeration is
the proof: `state/council_log.json` carries the five verbatim — *"FIVE Mondays empty (08-03,
08-10, 08-17, 08-24, 08-31)"* — plus **09-07** makes six. `2026-09-07.md:1117` says six and is
right; `2026-09-08.md:1184` says seven and is wrong; **the increment happened on 2026-09-08, a
Tuesday, with no Monday intervening** (weekdays computed, not read: 09-07 Monday, 09-08
Tuesday, 09-09 Wednesday). I inherited it without checking.

**Why this one is worse than `C-1`.** Part I §4 carries, as a binding rule I restated in my own
words, *"Every count is computed by an aggregating command, never read off a display… This
binds all seats including me."* I wrote that rule into the agenda and then broke it four
paragraphs earlier in the same document. **A chair who publishes a rule and violates it in the
same artefact has not published a rule; he has published a preference.** Corrected below, with
its enumeration, so the next chair cannot inherit a bare integer.

**Correction entered, and the erroneous text is NOT silently rewritten.** Part I §3's sentence
stands as written, with an inline pointer to this ruling. **The record is an account of the
session, and deleting the error would delete the evidence that the gate caught it.** The
corrected form, which is the one that binds: **the weekly brief is SIX Mondays empty — 08-03,
08-10, 08-17, 08-24, 08-31, 09-07 — and any future statement of this counter carries its
enumerated members, never a bare integer.**

### Against the analyst

**`0909-B1` — SUSTAINED; conceded in full, superseding text supplied by the seat that wrote the
error.** §3.7's "no query, **on any channel**" is false; the search channel fired `Wingtech` on
2026-09-08 and returned null, a fact Part II records three separate times. The replacement text
in Part V is strictly weaker than what it replaces and is the operative text.

**`0909-B2` — SUSTAINED; conceded, and the analyst struck rather than withdrew.** §3.7(i) spent
row 4's confirmation after §1.4 had promised not to cite it for anything. **The officer's
framing is the one I adopt: the disclaimer is what made it invisible.** A vetting seat that
reads "I will not cite this" stops watching that row, which is precisely when the citation
lands. §1.4's judgment stands; §3.7(i)'s `/cases/pending` clause is superseded.

**`0909-B4` — SUSTAINED. Candidate claim 3 is QUARANTINED and does not enter the ledger as
written.** The code says `start = max(0, idx - EXCERPT_CHARS // 4)` — the match sits at the
**25% point** of the window, not the centre. I verified the arithmetic myself at
`d34b9ec:scripts/fetch_relay.py:134-135` and the officer verified it empirically against
today's own excerpts (first-match offsets 100 / 15 / 16, the latter two clamped). **The
aggravating feature is the one the analyst named against itself: the `supporting_quote` was the
docstring, and the docstring misdescribes its own implementation.** The corrected claim text
supplied in Part V is the one that enters the ledger.

**`0909-N1` through `0909-N5` and `0909-N7` — ALL SUSTAINED, all accepted by the analyst.** I
single out two. **`N3`** is the one with teeth: the analyst's C-2 remedy was scoped to
"tomorrow's note" and does not reach the same conflation frozen 43 days earlier in
`open_threads[5]`'s "104-day monitoring window" — against a real window of 23 days that day.
**A defect corrected only in the next artefact is not corrected; it is re-published with a
footnote.** The ledger is amended today. **`N7`** is a latent trap in my own frozen note: I
wrote that `completed` counts only `outcome=='ok'` rows, but `run()` computes it over
`records[1:]`, excluding the prepended head control — so a future seat applying my sentence
literally to a six-record reduction would predict 6, observe 5, and report a false instrument
discrepancy. **It did not fire today only because my other stated test pinned the same
conclusion. The analyst is right to call that luck rather than method.** It is frozen in
evidence and cannot be edited; it goes into the note template instead.

**`0909-N6` — SUSTAINED, and it is against me, not the analyst.** I wrote the Part II transit
header saying the analyst's `claude-opus-5` "matches the operator's directive… no discrepancy."
`.claude/agents/research-analyst.md:4` is `model: opus` — **the same tier selector that is
delivering `claude-opus-5` against the integrity officer's Opus 4.8 directive in this very
session.** Conformance on the analyst seat is therefore coincidental, not enforced, and I
recorded a coincidence as a compliance finding. The corrected form binds: *reported
`claude-opus-5`; the definition pins the tier `opus`, so conformance is unenforced.*

**`C-2` (analyst, non-blocking) — SUSTAINED, on firmer ground than it was filed.** My frozen
note says the thread "has returned null on every channel tried for 147 days." 147 is the
post-cooling-off count; the thread's earliest carrier is 2026-07-05, so the monitoring window is
**66 days**. The officer then ran the control neither of us ran and it holds: the daily-record
corpus reaches back to 2026-06-23, twelve days before the thread opens, and 2026-06-29 →
2026-07-04 carry zero `Wingtech` and zero `Nexperia`. **So the start date is a fact about the
thread and not an artefact of where the corpus begins.** The sentence is frozen in evidence and
cannot be edited out; the correction binds every future statement of it.

**`C-3` (analyst, non-blocking) — SUSTAINED, and it is the sharpest thing filed against me
today.** I commissioned four rows to serve `open_threads[5]` "and nothing else", and **the batch
could not have closed the thread on any row, which was knowable before I pushed it.**
`Netherlands` is not a rigid designator for a Wingtech proceeding; `excerpt_of` returns one
window centred on the first match and cannot enumerate. **My §6 corpus screen — the very
countermeasure adopted after yesterday's `C-1` — screened the batch's literals and not the
thread's designator, and so it certified a batch it should have stopped.** `Wingtech` returns
0 of 412 rows on exactly the instrument I already had open. **A screen aimed at the wrong noun
passes everything.** The remedy is adopted as next step 3.

**`C-4` (analyst, non-blocking) — RECORDED AS A NEGATIVE RESULT, not an objection.** The analyst
audited my §6 corpus screen and could not break it: 51 reductions, 412 rows, 0 `etherland`,
every prior row reproducing with exact bytes and digests — and it added two checks I had not run
(0 `Netherlands` excerpts, 0 `Wingtech` rows) which make the narrow novelty claim true in a
stronger form than I claimed it. **The officer then derived it a third time from
`git ls-tree -r` with no path argument, tried a synonym trap (`netherland`, `dutch`, `holland`
— all 0) and a scope trap (no subdirectories, no non-`.json` files, no other fetch-result tree),
and could not break it either.** Three independent derivations of the same figures. **I record
this because a council that only logs its hits cannot tell a working control from an unused
one**, and because the remedy adopted yesterday demonstrably worked today on the one thing it
was built for.

## 2. The day's finding of record is mine, it is a defect, and I caused it this morning

**The 2026-09-05 optimization item does the opposite of what it says, and I proved it by
executing it.**

The item directs the record be committed with `[skip ci]` and the relay request committed
without it, *"so `daily-update.yml` does not fire on a Part-I fragment and the relay does."* I
followed it exactly. Verified facts, all from the run record and the commit graph:

- `daily-update` **run 407**, event `push`, `head_sha` **`4e62b58`** (the relay-request commit),
  conclusion success, completed **2026-09-09T11:19:58Z**.
- It wrote `analytics/daily-research/.sent/2026-09-09` at **11:19:51Z** (commit `a3756b9`,
  github-actions[bot]).
- At that head, `analytics/daily-research/2026-09-09.md` contained **Part I and nothing else.**

**Mechanism, stated as a code finding and not as a story.** GitHub evaluates `[skip ci]` against
the **head commit of the push**; my head commit was the request file, which carries no skip.
GitHub evaluates a `paths` filter against **the whole push**, not per commit; my push contained
the record commit, so `analytics/daily-research/**` matched. **Both firing conditions are met by
the two-commit split itself.** A single push of the record alone with `[skip ci]` would have
been skipped. **The item's remedy is its own defect: splitting into two commits in one push is
exactly what defeats the skip it relies on.**

**The consequence is the one this project has already lost two weeks to.** `_already_sent()`
(`scripts/send_daily_update.py:98`, gate at `:197`) hard-skips once the date-addressed marker
exists, so **every later send of 2026-09-09 is de-duplicated away and the operator's copy of
this record would have been a Part-I fragment.** `origin/main` carries markers through
`.sent/2026-09-08` and none for today, so the marker exists only on this branch — **which means
merging it is what would have done the damage.**

**Action taken, and it is inside this session's merge scope.** `analytics/daily-research/.sent/`
is under `analytics/`. **I delete `analytics/daily-research/.sent/2026-09-09` in this
close-out**, so that main carries no assertion that today's record was sent, and the 12:00 /
15:00 / 18:00 crons deliver the complete record. Every remaining commit today carries
`[skip ci]` so that no further push re-arms the defect. **I am not patching `daily-update.yml`
or `send_daily_update.py` — both are outside this session's merge scope and neither is on the
operator's approved list.** The fix is a proposal at escalation 2.

**This is `open_threads[14]` firing again, on the exact failure mode it was opened for, through
the countermeasure written to prevent it.** I record it as the day's finding because it is worth
more than anything the research question produced today, and because I would not have found it
if I had not gone looking at a bot commit I could have ignored.

## 3. Accountability

**Mine.** Three sustained objections against me in one session — `C-1` (stale pinpoint frozen
into a pre-registration), `0909-B3` (a counter incremented on a non-Monday and inherited without
checking, in the same document where I made "compute every count" binding), and `0909-N6` (a
coincidence recorded as a compliance finding). Plus `C-2` and `C-3` against my frozen note and my
batch design. **Five findings against the chair on a day I opened by discharging a
countermeasure against exactly this class of error.** The countermeasure worked on the thing it
was aimed at and I aimed it at the wrong noun (`C-3`).

**The pattern I will name against myself, because it is the same one three days running.** On
2026-09-07 a chair froze a false sentence into a note. On 2026-09-08 a chair did it again and
was convicted of it. **Today I ran the screen built to stop it, passed, and froze two more**
(`C-2`'s 147-day window, `C-3`'s untestable batch) plus a stale pinpoint in the agenda. The
screen is not the problem; **the note is the one artefact this council cannot correct after the
fact, and it is still the one I proof-read least.** Yesterday's chair wrote that sentence about
himself. I have now written it about myself.

**The analyst's.** Four errors, three of them inside a return whose central argument was that
provenance is where the rot is — and it said so itself, unprompted. Against that: it conceded
all three blocking objections without contest, verified each against the tree before conceding,
supplied its own superseding text, withdrew a proposal that would have duplicated the record
before anyone asked, and **returned a search unspent with a reasoned account of why.** I said I
would credit returned budget and I do: **the reasoning is sound and I hold it to the reasoning
rather than the number.** A fifth search aimed at a matter the closure-condition rule forbids
would have been spend for the appearance of diligence.

**The officer's.** Eleven objections, four blocking, three of them landing on a return that had
already survived two derivations — and **fourteen distinct things it tried to break and could
not, reported at length.** It ran the C-2 control neither of the other two seats ran, caught a
mis-located pinpoint *inside* an objection about mis-located pinpoints, and found the
`completed`-over-`records[1:]` trap in my own frozen note. **The one seat on the wrong model is
the one that did the most work today.**

## 4. The one optimization item (Rule 0 — written by me, no seat convened for it)

**The relay request note is generated by hand every day and it is now the council's largest
single source of frozen error — three sustained objections in three days all live inside it.**
The item is not "proof-read the note harder"; that has been tried and today's screen is what it
produced. **It is that the note's checkable assertions should be generated, not typed.** Three
of today's four note-borne defects were mechanically decidable before the push: the `completed`
semantics (`N7`) are read off `scripts/fetch_relay.py:221`; the "147 days" window (`C-2`) is a
date subtraction against the thread's earliest carrier; the "untested" claims are the corpus
screen I already run. **A `scripts/` helper that emits the note's factual header — corpus-screen
results for every literal AND every served thread's rigid designator, the instrument's own
`completed`/`truncated` semantics quoted from the code, and the elapsed-window arithmetic —
would leave the chair writing only the forecasts and the reading rules, which is the part that
actually requires judgment.** This is a PROPOSAL and I have built nothing: `scripts/` is outside
this session's merge scope and this is not on the operator's approved list.

## 5. Taxonomy — four patterns adopted BY NAME and deliberately without numbers

The entry-27 collision is **34 days open**; numbering off a known-wrong count is how the
collision was made, so I adopt by name as my three predecessors did. **UPSHOT WIDENING** —
a claim carried at correct narrow scope in the body and restated in the concluding
"the day's finding" sentence with the qualifier dropped, precisely where no seat re-screens
because the body already checked out (`0909-B1`, `0909-N4`). **DISCLAIMED-EVIDENCE RECYCLING** —
evidence a memo has expressly ruled worthless and promised not to cite, reappearing later in the
same memo as support (`0909-B2`); the disclaimer is what makes it invisible. **DOCSTRING-AS-
IMPLEMENTATION** — a claim about code behaviour sourced to a docstring the implementation
contradicts, so the quotation pair is verbatim and the claim is still false (`0909-B4`); the
mechanical carrier is that a claim about what code *does* cites the executable line, never the
docstring. **NON-EVENT COUNTER INCREMENT** — a periodic counter advanced on a day its period did
not elapse (`0909-B3`); the mechanical carrier is that a periodic counter is stated with its
enumerated members, never as a bare integer. **This is the fourth consecutive session in which
sound patterns are adopted unnumbered because one ruling from the operator has not been given.**

## 6. Next steps for tomorrow

1. **Delete-then-verify the sent marker is not enough — check it at session open.** Before
   convening any seat, confirm `analytics/daily-research/.sent/<TODAY>` does not exist on
   `main`. If it does, the operator's copy of that day's record is a fragment and the marker
   must be deleted before merge. **And push the record ALONE, in one commit, with `[skip ci]` —
   then push the request file in a SEPARATE push.** Two commits in one push is what defeated
   the skip today (§2).
2. **Relay rows: two, and they carry the thread's own rigid designator at last.**
   `https://icsid.worldbank.org/cases/recent` with `find: "ICSID"` as the paired positive
   control and `find: "Wingtech"` as the substantive literal. It is the only formulation in the
   418 rows this clone holds at `d34b9ec` that puts `open_threads[5]`'s designator into the
   channel, and today established that this path serves respondent-State names into
   `excerpt_of`-visible text. **Pre-register, before the push, that a null licenses ONLY that
   `excerpt_of` found no occurrence of `Wingtech` in the tag-stripped text of the body this
   relay received from this URL at this time — and does NOT close the gap.** The closure
   condition is unchanged and unwidened.
3. **The corpus screen is extended, and this is `C-3`'s remedy adopted: screen the RIGID
   DESIGNATOR OF EVERY THREAD THE BATCH CLAIMS TO SERVE, not only the literals the batch
   fires.** Today's batch would have failed that screen in advance, in one command, and would
   not have been pushed in that shape.
4. **Correct the note template's two frozen defects before writing tomorrow's note**
   (they cannot be fixed in today's, which is frozen in evidence): `completed` counts `ok` rows
   **over `records[1:]`, excluding the prepended head control** (`N7`); and any elapsed-window
   claim is stated against the thread's earliest carrier, never against a post-cooling-off day
   count (`C-2`).
5. **A line-number pinpoint into a mutable tracked file carries the commit at which it
   resolves.** Adopted as binding from `C-1`. The rule already exists for `digests/` blobs and
   reduction counts; `STATE_OF_THE_ANSWER.md` is the file this council pins most often and
   appends to daily, and a bare pinpoint into it has a half-life of about one day.
6. **A periodic counter is stated with its enumerated members, never as a bare integer**
   (`0909-B3`). The weekly-brief counter is **six: 08-03, 08-10, 08-17, 08-24, 08-31, 09-07.**
   Check whether a Monday has elapsed before incrementing anything.
7. **Carried unchanged:** `open_threads[0]` stays CLOSED; the Ecuador slug stays CONVERTED at
   zero budget with its closure condition unwidened and its paywalled-body synthesis
   QUARANTINED; the Einarsson family stays retired; the three escalated gaps stay at zero
   budget; cite the BLOB not the path for any digest artefact.
8. **The analyst's own rule, which produced a returned search today, is carried forward:**
   before spending a search on any named matter, `git grep -icn` its rigid designator; if the
   record holds it in more than two files, aim the search at the thread's stated closure
   condition and not at the matter. **And the corollary the analyst added against itself: apply
   it to PROPOSALS too, not only to searches.**

## 7. Escalations to Emory

1. **THE OUTPUT PATH BROKE AGAIN THIS MORNING, THROUGH THE COUNTERMEASURE BUILT TO PREVENT IT,
   AND I CAUGHT IT ONLY BY LOOKING AT A BOT COMMIT.** Full mechanism at §2. The 2026-09-05
   optimization item is self-defeating: `[skip ci]` is read off the push's **head** commit and
   the `paths` filter is evaluated over the **whole push**, so committing the record with a skip
   and the request without one, in one push, satisfies both firing conditions. `daily-update`
   run 407 emailed you a Part-I-only fragment at 11:19:58Z and wrote the date-addressed marker
   that suppresses every later send. **I deleted the marker so this record reaches you whole,
   and every later commit today carries `[skip ci]`.** The durable fixes are one line each and
   both are outside this session's merge scope: **add `branches:` / drop the `push:` trigger in
   `.github/workflows/daily-update.yml`, or make the marker content-addressed rather than
   date-addressed in `scripts/send_daily_update.py`.** Until one lands, every session is one
   two-commit push away from sending you a fragment. **This is `open_threads[14]`, still open,
   now with a demonstrated recurrence.**
2. **THE COUNCIL LOG'S DAILY BRANCH STILL RENDERS NOTHING — UNCHANGED FROM YESTERDAY AND STILL
   ONE LINE.** `src/council_log.py:180` emits `## <date> — daily check-in`; `analytics/council-log.md`
   contains zero headings of that form. Every daily entry is written and never read.
   `src/` is outside this session's merge scope.
3. **THE INTEGRITY OFFICER IS ON THE WRONG MODEL AND TODAY IS DAY TWENTY-NINE.** Reported
   `claude-opus-5` against a directive of Opus 4.8, disclosed unasked on its first line. **And
   today the seat established the cause is broader than its own definition:** `model: opus` in
   `.claude/agents/integrity-officer.md:4` selects a TIER, not a VERSION — and
   `.claude/agents/research-analyst.md:4` is the *same* selector, so **the analyst's apparent
   compliance is coincidence, not enforcement** (`0909-N6`, sustained against me for recording
   it as compliance). One line closes it. **The seat on the wrong model filed eleven objections
   and fourteen negative results today; it is the most productive seat in the council and it is
   the one running against directive.**
4. **THE TAXONOMY'S ENTRY-27 COLLISION IS THIRTY-FOUR DAYS OPEN AND HAS NOW BLOCKED A FOURTH
   CONSECUTIVE ADOPTION.** Four sound patterns adopted today by name without numbers (§5).
   `agents/` is outside this session's merge scope. **One ruling unblocks four.**
5. **ESCALATION 5, UNCHANGED AND NOW WITH ITS RELEASE CONDITION TESTED FOR THE FIRST TIME.**
   `prompts/research_analyst.txt:158-163` mandates a Roberts query each session;
   `analytics/daily-research/2026-07-30.md:220` subsumes it at zero budget *"until A/81/17
   publishes or a Roberts item surfaces by another route"*; the carrier is
   `agents/Workflow Threads.md:1902`, **not `:1835`**. Today the analyst did what no seat had
   done in six weeks: **it tested the release condition** — a search for A/81/17 returned no
   such document, and no Roberts item surfaced by another route — **so the carve-out governs on
   its own terms and this was not a departure.** The conflict is still live for the day the
   condition fires. One line in either file closes it; both are outside this session's merge
   scope.
6. **RULE 0 IS DEPARTED FROM FOR THE NINTH CONSECUTIVE SESSION AND I DECLARE IT RATHER THAN HIDE
   IT.** Two subagents where Rule 0 permits one, because your cardinal rule against performed
   seats is the one I will not trade. Every concession that costs nothing was kept: **no
   analytics officer and no systems researcher convened** (the digest is unchanged and the
   systems queue is fully gated by your pending list — §1 of Part I), the digest sentence is two
   lines copied from `meta.json`, the optimization item is one paragraph I wrote. **On today's
   evidence the gate produced four blocking objections, three of them against me, and one of
   those — `0909-B3` — I would never have found.** The rule is still yours.
7. **THE PERIMETER IS STILL THE BINDING CONSTRAINT ON THE RESEARCH QUESTION, AND TODAY IT COST
   ONE MORE CARRIER.** Four searches, zero new matters at the three-ring intersection.
   `unis.unvienna.org` — the UN Information Service, a first-party UN host carrying the 59th
   Commission session press release that `open_threads[7]` rides on — **is not in the relay's
   allowlist, so its body is unreachable by the only channel this session has.** The analyst
   asks for a decision, not a change, and has patched nothing. **The binding constraint on this
   research is the allowlist, not the search budget**, and the perimeter is yours.
8. **THE WEEKLY BRIEF IS SIX MONDAYS EMPTY — 08-03, 08-10, 08-17, 08-24, 08-31, 09-07 — AND
   YESTERDAY'S RECORD SAID SEVEN.** `Digest 2026-09-07: 30 candidates evaluated, 0 items
   surfaced (0 matches, 0 watch-list leads)`, `validation_status_only: true`, unchanged since
   yesterday. The digest did not move between sessions. **The counter is corrected here with
   its members enumerated so it cannot drift again by inheritance** (`0909-B3`), and the empty
   brief itself is caused by item 7.

## 8. Where this leaves the research question

**Unchanged, and I record it plainly rather than dress an instrument day as a research day.** IP
as a covered investment, a regulatory or judicial measure as the challenged conduct, and a live
litigated jurisdictional or admissibility doctrine still meet in the three decided seed awards
and in no new matter this council has found. `open_threads[5]` is **Day 147 post-cooling-off**
with no ICSID registration of a *Wingtech v. Netherlands* proceeding **found in accessible
sources — and it is never stated that none exists.** The Wingtech matter remains a **Ring 1–2
near-miss**: Ring 2 present, Ring 3 architecturally live but on the branch that does not bite,
**Ring 1 not established.** Nothing in Parts I–VI asserts anything about any dispute, docket,
tribunal or claimant, and the binding reading rule on the relay rows held in the register it was
written for: **no case number, party or matter from any excerpt was entered, screened, graded on
any ring, or carried as a lead.**

**What today actually bought, stated at the strength that survived vetting:**

- **The output path broke again this morning and I caught it and stopped it before merge.** That
  is worth more than the research question produced today, and it is a defect I caused by
  following a countermeasure exactly as written.
- **`/cases/recent` serves respondent-State names into `excerpt_of`-visible text** — a literal
  type the record had not established as reachable there. That is the narrow half of the
  analyst's insight and it is what survived two blocking objections. **Nothing is asserted about
  `/cases/pending`.**
- **The gate held, three times, on things it was not built for.** The corpus screen adopted
  yesterday survived three independent derivations and a synonym trap. The officer found the
  `completed`-over-`records[1:]` trap in my own note, and ran the C-2 control neither other seat
  ran. **Fourteen negative results are recorded alongside eleven objections, deliberately** —
  a council that logs only its hits cannot tell a working control from an unused one.
- **A screen aimed at the wrong noun passes everything.** `C-3` is the day's methodological
  finding: I ran yesterday's countermeasure faithfully, against the batch's literals rather than
  the served thread's designator, and it certified a batch that could not have closed the thread
  it was commissioned for.
- **Four errors of mine, three of the analyst's, all found by seats other than their authors.**
  On the one day this session's chair opened by discharging a countermeasure against frozen
  error, five findings landed against the chair.

---

## 9. Procedural caveat — the close-out re-anchor was applied in HALF, and I declare which half

`prompts/daily_council_protocol.md`'s close-out mandates `python3 scripts/reanchor.py` as the last
commit, staging `STATE_OF_THE_ANSWER.md` **and four `agents/` notes**. I ran the script. It moved
five anchors from `249fc8d` to `e323866`. **I have committed only the `STATE_OF_THE_ANSWER.md`
half and reverted the four `agents/` files, because `agents/` is outside this session's
authorised merge scope** (`analytics/`, `state/research_log.json`, `STATE_OF_THE_ANSWER.md`), and
a PR touching it may not be self-merged.

**This is a genuine conflict between two binding instruments**, and the protocol's own Precedence
section says such a conflict "is worth a line in the record's procedural caveat" — so here it is
rather than buried. The cost is measured, not guessed: `scripts/check_currency.py` reports **one
new STALE line** (`agents/Workflow Threads.md`: 6 commits touched its declared paths since
`249fc8d`). The three `FAIL … is not a commit` lines it also prints are **pre-existing
shallow-clone artefacts, not caused by this**: the same check at the pre-session tip `ec4ca67`
prints the same three. `.github/workflows/reanchor.yml` runs on every PR to `main` and is the
machinery built for exactly this path.

**The operator's call, and I am not making it silently:** either widen the daily session's merge
scope to include the tracked notes the protocol tells it to re-anchor, or amend the close-out
step to stage only what the session may merge. **As it stands the protocol instructs the chair to
do something the merge scope forbids, and every session either departs from one or the other.**

---
