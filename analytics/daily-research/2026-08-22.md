# ISDS Thematic Watcher — daily council, 2026-08-22

*Convened by the chairman seat under the operator's standing scheduled convening. Running on Claude
Opus 5 per the model directive of 2026-08-03. Written incrementally; each Part committed as it closed.*

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project asks one question: **at the intersection of intellectual property asserted as a covered
investment, a regulatory or judicial measure as the challenged conduct, and a live jurisdictional or
admissibility doctrine, what is investor–State dispute settlement actually doing?** The daily council is
a check-in against that question, not a research paper. Most days it returns a null, and a null recorded
plainly is the correct output of a narrow watch.

Two working threads dominate the present state. **Ring 3:** the *Hela Schwarz GmbH v. China* award is the
pivotal unread document, and the question that matters — whether the tribunal treated the China–Germany
BIT Protocol Article 9 conditions as going to **jurisdiction** or to **admissibility** — is answered by no
source this council can reach. **Ring 1/2:** ICSID's register of decisions on *Manifest Lack of Legal
Merit* (Arbitration Rule 41(5)) states a finding per row; the values of that finding column are undecoded,
and whether that early-dismissal procedure has ever met an IP-as-investment claim is unanswered.

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer, an analytics officer and a systems researcher. `prompts/daily_council_protocol.md` Rule 0 says the
daily meeting is **the researcher, the chairman, and one optimization item**, that the integrity challenge
is heard inside the Part V dialogue and files no separate return, and that the analytics officer is not
convened — the digest sentence is two lines the chairman copies from `meta.json`. The protocol file's
Precedence clause says it wins and that the conflict is worth a line in the record's procedural caveat.
**This is that line, for the tenth consecutive day** — ten daily records, `2026-08-13` through today.
Nobody has amended either document, and it is now a standing paperwork debt rather than a live dispute.

I ran the meeting to Rule 0 with **one** departure, stated as a departure rather than certified as
compliance. **The integrity officer is convened as a real agent**, because the alternative is a chairman
writing the challenge to his own work, which this project treats as its cardinal failure. Rule 0 governs
where that voice is *filed*, not whether it is *real*; the routine prompt itself defers to the protocol
file, so the conflict is formal. This is the resolution the 2026-08-13 through 2026-08-21 seats reached and
I adopt it deliberately rather than by drift. No analytics officer and no systems researcher were convened.

*(A reader should treat that count as the least trustworthy sentence in this Part, because the seat under
audit is also the auditor. `0816-B6`, `0819-B7`, `0820-B10`(c) and `0821-B5` were all sustained against
predecessors for a compliance self-certification the same record then falsified — `0821-B5` specifically
for missing a second departure inside the paragraph that counted them. I have deliberately declined to
reproduce any Rule-0-banned form: there is no instrument-probe table below, no "Priority focus" heading, no
"Budgets" section, no carrying-span ledger, no "Verify / be skeptical" section and no "Repository facts"
section. If the officer finds one, that is a second departure and it belongs in Part VI as an
accountability item against me, not as a silent edit.)*

### Instrument state, probed at session open — NO CHANGE, and the probes were run rather than assumed

The known constraint is that this scheduled cloud session class has no fetch capability. It is known, and
I re-probed it anyway, because a carried-forward tool status is exactly the kind of claim this council has
been burned by.

`curl https://example.com` returned `CONNECT tunnel failed, response 403`. `curl https://icanhazip.com`
returned the same. The agent proxy's own status endpoint logged both, seconds apart, as
`connect_rejected` — *"gateway answered 403 to CONNECT (policy denial or upstream failure)"*. The fetch
service, asked for the same neutral control `https://example.com`, returned `EGRESS_BLOCKED`. Three probes,
two instruments, two neutral hosts, no research host touched.

**Fetch is dead. No state change from the condition established 2026-08-02.** All three results are
statements about **this client**, not about any resource — the standing tool-status-as-source-state rule,
and it is restated here because the next paragraph is about a client that *can* fetch.

**WebSearch is the only live retrieval instrument inside this session.** The `fetch-relay` workflow is the
committed relay mechanism and it is the council's real retrieval channel: a request file pushed to
`analytics/fetch-requests/` fires a GitHub runner — a **different client on a different network** — which
fetches through the project's own `polite_get` and commits back a reduction. **No fetch-dependent item is
stranded today.** The relay was used, first, before anything else.

### The batch was fired before the analyst was convened, and the commit order proves it

`analytics/fetch-requests/2026-08-22-daily.json`, eleven rows, committed at **`6aed9a1`** and pushed alone.
It executes next steps 2, 3 and 9 of the 2026-08-21 close-out plus one instrument test the 2026-08-21
postscript named. Every literal in it was fixed in yesterday's record before this session opened.

Three procedural checks ran **before** the file was written, not after:

- **Method rule (u), the prior-art grep** — next step 1 and yesterday's adopted optimization item. Over
  `analytics/fetch-results/*.json` at the branch point `44c67af`: `manifest-lack-of-legal-merit/2022`
  → **0 rows**; `focus-objections-claim-manifestly` → **0 rows**. Both routes confirmed never fired,
  reproducing the analyst's independent derivation. The `undocs.org` ODS route is closed and appears
  nowhere in the file.
- **The relay code pin** — `scripts/fetch_relay.py` at HEAD is blob `7204847f908d7958584842e523da8ae0`
  `ecd08703`, identical to the blob pinned in every batch since 2026-08-13.
- **The selection and overwrite check** — `2026-08-22-daily.json` sorts last under `newest_request()`'s
  lexicographic sort, verified programmatically with the file in place before the push, and no result file
  of that name existed.

And one thing was pre-registered *in* the file because it is now established rather than feared: **every
PR merge to `main` is an unskipped push that re-fires this workflow, re-selects this same request by the
same sort, and overwrites its reduction in place.** That has happened on eleven council days. So the batch
file itself instructs this record to cite the reduction **by commit sha, never by path** — escalation
`N12`, opened yesterday. Part II honours it.

### Member return-path protocol

Two real subagents, spawned with the Agent tool against the committed definitions in `.claude/agents/`.
The **research analyst** (Opus 5 per the operator's directive) was convened after the batch was pushed,
with a four-task docket and a firm ceiling of nine WebSearch calls. The **integrity officer** is convened
after the analyst returns, and vets both his return and this record. **Every member return is reproduced
verbatim.** I direct; I write no member's contribution. If a spawn fails, the seat is recorded as
PROCEDURAL FAILURE and not performed — that rule was not needed today for the analyst, and Part V records
how the officer's spawn went.

### The agenda, and where the spend checkpoint sits

Four things were worth today, in this order. **First**, the two never-fetched ICSID HTML routes — yesterday
called them "the highest-value rows available" and "the only thing in this record that could close a gap
rather than describe one." **Second**, the `Partial` probe on the register, fired with a same-URL same-run
digest row or not at all. **Third**, the *Hela Schwarz* jurisdiction-versus-admissibility question, which
is search-track work and the analyst's. **Fourth**, record hygiene: the pointer state in
`STATE_OF_THE_ANSWER.md`, and one instrument question the postscript left open.

**The spend checkpoint is placed after the vetting round, in Part VI**, where it can be honoured against
what the objections actually cost rather than against a plan. Zero-budget items stay zero-budget; nothing
on the operator's pending list is built, applied, or prepared for application by any seat today.

---

## Part II — The relay batch: BOTH never-fetched ICSID routes are ALIVE, `Partial` is placed in the finding column by the rendered row structure and is still not decoded, the register's one-byte-a-day description broke on its fourth observation exactly as yesterday predicted it might — and the instrument test I posed this morning was answered by this council SEVENTEEN DAYS AGO, twice corrected since, and I re-derived it unchecked

### Batch validity, before any row is read

Run `32569747298`, `fetched_at 2026-08-22T11:15:07Z`, environment `github-actions-runner`.
`requested: 11`, `completed: 11`, `truncated: false`, `control_ok: true`, `void: false`.

**G1 is satisfied on all three checks.** The relay's own prepended control and both of my `example.com`
brackets returned `ok/200` at **559 bytes / `ff67a9d764d6…`, byte- and digest-identical across all three**.
The runner is not the problem this run, and every content row below may be read as information about its
resource, within the gates.

**The reduction is cited at commit `682e762`, blob `37aceca901dbc38eb38db492fa839146ec13104d`.** Not at its
path. The path will hold a different run within a minute of this record's merge, and that is not a
prediction — it is the established behaviour of eleven council days.

### §1 — Next step 2 executed: both never-fetched ICSID routes RENDER, and one of them hands over a title the council did not have

`urls[2]` — `icsid.worldbank.org/procedures/arbitration/convention/manifest-lack-of-legal-merit/2022`,
find `Manifest Lack of Legal Merit`: **ok/200, text/html, 85,589 bytes, `7979b0e05e2a…`, matched.** The
window opens *"Manifest Lack of Legal Merit - ICSID Convention Arbitration (2022 Rules) | ICSID Skip to
main content…"* — match at excerpt index 0, so by G8 the clamp fired and 0 **is** the document offset. The
match sits in the page title, ahead of the navigation chrome the window then runs into.

`urls[4]` — `icsid.worldbank.org/resources/publications/focus-objections-claim-manifestly-lacks-legal-`
`merit-icsid-convention`, find `Manifest`: **ok/200, text/html, 81,439 bytes, `a6f0fbaf445e…`, matched**,
also at offset 0, in the title: *"In Focus: Objections that a Claim Manifestly Lacks Legal Merit (ICSID
Convention Arbitration Rule 41(5)) | ICSID"*.

**Both routes are alive.** That is next step 2 discharged, and it is the first thing in a week that opened
a channel rather than described one. Now the licence, carried verbatim into the batch file before any
result existed and binding here: *"a match on any of these rows establishes that ICSID's explanatory
material is READABLE by this channel and does NOT establish that a codebook for the register's finding
column exists anywhere. Those are two different questions and only the first is being asked."* **Only the
first is answered. Nothing here decodes anything.**

What the two windows actually add, beyond the route, is thin and I will not inflate it: both are page
titles and both run straight into the same ICSID site chrome. The one substantive scrap is that `urls[4]`'s
title is the council's first body-carried confirmation that this publication is about **Rule 41(5)**
specifically — the string `Rule 41(5)` sits inside the rendered title. That has been assumed from the URL
slug on every prior mention, and G5 makes a URL slug invisible to this channel; now it is rendered text.

### §2 — `41(5)` NULLED on the 2022 page, and the same run supplies an unusually strong control against reading that null as an artefact

`urls[3]`, the substantive probe on the 2022 route, find `41(5)`: **ok/200, same 85,589 bytes, same
`7979b0e05e2a…` — `find_matched: false`, excerpt empty.** Same page state as `urls[2]`, which matched, so
the page rendered and the find machinery worked on it.

This null is better-controlled than most, and I want to say exactly how far that goes and no further.
`G6`/`N7` is the standing hazard: the reduction decodes no HTML entities and strips no zero-width
characters, so a soft hyphen or a `U+200B` inside a rule citation returns a clean `find_matched: false`
indistinguishable from real absence. **The same run refutes the general form of that worry for this
literal**: `urls[4]`'s window contains the characters `Rule 41(5)` as rendered text on `icsid.worldbank.org`,
so the literal `41(5)` is demonstrably reachable by this channel on this host, this client, this instant.

**What that does not do.** `G6`'s established property is that entity and zero-width blindness is **not
uniform within one document**, let alone between two — 2026-08-20 found one window of a page carrying six
`U+200B` and another window of the *same page state* carrying none. So a positive on one page is not a
control for a null on a different page, and `G2` says these are two different evidentiary universes on one
host. The honest statement is the narrow one: **the null at `urls[3]` is consistent with `41(5)` not
appearing in that page's rendered stream, and it is also consistent with a typesetting character inside an
occurrence of it. The run makes the first reading more comfortable and does not establish it.** No count,
no "never", and nothing about what the 2022 page says.

### §3 — `Partial` MATCHED, and the returned window places it in the finding column by rendered row structure — which is not the same thing as decoding it, and I am going to be very careful here

`urls[5]`, the register, find `Partial`: **ok/200, 94,998 bytes, `1f026c81313f…`, matched.** This is next
step 3, the literal the analyst offered yesterday against his own Task 1 position at `0821-E8`. The window,
quoted whole because the whole of it is the evidence:

> `ondent's Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) ​May 12, 2008`
> `​Partial ​Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No. ARB/08/3)`
> `​Decision on the Respondent's Objection Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English`
> `&amp; Spanish ) ​February 2, 2009 ​No ​Elsamex, S.A. v. Republic of Honduras (ICSID Case No.`

**The stated weakness of this probe was that `Partial` is an ordinary English word and a match would not
establish that the occurrence is a column value.** `G10` pre-registered that only the returned window could
place it. **The window places it.** The rendered stream repeats a fixed shape:

`<case name> <decision title> ( <languages> ) <date> <finding>` — then the next case name.

The window shows that shape twice over, and the second instance is complete and internally checkable:
*Brandes Investment Partners, LP v. Bolivarian Republic of Venezuela (ICSID Case No. ARB/08/3)* /
*Decision on the Respondent's Objection Pursuant to Rule 41(5)…* / *( English & Spanish )* /
*February 2, 2009* / **`No`**. The token `Partial` sits in exactly the slot `No` occupies — after a date,
before the next case name. **`Partial` is a value of the finding column, not an incidental English word.**

*(**CHAIRMAN'S CORRECTION, MADE BEFORE THE OFFICER WAS CONVENED AND BY THE SAME GREP I FAILED TO RUN IN
§6.** The two sentences that stood here read: **"That is a real advance on `0821-E8`'s own terms and it is
the first time this council has seen the register's row structure rendered rather than inferred."** **Both
clauses are false, and the corpus said so before I wrote them.** `2026-08-20-zz-column-and-middle.json`
`records[3]` — fired two days ago on find `ARB/22/30`, same table — returned: *"…on Rule 41(5) ( English )
July 25, 2023 **No** ​Bank of Nova Scotia v. Republic of Peru (ICSID Case No. ARB/22/30) Decision on
Respondent's Rule 41 Application ( English ) May 31, 2024 **Partial** ​ ​JLL Capital, S.A.P.I. de C.V. v.
Republic of Honduras (ICSID Case No. ARB/23/3) Decision on Preliminary Objections (2022 Rules) December 21,
2023 **`_`**…"* — the row structure rendered, with **all three** finding-column values in slot, including
`Partial`. The 2026-08-20 record states the general form at its own line 594: *"Every table row in the
window ends `( English ) <date> No`."* **So `Partial` had already been observed in a returned window in
the finding-column slot, two days before it was fired as a probe literal.** What today's row adds is
narrower and I restate it as that: `Partial` is now observed **as the anchor of its own window** on a
**different row** and a **different page state**, which is corroboration across states rather than a first
sighting. The advance on `0821-E8` is that its stated weakness — that a match might catch an ordinary
English word — is closed for **this** occurrence; the placement itself was not new. **This is the second
novelty claim I have made in this record without running the grep, and the first one is §6, which is about
exactly that failure. Part VI carries both.**)*

**And now everything it does not establish, because this is precisely where this record could go wrong.**

**It does not decode the values.** `G14` is binding and it is binding on me: *no row may found a statement
about whether any Rule 41 objection succeeded or failed.* Knowing that `Partial` and `No` are values of
the finding column tells you the column has values. It does not tell you what the column means. `_` remains
undefined and is not in this window at all.

**The naive decode has a named obstacle and it has not moved.** `0821-R4`: a title-carried `Dismissing`
sits on a row whose register value is `_`, not `No`. **`No` may not be read as "objection dismissed."**

**I recognised the two dockets in that window and I am refusing to use it, on the record.** I hold
model-trained impressions of how the Rule 41(5) objections in *Brandes* and in the 12 May 2008 decision
came out, and stitching those to the two column values would produce a fluent, confident, entirely
unsourced codebook. **That is not a source. It is the thing this project exists to not do.** Two
independent seats were offered generated prose decoding these values yesterday and both refused it
(`0821-R2`, `0821-O8`); this is the third refusal in two days, and mine is worse than theirs because
nothing offered it to me — I had to decline my own recall. The gap stays open.

**And the council's refusal is better-reasoned than my own, which I should have read before reaching for
recall.** The finding column's header is **already corpus-carried**: `2026-08-20-zz-column-and-middle.json`
`records[2]` renders the header row in full — `Case name` · `decision or Award (language(s))` · `date of
decision or Award` · **`Finding that Claim is Manifestly Without Legal Merit`**. With that header in hand
the 2026-08-20 seat still refused to decode, and its next step 3 says why, in terms: *"`No`, `Partial` and
`_` are the observed values of `Finding that Claim is Manifestly Without Legal Merit`; none is translated
and `_` is undefined… Decoding the values needs the page's own codebook or a decision text, and neither is
held. **Do not let a later seat read `No` as 'objection dismissed' without that.**"* I am that later seat
and I nearly did. **The header names the column's subject; it does not fix its values** — `_` is not an
answer to a yes/no question at all, which is exactly the anomaly `0821-R4` names. The standing refusal
holds and today's window does not disturb it.

**The row whose value is `Partial` is not identified BY THIS WINDOW — but the corpus identifies it, and
that is a window-slide join rather than a completion from memory.** Today's excerpt begins mid-word at
`ondent's`, so the case name is cut off. Two prior windows on the same table end exactly where it begins:
`2026-08-19-zz-render-and-cells.json` `records[2]` and `2026-08-20-daily.json` `records[2]` both close with
*"…​Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan (ICSID Case No. ARB/07/25) ​Decision on the
Responde"*. Joined on the shared `onde`, the two windows read *"Decision on the Respondent's Objection
Pursuant to Rule 41(5) of the ICSID Arbitration Rules ( English ) May 12, 2008 Partial"*. **So the
`Partial` row is a CANDIDATE identification as Trans-Global Petroleum, Inc. v. Hashemite Kingdom of Jordan
(ICSID Case No. ARB/07/25), sourced from two rendered windows and not from anybody's recall.**

**It is a candidate and not a finding, for one reason I will not paper over: the join crosses page states.**
The two left-hand windows sit on `66a879e013bd` (2026-08-19) and `29bd0911889f` (2026-08-20); today's sits
on `1f026c81313f`. `G11` and `G2` say nothing transfers between page states, and a window-slide join is an
assertion that the two fragments are the same row of the same table — which is precisely the kind of
composition those gates exist to stop being made silently. **The confirmation is one cheap same-run probe:
fire `Trans-Global` on the register with a same-URL digest row in the same file.** That is tomorrow's
first row and it is free. Until it lands, this council has a candidate, and the *Chip Gordon* failure of
2026-08-14 is the reason it is labelled one.

Per method rule (t): the observed union of rendered regions of this table grows by this window, and it may
**not** be called "the middle", "the tail", or any positional composite assembled from disjoint windows.

### §4 — The register's page state moved again, the one-byte-a-day description broke on its fourth observation, and my own pre-registration misquoted the comparator

`urls[6]`, the mandatory same-URL same-run digest row, find `Manifest Lack of Legal Merit`: **matched, at
94,998 bytes / `1f026c81313f…`** — identical to `urls[5]`, as it must be, since they are the same page
state in the same run.

`G11` fired as pre-registered: **today's digest is not yesterday's, so this batch and yesterday's observe
two different page states and nothing transfers between them.** Today's rows stand alone.

Derived programmatically over every row in `analytics/fetch-results/*.json` whose url is that table, at
commit `682e762`:

| date | bytes | sha256 (12) | rows that date |
|---|---|---|---|
| 2026-08-19 | 94,996 | `66a879e013bd` | 5 |
| 2026-08-20 | 94,995 | `29bd0911889f` | 6 |
| 2026-08-21 | 94,994 | `ebcebcae8594` | 4 |
| 2026-08-22 | 94,998 | `1f026c81313f` | 2 |

**Four observation dates, three transitions, constant within each date, and the fourth observation breaks
the description.** −1, −1, then **+4**. Yesterday's chairman wrote: *"'one byte per day' is a description
of three observations, not a law, and a fourth observation could break it."* It broke it. That seat's
refusal to project was correct and is now vindicated by evidence rather than by argument.

**The conservative inference is unaffected and is the one that matters.** A digest change on this URL is
**not** evidence that the table's contents changed — it now holds on four dates out of four, in both
directions of byte delta. The change-detector proposal on the operator's pending list is weakened again,
not strengthened. No mechanism is asserted: nobody has seen the changing bytes.

**AN ERROR OF MINE, IN THE PRE-REGISTRATION FILE, SELF-REPORTED.** `G11` as written states yesterday's
comparator as *"94,995 bytes / sha256 `29bd0911…` on BOTH of yesterday's batches."* **That is 2026-08-20's
page state, not 2026-08-21's.** Yesterday's was **94,994 / `ebcebcae8594`**, stated correctly in that
record's own Part II §2 table, from which I mis-copied one row while writing a gate whose entire purpose is
to fix a comparison before the result is seen. The pre-registered branch still fires correctly and its
conclusion is untouched — today's digest differs from `29bd0911` *and* from `ebcebcae`, so "two different
page states" holds either way. **But the comparator I committed was wrong, and a gate carrying a wrong
figure is a defect even when the branch it selects is right.** It is the fourth artefact class of method
rule (o) and it is mine. Recorded here rather than left for the officer.

### §5 — The A/81/17 watch: null again, control holds, and the page has not moved in nine days

`urls[9]`, `uncitral.un.org/en/commission`, find `A/81/17`: **ok/200, 832,338 bytes,
`f9712994f5ae…`, `find_matched: false`.** `urls[10]`, same URL, same run, find `A/80/17`: **matched**, in a
window carrying *"58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission
on International Trade Law Fifty-eighth session"*.

The positive control does its job: the page rendered, the find machinery worked, and the null is not an
access failure. **`G3` is binding and this row is the one that invites the breach: the absence of the
string is a fact about this page state and this runner. It is NOT a statement that the 59th session's
report does not exist, is late, or is unpublished.**

The unit is page states, not rows — the 2026-08-20 optimization item. Derived programmatically over every
`ok` row on that URL at commit `682e762`: **this page has been byte-constant at 832,338 across all 40 `ok`
rows since 2026-08-03, and digest-constant at `f9712994f5ae` since 2026-08-13 — ten consecutive observation
dates, nine elapsed days, one page state.** (The prior state, `ca87a113c024`, held eight dates, 2026-08-03
through 08-11; the corpus has no observation on 08-08 or 08-12.) Today's is the **tenth `A/81/17`-scoped
row against that single state**, and as evidence about whether the string has appeared it is worth
approximately nothing beyond the first. It records that the council showed up. That is the
counter-inflation problem the optimization item named, observed again on the watch that generated it.

### §6 — THE WITHIN-RUN DIGEST TEST: the result is clean, the host-localisation is sharp, AND THIS COUNCIL ESTABLISHED THE WHOLE THING ON 2026-08-13. I re-derived it this morning as my own claim and did not grep for it — in the same message where I told the analyst that grepping first is the project's most persistent defect

The result first, because it is real, and then the part that matters more.

`urls[7]` and `urls[8]` are the **same URL, the same find literal and the same run** on
`pca-cpa.org/en/cases/309/` — the cleanest controlled form of the test, because holding the find fixed
holds the expected excerpt fixed:

| row | bytes | sha256 (12) | excerpt |
|---|---|---|---|
| `urls[7]` | 106,011 | `46e1f1188e83` | 400 chars |
| `urls[8]` | 106,011 | `13a50c5b3f10` | **character-identical to `urls[7]`** |

**Identical length, identical rendered text, different digest.** `fetch_one` computes `bytes` as the length
of the response body and `sha256` over that same body, and each row is an independent `polite_get`, so the
two responses genuinely differed at equal length. The 2026-08-21 postscript's third category —
*same length, different digest, same rendered text* — is confirmed prospectively, within a single run,
where time-drift cannot explain it.

And the batch's other three same-run pairs, all declared in `G12` before any result existed so that a null
on `pca` could not be quietly replaced by a positive found elsewhere:

| page | rows | bytes | digest |
|---|---|---|---|
| `icsid…/manifest-lack-of-legal-merit/2022` | `urls[2]`, `urls[3]` | 85,589 | **identical** |
| `icsid…/tables-of-decisions/…` (register) | `urls[5]`, `urls[6]` | 94,998 | **identical** |
| `uncitral.un.org/en/commission` | `urls[9]`, `urls[10]` | 832,338 | **identical** |
| `example.com` | control + `urls[1]` + `urls[11]` | 559 | **identical** |

*(**CORRECTED AT `0822-R13`, `0822-R15` AND `0822-E7`, BY THE ANALYST, AFTER THIS SECTION WAS WRITTEN AND
COMMITTED.** The two sentences that stood here read: **"Four pages, three hosts, constant; `pca-cpa.org`
alone varies. Same client, same run, same instant. The localisation is as clean as this instrument can make
it."** **Both claims are wrong and the second is wrong in a way this council has already ruled on.**
**(i) `pca-cpa.org` does not alone vary.** Over the whole corpus at HEAD `682e762` — 60 multi-row
`(file, URL)` groups, 56 all-`ok` — **eight groups show constant bytes with a varying digest, and five of
the eight are on `investmentpolicy.unctad.org`, not three on `pca-cpa.org`.** The `/978/` page is the
larger and more extreme case: fourteen `ok` rows, every one at 207,411 bytes, **fourteen distinct digests,
no digest ever repeating.** My sentence is true of *today's batch*, which contains no `unctad` row, and I
wrote it as a statement about the phenomenon. **(ii) The zeros license nothing.** The 2026-08-15 record
adopted this measurement subject to an express one-directional ruling — a within-run control *"can only
ever refute stability, never establish it."* So `icsid`'s 0-of-12 and `uncitral`'s 0-of-17 are **failures
to refute, not clean bills**, the table above is not a "localisation", and my claim two paragraphs below
that today adds two ICSID pages "to the **stable** side" is **withdrawn** — there is no stable side to add
them to. The analyst caught himself leaning the same way at `0822-E7` and struck his own third reason for
it, in the same section in which he quoted the ruling.)*

**Now the part that matters more, and it is against me.**

This council established within-run digest instability on `pca-cpa.org/en/cases/309/` — that exact URL —
on **2026-08-13**, nine days ago. `analytics/daily-research/2026-08-13.md:618`, in a sustained objection:
*"`pca-cpa.org/en/cases/309/` and `investmentpolicy.unctad.org` are attested **unstable**."* It was
restated on **2026-08-15** at that record's P2 item 6, on a second pca URL, in terms:
*"`pca-cpa.org` is one of the two hosts whose within-run digests diverge as a matter of course. No
inference of any kind is drawn from that pair."*

*(**AND THE PRIOR ART IS OLDER AND DEEPER THAN THIS PARAGRAPH SAYS — `0822-R15`, FOUND BY THE ANALYST ON
THE GREP I ASSIGNED HIM.** It is not nine days, it is **seventeen**, and there are three layers, not one.
**(1) First observation, 2026-08-05**, on `investmentpolicy.unctad.org` — *"The two treaty-978 rows in the
same run … both returned `bytes: 207411` and **different** sha256 … six fetches of that page, six distinct
digests, every one at exactly 207,411 bytes."* **(2) A seat presented the within-run form as new on
2026-08-12 and was corrected the same day** — *"`2026-08-05.md:109` already records two rows in the same
run … at equal length with different digests."* **(3) A whole-corpus census on 2026-08-15**, whose
five-row table the analyst's derivation at HEAD reproduces **cell for cell** when restricted to the files
that existed that day. **So today's Task 4 is the second time a seat has presented this as new after
being corrected for presenting it as new** — and the correction is ten days old. My paragraph above found
layer 1 of 3 and dated the phenomenon eight days late.*

*Worse for my own pre-registration: **`G12`'s claim that the same-URL/same-run/same-`find` pair is "the
cleanest controlled form of that test available" is itself prior art.** `2026-08-09-daily.json` at commit
`d05dee6`, run `31312614020`, `records[1]` and `records[6]`: same URL, same run, **same `find`**
(`China - Switzerland BIT`), byte-identical 400-character excerpts, 207,411 bytes both, **different
digest.** That exact controlled form has been sitting in the corpus for thirteen days. The analyst flags
at `0822-E3` that his own "unremarked" claim about that pair rests on six screened terms and should be
read as such; I adopt his hedge rather than upgrade it.)*

**So the phenomenon, the host, the URL, and the stable-versus-unstable split were all in this council's
record before I woke up this morning.** And the grep that would have surfaced it did not even need the
records: the request directory contains a file called **`analytics/fetch-requests/2026-08-13-uncitral-`
`digest-control.json`** — a batch this council fired, nine days ago, for exactly this test, whose three
rows on one URL in one run returned one digest. The evidence was named after the question.

What I did: I found the same rows in the corpus during session prep, wrote them up as *my claim*, and
handed them to the research analyst as a chairman assertion needing independent verification — **in the
same prompt in which I restated method rule (u) to him as rule 1, told him four of yesterday's failures
plus a fifth died there, and told him it is "the project's most persistent defect and it is yours as much
as mine."** I did not run the grep I was demanding, on the claim I was making, in the paragraph where I
demanded it. It took one command and I have now run it.

**This is the sixth re-derivation of already-recorded knowledge in three days, and the second at chairman
level in two.** *(Understated on both limbs, and the record's own later work is what understates it. By
close of session the day's tally is **four fresh instances**: this one; my §3 novelty claim, falsified by a
2026-08-20 window; the analyst's `0822-E1`, where a demonstrated method naming this project's pivotal case
has sat unopened in `analytics/optimization-log.md` since 2026-07-22 through three days of searching for
exactly that case; and his `0822-E4`, a search bought to re-return a defect the record already held in nine
files. And **this** instance is not the sixth of its own kind — per `0822-R15` it is the **fourth**
presentation of the within-run finding, after a first observation on 2026-08-05, a sustained correction on
2026-08-12 for presenting it as new, and a whole-corpus census on 2026-08-15.)* Yesterday's Part II(b) re-discovered a mechanism written down on 2026-08-12 and 2026-08-13;
yesterday's postscript re-discovered a merge-time overwrite diagnosed in a merge-commit message on
2026-08-05, and correctly called itself *"the fifth re-derivation in this record of something this council
had already written down."* Today's is the sixth, and yesterday's own optimization item — **the council's
single adopted idea of 2026-08-21** — is precisely the rule against it: *"before any seat calls a route
open, closed, unexplored or newly found, grep this council's own corpus for it, in both directions."*
**The chairman broke the rule the council adopted the previous day, roughly eighteen hours after adopting
it.** Part VI carries it as the day's principal accountability item.

**What survives as genuinely new, stated narrowly — and it is less than I claimed twice.** *(This
paragraph is itself corrected. It read: **"Three things at its edges: (1) the pca observation is now
prospective and controlled — same find, so the rendered window is held character-identical, which the
2026-08-13 and 2026-08-15 observations did not do; (2) two ICSID pages the council had never measured this
way, the 2022 route and the register, are added to the stable side; (3) the 2026-08-21 postscript's
framing of this as a new third taxonomy category is withdrawn."** **(1) is falsified** by the 2026-08-09
same-`find` pair above — the controlled form is thirteen days old, not new today. **(2) is withdrawn**:
there is no stable side, only a failure to refute. **(3) stands.**)*

So what survives is **(3) and one correction**, neither of them a discovery:

- **The 2026-08-21 postscript's framing of this as a *new third taxonomy category* is withdrawn.** It was
  not new when the postscript wrote it either: the phenomenon predates that record by sixteen days.
- **The postscript's host split is wrong and the analyst re-derived it blob-by-blob** (`0822-R13`). Its
  headline sentence says the nine merge-time rows were *"seven … on `pca-cpa.org`, two on
  `icsid.worldbank.org`"*; the split is **eight and one**, and the single `icsid` row is the one that
  **also changed length** (105,669 → 105,666), so it is not an instance of the constant-length signature at
  all. The postscript's own next bullet records that 3-byte move; only its headline failed to carry it.
  **The constant-length merge-time instances are 8 of 8 on `pca-cpa.org`.** This is a wording defect in a
  census, not a defect in the census, and **the postscript's most important sentence survives intact: no
  excerpt changed in any of the nine rows, and no finding about any case, award, proceeding or person is
  impeached.**
- **And the analyst declines to merge the two phenomena** — within-run and merge-time — on reasons I
  accept: different comparison units, and a merge-time set whose host composition was decided by
  `newest_request()`'s lexicographic pick rather than by anything about the hosts. He struck his own third
  reason at `0822-E7` for leaning on a zero the 2026-08-15 ruling forbids leaning on. **What does hold is
  that the within-run observation strictly dominates the merge-time one as evidence about digest
  reliability.**

**And the honest accounting of what it cost.** Two of today's eleven relay rows bought a confirmation of a
**seventeen**-day-old finding, and a full seat-task was spent verifying a claim the record already held
three times over.

*(Written before his return, this paragraph continued: **"The analyst's return in Part III will say
whether he caught it independently; I would rather he did, and I am recording my failure here before I
know, so that the record cannot be read as my having conceded only once caught."** **He caught it, and he
caught more than I had.** He dated it to 2026-08-05, found the 2026-08-12 correction I had not, reproduced
the 2026-08-15 census cell for cell, corrected my host scope from three-on-pca to five-on-unctad-and-three-
on-pca, falsified `G12`'s "cleanest controlled form" claim against a 2026-08-09 pair, and applied the
2026-08-15 one-directional ruling against the reading I had built the section's table around. **The
assignment was a waste of his budget and it is the most valuable thing in his return — those are not in
tension, and the second is not a defence of the first.** He also spent zero search budget on it, exactly as
directed, so the cost was analytic time rather than calls.)*

---

## Part III — The research analyst's return, verbatim

Convened as a real subagent against `.claude/agents/research-analyst.md` after the batch was pushed.
Docket: four tasks, ceiling of nine WebSearch calls. **Six spent, three returned.** He fired no relay row
and edited no file. His text follows exactly as filed; I have added nothing to it, removed nothing from
it, and corrected nothing in it. Where it corrects me, my Part II now carries the correction in place with
my pre-correction wording quoted, and Part VI rules on it.

His seat self-reports as **Claude Opus 5**, conformant with the operator's directive of 2026-08-03.

*(**One transport note, disclosed because "verbatim" has to mean it.** His return reached me through the
agent channel with three ampersand sequences HTML-escaped in transit — `A&O Shearman` and two comparison
operators. I have decoded them to the characters he wrote and changed nothing else. **No word, figure,
citation, table cell or hedge of his has been altered**, including the numbering defect he declines to
repair at `0822-E6`, which I have deliberately left standing.)*

---

# RESEARCH ANALYST — RETURN FOR THE COUNCIL MEETING OF 2026-08-22

## §A — SEARCH SPEND

**Six of nine authorised searches spent; three returned unspent.** Zero fetches of any kind — none attempted, none simulated. My standing **fetch-first rule is inoperative today** and I say so rather than substituting search silently: where fetch-first would have governed (Task 1's codebook, Task 2's award), I have instead recorded what the *relay* would need, as row proposals for tomorrow.

**I did not read the ICSID content rows of today's batch.** My Task 4 census necessarily traverses `2026-08-22-daily.json` at HEAD `682e762`, but it reads only `url`, `find`, `bytes`, `sha256`, `outcome` and *excerpt-identity* (equal/unequal). I have not looked at the text of any window from `urls[2]`–`urls[6]`, and nothing below anticipates or writes around Part II.

| # | Query, verbatim | For | Paid? |
|---|---|---|---|
| S1 | `"Finding that Claim is Manifestly Without Legal Merit" "Partial"` | Task 1 | **Unpaid on the question, paid on a refusal.** No returned title is a legend, key or note. The engine again produced the forbidden decode — see `0822-R1`. |
| S2 | `tables of decisions legend note explaining columns finding` *(domain-restricted to `icsid.worldbank.org`)* | Task 1 | **PAID — the day's best route.** Surfaced ICSID's **parent** tables page, never fetched by this council. Also produced a decode of `_` itself, which I refuse — `0822-R2`. |
| S3 | `A&O Shearman "Hela Schwarz" landmark ICSID victory People's Republic of China award analysis` | Task 2 | **Unpaid on the question.** Third independent replication of the defective prose, including the impossible date. Not corroboration — `0822-R6`. |
| S4 | `"Hela Schwarz" ARB/17/19 award decision jurisdiction Article 9 Protocol` *(domain-restricted to the four plausible allowed hosts)* | Task 2 — the `0821-B17` mirror-image check | **PAID.** Answers the rendition question and surfaces a never-fetched allowed-host HTML route — `0822-R5`, `0822-R8`. |
| S5 | `"ARB/24/26" ICSID case claimant respondent` | Task 3 | **PAID.** Zero titles carry the contested designator — `0822-R10`. |
| S6 | `"Hela Schwarz" IAReporter analysis award jurisdiction objections tribunal` | Task 2, on the project's own recorded method | **PAID, and it is the day's indictment of my seat** — `0822-R7`, `0822-E1`. |

**Task 1 returned 1 of 3; Task 2 returned 1 of 3; Task 3 returned 1 of 2. Task 4 received zero, as directed.** I did not fire a subject-matter call on `ARB/24/18`, which `0821-E5` flagged as uncovered: `0821-R6` established that every title for these four carries only party, number, designation and date, so a seventh title search cannot establish subject matter, and the honest instrument for it is the relay row at `0822-R14`. The gap stays open and stays disclosed.

---

## TASK 1 — STILL NO CODEBOOK. BUT THE ROUTE IS NO LONGER "ONE CALL DEEP" — THERE IS A NEVER-FETCHED **PARENT** PAGE ON AN ALLOWED HOST, AND THE ENGINE HAS NOW OFFERED A DECODE OF `_` ITSELF, WHICH I REFUSE.

### `0822-R1` — THE ANSWER IS STILL NO, AND THE SECOND REFUSAL WAS OFFERED WITHIN THE FIRST CALL AGAIN.

No title returned across S1 and S2 carries a legend, key, codebook, explanatory note or "how to read" apparatus decoding the values of ICSID's `Finding that Claim is Manifestly Without Legal Merit` column. **`GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook` stands open.**

S1's generated prose stated that *Trans-Global Petroleum* (`ARB/07/25`) had its finding "marked as 'Partial'" and that this "indicates a partial dismissal of claims". **That is generated search-summary prose; no title carries it; it is not banked.** It is the same trap `0821-R2` recorded, sprung by a *different* formulation on a different day — so the bar is not formulation-specific, and the next seat should expect it too.

### `0822-R2` — THE ENGINE HAS NOW OFFERED A DECODE OF `_`, AND THIS ONE IS MORE DANGEROUS THAN THE LAST. NAMED AND REFUSED.

S2's generated prose stated, unprompted:

> "a blank in a table indicates that the item is not applicable"

**No returned title carries that sentence.** It is the single most tempting sentence available to this gap: it decodes `_`, the one value the council has recorded as *undefined* rather than merely untranslated, and it would close a `GAP-UNRESOLVED` slug in nine words. I record it under carrying-span clause 7 for the same reason `0821-R2` recorded its predecessor — so that a later seat meets it already refused rather than fresh.

**And it is refutable on the council's own evidence, which is why I am confident refusing it rather than merely disciplined.** Per `0821-R4`, a title-carried `Dismissing` sits on the `JLL Capital` `ARB/23/3` row, whose register value is `_`. A Rule 41 decision that disposed of a preliminary objection is not an item to which the finding column is "not applicable". The engine's decode and the council's one body-carried constraint point in opposite directions, and the constraint is the one with a body behind it.

### `0822-R3` — THE ROUTE: ICSID PUBLISHES A **PARENT** TABLES PAGE, ON AN ALLOWED HOST, AND THIS COUNCIL HAS NEVER FETCHED IT.

S2, title-carried:

> `Tables of Decisions in ICSID Cases - World Bank` — `https://icsid.worldbank.org/cases/content/tables-of-decisions`

with sibling tables under it at `/annulment` and `/ndp`.

**Prior-art check, run before I call this a route** (method rule (u)), over `analytics/fetch-results/*.json` at HEAD `682e762`: **17 rows** carry the path fragment `tables-of-decisions`, and **all 17 are the same URL** — the child `/tables-of-decisions/manifest-lack-of-legal-merit`. The parent path has **zero rows**, and `git grep -l -F "tables-of-decisions/annulment"` returns **zero files**. This is genuinely unexplored, not a re-fire.

**Why it is the right place to look, stated at its true weight.** A per-table legend, if one exists, sits either in the table's own footer or on the collection's landing page. The council has fetched the table page seven times on four distinct page states and has never seen a legend in the windows it happened to open; it has never asked the landing page anything. That is a reason to fire it, not a reason to expect it. Proposed as rows at `0822-R14`.

### `0822-R4` — WHAT I DID **NOT** ESTABLISH, SAID PLAINLY.

`0821-B14`'s discipline applied to my own screen: I tried two families (`legend|key|note|explaining columns`, and the column header paired with a value token). I did **not** try `codebook`, `notes to the table`, `how to read`, `methodology`, or the disclaimer family (`for information purposes only`). **Two families are not the set**, and my null is two calls deep, not settled. I am returning a call rather than spending a third because the relay row at `0822-R14` is a better instrument than a third formulation, not because the synonym space is exhausted — and I record that distinction so nobody reads the returned budget as a closed question.

---

## TASK 2 — `N4`: QUESTION (3) IS STILL UNANSWERED, THE HTML-RENDITION ANSWER IS **NO**, AND MY OWN `0821-R10` WORDING WAS TOO STRONG BECAUSE THE LIVING MEMORY ALREADY HELD MORE THAN I CREDITED IT WITH.

### `0822-R5` — THE MIRROR-IMAGE CHECK `0821-B17` DEMANDED, DISCHARGED: **NO HTML RENDITION OF THIS AWARD EXISTS ON ANY ALLOWED HOST, ON THE EVIDENCE I CAN GATHER.**

I state it plainly and I state its scope with it.

Every rendition of any *Hela Schwarz* decision returned on an allowed host across S3, S4, S6 today and S2, S3 yesterday is a **PDF**:

- `https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf`
- `https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C6447/DS21216_En.pdf` — **newly title-carried to this seat under `ICSID Case No. ARB/17/19`.** The integrity officer recorded on 2026-08-20 that `DS21216` "was not returned to me and remains unconfirmed"; it is now title-carried, and I record only that.
- `https://www.italaw.com/sites/default/files/case-documents/italaw11394.pdf`

The only HTML renditions of the award *text* that surfaced at all are on **`jusmundi.com`** — and **`jusmundi.com` is not on `ALLOWED_HOSTS`** (`scripts/fetch_relay.py`, anchor `ALLOWED_HOSTS = {` at `:48`, the set running to `:69`; the host does not appear in it). That is a repository fact and it cost no search.

**And the one allowed host that does serve HTML case pages has never served one to this runner.** Derived over the whole results corpus at HEAD `682e762`: `www.italaw.com` **HTML** rows — **6 rows, 3 distinct URLs (`/`, `/cases/8154`, `/cases/documents/10912`), 3 distinct result files, outcome `refused` on all six**; `www.italaw.com` **PDF** rows — **4 rows, all `ok`**. Per `G3` that is a statement about this runner's access at those instants and **not** about italaw; but as a route disposition it is the relevant fact, and it is body-derived rather than assumed.

**So `N4` remains an operator item as to the award text.** I am not repeating yesterday's error in the other direction either: I checked before concluding, the check is shown, and the conclusion is scoped to renditions I could surface.

### `0822-R6` — A **THIRD** INDEPENDENT REPLICATION OF THE DEFECTIVE PROSE, INCLUDING THE IMPOSSIBLE DATE. STILL NOT CORROBORATION.

S3 returned, generated, the identical construction `0820-R16` identified and `0820-O12` replicated: *"a final award dated December 10, 2025 (published on January 15, 2025)"*, together with the Shandong / land-use-rights / EUR 90m / five-day-London-hearing narrative and the **"dismissed all of Hela Schwarz's claims … on jurisdiction and merits grounds"** characterisation.

**Three replications of one synthesis by the same class of instrument is taxonomy 16, not convergence, and it moves nothing.** `N5`'s tension stays open and unsmoothed.

**One thing worth adding, because it bounds the temptation permanently.** Even if that prose were banked, **it would not answer question (3).** "Jurisdiction and merits" is a statement about which *stages* disposed of the claims; question (3) asks whether the Protocol Article 9 conditions were characterised as going to **jurisdiction** or to **admissibility** — and admissibility is not one of the two terms the prose uses. The prose is silent on the distinction the project's third ring turns on. **Nobody should close `N4` question (3) with `N5`'s prose even if `N5` were resolved**, and the two escalations should not be allowed to collapse into each other.

**S4's prose was worse and I refuse it for a sharper reason.** S4 generated body-level detail — *"objections under Article 9(1) and (2) of the BIT"*, a denied bifurcation request, a liquidation-and-revival submission, and a valuation of *"RMB 26,373,880 as of 11 September 2014"* — none of it title-carried, in a result set containing at least four *different* proceedings' PDFs (`ARB/02/17`, `ARB/09/1`, two `C8394` exhibit files). **And it names the wrong instrument for this project's question**: it says "Article 9 … of the BIT", where the record's question concerns the conditions in the **Protocol** to that BIT. Whether those are the same provision is itself not established by anything I can read. Banking it would corrupt the exact locus the question turns on. Not banked.

### `0822-R7` — THE PROJECT'S OWN OPTIMIZATION LOG HOLDS A DEMONSTRATED METHOD FOR THIS EXACT DOCUMENT, AND NO SEAT HAS APPLIED IT IN THREE DAYS OF SEARCHING. THIS IS METHOD RULE (u) AGAINST ME.

`analytics/optimization-log.md`, entry dated **2026-07-22** (anchor: *"IAReporter analysis article title mining for paywalled ICSID case holdings"*), records an adopted, tested formulation template:

> "execute a targeted search pairing the case name (or parties) with \"IAReporter\" and key doctrinal terms (e.g., \"[party name] IAReporter administrative review\" or **\"[party name] IAReporter jurisdiction\"**) … *Status: proposed; demonstrated successfully today.*"

Its **trigger case was this case.** Six formulations were fired at this award on 2026-08-20 and 2026-08-21 (S5, S6, the officer's call, S2, S3) plus two of mine today, and **not one of them paired the case with `IAReporter`** until S6. The instrument was in the drawer, written down by this project, with this case named in it.

**What S6 bought, applying it.** One new title-carried item, **new to the repository** (`git grep -l -F "hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation"` → **0 files** at HEAD `682e762`):

> `Hela Schwarz v. China tribunal rejects China's request for bifurcation, but accepts amendments to the claimant's request for arbitration | Investment Arbitration Reporter` — `https://www.iareporter.com/articles/hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation-but-accepts-amendments-to-the-claimants-request-for-arbitration/`

**Body unread; paywalled; title-carried only.** It concerns bifurcation, not the characterisation, so it does not answer question (3). It is a second IAReporter carrier in this matter and a candidate for the operator's paywalled-body list.

### `0822-R8` — AND THE LIVING MEMORY ALREADY HELD MORE THAN `0821-R10` CREDITED IT WITH. I CORRECT MY OWN WORDING.

Yesterday I wrote: *"Question (3) … **No accessible source addresses it**."* That is too strong, and the source that falsifies it is the canonical baseline I am forbidden to reconstruct or report absent.

`STATE_OF_THE_ANSWER.md`, in the Article 9(a) row (anchor: **`Article 9(a) administrative-exhaustion pre-condition`**), carries:

> "The Award (December 10, 2025) addressed China's \"third objection to jurisdiction\" based on Article 9(a)."

and `analytics/daily-research/2026-07-15.md` carries the same phrase, expressly at MEDIUM confidence *"from search-engine summaries"*, together with the line that shows the 15 July seat already saw the limit: *"Whether the tribunal actually upheld the Article 9(a) bar … is **unknown**."*

**What this does and does not change.**

- **Question (3) is still unanswered, and the gap slug stays open.** "China's third objection **to jurisdiction**" characterises how the *objection was framed*; question (3) asks how the *tribunal* characterised the conditions. Tribunals routinely re-characterise a condition pleaded as jurisdictional. The strongest carrier the project holds — the IAReporter analysis-article title, which says the tribunal **"gives effect to"** the administrative-review and fork-in-the-road clauses, "dismissing all claims" — uses a verb that takes no position between jurisdiction and admissibility. **`GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility` stands.**
- **My wording is withdrawn and replaced.** The accurate form: *no accessible source states the **tribunal's** characterisation; the record does hold, at MEDIUM to MEDIUM-HIGH and on title-and-snippet carriers only, that the Article 9(a) ground was pleaded as an objection to jurisdiction and that the tribunal gave effect to 9(a) and 9(c) in dismissing all claims.* Candidate claim 5 of yesterday, as amended at my second turn, should be amended again in this one respect before it goes anywhere near a ledger.
- **The direction is consistent across three artefacts and I still do not bank it.** The 2026-07-15 snippet, S3's prose and S4's prose all frame Article 9 as jurisdictional and none uses "admissibility". That looks like convergence and is not: two of the three are engine prose of the same class, and the third is itself a search-engine summary. Per taxonomy 16 and the officer's 2026-08-20 ruling, replication is not corroboration. **Direction noted; nothing moved.**

### `0822-R9` — A QUOTE-INTEGRITY DEFECT IN THE CANONICAL BASELINE, RAISED AND NOT EDITED.

The same `STATE_OF_THE_ANSWER.md` row presents three phrases in quotation marks — `"a compulsory precondition qualifying the Respondent's offer to arbitrate"`, `"did not even allege in the Memorial to have complied with the administrative review requirement"`, and `"third objection to jurisdiction"` — while **all three of its listed sources are unread bodies**: IAReporter `[title indexed; body paywalled]`, vLex UK `[body 403-blocked; snippet]`, JIDS Oxford `[abstract only]`. No per-quote pinpoint is given.

Under calibration item 2 and the carrying-span rule the correct label for all three is **span not pinned; source unread** — most likely snippet-carried, which is weak but not fabricated. **I have edited nothing.** I raise it because it bounds exactly how far `0822-R8` can move: the phrase that softens my yesterday's overstatement is itself resting on an unpinned quotation, and a seat tempted to lean on it should know that before leaning. Raising it rather than quietly using it is the disposition this seat is supposed to keep.

---

## TASK 3 — THE `ARB/24/46` / `ARB/24/26` DISCREPANCY STAYS **UNRESOLVED**. THE WEIGHT OF CARRIERS IS NOW ASYMMETRIC AND I SAY SO WITHOUT CALLING IT A RECONCILIATION.

### `0822-R10` — THE VERDICT: UNRESOLVED, WITH A DIRECTIONAL WEIGHT STATED AND NOT CONVERTED INTO A FINDING.

S5 put the contested string to the index as a designator in its own right. **Of nine returned results, zero titles carry `ARB/24/26`**; the returned ICSID case numbers were `ARB/05/8`, `ARB/21/26`, `ARB/24/6` and others, and the engine stated in terms that the results "do not contain specific information about ICSID case ARB/24/26".

**A docket number is a rigid designator** — a field cannot paraphrase `ARB/24/26` — so `0821-B14`'s synonym objection does not reach this zero. **But it is one notation.** I screened `ARB/24/26` and not `ARB 24/26`, `ARB.24.26` or a bare `24/26`; those are notational variants, not synonyms, and I did not try them.

**The state, exactly:**

| Designator | Carriers | Class |
|---|---|---|
| `ARB/24/46` | ICSID's own register, body-carried; italaw title `.../cases/13154` | one **body**, one independent title |
| `ARB/24/26` | one TDM Journal title | one title; **zero titles returned when searched as a designator** |

**That is an asymmetry, not a resolution**, and I record it as "not found in accessible sources", never as "ARB/24/26 does not exist". I do not silently prefer the majority. The instrument that would actually settle it is ICSID's own docket, and it is a relay row, not a search — proposed at `0822-R14`.

### `0822-R11` — WHAT I DID NOT DO, AND THE SECTOR PROSE.

No sector prose was offered to me today for any of the four, so there is nothing new to refuse; `0821-R7`'s refusals stand and remain banked in **neither** direction. The four proceedings sit where `0821-R6` and `0820-B2` put them: **UNEXAMINED, not EXCLUDED.** Nobody has read one of these decisions and no holding is asserted anywhere in this return. `0821-E5`'s uncovered item — a subject-matter search on `ARB/24/18` — is **still uncovered**, deliberately, for the reason at §A.

---

## TASK 4 — THE CHAIRMAN'S CLAIM IS **TRUE ON ITS FACTS, WRONG IN ONE MATERIAL RESPECT, AND NOT NEW**. THIS COUNCIL CENSUSED IT SEVEN DAYS AGO AND FIRST OBSERVED IT SEVENTEEN.

All derivations at HEAD **`682e762`**, working tree clean, over all **31** files and **208** records in `analytics/fetch-results/`. Script: `/tmp/claude-0/-home-user-isds-watcher/b8910b35-26ce-5bd8-a00a-e98b16b6eb10/scratchpad/census.py`.

### `0822-R12` — (a) THE CLAIM IS CORRECT AS TO BOTH NAMED FILES, AND THE MECHANISM READING IS CORRECT WITH ONE WRONG PINPOINT.

Both rows verified:

| File | commit holding it (`N12`) | URL | rows | `bytes` | distinct `sha256` |
|---|---|---|---|---|---|
| `2026-08-12-daily.json` | **`6cd27ac`** | `https://pca-cpa.org/en/cases/309/` | 3 (records 2, 3, 4) | 106011, constant | **3** |
| `2026-08-15-daily.json` | **`31d16fd`** | `https://pca-cpa.org/en/cases/336/` | 2 (records 3, 4) | 97343, constant | **2** |

**The mechanism reading is right.** `run()` builds one `records` list in one sequential loop (anchors `records = [fetch_one(CONTROL_URL)]` and `records.append(fetch_one(url, find))`) and writes one manifest (`out_path = out_dir / request_path.name`), so multiple rows on one URL in one file are necessarily one run, one process. Each row is an independent `resp = polite_get(url)`, and `bytes` and `sha256` are both computed over the same bound body.

**The pinpoint is wrong, and I flag it under rule (s) rather than replicate it.** `scripts/fetch_relay.py:171-173` is `content = resp.content or b""`, `content_type = …`, `record.update({` — **it contains neither field.** The correct anchors are:

```
178:        "bytes": len(content),
179:        "sha256": hashlib.sha256(content).hexdigest(),
```

both reading the `content` bound at `171: content = resp.content or b""`. The substance survives intact; the range does not. *(Also: the docket says "Cite `analytics/fetch_relay.py`". No such file exists; the file is `scripts/fetch_relay.py`.)*

### `0822-R13` — (b) IT IS **NOT** CONFINED TO `pca-cpa.org`. THE LARGER HALF IS ON `investmentpolicy.unctad.org`, AND IT IS ZERO ON THE TWO HOSTS THE CHAIRMAN ASKED ABOUT — WHICH LICENSES NOTHING.

Census of every `(file, URL)` group with more than one row, all rows `ok`:

| Host | multi-row groups | all-`ok` groups | rows in them | **bytes constant, sha256 varies** |
|---|---|---|---|---|
| `example.com` | 18 | 18 | 54 | **0** |
| `uncitral.un.org` | 17 | 17 | 36 | **0** |
| `icsid.worldbank.org` | 12 | 12 | 29 | **0** |
| `investmentpolicy.unctad.org` | 7 | 5 | 10 | **5** |
| `pca-cpa.org` | 3 | 3 | 7 | **3** |
| `www.italaw.com` | 2 | 0 | 0 | 0 *(all rows `refused`)* |
| `undocs.org` | 1 | 1 | 2 | **0** |
| **TOTAL** | **60** | **56** | **138** | **8** |

`bytes` was constant in **56 of 56** all-`ok` groups; `sha256` constant in **48 of 56**. **No group anywhere has both varying.** The eight divergent groups are `2026-08-03-targeted`, `2026-08-04-daily`, `2026-08-05-daily`, `2026-08-05-zz-attribution`, `2026-08-09-daily` (all at commit **`d05dee6`**, all on the China–Switzerland BIT `/978/` page, all at **207411** bytes), plus the two named above and today's own `2026-08-22-daily.json` at **`682e762`**.

**Per-URL, not per-host, and the zeros carry nothing.** `0813-B6(c)` fixed the first point and the 2026-08-15 record fixed the second as a condition of adopting this very measurement: a within-run control *"can only ever refute stability, never establish it."* **So `icsid.worldbank.org`'s 0-of-12 and `uncitral.un.org`'s 0-of-17 are not evidence that those pages' digests are trustworthy.** They failed to refute; that is all. I state the screened population — 12 and 17 all-`ok` groups, 29 and 36 rows, deduplicated to `(file, URL)` groups — per `0821-E7`, precisely so that the zeros are not read as a clean bill.

**On the `/978/` page the signature is total:** 14 `ok` rows across the corpus, **every one at 207411 bytes, 14 distinct digests, no digest ever repeating.**

**The two phenomena are TWO, and I decline to merge them.** Re-deriving the postscript's merge-time census blob-by-blob for its four digest-changing files:

| File | last commit | rows changed | hosts | excerpt changed? |
|---|---|---|---|---|
| `2026-08-11-zeph.json` | `0a67756` | 2 | pca ×2 | no |
| `2026-08-12-daily.json` | `6cd27ac` | 4 | pca ×4 | no |
| `2026-08-15-daily.json` | `31d16fd` | 2 | pca ×2 | no |
| `2026-08-16-daily.json` | `de3ed3c` | 2 | **icsid ×1**, pca ×1 | no |

**A correction to the postscript, on its own headline sentence.** It reads: *"Nine of the nine affected rows returned a DIFFERENT `sha256` at an IDENTICAL byte length … seven of them on `pca-cpa.org`, two on `icsid.worldbank.org`."* The split is **eight `pca-cpa.org` and one `icsid.worldbank.org`**, not seven and two — and **the single icsid row is the one that also changed length**, `105,669 → 105,666` (`find: "RER-03 - Expert Opinion of Doug Uffen"`). So it is **not** an instance of the constant-length signature at all, and the constant-length merge-time instances are **8 of 8 on `pca-cpa.org`**. The postscript's own next bullet already recorded the 3-byte move; the headline sentence did not carry it forward.

**Why they are two observations and not one:**

1. **Different comparison units.** Within-run compares two responses seconds apart in one process (bounded below by `MIN_INTERVAL_SECONDS = 3.0` per domain, and by nothing above that the record can establish — `0805`'s "minutes apart" was struck for exactly this). Merge-time compares two responses roughly a day apart, from different runner sessions.
2. **The merge-time set is not a sample of hosts.** Which files got overwritten was decided by `newest_request()`'s lexicographic pick at merge time, not by anything about the hosts. `investmentpolicy.unctad.org`'s divergent groups all sit in files dated 2026-08-03 to 2026-08-09, before the overwrite window opens on 2026-08-10 — so its **absence** from the merge-time set is an artefact of file selection and carries no information.
3. **On the one host where the two sets could have disagreed, they do.** `icsid.worldbank.org` shows **zero** within-run divergent groups over 12 groups, and its single merge-time change was a length change. Treating them as one phenomenon would require the merged account to predict a length change it does not predict.

**But one relation between them does hold, and it is the useful one.** The within-run observation **strictly dominates** the merge-time one as evidence about digest reliability: if two responses seconds apart differ in digest at equal length, then nothing can be inferred from two responses a day apart differing on that same URL. **On `pca-cpa.org` the merge-time observation adds no information the within-run data did not already carry** — which is the substance of `0822-R15`.

### `0822-R14` *(sic — see `0822-R16` for the row proposals; this number is used at `0822-R16` below)*

### `0822-R15` — (c) THE PRIOR-ART CHECK, AND IT IS THE MOST VALUABLE THING IN THIS SECTION. **THIS COUNCIL WROTE IT DOWN THREE TIMES, AND THE THIRD TIME WAS A WHOLE-CORPUS CENSUS.**

The chairman asked me to find this rather than let it surface in a week. It is there, in three layers:

**1. First observation — 2026-08-05.** `analytics/daily-research/2026-08-05.md`, anchor *"The two treaty-978 rows in the same run"*:

> "The two treaty-978 rows in **the same run, minutes apart**, both returned `bytes: 207411` and **different** sha256 (`c7afb6fd734d…` and `636c65c4033e…`). Across all three runs there are now **six fetches of that page, six distinct digests, every one at exactly 207,411 bytes**."

*(The words "minutes apart" in that sentence were themselves struck later the same day and should not be carried forward.)*

**2. The same mistake, already caught once — 2026-08-12.** `analytics/daily-research/2026-08-12.md`, anchor *"Today's batch is not the record's first same-run demonstration"*:

> "`analytics/daily-research/2026-08-05.md:109` already records two rows in **the same run, minutes apart**, at equal length with different digests — so 'today's batch demonstrated it three times' was not the first same-run demonstration."

**A seat presented the within-run form as new on 2026-08-12; the officer caught it that day.** Today's Task 4 is the same presentation, ten days later.

**3. The whole-corpus census — 2026-08-15, and it is the same census.** `analytics/daily-research/2026-08-15.md`, anchor *"every group of two-or-more rows on the same URL in the same run at constant byte length"*, reports:

| URL (host) | digest-IDENTICAL | digest-DIVERGENT |
|---|---|---|
| `uncitral.un.org/en/commission` | 9 | 0 |
| `example.com/` | 8 | 0 |
| `icsid.worldbank.org` case-detail (2 URLs) | 3 | 0 |
| `pca-cpa.org/en/cases/309/`, `/336/` | 0 | **2** |
| `investmentpolicy.unctad.org` treaties path | 0 | **5** |

**My census at HEAD reproduces that table exactly and extends it by seven days.** Restricting my figures to files existing on 2026-08-15 gives uncitral 9, example.com 8, icsid 3, pca 2, unctad 5 — identical in every cell. At HEAD the same cells read 17, 18, 12, 3 and 5, the pca increment being today's own batch. **The derivation is reproducible and stable, and it was already answered.**

**And the controlled form is prior art too, which bears on today's pre-registration.** Gate `G12` of `analytics/fetch-requests/2026-08-22-daily.json` describes `urls[7]`/`urls[8]` — same URL, same run, **same `find`** — as *"the cleanest controlled form of that test available."* That exact form has been sitting in the corpus since **2026-08-09**, on a different host, unnoticed:

```
2026-08-09-daily.json  (commit d05dee6)   run 31312614020
  records[1]  find='China - Switzerland BIT'  bytes=207411  sha=99f5417758472b2d…
  records[6]  find='China - Switzerland BIT'  bytes=207411  sha=7bb930f60ea8e647…
  excerpts byte-identical: True
```

Same URL, same run, same `find`, identical 400-character window, identical length, **different digest.** The 2026-08-15 census counted groups and did not isolate same-`find` pairs, so this specific pair is, as far as my grep reaches, unremarked — but the phenomenon it demonstrates is the one the record has held since 2026-08-05.

**The honest verdict on (a)–(c): the claim is true, its host scope is wrong, and it is a fourth re-derivation of something this council established on 2026-08-05, was corrected about on 2026-08-12, and censused corpus-wide on 2026-08-15.** That outcome is worth more to this council than a new finding, and it is the one I was asked to look for.

### `0822-R16` — (d) WHAT IT LICENSES. **NOTHING SUBSTANTIVE IS TOUCHED, AND I CHECKED RATHER THAN ASSUMED.**

**What it costs the digest.** On `investmentpolicy.unctad.org`'s `/978/` page and on `pca-cpa.org/en/cases/309/` and `/336/`, `sha256` is **worthless as a change signal**, in both directions and even at constant byte length: inequality shows nothing, and equality has never been observed on `/978/` at all. Change detection against those URLs must run on `outcome`, `status`, `bytes` and `find_matched` — which is next step 7 of the 2026-08-10 record, already adopted, and this adds no new host to it. **It costs the other hosts nothing and buys them nothing.**

**What it touches: no excerpt, no quoted window, no substantive finding.** Two independent screens:

1. **Within-run.** 21 same-`(URL, find)` groups with ≥2 `ok` rows across the corpus; **0 with a divergent excerpt.**
2. **Corpus-wide, across files.** 14 distinct `(URL, find)` keys have ≥2 `ok` rows, **130 rows** in them; **9 of the 14 keys show a varying digest; 0 of the 14 show a varying excerpt.** Including `Manifest Lack of Legal Merit` on the ICSID register — 7 rows, 7 files, **4 distinct digests, 1 excerpt** — so even the register's day-by-day one-byte movement never moved that window.

Add the field-by-field merge-time diff at `0822-R13`: **excerpt unchanged in all nine rows.**

**So: no excerpt in the corpus has ever differed between two rows that asked the same URL the same question.** That is the population — 14 keys, 130 rows, deduplicated by `(URL, find)`, scoped to `outcome == "ok"` — and it is the correct population for this negative, stated per `0821-E7`.

**What it emphatically does not license.** No mechanism. Nobody has seen the varying element; a rotating token, a nonce and a constant-width timestamp would each serve, and per the 2026-08-06 ruling this class of pattern is reported as *most consistent with* a constant-width per-response varying field and is **not diagnosed**. It says nothing about any case, award, proceeding, treaty or person; it is an instrument property of three URLs.

---

## `0822-R17` — RELAY ROWS PROPOSED FOR TOMORROW (`0821-R9` FORM). LICENCES FIXED HERE, BEFORE ANY RESULT EXISTS. I HAVE FIRED NOTHING.

Prior-art check run first, at HEAD `682e762`, over `analytics/fetch-results/*.json`: `cases/content/tables-of-decisions` (parent, exact) → **0 rows**; `CaseNo=ARB/17/19` → **0 rows** and **0 files repo-wide**; `CaseNo=ARB/24/26` and `CaseNo=ARB/24/46` → **0 rows**. All four routes are unfired. All are on `icsid.worldbank.org` (`ALLOWED_HOSTS`, `:49`), which has returned HTML this channel reads on every batch since 2026-08-19, and the `case-detail` form specifically has **16 `ok` rows across three case numbers** (`ARB/22/2` ×3, `UNCT/20/6` ×13).

| # | URL | `find` | What a match licenses; what a null licenses |
|---|---|---|---|
| 1 | `https://icsid.worldbank.org/cases/content/tables-of-decisions` | `Manifest Lack of Legal Merit` | **Render check on a never-fetched page.** A match licenses only that this page renders to this channel and that the find machinery worked on it, plus whatever the window shows. **Not** a control for the child table page or for any other row. |
| 2 | same URL | `not applicable` | **The `_` probe, fired against my own refusal at `0822-R2`.** A match licenses **only** the content of the returned window — it does **not** confirm the engine's prose, and only the window can place the string as a legend rather than prose. Weakness stated first: it contains a space, so per `G7` the relay can manufacture one and never remove one, making this a weaker test than a space-free literal. **If it nulls, the row banks nothing**: not-rendering, real absence and `G6`'s zero-width hazard are not separable by this row and I pre-register that I will not try to separate them afterwards. |
| 3 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/17/19` | `Status of Proceeding` | **Near-positive control**, body-observed on the `ARB/22/2` case-detail page. Lands a window in the metadata block. |
| 4 | same URL | `Decision on Jurisdiction` | **`N4`'s docket probe, at its true and small scope.** A match licenses **only** that ICSID's docket lists a document it designates that way. **A document designation is not the tribunal's characterisation and CANNOT answer question (3).** If a future seat cites this row for question (3), that citation is wrong and this sentence is why. |
| 5 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/24/26` | `Status of Proceeding` | **The digit discriminator, from ICSID's own body.** `find` is deliberately **not** `ICSID Case No.` and not the case number: a case-detail page may echo its own query parameter, and a `find` the page can generate from my request URL reports a property of the request (relay method rule 1 / taxonomy 23, this seat's own catch). **Branches pre-registered:** if the window carries party names other than ENCORE/Türkiye, the TDM title's designator resolves to a different proceeding and the discrepancy is closed **from a body**; if it carries ENCORE, the two designators both resolve to this matter and the discrepancy deepens; **if the row nulls or 404s, it banks nothing in either direction** — `G3`, a status is this runner's access at that instant. |
| 6 | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/24/46` | `Status of Proceeding` | Row 5's pair, same run, same `find`, so each is the other's context. **Neither is a control for the other** — they are different pages, which is `0821-B17`'s whole lesson. |

**Pre-registered nulls-of-design, licensing nothing, across all six:** `G3` — a refusal or 404 is this runner's access and never a statement about ICSID, a publication, or a proceeding; `G6`/`N7` — an undecoded entity or zero-width character inside any literal returns a clean `find_matched: false` indistinguishable from real absence; `G5` — anything living in an attribute is invisible; and **the cardinal one carried forward verbatim from yesterday: a match on rows 1–2 establishes that ICSID's explanatory material is readable by this channel and does NOT establish that a codebook for the register's finding column exists anywhere.**

---

## `0822-R18` — METHOD NOTE (self-training mandate): THE FAILING FORMULATION IS THE ONE I INVENT WHEN THE PROJECT ALREADY OWNS ONE.

`0821-R15` refined the rule at the level of query syntax — quote the party name, not the `ICSID Case No.` phrase. **Today shows the syntax was never the binding constraint.** S6 succeeded because it applied a template `analytics/optimization-log.md` has held since 2026-07-22, naming this exact case as its trigger; eight formulations across three days did not, because no seat opened the log. `0821-E3` diagnosed this as "I design the batch before I finish reading my own prior return" — that was too narrow. **The rule I actually need: the prior-art check under method rule (u) is not only over the daily records and the results corpus; it is over `analytics/optimization-log.md` and `analytics/insights.jsonl`, which is where this project stores methods that worked.** Cost of learning it: two days of Task-3 searching that a one-line grep would have redirected.

Second, cheaper note: **domain-restricting a search is a distinct instrument, not a filter.** S2 restricted to `icsid.worldbank.org` and surfaced a parent page eight unrestricted calls across four days never returned. It should be tried whenever the target is an institution's own apparatus rather than commentary about it.

---

## § CANDIDATE CLAIMS

```json
[
  {
    "claim_text": "No source title-carried to this council decodes the values of ICSID's 'Finding that Claim is Manifestly Without Legal Merit' column; the observed values No, Partial and _ remain untranslated and _ remains undefined. ICSID publishes a parent 'Tables of Decisions in ICSID Cases' page at https://icsid.worldbank.org/cases/content/tables-of-decisions which this council has never fetched.",
    "confidence": "medium-high",
    "sources": [
      "https://icsid.worldbank.org/cases/content/tables-of-decisions",
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"
    ],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM, scoped to this instrument, TWO CALLS DEEP AND NOT SETTLED. WebSearch S1 ('\"Finding that Claim is Manifestly Without Legal Merit\" \"Partial\"') and S2 ('tables of decisions legend note explaining columns finding', domain-restricted to icsid.worldbank.org) of 2026-08-22 returned 9 and 10 results; no returned title is a legend, key, codebook, explanatory note or how-to-read apparatus for the column. SYNONYM FAMILIES NOT TRIED: codebook, notes to the table, how to read, methodology, 'for information purposes only' — per 0821-B14 a zero on two families is not a zero on the set. The parent-page novelty is derived programmatically at HEAD 682e762: 17 rows in analytics/fetch-results/ carry 'tables-of-decisions', all 17 on the child URL /manifest-lack-of-legal-merit; the parent has 0 rows and 'tables-of-decisions/annulment' returns 0 files repo-wide. GENERATED PROSE DECODING BOTH 'Partial' AND '_' IS REFUSED AT 0822-R1 AND 0822-R2 AND IS NOT BANKED. Per the cross-BIT consistency rule this is 'not found in accessible sources', NOT 'does not exist'. GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook."
  },
  {
    "claim_text": "No HTML rendition of the Hela Schwarz GmbH v. People's Republic of China award (ICSID Case No. ARB/17/19, 10 December 2025) has been identified on any host in the relay's ALLOWED_HOSTS; every rendition surfaced on an allowed host is a PDF, and the sole HTML rendition of the award text surfaced anywhere is on jusmundi.com, which is not on ALLOWED_HOSTS.",
    "confidence": "medium-high",
    "sources": [
      "https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf",
      "https://icsidfiles.worldbank.org/icsid/ICSIDBLOBS/OnlineAwards/C6447/DS21216_En.pdf",
      "https://www.italaw.com/sites/default/files/case-documents/italaw11394.pdf"
    ],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM about a ROUTE, not about a document. Screened over WebSearch S3, S4 (domain-restricted to italaw.com, icsid.worldbank.org, icsidfiles.worldbank.org, investmentpolicy.unctad.org) and S6 of 2026-08-22 plus S2 and S3 of 2026-08-21 — five formulations, two days. Every award-bearing rendition returned on an allowed host ends in .pdf; excerpt_of returns '' for a PDF (scripts/fetch_relay.py, anchors 'if \"html\" not in content_type and \"text\" not in content_type:' / 'return \"\"'). jusmundi.com is absent from ALLOWED_HOSTS (anchor 'ALLOWED_HOSTS = {' at :48, set closing :69). ALL BODIES UNREAD. Derived at HEAD 682e762: www.italaw.com HTML rows in analytics/fetch-results/ number 6, across 3 distinct URLs and 3 result files, outcome 'refused' on all six; its PDF rows number 4, all 'ok'. PER G3 THE REFUSALS ARE THIS RUNNER'S ACCESS AT THOSE INSTANTS AND ARE NOT A STATEMENT ABOUT ITALAW. An HTML page on an allowed host that HAS never been fetched and IS newly title-carried is the ICSID case-detail page https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/17/19 (0 rows in the corpus, 0 files repo-wide); it is a DOCKET page, not the award, and cannot answer the characterisation question."
  },
  {
    "claim_text": "No accessible source states whether the Hela Schwarz tribunal characterised the China-Germany BIT Protocol Article 9 conditions as going to jurisdiction or to admissibility. The project record does hold, on title and search-engine-snippet carriers only, that the Article 9(a) ground was pleaded as an objection to jurisdiction and that the tribunal gave effect to Article 9(a) and 9(c) in dismissing all claims.",
    "confidence": "medium-high",
    "sources": [
      "https://www.iareporter.com/articles/analysis-icsid-tribunal-hearing-spice-investors-claim-against-china-gives-effect-to-idiosyncratic-administrative-review-and-fork-in-the-road-clause-found-in-bit-protocol-dismissing-all-clai/",
      "https://www.iareporter.com/articles/hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation-but-accepts-amendments-to-the-claimants-request-for-arbitration/"
    ],
    "supporting_quote": null,
    "supporting_locator": "AMENDS AND REPLACES candidate claim 5 of 2026-08-21 as amended at that seat's second turn. THE AMENDMENT IS AGAINST MY OWN PRIOR WORDING: 0821-R10's 'No accessible source addresses it' was too strong. STATE_OF_THE_ANSWER.md (anchor: 'Article 9(a) administrative-exhaustion pre-condition') and analytics/daily-research/2026-07-15.md both carry the phrase 'third objection to jurisdiction' for the Article 9(a) ground; the 2026-07-15 record labels it MEDIUM confidence 'from search-engine summaries' and states in terms that whether the tribunal upheld the 9(a) bar 'is unknown'. THAT CHARACTERISES HOW THE OBJECTION WAS PLEADED, NOT HOW THE TRIBUNAL CHARACTERISED THE CONDITION, AND SO DOES NOT ANSWER THE QUESTION. The strongest carrier the project holds, the IAReporter analysis-article title, uses the verb 'gives effect to', which takes no position between jurisdiction and admissibility. QUOTE-INTEGRITY CAVEAT TRAVELLING WITH THIS CLAIM (0822-R9): all three sources listed on that STATE_OF_THE_ANSWER.md row are unread bodies — IAReporter paywalled, vLex 403-blocked, JIDS abstract-only — and its three quoted phrases carry no pinpoint; under the carrying-span rule they are 'span not pinned; source unread'. Generated prose asserting 'jurisdiction and merits grounds' was replicated a THIRD time today (S3) including the impossible construction 'a final award dated December 10, 2025 (published on January 15, 2025)'; per taxonomy 16 replication by the same class of instrument is NOT corroboration and it is NOT banked, and it would not answer this question even if it were, since it never uses the word admissibility. The second IAReporter title is NEW to the repository (git grep -l -F 'hela-schwarz-v-china-tribunal-rejects-chinas-request-for-bifurcation' → 0 files at HEAD 682e762), body unread, and concerns bifurcation. GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility."
  },
  {
    "claim_text": "The discrepancy between ICSID Case No. ARB/24/46 (ICSID register body; italaw title) and ARB/24/26 (TDM Journal title) for ENCORE Investment Group Limited v. Republic of Türkiye is UNRESOLVED; a search treating ARB/24/26 as a designator in its own right returned zero titles carrying that string.",
    "confidence": "medium",
    "sources": [
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit",
      "https://www.italaw.com/cases/13154"
    ],
    "supporting_quote": "ENCORE Investment Group Limited (Malta) v. Republic of Türkiye (ICSID Case No. ARB/24/46) Decision on the Respondent Rule 41 Application ( English ) October 7, 2025 No",
    "supporting_locator": "Quote is the ICSID register half, body-carried in the window reproduced at analytics/daily-research/2026-08-20.md Part II section 1 (records[3], find 'ARB/24/'). WebSearch S5 of 2026-08-22, '\"ARB/24/26\" ICSID case claimant respondent', returned 9 results; ZERO returned titles carry ARB/24/26, and the engine stated the results 'do not contain specific information about ICSID case ARB/24/26'. A DOCKET NUMBER IS A RIGID DESIGNATOR so 0821-B14's synonym objection does not reach this zero; LIMITATION STATED: one notation only, and ARB 24/26, ARB.24.26 and a bare 24/26 were not screened. THIS IS 'NOT FOUND IN ACCESSIBLE SOURCES', NOT 'DOES NOT EXIST', and it is NOT a reconciliation: the carriers are asymmetric (one body plus one independent title for /46; one title and zero designator hits for /26) and the discrepancy STAYS UNRESOLVED. The trailing register token 'No' is reproduced as rendered and IS NOT TRANSLATED. NOBODY HAS READ THIS DECISION AND NO HOLDING IS ASSERTED. Relay rows that would settle it from ICSID's own body are proposed at 0822-R17 rows 5-6 and were NOT fired by me."
  },
  {
    "claim_text": "Within a single relay run, multiple rows on the same URL have returned identical bytes with different sha256, in 8 of 56 all-ok multi-row (file,URL) groups across the fetch-results corpus: 5 on investmentpolicy.unctad.org and 3 on pca-cpa.org, and 0 on icsid.worldbank.org, uncitral.un.org, undocs.org or example.com. No excerpt has ever differed between two rows asking the same URL the same question.",
    "confidence": "high",
    "sources": ["scripts/fetch_relay.py"],
    "supporting_quote": "The two treaty-978 rows in the same run, minutes apart, both returned `bytes: 207411` and **different** sha256",
    "supporting_locator": "NOT A NEW FINDING — PRIOR ART, and the quote is that prior art: analytics/daily-research/2026-08-05.md, anchor 'The two treaty-978 rows in the same run'. (The phrase 'minutes apart' in that sentence was struck later the same day and must not be carried forward.) The council was corrected for re-presenting this as new on 2026-08-12 (anchor 'Today's batch is not the record's first same-run demonstration') and ran the WHOLE-CORPUS census on 2026-08-15 (anchor 'every group of two-or-more rows on the same URL in the same run at constant byte length'), whose five-row table my derivation reproduces cell-for-cell when restricted to files existing on that date. CENSUS DERIVED PROGRAMMATICALLY at HEAD 682e762 over 31 files / 208 records / 186 ok rows: 60 multi-row (file,URL) groups, 56 all-ok, bytes constant in 56/56, sha256 constant in 48/56, zero groups with both varying. Divergent groups and their commit pins per N12: 2026-08-03-targeted, 2026-08-04-daily, 2026-08-05-daily, 2026-08-05-zz-attribution, 2026-08-09-daily (all d05dee6, all /978/, all 207411 bytes); 2026-08-12-daily (6cd27ac, pca 309, 106011); 2026-08-15-daily (31d16fd, pca 336, 97343); 2026-08-22-daily (682e762, pca 309, 106011). Mechanism anchors: 'content = resp.content or b\"\"' (:171), '\"bytes\": len(content),' (:178), '\"sha256\": hashlib.sha256(content).hexdigest(),' (:179) — NOTE the range :171-173 contains NEITHER field. THE ZEROS LICENSE NOTHING: per the 2026-08-15 ruling a within-run control 'can only ever refute stability, never establish it', so 0/12 on icsid and 0/17 on uncitral are not a clean bill. NO MECHANISM IS DIAGNOSED; per the 2026-08-06 ruling this is reported as most consistent with a constant-width per-response varying field. Excerpt-invariance screened over 14 distinct (URL,find) keys with >=2 ok rows, 130 rows: 9 keys show a varying digest, 0 show a varying excerpt."
  },
  {
    "claim_text": "The 2026-08-21 postscript's merge-time census misstates the host split of its nine digest-changing rows as seven pca-cpa.org and two icsid.worldbank.org; the split is eight and one, and the single icsid.worldbank.org row also changed byte length (105,669 to 105,666), so it is not an instance of the constant-length signature at all.",
    "confidence": "high",
    "sources": ["analytics/daily-research/2026-08-21.md"],
    "supporting_quote": "Nine of the nine affected rows returned a DIFFERENT `sha256` at an IDENTICAL byte length with an IDENTICAL rendered excerpt — seven of them on `pca-cpa.org`, two on `icsid.worldbank.org` case-detail (one of which also moved 3 bytes).",
    "supporting_locator": "Quote from the 2026-08-21 POSTSCRIPT, section 'An instrument finding that fell out of the census, and it is new'. Re-derived blob-by-blob (first commit vs last commit touching each file) for the four files the postscript names: 2026-08-11-zeph (cc3730b→0a67756) 2 rows, both pca-cpa.org; 2026-08-12-daily (4af5f11→6cd27ac) 4 rows, all pca-cpa.org; 2026-08-15-daily (f463d08→31d16fd) 2 rows, both pca-cpa.org; 2026-08-16-daily (1ec3e9c→de3ed3c) 2 rows, one icsid.worldbank.org (find 'RER-03 - Expert Opinion of Doug Uffen', 105669→105666) and one pca-cpa.org. Total 8 pca + 1 icsid = 9. THE POSTSCRIPT'S NEXT BULLET ALREADY RECORDS THE 3-BYTE MOVE; only its headline sentence fails to carry it forward, so this is a wording defect, not a derivation defect. CONSEQUENCE: the constant-length merge-time instances are 8 of 8 on pca-cpa.org. NO EXCERPT CHANGED IN ANY OF THE NINE ROWS, confirming the postscript's most important sentence; no finding about any case, award, proceeding or person is impeached."
  }
]
```

---

## § OBJECTIONS AND OBSERVATIONS AGAINST MY OWN WORK

**`0822-E1` — THE PRIOR-ART FAILURE AT `0822-R7` IS MINE THREE DAYS RUNNING, AND I FOUND IT ONLY BECAUSE I WAS ORDERED TO RUN A DIFFERENT GREP.** `analytics/optimization-log.md` has held, since 2026-07-22, a demonstrated method naming *this case* as its trigger. I did not open that file on 2026-08-20 or 2026-08-21, and I opened it today only while chasing the living memory's IAReporter citation for Task 2 — not as a prior-art step. Had I not been reading the Article 9(a) row for an unrelated reason, S6 would never have been designed and I would have filed a third day's null on a question the project had a tool for. **The check that saves me is the one I run for a different purpose, which means it is luck, not method**, and `0822-R18` is my attempt to convert it.

**`0822-E2` — MY TASK 4 CENSUS TRAVERSES A FILE WHOSE INTERPRETATION IS NOT MINE, AND ONE OF ITS ROWS IS THE CHAIRMAN'S OWN PRE-REGISTERED EXPERIMENT.** `2026-08-22-daily.json` records[7]/[8] are `G12`'s pre-registered pair, and they fall inside my category-A count. I have reported them as census rows — bytes constant at 106011, two distinct digests, excerpts byte-identical — because excluding today's file from a corpus census would be a silent scope cut, and because `G12` fixed both branches in advance so no reading of mine can be chosen after the fact. **But the licence attaching to that pair is Part II's to state, not mine**, and if the chairman considers I have pre-empted him, the two rows can be struck from my count and it falls from 8 to 7 without disturbing anything else. I flag it rather than let it pass unremarked.

**`0822-E3` — `0822-R15`'s "as far as my grep reaches" is doing real work and I want it visible.** I claim the 2026-08-09 same-`find` pair is unremarked in the record. I screened `within-run`, `same run`, `same-run`, and the byte counts `106011`, `106,011`, `97343`, `97,343`, `207411`, `207,411`. I did **not** screen for a seat describing the same observation without any of those strings — e.g. by digest prefix, or by run number `31312614020`. **A grep establishes absence from the repository under the terms screened, never absence from the project**, and given that today's whole Task 4 is a lesson about re-derivation, my own novelty claim is the last one that should get a pass. Treat it as "not found under six screened terms".

**`0822-E4` — I SPENT S3 ON A COMMENTARY WHOSE TITLE THE RECORD HAS HELD IN NINE FILES SINCE JULY, AND IT RETURNED WHAT THE RECORD SAID IT WOULD.** `git grep -l -F "landmark-icsid-victory-for-the-peoples-republic"` returns **9 files** at HEAD `682e762`; `aoshearman` returns **9**. The 2026-08-20 record already named A&O Shearman as a source that "emphasised the 9(c) court-withdrawal ground" and already recorded the impossible date. **I bought a third replication of a known defect for one call.** This is `0821-E3`'s pattern in its fourth instance: I designed S3 from the *gap* rather than from the *record*, and the record already contained the answer to what S3 would return. Two of my six calls today (S3, and arguably S1) were purchasable for free.

**`0822-E5` — WHAT I DID NOT CHECK, NAMED SO COVERAGE IS NOT READ AS SILENCE.** (i) No subject-matter search on `ARB/24/18` or `ARB/22/30`; my Task 3 verdict for those two still rests entirely on the 2026-08-20/21 title harvests, collected to answer a different question — `0821-E5`'s admission, uncorrected. (ii) Four synonym families for Task 1 untried (`0822-R4`). (iii) One notation only for `ARB/24/26` (`0822-R10`). (iv) **I did not verify that ICSID case-detail pages carry any field that would place a proceeding in a subject-matter ring**; `0821-R9` screened every stored case-detail excerpt for `Economic Sector|Subject Matter|Sector|Industry|Applicable Treaty|Convention` and got **zero hits**, and nothing today changes that, so rows 3–6 of `0822-R17` are probes into a metadata block whose contents I am partly guessing at. (v) I have not read any window from today's batch's ICSID rows, by design, so nothing I say about Task 1's route accounts for what those rows may already show.

**`0822-E6` — A DEFECT IN MY OWN NUMBERING, LEFT VISIBLE RATHER THAN SILENTLY REPAIRED.** `0822-R14` is a stub pointing forward to `0822-R16`/`0822-R17`; I drafted the row-proposal table before the Task 4 sub-answers and did not renumber when they grew. **Renumbering after the fact is how a citation to `0822-R14` in a later record comes to resolve to the wrong finding** — the same failure class as `0821-R13`(4)'s dead escalation number and `0821-R12`'s self-falsifying anchor. The stub stays; the chairman may collapse it, but if he does, he should note the collapse rather than close the gap silently.

**`0822-E7` — THE OBSERVATION I MOST EXPECT TO BE WRONG ABOUT.** At `0822-R13` I assert that the within-run and merge-time phenomena are two and decline to merge them, and my third reason leans on `icsid.worldbank.org` showing zero within-run divergence over twelve groups. **By the 2026-08-15 one-directional ruling I quote three paragraphs earlier, that zero refutes nothing** — so it cannot bear the weight of "the two accounts disagree on this host". The honest reduction of my third reason is narrower: *the merge-time set contains an icsid row that changed length, which the constant-length account does not predict; whether icsid also varies within a run is unmeasured, not measured-negative.* Reasons 1 and 2 stand unaffected and the refusal to merge survives on them. **I caught myself applying the asymmetric filter the officer named at `0815-B8`, in the same section where I quoted the ruling against it.**

---

*Return closed. Six of nine searches spent, three returned. Zero fetches attempted; the relay was not fired by me and no second batch is proposed for today. Nothing edited, nothing built, nothing applied — `STATE_OF_THE_ANSWER.md`, the pre-registration file and the operator's pending list are untouched by this seat. Two gap slugs stand open, one of my own findings from yesterday is withdrawn and re-worded against me, and the day's central assignment resolved to prior art this council wrote down on 2026-08-05, was corrected about on 2026-08-12, and censused on 2026-08-15.*

---
## Part IV — The digest of record (Rule 1)

`Digest 2026-08-17: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied verbatim from `digests/2026-08-17_ISDS-Thematic-Watch/meta.json` — `screened: 7`, `matches: 0`,
`watch_list_leads: 0` — read from the file, not restated from memory, and pinned to blob `e1d1ef76`
at commit `feb1b94`. Rule 1's fixed terminology is used and the banned bare word does not appear.

**The digest of record has not moved, for the fourth consecutive daily record.** It is the same digest the
2026-08-19, 08-20 and 08-21 records carried. The collection run is weekly and fires on Mondays; today is
Saturday, so **the next collection is Monday 2026-08-24** and no digest movement was possible today.

Rule 0 keeps this short when the digest is unchanged, and it is unchanged, so this is short. Two things
are worth one line each because they bear on how the next digest should be read.

**`meta.json` still carries no collection-run identifier** (escalation `N8`). The 2026-08-17 file was
rewritten three times across two hours and twenty-three minutes with `screened` going 13 → 17 → 7, and
nothing in the file distinguishes those three states. Today's reading is safe only because it is pinned to
a blob and because that file settled five days ago. **Monday's collection will produce a file with the same
defect**, and any seat reading it inside the rewrite window will read a number that does not survive.

**Source health, unchanged and not re-litigated:** the archived `source_health` block records `italaw` and
`unctad_isds` as `RECOVERED (Internet Archive)` each after one `403` refusal, `iareporter_headlines` as
`HEADLINE-ONLY`, `gdelt` as `NOT-READ (429)`, and `iisd_itn` as `QUIET (feed live; newest item
2026-04-21)`. Zero of the seven candidates reached a match. **Per the standing rule these are statements
about that run's access, not about the sources.** The health-column proposal that would surface them
routinely remains on the operator's pending list and is not built.


---
