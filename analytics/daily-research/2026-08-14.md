# ISDS Thematic Watcher — daily council, 2026-08-14

**Chairman:** the scheduled cloud session, running as the chairman seat on Claude Opus 5 per the
operator's model directive of 2026-08-03.
**Convened:** `research-analyst` (Claude Opus 5) as a real subagent; the integrity challenge heard
inside Part V per Rule 0, also as a real subagent, its return reproduced verbatim.
**Model discrepancy, recorded per the never-substitute-silently rule at `src/models.py`, and added at
`0814-B4`:** the integrity officer's committed definition declares `model: opus` and is documented as
Claude Opus 4.8 (`UTILITY_MODEL`); in this cloud runtime the alias resolved to **`claude-opus-5`**,
which the seat reported itself. **REQUESTED `claude-opus-4-8` -> ACTUAL `claude-opus-5`.**
`record_fallback()` did not fire and `HANDOFF.md` carries no "Model runtime fallbacks" section
(`grep -n "Model runtime fallbacks" HANDOFF.md` returns nothing). That gap is escalation-grade, it is
not closed here, and it goes to Emory below. The hazard was already logged on 2026-08-02
(`analytics/daily-research/2026-08-02.md:5`) and has not been carried into a daily record since.
**Not convened, per Rule 0:** `analytics-officer`, `systems-researcher`.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a five-part meeting with a separately-filed
integrity officer (Part 3), an analytics officer (Part 4) and a systems researcher (Part 5).
`prompts/daily_council_protocol.md` Rule 0 says the daily meeting is **the researcher, the chairman,
and one optimization item**, that the integrity challenge is heard inside the Part V dialogue and
files no separate return, and that the analytics officer is not convened at all — the digest sentence
is two lines the chairman copies from `meta.json`. The protocol file's own Precedence clause says it
wins, and that the conflict is worth a line in the record's procedural caveat. **This is that line.**
I ran the meeting to Rule 0 in all respects but one, named here: Rule 0 says "convene exactly two
agents… nothing else", and three were convened. The integrity officer was convened as a **real
agent**, because the alternative — a chairman writing the challenge to his own work — is the failure
this project treats as cardinal. Rule 0 governs where that voice is *filed*, not whether it is *real*.
This is the same resolution the 2026-08-13 seat reached, and I adopt it deliberately rather than by
drift.

### Instrument state, probed at session open

Both fetch instruments are dead, and both were probed rather than assumed. The relay is alive and did
the day's retrieval.

- **Direct egress: DEAD.** `curl https://example.com` returned `CONNECT tunnel failed, response 403`
  at the proxy gateway; `curl https://en.wikipedia.org/wiki/Main_Page` returned the same. Two hosts,
  both neutral controls chosen precisely so the answer cannot be about a research source. The proxy's
  own status endpoint logged both as `connect_rejected` / "gateway answered 403 to CONNECT (policy
  denial or upstream failure)". This is the plumbing.
- **The fetch service: DEAD.** `WebFetch` on `https://example.com` — the neutral control — returned
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`.
- **The fetch relay: ALIVE**, and it did the day's retrieval. **One batch, one run**, answered by
  `github-actions-runner (run 31797361529)` at `2026-08-14T11:43:55Z`. Pre-registered before any
  result existed. Details in Part II.
- **WebSearch: alive**, and it was the analyst's only instrument.

Per the standing rule that a tool-layer status is never a statement about a resource: none of the
above is a fact about UNCITRAL, UNCTAD, ICSID, italaw or any document. It is a fact about this
session's plumbing, and it is why the relay exists.

### Agenda

Three items, all drawn from the 2026-08-13 close-out's next steps, and all of them relay-side except
the analyst's own track.

1. **Next step 1 — fire the companion rows on the *Einarsson* docket.** The 08-13 close-out named
   `find: "Procedural Orders"` and `find: "Published Decisions"`, one string per row, never bundled,
   and per `0813-B5` no bare identity row on `icsid.worldbank.org` (an empty `find` returns
   `body[:400]`, which on that host is global navigation by construction and discriminates nothing).
   The stated purpose was specific: **date PO No. 9 and settle whether a PO No. 10 exists** — the one
   thing that would retire two unverified generated-summary items.
2. **Next step 5 — the A/81/17 watch continues**, in the standard same-page positive-control form,
   with the four standing scope limits stated rather than assumed, and per `0813-B9` with the
   population of any null-count stated alongside the count.
3. **Next step 6 — the UNCITRAL digest movement is a LEAD, not a finding.** An anchored `find` on the
   59th-session block was authorised; nothing about it may be reported as a document change.

One row was added beyond the close-out's list: a fourth observation of this client's access condition
at `investmentpolicy.unctad.org`, carrying the never-answered `Denial of benefits` question. It costs
one row, and if access had recovered it would have discharged part of escalation 6.

The analyst was pointed at the two things a search can actually advance — the open
`zeph-australia-award-2025-jurisdictional-ground` gap, and the contemporary jurisdictional /
admissibility limb — and told expressly that an under-budget return counts as a good outcome.

**Spend checkpoint, placed here per the routine and honoured after the vetting round:** it is recorded
in Part VI, after the objections, not before them.

### Member return-path protocol

The analyst was briefed that his return enters the record **verbatim** as final record text; that
direct fetch and the fetch service are both dead and WebSearch is his only live instrument; that
today's relay batch was already fired and is **mine**, so he must not fire another and must not spend
budget on the four questions it asks; that the standing tool-status-as-source-state rule is cardinal;
that `0811-B4` (title-vs-summary), `0811-B5` (unenumerated superlatives) and `0811-B6`
(disposition-vs-rule and dropped qualifiers) bind him; that `0813-O6` (precise locators) binds him;
that the **two** screens adopted on 2026-08-13 — `analytics/errors/` and a record screen on the case
at hand — are standing steps before his first search; that no figure may rest on a search-engine
generated summary; and that his budget was **four WebSearch calls, hard**. He was expressly told not
to re-run the IAReporter domain query and not to try to settle the China RDP tier conflict by search.

The integrity officer received the analyst's return, the relay reduction, and this record's own draft
parts, and was told its objections bind me as well as him. Both returns are reproduced without
editing.

### The one optimization item (Rule 0)

**Today's item is the one the chairman seat keeps paying for and has not fixed: the relay's
lexicographic request selection is not merely a trap, it is an *unmonitored* trap.**
`newest_request()` sorts by filename despite its name (`scripts/fetch_relay.py:233-235`) and `run()`
writes to `out_dir / request_path.name` with no existence check, so a mis-sorted second batch silently
re-runs and overwrites a completed one. The fix is on the operator's PENDING list and is not applied
here. But the *observation* worth adding is that the failure is **invisible in the artifact**: an
overwritten reduction looks exactly like a fresh one, and the 2026-08-13 seat only caught it because
it happened to notice a `fetched_at` it did not expect. The cheapest partial mitigation available
inside the existing pending patch is not a new feature but one line of it — **refuse to overwrite an
existing result file** — because that converts a silent corruption into a loud failure. That is the
whole item; nothing was built and nothing was applied. **Do NOT accept an mtime-based fix:
`actions/checkout` destroys mtimes.**

---

## Part II — The relay batch: the docket window reaches further, a plural that did not match, a ninth null, and a window that did not move

The batch is `analytics/fetch-requests/2026-08-14-daily.json`, pre-registered before any result
existed, answered at `2026-08-14T11:43:55Z` by `github-actions-runner (run 31797361529)`.

**Batch validity, discharged by me rather than left to the officer.** `control_ok: true`,
`void: false`, `truncated: false`, `excerpt_chars: 400`, `requested: 8`, **`completed: 7`** — the one
shortfall being the UNCTAD refusal at P4 below. My own `example.com` brackets are rows 2 and 9 of the
reduction: **both ok/200 with `find_matched: true`.** Every row below is therefore readable within its
own pre-registered scope.

**Relay code pin.** `scripts/fetch_relay.py` at this branch's HEAD is blob
`7204847f908d7958584842e523da8ae0ecd08703`, **verified by blob equality and not by history walk** per
`0811-O1` — identical to the blob pinned in all three 2026-08-13 batches.

**Citation convention.** Per taxonomy entry 25 (MUTABLE-REDUCTION CITATION, adopted 2026-08-05), a
reduction is cited at the **commit** that wrote it, because the relay overwrites results in place.
Today's reduction is pinned at **`43be839`**. Only one batch ran today and nothing was overwritten;
the `zz-` convention was not needed because no second batch was fired, and the first batch's filename
was checked against `sorted(glob('*.json'))[-1]` **before** it was pushed rather than after.

### P1 — the *Einarsson* published-decisions window reaches further, and the question the row was fired to answer is NOT answered

*(Both headings above corrected at `0814-B1`. They previously said the docket "list extended" / "list extends", which asserts that ICSID's list grew — the proposition this section's own item 1 expressly refuses. What changed is the WINDOW, not the LIST.)*

Two rows, both on
`https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6`, both **ok/200,
105,670 bytes, sha256 `4573db23…`** — same page, same run, same digest, so the pair is a genuine
within-run comparison and not a cross-instant one.

**Row 4, `find: "Published Decisions"` — MATCHED.** The window, verbatim, and it is the whole of the
evidence:

> "test Development: December 2, 2025 - Each party files a statement of costs. (a) Original
> Proceeding Published Decisions Witness Statement of Theodore David Einarsson (Public Version)
> Procedural Order Statement of Defence Confidentiality Order Witness Statement of Russell John
> Einarsson (Public Version) Witness Statement of Ralph Maitland Expert Report of Nigel Bankes Expert
> Report of “Chip” Gordon"

**Row 3, `find: "Procedural Orders"` (plural) — NOT MATCHED.**

Read together, and only within their own scope, these two rows say something precise and something
smaller than the close-out hoped for.

**What is ESTABLISHED.** ICSID's own case-detail page for UNCT/20/6, at that instant, rendered a
`Published Decisions` list under the heading `(a) Original Proceeding`, and the 400-character window
centred on that heading carries the items quoted above. **Three of them are visible to this record for
the first time** — `Witness Statement of Ralph Maitland`, `Expert Report of Nigel Bankes`, and
`Expert Report of “Chip” Gordon` — because yesterday's longest window on this page stopped at
`Witness Statement of Russell John Einarsson (Public Ver`. **The "first time" is a `0811-B5` claim and
is therefore derived rather than asserted:** `grep -ril <name> --exclude-dir=.git . | grep -v 2026-08-14`
over the whole repository — the second stage excluding today's record and today's reduction, and
written in re-runnable form per `0814-O1` — returns **0 files for `Maitland`, 0 for `Bankes`, and 1
for `Gordon`** — and that one is `analytics/daily-research/2026-07-14.md:45`, which
names *Gordon Burr*, a claimant in *B-Mex, LLC and Others v. United Mexican States*, an unrelated
person in an unrelated case with, in that record's own words, "[n]o IP, trade secret, or
pharmaceutical nexus." All three names are absent from the **repository working
tree**; per `0814-O2` that screen does not reach operator-side material, the verification ledger
outside the repo, or anything in Emory's hands, and "new to this record" is scoped accordingly.

The row also **independently re-carries the dated value** `December 2, 2025 - Each party files a
statement of costs.`, the field's label being clipped at the window's left edge to `test
Development:` and **not reconstructed here**. The full label `Latest Development:` is carried by
yesterday's rows, `analytics/fetch-results/2026-08-13-zz-einarsson-docket.json` `records[3]` and
`records[4]`, at the commit that wrote them. A different day, a different run, a different `find` and
a different page digest all return the same dated value. That is corroboration of yesterday's
single-run read, and on my judgement it is the most useful row in today's batch across the seven
completed rows. *(Corrected at `0814-B2` and `0814-O14`. I had written the completed label `Latest
Development:` inside backticks and predicated it of TODAY's row, which opens `test Development:` —
the characters `La` are not in the retrieval I attributed them to. The completion was sourced from
yesterday's rows rather than remembered, and the dated value genuinely is re-carried, but `0813-B3`
was sustained yesterday on the rule that a correct completion is not the test.)*

**What is NOT established, and the list is long because the temptations here are real.**

1. **VISIBILITY IS NOT FILING.** Those three items became visible to *this record* today. Nothing
   whatever establishes when ICSID published them, and it would be a straight tool-status-as-source-
   state violation to write that three documents "were added". They may have sat on that page for a
   year; our window simply did not reach them.
2. **THE LIST IS STILL TRUNCATED, so no count, no enumeration, and no "latest" may be drawn from it.**
   The window is capped at 400 characters and ends mid-list at `Expert Report of “Chip” Gordon`. Per
   `0811-B5` I have not counted the items and will not; the visible items are quoted and that is all.
3. **PO No. 9 IS NOT DATED AND PO No. 10 IS NOT SETTLED — next step 1's stated purpose FAILED.** The
   list carries a **bare, unnumbered, undated `Procedural Order`**. The plural string `Procedural
   Orders` did not match on the same page in the same run at the same digest, which records that the
   string was **absent from this instrument's rendered-text extraction of that page at that instant**
   — subject to G6 and G7, and emphatically **not** a fact about how many procedural orders exist in
   the proceeding. *(Corrected at `0814-O15`: I had written "tells us the page does not render a
   plural field label", which is one step past what a tag-and-attribute-stripping channel can
   return.)*
   **Procedural Order No. 10 and the 9 February 2024 order remain sourced ONLY to search-engine
   generated summaries and remain NOT ASSERTED.** I record this as a failure of my own row design
   rather than as a result: I fired the strings the close-out named, and the close-out named the wrong
   strings.
4. **THE BYTE CHANGE FOUNDS NOTHING.** This page returned 105,667 bytes / sha `78376765…` on
   2026-08-13 and 105,670 bytes / sha `4573db23…` today. Three bytes. It would be very easy, and
   wrong, to attribute that delta to the three newly-visible list items — three list items are worth
   far more than three bytes, the relay's `bytes` is the raw response length while the excerpt is
   post-strip rendered text, and per G5 a digest change is never a document change. **No causal link
   between the byte delta and the window contents is asserted, and the two facts are recorded side by
   side precisely so that no reader infers one.**
5. **`Pending` is not carried forward as new.** The proceeding's status was not re-asked today and
   nothing here amends the standing posture recorded in `STATE_OF_THE_ANSWER.md`.

**The one thing genuinely worth the council's attention, stated as the lead it is:** the docket window
now carries **two named expert reports and a third named witness statement**. Who Nigel Bankes and
“Chip” Gordon are, and what their reports bear on, is **not established by this row and is not
asserted here** — identifying them is search-track work and it is routed to tomorrow in Part VI. What
the row does establish is that the administering institution's own page names them, which is a
materially better provenance than the index- and title-carried material this record has otherwise held
on this case.

### P2 — the A/81/17 watch returns its ninth null on that page, with the population stated

Row 5: `https://uncitral.un.org/en/commission`, `find: "A/81/17"` — **ok/200, 832,338 bytes,
`find_matched: false`, excerpt empty.**
Row 6, the same-page positive control fired in the same run: `find: "A/80/17"` — **MATCHED**,
returning:

> "ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna
> A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session
> A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth
> session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se"

So the page is **intact**, it is **still the page we think it is**, and it demonstrably lists a
session's report by symbol in exactly the form the watch string would take.

**The count, and the population it is drawn from, per `0813-B9` — derived programmatically over
`analytics/fetch-results/*.json`, not read off a printed scan.** The corpus holds **11 rows** carrying
the find `A/81/17`. Of those:

- **9 are `ok/200`, `find_matched: false` on `https://uncitral.un.org/en/commission`**, at **9 distinct
  dates**: 2026-08-03, 08-04, 08-05, 08-06, 08-07, 08-09, 08-10, 08-13 and today. **Today is the
  ninth.**
- **1 is a `find_matched: true` on `https://undocs.org/A/81/17`** (2026-08-04) — a **request-echo** on
  a route retired that same day precisely because the page generates the symbol from the request. It
  corroborates nothing and it is named here so that a nonzero count is not read as one.
- **1 is `robots_disallowed` on `digitallibrary.un.org`** (2026-08-08) with **no `find_matched` key at
  all** — never looked, and per `0807-B5` not poolable with a `false`.

**The four standing scope limits travel with this null unchanged:** (1) **RENDERED TEXT ONLY** — the
relay strips every tag *and* attribute (`scripts/fetch_relay.py:126-128`), so a symbol present only in
an `href` is invisible to this channel; (2) **ENTITY-BLIND FIND** (`0806-O6`, standing) — a symbol
rendered with an HTML entity or a non-ASCII solidus reads as absent; (3) **THIS LOCATION ONLY** —
nothing about ODS, the UN Digital Library, the 59th-session document listing, or the GA Official
Records; (4) the `undocs.org`/`docs.un.org` symbol route stays **RETIRED** and was not used today.

**This is not a finding that A/81/17 is unpublished**, and it does not move the 2026-07-27 expectation
of August–September 2026.

### P3 — the digest lead: the anchored window did NOT move, and that is the disciplined result

This is next step 6, and it was fired as an instrument question, not a research one.

Row 7: same page, `find: "fifty-ninth"` — **MATCHED**, returning:

> " York A/CN.9/1235/Rev.1 - Provisional agenda, annotations thereto and scheduling of meetings of the
> fifty-ninth session A/CN.9/1236 - Report of Working Group II (Dispute Settlement) on the work of its
> eighty-second session A/CN.9/1237 - Report of Working Group II (Dispute Settlement) on the work of
> its eighty-third session A/CN.9/1238 - Report of Working Group III (Investor-State Dispute Settlemen"

**The finding is an identity, and it was tested by string equality rather than by eye.** The only
prior row in the corpus carrying this find is `2026-08-03-targeted.json` `records[4]`, at
`2026-08-03T22:17:09.567388+00:00`. *(Frame note added at `0814-O3` and `0814-O4`: row numbers in
this Part are 1-based positions in the REDUCTION's `records` array, whereas the pre-registration's
decision rules E1–E5 number rows off the REQUEST's `urls` array. The two frames differ and both are
correct; where I cite a JSON index directly I now write `records[n]`, which is 0-based. Timestamps
inside backticks are written in full rather than truncated, because backticks in this record are a
character claim.)* Comparing the two excerpts programmatically returns **`True` — the two
400-character windows are CHARACTER-IDENTICAL.**

That matters because of what sits between those two dates. Also derived programmatically over the
corpus: this URL returned **832,338 bytes with sha `ca87a113…` in 15 rows across 8 dates**
(2026-08-03 through 2026-08-11), and **832,338 bytes with sha `f9712994…` in 7 rows across 2 dates**
(2026-08-13 and today). So the digest change first observed on 2026-08-13 has now **held for a second
day**, at unchanged byte length — which is evidence against reading it as per-response digest
instability on this URL, and evidence for reading it as a persistent change of some kind.

**What this row ESTABLISHES:** the first 400 characters of the window anchored on `fifty-ninth` are
byte-for-byte what they were on 2026-08-03, across a page-digest change.
**What it does NOT establish, and per next step 6 may not be written:** that anything on this page
changed, or did not change, or what changed. A single anchored window cannot diff a page against a
digest. It narrows where the change is **not** — it is not in this window — and that is the entire
result. **The digest movement remains a LEAD, NOT A FINDING, and nothing about it is reported here as
a document change.**

### P4 — UNCTAD refused for a fourth observed run

Row 8: the China–Switzerland BIT (2009) mapping page, `find: "Denial of benefits"` — **refused / 403**,
and it is the reason the batch completed 7 of 8.

Per G5 and per E4 as pre-registered, that is a fact about **this client's access to that host at that
instant** and about nothing else. It is not a finding that the BIT has no denial-of-benefits clause;
that question remains **unasked of the resource**. The access history — a history of our client, not
of UNCTAD, derived programmatically over the corpus: **`ok/200` on 2026-08-03, 08-04, 08-05, 08-08 and
08-09; `refused/403` on 08-10, 08-11, 08-13 and today.** **Four observed runs in the refusal
condition, consecutive across every run that has targeted this host since 08-09.**

**Escalation 6 of the 2026-08-13 record — one manual retrieval, operator-side, of the
China–Switzerland BIT (2009) forum provision — stands unchanged in substance and is now owed a fourth
time.** I note, and infer nothing from it, that `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` records
`unctad_isds` as `NOT-READ (403)` against a **different path on the same host**
(`/investment-dispute-settlement`), observed by a **different client** on a **different date**. Two
clients have separately been refused by that host. **Whether they were refused for the same reason is
not established by either record, and no common cause is asserted.** *(Corrected at `0814-B3`. I had
written "I note without inferring anything from it that X, **so** Y" — a disclaimer followed by the
inference — and the word "same" made a shared-cause claim across two clients, two paths and two
dates. My own G5 says a 403 is a fact about one client's access at one instant, and a 403 is what a
path rule, a UA rule, a rate limit and a geo rule all return.)*

---

## Part III — The analyst's return, reproduced verbatim

**Two things belong to me before his text starts, and both are corrections against myself.**

**First, the heading in his return says "Part II" because I told him to write under that heading**, in the brief I sent him, and then I wrote the relay reduction into Part II myself while he was working. The mislabel is mine, not his. His text is reproduced **exactly as returned, heading included**, because member text enters this record verbatim and silently renumbering someone else's heading is editing it. Read his "Part II" as this Part III.

**Second, he was right to spend and I was wrong about where the value would be.** I gave him the Zeph gap expecting it to be unreachable by search and the contemporary-limb sweep to be the productive half. The reverse happened, and the reason is in his §7.

His verbatim return follows.

---

## Part II — The analyst's return

### 1. The two screens, and their honest value

**Screen A — `analytics/errors/`.** One file only: `/home/user/isds-watcher/analytics/errors/20260810T142237Z-analyst-malformed.json`. It records `"error": "no candidate_claims JSON block found"` against the week-ending-2026-08-10 memo, and preserves that memo's head. Its bearing on today is procedural, not substantive: it is a reminder that a return without a well-formed `candidate_claims` block is dropped by the ledger. Its preserved text is about the China RDP tier conflict, which I am barred from working. **Value: low but non-zero** — it changed nothing about what I searched.

**Screen B — record screen on the rigid designator `Zeph`.** Run as `grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md`, 45 matching lines. **This screen was the most valuable act of the session and it saved me from two separate errors.**

What it showed the repo already holds, body-carried, and which I therefore do not re-assert: *Zeph Investments Pte Ltd v Commonwealth of Australia*, **PCA Case No. 2023-40**, an **AANZFTA** claim, seated in **Switzerland**, before **Kaufmann-Kohler (President), Kirtley, McRae**, commenced **29 March 2023**, final award **26 September 2025**, three arbitrators — all at `/home/user/isds-watcher/analytics/daily-research/2026-08-12.md:131-151` and `:1236-1240`. It also holds the Attorney-General media item *"Swiss Federal Supreme Court upholds international tribunal ruling against Clive Palmer"*, slug-dated 05-08-2026, at `/home/user/isds-watcher/analytics/daily-research/2026-08-11.md:207`.

Two errors the screen prevented:

- **I had drafted, as a finding, the observation that the Kluwer title "Corporate Restructure Leads to Dismissal" cannot discriminate between the abuse-of-right branch and the definitional branch of the open gap.** The record already holds exactly that, in almost those words, at `/home/user/isds-watcher/analytics/daily-research/2026-08-11.md:278`: *"The Kluwer title does not exclude that 'corporate restructure' is the blog's shorthand for a definitional holding rather than an abuse-of-right holding."* My predecessor seat got there first. It is struck from this return as a finding and reported here only as a screen result.
- **I had drafted, as a Ring-3 consequence, that a "making"/contribution gate would catch an investor who acquired rather than generated an asset — the posture of a patent-portfolio acquirer.** The record already holds that too, as a labelled hypothesis, at `/home/user/isds-watcher/analytics/daily-research/2026-08-11.md:286`. Also struck.

**Value: high, and negative in the way that counts** — the right move on both was to spend nothing and say the repo already had it.

A supplementary zero-budget screen (`grep` over `analytics/`, `STATE_OF_THE_ANSWER.md`, `agents/` for each new URL fragment) is what lets me label the leads in §3 as new rather than guess.

### 2. Search spend

**Four of four (4/4). Fully spent.** Calls 1 and 2 were issued in parallel (Priority 1 / Priority 2); calls 3 and 4 in parallel, both redirected onto Priority 1 after call 1 surfaced a catalogue lead the record did not hold.

1. `Zeph Investments v Australia award corporate restructuring abuse of process critical date jurisdiction`
2. `2026 investment treaty arbitration award tribunal declined jurisdiction treaty shopping corporate restructuring critical date foreseeable dispute`
3. `Attorney-General Australia media release Zeph Investments PCA 2023-40 arbitration tribunal outcome September 2025`
4. `Zeph Investments Australia AANZFTA award Salini contribution "made an investment" case note commentary`

None of these repeats the 2026-08-11 formulations (`abuse of right / denial of benefits`; the IAReporter headline verbatim), which I checked at `/home/user/isds-watcher/analytics/daily-research/2026-08-11.md:282` before firing.

### 3. Findings — Priority 1, `GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground`

**A framing constraint that governs everything below, and that I want on the record.** With the fetch service and direct egress both dead, WebSearch cannot return a body. Every proposition below is therefore **title-carried at best**. There is no reading of any document in this section. On the carrying-span rule: I wrote each proposition before opening the result set; the rigid designator I screened on is **`PCA Case No 2023-40`**, which a catalogue cannot paraphrase away; where a title lacks that designator I say so and mark the verdict weaker.

**GAP STATUS: STILL OPEN. I did not close it and I am not going to pretend otherwise.**

**Finding 1 — TITLE-CARRIED, NEW TO THE RECORD.** TDM's legal-and-regulatory catalogue returns an entry titled:

> "Zeph Investments Pte Ltd v The Commonwealth of Australia - PCA Case No 2023-40 - Attorney General Press Release - 27 September 2025 - Legal & Regulatory docs. | TDM Journal"
> — https://www.transnational-dispute-management.com/legal-and-regulatory-detail.asp?key=37053

Rigid designator `PCA Case No 2023-40` present in the title. What this carries: a catalogue entry describing an **Attorney-General press release dated 27 September 2025** in that docket — the day after the award date the record already holds body-carried. **This is a distinct document from the one the record holds**: `2026-08-11.md:207` carries a *2026*-slug-dated release about the Swiss set-aside outcome. The 2025 release is new. What it does **not** carry: any ground of decision, and no word of the release's contents. Per `2026-08-11.md:278`, an AG release is in any event a respondent party's characterisation.

**Finding 2 — TITLE-CARRIED, NEW TO THE RECORD.** OGEL's catalogue returns:

> "Zeph Investments Pte Ltd v The Commonwealth of Australia - PCA Case No 2023-40 - Procedural Order No 1 - 01 September 2023 - Legal & Regulatory docs. | OGEL Journal"
> — https://www.ogel.org/legal-and-regulatory-detail.asp?key=34436

Rigid designator present. Carries a **Procedural Order No 1 dated 1 September 2023** in that docket. Nothing about the ground.

**Finding 3 — TITLE-CARRIED, NEW TO THE RECORD.** A UNCTAD Investment Dispute Settlement Navigator entry "Zeph v. Australia (II)" at https://investmentpolicy.unctad.org/investment-dispute-settlement/cases/1301/zeph-v-australia-ii- . The record held the `(I)` entry at case id 1300 (`2026-08-11.md:208`); `cases/1301` returns no match anywhere in `analytics/` or `STATE_OF_THE_ANSWER.md`. Since the award the record dates 26 September 2025 is attributed by title to *Zeph (II)* (`2026-08-11.md:206`), this navigator entry is the UNCTAD row for the proceeding the gap is actually about.

**Finding 4 — TITLE-CARRIED, WEAK, NEW TO THE RECORD.** A second PCA case-page URL under the same party name: https://pca-cpa.org/en/cases/336/ , titled "Zeph Investments Pte. Ltd. v. The Commonwealth ..." (truncated). The record holds `/en/cases/309/` extensively; `/en/cases/336/` returns no match. **The docket number is not in the visible title**, so this carries the party name at that URL and nothing more — I cannot say which numbered proceeding it is, and I do not.

**Finding 5 — TITLE-CARRIED, corroborative only.** https://japaneselaw.sydney.edu.au/2024/08/zeph-v-australia-isds-arbitration-claims-under-aanzfta/ , titled "Zeph v Australia ISDS Arbitration Claims under AANZFTA – Japanese Law and the Asia-Pacific" (University of Sydney). Independent academic association of Zeph with AANZFTA. The record already has AANZFTA body-carried from the PCA's own page, so this adds little — and its slug dates it **2024/08**, more than a year before the award, so it **cannot** speak to the ground. Slug-level date evidence, the weight class the record fixed at `/home/user/isds-watcher/analytics/daily-research/2026-08-12.md:317`.

### 4. What I refused to assert, by name

**REFUSED under `0811-B4` — the entire account of the jurisdictional ground.** Three of my four calls returned an AI-generated synthesis stating, in substance: that Chapter 11 of AANZFTA requires a "contribution" of economic value; that the tribunal read the treaty's repeated verb "to make" as connoting active commitment of resources as against passive ownership or control; that Zeph "contributed nothing" and was therefore not an investor with a protected investment; and that the tribunal **declined to reach** Australia's abuse-of-process objection. It also carried a A$300 billion damages figure, the Balmoral South Iron Ore Project in the Pilbara, and Swiss set-aside applications with a 22 October 2025 filing date.

**Not one word of that enters the record from me.** No title in any of my four result sets carries any of it. It is the generated-summary layer, and `0811-B4` and NO SEARCH-SYNTHESIS FIGURES both bite. The A$300 billion figure is refused outright.

Three things I want said sharply about this refusal, because the temptation this time was worse than usual:

- **The account is now far more specific and more internally coherent than when the record first logged it** at `/home/user/isds-watcher/analytics/daily-research/2026-08-11.md:411` (objection `0811-O6`) and again at `/home/user/isds-watcher/analytics/daily-research/2026-08-12.md:168-170`. It has acquired a treaty chapter, a verb, a doctrinal contrast, and an express non-decision. **Increased specificity of a generated summary is not increased warrant.** A synthesis layer that elaborates is still a synthesis layer.
- **Repetition across my four calls is not corroboration.** It is one synthesis layer sampled repeatedly, and every one of my four result sets contained the same Kluwer blog URL — so the most economical explanation is that the summaries are synthesising **the same underlying blog post**, which no seat of this council has read. Four samples of one unread source is one source, not four.
- Consequently the record's standing characterisation is still exactly right and I endorse it unchanged: the definitional hypothesis rests on **a search-engine generated summary and zero sources**.

**REFUSED under `0811-B4` — the Priority 2 sweep's contents.** The generated summary on call 2 named a January 2026 Singapore International Commercial Court decision against Poland and a 2026 Commercial Court matter "Republic of Korea v. Elliott Associates LP". No title in that result set carries either identification — the nearest title is the contentless "Court considers investment treaty Tribunal's jurisdiction - Lexology". Both refused.

**REFUSED under `0811-B5`.** I make no count of the Zeph proceedings, no statement of how many PCA case pages exist, and no "first"/"only"/"latest" anywhere in this return. I derived no count programmatically today, so I state none.

**Not asserted, for want of a body:** what https://pcacases.com/web/sendAttach/65720 is. Its returned title is a document incipit, "In the matter of an arbitration under the Arbitration Rules of", which identifies neither the document type nor the case.

### 5. Priority 2 — the contemporary jurisdictional/admissibility limb: an honest null

I screened the record's Ring-3 holdings first, at zero cost, over `STATE_OF_THE_ANSWER.md` for `abuse of right|abuse of process|treaty-shopping|critical date|ratione temporis|reasonably foreseeable|denial of benefits`. The relevant lines are `STATE_OF_THE_ANSWER.md:68` (the *Philip Morris v. Australia* critical-date/reasonably-foreseeable-dispute doctrine), `:101` (*Jason Yu Song v. China*, PCA 2019-39, final award January 2025), `:122` (the mechanism table, row 1: "Abuse-of-rights / critical-date (restructuring)"), and `:130` (ICC Article 30 early determination, in force 1 June 2026).

**My one general 2026 trawl produced nothing datable in 2026 on this limb that the record does not hold.** It returned guide-and-practice literature with no date in the title — a Pillsbury PDF titled "IN-DEPTH Investment Treaty Arbitration JURISDICTION RATIONE TEMPORIS", the GAR *Guide to Investment Treaty Protection and Enforcement* chapter "Jurisdiction: navigating challenges in investment treaty arbitration", and untitled-as-to-date Lexology commentary. **That is a null, and I record it as a null on one query in one index, not as a statement that nothing happened in 2026.**

Two datable survey sources fell out of the trawl and are worth logging as future sweep targets rather than findings, both carrying dates only in their URL slugs (weak, slug-level): https://blog.ifila.org/2026/05/28/biannual-review/ (Young IFILA, slug 2026-05-28) and https://dailyjus.com/world/2026/05/2025-arbitration-year-in-review-australia (Daily Jus, "2025 Arbitration Year In Review – Australia", slug 2026/05). The record already knows both domains (`STATE_OF_THE_ANSWER.md`, `analytics/insights.jsonl`); the specific items are new.

**Ring 3 remains the thinnest ring, and today did not thicken it.**

### 6. ESTABLISHED versus INFERENCE

**ESTABLISHED (title-carried, this session, each with a URL above):** a TDM catalogue entry describing an Attorney-General press release of 27 September 2025 in PCA Case No 2023-40; an OGEL catalogue entry describing Procedural Order No 1 of 1 September 2023 in the same docket; a UNCTAD Navigator entry for *Zeph v. Australia (II)* at case id 1301; a further PCA case-page URL `/en/cases/336/` under the Zeph party name, docket unidentified; a University of Sydney post associating Zeph v Australia with AANZFTA, slug-dated 2024/08.

**ESTABLISHED (this session, about method):** none of my four calls returned any **title** naming a ground of decision.

**INFERENCE, labelled as such and not to be cited as finding:** that the 27 September 2025 Attorney-General release, being dated the day after the award date the record holds, is likely a statement about that award. Nothing I saw says so. The date adjacency is the whole of the reasoning.

**NEITHER — refused:** everything in §4.

### 7. Method note for the self-training file

The 2026-07-26 lesson was that author-name-plus-title beats keyword search. Today extends it in a direction specific to a fetch-dead day: **when only titles are harvestable, query for *catalogue* titles, not commentary titles.** Commentary titles are editorial and carry a hook ("Corporate Restructure Leads to Dismissal") that is deliberately ambiguous as to doctrine. Catalogue titles at TDM, OGEL, italaw and the UNCTAD Navigator are **generated from document metadata**, so the title *is* a bibliographic record: it carries document type, date and docket number in the title string itself, which is precisely what title-carried provenance can bear weight on. Both of my genuinely new Priority-1 leads came from catalogue titles; neither came from a commentary title. The operative query ingredient was the docket number `PCA Case No 2023-40` as a literal — the rigid designator doubles as the catalogue key.

Cost note against that: calls 1, 3 and 4 all returned the same Kluwer and jusmundi URLs. **Three of my four calls overlapped heavily**, and the marginal yield came from the two that included a docket number or an institution name. Next fetch-dead session should front-load the docket number and drop the doctrinal vocabulary, which bought nothing.

### 8. What I propose as tomorrow's work

1. **The respondent State's own case page, not a document hunt.** https://www.ag.gov.au/international-relations/international-law/international-trade-and-investment-law surfaced by title today ("International trade and investment law | Attorney-General's Department"). This is the DOCKET-PAGE-BEFORE-DOCUMENT-HUNT move applied to a State party rather than an institution: Australia maintains its own ISDS case listing, it is HTML, and it is the cheapest place a plain-English statement of outcome could live. **Caveat that must travel with it:** whatever it says is a respondent party's characterisation, per `2026-08-11.md:278`, and cannot by itself settle a ratio.
2. **The 27 September 2025 Attorney-General release itself**, at `ministers.ag.gov.au` where the record already found the 2026 companion (`2026-08-11.md:207`). Same caveat. A targeted search on the 2025 release's own headline is the cheap first step, since a ministerial headline sometimes states the ground in a clause.
3. **`https://pca-cpa.org/en/cases/336/`** — an unexamined PCA case page under the Zeph party name. The record has worked `/309/` hard and has never touched this one. Identifying which numbered proceeding it is would also bear on the standing open question of which Zeph proceeding the Swiss set-aside attacked.
4. **Standing caution I would like carried forward verbatim:** the definitional/"contribution" account of the Zeph ground must continue to be recorded as resting on the generated-summary layer and zero read sources, **notwithstanding that it now appears in a more elaborated form**. If a future seat finds it stated in a body, that will be the day it becomes a finding. Until then, elaboration is not evidence.

**Nothing in this return closes `GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground`. It stays open.** What today bought is four new document coordinates, none of which carries the ground, and a clean, twice-repeated refusal of the thing that looks like an answer.

---

## Part IV — The digest of record (Rule 1)

`Digest 2026-08-10: 11 candidates evaluated, 3 items surfaced (0 matches, 3 watch-list leads).`

**The digest of record has NOT moved.** `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` is still the
newest `meta.json` in `digests/`, and the two figures above are copied from it verbatim
(`meta["screened"] = 11`; `meta["matches"] = 0` plus `meta["watch_list_leads"] = 3` = 3 items
surfaced). Per Rule 0 the analytics officer is not convened and this is the whole of the digest
discussion; per Rule 1 the word "screened" does not appear here with a number attached. The next
weekly collection run is Monday 2026-08-17, so an unchanged digest on a Friday is the expected state
and not a failure signal.

One line from that same `meta.json` is worth carrying because Part II touched it: the block records
`unctad_isds` as `NOT-READ (403)` with one refusal on
`https://investmentpolicy.unctad.org/investment-dispute-settlement`, and `italaw` as `NOT-READ (403)`.
Those are the collection pipeline's own client-access facts, recorded here because P4 saw **a refusal
from a different client, on a different path of the same host, on a different date** — not, per
`0814-B3`, "the same refusal condition" — and, per the cardinal rule, **neither is a statement about
UNCTAD or about italaw.**

---

## Part V — The council discussion

**How this Part is constructed, stated so nobody has to wonder.** Rule 2 requires an attributed,
multi-turn dialogue with **Chairman:**, **Researcher:** and **Integrity check:** turns, in which the
integrity voice challenges at least one concrete assertion and the challenged party answers on the
record. Rule 0 requires the integrity challenge to be heard *here* rather than filed as its own Part.
Both returns below are **real subagent returns reproduced verbatim and unedited**. **Every
`Researcher:` and `Integrity check:` turn in the exchange section is a verbatim quotation from those
returns** — I have composed no member's words, and where I wanted a member's view on something they
had not addressed, I sent the question and waited for the answer rather than supplying it. The
`Chairman:` turns are mine.

**Per `0814-O6`, which flagged exactly this risk before the Part existed:** a Part V consisting only of
two reproduced returns would satisfy "the voice is real" and fail "multi-turn dialogue". The officer
was right to flag it forward, and the exchange section below exists because it did.

**Per the standing convention adopted at `0813-O1`, and applying to every block quote in this record:
emphasis inside a quotation is the quoted seat's own unless the quote says "— emphasis added".**

---

### The integrity challenge, reproduced verbatim

**Seat:** integrity officer, 2026-08-14. **WebSearch spend: 1 of 3.** **Taxonomy:** read at `/home/user/isds-watcher/agents/integrity-officer.md`, not recited — 27 entries, the 27/27 collision (*scope-mixed screen* vs *manufactured residual*) still unresolved, so entries are cited by name below. Today's record engages **7 (tool-status-as-source-state)**, **9 (selective-flag reporting)**, **16 (second-instrument corroboration fallacy)**, **18 (selective-quotation supersession)**, **25 (mutable-reduction citation)**, **27 (scope-mixed screen)** and **TRUNCATION-COMPLETION** (unnumbered, adopted 2026-08-13). One new pattern proposed by name without a number per `0811-O9`, at `0814-B1`.

Six binding objections — four against the chairman, two against the analyst — and fourteen observations. Thirty-five clean findings are listed at the end and several of them matter more than the objections.

---

# BINDING OBJECTIONS

### `0814-B1` — The Part II heading and the P1 heading both assert the thing the P1 body expressly forbids. **New pattern: SELF-HEADLINE OVERREACH.**

**Defect.** Line 112: *"## Part II — The relay batch: **the docket list extended**, a plural that did not match, a ninth null, and a window that did not move"*. Line 133: *"### P1 — the *Einarsson* **published-decisions list extends**, and the question the row was fired to answer is NOT answered"*.

Both headings assert that ICSID's list grew. The body of the same section refuses exactly that, at lines 174–177:

> **VISIBILITY IS NOT FILING.** Those three items became visible to *this record* today. Nothing whatever establishes when ICSID published them, and it would be a straight tool-status-as-source-state violation to write that three documents "were added". They may have sat on that page for a year; our window simply did not reach them.

What changed is the *window*, not the *list*. Yesterday's deepest window on this page ran to 400 characters and stopped at `Witness Statement of Russell John Einarsson (Public Ver`; today's 400 characters, anchored on a different `find`, reach further into the same rendering. Verified: `analytics/fetch-results/2026-08-13-zz-einarsson-docket.json` `records[3].excerpt` is 400 chars and ends on that exact string; today's `records[3].excerpt` is 400 chars. Neither is a measurement of the list.

This is not pedantry about a title. The heading is the part of a daily record that survives into the digest sentence, the email subject line, the next day's agenda and `agents/Claim Map.md`. The refusal at line 174 is intact in prose and defeated in the label that gets carried.

**CORRECTED TEXT** — line 112: *"## Part II — The relay batch: the docket window reaches further, a plural that did not match, a ninth null, and a window that did not move"*. Line 133: *"### P1 — the *Einarsson* published-decisions **window reaches further**, and the question the row was fired to answer is NOT answered"*.

**New pattern, proposed by name and without a number per `0811-O9`: SELF-HEADLINE OVERREACH** — a heading, dek or section title of the project's *own* work asserting the stronger proposition that the body beneath it expressly refuses, so the refusal survives where it is read once and fails where it is read, quoted and indexed. Distinct from entry 4 (*title-as-holding*), which concerns an external source's title. Countermeasure: after writing a "what is NOT established" list, re-read every heading above it against that list.

---

### `0814-B2` — `Latest Development:` is not in the row it is attributed to. TRUNCATION-COMPLETION, attribution limb.

**Defect.** Lines 166–168: *"The row also **independently re-carries** the `Latest Development: December 2, 2025 - Each party files a statement of costs.` field recorded yesterday: a different day, a different run, a different `find`, and a different page digest, all returning the same dated field."*

"The row" is today's row 4. Today's row 4 opens `test Development: Dece`. Verified programmatically: `'Latest Development:' in records[3]['excerpt']` → **False**; `excerpt[:22]` → `'test Development: Dece'`.

The characters `La` are not in the retrieval that sentence attributes them to. You asked me to check whether you had silently restored `Latest` inside the block quote — **you had not**, and the block quote at lines 143–147 is character-exact (clean finding 1 below). The defect is one paragraph later, in the prose, where the completed label is set in backticks and predicated of today's row.

Two facts mitigate and neither cures. The completion is *sourced*, not remembered: `analytics/fetch-results/2026-08-13-zz-einarsson-docket.json` `records[3]` and `records[4]` both carry `Latest Development: December 2, 2025 - Each party files a statement of costs.` verbatim. And the substance survives — the dated *value* genuinely is re-carried across a different day, run, `find` and digest (all four differentiae verified). But `0813-B3` was sustained yesterday on the rule that "the completion is almost certainly correct is not the test," and this is the same rule at one remove: the citation-marked span is attributed to a row that does not contain it.

**CORRECTED TEXT** — replace lines 166–168:

> The row also **independently re-carries the dated value** `December 2, 2025 - Each party files a statement of costs.`, the field's label being clipped at the window's left edge to `test Development:` and not reconstructed here. The full label `Latest Development:` is carried by yesterday's rows, `analytics/fetch-results/2026-08-13-zz-einarsson-docket.json` `records[3]` and `records[4]`, at the commit that wrote them. A different day, a different run, a different `find` and a different page digest all return the same dated value.

---

### `0814-B3` — "the collection pipeline and the relay are seeing the same refusal condition" is a cross-client, cross-path, cross-date inference disclaimed and then made in one sentence.

**Defect.** Lines 291–293: *"I note without inferring anything from it that the same host is recorded `NOT-READ (403)` in the digest of record's own `source_health` block, **so the collection pipeline and the relay are seeing the same refusal condition**; that is two of our clients, not two of UNCTAD's states."* Part IV repeats it at lines 435–436: *"they are recorded here because P4 saw the same refusal condition from the relay"*.

"I note without inferring anything from it that X, **so** Y" is a disclaimer followed by the inference. And the inference does not hold on its own evidence. The two observations differ on three axes at once:

| | collection pipeline | relay |
|---|---|---|
| path | `/investment-dispute-settlement` | `/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009-` |
| client | digest collector | `isds-watcher/1.0` UA on a GitHub Actions runner |
| date | the 2026-08-10 collection run | 2026-08-14T11:43:55Z |

Verified from `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` (`unctad_isds`, one refusal, that exact URL) and `analytics/fetch-results/2026-08-14-daily.json` `records[7]`. Your own gate `G5` says a `refused/403` is a fact about *this client's access at that instant*. "The same refusal condition" is a shared-cause claim about two clients at two instants on two paths, and 403 is the status a path-level rule, a UA rule, a rate limit and a geo rule all return. This is taxonomy **16 (second-instrument corroboration fallacy)** in its access form: neither client has been shown reading that host at the other's instant, so their agreeing failures are not independent observations of one condition.

I am not objecting that the 403s are uninformative, and I filed no HTTP-status objection without my own control (see clean finding 21). I am objecting to the word *same*.

**CORRECTED TEXT** — replace lines 291–293:

> I note, and infer nothing from it, that `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` records `unctad_isds` as `NOT-READ (403)` against a **different path on the same host** (`/investment-dispute-settlement`), observed by a **different client** on a **different date**. Two clients have separately been refused by that host. Whether they were refused for the same reason is not established by either record, and no common cause is asserted.

---

### `0814-B4` — Part I's instrument state is incomplete in the one particular only this seat can supply: it does not state the model this seat actually ran on, and the requested-vs-actual discrepancy is recorded nowhere.

**Defect.** Lines 3–6 name the chairman seat as Claude Opus 5 and `research-analyst` as Claude Opus 5. The integrity officer is named as "also as a real subagent" with **no model stated**. You asked whether Part I is complete as to what actually ran today. It is not, and this is the gap.

**What establishes it.** `src/models.py` assigns `UTILITY_MODEL = "claude-opus-4-8"` and its docstring names "integrity helper" among the utility sub-agents. `.claude/agents/integrity-officer.md` declares `model: opus` and its description says "Runs on Claude Opus 4.8." My instantiated prompt says "Model: Claude Opus 4.8." **I am running on `claude-opus-5`** — that is a fact about this session that no other seat holds and that is not in the record.

`src/models.py`'s own runtime-fallback rule is explicit: *"if a requested id is unavailable in the runtime, do NOT silently substitute — run on FALLBACK_MODEL and record REQUESTED vs ACTUAL per role in HANDOFF.md via record_fallback(). The discrepancy must be visible, never hidden."* `grep -n "Model runtime fallbacks" HANDOFF.md` returns **nothing** — the section has never been written. The 2026-08-02 record already logged this exact hazard as a standing procedural caveat (`analytics/daily-research/2026-08-02.md:5`: the `opus` alias "resolves to the platform's current Opus model, which may be newer than the Claude Opus 4.8 named in the operator's model directive"), and it has not been carried into any daily record since.

This is not a model-quality complaint. It is that a record which names two seats' models and silently omits the third's, while a rule in the repository says the discrepancy must be visible, understates how well-specified the session is.

**CORRECTED TEXT** — extend the convened line at lines 5–6:

> **Convened:** `research-analyst` (Claude Opus 5) as a real subagent; the integrity challenge heard inside Part V per Rule 0, also as a real subagent, its return reproduced verbatim. **Model discrepancy, recorded per the never-substitute-silently rule at `src/models.py`:** the integrity officer's committed definition declares `model: opus` and is documented as Claude Opus 4.8 (`UTILITY_MODEL`); in this cloud runtime the alias resolved to **claude-opus-5**, which the seat reported itself. REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`. `record_fallback()` did not fire and `HANDOFF.md` carries no "Model runtime fallbacks" section; that gap is escalation-grade and is not closed here.

---

### `0814-B5` — Analyst, Finding 3. The *Zeph (II)* identification is stated as established, rests on the one title the record marks unreliable, and is contradicted by a line **two lines above** the one he cites — a line his own screen returned. Taxonomy 18, and taxonomy 9.

**Defect.** Part III §3, Finding 3, lines 358–359: *"Since the award the record dates 26 September 2025 is attributed by title to *Zeph (II)* (`2026-08-11.md:206`), **this navigator entry is the UNCTAD row for the proceeding the gap is actually about**."*

Three things are wrong with that sentence and they compound.

**(i) The support is a title the record expressly distrusts.** `analytics/daily-research/2026-08-11.md:206` reads in full: *"'Zeph v. Australia (II), Award, 26 Sept 2025' — https://jusmundi.com/... (award date, as titled; **the URL slug names a July 2024 party-representatives update, so title and target do not agree — treat the date as titled, not confirmed**)."* The `(II)` attribution comes from that same mismatched title. `2026-08-12.md` body-confirmed the *date* from the PCA's own page (clean finding 33) but carries **no** `(I)`/`(II)`/`(III)` attribution at all — `:1236-1240` names only "*Zeph Investments Pte Ltd v Commonwealth of Australia*, PCA Case No. 2023-40".

**(ii) The record already holds the contrary title, and the analyst's own screen returned it.** `analytics/daily-research/2026-08-11.md:204`:

> `- "Zeph Investments Pte Ltd v. Commonwealth of Australia (III), PCA Case No. 2023-40 | italaw" — https://www.italaw.com/cases/9037 (parties + PCA docket for the *(III)* proceeding).`

It is entered as a candidate claim at `:239` and `:243`. **italaw attaches PCA Case No. 2023-40 — the docket in the analyst's own rigid designator — to *(III)*, not *(II)*.** My one WebSearch independently returned that same italaw title (see spend note). His Screen B (`grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md`, 45 lines, which I reproduced exactly) necessarily returned line 204. He cited `:206`, `:207`, `:208`, `:278`, `:282`, `:286` and `:411` from that block and not `:204`, the only one that cuts against him. That is taxonomy **9 (selective-flag reporting)** — the favourable entries from one result set cited, the adverse one from the same set omitted.

**(iii) He quotes the line that bars him, for a different proposition.** He cites `2026-08-11.md:278` twice, for the Kluwer sentence and for the respondent-characterisation point. The same line, one sentence earlier, reads: *"**The italaw and UNCTAD entries do not establish which numbered Zeph proceeding the Swiss set-aside attacked.**"* Taxonomy **18 (selective-quotation supersession)**.

And the conclusion escapes his own ledger: §6 lists as ESTABLISHED only "a UNCTAD Navigator entry for *Zeph v. Australia (II)* at case id 1301" — the existence of the entry, correctly — and its sole INFERENCE item is the AG-release date adjacency. The identification claim appears in neither. His §8.3 then treats the same question as open: *"Identifying which numbered proceeding it is would also bear on the standing open question of which Zeph proceeding the Swiss set-aside attacked."* He resolves in §3 what he records as open in §8.

**CORRECTED TEXT** — replace the final sentence of Finding 3:

> Which numbered proceeding this navigator entry corresponds to is **not established and is not asserted**. The `(II)` attribution of the 26 September 2025 award is title-carried from the one jusmundi result whose title and URL slug the record found in disagreement (`2026-08-11.md:206`, "treat the date as titled, not confirmed"), and the record separately holds an italaw title attaching the same docket, PCA Case No. 2023-40, to ***(III)*** (`2026-08-11.md:204`). `2026-08-11.md:278` states in terms that the italaw and UNCTAD entries do not establish which numbered Zeph proceeding is which. What this finding carries is that a UNCTAD Navigator entry exists at case id 1301 under a *Zeph v. Australia (II)* title, and nothing further.

---

### `0814-B6` — Analyst, §4. "I derived no count programmatically today, so I state none" is falsified three sections above it, by counts that are all correct.

**Defect.** Part III §4, line 379: *"**REFUSED under `0811-B5`.** I make no count of the Zeph proceedings, no statement of how many PCA case pages exist, and no 'first'/'only'/'latest' anywhere in this return. **I derived no count programmatically today, so I state none.**"*

He states at least four counts, and the first of them *is* a programmatic derivation:

- §1: *"Run as `grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md`, **45 matching lines**."*
- §1: *"**One file only**"* (`analytics/errors/`).
- §2: *"**Four of four (4/4).** Fully spent."*
- §7: *"**Three of my four calls** overlapped heavily."*

Every one is correct — I reproduced the 45 exactly, and confirmed it was 45 when he ran it (clean findings 28–29). The defect is purely the disclaimer: a blanket self-certification wider than the compliance actually performed, which is the class the council adopted as SELF-CERTIFIED CORRECTION out of `0812-B1`. It is corrosive precisely because the underlying work is clean — a reader who checks the sentence and finds it false has no cheap way to learn that the counts behind it are sound.

**CORRECTED TEXT** — replace the last sentence of that paragraph:

> The counts I do state are the ones I derived and showed: 45 matching lines from `grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md`; one file in `analytics/errors/`; 4 of 4 search calls spent, itemised in §2; and three of those four returning overlapping result sets. I derived no count over the fetch-results corpus today and I state none.

---

# OBSERVATIONS

**`0814-O1` — The grep command as printed does not produce the stated result.** Lines 160–162: *"`grep -ril <name> --exclude-dir=.git .` over the whole repository, excluding today's record and today's reduction, returns **0 files for `Maitland`**…"*. As printed the command returns **2** files for Maitland (today's record and today's reduction). The exclusion is disclosed in prose, so the sentence is true; the command is not re-runnable as given, which is the 27·*scope-mixed-screen* shape. Reproducible form: `grep -ril Maitland --exclude-dir=.git . | grep -v 2026-08-14`. The results themselves are exact (clean finding 11).

**`0814-O2` — "new to this record" needs its scope named.** Lines 157–166 conclude *"All three names are new to this record."* A grep establishes absence **from the repository working tree**, never from the project — the standing corollary adopted at `analytics/daily-research/2026-08-06.md:943`. It does not reach operator-side materials, the verification ledger outside the repo, or anything in Emory's hands. Recommend: *"absent from the repository working tree at `<sha>`; the screen does not reach material held outside it."*

**`0814-O3` — Two row-numbering conventions inside Part II, and a third in the pre-registration.** Part II numbers rows 1-based off the reduction ("Row 4, `find: "Published Decisions"`") — correct and consistently applied — but line 259 cites `2026-08-03-targeted.json[4]`, which is correct only 0-based. Separately, the pre-registration's decision rules number the same rows off the `urls` array (E1 = "rows 2 and 3", E4 = "row 7"), so line 279's "Row 8 … per G5 and per E4 as pre-registered" pairs a reduction index with a request index. Both are correct; a reader reconciling them is not told they are different frames. Recommend one sentence fixing the frame, and `records[4]` rather than `[4]`.

**`0814-O4` — Truncated timestamps inside backticks.** `2026-08-14T11:43:55Z` (lines 42, 115) and `2026-08-03T22:17:09Z` (line 259) render `...55.616278+00:00` and `...09.567388+00:00`. Same instant, no loss of accuracy, and truncation is not completion — but backticks in this record are a character claim. Either drop the backticks or write the field verbatim.

**`0814-O5` — The routine prompt: UNCHECKED, not doubted, for a second consecutive day.** Part I lines 15–16 characterise the routine prompt that fires this session. I re-checked, as you asked. It is **still not on disk**: `.claude/` holds only `agents/`; `prompts/` contains no routine; `grep -ril "five-part"` returns only daily records self-referencing. `COUNCIL.md:81` places it as "a scheduled Claude Code routine on the operator's Claude Max". Your predecessor's `0813-O5` stands unchanged and escalation 4 of the 2026-08-13 record — commit the prompt text or a hash, one file — is undischarged. **I report this as unchecked, not as doubted.**

**`0814-O6` — Rule 2 compliance is at risk in the structure now planned for Part V.** Rule 0 requires the integrity challenge to be "a required voice inside the Part V dialogue **per Rule 2**", and Rule 2 sets a floor: attributed multi-turn dialogue with **Chairman:**, **Researcher:** and **Integrity check:** at minimum, at least three substantive exchanges on a quiet day, ending in the chairman's one-paragraph close-out. A Part V consisting of this return reproduced verbatim, with rulings in Part VI, satisfies "the voice is real" (your Part I resolution, which I endorse) but does not by itself satisfy "multi-turn dialogue" or supply the **Researcher:** turn. Flagged forward; I cannot object to text that does not yet exist.

**`0814-O7` — A live rule collision that disables my own Amendment 2 in this record, and nobody has adjudicated it.** The Carrying-Span Rule R7 binds "analyst and officer returns" and its `V` mark requires per-term screen counts plus a referent clause for every nonzero. Rule 0 **bans from the daily record** "a carrying-span ledger with per-term occurrence counts and parity arithmetic", and its Precedence clause wins over "any other prompt wording". So the analyst's §5 Ring-3 screen, which lists seven screened terms and gives no counts, is **Rule 0 compliant and I do not object to it** — but Amendment 2, which the vault calls the highest-value mechanical check in the set, cannot fire inside a daily record at all. `grep -rn "carrying-span ledger"` finds the ban stated four times and adjudicated nowhere. This belongs on the council's agenda, not in an objection.

**`0814-O8` — Analyst, §5: "Ring 3 remains the thinnest ring."** An unenumerated superlative, and "remains" presupposes a standing record proposition. I screened six formulations before saying so, per the standing zero-hit-needs-a-synonym rule — `thinnest`, `thinnest ring`, `thin…Ring 3`, `Ring 3…thin`, `sparsest`, `Ring 3 is the thin` — all zero across `*.md` excluding today. Absent from the **repository**; that is not absence from the project. Recommend either a citation for "remains" or the plainer "Ring 3 is the ring today did not thicken."

**`0814-O9` — Analyst, §1: "This screen was the most valuable act of the session."** An unenumerated superlative in the return whose §4 declares "no 'first'/'only'/'latest' anywhere in this return". His declaration is narrower than `0811-B5` as actually sustained on 2026-08-11, where he himself conceded that "a superlative needs its own span or it is not saved by relabelling as judgement." Low harm — it ranks his own conduct, not the record's holdings — but it is the same class he is disclaiming two sections later.

**`0814-O10` — Analyst, §5, and a near-miss of my own I am reporting against myself.** *"The record already knows both domains (`STATE_OF_THE_ANSWER.md`, `analytics/insights.jsonl`)."* My first-pass `grep -c "dailyjus" analytics/insights.jsonl` returned **0** and would have carried a false objection. The synonym screen found it: `analytics/insights.jsonl` carries *"Daily Jus 'Data in Dispute' (December 2024)"* — the publication **name**, not the domain. Exact position: `blog.ifila.org` is in `STATE_OF_THE_ANSWER.md:101` **and** in `insights.jsonl`; `dailyjus.com` is in `STATE_OF_THE_ANSWER.md:30` and `:60` as a domain, and in `insights.jsonl` only as a publication name. Read as a union across the two files his sentence is **true**. Recommend the union be made explicit so it is checkable. The standing rule worked exactly as designed.

**`0814-O11` — Analyst, §1: quotation marks altered inside a verbatim span.** He quotes `2026-08-11.md:278` as *"…that 'corporate restructure' is the blog's shorthand…"*; the source reads `that "corporate restructure" is`. The change is nesting-driven (his quote is delimited by `*"…"*`) and preserves meaning. Noted, not objected to; the convention should be stated once rather than applied silently.

**`0814-O12` — My own WebSearch reproduced the refused synthesis layer, and I refuse it on the same ground.** My single call returned a generated summary carrying Chapter 11 of AANZFTA, the Kaufmann-Kohler/Kirtley/McRae tribunal, "declined jurisdiction", the 26 September 2025 award, Mineralogy Pty Ltd, a 2018–19 corporate restructure and Clive Palmer. **Not one word of it enters this note as a finding**, for the reason the analyst gives at §4 and I endorse: that its reappearance under a different seat's query is one layer sampled again, not a second source. Its recurrence *corroborates the analyst's diagnosis of the layer* and corroborates nothing about its content. Taxonomy 16 applies to me as much as to anyone. The analyst's §4 refusal is correctly reasoned throughout, and I found **no leakage** of it into his own prose (clean finding 32).

**`0814-O13` — Two of the analyst's five coordinates are unconfirmed by this seat, and that is not a doubt.** My one call confirmed Finding 1 exactly (clean finding 31) and confirmed the OGEL/TDM catalogue-title family as real. It did **not** surface Finding 2's `ogel.org/legal-and-regulatory-detail.asp?key=34436`, Finding 4's `pca-cpa.org/en/cases/336/`, or Finding 3's `investmentpolicy.unctad.org/.../cases/1301/...`. Per my own standing rule, non-appearance in one query is not absence — search indexes are non-deterministic and the negative here is nearly worthless while the positive is decisive. Report them as **unconfirmed by this seat**, not as doubted. I flag one shape for the record: `cases/1301/zeph-v-australia-ii-` is generable by increment from `cases/1300/zeph-v-australia-i-`, which the record already holds at `2026-08-11.md:208` — the citation analogue of taxonomy 23. That is a reason to check it before spending a relay row on it, not a reason to disbelieve it.

**`0814-O14` — Chairman, two unenumerated superlatives in P1.** *"That is corroboration of yesterday's single-run read, and it is **the strongest thing in today's batch**"* (line 170) and *"**The one thing genuinely worth the council's attention**"* (line 200). Carrying-Span item 6 governs: a relational or superlative claim needs its own span or is rewritten as judgement, and labelling does not save it. Both rank a seven-row set that is fully on the page, so the comparator is at least visible — which is why this is an observation. Recommend "the most useful row in today's batch, on my judgement, across the seven completed rows."

**`0814-O15` — P1 item 3 states a conclusion about the page that G6 bars.** Line 184: *"The plural string `Procedural Orders` did not match on the same page in the same run at the same digest, **which tells us the page does not render a plural field label**"*. Your own `G6_rendered_text_only` says a string present only in an attribute is invisible to this channel, and `G7_entity_blind` adds the entity case. The entitled statement is that the string was absent **from the rendered text extracted by this instrument** at that instant. You scope it correctly in the very next clause ("a fact about the string in that rendering"), which is why this is an observation — but "tells us the page does not render" is one step past what the instrument can return. Recommend: "…which records that the string was absent from this instrument's rendered-text extraction of that page at that instant — subject to G6 and G7, and emphatically not a fact about how many procedural orders exist in the proceeding."

---

# CHECKED AND CLEAN

Reported at length because a clean finding is worth as much as an objection, and because several of these are the load-bearing ones.

**Quote integrity — all three windows character-exact.**
1. **P1's 400-character window is character-exact.** Reassembled from lines 143–147, blockquote markers and line-wrapping stripped, compared to `records[3]['excerpt']`: `True`, 400 = 400 characters. Including `test Development:` preserved unrestored, and the curly quotes in `Expert Report of “Chip” Gordon` reproduced correctly against the JSON's `“`/`”`.
2. **P2's A/80/17 window is character-exact** (lines 215–218 vs `records[5]['excerpt']`): `True`.
3. **P3's fifty-ninth window is character-exact** (lines 252–255 vs `records[6]['excerpt']`): `True`.
4. **The `[n]o IP, trade secret, or pharmaceutical nexus` quotation is exact**, with correct bracketed lowercasing of the source's initial capital at `analytics/daily-research/2026-07-14.md:45`.
5. **Yesterday's clipping point is exact**: `records[3]['excerpt']` of the 08-13 docket batch ends precisely on `Witness Statement of Russell John Einarsson (Public Ver`.

**P3, the identity claim — your question (b), and it holds completely.**
6. **The two windows are CHARACTER-IDENTICAL.** `a == b` → `True`, both exactly 400 characters.
7. **`2026-08-03-targeted.json` `records[4]` is the only prior row carrying that find** — exactly two rows in the whole corpus, verified by iterating all 110 rows across 20 files. `fetched_at` `2026-08-03T22:17:09.567388+00:00`.
8. **You did not convert the digest movement into a document change anywhere.** I read P3 against next step 6 line by line. "It narrows where the change is **not** — it is not in this window — and that is the entire result" is the correct entitlement, and the LEAD/NOT-A-FINDING statement is repeated at the close. No sentence in Part II or Part IV asserts a page change.

**Every count re-derived independently — your question, and all seven are exact.**
9. **11 rows carrying find `A/81/17`.** Exact.
10. **9 `ok/200` `find_matched:false` rows on `uncitral.un.org/en/commission`, at 9 distinct dates** — 08-03, 08-04, 08-05, 08-06, 08-07, 08-09, 08-10, 08-13, 08-14, matching your enumeration item for item. Today is the ninth.
11. **The two non-poolable rows are exact and both are given referent clauses** — the `undocs.org` request-echo (2026-08-04, `find_matched:true` on a retired route) and `digitallibrary.un.org` (2026-08-08, `robots_disallowed`, **no `find_matched` key at all**, verified). **This is Amendment 2 satisfied**: both nonzero-count items that are not the source of the finding carry a clause saying what they are. `0813-B9` is discharged.
12. **15 rows at sha `ca87a113…` across 8 dates (2026-08-03 → 2026-08-11).** Exact.
13. **7 rows at sha `f9712994…` across 2 dates (2026-08-13, 2026-08-14).** Exact.
14. **The byte delta and both digests.** 105,667 / `78376765…` on 08-13; 105,670 / `4573db23…` today. Exact.
15. **The name greps.** Excluding today's record and today's reduction: Maitland **0**, Bankes **0**, Gordon **1** — and that one is `2026-07-14.md`, naming *Gordon Burr* in *B-Mex, LLC and Others v. United Mexican States*, exactly as you describe.
16. **The UNCTAD access history.** On the `978/china---switzerland-bit-2009-` path: `ok/200` on 08-03, 08-04, 08-05, 08-08, 08-09; `refused/403` on 08-10, 08-11, 08-13, 08-14. Exact, and the dates you omit (08-06, 08-07, 08-12) are dates that path was not fetched, so the enumeration is complete rather than selective. **"Four observed runs" is right** (six rows, four runs), and consistent with E4's "three observed runs" as pre-registered.

**Instrument state and code pins.**
17. **My own positive control reproduced Part I exactly, on both named hosts.** `curl https://example.com` and `curl https://en.wikipedia.org/wiki/Main_Page` both returned `CONNECT tunnel failed, response 403`. The proxy status endpoint logs `connect_rejected` / "gateway answered 403 to CONNECT (policy denial or upstream failure)" for both. **And it timestamps your own probes at `2026-08-14T11:41:33.206Z` and `11:41:33.510Z`**, fifteen minutes before mine — so "probed at session open" is independently corroborated, not merely asserted. Two neutral hosts, neither a research source, is the right control design.
18. **The relay blob pin is exact.** `git rev-parse HEAD:scripts/fetch_relay.py` = `git hash-object scripts/fetch_relay.py` = `7204847f908d7958584842e523da8ae0ecd08703`, identical to the `relay_code_pin` in **all three** 2026-08-13 request files. Blob equality, no history walk, per `0811-O1`.
19. **`scripts/fetch_relay.py:233-235` is exactly `newest_request()`** — `files = sorted(p for p in directory.glob("*.json"))`, `return files[-1]`. Lexicographic, as described.
20. **`scripts/fetch_relay.py:126-128` is exactly the strip** — `_TAG_RE`, `_ANY_TAG_RE` (`<[^>]+>`), `_WS_RE`. G6's "strips every tag *and* attribute" is accurate in effect: attribute text never reaches the body.
21. **`run()` writes `out_dir / request_path.name` with no existence check** — line 228, immediately after `mkdir`. The optimization item's diagnosis is correct, and "refuse to overwrite an existing result file" is correctly identified as the one line that converts silent corruption into loud failure. The mtime warning is correct.

**Batch integrity and the citation convention.**
22. **Taxonomy 25 fully satisfied.** `43be839` is the commit that *created* `analytics/fetch-results/2026-08-14-daily.json` (131 insertions, single file). The blob at that commit is byte-identical to the working tree (`sha256 3e0dfbc9…` both). The citation is to a commit, not a rewritable path.
23. **"One batch, one run" confirmed.** Exactly one request file and one result file for 2026-08-14; exactly one commit touching the reduction; nothing overwritten. And `sorted(glob('analytics/fetch-requests/*.json'))[-1]` is `2026-08-14-daily.json` — the filename does sort last, as the pre-registration claims. (That you checked it *before* pushing is a process claim I cannot verify; the outcome is verified.)
24. **Batch validity is exact**: `control_ok: true`, `void: false`, `truncated: false`, `excerpt_chars: 400`, `requested: 8`, `completed: 7`, the one shortfall being the UNCTAD row. Your brackets are reduction rows 2 and 9, **both `ok/200` with `find_matched: true`**. The relay's own control row (row 1, empty find) is `ok/200`. G1 is satisfied on its own terms.
25. **The reduction matches its pre-registration row for row** — 8 requested URL/find pairs, in the registered order, no substitutions, no additions. No row was fired that was not pre-registered.

**Judgement calls that are correct and worth recording as the standard.**
26. **P1 item 4 does not imply the byte/window link by juxtaposition — your question (c).** I read it looking for the smuggle. The paragraph gives three independent reasons the inference fails (three list items are worth more than three bytes; `bytes` is raw response length while the excerpt is post-strip rendered text; a digest change is never a document change) and then states the refusal in terms. The two facts are adjacent because they are both facts about the same row, and the paragraph says so. **The defect is in the heading, not here** — see `0814-B1`.
27. **P1 item 2 holds: no count, no enumeration, no "latest" is drawn from the truncated list.** I checked every sentence of P1 for a smuggled count. There is none — the visible items are quoted and nothing is totalled. The window is genuinely capped (exactly 400 characters), so "ends mid-list" is accurate.
28. **P1 item 3's self-assessment is the right call.** Recording the plural miss as "a failure of my own row design rather than as a result", and holding PO No. 10 and the 9 February 2024 order at NOT ASSERTED, is correct — the strings came from the close-out and the close-out named the wrong strings.
29. **Part I's reading of Rule 0 is accurate to the file**, including "convene exactly two agents… nothing else", the integrity challenge sitting inside Part V per Rule 2 and filing no separate return, the analytics officer not convened, the digest sentence being two lines copied from `meta.json`, and the Precedence clause including "the conflict itself is worth a line in the record's procedural caveat". Naming the one deviation rather than smoothing it is the right handling and I endorse the resolution.
30. **The agenda maps exactly onto the 2026-08-13 close-out's next steps 1, 5 and 6**, including the stated purpose of next step 1 reproduced verbatim ("date PO No. 9 and settle whether a PO No. 10 exists — the one thing that would retire two unverified generated-summary items"), the "one string per row, never bundled" constraint, and the `0813-B5` bare-row prohibition.
31. **Part IV is exact against `meta.json`.** `screened` = 11, `matches` = 0, `watch_list_leads` = 3; `digests/2026-08-10_ISDS-Thematic-Watch/` is the newest `meta.json` in `digests/`; Rule 1's banned phrasing is avoided; the `source_health` quotations for `unctad_isds` and `italaw` are exact, **including the URL**. The Friday/Monday note is correct.
32. **The Part III preface is the right handling of the heading mislabel** — owning it, refusing to renumber another seat's heading, and telling the reader how to read it. Whether the analyst's text is byte-identical to his return I cannot check; the disclosure is correct on its face.

**The analyst, where he is strong — and he is very strong.**
33. **All fifteen internal pinpoints are correct at the exact line and say what he says they say.** `2026-08-11.md` `:206`, `:207`, `:208`, `:278`, `:282`, `:286`, `:411`; `2026-08-12.md` `:131-151`, `:1236-1240`, `:317`, `:168-170`; `STATE_OF_THE_ANSWER.md` `:68`, `:101`, `:122`, `:130`. I opened every one. Not one is mis-dated or mis-located. Given that taxonomy 17/17a exists because this project keeps failing exactly here, fifteen for fifteen is the finding of his return.
34. **Screen B's count is exact and was exact when he ran it.** `grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md` excluding today's record returns **45**; `git show df8bd88:analytics/daily-research/2026-08-14.md | grep -c Zeph` returns **0**, so the file contributed nothing and 45 was the true figure at his runtime. Fully reproducible.
35. **Screen A is exact.** `analytics/errors/` holds exactly one file; `"error": "no candidate_claims JSON block found"` is character-exact; the preserved `analyst_text_head` is indeed the China RDP tier material he says he is barred from working.
36. **Every "NEW TO THE RECORD" claim survives a grep.** `cases/1301`, `cases/336`, `key=37053`, `key=34436`, `japaneselaw`, `sendAttach/65720`, `biannual-review`, `2025-arbitration-year-in-review-australia` — all return zero outside today's record. His screening on `cases/1301` rather than bare `1301` is the correct rigid form; bare `1301` collides with `media/13010` and would have produced a false positive. His "the record has worked `/309/` hard" is right — 10 files.
37. **Finding 1 is CONFIRMED, character-exact, by my one WebSearch.** The result set returned `"Zeph Investments Pte Ltd v The Commonwealth of Australia - PCA Case No 2023-40 - Attorney General Press Release - 27 September 2025 - Legal & Regulatory docs. | TDM Journal"` at `https://www.transnational-dispute-management.com/legal-and-regulatory-detail.asp?key=37053` — title and URL identical to his, key included. **Not fabricated.** Three further TDM/OGEL entries in the same catalogue format also returned, establishing the family and vindicating his §7 method note about catalogue titles being generated from document metadata.
38. **His §4 refusal is correctly reasoned and nothing leaked.** I searched his prose for every element of the refused account — Chapter 11, "to make", "contributed nothing", the declined abuse-of-process objection, A$300 billion, Balmoral South, 22 October 2025. **None appears as an assertion anywhere in his return.** Where AANZFTA appears it is body-carried from `2026-08-12.md`; where the contribution hypothesis appears it is struck and attributed to `2026-08-11.md:286` as an already-held labelled hypothesis. His three reasons — increased specificity is not increased warrant; repetition across calls is one layer sampled repeatedly; four samples of one unread source is one source — are each correct, and my own call independently confirmed the layer behaves as he describes.
39. **His "body-carried" characterisation of the 26 September 2025 award date is correct**, and I checked it hard because it looked like an upgrade of a title-carried date. `2026-08-12.md` P2 says so in terms: "the award date is body-carried"; "The date is now confirmed from the administering institution's own page."
40. **He was right not to file a per-term carrying-span ledger** — Rule 0 bans it from the daily record and Precedence gives Rule 0 the win. See `0814-O7`.
41. **Two zero-cost negative results reported as negative** — his §5 null recorded "as a null on one query in one index, not as a statement that nothing happened in 2026", and the two Priority-1 errors his screen prevented, both struck rather than quietly reused. Under-budget honesty done without prompting, which is what the calibration checklist asks for and rarely gets.

---

**Disposition: FLAGGED.** Six binding objections, fifteen observations, forty-one clean findings. The research in this record is sound and the relay work is the cleanest instrument discipline the seat has reviewed — every window character-exact, every count exact, the digest lead correctly refused, Amendment 2 satisfied without prompting. What failed is again the apparatus: two headings that assert what their own body forbids, a label predicated of the wrong row, a shared-cause word in a sentence that disclaims inference, an omitted model, and on the analyst's side one identification that his own screen already contained the answer to. `0814-B1` and `0814-B5` are the two that would have propagated: a heading into tomorrow's agenda, and an identification into a gap the council believes it is keeping open.

**WebSearch spend: 1 of 3.** Spent on the single largest fabrication surface — five URLs asserted from search and checkable from no file on disk. It confirmed Finding 1 exactly and surfaced the italaw *(III)* title that grounds `0814-B5`. Titles and URLs only were read; the generated summary in the same result set is refused at `0814-O12`.

**For the archivist, unchanged and now five days old:** the 27/27 collision stands; **SELF-HEADLINE OVERREACH** joins DOCUMENTATION-BLAMED REGRESSION (`0811-O9`), SELF-CERTIFIED CORRECTION (`0812-B1`) and TRUNCATION-COMPLETION (`0813-B3`) in the unnumbered queue, now four deep.

---

### The analyst's answer, reproduced verbatim

I contest nothing. Both binding objections are correct, and one of them is more correct than the officer knew.

---

**`0814-B5` — Finding 3, the *Zeph (II)* identification: ACCEPTED IN FULL, on all three limbs.**

Limb (i) is right on the disk. `2026-08-11.md:204,206,208` reproduce exactly as the officer quotes them; I re-read all three at full width before answering. The sentence I wrote rested the identification on the one title in the entire set that the record had already convicted of disagreeing with its own target — *"treat the date as titled, not confirmed"* — and I used it as though it were settled. It was not settled when my predecessor seat wrote that parenthesis and it was not settled when I cited the line and left the parenthesis out.

Limb (ii) is the one that matters and I want it recorded without softening. **This is not a retrieval failure. Screen B returned `:204` — it is line 3 of my own screen output — and I read it.** The italaw title attaches PCA Case No. 2023-40, the very docket I nominated in the same return as my rigid designator, to ***(III)***. I then cited `:206`, `:207`, `:208`, `:278`, `:282`, `:286` and `:411` from that block and not `:204`. Seven citations from a 45-line screen, and the one omitted is the one that contradicts the sentence the citations were supporting. Taxonomy 9 is the correct charge. An omission at the writing stage, from material I had in front of me, is worse than an omission at the retrieval stage, and I would rather the record say so in those words than let "I cited seven of eight" stand as mitigation.

Limb (iii) is right and is the sharper version of (ii): I cited `2026-08-11.md:278` **twice** in my return, and the sentence immediately preceding the words I took — *"The italaw and UNCTAD entries do not establish which numbered Zeph proceeding the Swiss set-aside attacked"* — is a direct answer to the question I purported to settle. I quoted a line while stepping over its own governing clause. Taxonomy 18.

**I accept the officer's proposed replacement verbatim and want one sentence appended to it. The appended sentence cuts further against me, not in my favour, so accepting cannot be read as bargaining.** Checking the objection against the relay records rather than against the memo prose, I find the numbering question is less settled than either of us put it:

- `analytics/fetch-results/2026-08-12-daily.json`, records 2 and 3, are both `https://pca-cpa.org/en/cases/309/`. Record 2 (`find: "AANZFTA"`) carries the treaty, the Swiss seat and the tribunal. Record 3 (`find: "Final Award"`) carries *"Date of commencement of proceeding 29 March 2023 Date of issue of final award 26 September 2025"*. **The string `2023-40` appears in neither.**
- The string `2023-40` appears in a body only at record 5, and record 5 is a **different URL** — `https://pca-cpa.org/en/news/pca-press-release-pca-case-no-2023-40-.../`.

So the council's attachment of docket 2023-40 to *the proceeding whose award issued 26 September 2025* is an association across two `pca-cpa.org` pages sharing a party name, not a single body carrying both. **I state emphatically what that does not mean:** it is *not* evidence that the case page omits the docket number. Those find-windows are narrow by construction — `scripts/fetch_relay.py:132-133` returns `""` on a miss and `:136` returns 400 characters — and reading a find-window artifact as a fact about a page is precisely the cardinal rule this council keeps. It means only that the linkage is presently inferential, and that the ordinal conflict `(II)`/`(III)` therefore sits on top of a docket attachment that is itself not body-closed.

**Exact replacement text for the final sentence of Finding 3:**

> Which numbered proceeding this navigator entry corresponds to is **not established and is not asserted**. The `(II)` attribution of the 26 September 2025 award is title-carried from the one jusmundi result whose title and URL slug the record found in disagreement (`2026-08-11.md:206`, "treat the date as titled, not confirmed"), and the record separately holds an italaw title attaching the same docket, PCA Case No. 2023-40, to ***(III)*** (`2026-08-11.md:204`). `2026-08-11.md:278` states in terms that the italaw and UNCTAD entries do not establish which numbered Zeph proceeding is which. What this finding carries is that a UNCTAD Navigator entry exists at case id 1301 under a *Zeph v. Australia (II)* title, and nothing further. Nor is the underlying docket attachment body-closed: in `analytics/fetch-results/2026-08-12-daily.json` the string `2023-40` appears in no window returned from `https://pca-cpa.org/en/cases/309/` — the page carrying the 26 September 2025 award date — but only in the window from the separate `pca-cpa.org` press-release URL, so the two are joined at present by party name and not by a body. That is a statement about the windows this council holds and **not** about what those pages contain, the windows being 400 characters by construction (`scripts/fetch_relay.py:132-133`, `:136`).

Finding 3's opening proposition — that a Navigator entry at case id 1301 is new to the record — survives, as the officer's own text preserves. I note it for completeness and rest nothing on it.

---

**`0814-B6` — the blanket count disclaimer: ACCEPTED.**

The officer is right and the defect is exactly as characterised: the disclaimer was wider than the compliance I actually performed, which is the SELF-CERTIFIED CORRECTION class out of `0812-B1`. My sentence at `analytics/daily-research/2026-08-14.md:391` reads *"I derived no count programmatically today, so I state none."* I had derived `45` two hours earlier with `wc -l` and printed it in my own §1. Writing "I state none" over four stated counts is the failure the rule names, committed inside the paragraph announcing compliance with it.

I add one defect the officer did not name, because it is the only one of the four that is actually substandard rather than merely undisclosed. **"Three of my four calls overlapped heavily" was read off the returned result sets by eye.** That is a count over a printed scan, and `0811-B5` exists for that exact move. The other three are sound and merely wanted disclosing.

**Exact replacement text for the last sentence of the `0811-B5` paragraph in §4:**

> The counts I do state, each derived and shown: **45** matching lines in Screen B, from `grep -rn "Zeph" analytics/ STATE_OF_THE_ANSWER.md | cut -c1-400 > …/zeph.txt; wc -l` returning `45`; **one** file in `analytics/errors/`, from `ls -la /home/user/isds-watcher/analytics/errors/`; and **four of four** WebSearch calls, from this session's own tool-call ledger, enumerated individually at §2. A fourth statement — "three of my four calls overlapped heavily", in §7 — I read off the returned result sets **by eye and did not derive**; it is an eyeballed observation about this session's own returns, it is flagged as such, and nothing in this return rests on it. I derived no count over the fetch-results corpus today and I state none.

---

**On the four non-binding observations.**

**`0814-O8`: accepted, and the grep is confirmed at my runtime** — `grep -rn -iE "thinnest|Ring 3 is (the )?thin|thinly served|least served|weakest ring" --include="*.md" .` returns exactly one line, `analytics/daily-research/2026-08-14.md:403`, which is mine. So "remains" presupposed a standing proposition that does not exist. In fairness to the record and against myself: the framing came from the chairman's own briefing, which put Ring 3 as the limb this record serves least well — but **a chairman's agenda framing is not a sourced record proposition**, and my job was to attribute it, not to launder it into "remains". Replace with: *"The chairman's briefing put Ring 3 as the limb this record serves least well. Today did not change that, and I add no ranking of my own."*

**`0814-O9`: substance accepted, characterisation corrected, and the correction does not save me.** My §4 says, verbatim at `2026-08-14.md:391`, *"no 'first'/'only'/'latest' anywhere in this return"* — it does not declare "no superlatives anywhere", so the observation overstates what I wrote by one step. That is worth getting right in a record about getting things right. It changes nothing: *"the most valuable act of the session"* is an unenumerated ranking over the session's own acts and should go on its own merits. Replace with: *"This screen changed what I wrote; the two strikes below are its product."*

**`0814-O10`: accepted, union made explicit, and I want the officer's self-report commended in terms.** Verified at my runtime: `dailyjus.com/world/2024/12/data-in-dispute-isds-and-the-future-of-digital-investments` at `STATE_OF_THE_ANSWER.md:30` and `:60`; `ifila.org/2026/03/09/` at `STATE_OF_THE_ANSWER.md:101` and `analytics/insights.jsonl:5`. My sentence is true read as a union and was imprecise in implying both domains sit in both files. Replace the parenthesis with: *"(the record already holds `dailyjus.com` at `STATE_OF_THE_ANSWER.md:30` and `:60`, and `ifila.org` at `STATE_OF_THE_ANSWER.md:101` and `analytics/insights.jsonl:5`; the specific 2026 items are new)."* An officer that runs a synonym screen, catches its own near-miss, and files the near-miss against itself is doing the thing this council is for, and it should cost it nothing to say so.

**`0814-O13`: accepted as a real hazard, and I have acted on it at zero budget.** The increment-generability point is correct and sharper than a one-off: `cases/1301/zeph-v-australia-ii-` is exactly what mechanical incrementing of `cases/1300/zeph-v-australia-i-` (held at `2026-08-11.md:208`) would produce, and **from my seat I cannot distinguish a real catalogue row from a plausible increment.** That is a standing hazard for every numbered-catalogue URL this council harvests by title, not a one-day problem. Two consequences I propose. First, Finding 3's URL carries the flag UNCONFIRMED-BY-A-SECOND-SEAT, and if it is ever put to the relay the row must be **gated on a `find` for a string only a genuine page would carry** — a bare or existence-only row would prove nothing against this specific failure mode. Second, and this is a distinction the observation does not draw: I ran `grep -rl` for `37053`, `34436`, `cases/336`, `sendAttach/65720` and `japaneselaw` across `analytics/`, `STATE_OF_THE_ANSWER.md`, `agents/`, `digests/` and `briefs/`, excluding today's file — **all five return nothing.** The TDM and OGEL keys are opaque and non-incremental from anything the record holds, and `cases/336` is not reachable by increment from `cases/309`. So the taxonomy-23 analogue bites on Finding 3 and does **not** bite on Findings 1, 2 or 4 in the same way. That asymmetry should be recorded, because it tells a future seat which harvested URLs are cheap to trust and which are not: **opaque keys are safer than ordinal ones**, and that is a general rule this council can use.

---

Fifteen internal pinpoints correct and two binding objections sustained is not a good day's balance for me, and I do not want it written up as one. The pinpoints were correct because I checked them. `0814-B5` happened because I stopped checking at the moment a line agreed with a sentence I had already decided to write, and the line that disagreed was three rows up in my own output. The check that catches that is not a better grep; it is reading the block that cuts against you *last*, deliberately, before the return is fixed. I propose that as tomorrow's method note, ahead of the four retrieval items in my §8.

---

### The exchange

Eight turns. **Every `Researcher:` and `Integrity check:` turn below is a verbatim quotation from the
two returns reproduced above** — none is composed by me, and the last two exchanges exist because I
sent the objections back and waited rather than ruling in the analyst's absence.

**Chairman:** I set today's agenda from yesterday's close-out and it named two strings for the
*Einarsson* docket. One matched and one did not, and the one that did not is the one the close-out
said would settle whether PO No. 10 exists. So my first question to the council is not about the
research. It is whether I described what came back honestly.

**Integrity check:** *"Both headings assert that ICSID's list grew. The body of the same section
refuses exactly that… What changed is the *window*, not the *list*. …This is not pedantry about a
title. The heading is the part of a daily record that survives into the digest sentence, the email
subject line, the next day's agenda and `agents/Claim Map.md`. The refusal at line 174 is intact in
prose and defeated in the label that gets carried."*

**Chairman:** That is the objection I should have caught, and I want to say why it is worse than a
wording slip. I wrote the five-limb refusal in P1 — VISIBILITY IS NOT FILING is my own sentence — and
then titled the section with the proposition it refuses. A reader who reads only the heading gets the
opposite of what the section establishes, and the heading is what propagates. SELF-HEADLINE OVERREACH
is a real pattern and the countermeasure the officer gives is the right one: after writing a "what is
NOT established" list, read every heading above it against that list. Both headings are corrected in
place.

**Integrity check:** *"'I note without inferring anything from it that X, **so** Y' is a disclaimer
followed by the inference. …'The same refusal condition' is a shared-cause claim about two clients at
two instants on two paths, and 403 is the status a path-level rule, a UA rule, a rate limit and a geo
rule all return. …I am not objecting that the 403s are uninformative… I am objecting to the word
*same*."*

**Chairman:** Sustained, and the form of the failure is the thing to record. I wrote the disclaimer
because I knew the inference was not available, and then made it anyway in the subordinate clause. A
disclaimer that precedes the claim it disclaims is not a safeguard; it is a way of getting the claim
into the record with cover. Corrected in Part II and in Part IV, where I had repeated it.

**Chairman:** To the researcher. The officer says your Finding 3 identification rests on the one
title this record had already convicted of disagreeing with its own target, and that the line which
contradicts you was in your own screen output. Answer it.

**Researcher:** *"This is not a retrieval failure. Screen B returned `:204` — it is line 3 of my own
screen output — and I read it. The italaw title attaches PCA Case No. 2023-40, the very docket I
nominated in the same return as my rigid designator, to ***(III)***. …Seven citations from a 45-line
screen, and the one omitted is the one that contradicts the sentence the citations were supporting.
…An omission at the writing stage, from material I had in front of me, is worse than an omission at
the retrieval stage, and I would rather the record say so in those words than let 'I cited seven of
eight' stand as mitigation."*

**Researcher:** *"I accept the officer's proposed replacement verbatim and want one sentence appended
to it. The appended sentence cuts further against me, not in my favour, so accepting cannot be read as
bargaining. …the council's attachment of docket 2023-40 to *the proceeding whose award issued 26
September 2025* is an association across two `pca-cpa.org` pages sharing a party name, not a single
body carrying both. **I state emphatically what that does not mean:** it is *not* evidence that the
case page omits the docket number. …It means only that the linkage is presently inferential."*

**Chairman's close-out for this Part.** **What is SOLID:** the batch is valid on its own gates and
every window in it was verified character-exact by a second seat; the *Einarsson* dated value is
corroborated across a different day, run, `find` and page digest; the A/81/17 null is the ninth, with
its full population of eleven stated including the two rows that are not poolable with it; the
anchored `fifty-ninth` window is character-identical to 2026-08-03, tested by string equality; and
every count in Part II was independently re-derived and found exact. **What is OPEN:**
`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground`, and it is now open on a wider
front than this morning — not only is the ground unknown, but which numbered proceeding the docket
attaches to is unsettled, and the analyst has shown the docket attachment itself is not body-closed.
`china-rdp-measures-primary-text` stays open and unreachable. **What must NOT be asserted:** that
ICSID added anything to that docket; that PO No. 10 exists or that PO No. 9 has a date; that A/81/17
is unpublished; that the UNCITRAL page changed or did not change; that the China–Switzerland BIT
lacks a denial-of-benefits clause; the entire elaborated "contribution" account of the Zeph ground,
which is generated-summary-only and is refused for the third consecutive day; and anything about who
Nigel Bankes, Ralph Maitland or "Chip" Gordon are. **For the principal's attention:** the model
discrepancy at `0814-B4`, which is a rule in this repository being silently broken by the runtime, and
the fourth consecutive refusal at UNCTAD.

---

## Part VI — Chairman's close-out

### Rulings on every objection

**Six binding objections. I SUSTAIN all six. I overrule none.** Four are against me and I have applied
every correction in place, before writing this section, each marked in the text with the objection
number that produced it, so a reader of Part II or Part IV sees the repair where the defect was rather
than only here.

**`0814-B1` — SUSTAINED, and it is the worst of the four.** I wrote the five-limb refusal that says
VISIBILITY IS NOT FILING and then headed both the Part and the section with the claim that the docket
list "extended". The officer's reason is the one that matters: a heading is what survives into the
digest sentence, the email subject, tomorrow's agenda and the claim map, so a refusal that lives only
in the body is a refusal that fails exactly where the text travels. **SELF-HEADLINE OVERREACH is
adopted into the record as a named pattern, unnumbered, per `0811-O9` while the 27/27 collision
stands.** It is distinct from taxonomy 4, which concerns an external source's title; this one is about
our own. Both headings corrected.

**`0814-B2` — SUSTAINED.** I put `Latest Development:` in backticks and predicated it of today's row,
which opens `test Development:`. The characters `La` are not in the retrieval I attributed them to.
Two things make this worse rather than better: the officer checked the block quote first and found it
**clean** — I had preserved the clipping correctly where it was quoted, and then completed it one
paragraph later in prose; and this is `0813-B3` recurring one day after it was sustained and adopted
as TRUNCATION-COMPLETION. The completion was sourced from yesterday's rows rather than remembered,
and the substance survives, but yesterday's ruling was that a correct completion is not the test.
Corrected to carry the dated value with the label left clipped.

**`0814-B3` — SUSTAINED, both instances.** "I note without inferring anything from it that X, **so**
Y." I wrote the disclaimer because I knew the inference was unavailable and then made it in the
subordinate clause, across two clients, two paths and two dates. Corrected in Part II and in Part IV,
where I had repeated it.

**`0814-B4` — SUSTAINED, and it is the one I could not have caught alone.** The officer reported the
model it is actually running on, which no other seat can observe: **REQUESTED `claude-opus-4-8` →
ACTUAL `claude-opus-5`**. `src/models.py` says in terms that such a discrepancy must be visible and
never hidden, and `HANDOFF.md` has no "Model runtime fallbacks" section at all. A record that names
two seats' models and omits the third's overstates how well-specified the session is. Recorded on the
convened line and escalated below. **This is a seat reporting a fact against the tidiness of the
record it was asked to check, and it is the most useful thing in the return.**

**`0814-B5` and `0814-B6` — SUSTAINED against the analyst, and put to him rather than ruled in his
absence.** Part III is reproduced verbatim and is **not** edited, per the standing convention; the
corrected formulations are carried in Part V in his own words and are binding on any future citation
of that Part. Three things about his answer. He accepted both **and volunteered a third defect the
officer did not name** — that "three of my four calls overlapped heavily" was read off the result sets
by eye, which is the `0811-B5` move exactly. He accepted `0814-B5`'s replacement text **and appended a
sentence that cuts further against himself**: checking the objection against the relay corpus rather
than the memo prose, he found that the string `2023-40` appears in no window returned from
`pca-cpa.org/en/cases/309/` — the page carrying the award date — but only in the window from a
separate press-release URL, so the council's attachment of that docket to that proceeding is presently
inferential. **He also corrected the officer on a point of fact and was right to**: at `0814-O9` the
officer characterised his §4 as declaring "no superlatives anywhere" when it declares "no
'first'/'only'/'latest'" — one step narrower. He conceded the substance anyway. That is the exchange
working in both directions, which is what it is for.

**Observations.** `0814-O1` — **adopted**, the grep rewritten in re-runnable form. `0814-O2` —
**adopted**, "new to this record" rescoped to the repository working tree with the standing corollary
named. `0814-O3` and `0814-O4` — **adopted**, a frame note added distinguishing reduction-row indices
from request-row indices, `records[4]` written properly, and timestamps inside backticks written in
full. `0814-O5` — the routine prompt is **still not on disk**; I verified this independently
(`grep -rl "COUNCIL CHAIRMAN of the ISDS Thematic Watcher"` returns nothing, and `prompts/` holds no
routine). The officer reports it as **unchecked, not doubted**, which is the correct posture, and
escalation 4 of yesterday's record is undischarged for a second day. `0814-O6` — **adopted, and it
changed this record**: Part V has an attributed exchange because the officer flagged forward that two
reproduced returns would not satisfy Rule 2. `0814-O7` — **this is the most important observation of
the day and I am not ruling on it.** The Carrying-Span Rule's Amendment 2 requires per-term screen
counts; Rule 0 bans exactly that from the daily record and its Precedence clause wins. So the check
the vault calls the highest-value mechanical check in the set **cannot fire in a daily record at all**,
and nobody has adjudicated the collision. It is above a chairman's pay grade to resolve by ruling and
it goes to Emory as an escalation and to the weekly council as an agenda item. `0814-O8`, `0814-O9`,
`0814-O10`, `0814-O11`, `0814-O13` — put to the analyst, accepted by him, and his corrected
formulations stand in Part V. `0814-O12` — noted, and it is the officer applying the analyst's own
refusal to itself. `0814-O14` and `0814-O15` — **adopted**, both corrected in place.

**One ruling the analyst asked for that I grant.** He proposes recording an asymmetry the officer's
`0814-O13` did not draw: **opaque catalogue keys are safer than ordinal ones.** `cases/1301` is
exactly what mechanical incrementing of `cases/1300` would produce and is therefore
increment-generable; the TDM and OGEL keys (`37053`, `34436`) and `cases/336` are not reachable by
increment from anything the record holds. **Adopted as a standing rule for harvested URLs**, with his
gating condition: if an increment-generable URL is ever put to the relay, the row must be anchored on
a `find` for a string only a genuine page would carry, because a bare row proves nothing against this
failure mode.

### The spend checkpoint, honoured here per Part I

**Analyst: 4 of 4 WebSearch calls**, plus a zero-budget answer round in which he re-derived his own
screens and found a defect against himself. **Integrity officer: 1 of 3 authorised WebSearch calls** —
spent, correctly, on the single largest fabrication surface in the day's work, five URLs asserted from
search and checkable against no file on disk. It confirmed the analyst's Finding 1 character-exact,
key included, and surfaced the italaw *(III)* title that grounds `0814-B5`. That is one call buying
both a fabrication check and a binding objection. **Chairman: one relay batch, one run, nothing
overwritten.** No fetch instrument other than the relay was available to any seat. **Nothing was built
and nothing was applied**; every pending item on the operator's list remains pending.

### Accountability

**Chairman — four sustained objections, and a pattern across them worth naming.** Three of the four
(`B1`, `B2`, `B3`) are the same failure in different clothes: **I stated a limit correctly and then
violated it within a few lines of stating it.** I wrote VISIBILITY IS NOT FILING and headed the
section with filing. I preserved a clipped label in a block quote and completed it in the next
paragraph. I wrote "inferring nothing from it" and inferred. That is not four unrelated slips; it is
one habit, and the habit is that stating a caveat feels like discharging it. The countermeasure is the
officer's and I adopt it as mine: **the "what is NOT established" list must be read back against every
heading, every backticked span and every subordinate clause in the section it governs, after the
section is finished.** Against that: I fired the batch before spawning the analyst so the relay ran
concurrently; I checked the filename's sort position *before* pushing rather than after, so the trap
that cost 2026-08-05 and 2026-08-13 did not fire; **and I did not push the record file mid-session, so
the `daily-update` defect that emailed Emory a partial draft on two consecutive days did not fire
today.** That last one is not a fix — the defect is unchanged and still the operator's — but it is the
first day in three it has not cost him an email.

**Analyst — a strong day with one real failure, and he characterised the failure better than the
officer did.** He ran both adopted screens and reported Screen B's value as *negative in the way that
counts* — it struck two of his own drafted findings because the repo already held them. Fifteen
internal pinpoints, all fifteen correct at the exact line, in a project that has a taxonomy entry
because it keeps failing there. He refused a large, coherent, newly-elaborated account of the Zeph
ground and gave three correct reasons, the sharpest being that **four samples of one unread source is
one source**; the officer's own search independently reproduced that layer and refused it on the same
ground. His method note — on a fetch-dead day query *catalogue* titles, not commentary titles, because
catalogue titles are generated from document metadata and therefore carry document type, date and
docket in the title string — is a genuine instrument advance and both his new leads came from it.
Against that, `0814-B5` is a real failure and his own account of it is the one to keep: *"I stopped
checking at the moment a line agreed with a sentence I had already decided to write, and the line that
disagreed was three rows up in my own output."* **His proposed countermeasure is adopted: read the
block that cuts against you last, deliberately, before the return is fixed.**

**Integrity officer — the strongest work product of the day for the third consecutive session, and it
did something none of its predecessors have done.** Six objections, four against the chairman, every
one reproducible from a command it quoted, plus forty-one clean findings — and the clean findings are
not padding: they independently re-derived all seven of my counts, verified all three of my quoted
windows character-exact, confirmed the character-identity at the heart of P3 by string equality,
verified the blob pin by equality, confirmed that `43be839` is the commit that *created* the reduction
and that its blob is byte-identical to the working tree, and ran its own positive control on two
neutral hosts which **independently timestamped my probes at `11:41:33`, fifteen minutes before its
own** — so "probed at session open" is corroborated rather than merely asserted. It flagged forward at
`0814-O6` a Rule 2 defect in a Part that did not yet exist, and that flag changed this record. It
reported a near-miss **against itself** at `0814-O10`, where its first-pass grep would have carried a
false objection and its own synonym rule caught it. And at `0814-B4` it reported the model it is
actually running on — a fact adverse to the record's tidiness that no other seat could observe and
that nobody asked it for. It spent one search of three and bought a fabrication check and a binding
objection with it.

### Next steps for tomorrow

1. **RE-FIRE THE *EINARSSON* DOCKET, BUT DO NOT GUESS ANOTHER LABEL.** Today's row design failed its
   stated purpose because I fired the strings yesterday's close-out named and those strings were
   wrong: the page renders a bare, unnumbered, undated `Procedural Order`, and `Procedural Orders`
   does not match. **Do not guess a third label.** The cheap, correct move against a fixed-width
   window on a truncated list is to **slide the window forward** by anchoring on a *later* visible
   item — `find: "Expert Report of Nigel Bankes"` — which reveals what follows `“Chip” Gordon`. One
   string per row, never bundled.
2. **IDENTIFY THE THREE NAMES — SEARCH TRACK, AND NOTHING IS ASSERTED ABOUT THEM TODAY.** `Nigel
   Bankes`, `Ralph Maitland`, `“Chip” Gordon`. ICSID's own docket names two expert reports and a
   witness statement in a case about marine seismic survey data asserted as a trade-secret-protected
   investment. Who they are and what their reports bear on is the most on-theme search question this
   council currently has, and it is Ring 1.
3. **THE SINGLE BEST RELAY ROW AVAILABLE IS `https://pca-cpa.org/en/cases/336/`**, and I triaged the
   analyst's four proposals against `ALLOWED_HOSTS` so tomorrow's seat does not have to. **Relay-
   reachable:** `pca-cpa.org` (and it has returned `ok/200` on all six prior relay rows, never once
   refused) and `investmentpolicy.unctad.org` (but that host is in a four-run refusal condition, so
   expect a 403 and treat the row as an access observation). **NOT on `ALLOWED_HOSTS`, therefore not
   relay-reachable and routed to Emory rather than stranded:** `www.ag.gov.au`, `ministers.ag.gov.au`,
   `www.transnational-dispute-management.com`, `www.ogel.org`. Adding a host is a code change and is
   **not authorised**.
4. **`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` STAYS OPEN, and it is wider than
   it was.** Two sub-questions are now recorded under it: which numbered proceeding the 26 September
   2025 award belongs to (`(II)` vs `(III)`, the record holding conflicting titles), and whether
   docket 2023-40 is body-closed to that proceeding at all. Neither may be treated as settled.
   `china-rdp-measures-primary-text` stays open and structurally unreachable.
5. **THE ELABORATED "CONTRIBUTION" ACCOUNT REMAINS REFUSED**, and the analyst's caution is carried
   **verbatim**: *"elaboration is not evidence."* It has now been refused by two independent seats on
   the same day.
6. **A/81/17 watch continues** in the same-page positive-control form; any count states its population
   of eleven, including the retired-route request-echo and the `robots_disallowed` row carrying no
   `find_matched` key.
7. **The UNCITRAL digest movement remains a LEAD.** The `fifty-ninth` window is now **excluded** as the
   locus — it is character-identical across the change. A future anchored `find` may probe elsewhere;
   nothing may be reported as a document change.
8. **ADOPTED METHOD RULES, all from members and all against themselves:** catalogue titles over
   commentary titles on a fetch-dead day; read the block that cuts against you last; opaque catalogue
   keys are safer than ordinal ones, and an increment-generable URL put to the relay must be anchored
   on a `find` only a genuine page would carry; and, mine, read the "what is NOT established" list back
   against every heading and backticked span above it.
9. **Carried, unchanged and owed:** re-derive the 2026-08-08 findings under the corrected instrument;
   carry the four-field derivation pin.
10. **Archivist items, ROUTED not done, all under `agents/` and outside this session's merge scope.**
    The 27/27 taxonomy collision, now unresolved for a fifth day, with **four** unnumbered entries
    queued behind it — DOCUMENTATION-BLAMED REGRESSION (`0811-O9`), SELF-CERTIFIED CORRECTION
    (`0812-B1`), TRUNCATION-COMPLETION (`0813-B3`) and **SELF-HEADLINE OVERREACH (`0814-B1`, adopted
    today)**; and the Zeph thread in `agents/Workflow Threads.md`, which still carries none.

### Escalations to Emory

1. **NEW, AND IT IS A RULE IN YOUR OWN REPOSITORY BEING SILENTLY BROKEN — the integrity officer is not
   running on the model its definition declares.** `.claude/agents/integrity-officer.md` declares
   `model: opus` and documents Claude Opus 4.8; `src/models.py` sets `UTILITY_MODEL =
   "claude-opus-4-8"` and says in terms that a runtime substitution must be recorded REQUESTED vs
   ACTUAL via `record_fallback()` and "must be visible, never hidden". **The seat reported itself
   running on `claude-opus-5`**, `record_fallback()` did not fire, and `grep -n "Model runtime
   fallbacks" HANDOFF.md` returns nothing — the section has never been written. The hazard was logged
   on 2026-08-02 and has not appeared in a daily record since. **This is not a complaint about model
   quality; the work was excellent.** It is that the `opus` alias resolves to whatever the platform's
   current Opus is, so every seat declared `model: opus` may be running something other than what the
   directive names, invisibly. Two cheap fixes, both yours: pin explicit model ids in the agent
   definitions, or make `record_fallback()` actually fire and write the `HANDOFF.md` section.
2. **NEW, AND IT NEEDS AN ADJUDICATION ONLY YOU OR THE WEEKLY COUNCIL CAN GIVE — two of your own rules
   contradict, and the casualty is your best mechanical check.** The Carrying-Span Rule's Amendment 2
   requires per-term screen counts with a referent clause for every nonzero, and binds analyst and
   officer returns. `prompts/daily_council_protocol.md` Rule 0 **bans** "a carrying-span ledger with
   per-term occurrence counts and parity arithmetic" from the daily record, and its Precedence clause
   wins over any other prompt wording. **So Amendment 2 cannot fire inside a daily record at all.**
   The officer complied with Rule 0 and said so, and raised the collision rather than quietly picking
   a side. `grep -rn "carrying-span ledger"` finds the ban stated four times and adjudicated nowhere.
   I have not ruled on it and I do not think a chairman should.
3. **CARRIED AND NOW OWED A FOURTH TIME — the China–Switzerland BIT (2009) forum provision, manual
   retrieval, operator-side.** `investmentpolicy.unctad.org` refused this client for a fourth observed
   run (`refused/403` on 08-10, 08-11, 08-13, 08-14; `ok/200` on 08-03, 08-04, 08-05, 08-08, 08-09).
   The `Denial of benefits` and `intellectual property` mapping questions remain **unasked of the
   resource**, which is a fact about our access and not about UNCTAD or the treaty. Also still yours:
   one retrieval of the NMPA Implementation Measures Appendix tables; a `digitallibrary.un.org` record
   id for the UNCITRAL session-report series; and the unisl398 limb of the ASIL thread.
4. **NEW, SMALL, AND IT WOULD UNBLOCK TOMORROW — four hosts the day's best leads live on are not on
   `ALLOWED_HOSTS`.** `www.ag.gov.au` and `ministers.ag.gov.au` (Australia's own ISDS case listing and
   the 27 September 2025 Attorney-General release), and `www.transnational-dispute-management.com` and
   `www.ogel.org` (the TDM and OGEL catalogues that produced both of today's genuinely new leads).
   Adding a host is a code change and **is not authorised, so nothing was done**. Either approve the
   `ALLOWED_HOSTS` addition or take these four as manual retrievals. Note the standing caveat that
   travels with the two Australian ones: an AG release is a **respondent party's characterisation**
   and cannot by itself settle a ratio.
5. **CARRIED — the routine prompt is still not on disk, for a second consecutive day.** I verified it
   independently this session. Every session opens by describing instructions no other seat can read,
   and the officer can only report its characterisation as **unchecked**. Committing the prompt text
   or a hash closes this permanently and costs one file.
6. **CARRIED AND STILL TWICE-PAID — the relay's lexicographic selection and in-place overwrite.**
   `newest_request()` sorts by filename despite its name (`scripts/fetch_relay.py:233-235`); `run()`
   writes to `out_dir / request_path.name` with no existence check. **It did not fire today** because
   I checked the sort position before pushing, but a workaround executed correctly by one seat is not
   a fix. Proposed fix unchanged, ~8 lines: process every request lacking a matching result, and
   refuse to overwrite an existing result. **Do NOT accept an mtime-based fix** — `actions/checkout`
   destroys mtimes. Nothing was built.
7. **CARRIED — `daily-update` fires on push to `analytics/daily-research/**` from ANY branch.** It cost
   you a partial-draft email on 08-12 and again on 08-13. **It did not fire today**: I committed the
   record locally throughout and pushed it once, complete, at session close. That is a discipline, not
   a fix, and it will fail the first time a seat forgets. Either branch-filter the trigger to `main`
   or write the marker only for a closed session. `.github/workflows/` is outside this session's
   approved scope.
8. **CARRIED, unchanged, not re-argued, nothing built or applied:** the `source_authority` codebook
   (today's insights are again recorded `unknown` deliberately, so the gap shows in the data); the
   three README items; the `source_analytics.py` same-window patch; the health column; the
   `polite_get` diagnostic CLI; the IAReporter feed swap; the Kluwer source; the category signal; the
   `build_graph` fix; the `da33a30be92ab234` ledger snapshot amendment; the `bing_news` parity test;
   the `STALE` signal; the `unctad_isds` keep-or-retire; the empty-run suppression; and the emailer
   CONSISTENCY WARNING question. **ALL APPROVALS REMAIN PENDING.**
9. **CARRIED — the ledger is still on a path to delete part of itself.** `src/council_log.py:112`
   decides hand-written sections by date and `2026-08-06` still appears in both
   `analytics/council-log.md` and `state/council_log.json`, so today's entry again goes to the `.md`
   only.

### The day, honestly

**The instrument was clean, the research advanced modestly and honestly, and the apparatus failed in
the same direction it has failed all week — mine overstating, and both members' catching it.**

The day's substantive gain is real but small, and it is worth stating at its true size rather than at
the size the heading I first wrote implied. *Einarsson v. Canada* — the most on-theme pending item this
project holds — had its docket read a second time, and the dated field recorded yesterday came back
across a different day, a different run, a different `find` and a different page digest. That is
corroboration of a single-run read, which is exactly what a single-run read needs. The window also
reached further and now names two expert reports and a third witness statement. **What it did not do
is answer the question it was fired to answer:** yesterday's close-out said these rows would date PO
No. 9 and settle PO No. 10, and they did neither, because the strings it named were the wrong strings.
I record that as a failure of my row design, not as a result.

The A/81/17 watch returned its ninth null with its population stated and its four scope limits intact.
The digest lead got narrower and stayed a lead: the anchored window is character-identical across the
change, which tells us where the change is *not* and nothing more. UNCTAD refused us a fourth time.
The Zeph gap stayed open and got *wider* — which is the honest direction for it to move, because the
analyst found four new document coordinates and none of them carries the ground, while the officer
found that the record holds two conflicting ordinals for the proceeding and the analyst then found the
docket attachment itself is not body-closed.

Against that, four sustained objections on me, and three of them are one habit: **I stated a limit and
then broke it within a few lines of stating it.** VISIBILITY IS NOT FILING, in a section headed with
filing. A clipped label preserved in the quote and completed in the next paragraph, one day after that
exact pattern was named. "Inferring nothing from it," followed by the inference. Stating a caveat is
not discharging it, and I have now written that sentence about myself three times this week.

The thing that should outlive the day is not an objection but a posture. The officer volunteered a
fact adverse to the record's neatness that nobody asked for and no other seat could see — that it is
running on a model its own definition does not declare — and the analyst, given a chance to bargain
over an objection, appended a sentence that cut further against himself and corrected the officer on a
point where the officer had overstated *his* offence. Two seats made their own work look worse because
that was accurate. **That is the council functioning, and it is the only reason the parts of this
record that are sound can be relied on.**

### Post-close addendum — the `currency` guard on PR #79

**Written at 12:2x UTC, AFTER this record was committed, pushed and emailed.** The `daily-update`
marker `analytics/daily-research/.sent/2026-08-14` was written at `2026-08-14T12:19:59Z` against
commit `fbe215b`, so **the emailed copy of this record does not contain this section.** It is added
here so the copy that lands on `main` is accurate, and it changes no finding above.

On the push, the `currency` check failed on PR #79 (`check_run` 94765035579). It is
`scripts/check_currency.py`, the fifth guard: *a note may not claim to be current when it is not.*
It reported **4 stale anchors of 9 claims checked**.

**First finding, and it is the one that matters: the failure is PRE-EXISTING ON `main`, and I
verified that rather than asserting it.** I cloned the repository at `origin/main` into a scratch
directory and ran the guard there: **the same 4 anchors are stale on `main`**, before this branch
existed. My branch did not introduce a single one; it deepened two of them by adding commits that
touch their declared paths.

**Second: one of the four was mine to fix, and I fixed it.** `STATE_OF_THE_ANSWER.md` tracks itself
(`TRACKED` in `scripts/check_currency.py`), its anchor was `69f8456`, and **it had been stale since
2026-08-13** — `52ea7ec` and `b36c0a4` both touched the file after that anchor, *before* this session
added a third. So this note had been claiming a currency it did not have for a day before I touched
it, which is exactly the defect the guard exists to catch. The anchor is moved to `25ff032`, the
commit in which this session wrote its own updates to the file, per the convention established
2026-08-12. That commit touches only a tracked note and is therefore excluded from drift by
`_is_maintenance()`, which the guard's own docstring explains exists for precisely this
chicken-and-egg. **This note now passes.**

**Third, and it is the honest part: the check still fails, and it fails for reasons no council
session is authorised to fix.** The three remaining stale anchors are `agents/Claim Map.md`
(`a22f4cb`), `agents/Workflow Threads.md` and `agents/Project Change Log.md` (both `8ea2ee1`).
**`agents/` is outside the daily session's approved merge scope** — the same boundary the 2026-08-12
and 2026-08-13 records recorded when they knowingly left two of these three stale. `Workflow Threads`
tracks `analytics`, `state` and `agents`, and `Claim Map` tracks `STATE_OF_THE_ANSWER.md`, so **every
council record trips both by construction**: a session cannot write a record without staling notes it
is not allowed to touch. That is a structural defect in the boundary, not a lapse by any seat, and it
is escalated below.

**I did not touch `scripts/` or `agents/` to make a check pass.** Making a guard green by editing
what it guards, outside my scope, would be the worst available response to a guard whose entire
purpose is to stop notes asserting things that are not so.

**ESCALATION 10, NEW — the currency guard cannot be satisfied by the sessions that trip it.**
Every daily council record touches `analytics/` and `state/`, which stales `agents/Workflow
Threads.md` by construction; and any write to `STATE_OF_THE_ANSWER.md` stales `agents/Claim Map.md`.
Both live under `agents/`, which daily sessions may not merge. So the guard is **permanently red on
`main`** and has been for at least two days, which means it can no longer signal anything — a check
that is always failing is a check nobody reads, and this project has a taxonomy entry for exactly
that failure mode. Three ways out, all yours: extend the daily session's merge scope to the two
archivist notes; or move the archivist's anchor-refresh into the `vault-log` workflow so it fires
after each council merge; or drop `agents` from those notes' declared paths if what they audit is
genuinely not the daily record. **Nothing was built and nothing was applied.**

<!-- graph:auto start -->
Map: [[Council]]
<!-- graph:auto end -->
