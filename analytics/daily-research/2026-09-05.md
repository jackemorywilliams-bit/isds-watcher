# ISDS Thematic Watcher — daily council meeting, 2026-09-05

**Convened:** the scheduled cloud session, chairman seat running on Claude Opus 5 per the operator's model
directive of 2026-08-03. **Seats convened:** research analyst; integrity officer (as the Part V challenge
voice — see the procedural caveat at §7). **Digest of record:** 2026-08-31.

---

## Part I — Agenda, instrument posture, and what today is for

**Everything in this Part I was written and committed before any seat was convened. It certifies nothing
about Parts II onward and it is not retouched afterwards.**

### 1. The instruments, probed at session open

Three probes. Each is stated as a fact about the FETCH LAYER and never as a fact about a resource — the
standing tool-status-as-source-state rule:

- **Direct egress is DEAD.** `curl https://example.com` returned `curl: (56) CONNECT tunnel failed,
  response 403`. A second neutral host, `https://example.org`, returned the identical error. That is the
  proxy gateway refusing the tunnel; it is not a statement about either site.
- **The fetch service is DEAD.** `WebFetch` on the neutral control `https://example.com` returned
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`. The service, not the site.
- **The search track is ALIVE.** `WebSearch` returned results at session open.

**Fetch capability is UNCHANGED from the constraint established 2026-08-02/08-03. Today is NOT a
fetch-alive state change**, and I say so plainly rather than dressing the relay up as one.

**The committed fetch-relay is the retrieval channel.** `.github/workflows/fetch-relay.yml` fires on a push
touching `analytics/fetch-requests/**`. It is adopted machinery and it works, so **no relay-reachable item
is stranded today** and the standing instruction to route fetch-dependent items to operator-side sessions
does not apply where the relay reaches. It reaches only `ALLOWED_HOSTS` in `scripts/fetch_relay.py:48-69`;
outside that set an item is stranded for host permission, which is the operator's call and not mine.

### 2. A find I made before convening anyone, and the screen that licenses me to call it new

Next step 2 from yesterday — *"fire ONE ITN row with a same-host positive control"* — was filed as "the
cheapest open question in the record." **It is cheaper than yesterday's chair knew, because this project
already holds the answer's shape in its own source tree and no seat has ever connected it.**

`src/sources/iisd_itn.py:3` carries this comment, verbatim from the file at HEAD:

> `Feed: https://www.iisd.org/itn/feed/  (use this, NOT /itn/en/feed/ which 403s)`

**That is a committed record of PATH-SPECIFIC 403 behaviour at `www.iisd.org`** — one path on that host
answers this project and a sibling path refuses it. Yesterday's relay 403 was on
`https://www.iisd.org/itn/en/2014/11/18/…`, which sits under **the same `/itn/en/` prefix the comment names
as the refusing one.**

**Entry 28 binds me, so the screen is in the sentence.** Whitespace-normalised, case-insensitive,
`grep -rI -F` over `analytics/`, `agents/`, `STATE_OF_THE_ANSWER.md` and `docs/` at commit `42b5689`:
literal `itn/feed` → **0 files**; `itn/en/feed` → **0 files**; `NOT /itn/en/feed` → **0 files**. The only
carriers anywhere in the tree are `src/sources/iisd_itn.py:3` and `:22`. Known-positive check on the same
instrument and scope: `same-host` → 16 files, `path-specific` → 8 files, so the sweep finds what is there.
**The claim I am entitled to make is exactly this: the asymmetry is committed in `src/`, and it has never
been carried into the record or connected to the relay's refusal.** I am NOT entitled to claim the
asymmetry itself is new — it is the project's own long-standing knowledge — and I do not.

**What it is worth today:** it converts yesterday's uninterpretable 403 from a guess into a *replicable
pre-registered test*, because it names a same-host path that is predicted to SUCCEED and a same-host path
that is predicted to FAIL. Rows 4, 5 and 6 below fire all three.

### 3. What today is for

Yesterday's next steps, in order of weight:

1. **Discriminate the `www.iisd.org` 403: path-specific vs host-wide vs UA-gated** (next step 2), using the
   three-point same-host design §2 makes possible. This is the day's highest-value fetch item.
2. **Regrade the standing anchor.** UNCT/20/6 (*Einarsson v Canada*), both windows pre-registered against
   yesterday's graded baseline, plus one differently-anchored third window (next step 3).
3. **Verify the five unverified `Status of Proceeding` rows** — ARB/22/2, ARB/22/30, ARB/24/18, ARB/24/26,
   ARB/24/46 (next step 4). The class attribute "pending" is inherited from the record for five of the
   seven and costs five rows to establish.
4. **The analyst's search-track work**, bounded, on the contemporary question.

**Carried prohibitions, restated as prohibitions.** The ARB/12/1 character-geometry programme stays closed
(2026-09-03 ruling; no continuity row is spent on it today). The ARB/23/3 `No References Available.`
resolution is not revived — the ICSID register carries a *Decision on Preliminary Objections* of
21 December 2023 and any future attempt starts there (next step 5). Both literature gaps stay open,
stranded, and **nothing may be asserted from either title** (next step 7). The Upreti/ARIA slug is not
fired: `aria.law.columbia.edu` and `papers.ssrn.com` are outside `ALLOWED_HOSTS`, and widening that
perimeter is the operator's decision, not a side effect of a batch.

### 4. The batch, fired BEFORE any seat existed

`analytics/fetch-requests/2026-09-05-daily.json`. **That ordering is the whole point** — a forecast written
after a seat has spoken is fitted, not pre-registered — and it is verifiable against git rather than
against my word.

Twelve requested rows against `MAX_URLS` of 12, plus the relay's own prepended head control, so **nothing
is truncated and the reduction should carry thirteen records.** A reduction with `truncated: true`, or any
count other than requested 12 / completed 12, is an instrument discrepancy and must be reported as one
before any row is read.

**The note field is proof-read against the row list as a step**, because `run()` copies `request["note"]`
into the manifest verbatim and a false sentence there is frozen into the evidence and cannot be edited out.
That is yesterday's lesson, applied.

### 5. The forecasts, so they are legible without opening the JSON

Baseline for A and B, cited by blob and never by path: reduction blob **`9d5a8ba5`** (request
`2026-09-04-daily.json`), page state **105,703 bytes, sha256 `c946fe61f1e7…`**.

- **FORECAST A** (row 1): the 400-character `Status of Proceeding` window on UNCT/20/6 returns
  character-identical to yesterday's, which reads `Status of Proceeding: Pending` and `Latest Development:
  December 2, 2025 - Each party files a statement of costs.`
- **FORECAST B** (row 2): the 400-character `Subject of Dispute` window returns character-identical to
  yesterday's, which reads `Subject of Dispute: Seismic data enterprise`, `Instrument(s) Invoked: (i)
  NAFTA`, `Applicable Rules: UNCITRAL`.
- **Disclosed non-independence, in advance:** `Latest Development:` and its value sit INSIDE forecast A's
  own window and are graded by it. They are not a third confirmation and I will not count them as one.
- **Row 3 carries no forecast.** It anchors a third window on the same page (`Date Registered`) to sample a
  region A and B do not cover. **One row cannot locate the +33 bytes** between the 09-03 and 09-04 reads,
  and I claim no such thing: it samples a third region, and that is all.

**The three-point same-host discrimination — the day's real test.** Grounds are §2's committed source
comment, not a hunch:

- **FORECAST C** (row 4): `https://www.iisd.org/itn/feed/` returns `outcome: "ok"`, status **200**.
- **FORECAST D** (row 5): `https://www.iisd.org/itn/en/feed/` returns **non-200**.
- **FORECAST E** (row 6): the Grosse Ruse-Khan article URL, under the same `/itn/en/` prefix, returns
  **non-200** — a third observation of the same refusal.

**The decision rule is pre-registered here, before the answer exists, and it binds every seat today:**

| Rows 4 / 5 / 6 | What it licenses | What it does NOT license |
|---|---|---|
| 200 / non-200 / non-200 | The refusal is **path-prefix-specific** at this host under this runner and UA. Forecasts C, D, E all confirmed; the `/itn/en/` prefix is the discriminated variable. | Nothing about *why* the prefix refuses, and nothing about the article. |
| non-200 / non-200 / non-200 | The refusal is **host-wide** for this runner today. Forecast C **falsified**, and the source comment does not describe this runner's experience. | Nothing about UA-gating, which stays untested — this relay sends one UA. |
| 200 / 200 / non-200 | The refusal is **specific to the article URL**, not to the `/itn/en/` prefix. Forecast D falsified. | Nothing about the article's existence or content. |
| 200 / 200 / 200 | Yesterday's 403 was **transient**. Forecasts D and E both falsified, and the two prior observations were a passing condition. | A 200 on row 6 is a fact about the fetch. **Nothing in this record may be asserted from that article's body or title on the strength of a status code.** |
| any row 4 non-200 with rows 5/6 200 | **Instrument discrepancy.** Report it as one; enter no branch. | Any reading whatever. |

**Rows 7–11 carry no forecast and I claim none. They are probes**, and their whole purpose is that the
"pending" attribute for five of the seven dockets is currently inherited from the record rather than read.

**Standing reading rules, restated because they are the ones this council keeps having to re-learn:** a
returned excerpt establishes what its own 400 characters contain and NOTHING about what lies outside them;
an absent literal establishes that `excerpt_of` found no match on the returned body — **a statement about
the fetch, not about the docket**; a 404 or non-200 is information about the URL pattern, **never** about
the case. Every count derived from this reduction carries its COMMIT as well as its scope (`0903-B2`).

### 6. The digest sentence (Rule 1), copied from `meta.json`, not from memory

`Digest 2026-08-31: 67 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Read from `digests/2026-08-31_ISDS-Thematic-Watch/meta.json`, which also carries
`validation_status_only: true`. **Unchanged.** Today is a **Saturday**; no digest was due. The digest of
record has now stood unchanged for five council sessions, and I checked the file rather than incremented a
remembered figure.

### 7. Member return-path protocol, and the procedural caveat it forces

Members are REAL subagents spawned from the committed definitions in `.claude/agents/`. **I direct; I never
write a member's contribution; member text enters this record verbatim.** If a spawn fails, that seat is
recorded as **PROCEDURAL FAILURE** for the day and nothing is written in its place. If a seat reports a
model differing from the directive, I record the discrepancy as a procedural caveat on its first line and
never substitute silently.

**Rule 0 departure, declared as a departure and not as a harmonization — fifth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 directs that exactly two agents be convened, analyst and
chairman, and says the integrity challenge *"is not a separately convened seat and does not file its own
return."* **I convene the research analyst and the integrity officer — three seats with me — and the
officer files Part IV.** I do it because the operator's cardinal rule against performed seats is the one I
will not trade: Rule 0's compliant reading would have me write the integrity voice myself, and that is
precisely the prohibited act. The rule is the operator's and one line closes it either way; it is
escalation 1 below. **Per Rule 0 the analytics officer and the systems researcher are NOT convened:** the
digest sentence at §6 is two lines I copied from `meta.json`, and the one optimization item is a paragraph
I write at close-out. The systems researcher's queue has no ungated work — every item on it is on the
operator's pending-approval list and is therefore zero-budget today.

### 8. Traps carried into today, named in advance so a seat cannot walk into one

1. **Entry 28 binds from today.** No memo may carry *"first"*, *"never"*, *"no seat has ever"*, or *"new to
   this corpus"* without an adjacent line stating the literals tried, the synonyms tried, the file count,
   the scope and the commit. **§2 above is the first application and it was run against myself.**
2. **Entry 28 covers negative-form novelty too** — a claim that something CANNOT BE DONE by this project is
   a novelty claim and carries the same screening burden. That is yesterday's second addendum, learned at
   the operator's expense, and it is the one I am personally most likely to repeat.
3. **`wsgrep_at.py` before any absence claim**, whitespace-normalised, and check a known positive before
   trusting a zero. A hard-wrapped phrase does not match a naive grep.
4. **Tool status is never source state.** A 403 is a fact about a fetch.
5. **No search-synthesis figures.** A search-result summary is not a source; nothing is graded from one.
6. **Nothing on the operator's pending list is built, applied, or proposed as built** — the
   `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed
   swap, the Kluwer source, the category signal, the `build_graph` fix, and any ledger amendment. Standing
   zero-budget items stay zero-budget.

---

# Part III — Chairman's grading of the pre-registered batch

**Reduction of record, cited by blob and never by path:** blob **`0824d1a458720124919a7410dca10bcdb10a9fd6`**,
request `2026-09-05-daily.json`, relay run **`33962772344`** (run number 90), fired by the push of
**`58cb1d8`** and committed back at **`e47b879`**. `fetched_at` **`2026-09-05T11:14:48.050320+00:00`**,
`environment` `github-actions-runner (run 33962772344)`, `control_ok: true`, `void: false`,
`truncated: false`, `requested: 12`, `completed: 9`, thirteen records.

Every figure below is cited to that blob. If a later run overwrites the path, the figures remain verifiable.

## 0. An instrument-discrepancy rule I pre-registered THIS MORNING, and it was wrong as written

Part I §4 says: *"A reduction with `truncated: true`, or any count other than requested 12 / completed 12,
is an instrument discrepancy and must be reported as one before any row is read."* **The reduction reports
`completed: 9`. Under my own rule as written, I would now be reporting an instrument fault. There is no
instrument fault, and the rule is the thing that is broken.**

`scripts/fetch_relay.py:221` defines it: `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")`.
**`completed` counts SUCCESSES, not attempts.** Twelve rows were requested, twelve rows were attempted,
nine returned `ok` and three returned `refused` — so `completed: 9` is the correct and expected encoding of
a batch with three refusals, and it is in fact the very result the batch was designed to produce.

**I own this and I do not bury it.** I wrote a falsifiable instrument-integrity rule without reading the
line of code that defines the field it ranges over, on the same morning I told the analyst that entry 28
binds and that negative-form claims carry the same screening burden. The correct rule, and the one that
governs from here: **the instrument is sound when `requested` equals the row count, `truncated` is false,
`control_ok` is true, `void` is false, the record count is requested + 1, and every requested URL has a row
— and `completed` is a yield figure, not an integrity figure.** All six conditions hold: 12 requested,
12 rows, 13 records with the prepended head control, no truncation, both controls `ok`/200 on
`example.com`, `void: false`.

**The instrument was live at both ends of the run** — record 0 (head control) and record 12 (tail control)
both `ok`, status 200, 559 bytes, sha `ff67a9d764d6a236…`, identical to each other. **No refusal below is
an artefact of a broken run.**

## 1. UNCT/20/6 — both forecasts CONFIRMED, and the page did something neither of them could see

**FORECAST A: CONFIRMED.** Row 1's 400-character `Status of Proceeding` window is **character-identical**
to the pre-registered string — verified by string equality in Python against the text frozen in the request
note at `58cb1d8`, not by eye, both 400 characters. It reads `Status of Proceeding: Pending` and
`Latest Development: December 2, 2025 - Each party files a statement of costs.`

**FORECAST B: CONFIRMED**, on the same test. `Subject of Dispute: Seismic data enterprise`,
`Economic Sector: Other Industry`, `Instrument(s) Invoked: (i) NAFTA`, `Applicable Rules: UNCITRAL`.

**Disclosed non-independence, honoured:** `Latest Development:` sits inside forecast A's own window. It is
graded by A and is **not** counted as a third confirmation.

**Row 3 — `Date Registered` — `find_matched: false`, empty excerpt.** Under the standing rule this is a
statement that `excerpt_of` found no match on the returned body, **and it is a fact about the fetch, not
about the docket.** It does not mean the case has no registration date. Row 3 carried no forecast and I
claim nothing from it beyond that the literal is absent from what came back.

### The finding rows 1–3 were not designed to catch, and it is the more important one

| Read | `fetched_at` | bytes | sha256 (16) |
|---|---|---|---|
| `2026-08-13-zz-einarsson-docket` | 2026-08-13T12:03:31 | 105,667 | `7837676559caa62f` |
| `2026-08-26-daily` | 2026-08-26T12:38:06 | 105,669 | `5c4444e2a039cab5` |
| `2026-09-03-daily2` | 2026-09-03T12:06:55 | 105,670 | `246960f45cf919ef` |
| `2026-09-04-daily` | 2026-09-04T11:22:25 | 105,703 | `c946fe61f1e79866` |
| **`2026-09-05-daily`** | **2026-09-05T11:14:48** | **105,703** | **`c167c772f53ef32f`** |

**Today the page returned the IDENTICAL BYTE COUNT and a DIFFERENT SHA256.** 105,703 both days; the hash
moved. Both 400-character windows are character-identical across the two reads. **So the page changed
content, outside both observed windows, with exactly zero net change in length.**

**What that licenses.** A same-length substitution somewhere in the ~104,900 characters this council has
never looked at. A fixed-width mutable field is the obvious class — a nonce, a CSRF or session token, a
build or asset hash, a fixed-format timestamp — but **I have tested none of them and I name the class, not
the cause.**

**What it costs us, and this is the operational point.** This council has been reading `sha256` as a
change-detector for ICSID case pages. **A page carrying a per-request rotating fixed-width field cannot be
monitored by its hash**: the hash will differ on every read whether or not anything substantive moved, and
a difference will look like news. Today's pair is the first observation in this table that **separates**
the two signals — same length, different hash — and it points at byte-length plus anchored windows as the
signals that carry information here, and at sha256 as the one that may not. **One observation is not a
diagnosis.** The cheap test is two reads of this page in the same batch: if their hashes differ from each
other, the field rotates per request and the matter is settled in one row-pair. That is tomorrow's first
next step and it costs nothing.

**And it retires yesterday's "+33 bytes" question in its old form.** Next step 3 asked for one row to
locate a +33-byte delta. Today's delta is **+0 bytes with a changed hash**, so the quantity to explain was
never a stable accretion. Row 3 was spent on the old framing and returned nothing; that is a fair loss and
I record it as one.

## 2. The `www.iisd.org` discrimination — MY FORECAST C IS FALSIFIED, and the falsification is the result

This was the day's designed test and the three rows are unanimous:

| Row | URL | Outcome | Forecast | Grade |
|---|---|---|---|---|
| 4 | `https://www.iisd.org/itn/feed/` | **refused, 403** | C: `ok`/200 | **FALSIFIED** |
| 5 | `https://www.iisd.org/itn/en/feed/` | **refused, 403** | D: non-200 | CONFIRMED |
| 6 | the Grosse Ruse-Khan article URL | **refused, 403** | E: non-200 | CONFIRMED |

All three carry `user_agent: isds-watcher/1.0 (+https://github.com/jackemorywilliams-bit/isds-watcher)`,
`final_url: null`, `bytes: null`, `sha256: null` — **the origin was never read, so `no_contact` is not
being recorded as an origin answer.**

**The pre-registered decision rule selects branch 2, and I apply it as written rather than reaching past
it:** non-200 / non-200 / non-200 licenses ***"the refusal is host-wide for this runner today"***, falsifies
C, and leaves **UA-gating untested — this relay sends exactly one User-Agent.** That is the whole of what
today establishes about that host.

**What the falsification of C actually tells us, and it is worth more than a confirmation would have been.**
`src/sources/iisd_itn.py:3` says `use this, NOT /itn/en/feed/ which 403s` — a *path asymmetry*. **Today
there is no asymmetry: the path the comment endorses is refused exactly like the path it warns against.**
So the committed comment does not describe this runner's experience today. Either the condition changed, or
it never applied to this runner, and **today's evidence does not distinguish those.**

**My Part I §2 claimed this find "converts yesterday's uninterpretable 403 into a replicable pre-registered
test." That claim holds and I am not withdrawing it — but the value was in the DESIGN, not in my guess.**
The design was right: a same-host predicted-positive alongside a predicted-negative turns an
uninterpretable status into a discriminating one in a single run, and it did, in one direction I did not
expect. **The guess was wrong, and a day where the instrument defeats the chairman's forecast is the
instrument working.** Three days of ITN refusals — 2026-09-04 at 11:50:42Z, 2026-09-04 at 12:48:31Z, and
today at 11:14:48Z — now sit on the record, the third of them with same-host controls the first two lacked.

**None of it is a statement about the article.** The gap slug
`grosse-ruse-khan-itn-ip-isds-plain-packaging-to-patent-revocation` stays **OPEN and stranded**, no seat has
read that body, and nothing may be asserted from its title.

### 2b. The consequence for the collector — and a false alarm I nearly filed, caught by reading the code

**`https://www.iisd.org/itn/feed/` is not an arbitrary URL. It is `FEED_URL` at `src/sources/iisd_itn.py:22`
— the exact URL this project's `iisd_itn` collector polls, and the one `_probe_iisd_itn` health-probes
(`src/source_health.py:81-87`).** And the fetch path is shared, not merely similar: `fetch_rss`
(`src/sources/base.py:284-286`) calls `polite_get`, which sends `USER_AGENT` at `base.py:25` — the identical
string the relay's refused rows record, because `scripts/fetch_relay.py:36` imports the same two symbols.
**Same URL, same fetcher, same User-Agent, same runner class.**

**The dated state change:** the digest of record, 2026-08-31, carries `iisd_itn` as
`QUIET (feed live; newest item 2026-04-21)`. That exact string can only be produced by `_probe_iisd_itn`
receiving entries with parseable dates (`source_health.py:88-94`). **So that feed answered this project on
2026-08-31 and refuses it today**, and the next weekly collection is **Monday 2026-09-07**.

**Here is the false alarm I nearly filed, and I am recording it because catching it is the day's discipline
working and filing it would have been yesterday's mistake a sixth time.** I began drafting an escalation
that a refusal would be displayed to the operator as `QUIET (feed live)` — a true statement about the world
(the feed 403s) attached to a false statement about how this project works. **It is false.** `fetch_rss`
returns `None` on a refusal, so `_probe_iisd_itn` returns `None`, and `apply_to_source_health`
(`source_health.py:247-249`) maps `None` to `DEGRADED (N zero runs)` **and appends the source to the
degraded list. The design fails toward alarm, deliberately** — the comment at `source_health.py:72-74` says
so in terms. **The operator will be alarmed, not reassured.** I checked the code instead of escalating my
inference, which is the only reason this paragraph reads the way it does.

**What survives, narrowly and concretely.** The alarm that fires would be the **generic** one —
`DEGRADED (N zero runs)`, whose documented meaning (`source_health.py:57-64`) is *"fetchers likely no
longer match the live site."* **For a 403 that is the wrong diagnosis, and it is precisely the failure the
probe system was built on 2026-08-17 to end** — that comment records three sources given one label for
three different diseases, and names `italaw`'s 403 as one of them. `_probe_italaw` therefore has a refusal
branch that yields `NOT-READ (HTTP NNN …)`. **`_probe_iisd_itn` has no such branch**: `fetch_rss` collapses
refused, unparseable and empty into a single `None`, so the probe cannot tell the operator which disease
this is. **That is a real, component-specific defect and I am reporting it, not fixing it** — it is a build
and builds are the operator's. It is escalation 3.

## 3. The five inherited "pending" attributes — ALL FIVE NOW READ, and the record was wrong about the class

Rows 7–11 carried no forecast. All five returned `ok`/200 with `find_matched: true`. **The class attribute
"pending", inherited from the record for five of the seven dockets, is now observed for all five**, and the
excerpts carry more than the status:

| Docket | bytes | sha256 (12) | Status | Latest Development, verbatim from the returned window |
|---|---|---|---|---|
| ARB/22/2 | 94,751 | `9f197f396c34` | Pending | `July 10, 2025 - Each party files a reply submission on costs.` |
| ARB/22/30 | 95,712 | `ae24da73b61a` | Pending | `August 30, 2026 - The Respondent files a rejoinder on the merits.` |
| ARB/24/18 | 93,692 | `6d8440a5bf44` | Pending | `August 13, 2026 - The Respondent files a reply on jurisdiction and observations on the Republic of Bulgaria and the non-disputing party's written submissions.` |
| ARB/24/26 | 86,797 | `ce4b5d8537c3` | Pending | `June 18, 2025 - The proceeding is suspended pursuant to Arbitration Rule 54(1).` |
| ARB/24/46 | 88,573 | `d56a44f424ae` | Pending | `August 26, 2026 - The Tribunal issues Procedural Order No. 3 concerning the Respondent's request to address the objections to jurisdiction as a preliminary question; as a result, the objections to jurisdiction are joined to the merits of the dispute.` |

Each cell is a substring of its row's own 400-character window in blob `0824d1a4`; the two apostrophes and
one `&nbsp;` are HTML entity escapes (`&rsquo;`, `&nbsp;`) in the returned text, rendered here as the
characters they encode and flagged so no seat later reads this table as byte-exact.

**Three corrections of record follow, and all three exist only because these rows were read rather than
inherited.**

**(a) The record's belief about which pending docket is most recently active is FALSE.** Yesterday's Part I
§3 and the batch note both state that ARB/23/3's `February 11, 2026` development is *"the most recent of
the seven."* **Three of the five read today are more recent than that**: ARB/22/30 on **30 August 2026**,
ARB/24/46 on **26 August 2026**, ARB/24/18 on **13 August 2026**. The most recent of the seven is ARB/22/30,
six days ago. **The error was not a fabrication — it was a true statement about what the record held, stated
as a statement about the seven dockets.** That is exactly the class yesterday's escalation 2 named, and the
fix was five rows.

**(b) "Pending" is uniform in label and NOT uniform in substance.** ARB/24/26 reads `Pending` and its latest
development is its own **suspension** under Arbitration Rule 54(1), 18 June 2025 — fifteen months ago. A
class treated as seven live matters contains at least one suspended one. **No seat may write "seven pending
dockets" again without that qualification.**

**(c) The class is not dormant, and two members are litigating jurisdiction right now.** ARB/24/18 has a
reply **on jurisdiction** filed 13 August 2026; ARB/24/46 has objections to jurisdiction **joined to the
merits** by Procedural Order No. 3 on 26 August 2026. **Ring 1's third limb — a live, litigated
jurisdictional or admissibility doctrine — is satisfied in form by both.**

**And here is the limb that is NOT satisfied, stated before anyone gets excited.** Yesterday's Ring 1 screen
read ICSID's own `Subject of Dispute` field across all seven and found **exactly one** surfacing as an
IP-as-investment matter: UNCT/20/6. **Nothing today touches that screen.** A live jurisdictional fight in a
matter whose subject is not IP does not reach this council's question — the three limbs must meet in one
matter, not one apiece across a class. **ARB/24/18 and ARB/24/46 are limb-3-live and limb-1-absent on the
evidence this council holds, and they are recorded as near-misses, not as movement.** What today changes is
that the class is now **observed** rather than assumed, and observed to be active.

## 4. What Part III does NOT establish

It does not establish that the `www.iisd.org` refusal is permanent, UA-gated, or IP-based — one runner, one
UA, one day, three rows. It does not establish what the UNCT/20/6 hash change consists of, only that it is
same-length and outside both windows. It does not establish anything about the ARB/23/3 `No References
Available.` question, which was deliberately not fired. It reads nothing about any article body. **And it
does not move the research question**, which is Part VI's business.
