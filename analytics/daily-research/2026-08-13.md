# ISDS Thematic Watcher — daily council, 2026-08-13

**Chairman:** the scheduled cloud session, running as the chairman seat on Claude Opus 5 per the
operator's model directive of 2026-08-03.
**Convened:** `research-analyst` (Claude Opus 5) as a real subagent; the integrity challenge heard
inside Part V per Rule 0, also as a real subagent, its return reproduced verbatim.
**Not convened, per Rule 0:** `analytics-officer`, `systems-researcher`.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a five-part meeting with a separately-filed
integrity officer (Part 3), an analytics officer (Part 4) and a systems researcher (Part 5).
`prompts/daily_council_protocol.md` Rule 0 says the daily meeting is **the researcher, the chairman,
and one optimization item**, that the integrity challenge is heard inside the Part V dialogue and
files no separate return, and that the analytics officer is not convened at all — the digest sentence
is two lines the chairman copies from `meta.json`. The protocol file's own Precedence clause says it
wins, and that the conflict is worth a line in the record's procedural caveat. **This is that line.**
I ran the meeting to Rule 0. The integrity officer was still convened as a real agent, because the
alternative — a chairman writing the challenge to his own work — is the failure this project treats
as cardinal; Rule 0 governs where its voice is *filed*, not whether the voice is *real*.

### Instrument state, probed at session open

Both fetch instruments are dead, and both were probed rather than assumed.

- **Direct egress: DEAD.** `curl https://example.com` returned `CONNECT tunnel failed, response 403`
  at the proxy gateway. `https://www.italaw.com/` returned the same. Two hosts, one of them the
  neutral control, so this is the plumbing.
- **The fetch service: DEAD.** `WebFetch` on `https://example.com` — the neutral control, chosen
  precisely so the answer cannot be about a research source — returned
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`.
- **The fetch relay: ALIVE**, and it did the day's retrieval. Two batches, both pre-registered before
  any result existed, both answered by `github-actions-runner`.
- **WebSearch: alive**, and it was the analyst's only instrument.

Per the standing rule that a tool-layer status is never a statement about a resource: none of the
above is a fact about UNCITRAL, UNCTAD, italaw or any document. It is a fact about this session's
plumbing, and it is why the relay exists.

### Agenda

Two items, both drawn from the 2026-08-12 close-out's next steps, and one of them arrived unplanned.

1. **The A/81/17 standing watch, and the China–Switzerland BIT (2009) mapping** — the two queued
   fetch items reachable from the relay's `ALLOWED_HOSTS`. A/81/17 is the UNCITRAL 59th-session
   report, recorded on 2026-07-27 as expected August–September 2026; today falls inside that window
   and the watch was last fired on 08-10. The BIT mapping carries escalation 8's forum-provision
   question and two finds never asked of it.
2. **Unplanned, and it became the day's instrument work:** the first batch returned an observation
   about `uncitral.un.org` that the council's own 2026-08-08 rule forbids me to interpret from a
   single row. I fired a second batch to control it rather than choose the reading I preferred. That
   is Part II.

The analyst was pointed at the two things a search can actually advance — the Einarsson award watch,
and the contemporary jurisdictional/admissibility limb — and told that an under-budget return counts
as a good outcome.

**Spend checkpoint, placed here per the routine and honoured after the vetting round:** it is
recorded in Part VI, after the objections, not before them.

### Member return-path protocol

The analyst was briefed that his return enters the record **verbatim** as final record text; that
direct fetch and the fetch service are both dead and WebSearch is his only live instrument; that
today's relay rows were already fired and were mine, so he must not fire another and must not spend
budget on the three questions they asked; that the standing tool-status-as-source-state rule is
cardinal; that `0811-B4` (title-vs-summary), `0811-B5` (unenumerated superlatives) and `0811-B6`
(disposition-vs-rule and dropped qualifiers) bind him; that the `analytics/errors/` screen adopted
yesterday is a standing step before his first search; that no figure may rest on a search-engine
generated summary; and that his budget was **four WebSearch calls, hard**. He was expressly told not
to re-run the IAReporter domain query, not to try to settle the China RDP tier conflict by search,
and not to re-fire the Zeph `contribution` question.

The integrity officer received the analyst's return, both relay reductions, and this record's own
draft parts, and was told its objections bind me as well as him. Both returns are reproduced without
editing.

---

## Part II — Two relay batches: a null that got sharper, an access refusal that got its third run, and a digest that moved

Both batches are pinned to the same relay code. `scripts/fetch_relay.py` at this branch's HEAD is
blob `7204847f908d7958584842e523da8ae0ecd08703`, **verified by blob equality and not by history walk**
per `0811-O1` — identical to the blob pinned in the 2026-08-12 request and used for the 08-11 and
08-12 runs. I discharge this myself rather than leave it to the officer.

Batch 1: `analytics/fetch-requests/2026-08-13-daily.json`, answered at `2026-08-13T11:26:29Z` by
`github-actions-runner (run 31695467710)`. Batch 2:
`analytics/fetch-requests/2026-08-13-uncitral-digest-control.json`, answered at `11:29:10Z` by
`run 31695670971`. **Both batches: control brackets ok/200, relay's own control ok, `void: false`.**
Every row below is therefore readable within its own pre-registered scope.

**Citations here name a COMMIT, not a path, and today that precaution was needed rather than
ceremonial.** Per taxonomy entry 25 (MUTABLE-REDUCTION CITATION, adopted 2026-08-05), a reduction is
cited at the commit that wrote it, because the relay overwrites results in place. Batch 2's reduction
as I read it is pinned at **`95c0b53`** (run 31695670971, `11:29:10Z`). The file at that path has since
been overwritten at `73e7cb4` by run 31695899871 at `11:32:13Z` — **by my own mistake, described in
Part IV** — and the diff between the two commits is **two lines, `fetched_at` and `environment`.
Every row's `bytes`, `sha256`, `find_matched` and excerpt is identical across the two runs.** So the
accident supplied a third independent run of the same URL, and I record its evidentiary effect below
rather than pretend it did not happen.

### P1 — the A/81/17 watch returns a null for the eighth time, and this time the null is worth more

Row 2 of batch 1: `https://uncitral.un.org/en/commission`, find `A/81/17`, **ok/200, 832,338 bytes,
`find_matched: false`, excerpt empty.** The recorded fact is exactly what P1 pre-registered: *the
string "A/81/17" was not found in that page's rendered text at that instant.* It is **not** a finding
that the report is unpublished, and it does not move the 2026-07-27 expectation of August–September
2026.

The null is reported in the **standard same-page positive-control form**, and I want to be accurate
about whose idea that is: it is not today's innovation. The 2026-08-06 record already logged "the
A/81/17 day-5 null in the standard same-page positive-control form (A/80/17 matched)". What batch 2
adds is that the control was fired *in the same run as the digest question*, so one run answers both.
Row 4: same URL, find `A/80/17` — **matched**, returning this window:

> "ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna
> A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session
> A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth
> session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se"

So the page is **intact**, it is **still the page we think it is**, and — this is the part that
matters — **it lists each session's report by symbol**: the 58th session block carries
`A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session`.
The watch string is therefore absent from a page that demonstrably *would* carry it in that form.
That converts eight bare `false` booleans into an absence with a shape. Prior record, and attributed
to its date rather than to today: on **2026-08-03** a find on `fifty-ninth` matched on this same page
and returned `A/CN.9/1235/Rev.1 - Provisional agenda, annotations thereto and scheduling of meetings
of the fifty-ninth session`, so a 59th-session block existed then and listed a provisional agenda.
**What is NOT established:** that the 59th-session block exists today, what it now lists, or anything
about whether A/81/17 has been issued. I did not fetch that block today and I will not infer it.

### P2 / P3 / P4 — UNCTAD refused, for the third observed run, and the escalation is unchanged but heavier

Rows 3 and 4 of batch 1 both targeted
`investmentpolicy.unctad.org/.../treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009-`
with finds **never asked of that page before**: `intellectual property` (limb (a) — whether IP
appears in UNCTAD's mapping of this BIT's investment definition) and `Denial of benefits` (limb (c) —
the jurisdictional doctrine most directly bearing on treaty-shopping). **Both returned
`refused / 403`.**

Per G5 and per P4 as pre-registered, that is a fact about **this client's access to that host at that
instant** and about nothing else. It is not a finding that the mapping omits IP, and it is not a
finding that the BIT has no denial-of-benefits clause. Both questions remain **unasked of the
resource**. The access history, which is a history of our client and not of UNCTAD: `ok/200` on
2026-08-03, 08-04, 08-05, 08-08 and 08-09; `refused/403` on 08-10, 08-11 and now 08-13 — **three
observed runs in the refusal condition, and no observation on 08-12** because that day's batch did
not target this host. Escalation 8 of the 2026-08-12 record — one manual retrieval, operator-side, of
the China–Switzerland BIT (2009) forum provision — **stands unchanged in substance and is now the
third consecutive run's worth of reason to do it.**

### The unplanned finding: the page's digest moved, and I controlled it before reading it

Batch 1's row 2 returned byte length **832,338** and sha256 **`f9712994f5aec4c9a8c5…`**. Across
**eleven rows at nine distinct instants**, from `2026-08-03T18:25:23Z` to `2026-08-11T11:30:04Z`, that
same URL returned byte length **832,338** and sha256 **`ca87a113c024605cac35…`** — every time, length
and digest both. So today: **same length, different digest.**

This council has been wrong about exactly this phenomenon inside the last week, and the chairman was
the one who got it wrong. On 2026-08-12 `pca-cpa.org` returned **three different digests at one byte
length within a single run** — per-response variation, which makes a digest useless as a change
detector on that host. Two explanations therefore fit today's observation equally well: **(A)**
something of constant length on the UNCITRAL page changed between 08-11 and 08-13, or **(B)** this
host also varies per response and the eleven prior digests coincided. Batch 1 fetched the URL once
and **cannot distinguish them.** So I fired batch 2 and pre-registered the rule before the answer
existed, rather than choosing the reading I preferred.

**D1 resolved on its affirmative branch.** Batch 2 rows 2, 3 and 4 fetched the same URL three times
in one run: **832,338 bytes and `f9712994f5aec4c9a8c55bcf…` on all three, identical.** Adding batch
1's row from three minutes earlier, that is **four rows across two separate runs, all identical** —
and adding the accidental re-run at `11:32:13Z`, **seven rows across three runs spanning six minutes,
every one of them 832,338 bytes and `f9712994…`**. Explanation (B) is not supported on this host. The
cross-day difference is attributable to something that differed between the 08-11 and 08-13 fetches.

One qualification the officer would otherwise have to extract from me, so I state it myself: this
does **not** generalise to `investmentpolicy.unctad.org`, where the 2026-08-05 session measured
**twelve re-fetches returning twelve distinct sha256**. Per-response digest instability is a property
of a host, measured host by host. Today measures it for one host, on one day, at seven rows.

**What that does and does not license, stated tightly, because the temptation to over-read it is the
exact failure of 2026-08-12.** At *constant* byte length, a changed digest says a constant-width
element differed — a token, a build identifier, a fixed-format timestamp. It does **not** establish
that the document changed, and it establishes nothing whatever about A/81/17: the same run that saw
the new digest saw `find_matched: false` on the watch string. What it *does* give the council is the
second attested host for next step 7 of 2026-08-10, **"choose the sentinel to the host"** — and
UNCITRAL's profile is the **opposite** of the PCA's. On `pca-cpa.org` the digest varies per response
and is worthless as a sentinel. On `uncitral.un.org` the digest was stable across nine instants and
ten days and is now demonstrably stable within a run, while **byte length has not moved at all across
the entire observation window** — so on this host length is the quieter signal and the digest carries
a constant-width volatile element. Same rule, two hosts, opposite readings. That is what "choose the
sentinel to the host" was adopted to mean.

---

## Part III — The research analyst's return

Reproduced **verbatim**, without editing, as briefed. The analyst is a real subagent
(`research-analyst`, Claude Opus 5 per its committed definition). His Finding 3 became the day's
third relay batch; that is recorded in Part IV, after his text, so that nothing in his return is
read backwards through my response to it.

---

**Council meeting 2026-08-13. Instruments: WebSearch only (curl and the fetch service both established DEAD this session by the chairman's probe; the relay's batch was fired before I was convened and I did not touch it).**

---

### 1. The `analytics/errors/` screen (standing step)

The directory holds one file, `20260810T142237Z-analyst-malformed.json`, rejected for want of a `candidate_claims` block. Read in full: it carries the three-way China RDP tier conflict with all three URLs live in the text — Jones Day at 6/4/3 keyed to NMPA registration category, Ropes & Gray at **three** years for improved new drugs (Cat. 2), Marks & Clerk at **four** with a six-year uplift for a globally new indication — plus the marker `GAP-UNRESOLVED: china-rdp-measures-primary-text`.

**What it changed:** nothing in my search plan, but it confirms the chairman's bar was correctly placed. The repository *already holds* the conflict, named, sourced and URL-bearing; anything a seat could produce by search today would be a weaker copy of it. The screen's value today is negative and real: it removes any temptation to spend a call there. Note for the operator — this material is quarantined in `analytics/errors/`, not in the record, so a seat that does not run this screen cannot see it.

---

### 2. Instrument budget

**Four of four spent.** In order:

1. `ICSID case details "UNCT/20/6" Einarsson Geophysical Service Canada status of proceeding latest development` — Priority 1, docket-vocabulary formulation (deliberately *not* yesterday's award-vocabulary formulation).
2. `investment treaty arbitration 2026 decision on jurisdiction "abuse of right" corporate restructuring "critical date" foreseeable dispute` — Priority 2, doctrine + year.
3. Same doctrine narrowed to IP/data, `allowed_domains` = Kluwer/EJIL:Talk/IISD — Priority 2, the venue formulation proposed for adoption in yesterday's method note (c), here given its first test.
4. `icsid.worldbank.org case database case detail "UNCT/20/6" Einarsson Canada`, `allowed_domains` = icsid.worldbank.org — spent to de-risk a relay row rather than to answer a question; see finding 3.

---

### 3. Findings

**Finding 1 — Priority 1, *Einarsson v. Canada* (UNCT/20/6): no award surfaced. Null, and a null of the same shape as yesterday's.**
ESTABLISHED (title-level, `0811-B4`): the eight returned results are document- and index-level entries — Federal Court of Canada judgment 2020 FC 984 (20 Oct 2020) and Decision on Claimant's Motion to Disqualify Counsel (24 Feb 2022) at Jusmundi; Notice of Intent (15 Oct 2018), Notice of Arbitration (3 April 2019) and Statement of Defence (9 June 2022) at OGEL/TDM; the UNCTAD Navigator entry at case id 1065. **The latest dated document carried by any returned title is 9 June 2022.** Nothing displaces the record's own last docket read (PO No. 9, `2026-07-10.md:93`). Confidence HIGH that this query surfaced no award; that is a statement about one index at one instant and **not** a statement that no award has issued — awards are routinely published weeks to months after dispatch to the parties.
Standing-watch entry: **no award surfaced as of 2026-08-13; approximately seventeen months post-merits-hearing** on the record's March 2025 hearing date.

**Finding 2 — an unvetted increment on the same case, which I am explicitly NOT asserting.**
The generated summary attached to query 1 states that "Procedural Order No. 4 was issued on 9 Feb 2024 regarding consequences for the proceedings of the passing of Claimant Theodore David Einarsson," that "Procedural Order No. 10 was issued on 8 July 2024," and that "Party Representatives were updated on 7 Apr 2025." **None of these is carried by any returned title or URL. Each exists only in generated summary and is therefore not assertible** (no-search-synthesis-figures rule) and no candidate claim is proposed for any of them. I flag them because if a PO No. 10 exists it displaces the record's "PO No. 9 is the latest document," and because the "Party Representatives 7 Apr 2025" item resonates with the known 2026-08-11 Jusmundi artifact whose title and slug disagreed — i.e. this is exactly the material the record has already been burned by. It is a question for the relay, not for a search. Confidence: LOW as information, HIGH that it must not enter the record in this form. Note also that both dates, if real, are *pre*-hearing and would not be post-merits developments.

**Finding 3 — the most actionable thing I found today: ICSID's own docket page for UNCT/20/6 exists, is on `ALLOWED_HOSTS`, and has never been asked.**
ESTABLISHED: query 4 returned `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6`, titled "Case Details | ICSID" — the URL and its title are the evidence; the body was not retrieved. ESTABLISHED by disk, with the population named: grepping `UNCT/20/6`, `UNCT%2F20%2F6` and `CaseNo=UNCT` across **`analytics/fetch-requests/` and `analytics/fetch-results/`** returns zero occurrences — the relay has never been pointed at this docket. INFERRED, at MEDIUM-HIGH: the mechanism is proven, because the identically-shaped row `case-detail?CaseNo=ARB/22/2` with `find: "Latest Development"` was fired on 2026-08-06 and the resulting docket read is what put Huawei's costs-submission dates and "no PO 9+, no award, no closure event" into the record at HIGH confidence on 2026-07-31. The residual risk is that ICSID's UNCT-series pages may not render the same field set as ARB-series pages; that is unknown and the row should be read with that in mind.
This is the docket-page-before-document-hunt rule with a live target: **the council has been hunting the *Einarsson* award through indexes for weeks while never asking the administering institution's own docket.** `GAP-UNRESOLVED: einarsson-unct-20-6-docket-unread`.

**Finding 4 — Priority 2, limb (c) contemporary: DRY. Reported as a null, twice-formulated.**
Query 2 returned the canon and nothing else — Philip Morris v. Australia, Tokios Tokelės, GAR/Lexology practitioner guides, an arbitration-academy student paper. This is the recency failure the record diagnosed yesterday (method note (c)): doctrine + year surfaces the most-cited, not the most recent. Query 3, the venue-restricted retest, produced no dated 2026 item joining the doctrine to an IP or data asset either. **There is no 2026 development, on these two formulations, applying abuse of right / treaty-shopping / the critical-date test in a case where the asserted investment is IP or data.** That is a null on two queries in one index, not an absence in the world. Confidence HIGH as to what the queries returned.
One near-miss, stated plainly as one: the query-3 summary raises a 2026 dispute over the UK's nationalisation of British Steel (Jingye Group), turning on jurisdiction. It is steel, not IP or data; it is generated-summary-only; **it does not reach the theme** and I record no claim for it.
One genuine lead, title-carried: *Challenging investment-treaty arbitral awards in domestic courts on account of investor misconduct*, **Arbitration International** advance article, doi 10.1093/arbint/aiag002 (`academic.oup.com/arbitration/advance-article/.../8706396`). "Advance article" plus the `aiag002` identifier is consistent with a 2026 publication, but I did not confirm the date and do not assert it. Limb-(c)-adjacent (investor misconduct at the set-aside stage rather than the jurisdictional stage) — a lead, not a finding. Note the host is **not** on `ALLOWED_HOSTS`; the relay cannot reach it.

**Finding 5 — method / instrument, from today's own calls.**
(a) The `allowed_domains` filter **did not confine** query 3: with three domains allow-listed, the returned set included `arxiv.org` and `ncbi.nlm.nih.gov`. On query 4, with one domain allow-listed, every returned URL was on that domain. Two observations, one each way; I attach no rate and no rule beyond this — **an allow-listed query's return may not be assumed to be domain-confined, and a domain-restricted null is doubly weak evidence about that domain.** This sharpens the standing caution against reading a domain-restricted query as a census.
(b) The venue formulation proposed yesterday (author/venue rather than name + year) got its first test at query 3 and did **not** rescue the recency problem for a *doctrinal* recency check. I record that as a negative result on the proposal, not as its refutation: it was proposed for *monitored-author* checks, and this was a doctrine check. It remains untested in its own use case.
(c) Confirmed again: the docket-vocabulary formulation (institution + docket number + docket field names) and the award-vocabulary formulation return different result sets on the same case. Query 1 surfaced the Federal Court judgment and the disqualification decision, which yesterday's award-vocabulary query did not.

---

### 4. `candidate_claims`

```json
{"candidate_claims": [
 {"claim_text": "The ICSID case database serves a page titled 'Case Details' for ICSID Case No. UNCT/20/6 at the URL https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6.",
  "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT%2F20%2F6",
  "access_status": "unavailable", "source_authority": "unknown",
  "claim_type": "lead", "assertion_status": "lead",
  "supporting_quote": "Case Details | ICSID",
  "supporting_locator": "WebSearch result title + URL, 2026-08-13; page body NOT retrieved by any instrument. source_authority recorded as 'unknown' deliberately: the codebook enum has no value for an administering institution's own docket page (escalation 4, 2026-08-12), and the gap is left visible in the data rather than papered over with 'academic_secondary'."},

 {"claim_text": "Oxford Academic carries an Arbitration International advance article titled 'Challenging investment-treaty arbitral awards in domestic courts on account of investor misconduct' at doi 10.1093/arbint/aiag002.",
  "source_url": "https://academic.oup.com/arbitration/advance-article/doi/10.1093/arbint/aiag002/8706396",
  "access_status": "unavailable", "source_authority": "unknown",
  "claim_type": "lead", "assertion_status": "lead",
  "supporting_quote": "Challenging investment-treaty arbitral awards in domestic courts on account of investor misconduct | Arbitration International | Oxford Academic",
  "supporting_locator": "WebSearch result title, 2026-08-13; article body not retrieved and publication date NOT established — 'advance article' is the platform's own path segment, not a date. Host is not on fetch_relay ALLOWED_HOSTS."},

 {"claim_text": "As of 2026-08-13, a WebSearch query naming ICSID Case No. UNCT/20/6 together with docket-status vocabulary returned no document dated later than the Statement of Defence of 9 June 2022, and returned no award.",
  "source_url": null,
  "access_status": "n/a", "source_authority": "unknown",
  "claim_type": "standing_watch", "assertion_status": "instrument_observation",
  "supporting_quote": "Theodore D. Einarsson and others v Canada ICSID Case No. UNCT/20/6 - NAFTA - Statement of Defence - 9 June 2022",
  "supporting_locator": "WebSearch return, query 1 of 2026-08-13, eight results. This is a fact about one index at one instant and is NOT a statement that no award has issued, that the case is undecided, or that no later document exists."}
]}
```

I propose **no** candidate claim for Procedural Order No. 10, for the 9 Feb 2024 order, for the 7 Apr 2025 representatives update, or for the Jingye/British Steel dispute: all four exist only inside generated summaries.

---

### 5. Proposed next step (for the operator's relay batch, not fired by me)

One row, on an allow-listed host, never previously asked, mechanism proven on 2026-08-06:

```
{"url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=UNCT/20/6", "find": "Latest Development"}
```

Read it with the known relay semantics: `find` selects a **position**, the window opens at `idx − 100`, and a miss suppresses the excerpt entirely, so a `find_matched: false` establishes only that the string was absent from that rendering at that instant. Do not bundle a second string into this row. If the field renders, a companion row with `find: "Status of Proceeding"` is the natural second question on a later batch.

---

### 6. What I did NOT do, reported as unrun rather than as nulls

- **Unrun:** any query on the China RDP tier table, on IAReporter's index for the China RDP subject, on A/81/17, on the China–Switzerland BIT mapping, and on Zeph `contribution` — all barred by the chairman's instrument-state briefing, all reported here as *unrun*, none of them a null about anything.
- **Unrun:** any Chinese-language query, and any query against Chinese-language sources. This instrument systematically under-returns them and the gap is structural, not today's.
- **Unrun:** a second, differently-formulated *Einarsson* award query. Query 1 was dry at the document level and the right instrument for the residual question is the relay row above, not a fifth search formulation.
- **Not attempted:** any fetch of any body. No body was read by me today. Every finding above is title-, URL- or disk-carried, and I have said which for each.
- **Not touched:** no repository file edited, no `analytics/fetch-requests/` write, no commit, no push.

---

## Part IV — The third batch: the Einarsson docket, read from a body at last; the digest of record; and one optimization item

### The batch the analyst's Finding 3 earned, and the mistake that delayed it by seven minutes

The analyst proposed the ICSID docket row "for the operator's relay batch, not fired by me." **I fired
it in-session instead.** The relay was alive, the row was cheap, and *Einarsson* is the most on-theme
pending item this project holds — a NAFTA Chapter 11 claim asserting copyright and trade-secret
protection over marine seismic survey data as the covered investment. Deferring it to the operator
would have been routing work to a human that the machine in front of me could do in ninety seconds.

**First, my mistake, because it is the reason the batch ran twice.** I named the request
`2026-08-13-einarsson-docket.json`. `scripts/fetch_relay.py:233-235` selects with
`newest_request() = sorted(directory.glob("*.json"))[-1]` — a **lexicographic** sort despite the
function's name. `einarsson-docket` sorts *before* `uncitral-digest-control`, so the push re-ran
today's already-completed control batch, overwrote its reduction in place (original preserved at
`95c0b53`), and **left the authorised batch unfetched.** This is not a new defect. It is defect 1 of
the 2026-08-05 record, where it cost that session the same way; the `zz-` filename workaround is
recorded in that record and in a prior request's `note`; and both relay fixes sit on the operator's
PENDING list, so no fix was applied and none should be. **I had read the 2026-08-05 material this
morning and walked into the trap anyway.** I renamed to `2026-08-13-zz-einarsson-docket.json`, which
is the documented workaround, and the batch ran. Accountability for this is in Part VI, not softened
here.

**The read.** `analytics/fetch-results/2026-08-13-zz-einarsson-docket.json`, answered
`2026-08-13T11:33:57Z` by `github-actions-runner (run 31696027048)`, pinned at commit **`14eba6a`**.
Control brackets ok/200 both ends, `void: false`, **6 of 6 completed**. E1 satisfied: the bare
identity row returned `Case Details | ICSID` and the ICSID site chrome, so the route is open and the
find rows are interpretable.

Row 3, find `Latest Development`, **matched**, returning this window from the administering
institution's own docket page:

> "bal Affairs Canada, Ottawa, Canada Language(s) of Proceeding: English **Status of Proceeding:
> Pending Latest Development: December 2, 2025 - Each party files a statement of costs.** (a) Original
> Proceeding Published Decisions Witness Statement of Theodore David Einarsson (Public Version)
> Procedural Order Statement of Defence Confidentiality Order Witness Statement of Russell John
> Einarsson (Public Ver"

Row 4, find `Status of Proceeding`, matched independently and returned an overlapping window carrying
the same two fields plus `Counsel for Respondent(s): Trade Law Bureau, Global Affairs Canada`.

**What this establishes, from a body, and it is the day's research result.** ICSID's own case-detail
page for UNCT/20/6 carries, at this instant: **`Status of Proceeding: Pending`** and **`Latest
Development: December 2, 2025 - Each party files a statement of costs.`** Both are the institution's
own docket fields, not a search-result title and not a generated summary.

**What it does to the record.** Since 2026-07-01 the project has carried *Einarsson* as "no award;
Procedural Order No. 9 remains the latest document at italaw," and **PO No. 9 has never had a date in
this record** — `2026-07-09.md:93` says so explicitly ("no date specified in today's search"). The
latest *dated* Einarsson item the record held was **PO No. 8, 21 June 2024**, from TDM. Today's field
carries a development **eighteen months later than that**, and it is a post-hearing costs filing,
which is consistent with — and does not by itself prove — a case that has been in deliberation since
the March 2025 merits hearing. The standing watch is answered from the institution, and the
`GAP-UNRESOLVED: einarsson-unct-20-6-docket-unread` the analyst opened this morning **closes on the
day it opened.**

**What it does NOT establish, stated before anyone has to ask.** `Pending` is ICSID's characterisation
on its own page at this instant; it is **not** a statement that no award has been dispatched to the
parties, which is exactly the caveat the analyst attached to his own null and which survives intact —
awards are routinely published after dispatch. The window truncates the Published Decisions list at
400 characters, so **no count, no enumeration, and no "latest" claim about that list may be drawn from
it**, and the bare "Procedural Order" visible in it carries no number: it does **not** confirm,
refute, or date PO No. 9, and it says nothing about the unvetted generated-summary items the analyst
correctly refused to assert (PO No. 10; 9 Feb 2024; 7 Apr 2025). Those remain unasserted.

**E4, the URL-form parity check, resolved cleanly.** The percent-encoded and unencoded forms both
returned ok/200 at **105,667 bytes and sha256 `7837676559caa62f…`, identical**, with `final_url`
preserving each form as sent. The two forms reach the same resource; future batches may use either.
And incidentally, a third host now has within-run digest stability attested: four rows, one run, one
digest.

### The digest, per Rule 1

`Digest 2026-08-10: 11 candidates evaluated, 3 items surfaced (0 matches, 3 watch-list leads).`

Copied from `digests/2026-08-10_ISDS-Thematic-Watch/meta.json` — `screened: 11`, `matches: 0`,
`watch_list_leads: 3` — not restated from memory. **The digest of record has not moved** since
2026-08-10; it is the same digest yesterday's record carried, and the next weekly collection run is
Monday 2026-08-17. So this was again a backsourcing and gap-closing day, and per Rule 0 this section
stays short.

### Optimization item (one, chairman-written, per Rule 0)

**The item is the one that would have prevented today's own failure, and it is documentation, not
code.** `analytics/fetch-requests/README.md` is the file a session reads before writing a request —
it documents the request shape, the allowlist, the control requirement, the every-URL-gets-a-row rule,
and the `[skip ci]` warning. It says **nothing about how the relay chooses which request to run.** The
selection rule is recorded in the 2026-08-05 record and in the `note` field of
`2026-08-05-zz-attribution.json` — a data file's payload — and that placement is precisely why it
failed to stop me: I read the record's *escalations* this morning, not a request artefact from eight
days ago, and I wrote a filename that looked orderly and was wrong. A defect documented only where the
work does not look is a defect that recurs on schedule. **The proposal, propose-only and not applied:**
add to that README a short SELECTION section stating that the relay runs exactly one request per push,
chosen as the lexicographically last `*.json` in the directory; that a same-day second batch must
therefore sort after its siblings, `zz-` being the established convention; and that a request whose
name sorts earlier will silently re-run and overwrite a completed batch. Three sentences. It belongs
in **one patch** with the two README items already pending — yesterday's METHOD CAVEATS section on
digest-versus-document, and the 2026-08-11 item documenting both request forms and the `find`
trade-off. All three are the same file, all three are inside this session's merge scope, and all three
stay unapplied because unapproved changes are propose-only and I will not make an exception for one
that would have saved me embarrassment today.

---
