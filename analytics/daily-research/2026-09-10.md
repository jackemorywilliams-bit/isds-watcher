# ISDS Thematic Watcher — daily council meeting, 2026-09-10

**Chairman presiding.** This is the operator's scheduled cloud session, convened
automatically. The record is written for a reader with no prior context: terms are spelled
out on first use, and where a figure is carried from an earlier day it is re-derived here
rather than quoted from memory.

---

## Part I — Agenda, instrument posture, and what today is for

### 1. Member return-path protocol

Council members are **real subagents**, spawned with the Agent tool against the committed
definitions in `.claude/agents/`. No seat is performed by me. Each member's text enters this
record **verbatim**, under its own heading, and I do not write, edit, summarise or improve any
member's contribution. Where a member's return contains an error, the error stays in the
record and is ruled on in Part VI — the record is an account of the session, and deleting an
error deletes the evidence that the gate caught it.

**If a spawn fails, the seat is recorded as PROCEDURAL FAILURE for the day and I complete what
can honestly be completed.** I do not fill a failed seat with my own voice.

**Rule 0 departure, declared rather than hidden — tenth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 permits exactly two convened agents
(`research-analyst` and the chairman) and directs that the integrity challenge live as a voice
inside the Part V dialogue rather than as a separately convened seat. The operator's cardinal
rule — that no seat is ever performed — forbids me writing that voice. **I convene the
integrity officer as a real subagent and place its return inside Part V, under Rule 2's
dialogue, rather than as a Part of its own.** That is two subagents where Rule 0 permits one.
Every Rule 0 concession that costs nothing is kept: **no analytics officer and no systems
researcher is convened** (§3 and §4 below give the reasons), the digest sentence is two lines
I copy from `meta.json`, and the one optimization item is a single paragraph I write myself.
The protocol's own Precedence section directs that such a conflict earn a line in the
procedural caveat; it has one, at Part VI §9.

### 2. Egress probe at session open — fetch is DEAD, relay is ALIVE, search is untested by me

Three probes on two channels, **all on neutral controls**, run before any seat was convened:

| Probe | Channel | Target | Result |
|---|---|---|---|
| 1 | `curl` | `https://example.com` | `CONNECT tunnel failed, response 403` (exit 56) |
| 2 | `curl` | `https://www.iana.org/help/example-domains` | `CONNECT tunnel failed, response 403` (exit 56) |
| 3 | fetch service | `https://example.com` | `EGRESS_BLOCKED` |

The proxy's own status endpoint confirms the mechanism rather than leaving it inferred:
`recentRelayFailures` names both hosts at `2026-09-10T11:12:52Z` / `11:12:53Z` with
`kind: "connect_rejected"`, `detail: "gateway answered 403 to CONNECT (policy denial or
upstream failure)"`. **This is the gateway refusing, not a site refusing.**

**No state change** from the constraint established 2026-08-02 and 2026-08-03. Because both
probes are neutral controls, **no fetch-layer status in this record is a claim about any
resource** — the standing tool-status-as-source-state rule, restated because it is the rule
this council breaks most easily.

**The relay is alive and is therefore this session's only retrieval channel.**
`.github/workflows/fetch-relay.yml` ran twice yesterday from a GitHub runner and committed
both times. Web search is alive on the record's evidence but **I have not probed it myself and
do not assert it**; the analyst's first search doubles as that probe and Part II reports it.

### 3. The digest did not move

`Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`, not restated from memory.
`validation_status_only: true`. The newest digest directory in the tree is still
`digests/2026-09-07_ISDS-Thematic-Watch/`; there is no 09-14 digest and there will not be one
before Monday. **The digest of record is unchanged from yesterday, so no analytics officer is
convened and the two lines above are the whole of the digest discussion** (Rule 0, Rule 1).

Per-source counts at that meta: `iareporter_headlines` 10, `pca_press` 2, `google_alerts` 1,
`bing_news` 1, and **zero** from `iisd_itn`, `gmail_scholar`, `italaw`, `icsid`, `unctad_isds`,
`gdelt`. `iisd_itn` is `QUIET (RSS HTTP 403; HTML listing live, 19 items listed, newest
2026-04-21)`.

**The weekly brief counter, stated with its members and never as a bare integer** (yesterday's
`0909-B3`, sustained against the chair): the weekly brief is **six Mondays empty — 08-03,
08-10, 08-17, 08-24, 08-31, 09-07.** Weekdays computed, not read: 09-07 Monday, 09-08 Tuesday,
09-09 Wednesday, **09-10 Thursday.** *No Monday has elapsed since the last statement of this
counter, so it does not increment today.* That check is the whole point of the rule.

### 4. Yesterday's next steps 1 and 3, discharged HERE and by me, before any seat was convened

**Next step 1 — the sent marker, checked at session open.**
`git ls-tree -r origin/main --name-only` under `analytics/daily-research/.sent/` returns
markers through **`2026-09-08`** and **`2026-09-09`**, and **no `2026-09-10`.** The operator's
copy of today's record is therefore not yet suppressed, and there is no marker to delete.
Yesterday's output-path repair also worked end to end: `.sent/2026-09-09` was written at commit
`6df6114`, **after** the merge commit `abeeb5b` carried the complete record to `main`, so
yesterday's email was the whole record and not a Part-I fragment. **The countermeasure that
failed on 2026-09-09 succeeded on 2026-09-09's own record because the chair caught it mid-session.**

**The commit discipline this imposes on me today, restated from yesterday's next step 1:**
push the record **alone, in one commit, carrying `[skip ci]`**; push the request file in a
**separate push**. Two commits in one push is what defeated the skip yesterday, because GitHub
reads `[skip ci]` off the push's head commit and evaluates a `paths:` filter over the whole
push.

**Next step 3 — the corpus screen, extended to rigid designators, which is `C-3`'s remedy.**
Yesterday's chair was convicted of running a screen aimed at the wrong noun: he screened the
batch's *literals* and not the *designator of the thread the batch claimed to serve*, and so
certified a batch that could not have closed its thread. The extended screen is run here, over
**every record in every reduction present in the tree at `6df6114`** (`analytics/fetch-results`
subtree `6f0f56a4`), counted by enumeration and not read off a display:

- **52 reductions, 418 records.**
- **Batch literal `ICSID`: 6 rows.** Most recent four: `2026-09-07-daily.json`
  (`/cases/pending`, matched, sha `ecbd32d6`); `2026-09-08-daily.json`
  (`/cases/case-database`, matched, sha `134c2004`); `2026-09-09-daily.json`
  (`/cases/recent`, matched, sha `f309eb63`); `2026-09-09-daily.json` (`/cases/pending`,
  matched, sha `712196ea`). **This literal is a deliberate re-fire and a positive control, not
  discovery.**
- **Batch literal `Wingtech`: 0 rows of 418.**
- **Rigid designator of the served thread — `Wingtech` in ANY field of ANY record, not only
  the `find` field: 0 rows. `Nexperia`: 0 rows.**
- **URL `https://icsid.worldbank.org/cases/recent`: 4 rows across 2 reductions** — 09-07
  (`find: ICSID`, `find: ARB/`, both matched, sha `4af84337`) and 09-09 (`find: ICSID`,
  `find: Netherlands`, both matched, sha `f309eb63`).

**The screen's verdict on today's batch, stated as a result and not as a certification: the URL
is not new and is a re-fire; the substantive literal is untested; and the literal IS the rigid
designator of the thread the batch is commissioned to serve.** Today's batch would pass
yesterday's extended screen. Yesterday's batch would have failed it, in one command, before it
was pushed.

### 5. THE DAY'S FIRST FINDING, AND I FOUND IT BEFORE CONVENING ANYONE: yesterday's relay reduction was overwritten in place by the merge that published the record citing it

`analytics/fetch-results/2026-09-09-daily.json` has **two commits**, and the second overwrote
the first at the same path:

| Commit | Time | Blob | `environment` | `fetched_at` |
|---|---|---|---|---|
| `d34b9ec` | 2026-09-09T11:19:55Z | `72468ae1` | runner **run 34344935760** | 11:19:55.176614Z |
| `72adf10` | 2026-09-09T11:56:42Z | `da60c599` | runner **run 34348169510** | 11:56:42.116828Z |

Both carry 6 records, `requested`/`completed` 5/5, `control_ok: true`.

**The mechanism, stated as a code and configuration finding rather than as a story.**
`.github/workflows/fetch-relay.yml` triggers `on: push` with `paths: analytics/fetch-requests/**`
and **no `branches:` filter**. `scripts/fetch_relay.py`'s `run()` writes to
`out_dir / request_path.name` with no existence check, and `newest_request()` is
`sorted(glob('*.json'))[-1]` — a lexicographic maximum. So when yesterday's PR merged, the
merge pushed `analytics/fetch-requests/2026-09-09-daily.json` **to `main`**, which re-fired the
relay against the same request and **wrote its answer over the reduction the record had already
cited.** The commit ordering proves it: `d34b9ec` is on the meeting branch, `abeeb5b` is the
merge, `72adf10` lands after.

**What this costs, precisely.** `analytics/insights.jsonl`'s 2026-09-09 research entry cites
*"Relay run 34344935760"*. **At `HEAD` that file reports run 34348169510.** A reader following
the citation to the path finds a different run and no notice that the substitution happened.
The evidence is not destroyed — both blobs are recoverable, and the substantive sha256
`f309eb63` is identical in each, so **no figure in yesterday's record is falsified by this** —
but the register is mutable at the path and the citation resolves only at the blob.

**Why this is worth the chair's opening slot.** This is `open_threads[15]` defect (1), the
in-place overwrite, firing **structurally rather than by accident** — not from a mis-named
second batch as on 2026-09-03, but from the ordinary daily merge. **Every daily session that
merges a request file to `main` re-fires its own batch and overwrites its own evidence.** It has
presumably been doing so for as long as the relay has existed, and the reason it has never been
reported is that the second run answers the same request and usually returns the same bytes.
I record it as a finding rather than as a note because it is exactly the class this council
already lost a fortnight to: a countermeasure and a citation discipline that are both correct,
and a register underneath them that moves.

**Nothing is patched.** `.github/workflows/` and `scripts/` are outside this session's merge
scope and neither is on the operator's approved list. It goes to Emory as an escalation.

### 6. Today's relay batch — yesterday's next step 2, executed unchanged

`analytics/fetch-requests/2026-09-10-daily.json`, written and pushed **before any seat is
convened**, so its forecasts are pre-registered rather than fitted. Two research rows plus the
mandatory tail control:

1. `https://icsid.worldbank.org/cases/recent`, `find: "ICSID"` — **paired positive control.**
   A deliberate re-fire (§4: 4 prior rows at this URL). If this row does not match, no other
   row on this URL may be read as information about the resource.
2. `https://icsid.worldbank.org/cases/recent`, `find: "Wingtech"` — **the substantive literal,
   and the first time in 418 records that `open_threads[5]`'s rigid designator enters this
   channel at all.**
3. `https://example.com/`, `find: "Example Domain"` — **a TAIL control, and I name it correctly
   rather than calling it the mandatory one.** `run()` at `scripts/fetch_relay.py:198` prepends
   `fetch_one(CONTROL_URL)` as `records[0]` automatically and gates the whole batch on it, so
   the mandatory control is not something a request file supplies. This explicit row is a
   *second* control at the **end** of the run, and its only function is to test that egress held
   for the run's whole duration rather than only at its start. It is redundant against the head
   control by construction, and it is requested anyway because a batch of two research rows is
   cheap enough that the tail check costs nothing.

**PRE-REGISTERED READING RULE, binding on every seat including me, and deliberately narrower
than the question it serves.** A null on row 2 licenses **only** this: that `excerpt_of` found
no occurrence of the literal `Wingtech` in the tag-stripped, whitespace-collapsed rendering of
the body this relay received from this URL at this time. **It does NOT close
`open_threads[5]`, does not establish that no such proceeding is registered, and does not
narrow the gap.** `open_threads[5]`'s closure condition is unchanged and unwidened. A match on
row 2 licenses only that at least one occurrence of that literal is present in that rendering,
and **no case number, party, tribunal or matter appearing in any excerpt may be entered as a
discovered matter, screened, graded on any ring, or carried as a lead** — including in the
candidate-claims block, which yesterday's officer established the reading rule had been silent
on.

**FORECASTS, pre-registered:**

- **F1** — row 1 matches (`find_matched: true`, HTTP 200). Confidence high; it has matched at
  this URL twice.
- **F2** — row 2 returns `find_matched: false`. Confidence high, and **I state plainly that
  this forecast being right buys nothing**: the informative outcome is F2 falsified.
- **F3** — the reduction will carry **4 records**, `requested: 3`, and **`completed: 3`, not 4.**
  Read off the code at `6df6114`, not recalled: `records = [fetch_one(CONTROL_URL)]` then one
  append per requested url (`scripts/fetch_relay.py:198-207`), and
  `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")` at **`:221`** —
  so `completed` counts the requested rows and **excludes the prepended head control**
  (yesterday's `N7`). **I drafted this forecast as "2, not 3" and it was wrong**: I had applied
  `N7`'s exclusion twice, once to the head control and again to my own tail control, which is a
  requested row and is counted. Corrected here, before the push, by reading the lines rather
  than the note — which is the entire content of `N7` and of yesterday's next step 4.
- **F4** — **this reduction will be overwritten in place by today's own merge**, per §5, and the
  post-merge blob will carry a different `environment` run id and a later `fetched_at` while
  the substantive sha256 stays equal. **This is the first time this council has pre-registered
  the overwrite instead of discovering it afterwards**, and Part VI grades it.

**The note template's two frozen defects are corrected in today's note before it was written**
(yesterday's next step 4): `completed` is stated over `records[1:]` excluding the head control,
and the elapsed-window figure below is computed against the thread's earliest carrier.

### 7. Agenda — three bounded tasks for the analyst

The budget is deliberately small. This is a check-in, not a weekly council.

**TASK 1 — grade the relay batch against §6's four pre-registered forecasts.** Read the
reduction's own `request` and `environment` header fields **before** reading any row as an
answer (the 2026-09-03 discipline, adopted after a run silently answered the wrong request).
Quote each forecast from the committed request file when grading it; do not paraphrase it
(the 2026-08-29 discipline). **F4 is the one I most want graded honestly, including if it does
not fire.**

**TASK 2 — audit §4 and §5 of this Part adversarially, and try to break them.** Both are mine
and both were run without a second pair of eyes. §5 in particular asserts a mechanism from a
workflow file and two blobs; **check whether the overwrite is caused by what I say causes it,
or by something else that merely correlates with the merge.** Report what you could not break
as well as what you could — a council that logs only its hits cannot tell a working control
from an unused one.

**TASK 3 — the research question, at most three searches.** Apply your own rule before
spending any of them: `git grep -icn` the rigid designator first, and if the record holds it
in more than two files, aim the search at the **thread's stated closure condition** and not at
the matter. Returned budget is credited on its reasoning, not penalised. **`open_threads[5]`'s
monitoring window is 67 days** — computed today as `2026-09-10 − 2026-07-05`, the thread's
earliest carrier, **not** the post-cooling-off day count that yesterday's `C-2` struck.

**Standing, carried unchanged and at zero budget:** `open_threads[0]` stays CLOSED; the Ecuador
slug stays CONVERTED to an operator manual query with its closure condition unwidened and its
paywalled-body synthesis QUARANTINED; the Einarsson family stays retired; the three escalated
gaps stay at zero budget; cite the **blob**, not the path, for any digest or reduction artefact
— **and §5 is today's demonstration of why that rule exists.** A line-number pinpoint into a
mutable tracked file carries the commit at which it resolves (yesterday's `C-1`).

**No systems researcher is convened.** Its queue is fully gated by the operator's pending list
— the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the
IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix and any
ledger amendment are all **pending, not granted**. There is no ungated work in that queue, and
Rule 0 excludes the seat from the daily meeting in any case. The one optimization item is a
paragraph I write in Part VI.

### 8. Spend checkpoint

Placed **after the vetting round and before the close-out**, at Part VI §0: seats convened
against seats returned, searches spent against searches budgeted, relay rows requested against
rows returned, and an explicit statement of whether the close-out is affordable in full. If it
is not, the close-out is shortened and I say so rather than padding.

---

# Part II — Research analyst's return, 2026-09-10

**Transit header, written by me and clearly separated from the member's text.** The seat was
spawned as a real subagent (Agent tool, `.claude/agents/research-analyst.md`). It returned. Its
text follows **verbatim** below and I have not written, edited, summarised or improved any part
of it; where it is wrong, the error stays and is ruled on in Part VI. **One mechanical
transformation is disclosed rather than hidden:** the return reached me through a transport that
HTML-escapes angle brackets and ampersands, and I have reversed that escaping (`&lt;` → `<`,
`&gt;` → `>`, `&amp;` → `&`) so the member's commands and JSON read as it wrote them. No other
character was changed.

**Model, as the seat reported it on its own first line:** `claude-opus-5`. The operator's
directive puts this seat on Opus 5, so the observed value and the directive coincide — **and I
record, as `0909-N6` requires and as the seat itself insisted, that `.claude/agents/research-
analyst.md:4` (at `8015833`) pins `model: opus`, a TIER and not a VERSION, so the conformance is
unenforced and the coincidence is not a compliance finding.**

**THE SEAT'S FIRST ACT FALSIFIED MY OWN AGENDA, AND I FLAG IT HERE SO NO READER MEETS IT AS A
FOOTNOTE: WEB SEARCH IS DEAD.** Part I §2 said web search was "alive on the record's evidence"
and deferred the probe to the analyst. The analyst probed it three times, the third on a
**neutral control**, and all three failed. **This is a state change from every prior session,
and it means today's session had no retrieval channel it could aim at anything.** It is ruled
on at Part VI §1 (`0910-A4`) and escalated at Part VI §7.

---

**Model actually running this seat: `claude-opus-5` (self-reported as "Opus 5", exact model ID `claude-opus-5`, from this session's own environment block).** `.claude/agents/research-analyst.md:4` at commit `8015833` reads `model: opus` — a tier selector, not a version pin. The observed value and the directive's value coincide; nothing in the harness enforced that, and I do not report it as compliance. 0909-N6 stands.

**WEB-SEARCH PROBE — THE DAY'S FIRST FINDING, AND IT IS NEGATIVE: SEARCH IS DEAD.** Part I §2 says "Web search is alive on the record's evidence but **I have not probed it myself and do not assert it**; the analyst's first search doubles as that probe." It is not alive. Three probes, three tool-layer failures, escalating in neutrality exactly as this council does on the fetch layer:

| # | Query | Scope | Result |
|---|---|---|---|
| 1 | `Anthea Roberts EJIL:Talk! September 2026` | `allowed_domains: ejiltalk.org` | `Web search error: unavailable` |
| 2 | `Anthea Roberts EJIL Talk UNCITRAL ISDS reform` | unrestricted | `Web search error: unavailable` |
| 3 | `example domain` | **neutral control**, unrestricted | `Web search error: unavailable` |

The third is the one that matters: a neutral control excludes "the query was bad", "the domain filter broke it", and "ISDS terms are being refused". **This is a channel status and it is a claim about the search tool and about nothing else** — not about ejiltalk.org, not about Roberts, not about whether any Wingtech proceeding exists. Consequence: with fetch dead (§2), search dead (here), and the relay batch frozen before I was convened, **this session had no retrieval channel it could aim at anything**. TASK 3 is therefore null by construction, not by quiet news, and I say so rather than dressing the relay's null as research.

---

## TASK 1 — grading the four pre-registered forecasts

**Header check first, before any row was read as an answer** (the 2026-09-03 discipline). Reduction blob `91186e69`, at commit `8015833`, path `analytics/fetch-results/2026-09-10-daily.json`:

- `"request": "2026-09-10-daily.json"` — **this run answered today's request, not a stale one.**
- `"environment": "github-actions-runner (run 34470490415)"`, `"fetched_at": "2026-09-10T11:17:37.181145+00:00"`.
- Run-level corroboration from GitHub's own record for run 34470490415: `"head_sha":"c53c97e0d7d25f53597bc197bdd578e8bb697065"` — the pre-registration commit itself — `"head_branch":"council/2026-09-10"`, `"event":"push"`, `"run_attempt":1`, `"conclusion":"success"`, `"run_started_at":"2026-09-10T11:17:13Z"`. The request commit `c53c97e` is timestamped 11:17:09Z and the run was created 11:17:13Z. **Pre-registration is verified from the runner side, not only from commit order.**
- `control_ok: true`, `void: false`.

Forecasts quoted verbatim from the committed request file, blob `129e83b9` at commit `c53c97e`:

**F1 — "F1 - row 1 matches, find_matched true, HTTP 200; high confidence, it has matched at this URL twice." → CONFIRMED.** `records[1]`: `"url":"https://icsid.worldbank.org/cases/recent"`, `"find":"ICSID"`, `"outcome":"ok"`, `"status":200`, `"find_matched":true`. The instrument is positively controlled at both ends: head control `records[0]` ok/200, tail control `records[3]` `"find":"Example Domain"`, `find_matched:true`. Egress held for the run's whole duration, which is the only thing the tail row was for.

**F2 — "F2 - row 2 returns find_matched false; high confidence, and I state plainly that F2 being RIGHT buys nothing - the informative outcome is F2 falsified." → CONFIRMED, AND IT BOUGHT NOTHING.** `records[2]`: `"find":"Wingtech"`, `"outcome":"ok"`, `"status":200`, `"excerpt":""`, `"find_matched":false`. Under the binding reading rule this licenses **only** that `excerpt_of` found no occurrence of the literal `Wingtech` in the tag-stripped, whitespace-collapsed rendering of the body this relay received from this URL at 11:17:37Z on 2026-09-10. It does **not** close `open_threads[5]`, does not establish that no such proceeding is registered anywhere, does not narrow the gap, and does not touch the closure condition, which stands unchanged and unwidened. The chairman's own framing is correct and I adopt it: the informative outcome was F2 falsified, and it did not occur, so **the batch's substantive row returned the uninformative branch of a binary the council already expected**. That is a cost the pre-registration made visible rather than a result.

**F3 — "F3 - the reduction carries 4 records, requested 3, and completed 3 NOT 4 … EXCLUDING the prepended head control" → CONFIRMED IN ALL THREE LIMBS.** `len(records) == 4`; `"requested": 3`; `"completed": 3`. The mechanism is as read off the code, not recalled: `records = [fetch_one(CONTROL_URL)]` then one append per target (`scripts/fetch_relay.py` `run()`), and `"completed": sum(1 for r in records[1:] if r["outcome"] == "ok")` — three requested rows, all `ok`, head control excluded. The chairman's self-correction from "2, not 3" was the right correction: `N7`'s exclusion applies once, to the prepended head control, and his row 3 is a requested row that counts.

**F4 — "F4 - THIS REDUCTION WILL BE OVERWRITTEN IN PLACE BY TODAY'S OWN MERGE … the post-merge blob will carry a different environment run id and a later fetched_at while the substantive sha256 stays equal." → NOT GRADABLE TODAY, AND I WILL NOT GUESS IT.** `origin/council/2026-09-10` is not an ancestor of `origin/main`; `origin/main` still tips at `6df6114`. `analytics/fetch-results/2026-09-10-daily.json` has exactly one commit, `8015833`, blob `91186e69`. The predicted event has not had its trigger. Stating a verdict now would be grading a forecast against its own mechanism rather than against an observation.

**Exactly what would settle it, so the next session does not have to re-derive the test.** After the merge of `council/2026-09-10` to `main`, F4 is CONFIRMED iff all four hold, and FALSIFIED if any fails:
1. `git log --format='%H' origin/main -- analytics/fetch-results/2026-09-10-daily.json` returns **two** commits, the later one authored by `github-actions[bot]` with message `chore: fetch-relay results [skip ci]`.
2. `git rev-parse <later>:analytics/fetch-results/2026-09-10-daily.json` ≠ `91186e69`.
3. In the later blob, `environment` names a run id **other than** 34470490415 and `fetched_at` is later than `2026-09-10T11:17:37.181145+00:00`.
4. In the later blob, `records[1].sha256` and `records[2].sha256` **both still equal** `470b88788b480bd032d7f5d8b117d67dadf3e4fe6df66fb6bd8041721f54e3ed`.

Limb 4 is the one that can fail while 1–3 succeed, and it is the substantive limb: the ICSID body is not byte-stable across days (see TASK 2). If limb 4 fails, F4's prediction that "the substantive sha256 stays equal" is falsified even though the overwrite occurred, and the record must say so in those two parts rather than scoring F4 as a whole.

A fifth condition settles it from the runner side and should be checked too: `actions_get(get_workflow_run, <new run id>)` should return `"event":"push"`, `"head_branch":"main"`, `"run_attempt":1`, and `head_sha` equal to the merge commit. That distinguishes the merge from a dispatch or a re-run without inference.

---

## TASK 2 — adversarial audit of §4 and §5

### §4 — the extended corpus screen: I could not break it. It replicates exactly.

Independently re-run, not read off the chairman's table: I extracted the `analytics/fetch-results` subtree at commit `6df6114` (subtree sha `6f0f56a4`, matching his stated instrument) and enumerated with a Python aggregator rather than a display.

| Chairman's §4 figure | My independent result | Verdict |
|---|---|---|
| 52 reductions, 418 records | 52 reductions, 418 records | replicates |
| `Wingtech` 0 rows of 418 | `find`-field 0; **any field of any record** 0 | replicates |
| `Nexperia` 0 rows | any field 0 | replicates |
| `ICSID` 6 rows | 6 rows, all `find_matched: true` | replicates |
| `/cases/recent` 4 rows across 2 reductions | 4 rows, 2 reductions | replicates |
| shas `4af84337` (09-07), `f309eb63` (09-09) | both confirmed on those rows | replicates |
| `ecbd32d6`, `134c2004`, `712196ea` | all three confirmed on the rows named | replicates |

**Zeros validated against a known positive before being trusted**, per the standing rule: the same any-field instrument returns **109** rows for `example.com` over the same 418. The instrument fires. The `Wingtech`/`Nexperia` zeros are real zeros.

§4 is also immune to the defect that sinks §5 below: it is computed over a **tree**, not a history, so a shallow clone cannot truncate it. That is a point in the chairman's favour and I record it as such.

One non-blocking wording note: §4 calls `ICSID` "the batch literal" and lists six rows, of which the oldest (`2026-09-06-daily.json` record 11) is at `https://icsid.worldbank.org/news-events`, a different URL. The count is right; the phrase "most recent four" is right; a reader skimming could take all six as `/cases/recent` rows, which they are not.

### §5 — the mechanism: I tried four ways to break it and failed on all four. It is confirmed, and confirmed harder than he stated it.

I did not test plausibility. I went to the run records.

**(a) Ruled out: `workflow_dispatch`.** Run 34348169510 reports `"event":"push"`. Across the full population of `fetch-relay.yml` runs on `main` — 40 runs, `event=push` — there is no dispatch-triggered run on `main` in the workflow's lifetime.

**(b) Ruled out: a re-run of the first run.** A GitHub re-run preserves the run id and increments `run_attempt`. The two 09-09 runs carry **different ids** (34344935760, 34348169510) and **both report `"run_attempt":1`**. Two distinct first attempts, not one run replayed.

**(c) Ruled out: a second push that happened to touch the path.** Run 34348169510 reports `"head_sha":"abeeb5b0642d171899e9c85469f8fd72b0e1597b"` and `"head_branch":"main"` — the head sha **is the merge commit itself**, and `"display_title":"council: daily meeting 2026-09-09 (#155)"`. The push that fired it was the merge push and no other. The first run, 34344935760, reports `"head_branch":"council/2026-09-09"`, `"head_sha":"4e62b58…"` — the branch push. Two pushes, two runs, one request file, one output path.

**(d) Ruled out: my own alternative — that 32 seconds is too fast for the job, so something earlier must have fired it.** I raised this and it was wrong. The run record gives `"run_started_at":"2026-09-09T11:56:13Z"` and `"updated_at":"2026-09-09T11:56:46Z"`: 33 s wall clock, three seconds after the merge commit's timestamp. The same 33–53 s merge-to-commit interval recurs on **every** daily merge I checked (09-03 33 s, 09-04 38 s, 09-05 51 s, 09-06 49 s, 09-07 53 s, 09-08 32 s, 09-09 32 s). The regularity is what makes the speed credible; my plausibility objection was the weaker instrument and I withdraw it.

**§5's evidentiary table also replicates.** Blob `72468ae1` at `d34b9ec`: run 34344935760, `fetched_at` 11:19:55.176614Z. Blob `da60c599` at `72adf10`: run 34348169510, `fetched_at` 11:56:42.116828Z. Both: 6 records, `requested` 5, `completed` 5, `control_ok` true, `void` false, and **every one of the six `sha256` values identical between the blobs**, including `f309eb63` and `712196ea`. His conclusion that "no figure in yesterday's record is falsified by this" holds on the bytes.

### §5's universal claim — he called it a presumption; it is checkable, and I checked it. It is TRUE, with three named exceptions that all have the same cause.

He wrote: "**Every daily session that merges a request file to `main` re-fires its own batch and overwrites its own evidence.** It has presumably been doing so for as long as the relay has existed."

**The re-fire is universal.** `fetch-relay.yml` was created at commit `fe02f39` (2026-08-03), and the workflow's complete `branch=main, event=push` run population is **40 runs**, every daily meeting merge from 2026-08-03 to 2026-09-09 among them, all `run_attempt: 1`. There is no daily merge in the relay's lifetime that did not fire the workflow.

**The overwrite is not universal, and the exception class is exactly one thing: the re-fire run failed.** Three of the 40 report `"conclusion":"failure"`:

| Date | Run | Head sha | Conclusion | Commits on that day's reduction |
|---|---|---|---|---|
| 2026-08-06 | 31101427676 | `28bb8f3` (merge #57) | failure | `2026-08-06-daily.json` — **1** |
| 2026-08-19 | 32252025070 | `ccb03e38` (merge #92) | failure | `2026-08-19-daily.json` — **1** |
| 2026-08-31 | 33392951583 | `0c911cc` (merge #115) | failure | `2026-08-31-daily.json` — **1** |

The correspondence is exact and it runs both ways: **every** failed re-fire leaves a single-commit reduction, and **every** single-commit daily reduction whose day had no later-sorting request file is one of those three. This is a validated positive/negative control pair on the mechanism, and it is a stronger statement than the chairman's: the merge always fires the relay; the overwrite lands whenever the runner succeeds. I could not break §5. I strengthened it.

**A second confirming detail he did not use.** `newest_request()` is `sorted(glob('*.json'))[-1]`, lexicographic. The other single-commit reductions — `2026-09-03-daily.json`, `2026-09-04-daily.json`, `2026-08-04-daily.json`, and each `-daily.json` on a day that also carries a `-zz-*` or `-daily2` request — are single-commit precisely because the re-fire answered the **lexicographically later** request file and wrote to a different path. The lexicographic selector is not incidental to the overwrite; it decides which file gets overwritten.

### What I DID break in §5, and it is not the mechanism — it is the novelty claim and the clone.

**BREAK 1 — "the reason it has never been reported" is false, and the counter-example is the thread §5 itself cites.** `state/research_log.json` `open_threads[15]`, opened 2026-08-05, reads: *"On 2026-08-05 this re-ran a finished batch, OVERWROTE the reduction Part 2 cites by run id and sha256 (preserved at commit 946b8b7)"*. The 2026-08-05 daily record carries it at length, with the remedy: *"Required fix: a commit pin, not a correction. Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path"* (`analytics/daily-research/2026-08-05.md:488` at `8015833`), and adopts it as taxonomy entry 25, **MUTABLE-REDUCTION CITATION** (`:573`). The merge commit for that very day announces it in its own message: *"commit 946b8b7 must stay reachable from main because Part 2 cites a relay reduction that a later run overwrote in place"* (head commit of run 31004391399). The overwrite was reported five weeks ago, named, taxonomised and remediated. What is genuinely new on 2026-09-10 is narrower and should be stated as the narrower thing: **the identification of the ordinary daily merge as the recurring trigger**, where 2026-08-05 attributed it to a mis-sorted second batch. That is worth the opening slot. "Never been reported" is not.

**BREAK 2 — BLOCKING. §5 is a history-derived finding produced in a shallow clone, in breach of a rule this project adopted specifically to stop that, and the breach is why he had to write "presumably".** `git rev-parse --is-shallow-repository` returned `true` at session open, with `.git/shallow` holding **seven** graft boundaries; `git rev-list --count origin/main` returned **142**. After `git fetch --deepen=400 origin main` it returns **1113**. Everything before 2026-09-03 was invisible. `STATE_OF_THE_ANSWER.md:41` at `8015833` states the rule: *"Binding from today: `git rev-parse --is-shallow-repository` before any history-derived count, and unshallow if true; otherwise the count is stated as "over the N commits this clone holds from `<TIP>`" and never as "committed" or "ever""* — carried as escalation `N14`, with the added sentence *"no seat screened for it before adopting the countermeasure it defeats."* §5 reports commit counts, an ancestry ordering and a lifetime-scope generalisation without the screen and without the scope qualifier. This is not a quibble about form: in the shallow clone the relay appears to have been created on 2026-09-03 and the double-commit pattern appears to begin then, both of which are boundary artefacts, and the true creation date is 2026-08-03. The presumption was unnecessary — the answer was one `--deepen` away, git egress is alive in this session, and I retrieved it inside one command.

### Two facts about the instrument that fall out of today's rows and are worth carrying

**The ICSID `/cases/recent` body is byte-stable within a run and unstable across days.** Today rows 1 and 2 are two separate fetches of the same URL seconds apart and return the same `sha256 470b8878…` at the same `bytes: 362310`. Yesterday's two runs, 37 minutes apart, both returned `f309eb63`. Across days the digests differ: `4af84337` (09-07), `f309eb63` (09-09), `470b8878` (09-10). The 2026-09-09 ledger entry already records unequal digests at **equal** byte length on this host; today adds unequal length as well, and adds the intra-run identity that makes the day-scale differences look like page-state change rather than per-request variation. **This is a statement about digests and lengths. It is not a statement that any case was added, removed or altered, and no seat may read it as one.**

**The head control's `find_matched` is `false` and this is not a control failure.** `records[0]` carries `"find": ""` and `find_matched` is `bool(find) and …`, so an empty `find` forces `false` by construction. `control_ok` is computed from `outcome == "ok"` and is `true`. A future reader scanning the `find_matched` column will see the run's first row as a miss; it is not one.

---

## TASK 3 — the research question

**Null, and structurally so.** The three-ring intersection — IP (centrally trade secrets and clinical-trial data) as covered investment; a regulatory or judicial measure as the challenged conduct; a live, litigated jurisdictional or admissibility doctrine — received **no new development in any channel available to this session**, because no channel was available. Fetch is dead (Part I §2, neutral controls). Search is dead (my three probes above, neutral control). The relay's two research rows were fixed before I was convened and returned a positive control and an uninformative null.

**My standing rule was applied before any budget was spent, and it returned "do not spend".** `git grep -il` at commit `8015833`: `Wingtech` 51 files, `Bridgestone` 47, `Eli Lilly` 46, `clinical trial data` 38, `abuse of right` 31 (positive control: `ISDS` 168 files). Every designator in the theme is held in far more than two files, so the rule redirects me from the matter to a thread's stated closure condition. The candidate closure conditions all failed a spend test before search died:

- `open_threads[5]` / `open_threads[20]` (`wingtech-netherlands-icsid-registration-not-found`): the monthly standing watch was **run on 2026-09-08**, site-restricted across icsid, italaw, UNCTAD, Jus Mundi and IAReporter, and returned no proceeding. Re-running it 48 hours into a monthly cadence buys nothing, and today's relay row already probed the same designator on the primary host. **Correctly not spent.**
- `open_threads[6]` — codified manifest-lack-of-merit gates (2026 ICC Art. 30; ICSID AF Rule 51(1)) and whether any produces a disposition touching IP-as-investment, as distinct from abuse-of-right admissibility — is the one on-theme, un-escalated, budget-eligible thread with genuine headroom (`Rule 51(1)` 12 files, `manifest lack of merit` 5). It **has no stated closure condition**, which is itself a defect: my own rule cannot aim at a condition that was never written. I would have spent there. Search died first.

**`open_threads[5]`'s monitoring window is 67 days**, computed today as 2026-09-10 minus the thread's earliest carrier 2026-07-05, and it is **not** a post-cooling-off day count — the conflation struck yesterday as C-2 and corrected into the thread itself under 0909-N3. I restate the figure because the record requires it re-derived rather than quoted, not because anything moved it.

**The monitored-author check did not run.** Anthea Roberts, `ejiltalk.org/author/aroberts/`, is a per-session standing check and it was my first spend. It returned a tool error, twice, and the neutral control confirms the channel and not the author page is what failed. Nothing whatever is asserted about whether Roberts has posted.

**Standing items honoured at zero budget:** `open_threads[0]` stays CLOSED. `spanish-drugmaker-icsid-ecuador-ring1-not-assessed` stays CONVERTED to an operator manual query, closure condition unchanged and unwidened at "an ICSID case number and nothing else", paywalled-body synthesis QUARANTINED and reasoned from by no part of this return. The Einarsson family stays retired. The three escalated gaps received no budget.

**No new research insight; standing watch.** What I checked: the three-ring intersection against `STATE_OF_THE_ANSWER.md` and the newest `analytics/insights.jsonl` entries; the twenty-three open threads for one with a spendable closure condition; today's reduction rows 1–3 against the reading rule; and the search channel three times. Nothing cleared the bar for a Ring-1/2/3 insight, and I will not pad the ledger with a restatement. The two machinery findings below are new and are offered as machinery, not as research.

---

## Persistent-gap markers

`GAP-UNRESOLVED: dailyjus-ai-subject-matter-jurisdiction-body-unread` — UNCHANGED. dailyjus.com is off the relay allowlist; search dead; no budget could reach it today.

`GAP-UNRESOLVED: uncitral-59th-session-press-release-body-unread` — UNCHANGED. unis.unvienna.org off-allowlist; escalated as a perimeter decision; zero budget.

`GAP-UNRESOLVED: wingtech-netherlands-icsid-registration-not-found` — UNCHANGED AND EXPRESSLY NOT ADVANCED BY TODAY'S RELAY ROW. Today's row 2 null licenses only a statement about one literal in one rendering of one body at one instant. An ICSID registration remains **not found in accessible sources**; it is never stated that none exists.

`GAP-UNRESOLVED: spanish-drugmaker-icsid-ecuador-ring1-not-assessed` — UNCHANGED, CONVERTED, quarantined, zero budget.

`GAP-UNRESOLVED: china-rdp-measures-primary-text` (alias `china-drug-rdp-measures-2026-exclusivity-terms`) — UNCHANGED; structurally unreachable, operator-side only.

`GAP-UNRESOLVED: einarsson-unct-20-6-expert-identities` — UNCHANGED; family retired, no budget.

`GAP-UNRESOLVED: icsid-arb-12-1-c-revision-proceeding-status-and-outcome` — UNCHANGED.

`GAP-UNRESOLVED: zeph-australia-award-2025-jurisdictional-ground` — UNCHANGED.

`GAP-UNRESOLVED: china-france-bit-2007-protocol-text` — UNCHANGED.

I open **no** new slug for the Roberts check. Its failure was a channel status, not a named-document gap, and minting a dated slug for a tool outage would corrupt a counter that escalates on document inaccessibility.

---

## Challenge to the chair

**0910-A1 — BLOCKING. §5 is a history-derived finding computed in a shallow clone, in breach of the binding N14 rule at `STATE_OF_THE_ANSWER.md:41` (commit `8015833`), and the breach is the direct cause of its weakest sentence.** Evidence: `is-shallow-repository` → `true` at session open, seven grafts, 142 commits reachable from `origin/main`; after `git fetch --deepen=400 origin main`, 1113. The rule quoted verbatim above requires the screen *"before any history-derived count, and unshallow if true"*. §5 reports commit counts and an ancestry ordering, and generalises to "as long as the relay has existed", without the screen and without the fallback scope qualifier the rule prescribes. In the un-deepened clone `fetch-relay.yml` appears to originate on 2026-09-03; it originates at `fe02f39` on 2026-08-03. Remedy: state §5's lifetime claim over the deepened graph, and record that git egress is alive so no future session need presume.

**0910-A2 — BLOCKING. "the reason it has never been reported" is false and fails a screen the chair had the instrument to run.** It was reported on 2026-08-05, in `open_threads[15]` defect (1) — the same thread §5 cites — with the pin `946b8b7`, the taxonomy entry 25 *MUTABLE-REDUCTION CITATION*, and the countermeasure *"every citation to a reduction carries its commit sha"*. Screen at `8015833`: `946b8b7` appears in `analytics/council-log.md`, `analytics/daily-research/2026-08-05.md` (13), `2026-08-06.md` (10), `2026-08-21.md`, `state/research_log.json` (2); the phrase family *overwrote/overwritten/in-place overwrite* appears in 30+ files, positive control non-zero throughout. Remedy: narrow the novelty claim to what is actually new — **the daily merge identified as the recurring trigger**, superseding 2026-08-05's mis-sorted-batch attribution — and drop "never been reported".

**0910-A3 — NON-BLOCKING, and it cuts in the chair's favour. "Presumably" understates what he could have shown.** The claim is not merely probable; it is verified across the workflow's whole lifetime: 40 `main`/`push` runs, all `run_attempt: 1`, one per daily merge, with exactly three failures (runs 31101427676, 32252025070, 33392951583 on 08-06, 08-19, 08-31) corresponding one-to-one with the three single-commit daily reductions. The mechanism is stronger than stated, and the exception class is "the runner failed", not "the bytes matched". His stated reason for the silence — "the second run answers the same request and usually returns the same bytes" — is not the operative reason for any of the three exceptions.

**0910-A4 — NON-BLOCKING. §2 asserts a channel posture it declined to test, and the untested half was wrong.** "Web search is alive on the record's evidence" is a claim about a channel supported by yesterday's evidence, in a record that elsewhere enforces the rule that a tool status is never carried forward as a state. It is dead today on a neutral control. No harm followed because he deferred the assertion to me; the drafting still carries a live status forward, which is the failure mode this council names most often.

**0910-A5 — NON-BLOCKING. F4 was pre-registered without a falsification condition, and one limb of it can fail independently.** The forecast conjoins "will be overwritten in place" with "the substantive sha256 stays equal". The second limb is a prediction about ICSID's server, not about the workflow, and today's own reduction shows the digest on this URL changing day over day. A conjunctive forecast graded as one unit will score CONFIRMED or FALSIFIED for the wrong reason. Remedy: grade limbs 1–3 and limb 4 separately, per the five conditions I set out in TASK 1.

**0910-A6 — NON-BLOCKING. §4's `ICSID` row list mixes URLs under one heading.** Six rows are correct; one of the six (`2026-09-06-daily.json` record 11) is at `/cases/news-events`, not `/cases/recent`. The "most recent four" gloss is accurate; the heading is skimmable into an error.

**0910-A7 — NON-BLOCKING, addressed to the register rather than to the chair. `open_threads[6]` has no stated closure condition.** It is the only on-theme, un-escalated, budget-eligible thread with search headroom, and the analyst's own aiming rule cannot be applied to it. It should be given one before the next session that has a live search channel.

---

## Reported against myself

**Self-1.** I ran history-derived counts — per-file commit counts across the whole reduction corpus, and the claim that the double-commit pattern begins 2026-09-03 — **before** checking `is-shallow-repository`. Both were shallow-boundary artefacts and both were wrong. I caught it only because a parentless commit dated 2026-09-05 that appeared to add the entire workflow tree made no sense. This is the same N14 failure I charge the chair with in 0910-A1, committed in the same session, and the charge stands against me equally. The corrected figures above are all computed post-deepen.

**Self-2.** I argued that a 32-second merge-to-commit interval was "implausibly fast" and therefore evidence **against** the chairman's causation. It was not evidence of anything. The run record gives 33 s wall clock and the same interval recurs on seven consecutive daily merges. I substituted plausibility for the run metadata that was one API call away, in a session whose entire method is that plausibility is not evidence. Withdrawn in full.

**Self-3.** I spent all three units of budget on the search channel and returned none. I record this as a spend, not as a saving: the first probe was mandated, the second isolated the domain filter, and the third supplied the neutral control that makes the finding usable — but the result is that TASK 3 had no budget left even if a channel had existed. Given the outcome I would spend the same way again; a reader should still see that the theme got zero.

---

## Budget spent, itemised

| # | Unit | Query | Purpose | Outcome |
|---|---|---|---|---|
| 1 | web search | `Anthea Roberts EJIL:Talk! September 2026` (`allowed_domains: ejiltalk.org`) | monitored-author standing check; doubles as the §2 search probe | tool error: `unavailable` |
| 2 | web search | `Anthea Roberts EJIL Talk UNCITRAL ISDS reform` (unrestricted) | isolate the domain filter as a cause | tool error: `unavailable` |
| 3 | web search | `example domain` (unrestricted, **neutral control**) | attribute the failure to the channel, not to query or subject | tool error: `unavailable` |

**3 of 3 spent, 0 returned, 0 reaching the research question.** Relay rows: 3 requested, 3 completed, 4 records — all fixed before I was convened. Zero budget touched `open_threads[0]`, the Ecuador slug, the Einarsson family, or the three escalated gaps. Non-budget instruments used without limit, as permitted: `git` (including one `--deepen`, which is how 0910-A1 was found), Python aggregation over the extracted `fetch-results` subtree, and the GitHub Actions run API, which supplied every decisive fact in the §5 audit.

---

## Candidate claims

Scope note, binding on this block and not only on my prose: **no case number, party, tribunal or matter appearing in any excerpt of the 2026-09-10 reduction is entered here as a discovered matter, screened, graded on any ring, or carried as a lead.** No claim below asserts anything about any resource; each is a claim about a workflow run, a repository object, or a code path. The search-channel failure is deliberately **not** proposed as a candidate claim: it has no source URL, and under the contract no URL means no claim.

```json
{"candidate_claims": [
  {"claim_text": "GitHub Actions run 34348169510 of .github/workflows/fetch-relay.yml in jackemorywilliams-bit/isds-watcher was triggered by a push event whose head_sha was abeeb5b0642d171899e9c85469f8fd72b0e1597b on branch main, on its first attempt.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/runs/34348169510",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"event\":\"push\",\"status\":\"completed\",\"conclusion\":\"success\",\"head_branch\":\"main\",\"head_sha\":\"abeeb5b0642d171899e9c85469f8fd72b0e1597b\"",
   "supporting_locator": "GitHub REST API, GET /repos/jackemorywilliams-bit/isds-watcher/actions/runs/34348169510; fields event, head_branch, head_sha, run_attempt"},

  {"claim_text": "The two 2026-09-09 fetch-relay runs on the isds-watcher repository are distinct runs on first attempt, not two attempts of one run: run 34344935760 has head_branch council/2026-09-09 and run 34348169510 has head_branch main, and both report run_attempt 1.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/runs/34344935760",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"id\":34344935760,\"run_number\":98,\"run_attempt\":1,\"event\":\"push\",\"head_branch\":\"council/2026-09-09\"",
   "supporting_locator": "GitHub REST API run records 34344935760 and 34348169510; field run_attempt"},

  {"claim_text": "Across the complete population of fetch-relay.yml workflow runs on branch main in jackemorywilliams-bit/isds-watcher (40 runs, 2026-08-03 to 2026-09-09), every run reports event push and run_attempt 1, and exactly three report conclusion failure: 31101427676 (2026-08-06), 32252025070 (2026-08-19) and 33392951583 (2026-08-31).",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"total_count\":40",
   "supporting_locator": "GitHub REST API, GET /repos/jackemorywilliams-bit/isds-watcher/actions/workflows/fetch-relay.yml/runs?branch=main&event=push; per-run conclusion fields"},

  {"claim_text": "Each of the three fetch-relay reductions whose post-merge re-fire run failed (analytics/fetch-results/2026-08-06-daily.json, 2026-08-19-daily.json and 2026-08-31-daily.json) carries exactly one commit in the repository's full history, whereas every other daily reduction whose day had no later-sorting request file carries two.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/tree/main/analytics/fetch-results",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "git log --format='%H' origin/main -- <path>, run per file over the un-shallowed clone (1113 commits reachable from origin/main); counts enumerated by an aggregating loop, not read off a display"},

  {"claim_text": "The fetch-relay workflow file .github/workflows/fetch-relay.yml was created in jackemorywilliams-bit/isds-watcher at commit fe02f39 on 2026-08-03, not on 2026-09-03; the later date is an artefact of the shallow clone the scheduled session receives.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/commits/main/.github/workflows/fetch-relay.yml",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "feat(relay): fetch relay for the network-less council session",
   "supporting_locator": "git log --format='%h %ci %s' origin/main -- .github/workflows/fetch-relay.yml, after git fetch --deepen=400 origin main; before deepening the same command returns only 022d117 (2026-09-03)"},

  {"claim_text": "The council record reported the fetch-relay in-place overwrite of a cited reduction on 2026-08-05, before 2026-09-10, and adopted a commit-pin remedy and a named taxonomy entry for it.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/main/analytics/daily-research/2026-08-05.md",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Required fix: a commit pin, not a correction. Every Part 2 citation to `analytics/fetch-results/2026-08-05-daily.json` must read `…@946b8b7`, because the values are no longer at the path.",
   "supporting_locator": "analytics/daily-research/2026-08-05.md:488, resolving at commit 8015833; taxonomy entry 25 MUTABLE-REDUCTION CITATION at :573"},

  {"claim_text": "The 2026-09-10 fetch-relay reduction (blob 91186e69, commit 8015833) answered request file 2026-09-10-daily.json under run 34470490415, carries 4 records with requested 3 and completed 3, control_ok true and void false.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/council/2026-09-10/analytics/fetch-results/2026-09-10-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"request\": \"2026-09-10-daily.json\"",
   "supporting_locator": "blob 91186e69eb34e70e34d7f67001f6aaa4b4f85ada at commit 8015833; fields request, environment, requested, completed, control_ok, void"},

  {"claim_text": "In the 2026-09-10 fetch-relay run, two separate fetches of https://icsid.worldbank.org/cases/recent returned bodies of identical length (362310 bytes) and identical sha256 (470b8878...), while the sha256 recorded for the same URL on 2026-09-09 (f309eb63...) and 2026-09-07 (4af84337...) differ from it and from each other.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/council/2026-09-10/analytics/fetch-results/2026-09-10-daily.json",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"bytes\": 362310, \"sha256\": \"470b88788b480bd032d7f5d8b117d67dadf3e4fe6df66fb6bd8041721f54e3ed\"",
   "supporting_locator": "blob 91186e69 records[1] and records[2]; compared against blob 72468ae1 (commit d34b9ec) records[1] and blob 4af84337-bearing rows in 2026-09-07-daily.json at commit 6df6114. This is a claim about digests and byte lengths only and asserts nothing about the content, composition or currency of any case listing."},

  {"claim_text": "The head control row of a fetch_relay.py reduction always reports find_matched false because find_matched is computed as bool(find) and the head control is fetched with an empty find string, so a false value in that row is not evidence of control failure.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher/blob/main/scripts/fetch_relay.py",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "\"find_matched\": bool(find) and bool(",
   "supporting_locator": "scripts/fetch_relay.py, fetch_one(), find_matched assignment, resolving at commit 8015833; control_ok is computed separately as records[0][\"outcome\"] == \"ok\" in run()"},

  {"claim_text": "The clone of jackemorywilliams-bit/isds-watcher delivered to the 2026-09-10 scheduled session arrived shallow, with seven graft boundaries and 142 commits reachable from origin/main, and git fetch --deepen=400 origin main succeeded from within that session, yielding 1113.",
   "source_url": "https://github.com/jackemorywilliams-bit/isds-watcher",
   "access_status": "full",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "git rev-parse --is-shallow-repository (true, then false); wc -l on .git/shallow (7); git rev-list --count origin/main (142, then 1113). Bears on the binding rule at STATE_OF_THE_ANSWER.md:41, resolving at commit 8015833."}
]}
```

---
