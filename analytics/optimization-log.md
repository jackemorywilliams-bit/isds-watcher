# Optimization log — system & method improvement ideas

The full council raises **at most one genuinely new idea per daily meeting** to improve or
optimize the system, the method, the fingerprint, or the search strategy toward answering the
research question or finding a breakthrough. Each is a dated bullet; the council dedups against
this list before adding. This is the running idea bank — ideas move to a PR/roadmap when adopted.

## Ideas (newest first)

- 2026-09-02 (chairman) — **A discriminator's cell carries its OCCURRENCE count as well as its REGION count, and a region count of one is not a pass on its own** (the council's one idea today; **PROPOSED ONLY — procedure, no code change, nothing built or applied**). **The evidence, from inside one session, and it is the direct lesson of a blocking objection sustained against the chair.** Yesterday's item established that a discriminator must be screened for uniqueness in held material and that a count other than exactly one disqualifies it. **Today that rule was satisfied and still let an unsafe row through, because it is written in terms of REGIONS and regions conceal multiplicity.** `ALEXANDROV` — the row the chairman added to anchor run A on the day's own page state — is **region-count 1 at the binding scope and occurs TWICE INSIDE RUN A ALONE, at offsets 208 and 496**, and in 3 regions across all held states. That is the identical signature to `Klaus SACHS`, which the same session's analyst screen singles out as *"the screen earning its keep."* **Since `excerpt_of` anchors on the FIRST case-insensitive occurrence and on nothing else (`scripts/fetch_relay.py:131-135`), a literal occurring twice inside one region has an anchor that can move without the region count changing by a single unit.** **What it cost not to have it today: nothing, by luck** — the window returned byte-identical to the prior day's and covered the span it was added to cover, verified after the fact by two seats. **A row that works unscreened is luck, not method.** **The proposed rule:** *a discriminator's cell prints `region-count` AND `occurrence-count`, at every labelled scope; a region count of 1 with an occurrence count above 1 is a CONDITIONAL pass whose condition — which occurrence anchors — must be stated before firing and exhibited after.* **Provenance, stated because it is against me:** filed by the integrity officer as `0902-B1`, sustained BLOCKING against the chairman, who made the region-count screen binding in Part I §4 of the same record and then exempted from it the one row he added himself; the analyst filed the origin defect against itself, having proposed the row unscreened in a memo whose whole subject was the screen. **Dedup, with the receipt:** screened at HEAD against this log — `occurrence count` **0**, `occurrence-count` **0**, `region count` **0**, `anchor can move` **0**; `discriminator` **1**, that single hit being the **2026-09-01 entry**, which is the entry this refines and which it names as such rather than claiming novelty against it. **Nearest neighbour on the other axis** is the 2026-08-18 anchor-rigidity entry, which governs choosing a find literal; this governs validating one that has already passed a uniqueness screen. **Screened in the honest form and not as a novelty claim:** this log at HEAD holds **44 dated bullets across 41 distinct days** and is disclosed by its own 2026-08-18 entry as missing many of August's council days — so this is *"not found in a baseline known to be incomplete"*, not a novelty claim against the corpus. Effort: zero (procedure). _Status: proposed._
- 2026-09-01 — **A pre-registered discriminator must be screened for UNIQUENESS in the material this project already holds before it is registered as a test — because a discriminator can be NON-DISCRIMINATING ON ITS OWN PAGE STATE, not merely stale on a new one** (chairman, adopting the research analyst's item and its formulation unchanged because it is better than mine, and the integrity officer's `0901-B8` which found the second instance; the council's one idea today; **PROPOSED ONLY — procedure, no code change, nothing built or applied**). **The evidence, from inside one session.** The 2026-08-31 record designated a 118-character prefix — `ion of Tribunal President: Klaus SACHS (German) - Appointed by the Parties Arbitrators: Stanimir A. ALEXANDROV (Bulgar` — as the signature of a `(c)`-block landing, writing that *"anything else is region territory."* **Today's `ALEXANDROV` window matches that prefix character-for-character — `W3.startswith(...)` returns True, verified independently by ALL THREE SEATS, twice read from the 2026-08-31 record itself rather than from any seat's rendering — while its own left context, reached by a 292-character byte-identical join, carries `Date Registered: January 12, 2012` and `Date of Constitution of Tribunal: July 12, 2012`, NOT the 2021 dates that record ascribed to the `(c)` block.** The ARB/12/1 template repeats an identical composition string across blocks, so a prefix drawn from one block does not distinguish it from another. **The council was protected by a DIFFERENT rule: the READ_ORDER clause voided the discriminator because the PAGE STATE had moved — the right outcome for the wrong reason. State-boundedness was the risk every seat named; NON-UNIQUENESS was the defect no seat named, and had the state not moved the discriminator would have graded the day's best return a mis-anchor and thrown it away.** **The mechanism is visible twice in one batch:** the integrity officer's `0901-B8` found the second instance inside a sentence written to dismiss it as noise at the overlap floor — run B's truncated `Compositio` meeting W3's `ion of Tribunal` is the same template label recurring across blocks. **The proposed rule:** *any string offered as a discriminator in a pre-registration must carry, in its own cell, the count of distinct regions in this project's held reduced-body material in which that string occurs, run case-insensitively; and if the count is not exactly one, the string is NOT a discriminator and may not be registered as a test — it may still be recorded as a description.* **What it would have cost today: one `find`-loop over the corpus, under a minute. What it cost not to have: nothing, by luck.** **Dedup, with the receipt:** screened at HEAD against this log — `discriminator` **0**, `uniqueness` **0**, `prefix test` **0**, `non-discriminating` **1**, that single hit being the 2026-08-21 entry's description of the `undocs.org` route and unrelated. **Nearest neighbours, and why this is neither:** the **2026-08-18** anchor-rigidity entry governs choosing a **find literal**, where this governs validating a **prefix used to grade a return AFTER it lands**; the **2026-08-31** check-scope entry immediately below would have caught a seat *claiming* to have screened a discriminator and **would not have caught this**, because the 118-character string was derived honestly and correctly from the window that seat had. **Screened in the honest form and not as a novelty claim:** this log at HEAD holds **43 dated bullets across 40 distinct days**, of which **14 are August days**, and it is **missing seventeen of August's thirty-one council days** — so this is *"not found in a baseline known to be incomplete"*, not a novelty claim against the corpus. Effort: zero (procedure). _Status: proposed._
- 2026-08-31 — **This council fixes the scope of what a seat OBSERVES and has no rule about the scope of what a seat CHECKS — and today all four sustained defects against the two most senior seats were of the second kind** (chairman, adopting the research analyst's own diagnosis and its request that this be logged separately rather than folded into the observation-scope entry; the council's one new idea today; **PROPOSED ONLY — procedure, no code change, nothing built or applied**). **The evidence, from inside one session.** Four defects, one shape: *a sentence asserting that a procedure was run, written from a weaker procedure that actually was.* `0831-B1` — the analyst verified that each candidate-claim quote came from the field named, which is true, and certified that the **character spans** had been "checked one by one", which they had not; **four of four spans were wrong by one to three characters**, two of them terminating mid-word. `0831-B2` — a section preamble reading *"every count is the length of its enumeration"* was asserted over a cell whose enumeration **was never run at all**; the true figures were 4 and 18 against a filed 1 and 2. `0831-B5`, against the chairman — a `relay_code_pin` stating the instrument's constants had been "re-read at this HEAD" covered `MAX_URLS`'s **value** but not the six lines that decide what `MAX_URLS` **counts**, so a batch was written with four free slots declared where five existed, and a row that would have fixed a region's boundary on-state was never offered. `0831-C1`, also the chairman's — *"no candidate meeting its two conditions has been derived"* covered one decision's text and the two candidates it declined, not the seat's own not-proposed list, where a candidate was sitting. **In every one the artefact was real and the certification outran it, always in the direction that flattered the writer.** **The increment over the standing rules is exact and it is a new limb, not a restatement:** the 2026-08-23 entry fixes the **field** limb (*for "the field does not contain X" the operative check is `len(field)` and `field.find(X)`, never a visual reading*), the 2026-08-20 entry the **counting** limb (*a null counter is scoped to page states, not rows*), and the 2026-08-30 entry the **region** limb (*a claim that X is absent from region R must enumerate the windows intersecting R and name R's unobserved remainder*) — **all three govern the scope of an OBSERVATION, and none reaches a claim about a CHECK.** The analyst's point that decides it: the existing remedy — *re-derive the observation at the correct scope* — **would not have caught either of its two failures**, because one figure was derived correctly at the wrong moment and the other was never derived at all. **The proposed rule, one sentence:** *a sentence of the form "I verified X" must name the operation that was actually run and state what it does not cover; and where a preamble certifies a procedure for a section, each cell states whether that procedure was run for it.* **Its mechanical form, in the analyst's own words:** *no sentence of the form "I checked X" ships unless the command that checks X appears in the same working pass, and every count in a prior-art cell is printed by the enumeration that produced it, per row, not per section.* **Screened, in the honest form this council adopted today rather than as a novelty claim:** this log at HEAD holds **42 dated bullets across 39 distinct days**, of which only **13 are August days** — `certif` returns **0**, `spot check` **0**, `scope of the check` **0**, `attested` **0**, and `procedure` **9**, none of the nine on this point. **I find no neighbour. But this log is missing seventeen of August's thirty council days (`0831-C3`), including the 2026-08-30 region-limb entry that this very session adopted and applied twice, so this is "not found in a baseline known to be incomplete" and NOT a novelty claim against the corpus.**
- 2026-08-26 — **The prior-art corpus excludes the only artefacts in this project that can falsify a claim about an award — and today that gap put an uncarried tribunal quotation in the operator's published answer file** (chairman, adopting the integrity officer's `0826-B3` methodological finding and the analyst's concession of it; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). **The evidence, from inside one record.** The five corpora this council screens (`analytics/fetch-results/`, `analytics/daily-research/`, `analytics/optimization-log.md`, `analytics/insights.jsonl`, `STATE_OF_THE_ANSWER.md`) **do not reach `working/one-pagers/`**, which is where this project's **operator-verified primary reads of awards, with paragraph pincites**, live. The phrase *"investment" can reasonably include intellectual property rights* — attributed to the *Bridgestone* tribunal **as its holding**, under the heading "What Is Established" — returns **exactly one file** under `git grep -l -F` at `f94ea71`: **`STATE_OF_THE_ANSWER.md` itself.** Not in `analytics/verification_ledger.jsonl`, not in `working/one-pagers/bridgestone-v-panama.md`, not in any daily record. **The proposition is supported and the quotation is not** — the one-pager records the Tribunal ruling that BSAM's FIRESTONE licence *"was an investment under the TPA and the ICSID Convention"* (¶¶ 123–24), at the **Expedited Objections** phase (13 December 2017, incorporated at ¶ 46), not in the 2020 Award the line cites. Three further defects sat in the adjacent *Eli Lilly* sentence, including *"jurisdiction was uncontested"* (**false** — Canada raised a *ratione temporis* objection, dismissed at ¶ 480(1)) and a question **expressly reserved** at ¶ 220 restated as the tribunal's holding. **Two seats screened that file for unmarked dispositions against a corpus definition that structurally cannot contain the contradicting evidence.** That is why six *Hela Schwarz* instances took six days, and why today's two were found by an officer **reading the paragraph beside the one the analyst flagged** — not by any screen this council runs. **The proposal:** any `GAP-UNRESOLVED` check, and any claim of the form *"no carrier in this repository"*, must screen **`working/one-pagers/` and `lit-review/`** in addition to the five, **or state in terms that it did not.** **Prior-art check with inline receipt**, run by the chairman at `f94ea71` over all **39** prior entries of this log (`grep -c "^- 20"`): `percent-encoded` → 0, `URL form` → 0, `encoding variant` → 0, and **no entry in this log addresses the prior-art corpus definition at all**; the single `normalis` hit is the 2026-08-09 entry's reference to `_WS_RE` whitespace normalisation, a different mechanism. **Nearest neighbour is the 2026-08-22 entry and this extends rather than repeats it:** that entry's limb (1) widened the corpus from two locations to four — this widens it to the **primary-read artefacts**, a different class of file — and its limb (2) governs **novelty** claims where this governs **carrier** claims. **`0825-R14` and `0825-O5` named this gap as a CAVEAT one day ago and I say so rather than claim it new.** The increment is that **a caveat travelling with a figure does not stop a seat screening**, and today measures what that costs: the defect the caveat predicted landed in the operator's own file and **survived two screens**. This converts a caveat into a required screen — the same conversion the 2026-08-22 entry made for novelty receipts. **What it would have cost today: one `git grep` against two directories. What it cost not to run: a fabricated-looking quotation live in the deliverable the principal reads.** Effort: zero (procedure). _Status: proposed._
- 2026-08-23 — **A negative claim about a field is not a reading; it is `len(field)` and `field.find(X)` — and the rule that says so is one another seat wrote, because the rule I wrote for the same failure does not catch my own errors** (chairman, adopting the analyst's `0823-R14` verbatim on the integrity officer's binding direction at `0823-O1`; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). **The evidence is three chairman failures in one session, and the test of the rule is which of them it catches.** (1) I read a **truncated console rendering** of a 400-character `excerpt` field — roughly its first 300 characters — and from it told the research analyst, *as a direction*, that the window "truncates at `May 12` before Trans-Global's own finding-column value" and that the day's candidate was therefore NOT confirmed. The field falsifies it: `Partial` sits at offset **308**. **The evidence was complete on disk, in a file I had already committed**, and I destroyed it in the act of looking and transmitted the destroyed version as an instruction; only the analyst's independent `json.load` kept my error out of the record as his finding. (2) I published the N12 overwrite exposure as **"twelve council days"**, a figure I inherited from yesterday's **"eleven"** and never derived. (3) I corrected it to **"seven"** — derived, but on a clone I had not checked was complete, and which was **shallow at 189 commits against 758**, on which `check_currency.py` also reported **three hard `FAIL`s that do not exist**. **My proposed rule was *"distrust the layer between you and the evidence."* The integrity officer refused it** (`0823-O1`): it is true, unenforceable, spreads the cause onto tooling, and — the decisive objection — **it catches only failure (1) of the three.** **The rule adopted instead is the analyst's, in his words: for any claim of the form "the field does not contain X", the operative check is `len(field)` and `field.find(X)`, never a visual reading; and no count enters the record without a shown derivation.** It catches **all three**, because (2) and (3) are the count limb and (1) is the field limb of one discipline. It is also the same discipline as gate `G8`'s clamp rule — *state where the match sat, never assume it* — **applied to the reader instead of to the instrument**, which is why it needs no new machinery. **Prior-art check with inline receipt**, run at branch point `dc46ff6` over all four corpora (`analytics/fetch-results/`, `analytics/daily-research/`, `analytics/optimization-log.md`, `analytics/insights.jsonl`): `truncated rendering` → **0 / 0 / 0 / 0**; `display layer` → **0 / 0 / 0 / 0**; `read the raw JSON` → **0 / 0 / 0 / 1**. **Nearest neighbour is the 2026-08-15 `"Chip" Gordon` insight and it is the OPPOSITE case:** there the relay's own 400-character cut genuinely removed the text from the field and the fix was to slide the window; **here the field was complete and the reader lost it.** No entry in this log addresses the reader's own display layer, and the 2026-08-22 entry immediately above governs *novelty* claims where this governs *negative* claims about a field and *any* count. **What it would have cost today: two lines of Python. What it cost not to run: a false direction issued to another seat, and two published figures that were wrong in different ways.** Effort: zero (procedure). _Status: proposed._
- 2026-08-22 — **The prior-art check works when it is a REQUIRED FIELD and fails when it is a rule, and today's record is a controlled experiment proving it: every novelty claim that carried a written receipt was right, and every novelty claim that did not was wrong** (chairman, extending the analyst's `0822-R18`; the council's one new idea today; PROPOSED ONLY — procedure plus one optional template change, nothing built or applied). **The evidence, from inside one record.** Yesterday's adopted item was the prior-art rule itself: *grep this council's own corpus before any seat calls a route open, closed, unexplored or newly found.* It was in force for roughly eighteen hours and this session produced **four fresh re-derivations** anyway: the chairman's within-run digest claim (`0822-R15`: first observed 2026-08-05, a seat corrected for presenting it as new on 2026-08-12, censused corpus-wide on 2026-08-15 — a **fourth** presentation); the chairman's Part II §3 claim to have seen the register's row structure "for the first time" (falsified by a 2026-08-20 window holding all three finding-column values in slot); the analyst's `0822-E1` (a demonstrated search method naming this project's pivotal case as its trigger, sitting unopened in this very file since 2026-07-22 through three days of searching for that case); and his `0822-E4` (a search bought to re-return a defect already held in nine files). **Now the controlled half.** The same session made **two** novelty claims that carried a written prior-art receipt — the two never-fetched ICSID routes, whose zero-row greps were run and recorded in `analytics/fetch-requests/2026-08-22-daily.json`'s required `prior_art_check_run_before_this_file_was_written` field **before the file was written** — and **both were correct**, independently reproduced by the analyst. **Four unreceipted novelty claims, four wrong; two receipted, two right.** The difference is not diligence or seniority: the same chairman made both kinds in the same hour, and the receipted ones are the ones the request-file template *forced* him to write down. **The proposal, in two parts.** (1) **Scope** (the analyst's half, `0822-R18`): the prior-art corpus is not only `analytics/daily-research/` and `analytics/fetch-results/` — it is also `analytics/optimization-log.md` and `analytics/insights.jsonl`, which is where this project stores methods that already worked. (2) **Mechanism** (the chairman's half, and the part today's evidence uniquely supports): **any sentence in a council record asserting novelty — "new", "first", "never", "unexplored", "candidate for tomorrow", or any superlative — must carry an inline receipt in the same sentence or its parenthetical, naming the terms screened and the counts returned, in the same shape the relay request file already requires.** A novelty claim without a receipt is treated as unverified and is downgraded to "not found under terms not stated", exactly as `0811-B5` treats an underived count. **Deduped by the proposing seat against all prior entries.** Nearest neighbour is the **2026-08-21** entry, which this supersedes in scope rather than repeats: that one governs *routes and hosts* at *batch-firing time*, and every one of today's four failures was a claim about a **finding, a method or a source**, in **prose**, after the batch had fired — outside its trigger by construction. The **2026-08-09** adversarial-alternative pre-flight asks what *else* could produce a reading; this asks whether the reading is a *repeat*. `0811-B5` and method rule (o) govern *counts*, not novelty. Method rule (u) states the duty and is the rule that failed today; this converts it from a duty into a field. **What it would have cost and bought today:** four greps, under a minute in total, against a session that spent two relay rows, one full seat-task and two of six search calls re-deriving what the record already held. **Effort: low** (a documentation change to the record template; optionally one line in a lint script that flags an unreceipted novelty token in `analytics/daily-research/`). _Status: proposed._
- 2026-08-21 — **Before a batch fires a host, and before any seat calls a route open, closed, unexplored or newly found, grep this council's own corpus for it — in both directions** (chairman; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). The standing rule is *grep the record before asserting something is absent from it*. **Today produced FOUR violations of its unwritten converse, in one session, across two seats.** (1) The `undocs.org` lag route was re-fired as an open question **seventeen days** after this council established it non-discriminating against an `A/81/99` control — one `git grep -l undocs analytics/fetch-results/` would have stopped it. (2) A Part headed **RESOLVED** re-derived the `daily-update` emailer mechanism that is **escalation 1 of both the 2026-08-12 and the 2026-08-13 records**, citing neither, in the same record whose lead section confesses failure (1). (3) An intra-day drift experiment was designed on the pre-registered premise that within-date constancy "HAS ONLY EVER BEEN OBSERVED WITHIN A SINGLE RUN" when the corpus already held it at **22 minutes** (2026-08-19) and **54 minutes** (2026-08-20) — so the day's second relay batch was spent re-establishing, at 10 minutes, something five times stronger already in hand. (4) The analyst closed an ICSID retrieval route as PDF-only without checking for another rendition; the integrity officer's single extra call surfaced **two HTML URLs on the same allowed host**, never fired by this council. **The check is two commands** — `git grep -l <host> analytics/fetch-results/` and `git grep -l <mechanism> analytics/daily-research/` — and it costs nothing against a relay row, a search call, or a Part. TWO CONDITIONS OF ADOPTION, both against the proposing seat. (a) It binds **route and mechanism dispositions**, not every sentence: a seat need not grep before recording a byte count, but must before writing *closed*, *unavailable*, *unexplored*, *new*, or *resolved*. (b) **A negative result from the grep is not a licence** — the corpus opens 2026-08-03 and observations predating it are record-carried rather than artefact-carried, so "0 rows" means this council has no reduction, never that no seat ever looked. **Dedup, checked today against all 34 prior entries** (`grep -c "^- 20"`): nearest neighbours are the **2026-08-08 route-admissibility pre-flight**, which asks whether a third-party path is open *to us* before rows are spent, and the **2026-08-09 adversarial-alternative pre-flight**, which asks what other state of the world a reading is consistent with. Neither asks the question this one asks — **has this council already answered this?** — and no entry in this log addresses prior art in the council's own corpus at all. Effort: zero (procedure). _Status: proposed._
- 2026-08-20 — **A null counter must be scoped to page states, not to rows, or it measures the council's diligence instead of the world** (chairman; the council's one new idea today; PROPOSED ONLY — procedure plus one deferred code change, nothing built or applied). Today's derivation: the `A/81/17` watch on `uncitral.un.org/en/commission` stands at 15 find-scoped commission-page rows, all nulls, at 15 distinct dates — but those 15 rows were taken against exactly **two** distinct page digests (7 against `ca87a113c024`, 2026-08-03 → 08-10; 8 against `f9712994f5ae`, 2026-08-13 → today), with byte length **constant at 832,338 across all 18 `ok` runs** of that URL since 2026-08-03. As evidence about whether the string has appeared, fifteen observations are worth **two**; the page has not moved in eight days and each day's row recorded that the council showed up. **The proposal:** any recurring find-watch on a stable URL reports its figure as a **pair — (rows, distinct page digests)** — with the second number carrying the evidential weight, and the find fires **only when the digest moves**. This converts the change-detector idea the 2026-08-19 analyst proposed for efficiency into the *correct form of the watch*, on evidence rather than on convenience, and it generalises past `A/81/17` to every standing watch this project runs. **Deduped by the proposing seat against all prior entries**; nearest neighbours are the 2026-08-09 adversarial-alternative pre-flight (which asks what *else* could produce a reading — this asks whether the reading is a *repeat*) and the 2026-08-18 anchor-rigidity entry (which governs window anchors, not counters). Neither reaches a repeated observation of an unchanged resource, and no existing entry addresses counter inflation at all. Effort: low (one field in the watch's reporting; the digest is already in every reduction). _Status: proposed._
- 2026-08-18 — **Anchor rigidity is two-sided: a window-slide anchor must carry BOTH the designator and the case, because each is ambiguous in exactly the direction the other resolves** (chairman; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). The 2026-08-17 entry established one half: a case-relative designator such as `Procedural Order No. 4` is a HOMONYM ACROSS CASES, and rigidity is supplied by scoping the corpus to the resource. Today's windows established the converse WITHIN a single page. On the ICSID `UNCT/20/6` docket the string `Expert Report of “Chip” Gordon C. Gill` occurs TWICE — bare in the window returned 2026-08-15, and prefixed as `CER-05, Expert Report of …` in the window returned 2026-08-18 — and because `excerpt_of` returns the FIRST case-insensitive match (`scripts/fetch_relay.py:130-135`), a seat anchoring on the bare title lands in the earlier region and cannot know a second entry exists. **So: across cases the designator is ambiguous and the title disambiguates; within a page the title is ambiguous and the designator disambiguates. Neither is a rigid designator alone.** THREE CONDITIONS OF ADOPTION, all three added in vetting and all three against the proposing seat, which is why they are the useful part. (a) This says NOTHING about whether the two entries are the same DOCUMENT (`0818-B2`): what is established is that the same NAME is carried twice, the page shows two differently-formed lists, and neither `Nigel Bankes` nor `Paul Sharp` appears in the second — the chairman's heading asserted document identity and was struck as `0814-B1` SELF-HEADLINE OVERREACH, in the batch whose own decision rule `E7` pre-registered the rule against it. (b) The relay does NOT centre its window: `scripts/fetch_relay.py:134` computes `start = max(0, idx - EXCERPT_CHARS // 4)`, the QUARTER POINT — 100 characters of left context and 300 of right, confirmed at offset 100 on two independent rows today — so a seat computing the next anchor from a centred window is wrong by 100 characters (`0818-B14`). The function's own docstring at `:118` says "centred" and is WRONG; the record must carry the code, not the docstring. (c) The rule governs where an anchor LANDS, not what a page contains, and per G5 no anchor outcome may be restated as a fact about any document, person or proceeding. Dedup, checked today against all **33** prior entries (`grep "^- 20" | grep -cv "^- 2026-08-18"`), and the dedup turned up a defect in this log itself. The nearest neighbour is the scope-the-corpus item of 2026-08-17, which this extends rather than repeats — that item supplies rigidity by narrowing the CORPUS, this one by widening the ANCHOR — but **it is not in this file**: the newest entry before today is 2026-08-09, so the daily optimization items of 2026-08-10 through 2026-08-17 were written into their daily records and never deduped here, contrary to Rule 0's own instruction that the item be "deduped into `analytics/optimization-log.md`". **An eight-day gap in the idea bank means every dedup check since 08-10, including this one, has been run against an incomplete list**, and I state that rather than let my own dedup claim rest on it. Backfilling those eight items is an archivist task and is routed, not done. Against the 33 entries this file does hold, no entry touches window-sliding, excerpt centring, or within-page homonymy. Effort: zero (procedure). _Status: proposed._
- 2026-08-09 — Adversarial-alternative pre-flight: before any instrument reading is cited as ESTABLISHING something, name one alternative state of the world consistent with the same reading — and if none can be constructed, the check is tautological and establishes nothing (chairman; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). Triggered by today's session: `len(excerpt) == bytes == 150` was cited as corroborating the LINE STRUCTURE of digitallibrary.un.org's robots.txt, and the integrity officer did not argue that the check was weak — it CONSTRUCTED the readings the check cannot distinguish (six-line, five-line, one-line, all consistent with every datum held), parsed each under the project User-Agent, and found that the five-line reading returns `/search` ALLOWED and inverts the day's admissibility map. The reduction's own `_WS_RE` whitespace normalisation had destroyed the discriminating information before the check was run, so the check could not have returned a different number for a different world. The procedure is one sentence appended to any decision rule or memo paragraph citing an instrument reading: *the alternative this reading excludes is X; the alternative it does not exclude is Y* — where Y is empty the reading is decisive, where Y cannot be written at all the check is tautological. TWO CONDITIONS OF ADOPTION, not decoration: (a) it binds readings offered as EVIDENCE, not every datum recorded — a logged byte length needs no alternative, a byte length cited as proof of a file's structure does; (b) the alternatives must be constructed and, where the instrument permits, RUN, as the officer ran three parses — naming a hypothetical alternative without testing it is the same species of gesture as the check it disciplines. Dedup, checked today at 12:03:57Z against all 32 prior entries (`grep -c "^- 20"`): nearest neighbours are the 2026-08-08 route-admissibility pre-flight, which tests whether a third-party PATH is open to us before rows are spent, and the 2026-08-03 egress-triage protocol, which triages OUR OWN instrument LAYERS before an agenda is set. Neither asks whether a check can discriminate. The integrity taxonomy's entry 26, 'tautological instrument check', CLASSIFIES this defect after it has been committed; nothing in this log turns it into a pre-flight. Effort: zero (procedure). _Status: proposed._
- 2026-08-08 — Route-admissibility pre-flight: the first fetch-relay batch that touches a NEW PATH on a host spends its first row on that host's `robots.txt` (chairman; the council's one new idea today; PROPOSED ONLY — procedure, no code change, nothing built or applied). Triggered by today's session: three of seven rows were spent discovering that this project's own robots compliance forbids `https://digitallibrary.un.org/search`, so the whole of Group 1 — a pre-registration, a decision rule with a P0 and an echo control, and the council's attention — bought no observation of any kind. One `robots.txt` row would have returned the same fact *plus* the disallow rules, i.e. which paths on that host we may actually use, converting a spent batch into a route map. The cost is one row on a channel where rows are free. TWO CONDITIONS OF ADOPTION, not decoration: (a) a `robots.txt` row is itself subject to the same robots check and to the fail-open path at `src/sources/base.py:171-172,180`, so a null on it is uninformative in exactly the way today's Group 1 was, and must be written up as such rather than as "the host has no restrictions"; (b) `EXCERPT_CHARS = 400` will not carry a long rules file, so the row establishes reachability and the head of the file, never the full policy — it may not be cited as evidence that a path IS allowed. Dedup, checked against all 31 prior entries: nearest neighbours are the 2026-08-03 egress-triage protocol, which triages OUR OWN instrument layers before an agenda is set (this tests a third-party path's admissibility to us, after the instrument is known good), and the 2026-08-02 UN Digital Library entry, which proposes a route but offers no way to learn whether that route is open to us before rows are spent on it. No other entry touches robots compliance or pre-flight route testing. Effort: zero (procedure). _Status: proposed._
- 2026-08-04 — A `STALE` per-source signal, distinct from `DEGRADED` (systems researcher; the council's one new idea today; PROPOSED ONLY — not built, not applied, awaiting operator sign-off; deduped by the proposing seat against all 27 prior entries, nearest neighbour being its own 07-31 health-column entry, which surfaces existing health data rather than computing a signal that does not exist). `src/source_health.py` would gain a second, weaker per-source signal keyed on FRESH counts — `fresh_zero_streak`, persisted alongside `zero_streak`, surfacing as a status SUFFIX (`RETURNED · STALE (5 runs, 0 new items)`), never a replacement, and suppressed whenever a stronger status (DEGRADED / FAILED / NOT-READ / DISABLED) is present. The archived data shows fresh count is the only signal that discriminates among undated sources: `iareporter_headlines` and `unctad_isds` are architecturally identical — both undated (`date_inferred: True`), both unfiltered by `since`, both ~12–25 raw items every run — and their fresh counts are 11/10/11/10/11 versus 0/0/0/0/0. A rotating feed and a static furniture-scrape are indistinguishable by every signal the system currently computes and perfectly separated by this one. BINDING CONDITION OF ADOPTION: the signal may never assert breakage — a dated feed publishing monthly would show `fresh_zero_streak >= 3` while perfectly healthy — so it states only what was observed, the same integrity discipline as `NOT-READ`. `per_source` is already computed and already archived in every `meta.json`, so it needs no new plumbing and can be backfilled and validated against real data before it ever runs live (~15 lines, one new state field). What it would have bought: `unctad_isds` would have carried `STALE (3 runs, 0 new items)` in the 2026-07-20 digest — four weeks and one hand-audit before the analytics officer found it. Raised in `analytics/daily-research/2026-08-04.md` Part 5(d).
- 2026-08-03 — Egress-triage protocol for delegated sessions (the council's one new idea today; adopted in-session as method, no code change, no operator approval required): before any HTTP-status audit or any fetch-dependent agenda in a delegated environment, (a) run one positive control pairing a suspect-host probe with a **neutral-host probe on the same instrument** (example.com / wikipedia.org — a control drawn only from the suspect set cannot separate a targeted artifact from a general instrument failure), and (b) on any `HTTP 000` / CONNECT failure, read the sandbox proxy's status endpoint (`$HTTPS_PROXY/__agentproxy/status`) — a free, local diagnostic that cleanly separates proxy-policy denial from origin answers. Triggered by today's session: the scheduled cloud environment proved fetch-dead at both instruments (blanket proxy CONNECT denial; fetch-service 403 on 6 of 6 URLs including neutral controls), the analyst's positive control correctly aborted six planned re-tests before they could be recorded as origin states, and the officer's neutral-host controls falsified the analyst's residual CDN attribution (B1). The project's instrument language now distinguishes four artifact layers: proxy CONNECT rejection (no origin information), fetch-service blanket 403 (no origin information), CDN user-agent gating (2026-07-31 finding; no existence information), origin answers 200/404 (the only evidentiary layer). Chairman's template addition: the pre-agenda probe decides whether a session's agenda is fetch-scoped or search-scoped. Dedup: distinct from the 2026-07-31 health-column proposal (pipeline observability), the 07-01/06-30 access-route entries (retrieval technique, not instrument triage), and all case/treaty monitoring entries. Effort: zero (procedure only). _Status: adopted 2026-08-03 (session method; recorded in the 2026-08-03 Part 6 rulings and the analyst's/officer's self-training notes)._
- 2026-08-02 — UN Digital Library as a second canonical retrieval route for UN document symbols (integrity officer, in vetting; the council's one new idea today): when a UN document symbol (A/CN.9/..., A/<session>/17) is needed and the UNCITRAL-site path and the ODS-by-symbol route (docs.un.org, the 2026-06-30 entry's route) are inaccessible, attempt the UN Digital Library record route (digitallibrary.un.org) as a distinct third surface. Triggered during the 2026-08-02 egress-denied session: the officer's independent A/81/17 search surfaced a digitallibrary.un.org record URL — a route the record has never tried, on separate infrastructure from both uncitral.un.org and docs.un.org. Dedup: distinct from the 2026-06-30 ODS entry (different host, different system; ODS remains the canonical symbol-archive argument) and from the 2026-07-01 quoted-phrase fallback (this is a fetch route, not a snippet technique). Offered as a cheap second fetch target, not a finding; the surfaced URL is a search-layer return, unretrieved, resolution unverified 2026-08-02. Effort: very low (one fetch per needed symbol in a fetch-capable session). _Status: proposed._
- 2026-07-31 — Source-health column in the receptivity report (systems researcher, first seated session; the council's one new idea today): `scripts/source_analytics.py` gains a `Health` column carrying each source's latest status and consecutive-zero streak from `state/source_health.json` / `meta.json["source_health"]` — data `src/source_health.py` already computes, persists, and tests (zero-streak escalation to `DEGRADED` at three; `COLLECTION ANOMALY` warning), but which the receptivity ledger — the artifact the council actually reads to judge which sources earn their place — never sees, forcing the analytics seat to re-derive zero-streaks by hand each session against a seat rule that exists because hand-copying is error-prone. A zero row becomes self-explaining ("0 surfaced, RETURNED, 4-run zero streak" vs. "0 surfaced, DEGRADED"). Model: dbt's source-freshness pattern (docs.getdbt.com/docs/build/sources, verified by fetch 2026-07-31) — per-source pass/warn/error status generated alongside the source's output as a first-class artifact, not re-derived by a human. Guardrail as a condition of adoption, not a nicety: the report is longitudinal while health is point-in-time, so the column must carry an explicit "as of <latest run date>" label or it reintroduces the window-mixing failure the same-window fix removes, one column over. Sequencing: land after — never bundled with — the same-window patch. Dedup: checked against all prior entries; none touches the analytics report, source health, or instrument observability. Effort: low. _Status: proposed._
- 2026-07-29 — New-procedural-mechanism first-application tracker: when a codified early-determination mechanism enters the research record as a named open thread (ICC Art. 30, ICSID Rule 41/51(1), UNCITRAL Supplementary Provisions early-dismissal provision), create a dedicated standing monitoring item for the first reported application of that mechanism in any investment case touching IP, data, or trade secrets. Implementation: periodic (monthly or quarterly) search for each named mechanism paired with IP/data keywords ("ICC Article 30 investment" or "ICSID Rule 41 intellectual property" or "manifest lack of merit IP data investment"). Triggered by confirmation that ICC Art. 30 entered into force June 1, 2026 with no reported IP-adjacent application as of July 29. Distinguishes from: (a) tagged-case posture tracker (targets known cases by docket identifier, not mechanism applications); (b) standing practitioner blog watch (July 28, targets thematic commentary, not specific procedural dispositions); (c) academic case note intake (July 12, targets journals). Effort: very low (two or three targeted searches per monitoring cycle). _Status: **adopted 2026-07-31** (chairman, per the 2026-07-30 close-out; analytics: CORROBORATED at 3 queries on first use) — monthly cadence, analyst executes, next cycle late August 2026; baseline is the officer-verified null against ICSID's MLLM decisions table (60+ decisions 2008–2025, none IP/trade-secret/pharma/data); the withdrawn FY2025 "3 decisions within 60 days" statistic stays barred from the baseline._
- 2026-07-28 — Standing practitioner blog watch for data-as-investment analyses: establish a periodic (monthly or quarterly) targeted search across practitioner publication platforms — Kluwer Arbitration Blog, IAReporter "Analysis" and "Looking Back" series, GAR (Global Arbitration Review), and specialist firm client alerts — for new articles specifically addressing data assets (trade secrets, clinical trial data, algorithm-derived results) as covered investments in ISDS. Triggered by today's session: a Kluwer Arbitration Blog article on "data assets as investments" was newly surfaced in search results but returned HTTP 403; the title alone signals a growing practitioner literature on the data-as-investment theme that the project's current monitoring architecture (which relies on case-specific and keyword-based searches) may underindex. Distinct from: (a) the July 20 author preprint / institutional repository search (which targets academic articles behind paywalls); (b) the July 22 IAReporter analysis article title mining (triggered by specific paywalled ICSID case coverage, not a standing topic-based practitioner blog watch); (c) the July 26 EJIL:Talk author-page monitoring (targets a named academic's series, not practitioner blogs broadly). The practitioner blog watch targets the emerging thematic commentary on data-as-investment as a cross-case standing topic; the practitioner conversation is growing (Daily Jus "Data in Dispute" Dec 2024; Kluwer Blog Jan 2026; Einarsson commentary) and tracking it thematically rather than case-by-case would improve Ring 1 coverage at low cost. Effort: very low (two or three targeted searches per monitoring cycle). _Status: proposed._
- 2026-07-26 — EJIL:Talk author-page monitoring: when the project relies on a specific academic's ongoing series for post-session analysis (as with Anthea Roberts' UNCITRAL ISDS reform series on EJIL:Talk), monitor the author's page (ejiltalk.org/author/aroberts/) directly rather than relying on keyword searches. Today's session found the newly-indexed "Lifelong Learning" article only when reformulating the search with Roberts' name and article title; yesterday's keyword search did not surface it. Direct author-page monitoring would reliably catch new articles in the series without guessing the title. Distinguishes from the July 21 UNCITRAL Indico/press-releases monitoring proposal (which targets UNCITRAL's own channels, not the academic blogger's author page). Effort: very low (one URL check per monitoring cycle). _Status: proposed._
- 2026-07-23 — UNCTAD IIA Mapping Navigator escalation protocol: when a treaty provision gap persists across three or more sessions without resolution from web search (blocked PDFs, AI synthesis without named accessible primary text, conflicting signals rejected by the security officer), generate a specific operator-action item directing the operator to execute a manual UNCTAD IIA Mapping Navigator query (https://investmentpolicy.unctad.org/international-investment-agreements/iia-mapping) for the specific provision. The Mapping Navigator provides structured provision-level treaty data — provision type, formulation characterization, coverage notes — accessible to a human user navigating the web interface; it cannot be systematically queried by automated search but provides reliable provision-level data for named treaty-provision pairs. Implementation: (a) three-session threshold: if a targeted provision is UNCONFIRMED across three or more council sessions, escalate; (b) generate a specific operator-action item naming the treaty, the provision, and the Navigator URL with pre-populated search parameters where possible; (c) record the escalation in STATE_OF_THE_ANSWER.md and the relevant open thread in state/research_log.json. Triggered by the China-France BIT (2007) Protocol administrative exhaustion formulation: persisting UNCONFIRMED for 19+ sessions (since July 5, 2026) despite five retrieval attempts (two conflicting signals July 20; three permissive signals July 23 — all AI synthesis of blocked sources, all rejected by security officer). The UNCTAD Navigator's IIA Mapping tool (distinct from the treaty download URL at investmentpolicy.unctad.org/international-investment-agreements/treaty-files/...) provides provision-level characterization data accessible via human browser navigation. Distinguishes from all 22 prior proposals: all others target automated search or document retrieval; this creates a formal operator-escalation pathway for data gaps that require human browser access to structured provision databases — a distinct document class. Effort: very low for Claude (generate the escalation note and operator-action item); low for operator (navigate Navigator, ~10–15 minutes per provision). Immediate application: China-France BIT (2007) Protocol administrative exhaustion formulation (operator action requested). _Status: proposed; immediate escalation triggered._
- 2026-07-22 — IAReporter analysis article title mining for paywalled ICSID case holdings: when an ICSID case award or decision PDF is 403-blocked, execute a targeted search pairing the case name (or parties) with "IAReporter" and key doctrinal terms (e.g., "[party name] IAReporter administrative review" or "[party name] IAReporter jurisdiction"). IAReporter's "Analysis:" article titles consistently carry the key holding — naming specific treaty articles and characterizing specific dispositions in the title itself — making the indexed title an accessible artifact at MEDIUM-HIGH confidence even when the subscription body is paywalled. Triggered by today's Hela Schwarz 9(a) finding: the IAReporter article "Analysis: ICSID tribunal hearing spice investor's claim against China gives effect to idiosyncratic administrative review and fork-in-the-road clause found in BIT Protocol, dismissing all claims" resolved the longest-standing uncertainty in the Ring 3 three-condition architecture (UNCONFIRMED since seq 13, June 28). Distinct from: (a) academic case note intake (July 10) — targets peer-reviewed journals with 6–12 month lag; (b) counsel identification step (July 13) — targets law firm victory announcements (potentially biased source); (c) author preprint search (July 20) — targets author manuscript copies of academic articles. IAReporter analysis article titles are specialist editorial conclusions from a neutral ISDS outlet — more immediate than academic journals, more reliable than party counsel announcements. Effort: very low (one additional targeted search per blocked case per monitoring cycle). Immediate applications: AsiaPhos v. China (ICSID ADM/21/1); Einarsson v. Canada (UNCT/20/6, pending award). _Status: proposed; demonstrated successfully today._
- 2026-07-21 — UNCITRAL Indico session page + UNCITRAL press releases page as primary post-session monitoring channels: after the close of a UNCITRAL Commission or Working Group session, monitor (a) the session's Indico page (e.g., https://indico.un.org/event/1022838/ for the 59th Commission session) for posted documents, presentations, and side-event recordings; and (b) the UNCITRAL press releases page (https://uncitral.un.org/en/about/pressreleases) for official post-session announcements — both should be checked within 24–48 hours of session close and weekly thereafter until IISD ITN or EJIL:Talk post-session analysis appears. Triggered by the 59th UNCITRAL Commission session (July 10, 2026 close): eleven days post-close and neither IISD ITN nor EJIL:Talk analysis has been indexed; the project has no standing mechanism for checking UNCITRAL's own official press releases or the Indico session page for outcome documents during the 0–14 day gap before NGO/academic analysis appears. Distinguishes from all 21 existing proposals: (a) UNCITRAL-related optimizations in the log focus on document tracking (A/CN.9/series) — this targets the official session outcome reporting channels themselves; (b) the July 10 academic case note intake targets journals (6–12 month lag) — this targets official UN channels (0–7 day lag). Effort: very low (two URL checks per monitoring cycle; no retrieval needed beyond confirming post-session press release existence). _Status: proposed._
- 2026-07-20 — Author preprint / institutional repository search: when a key academic article is confirmed to exist but is systematically 403-blocked at the publisher's URL, extend the retrieval attempt to: (a) the author's SSRN author page (search: "[Author name] SSRN"); (b) the author's Google Scholar profile (search: "[Author name] Google Scholar"); (c) the author's institutional repository or university faculty page (for China-based authors: UIBE, PKU, or THU institutional repositories; for EU authors: Cardiff Research Online or equivalent); (d) ResearchGate author page (search: "[Author name] ResearchGate"). Triggered by persistent failure to retrieve the body of Manjiao Chi's JIDS 2021 article ("Gilding the Lily in Disguise") — the most likely source for resolving the China-France BIT (2007) Protocol administrative exhaustion formulation gap. Chi is based at ZUEL/UIBE; institutional repositories for Chinese law faculty frequently host author-accepted manuscripts of Western journal articles. Distinguishes from all 20 existing proposals: the July 1 proposal targets document snippet reconstruction; the June 30 proposal targets UN ODS; the July 10 academic case note intake proposal targets journals themselves — this proposal targets the author's personal or institutional manuscript copy, a separate document class with different access architecture. Effort: very low (one or two additional search queries per blocked article, keyed to author name + affiliation). Immediate applications: Manjiao Chi (JIDS 2021, "Gilding the Lily") and Gáspár-Szilágyi (JIDS 2024, "When the Dragon Comes Home to Roost"). _Status: proposed._
- 2026-07-19 — Pre-ICSID gate typology table: produce a comparative table classifying all China BITs in scope along two orthogonal dimensions: (1) administrative exhaustion — mandatory vs. permissive vs. absent; (2) court-withdrawal bar — mandatory/no-U-turn vs. permissive vs. absent. Triggered by today's finding that the China-Switzerland BIT (2009) Protocol uses permissive 'may require' administrative exhaustion while the China-Germany BIT (2003) and China-Netherlands BIT (2001) use mandatory language — a material cross-BIT strategic distinction confirmed in a single session by applying the July 18 cross-BIT consistency check. The typology adds a strategic-selection dimension absent from the existing optimization proposals: (a) the July 15 Chinese BIT Protocol provision audit maps what each provision says; (b) the July 18 cross-BIT consistency check verifies that provisions found in one BIT are checked across others; (c) this typology classifies the confirmed provisions into a two-dimensional strategic comparison matrix, enabling treaty selection by investor for the pharmaceutical scenario. Outstanding data gap: China-France BIT (2007) Protocol administrative exhaustion formulation unconfirmed — filling this gap is the minimum prerequisite for a complete typology. Output: analytics/china-bit-gate-typology.md. Effort: low (one targeted search for China-France BIT Protocol; then tabulate confirmed findings). _Status: proposed._
- 2026-07-18 — Cross-BIT provision consistency check: when a new Protocol or Exchange-of-Notes provision is discovered for one Chinese BIT in scope, immediately run the same targeted search for all other Chinese BITs in scope to verify whether the same provision exists, and in what form. Triggered by today's finding: the China-Netherlands BIT (2001) Exchange of Notes contains the same administrative review exhaustion pre-condition (maximum three-month period before ICSID filing) as the China-Germany BIT (2003) Protocol Article 9(a) — a cross-BIT architectural pattern confirmed in a single research session by laterally applying the July 18 China-Netherlands Protocol search. The project had documented the China-Netherlands Article 10 court-withdrawal asymmetric architecture (July 9) but had not searched for the matching administrative exhaustion provision until today, reflecting a gap in the cross-treaty lateral-verification step. Distinguishes from: (a) Chinese BIT Protocol provision audit (July 15) — a retroactive one-time mapping of existing BITs in scope; (b) New-treaty-intake Protocol scan (July 17) — a forward-looking rule for newly identified BITs; this proposal is a triggered lateral-search rule: every new Protocol/Exchange-of-Notes provision discovery triggers a same-provision verification search across all other in-scope BITs. Effort: very low (one triggered search per new provision discovery; immediate applicability to all four core China BITs). _Status: proposed._
- 2026-07-17 — New-treaty-intake Protocol scan: when a BIT or FIPA is first identified as a Ring 1 treaty vehicle in the project record, execute a complete Protocol provision reconstruction in the same intake session: (a) attempt UNCTAD Navigator download of the Protocol text; (b) run targeted searches for all Protocol sub-provisions, using the provision number plus keywords covering administrative exhaustion, temporal gates, concurrent-proceedings, fork-in-the-road, and local-litigation requirements; (c) record the full sub-provision architecture in STATE_OF_THE_ANSWER.md before the treaty enters the open-thread list. Triggered by today's finding: Protocol Article 9(b) of the China-Germany BIT (2003) — a mandatory three-month temporal gate between administrative review initiation and ICSID filing — was absent from the project record until Day 19 of active monitoring, despite Article 9(a) being discovered July 15 and Article 9(c) July 11 (a 19-day sequential-discovery lag). The intake session that added the China-Germany BIT to scope (June 28) could have reconstructed all three sub-provisions at once. Distinguishes from the July 15 retroactive Protocol audit (one-time backward-looking map of BITs already in scope; this is a forward-looking intake rule applied to BITs newly entering scope). Effort: very low (one additional structured multi-keyword search at first intake; no ongoing cost). _Status: proposed._
- 2026-07-15 — Chinese BIT Protocol provision audit: one-time systematic mapping of all Protocol Article 9 sub-provisions for each of the four key China BITs in scope (China-Germany 2003; China-Switzerland 2009; China-France 2007; China-Netherlands 2001) to document the complete pre-ICSID gate architecture. For each treaty: retrieve or reconstruct the full Protocol Article 9 text and map every sub-provision with its operative condition (administrative exhaustion gate, concurrent-court-proceedings gate, fork-in-the-road vs. no-U-turn structure, local-litigation period, MFN-scope restrictions). Commit result as a treaty-comparison table to analytics/china-bit-protocol-map.md. Triggered by today's finding: Protocol Article 9(a) of the China-Germany BIT (2003) — a mandatory administrative-exhaustion pre-condition to ICSID arbitration — was not in the project record until July 15, despite the project having documented Article 9(c) since June 28 (a 17-day gap). A complete Protocol mapping at project inception (or when a treaty is first identified as a Ring 1 vehicle) would prevent such gaps. Distinguishes from: Tagged-Case Posture Tracker (case dockets, not BIT text); Host-state domestic law monitor (regulatory law, not treaty provisions); Academic Case Note Intake (journals, not treaty text); Pre-ICSID concurrent-proceedings monitor (investor conduct, not BIT text); MFN-scope tag in fingerprint (classifier, not BIT text audit); Counsel identification step (post-ruling investigation, not BIT text). Effort: low to medium (one-time structured text search per BIT; Protocol text from UNCTAD Navigator and EDIT/WTI; likely 403-blocked at primary URL but recoverable from academic commentary). _Status: proposed._
- 2026-07-13 — Counsel identification and counsel-victory-search step: when a new ruling is confirmed in a tracked case, (a) identify the claimant's counsel from the case docket (italaw, UNCTAD Navigator, IAReporter coverage) and add their firm name to the search query bank; (b) within days of the ruling, search "[counsel firm name] + [claimant name] + victory" or "[counsel firm name] + [case name]" to find the winning party's immediate press release; (c) treat the winning party's counsel announcement as a MEDIUM-HIGH confidence source for the operative disposition (remand/restore/affirm) — biased in tone but reliable on procedural facts determined by the court's order. Triggered by today's finding: Westerberg & Partners ("Hallmark victory before the Swedish Supreme Court," 403-blocked) provided the Svea Court of Appeal remand language for the Okuashvili Swedish SC ruling (June 26, 2026) — a material posture correction (SCC arbitration is NOT immediately restored; remanded to Svea CoA) — 17 days after the ruling and before any neutral practitioner analysis (Baker Botts, Vinge post-ruling) has appeared. The prior project search had targeted Vinge and Baker Botts (Georgia's counsel, the losing party) without also identifying and searching Okuashvili's own counsel (Westerberg). Winning-party counsel announcements are immediate (same day or within days of ruling); they are structurally more likely to document the full operative disposition (including remand instructions) than neutral observers who may summarise only the headline outcome. Distinguishes from: Tagged-Case Posture Tracker (queries UNCTAD Navigator/italaw/ICSID — not law firm websites); Academic Case Note Intake (targets peer-reviewed journals — 6–12 month lag); Pre-filing BIT dispute notice monitor (targets notices, not post-ruling announcements); State-Respondent Post-Award Challenge Monitor (respondent state counsel after awards, not claimant counsel after interim rulings). Effort: very low (add counsel names to search bank when a new ruling is logged; one additional targeted search step per ruling). Immediate applications: Wingtech/Nexperia (identify Wingtech's BIT counsel; search client announcements); future Einarsson v. Canada award (identify claimant's counsel from UNCT/20/6 docket). _Status: proposed._
- 2026-07-12 — Sector labeling in insights.jsonl: add a `sector` metadata field to each entry in `analytics/insights.jsonl` with enumerated values: `"pharmaceutical"` (clinical data, drug approval, pharmaceutical IP), `"ip-non-pharma"` (trade marks, patents, copyright in non-pharmaceutical contexts), `"data-digital"` (data assets, algorithms, software — non-pharma), `"general"` (no IP or data nexus — real estate, manufacturing, financial). Retrospectively label the 25 existing entries. Apply prospectively to all new entries. Purpose: enable the council to distinguish (a) precedents directly from pharmaceutical-sector ISDS cases (Eli Lilly: pharmaceutical); (b) structural analogues from data/IP cases in other sectors applicable by analogy (Einarsson: data-digital; Bridgestone: ip-non-pharma); (c) Ring 3 precedents from non-IP general ISDS disputes applicable by structural reasoning (Hela Schwarz: general/land-use; Jason Yu Song: general/expropriation; Philip Morris: general/regulatory). Triggered by today's confirmation that Hela Schwarz GmbH was a food-additives/real-estate company (not pharmaceutical), making Hela Schwarz a `"general"` sector case whose Article 9(c) Protocol holding applies to pharmaceutical claims by cross-sector structural analogy — a materially different analogical distance than if the case were pharmaceutical. Without a sector field, future analysts reading the ledger could misread the precedent's directness. Distinguishes from: content-type metadata tag (July 7, classifier output metadata); Ring 3 MFN-scope tag (July 2, fingerprint); all other 13 proposals (none address insight ledger schema enrichment). Effort: very low (one-time schema extension; retrospective labeling of 25 entries). _Status: proposed._
- 2026-07-10 — Academic case note intake: add targeted quarterly searches of top international law journals — AJIL (American Journal of International Law), ICLQ (International and Comparative Law Quarterly), ICSID Review, and JIDS (Journal of International Dispute Settlement) — for case notes and International Decisions analyses of cases already in the project record (Hela Schwarz v. China, Einarsson v. Canada, Jason Yu Song v. China, Okuashvili v. Georgia, Wingtech v. Netherlands). Triggered by today's discovery: the American Journal of International Law published a case note on the Okuashvili v. Georgia SCC Partial Award (August 31, 2022) in 2023, with an open-access ResearchGate PDF, documenting the MFN/dual-nationality analysis that the Swedish Supreme Court upheld on June 26, 2026. The note may contain verbatim award excerpts unavailable from paywalled primary sources. Academic case notes in these journals systematically reconstruct primary-text holdings — verbatim excerpts, specific article numbers, doctrinal critique — and are often available open-access from ResearchGate or university repositories where paywall-blocked primary sources are not. The council's intake currently covers practitioners (IAReporter, Kluwer), NGOs (IISD ITN), primary dockets (italaw, UNCTAD Navigator), and web-search snippets of 403-blocked PDFs — no academic journal track. **Immediate application:** search ICSID Review and JIDS for case notes on Hela Schwarz GmbH v. China (ICSID ARB/17/19, Award December 10, 2025) — a case note, if published in the 6–12 months following the December 2025 award, would likely contain verbatim Protocol Article 9 language and the specific sub-provision designation, upgrading the current MEDIUM/LOWER confidence identification. Effort: low (quarterly targeted searches, keyed to case names). Distinguishes from all 12 existing proposals (which address regulatory monitoring, docket tracking, pre-filing intake, post-award proceedings, or classifier quality — none target academic synthesis). _Status: proposed._
- 2026-07-09 — Pre-ICSID Concurrent-Proceedings Monitor: add a monthly monitoring track for each case in the project record that has a pending BIT notice or is in the cooling-off period before ICSID registration. Search: (a) investor name + "court" or "domestic proceedings" in each relevant jurisdiction; (b) major news aggregators (Bloomberg, Reuters, Caixin) for litigation updates; (c) ICSID case database for formal registration. Triggered by today's finding: Wingtech/Nexperia had added an AFSL claim in China's Dongguan Intermediate People's Court (accepted May 22, 2026) and a new Dutch court filing (June 8, 2026) since the July 5 project record — both discovered 6–10 weeks after the fact. Concurrent domestic proceedings during the cooling-off period are the exact factual predicate for a Hela Schwarz-type Ring 3 dismissal. Distinguishes from: (a) Pre-filing BIT dispute notice monitor (July 5) — catches new notices, not concurrent proceedings alongside existing notices; (b) Tagged-case posture tracker (July 3) — monitors registered ICSID cases, not cooling-off period; (c) State-Respondent Post-Award Challenge Monitor (July 8) — post-award national court proceedings. Effort: low (monthly targeted searches, keyed to currently monitored pending cases). Immediate application: Wingtech/Nexperia. _Status: proposed._
- 2026-07-08 — State-Respondent Post-Award Challenge Monitor: add a periodic search track targeting post-award judicial proceedings for cases already in the project record — specifically: (a) set-aside applications at the seat of arbitration (Swiss Bundesgericht for PCA-Geneva cases; Svea Court / Swedish SC for SCC cases; English High Court for LCIA/ICC-London cases); (b) enforcement challenges at key enforcement courts; (c) parallel domestic proceedings (criminal or civil) that state-respondents invoke as grounds for post-award jurisdictional challenges. Triggered by today's discovery: China's two Swiss FSC set-aside applications against the Jason Yu Song v. China award (4A_46/2024, dismissed April 17, 2025; 4A_528/2024, dismissed June 26, 2025) were in the public record for over a year but absent from the project record. The second challenge used a Chinese criminal court judgment arguing illegal investment — a post-award jurisdictional attack on the only publicly known ISDS award against China. Implementation: quarterly targeted searches of the Swiss Bundesgericht public search (bger.ch) and domstol.se for Swedish SC, keyed to case names and parties from the project's insight ledger. Distinguishes from the Tagged-Case Posture Tracker (July 3) which targets UNCTAD Navigator, italaw, and ICSID case pages — national court post-award challenge proceedings for PCA/SCC cases do not appear reliably on those databases. Effort: low. _Status: proposed._
- 2026-07-07 — Content-type metadata tag for IAReporter "Looking Back" articles: add a `content_type` metadata field to the classifier output with values including `new_filing`, `new_ruling`, `retrospective_review`, `commentary`, and `regulatory`. When the source is IAReporter Headlines and the title begins with "Looking Back:", assign `content_type: retrospective_review` and apply a relevance score cap of 30. Triggered by today's digest item — the first IAReporter "Looking Back" retrospective (Suez v. Argentina (1), ICSID ARB/03/17, 2006) to appear in the digest. The current system correctly scored it at 25 (below threshold), but the retrospective format was not immediately distinguishable from a new-ruling item by title alone. Impact: (1) Allows analytics officer to distinguish new-development from retrospective-survey receptivity in per-source yield analysis; (2) Enables the chairman to calibrate backsource work — `retrospective_review` items are contextual rather than new developments; (3) Prevents historical retrospectives from ever reaching the Attention threshold (≥ 40). Extend to other known retrospective/review series as they appear. Effort: low. _Status: proposed._
- 2026-07-06 — Ring 2 classifier: require state-as-measure-taker. Add a mandatory sub-criterion to the Ring 2 "judicial or regulatory measure" definition in `fingerprint.yaml`: the measure must be taken by or attributable to a state or state body acting in its public capacity within a treaty or investment law framework — not a domestic court adjudicating a private commercial dispute between private parties. Triggered by today's backsource of digest item 2 (June 29): the classifier scored the Court of Appeal of Santiago's set-aside of the Joyvio/Australis commercial arbitration award at Ring 2 (score 25, WATCH) because a court issued a decision affecting an award — but Joyvio/Australis is a private commercial dispute with no BIT, no state party, no IP-as-investment nexus. The "judicial measure" Ring 2 concept in the research question requires a state act challenging an investor's treaty-protected rights. Implementation: add `measure_taker: state_or_state_body_in_public_capacity` as a required Ring 2 sub-criterion in `fingerprint.yaml`, with Joyvio/Australis as a labeled false-positive exemplar and Einarsson (Canada petroleum disclosure regime) as a labeled true-positive exemplar. Impact: eliminates the false-positive class of domestic commercial arbitration set-aside decisions; reduces low-relevance IAReporter noise. Effort: low. _Status: proposed._
- 2026-07-05 — Pre-filing BIT dispute notice monitor: add a periodic search track targeting Chinese stock exchange disclosures, IAReporter notices-of-intent/arbitration coverage, and company announcement databases (EDGAR 8-K, RNS/Regulatory News Service) for IP/technology/pharmaceutical BIT dispute notices filed under Chinese BITs (or by Chinese investors against European respondent states) during the pre-ICSID-registration cooling-off period. Triggered by the Wingtech/Nexperia v. Netherlands discovery (formal notice October 2025; found July 5, 2026 — 9-month lag): a Chinese investor filed a USD 8 billion BIT dispute notice over IP/technology assets in October 2025; the Kluwer Arbitration Blog analyzed it in January 2026; the project found it only in July 2026 because the digest's scoring architecture tracks registered ICSID cases, not pre-filing notices. Distinguishes from: tagged-case posture tracker (tracks *registered* cases in the project record); full-text intake source (credentialed ISDS dockets — post-registration); host-state domestic law monitor (regulatory changes, not dispute notices). This addresses the pre-registration intake gap. Effort: low (monthly searches of IAReporter notices, Chinese stock exchange filings on IP/BIT disputes). _Status: proposed._
- 2026-07-04 — Host-state domestic law monitor: add a dedicated periodic search track targeting regulatory developments in key BIT host states (China, India, Brazil) for domestic law changes that define, protect, or restrict trade secrets, pharmaceutical data, and data assets in ways that affect the Ring 1 covered-investment characterization and Ring 2 triggering-measure analysis. Triggered by today's SAMR Order No. 126 finding (issued February 24, 2026; surfaced by the project July 4, 2026 — a 4+ month lag): China's most substantial trade-secret reform in 30 years, explicitly covering "data," was not in the project record until a proactive T04 search today. Distinguishes from existing ideas: tagged-case posture tracker follows ISDS case progression; full-text intake source covers ISDS dockets; cross-model verifier addresses quality. This addresses a structural intake blind spot — the project monitors what ISDS tribunals say about trade secrets, but not what key host states are doing domestically to define and protect the very assets at issue. Implementation: 4–6 targeted searches per month covering SAMR, NMPA, MOFCOM (China), DPIIT, MoHFW (India), ANVISA, INPI (Brazil) plus EU pharmaceutical regulatory developments. Effort: low. _Status: proposed._
- 2026-07-03 — Tagged-case posture tracker: add a periodic docket-watch module that queries UNCTAD Navigator, italaw, and ICSID case pages for cases already in the project record (tagged by ring assignment and case identifier in the insight ledger), surfacing new documents or proceedings to the chairman's agenda when the case progresses to a new institutional or judicial layer (e.g., annulment proceeding, appeal filing, Supreme Court ruling). Addresses the temporal-depth gap: in *Okuashvili v. Georgia*, the Svea Court of Appeal ruling (November 2024) and the Swedish Supreme Court leave-grant (September 2025) were not surfaced during the nine months between those events and the final Supreme Court ruling (June 2026). Distinguishes from existing ideas: cross-model verifier addresses *quality*; full-text intake addresses *breadth*; agent-SDK subagents address *architecture*. This addresses *temporal depth* — following known cases through their full institutional and judicial lifecycle, not just at first intake. _Status: proposed._
- 2026-07-02 — MFN-scope tag in Ring 3 fingerprint: add `mfn_dispute_resolution_scope` as a dedicated Ring 3 classifier tag in `fingerprint.yaml` to surface cases where an MFN clause is applied to dispute-resolution provisions (the Okuashvili mechanism). Currently the Ring 3 fingerprint captures `treaty_shopping`, `abuse_of_right`, and `critical_date` but does not specifically flag MFN-forum-access as a distinct mechanism. Adding the tag would: (a) surface future cases combining MFN-scope forum import with an IP investment claim at a higher Ring 3 relevance score; (b) flag Chinese BIT cases invoking this mechanism, providing a monitoring signal if doctrine shifts. Triggered by Okuashvili v. Georgia (Swedish SC, June 2026); confirmed closed for China BITs by July 2 session. Effort: low. _Status: proposed._
- 2026-07-01 — Targeted quoted-phrase search as substitute for 403-blocked PDFs: when a PDF is consistently inaccessible (UNCITRAL site, ICSID, italaw), construct search queries combining the document symbol (e.g., "A/CN.9/1246"), known subject-matter terms, and hypothesized specific language to trigger search-engine snippets that may include verbatim or near-verbatim document language. Today's example: querying "A/CN.9/1246 supplementary provisions scope application temporal 1 April 2027" returned a snippet with language appearing to reproduce document phrasing ("they could apply, for example, to treaties concluded on or after 1 April 2027"; "may need to be developed"), yielding a medium-confidence characterisation of the scope provision without direct PDF access. This technique is immediately actionable and complements (without substituting for) the UN ODS direct-retrieval proposal from June 30. **Correction 2026-07-31 (systems researcher, on the 2026-07-30 record): re-sequenced from substitute to fallback.** That session weakened this entry — two direct fetches of hosts the record treated as blocked (icsidfiles.worldbank.org, Procedural Order No. 8 in Huawei v. Sweden; uncitral.un.org/sites/default/files/2026-06/sps.pdf) each returned a full read, where the quoted-phrase technique had yielded only a medium-confidence characterisation of comparable material — so the correct sequence is to attempt the direct fetch first and fall back to quoted-phrase reconstruction only once a fetch has actually failed; the title's "substitute" framing is superseded, and a search snippet must never be cited in place of a document that an unattempted fetch would have produced (the 2026-07-30 B4 objection is what that failure looks like in practice). _Status: proposed; re-sequenced 2026-07-31 to fallback-after-attempted-fetch (was: substitute for a blocked PDF)._
- 2026-06-30 — UN ODS as alternative UNCITRAL document retrieval route: UNCITRAL PDF links at uncitral.un.org/sites/default/files/ are consistently 403-blocked across sessions (A/CN.9/1246, session agenda PDF, and others). UN Commission documents (A/CN.9/... series) are submitted to the General Assembly and archived in the UN Official Document System (https://documents.un.org), which provides free public access to UN official documents. Searching the document symbol "A/CN.9/1246" in the UN ODS may successfully retrieve documents otherwise inaccessible via UNCITRAL's own site. This would directly resolve the open thread on A/CN.9/1246 temporal scope (non-retroactivity confirmation) and the broader UNCITRAL document access bottleneck. **Correction 2026-07-31 (systems researcher, on the 2026-07-30 record): the access premise stated above is partly falsified.** The opening justification — that UNCITRAL PDF links at uncitral.un.org/sites/default/files/ are "consistently 403-blocked across sessions" — does not hold for that directory as a whole: https://uncitral.un.org/sites/default/files/2026-06/sps.pdf fetched cleanly on 2026-07-30 and was read in full, so the 403 pattern is path-specific rather than directory-wide or site-wide, and no path under that directory may be recorded as blocked without an attempted fetch. The recommendation itself is unaffected, because it rests on this entry's independent second ground — that A/CN.9 documents are submitted to the General Assembly and archived in ODS by symbol — which is a document-identity argument, not an access-failure one; ODS remains the right canonical route for A/CN.9 symbols. _Status: proposed; justification corrected 2026-07-31 (access premise partly falsified; recommendation stands on the symbol-archiving ground)._
- 2026-06-23 — Cross-model verifier: run the security/integrity officer on a different model from
  the analyst (the single-threaded generator–verifier pattern the literature endorses). Staged as
  Stage 1 in `MULTI_AGENT_ROADMAP.md`. _Status: proposed._
- 2026-06-23 — Real full-text intake source: add a credentialed/structured docket feed
  (Jus Mundi / ICSID / UNCTAD Navigator) to widen recall beyond headline-only sources, since web
  search deepens known items but does not widen intake. _Status: proposed (needs access/$)._
- 2026-06-23 — Agent-SDK subagents on Max in cron: a researcher subagent + an isolated-context
  verifier subagent, billed to the Max headless pool. The genuine multi-agent step. _Status:
  proposed (Stage 2 in the roadmap)._

<!-- graph:auto start -->
Map: [[Evidence Ledger]]
<!-- graph:auto end -->
- 2026-08-29 (chairman) — Quote a pre-registered branch when grading it; never restate it. When a record reports whether a pre-registered test fired, the test's terms must be **pasted from the committed request file**, not paraphrased from memory. Triggered by `0829-B2`: the analyst restated a five-element pre-registered signature as four, silently dropping the one element that did not conform — `Status of Proceeding: English/Concluded`, which was in fact present in the window — **inside the sentence reporting whether that test fired**. Nothing downstream could ever have surfaced it, because the only witness is the pre-registration and the only reader likely to check it is the seat that wrote it. The conclusion was unaffected, which is precisely what makes the class dangerous. This council already requires that a pre-registration is never edited and that the record is measured against it; what it does not require is that a branch, when graded, is quoted rather than paraphrased. Effort: none — it is a writing discipline, and the quote already exists in a committed file. **Prior-art screen, at the log's true and incomplete scope:** this log holds 38 distinct dated days across 40 entries and is disclosed by its own 2026-08-18 entry as missing 08-01, 05, 06, 07, 10–17, 19, 25, 27 and 28; over those 40 entries none addresses quoting-versus-restating a pre-registration. Nearest neighbour is the 2026-08-24 entry below, which requires a correction block to state the screen that produced its enumeration — that governs *corrections*, not *branch gradings*. The daily records were not screened, so this is a claim against the log and not against the wider corpus. _Status: proposed. Nothing built, nothing applied._
- 2026-08-29 (research analyst) — Pin a corpus figure to the `analytics/fetch-results` **subtree object**, not to the commit sha. The 2026-08-28 item requires a figure derived over the results corpus to carry "the tree state it was derived at"; it does not say which identifier, and the obvious reading — the commit sha — is the one that fails. Triggered by a live near-miss: `git rev-parse HEAD` moved under the analyst mid-session (a `[skip ci]` record commit landed between his brief and his first derivation), so figures derived at `8d57f48` and at `2f9075e` would have looked non-re-derivable against each other — while `analytics/fetch-results` was **byte-identical at both** (`git rev-parse <rev>:analytics/fetch-results` returns `615ef41d…` at each). The subtree object changes **if and only if** the corpus changes, which is exactly the invariant a re-derivability anchor needs. Proposed form: a corpus figure carries `HEAD <commit>, analytics/fetch-results subtree <sha>`, and a later reader seeing a matching subtree sha may reuse the figure without re-running anything. **What it does not do, named:** it does not touch the overwrite defect (N6-as-upgraded / N12) — a subtree sha records *which* corpus a figure came from, it does not stop the corpus being rewritten under a path — and it does not substitute for running the command rather than copying a handed-down anchor, which is what actually made the near-miss visible. **Prior-art screen at the log's true scope, seven literals, all zero:** `tree state`, `subtree`, `rev-parse`, `commit sha`, `blob sha`, `re-derivab`, `by SHA and never by path`. **Nearest neighbours on two axes, per `0829-B8` sustained:** on the object axis, the 2026-08-28 item (not in this log); on the **identifier** axis and closer, the 2026-08-24 entry below, which requires a correction block to state "the literals tried, **the anchor commit**, and the hit count" — the proposal being precisely that the anchor commit is the wrong identifier. Synonym screen, occurrence counts: `anchor` 11, `HEAD` 10, `path` 11, `sha` 6, `re-derive` 3. Effort: low (one extra `git rev-parse` per figure; no code change). _Status: proposed. Nothing built, nothing applied._
- 2026-08-24 — Correction blocks must prove their own coverage: when a dated correction block in a living file (`STATE_OF_THE_ANSWER.md`) enumerates the instances it corrects, it must also state **the screen that produced the enumeration** — the literals tried, the anchor commit, and the hit count — so the next seat can re-run the screen instead of counting bullets. Triggered by `0824-B1`: the 2026-08-23 `0823-B8` block enumerated four offending rows asserting the *Hela Schwarz* tribunal's disposition; there were **five**, and the fifth built a doctrinal inference on the withdrawn premise. The chairman grepped one literal, found three surviving hits, read four bullets, concluded four covers three, and published a self-correction saying the repair was complete — matching a count against a count, never an instance against a bullet, and missing the row whose wording used a synonym his literal could not match. Had the block carried *"screened on `dismissed on jurisdiction`, `dismissed all`, `upheld the … defense`; 4 hits at `<sha>`"*, one line would have shown that a single literal had been tried and a synonym search was owed. Distinguishes from existing entries: no entry in this log addresses correction blocks, enumeration completeness, or self-proving screens; the nearest neighbours are about citation anchors (rule (s)) and about figure derivation (`0823-B2`), both of which govern *how a claim is pointed at* rather than *whether a correction reached everything it claimed to*. Effort: low — it is a writing discipline, not a build. Related, adopted into the record and **routed to the archivist rather than written by this session** (`agents/` is outside the daily council's merge scope): two taxonomy entries proposed by the integrity officer — **ENUMERATED-CORRECTION CLOSURE** (an incomplete enumeration trusted by a later seat as proof of coverage) and **PHANTOM-CORPUS ZERO** (a figure published at a corpus scope wider than the code that derived it; instance `0824-E9`). _Status: proposed._
- 2026-09-03 (chairman) — **Read the reduction's own `request` and `environment` header fields before reading any row as an answer to your batch, and name a same-day second batch so that it sorts LEXICOGRAPHICALLY ABOVE the first.** `scripts/fetch_relay.py`'s `newest_request()` is `sorted(glob('*.json'))[-1]` — a lexicographic maximum over filenames, **not** a modification time, despite its name — and `.github/workflows/fetch-relay.yml` runs the script bare, so the workflow always takes the lexicographic maximum of the whole request directory. Triggered by a live near-miss: today's second batch was committed as `2026-09-03-daily-b.json`, which sorts BELOW `2026-09-03-daily.json` (`-` is 0x2D, `.` is 0x2E), so run `33750052352` silently re-ran the MORNING request and wrote its output over run `33748910532`'s result — **and reported success.** Had the rows been read without checking provenance, a forecast would have been graded against a batch that never ran. The remedy is free: the manifest already carries `"request"` and `"environment"` in its own header, so one look confirms which request a run answered; and one naming convention makes a same-day second batch reachable at all. **What it does NOT do, named:** it does not touch the in-place overwrite defect (`run()`'s `out_path = out_dir / request_path.name` at `:228`), which is a **distinct** defect — the integrity officer sustained that distinctness at `0903-B6` after testing it four ways — and it does not stop a result path being rewritten under a citation. **Prior-art screen, run whitespace-normalised per `0903-B19` and at the log's own disclosed-incomplete scope:** `newest_request` 0, `lexicograph` 0, `mtime` 0, `request file` 0, `--request` 0, `provenance` 0. Nearest neighbour is the 2026-08-29 analyst entry below, which pins a corpus figure to a subtree object — that governs WHICH corpus a figure came from, not WHICH REQUEST a run answered. Effort: none — it is a reading discipline. **The code fix (sort by `st_mtime`, or pass `--request` explicitly in the workflow) is the operator's call.** _Status: proposed. Nothing built, nothing applied — `scripts/fetch_relay.py` and every workflow are untouched by this session._

