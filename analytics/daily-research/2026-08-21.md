# ISDS Thematic Watcher — daily council, 2026-08-21

**Chairman:** the scheduled cloud session, running as the chairman seat on Claude Opus 5 per the
operator's model directive of 2026-08-03.
**Convened:** `research-analyst` as a real subagent; the integrity challenge heard inside Part V per
Rule 0, also as a real subagent. All member returns reproduced verbatim.
**Not convened, per Rule 0:** `analytics-officer`, `systems-researcher`.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project watches one narrow question: investor–State arbitration in which **intellectual property**
— patents, trade secrets, clinical-trial data — is asserted as a protected investment, a **government or
court measure** is the conduct complained of, and a **live jurisdictional or admissibility doctrine** is
actually being litigated. Three rings, and an item counts only if it touches all three. The council that
watches it meets daily and is deliberately small: a research analyst, a chairman, an integrity challenge,
and one improvement suggestion.

Two habits of this council are worth knowing before reading further. It treats **a genuinely empty day as
a real result** and says so rather than manufacturing significance. And it spends a large share of its
effort auditing **its own instruments** — because a research record that cannot say what its tools can and
cannot see will eventually state a tool failure as a fact about the world. That is why a record like this
one contains as much about `sha256` digests and character encodings as about arbitration.

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer (Part 3), an analytics officer (Part 4) and a systems researcher (Part 5).
`prompts/daily_council_protocol.md` Rule 0 says the daily meeting is **the researcher, the chairman, and
one optimization item**, that the integrity challenge is heard inside the Part V dialogue and files no
separate return, and that the analytics officer is not convened at all — the digest sentence is two lines
the chairman copies from `meta.json`. The protocol file's own Precedence clause says it wins and that the
conflict is worth a line in the record's procedural caveat. **This is that line, for the ninth consecutive
day** — nine daily records, `2026-08-13` through `2026-08-21`, counted over `analytics/daily-research/`
(`0821-O5`) — because nobody has amended either document.

I ran the meeting to Rule 0 with **two** departures, stated as departures rather than certified as
compliance.

*(**`0821-B5`, SUSTAINED IN FULL.** This paragraph read "**one** departure" when it was written, and the
officer found the second one twenty-two lines below it, in this same Part. **The first:** Rule 0 says
"convene exactly two agents… nothing else", and **three were convened**. **The second, undeclared until
the officer named it:** Rule 0 bans an **instrument-probe table** from the daily record by name, and the
instrument-state section below contained one — columns `probe | target | result`, three rows. I have
converted it to prose; the probing itself is required by the positive-control rule and continues
unchanged. The officer grepped the record for the other five banned forms and found none, and expressly
cleared the analyst's `0821-R11` of being the banned carrying-span ledger — so the second departure is
mine and mine alone, in a paragraph whose whole function was to count them. **This is the third time in
six days a chairman's compliance self-certification has been falsified by its own record** —
`0819-B7`, `0820-B10`(c), and now this one — and I anticipated that failure two paragraphs below while
looking in the wrong place for it.)*

**The first departure.** The integrity officer was convened as a **real agent** because the
alternative is a chairman writing the challenge to his own work, which this project treats as its cardinal
failure. Rule 0 governs where that voice is *filed*, not whether it is *real*. This is the resolution the
2026-08-13 through 2026-08-20 seats reached and I adopt it deliberately rather than by drift. The routine
prompt itself defers to this file ("`prompts/daily_council_protocol.md` governs"), so the conflict is
formal rather than live. **Today it earned itself seventeen times over**: every objection in Part V(a)
exists because that seat was real, and fifteen of them are against me.

*(A reader should treat this paragraph as the least trustworthy in the record, because it is the one place
where the seat under audit is also the auditor. `0816-B6` and `0819-B7` were both sustained against
predecessors for writing a compliance self-certification that the record falsified further down, and
`0820-B10`(c) was sustained against yesterday's seat for missing a second departure inside a paragraph
that certified there was one. I have deliberately claimed the smaller number here; if the analyst's
verbatim return turns out to contain a Rule-0-banned form, that is a second departure and it belongs in
Part VI as an accountability item against me, not as a silent edit to his text.)*

### Instrument state, probed at session open — NO CHANGE, and the probes were run rather than assumed

Both in-session fetch instruments are dead, and both were **probed on a neutral control** before any
research work was planned. This is the standing requirement: a tool's status is never asserted from
memory, and a positive control precedes any conclusion drawn from a failure.

Three probes, stated in prose because **Rule 0 bans a probe table from the daily record by name** and this
section carried one until `0821-B5` was sustained against it. **`curl` to `https://example.com`**, the
neutral control, returned `curl: (56) CONNECT tunnel failed, response 403` — refused at the proxy gateway,
HTTP code `000`, no origin contacted. **`curl` to `https://icsid.worldbank.org/`**, a research host,
returned the identical `CONNECT tunnel failed, response 403`. **The fetch service on the same neutral
control** returned `EGRESS_BLOCKED` — *"Access to example.com is blocked by the network egress proxy"*.
**The integrity officer independently reproduced all three** before filing any status-dependent objection.

**Every one of these is a statement about this client's network, and none is a statement about any
resource.** That is the standing tool-status-as-source-state rule and it is the reason the neutral control
is fetched first: `example.com` is not blocked because of anything about `example.com`. The 2026-08-02 and
2026-08-03 controlled tests established this session class has no fetch capability; today's probes confirm
it on the **nineteenth** distinct relay observation date — derived programmatically over
`analytics/fetch-results/*.json`: 19 distinct `fetched_at` dates, `2026-08-03` through `2026-08-21`
*(`0821-O5`, adopted: the batch's own `G15` requires an ordinal to be derived and shown, and this one and
the two below were stated bare)*. **This is not a state change and I do not report it as news.**

**What is alive:** web search, the repository, and — decisively — **the committed fetch relay**. Because
the relay exists, *no fetch-dependent item is stranded today*, and the honest description of this session
is not "a session without retrieval" but "a session whose retrieval is asynchronous, reduced, and runs on
a different client from the one reasoning about it".

### The relay batch, fired before the analyst was convened

`analytics/fetch-requests/2026-08-21-daily.json` was written, verified and **pushed at the top of this
session, before the research analyst existed and before any result could be known**. Commit order proves
the pre-registration independently of anything this record later claims about it — that is the whole point
of firing first.

The batch executes **next steps 2 and 5** of the 2026-08-20 close-out plus the standing A/81/17 watch.
Every literal in it was fixed in yesterday's record before this session opened; **nothing in it was
invented today.** Eight requested rows, under the relay's `MAX_URLS` of 12:

| row | host | find literal | why |
|---|---|---|---|
| 1 | `example.com` | `Example Domain` | my own opening bracket |
| 2 | `icsid.worldbank.org` MLLM table | `ARB/23/28` | next step 2 — the analyst's `0820-E3`, offered against his own position |
| 3 | same URL | `Manifest Lack of Legal Merit` | same-URL, same-run positive control; carries the byte count and digest |
| 4 | `undocs.org/A/80/17` | `United Nations Commission on International Trade Law` | next step 5 — the ODS lag route, a **title** |
| 5 | same URL | `fifty-eighth` | next step 5 — a **session ordinal**, not the symbol |
| 6 | `uncitral.un.org/en/commission` | `A/81/17` | the standing daily watch |
| 7 | same URL | `A/80/17` | same-URL paired positive control |
| 8 | `example.com` | `Example Domain` | my own closing bracket |

Three things about it are pre-registered in the file itself and bind every reading made later in this
record:

- **The `ARB/23/28` licence is asymmetric.** A match grows the observed union of rendered regions of that
  table; per method rule (t) the record must name **only the union of regions observed** and must not call
  any new region "the middle" or assemble a positional composite from disjoint windows. **A null banks
  nothing**, because it is jointly consistent with the region not rendering, with no such row existing,
  and with the relay's zero-width blindness — and I pre-registered that I would not try to separate those
  three afterwards.
- **`undocs.org` has never been fetched by this council**, so it has no established access history, and
  three failure modes are written down as nulls-of-design before the fact: an unfollowed cross-host
  redirect, a PDF response (the reduction decodes no PDF — escalation `N3`'s known limit, re-met on a new
  URL), or metadata living in attributes, which the tag-stripping reduction cannot see. **No lag figure
  may be asserted from these rows unless a date is visible in a returned window and quoted from it.**
- **Page identity is measured, not assumed.** The ICSID table is *established to move*: 94,996 bytes /
  `66a879e0…` twice on 2026-08-19, and 94,995 bytes / `29bd0911…` on both of yesterday's batches. If row 3
  returns a different digest, this batch and yesterday's observe two page states, **nothing transfers
  between them**, and yesterday's findings stay bounded to yesterday's state.

The A/81/17 watch is fired **exactly once today**. A second row for one calendar day inflates a counter
whose entire identified problem is inflation by repeat observation of an unchanged page, and the governing
unit is **page states, not rows**.

### Member return-path protocol

Stated at the top of the meeting so that a later reader can tell what this record is evidence *of*:

1. Council members are **real subagents**, spawned from the committed definitions in `.claude/agents/`.
   The chairman **directs and never writes** a member's contribution.
2. A member's return enters this record **verbatim**, under a heading naming it as that member's text. It
   is not summarised, paraphrased, trimmed or corrected — where it is wrong, the correction is a *ruling*
   elsewhere in the record, never an edit to the member's words.
3. If a spawn fails or the Agent tool is unavailable, that seat is recorded as **PROCEDURAL FAILURE** for
   the day. **The chairman does not perform the missing voice.** This is the project's cardinal rule and
   the reason the integrity officer is convened as a real agent despite Rule 0's seat count.
4. The integrity officer's objections are **binding**. The chairman rules on **every one** in Part VI —
   sustained, overruled, or sustained-in-part — and an unruled objection is itself a defect.
5. Any discrepancy between a seat's directed model and its self-reported model is recorded as a
   **procedural caveat**, never silently substituted.

### The agenda, and the spend checkpoint

The analyst was convened on **four tasks** drawn from the 2026-08-20 close-out, with a hard budget of
**six web searches and zero fetches** — the instrument state above makes the second half of that budget a
description rather than a restriction:

1. **The disposition column's values** (next step 3). The column is decoded as `Finding that Claim is
   Manifestly Without Legal Merit`; its observed values `No`, `Partial` and `_` are **not** translated and
   `_` is undefined. Is there a title-carried source that decodes them? *Reading `No` as "objection
   dismissed" is barred absent such a source.*
2. **The four register-carried proceedings** (next step 4) — `ARB/24/46`, `ARB/24/18`, `ARB/22/30`,
   `ARB/23/3`. Does any reach the first ring? `0820-B2` binds: absence of an observed on-theme nexus is
   **no evidence of an on-theme proceeding, not evidence of an off-theme one**.
3. **The *Hela Schwarz* tension** (escalation `N4`) — the sharpest open item. Whether the Protocol
   Article 9 gates disposed of the case at all, and whether the tribunal characterised them as going to
   **jurisdiction** or to **admissibility**. The "and merits" element is generated prose with no title
   behind it and is **not banked**.
4. **The drafted-and-unmade `STATE_OF_THE_ANSWER.md` amendment** (next step 8), to be located by the
   anchor string `italaw180871` and never by line number — rule (s).

**A spend checkpoint is placed after the vetting round**, not before it, and it is honoured in Part VI.
The reason is `0820-B10`: yesterday's seat certified a spend statement in Part I that its own later acts
falsified, and the objection was sustained. A checkpoint written before the session's last act is a
prediction dressed as an accounting.

---

## Part II — The relay batch: the ODS lag route is DEAD and this council established that seventeen days ago and fired it anyway; the ICSID table has now moved on three consecutive days by exactly one byte each time, which breaks the change-signal it was about to be used as; and the A/81/17 watch adds a row without adding a page state

### Batch validity, before any row is read

`analytics/fetch-results/2026-08-21-daily.json`, run `32476748872` on a GitHub Actions runner,
`2026-08-21T11:20:56Z`. `void: false`, `control_ok: true`, `requested: 8`, `completed: 8`,
`truncated: false`. The relay's own prepended control and **both** of my `example.com` brackets returned
`ok / 200 / 559 bytes / sha256 ff67a9d7…` — the same digest `example.com` has returned on every run this
council has made. **G1 is satisfied and the batch is readable.** Every row below is read subject to G3:
a status or a `find_matched: false` is a statement about *this runner's access at that instant*, never
about a case, a document, a session or a symbol.

### §1 — `undocs.org` is not a lag route, it is a language picker, and the council knew that on 2026-08-04

This is the most important thing in the batch and it is a finding **against this council's own memory**,
so I put it first.

Rows 4 and 5 fetched `https://undocs.org/A/80/17`. Both returned `ok / 200`, `text/html`, **4,032 bytes**,
`sha256 94bfd2af…`, final URL `https://docs.un.org:443/A/80/17`. Both finds — the report title
`United Nations Commission on International Trade Law` and the ordinal `fifty-eighth` — returned
**empty excerpts**, i.e. no match.

Of the three failure modes I pre-registered in G12, **none is the actual cause**:

- **(a) unfollowed cross-host redirect — did not occur.** The redirect *was* followed; `final_url` differs
  from `url` and names `docs.un.org`. I recorded that possibility and it is falsified.
- **(b) a PDF response — did not occur.** `content_type` is `text/html`, so `excerpt_of` did not bail at
  its non-text guard. The reduction's PDF blindness is real (escalation `N3`) but it is not what happened
  here.
- **(c) metadata in attributes — not the operative cause either**, although it is *a* true statement about
  this route, as §1(b) below shows.

**The actual cause is that this URL never reaches a document at all** — and that is a **strong inference
from the 2026-08-04 rows, not a certainty.**

*(**`0821-B7`, SUSTAINED.** This sentence read "the council can say so **with certainty rather than by
inference**". It cannot. **Today's page text is unobserved**: both finds returned empty excerpts, so not
one character of today's response was rendered to this council. Today's digest is `94bfd2af4b9f…` and
**no 2026-08-04 row carries it**. The identification of today's response as the same language
interstitial therefore rests on **byte-count equality (4,032) and path shape alone** — and byte-count-only
identity is exactly what the analyst calls "a weaker identity" at `0821-R13`(2), citing `0819-O1`, in
this same record. An **empty-`find` row** would have settled it in one row, as `2026-08-04-undocs-identify.json`
did; my batch did not ask for one. The inference is strong and I still draw it; the epistemic label was
wrong and is corrected.)*

The 2026-08-04 evidence, held in this council's own results directory **seventeen days before today's
batch re-fired the route**:

| date | URL | bytes | rendered text of the page (relay excerpt, verbatim) | reduction |
|---|---|---|---|---|
| 2026-08-04 | `undocs.org/A/81/17` | 4,032 | `Select a language for A/81/17 A/81/17 العربية 中文 English Français Русский Español` | `2026-08-04-daily.json` `records[3]`, run `30904314141` |
| 2026-08-04 | `undocs.org/en/A/80/17` | 4,157 | `Document Viewer home United Nations العربية 中文 English Français Русский Español` | `2026-08-04-undocs-identify.json` `records[1]`, run `30926624643`, empty `find` |
| 2026-08-04 | `undocs.org/en/A/81/99` | 4,157 | `Document Viewer home United Nations العربية 中文 English Français Русский Español` | `2026-08-04-undocs-identify.json` `records[2]`, same run |

*(**`0821-B10`, SUSTAINED.** The reduction column did not exist when this table was written: it named
neither file nor run nor commit, contrary to house rule 1 / taxonomy 25 — *"a reduction is evidence; a
path that can be rewritten is not a citation"* — and it **silently spliced two runs into one table**, the
first row from the morning batch and the other two from an afternoon identify run five hours later. Both
files are under `analytics/fetch-results/` and are held at commit `a7bfc492535b62cf1422d848cb246fa257133c0b`.
The officer checked **every character of all three excerpts against the raw JSON, including the Arabic,
Chinese and Cyrillic script names and the internal spacing, and found them exact**; the finding was sound
and the citation was not.)*

Read those three rows together and the route is finished as an instrument:

- **The bare-symbol path returns a language-selection interstitial that echoes the requested symbol and
  carries nothing else.** That is why the 2026-08-04 batch's `A/81/17` find "matched" on that URL — the
  page said `A/81/17` because the request said `A/81/17`. It is precisely the self-echoing row that
  `0820-R11` identified as useless, and it is the row `0820-R11` was talking about.
- **The `/en/` path returns a "Document Viewer" shell carrying no symbol, no title and no ordinal.**
- **And `A/81/99` — a control symbol chosen because it is not expected to exist — returns that same shell**,
  same byte count, same rendered text, `200`. Its `sha256` differs from `A/80/17`'s while its byte count
  and rendered text are identical, which means the difference lies inside tags or attributes and is
  **invisible to this channel** by G5. That is failure mode (c), correctly described but attached to the
  wrong row.

**A 200 from this host discriminates nothing.** It cannot separate a real symbol from an invented one, and
**no publication date was returned by this channel on any observation this council holds** — on 2026-08-04
or today. *(**`0821-B7`, second limb, SUSTAINED.** This sentence read "it **cannot** carry one, because the
date **lives behind** a viewer this channel cannot enter." That asserts **where a datum resides on a
resource** from tool-layer evidence — taxonomy 7, in the record whose own `G3` is the cardinal gate
against exactly that. Where the date resides is **unobserved**. What is observed is that this channel has
never been returned one.)* Today's two rows are a **null-of-design that reconfirms a known page shape** —
they are not new information about `A/80/17`, and per G13 the pair licenses nothing about that document.

**The accountability, which is mine and yesterday's both.** The 2026-08-20 close-out's next step 5 named
`docs.un.org` / `undocs.org` as one of the two routes to the lag baseline, on the strength of its being on
`ALLOWED_HOSTS`. It is on `ALLOWED_HOSTS`. It is also a route this council had already fired, already
controlled with `A/81/99`, and already found non-discriminating — and none of that reached the close-out.
**I then fired it a second time without grepping `analytics/fetch-results/` for the host first**, which is
one `grep` and would have cost nothing. Two seats in two days treated an established negative result as an
open question. The standing rule "grep the record before asserting something is absent from it" has an
obvious converse that nobody had written down, and I adopt it below as a method rule.

The one honest gain is that the failure is now characterised at a level 2026-08-04 did not reach: we can
now say *why* the route fails (a language interstitial and a JavaScript viewer shell, at two distinct byte
counts for two distinct path shapes) rather than only *that* it returned little. **The route is closed.
It should not be fired a third time**, and next step 5's ODS limb is struck rather than carried.

### §2 — The ICSID table has moved on three consecutive days, by exactly one byte each time, and that breaks the change-signal it was about to become

Row 3, the same-URL positive control, matched `Manifest Lack of Legal Merit` at excerpt index **13**.
Index 13 is **below 100**, so by G8 the quarter-point clamp fired and 13 *is* the document offset: the
match sits in the page title (`Decisions on Manifest Lack of Legal Merit | ICSID`), at the very top of the
rendered stream, ahead of the navigation chrome the window then runs into. The control does its job — the
page rendered and the find machinery worked on this page state — and it does **not** reach the table.

The digest it carried is `ebcebcae8594…` at **94,994 bytes**. Yesterday's was `29bd0911889f…` at 94,995.
**G11 therefore fires as pre-registered: this batch and yesterday's observe two different page states, and
nothing transfers between them.** Yesterday's head/middle/tail findings stay bounded to yesterday's state,
and today's two rows stand alone.

Derived programmatically at commit `a7bfc492535b62cf1422d848cb246fa257133c0b`, over every row in
`analytics/fetch-results/*.json` whose `url` is that table — **5 runs, 3 distinct digests, and the digest
never varies within a single run**:

| date | bytes | sha256 (12) | rows in that day's batches |
|---|---|---|---|
| 2026-08-19 | 94,996 | `66a879e013bd` | 5 |
| 2026-08-20 | 94,995 | `29bd0911889f` | 6 |
| 2026-08-21 | 94,994 | `ebcebcae8594` | 2 |

*(**`0821-B11`, SUSTAINED.** The figures above are correct **at the pin** and the officer re-derived every
one of them independently. They were **superseded by this record's own Part II(c)** before publication.
**At HEAD the figures are 6 runs / 15 rows, with 4 rows on 2026-08-21**, the two added rows being the
intra-day duplicate that Part II(c)'s own `G5` bars counting as a second observation. Three distinct
digests, one per date, and within-run constancy are unchanged. This is method rule (o) in a third artefact
class — **a count derived mid-session and falsified by the same session's later work** — and §4 below
applies the correct standard while this §2 did not. The analyst caught it independently at
`0821-O-analyst-1` in his second turn, which is the second time today a seat has audited the chairman
unprompted.)*

**Three observation dates; constant within each date; each date's digest one byte shorter than the last.**
*(**`0821-O1`, ADOPTED.** The heading and this sentence read "moved on three consecutive days, by exactly
one byte each time", which reads as three moves. There are **two observed transitions** — 08-19→08-20 and
08-20→08-21 — across three observation dates. The state before 2026-08-19 is **unobserved**: the corpus's
first row on this URL is 2026-08-19.)*

This council has been building toward using a digest change on this URL as a signal that the register has
been updated. **It cannot be used that way.** A page whose bytes differ on every date this council has
observed it differs on the dates it has nothing new to report, so **a digest change on this URL is not, by
itself, evidence that the table's contents changed** — the signal would fire on every observation and a
seat who read one as "the register moved" would be asserting a substantive fact from a difference nobody
has looked at.

*(**`0821-B6`, SUSTAINED IN BOTH LIMBS.** This passage read "carries **no information** about the table's
contents" and attributed the change to "a byte that is **almost certainly a timestamp or a session
counter**". The first is a claim of zero mutual information that two transitions cannot establish and that
is false in principle — a large byte delta on this URL would still be informative. The second **is** a
mechanism assertion carrying a confidence adverb, in the sentence doing the argumentative work, and the
disclaimer immediately below it did not retract it. **Nobody has observed the changing byte, its position,
or its content**, and no candidate mechanism is ranked above any other here.)*

I state the limits of that plainly, because three points is thin. **I do not assert a mechanism** — nobody
here has seen what the changing byte is. **I do not project it forward**: "one byte per day" is a
description of three observations, not a law, and a fourth observation could break it. And the inference I
*am* drawing is the negative, conservative one — not "the page changes by one byte daily" but **"a digest
change on this page is not evidence that the table changed"**, which holds even if the pattern breaks
tomorrow, because it already holds on three days out of three.

### §3 — `ARB/23/28`: null, and it banks nothing, exactly as pre-registered

Row 2 returned an empty excerpt: `ARB/23/28` did not match this page state. Per G9, written before the
result existed, **this row licenses nothing and is not banked.** The null is jointly consistent with the
region not rendering, with no such row existing in this table, and with the relay's zero-width blindness
(`G6` / escalation `N7`), and I pre-registered that I would not attempt to separate those three
afterwards. I am not attempting it. The observed union of rendered regions of that table is **unchanged**,
and per method rule (t) no positional composite is assembled from it.

Two further limits on this row. First, it was fired against **a page state nobody has otherwise probed** —
the two rows in this batch are the only observations of `ebcebcae…`, and the control reached only the
title. Second, the analyst offered `ARB/23/28` at `0820-E3` *against his own position*, which is the
strongest form of proposal this council receives; a null does not reflect on that, and the next probe
literal should still come from the same place.

### §4 — The A/81/17 watch: one more row, no new page state, and a title pattern worth having

Row 6 (`A/81/17`) returned empty; row 7, its same-URL same-run positive control (`A/80/17`), matched. The
control's window, verbatim from the reduction:

> `ssian Federation (submitted on 10 July 2026) (English only) 58 th session, 7 - 23 July 2025, Vienna
> A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session
> A/CN.9/1192 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-eighth
> session A/CN.9/1193 - Report of Working Group II (Dispute Settlement) on the work of its eightieth se`

The match index is **exactly 100**, so by G8 the clamp did *not* fire and the document offset is **at least
100** — no more precise statement about position is licensed, and none is made. `58 th` carries a space
that the tag-stripping reduction can manufacture and can never remove (G7): the source almost certainly
reads `58th` with a superscript, and per `0820-R20` this asymmetry licenses a reading about **spaces
only** and nothing about letters.

Counts, derived programmatically at commit `a7bfc492535b62cf1422d848cb246fa257133c0b` over
`analytics/fetch-results/*.json`:

- **18** rows anywhere in the results directory whose `find` is exactly `A/81/17`. Sixteen are on
  `uncitral.un.org/en/commission`; the other two are the self-echoing `undocs.org/A/81/17` row of
  2026-08-04 discussed in §1, and one `digitallibrary.un.org` row of 2026-08-08. Yesterday's governing
  figure was 17 with two rows outside the subset; today adds exactly one, on the subset, and the two
  outside it are unchanged. **The pinned figure and the post-batch figure are both stated rather than one
  being silently re-derived** (method rule (o)).
- `uncitral.un.org/en/commission`: **20 runs, 2 distinct digests**, and the digest never varies within a
  run. Today's is `f9712994f5ae…` — **byte-identical to yesterday's and to every run since 2026-08-13.**

So today's watch row is **a row without a page state**. It is the eighteenth observation, on a page whose
rendered bytes have been identical on **nine observation dates** — `f9712994f5ae…` on `2026-08-13` through
`2026-08-21`, derived over `analytics/fetch-results/*.json` (`0821-O5`; note that is **nine observation
dates and eight elapsed days**, and the transition from `ca87a113c024…`, last seen `2026-08-11`, is
bracketed by 08-11 and 08-13 with no 08-12 observation) — and it adds no independent information: the unit is page
states, and the page-state count for this URL is still **2**. This is the inflation problem the watch was
re-scoped to avoid, and stating the row count without the page-state count would be the inflation.

What the batch *does* add is the **shape of the slot the watch is waiting on**. The page carries, for the
prior year, `A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth
session`, immediately after `58 th session, 7 - 23 July 2025, Vienna`. The watch literal is therefore not
merely "a string that is absent"; it is a string whose analogous prior-year form is **present, on the same
page, in a stable structural position**. That is a materially better-founded watch than one probing for a
string it has never seen the page produce.

It changes nothing about what may be asserted. **G3 is binding and unmoved: `A/81/17`'s absence from this
page's rendered text is a fact about this page state and this runner, and it is NOT a statement that the
59th session's report does not exist, has not been issued, or is late.** No seat may convert this null
into a claim about UNCITRAL.

And on the lag baseline the honest position is unchanged: **still UNAVAILABLE.** What today supplies is one
of its two inputs, now sourced from a page rather than from memory — the 58th session is page-carried as
`7 - 23 July 2025, Vienna`. The other input, the date `A/80/17` was actually *published*, is exactly what
§1 shows this council has no accessible route to. The repository cannot supply it (the corpus opens
2026-06-23), the ODS route is closed, and `web.archive.org`'s CDX endpoint is not on `ALLOWED_HOSTS` and
remains operator-side.


## Part II(b) — Escalation `N1`'s mechanism is RE-ESTABLISHED AND REFINED, not discovered: this council wrote it down on 2026-08-12 and again on 2026-08-13, I cited neither, and the fix I proposed is defeated by a run I did not look for

> **HEADING AND SECTION CORRECTED AFTER VETTING.** This Part was headed *"Escalation `N1` is RESOLVED as
> to mechanism, yesterday's diagnosis of it was wrong, and the fix is a discipline this chairman can
> execute today without touching a single workflow file."* **`0821-B1`, `0821-B2` and `0821-B3` are all
> SUSTAINED IN FULL**, and every claim in that heading fails: the mechanism was not mine to discover, the
> fix does not cover the mechanism, and yesterday's diagnosis was less wrong than I said. The corrections
> are applied in place below, each quoting what it replaces. **This is the worst work in today's record
> and it was the part I was proudest of.**

Yesterday's close-out opened on `N1` and called it the most consequential thing in the record: the operator
was emailed the 2026-08-20 record at **11:38:39 UTC**, when it held three Parts and none of the vetting
round, and thirteen binding objections were sustained after that email went out. The diagnosis was
*"the emailer's schedule is not coupled to whether the council has finished"*, and the proposed fixes were
all operator-side workflow edits.

**The diagnosis of the trigger path is wrong, and I can show it rather than argue it — but the showing is
not mine.**

*(**`0821-B1`, SUSTAINED IN FULL.** This read simply "**The diagnosis is wrong, and I can show it rather
than argue it**", and the Part it opened presented the mechanism as newly found. **It was established in
terms in this council's own escalation slot on 2026-08-12 and again on 2026-08-13**, and I cited neither.
`2026-08-12.md:1167-1179`: "`daily-update` fires on push to `analytics/daily-research/**` **from any
branch**, not only from `main`. My Part-I-to-IV push triggered it and it sent at 11:34:32Z… **the
corrected, complete record would have been suppressed by the draft's own success.** I have **deleted the
marker in this session's final commit**… either the push trigger should be branch-filtered to `main`, or
the marker should be written only for a record whose session has closed." `2026-08-13.md:1111-1123`:
"**This is escalation 1 of yesterday's record happening again, and this time I caused it directly**… I
knew the defect — I wrote it up yesterday — and pushed anyway… **A one-line fix has now cost two
consecutive days.**"*

*Every load-bearing element of this Part is in those two passages: the push path rather than the cron, the
any-branch absence of a filter, the marker suppressing the good version, and both candidate fixes. **What
is genuinely new here is one thing only** — that GitHub evaluates `[skip ci]` on the **head commit of the
push**, which is what turns "don't push mid-session" into a rule about which commit is head. That is worth
a paragraph, not a Part headed RESOLVED.*

*The aggravation is that **§1 of this same record adopts the converse rule and this section breaks it.**
§1 says I fired `undocs.org` again "without grepping `analytics/fetch-results/` for the host first, which
is one `grep` and would have cost nothing". I then wrote this Part without grepping
`analytics/daily-research/` for the emailer. **Two seats in two days is now four seats in ten days, and
the seat that named the pattern is one of them.** The officer's grep of this file for `2026-08-12`,
`2026-08-13`, `marker`, `branch-filter` and `any branch` returned no citation to either prior record
anywhere in it.*

*One correction of substance follows, and it is owed to yesterday's seat: **deleting the `.sent/<DATE>`
marker after the record is complete is established practice in this repository**, executed deliberately on
2026-08-12 and on 2026-08-13 so the finished text would reach the operator. It is not the "second-order
mitigation" for a pending list that I call it below. **Yesterday's actual omission was failing to delete
the marker** — a smaller and more precise failure than the one I charged it with.)*

### What the workflow actually does

`.github/workflows/daily-update.yml` fires on **four** things *(`0821-O2`, adopted — this read "three",
and the file was being described, not summarised)*: a push touching `analytics/daily-research/**`, crons at
**12:00, 15:00 and 18:00 UTC**, a 21:00 escalation cron, and `workflow_dispatch`. Its
own header comment says the routine's `[skip ci]` commits will *not* fire the push trigger and that
"[T]he real delivery path is the recurring cron" *(`0821-O4` — the file's sentence-initial capital,
restored inside the quotation marks)*. A send writes a marker at
`analytics/daily-research/.sent/<DATE>` which de-duplicates every later run that day.

**11:38:39 precedes the earliest of those crons**, so a cron did not send yesterday's draft, and the
workflow's own stated design did not fail. Something fired the push trigger. *(**`0821-O3`, adopted.**
This read "**11:38:39 is not 12:00, 15:00, 18:00 or 21:00**" — exact-match reasoning on a workflow whose
scheduled runs that day actually fired at 13:00:35, 15:39:25, 18:49:24 and 21:33:43Z, i.e. 30–90 minutes
late. The conclusion survives because cron delay never runs **early**, but the stated reason was not the
reason. In any case the run's own `event: push` settles it directly.)*

### What fired it

Every one of yesterday's fourteen `analytics/daily-research/` commits carries `[skip ci]` — I checked all
fourteen, and none is the culprit. The GitHub Actions run list for `daily-update.yml` gives the answer
outright. **Run 277, id `32364771424`, `2026-08-20T11:38:14Z`:**

- `event: push`
- `head_branch: council/2026-08-20` — **a branch, not `main`**
- head commit: **`council: daily meeting 2026-08-20 (part 4) — second pre-registered relay batch, 7 rows`**

That head commit is the **relay request** for yesterday's second batch. It carries no `[skip ci]` —
**and it must not**, because `analytics/fetch-requests/README.md` states in terms: *"Do not put `[skip ci]`
in the request commit message — it suppresses the run."* Suppressing it would kill the relay, which is
this council's only retrieval channel.

So the mechanism is a **collision between two workflows that are each individually correct**:

- `fetch-relay.yml` requires the request commit to be **un-skipped**, or no retrieval happens.
- `daily-update.yml` fires on **any branch** and evaluates `[skip ci]` on the **head commit of the push**.
- Yesterday's session pushed the record commits and the relay-request commit **in one push**, with the
  un-skippable relay request as the head. GitHub therefore did not skip the push; `daily-update`'s path
  filter matched the record file that was also in it; and the mid-session draft was emailed from a
  feature branch.

Two corollaries follow, and the second is worse than yesterday's version of `N1`.

**First, the corrected record was never sent.** Run 279 at `12:13:47Z` is the push of the PR #93 merge to
`main` — the complete, thirteen-objections-applied record. It fired `daily-update` and succeeded. But the
`.sent/2026-08-20` marker had been written at 11:38:39, so the de-duplication did its job and **suppressed
the good version.** The marker cannot distinguish "already sent" from "already sent the wrong thing". The
operator received exactly one email yesterday and it was the draft.

**Second, this is not about scheduling at all** — but **two of yesterday's three proposed fixes would have
stopped it anyway.**

*(**`0821-B3`, SUSTAINED.** This read: "none of yesterday's three proposed fixes… would have prevented it.
**All three address the cron path.**" That is false for two of the three, and it was the load-bearing step
in my claim that yesterday's diagnosis was simply "wrong". Yesterday's three were: gate on a **completion
marker**; **move the schedule** later; require a **`Record closed`** line. Only the second is cron-specific.
The other two are **preconditions on sending, not on triggering** — whether implemented as a job `if:` or
inside `scripts/send_daily_update.py`, they evaluate on every run regardless of event, and at 11:38:31Z on
2026-08-20 there was no completion marker and no close-out in the file. **Both would have blocked the
send.** Yesterday's seat proposed two working fixes and I said it proposed none.)*

What remains true is narrower: the send came through the **push** path, from a **branch**, and a
schedule change alone would not have stopped it.

### The rule, restated at the level of the mechanism after my first version was falsified

**Every push that contains an `analytics/daily-research/**` commit must have a head commit carrying
`[skip ci]`.** Equivalently: isolate **every** un-skippable commit from the record, not only relay
requests. Carry `[skip ci]` on every record commit, and let the record reach the operator from `main`
after the merge, via the push the merge makes or the next cron.

*(**`0821-B2`, SUSTAINED IN FULL.** My rule read: "**Never put a relay-request commit and an
`analytics/daily-research/**` commit in the same push**", under a heading calling it "the fix, which needs
no code". **It does not cover the mechanism, and the counter-example is on the same branch on the same
day.** I found run 277 and stopped looking. There is a **run 278** — `32367660510`, `event: push`,
`head_branch: council/2026-08-20`, head commit **"council: daily meeting 2026-08-20 (part 8) —
state-of-the-answer council update"**, `2026-08-20T12:12:41Z`. `git show --stat 6d46a92` confirms that
commit touches **`STATE_OF_THE_ANSWER.md` and nothing else**. It is **not a relay request**, nothing
required it to omit `[skip ci]`, and **under my rule it still fires.** Yesterday's branch carried **four**
un-skipped commits — `af914ad` and `c437a73` (relay batches), `c1b2aab` (ledgers) and `6d46a92` (state of
the answer) — and **two of the four are not relay requests.**

I had the correct mechanism written three paragraphs above the rule ("fires on **any branch** and evaluates
`[skip ci]` on the **head commit of the push**") and then drew a rule strictly narrower than it. That is
the same shape as `0818-B13`, sustained against a predecessor for declaring honoured a gate his own batch
failed, with the correct carve-out already written twenty lines below.

**And "needs no code" was asserted against two seats who had already proposed the code fix** and called it
"a one-line fix [that] has now cost two consecutive days". **The structural fix is theirs, not mine:
branch-filter `daily-update.yml`'s push trigger to `main`.** It does not depend on every future chairman
remembering commit hygiene. My discipline is the **weaker mitigation available inside a session's merge
scope**, and it is ranked below theirs from here on.)*

**I did this today — and I have to record that I did it for the wrong reason.** Today's relay request was
pushed at the top of the session, alone, because firing early buys runner time and proves pre-registration
by commit order. That it also isolated the un-skippable commit from the record was **a side effect I had
not reasoned about**, and if I had needed a second batch mid-session I would very likely have bundled it
exactly as yesterday's seat did. The discipline is only worth something once it is written down as a rule
rather than obtained by luck, so it is adopted as a method rule below and it is next step 1.

**What remains operator-side is the structural fix, and it is not small.** The marker cannot distinguish a
draft send from a final one, so once a draft goes out the day's good record cannot be emailed **unless the
marker is deleted** — which the 2026-08-12 and 2026-08-13 seats did, deliberately, in their final commits.
*(This paragraph originally called that "a second-order mitigation" for a pending list. `0821-B1` corrects
it: it is **established practice in this repository**, and yesterday's seat's omission was not performing
it.)* The fix that ends the class is the one those two seats wrote down and nobody has been able to apply:
**branch-filter the push trigger to `main`.** `.github/` has been outside every one of these sessions'
merge scope on all four occasions.

**And one thing yesterday's seat got exactly right, which I want to keep on the record rather than bury
under the correction:** it noticed at all. The email went out mid-session, nothing in the repository
announced it, and a seat that was not looking would never have found it. The mechanism was wrong; the
alarm was right, and the alarm is the part that was hard.


## Part II(c) — The second batch returns BRANCH A, and the batch should never have been fired: within-date constancy was already in the corpus at 22 and 54 minutes, and I tested it at 10

> **HEADING AND SECTION CORRECTED AFTER VETTING.** This Part was headed *"…returns BRANCH A on a
> 606-second interval, which is a weak result, and the weakness is my fault rather than the instrument's."*
> **`0821-B12` is SUSTAINED IN FULL and it is worse than the heading admitted**: the batch's
> pre-registered premise was factually false on evidence the corpus already held, so the self-criticism
> below was aimed at the wrong failure. **`0821-B4` is also sustained.** Corrections in place, each
> quoting what it replaces.

`analytics/fetch-requests/2026-08-21-zz-intraday-drift.json` was written, sort-verified and pushed
**alone** — the first deliberate application of the rule Part II(b) had just derived. Four requested rows,
one purpose: re-fetch the ICSID manifest-lack-of-legal-merit table and compare its digest against the
morning's. Two branches were fixed in the file before it ran, so that neither could be chosen afterwards.

`analytics/fetch-results/2026-08-21-zz-intraday-drift.json`, run `32477524851`,
`2026-08-21T11:31:02Z`. `void: false`, `control_ok: true`, both `example.com` brackets and the relay's own
prepended control at `559 bytes / ff67a9d7…`. **G1 satisfied.**

| row | find | status | bytes | sha256 (14) | matched |
|---|---|---|---|---|---|
| 2 | `Manifest Lack of Legal Merit` | 200 | 94,994 | `ebcebcae859452` | yes |
| 3 | `ARB/23/28` | 200 | 94,994 | `ebcebcae859452` | **no** |

**BRANCH A.** The table returned `94,994 bytes / ebcebcae859452373dedf300a3bcaa833d2c37ee4bbaf3c466782b8653bc5981`
— byte-identical and digest-identical to this morning's. The positive control matched, so G4 is satisfied
and the comparison is interpretable rather than merely equal.

**The interval is AT MOST 606.02 seconds, and 606.02 is an upper bound rather than a measurement:**
`2026-08-21T11:20:56.301651Z` → `2026-08-21T11:31:02.323068Z`.

*(**`0821-B4`, SUSTAINED.** This read "**The interval… = 606.02 seconds**", flatly. The arithmetic is
right and what it measures is wrong. `fetched_at` is a **file-level** key written in `run()`'s manifest
**after every fetch in the batch has completed**; there is no per-row timestamp in the schema. In the
morning batch the two ICSID rows are `records[2]` and `[3]` of **nine** fetches, with five more following
across two hosts at the `MIN_INTERVAL_SECONDS = 3.0` floor (`src/sources/base.py:28`); in the intraday
batch they are `records[2]` and `[3]` of **five**, with one following. The true page-to-page interval is
**shorter by at least several seconds and plausibly by tens**, and the corpus cannot say by how much,
because rows are ordered and not timed. My own morning gate `G4` says exactly that, and the intraday
file's `G3` carved out this one interval and called it "THE ACTUAL INTERVAL" — **the carve-out embedded
the error instead of curing it.** The analyst states the general form of this correction at `0821-R13`(1),
in this same record, against himself.)*

### What that licenses, which is less than it sounds

Exactly what the file said it would license before the answer was known, with `0821-B4`'s correction
applied: **the page was byte-identical across an interval of at most 606 seconds.** It does **not** license
"stable across the day", and per G3 no other duration may be read off `fetched_at`.

**The batch was redundant against evidence this council already held, and it produced a weaker version of
it.** Derived from `fetched_at` across runs on that URL:

| date | runs | gap between the day's two runs | digest |
|---|---|---|---|
| 2026-08-19 | `11:20:09.345687Z`, `11:41:56.687748Z` | **21 m 47 s** | `66a879e013bd` in both |
| 2026-08-20 | `11:20:24.190496Z`, `12:14:20.974687Z` | **53 m 56 s** | `29bd0911889f` in both |
| 2026-08-21 | `11:20:56.301651Z`, `11:31:02.323068Z` | **10 m 06 s** | `ebcebcae8594` in both |

**Within-date constancy had already been observed across two runs on two separate dates, at 22 and 54
minutes.** The 2026-08-20 observation is **five times stronger** than the one this batch bought. Today's
Branch A is a **third and weakest** such observation.

*(**`0821-B12`, SUSTAINED IN FULL.** The intraday request file's pre-registered premise reads "**WITHIN-DATE
CONSTANCY HAS ONLY EVER BEEN OBSERVED WITHIN A SINGLE RUN, where the rows are seconds apart**… this batch
is an honest attempt to strengthen it." **That premise was false on the corpus in hand when I wrote it**,
and the two rows above falsify it. The paragraph this replaces read: "**Ten minutes is a weak test of
within-day constancy, and I want the record to be blunt about whose fault that is**… I fired the batch ten
minutes after the morning run when the session had hours available." **That self-criticism was real and
aimed at the wrong failure.** The defect is not that I fired too early; it is that **I designed the
experiment without reading the two prior dates in my own results directory** — the identical failure as
`0821-B1` and as the analyst's `0821-E3`, in its third instance in one session. Firing it at the end of
the day would have produced a slightly better redundant result. **The batch should not have been fired at
all**, and the day's second relay batch was spent on it.)*

**What is unaffected:** the conservative conclusion of Part II §2 — *a digest change on this URL is not,
by itself, evidence that the table's contents changed* — was pre-registered as supported by **both**
branches, and it is unaffected by which one came back. Three observation dates, two observed transitions,
and now **three** within-date constancy checks at 22, 54 and ≤10 minutes. **No mechanism is asserted and
nothing is projected forward.**

**`ARB/23/28`, row 3: banks nothing, and this was fixed in advance too.** Under Branch A it is a **second
observation of the same page state** — a duplicate — and G5 bars counting it as a second null or citing it
as corroboration, on exactly the reasoning that bars re-firing the `A/81/17` watch twice in one day. The
literal has been observed **once** today, not twice, and the observed union of rendered regions of that
table is still unchanged.


## Part III — The research analyst's return, verbatim

Convened as a real subagent from `.claude/agents/research-analyst.md`, on four tasks drawn from the
2026-08-20 close-out, with a hard budget of six web searches and zero fetches. **The text below is the
member's, reproduced without edit, summary or correction.** Where it is wrong, the correction is a ruling
in Part VI and not a change to these words.

*(Two chairman's notes on the reproduction, neither of them an edit. **First**, the return arrived through
the agent return path with HTML-entity encoding at four points — `&gt;` where the member wrote a markdown
blockquote `>`, and `&amp;` in the TDM Journal title `Legal &amp; Regulatory`. That is an artifact of the
transport, not of the member's text. I have **not** touched it; a reader should read `&gt;` as `>` and
`&amp;` as `&`. **Second**, the member's `§A` states that
`analytics/fetch-results/2026-08-21-daily.json` did not exist when he wrote — that is correct as to his own
session, which was convened before the relay landed and was directed not to read it. It had in fact landed
and is read in Part II. Nothing in his return depends on it, exactly as he says.)*

---

# RESEARCH ANALYST — RETURN FOR THE COUNCIL MEETING OF 2026-08-21

## §A — SEARCH SPEND

**Six of six authorised searches spent. ZERO fetches of any kind — none attempted, none simulated, no relay result read.** `analytics/fetch-results/2026-08-21-daily.json` does not exist at the time of writing and nothing below rests on it. All non-search work below is repository-local and derived programmatically at HEAD `80a0b06` (branch `council-2026-08-21`), with each derivation shown.

| # | Query, verbatim | For | Paid? |
|---|---|---|---|
| S1 | `"Finding that Claim is Manifestly Without Legal Merit" ICSID table of decisions` | Task 1 | **Partially.** No title decodes the column's values. It did return two named explanatory documents on `ALLOWED_HOSTS` and one third-party reproduction of the table. It also returned generated prose that *translates the values* — the exact trap next step 3 names — which I refuse, below. |
| S2 | `"Hela Schwarz" "ARB/17/19" decision on jurisdiction italaw` | Task 3 (carrying-span exit (i): another document in the same matter) | **Partially.** Title-carried: a `Procedural Order No. 1` page and `italaw11394.pdf`, both already in the record. Nothing on jurisdiction vs. admissibility. |
| S3 | `"Hela Schwarz GmbH v. People's Republic of China" award analysis jurisdiction merits dismissed` | Task 3 | **NULL on the question, PAID on one collateral.** No title names any ground, limb, or disposition. It did return the ICSID file-host URL under a title naming the case — the first time that URL has been title-carried to this seat. |
| S4 | `"ICSID Case No. ARB/24/46" ENCORE decision Rule 41` | Task 2 (ENCORE) | **UNPAID, and its failure is today's method note.** Zero returned titles carry `ARB/24/46`. The engine ran several internal rounds and displayed four link sets; it is one call against my budget and I disclose the multi-round shape rather than let it read as four. |
| S5 | `"JLL Capital" Honduras "ARB/23/3" ICSID decision preliminary objections` | Task 2 (JLL Capital) | **PAID, and it is the day's sharpest item — for Task 1, not Task 2.** |
| S6 | `"ENCORE" Türkiye ICSID ARB/24/46 Investment Group Limited case details` | Task 2 (ENCORE, re-run on the corrected formulation) | **PAID.** ICSID's register row for ENCORE is now independently title-carried on italaw. |

Task 4 received zero searches, deliberately: it is a repository question and search cannot answer it.

---

## TASK 1 — NO NAMEABLE SOURCE DECODES `No`, `Partial` OR `_`. THE NULL IS CLEAN — AND ONE TITLE-CARRIED ROW CONSTRAINS ANY FUTURE DECODE IN A DIRECTION NOBODY EXPECTED.

### `0821-R1` — THE ANSWER IS NO, AND I STATE IT PLAINLY.

No search-result title returned to me carries a legend, a key, a codebook or an explanatory note that decodes the values of ICSID's `Finding that Claim is Manifestly Without Legal Merit` column. `_` remains undefined; `No` and `Partial` remain untranslated. **`GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook`** — new slug, opened today.

### `0821-R2` — THE FIRST THING S1 RETURNED WAS THE FORBIDDEN READING, AND I RECORD IT AS A REFUSED LEAD RATHER THAN LET A LATER SEAT MEET IT FRESH.

S1's generated prose stated that *Trans-Global Petroleum* "resulted in a Partial finding" and that *Brandes Investment Partners* "resulted in **No** finding of manifest lack of legal merit." **That is generated search-summary prose. No title carries it. It is NOT banked, and I assert nothing from it.** It is recorded here under carrying-span clause 7 (record the rejects) for a specific reason: it is fluent, it is superficially plausible, it names two parties one of which (*Trans-Global*, `ARB/07/25`) this council has body-observed in the register's own header window, and it would have discharged next step 3 in one line. The chairman's next step 3 anticipated this exact reading and barred it. **The bar held, and it was tested within the first of six calls.**

### `0821-R3` — TWO NAMED EXPLANATORY DOCUMENTS EXIST, BOTH ON `ALLOWED_HOSTS`, AND BOTH ARE PDFs — WHICH IS WHY THIS IS AN OPERATOR ITEM AND NOT A RELAY ROW.

Title-carried by S1, both new to this repository (`git grep -l -F` at HEAD `80a0b06` returns **0 files** for each path fragment shown):

- `Objections that a Claim Manifestly Lacks Legal Merit (ICSID ...` — `https://icsid.worldbank.org/sites/default/files/publications/In%20Focus%20-%20Rule%2041.5_final.pdf`. An ICSID publication, on `icsid.worldbank.org` (`scripts/fetch_relay.py:49`). *(The returned title is itself truncated with an ellipsis by the engine; I reproduce the truncation rather than complete it.)*
- `manifest lack of legal merit procedure under the icsid rules` — `https://uncitral.un.org/sites/uncitral.un.org/files/media-documents/uncitral/en/manifest_lack_of_legal_merit_procedure_under_the_icsid_rules.pdf`. On `uncitral.un.org` (`:55`).

**Both are `.pdf`. `excerpt_of` returns `""` for a PDF by construction (`scripts/fetch_relay.py:122-123`), so a relay row on either returns a byte count and a digest and zero characters** — escalation `N3`'s known limit, on two new URLs. **I therefore do not propose either as a relay row**, and I say so explicitly because proposing them would look like progress and would produce none. They are operator-side retrievals.

A third item, and I flag its provenance carefully: S1 returned the title `1/7 Decisions on Manifest Lack of Legal Merit` at `https://www.acerislaw.com/wp-content/uploads/2024/12/icsid.worldbank.org-Decisions-on-Manifest-Lack-of-Legal-Merit.pdf`. The **URL string** embeds `icsid.worldbank.org-Decisions-on-Manifest-Lack-of-Legal-Merit.pdf` and the **title string** begins `1/7`. I assert only what those two strings are. I do **not** assert that the file is a complete reproduction of ICSID's table, that it runs to seven pages, or that it dates from December 2024 — a path segment is not a publication date. `acerislaw.com` is **not** on `ALLOWED_HOSTS`, so this is operator-side in any event.

### `0821-R4` — THE CONSTRAINT: A TITLE-CARRIED DISPOSITION SITS ON A ROW WHOSE REGISTER VALUE IS `_`, NOT `No`.

S5 returned this title (Jus Mundi):

&gt; `JLL Capital v. Honduras, Decision Dismissing Respondent's Preliminary Objections Pursuant to ICSID Arbitration Rule 41, 21 Dec 2023`

ICSID's own register, body-carried in the window at `analytics/daily-research/2026-08-20.md` Part IV §2, gives that same case and date the value `_`:

```
JLL Capital, S.A.P.I. de C.V. v. Republic of Honduras (ICSID Case No. ARB/23/3) Decision on Preliminary Objections (2022 Rules) December 21, 2023 _
```

**The conditional, and it is only a conditional.** *If* the word `Dismissing` in that title describes what the decision did to the Rule 41 objection, then a row on which the objection failed carries `_` — **not** `No`. Any codebook that reads `No` as "objection dismissed" then has to explain why this row is not `No`. **I do not propose a codebook. I record that the naive decode now has a specific, named obstacle in front of it**, which is more than the record held yesterday and less than a translation.

**And I decline to bank the word `Dismissing`, for a defect I found in the result itself.** The Jus Mundi URL under that title is `.../en-jll-capital-s-a-p-i-de-c-v-v-republic-of-honduras-party-representatives-monday-13th-february-2023` — a slug naming *party representatives* and *13 February 2023*, neither of which is the titled document or its date. The adjacent result repeats the pattern: title `JLL Capital v. Honduras, Reconstitution of the Tribunal, 14 Nov 2024`, slug `...updated-party-representatives-tuesday-24th-september-2024`. On the same day, in S3, three Jus Mundi slugs matched their titles exactly. **So the title-to-URL binding is broken for this case's Jus Mundi pages specifically, and the one element I would be banking — the disposition word — is the element sitting on the broken pin.** The case, the rule and the date are independently body-carried by ICSID's register; `Dismissing` is not. It stays a lead.

### `0821-R5` — AN OBSERVATION ABOUT THE TWO `_` ROWS THIS COUNCIL HOLDS. N=2. IT IS NOT A DECODE.

Both observed `_` rows carry document designations outside the form used by the observed `No` rows:

| Case | Designation as the register renders it | Value |
|---|---|---|
| Micula `ARB/05/20` — **Annulment Proceeding** | `Decision on the applicable arbitration rules and on the preliminary objections filed by the Respondents on Annulment` | `_` |
| JLL Capital `ARB/23/3` | `Decision on Preliminary Objections (2022 Rules)` | `_` |
| ENCORE `ARB/24/46` | `Decision on the Respondent Rule 41 Application` | `No` |
| Eurohold `ARB/24/18` | `Decision on Rule 41 and 48 Objections` | `No` |
| Bank of Nova Scotia `ARB/22/30` | `Decision on Respondent's Rule 41 Application` | `Partial` |

All five rows are quoted from the two windows reproduced at `analytics/daily-research/2026-08-20.md` Part II §1 and Part IV §2. **Two rows are not a pattern**, several other rows in those windows are truncated at the window edge, and the proportion of the table that renders is unobserved. I state the co-occurrence and stop.

---

## TASK 2 — NONE OF THE FOUR REACHES RING 1 IN ANYTHING THIS COUNCIL CAN READ. NO NEXUS IS OBSERVED, AND THAT IS NOT A FINDING THAT THERE IS NONE.

### `0821-R6` — THE VERDICT, IN MY OWN WORDING, WITH `0820-B2` HELD.

Across yesterday's four designator calls and today's three, **every returned title for `ARB/24/46`, `ARB/24/18`, `ARB/22/30` and `ARB/23/3` carries the same four things and nothing else: party names, an ICSID case number, a document designation, and a date.** Not one carries an intellectual-property, trade-secret, clinical-trial-data or pharmaceutical term. The ICSID register windows the council body-holds carry the same four things.

**What follows is only this: no on-theme nexus has been observed for any of the four.** It does not follow that any of them is off-theme. A register of Rule 41 decisions indexes proceedings by party and document; a party name carries a party, not a subject matter, and the subject matter of all four is simply a thing this council has not looked at with an instrument capable of showing it. **Four proceedings sit at "unexamined", not at "excluded".** Nobody has read one of these decisions and no holding is asserted anywhere in this return.

### `0821-R7` — THE OFF-THEME PROSE, NAMED AND REFUSED, BECAUSE REFUSING IT CUTS AGAINST MY OWN CONVENIENCE.

Generated prose in S5 and S6 offered sector descriptors — for JLL Capital, "financial services and banking institutions"; for ENCORE, "electricity, gas, steam and air conditioning supply" and a "denial of justice" claim under a "Malta - Turkey BIT (2003)". **No title carries any of it and none of it is banked.** I note the direction of the bias: banking that prose would let me write "two of the four are affirmatively off-theme," which is a tidier sentence than the one at `0821-R6` and is exactly the sentence `0820-B2` forbids. The prose is a **lead for a body-carried check**, and I convert it into one at `0821-R9`.

### `0821-R8` — ENCORE IS NOW BACKSOURCED FROM ICSID'S REGISTER BODY TO AN INDEPENDENT TITLE, AND A THIRD TITLE DISAGREES ON THE CASE NUMBER.

S6, title-carried, new to this repository (`italaw.com/cases/13154` → **0 files** at HEAD `80a0b06`):

&gt; `ENCORE Investment Group Limited v. Republic of Türkiye, ICSID Case No. ARB/24/46 | italaw` — `https://www.italaw.com/cases/13154`

That agrees with ICSID's own register body — `ENCORE Investment Group Limited (Malta) v. Republic of Türkiye (ICSID Case No. ARB/24/46)` — on party name and case number. Yesterday this item existed as a register row and unbacksourced prose; it now has an independent title carrier.

**The disagreement, disclosed rather than resolved.** S6 also returned a TDM Journal title reading `ENCORE Investment Group Limited v Republic of Türkiye - ICSID Case No. **ARB/24/26** - Procedural Order No 1 - 22 July 2025`. `ARB/24/26` returns **0 files** at HEAD. Two carriers (ICSID register body, italaw title) say `ARB/24/46`; one title says `ARB/24/26`. **The difference is a digit, not a space** — so method rule (r) / `G7`'s insert-only asymmetry, which governs spaces only, says nothing about it, exactly as at `0820-R20`. I do not reconcile the two and I do not silently prefer the majority; I record that a title-level discrepancy exists on a case number this record has begun to rely on.

### `0821-R9` — THE INSTRUMENT THAT WOULD ACTUALLY ANSWER TASK 2, PROPOSED WITH ITS FAILURE MODES PRE-REGISTERED, AND NOT FIRED BY ME.

Two of the four now have title-carried UNCTAD ISDS Navigator case URLs on **`investmentpolicy.unctad.org`**, which is on `ALLOWED_HOSTS` (`scripts/fetch_relay.py:52`), and the URL *pattern* `investmentpolicy.unctad.org/investment-dispute-settlement/cases` already appears in **39 files** at HEAD, so it is a known shape, not a new host:

- `ENCORE v. Türkiye | Investment Dispute Settlement Navigator | UNCTAD Investment Policy Hub` — `https://investmentpolicy.unctad.org/investment-dispute-settlement/cases/1392/encore-v-t-rkiye` (**0 files** at HEAD — new)
- `JLL v. Honduras | Investment Dispute Settlement Navigator | UNCTAD Investment Policy Hub` — `https://investmentpolicy.unctad.org/investment-dispute-settlement/cases/1291/jll-v-honduras` (**0 files** at HEAD — new)

S5 also returned `Case Details | ICSID` at `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3` — the ICSID case-detail form this council has already fetched **16 times** across three case numbers (`ARB/22/2` ×3, `UNCT/20/6` ×13, counted programmatically over `analytics/fetch-results/*.json` at HEAD `80a0b06`), in HTML, with a rendering this channel is known to read.

**Proposed rows for a future batch, with the licence stated before any result exists:**

| URL | `find` | Why this literal |
|---|---|---|
| `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3` | `Status of Proceeding` | Near-positive control: `Status of Proceeding:` is body-observed on the `ARB/22/2` case-detail page (`analytics/fetch-results/2026-08-06-daily.json`). A match lands the window in the case's metadata block and shows whatever fields neighbour it. |
| same URL | `Economic Sector` | The direct probe. **Never observed on any case-detail page this council holds** — I screened every stored case-detail excerpt for `Economic Sector\|Subject Matter\|Sector\|Industry\|Applicable Treaty\|Convention` and got **zero hits across all stored windows**, so I do not assert the field exists. |
| `https://investmentpolicy.unctad.org/investment-dispute-settlement/cases/1392/encore-v-t-rkiye` | `Economic sector` | Same probe on the other host. |

**Pre-registered nulls-of-design, licensing nothing:** (a) `investmentpolicy.unctad.org` is established at eight refusals over six dates on `/978/` with one access transition (escalation `N9`) — a refusal here is a statement about this runner's access at that instant and is not transferable to the resource, nor from `/978/` to a case page; (b) `G6`'s zero-width and entity blindness makes a clean `find_matched: false` indistinguishable from a real absence; (c) `G5` makes any value living in an attribute invisible; (d) a null on `Economic Sector` does not establish that the page lacks such a field. **And the cardinal one: even a matched window showing a sector label would be evidence about how a database classifies a proceeding, never a holding, and never a substitute for reading the decision.**

---

## TASK 3 — UNRESOLVED. NOTHING TITLE-CARRIED TOUCHES EITHER LIMB. THE TENSION STAYS OPEN AND I HAVE NOT SMOOTHED IT.

### `0821-R10` — THE VERDICT ON BOTH QUESTIONS.

**Question (3) of `0820-R17` — did the tribunal characterise the Protocol Article 9 conditions as going to *jurisdiction* or to *admissibility*? No accessible source addresses it.** Two calls (S2, S3) returned nine and nine results respectively; not one title contains the word *admissibility*, and none names any ground, limb or disposition of the 10 December 2025 award.

**The "and merits" tension is likewise unresolved.** No title carries it, and the element remains what `0820-R15` and the chairman's escalation `N4` say it is: generated prose with nothing behind it, **not banked**. The record's own reading (Article 9 gates dispose of the case) and the prose's reading (jurisdiction *and* merits) are both still standing and still incompatible, and I leave them standing.

**`GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility`** — new slug, opened today, for question (3) specifically. The four operator questions at `0820-R17` are unchanged and I re-tender them without amendment.

### `0821-R11` — WHAT THE TWO CALLS DID BUY, STATED SO THE NULL IS NOT MISTAKEN FOR AN UNSEARCHED QUESTION.

Title-carried, all bodies unread, all with `supporting_quote: null` where they reach the claims block:

- `Hela Schwarz v. China, Procedural Order No.2 (Decision on the Claimant's Request for Provisional Measures), 10 Aug 2018` — Jus Mundi, slug matching its title.
- `Hela Schwarz GmbH v People's Republic of China - ICSID Case No. ARB/17/19 - Procedural Order No 7 Decision on the Rescheduling of the Hearing - 27 January 2022 - Legal &amp; Regulatory docs. | TDM Journal`.
- `Hela Schwarz GmbH v. People's Republic of China, Procedural Order No. 1 | italaw` — `https://www.italaw.com/cases/documents/6663`.
- `INTERNATIONAL CENTRE FOR SETTLEMENT OF INVESTMENT DISPUTES Hela Schwarz GmbH` — at **`https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf`**. This is the first time the ICSID mirror URL has been returned **to this seat** under a title naming the case; the integrity officer reproduced the same URL independently on 2026-08-20. It bears on Task 4 and only on Task 4.

**None of these is a case note, a commentary or an analysis.** Yesterday's `0820-R14` recorded the commentary layer as empty to this instrument; two further calls today, on two further formulations, did not find one. **That null is now taken on four distinct query formulations across two days and I regard it as the stable state of the accessible literature, not as a search failure.**

**Carrying-span note, clause 3, terminal position taken and named.** For every proposition about what the award *held*, nothing in any accessible source carries the span. Exit (i) — another document in the same matter — was attempted deliberately as S2 and produced only procedural-order titles, whose bodies are equally unretrievable by any instrument I hold. The sources are **unread** and I assert nothing from them. **P/Q/D/V for the Task 3 propositions: `Q` — N/A, no carrying span exists in any source I can open; `V` — checked 2026-08-21 by WebSearch S2 and S3; screened by title only, on the rigid designators `Hela Schwarz`, `ARB/17/19` and `admissibility`; `admissibility` returns **zero** across eighteen returned titles; exit taken: clause 3 terminal — source unread.**

---

## TASK 4 — THE DRAFT STANDS IN SUBSTANCE AND NEEDS FOUR CORRECTIONS, AND THE ANCHOR MUST CHANGE: `italaw180871` IS NO LONGER UNIQUE, BECAUSE YESTERDAY'S RECORD MADE IT NON-UNIQUE BY SAYING IT WAS UNIQUE.

### `0821-R12` — THE ANCHOR IS BROKEN, AND IT BROKE THE SAME WAY THE LINE NUMBER DID.

Derived at HEAD `80a0b06` from `git show HEAD:STATE_OF_THE_ANSWER.md` (working tree clean for this file; `git status --porcelain` returns nothing for it):

```
2  'italaw180871'
6  '[403-blocked]'
1  '[(italaw, Award PDF)]('
1  '[(italaw, Award PDF)](https://www.italaw.com/sites/default/files/case-documents/italaw180871%20-%20Hela%20Schwarz%20v.%20China,%20Award%20of%20the%20Tribunal,%20Dec%2010,%202025.pdf) [403-blocked].'
227 lines
```

The 2026-08-20 council-update block, at line 5 of that file, says of the annotation: *"find it by that string … **`italaw180871`**, which is unique in this file."* **Writing that sentence into the file is what made the string non-unique.** It now occurs twice: once in the instruction and once in the target. This is `0820-R18`'s self-falsification one generation on, and rule (s) did not prevent it — rule (s) fixed the *kind* of pointer and said nothing about a pointer that names itself into the file it points into.

**It is less dangerous than the line number and it is still wrong.** A seat searching `italaw180871` lands first on the instruction telling it to search `italaw180871`, which is visibly not a Sources line — it fails loudly. `:106` landed on a blank line and failed silently. The failure mode is better; the pointer is still broken.

**The anchor the applying seat must use** — verified count exactly **1** at HEAD `80a0b06`:

```
[(italaw, Award PDF)](
```

and, for a whole-string find-and-replace, the full unique fragment, also count **1**:

```
[(italaw, Award PDF)](https://www.italaw.com/sites/default/files/case-documents/italaw180871%20-%20Hela%20Schwarz%20v.%20China,%20Award%20of%20the%20Tribunal,%20Dec%2010,%202025.pdf) [403-blocked].
```

**`[403-blocked]` must NOT be used as the anchor: it occurs six times in the file.** No line number appears anywhere in this instruction.

### `0821-R13` — FOUR CORRECTIONS TO MY OWN DRAFT, EACH BECAUSE THE DRAFT SAYS SOMETHING I CAN NOW SEE IS NOT EXACTLY TRUE.

1. **The timestamp is the run's, not the row's, and my draft truncated it.** `0820-R19` wrote "at `2026-08-19T11:41:56Z`". The field is `fetched_at: "2026-08-19T11:41:56.687748+00:00"` and it is a **file-level** key of `analytics/fetch-results/2026-08-19-zz-render-and-cells.json`, not a per-record key — there is no per-row timestamp in that schema. Writing "the URL returned X at 11:41:56Z" attributes an instant to a row, which is precisely what `G4` forbids: rows are ordered, not timed.
2. **The digest belongs in the annotation and my draft omitted it.** Per `0819-O1`, the sha256 is the only field that permits a later comparison; a byte count alone is a weaker identity. The row carries `sha256: 035e33eddf44dcdc373a14de48c91225a53f6b78e4f67e91607d62f495dc36f8`.
3. **The mirror sentence overstated.** My draft wrote "Mirrored on ICSID's own file host at …". Nobody has fetched that URL. It is title-carried by two seats' independent search calls and nothing more, and the annotation must say so.
4. **THE DRAFT CITES A DEAD ESCALATION NUMBER, AND THIS IS THE SAME DEFECT A THIRD TIME.** `0820-R19` ends "Operator-side text extraction is escalation `N1`." That was true when I wrote it and was falsified within the same session: the 2026-08-20 close-out renumbered, and the *Hela Schwarz* operator request is now **`N3`**, while `N1` is the emailer/record-completion race. **An escalation number is a pointer into a list that is re-issued daily.** Publishing an undated `N`-number into the living memory is the line-number failure wearing different clothes — and it fails *silently*, because `N1` still exists and still resolves, to the wrong item. **Every escalation reference written into `STATE_OF_THE_ANSWER.md` must carry the record date that fixes the numbering.** I propose this as the natural extension of rule (s) rather than a new rule; the chairman should rule.

### `0821-R14` — THE EXACT WORDING I WANT APPLIED TODAY. I HAVE EDITED NOTHING; THE CHAIRMAN APPLIES IT.

**Operation:** locate the unique fragment at `0821-R12`; replace the trailing bracketed annotation `[403-blocked].` — and only that — with the text below, leaving the markdown link, its URL and the surrounding Sources line untouched.

&gt; `[(italaw, Award PDF)](https://www.italaw.com/sites/default/files/case-documents/italaw180871%20-%20Hela%20Schwarz%20v.%20China,%20Award%20of%20the%20Tribunal,%20Dec%2010,%202025.pdf)` **[This URL returned `outcome: "ok"`, `status: 200`, `content_type: "application/pdf"`, `bytes: 1272557`, `sha256: 035e33eddf44dcdc373a14de48c91225a53f6b78e4f67e91607d62f495dc36f8` to this project's fetch relay, as record 4 of the pre-registered batch `analytics/fetch-results/2026-08-19-zz-render-and-cells.json`, whose run carries `fetched_at: 2026-08-19T11:41:56.687748+00:00`, `control_ok: true`, `void: false`. That stamp is the run's and not the row's: rows in that file are ordered, not timed. TEXT NOT READ. The relay decodes no PDF, so that record's `excerpt` is `""` by construction and the reduction is status, byte count and digest only; its `find` was the empty string, so its `find_matched: false` is not an absence of anything. No proposition anywhere in this file rests on the award's words, and no confidence level in this file moves on the strength of this note. The prior `[403-blocked]` label was carried from `403` observations recorded on 2026-06-30 and 2026-07-01 by a different client on different dates; those observations are record-carried rather than artefact-carried — this project's fetch-results corpus opens 2026-08-03 — and they are NOT withdrawn and NOT wrong. A tool-layer status is never a statement about a resource, and that rule runs in both directions: it forbids reading this `200` as a fact about italaw exactly as it forbade reading those `403`s as one. The label is superseded as to this client and this instant, and as to nothing else. A URL for this award on ICSID's own file host — `https://icsidfiles.worldbank.org/icsid/icsidblobs/OnlineAwards/C6447/DS11402_En.pdf` — is title-carried by two seats' independent search calls (integrity officer, 2026-08-20; research analyst, 2026-08-21, under the title `INTERNATIONAL CENTRE FOR SETTLEMENT OF INVESTMENT DISPUTES Hela Schwarz GmbH`) and HAS NEVER BEEN RETRIEVED BY THIS COUNCIL; it would return text no more readably, being a PDF on the same reduction. Operator-side text extraction is escalation `N3` **of the 2026-08-20 record**.]**

**What this asserts:** that one named client, on one named pre-registered row, received a `200`, a content type, a byte count and a digest from that URL; that none of those is text; that a second URL for the same award is title-carried and unfetched.

**What it must not assert, unchanged from `0820-R19` and each omission still deliberate:** not that the document is *accessible* (retrievability is a status; textual access is what the research needs and we do not have it); not that italaw's policy changed or that the July `403`s were wrong; not that the award's contents are available, confirmed or upgraded in confidence — the Article 9(a) and 9(c) entries stay at MEDIUM-HIGH on the IAReporter title, unchanged; not that `[403-blocked]` was a fabrication.

**Accuracy against the file as it now stands:** verified. The target fragment is present exactly once at HEAD `80a0b06`; the file is unmodified in the working tree; the `[403-blocked]` label is still standing and still unedited; and the amendment remains on the operator's PENDING list until Emory rules — **I have edited nothing.**

---

## `0821-R15` — METHOD NOTE (self-training mandate): YESTERDAY'S RULE IS TOO COARSE, AND TWO OF TODAY'S CALLS SHOW WHERE IT BREAKS.

`0820-R9` generalised: *the doctrinal phrase retrieves the explainer literature; the rigid designator retrieves the document.* **S4 falsifies the second half as stated.** `"ICSID Case No. ARB/24/46" ENCORE decision Rule 41` is designator-led, and it returned the explainer literature — a Jus Mundi wiki note, a Mayer Brown quantitative analysis, unrelated `DS…_En.pdf` files, and, on its later internal rounds, US Supreme Court term-opinion pages and `2025 in Turkey`. **Zero returned titles carry `ARB/24/46`.** S6, on the same case, returned the italaw title carrying both the full party name and the designator, first attempt.

**The operative difference is not designator-vs-keyword. It is that S4 quoted `"ICSID Case No. ARB/24/46"` as an exact phrase and S6 did not.** Quoting the full institutional phrase forces a match on a string that few indexed *titles* carry; the retrievable key is **party name + respondent State**, with the bare designator as an unquoted co-term. Yesterday's two successes (`S3`, `S4`) both had that shape and I mis-described why they worked. **Refined rule: quote the party name, never the `ICSID Case No.` phrase; add the respondent State; leave the docket number unquoted.** Cost of learning it: one of six calls.

---

## § CANDIDATE CLAIMS

```json
[
  {
    "claim_text": "No source title-carried to this council decodes the values of ICSID's 'Finding that Claim is Manifestly Without Legal Merit' column; the observed values No, Partial and _ remain untranslated and _ remains undefined.",
    "confidence": "high",
    "sources": ["https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM, scoped to this instrument. WebSearch call S1 of 2026-08-21, query '\"Finding that Claim is Manifestly Without Legal Merit\" ICSID table of decisions', returned eight results; no returned title is a legend, key, codebook or explanatory note decoding the column's values. Per the standing cross-BIT consistency rule this is 'not found in accessible sources', NOT 'does not exist'. The engine's generated prose DID translate two values and is expressly refused at 0821-R2. GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook."
  },
  {
    "claim_text": "A document titled 'JLL Capital v. Honduras, Decision Dismissing Respondent's Preliminary Objections Pursuant to ICSID Arbitration Rule 41, 21 Dec 2023' is indexed by Jus Mundi, and ICSID's own manifest-lack-of-legal-merit register gives the same case and date the finding-column value '_'.",
    "confidence": "medium",
    "sources": [
      "https://jusmundi.com/en/document/decision/en-jll-capital-s-a-p-i-de-c-v-v-republic-of-honduras-party-representatives-monday-13th-february-2023",
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"
    ],
    "supporting_quote": "JLL Capital, S.A.P.I. de C.V. v. Republic of Honduras (ICSID Case No. ARB/23/3) Decision on Preliminary Objections (2022 Rules) December 21, 2023 _",
    "supporting_locator": "The quote is the ICSID register half only, body-carried in the window at analytics/daily-research/2026-08-20.md Part IV section 2 (batch 2, records[3], find ARB/22/30). The Jus Mundi half is TITLE-CARRIED ONLY, from WebSearch call S5 of 2026-08-21, body unread. CONFIDENCE HELD AT MEDIUM AND NOT HIGHER FOR A NAMED DEFECT: the Jus Mundi URL under that title carries the slug '...party-representatives-monday-13th-february-2023', naming a different document and a different date, so the title is not pinned to its URL; three other Jus Mundi slugs returned the same day matched their titles exactly. THE WORD 'Dismissing' IS NOT BANKED AS A DISPOSITION. No decode of '_' is proposed and NO HOLDING IS ASSERTED; nobody has read this decision."
  },
  {
    "claim_text": "ENCORE Investment Group Limited v. Republic of Türkiye, ICSID Case No. ARB/24/46, is title-carried by italaw, independently of ICSID's own register row for the same proceeding.",
    "confidence": "high",
    "sources": [
      "https://www.italaw.com/cases/13154",
      "https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"
    ],
    "supporting_quote": "ENCORE Investment Group Limited (Malta) v. Republic of Türkiye (ICSID Case No. ARB/24/46) Decision on the Respondent Rule 41 Application ( English ) October 7, 2025 No",
    "supporting_locator": "Quote is the ICSID register half, body-carried in the window at analytics/daily-research/2026-08-20.md Part II section 1 (records[3], find ARB/24/). The italaw half is TITLE-CARRIED ONLY, from WebSearch call S6 of 2026-08-21: 'ENCORE Investment Group Limited v. Republic of Türkiye, ICSID Case No. ARB/24/46 | italaw'; body unread; italaw.com/cases/13154 returns 0 files at HEAD 80a0b06. DISCLOSED DISCREPANCY: a TDM Journal title returned by the same call reads 'ICSID Case No. ARB/24/26'; ARB/24/26 returns 0 files at HEAD and the discrepancy is NOT reconciled. The trailing token 'No' is reproduced as the register renders it and IS NOT TRANSLATED. NO HOLDING IS ASSERTED."
  },
  {
    "claim_text": "No IP, trade-secret, clinical-trial-data or pharmaceutical nexus has been observed for any of ARB/24/46, ARB/24/18, ARB/22/30 or ARB/23/3 in any source this council has read.",
    "confidence": "high",
    "sources": ["https://icsid.worldbank.org/cases/content/tables-of-decisions/manifest-lack-of-legal-merit"],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM, scoped and NOT convertible. Basis: every search-result title returned for these four case numbers across WebSearch calls S3-S6 of 2026-08-20 and S4-S6 of 2026-08-21 carries party names, an ICSID case number, a document designation and a date, and no subject-matter term; the ICSID register windows the council body-holds carry the same four elements. PER 0820-B2 THIS IS NO EVIDENCE OF AN ON-THEME PROCEEDING AND EXPRESSLY NOT EVIDENCE OF FOUR OFF-THEME ONES. Nobody has read any of these four decisions. Generated search-engine prose offering sector descriptors for two of the four is refused at 0821-R7 and is not banked in either direction."
  },
  {
    "claim_text": "No accessible source states whether the Hela Schwarz tribunal characterised the China-Germany BIT Protocol Article 9 conditions as going to jurisdiction or to admissibility, and none resolves whether the claims were disposed of on jurisdiction alone or on jurisdiction and merits.",
    "confidence": "high",
    "sources": [
      "https://www.italaw.com/cases/5973",
      "https://jusmundi.com/en/document/decision/en-hela-schwarz-gmbh-v-people-s-republic-of-china-award-wednesday-10th-december-2025"
    ],
    "supporting_quote": null,
    "supporting_locator": "NEGATIVE CLAIM. WebSearch calls S2 and S3 of 2026-08-21 returned eighteen results between them; ZERO returned titles contain 'admissibility', and none names any ground, limb or disposition of the 10 December 2025 award. This is the fourth distinct query formulation across two days to return no commentary layer for this award. The award is UNREAD by every seat; carrying-span clause 3 terminal position taken. The 'jurisdiction and merits' element remains generated prose with no title behind it and is NOT banked, per 0820-R15 and escalation N4 of the 2026-08-20 record. GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility."
  },
  {
    "claim_text": "The anchor string italaw180871 occurs twice in STATE_OF_THE_ANSWER.md at HEAD 80a0b06 and is therefore no longer the unique locator the 2026-08-20 council-update block declares it to be.",
    "confidence": "high",
    "sources": ["STATE_OF_THE_ANSWER.md"],
    "supporting_quote": "The annotation under discussion is the one attached to the URL containing **`italaw180871`**, which is unique in this file",
    "supporting_locator": "Derived programmatically from `git show HEAD:STATE_OF_THE_ANSWER.md` at HEAD 80a0b06, branch council-2026-08-21, working tree clean for that file: str.count gives italaw180871 = 2, '[403-blocked]' = 6, '[(italaw, Award PDF)](' = 1, and the full Sources-line fragment = 1; file is 227 lines. The quoted span is the first of the two occurrences and is itself one of them: the sentence asserting uniqueness is what destroyed it. Replacement anchors, both count 1, are given at 0821-R12. NO LINE NUMBER IS USED ANYWHERE IN THIS CLAIM."
  }
]
```

---

## § OBJECTIONS AND OBSERVATIONS AGAINST MY OWN WORK

**`0821-E1` — MY TASK 1 CONSTRAINT AT `0821-R4` IS ONE ROW WIDE AND I HAVE PRESENTED IT AS IF IT CONSTRAINS A CODEBOOK.** Strictly, it constrains one candidate mapping (`No` = "objection dismissed") on the assumption that a single Jus Mundi title's participle describes a disposition — an assumption I *also* say I refuse to bank at the end of the same section. **A reader could fairly say I banked it in the reasoning and refused it in the label.** The defensible core is narrower than the section sounds: two `_` rows and three non-`_` rows are observed; the `_` rows' document designations differ in form from the others'; nothing else follows. If the officer strikes the conditional at `0821-R4` and leaves only `0821-R5`, I do not resist.

**`0821-E2` — THE JUS MUNDI SLUG DEFECT MAY BE THE ENGINE'S, NOT JUS MUNDI'S, AND I DID NOT SEPARATE THE TWO.** I attributed the title/URL mismatch to the source. An equally available explanation is that the search intermediary paired a title from one page with a URL from another, which is a defect in **my instrument**, not in Jus Mundi — and I have no way to separate them without opening the URL, which this session cannot do. The consequence is the same (do not bank the disposition word) but my attribution is unearned, and it matters because this council relies on Jus Mundi titles as a backsourcing carrier. **The honest form: a title and a URL arrived paired and the pairing is internally inconsistent; which side is at fault is unobserved.**

**`0821-E3` — I SPENT A CALL DISCOVERING A CONSTRAINT MY OWN METHOD NOTE ALREADY IMPLIED, AND THEN CREDITED MYSELF WITH THE CORRECTION.** S4 was mine, it was designed by me the same morning I re-read `0820-R9`, and its formulation (`"ICSID Case No. ARB/24/46"` as a quoted phrase) is one my own note's two successful examples do not use — both left the designator unquoted. **The refinement at `0821-R15` was derivable for free from yesterday's record before I spent anything.** That is the second consecutive day I have paid an external call for something the free instrument held, and `0820-E3` was the first. The pattern is not the individual query; it is that I design the batch before I finish reading my own prior return.

**`0821-E4` — A DEFECT IN THE RELAY SCHEMA BIT ME MID-SESSION TODAY, AND IT IS AN ESCALATION THAT FELL OFF THE LIST.** Verifying the Task 4 row, I ran a census filtering `r.get('status') == 403` over `analytics/fetch-results/*.json` and got **zero rows**, thirty seconds after printing a row whose status displayed as `403`. Cause, at source: `scripts/fetch_relay.py:175` writes `resp.status_code`, an `int`, on contact; `:168` writes `entry.get("detail") or None`, a **string**, on failure. So `status` is `200` (int) on ok rows and `'403'` (str) on refused ones, and any equality filter silently returns a false null on half the corpus. **This is not new** — `analytics/daily-research/2026-08-08.md:114` records `status` carrying three Python types in one file, and `analytics/daily-research/2026-08-10.md:258` says in terms *"Escalate as a second schema item: refused rows record `status` as a string while `ok` rows record it as an int."* **It does not appear anywhere in the 2026-08-20 escalation list `N1`–`N10`.** An escalation raised eleven days ago, still live in the code, dropped from the list that is supposed to carry it, and it produced a false zero in this seat's own working today. I re-tender it and I note the general risk: **the escalation list is re-derived each day and items leave it silently** — the same class of failure as `0821-R13`'s item 4, from the other end.

**`0821-E5` — WHAT I DID NOT CHECK, NAMED SO COVERAGE IS NOT READ AS SILENCE.** I did not search `ARB/24/18` (Eurohold) or re-search `ARB/22/30` (Bank of Nova Scotia) today; my Task 2 verdict for those two rests entirely on yesterday's title harvest, which was collected to answer a different question (does the document exist) and not this one (does it touch ring 1). **A title collected for existence may not have been the best title for subject matter, and I did not test that.** I also did not attempt any second Task 1 formulation after S1 — the null at `0821-R1` is one call deep, not four like Task 3's. And I did not verify that ICSID case-detail pages carry any sector field; `0821-R9` says so, and the probe I propose is a probe precisely because I do not know.

**`0821-E6` — THE PROBE LITERAL I OFFER FOR TOMORROW, AND ITS WEAKNESS STATED BEFORE ANY RESULT EXISTS.** `Status of Proceeding` on `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3`. It is corpus-sourced (body-observed on the `ARB/22/2` case-detail page in `analytics/fetch-results/2026-08-06-daily.json`), pure ASCII, cannot be a case number or a party name, and lands a window in the metadata block where a subject-matter field would sit if one exists. **Its weakness: it contains two spaces.** Per method rule (r) / `G7` the relay can manufacture a space and never remove one, so an *inserted* space cannot break this literal — but the literal is also subjected to the same transform on the `find` side, and a match therefore tests a tag-stripped string against tag-stripped text, which is a weaker test than a space-free designator. If it nulls, `G6`'s zero-width hazard and simple non-rendering are not separable by that row and I pre-register that I will not try to separate them afterwards. The stronger-but-blinder alternative is `Economic Sector`, which has **never** been observed on any ICSID page this council holds and would be a probe into the dark; fire both on the same URL in the same file so each is the other's context, and treat neither as a control for the other.

---

## Part IV — The digest of record (Rule 1)

**Digest 2026-08-17: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).**

Copied from `digests/2026-08-17_ISDS-Thematic-Watch/meta.json`, blob
`e1d1ef764bdc22ef01a88b458939e62d61432b63`, committed at `feb1b94` — `screened: 7`, `matches: 0`,
`watch_list_leads: 0`. The commit is recorded because escalation `N8` is live: **`meta.json` carries no
collection-run identifier**, the 2026-08-17 file was rewritten three times in twenty-three minutes with
`screened` going 13 → 17 → 7, and a record quoting it faithfully at three different moments would have
quoted three different numbers. Rule 1 guarantees fidelity to the file, not stability of the figure, so the
blob is pinned. The digest's own HTML header independently prints `Screened: 7`, which agrees.

*(**`0821-B8`(a), SUSTAINED.** This paragraph read "rewritten three times **in twenty-three minutes**".
`git log -- digests/2026-08-17_ISDS-Thematic-Watch/meta.json` gives `0bb321e` **21:17:09**, `95f2f66`
**23:19:01**, `feb1b94` **23:40:38** — a span of **two hours, twenty-three minutes and twenty-nine
seconds**, and no pair of those stamps is twenty-three minutes apart. The 2026-08-18 record stated this
correctly; today's is a fresh regression on a dropped "two hours and". The 13 → 17 → 7 sequence and the
blob pin are both correct and the officer read each blob to confirm them.)*

**The digest of record is unchanged from yesterday and the two records before it** *(**`0821-B8`(b),
SUSTAINED** — this read "**the four records before it**". `Digest 2026-08-17` is the digest of record in
the 08-18, 08-19 and 08-20 records: **three** prior records, not five. The 08-17 record itself carries
`Digest 2026-08-10`.)*. **A section on an unchanged digest should be short, and this is that short
section** — on calibration item 6 and on Rule 0's stated rationale that a daily check-in costing a weekly
council stops being read. *(**`0821-B9`, SUSTAINED.** This sentence read "**Rule 0 says** a section on an
unchanged digest is short." Rule 0 says no such thing: it governs seat count, the one optimization item,
and six banned forms. Citing a binding protocol for a rule it does not contain is the same defect class the
analyst names at `0821-R13`(4) — a pointer into a living authority that still resolves, to the wrong
thing — and I committed it against the protocol that governs this very record.)*

The next collection is **Monday 2026-08-24**, and the seat that reads it should record which commit it read.

No item in this record derives from the digest. Everything today came from the relay, the repository, and
the analyst's six searches.


---

## Part V(a) — The integrity officer's vetting note, verbatim

Convened as a real subagent from `.claude/agents/integrity-officer.md` and directed at **my own work as the
priority target**, on the record that four of the five parts it vets are mine. Its objections are binding
and I rule on every one in Part VI. **The text below is the member's, reproduced without edit, summary or
correction**; the same transport entity-encoding note applies (`&gt;` is `>`, `&amp;` is `&`).

*(One chairman's note, made here so nobody has to hunt for it: the officer's disposition is that this
record **may not be published as it stands**. It is being published, after every sustained correction was
applied in place, and Part VI records the disposition of each. Nothing below has been softened, and the
objections that hurt most — `0821-B1`, `0821-B2`, `0821-B12` — are all sustained in full.)*

---

# PART V(a) — INTEGRITY OFFICER'S VETTING NOTE, 2026-08-21

**Seat:** integrity officer, convened as a real subagent from `.claude/agents/integrity-officer.md`.
**Record vetted:** `/home/user/isds-watcher/analytics/daily-research/2026-08-21.md` at HEAD `4098c09aba262cfc28596a8ce619b8a92a9d05a9`, branch `council-2026-08-21`, **793 lines**. The record grew during my session: it was 771 lines at `d70fbf1` when I was convened and a **Part IV** was added at `4098c09`. I vet the 793-line state and say so, because a note that vets a superseded state certifies nothing.

**Procedural caveat, ninth consecutive day.** `.claude/agents/integrity-officer.md` declares `model: opus`, a tier alias that pins nothing, and states "Runs on Claude Opus 4.8" / "Model: Claude Opus 4.8". This seat self-reports **`claude-opus-5`**. That is the **ninth** consecutive self-reported discrepancy (escalation `N2` of 2026-08-20 recorded the eighth). Recorded, never silently substituted.

**Instrument probes, run by me before any status-dependent objection.** `curl https://example.com` → `curl: (56) CONNECT tunnel failed, response 403`, HTTP `000`. Fetch service on the same neutral control → `EGRESS_BLOCKED`. `curl https://icsid.worldbank.org/` → identical `CONNECT tunnel failed, response 403`. **I independently reproduce all three rows of Part I's probe table.** WebSearch works (three calls spent, disclosed below). GitHub MCP works. Every statement below about a fetch or a status rests on a control I fired myself.

**Verdict in one line: this record may NOT be published as it stands.** Seventeen binding objections follow, fifteen against the chairman (two jointly) and five against the analyst. The largest is `0821-B1`, and it is the same defect the chairman diagnoses and repents in Part II §1, committed again one section later in Part II(b).

---

## BINDING OBJECTIONS

### `0821-B1` — CHAIRMAN. Part II(b) presents as a new discovery a mechanism this council established in terms on **2026-08-12** and again on **2026-08-13**, and cites neither. This is Part II §1's own converse rule, broken in the same record.

**Text objected to** (Part II(b), lines 347–356, 383–391):

&gt; "## Part II(b) — Escalation `N1` is RESOLVED as to mechanism, yesterday's diagnosis of it was wrong…"
&gt; "**The diagnosis is wrong, and I can show it rather than argue it.**"
&gt; "So the mechanism is a **collision between two workflows that are each individually correct**…"

**Why it fails.** `analytics/daily-research/2026-08-12.md:1167–1179`, escalation 1, verbatim:

&gt; "**NEW, AND READ THIS ONE FIRST — you were emailed today's record MID-SESSION, and the de-duplication marker would have guaranteed you never received the finished one.** `daily-update` fires on push to `analytics/daily-research/**` **from any branch**, not only from `main`. My Part-I-to-IV push triggered it and it sent at **11:34:32Z**… `scripts/send_daily_update.py:98` skips any date whose marker exists — so **the corrected, complete record would have been suppressed by the draft's own success.** I have **deleted the marker in this session's final commit**… **The underlying defect is not fixed and is yours:** either the push trigger should be branch-filtered to `main`, or the marker should be written only for a record whose session has closed."

And `analytics/daily-research/2026-08-13.md:1111–1123`, escalation 1:

&gt; "**This is escalation 1 of yesterday's record happening again, and this time I caused it directly:** `daily-update` fires on push to `analytics/daily-research/**` from **any branch**, and I pushed a commit carrying the record file mid-session. I knew the defect — I wrote it up yesterday — and pushed anyway. As yesterday, **I have deleted `analytics/daily-research/.sent/2026-08-13`**… **A one-line fix has now cost two consecutive days.**"

Every load-bearing element of today's Part II(b) is in those two passages: the **push** path rather than the cron, the **any-branch** absence of a branch filter, the **marker suppressing the good version**, and two candidate fixes. Today's record supplies one genuinely new element — that GitHub evaluates `[skip ci]` on the **head commit of the push** — and presents the whole as `RESOLVED` and as a refutation of yesterday.

I grepped in both directions before asserting this. `grep -n "2026-08-12\|2026-08-13\|marker\|branch-filter\|any branch"` over `analytics/daily-research/2026-08-21.md` returns lines 44, 320, 362, 386, 396–397, 401, 418, 420 — **no citation to either prior record anywhere in the file.**

The aggravation is structural. Part II §1 of this same record says, of the `undocs.org` re-fire: *"**I then fired it a second time without grepping `analytics/fetch-results/` for the host first**, which is one `grep` and would have cost nothing… The standing rule 'grep the record before asserting something is absent from it' has an obvious converse that nobody had written down, and I adopt it below as a method rule."* The chairman adopts the converse in §1 and violates it in §II(b) against `analytics/daily-research/`. **Two seats in two days** became **four seats in ten days**, and the seat that named the pattern is one of them.

**Required fix.** Cite `2026-08-12.md:1167–1179` and `2026-08-13.md:1111–1123`. Retitle Part II(b): the mechanism is **re-established and refined**, not resolved-for-the-first-time. State that the marker-deletion remedy is **established practice in this repository, executed on 2026-08-12 and 2026-08-13**, not a "second-order mitigation" on a pending list — and that yesterday's seat's actual omission was failing to delete it. Record the recurrence against the chairman's own newly adopted converse rule.

---

### `0821-B2` — CHAIRMAN. The proposed fix does not cover the mechanism. **GitHub Actions run `32367660510` (run 278) falsifies it, on the same day, from the same branch, from a non-relay commit.**

**Text objected to** (lines 404–409):

&gt; "**Never put a relay-request commit and an `analytics/daily-research/**` commit in the same push.**"
&gt; "### The fix, which needs no code and is available to every future chairman"

**Why it fails.** The chairman found run 277 and stopped. There is a **run 278**. Retrieved by me from the Actions API:

```
id 32367660510 · run_number 278 · event push · conclusion success
head_branch council/2026-08-20
head_sha  6d46a92b91bc89ff4ad16b4612938856a659a3c7
head_commit "council: daily meeting 2026-08-20 (part 8) — state-of-the-answer council update"
created_at 2026-08-20T12:12:41Z
```

`git show --stat 6d46a92` — that commit touches **`STATE_OF_THE_ANSWER.md` and nothing else**. It is **not** a relay request. It carries no `[skip ci]`, and no README requires it to omit one. It was pushed as head of a push containing the record commits at 12:02–12:10, `daily-update`'s path filter matched, and the workflow fired from the feature branch a second time. Under the chairman's proposed rule, **run 278 still fires.**

Yesterday's branch carried **four** un-skipped commits, not one: `af914ad` (part 1 relay batch), `c437a73` (part 4 relay batch), `c1b2aab` (part 7 — ledgers: `analytics/insights.jsonl`, `analytics/optimization-log.md`, `state/council_log.json`, `state/research_log.json`), `6d46a92` (part 8). **Two of the four are not relay requests.** The record's assertion at line 370 — *"Every one of yesterday's fourteen `analytics/daily-research/` commits carries `[skip ci]` — I checked all fourteen"* — is **true and I verified all fourteen**, but it is the wrong population: the trigger is the head commit of the *push*, which may touch no record file at all.

The record states the correct mechanism at line 386 (*"fires on **any branch** and evaluates `[skip ci]` on the **head commit of the push**"*) and then draws a rule strictly narrower than it. Further, "the fix needs no code" is asserted against two prior seats who proposed a **code** fix and called it "a one-line fix [that] has now cost two consecutive days" — a branch filter to `main`, which does not depend on every future chairman remembering commit hygiene.

**Required fix.** Restate the rule at the level of the mechanism: **every push that contains an `analytics/daily-research/**` commit must have a head commit carrying `[skip ci]`** — equivalently, isolate *every* un-skipped commit, not only relay requests. Name run `32367660510` in the record. Acknowledge the 2026-08-12/13 branch-filter proposal as the structural fix, and rank the no-code discipline as the weaker mitigation it is.

---

### `0821-B3` — CHAIRMAN. *"none of yesterday's three proposed fixes … would have prevented it. All three address the cron path"* is **false for two of the three**.

**Text objected to** (lines 400–403).

**Why it fails.** Yesterday's three, verbatim from `2026-08-20.md:1672–1676`: *"gate `daily-update.yml` on a completion marker the chairman writes in Part VI, or move its schedule after the council's typical close, or have it send the record only when a `Record closed` line is present."*

- "Move its schedule" is cron-only. The chairman is right about this one.
- "Gate on a completion marker" is a **precondition on sending**, not on triggering. Whether implemented as a job `if:` or inside `scripts/send_daily_update.py`, it evaluates on **every** run regardless of event. At 11:38:31Z on 2026-08-20 no completion marker existed. It would have blocked the send.
- "Send only when a `Record closed` line is present" is likewise a trigger-agnostic content precondition on the record file, which at 11:38 held three Parts and no close-out. It would have blocked the send.

This is the load-bearing step in the record's claim that yesterday's diagnosis was "wrong" and that only the new no-code discipline works. It does not hold.

**Required fix.** Withdraw "All three address the cron path." State that fix 2 is cron-specific and fixes 1 and 3 are sender-side and would have prevented the 11:38:39 send. That materially reduces how wrong yesterday's seat was.

---

### `0821-B4` — CHAIRMAN. Part II(c)'s **"606.02 seconds"** is not the interval between the two observations of the page. It is the interval between two **run-completion** stamps, and reporting it as the page interval is the exact defect the analyst corrects at `0821-R13`(1) in the same record.

**Text objected to** (lines 449–455):

&gt; "**The interval, computed from the two `fetched_at` values and printed rather than characterised, as pre-registered: `2026-08-21T11:20:56.301651Z` → `2026-08-21T11:31:02.323068Z` = 606.02 seconds.**"
&gt; "**the page was byte-identical across an interval of 606 seconds.**"

**Why it fails.** The arithmetic is right — I recompute `606.021417 s`. What it measures is wrong. `fetched_at` is written at `scripts/fetch_relay.py` in `run()`'s manifest construction, **after every fetch in the batch has completed**; it is a file-level key and there is no per-row timestamp (I confirmed: no record in `2026-08-19-zz-render-and-cells.json` or either of today's files carries a `fetched_at`). In the morning batch the two ICSID rows are `records[2]` and `records[3]` of **nine** fetches — five further fetches followed, across two hosts, each host pair separated by the `MIN_INTERVAL_SECONDS = 3.0` floor at `src/sources/base.py:28`. In the intraday batch the ICSID rows are `records[2]` and `records[3]` of **five** — one fetch followed. The true page-to-page interval is therefore **shorter than 606.02 s by at least several seconds and plausibly by tens**, and the corpus cannot say by how much, because rows are ordered and not timed.

The record's own morning gate `G4` reads: *"No duration may be read off `fetched_at`. Rows are ordered, not timed."* The intraday request file's `G3` carves out this one interval and calls it "THE ACTUAL INTERVAL" — the carve-out embeds the error rather than curing it. And `0821-R13`(1), 230 lines later in the same record, states the correction in general form: *"Writing 'the URL returned X at 11:41:56Z' attributes an instant to a row, which is precisely what `G4` forbids."*

**Required fix.** Report **606.02 s as an upper bound on the interval between the two observations, not as the interval**, and say why: `fetched_at` is the run's completion stamp and the schema records no per-row time. Nothing else in Part II(c) changes.

---

### `0821-B5` — CHAIRMAN. Part I's compliance self-certification **under-counts the Rule 0 departures**. There are two, not one: Part I contains an **instrument-probe table**, which Rule 0 bans by name.

**Text objected to** (lines 39–40, 62–66):

&gt; "I ran the meeting to Rule 0 with **one** departure… Rule 0 says 'convene exactly two agents… nothing else', and **three were convened**"

followed, twenty-two lines later, by

&gt; `| probe | target | result |` — three rows, `curl` egress ×2 and fetch service ×1.

**Why it fails.** `prompts/daily_council_protocol.md` Rule 0: *"**Banned from the daily record, all of it:** an instrument-probe table; a 'Priority focus' agenda section; a 'Budgets' section; a carrying-span ledger with per-term occurrence counts and parity arithmetic; a 'Verify / be skeptical' section; a 'Repository facts' section."* Lines 62–66 are an instrument-probe table on its face: columns `probe | target | result`, three probe rows.

I grepped the full record for the other five banned forms before asserting anything about them. **None is present** — no "Priority focus", no section headed "Budgets", no "Verify / be skeptical", no "Repository facts". The analyst's `0821-R11` carrying-span note is a single four-mark note, **not** a ledger with per-term occurrence counts and parity arithmetic, so the chairman's anticipated second departure inside Part III does **not** materialise. The undeclared departure is his own, in Part I, twenty-two lines below the certification.

This is the shape sustained at `0820-B10`(c) — a paragraph certifying a departure count that the same record falsifies.

**Required fix.** Amend the certification to **two departures**: the third convened seat, and the probe table. The probing itself is required by the positive-control rule and must continue; Rule 0 bans the **table form**, so state the three probes and their results in prose.

---

### `0821-B6` — CHAIRMAN. Part II §2 asserts a mechanism in one paragraph and disclaims asserting one in the next; and *"carries **no information** about the table's contents"* overreaches what three observations can support.

**Text objected to** (lines 265–277):

&gt; "...a seat who read one as 'the register moved' would be asserting a substantive fact from a byte that is **almost certainly a timestamp or a session counter**."
&gt; "...a digest change on this URL carries **no information about the table's contents**"

against, in the next paragraph:

&gt; "**I do not assert a mechanism** — nobody here has seen what the changing byte is."

**Why it fails.** "Almost certainly a timestamp or a session counter" **is** a mechanism assertion carrying a confidence adverb, and it is the sentence doing the argumentative work; the disclaimer that follows does not retract it. Nobody has observed the changing byte, its position, or its content. Separately, "carries **no** information about the table's contents" is a claim of zero mutual information which two observed transitions cannot establish, and which is false in principle — a large byte delta on this URL would still be informative. The record states the defensible form correctly at line 275 (*"a digest change on this page is **not evidence** that the table changed"*) and the indefensible form ten lines earlier.

**Required fix.** Strike "almost certainly a timestamp or a session counter" or restate as an unranked list of candidate mechanisms, none observed. Replace "carries no information about the table's contents" with the form the record already uses: **"is not, by itself, evidence that the table's contents changed."**

---

### `0821-B7` — CHAIRMAN. Part II §1's *"the council can say so with certainty rather than by inference"* is not available: **today's page was never rendered**, and its identification as the 2026-08-04 interstitial rests on a byte count alone.

**Text objected to** (lines 200–202, 223–225):

&gt; "**The actual cause is that this URL never reaches a document at all.** And the council can say so **with certainty rather than by inference**"
&gt; "…it carries no publication date, and it **cannot carry one, because the date lives behind a viewer this channel cannot enter**."

**Why it fails.** Today's rows 4 and 5 returned `find_matched: false` with **empty excerpts**. No character of today's page was rendered to this council. Today's digest is `94bfd2af4b9f…`; **no 2026-08-04 row carries it** (`/A/81/17` is `140ccb14fd83…`; the two `/en/` rows are `6120ed9cf698…` and `91c4c786ef8f…`). The identification of today's `/A/80/17` response as "the same language-selection interstitial" therefore rests on **byte-count equality alone (4,032)** across a *different symbol on the same path shape* — and byte-count-only identity is precisely what the analyst calls "a weaker identity" at `0821-R13`(2), citing `0819-O1`, in the same record.

The inference is a good one and I do not dispute the conclusion. What I dispute is its epistemic label. "With certainty rather than by inference" is exactly wrong: it is an inference, from a byte count and a path shape, about a page whose text was not returned. And "the date lives behind a viewer this channel cannot enter" asserts **where a datum resides on a resource** from tool-layer evidence — taxonomy 7, tool-status-as-source-state, in the record whose own `G3` is the cardinal gate against it.

**Required fix.** Replace "with certainty rather than by inference" with "by a strong inference from the 2026-08-04 rows". State that today's page text is **unobserved** — both finds null, both excerpts empty — and that the identification is by byte count and path shape only. Restate the viewer sentence as: the date **was not returned by this channel on any observation this council holds**, and where it resides is unobserved. (An empty-`find` row on `undocs.org/A/80/17` would have settled it in one row, as `2026-08-04-undocs-identify.json` did; the batch did not ask for one.)

---

### `0821-B8` — CHAIRMAN. **Part IV carries two wrong counts, both mechanically checkable.**

**Text objected to** (Part IV, lines 782–790):

&gt; (a) "the 2026-08-17 file was rewritten three times **in twenty-three minutes** with `screened` going 13 → 17 → 7"
&gt; (b) "**The digest of record is unchanged from yesterday and the four records before it**"

**Why it fails.**

(a) `git log -- digests/2026-08-17_ISDS-Thematic-Watch/meta.json` gives three writes: `0bb321e` **2026-08-17 21:17:09**, `95f2f66` **23:19:01**, `feb1b94` **23:40:38**, with `screened` 13 → 17 → 7 (I read each blob). The span is **2 hours 23 minutes 29 seconds**, not twenty-three minutes. No pair of those stamps is 23 minutes apart. The 2026-08-18 record got this right ("at 21:30 UTC… at 23:30… at 23:45"); today's is a fresh regression, apparently a dropped "two hours and".

(b) Derived over every daily record:

| record | digest of record |
|---|---|
| 2026-08-17 | Digest 2026-08-10 |
| 2026-08-18 | Digest 2026-08-17 |
| 2026-08-19 | Digest 2026-08-17 |
| 2026-08-20 | Digest 2026-08-17 |
| 2026-08-21 | Digest 2026-08-17 |

`Digest 2026-08-17` stands in **three** prior records, not five. The correct sentence is "unchanged from yesterday and the two records before it."

Everything else in Part IV verifies: `screened: 7`, `matches: 0`, `watch_list_leads: 0`; blob `e1d1ef764bdc22ef01a88b458939e62d61432b63` (`git hash-object` — exact); commit `feb1b94`; `README.md:3` and `index.html:224` both print `Screened: 7`; the Rule 1 sentence is in the mandated form with the mandated terms.

**Required fix.** "two hours and twenty-three minutes"; "the two records before it".

---

### `0821-B9` — CHAIRMAN. Part IV attributes to **Rule 0** a directive Rule 0 does not contain.

**Text objected to** (line 787): *"**Rule 0 says a section on an unchanged digest is short.**"*

**Why it fails.** Rule 0 governs seat count, the one optimization item, and the six banned forms. It says nothing about digest sections. The nearest true statement is the *rationale* ("a daily check-in that costs a weekly council is a daily check-in that stops being read") and calibration item 6. Citing a binding protocol for a rule it does not contain is taxonomy 17, mis-dated/mis-attributed internal-authority citation, and it is the same class of defect the analyst identifies at `0821-R13`(4) — a pointer into a living authority that still resolves, to the wrong thing.

**Required fix.** Attribute the brevity to the calibration checklist item 6 and to Rule 0's stated rationale, or to the chairman's own judgement. Do not put words in Rule 0.

---

### `0821-B10` — CHAIRMAN. The **2026-08-04 excerpt table in Part II §1 names no file and no commit**, and two of its three rows come from a different run than the row that produced the third.

**Text objected to** (lines 200–208): *"it has the answer already in its own results directory, from **2026-08-04**"*, followed by a three-row table headed *"rendered text of the page (relay excerpt, verbatim)"*.

**Why it fails.** House rule 1 of 2026-08-05 / taxonomy 25, mutable-reduction citation: *"every citation to a reduction carries the commit sha that holds it. A reduction is evidence; a path that can be rewritten is not a citation."* The table cites neither a filename nor a commit. A reader cannot re-derive it without knowing that row 1 is `analytics/fetch-results/2026-08-04-daily.json` `records[3]` (run `30904314141`) and that rows 2–3 are `analytics/fetch-results/2026-08-04-undocs-identify.json` `records[1]` and `[2]` (run `30926624643`, empty `find`, 15:57:05Z) — **not** the 11:37:26Z control run, whose rows on those same two URLs returned empty excerpts. The record silently splices two runs into one table.

**I verified every character of all three excerpts against the raw JSON and they are exact**, including the Arabic, Chinese and Cyrillic script names and the internal spacing. So is every byte count (4,032 / 4,157 / 4,157) and the differing-digest-identical-text point (`6120ed9cf698…` vs `91c4c786ef8f…`, both 4,157, identical rendered text). The finding is sound; the citation is not.

**Required fix.** Name both result files and the commit that holds them, and mark which run each excerpt came from.

---

### `0821-B11` — CHAIRMAN. Part II §2's row-count table is **stale at publication** and states only the pinned figure, in a record that applies the opposite standard in §4.

**Text objected to** (lines 255–263): *"Derived programmatically at commit `a7bfc492535b62cf1422d848cb246fa257133c0b`… **5 runs, 3 distinct digests**"* and the row `| 2026-08-21 | 94,994 | ebcebcae8594 | 2 |`.

**Why it fails.** I re-derived it independently over every row in `analytics/fetch-results/*.json` whose `url` is that table. **The chairman's figures are exactly right at the pin:** 2026-08-19 → 5 rows (3 + 2, two runs), 2026-08-20 → 6 rows (3 + 3, two runs), 2026-08-21 → 2 rows, three distinct digests `66a879e013bd` / `29bd0911889f` / `ebcebcae8594` at 94,996 / 94,995 / 94,994, and the digest never varies within a run. I confirmed that last point is **not tautological**: `fetch_one` is called per row with no cache, so two rows on one URL in one run are two separate `polite_get` calls at least 3.0 s apart.

But the record now contains Part II(c), which adds two more rows on 2026-08-21 in a sixth run. At publication the figures are **6 runs and 4 rows on 2026-08-21**, and §2's table says 5 and 2 with no forward pointer. §4 of the same Part does this correctly and says why: *"**The pinned figure and the post-batch figure are both stated rather than one being silently re-derived** (method rule (o))."* §2 applies the lower standard.

**Required fix.** One line under the §2 table: at commit `a7bfc492` the figures were 5 runs / 2 rows on 08-21; after the batch read in Part II(c) they are 6 runs / 4 rows; three distinct digests and within-run constancy are unchanged.

---

### `0821-B12` — CHAIRMAN. Part II(c)'s **pre-registered premise is factually wrong**, and the experiment it justified is **weaker than an observation the corpus already held**.

**Text objected to** — `analytics/fetch-requests/2026-08-21-zz-intraday-drift.json`, `H_what_is_being_tested`, incorporated into the record by Part II(c):

&gt; "**BUT WITHIN-DATE CONSTANCY HAS ONLY EVER BEEN OBSERVED WITHIN A SINGLE RUN, where the rows are seconds apart.** That is a weak test of intra-day stability and this batch is an honest attempt to strengthen it."

and Part II(c) lines 458–465: *"**Ten minutes is a weak test of within-day constancy, and I want the record to be blunt about whose fault that is.**"*

**Why it fails.** The premise is false on the corpus that was in hand when it was written. Derived from `fetched_at` across runs:

| date | runs on that URL | gap between runs | digest |
|---|---|---|---|
| 2026-08-19 | `11:20:09.345687Z`, `11:41:56.687748Z` | **21 m 47 s** | `66a879e013bd` in both |
| 2026-08-20 | `11:20:24.190496Z`, `12:14:20.974687Z` | **53 m 56 s** | `29bd0911889f` in both |

Within-date constancy had **already** been observed **across two runs on two separate dates**, at 22 and 54 minutes. The 54-minute observation of 2026-08-20 is **five times stronger** than the ~10-minute Branch A result the experiment produced. The batch therefore did not "strengthen" anything: it re-established a weaker version of a fact the corpus already carried, and it burned the day's second batch to do it.

This answers the chairman's own question about whether Part II(c)'s self-criticism is doing work. It is real but **misdirected**: the record flogs itself for firing ten minutes after the morning run, when the actual defect is that it did not read its own two prior dates before designing the experiment. That is the same failure as `0821-B1` and `0821-E3` — designing the batch before finishing the reading — in its third instance today.

**Required fix.** Correct the premise in the record: within-date constancy had been observed across two runs on 2026-08-19 (22 min) and 2026-08-20 (54 min). State that today's Branch A adds a **third**, and a **weaker**, such observation. Withdraw "this batch is an honest attempt to strengthen it" and the interval self-criticism built on it, and replace both with the accurate accounting: the batch was redundant against evidence already held.

---

### `0821-B13` — ANALYST. `0821-R11`'s carrying-span note **fails mark parity and Amendment 2**, the check this seat is specifically charged with policing.

**Text objected to** (line 634):

&gt; "**P/Q/D/V for the Task 3 propositions: `Q` — N/A, no carrying span exists in any source I can open; `V` — checked 2026-08-21 by WebSearch S2 and S3; screened by title only, on the rigid designators `Hela Schwarz`, `ARB/17/19` and `admissibility`; `admissibility` returns **zero** across eighteen returned titles; exit taken: clause 3 terminal — source unread.**"

**Why it fails.** `prompts/carrying_span_rule.md` binds analyst returns expressly ("It binds… analyst and officer returns") and requires **four** marks, present and non-empty.

- **`P` is absent.** No one-sentence proposition is written. Step 1 requires it before the source is opened, and it is what gets verified.
- **`D` is absent.** No document, date, dispute, stage, or outcome-on-the-point-cited. The document is identifiable (the 10 December 2025 award) and its stage/outcome field should read `unread`.
- **Amendment 2 is violated.** The V mark's required form is `screened &lt;term:count&gt;`, and *"for every screened term with a nonzero count whose occurrences are NOT the source of Q, one clause in your own words saying what the source is discussing there."* A count is given for exactly **one** of three screened terms. `Hela Schwarz` and `ARB/17/19` are certainly **nonzero** — `0821-R11` itself lists four returned titles, at least three containing `Hela Schwarz` and one containing `ARB/17/19`. `Q` is N/A, so neither term can be the source of `Q`. **Both require a count and a referent clause, and neither has either.** A nonzero screen reported without its referent is precisely what reads to a later seat as corroboration, which is why this amendment exists.

**Required fix.** Write `P`; write `D` with `unread` in the outcome field; give counts for all three screened terms and a referent clause for each nonzero one ("the occurrences of `Hela Schwarz` are in procedural-order and case-page titles, none of which addresses the award's grounds").

---

### `0821-B14` — ANALYST. **`admissibility` is not a rigid designator**, and a zero on it is not absence until a synonym is tried.

**Text objected to** (line 634, "the rigid designators `Hela Schwarz`, `ARB/17/19` and `admissibility`") and the candidate claim at lines 738–745 (*"ZERO returned titles contain 'admissibility'"*).

**Why it fails.** The rule defines a rigid designator as *"a name the field cannot paraphrase away"*. `Hela Schwarz` and `ARB/17/19` qualify. `admissibility` is a **doctrinal term with live paraphrases** — receivability, competence, "the tribunal declined to hear", "preliminary objection", ratione temporis — and the rule states in terms: *"Zero on a descriptive word: try its synonym."* This is also a standing rule of this seat adopted 2026-08-07 (*"a zero-hit screen is not absence until the synonym is tried"*), and it is one of the two traps my own definition names as mine in my own findings.

The consequence is that the negative claim at high confidence — *"No accessible source states whether the Hela Schwarz tribunal characterised the … Article 9 conditions as going to jurisdiction or to admissibility"* — rests on an unsynonymised zero, mislabelled as a rigid-designator zero.

**Required fix.** Reclassify `admissibility` as a descriptive term whose zero is uninformative until synonymised. Either run the synonym screen on the eighteen titles already in hand (free — it is a re-read, not a call), or hedge the claim's confidence and say the screen is single-term.

---

### `0821-B15` — ANALYST. Candidate claim 2 asserts a document **"is indexed by Jus Mundi"** while citing, as its source, a URL that on the analyst's own account carries a different document. `0821-E1`'s concession does not reach this.

**Text objected to** (lines 711–718):

&gt; `"claim_text": "A document titled 'JLL Capital v. Honduras, Decision Dismissing Respondent's Preliminary Objections Pursuant to ICSID Arbitration Rule 41, 21 Dec 2023' is indexed by Jus Mundi…"`, `"confidence": "medium"`, `"sources": ["https://jusmundi.com/en/document/decision/en-jll-capital-s-a-p-i-de-c-v-v-republic-of-honduras-party-representatives-monday-13th-february-2023", …]`

**Why it fails.** `0821-R4` establishes that this URL's slug names *party representatives* and *13 February 2023* — a different document and a different date — and `0821-E2` concedes the analyst cannot tell whether the mismatch is Jus Mundi's or the search intermediary's. If he cannot tell, then **"is indexed by Jus Mundi" is not what he observed.** What he observed is that a search call returned that title paired with that URL and that the pairing is internally inconsistent. Citing the mismatched URL as the claim's *source* compounds it: the source field asserts a location for a document he has just said is not at that location. This is quote/access integrity, and it is the one place in an otherwise scrupulous return where a title is allowed to stand for a resource.

`0821-E1` concedes only that the `0821-R4` conditional is one row wide. It does not reach the candidate claim's `claim_text` or its `sources` array, and the concession as offered ("strike the conditional at `0821-R4` and leave only `0821-R5`") would leave this claim standing unamended.

**Required fix.** Restate `claim_text` as: *a WebSearch call of 2026-08-21 returned the title "…" paired with Jus Mundi URL "…"; the pairing is internally inconsistent and which side is at fault is unobserved.* Either annotate the URL in `sources` as the returned-but-mismatched pairing or move it into the locator. Confidence stays medium. **Do not strike `0821-R4`'s conditional** — see `0821-O8`: it is the record's only guard against the disposition prose I met independently today.

---

### `0821-B16` — BOTH. **Two wrong source pinpoints**, one of them replicated by the chairman from the analyst's line range into a pre-registration gate.

**Text objected to.**
(a) `0821-R3`: *"`excerpt_of` returns `""` for a PDF by construction (`scripts/fetch_relay.py:122-123`)"* — and the same range in the chairman's `analytics/fetch-requests/2026-08-21-daily.json`, gate `G12`: *"(:122-123, the reduction decodes no PDF)"*.
(b) `0821-R9`: *"`investmentpolicy.unctad.org`, which is on `ALLOWED_HOSTS` (`scripts/fetch_relay.py:52`)"*.

**Why it fails.** At the blob pinned in both batches:

```
122:     travels is a locator-sized quotation, never the document.
123:     """
124:     if "html" not in content_type and "text" not in content_type:
125:         return ""
```

The non-text guard is at **:124-125**. Lines 122–123 are the tail of the docstring. And:

```
52:     "italaw.com",
53:     "investmentpolicy.unctad.org",
```

`investmentpolicy.unctad.org` is at **:53**; `:52` is `italaw.com`.

Taxonomy 17a, mis-located internal citation — right file, adjacent lines — the same class corrected against this project at `0812-B2`. The chairman's replication is the aggravating half: a wrong pinpoint inside a **pre-registered gate** is a wrong pinpoint that binds every later reading.

Everything else in this family verifies exactly: `:49` icsid, `:55` uncitral, `:56` undocs, `:68` example.com, `:76` `MAX_URLS = 12`, and `0821-E4`'s `:175` / `:168` status-type pinpoints — I confirmed `:175` writes `resp.status_code` and `:168` writes `entry.get("detail") or None`, and I reproduced his census: **`status == 403` returns 0 rows; `status == '403'` returns 15**, across 174 int, 19 str and 3 None values in the corpus. `0821-E4` is correct and its escalation is properly re-tendered.

**Required fix.** `:124-125` and `:53` in both the record and the request file's gate text.

---

### `0821-B17` — ANALYST. `0821-R3`'s conclusion *"I therefore do not propose either as a relay row… They are operator-side retrievals"* does not follow. **The ICSID publication has an HTML carrier on `ALLOWED_HOSTS`**, and one WebSearch call of mine surfaced it.

**Text objected to** (line 534):

&gt; "**Both are `.pdf`. `excerpt_of` returns `""` for a PDF by construction…, so a relay row on either returns a byte count and a digest and zero characters** — escalation `N3`'s known limit, on two new URLs. **I therefore do not propose either as a relay row**, and I say so explicitly because proposing them would look like progress and would produce none."

**Why it fails.** The reasoning conflates *this rendition of the document* with *this document's availability to the relay*. WebSearch call **X3** of this vetting (`ICSID "In Focus" "Rule 41.5" "Objections that a Claim Manifestly Lacks Legal Merit" publication pdf`) returned, title-carried:

- `In Focus: Objections that a Claim Manifestly Lacks Legal Merit (ICSID Convention Arbitration Rule 41(5)) | ICSID` — `https://icsid.worldbank.org/resources/publications/focus-objections-claim-manifestly-lacks-legal-merit-icsid-convention`
- `Manifest Lack of Legal Merit - ICSID Convention Arbitration (2022 Rules) | ICSID` — `https://icsid.worldbank.org/procedures/arbitration/convention/manifest-lack-of-legal-merit/2022`

Both are on `icsid.worldbank.org` (`ALLOWED_HOSTS`, `:49`), neither has a `.pdf` extension, and this council has read HTML from that host on every batch since 2026-08-19. **I assert only that those two titles and those two URL strings were returned to me on 2026-08-21.** I do not assert that either page decodes `No`, `Partial` or `_`, that either is HTML on retrieval, or anything about their contents; the engine's prose about what the In Focus brief contains is generated prose and I refuse it. What is established is that `0821-R3`'s route conclusion — the only route conclusion Task 1 produced — was drawn without the landing page, and is wrong as to route availability.

This compounds `0821-E5`'s own concession that *"the null at `0821-R1` is one call deep"*. A one-call null that also mis-states the retrieval route is not a clean null.

**Required fix.** Withdraw "they are operator-side retrievals" as a route finding. Record the two HTML ICSID URLs as candidate relay rows for tomorrow (with pre-registered nulls, as `0821-R9` does properly), and mark `GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook` as **one call deep with an unexplored allowed-host route**, not as a settled null.

---

## OBSERVATIONS

**`0821-O1`** — Part II §2's headline *"moved on three consecutive days, by exactly one byte each time"* describes **two** observed transitions (08-19→08-20, 08-20→08-21) across three observation dates. The state before 2026-08-19 is unobserved — the corpus's first row on that URL is 2026-08-19. "Three… each time" reads as three moves. Not binding; the conservative conclusion is unaffected.

**`0821-O2`** — Line 359: *"`.github/workflows/daily-update.yml` fires on **three** things"*. It fires on four: `push`, two `schedule` crons, and `workflow_dispatch`. The omission does not damage the argument (the run's `event` is `push`), but the file was being described, not summarised.

**`0821-O3`** — Line 365's inference — *"**11:38:39 is not 12:00, 15:00, 18:00 or 21:00.** So a cron did not send yesterday's draft"* — is exact-match reasoning on a workflow whose own scheduled runs that day fired at **13:00:35, 15:39:25, 18:49:24 and 21:33:43Z**, i.e. 30–90 minutes late. Cron delay never runs *early*, so the conclusion survives a fortiori; but the stated reason is not the reason. The sound reason is that 11:38 precedes the earliest cron, and in any case `run.event: push` settles it directly.

**`0821-O4`** — Line 361 quotes the workflow header as *"the real delivery path is the recurring cron"*; the file reads "**T**he real delivery path…". Sentence-initial case inside quotation marks. Trivial, recorded because I check quotes character-exact and report what I find.

**`0821-O5`** — "a nineteenth consecutive time" (line 72), "for the ninth consecutive day" (line 36) and "a page whose rendered bytes have not changed in **nine days**" (line 323) are ordinals stated without derivation, which the batch's own `G15` requires to be derived programmatically and shown. All three are defensible on the corpus — 2026-08-03→08-21 is nineteen dates; 08-13→08-21 is nine council seats; and `f9712994f5ae…` stands across **nine observation dates** from 2026-08-13 (though only eight elapsed days, and the transition from `ca87a113c024…` is bracketed by 08-11 and 08-13, with no 08-12 observation). Show them.

**`0821-O6`** — Length. Part II's heading is a single ~350-character four-clause sentence; Part II(b)'s and II(c)'s are similar. Calibration item 6 says default shorter than necessary, and Rule 0's rationale is that an unread record verifies nothing. A record that has to be read by an operator on a phone should not have paragraph-length headings.

**`0821-O7`** — The analyst's candidate-claim locators cite the *record* window (`analytics/daily-research/2026-08-20.md` Part II §1 / Part IV §2) plus `records[N]` and `find`, but not the reduction file plus commit. House rule 1 / taxonomy 25 asks for the reduction and its sha. The locators are precise enough that I resolved both quotes to the raw file in one pass, so this is an observation, not an objection.

**`0821-O8` — A REFUSED LEAD, RECORDED SO THE CHAIRMAN MEETS IT HERE AND NOT FRESH.** My own WebSearch call X2 on JLL Capital returned generated prose stating that *"the tribunal denied Honduras's preliminary objection, ruling that it did not meet the high standard of Rule 41(5)"* and that it *"later bifurcated the proceedings to hear Honduras's objection based on the claimant's failure to exhaust local remedies."* **No returned title carries any of it. It is not banked and I assert nothing from it.** It is recorded under clause 7 because it is fluent, it would decode `_` in one line, and it is exactly what `0821-R4`'s conditional and `0821-R2`'s refusal exist to stop. This is why `0821-B15` says amend the candidate claim and **keep** `0821-R4`'s conditional: the conditional is the guard.

**`0821-O9`** — My call X2 **independently reproduced the Jus Mundi title/slug mismatch the analyst reports at `0821-R4`**, on both the 21 Dec 2023 decision title and the adjacent 14 Nov 2024 Reconstitution title, with the same two slugs. That corroborates the *phenomenon*. It does **not** resolve `0821-E2`'s attribution question, because my instrument is the same class as his; his honest form ("a title and a URL arrived paired and the pairing is internally inconsistent; which side is at fault is unobserved") is the right one and I adopt it. Taxonomy 16 — second-instrument corroboration fallacy — bars me from treating my reproduction as independent confirmation of the *cause*.

**`0821-O10`** — I independently confirmed `https://www.italaw.com/cases/13154` as title-carrying `ENCORE Investment Group Limited v. Republic of Türkiye, ICSID Case No. ARB/24/46` (call X1). `0821-R8`'s backsourcing is sound and no fabrication risk attaches to it. The `ARB/24/26` TDM discrepancy is properly disclosed and properly unresolved.

**`0821-O11`** — Corroboration **for** the chairman, recorded because I looked for the opposite. Today's relay-request commits `80a0b06` and `2e4ebae` each touch exactly one file under `analytics/fetch-requests/`, and **no `daily-update` push run has fired today** — the most recent is run 279 of 2026-08-20. The isolation discipline held in practice today, whatever `0821-B2` says about the rule's coverage. Part II(b)'s admission that it held "for the wrong reason" is honest and is the best paragraph in that section.

**`0821-O12`** — The vault taxonomy at `agents/integrity-officer.md` stands at **27 entries, last extended 2026-08-07**, with the entry-27 collision still unresolved and escalated at `Workflow Threads` C11. Fourteen days without an extension, in the file the self-training mandate names, in a period that produced `0820-B1` through `0820-B13`. Today's record supplies at least one candidate — *stale-count-at-pin published beside a later act of the same session* (`0821-B11`) — and one recurrence that belongs in the change log under 27's corollary (`0821-B1`). I do not extend the taxonomy from this seat without the chairman's adoption; I record that it is due.

**`0821-O13`** — `scripts/check_marks.py` **does not exercise this record at all.** Its docstring is explicit that coverage is conditional on the four-mark form, and the carrying-span rule states expressly that it "does not bind the pipeline's structured `candidate_claims`". So the six candidate claims are governed by the pre-ledger system, and `0821-R11`'s four-mark note is the only artifact in the record within tier 2/3 scope. Everything in `0821-B13` was checked by hand, as my definition says it must be.

---

## COVERAGE STATEMENT

What I checked, by name, so the council can tell coverage from silence.

**Instruments, probed not assumed.** `curl` → `example.com` (neutral control) and `icsid.worldbank.org`; fetch service → `example.com`. All three refused, reproducing Part I's table. Three WebSearch calls spent: X1 (`italaw "ENCORE Investment Group Limited" Türkiye "ARB/24/46"`), X2 (`"JLL Capital" Honduras "Rule 41" decision "21 Dec 2023" jusmundi`), X3 (`ICSID "In Focus" "Rule 41.5" …`). GitHub MCP: `actions_get` on run `32364771424`; `actions_list` workflow runs for `daily-update.yml` (30 rows, plus the full push-event listing back to 2026-08-06); `actions_list` jobs and `get_job_logs` on runs `32364771424` and `32367660510`.

**Derived programmatically at HEAD `4098c09aba262cfc28596a8ce619b8a92a9d05a9`**, over all 30 files in `analytics/fetch-results/*.json`:
- ICSID MLLM table: 15 rows, 6 runs, 3 distinct digests; per-date splits 5 / 6 / 4; within-run constancy confirmed non-tautological against `fetch_one`.
- `find == "A/81/17"`: **18** rows — 16 on `uncitral.un.org/en/commission`, 1 on `undocs.org/A/81/17` (2026-08-04), 1 on `digitallibrary.un.org` (2026-08-08). Exactly as §4 states.
- `uncitral.un.org/en/commission`: **20 runs, 2 distinct digests** (`ca87a113c024…` through 2026-08-11, `f9712994f5ae…` from 2026-08-13), never varying within a run. Exactly as §4 states.
- `example.com`: **64 observations, all 559 bytes / `ff67a9d764d6…`**. "The same digest on every run this council has made" — verified.
- ICSID case-detail rows: **16** (`ARB/22/2` ×3, `UNCT/20/6` ×13 across two URL encodings) — the analyst's figure and his split are both right.
- `status` typing census: 0 rows at int `403`, 15 at str `'403'`; 174 int / 19 str / 3 None overall. `0821-E4` verified.
- Screen of every stored case-detail excerpt for `Economic Sector|Subject Matter|Sector|Industry|Applicable Treaty|Convention`: **zero hits**, as `0821-R9` states. `Status of Proceeding` present in the `ARB/22/2` excerpt of `2026-08-06-daily.json`, as `0821-E6` states.

**Quotes checked character-exact against the reduction JSON, not against the record:**
- Today's `A/80/17` window at `records[7]`, all 400 characters including the terminal `eightieth se` and the `58 th` space. **Exact.** Match index **100**; `Manifest Lack of Legal Merit` index **13**. Both `G8` readings correct.
- All three 2026-08-04 `undocs.org` excerpts, all three byte counts, both digests, both final URLs. **Exact** (citation defect at `0821-B10`).
- Candidate claim 2's supporting quote → `analytics/fetch-results/2026-08-20-zz-column-and-middle.json` `records[3]`, `find "ARB/22/30"`. **Exact.**
- Candidate claim 3's supporting quote → `analytics/fetch-results/2026-08-20-daily.json` `records[3]`, `find "ARB/24/"`. **Exact.**
- Candidate claim 6's supporting quote → `git show 80a0b06:STATE_OF_THE_ANSWER.md`. **Present, exact, line 5 of the 2026-08-20 council-update block.**

**Repository claims re-derived at `80a0b06`:** `italaw180871` = **2**; `[403-blocked]` = **6**; `[(italaw, Award PDF)](` = **1**; the full Sources-line fragment = **1**; `git grep -l -F` returns **0** files for `italaw.com/cases/13154`, `ARB/24/26`, both UNCTAD case URLs, and both PDF path fragments, and **39** for `investmentpolicy.unctad.org/investment-dispute-settlement/cases`. Every one of the analyst's Task 4 and Task 2 counts is correct.

**Git and Actions.** All fourteen 2026-08-20 `analytics/daily-research/` commits carry `[skip ci]` — checked individually. The four un-skipped branch commits identified. Run 277 head commit message verified character-for-character against `c437a735…`. Run 277's job log shows a 7-second SMTP send; runs 278 and 279's show `state: FOUND (already sent today — skipping)`. **Exactly one email went to the operator on 2026-08-20 and it was the draft** — the chairman is right about that, and I confirmed it rather than accepting it.

**Read in full:** `prompts/daily_council_protocol.md`, `prompts/council_security.txt`, `prompts/council_calibration.md`, `prompts/carrying_span_rule.md`, `scripts/check_marks.py` module docstring, `agents/integrity-officer.md` (27-entry taxonomy, the collision note, the standing screening rule), `scripts/fetch_relay.py`, `.github/workflows/daily-update.yml`, both of today's request files, both of today's result files, the three 2026-08-04 `undocs` files, `analytics/daily-research/2026-08-12.md` and `2026-08-13.md` escalation 1, `2026-08-20.md` escalations and next steps and Part V(a), `analytics/insights.jsonl:49`, `digests/2026-08-17_ISDS-Thematic-Watch/meta.json` at all three blobs.

**NOT checked, named so silence is not read as coverage.** I did not verify Rule 1's claim that the emailer independently prints `meta.json`'s numbers (I did not read `scripts/send_daily_update.py`). I did not audit the optimization item — the record contains none yet. I did not check `src/integrity_gate.py` or `scripts/verify.py` against the six candidate claims; the ASSERTION decision is theirs and I did not run them. I did not verify `0821-R15`'s account of what S4's later internal rounds returned — that is unreproducible from the record and I take it on the analyst's word. I did not verify the 2026-06-30 / 2026-07-01 `403` observations behind `[403-blocked]`; they predate the corpus and `0821-R14` correctly says so. I did not attempt any synonym screen on the eighteen *Hela Schwarz* titles myself — that is the analyst's to run under `0821-B14`, and I spent my calls elsewhere.

---

## NEAR-MISSES, DISCLOSED

Objections I drafted and withdrew, with the reason each died.

1. **"'The council knew that on 2026-08-04' overstates the 2026-08-04 record."** Drafted on the theory that 2026-08-04 retired the route as a *publication-detection* path, not as a *date-extraction* path. **Withdrawn** after reading `analytics/insights.jsonl:49` and `2026-08-04.md:701–707`: the 08-04 finding is that the route returns a client-rendered viewer shell and the relay executes no JavaScript, "so it sees the shell and never the document." That covers date extraction. The chairman's characterisation is fair, and his self-accusation is correctly severe.

2. **"'The digest never varies within a single run' is a tautological instrument check (taxonomy 26)."** Would have been fatal if the relay cached per URL. **Withdrawn** after reading `fetch_one` and `run`: no cache, each row is a fresh `polite_get` with a 3.0 s per-domain floor. Two rows on one URL in one run are two real fetches seconds apart. The claim is weak evidence but it is evidence.

3. **"`0821-R11` misattributes the `icsidfiles.worldbank.org` URL to the integrity officer."** The 2026-08-20 record attributes that host to the *analyst's* S5/S6 at line 690. **Withdrawn** on reading line 1319, which is inside Part V(a) — yesterday's officer reproduced the `C6447/DS11402_En.pdf` path on an independent call and flagged `DS21216_En.pdf` as unconfirmed. The analyst's attribution, and `0821-R14`'s "integrity officer, 2026-08-20", are both correct.

4. **"`0821-R12`'s '227 lines' is wrong."** `wc -l` and `str.splitlines()` both give **226**; `len(t.split('\n'))` gives 227 because the file ends with a newline. **Withdrawn as de minimis** — no proposition turns on it and the locator method (anchor string, never line number) is the point of the section. Recorded here rather than as an objection.

5. **"Part I contains a Rule-0-banned 'Budgets' section."** **Withdrawn**: the section is headed "The agenda, and the spend checkpoint" and Rule 0 bans the named form. I grepped all six banned forms before asserting anything about any of them; only the probe table is present, and that one is sustained at `0821-B5`.

6. **"The analyst's 16 case-detail fetches double-count two URL encodings of `UNCT/20/6`."** **Withdrawn**: 3 + 12 + 1 = 16, and his stated split (`ARB/22/2` ×3, `UNCT/20/6` ×13) already aggregates the two encodings correctly. The figure is right and better reasoned than my objection.

7. **"Part II(c)'s Branch A digest comparison is uninterpretable because row 3 nulled."** **Withdrawn**: `G4` of the intraday file makes `urls[2]`, not `urls[3]`, the positive control, and it matched. The comparison is interpretable. `0821-B12` is a different and larger objection about the same batch.

8. **The one that got away and then did not.** I nearly closed the emailer investigation at run 277, exactly where the chairman closed it, because run 277 answers the question that was asked. What made me look further was the standing rule that a grep establishes absence from the repository and never from the project — applied to the Actions API rather than to `git grep`. Run 278 was in the second page of the same listing. `0821-B1` and `0821-B2` both come from that one extra call, and I record the near-miss because the chairman's failure and my near-failure have the same shape.

---

## DISPOSITION

**This record may NOT be published in its present state.**

It may be published once **`0821-B1` through `0821-B17` are ruled on and the sustained corrections applied in place**, with the pre-correction wording quoted at each site per this council's practice. Four are structural and must be applied before any send:

- **`0821-B1`, `0821-B2`, `0821-B3`** — Part II(b) is the record's headline claim and it is wrong in three separate ways: it re-discovers a nine-day-old finding without citing it, proposes a fix its own mechanism defeats, and mischaracterises two of the three prior proposals it dismisses. It must not reach the operator as "RESOLVED".
- **`0821-B5`** — the compliance self-certification must not go out under-counted for the third time in six days.

The remainder are corrections of degree, arithmetic, citation and hedging, and none of them touches a finding about ICSID, UNCITRAL or any proceeding. **No fabrication was found anywhere in this record.** Every case number, party name, digest, byte count, URL and quoted window I could reach was real and, where I could reach the raw source, exact. The chairman's Part II §1 and §4 are the strongest instrument work this council has produced, and the analyst's return is the cleanest I have vetted: he conceded ground before I arrived at `0821-E1` through `0821-E6`, disclosed a schema defect that bit him mid-session and had fallen off the escalation list, refused generated prose that would have made his own sections tidier, and every one of his eleven repository derivations reproduced exactly under my own hand. `0821-B13` through `0821-B17` are five real defects in a return that made itself easy to check, and that is the opposite of the failure this seat exists to catch.

**One item for the operator's attention that is not a defect in the record.** Escalation `N1` has now been raised, in the same terms, in the escalation slot of the 2026-08-12, 2026-08-13 and 2026-08-20 records, and re-derived from scratch today. Four seats have spent four sessions on a defect whose fix two of them wrote down: **branch-filter `daily-update.yml`'s push trigger to `main`.** `.github/` is outside every one of these sessions' merge scope. Until it is inside somebody's scope, this council will keep paying for it, and the record that pays for it will keep being the one that says so.

Filed by the integrity officer, 2026-08-21. Every objection above is binding; the chairman rules on each in Part VI, and an unruled objection is itself a defect.
