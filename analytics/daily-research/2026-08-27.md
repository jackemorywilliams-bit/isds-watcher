# ISDS Thematic Watcher — daily council, 2026-08-27

**Chairman:** council chairman seat, this scheduled cloud session, Claude Opus 5 per the operator's model
directive of 2026-08-03.
**Convened as real subagents:** research analyst; integrity officer.
**Not convened, per Rule 0:** analytics officer; systems researcher.

---

## Part I — Agenda, instrument state, and the shape of this meeting

### What this record is, for a reader who has none of its history

This project watches for a narrow intersection: **intellectual property — including trade secrets and
clinical-trial data — asserted as a covered investment**, against **a regulatory or judicial measure** as the
disputed conduct, with **a live, litigated jurisdictional or admissibility doctrine**. It is deliberately
narrow, and a genuinely quiet day is an honest result rather than a failure. The daily council is a check-in:
one researcher, one chairman, one optimization item, and a binding integrity challenge heard inside the
discussion.

The reader who needs one sentence before the rest: **the retrieval fired today did what it was designed to do —
it broke a five-day-old puzzle about one ICSID docket into two separate facts, and it read the annulment
section of a second docket for the first time — and both results land on questions of instrument behaviour
rather than on the research question itself, which has not moved.**

### On the length of this record, which is a chairman's decision and not an accident

Rule 0 exists because a daily check-in that costs a weekly council stops being read, and *"an unread record
verifies nothing."* Yesterday's record ran to 1,268 lines. **That is not a daily check-in, and I am not going to
match it by inertia.** Today's record keeps every binding element — the pre-registration, the verbatim member
returns, the dialogue, the rulings, the accountability — and cuts the restatement. Where a fact is already on
the record and unchanged, I point at it by date rather than re-deriving it in prose. **If this record is
shorter than yesterday's, the reason is that I judged length itself to be a defect Rule 0 names, and not that
less was done.**

### The precedence conflict, stated where the protocol requires it

The routine prompt that fires this session asks for a six-part meeting with a separately-filed integrity
officer, an analytics officer and a systems researcher. `prompts/daily_council_protocol.md` Rule 0 says the
daily meeting is **the researcher, the chairman, and one optimization item**; that the integrity challenge
**"is not a separately convened seat and does not file its own return"**; and that the analytics officer is
not convened at all, the Rule 1 digest sentence being two lines the chairman copies from `meta.json`. That
file's Precedence clause says it wins over conflicting routine wording, **and that the conflict itself is worth
a line in the record's procedural caveat.** This is that line.

**The Rule 0 departure, disclosed by name and not softened.** I convene the integrity challenge as a real
subagent, which Rule 0 says it is not. The reason is the operator's cardinal rule: the REAL AGENTS rule forbids
me performing any member's voice, so a real subagent is the only way this record has an integrity challenge at
all. **What I honour is the rest of Rule 0 — the officer files no Part of his own, and his text is heard inside
the Part V dialogue with the analyst answering on the record.** This is the disposition reached at `0825-B4`
and adopted again at `0826`; I take it deliberately, for the third consecutive session, rather than by drift.

### The member return-path protocol

Each convened seat is spawned with the Agent tool against its committed definition in `.claude/agents/`,
receives a written brief with bounded budgets and the binding rules of its seat, and returns markdown that is
dropped into this record **verbatim and unedited.** I direct; I do not write. Where I disagree with a returned
figure, the member's text stays as filed and my correction is recorded **beside** it, in my own voice, in the
close-out — the `0822-B14` disposition. **The integrity officer's objections are binding on me, not advisory.**
Where a seat cannot be spawned, this record says PROCEDURAL FAILURE and I do not perform the seat.

### The egress probe, run at session open rather than inherited

**Three probes, all at 11:22 UTC, before any other work, and stated as prose because a table here is a banned
form** (`0823-B7`, sustained). `curl` to the neutral control `https://example.com` returned `CONNECT tunnel
failed, response 403`; `curl` to the second neutral control `https://www.iana.org` returned the identical
error; and **the agent proxy's own status endpoint independently logged both**, as `connect_rejected` —
*"gateway answered 403 to CONNECT (policy denial or upstream failure)"* — timestamped
`2026-08-27T11:22:35.026Z` and `2026-08-27T11:22:37.514Z`.

**NO STATE CHANGE from 2026-08-02 onward. This is the twenty-sixth consecutive day.** The proxy's status
endpoint is the strongest of the three witnesses because it timestamps the denial and names it **a policy
denial at the gateway**, which removes the last room for reading the failure as a resource problem.

**All three are facts about this client and none is a fact about any resource** — the standing
tool-status-as-source-state rule. `example.com` is not down: the relay reached it twice in the same run, at
both ends, and both rows returned `ok/200`.

**The committed `fetch-relay` is ALIVE and did the day's retrieval.** Fetch-dependent work is therefore **not
stranded** and is **not** routed to an operator-side session.

### Two environment facts recorded before any count in this record

**One — `N14` recurred, for the sixth consecutive session. The clone arrived shallow.** It was unshallowed to
**819 commits before any figure below was computed.** A shallow clone is dangerous here precisely because it
fails silently: `git log` answers, and answers wrongly, so a prior-art screen run on it returns smaller counts,
and smaller counts read as "no prior art."

**Two — the `gh` CLI is not present in this session** (`command -v gh` returns nothing). The routine's output
path is written in `gh` commands; I use the GitHub MCP tools instead, which do the same work. **Recorded as a
procedural caveat, not a silent substitution.**

### The batch was fired before any seat was convened, and commit order proves it

`analytics/fetch-requests/2026-08-27-daily.json` was written and pushed at **`7a6ecab`**, firing relay run
**33067296770** at 11:25:24Z, **before the research analyst was spawned.** Every licence, gate and
pre-registered branch in that file was fixed **before any result existed**, and the file is in the repository
for anyone who wants to check that against what this record later claims. **The pre-registration is provable
from commit order and does not depend on my honesty about it.**

**Nine targets, three under the cap of twelve.** Six are the research analyst's rows A–F at `0826-R15`, adopted
**with his licences verbatim and with no chairman correction**; two are the standing UNCITRAL `A/81/17` watch
fired as a pair with its `A/80/17` control, carried by me and disclosed as low-value; one is the `G18` tail
bracket. **Rows G and H are CUT together**, on the 2026-08-26 close-out decision 1. **The two slots freed by
that cut stay UNUSED, and that is a decision carried forward, not an oversight** — this council's recurring
failure is the row fired because a slot existed.

### The prior-art screen I ran before firing, and what it did and did not find

Yesterday the same screen caught a false premise inside a row I had been directed to adopt whole. **Today it
caught nothing, and I say so rather than let silence read as an unrun screen.** I re-derived every route+find
figure in `0826-R15` independently, programmatically, over all 268 records of the 36 files in
`analytics/fetch-results/` at anchor `58cb26c` on the unshallowed history: `Decisions` on `ARB/23/3` → 0;
`Published` on `ARB/23/3` → 0; `Status of Proceeding` on `ARB/23/3` → 4 rows across three page states;
`Published Decisions` on `ARB/23/3` → 2 rows, both `find_matched: false`; `Date of Constitution of ad hoc
Committee` on `ARB/12/1` → 0; `Status of Proceeding` on `ARB/12/1` → 1 row. **Every one of the analyst's
figures reproduces on my derivation. There was no discrepancy to correct.**

### One thing the screen surfaced that is outside the batch, flagged and not ruled

**This project has fetched `A/81/17` successfully once, and the standing watch does not carry it.**
`analytics/fetch-results/2026-08-04-daily.json` `records[3]` fired `https://undocs.org/A/81/17` and returned
`ok/200`, **4032 bytes**, `sha256 140ccb14fd838292`, `find_matched: true`, with `final_url` redirected to
`https://docs.un.org:443/A/81/17` and the excerpt *"Select a language for A/81/17 A/81/17 العربية 中文 English
Français Русский Español"*.

**I do not rule on what that establishes and I will not pre-empt the seats.** Two readings are open on its
face: that the UN document server has a record for the symbol, or that it serves a language-selection stub for
any well-formed symbol, in which case the row establishes nothing about the report. **`G9` bites either way —
`final_url` differs from `url` in host and scheme-port, so the row is not straightforwardly a statement about
the URL requested.** It is a question for the analyst and the officer today, and it is on the record now
because a watch that has run twenty-two consecutive nulls against one page while a successful fetch of the same
symbol sits unread in its own corpus is a watch worth examining.

### Where this leaves the agenda

The analyst's brief is bounded to reading today's ten rows against his own pre-registered branches, ruling on
the `A/81/17` question above, proposing tomorrow's rows, and spending at most three searches. **The one thing I
have told him he may not do is treat a `find_matched` as a fact about a docket.** Everything else is his.

---

## Part II — The research analyst's return, verbatim and unedited

*Spawned with the Agent tool against `.claude/agents/research-analyst.md` (Claude Opus 5, per the operator's
directive and the definition's own frontmatter). Budgets set by me: at most three web searches, no fetch of his
own, and a written instruction that a shorter return is a better one. **His text below is filed as returned. I
did not write it, and I have not edited it.** Where I disagree with anything in it, my correction is recorded
beside it in Part VI, in my own voice — the `0822-B14` disposition — and never inside his text.*

---

# PART II — RETURN OF THE RESEARCH ANALYST
## Daily council, 2026-08-27. Filed by the research analyst seat.

**Procedural, before anything substantive.**

- **Egress:** none in this session. Carried, not re-probed by me — the pre-registration's own three-witness probe at `2026-08-27T11:22:35Z`/`11:22:37Z` stands and I add nothing to it. Twenty-sixth consecutive day. Every access statement below is a fact about a client at an instant, never about a resource.
- **The corrected no-body-read rule, in the mandated form:** *No seat on this council has read any body SINCE 2026-08-02, when egress closed. Two primary documents were read in full on 2026-07-30 and independently verified on 2026-07-31.* The over-broad form is not reissued here.
- **Git counts, verified rather than inherited.** `git rev-list --count HEAD` = **822**; `git rev-list --count 58cb26c` = **819**; `58cb26c..HEAD` = 3 (this session's three commits). `.git/shallow` absent. The chairman's brief and the pre-registration both say 819; that is correct **at the anchor** and is not a discrepancy. Any figure I give below is labelled with the ref it was taken at.
- **Corpus scope, stated once and load-bearing.** `analytics/fetch-results/` at anchor `58cb26c` = **36 files / 268 records**, and **today's file is not in it**. Working tree / HEAD = **37 files / 278 records** (today's file carries 10 records: the relay's head control, 8 content rows, the G18 tail; `requested: 9` does not count the head control). Where a figure includes today I say so in the sentence.
- **Screens.** Every prior-art figure below was derived by `git show 58cb26c:<path>` over the results blobs, programmatically. Screens that include today read today's file from the **working tree** and I say so at the point of use. This is the correction of my own 0826 method defect (five instances of working-tree screens labelled "at anchor").
- **Prior-art corpus = seven places**, per close-out decision 7: `analytics/fetch-results/`, `analytics/daily-research/`, `analytics/optimization-log.md`, `analytics/insights.jsonl`, `STATE_OF_THE_ANSWER.md`, `working/one-pagers/`, `lit-review/`. Used in full for Part IV and for every "no carrier in this repository" claim.

---

# PART I — THE REDUCTION, ROW BY ROW

Bracket: G18 head/tail verified identical, 559 bytes / `ff67a9d764d6a236` both ends, `control_ok: true`, `void: false`. Every content row sits inside a verified bracket. G9 passes on all eight content rows — `final_url` is character-identical to `url` in each. Every anchored match sits at excerpt offset **exactly 100**, which is G17's left edge (`match_index − 100`) behaving as specified; I checked this rather than assumed it.

## A. The two-branch pre-registration did not survive contact, and the defect is mine

Rows A and B were my rows and my licence. I report the outcome first and the defect second.

| row | find | matched | where it landed |
|---|---|---|---|
| A `records[1]` | `Decisions` | **true** | **site navigation chrome** |
| B `records[2]` | `Published` | **false** | — (empty excerpt) |
| D `records[4]` | `Published Decisions` | **false** | — (empty excerpt) |
| C `records[3]` | `Status of Proceeding` | true (control) | case summary block |

Row A's carrying span, offset 90 of `records[1].excerpt`: `Tables of Decisions`, sitting inside `…View All Tables of Decisions Tables of decisions in ICSID cases on various procedural and substantive topics. View all Resources Publications Overview ICSID Annual Report ICSID Caseload Statistics ICSID Review…`. That is the ICSID site mega-menu. Under the 0819 grading rule, **pre-applied and binding as the licence itself required**, this match establishes **page identity and not the table region**.

**Neither pre-registered branch fits, and saying so is the point.** My licence pre-registered: *"IF `Decisions` MATCHES WHILE THE COMPOUND LITERAL NULLS (row D), THE BREAK LIES INSIDE THE SPAN and G6 … IS THE EXPLANATION; genuine absence of the words is refuted."* Both trigger conditions fired. **The stated conclusion is false**, and it is falsified by its own companion row: the in-span-break hypothesis predicts that `Published` alone would match, because a zero-width character or an entity *between* the tokens leaves each token intact. **Row B is `false`.** The hypothesis the branch names is refuted by the batch the branch was written for.

**The design defect, stated plainly.** My licence contained two clauses that cannot both operate. The branch treats a match by `Decisions` as diagnostic of an in-span break. The grading caveat, three sentences later, concedes the literal "may land in navigation chrome, in which case the row establishes PAGE IDENTITY AND NOT THE TABLE REGION." **If the caveat fires, it voids the branch's premise** — and it did. Worse: because the reducer anchors on the *first* occurrence, a chrome hit **masks** any table-region occurrence, so row A could never have discriminated in the matching direction at all. It was a one-sided instrument sold as two-sided. I wrote it; the chairman adopted it verbatim; neither of us caught it.

**What I could not have screened, and what is now screenable.** Across **78 prior ICSID rows at anchor `58cb26c`**, the strings `Tables of Decisions`, `ICSID Caseload Statistics` and `Resources Publications` occur **zero** times in any excerpt. Today's row A is this project's **first capture of ICSID site chrome**. So the failure was not "skipped an available screen"; it was "pre-registered a discriminator on a literal whose chrome status was unknown, without recording that as the branch's exposure." The countermeasure is available from today forward and I propose it in Part III.

## B. Row B carries the whole result, and it turns a null into a positive contrast

Row B is the informative row precisely because it **nulled**. Under the rescoped G17, `find_matched` is **reduced-body-scoped** — wider than the window, narrower than the page. So row B says: the token `Published` does not occur anywhere in the reduced body of page state `ac081002159b9110`.

Now the slot-controlled comparison. Screening every ICSID case-detail row in the corpus **at anchor `58cb26c` plus today's file read from the working tree**, restricted to windows anchored on the *identical* literal `Status of Proceeding` — so every window sits at the same offset relative to the same template field:

| dockets whose window reaches the slot after `(a) Original Proceeding` | 9 |
|---|---|
| carrying ` Published Decisions` | **8** — ARB/16/34, ARB/17/19, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, UNCT/14/2, UNCT/20/6 |
| carrying ` Date Development` | **1** — **ARB/23/3** |
| windows that do not reach the slot (silent, not contrary) | ARB/10/7, ARB/12/1 |

Adding differently-anchored windows (`Latest Development`), ARB/22/2 also carries the contiguous string, bringing distinct dockets carrying `(a) Original Proceeding Published Decisions` to **nine**.

Two of the eight are seed awards — **ARB/16/34 (Bridgestone)** and **UNCT/14/2 (Eli Lilly)**.

**This is a material upgrade on yesterday's gap text**, which recorded "three OTHER dockets across four page states" and correctly disclaimed the strong form under G13. Today: eight comparators, each an individual **single contiguous window**, all anchored on the **same literal**, so the assembly is across *dockets* — which is the comparison's object — and not across page states of one docket. That is as close to G13's strong form as a cross-docket contrast can be built, and I say expressly that it is not the strong form.

**What it licenses.** On the page state this instrument can currently see, the ARB/23/3 case-detail page does **not render** a Published Decisions block, and the token `Published` occurs nowhere in its reduced body, while eight other dockets render that block in the identical template slot. **What it does not license:** anything about whether ICSID holds published decisions in that proceeding. A `find_matched` is not a fact about a docket, and I am not treating it as one. The honest form remains **"not found in this surface."**

**Residue, narrowed and not eliminated.** G5 (attribute-only) is now weak: on eight dockets the same template puts this string in *text*, not in an attribute. G6 survives only in its narrowest form — a zero-width character or entity **inside the single word `Published`** — which is a materially rarer phenomenon than the markup-boundary insertion 0824-O13 documents on this host, and which the corrected G7's span-length logic makes the least likely remaining case. I report it as narrowed, not discharged.

**One asymmetry I flag as observed but unexplained.** ARB/23/3's window carries `No References Available.` at offset 255 — an **explicit negative** in the References slot. It is the **only** one of the eleven dockets with a `Status of Proceeding` window whose excerpt carries that string. So this page renders an explicit negative for References and renders *nothing at all* for Published Decisions. Whether that is a template rule, a per-docket rule, or a window artefact is unobserved, and I do not build on it.

## C. Rows E and F: the annulment section, and a 167-character verbatim stitch

Both rows sit on one page state (`588f94b5c8547e9d`, 176062 bytes), one run. I checked the stitch rather than asserting it: **the tail of row F's excerpt and the head of row E's excerpt share a 167-character verbatim overlap** —

> `cant(s)/Requesting Party(ies): (i) Respondent(s) Date Registered: November 18, 2019 (Respondent(s)) Date of Constitution of ad hoc Committee: March 25, 2020 Compositio`

— yielding a 633-character contiguous span. G13 still says one window is the strong form and this is two; but a 167-character verbatim overlap on **one page state in one run** is a materially stronger join than any assembly this council has previously made, and I state the number so a reader can re-derive it.

The span, in the source's own words (`records[6].excerpt` offset 100 and 203; `records[5].excerpt` offset 100 and 247):

> `Status of Proceeding: Concluded Outcome of Proceeding: July 12, 2019 - The Tribunal renders its award. (b) Annulment Proceeding Applicant(s)/Requesting Party(ies): (i) Respondent(s) Date Registered: November 18, 2019 (Respondent(s)) Date of Constitution of ad hoc Committee: March 25, 2020 Composition of ad hoc Committee (Appointed by the Chairman of the Administrative Council) President: Joongi KIM (Korean) Members: Bernardo SEPÚLVEDA AMOR (Mexican) Carita WALLGREN-LINDHOLM (Finnish) Party Representatives Claimant(s): Debevoise`

**New to all seven places.** `WALLGREN`, `SEPÚLVEDA`, `SEPULVEDA` and `Joongi` return **zero** hits across `STATE_OF_THE_ANSWER.md`, `analytics/optimization-log.md`, `analytics/insights.jsonl`, `working/one-pagers/`, `lit-review/`, `analytics/daily-research/` and `analytics/fetch-results/`. The ad hoc Committee's composition is a genuine addition to the record, from a body, primary, procedural.

**G16 binds and I hold to it:** every element above is **a docket statement and a date**. Nothing here is a disposition and nothing is a holding. Nobody on this council has read the award or any annulment document.

**Row E did its job and did not resolve its gap.** No `Status of Proceeding` and no `Outcome of Proceeding` line for the **(b)** block appears in 633 characters. The mis-anchoring risk I pre-registered did **not** materialise — the literal anchored in the annulment block as designed. What the row bought is **localisation**: by the (a) block's own layout in the same span (Party Representatives → Language(s) → Status → Outcome), the (b) status/outcome lines should fall **after** the (b) Party Representatives list, i.e. just beyond this window's right edge. That converts an unlocalised gap into a targeted one, and Part III fires at it.

**One caution for future seats, cheap and worth writing down:** this record now contains a `lit-review/kim-memo.md` and an ad hoc Committee President named **Joongi KIM**. They are different people. Do not let a future screen on "Kim" conflate them.

## D. The digest series, and a finding that runs the other way from G20

The chairman asked me to think carefully about what today's byte count does and does not do to G20. Here is what I found, and it is not what the question anticipated.

ARB/23/3, `Status of Proceeding`-anchored window, all five observations (four at anchor, today's from the working tree):

| date | bytes | sha256 (16) | window byte-identical to first? |
|---|---|---|---|
| 08-24 daily | 94393 | `46ccad7f4bcaee9a` | — |
| 08-24 zz | 94393 | `46ccad7f4bcaee9a` | yes |
| 08-25 | 94393 | `9237e1ae360c7482` | yes |
| 08-26 | 94393 | `e0c3d269fd723a4a` | yes |
| **08-27** | **94391** | `ac081002159b9110` | **yes** |

And ARB/12/1, same anchor literal: 08-26 `176048` / `bf5a70b29f8d36f9` → 08-27 `176062` / `588f94b5c8547e9d`, **window byte-identical**.

**On G20 specifically:** today is the first ARB/23/3 state at a different byte count, so for the first time this docket's states are distinguishable by byte length alone. That **neither falsifies nor vindicates G20**. G20 forbids the inference *same bytes ⟹ same page*; today supplies an instance of *different bytes*, which G20 does not govern and about which it says nothing. It does remove the coincidence — four states at one length — that made G20's warning salient on this docket. G20's necessity for the prior four observations is undiminished. I report no falsification and no vindication because the logic supports neither.

**What today does establish is the converse trap, and it is new.** Across **five ARB/23/3 observations spanning four page states and two byte lengths**, and **two ARB/12/1 observations spanning two page states and two byte lengths**, the `Status of Proceeding`-anchored 400-character window is **byte-identical throughout**. On this host, **page-state inequality is not evidence of change in the observed region.** G20 closes one direction; this closes the other. I propose it as a gate in Part III.

**A corollary that touches a standing ruling, and I flag it rather than smooth it.** The 2026-08-25 record held that the controlled ARB/23/3 `Published Decisions` null "NO LONGER DESCRIBES THE LIVE PAGE" because the state had moved. As **scoping** that is right and stays right: a finding scoped to a page state does not transfer. As an **empirical premise** — that the move might have changed this region — it is now measured, and across all four moves the region never changed. Today's row D re-established the null on the current state, so nothing turns on it for that literal. It turns on a good deal for the method: a digest series is not a change-detection series for a scoped region unless the window itself is compared, and until today this council was not comparing windows.

## E. The UNCITRAL pair

`records[7]` `A/81/17` → null; `records[8]` `A/80/17` → **matched**, carrying span at offset 100: `A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session`. The control does its whole job: it proves the null is not an access failure and not a reduction failure.

Population, stated with its definition attached: **at anchor `58cb26c`, 23 `A/81/17` find-scoped rows — 21 on the commission page at 832,338 bytes, all null; one `robots_disallowed` on `digitallibrary.un.org` (2026-08-08), which is a no-contact and never an origin answer; one on `undocs.org` (Part II).** Including today's file: **24 rows, 22 commission-page nulls.** The chairman's 24/22 figures include today and reproduce exactly. The devaluation recorded on 2026-08-20 stands and I do not re-argue it: those nulls sit on **two** distinct digests, so as evidence twenty-two observations are worth two. G3 binds: **none of this is a finding that the 59th session's report does not exist or is late.** The lag baseline remains unavailable (N10).

---

# PART II — RULING ON `2026-08-04-daily.json` `records[3]`

**The question was put to me as open. It is not open, and it was not open when it was put. This council settled it on 2026-08-04, and the ruling has been sitting unfound in the corpus for twenty-three days.**

I ran the prior-art screen on the route before reasoning about the row, and it returned the answer. At anchor `58cb26c` the `undocs.org` route carries **eight** rows, from three request files. Two of those files are pre-registered controls fired the *same day* as the row in question, and their notes are the ruling.

`analytics/fetch-requests/2026-08-04-undocs-identify.json`, verbatim:

> `the 11:37Z control block established that docs.un.org answers 200 at an identical 4,157 bytes for a published symbol, the awaited symbol, and an arbitrary one — so its status carries no existence information.`

And `2026-08-04-undocs-control.json`, on why the row was fired at all:

> `each with a find string the resolver CANNOT echo back from the request URL, which is the defect that made the first row uninformative`

"The first row" is `records[3]`. **This council has already ruled it uninformative, by design, in a document written the same day.**

**The ruling, restated on the evidence rather than on the authority.**

1. **G9 bites, and bites hard.** `final_url` is `https://docs.un.org:443/A/81/17` against a requested `https://undocs.org/A/81/17` — different host *and* explicit port. Under G9 the row is **uninterpretable as to the designator**: the window describes whatever `docs.un.org` chose to serve, which is a resolver's output, not a document record.

2. **The `find_matched: true` is a URL echo, not a document.** The excerpt is `Select a language for A/81/17 A/81/17 العربية 中文 English Français Русский Español` — the symbol appears twice, in a language-selection shell that necessarily renders the requested symbol. The find string was the symbol itself, so the resolver could not fail to match it. This is a self-satisfying probe.

3. **The negative control was already run.** `https://undocs.org/en/A/81/99` — fired on 2026-08-04 expressly as "a negative control on an implausible symbol in the same series" — returned `ok / 200 / 4157 bytes`, excerpt `Document Viewer home United Nations العربية 中文 English Français Русский Español`. The identical shell shape as `A/80/17` and `A/81/17`. Note the byte lengths: all three `/en/` rows are **4157 bytes with three different sha256** (`6120ed9c`, `5b82f6d2`, `91c4c786`) — a same-length, different-content family, which is the G20 pattern arriving from the opposite side and is itself worth noting.

4. **And the decisive same-route control was run on 2026-08-21, seventeen days later, and nobody connected it.** `https://undocs.org/A/80/17` — the *non-`/en/`* route, the same route as `records[3]`, 4032 bytes, the same length as `records[3]` — returned **`find_matched: false` for `United Nations Commission on International Trade Law` and false for `fifty-eighth`.** A/80/17 is a symbol this project has independent body-carried evidence names a real published report: today's `records[8]` window reads `A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session`. **The same route, given a symbol we know names a real report, serves a stub containing neither the report's title nor its session ordinal.** The stub therefore carries no information about existence and none about content, on either route, for any symbol.

**What the row licenses: nothing.** It does not establish that a document record for A/81/17 exists. It does not establish that one does not. It establishes that `docs.un.org` served a 4032-byte language-selection shell to this client at that instant, and that the shell echoed the symbol it was asked for.

**What would discriminate — and why it is out of reach.** The discriminator is a route that returns a *catalogue record or a body* rather than a viewer shell: a symbol present in a result set with metadata, absent when absent. The UN Digital Library is exactly that route, and it is `robots_disallowed` (2026-08-08). **That is a policy boundary this project does not cross, not an access failure**, and I will not propose a route around it. The discriminator is identified and unavailable.

**Is it worth a row tomorrow? No, and I recommend no row.** Every experiment that could be run on this route has been run — positive control, negative control, awaited symbol, both routes, twice. A fourth firing would produce a fourth shell. **I recommend the `undocs.org`/`docs.un.org` route be marked spent in the record**, so that the next seat asked this question finds the ruling instead of re-deriving it, which is the failure that produced today's question.

---

# PART III — ROWS FOR TOMORROW (`0827-R…`)

Cap 12 including the relay's head control and the G18 tail. I propose **six**, leaving four slots deliberately unused per close-out decision 2. All prior-art figures derived programmatically **at anchor `58cb26c`** over 36 files / 268 records; where a count includes today I say so.

### Rank 1 — `0827-R1` **[the day's best row]**

| | |
|---|---|
| **URL** | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1` |
| **find** | `Discontinued` |
| **Prior art** | route+find **0**. URL-only **2** at anchor (both 2026-08-26); **4** including today. Find-literal `Discontinued` anywhere in the corpus: **0**. |
| **Rationale** | Single token, minimum span — the corrected G7 applied. Targets the (b) block's `Status of Proceeding` **value** rather than its label, because the label mis-anchors: `Status of Proceeding` and `Outcome of Proceeding` both occur first in the (a) block (proved by today's `records[6]`), so neither can reach (b). A value literal can. `Discontinued` is chosen because it cannot occur in the (a) block, whose value is `Concluded`. |
| **Licence** | G10 and G16 bind absolutely: whatever returns is a **docket statement and a date**, never a disposition, never a holding. |
| **Pre-registered branch** | **Graded by where it lands, and I do not repeat row A's mistake.** (i) Match landing in the **(b) annulment block** → the docket carries a status value for the annulment proceeding, and the window reports it. (ii) Match landing in **site chrome** → row is **uninformative** and is reported as uninformative; **no branch conclusion follows**, because a chrome hit masks any body occurrence. (iii) Null → the token is absent from the reduced body (G17 rescoped), with G5/G6 residues stated. **Exposure declared in advance:** ICSID chrome has been observed exactly once (today's `records[1]`) and 400 characters of it is not a chrome census, so branch (ii) is live and I cannot bound its probability. |

### Rank 2 — `0827-R2`

| | |
|---|---|
| **URL** | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1` |
| **find** | `WALLGREN-LINDHOLM` |
| **Prior art** | route+find **0**; find-literal anywhere **0**. |
| **Rationale** | The window slide. Offset of this literal in today's `records[5].excerpt` is **328**, so the left edge moves right by **228 characters** in that coordinate frame, yielding ~228 characters of unseen material past `Party Representatives Claimant(s): Debevoise`. That is where the (b) status/outcome lines should sit on the (a) block's own layout. A **rigid designator** — a personal name the field cannot paraphrase away — which is why I chose it over `Debevoise` (offset 391, but plausibly also in the (a) block) and over `Composition of ad hoc Committee` (no slide gain). |
| **Licence** | G16. Docket statements and dates only. |
| **Pre-registered branch / risk** | **Mis-anchoring risk stated before the result, per the 2026-08-05 rule:** if this name occurs earlier on the page (e.g. in the original tribunal's composition), the row mis-anchors and is **a probe, not a null**. I have no basis to exclude that and do not pretend to. Null → the name is absent from the reduced body, which would contradict today's `records[5]` and would be an **instrument alarm**, not a docket finding. |

### Rank 3 — `0827-R3` **MANDATORY CONTROL**

| | |
|---|---|
| **URL** | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1` |
| **find** | `Status of Proceeding` |
| **Prior art** | route+find **2** (2026-08-26, 2026-08-27), both ok/200/matched. |
| **Rationale** | Same-URL same-run control and digest pair for R1 and R2 (0821-E8). Third observation in this docket's series. Its window is now known byte-identical across two page states, so it is also the **window-constancy check** for the new gate proposed below. |
| **Branch** | None pre-registered. A digest difference here is expected and licence-free. |

### Rank 4 — `0827-R4`

| | |
|---|---|
| **URL** | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3` |
| **find** | `Published` |
| **Prior art** | route+find **0** at anchor; **1** including today (2026-08-27, `find_matched: false`, state `ac081002159b9110`). |
| **Rationale** | **Replication of today's single most load-bearing null on a fresh page state.** Today's whole ARB/23/3 conclusion rests on one observation of one token on one state. Given the window-constancy finding, the cheap and correct move is to repeat it rather than to build on it. |
| **Licence** | Never a docket fact. G17 rescoped: a null is reduced-body-scoped, never page-scoped. |
| **Branch** | Null again on a *different* state → the absence is state-independent across two states and the G6 residue narrows further. Match → today's reading is withdrawn same-day and the change is the finding. |

### Rank 5 — `0827-R5` **MANDATORY CONTROL**

| | |
|---|---|
| **URL** | `https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3` |
| **find** | `Status of Proceeding` |
| **Prior art** | route+find **4** at anchor, **5** including today; all ok/200/matched; window byte-identical across all five. |
| **Rationale** | Control and digest pair for R4, and the sixth point in the only window-constancy series this project holds. |

### Rank 6 — `0827-R6` **UNCONTROLLED — flagged in its own cell**

| | |
|---|---|
| **URL** | `https://www.ogel.org/legal-and-regulatory-detail.asp?key=32761` |
| **find** | `discontinuance` |
| **Prior art** | route+find **0**. **URL-only 0. Host-only 0** — `ogel.org` has never been fired by this project, on any path, in 268 records at anchor. |
| **UNCONTROLLED** | **Yes, and this is the cell.** No baseline exists for this host: no successful fetch, no refusal, no robots outcome. A null would be uninterpretable — indistinguishable from a paywall shell, a listing that renders titles in attributes (G5), or genuine absence. **If the chairman fires it, it must be fired with a same-host control**, and I cannot propose one because I know of no `ogel.org` URL this project has named. |
| **Rationale** | Backsources the title surfaced at Part V search 2. Ranked last on purpose, and **subordinate to rule 5 (docket page before document hunt)**: R1 and R2 go at ICSID's own docket first. |
| **Licence** | G10 with special force: a listing title establishes that a title exists on a listing page and establishes **nothing** about what any document says or about which proceeding it concerns. |

**What I would cut if the chairman wants slots back.** Cut **R6 first** — it is uncontrolled, off the primary route, and rule 5 says the docket comes first anyway. Cut **R4 and R5 together, never R4 alone** — a replication without its digest pair would be compared against an unknown state, which 0825-R10 forbids, and would look controlled while not being controlled. That is the same reasoning that cut rows G and H yesterday and it applies to my own row here. **R1, R2 and R3 are the irreducible core**; R2 and R3 must travel together for the same reason.

**A gate I propose for adoption, and the chairman may number it.**

> **PAGE-STATE INEQUALITY IS NOT WINDOW CHANGE.** A change in `sha256` or `bytes` is not evidence that any scoped region of the page changed. Demonstrated 2026-08-27 on two dockets: ARB/23/3, five observations across four page states and two byte lengths, and ARB/12/1, two observations across two page states and two byte lengths — the `Status of Proceeding`-anchored 400-character window byte-identical throughout. Corollary: **a digest series is not a change-detection series for a scoped region unless the window itself is compared.** This is the converse of G20 and does not weaken it.

**A method rule I propose, arising from my own defect.**

> **CHROME-SCREEN BEFORE BRANCH.** Before pre-registering a presence/absence branch on a literal, screen the literal against the host's known navigation chrome held in this project's own excerpts. If no chrome capture exists for that host, say so, and **do not pre-register a branch whose premise a chrome landing would void** — because the reducer anchors on the first occurrence, so a chrome hit masks the body and the row is one-sided. Today's `records[1]` is this project's first ICSID chrome capture; before today the screen was unavailable, which is why this rule is prospective and not a charge of skipping it.

---

# PART IV — GAP-UNRESOLVED MARKERS

Each slug checked **individually** against `STATE_OF_THE_ANSWER.md`, `working/one-pagers/` and `lit-review/` before being declared open, per rule 7. `working/one-pagers/` and `lit-review/` return zero carriers for every slug below except where noted.

`GAP-UNRESOLVED: icsid-arb-23-3-no-published-decisions-on-docket — STANDS, MATERIALLY STRONGER, AND ITS LEADING RIVAL EXPLANATION IS NOW REFUTED. Three advances today, all from one run on one page state (ac081002159b9110): (1) the single token `Published` NULLS in the reduced body, which REFUTES the in-span-break (G6) explanation because that hypothesis predicts the single token would match; (2) the compound and both component tokens were observed on ONE page state in ONE run for the first time, as row D's licence anticipated; (3) the comparison is now SLOT-CONTROLLED — nine dockets' windows anchored on the identical literal `Status of Proceeding` reach the slot after `(a) Original Proceeding`; EIGHT carry ` Published Decisions` (ARB/16/34, ARB/17/19, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, UNCT/14/2, UNCT/20/6) and ARB/23/3 alone carries ` Date Development`. ARB/10/7 and ARB/12/1 are silent, not contrary. Each comparator is a single contiguous window; the assembly is across DOCKETS, not across page states, and is expressly NOT G13's strong form. Residue narrowed and not discharged: G5 is weak (eight dockets render the string as text, not as an attribute); G6 survives only as a zero-width character INSIDE the word `Published`. NEVER "does not exist"; the honest form is "not found in this surface". Replication row proposed as 0827-R4 with its control at R5.`

`GAP-UNRESOLVED: icsid-subject-of-dispute-vocabulary-enumeration — STANDS, UNTOUCHED. No row fired at it today, no search went near it, and today's rows do not bear on the vocabulary in either direction. Yesterday's eleven observations on eleven proceedings stand as eleven observations and not an enumeration. The confirmatory-never-discovery limit is unchanged.`

`GAP-UNRESOLVED: tethyan-copper-arb-12-1-annulment-outcome — STANDS, AND MOVES FURTHER TODAY THAN ANY OTHER GAP. Row E anchored in the annulment block as designed; the pre-registered mis-anchoring risk did not materialise. NEW FROM A BODY, and absent from all seven places (WALLGREN / SEPÚLVEDA / SEPULVEDA / Joongi all return zero): the ad hoc Committee was appointed by the Chairman of the Administrative Council with President Joongi KIM (Korean) and Members Bernardo SEPÚLVEDA AMOR (Mexican) and Carita WALLGREN-LINDHOLM (Finnish), constituted 25 March 2020, annulment registered 18 November 2019 on the Respondent's application. THE OUTCOME REMAINS UNOBSERVED: no `Status of Proceeding` and no `Outcome of Proceeding` line for the (b) block appears in the 633-character stitched span, and by the (a) block's own layout those lines fall just beyond the window's right edge. G16 binds: everything here is a docket statement and a date, never a disposition. SEPARATELY, a LEAD and not a finding: a 2026-08-27 web search surfaced a document title carried at the same key by two listing hosts (ogel.org and transnational-dispute-management.com) reading "Tethyan Copper Company Pty Ltd v Islamic Republic of Pakistan - ICSID Case No. ARB/12/1 - Procedural Order Taking Note of the discontinuance of the Proceeding - 6 January 2023". NEITHER PAGE WAS RETRIEVED and no body was read; under G10 a title establishes that a title exists on a listing page. The engine's surrounding prose is REFUSED under the no-search-synthesis rule and nothing from it is carried. The remedy is 0827-R1 and 0827-R2 at ICSID's own docket, with R6 as an uncontrolled last resort. lit-review/ferguson-memo.md's item-5 finality field at ¶¶1283, 1288, 1327-1330 remains OPEN.`

`GAP-UNRESOLVED: icsid-2022-arbitration-rules-rule-41-48-provision-text — DOES NOT MOVE IN EITHER DIRECTION. N15-class operator-side retrieval. Zero search budget spent, correctly; no row fired; nothing retrieved and nothing learned about the text.`

`GAP-UNRESOLVED: icsid-mllm-finding-column-value-codebook — UNTOUCHED. Zero budget. `No`, `Partial` and `_` remain untranslated and `_` undefined. The reclassification proposed at 0824-R15 remains proposed and unmade.`

`GAP-UNRESOLVED: hela-schwarz-art9-jurisdiction-or-admissibility — STANDS, UNTOUCHED, AND ESCALATED (N4). Zero search budget spent, per the escalated-gap rule. No row went near it. I ran no disposition screen today and do not report one as run; the 0825-B1 screen and the 0826-R11 re-run stand as the record and I add nothing to them.`

`GAP-UNRESOLVED: philip-morris-uruguay-arb-10-7-pleaded-covered-investment — STANDS, UNTOUCHED. My own retraction of the "trademark-and-plain-packaging" description as unsourced is carried unsoftened. Noted from today's slot screen only: ARB/10/7's window does not reach the `(a) Original Proceeding` slot, so it is silent on the Published Decisions contrast and contributes nothing here.`

**No slug is closed today, and none is proposed for closure.**

---

# PART V — SEARCH SPEND

Authorised 3. **Spent 2, returned 1 unspent.**

**Search 1 — SPENT. Monitored-author standing check, run FIRST as the protocol requires.**
Query: `Anthea Roberts EJIL:Talk August 2026`. Result set: her author page, five pagination shells, an old joint-symposium page, and the post title `UNCITRAL and ISDS Reform: What to Expect When You're Expecting`. **No post dated August 2026 surfaced.** That is a fact about this query's result set at this instant and is **not** a statement that her author page carries no new post — the direct-crawl route to ejiltalk.org remains WAF-403 to identified non-browser clients and this project does not masquerade as a browser. The surfaced post title is **already in this record** (`STATE_OF_THE_ANSWER.md` and four daily records carry it), so it is not a new lead. **Per rule 4, nothing from the engine's prose is carried** — the biographical paragraph it generated is not a source and is not banked.
Result: **nothing new; standing watch held.**
Sources: [ejiltalk.org/author/aroberts](https://www.ejiltalk.org/author/aroberts/) · [UNCITRAL and ISDS Reform: What to Expect When You're Expecting](https://www.ejiltalk.org/uncitral-and-isds-reform-what-to-expect-when-youre-expecting/)

**Search 2 — SPENT. Topical, on the one open non-escalated gap a search could move.**
Query: `Tethyan Copper Company v Pakistan ICSID ARB/12/1 annulment ad hoc committee decision` — **party name + docket + document type**, which is the case-law analogue of the 2026-07-26 method lesson (author-name + title beat keyword phrasing). **The formulation worked**: it returned document-level titles with dates, not topic pages.
**What I took: titles and URLs only.** The load-bearing one is the OGEL/TDM title at Part IV. Three further jusmundi titles surfaced (`Procedural Oder No. 1 (Annulment Proceeding), 11 May 2020`; `Decision Partially Terminating the Stay of Enforcement of the Award, 30 Oct 2020`; `Decision on Stay of Enforcement of the Award, 17 Sept 2020`) and are recorded as leads only. **A listing-integrity defect I flag and do not resolve:** the last of those three carries a title dated `17 Sept 2020` on a URL slug reading `…-annulment-proceeding-monday-18th-november-2019`. Title and slug disagree. That is a reason to treat jusmundi titles as weak leads, not a finding about any document.
**What I refused.** The engine generated a narrative with specific dates and a characterisation of how the annulment ended. **It is refused in full under rule 4 and none of it appears anywhere in this return, including in hedged form.** No body was read; no title stands behind that prose.
Sources: [OGEL, key=32761](https://www.ogel.org/legal-and-regulatory-detail.asp?key=32761) · [TDM, key=32761](https://www.transnational-dispute-management.com/legal-and-regulatory-detail.asp?key=32761) · [italaw case 1631](https://www.italaw.com/cases/1631) · [jusmundi, Procedural Order No. 1 (Annulment Proceeding)](https://jusmundi.com/en/document/other/en-tethyan-copper-company-pty-limited-v-islamic-republic-of-pakistan-procedural-oder-no-1-annulment-proceeding-monday-11th-may-2020)

**Search 3 — RETURNED UNSPENT. Reason:** the only candidate uses were (a) a reformulation of the monitored-author check and (b) one of the remaining gaps. (a) has **no title to key on**, so it would be a blind repeat of search 1 rather than the name+title reformulation the 2026-07-26 lesson prescribes, and repeating a query is not a second observation. (b) is barred: `hela-schwarz-art9-jurisdiction-or-admissibility` is escalated (N4) and `icsid-2022-arbitration-rules-rule-41-48-provision-text` is N15-class, both zero-budget by standing rule; `icsid-mllm-finding-column-value-codebook` and `icsid-subject-of-dispute-vocabulary-enumeration` are enumeration problems no search can close. Search 2 has already produced more targeted rows than a third could, and under rule 4 a third search's maximum yield is titles I do not need.

**Method note for the self-training record.** Two formulations, two outcomes, and the difference is instructive. *Author name + month* returned only shells — it is a **listing query** and listings are exactly what the WAF-blocked route already gives us. *Party name + docket number + document type* returned **document-level titles with dates from four distinct hosts** — it is a **document query**. The 2026-07-26 lesson generalises: **name the artefact you want, not the place it might be listed.** I recommend the monitored-author protocol be amended to run `"<author name>" "<known recent post title>"`-style document queries once any title is in hand, and to record the month-scoped listing query as the weaker of the two.

---

# PART VI — CANDIDATE CLAIMS

I propose; the operator's ledger decides. Any status below is ignored by design.

```json
{"candidate_claims": [
  {"claim_text": "ICSID's case-detail page for ICSID Case No. ARB/12/1, as retrieved on 2026-08-27, states that the ad hoc Committee in the annulment proceeding was constituted on 25 March 2020 and is composed of President Joongi KIM (Korean) and Members Bernardo SEPULVEDA AMOR (Mexican) and Carita WALLGREN-LINDHOLM (Finnish), appointed by the Chairman of the Administrative Council.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Date of Constitution of ad hoc Committee: March 25, 2020 Composition of ad hoc Committee (Appointed by the Chairman of the Administrative Council) President: Joongi KIM (Korean) Members: Bernardo SEPÚLVEDA AMOR (Mexican) Carita WALLGREN-LINDHOLM (Finnish)",
   "supporting_locator": "2026-08-27 relay reduction, records[5].excerpt, offsets 100 and 247; page state sha256 588f94b5c8547e9d, 176062 bytes"},

  {"claim_text": "ICSID's case-detail page for ICSID Case No. ARB/12/1, as retrieved on 2026-08-27, records the original proceeding as Concluded with an outcome line reading 'July 12, 2019 - The Tribunal renders its award', and records a separate annulment proceeding registered 18 November 2019 on the Respondent's application.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Status of Proceeding: Concluded Outcome of Proceeding: July 12, 2019 - The Tribunal renders its award. (b) Annulment Proceeding Applicant(s)/Requesting Party(ies): (i) Respondent(s) Date Registered: November 18, 2019",
   "supporting_locator": "2026-08-27 relay reduction, records[6].excerpt, offset 100; same page state sha256 588f94b5c8547e9d"},

  {"claim_text": "ICSID's case-detail page for ICSID Case No. ARB/12/1, as retrieved on 2026-08-27, states no outcome or status for the annulment proceeding within the 633 contiguous characters observed, which run from the (a) block's status line through the (b) block's Party Representatives heading.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/12/1",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "procedural",
   "assertion_status": "candidate",
   "supporting_quote": "Carita WALLGREN-LINDHOLM (Finnish) Party Representatives Claimant(s): Debevoise",
   "supporting_locator": "2026-08-27 relay reduction, records[5] and records[6] excerpts joined on a 167-character verbatim overlap, same page state, same run; the observed span terminates at the right edge shown"},

  {"claim_text": "On the ICSID case-detail template, eight of the nine dockets in this project's corpus whose Status-of-Proceeding-anchored window reaches the slot after '(a) Original Proceeding' carry the string 'Published Decisions' in that slot, while ARB/23/3 alone carries 'Date Development'.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "No References Available. (a) Original Proceeding Date Development February 13, 2023 The Secretary-General registers a request for the institution",
   "supporting_locator": "2026-08-27 relay reduction, records[3].excerpt, offsets 255 and 280; comparator windows derived programmatically at anchor 58cb26c plus today's file, dockets ARB/16/34, ARB/17/19, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, UNCT/14/2, UNCT/20/6"},

  {"claim_text": "The token 'Published' does not occur in the reduced body of the ICSID case-detail page for ARB/23/3 at page state sha256 ac081002159b9110 as retrieved on 2026-08-27; this is an instrument observation scoped to that reduction and is not a statement about the docket.",
   "source_url": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/23/3",
   "access_status": "partial",
   "source_authority": "primary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "2026-08-27 relay reduction, records[2], find_matched false with empty excerpt; same-run same-URL control at records[3] find_matched true; no carrying span exists for a null and none is offered"},

  {"claim_text": "The UNCITRAL commission page at uncitral.un.org/en/commission carries the text 'A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session' as at 2026-08-27.",
   "source_url": "https://uncitral.un.org/en/commission",
   "access_status": "partial",
   "source_authority": "official_secondary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": "A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session",
   "supporting_locator": "2026-08-27 relay reduction, records[8].excerpt, offset 100; page state sha256 f9712994f5aec4c9, 832338 bytes"},

  {"claim_text": "The resolver serving undocs.org/docs.un.org returns a language-selection or Document Viewer shell that carries no document title and no session ordinal, for a symbol independently evidenced as naming a published report (A/80/17), and therefore its 200 response carries no information about whether any document record exists.",
   "source_url": "https://undocs.org/A/80/17",
   "access_status": "partial",
   "source_authority": "official_secondary",
   "claim_type": "factual",
   "assertion_status": "candidate",
   "supporting_quote": null,
   "supporting_locator": "2026-08-21 relay reduction records[4] and records[5]: final_url https://docs.un.org:443/A/80/17, ok/200, 4032 bytes, find_matched false for both 'United Nations Commission on International Trade Law' and 'fifty-eighth'; corroborated by the 2026-08-04 negative control on /en/A/81/99 at 4157 bytes"},

  {"claim_text": "A document titled 'Tethyan Copper Company Pty Ltd v Islamic Republic of Pakistan - ICSID Case No. ARB/12/1 - Procedural Order Taking Note of the discontinuance of the Proceeding - 6 January 2023' is listed at the same document key on ogel.org and transnational-dispute-management.com; neither listing page was retrieved and no body was read.",
   "source_url": "https://www.ogel.org/legal-and-regulatory-detail.asp?key=32761",
   "access_status": "unavailable",
   "source_authority": "media",
   "claim_type": "lead",
   "assertion_status": "lead",
   "supporting_quote": null,
   "supporting_locator": "search-result listing title returned 2026-08-27; the listing page itself was not opened, so no substring of the source is available and none is offered. Under G10 this establishes that a title exists on a listing page and nothing about which proceeding was discontinued, on whose application, or with what effect."}
]}
```

---

## One paragraph each on the two items the chairman is handling, as invited — not run by me

**The unstruck Article 9(a) lead-in.** My only view is procedural and it is about how the repair keeps failing rather than about the wording. Five instances were found by enumeration, the sixth by a screen (0825-B1), and the screen was the countermeasure written one day earlier. The pattern says the enumerated list is the wrong artefact: each repair round certifies against the previous round's list, so a new instance introduced by the repair itself is invisible to it. What would break the cycle is stating the **screen** — the sixteen disposition phrasings — at the marker and requiring the next certifier to re-run it rather than to count against a list. The 0825-B1 marker already does this; I would make it the required form.

**The `STATE_OF_THE_ANSWER.md`-against-`working/one-pagers/` screen.** One caution from today's work: `working/one-pagers/` and `lit-review/` returned **zero** carriers for every one of the seven gap slugs and for all three ad hoc Committee names. That is a real result, but it also means that on today's evidence those two directories are contributing nothing to the gap-check and their addition to the corpus is so far unexercised. If the systematic screen finds the same, the honest conclusion is that the seven-place rule is currently a five-place rule with two directories held in reserve — which is fine, and is worth recording as such rather than letting seven-place language imply seven-place coverage.

---

*Filed 2026-08-27 by the research analyst. Two of three searches spent, one returned unspent with the reason stated; one engine's generated prose refused in full and recorded as refused. Nothing committed, nothing edited, no fetch attempted and none available to this seat. Every corpus figure derived programmatically at anchor `58cb26c` (819 commits at that ref; 822 at HEAD; `.git/shallow` absent, checked by me) with today's file read separately from the working tree and labelled at each use. Every quoted window verified as an exact substring at a stated offset before this text was written. **My own pre-registered branch on rows A and B failed, and it failed because the licence I wrote contained two clauses that cannot both operate — I report that before reporting what the batch nevertheless bought.** The question put to me at Part II was settled by this council on 2026-08-04 and the ruling had gone unfound for twenty-three days; I recommend the route be marked spent so the next seat finds it. Two gates proposed, one gap materially advanced, none closed, no proceeding moved. Nobody read a body.*

---

## Part III — The chairman's own verification, and the corrections it forced on me

*Everything in this Part I derived myself, programmatically, before ruling on anything that depends on it. The
rule I work to is that I may not adopt a figure I have not run — and today it caught me twice.*

### The analyst's load-bearing figure reproduces, and my first derivation of it was wrong

The slot-controlled comparison is the figure his whole ARB/23/3 reading rests on. I re-derived it from the
results corpus rather than accept it.

**My first pass returned nine dockets carrying ` Published Decisions`. His figure is eight. Mine was one high,
and the error was mine.** `UNCT/20/6` sits in this corpus under **two URL notations** — the raw form and the
percent-encoded `UNCT%2F20%2F6` — and I counted notations where the claim counts dockets. Collapsing them
gives, on my derivation: **21 `Status of Proceeding`-anchored ICSID case-detail rows across 11 distinct
dockets; 9 dockets reach the slot after `(a) Original Proceeding`; 8 carry ` Published Decisions` — ARB/16/34,
ARB/17/19, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, UNCT/14/2, UNCT/20/6; ARB/23/3 alone carries
` Date Development`; ARB/10/7 and ARB/12/1 do not reach the slot.** That is his list, exactly, and the
integrity officer's independent derivation returns the same.

**I record my own error rather than quietly publish the corrected figure.** It is the same arithmetic failure
this council recorded against the officer's seat yesterday — a count taken at the wrong unit — arriving at my
seat today, and the only reason it is not in the record as a defect is that I ran it twice.

### `0827-B1`, which is the day's most serious objection, re-derived by me before I ruled

The officer says the analyst's *"first capture of ICSID site chrome"* is false. **I ran it myself and it
reproduces.** Screening the anchor corpus on the synonym `Skip to main content` returns **twelve ICSID rows
carrying the site header chrome verbatim**, and **two of them sit on a case-detail page** —
`2026-08-13-zz-einarsson-docket.json` `records[2]` and `records[5]`, both `CaseNo=UNCT/20/6`, both encodings,
in the reduction held at commit `4d124af`.

**And the limb that decides the gate, which I also checked myself.** I read both case-detail chrome captures in
full. Each is the first 400 characters of the reduced body and each terminates mid-word at `Young ICS`. Neither
reaches the Resources/Publications mega-menu where `Tables of Decisions` sits. **`Decisions` returns zero in
both.** So a `CHROME-SCREEN BEFORE BRANCH` run against this project's actual chrome holdings on 2026-08-26
would have returned zero and **passed row A** — the row it exists to catch.

**That is worse than a rule that does not exist, and I rule accordingly in Part VI.** A gate that returns clean
on the case it was written for does not merely fail; it certifies.

### The item-6 screen — run, and cut from three to two before publication

The 2026-08-26 close-out required the systematic screen of the answer file against `working/one-pagers/`, which
this council had never run. I ran the broader form of it: a repo-wide census of every `GAP-UNRESOLVED:` marker
at anchor `58cb26c`, **27 distinct slugs**, with per-file attribution.

**I put three findings to the officer before writing any of them into this record, and he struck one.**

- `landreau-1922-holding-and-pinpoint` — `lit-review/ferguson-memo.md`. **Open.** Its governing sentence reads
  *"No pinpoint is offered and no holding is asserted."* `Landreau` returns zero across the other six places.
- `philip-morris-uruguay-2013-jurisdiction-para-185` — `lit-review/kim-memo.md`. **Open.** Its governing
  sentence closes *"…cannot be determined from any source in hand, and the retrieval task stands."*
- ~~`vanda-cfc-2024-2025-takings-record`~~ — **STRUCK, and it was my error.** The marker string sits inside a
  sentence that **opens** *"**Related matter — retrieved and read; gap closed 2026-08-08.**"* and **closes**
  *"(Entry history: recorded as `GAP-UNRESOLVED: vanda-cfc-2024-2025-takings-record` on 2026-08-07; closed on
  retrieval verification 2026-08-08.)"* **I verified both clauses myself at the blob before accepting the
  strike.** My screen matched a marker string inside a closed-gap entry history and I read the string instead
  of the sentence.

**The corrected finding, which is what this record publishes: TWO open gap markers live only in `lit-review/`,
absent from every daily record and from `STATE_OF_THE_ANSWER.md`.** And the scoping the officer required, which
I adopt: that is true of the **markers**, not of the **subjects** — *Philip Morris* is carried throughout this
project, and daily tracking already carries an adjacent slug on the same case on a different question.

**Had I published the finding as I first derived it, I would have put a false open gap into the very tracking
this council is trying to make complete.** That is the exact defect class the item-6 screen exists to catch,
committed by the screen's own author, and it is why the finding went to the gate before it went to the record.

### Reduction citations, corrected under `0827-B3`

The officer is right and the failure is a self-certification failure: my own pre-registration says in capitals
that today's record *"CITES TODAY'S REDUCTION BY COMMIT SHA AND NEVER BY PATH"*, and my Part I then cited the
2026-08-04 row by path. The mechanism matters — the merge that publishes this record re-fires the relay,
re-selects today's request file and **overwrites today's reduction in place** (escalations `N6`-as-upgraded and
`N12`, both live and unfixed). **The holding commits, supplied here so every citation in this record resolves
after that overwrite:**

- today's reduction — **`7e755e2`**
- `2026-08-04-daily.json` — **`4e5f4ca`**
- `2026-08-04-undocs-identify.json` — **`7f452a3`**
- `2026-08-04-undocs-control.json` — **`6861103`**
- `2026-08-21-daily.json` — **`b04c81b`**
- `2026-08-13-zz-einarsson-docket.json` — **`4d124af`**

**And the `0825-B13` hazard travels with all six:** a SHA cited from a commit later re-authored verifies for
nobody.

### What my prior-art screen is incapable of catching, stated because the officer required it

I wrote in Part I that the screen "caught nothing." **That is true of what it screens and it is not a clean
bill, and I add the sentence he asked for rather than let the first form stand.** A route+find screen tests
whether a URL and a literal have been fired before. **It cannot test whether a licence is coherent.** The
defect that actually mattered today was exactly that: row A's branch and row A's grading caveat cannot both
operate, the caveat voids the branch's premise whenever it fires, and it fired. The analyst wrote the licence,
**I adopted it verbatim and called the adoption "with no chairman correction" as though that were a virtue**,
and neither of us read the two clauses against each other. No screen this council currently runs would have
caught it.

### One correction to my own Part I wording, under `0827-B8`

I wrote that *"this project has fetched `A/81/17` successfully once."* **Every particular I gave is exact** —
the officer verified the status, the byte count, the full digest, the `final_url` and the excerpt
character-for-character. **But the topic sentence is tool-status-as-source-state and I cure it here:** the
correct form is that this project **has received a `200` on a request for `A/81/17` once**, from a resolver on
a different host, and what that 200 carries is settled at Part V.

### One correction under `0827-B14`

`lit-review/ferguson-memo.md` gives the Tethyan pincite list as **`¶¶ 1283, 1288, 1327–1330, 1331–1333`**. The
pre-registration and every carrier in this council's records give it as `1283, 1288, 1327–1330` — **short by
one range.** Verified by me at the blob. Corrected here so a reader re-running the pincite does not meet an
unlisted range and take it for new.

---

## Part IV — Analytics (Rule 1), which is two lines because the digest of record has not moved

`Digest 2026-08-24: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

**Unchanged since 2026-08-24.** `digests/2026-08-24_ISDS-Thematic-Watch/meta.json` is the newest `meta.json` in
the repository; `screened` is 30, `matches` is 0 and `watch_list_leads` is 0, copied from that file and not
restated from memory. Per Rule 0 the analytics officer is not convened and this is the whole of the analytics
content. **`validation_status_only` is `true` in that file** — the same `VALIDATION_STATUS_ONLY` gate that is
escalation 1 to the operator, visible in the digest's own metadata.

**On the weekly brief I carry forward the figure the record already holds and do not increment it.** The
2026-08-25 record put the interpretive brief at **four consecutive empty Mondays**; 2026-08-24 was the most
recent Monday and no Monday has fallen between that record and this one, **so the figure is still four and I
say so rather than let a carried number drift upward by inertia.**

---

## Part V — The council discussion (Rule 2), as an attributed dialogue

*Rule 0 says the integrity challenge files no Part of its own and is heard inside this dialogue. It is heard
here. **Every turn below is a real filing by a real subagent, reproduced verbatim** — three exchanges between
two seats plus my own turns, and not one word of any member's contribution was written by me. Where the
protocol asks for at least six substantive exchanges, this dialogue contains three full adversarial rounds on
the merits and a fourth on the instrument's source code.*

**Chairman, opening.** Two questions decide today. The first is what the ARB/23/3 rows bought, given that the
analyst reports his own pre-registered branch as failed. The second is whether the countermeasure he proposes
for that failure is a countermeasure at all. I put both to the officer with four of my own claims attached, and
I told him the analyst is a strong seat and that I wanted neither indulgence nor manufacture.

---

### Turn 1 — The integrity officer's vetting note, verbatim and unedited

*Spawned with the Agent tool against `.claude/agents/integrity-officer.md`. **Model discrepancy, reported by the
seat unasked, as every session: `REQUESTED claude-opus-4-8 → ACTUAL claude-opus-5`. This is the fifth-plus
consecutive session carrying it and the escalation stands open** — recorded as a procedural caveat, never a
silent substitution.*

# VETTING NOTE — INTEGRITY OFFICER, DAILY COUNCIL 2026-08-27

**Seat disclosure, unasked, as every session:** `REQUESTED claude-opus-4-8 → ACTUAL claude-opus-5`. Fifth-plus consecutive report; escalation stands open.

**Anchor verified, not inherited.** `58cb26c` = 819 commits; `.git/shallow` absent; `analytics/fetch-results/` at anchor = **36 files / 268 records**; today's file = 10 records. **HEAD is now `ff6f6a6` at 823 commits, four past the anchor** — the analyst's "822 / three commits" was correct at `dcfb693` when he filed and is stale now. Do not restate 822 as current.

**Web search budget: 3 authorised, 0 spent. Fifth consecutive session at zero, and the reason is unchanged and I restate it rather than let it decay:** every question put to me today was answerable from committed blobs by programmatic derivation, and an engine return can neither correct a derivation over a committed blob nor an operator-verified primary read. Nothing today required an engine and I will not spend to look diligent.

---

## BLOCKING

### `0827-B1` — **ANALYST.** "This project's first capture of ICSID site chrome" is false, and the gate built on it would have passed the row it was written to catch. This is the most serious thing I found.

**The claim.** Part I.A: *"Across **78 prior ICSID rows at anchor `58cb26c`**, the strings `Tables of Decisions`, `ICSID Caseload Statistics` and `Resources Publications` occur **zero** times in any excerpt. Today's row A is this project's **first capture of ICSID site chrome**."*

**The three-string screen reproduces. The conclusion does not.** I confirm 78 ICSID rows at anchor and zero occurrences of all three literals — in ICSID excerpts and in all 268 anchor records. **Then I tried the synonym, which is this seat's own standing rule and the reason it exists.** `Skip to main content` returns **twelve** ICSID rows at anchor, each carrying the ICSID header chrome verbatim:

> `Skip to main content File a Request Languages English Home About Member States Overview Role of Member States Administrative Council Database of Member States Joining ICSID Secretariat Overview Secretary General Secretariat Staff Internships Panel of Arbitrators and of Conciliators Overview Database of Panel Members Qualifications for the Panels Designation Procedure Young ICS`

Two of the twelve are on a **case-detail page** — `2026-08-13-zz-einarsson-docket.json` `records[2]` and `records[5]`, both `CaseNo=UNCT/20/6`, both encodings. The other ten are `2026-08-19/20/21/22/23` rows on `tables-of-decisions/manifest-lack-of-legal-merit` and two `procedures/` and `resources/` paths.

**And the council did not merely hold the capture — it ruled on it.** `analytics/daily-research/2026-08-19.md:528`: *"That is the `<title>` and the navigation menu — the match sits at offset 0. **It establishes page identity. It establishes nothing about whether the decisions table itself is present in the tag-stripped text.**"* And `:546`: *"HIGH that the control matched in chrome (**I read the window**)."* **That is the "0819 grading rule" the analyst applies three paragraphs earlier, and it is a ruling about an ICSID chrome capture.** He invokes the rule and denies the capture it was derived from, in the same section.

**The consequence, which is worse than the error.** The proposed gate reads: *"Today's `records[1]` is this project's first ICSID chrome capture; **before today the screen was unavailable, which is why this rule is prospective and not a charge of skipping it.**"* The screen was available from 2026-08-13. **And I ran it, on the captures that actually existed: `Decisions` does not occur in any of the twelve case-detail/nav chrome captures** — it appears at index 0 only in the `<title>` of a *different* page (`Decisions on Manifest Lack of Legal Merit | ICSID`). **A CHROME-SCREEN BEFORE BRANCH run against this project's real chrome holdings on 2026-08-26 would have returned zero for `Decisions` and would have passed row A.** The captures are the first ~400 characters of the reduced body and do not reach the Resources/Publications mega-menu where `Tables of Decisions` sits.

So the gate as drafted is a check that (i) rests on a false premise about the record and (ii) would not have caught the defect it exists to prevent, while reading as though it would. That is one step short of taxonomy 26 and it is sold as a countermeasure.

**REQUIRED, and blocking on adoption:**
1. The chairman must record beside Part II (per `0822-B14`; the analyst's text stands verbatim) that the "first capture of ICSID site chrome" statement is **false**, naming the twelve rows and `2026-08-19.md:528`, and must not restate it in his own voice anywhere.
2. The true statement is narrower and should replace it: today's `records[1]` is this project's **first capture of the ICSID mega-menu / Resources-Publications region**.
3. **CHROME-SCREEN BEFORE BRANCH may not be adopted as drafted.** Strike the "screen was unavailable" clause. A gate that works must require either (a) a chrome capture that demonstrably covers the region the literal could land in, or (b) that where chrome coverage for the candidate region is unestablished, **no presence-branch may be pre-registered on that literal at all.** Absent (a) or (b) the rule licenses false reassurance, which is worse than no rule.

---

### `0827-B2` — **CHAIRMAN.** Your finding (C) is right about two markers and wrong about the third, and the third is closed.

I ran the repo-wide `GAP-UNRESOLVED:` screen at `58cb26c` independently: 27 distinct slugs. **Exactly three are confined to `lit-review/`**, and your file attributions are exact. Two are genuinely open:

- `landreau-1922-holding-and-pinpoint` — `lit-review/ferguson-memo.md:289`, in a sentence that reads *"No pinpoint is offered and no holding is asserted."* Open. `Landreau` returns **zero** hits across the other six places at anchor. Genuinely new to daily tracking.
- `philip-morris-uruguay-2013-jurisdiction-para-185` — `lit-review/kim-memo.md:92`, closing *"…cannot be determined from any source in hand, and the retrieval task stands."* Open.

**The third is not an open marker.** `lit-review/kim-memo.md:206`, in full, opens **"Related matter — retrieved and read; gap closed 2026-08-08."** and ends:

> `(Entry history: recorded as `GAP-UNRESOLVED: vanda-cfc-2024-2025-takings-record` on 2026-08-07; closed on retrieval verification 2026-08-08.)`

Your screen matched the marker string inside a **closed-gap entry history**. The gap was opened 2026-08-07, worked, and closed on 2026-08-08 with both slip opinions retrieved and read. It is the only `gap closed` note in `lit-review/` and `working/one-pagers/` combined — I checked. **Do not publish "three open gap markers ... never carried, listed, or worked."** One of the three was carried, worked, and closed twenty days ago, and publishing it would put a false open gap into the daily tracking this council is trying to make complete.

**Two further scoping fixes before you publish the corrected finding:**
- "**never carried, listed, or worked**" is true of the **markers**. It is not true of the **subjects**: *Philip Morris* is carried in `STATE_OF_THE_ANSWER.md` and in ten-plus daily records, and daily tracking already carries an adjacent slug on the same case (`philip-morris-uruguay-arb-10-7-pleaded-covered-investment`, 08-25 and 08-26) on a different question. Say marker, not subject.
- The `¶ 185` question was itself **worked on 2026-08-08** — the memo's dated correction is what reopened it. "Never worked" is wrong even at marker level for that one; "never carried by this council's *daily* tracking" is right.

**Corrected finding, which I sustain and which you should publish:** *two* open gap markers live only in `lit-review/`, both absent from every daily record and from `STATE_OF_THE_ANSWER.md`; a third marker string in the same directory belongs to a gap closed 2026-08-08 and is not open.

---

### `0827-B3` — **CHAIRMAN AND ANALYST.** Every citation to a reduction in this record lacks the commit sha, in a record whose own pre-registration binds it to carry one, and whose own escalation says today's reduction will be overwritten in place.

Taxonomy **25, mutable-reduction citation**, adopted 2026-08-05 as a house rule: *a citation to a reduction carries the commit sha that holds it; a path that can be rewritten is not a citation.*

The pre-registration says so itself, in capitals: *"ACCORDINGLY TODAY'S RECORD CITES TODAY'S REDUCTION BY COMMIT SHA AND NEVER BY PATH."* It then documents the exact mechanism (`N6`-as-upgraded and `N12`) by which the publishing merge re-fires the relay, re-selects this same request file and **overwrites today's reduction in place** — demonstrated by two-hash comparison on 2026-08-24.

**The record does not deliver it.** Chairman's Part I cites `analytics/fetch-results/2026-08-04-daily.json` `records[3]` **by path**. The analyst's eight candidate-claim locators read *"2026-08-27 relay reduction, records[5].excerpt, offsets 100 and 247"* and *"2026-08-21 relay reduction records[4] and records[5]"* — **date and index, no sha**. After the merge re-fires, `records[5]` of that path is not guaranteed to be the row the ledger entry describes.

**Partially mitigated and I say so:** each 2026-08-27 locator carries the **page-state** `sha256`, which lets a later reader *detect* a mismatch. It does not let them *resolve* the citation.

**REQUIRED, blocking on the candidate-claims block reaching the gate:** every locator gains the commit that holds the blob — `7e755e2` for today's reduction, and the holding commit for the 08-04 and 08-21 reductions. One clause each. The pre-registration already promised it; this is a self-certification failure, which is the class this seat has now recorded against the chairman on multiple days.

---

### `0827-B4` — **ANALYST.** Candidate claim 7 says more than its locator supports, and its supporting inference is the exact move he forbids elsewhere in the same return.

`claim_text`: *"The resolver serving undocs.org/docs.un.org **returns a language-selection or Document Viewer shell that carries no document title and no session ordinal**, for a symbol independently evidenced as naming a published report (A/80/17)…"*

`supporting_locator`: 2026-08-21 `records[4]` and `records[5]`.

**I pulled the raw rows.** Both are `find_matched: false` with **empty excerpts**, 4032 bytes, `sha256 94bfd2af…`. **Nothing in those two rows shows a shell.** What they establish is that two literals did not match in a 4032-byte body. The word "shell" enters only from byte-length equality with the 2026-08-04 A/81/17 row (4032 bytes) — **whose `sha256` is `140ccb14…`, demonstrably different content.** In Part I.C of his own return the analyst is emphatic that `G20` forbids reading equal byte length as page identity; in Part II he does it.

**REQUIRED, blocking on this claim:** narrow `claim_text` to what the two rows carry — *"…returned a 4032-byte body in which neither the report's title string nor its session ordinal matched…"* — or withdraw the claim. The same hedge is owed to the Part II prose sentence *"serves a stub containing neither the report's title nor its session ordinal."*

**Note for the record: the ruling this claim supports survives the fix intact**, because the ruling rests on the negative control at `/en/A/81/99` and the two nulls, not on the stub inference.

---

## NON-BLOCKING

### `0827-B5` — **CHAIRMAN.** "Length itself is a defect Rule 0 names" is a formulation this council corrected six days ago. Ruling on your question B.2.

You asked whether that is a fair reading of Rule 0 or cover for doing less. **It is neither, and it is the first thing precisely.** `prompts/daily_council_protocol.md` Rule 0 governs **seat count**, **the one optimization item**, and **six named banned forms**. It does not name length. This seat ruled on exactly this citation at `analytics/daily-research/2026-08-21.md:1203`: *"Rule 0 governs seat count, the one optimization item, and the six banned forms… The nearest true statement is the rationale ('a daily check-in that costs a weekly council is a daily check-in that stops being read') and calibration item 6. Citing a binding protocol for a rule it does not contain is taxonomy 17."* Today's Part I restates the corrected formulation. Taxonomy **10** on top of **17**.

**The proposition is right; the authority is wrong.** Cite Rule 0's *"Why"* paragraph and `prompts/council_calibration.md` item 6 (*"Default shorter than necessary"*), and the sentence becomes true.

**And on "cover for doing less" — no, and I looked for it rather than assume it.** I re-derived every figure in the record independently and all reproduce. The screens are not thinner than yesterday's. **But the compression cost one real thing, and it is `0827-B6`.**

### `0827-B6` — **CHAIRMAN.** The Rule 0 departure disclosure under-counts. Again, and it is the same finding as `0821-B5`.

Part I discloses **one** departure (convening the integrity challenge as a real subagent). Rule 0's banned list includes **an instrument-probe table**, by name. The analyst's Part II opens with one on its face — columns `row | find | matched | where it landed`, four probe rows — and carries a second table form at Part I.D. `0821-B5` sustained exactly this against a chairman, and `2026-08-21.md:69` records the governing disposition: *where a verbatim return turns out to contain a Rule-0-banned form, that is a second departure and it belongs in the disclosure.*

I checked the other five banned forms across the whole record before saying anything about them: **no "Priority focus", no "Budgets" section, no "Verify / be skeptical", no "Repository facts", and no carrying-span ledger with per-term occurrence counts and parity arithmetic.** Only the probe table. **Fix:** one disclosed line, not a rewrite of Part II.

### `0827-B7` — **CHAIRMAN.** `G6`'s citation of `0824-O13` states the opposite of what `0824-O13` says.

`G6`, pre-registered: *"The hazard is proven live on this host, not theoretical, and **0824-O13 records that this host inserts U+200B in exactly the table region rows A, B and D probe.**"*

`analytics/daily-research/2026-08-24.md:1607`, in full: *"The register file `2026-08-20-zz-column-and-middle.json` `records[3]` shows ICSID inserting U+200B repeatedly inside exactly this **class** of table/list region… **That touches neither the literal nor the page state in question**, and his verdict survives intact — but it is a reason to keep the residue live rather than let 'unevidenced on this literal' harden into 'excluded.'"*

The observation was **class**-level, on a different page, and the governing line's second clause expressly disclaims the region identity `G6` asserts. Taxonomy **18 / 24**: the qualification that was the price of the finding is dropped in the restatement. **Fix:** `G6` reads "in the same *class* of table/list region on this host, on a different page and page state."

**The analyst's own use is accurate** — *"the markup-boundary insertion `0824-O13` documents on this host"* — and I say so.

### `0827-B8` — **CHAIRMAN.** "This project has fetched `A/81/17` successfully once" is tool-status-as-source-state in the topic sentence.

Every particular you give is exact — I verified `records[3]`, `ok/200`, 4032 bytes, full `sha256 140ccb14fd83829235f71c8c3c2415f8757b9f45e2592e1423db95c73476bfac`, `final_url https://docs.un.org:443/A/81/17`, `find_matched: true`, and the excerpt character-for-character. **But a 200 from a resolver's language-selection shell on a different host is not a successful fetch of A/81/17.** You cure it three lines later; the topic sentence is what travels. **Fix:** *"has received a 200 on a request for `A/81/17` once."*

### `0827-B9` — **ANALYST.** Three locator defects in the paragraph that carries your ruling against the chairman. The ruling survives all three; the paragraph should not go out carrying them.

1. *"the `undocs.org` route carries **eight** rows, from **three** request files."* Eight rows is right. They come from **four** files — `2026-08-04-daily.json` (1), `2026-08-04-undocs-control.json` (3), `2026-08-04-undocs-identify.json` (2), `2026-08-21-daily.json` (2) — across two dates.
2. **The `/en/A/81/99` excerpt is attributed to the wrong row.** You write that the negative control *"returned `ok / 200 / 4157 bytes`, excerpt `Document Viewer home United Nations …`"*. **The negative-control row (`2026-08-04-undocs-control.json` `records[3]`, find `Fifty-ninth`) has an EMPTY excerpt.** The text you quote is `2026-08-04-undocs-identify.json` `records[2]`, find `""`. Same URL, same day, **identical `sha256 91c4c786…`** — so the join is sound and I am not challenging the substance. It is unstated, and an unstated join between two rows is the thing this council spent today's Part I.C proving you have to state.
3. *"Two of those files are pre-registered controls… **and their notes are the ruling.**"* A pre-registration note is not a ruling. **The ruling is in the daily record and it is stronger than what you quoted** — `analytics/daily-research/2026-08-04.md:263`: *"This row establishes essentially nothing about whether A/81/17 has been published, and I want that stated as flatly as possible, because it is exactly the kind of row that could contaminate the record."* And `:284`: *"the correct entry for today's `undocs.org` row is: route tested, resolver reachable, splash observed, no existence information obtained."*

**Both quotes you did give verify.** `2026-08-04-undocs-identify.json` carries yours exactly but for a leading capital `T` (mid-sentence lift, labelled "verbatim"); `2026-08-04-undocs-control.json` carries yours character-exact. **And "the first row" = `records[3]` is confirmed independently** at `2026-08-04.md:88`: *"That defect is realised in today's `undocs.org` row (Task 3(b))."*

**Your ruling stands and I sustain it against the chairman**: the question was settled on 2026-08-04, in the daily record itself, and it had gone unfound for twenty-three days. Cite `2026-08-04.md:263` and `:284` and the paragraph becomes unassailable.

### `0827-B10` — **ANALYST.** PAGE-STATE INEQUALITY IS NOT WINDOW CHANGE: the demonstration is real; the operative sentence is absolutized.

**The evidence reproduces exactly and I derived it independently.** ARB/23/3: five observations, `46ccad7f4bcaee9a` ×2 / `9237e1ae360c7482` / `e0c3d269fd723a4a` / `ac081002159b9110` — **four page states, two byte lengths (94393, 94391), 400-character window byte-identical across all five.** ARB/12/1: `bf5a70b29f8d36f9` 176048 → `588f94b5c8547e9d` 176062, **window byte-identical**. Every cell of your Part I.D table is correct.

**And unlike `G20` at `0826-B11`, this one was at risk.** Today's ARB/12/1 pair is this project's first cross-state window comparison on that docket, and today's ARB/23/3 row is its first at a different byte length. Either could have come out the other way. **It is not a tautological instrument check and I will not file it as one.**

**But the rule as drafted is taxonomy 13.** *"A change in `sha256` or `bytes` is **not evidence** that any scoped region of the page changed"* is exceptionless, and it is the sentence that will be lifted alone. n = 2 dockets, 7 observations, 1 host, 4 days, one 400-character window each. Also: a page-state change **is** weak evidence that *something* changed; what it is not is evidence about *where*. **Fix:** *"…does not localise the change and is not **by itself** evidence that any **particular** scoped region changed,"* with the conditions of the observation folded into the operative sentence rather than the following one.

### `0827-B11` — **ANALYST.** `0827-R1`'s find literal is seeded by an unretrieved third-party title, and the cell does not say so.

`Discontinued` occurs **nowhere in this repository** except today's record — zero in 268 anchor excerpts, zero as a find literal, and `discontinu*` appears in exactly one prior daily record (2026-07-30). Its only provenance is the OGEL/TDM title returned by search 2: *"…Procedural Order Taking Note of the **discontinuance** of the Proceeding — 6 January 2023."*

R1's rationale gives only the internal reason (*"it cannot occur in the (a) block, whose value is `Concluded`"*). **That is a real reason and I accept it.** But if `Discontinued` matches tomorrow, the match will be read as corroborating the title that supplied it, and nothing on R1's face stops that. Sibling of taxonomy **23** — the echo comes from an unretrieved title rather than from the request URL, which is why the existing entry does not catch it.

**Fix, one clause in the cell:** disclose the title provenance and pre-register that **a match corroborates nothing about the title, because the token came from it.** R6 makes exactly this disclosure for `discontinuance`; R1 owes the same.

**On the search-refusal question the chairman put to me: the refusal held in every assertion, and I checked hard.** No date, no ordinal, no characterisation of how the annulment ended appears anywhere in the return outside (a) rows `records[5]`/`records[6]`, which I verified character-exact, and (b) the disclosed titles. Part IV says *"THE OUTCOME REMAINS UNOBSERVED"* and the three jusmundi items are recorded as titles with a disclosed title/slug contradiction. **The one place the refused material leaves a trace is the row design, not the prose** — which is `0827-B11` and is a disclosure fix, not a leak.

### `0827-B12` — **ANALYST.** "So far unexercised" is a conclusion at a wider scope than your screen, and it was falsified the same day, in this same record.

Your closing paragraph: *"`working/one-pagers/` and `lit-review/` returned **zero** carriers for every one of the seven gap slugs and for all three ad hoc Committee names… on today's evidence those two directories are contributing nothing to the gap-check and their addition to the corpus is **so far unexercised**."*

**The screen is right.** I re-ran it: zero carriers for all seven daily slugs, and `WALLGREN`, `SEPULVEDA` (any accent), `Joongi`, `Lindholm`, `Carita` all return **zero** across all seven places at anchor — synonyms tried, per this seat's own rule. **The conclusion is not.** Your screen asked *"do these two directories carry the seven slugs I track?"* The chairman's repo-wide marker screen asked *"what gap markers do they carry at all?"* and found **two open markers that live only in `lit-review/` and nowhere else in the project** (`0827-B2`). Taxonomy **27, scope-mixed screen**: the referent clause is populated at a wider scope than the screen it annotates.

**Ruling on the tension the chairman asked me to settle:** there is no tension. The two statements answer different questions, both screens are correctly run, and **the chairman's cuts against yours.** `lit-review/` is not unexercised — it is the sole carrier of two open gaps this council has never tracked, and that is the strongest single vindication the seven-place rule has received since decision 7 adopted it. **Fix:** *"those two directories carried no carrier for the seven slugs I screened"* — and stop there.

### `0827-B13` — **ANALYST.** "The same template field" is not established, and your own eleven-row screen contradicts it.

Part I.B: *"restricted to windows anchored on the *identical* literal `Status of Proceeding` — **so every window sits at the same offset relative to the same template field**."*

The anchor literal is identical; **the field is not shown to be.** In the 9 dockets that reach the slot, the anchored Status line is followed within ~200 characters by `(a) Original Proceeding`. In **ARB/10/7 and ARB/12/1** — the two you correctly call silent — the identical anchor is followed by `(b) Rectification Proceeding` and `(b) Annulment Proceeding` respectively, with no `(a)` header in the window at all. Those two anchors are demonstrably not at the same position in the same template as the other nine.

**This does not touch the finding**, because the discriminator is the literal `(a) Original Proceeding`, not the anchor — which is why I am filing this non-blocking. **Fix:** delete the "same template field" clause; "anchored on the identical literal" is what you established and it is sufficient.

### `0827-B14` — **CHAIRMAN.** The `ferguson-memo` pincite list is short by one range.

`lit-review/ferguson-memo.md:273` reads *"ICSID Case No. ARB/12/1, Award, **¶¶ 1283, 1288, 1327–1330, 1331–1333** (July 12, 2019)."* The pre-registration and the analyst's Part IV both give *"1283, 1288, 1327-1330."* Taxonomy **14**, in miniature: a reader re-running the pincite will meet an unlisted range and may take it for new. **Fix:** quote the full list or mark it a subset.

---

## RULINGS REQUESTED OF ME

**On your B.1 — "my prior-art screen caught nothing" and "every one of the analyst's figures reproduces."** **The second is TRUE and I derived all six independently at `58cb26c` over 36 files / 268 records:** ARB/23/3 + `Decisions` → 0 (URL-only 8); + `Published` → 0; + `Status of Proceeding` → **4 rows across three page states**, `46ccad7f4bcaee9a` ×2 / `9237e1ae360c7482` / `e0c3d269fd723a4a`, all ok/200/matched; + `Published Decisions` → **2 rows, both false**, states `46ccad7f4bcaee9a` (08-24) and `e0c3d269fd723a4a` (08-26); ARB/12/1 + `Date of Constitution of ad hoc Committee` → 0 (URL-only 2, both 08-26); + `Status of Proceeding` → 1 row, 08-26, 176048, `bf5a70b29f8d36f9`. **No discrepancy. Clean.** **The first is true of what you screened and should not stand as a clean bill:** a route+find screen cannot catch a logical defect in a licence, and the defect that actually mattered today was one — row A's branch and its grading caveat cannot both operate, you adopted the licence verbatim, and neither of you caught it. **Add one sentence saying what the screen is incapable of catching.** That is the honest form and it costs you nothing.

**On your B.2 — Rule 0 and length.** Ruled at `0827-B5`. Proposition right, authority wrong and previously corrected, not cover for doing less — with the one cost at `0827-B6`.

**On your B.3 — "NO STATE CHANGE from 2026-08-02 onward. This is the twenty-sixth consecutive day."** **Arithmetic correct: 2026-08-02 → 2026-08-27 inclusive = 26.** A daily record exists for every one of those 26 dates, and **all 26 carry a `CONNECT tunnel` statement** — I ran the synonym screen (`egress`, `403`, "no network") rather than a single term, per this seat's own rule. The claim is correctly built, correctly scoped, re-probed rather than inherited, and carries the tool-status-as-source-state rule. **No objection. This is clean and I say so plainly.**

**On your B.4 — G20 and the fourth page state.** **Nothing you wrote is falsified and you did not overclaim.** At the moment `G20` was written this project held exactly **three** ARB/23/3 page states and **all three were 94393 bytes** — I confirmed both, at anchor. Today's `ac081002159b9110` at 94391 makes a fourth; `G20`'s operative rule (*byte-length equality is not page-state identity*) is untouched, because today supplies an instance of *inequality*, which the gate does not govern. Your enumeration is now **stale by one** and must not be restated without the fourth state. **And the sentence that most protects you is your own:** *"THE THIRD INSTANCE STRENGTHENS THE GENERALISATION; IT DOES NOT VINDICATE THE GATE, WHICH WAS UNFALSIFIABLE ON THE DAY IT WAS WRITTEN."* That is the opposite of overclaiming, it carries `0826-B11`'s second limb forward without softening, and it is the best-calibrated sentence in the pre-registration.

**On your C — the three gap markers.** Ruled at `0827-B2`: **two, not three**, and the tension with the analyst's "unexercised" resolves in your favour — see `0827-B12`.

**On the two proposed gates.** PAGE-STATE INEQUALITY: **sound, genuinely at risk, adopt with `0827-B10`'s rewording.** CHROME-SCREEN BEFORE BRANCH: **do not adopt as drafted** — `0827-B1`. Not tautological in `0826-B11`'s sense (it can fail), but its premise is false and it would have passed the row it was written for, which is a worse failure mode than a tautology because it produces false reassurance.

---

## OBSERVATIONS

`0827-O1` — **The load-bearing figure of the day reproduces exactly, and I derived it from scratch.** 21 `Status of Proceeding`-anchored ICSID case-detail rows across **11 distinct dockets** (`UNCT%2F20%2F6` and `UNCT/20/6` are one docket in two encodings — count them once). **Nine dockets' windows reach the slot after `(a) Original Proceeding`. Eight carry ` Published Decisions` — ARB/16/34, ARB/17/19, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, UNCT/14/2, UNCT/20/6, exactly his list. ARB/23/3 alone carries ` Date Development`. ARB/10/7 and ARB/12/1 do not reach the slot.** Adding differently-anchored windows, **ARB/22/2** (`2026-08-06-daily.json` `records[4]`, find `Latest Development`) carries the contiguous string, bringing distinct dockets to **nine**. Seed identifications confirmed from `working/one-pagers/`: ARB/16/34 = Bridgestone, UNCT/14/2 = Eli Lilly. **The "No References Available." asymmetry is also exactly right: of the eleven dockets, ARB/23/3 is the only one whose window carries it.** Subject only to `0827-B13`, this is the best-derived figure this council has produced and I am recording that it survived an adversarial re-derivation intact.

`0827-O2` — **The stitch is exact, to the character.** Maximum verbatim overlap between the tail of `records[6]` and the head of `records[5]` is **167 characters**, and the string he quotes is that overlap character-for-character. `400 + 400 − 167 = 633`. His 533-character source quotation is `stitch[100:633]`, exact. Every offset he states is exact: `Tables of Decisions` at **90**; `Decisions` at **100**; `No References Available.` at **255**; `(a) Original Proceeding` at **280**; `Date Development` at **304**; `WALLGREN-LINDHOLM` at **328**; `Debevoise` at **391**; `Composition` at 157; `President` at **247**; `(b) Annulment Proceeding` at **203**. **I checked the offsets, not just the substrings, as instructed.** All five candidate-claim `supporting_quote` values that are non-null are exact substrings at the stated offsets. `G9` passes on all eight content rows; the `G18` bracket is verified identical at both ends (559 bytes / `ff67a9d764d6a236`).

`0827-O3` — **One unqualified sentence in an otherwise exact paragraph.** *"Every anchored match sits at excerpt offset **exactly 100**."* True of all five matching **content** rows. **False of `records[9]`**, the `G18` tail, where `Example Domain` sits at offset **0** because the clamp fired — the same clamp `0824-O14` verified. The preceding sentence scopes him to content rows so I am not filing it; a reader lifting the sentence alone would be wrong.

`0827-O4` — **The pre-registration is provable from commit order and I checked it rather than take it.** `7a6ecab` committed **11:25:20Z**; relay results `7e755e2` at **11:26:05Z**; council part 1 `dcfb693` at 11:33:52Z; part 2 `ff6f6a6` at 11:45:12Z. The batch was fixed before any result existed. **This is the strongest procedural feature of this council's method and it should stay.**

`0827-O5` — **Rule 4 compliance is clean on both seats.** The corrected no-body-read rule is carried in the mandated form in the pre-registration and in Part II. **I grepped the whole record for the over-broad form and its variants: zero occurrences.** Nobody reissued it. **Rule 1 compliance is clean so far:** no "N screened" phrase anywhere in the record. **For the Part IV sentence you still owe, the numbers from `digests/2026-08-24_ISDS-Thematic-Watch/meta.json` are: `Digest 2026-08-24: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`** Copy them; do not restate them.

`0827-O6` — **Taxonomy extension, three entries, checked against the full canonical table at `agents/integrity-officer.md` and not from memory.** Numbering left to you, given the standing entry-27 collision escalated at C11 — **cite by name until that is settled.**
- **TRUNCATED-CAPTURE ABSENCE** — a screen run against captures that structurally cannot contain the target region, reported as absence of the target. Sibling of 27 (scope-mixed screen), but the defect is the **capture's extent**, not the corpus's boundary: here twelve chrome captures covering the first ~400 characters of the reduced body cannot reach a mega-menu at character ~90 of a *different* anchor's window, so a zero is guaranteed by the capture and discriminates nothing. *Countermeasure: before reading a zero as absence, state what region the captures being screened actually cover, and whether the target could appear inside it.* Instance: `0827-B1`.
- **TITLE-SEEDED FIND LITERAL** — a find literal drawn from an unretrieved third-party listing title, so a match corroborates the title that supplied it. Sibling of 23 (echoed-find), where the echo is from the request URL; here it is from an external artefact the project has not read. *Countermeasure: a find literal whose provenance is a title carries that provenance in its cell, and its branch pre-registers that a match is not independent of the title.* Instance: `0827-B11`.
- **CLOSED-MARKER REVIVAL** — a gap-marker string counted as an open gap when the sentence carrying it records the gap closed. Sibling of 11 (status-as-record-artifact) and 18 (selective-quotation supersession). *Countermeasure: a marker census reads the governing sentence of every hit; a grep for the marker string is a census of strings, not of open gaps.* Instance: `0827-B2`.

`0827-O7` — **The vault taxonomy stands at 27 entries with the entry-27 collision unresolved, and the archivist has not synced since 2026-08-09.** I read it there, per the mandate, and did not recite. The three above are not written into it by me — `agents/` is outside this seat's hands and the routing at `0826` decision-adjacent escalation 7 still stands. **This is the second consecutive session in which entries adopted at vetting have no path into the file the mandate names.**

---

## COVERAGE STATEMENT

**Derived independently and programmatically, at `58cb26c` over 36 files / 268 records with today's file read separately from the working tree, and labelled at each use:** the 36/268 corpus scope; all six chairman route+find figures; the 78 ICSID rows and the three chrome literals; the twelve-row `Skip to main content` synonym screen; the 21-row / 11-docket / 9-slot / 8-vs-1 slot comparison and the ARB/22/2 addition; the "No References Available." uniqueness; the 167-character overlap and 633-character stitch by maximum-suffix match; all ten find-offsets; all eight candidate-claim quotes and locators; the five-observation ARB/23/3 and two-observation ARB/12/1 window-constancy series with page states and byte lengths; the 23/24 and 21/22 A/81/17 populations with the `robots_disallowed` row dated to 2026-08-08; all eight `undocs.org`/`docs.un.org` rows with bytes, shas, final_urls and excerpts; the four Part III prior-art cells (`Discontinued` 0 anywhere, `WALLGREN-LINDHOLM` 0, `ogel.org` host-only 0, ARB/12/1 URL-only 2→4); the repo-wide 27-slug `GAP-UNRESOLVED` census with per-file attribution; the four-name committee screen across all seven places **with synonyms** (`Lindholm`, `Carita`, accent variants); the 26-day egress screen across all 26 daily records **with synonyms**; commit timestamps for the pre-registration ordering; both pre-registration quotes against the raw request files; all seven 2026-08-26 close-out decisions against `2026-08-26.md:1044–1078`.

**Rule 1 (positive control before any HTTP-status objection): NOT TRIGGERED.** I filed no objection resting on an HTTP status. `0827-B8` is about the *characterisation* of a 200 the record already holds, not about the status.
**Rule 2 (raw source for any quote claim): HONOURED.** Every quotation I challenged, I hold the raw source for — `analytics/fetch-results/2026-08-04-{daily,undocs-control,undocs-identify}.json`, `2026-08-21-daily.json`, `2026-08-27-daily.json`, `analytics/fetch-requests/2026-08-04-undocs-{identify,control}.json`, `lit-review/kim-memo.md`, `lit-review/ferguson-memo.md`, `analytics/daily-research/{2026-08-04,08-19,08-21,08-24,08-26}.md`.
**Rule 3 (grep the record before asserting absence): HONOURED, and it killed two objections** — see NEAR-MISSES.
**Rule 5:** no tool status anywhere in this note is treated as a statement about a resource.

**NOT checked, and I name it rather than let coverage read as complete:** I did not verify the relay's own code paths this session (`0824-O14` stands and I relied on it); I did not re-derive the 2026-08-19/20/21/22/23 chrome rows' page states beyond the excerpt text; I have read no body, and nothing in this note rests on one. **`scripts/check_marks.py` was not run and would not have exercised anything here** — I read its docstring, which is the single statement of its scope: tiers 2, 3 and 5 only, tier 4 never, and **tiers 3 and 5 do not run at all on legacy-form entries**, which is what `lit-review/` still holds. Today's Amendment 2 work was mine by hand, and it comes out clean: every screened term with a nonzero count in this record is either the source of a quotation or carries a referent clause — the chairman's `Status of Proceeding` 4 and `Published Decisions` 2 both name their page states; the analyst's `(a) Original Proceeding` 9 names its eight dockets; `Discontinued`/`WALLGREN-LINDHOLM`/`ogel.org` are true zeros with the synonym tried. **No Amendment 2 violation today.**

---

## NEAR-MISSES — objections I considered and killed

**1. That every "close-out decision N" citation in this record was mis-numbered. KILLED, and it would have been my largest false objection of the session.** I read `2026-08-26.md`'s **"Next steps for tomorrow"** list (six items, line 1145) and mapped today's citations against it: decision 2 came out as "carry the corrected figures", decision 3 as "close-out item 4 not discharged", decision 6 as "run the systematic screen", and there was no decision 7 at all. I had four sustained objections drafted. **Then I obeyed my own Rule 3 and grepped for the section I had not read.** There is a separate `### Decisions` block at line 1044 with **seven** numbered decisions, and **every one of today's citations is exact**: 1 = rows G and H cut together; 2 = the two freed slots stay unused; 3 = `G7` corrected on `0826-R7`; 4 = `G17` rescoped; 6 = the no-body-read rule reworded in the exact form carried today; 7 = `working/one-pagers/` and `lit-review/` added to the corpus. **Both seats cited seven authorities across two documents and got all seven right.** This is the near-miss the section exists for: I was one grep from filing four objections against a clean record.

**2. That the "0819 grading rule" does not exist. KILLED by the synonym, which is my own standing rule and I nearly failed it twice in one session.** `grep "grading rule"` on `2026-08-19.md` returns nothing. `grade|chrome|page identity` returns `:299`, `:306`, `:524–528`, `:546` — the rule in substance, and the sentence *"It establishes page identity. It establishes nothing about whether the decisions table itself is present."* The citation is sound. **The same screen is what produced `0827-B1`**, which is the argument for the rule as strongly as anything I could write.

**3. That the ARB/12/1 window-constancy result is a `G21` violation — equal windows across two page states read as evidence of nothing changing. KILLED:** the analyst asserts the converse and only the converse (inequality of state does not establish change in the region), never that the window's constancy establishes the page did not move. `G20` is left explicitly intact. **Correctly built, and it is the harder direction to get right.**

**4. That `0827-R3` and `0827-R5` are controls drawn from inside the suspect set (taxonomy 15). KILLED:** their function is the same-URL same-run digest pair adopted at `0821-E8`, not host-artefact separation, and the analyst says so. Filing it would have pointed the council at the wrong repair.

**5. That the analyst's "reduced-body-scoped" contradicts my own `0824-O14`, which found a null "genuinely page-scoped". KILLED:** `0826` decision 4 rescoped it on this seat's own ruling, and he carries the rescoped form. Restating my 08-24 wording would have been taxonomy 10 by me.

**6. That candidate claim 4 is unsupported because its `supporting_quote` carries only the ARB/23/3 half of an eight-vs-one comparison. KILLED:** the locator names the programmatic derivation and the eight comparator dockets by designator, and a cross-docket comparison has no single carrying span by construction. The honest form is the one he used.

**7. That the analyst's `R6` proposes an uncontrolled row. KILLED, and it is the opposite of a defect:** he declares `UNCONTROLLED` in its own cell, states that no `ogel.org` baseline exists in 268 records (I verified: host-only 0), refuses to invent a control, ranks it last, and subordinates it to the docket-first rule. **That is taxonomy 22's countermeasure applied correctly and unprompted.**

**8. That the `Skip to main content` chrome rows might be `<title>` text rather than navigation. KILLED by reading all twelve windows rather than counting them:** every one carries the skip-link, the language switcher and the full top-level menu, and two sit on a **case-detail** URL — the same page family as row A.

---

## DISPOSITION

**FLAGGED. Four blocking objections, ten non-blocking, seven observations.** Blocking on the merge: `0827-B1` (the chrome claim and the gate built on it), `0827-B2` (the third gap marker is closed), `0827-B3` (reduction citations without the holding commit), `0827-B4` (candidate claim 7 exceeds its locator).

**What is clean, said in one line each because it is owed:** every quoted window, every offset, every candidate-claim substring, the 167/633 stitch, the eight-vs-one slot comparison, the four-name novelty screen, the whole window-constancy series, the `undocs.org` ruling's substance, the 26-day egress arithmetic, the six prior-art figures, the seven close-out citations, the pre-registration's commit order, Rule 1 and Rule 4 compliance — **all verified and all correct.** The analyst's report of his own two-clause licence defect, before reporting what the batch bought, is the most creditable thing in the return, and the chairman's `G20` self-limitation is the most creditable thing in Part I. **Neither is a reason to soften anything above, and I have not.**


---

### Turn 2 — The research analyst's answer on the record, verbatim and unedited

*Rule 2 requires the challenged party to answer. I sent him the officer's objections against his seat in full,
with the derivations, and told him a concession is not a failure and a contest with a derivation is not
insubordination.*

# RESEARCH ANALYST — ANSWER ON THE RECORD
## To the integrity officer's `0827-B1`, `B4`, `B9`, `B10`, `B11`, `B12`, `B13`, and to the chairman's question

---

**`0827-B1` — CONCEDED IN FULL, on every limb, and I re-derived it rather than take it.** `Skip to main content` returns **12** ICSID rows at anchor `58cb26c`, **2** on case-detail (`2026-08-13-zz-einarsson-docket.json` records[2] and records[5], both `UNCT/20/6`, both encodings, both `find:""` so both are `body[:400]`). Both are byte-identical, both truncate at `Young ICS`, and **`Decisions` returns zero in both** — I checked the substring myself. So my "first capture of ICSID site chrome" is false, and the officer's sharpest limb is the one that matters: **my gate, run against this project's actual holdings on 2026-08-26, would have returned zero and passed row A.** I proposed a countermeasure that does not catch the defect it was written for. `2026-08-19.md:528` reproduces verbatim and is a ruling about an ICSID chrome capture — the reasoning was already in the record and I cited its rule while missing its instance. **The "screen was unavailable" clause is struck and I withdraw the gate as drafted.**

**On the redraft — I accept it as a floor and contest that it is the best form, with a derivation.** Its first disjunct is close to unsatisfiable: chrome on this host runs past 400 characters (the case-detail captures stop at `Young ICS`, well short of the Resources/Publications menu where `Tables of Decisions` sits at offset ~90 of today's window), and you cannot tile chrome with anchored windows without already knowing what is in it. So the rule collapses to its second disjunct and bars presence-branches almost everywhere. **The cheaper and stronger cut is an asymmetry that needs no chrome census at all: chrome can create false matches but can never create a false null.** A null is reduced-body-scoped and therefore chrome-immune; a match returns only the *first* occurrence, so any earlier occurrence — chrome or body — masks the rest. Hence: **pre-register the null branch; never pre-register a conclusion that follows from a match. Grade a match by where it lands, at read time.** That catches row A on its face without any screen — my error was pre-registering a conclusion from a match, and no chrome holding was needed to see it was one-sided. I ask that the officer's form be adopted and this substituted if he agrees it is strictly stronger; if he does not, his form stands and I do not press further.

---

**`0827-B4` — CONCEDED IN FULL. He is right and the error is exactly the one I named in Part I.D.** Both 2026-08-21 rows have **empty excerpts**; nothing in them shows a shell. "Shell" entered my claim only from byte-length equality (4032) with the 2026-08-04 A/81/17 row at sha `140ccb14`, against `94bfd2af` — **demonstrably different content at equal length, which is the precise inference G20 forbids and which I was emphatic about two pages earlier.** Claim 7 is narrowed to his text: *"…returned a 4032-byte body in which neither the report's title string nor its session ordinal matched…"*, and the same hedge is owed to and taken in my Part II prose. I note with him that **the ruling survives intact**: it rests on the negative control at `/en/A/81/99` — which has an actual excerpt — and on the two same-route nulls against a symbol independently evidenced as naming a real report. The stub inference was decoration on a sound argument, and it was decoration I had no business adding.

---

**`0827-B9` — all three locator fixes accepted; all three reproduce on my own derivation.** (i) **Four** request files, not three: `2026-08-04-daily.json` (1), `-undocs-control.json` (3), `-undocs-identify.json` (2), `2026-08-21-daily.json` (2) = 8 rows. (ii) The `/en/A/81/99` excerpt is `2026-08-04-undocs-identify.json` records[2]; the control row (`-undocs-control.json` records[3]) has an **empty** excerpt. Same URL, same day, sha `91c4c786` on both, so the join holds — **but I did not state it, and an unstated join is the defect, not the join.** (iii) Conceded, and this one stings: **a pre-registration note is not a ruling.** I built a ruling-against-the-chair on the strongest thing I found rather than on the strongest thing there was. The actual ruling at `2026-08-04.md:263` is stronger than my quotation and reproduces verbatim — *"exactly the kind of row that could contaminate the record"* — which is a better statement of my own conclusion than my own conclusion. It replaces my quotation.

---

**`0827-B10` — ACCEPTED, redraft adopted verbatim.** *"…does not localise the change and is not BY ITSELF evidence that any PARTICULAR scoped region changed."* My operative sentence was exceptionless on n = 2 dockets, 7 observations, 1 host, 4 days, and an exceptionless sentence from that base is the same overreach as the "shell" inference wearing better clothes. The conditions belong inside the operative sentence, not in the demonstration that follows it. I note his refusal to file it as a tautology, and that he is right that either docket could have come out the other way — that is why the gate was worth deriving, and it is not a reason to state it wider than it was derived.

---

**`0827-B11` — ACCEPTED, and this is the objection I am most grateful for.** `Discontinued` occurs nowhere in this repository except today's record; its **only** provenance is the unretrieved OGEL/TDM title. A match tomorrow would read as corroborating the title that supplied the literal, which is circular. Cell clause added: *"PROVENANCE: this literal is seeded by an unretrieved third-party listing title (Part V search 2) and by nothing in this repository. PRE-REGISTERED: a match corroborates the ICSID docket's own text and corroborates NOTHING about that title, about which proceeding it names, or about its date."* R6 made this disclosure and R1 owed the same; the inconsistency was mine. **It is also the contamination hazard `2026-08-04.md:263` names, arriving by a different door on the same day I quoted it** — which I record against myself.

---

**`0827-B12` — ACCEPTED; narrow to "those two directories carried no carrier for the seven slugs I screened."** My screen was right and my conclusion was at a wider scope than the screen, which is the same class of error as B4 and B10 — three instances today of a sound derivation stated one size too large, and I take that as the session's pattern rather than three separate slips. The chairman's census does not merely correct me, it inverts me: **`lit-review/` is not unexercised, it is the sole carrier of open GAP-UNRESOLVED markers this council has never tracked** — a larger finding than the sentence it corrects, and an argument for the seven-place rule rather than against it. I note his strike of `vanda-cfc-2024-2025-takings-record` as sitting inside a sentence opening *"gap closed 2026-08-08"*, and that publishing two rather than three is the right call.

---

**`0827-B13` — ACCEPTED; clause deleted.** Identical anchor literal does not establish identical template field, and ARB/10/7 and ARB/12/1 prove it — the same anchor is followed by `(b) Rectification Proceeding` and `(b) Annulment Proceeding` with no `(a)` header in the window. The clause was a decorative strengthener on a finding that did not need it, because **the discriminator is the literal `(a) Original Proceeding`, not the anchor.** The 8-of-9 contrast is untouched.

---

**The chairman's question — Ferguson `:28` and `:273`. Both verified; they change the scope, not the ranking.**

**Ranking unchanged — R1 and R2 stay first and second, and their rationale upgrades from convenient to sole-reachable.** But `:28` forces a scope correction I had wrong: the pinned paragraphs are unreadable **by redaction, not by access failure** — Emory's own auditor's note records IIC 1603 (2019) paywalled behind Investor-State LawGuide and the paragraphs `[1283]`, `[1288]`, `[1327]`–`[1330]` **missing from the publicly accessible redacted italaw version**, as one of five citations flagged for the library, none cleared. That means **no relay row, tomorrow or ever, can answer what those paragraphs hold**, because the text is not in the reachable document — the carrying-span rule's own words apply: *"If you cannot retrieve enough to look, the source is unread."* So two problems I had been running together come apart: **the finality field (item 5) is answerable from ICSID's docket and R1/R2 target it correctly; the span problem (steps 2–3) is not, and is N15-class library retrieval already sitting uncleared on Ferguson's five-item list.** The rule's own remedy is exit (i) — another document in the same matter — and `:273` already names one the memo cites and I had not: the **Decision on Jurisdiction and Liability (Nov. 10, 2017)**, which `:28` suggests may survive the redaction where the award paragraphs do not. That is a better R6 than OGEL, and I would swap it in if a retrievable route existed; `italaw.com/cases/1631` is the `/cases/NNNN` route, on which this project is **0 for 3, all 403**, so I propose no row and record the route as the obstacle. Finally, `:273` gives the pincite list as **¶¶ 1283, 1288, 1327–1330, 1331–1333** — one range longer than the gap slug has been carrying in my text and yours. **The slug's locator is corrected to the full list, with a note that `:28` and `:273` differ in extent and that `:28` is silent on whether 1331–1333 is also redacted.** That correction is owed whatever tomorrow's rows return.

---

*Answered 2026-08-27. Two blocking objections conceded in full on every limb, both re-derived by me before answering rather than accepted on the officer's word. One redraft accepted as a floor and contested on form with a stated derivation, yielding to the officer if he disagrees. Four non-blocking objections accepted without qualification. Three of today's defects — B4, B10, B12 — are one defect three times: a sound derivation published one size larger than it was derived, and I would rather the record name the pattern than the instances. My gate at Part III is withdrawn; my figure at Part I.B stands, adversarially re-derived by two seats.*


---

### Turn 3 — The integrity officer's ruling on the substituted gate, verbatim and unedited

*The analyst conceded the objection and contested the redraft, asking that the substitute be put to the officer
rather than decided by me. I put it, with two conditions: that he test the load-bearing asymmetry against the
reducer's actual behaviour rather than accept it, and that he give me the exact operative sentence so I adopt
words and not a paraphrase.*

# INTEGRITY OFFICER — RULING ON THE SUBSTITUTE GATE, 2026-08-27

**I read the reducer rather than rely on `0824-O14`'s summary of it, because this now binds a batch.** `scripts/fetch_relay.py:116–136` and `:180–182`. Everything below is derived from the code, not from the prior seat's note.

---

## 1. Strictly stronger, weaker, or non-comparable? — **COMPLEMENTARY, and his cut is the load-bearing one. Adopt the combined form at §3.**

**He is right about my first disjunct and I concede it without reservation.** "A chrome capture that demonstrably covers the region the literal could land in" is close to unsatisfiable on this host, and I can now put a number on it: the two case-detail chrome captures (`2026-08-13-zz-einarsson-docket.json` `records[2]`, `records[5]`) truncate at `Young ICS` — 400 characters into a navigation menu that has not yet reached Resources, Publications, or `Tables of Decisions`, which today's row A found at offset 90 of a window anchored *deeper still*. **You cannot tile chrome with anchored windows without already knowing what is in it, which is the thing the screen was supposed to tell you.** My rule therefore collapses to its second disjunct and becomes a blanket bar. That is safe and it is expensive, and he is right that it is the wrong place to cut.

**His cut is better placed and needs no holdings at all.** It catches row A on the face of the licence, prospectively, from a council with zero chrome captures. That is a strictly better property than mine.

**But it is not strictly stronger, because it has a hole mine does not, and it drops a limb mine has.**

**The hole: as worded, "never pre-register a conclusion that follows from a match" bars the positive control.** `G18`'s tail bracket is a pre-registered conclusion from a match — *if the tail matches `Example Domain` and the digests agree, every content row sits inside a verified bracket.* So is every mandatory same-URL control this council fires, including today's `records[3]` and `records[6]`. Those are not merely permitted, they are **mandatory**, and a rule that bars them would break the method it is written to protect.

**The correct cut is one level finer than his, and it dissolves the hole exactly.** On this instrument:

- **`find_matched: true` establishes OCCURRENCE in the reduced body. That is chrome-proof and unconditional.**
- **The excerpt establishes only the neighbourhood of the FIRST occurrence, whose position is unknown before the result exists.**

So a pre-registered conclusion that depends **only on occurrence** is sound — that is exactly what a control is, and a chrome landing does not disturb it, because a chrome landing still proves the page was served and the reducer ran. A pre-registered conclusion that depends on **where** the literal landed is void by construction. **Row A's branch inferred an in-span break in the table region: it depended on where. `G18`'s bracket depends only on occurrence.** One distinction, both cases decided, no chrome census required.

**The dropped limb: his rule removes the disclosure.** Row A's deepest failure was not that the branch was one-sided — it was that the licence never said so. Even under his rule, a seat grading a match at read time should state, in the licence, what the project's chrome holdings for that host do and do not cover. That costs one clause and it is the clause whose absence produced today's defect.

---

## 2. Is "chrome can never create a false null" TRUE on this instrument? — **TRUE as stated about chrome. FALSE if it is read as "the null branch is safe," and I found two false-null sources this council has never named.**

**The asymmetry itself holds, at the code level.** `excerpt_of` builds `body` by stripping tags from the whole document, then `idx = body.lower().find(find.lower())`; `find_matched = bool(find) and bool(excerpt_of(...))`. Chrome text sits **inside** `body`. **Adding text cannot remove an occurrence.** Chrome can only manufacture a match or steal the window; it cannot convert a match into a null. His asymmetry is sound and I sustain it.

**But a null on this instrument has four false-null sources, and only two are gated.** `G5` and `G6` are the two the council carries. **The code shows two more:**

- **`<script>` / `<style>` block deletion.** `_TAG_RE = re.compile(r"<(script|style)[^>]*>.*?</\1>", re.S | re.I)` runs **before** `_ANY_TAG_RE` and removes those blocks **entire, contents included**. A literal carried in inline JSON, a JSON-LD island, or a templating payload is deleted before the search ever runs. This is not `G5` — `G5` is about attributes — and on a site of this construction it is not exotic.
- **Non-textual content type.** `excerpt_of` returns `""` whenever `"html" not in content_type and "text" not in content_type`. `find_matched` is then **false regardless of the literal**. A null can mean "the response was not textual," not "the literal is absent."

Both return a clean `find_matched: false` **indistinguishable from real absence**, and neither is chrome. **So the substitute is sound but must not be written as "the null branch is safe."** The true form is: *a null is chrome-safe, not safe.* If it goes out as "safe," it will be read as discharging `G5` and `G6`, and this council has spent three days keeping those residues alive.

**On your second question — does the asymmetry hold for chrome but fail for another first-occurrence masker?** **The masker class is "any earlier occurrence," and chrome is only a special case of it.** Masking cannot produce a null, because `find_matched` is a boolean over the whole body and is indifferent to which occurrence is first — so the asymmetry does not fail. **But today's own record contains a body-on-body masker with no chrome in it:** on **ARB/10/7 and ARB/12/1** the first occurrence of `Status of Proceeding` sits in a per-proceeding block rather than the case summary, which is why their windows open onto a `(b) …Proceeding` header where the other nine open onto `(a) Original Proceeding`. That is the 2026-08-05 mis-anchoring rule, and it is the same mechanism as row A with chrome subtracted out. **It is the strongest argument for his form over mine: cutting at chrome cuts at a special case; cutting at the occurrence/location distinction cuts at the mechanism.**

**`0827-O8` — one further finding from the code, which strengthens today's load-bearing null and which nobody has recorded.** The search is **case-insensitive** (`body.lower().find(find.lower())`), while every "exact substring" this council verifies is case-sensitive. Two consequences, both material: **row B's null is stronger than it has been claimed** — it establishes that neither `Published` nor `published` nor any case variant occurs anywhere in the reduced body of `ac081002159b9110` — and **a match is weaker than assumed**, since any case variant anywhere fires it. The second consequence cuts the same way as the ruling above.

**`0827-B15` — CHAIRMAN, NON-BLOCKING, REQUIRED BEFORE THE NEXT BATCH.** `G5` and `G6` do not enumerate the residues this instrument actually has. Amend the null-residue statement to carry four, not two: attribute-only (`G5`); zero-width or entity inside the literal (`G6`); **`<script>`/`<style>` block deletion**; **non-textual content type**. Nothing already recorded is falsified — today's rows are all `text/html` and row B's null survives intact — but a null branch cannot be pre-registered as safe against a residue list that is short by two.

---

## 3. The exact operative text. Adopt these words.

> **GRADE THE MATCH, PRE-REGISTER THE NULL.** On this instrument `find_matched` is a boolean over the whole tag-stripped body while the excerpt is centred on the **first** occurrence, so **a match establishes OCCURRENCE and never LOCATION.**
>
> **(a)** A conclusion that depends **only on occurrence** — a positive control, a digest pair, the `G18` bracket — **may** be pre-registered on a match.
>
> **(b)** A conclusion that depends on **where** the literal landed **may not** be pre-registered on a match. It is graded at read time under the 0819 rule. Any earlier occurrence — in navigation chrome or in the body — masks every later one, so a match-branch of this kind is one-sided by construction and cannot discriminate in the matching direction.
>
> **(c)** A null branch **may** be pre-registered, because added text cannot remove an occurrence and chrome therefore cannot produce a false null. **A null is chrome-safe, not safe:** it remains subject to `G5` (attribute-only), `G6` (zero-width character or entity inside the literal), `<script>`/`<style>` block deletion, and a non-textual content type — each of which returns a clean `find_matched: false` indistinguishable from absence.
>
> **(d)** Where a presence-branch is to be graded at read time, the licence states in advance what this project's chrome holdings for that host do and do not cover, so the exposure is on the record before the result is.
>
> *Derived 2026-08-27 from `scripts/fetch_relay.py:116–136, 180–182`. Row A's branch failed limb (b); `G18`'s tail bracket passes limb (a); row B's null is limb (c) correctly used.*

**Attribution, because it matters and because he is entitled to it:** limbs (b) and (c) are the analyst's substitute; limb (a) is the correction his wording needs; limb (d) is what survives of mine. **He was right to press it, my redraft was the weaker instrument, and the record should say so in those terms.**

---

## 4. On the "one defect three times" framing — you asked for my reading, so here it is unsoftened.

**The diagnosis is right, it is not self-flattering, and it understates his own case.**

**It is right for `B10` and `B12`.** The gate stated exceptionless from n=2 dockets on one host, and the screen's conclusion written at a wider scope than the seven slugs it ran on, are the same defect: a derivation sound at the scope it was run, published one scope larger.

**It is right for a third instance he did not claim.** `0827-B13` — *"every window sits at the same offset relative to the same template field"* — is the cleanest example of the pattern in the entire return, and it is missing from his list. **A seat constructing a self-flattering umbrella does not leave out the tidiest instance.** That omission is the best evidence that the framing is honest.

**It is half right for `B4`, and the other half must not travel under it.** Limb one — `claim_text` asserting a shell that its two empty-excerpt rows cannot show — is the pattern. **Limb two is not.** Inferring "serves a stub" from `4032 == 4032` across two rows whose `sha256` values differ is not a sound derivation published one size too large; it is an **unsound inference of a different kind**, and it is the exact move his own Part I.D says `G20` forbids. Folding it into a size story would let a `G20` violation exit the record as a matter of phrasing. **It is not. Keep it named separately.**

**On the taxonomy: name the genus, do not number it fresh.** The pattern is the genus of which two adopted entries are species — **13, absolutized heuristic** (the rule-shaped form) and **27, scope-mixed screen** (the screen-shaped form). Record it as **PUBLICATION-SIZE DRIFT** — *a derivation sound at the scope it was run, written up one scope larger* — with the countermeasure that actually bites: **the sentence stating a finding names the scope of the run that produced it, in that same sentence, not in the one after it.** Cite it by name, not by number, until the entry-27 collision is settled.

**And say this plainly in the close-out, because it is the part that could be misread:** naming the pattern is a **countermeasure, not a mitigation**. He conceded `B1` and `B4` in full, re-derived `B1` against me rather than taking it, and accepted `B9` through `B13` without qualification. **The concessions are what earn the naming. Three errors do not become one error because they share a mechanism — they become one repair.**

---

**Nothing else from me. `0827-B2` strike and `0827-B3` application both acknowledged and verified as you describe. My blocking objections are discharged on the terms above; `0827-B15` is the only new item and it is non-blocking. Search budget closes at 0 of 3 spent.**


---

### Turn 4 — Chairman, verifying the code reading myself before adopting it

**The officer went to `scripts/fetch_relay.py` rather than rely on a prior seat's summary of it, and because
what he found will bind a future batch I read it too rather than take it.** Every limb verifies:

- `_TAG_RE` at line 78 is `<(script|style)[^>]*>.*?</\1>` with `re.S | re.I`, and it runs **before**
  `_ANY_TAG_RE`. It removes those blocks **entire, contents included.** A literal carried in inline JSON or a
  JSON-LD island is deleted before the search runs. **That is a false-null source and it is not `G5`.**
- `excerpt_of` opens `if "html" not in content_type and "text" not in content_type: return ""`, and
  `find_matched` is `bool(find) and bool(excerpt_of(...))`. **A non-textual response returns a clean
  `find_matched: false` regardless of the literal.** Second false-null source.
- The search is `body.lower().find(find.lower())`. **Case-insensitive.** So row B's null is stronger than it
  was claimed — it covers `Published`, `published` and every case variant — and a match is correspondingly
  weaker.
- `start = max(0, idx - EXCERPT_CHARS // 4)` with `EXCERPT_CHARS = 400` gives the left edge at `idx − 100`,
  and the `max(0, …)` clamp is why the `G18` tail sits at offset 0 rather than 100 — which is exactly the
  scoping `0827-O3` flags.

**This is the most valuable thing produced today and it did not come from a retrieval.** It came from a seat
reading the instrument's own source because a rule was about to bind on an assumption about it. **Two of the
four false-null sources this instrument actually has were unrecorded in twenty-six days of null-based
reasoning**, and every null this council has banked was banked against a residue list short by two.

---

### Turn 5 — Chairman, on the exchange that produced the gate

**The analyst was right to press and the officer was right to concede, and the record should say so in those
terms.** The officer's redraft required a chrome capture demonstrably covering the region a literal could land
in. The analyst showed that is close to unsatisfiable here — chrome on this host runs past the 400-character
capture, so you cannot tile it without already knowing what is in it — and the rule collapses into a blanket
bar on presence-branches. His substitute cuts at the occurrence/location distinction instead and needs no
holdings at all.

**And the officer then improved the substitute by finding the hole in it**: as the analyst worded it,
*"never pre-register a conclusion that follows from a match"* would bar the `G18` tail bracket and every
mandatory same-URL control this council fires — which are not merely permitted but **mandatory**. The fix is
one level finer: a match establishes **occurrence**, never **location**; a conclusion resting on occurrence
alone is a control and is sound, and a conclusion resting on where the literal landed is void by construction.

**That is three seats' work converging on one rule that none of them had at the start of the day**, and the
attribution the officer volunteered is the right one: limbs (b) and (c) are the analyst's, limb (a) is the
officer's correction of the analyst's wording, limb (d) is what survives of the officer's own draft.

---

### Chairman's close of the dialogue

**SOLID.** The `G18` bracket closed identical at both ends and `G9` held on all eight content rows, so every row
today sits inside a verified bracket. The slot-controlled contrast — nine dockets reaching the slot, eight
carrying ` Published Decisions`, ARB/23/3 alone carrying ` Date Development` — was derived independently by
three seats and survives adversarially; it is the best-derived figure this council has produced. ICSID's own
docket for `ARB/12/1` yielded the ad hoc Committee's constitution date and composition, new to all seven
corpora, from a body, with a 167-character verbatim overlap joining the two windows in one run on one page
state. The reducer's four false-null sources are now on the record, two of them for the first time.

**OPEN.** The Tethyan annulment **outcome** remains unobserved and the window is cut just short of where the
(b) block's status line should sit. The `ARB/23/3` reading rests on one observation of one token on one page
state and is not replicated. `A/81/17` is a twenty-second consecutive null worth two observations, not
twenty-two, and the lag baseline is still unavailable.

**WHAT MUST NOT BE ASSERTED.** That ICSID holds no published decisions in `ARB/23/3` — the honest form is
**"not found in this surface,"** and the residue is now four sources wide, not two. That anything in the
`ARB/12/1` annulment block is a disposition or a holding; it is a docket statement and a date, and **nobody on
this council has read the award, the annulment application, or any decision in either.** That the Tethyan
proceeding was discontinued — that word entered this record from an **unretrieved third-party listing title**
and from nothing else. That `A/81/17` is unpublished or late. **And the sentence this council came closest to
publishing falsely today: that three open gap markers were never tracked. It is two.**

**FOR THE PRINCIPAL'S ATTENTION.** The research question did not move today, and no proceeding moved. What
moved is the instrument, and one item on it is escalation-grade: this council has been reasoning from nulls for
twenty-six days against a residue list that was short by two, and it found that out by reading its own source
code rather than from any retrieval.

---

## Part VI — Chairman's close-out

### The Rule 0 departure disclosure, corrected under `0827-B6`

Part I disclosed **one** departure. **There are two, and the officer is right that the second belongs in the
disclosure rather than in a footnote to it.** The analyst's Part II opens with an **instrument-probe table** —
`row | find | matched | where it landed`, four probe rows — and carries a second table form at his Part I.D.
An instrument-probe table is on Rule 0's banned list **by name**. `0821-B5` sustained exactly this against a
chairman, and the governing disposition is that where a verbatim return turns out to contain a Rule-0-banned
form, **that is a second departure and it is disclosed, not rewritten** — the member's text stands.

**So: two departures today.** Convening the integrity challenge as a real subagent, for the cardinal-rule
reason given in Part I; and a Rule-0-banned probe table inside a verbatim member return, disclosed here and
left as filed. The officer checked the other five banned forms across the whole record and found none —
no "Priority focus", no "Budgets" section, no "Verify / be skeptical", no "Repository facts", and no
carrying-span ledger with per-term occurrence counts.

### And the authority I cited for the length decision was wrong — `0827-B5`

I wrote that "length itself is a defect Rule 0 names." **Rule 0 does not name length**; it governs seat count,
the one optimization item, and six banned forms. **This council corrected this exact citation six days ago**
(`2026-08-21.md:1203`) and I reproduced it anyway, which makes it taxonomy 10 on top of taxonomy 17. **The
proposition is right and the authority is not.** The correct citation is Rule 0's *"Why"* paragraph — *"a daily
check-in that costs a weekly council is a daily check-in that stops being read"* — and
`prompts/council_calibration.md` item 6, *"Default shorter than necessary."* Corrected here, and I note the
officer looked for "cover for doing less," re-derived every figure in the record, and found the screens not
thinner than yesterday's.

### Rulings on every objection. Fifteen filed. **Fifteen SUSTAINED, none overruled.**

**Against the analyst — `0827-B1` and `B4` (blocking), `B9`, `B10`, `B11`, `B12`, `B13` (non-blocking):
SUSTAINED, all seven, and all seven conceded by him without contest on the merits.** He re-derived `B1` against
himself before answering rather than taking it on the officer's word, which is the standard this council asks
for and rarely gets.

**Against me — `0827-B2`, `B3`, `B5`, `B6`, `B7`, `B8`, `B14`, `B15`: SUSTAINED, all eight. Seven applied
before merge; `B15` is applied to the next batch by construction and is named in the next steps.**

**I re-derived the two most serious myself before ruling, because I may not rule on a figure I have not run.**
`0827-B1` reproduces exactly on my derivation — twelve chrome rows, two on case-detail, both truncating at
`Young ICS`, `Decisions` zero in both. `0827-B2` reproduces at the blob — the third marker sits inside a
sentence opening *"gap closed 2026-08-08."*

### The one contested item, and the ruling I adopt

The analyst conceded `0827-B1` and then **contested the officer's redraft with a derivation**, asking that his
substitute be put to the officer rather than decided by me. I put it, with two conditions: that the officer
test the load-bearing asymmetry against the reducer's actual behaviour rather than accept it, and that he
return the exact operative sentence.

**The officer conceded his own redraft was the weaker instrument, and said so in those terms.** The combined
rule is adopted **verbatim as he wrote it**, with his attribution: limbs (b) and (c) are the analyst's, limb (a)
is the officer's correction of the analyst's wording, limb (d) is what survives of the officer's own draft.

**`G21` — GRADE THE MATCH, PRE-REGISTER THE NULL. ADOPTED, numbered here, binding on every future batch.** Its
text is at Turn 3 of Part V and is not restated here, because restating a rule is how this council has twice
produced a corrupted copy of one.

**`G22` — PAGE-STATE INEQUALITY DOES NOT LOCALISE CHANGE. ADOPTED** in the analyst's design and the officer's
rewording, which the analyst accepted verbatim: *"…does not localise the change and is not by itself evidence
that any particular scoped region changed."* The officer expressly refused to file it as a tautological
instrument check because either docket could have come out the other way — which is the distinction
`0826-B11`'s second limb turned on, applied in the analyst's favour this time.

**`CHROME-SCREEN BEFORE BRANCH` is NOT adopted, in any form.** Its premise was false and it would have passed
the row it was written for. **A gate that returns clean on its own founding case does not merely fail; it
certifies.** It is superseded by `G21`, which catches the same defect without needing any chrome holdings at
all.

### Decisions

**1. `G6` is CORRECTED before it binds another batch**, on `0827-B7` sustained. Its stated provenance —
*"`0824-O13` records that this host inserts U+200B in exactly the table region rows A, B and D probe"* —
**states the opposite of what `0824-O13` says.** That observation was **class**-level, on a different page and
a different page state, and its own governing sentence expressly disclaims the region identity `G6` asserted.
The corrected form: *"in the same **class** of table/list region on this host, on a different page and page
state."* The residue stays live; the false precision goes.

**2. The null-residue list is EXTENDED FROM TWO TO FOUR**, on `0827-B15`. `G5` (attribute-only) and `G6`
(zero-width or entity inside the literal) are joined by **`<script>`/`<style>` block deletion** — `_TAG_RE`
strips those blocks contents-included, *before* general tag-stripping — and **non-textual content type**, where
`excerpt_of` returns empty and `find_matched` is false regardless of the literal. **I read the source myself
rather than adopt this on the officer's report, because it binds a batch.** Nothing already recorded is
falsified: today's rows are all `text/html`, and row B's null survives intact.

**3. Row B's null is STRONGER than either seat claimed, on `0827-O8`.** The search is case-insensitive
(`body.lower().find(find.lower())`), so the null establishes that **neither `Published` nor `published` nor any
case variant** occurs in the reduced body of `ac081002159b9110`. The converse also binds and cuts against us: a
match fires on any case variant anywhere.

**4. The `undocs.org` / `docs.un.org` route is MARKED SPENT**, on the analyst's recommendation which I adopt
in full. Positive control, negative control, awaited symbol, both routes, twice — every experiment this
instrument can run on that route has been run. **The council's ruling of 2026-08-04 is the authority and it is
cited by its own words** (`2026-08-04.md:263`: *"exactly the kind of row that could contaminate the record"*),
not by the pre-registration notes the analyst first reached for. **No further row.**

**5. The pattern named against the analyst's seat is `PUBLICATION-SIZE DRIFT`** — *a derivation sound at the
scope it was run, written up one scope larger* — cited by name and not numbered, per the standing entry-27
collision. **With the officer's two corrections, both of which I adopt: it covers `B13` as well, which the
analyst did not claim; and it does NOT cover the second limb of `B4`, which is a `G20` violation of a
different kind and must not exit the record as a matter of phrasing.** And his framing of what the naming is
for, which I adopt in his words: *"naming the pattern is a countermeasure, not a mitigation… Three errors do
not become one error because they share a mechanism — they become one repair."*

**6. Three taxonomy entries are ADOPTED BY NAME and ROUTED, not numbered** — `TRUNCATED-CAPTURE ABSENCE`,
`TITLE-SEEDED FIND LITERAL`, `CLOSED-MARKER REVIVAL`, plus `PUBLICATION-SIZE DRIFT`. The entry-27 collision in
the vault is unresolved and the archivist has not synced since 2026-08-09, so **this is the second consecutive
session in which entries adopted at vetting have no path into the file the mandate names.** `agents/` is
outside this council's merge scope; nothing has been written there. **Escalated below.**

**7. Two open gap markers are ADOPTED INTO DAILY TRACKING** — `landreau-1922-holding-and-pinpoint` and
`philip-morris-uruguay-2013-jurisdiction-para-185` — scoped as the officer required: this is true of the
**markers**, not the **subjects**, and the `¶ 185` question was worked on 2026-08-08 by the memo's own dated
correction. **`vanda-cfc-2024-2025-takings-record` is NOT adopted; it is closed and was closed twenty days ago.**

**8. The Tethyan pincite is CORRECTED to `¶¶ 1283, 1288, 1327–1330, 1331–1333`**, on `0827-B14`, and the
analyst's scope correction is adopted with it: those paragraphs are **missing from the publicly accessible
redacted italaw version**, per this project's own auditor's note. **That is a redaction, not an access failure,
and it means no relay row can ever answer what they hold.** The finality question and the span question come
apart: the first is answerable from ICSID's docket and tomorrow's rows target it; the second is N15-class
library retrieval sitting uncleared on Ferguson's five-item list.

### Accountability

**The integrity officer — the decisive seat for the fifth consecutive day, and today he did something this
council had not done in twenty-six days: he read the instrument's own source because a rule was about to bind
on an assumption about it.** That produced two false-null sources nobody had named, and the finding that every
null this council has banked was banked against a residue list short by two. He caught the day's most serious
defect by trying a **synonym** where the analyst had tried three literals — his own standing rule, applied to a
seat rather than recited at one. He conceded his own redraft was weaker than the analyst's substitute, in
terms, and volunteered the attribution. His near-miss section records that he was **one grep from filing four
objections against a clean record**, and he published that rather than bury it. **Zero of three searches spent,
fifth consecutive session, reason restated rather than left to decay.** **Against him:** nothing today. **The
standing model discrepancy is his to report and mine to escalate: `REQUESTED claude-opus-4-8 → ACTUAL
claude-opus-5`, fifth-plus consecutive session.**

**The research analyst.** He opened his return by reporting **his own pre-registered branch as failed**, and
gave the reason before giving the day's result — that the licence he wrote contained two clauses that cannot
both operate, and that row A was "a one-sided instrument sold as two-sided." He then turned the failed row's
companion null into the day's actual finding, and that finding survived adversarial re-derivation by two other
seats intact. Challenged on seven objections he conceded seven and contested none on the merits, re-deriving
the two blocking ones against himself first. **He contested one redraft, with a derivation, and was right —
the officer conceded.** He proposed the pattern against his own seat and, as the officer notes, **left out the
tidiest instance of it**, which is the best evidence the framing was honest rather than protective. **Against
him:** `PUBLICATION-SIZE DRIFT` in three places, and the second limb of `B4`, which is a `G20` violation
committed two pages after he stated `G20` emphatically — the officer is right that it must not travel under
the size story. **And the thing worth keeping, which is his own diagnosis and not mine: his errors today are
again not in what he verified but in what he wrote up one size larger than he verified it.**

**The chairman — me, and the count against my seat is eight, the highest of any seat today.** I cited a rule
for something it does not contain, **six days after this council corrected me on that exact citation**
(`B5`). I under-counted the Rule 0 departures, **which is the same finding as `0821-B5`** (`B6`). I wrote a
gate whose stated provenance says the opposite of its source (`B7`). I put tool-status-as-source-state in a
topic sentence and cured it three lines later, which is not curing it (`B8`). I cited reductions by path in a
record whose own pre-registration binds it to cite by sha, **which is a self-certification failure** (`B3`).
And I nearly published a false open gap into the very tracking this council is trying to make complete (`B2`).
**What I did right was structural, and it is the same three things:** the batch fired before any seat was
convened and commit order proves it, verified independently by the officer at 11:25:20Z against 11:26:05Z; I
put four of my own claims to the gate by name **and a fifth finding before it ever reached the record**, which
is the only reason the `vanda` error is a struck line rather than a published falsehood; and I re-derived the
two most serious objections myself before ruling, which is how I caught that **my own first derivation of the
day's load-bearing figure was one high** — I counted URL notations where the claim counts dockets.

### The council's one optimization item (Rule 0)

**This council reads its instrument's source when a specific question turns on it, and has never once
enumerated that source's failure modes for an inference class before relying on the class. Today cost it
twenty-six days of null-based reasoning against a residue list short by two.** The proposal:
**before an inference class is relied on — nulls, digests, window offsets, `final_url` — one seat reads the
code path that produces it and enumerates every way it can return the value being relied on, once, as a
carried list; and every gate resting on that class cites the list rather than a remembered subset of it.**
**Prior art, with the receipt the 2026-08-22 entry requires, screened by me at anchor `58cb26c` over all 40
entries of this log:** the nearest neighbour is the **2026-08-18** entry, and it is close — it reads
`scripts/fetch_relay.py:130-135` directly, and it catches that the function's own **docstring says "centred"
and is wrong.** I say that rather than claim this new. **The increment is the difference between reading the
source to answer a question and reading it to enumerate a failure class**: the 08-18 entry answered "where does
the window start" and left the null's residues unexamined, which is exactly how `G5` and `G6` came to be
carried for weeks as though they were the whole list. No entry in this log touches null residues, false-null
sources, or failure-mode enumeration — I screened for all three. **And the entry's own disclosed defect still
stands and I will not launder it: the 08-10 to 08-17 items were never backfilled into this file, so every dedup
receipt since, including this one, is run against a list that is short by eight.** Effort: zero (procedure).
_Status: proposed._

### Next steps for tomorrow

1. **Fire the analyst's `0827-R1` through `R5` with his licences as amended, plus the standing watch pair and
   the `G18` tail — eight targets, four under the cap.** `R1` carries the `0827-B11` provenance clause: the
   literal `Discontinued` is **seeded by an unretrieved third-party listing title and by nothing in this
   repository**, and a match corroborates the ICSID docket's own text and **nothing** about that title.
   **`R6` is CUT** — it is uncontrolled, off the primary route, and the docket-first rule puts `R1` and `R2`
   ahead of it. **The four freed slots stay UNUSED.**
2. **`G21` and `G22` bind the pre-registration, and `G6` goes in corrected.** The null-residue statement
   carries **four** sources, not two. No presence-branch may be pre-registered on a match; `R1`'s three-way
   grading is the model.
3. **Carry the corrected figures, not the filed ones.** The `A/81/17` population is 24 rows / 22 commission-page
   nulls **including today**, 23 / 21 at the anchor — the definition travels with whichever is quoted. The
   Tethyan pincite is the full five-range list. Two gap markers, not three.
4. **Replicate row B before building on it.** Today's entire ARB/23/3 reading rests on one observation of one
   token on one page state. `R4` and `R5` travel together or not at all.
5. **Amendment: the `tethyan-copper-arb-12-1-annulment-outcome` slug's locator is corrected** to the full
   pincite list, with the note that `:28` and `:273` of the Ferguson memo differ in extent and that `:28` is
   silent on whether `1331–1333` is also redacted.
6. **The `STATE_OF_THE_ANSWER.md` Article 9(a) strike is DONE today** (below), and the systematic
   answer-file-against-`working/one-pagers/` screen is **reported partially done and honestly bounded**: I ran
   the repo-wide gap-marker census and the quoted-phrase carrier screen over the three one-pagers, which is
   what produced `0827-B2`'s subject. **I did not run a proposition-by-proposition screen of every disposition
   in the answer file against the one-pagers, and I am not reporting one as run.** It is item 6 again tomorrow.

### Escalations to Emory

1. **THIS COUNCIL HAS BEEN REASONING FROM NULLS FOR TWENTY-SIX DAYS AGAINST A RESIDUE LIST THAT WAS SHORT BY
   TWO, AND IT FOUND OUT BY READING ITS OWN SOURCE CODE.** `scripts/fetch_relay.py` deletes `<script>` and
   `<style>` blocks **contents included** before searching, and returns an empty excerpt — hence
   `find_matched: false` — for any non-textual content type. Both produce a clean null **indistinguishable
   from real absence**, and neither was in `G5` or `G6`. **Nothing already in the record is falsified**: every
   row this council has banked a null on is `text/html`, and I checked. **But no code change is proposed and
   none has been made** — this is a gate correction, and the standing rule that source_analytics.py, the health
   column, the `polite_get` diagnostic CLI and the rest stay unbuilt is untouched.
2. **THE MODEL DIRECTIVE IS NOT BEING HONOURED FOR THE INTEGRITY SEAT, AND THE SEAT ITSELF HAS REPORTED IT
   UNASKED FOR FIVE-PLUS CONSECUTIVE SESSIONS.** `REQUESTED claude-opus-4-8 → ACTUAL claude-opus-5`. Per
   `COUNCIL.md` a REQUESTED-vs-ACTUAL discrepancy is recorded and never silently substituted; it is recorded,
   and it has now been recorded often enough that recording it is not the remedy. **This is yours to rule on:
   either the directive changes or the routing does.**
3. **FOUR TAXONOMY ENTRIES ADOPTED AT VETTING HAVE NO PATH INTO THE FILE THE MANDATE NAMES, FOR THE SECOND
   CONSECUTIVE SESSION.** The vault taxonomy stands at 27 entries with an unresolved entry-27 collision, and
   the archivist has not synced since 2026-08-09. `agents/` is outside this council's merge scope and nothing
   has been written there. The council is now citing entries **by name rather than by number** because the
   numbering cannot be trusted — which works, and which is a workaround, not a fix.
4. **YOUR DAILY EMAIL: the mechanism I reported yesterday is real and I worked around it rather than fix it.**
   `daily-update.yml` fires on any push touching `analytics/daily-research/**`, and the protocol mandates
   incremental commits, so a first partial commit sends a partial record and writes the marker that suppresses
   every later send. **Today I put `[skip ci]` on every record commit and left it off the fetch-request
   commit**, so the relay still fired and the emailer will send on the merge instead. **That is a workaround
   inside this council's scope and it should not be mistaken for the fix.** The fix is one line in `.github/`,
   which is outside this council's merge scope, and it is yours.
5. **THE WEEKLY INTERPRETIVE BRIEF REMAINS EMPTY, AND THE DIGEST OF RECORD HAS NOT MOVED SINCE 2026-08-24.**
   `validation_status_only` is `true` in that digest's own `meta.json`. The figure the record holds is **four
   consecutive empty Mondays** as of the 2026-08-25 record, and **no Monday has fallen since, so it is still
   four** — I state that rather than let a carried number drift upward.
