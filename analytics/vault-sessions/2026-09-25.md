# Archivist session — 2026-09-25

**Audited against `b577683`** (`main`), on a complete **1,598-commit** history. The container's
clone arrived shallow at **127** — the **eleventh consecutive** session — so every sha below was
resolved only after `git fetch --unshallow`.

**The window is `1cf6108..b577683` — 67 commits**, three days.

**The short version.** For the third consecutive session **nothing this seat audits for contract
drift moved**: zero commits to `.claude/agents/`, `prompts/` or `src/models.py`, zero to `scripts/`,
`tests/`, `.github/` or `src/`, and zero to every professor-facing surface. The entire window is
records. (Screened rather than asserted: the 2026-09-19 window was also zero on both sets, so this
is the **second** such window, not the first.)

**But the taxonomy half produced the sharpest finding this seat has filed, and it cuts both ways at
once.**

**The good half, and it is genuinely new.** The taxonomy entries this seat filed on 2026-09-22 were
**read off the table by the integrity officer on 2026-09-23** and described back exactly — including
extension `40a`, whose absence that same seat had *measured* against that same file the day before.
For the first time in this vault's record, a filing of mine is demonstrably in a bound seat's hands
on the next sitting.

**The bad half, and it reached your inbox.** A rule adopted **for the chairman on 2026-09-07** was
applied correctly by him on **2026-09-23** — and contradicted by him on **2026-09-25**, in
escalation 13, which asks you for a decision on the strength of a claim that is false. I have
verified the underlying fact three ways and corrected it. **That is §1, and it is the one thing in
this record you should read.**

**Carried state, all re-measured by execution rather than restated.** D20 is at day eleven with the
drift unchanged — which is what last session's stated growth law predicts, so I record it as a
prediction that survived. D16 produced six more stranded branches, all harmless, and I can now
account for the mechanism from both ends. D18 is 1 of 3 days red.

**Nothing new needs you that is not already on the escalation table.** What is new is that one item
on your table should be **re-priced downward** and one should be **re-read**.

---

## 1. NEEDS YOU — escalation 13 asks you to decide something on a false premise, and the machinery it asks you to build already exists and already ran

This is filed as **D22** in [[Workflow Threads]], and written into [[council-chairman]] twice — once
as a binding line where that seat reads its discipline, once as a dated change-log entry.

**The rule, which is eighteen days old and names the chairman first.** `agents/Agent Registry.md:505`:

> *A `check_currency.py` "FAIL … is not a commit" is a statement about the clone, not about the
> note* — binding from 2026-09-07. *No seat may cite that guard's output as a defect in the note it
> names without first running `git rev-parse --is-shallow-repository`.*

It was adopted over three shas exactly: `373cce6`, `9efafb0`, `ae42639`.

**The chairman demonstrated the rule working two days before he broke it.** On 2026-09-23 he ran the
probe, got `true`, and wrote the conclusion out in full — *"All three are false, and the cause is
this session class"*, `git rev-list --count HEAD` returning **117** against **1,541**, *"Ninety-two
percent of the history was absent and nothing in the session announced it"*
(`analytics/daily-research/2026-09-23.md:1689-1694`, `64dead2`).

**On 2026-09-25 he wrote the opposite, twice, and escalated it to you as item 13.** That
`agents/Workflow Threads.md` *"cites `ae42639`, which is not a commit in this repository"*, and that
this is *"pre-existing and unfixable by any re-anchor"* (`:1036`, `68f1987`; repeated at `:1152`,
`6ef0688`). **The token `shallow` occurs 0 times in that record** — 0 occurrences, case-insensitive,
across 1,232 lines / 123,802 bytes; `unshallow` and `is-shallow-repository` are 0 as well.

**What is actually true, measured here on complete history.**

| Claim in escalation 13 | Measured at the head it names (`73283fd`) |
| --- | --- |
| `ae42639` "is not a commit in this repository at all" | **It is.** `2026-08-06`, *feat(guard): check_sources.py*, and an ancestor of `HEAD`. So are `373cce6` and `9efafb0`. |
| "fails 5 of its 9 claims" | **3 failed** — and all three are `STALE`. **Zero hard `FAIL`s.** |
| "four of them the `agents/` anchors I deliberately did not move" | **Two.** `Claim Map` and `Workflow Threads`. `Agent Registry` and `Project Change Log` were current. |
| "one a pre-existing failure citing `ae42639` … which no re-anchor of mine would fix" | **`ae42639` produced no failure at all.** |

**And the remedy the escalation asks you to authorise already exists, already ran, and passed on
that very merge.** The `currency` check-run was moved into `.github/workflows/reanchor.yml` behind
`needs: reanchor` on 2026-09-10, for the reason its own header gives: *"Every council merge went red
on `currency` and green again seconds later, and a red that is routinely false is a red nobody
reads"* (`:36-41`). On the chairman's own merge, **`reanchor` run 146 at `0098a80` ran both jobs:
`reanchor` success — it committed `b577683`, moving all five anchors including the four he reverted —
and `currency` success.** Independently: `check_currency.py` returns **0 failed** at both `c46fc11`
and `b577683`. **On `main`, the guard was never red for that merge.**

**What survives, stated so this correction does not overreach.** The **scope conflict is real**:
`prompts/daily_council_protocol.md` does direct the close-out to commit five tracked notes, four of
which sit outside the daily session's merge scope, and the chairman did revert them rather than
quietly widen his diff — which is the correct call on scope. And the narrow exposure he quotes from
the workflow's own header — *a session that merges without waiting for checks merges before the
re-anchor commit lands* — is correctly stated. **Only the consequence he priced it at was wrong.**

> **NEEDS YOU — escalation 13 is a smaller decision than it was filed as.** You are not choosing
> between widening the daily session's merge scope and building new CI ordering; **the ordering
> exists and works.** What is left is whether the transient window between a merge and the
> re-anchor commit matters to you. **I price it as cosmetic** — the only reader harmed is a human
> looking at the CI list, because `pipeline-guards` never re-runs on the healed head (the re-anchor
> commit carries `[skip ci]`), so its last word on `main` is always the pre-heal state. **That is
> the inverse of D21 and it is why this escalation was priced as it was.** One line from you settles
> it either way, and it is a much cheaper line than the one you were asked for.

> **FOR EMORY — the routing question underneath it, which is D5 and is the one that keeps paying
> out.** `.claude/agents/council-chairman.md` does not name `agents/council-chairman.md`. Eight of
> nine definitions have that gap. **The integrity officer's is the sole exception — and the officer
> is the one seat observed this window opening its own note and reading a table off it rather than
> reciting it.** That contrast is now evidence rather than a hypothesis, and it is a one-line
> contract change per seat. It is yours because editing `.claude/agents/` is a contract change.

---

## 2. Agent-context audit — no contract drift anywhere, and the model gap is at day forty-one

- `git log 1cf6108..b577683 -- .claude/agents/ prompts/ src/models.py` returns **zero** commits
  across the 67-commit window. **No seat's contract was edited by anyone**, third consecutive
  session.
- All nine definitions declare `model: opus`. `scripts/check_models.py` exits **0** over 12 cards;
  `scripts/check_currency.py` exits **0** over 9 claims; `node tools/isds-workflow-3d/validate.mjs`
  exits **0** — 30 cards, 10 chips, 44 edges, SVG fresh against its inputs-sha256, zero "Jack"
  tokens.
- **"Jack" discipline:** professor-facing surfaces (`README.md`, `docs/`, `scripts/site_templates/`,
  `METHODOLOGY.md`, `COUNCIL.md`, `HANDOFF.md`) are **clean — 0 occurrences.** The two known live
  defects are unchanged and both outside my paths: `HUMAN_REVIEW.md:115` and
  `prompts/council_roundtable.txt:73`. (`HUMAN_REVIEW.md:173` is your signature as reviewer and is
  correct as written.)
- **Seven adopted method rules were not written where the seats bound by them would read them, and
  now are** — three sittings' worth, **D15 at occurrences fourteen through twenty**:

| Adopted | Rule | Source |
| --- | --- | --- |
| 2026-09-23 | **Before any seat writes that a channel is unavailable, it names the instrument and probes THAT instrument.** *"'Fetch is dead' is a fact about `curl` … It is not a fact about retrieval."* | `2026-09-23.md:1821` (`e05811d`) |
| 2026-09-23 | **Every proposed pre-registration row carries the officer's §4(2) test answered** — pre-registered outcome that would change a council decision, and a falsifier the council would act on — **or it is not proposed.** | `:1934`, `:1937` (`dbaa2c8`) |
| 2026-09-24 | **The register-window class test redrawn, the chairman's own wording overturned** — two complete register triples for different cases, the literal inside a case-number parenthetical, a terminating boundary on one side. | `2026-09-24.md:1083` (`5fa964d`) |
| 2026-09-24 | **The anchor-quote rule** — a memo proposing an instrument parameter carries the carrying span inline, by file and `records[i]`, in the sentence making the proposal. | `:1088` (`5fa964d`) |
| 2026-09-24 | **Six elements are the general form for every suspension** in `state/research_log.json`, because suspension and lapse are otherwise indistinguishable in the artefacts. | `:1168` (`5fa964d`) |
| 2026-09-25 | **Hand-off by path plus sha256 is compliance, not a shortcut**, provided the chairman verifies the hash on both sides and commits the file himself. | `2026-09-25.md:644-646` (`1ff7078`) |
| 2026-09-25 | **A reporting-only length line in every close-out.** No ceiling, no gate, no exemptions; revisit a ceiling only after five sessions if the series has not bent. | `:722` (`1ff7078`) |

**One caveat entered against the newest of those rules, in the registry row itself.** The length
report's **first entry** gives `analytics/daily-research/2026-09-25.md` as **118,390 bytes**; the
file on `main` is **123,802**, because the session appended its post-merge addendum after writing
the line. **A 4.4% understatement on the rule's first application.** Not escalated — the rule is one
sitting old and the fix is a sentence about *when* in the close-out the line is measured. Owner:
council-chairman.

> **TO VERIFY — D6, ninth first-person observation, day forty-one.** I queried this session's own
> runtime: `configured_model`, `session_context.model` and `external_metadata.last_served_model` are
> all **`claude-opus-5`**. `.claude/agents/obsidian-archivist.md:3` and `:24` say "Runs on Claude
> Opus 4.8." **REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`.** Consistent with 2026-08-16,
> 08-25, 09-04, 09-07, 09-13, 09-16, 09-19 and 09-22. `check_models.py` cannot see this and is not
> failing — it compares three declarations to each other, never a runtime. **I have again left the
> rows unchanged**, because they record your directive and rewriting them would ratify a
> substitution nobody authorised.

**The description split, re-enumerated from the files rather than carried** — the discipline of
taxonomy entry 40, which D6 owes because its whole argument is a count:

| | Count | Seats |
| --- | --- | --- |
| Description says **Opus 5** | **4** | council-chairman, research-analyst, site-experience, systems-designer |
| Description says **Opus 4.8** | **5** | analytics-officer, integrity-officer, obsidian-archivist, research-editor, systems-researcher |
| Names no model | **0** | — |

**4 / 5 / 0** — identical to 2026-09-19 and 2026-09-22, which is what a zero-commit window should
produce and is why I state it as a measurement rather than as "unchanged".

**D5 unchanged at day forty-three**, and §1 is now its most expensive instance.

---

## 3. Where a note would have prevented a recorded failure — recorded in that seat's note, as the mandate requires

Written into [[council-chairman]] in two places, deliberately:

- **A binding line in its Discipline highlights**, where the seat reads its practice rules, opening
  *"BINDING SINCE 2026-09-07, AND BROKEN BY THIS SEAT ON 2026-09-25 — read this before citing any
  guard output as a defect."*
- **A dated change-log entry** carrying the full measurement: the rule, the seat applying it
  correctly on 09-23, the seat contradicting it on 09-25, the archivist's three-way verification,
  the `reanchor` run 146 evidence, and an explicit statement of **what of escalation 13 stands.**

That is a precise, bounded, verifiable statement of what the gap cost, and it is written where the
next seat to open that file will see it — which is the half that has been missing.

---

## 4. Deliverable drift — the surface did not move at all

`git log 1cf6108..b577683 -- README.md docs/ scripts/site_templates/ METHODOLOGY.md HANDOFF.md
COUNCIL.md views/isds-workflow-3d/workflow.json` returns **zero** commits. Nothing was passed over.

**D7 re-derived from the code today rather than restated, and it is unchanged:**

> **FOR EMORY — `METHODOLOGY.md` §III, two numbers.**
> 1. `METHODOLOGY.md:33` says Bing News is polled through **eight** fixed, fingerprint-derived
>    queries. `len(bing_news.QUERIES)` is **12**.
> 2. The same sentence enumerates seven open repositories plus two added channels — **nine** —
>    while `len(all_sources())` is **10**.
>
> **Where GDELT is named today:** `README.md` **1**, `docs/` **10 files**,
> `scripts/site_templates/` **1**, `METHODOLOGY.md` **0**. Every professor-facing surface names the
> tenth source except the one document a reviewer reads to judge the method. **The count did not
> widen this window because no weekly digest fell inside it; the next one is due 2026-09-28.** It is
> a two-number edit and it is yours — this seat does not edit your prose. Tracked as **D7**.

**Escalated, not touched:** the flowchart manifest is the systems designer's; `README.md` and
`scripts/site_templates/` are site-experience's; `METHODOLOGY.md` is yours. Nothing in this change
set is outside `agents/` and `analytics/`.

---

## 5. Vault mechanics — clean, and clean the first time

**Graph:** measured on the tree before this record was written — **198 notes, 409 edges, 0
orphans**, 88 managed blocks would update, **17 WARNs**. Growth since the 2026-09-22 record's own
post-write measurement (194 / 402): **+4 notes, +7 edges**. All 17 WARNs are index or session notes
exceeding the four-link cap, expected under the standing convention; none is drift.

**Broken wiki-link targets: four, across three files, all under `think-tank/`, all long-standing,
all outside my paths.** Unchanged from 2026-09-22, 09-19 and 09-16.

**No broken link was introduced by this change set.** I ran `build_graph.py --dry-run` after **each**
of the six note edits rather than once at the end, and confirmed after each that both managed-block
markers survived in every file I touched. Node and edge counts did not move, because every wikilink
I wrote points at a note that already existed.

**The thread-ID collision is unchanged: three pairs.** `grep -o '^### [A-Z][0-9]*' | sort | uniq -d`
still returns **B5, C11, C12**. Not renumbered, same disposition and same reason. Your ruling of
2026-09-13 on the taxonomy is the template this needs.

**C8 is still real in the code and still not firing.** `scripts/build_graph.py:198` remains
`pat.sub(block, text)` with no count argument. The managed blocks in this vault are regenerable and
were not hand-edited in this change set.

---

## 6. D18 — 1 of 3 days red, and this window's red was the random kind

`main` is **green** as I write: `check_currency.py` exits **0** at `b577683`.

Same method as the last four sessions — `main`'s tip at the end of each UTC day, every tracked
note's anchor evaluated against it, in a detached worktree on complete history.

| Day | Tip | Verdict | What staled it |
| --- | --- | --- | --- |
| 09-23 | `dab5b51` | green | tip was a re-anchor |
| 09-24 | `6f8614a` | **RED** | `chore: scholar intake 2026-09-24 [skip ci]` |
| 09-25 | `b577683` | green | tip was a re-anchor |

The one red carries **exactly one** `STALE` line and it is [[Workflow Threads]] — *"1 commit(s)
touched its declared paths since `465ce4e`"* — the other four tracked notes clean. **Fifth
consecutive session in which every red resolves to that one note's `analytics` declaration meeting a
`[skip ci]` bot writer.** Last window's red was the **weekly digest**, so *due*; this one is the
**scholar-intake** writer, so chance. **Both kinds are now observed, which is the distinction the
2026-09-22 record asked to be held open rather than collapsed into a rate.**

**The fix I am still NOT proposing**, restated: narrowing this note's declared paths so its own guard
stops going red would be this seat marking its own homework, which is the precise failure the guard
exists to prevent. The two real options are unchanged and both need pricing I cannot do — drop
`[skip ci]` from the bot writers, or give `reanchor.yml` a scheduled run after the day's last
automated writer. **Still systems-designer's.**

---

## 7. D20 — day eleven, and the drift is unchanged, which is the point

`pipeline-guards` run **246**, head `0098a80`, 2026-09-25 13:22Z. Job `suite` **failure**; job
`guards` **success in all 14 steps** — read from the run's own job record through the Actions API,
not inferred.

**Re-measured against the live archive by execution, not restated:**

| Assertion | Pinned in `tests/` | Live archive |
| --- | --- | --- |
| `runs` | 16 | **18** |
| `screened` | 492 | **520** |
| digests carrying `per_source` | 11 | **13** |
| `per_source` sum | 191 | **219** |
| their `screened` | 230 | **258** |

**Every figure is identical to 2026-09-22.** The 2026-09-22 record stated a growth law — *the drift
grows by one digest every Monday, because the weekly digest writer changes the published figures the
constants are pinned against* — and **this window contains no Monday** (last digest 2026-09-21, next
due 2026-09-28). **A zero delta is what that law predicts, so I record it as a prediction that
survived a window rather than as "unchanged".** `digests/` holds **18** directories, which is exactly
the actual value the test scores against its pinned 16; the two unpinned digests are 2026-09-14 (7)
and 2026-09-21 (21), and 7 + 21 = 28 = 520 − 492.

**What eleven days has cost, recounted.** `git log 529e32d0..origin/main --grep='(#[0-9]'` now
returns **29** merged pull requests, **#192 through #219**. **Three days ago it was 20; six days ago
12.** Nine more went in against a red base this window, none gated by the suite.

> **NEEDS YOU — same ask as 2026-09-16, 09-19 and 09-22; only the price has changed.**
> **systems-designer or site-experience:** re-measure the five constants in one change set —
> `tests/test_site_claims.py:155-156` and `:305-308`. **Emory:** the second-order question is still
> the one that matters — the weekly digest is a scheduled writer that changes published figures
> *and* carries `[skip ci]`, so the commit that breaks the suite is the one commit the suite never
> runs on. Either those commits run the suite, or the constants are derived by the job that writes
> the digest. **I price neither**; `tests/`, `scripts/` and `.github/` are all outside this seat's
> paths. The chairman filed the same two asks today as his escalations 5 and 6.

---

## 8. Orphan sweep — six more, and the mechanism is now traced from both ends

**190 remote refs** (excluding `main`); **44 are not ancestors of `origin/main`**, up from 38. The
repository squash-merges some pull requests and true-merges others, so a tip's ancestry alone proves
nothing — **I classified by diff content against each branch's own merge-base.**

**Six are new since 2026-09-22, and all six are the D16 mechanism:**

| Branch | Tip | Diff against its merge-base |
| --- | --- | --- |
| `council/daily-2026-09-23-addendum` | `0cf5249` | 5 files, 5 insertions, 5 deletions — anchor shas only |
| `council/daily-2026-09-24-addendum` | `67057ae` | identical shape |
| `council/daily-2026-09-25` | `798565d` | identical shape |
| `council/daily-2026-09-25-addendum` | `d9030dd` | identical shape |
| `vault/archivist-2026-09-22` | `83bc860` | identical shape |
| `vault/archivist-2026-09-22-guard` | `7f87e0e` | identical shape |

**No substantive work is stranded in any of the six.**

**What is new this session is the causal account, and it makes D16 cheaper than it has read.** These
six are `reanchor.yml`'s commit to the **PR head**, losing a race to a merge that did not wait for
it. `c46fc11` and `b577683` are the same workflow's commit to **`main`**, winning it — and both were
measured this session at `check_currency.py` **0 failed**. **So D16's stranded branches and the
transient redness behind escalation 13 are one mechanism seen from two ends, and the stranded side
is the harmless one.**

The carried set, none of it moved:

| Branch / PR | Tip | What is stranded | Age |
| --- | --- | --- | --- |
| **PR #184** `fix/repoint-r21-citations` | `529c744` | Ruling 1's citation half, 409 insertions — §9 | **12 days** |
| `council/analyst-gaps-and-methodology` | `74958fd` | 9 files, 26 insertions; outside my paths | 14 days |
| **PR #150** `chore/reanchor-2026-09-08` | `f73ac06` | anchor edits only | **17 days** |
| **PR #110** `vault/archivist-2026-08-28` | `de73f9f` | five note edits; the 350-line record is safely on `main` | **28 days** |
| `chore/operator-marks-2026-07-27` (**F1**) | `6f9e1da` | 20 operator ledger marks | **60 days** |
| `feat/research-council` | `af00584` | `METHODOLOGY.md` + 8 June digests | 95 days |
| `claude/sweet-mccarthy-*` (×3) | — | 07-30/07-31/08-01 council records | 55–57 days |
| the rest | — | stranded anchor edits and `.sent` markers only | — |

**PR #110 is at day twenty-eight** and I again did not cherry-pick it, for the reason seven prior
seats gave and which still holds: those are live statements about a repository that has moved far
past them, and re-deriving beats importing. **F1 is at day sixty.**

**Branch deletion still fails from this container — fifth consecutive session.**

---

## 9. PR #184 — day twelve, and the guard it carries is still dormant while the condition it guards is true

Read from the API this session. PR **#184** is open at **day twelve**, head unchanged at `529c744`,
last updated 2026-09-13T22:49Z — **nothing pushed and nothing said on it.** `src/rings.py:923` and
`scripts/check_lock.py:20` both still carry the bare *"R2.1 record"* citation on `main`, and the
record they should point to — `analytics/locked_set/VALIDATION_RECORD.md` — **is** on `main`.

**So the branch's strict-xfail forcing function — written so it fails the build the moment the cited
record can be opened — has now been dormant for twelve days while exactly that condition is true.**

The chairman listed it again on 2026-09-25 as escalation 9, *"hygiene, not a finding"*, alongside
#150 (day 17) and #110 (day 28). That is right about the council's own exposure and still does not
reach the dormant guard, which is why the thread stays open.

> **NEEDS YOU — land PR #184, or close it and say which of those two citations stands as written.**
> Every path in it is outside this seat's merge scope, so I cannot land it and did not try. Tracked
> as **D19**.

---

## 10. What I could NOT verify

Stated rather than omitted.

- **I ran `check_currency.py` only AFTER unshallowing, so I cannot report this session's
  arrival-state failures.** The last ten sessions recorded three hard `FAIL`s on arrival
  (`373cce6`, `9efafb0`, `ae42639`) clearing to zero after `git fetch --unshallow`. I unshallowed
  first out of habit and so lost that datapoint for 2026-09-25. **Saying so rather than implying a
  continuous series**, and noting that the hazard itself is thoroughly evidenced by §1, which is the
  same artefact observed in another seat's session.
- **Why the clone keeps arriving shallow** — eleventh consecutive session, now at **127** commits
  against a real 1,598. I can show it happens; I cannot see the container provisioning.
- **`gh` is still not installed in this container**, for the **fifth** session running. My standing
  instructions say to close with `gh pr create` and `gh pr merge`; I used the GitHub MCP tools
  instead, which reach the same API. **Flagging it a fifth time so the instruction and the
  environment get reconciled rather than silently diverging** — the instruction is not wrong about
  what should happen, only about how.
- **I did not run the test suite locally this session.** The 2026-09-19 record established that it
  takes four rounds of dependency installation and that a third failure
  (`tests/test_one_pagers.py::test_missing_pdf_skips_case`) is a container artefact from gitignored
  `seeds/`. **I measured the five drifted constants directly against the archive instead**, which is
  the number D20 actually turns on, and read the failure from CI run 246's own job record.
- **I did no external Obsidian/PKM research this session and am not dressing anything up as it.** My
  mandate asks for it *periodically*; the 2026-09-16 session did a bounded ecosystem scan whose
  finding was that nothing needs changing, and nine days is not long enough for that to have
  changed. The session's time went to §1 and to re-measuring the carried table by execution.
- **A correction against my own first draft, recorded because the shortcut that produced it is the
  one taxonomy 28 exists to catch.** I wrote that this was the first window with zero commits to
  every professor-facing surface. **Screening `agents/obsidian-archivist.md` falsified it**: the
  2026-09-19 entry records the same, and I re-measured that window by execution
  (`git rev-list --count 98dcff9..f1a7850` over the deliverable and code sets — both **0**) rather
  than trusting its prose. **This is the second such window, and both notes now say so.**
- **Whether D11 fires on this pull request** — I cannot know before pushing. If it does, the addendum
  is below; if there is no addendum, it did not.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D22 — escalation 13 asks you to decide on a false premise.** `ae42639` IS a commit; the head it names fails 3 not 5, all `STALE`, zero hard `FAIL`s; and the CI ordering it asks for exists and passed on that merge (`reanchor` run 146). **The scope conflict is real; its priced consequence is not.** | **Emory** (one cheap line, not the one asked for) | 0 days |
| 2 | **D5** — eight of nine definitions never name their own vault note; §1 is its most expensive instance, and the one exception is the one seat observed reading its note this window | **Emory** (contract change) | 43 days |
| 3 | **D20** — `main` red at day eleven; **29** merged PRs (#192–#219) ungated, up from 20; five constants need re-measuring; drift unchanged this window exactly as the stated growth law predicts | systems-designer / site-experience; **Emory** for the writer question | 9 days |
| 4 | **D19** — PR #184 at day twelve; `main` still carries two dangling "R2.1 record" citations, and the branch's forcing guard is dormant while its condition is true | **Emory** | 9 days |
| 5 | **D15** — seven adopted rules from three sittings unfiled until today; occurrences fourteen to twenty. **Against it: the first hard evidence that filing works** (officer read the 09-22 table on 09-23) | **Emory** (routing or cadence) | 18 days |
| 6 | **D18** — 1 of 3 days red; both the *due* and the *chance* kind of red are now observed | systems-designer / **Emory** | 12 days |
| 7 | **D7** — `METHODOLOGY.md` §III: eight queries against 12; nine sources against 10; still the only surface not naming GDELT. Did not widen this window; next digest due 2026-09-28 | **Emory** | carried |
| 8 | **D16** — six new stranded-anchor branches, all harmless; **mechanism now traced from both ends**; branch deletion fails from this container for a fifth session | **Emory** (one repository setting) | carried |
| 9 | **PR #110** — 2026-08-28 session's five note edits still stranded | **Emory** | 28 days |
| 10 | **Thread-ID collision** — three pairs (B5, C11, C12), unchanged; your 2026-09-13 taxonomy ruling is the template | **Emory** to authorise; archivist to execute | 21 days |
| 11 | **D6** — seat models: requested Opus 4.8, served `claude-opus-5`; ninth first-person observation. Split re-measured at **4 / 5 / 0** | **Emory** | 41 days |
| 12 | **F1** — operator-marks branch never merged (`6f9e1da`), 20 ledger marks | **Emory** | 60 days |
| 13 | **D21** — `check_currency.py`'s PR-citation regex reads a minority of this history's merge subjects; latent | systems-designer | 3 days |
| 14 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |

---

## Length of this record — kept because I filed the rule, and it caught me first

**30,417 bytes**, against the 2026-09-22 record's **28,278** and the 2026-09-19 record's **30,746**.
`os.path.getsize`, so the unit is bytes and locale-independent.

**And the caveat I entered against the chairman at §2 fired on me within the hour.** My first draft
of this line asserted 26,214 bytes — a figure I had estimated rather than measured, in the same
paragraph as a finding about an unmeasured figure. **A self-describing count is self-invalidating:
writing it changes the file it measures.** I resolved it by iterating `os.path.getsize` against the
line until the value stopped moving — one iteration, because the two figures happened to be the same
width — and **that is the fix the chairman's rule needs stated in it: measure at the last write, or
converge.** Recorded against myself because I would have escalated it against anyone else.
