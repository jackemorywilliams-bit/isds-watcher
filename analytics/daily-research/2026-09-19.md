# ISDS Thematic Watcher — daily council meeting, 2026-09-19

**Convened by:** the council chairman, running as the operator's standing scheduled cloud
session. **Seats convened:** research analyst, integrity officer. **Model directive:**
`claude-opus-5` for the chair and the analyst, `opus` (alias) for the officer, per the
committed definitions in `.claude/agents/`.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

Every convened seat returns its contribution as text. **Member text enters this record
verbatim, under a heading naming the seat, and I do not edit it.** Where a member's
sentence is later superseded by a ruling, the sentence stays on the page and does not
travel; the substitute form is filed separately. My own superseded sentences are annotated
**in place**, per the `0917-B8` ruling, because the sentence a later seat will cite must
carry its own correction. If a spawn fails, the seat is recorded as PROCEDURAL FAILURE and
**I do not perform it** — that is the operator's cardinal rule.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

Two probes, both on neutral hosts, both fired before anything else in this session:

1. **`curl https://example.com/`** → `curl: (56) CONNECT tunnel failed, response 403`.
   A second identical result for `https://icanhazip.com/`. The agent proxy's own status
   endpoint logs both under `recentRelayFailures` as `connect_rejected`, *"gateway answered
   403 to CONNECT (policy denial or upstream failure)"*, at **2026-09-19T11:09:47.636Z**
   (`example.com:443`) and **2026-09-19T11:09:47.865Z** (`icanhazip.com:443`).
2. **The fetch service** on the same neutral control returned
   `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com
   is blocked by the network egress proxy."}`.

**This session has no fetch capability of its own.** That is **not a state change** — the
posture is unchanged for every session in this record, and no ordinal is asserted (the
`0918-N13` correction stands and I will not re-commit the error it corrected). **It is a
fact about this container and NOT a fact about any resource on the internet**, and no
sentence in this record may be read the other way: the standing
tool-status-as-source-state rule.

**The relay is alive and was available all session.** `.github/workflows/fetch-relay.yml`
fires on a push touching `analytics/fetch-requests/**` and answers through the project's
own `polite_get`. It answered ten of ten rows yesterday with the control passing
(`analytics/fetch-results/2026-09-18-daily.json`). **Nothing today is stranded for want of
a channel.** Where something is out of reach today it is because the host is not on
`scripts/fetch_relay.py`'s `ALLOWED_HOSTS`, which is a different and narrower fact, and I
say which one applies wherever it matters.

### 1.3 I fired no session-open batch, and the reason is my predecessor's own next steps

Yesterday's close-out retired the two instrument questions that have driven every batch for
three weeks. Next step 1: *"THE FIELD-TYPE QUESTION IS ANSWERED WELL ENOUGH TO STOP WORKING
ON IT."* Escalation 3: the enumeration question is *"not dischargeable"* and *"not
load-bearing"* — *"three weeks and at least twenty-four relay rows have gone into a question
whose answer would change nothing."* The five rows the analyst left queued at Part 4 §5 of
that record (ARB/26/37, ARB/26/4, ARB/20/13) all serve the retired line. **I decline to fire
them.** A queued row is not a mandate, and firing a batch to keep a channel warm is the
padding this council's anti-inflation rule exists to stop.

**The criterion I gave the analyst instead, and the only one under which a row gets fired
today: propose a row only where BOTH a hit and a null would move something this council
actually relies on.** That is a deliberately high bar and I expect it to return zero rows
on a quiet day. Zero is an acceptable answer; a batch fired for form is not.

I also decline, for the avoidance of doubt, to re-read `ARB/22/2` (Huawei v. Sweden, the
standing award watch). It was read yesterday — `Pending`, `Latest Development: July 10,
2025` — and a second read twenty-four hours later on a docket that has not moved since
July 2025 has close to zero expected information. The watch is not retired; its cadence is
weekly-to-opportunistic and it was last discharged on 2026-09-18.

### 1.4 What I asked the analyst to work on

**Priority.** The Ring-2 object this council banked yesterday is the thinnest thing in the
ledger and it sits directly on the trade-secret / clinical-data sub-question. The final line
of `analytics/insights.jsonl` records that an EU legislative package concerning regulatory
data protection has been agreed and that **its direction, its instruments and any ISDS nexus
are all uncarried**. That is an honest gap and it is the one worth the day's four search
calls.

**The constraint on it, stated before the work rather than after.** No EU host —
`eur-lex.europa.eu`, `europa.eu`, `ema.europa.eu` — is on the relay's `ALLOWED_HOSTS`
(`scripts/fetch_relay.py:48–69`). There is therefore **no channel in this session by which
the instrument's own text can be read**, and yesterday's `0918-B6` was sustained on exactly
the failure that fills such a hole: model knowledge re-entering the record through a query
string and wearing a screen's authority. **Search titles cannot identify a legislative
instrument.** The honest deliverables are (a) whatever a body actually served and (b) a
properly-formed gap marker with the escalation that follows from it.

**Second.** The standing question the instrument exists to answer: any investor–State case
at the intersection of IP as a covered investment, a regulatory or judicial measure as the
challenged conduct, and a live jurisdictional or admissibility doctrine. Four search calls,
queries recorded verbatim, findings tied back to a screened item or to the question.

**Off the agenda today, and I name them so no seat spends a minute on them:** the field-type
question; the enumeration route at any ICSID locator; the `Subject of Dispute` value-set;
`K ≈ 130` in any form other than as an illustration under its stated conditions.

### 1.5 The standing prohibitions, carried unreleased

Carried from 2026-09-18 §7.5 item 8, unchanged, and **a live relay releases none of them —
only an operator ruling does:** the Ecuador slug at zero budget with its paywalled-body
synthesis quarantined; `dailyjus.com` and `unis.unvienna.org` at zero budget; the Einarsson
family retired; A/81/17 at zero budget; the thread closed 2026-09-08 closed; all three
Spentech synthesis passes QUARANTINED; the monitored-author check at zero search budget; the
Vadi citation never cited without retrieval; no thread referred to by array index; the
ARB/24/16 **docket** off the agenda, its committed field value admissible as a datum only
and never in a sentence whose subject is the proceeding. The 2026-08-25 asymmetric licence
stays carried unreleased, with the risk to its *"never the claimant's pleading"* limb
recorded at yesterday's escalation 2 and still unanswered.

Nothing at OPERATOR APPROVAL PENDING may be built or applied today: the
`source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the
IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, and any
ledger amendment.

### 1.6 The digest of record

`Digest 2026-09-14: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list
leads).` Copied from `digests/2026-09-14_ISDS-Thematic-Watch/meta.json` (`screened: 7`,
`matches: 0`, `watch_list_leads: 0`). **Unchanged from yesterday's record** — the weekly run
of 2026-09-14 is still the digest of record and no new digest has been produced since; the
next weekly run falls on Monday 2026-09-21. `meta.json` carries a standing health warning:
`gdelt` is DEGRADED at four consecutive zero-item runs, so zero items from that source is
**not** evidence of a quiet week. `italaw` and `unctad_isds` are both recorded as RECOVERED
(Internet Archive) rather than served live, and `iareporter_headlines` as HEADLINE-ONLY.

---
