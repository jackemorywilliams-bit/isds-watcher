# ISDS Thematic Watcher — daily council meeting, 2026-09-22

**Chair:** council chairman (this session, `claude-opus-5` per the operator's model directive of
2026-08-03). **Convened seats:** `research-analyst` and `integrity-officer` — REAL subagents spawned
with the Agent tool against their committed definitions in `.claude/agents/`. No seat is performed.
Where a member's text appears it is filed VERBATIM and is marked as such.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

Each member is spawned with the Agent tool against its committed definition in `.claude/agents/`, is
given its own bounded task list, and returns a finished memo as its final message. I paste that memo
into this record unaltered, inside a fenced block, under the seat's own heading. **I write no
member's contribution.** If a spawn fails or a member does not return, that seat is recorded as
PROCEDURAL FAILURE for the day and the failure is stated both in my close-out and in my final
summary — it is never covered by my writing the part myself. Members do not commit; they hand me
file bodies and I commit them, so every artefact in the day's diff has one author of record.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

I inherited no channel status. The convening instruction requires one re-probe rather than a
re-derivation, and that is what I ran. **Both probes were fired at neutral controls, not at research
hosts,** so that nothing in this section can be misread as a statement about a source. This is the
standing tool-status-as-source-state rule and it binds the whole record.

| Probe | Target (neutral control) | Result |
|---|---|---|
| `curl` egress | `https://example.com` | `curl: (56) CONNECT tunnel failed, response 403` |
| `curl` egress | `https://www.iana.org/help/example-domains` | `curl: (56) CONNECT tunnel failed, response 403` |
| fetch service | `https://example.com` | `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by the network egress proxy."}` |

The proxy's own status endpoint corroborates the first two at the gateway rather than at the client:
it logged both as `connect_rejected`, *"gateway answered 403 to CONNECT (policy denial or upstream
failure)"*, at `2026-09-22T11:10:51Z`. **No state change.** Direct fetch is dead on both channels for
this session class, as it has been since 2026-08-02. I say plainly what that does and does not mean:
it means *this session* cannot reach those hosts. It means nothing whatever about whether any host is
serving anything.

**The web-search track is alive and the committed relay is alive.** Retrieval therefore is *not*
stranded today, and no item may be declared stranded on fetch-layer grounds:
`.github/workflows/fetch-relay.yml` fires on any push touching `analytics/fetch-requests/**`, runs
`scripts/fetch_relay.py` on a GitHub runner that does have egress, and commits a reduction —
status, final URL, content-type, byte length, sha256, timestamp, user-agent, and one excerpt capped
at 400 characters — back to `analytics/fetch-results/`. The document itself never travels; the
repository is public and a commit is publication. That path is inside this session's merge scope.

### 1.3 Duplicate guard

`analytics/daily-research/2026-09-22.md` did not exist on `origin/main` at session open — I checked
with `git ls-tree --name-only origin/main analytics/daily-research/`, which returned twelve records
ending at `2026-09-21.md` and none for today. No manual session had convened. This is the day's only
meeting and it is not a duplicate.

### 1.4 What this session inherits, and the agenda it sets

**The digest of record changed overnight, and that is the first substantive change in this slot for
eight days.** Yesterday's council met at 11:12 UTC on a Monday, before `weekly.yml` fires at 13:00
UTC, so it necessarily worked from the 2026-09-14 digest. That run has since completed. Today's
figures are at §4.3, copied from `meta.json` per Rule 1; the short version a reader needs here is
that the collection run got considerably larger and surfaced nothing, which is a different fact from
a small run surfacing nothing and the analyst is directed at the difference.

Two gaps are carried open, both of them one question wearing two faces:

- `GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested` — whether **any** host on
  `ALLOWED_HOSTS` serves NAFTA Chapter Eleven in a content type from which `excerpt_of` can produce
  text at all. Closure condition as fixed at yesterday's §6.4: **one `ok` row with a non-empty
  excerpt at any allowlisted locator, and nothing else.**
- `GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread` — whether NAFTA Article 1110
  contains an IP-specific internal limitation at a paragraph (7). No spelling of it appears anywhere
  in the tracked repository.

**PRIORITY FOCUS.** The first gap is the one this council can actually move, and yesterday's batch
narrowed the space it lives in rather than closing it. Fourteen of fourteen non-PDF rows ever fired
at `www.italaw.com` have been refused; five of five PDF-suffixed rows have returned 200; and a PDF
cannot satisfy the closure condition because `excerpt_of` returns the empty string for every content
type that is neither html nor text. So the productive move is **not** another shape at italaw and
**not** a re-fire at `investmentpolicy.unctad.org` — both are prohibited by yesterday's close-out and
both prohibitions stand. It is to ask, for the first time, whether **any of the allowlisted hosts
nobody has yet tried for this** serves treaty text as HTML. The analyst is directed to identify such
candidates from the record and from bounded search, and to build the batch against them.

**LIVE THREADS.** (1) The allowlisted-host HTML question above. (2) The digest's own `source_health`
block, which this council has never read closely and which today reports two sources RECOVERED
through the Internet Archive and one DEGRADED at five consecutive zero runs — a fact about the
instrument's reach that bears directly on what "0 items surfaced" is evidence of. (3) Everything at
yesterday's §6.7(9) stays carried and unreleased.

**VERIFY / BE SKEPTICAL.** Three things specifically. A search engine's indexed title is the
engine's, not the document's — the whole `italaw6187` episode turned on that, and it ended with two
paths proving to be one byte-stream. A 403 is a fact about this relay at that path and is never a
fact about the resource. And every count in prose is computed in the session or deleted: procedures
(a), (b) and (c) adopted at yesterday's §6.7(4) bind both seats today, as do Amendment 2 without its
struck qualifier and extension 40a.

### 1.5 Budgets, set here and bounded

- **Research analyst:** at most **8** web-search calls; at most **9** relay rows including controls;
  one memo; then one reconvening to answer the officer's objections.
- **Integrity officer:** unbounded reading of the repository and of the day's artefacts; no search
  budget; objections binding on me only through my rulings at §6.1.
- **Relay:** one batch, one push. A second batch requires a reason written before it is fired.

### 1.6 Spend checkpoint (placed here, taken after the vetting round at §3)

The batch is **not** fired when the analyst proposes it. It is fired after the integrity officer has
vetted every row — host allowlisted, no constructed URL, pairing discipline satisfied, carried
prohibitions honoured, outcome pre-registered per class. This is the checkpoint and it is placed
before the spend, not after it.

### 1.7 Rule 0 departure, declared before the seats are convened

`prompts/daily_council_protocol.md` Rule 0 directs that exactly two agents be convened,
`research-analyst` and `council-chairman`, and that the integrity challenge live as a voice inside the
Part V dialogue rather than as a separately convened seat. The operator's convening instruction for
this session directs that council members be REAL subagents and that performing a member's voice is
this project's cardinal failure. **Those two cannot both be satisfied literally**, because Rule 0's
integrity voice would have to be written by me and the cardinal rule forbids exactly that. I resolve
it the way every session since 2026-08-25 has resolved it, and I state the resolution rather than
letting it pass silently: **the cardinal rule governs who writes, Rule 0 governs size and shape.** I
convene `research-analyst` and `integrity-officer` as real subagents and file both verbatim; I
convene no `analytics-officer` and no `systems-researcher`; the Rule 1 digest sentence at §4.3 is two
lines I copy from `meta.json` myself, which is a mechanical transcription and not a performed seat;
and the one optimization item at §6.6 is one paragraph written by me with no seat convened. This is
the **nineteenth consecutive session** carrying this departure, and it is a caveat at §7, not a
silent practice.

---

## 2. Research analyst — filed verbatim

Spawned with the Agent tool against `.claude/agents/research-analyst.md`. It returned a finished memo
as its final message. **Everything between the two rules below is the analyst's text, unaltered. I
wrote none of it** — it is piped into this file byte-for-byte from the seat's return rather than
retyped, so no transcription error of mine can enter it. It committed nothing and pushed nothing, as
the return-path protocol requires; the two file bodies it hands me at its §4 are proposed, not
written, and I deal with them at §4 below under my own hand. It spent 5 of the 8 search calls I
budgeted and fired no relay row.

---

# Research analyst — return for the daily meeting of 2026-09-22

Chairman — filed for verbatim entry at `analytics/daily-research/2026-09-22.md` §2. Nothing committed by me. Repository state read at `f32b005`, branch `council/daily-2026-09-22`. Every count below was computed in this session; none is restated from memory.

---

## 0. What I had, and what I spent

Read before starting: `.claude/agents/research-analyst.md`, `prompts/council_calibration.md`, `prompts/carrying_span_rule.md` in full, `STATE_OF_THE_ANSWER.md` (head and §IV), `state/research_log.json` `open_threads` (all five), `analytics/insights.jsonl` (202 lines), `analytics/daily-research/2026-09-21.md` §§6.3, 6.4, 6.7, 6.8, `analytics/fetch-requests/README.md` in full, `scripts/fetch_relay.py` in full.

Spent: **5 of 8 WebSearch calls. 0 relay rows fired** (I do not fire; the batch below is proposed for your checkpoint). Three search calls unspent, deliberately — I had the answer and adding calls would have been padding.

I did not attempt WebFetch or curl. I inherit your channel probe and do not re-derive it, and I state nothing about any research host on the strength of it.

**Quote-verification pass (procedure (a)), run before filing.** Every string appearing inside a `Q` mark or a `supporting_quote` below was pasted back into its named file at `f32b005` and counted. Occurrence counts, in order of appearance in this memo:

| string (truncated) | file | count |
|---|---|---|
| `Their fetchers likely no longer match the live site; zero items from them is NOT evidence of a quiet week.` | `digests/2026-09-21_ISDS-Thematic-Watch/meta.json` | 1 |
| `QUIET (page live; 20 releases listed, newest 2026-09-04; none in the run window)` | same | 1 |
| `DEGRADED (5 zero runs)` | same | 2 |
| `RECOVERED (Internet Archive)` | same | 2 |
| `HEADLINE-ONLY` | same | 1 |
| `"validation_status_only": true` | same | 1 |
| `"omitted_by_cap": 62` / `"omitted_by_cap": 28` | same | 1 / 1 |
| `"capture_age_days_max": 23` / `"capture_age_days_median": 3` | same | 1 / 1 |
| `    if status_only:\n        return []` | `src/main.py` | 1 |
| `stats["held_for_review"] = len(held)` | `src/main.py` | 1 |
| `held = [c for c in counterfactual if id(c) not in published_ids]` | `src/main.py` | 1 |
| `NOTHING publishes at item level` | `src/main.py` | 1 |
| `matches = stats.get("above_threshold", 0)` | `src/render.py` | 1 |
| `watch_list_leads = accepted - (matches - held_for_review)` | `src/render.py` | 1 |
| `on, NO item-level entry is published or emailed at all` | `src/config.py` | 1 |
| `NOT the age of ``oldest_capture`` — the oldest eligible capture was never` | `src/source_recovery.py` | 1 |
| `over the FETCHED items only` | `src/source_recovery.py` | 1 |
| `attempted and dropped, so ``fetched`` can be lower` | `src/source_recovery.py` | 1 |
| `the cap is politeness, not a filter` | `src/source_recovery.py` | 1 |
| `max_items: int = 12` | `src/source_recovery.py` | 1 |
| `if "html" not in content_type and "text" not in content_type:\n        return ""` | `scripts/fetch_relay.py` | 1 |
| `    if host not in ALLOWED_HOSTS:\n        return "host_not_allowed"` | `scripts/fetch_relay.py` | 1 |
| `It retrieves what the sources or the record already identified` | `analytics/fetch-requests/README.md` | 1 |
| `the neutral control `example.com` expressly` | `analytics/daily-research/2026-09-21.md` | 1 |
| `a `content_type` of `application/pdf` expressly insufficient however cleanly the row` | same | 1 |
| `DO NOT RE-FIRE AT `investmentpolicy.unctad.org`.` | same | 1 |
| `DO NOT FIRE ANOTHER `/investment-treaties/` ROW AT `www.italaw.com`` | same | 1 |

Zero strings failed. One string I had drafted — `a single `find:""` diagnostic` — returned **0** in `2026-09-21.md` and I struck it rather than quote it; the prohibition's actual wording is quoted below instead.

---

## 1. TASK 1 — the allowlisted-host HTML question

### 1.1 The question splits in two, and the first half is already answered — 189 times — in our own committed reductions

You asked whether any allowlisted host not yet tried serves treaty text, or NAFTA Chapter Eleven, as HTML. That is two questions, and this council has been carrying them as one.

**Limb A — can `excerpt_of` produce visible prose from an allowlisted host at all?** I censused every committed relay record: **71 result files, 559 records** at `f32b005`. Outcome by host:

| host | rows | outcomes | rows `ok` + html/text with a **non-empty** excerpt |
|---|---|---|---|
| `icsid.worldbank.org` | 251 | 250 ok, 1 refused | **189** |
| `example.com` | 136 | 136 ok | 136 |
| `uncitral.un.org` | 66 | 66 ok | 35 |
| `investmentpolicy.unctad.org` | 38 | 15 ok, 23 refused | 14 |
| `www.iisd.org` | 22 | 1 ok, 21 refused | 1 |
| `www.italaw.com` | 20 | 6 ok, 14 refused | 0 (all six are `application/pdf`) |
| `pca-cpa.org` | 11 | 11 ok | 9 |
| `undocs.org` | 8 | 8 ok | 3 |
| `digitallibrary.un.org` | 5 | 2 ok, 3 robots_disallowed | 2 |
| `icsidfiles.worldbank.org` | 2 | 2 ok | 0 (both `application/pdf`) |

**Eight allowlisted hosts have returned at least one `ok` row with html-or-text content type and a non-empty excerpt; seven of them are research sources rather than the control.** Limb A is not untested. It is answered abundantly and has been for weeks. The gap slug's phrase *visible-prose-untested* is therefore true only of the NAFTA-specific limb, and I say so plainly because a later seat reading the slug alone could reasonably think the instrument itself was in doubt. It is not.

**Limb B — does any allowlisted host serve NAFTA Chapter Eleven?** This is the whole of what is open, and it is the closure condition you fixed at §6.4: an `ok` row, html or text content type, non-empty excerpt, *"at a locator on `ALLOWED_HOSTS` that serves NAFTA Chapter Eleven"*, with *"the neutral control `example.com` expressly"* excluded and *"a `content_type` of `application/pdf` expressly insufficient however cleanly the row"* returns.

**The reading I act on, stated before any row is fired, as last seat did.** "Serves NAFTA Chapter Eleven" means a locator whose own visible prose carries the chapter's operative text. A page that *mentions* NAFTA Chapter Eleven — a PCA case description, an ICSID docket, a commentary — does not serve it. I will not close your gap on a mention, and I pre-register that so I cannot be tempted to later.

### 1.2 The host census: 19 allowlisted hosts, 10 ever fired, 9 never

`ALLOWED_HOSTS` at `scripts/fetch_relay.py` holds **19** entries. Ten appear in the 559 records (table above). **Nine have never had a single row fired at them, ever:**

`docs.un.org` · `iareporter.com` · `iisd.org` · `italaw.com` · `unctad.org` · `www.bing.com` · `www.iareporter.com` · `www.pca-cpa.org` · `www.un.org`

Four of those nine are the bare-or-`www` spelling of a host we *have* fired (`iisd.org`/`www.iisd.org`; `italaw.com`/`www.italaw.com`; `www.pca-cpa.org`/`pca-cpa.org`; and `unctad.org` beside `investmentpolicy.unctad.org`, which is a different registrable path entirely, not a spelling). **`validate()` tests `host not in ALLOWED_HOSTS` — exact string membership, not suffix** — so the two spellings are two hosts to this relay and nobody has ever checked whether they are two hosts to the origin. That is a cheap, never-asked question and I have put it in the batch.

**`www.bing.com` I name and decline.** It is on the allowlist for the `bing_news` fetcher. Using it to run queries would make the relay a discovery channel, which its own README forbids: *"It retrieves what the sources or the record already identified"* (`analytics/fetch-requests/README.md`). No row.

### 1.3 Five bounded searches — every row screened, counts computed

Artefact body for `analytics/search-results/2026-09-22.json` is at §4.1. Call log:

| call | `allowed_domains` passed | rows |
|---|---|---|
| S1 | `italaw.com, unctad.org, un.org, undocs.org, iisd.org, pca-cpa.org, worldbank.org, iareporter.com` | 10 |
| S2 | `un.org, undocs.org, worldbank.org, pca-cpa.org` | 10 |
| S3 | `icsid.worldbank.org, uncitral.un.org, pca-cpa.org, undocs.org, docs.un.org, digitallibrary.un.org` | 30 (one call; the engine returned three result blocks) |
| S4 | `pca-cpa.org` | 8 |
| S5 | `investmentpolicy.unctad.org` | 10 |

**Prohibited-host screen, executed as next step 3 of 2026-09-21 requires it.** No call passed `blocked_domains` (schema-excluded alongside `allowed_domains`, `insights.jsonl:199`); every call passed an `allowed_domains` list containing neither prohibited host. Screen of all returned rows — **unit: result rows**, string containment on the URL: **`dailyjus.com` = 0 rows. `unis.unvienna.org` = 0 rows.** Total rows screened: **68**.

**`ALLOWED_HOSTS` screen, unit: result rows, exact host match against `scripts/fetch_relay.py` at `f32b005`: 61 in set, 7 out of set.** All 7 out-of-set rows are at **`files.pca-cpa.org`** — the same subdomain leak `analytics/insights.jsonl:200` recorded yesterday on 1 row, reproduced today on 7. I do not re-log the mechanism; see §3.5.

**Content-type screen, unit: result rows, URL suffix `.pdf`: 52 of 68.** Distinct URLs across all rows: 54. Rows at `http://` rather than `https://`: 9 (those cannot be relayed at all — `validate()` returns `scheme_not_https`).

### 1.4 What Task 1 found. It is mostly negative, and the negative is the finding

**Non-PDF rows at an allowlisted host: 16, across exactly two hosts** — `pca-cpa.org` (5) and `investmentpolicy.unctad.org` (11). I read every one of the sixteen locators. **Not one of them is a page that serves the text of NAFTA Chapter Eleven.** Five are PCA case and news pages. Ten are UNCTAD Navigator index, country, mapping and news pages. The sixteenth is `treaty-files/2413/download`, a download endpoint the council already holds and has already been refused at.

**Every NAFTA-Chapter-Eleven text carrier that five bounded calls identified anywhere is a PDF.** The two best-provenanced are new to this council and both are at `files.pca-cpa.org`, filed as legal authorities in a PCA arbitration:

- `https://files.pca-cpa.org/pcadocs/bi-c/2.%20Canada/4.%20Legal%20Authorities/RA-47%20-%20NAFTA%20Preamble,%20Ch.1.,%202%20and%2011.pdf` — engine title *"North American Free Trade Agreement PREAMBLE Skip to Section >>"* (S2 r2, S3 r4, S3 r43).
- `https://files.pca-cpa.org/pcadocs/bi-c/1.%20Investors/4.%20Legal%20Authorities/CA237.pdf` — engine title *"ARTICLE 1103 Most-Favored-Nation Treatment 1."* (S4 r4).

These are **better provenanced than anything this thread has had**: not an engine's guess at a database file's contents, but a document a party filed and the PCA published as a numbered exhibit in a NAFTA Chapter Eleven arbitration, whose engine-indexed title is an article heading of the chapter itself. **And we cannot touch either.** I ran `validate()` on RA-47 in this session: it returns **`host_not_allowed`**, because `files.pca-cpa.org` is not in the set while `pca-cpa.org` and `www.pca-cpa.org` both are. I establish that from the code and spend no row on it.

Every caveat stands and I attach all of them: **an engine's indexed title is the engine's, not the document's**; no seat has read a byte of either file; nothing identifies either as NAFTA; and a `.pdf` suffix is not a content type.

**So the honest conclusion, and it is against my own interest because it means I am proposing a batch I do not expect to close your gap with:** after a full census of 559 relay records and five bounded calls of which three were restricted to allowlisted registrable domains, **this council has identified zero candidate locators capable of satisfying the §6.4 condition.** Not one non-PDF, allowlisted-host locator serving NAFTA Chapter Eleven text exists in our record or in what today's searches returned. I do not say none exists — the cross-BIT rule binds here as everywhere: **not found in accessible sources, never "does not exist."** But the council should know that the condition as fixed may have no satisfier reachable by this instrument, and should decide that deliberately rather than by firing rows at it indefinitely.

### 1.5 An error against myself, found by checking my own record before proposing a row

S5 returned `https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/3104/nafta-1992-` under the engine title *"NAFTA (1992) | International Investment Agreements Navigator | UNCTAD Investment Policy Hub"*. I drafted it as today's headline finding: the first allowlisted **HTML** locator whose title names NAFTA and a year.

**It is not a finding. It is already in our record, it was already fired, and it was already refused — twice.** `analytics/fetch-results/2026-09-20-daily.json` carries two rows at that exact URL, `find: ""` and `find: "Chapter Eleven"`, both `refused`, status `403`, `content_type` null, `bytes` null, on a run with `control_ok: true` and `void: false`. The string `3104` occurs in `analytics/daily-research/2026-09-20.md` **21 times**. My search re-found, at the cost of one call, a locator the council had disposed of two days earlier. **That is the value of the record-first check and I nearly skipped it.** Recorded as a method failure at §3.7.

### 1.6 The batch — 8 requested rows, plus the runner's prepended control = 9 total

Budget read conservatively: the runner prepends `https://example.com/` itself (`scripts/fetch_relay.py`, `records = [fetch_one(CONTROL_URL)]`), so **8 requested + 1 prepended = 9 rows total**, exactly your cap. All eight pass `scripts.fetch_relay.validate()` — I ran it in this session; all eight returned `None`. 8 + 1 = 9 ≤ `MAX_URLS` 12, no truncation.

Full body at §4.2. Rows:

| # | URL | `find` | group |
|---|---|---|---|
| 1 | `https://pca-cpa.org/en/cases/50/` | `""` | A — companion control |
| 2 | `https://pca-cpa.org/en/cases/50/` | `North American Free Trade Agreement` | A |
| 3 | `https://pca-cpa.org/en/cases/50/` | `Chapter Eleven` | A |
| 4 | `https://www.pca-cpa.org/en/cases/50/` | `""` | B — host-spelling axis |
| 5 | `https://www.iisd.org/system/files/publications/nafta.pdf` | `""` | C — path-shape axis |
| 6 | `https://iisd.org/system/files/publications/nafta.pdf` | `""` | C — host-spelling axis |
| 7 | `https://investmentpolicy.unctad.org/international-investment-agreements/treaty-files/2412/download` | `""` | D — the one permitted UNCTAD row |
| 8 | `https://example.com/` | `Example Domain` | closing bracket |

**Provenance of every URL, because I expect to be checked on it.** Rows 1–4: `https://pca-cpa.org/en/cases/50/` was returned by S4 result 1, engine title *"Bilcon of Delaware et al v. Government of Canada"*. Rows 5–6: `https://www.iisd.org/system/files/publications/nafta.pdf` was returned by S1 result 2, engine title *"NAFTA's Chapter 11 and the Environment"*. Row 7: the record. Row 8: the runner's own control URL. **No URL in this batch was constructed by slug analogy, pattern, or guess.** Rows 4 and 6 change only the host spelling of a URL a source supplied, and I flag that as the one place a reviewer should push back if it reads as construction to you; my position is that a host-spelling variant of a supplied URL is a diagnostic of our own allowlist, not a discovery of a new resource.

**Compliance with your three standing prohibitions, checked row by row.**
- *"DO NOT FIRE ANOTHER `/investment-treaties/` ROW AT `www.italaw.com`"* — **no row in this batch is at `www.italaw.com` or `italaw.com` at all.**
- *"DO NOT RE-FIRE AT `investmentpolicy.unctad.org`."* as a batch — **exactly one UNCTAD row, `treaty-files/2412`, `find: ""`**, which is the single permitted diagnostic.
- The Grosse Ruse-Khan closure's *no twelfth row* — **rows 5 and 6 are not a twelfth row.** The eleven rows in that thread are all at `/itn/2014/11/18/litigating-intellectual-property-rights-in-investor-state-arbitration-from-plain-packaging-to-patent-revocation/` in both spellings; I censused `www.iisd.org`'s 22 records this session and **all 21 refusals are `/itn/` paths**. `/system/files/publications/` is a different path shape and a different document, and I say so expressly so the officer can check it rather than infer it.

**Companion-control discipline (README, binding).** Rows 2 and 3 are `find` rows that can null; their companion on the same URL in the same run is row 1. Rows 4, 5, 6 and 7 carry `find: ""` and so are self-companioning — there is no find to miss. Row 8 is the closing bracket, distinct from the prepended control: the prepended row proves reachability, row 8 proves the find path executed. **If row 8 is `ok` with `find_matched` false, every empty excerpt in this batch is uninterpretable and the batch is void on that axis whatever else it says.** `control_ok: false` voids every row, including the ones that look like answers.

### 1.7 PRE-REGISTRATION — written before any row is fired, per outcome class, and capable of being wrong

**The headline prediction, stated first and against my own interest: THIS BATCH WILL NOT CLOSE `nafta-chapter-11-allowlisted-host-visible-prose-untested`. No row in it can.** Rows 1–4 are at a case page, which under my §1.1 reading does not serve the chapter. Rows 5–6 are PDF-suffixed and `excerpt_of` returns `""` for any content type that is neither html nor text. Row 7 is a download endpoint, same bar. Row 8 is the control. **I am proposing a batch that cannot win the thing I was sent to win, and the reason is §1.4: no winning row exists to propose.** If you would rather I proposed nothing, that is a defensible instruction and I will take it; I think eight rows that settle four live questions beat zero rows.

**Row-level predictions, per outcome class.**

*Rows 1–3 (`pca-cpa.org/en/cases/50/`).* `pca-cpa.org` is 11 of 11 `ok`, 9 with non-empty html excerpts, across three prior paths — but **all three prior paths are `/en/cases/309/`, `/en/cases/336/` and one `/en/news/…` page; `/en/cases/50/` has never been fired.** **I EXPECT ROWS 1–3 TO RETURN `ok`, HTTP 200, `text/html`, WITH NON-EMPTY EXCERPTS ON ALL THREE.**
- *`ok` + html + non-empty on row 1 and hits on 2 and 3:* establishes that this relay obtains visible prose naming NAFTA and Chapter Eleven at an allowlisted host — the nearest approach to the gap ever achieved. **It does NOT close the gap**, and I will say so on the day. It licenses no statement about what NAFTA Article 1110 contains.
- *`ok` + html + non-empty on row 1, null on 2 or 3:* the page answers but does not carry those strings in visible prose. That is informative about this page and nothing else; it says nothing about PCA, about NAFTA, or about any other page.
- *`refused`:* a tool-layer outcome. It would falsify my prediction and tell us `pca-cpa.org`'s 11-for-11 record is path-discriminating like italaw's (`insights.jsonl:73`). **It would say nothing about what PCA serves a browser**, and no seat may write that it does.
- *`no_contact` / `robots_disallowed`:* neither is an origin answer; `robots_disallowed` is our own politeness layer, not the host's.

*Row 4 (`www.pca-cpa.org`, same path).* **I EXPECT `ok`** — most likely a redirect whose `final_url` drops the `www.`, which the reduction records. Against row 1 this is a clean two-cell experiment.
- *Both `ok`:* the spelling is immaterial at this origin, and the four never-fired `www`/bare twins on our allowlist are probably four redundant entries rather than four untested hosts.
- *Row 1 `ok`, row 4 `refused` or `no_contact`:* **the spelling is material**, and that is a genuinely new instrument fact with direct bearing on `italaw.com` (never fired) sitting beside `www.italaw.com` (14 of 14 non-PDF refused). That branch is the one I would most like to be wrong on, because it would be worth more than the branch I predict.

*Rows 5–6 (IISD `/system/files/publications/nafta.pdf`).* `www.iisd.org` is 21 refusals and 1 `ok`, and **every refusal is an `/itn/` path.** **I EXPECT ROW 5 TO RETURN `ok`, HTTP 200, `application/pdf`, WITH AN EMPTY EXCERPT, AND ROW 6 TO MATCH IT.**
- *`ok` + `application/pdf`:* **cannot close the gap — pre-registered now, not argued afterwards.** What it would establish: IISD's refusal regime is path-scoped, not host-wide, which narrows what the Grosse Ruse-Khan closure can be read to mean *without reopening it* (that closure is as to that article's body, and stands). And it hands the operator a second, independently-hosted NAFTA Chapter 11 document — though an IISD commentary, **not treaty text**, and therefore not an answer to Article 1110(7).
- *`refused` on both:* the refusal regime is host-wide at this relay, and the Grosse Ruse-Khan eleven were never about `/itn/` in particular. Also informative.
- *Row 5 `ok`, row 6 `refused` (or the reverse):* the spelling is material at a second origin — with row 4, that would make it a pattern rather than a quirk.

*Row 7 (UNCTAD `treaty-files/2412/download`).* **I EXPECT `refused`, status 403.** The host has refused **23 of 23 rows from 2026-08-10 onward**, across five path shapes, while its 15 `ok` rows are all dated **2026-08-09 or earlier** — a regime unmoved for 43 days.
- *`refused`:* the regime holds at day 44. Cheap, and it keeps the number honest.
- *`ok` + `application/pdf` + empty excerpt:* **this is the outcome I am actually buying, and it still does not close the gap.** What it would buy is forward-looking and concrete: **if UNCTAD answers again, then `treaties/bit/3104/nafta-1992-` — an HTML locator, already identified, refused only because of this regime — becomes a fireable candidate tomorrow, and it is the only identified candidate that could ever satisfy §6.4.** That is the whole reason I took the permitted UNCTAD slot when yesterday's seat rightly declined it. The facts changed: yesterday the slot bought a re-test; today it buys a gate on the one path to closure.
- *`ok` + html:* would falsify my content-type expectation for that endpoint, and would be worth logging in its own right.

*Row 8.* `ok`, 200, `text/html`, 559 bytes, `find_matched: true`. Anything else voids the batch's find axis.

**Predictions I could be graded wrong on, listed so nobody has to hunt for them:** rows 1–3 all `ok` with three non-empty excerpts; row 4 `ok`; row 5 `ok`/`application/pdf`; row 6 matching row 5; row 7 `refused`/403. **Six falsifiable calls. If four or more fall the other way, my model of this relay is wrong and I want that said in the close-out.**

---

## 2. TASK 2 — the digest's `source_health` block

Digest of record: `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`. Produced after yesterday's council adjourned — `.github/workflows/weekly.yml` fires `0 13 * * 1` and 2026-09-21 was a Monday (computed in session), so yesterday's §4.3 correctly took the 2026-09-14 digest as its record and today's has moved. Every figure below is copied from the file or computed from it in this session.

**Headline figures, copied:** `screened` **21**, `matches` **0**, `held_for_review` **0**, `watch_list_leads` **0**, `accepted` **0**, `reserved_excluded` **0**, `triage_calls` **21**, `validation_status_only` **true**.

**Computed in session:** `per_source` sums to **21**, exactly equal to `screened` — so **0** candidates were rebuilt from the deferred queue this run (contrast 2026-09-07's 30 − 14 = 16, the closed `per-source-count-semantics` thread). `source_health[].count` sums to **32**. The two columns differ on **3 of 10** sources — italaw 7 vs 12, `iareporter_headlines` 11 vs 12, `unctad_isds` 0 vs 5 — and `count ≥ per_source` holds on **10 of 10**, as the closed thread's rule requires. I cite that closure rather than re-deriving it.

### What "0 items surfaced" IS evidence of

**`matches: 0` is a real measurement and it is the one number in this file that speaks to the research question.** `src/render.py` sets `matches = stats.get("above_threshold", 0)` — the count of classified candidates scoring at or above the threshold. It is **not** a function of the publication gate. So: **of the 21 candidates the instrument actually judged, none scored at or above threshold.**

**`held_for_review: 0` is also a real measurement, and it is the corroborating one.** `src/main.py` computes `held = [c for c in counterfactual if id(c) not in published_ids]` against a counterfactual built by `would_surface(...)` with the gate bypassed, then `stats["held_for_review"] = len(held)`. With the gate on, `published_ids` is empty, so `held_for_review` is the full count of what the score-and-fill rule *would* have published. Zero means **nothing cleared it even counterfactually.** The two numbers agree and they were produced by different code paths.

**`icsid`'s zero is a live read.** Its status string is `QUIET (page live; 20 releases listed, newest 2026-09-04; none in the run window)`. That zero is a negative observation of a page that answered, with the newest item on it **17 days** before the run (computed). It is the only zero in the file that means what a reader would assume a zero means.

### What "0 items surfaced" is NOT evidence of

**(1) `accepted: 0` and `watch_list_leads: 0` are tautologies under this run's configuration, and no reader could tell from the file.** `validation_status_only` is `true`. `src/main.py`'s `select_surfaced` reads `if status_only: return []` — and the docstring says why: *"NOTHING publishes at item level"*; `src/config.py` says *"on, NO item-level entry is published or emailed at all"*. `accepted = len(items)`, and `items` derives from that empty list, so `accepted` is **0 by construction**. `watch_list_leads = accepted - (matches - held_for_review)` = 0 − (0 − 0) = **0 by arithmetic**. **Two of the four zeros in the headline are gate output, not observation.** The phrase "0 items surfaced (0 matches, 0 watch-list leads)", which this council has copied into its records for weeks, presents a measured zero and a determined zero side by side as if they were the same kind of thing. **They are not, and the distinction should be carried whenever that sentence is written.** I could not find this recorded anywhere: `validation_status_only` returns 0 lines in `analytics/insights.jsonl`, as do `above_threshold`, `held_for_review`, `watch_list_leads`, `select_surfaced` and `would_surface`.

**(2) `gdelt`'s zero is not evidence of anything, and the file says so in its own words.** Status `DEGRADED (5 zero runs)`, `count` 0, one refusal at HTTP **429**. `health_warnings[0]`: *"Their fetchers likely no longer match the live site; zero items from them is NOT evidence of a quiet week."* A 429 is rate limiting — a tool-layer outcome. It is not an origin statement that the world was quiet.

**(3) `iisd_itn` reports `RETURNED` with `count: 0` and a 403.** The status word and the refusal disagree in tone. This is a zero attached to a refusal, and a refusal has no body to screen.

**(4) The two `RECOVERED (Internet Archive)` sources did not read the live web at all, and 90 items they could have read were never fetched.** Computed from the file:

| | italaw | unctad_isds |
|---|---|---|
| `eligible` | 74 | 40 |
| `fetched` | 12 | 5 |
| `omitted_by_cap` | 62 | 28 |
| `eligible − omitted_by_cap` | 12 | 12 |
| drop count (attempted, not produced) | **0** | **7** |

**62 + 28 = 90 eligible captures were never fetched this run.** `src/source_recovery.py` sets `max_items: int = 12` and the docstring calls the cap *"politeness, not a filter"* — the omitted items are deferred, not lost. But a reader of "0 items surfaced" is reading a screen of **17** archive items when the Archive offered **114**. The unctad row's 7-item shortfall is not an arithmetic error in the file: the docstring says items *"attempted and dropped, so `fetched` can be lower"* than `eligible − omitted_by_cap`, so 7 snapshots were fetched and discarded as refusals or boilerplate titles.

**(5) The capture-age figures do not mean what they look like, and the code says so deliberately.** italaw: `oldest_capture` 20260810 (**42 days** before the run, computed), `newest_capture` 20260918 (**3 days**), but `capture_age_days_max` **12** and median **3**. unctad: `oldest_capture` 20260807 (**45 days**), `newest_capture` 20260829 (**23 days**), `capture_age_days_max` **23**, median **23**. The docstring: the ages run *"over the FETCHED items only"*, and *"NOT the age of `oldest_capture` — the oldest eligible capture was never fetched."* **The practical consequence for a reader of this digest: everything UNCTAD published in the 23 days to 2026-09-21 was outside what this run could see, and the run reports no gap where that gap is.**

**(6) `iareporter_headlines` is `HEADLINE-ONLY` at count 12.** A body-level match cannot be detected in a headline. Its contribution to `matches: 0` is a statement about 12 titles.

**The summary I would give a reader with no context.** Of ten sources, **one** (`icsid`) produced a zero that is a live negative observation. Two produced zeros attached to refusals (`iisd_itn` 403, `gdelt` 429), one of which the file itself disclaims. Two produced their items from archive snapshots up to 23 days stale with 90 further items left unread. One produced headlines only. And two of the four headline zeros are configuration output rather than measurement. **What the file establishes is narrow and real: of the 21 candidates the classifier judged, none scored at or above threshold and none would have published. What it does not establish is that the week was quiet.**

---

## 3. TASK 3 — substance

### 3.1 What today establishes, and what it does not

**Against the research question — the intersection of IP (including trade secrets and clinical-trial data) as a covered investment, a regulatory or judicial measure as the disputed conduct, and a live litigated jurisdictional or admissibility doctrine — today establishes nothing. THE ANSWER DOES NOT MOVE.** No claim in `STATE_OF_THE_ANSWER.md` §§I–IV is added, amended or withdrawn by anything in this return. This is the **eleventh consecutive session** at which the theme is quiet at the three-ring intersection, and the honest form is **not found in accessible sources**, never "does not exist".

**Today's work is instrument work, and I will not dress it as substance.** Five searches and a census of 559 relay records produced: a host table, a negative about NAFTA text carriers, a correction against myself, and a reading of a configuration gate. **None of that is a finding about IP as a covered investment.** The nexus between today's output and the research question is **thin and mediated** — it bears on the question only through the Ring-3 treaty-access thread (can we read the expropriation chapter of our own seed treaty), and I report it as that and nothing more.

**What today does NOT establish, itemised because the temptation runs the other way:** nothing about what NAFTA Article 1110 contains; nothing about whether a paragraph (7) exists; nothing about whether any identified file is NAFTA; nothing about what any host serves a browser; nothing about whether the field was quiet in the week to 2026-09-21.

### 3.2 Errors reported against myself

1. **I nearly filed a re-discovery as a discovery.** `treaties/bit/3104/nafta-1992-` was fired and refused on 2026-09-20 and occurs 21 times in that day's record. One of my five calls bought nothing. Caught by checking our own fetch-results before proposing a row — a check I did *after* drafting the finding, not before, which is the wrong order. §3.7.
2. **I drafted a quotation that does not exist.** `a single `find:""` diagnostic` returned **0** occurrences in `2026-09-21.md`. It is a paraphrase of the prohibition that had become, in my draft, a quotation of it. Procedure (a) caught it before filing. This is exactly the `0921-B1` failure class and it recurred in me one day after adoption.
3. **My first framing of the gap was wrong on its face.** I began from the slug's words — *visible-prose-untested* — and had to correct myself against our own 189 non-empty excerpts at one host. The limb that is untested was never the instrument.
4. **I am proposing rows I predict cannot close the gap I was sent to close.** I state it as a defect in the day, not a virtue of the memo.

### 3.3 Carrying-span marks

None of the four propositions below has a **rigid designator** in the legal sense — they are propositions about code and artefacts, not about cases or treaty provisions — so I screen on exact identifiers instead and **I write down that the verdict is weaker for it**, as step 2 requires. Item 5 (card every case) does not apply: none of these documents is a case, and I say so rather than leaving the field blank.

---

**P** — Under this run's configuration the digest's `accepted` and `watch_list_leads` are determined by a publication gate, while `matches` and `held_for_review` are measurements of the classifier's output.
**Q** — `    if status_only:\n        return []` [`src/main.py`, inside `select_surfaced`], whose docstring carries *"NOTHING publishes at item level"*; and `matches = stats.get("above_threshold", 0)` with `watch_list_leads = accepted - (matches - held_for_review)` [`src/render.py`]; and `stats["held_for_review"] = len(held)` over `held = [c for c in counterfactual if id(c) not in published_ids]` [`src/main.py`].
**D** — `src/main.py`, `src/render.py`, `src/config.py`, read at `f32b005`, 2026-09-22. Not a case; no stage, no disposition. The artefact the propositions are about is `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, which carries `"validation_status_only": true`.
**V** — checked 2026-09-22 at the working tree, `f32b005`. Screened, unit: occurrences, `str.count`: `if status_only:\n        return []` 1; `NOTHING publishes at item level` 1; `matches = stats.get("above_threshold", 0)` 1; `watch_list_leads = accepted - (matches - held_for_review)` 1; `stats["held_for_review"] = len(held)` 1; `held = [c for c in counterfactual if id(c) not in published_ids]` 1; `accepted = len(items)` **2**. **Referent clause for the one nonzero term whose occurrences are not wholly the source of Q:** `accepted = len(items)` occurs twice in `src/render.py`; the occurrence I quote is in the meta-building function that emits `meta.json`; I did not read the second occurrence's surrounding function and I do not assert what it does. No exit taken. **No rigid designator: these are code identifiers, and a rename would defeat the screen.**

---

**P** — The digest file itself states that gdelt's zero item count is not evidence of a quiet week.
**Q** — *"Their fetchers likely no longer match the live site; zero items from them is NOT evidence of a quiet week."* [`digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, `health_warnings[0]`]
**D** — `meta.json` of the 2026-09-21 digest folder, read 2026-09-22. Not a case. The run it describes is dated `2026-09-21`.
**V** — checked 2026-09-22 in the working tree. Screened, unit: occurrences: the quoted sentence 1; `DEGRADED (5 zero runs)` **2**; `RECOVERED (Internet Archive)` **2**; `HEADLINE-ONLY` 1; `QUIET (page live; 20 releases listed, newest 2026-09-04; none in the run window)` 1. **Referent clauses for the nonzero terms not sourcing Q:** `DEGRADED (5 zero runs)` occurs once as gdelt's own `status` value and once inside the `health_warnings` string that names gdelt — the same source, quoted twice by the renderer. `RECOVERED (Internet Archive)` occurs once as italaw's `status` and once as `unctad_isds`'s; neither concerns gdelt. `HEADLINE-ONLY` is `iareporter_headlines`'s status and concerns body access, not degradation. The `QUIET…` string is `icsid`'s status and is the file's only live-read zero. No exit taken. **`gdelt` is the closest thing here to a rigid designator — it is a source key the renderer cannot paraphrase — and I rely on it.**

---

**P** — In this pipeline the reported capture ages describe only the snapshots a run fetched, not the oldest capture the Archive offered.
**Q** — *"NOT the age of ``oldest_capture`` — the oldest eligible capture was never fetched."* and *"``capture_age_days_median``  timestamp, over the FETCHED items only."* [`src/source_recovery.py`, `RecoveryReport` docstring]
**D** — `src/source_recovery.py` at `f32b005`, read 2026-09-22. Not a case. The docstring additionally names a test, `test_oldest_capture_is_eligible_basis_while_ages_are_fetched`, which I did **not** open; I assert nothing about what it does.
**V** — checked 2026-09-22 in the working tree. Screened, unit: occurrences: `NOT the age of ``oldest_capture`` — the oldest eligible capture was never` 1; `over the FETCHED items only` 1; `the cap is politeness, not a filter` 1; `attempted and dropped, so ``fetched`` can be lower` 1; `max_items: int = 12` 1. **Referent clauses for nonzero terms not sourcing Q:** `the cap is politeness, not a filter` is the docstring explaining that `omitted_by_cap` items are deferred to a later run rather than discarded — a different proposition, which I use separately. `attempted and dropped, so ``fetched`` can be lower` is the docstring explaining the 7-item shortfall in the unctad row — also a different proposition, used separately. `max_items: int = 12` is the `RecoverySpec` field default, which I use to check that 74 − 12 = 62 and 40 − 12 = 28. No exit taken. **No rigid designator; field names are the strongest identifiers available and a rename defeats them.**

---

**P** — This relay cannot fetch `files.pca-cpa.org`, and cannot produce any excerpt from a PDF, whatever the host does.
**Q** — `    if host not in ALLOWED_HOSTS:\n        return "host_not_allowed"` and `if "html" not in content_type and "text" not in content_type:\n        return ""` [`scripts/fetch_relay.py`]
**D** — `scripts/fetch_relay.py` at `f32b005`, read in full 2026-09-22. Not a case. I additionally **executed** `validate()` in this session against the RA-47 URL and it returned `host_not_allowed`; against all eight proposed rows it returned `None`.
**V** — checked 2026-09-22 in the working tree. Screened, unit: occurrences: the two-line host gate 1; the two-line content-type gate 1; `"pca-cpa.org",` 1; `"www.pca-cpa.org",` 1; `"iisd.org",` 1; `"www.iisd.org",` 1. **Referent clauses:** the four host-literal terms each occur once, as entries in the `ALLOWED_HOSTS` set literal; they are not the source of Q but they are what makes Q bite, since membership is exact-string and `files.pca-cpa.org` is absent from the set. No exit taken. **`ALLOWED_HOSTS` is a rigid designator within this repository and nowhere outside it.**

**Rejects (item 7).** Screened and not used: `src/source_health.py` (not opened; I assert nothing about how the DEGRADED streak is computed and took the file's status string as given). `tests/test_archive_integrity.py:51` (a fixture, not an artefact of this run). `analytics/insights.jsonl:103` (the 2026-08-24 finding that `VALIDATION_STATUS_ONLY` suppresses the weekly brief — screened, read, and **not** re-logged; today's point is about the digest's counters, not the brief). `digests/2026-09-14_ISDS-Thematic-Watch/` (not the digest of record). The `test_oldest_capture_is_eligible_basis_while_ages_are_fetched` test named in the docstring (not opened).

### 3.4 Gap markers

**`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested`** — CARRIED OPEN, condition unchanged at your §6.4 wording. Today's addition to it, for the log: *Limb A (can `excerpt_of` produce visible prose at an allowlisted host) is settled affirmative — 8 allowlisted hosts, 189 non-empty html excerpts at `icsid.worldbank.org` alone, over 559 committed records at 71 files at `f32b005`; the slug's wording overstates what is open. Limb B is the whole of the gap. Across the record and 68 result rows from 5 bounded calls (3 of them restricted to allowlisted registrable domains), **zero non-PDF allowlisted-host locators serving NAFTA Chapter Eleven text have been identified.** 52 of 68 returned rows are `.pdf`-suffixed. The two best-provenanced carriers ever found — `files.pca-cpa.org/pcadocs/bi-c/2.%20Canada/4.%20Legal%20Authorities/RA-47…pdf` and `files.pca-cpa.org/pcadocs/bi-c/1.%20Investors/4.%20Legal%20Authorities/CA237.pdf`, PCA-published numbered legal-authority exhibits in NAFTA Chapter Eleven arbitrations, engine titles "North American Free Trade Agreement PREAMBLE Skip to Section >>" and "ARTICLE 1103 Most-Favored-Nation Treatment 1." — are at a host `validate()` rejects (`host_not_allowed`, run in session) and are PDFs besides. The one identified allowlisted HTML locator whose engine title names NAFTA, `investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/3104/nafta-1992-`, was fired twice on 2026-09-20 and refused 403 twice on a run with `control_ok: true`. **THE COUNCIL SHOULD DECIDE WHETHER THIS CONDITION HAS ANY REACHABLE SATISFIER, RATHER THAN CARRYING IT INDEFINITELY.** Nothing here states that no such locator exists — not found in accessible sources.*

**`GAP-UNRESOLVED: nafta-article-1110-paragraph-7-ip-carve-out-unread`** — unchanged, operator item, zero search budget, and I spent none on it. **Nothing today licenses any statement about Article 1110's contents.**

**`GAP-UNRESOLVED: eu-pharma-package-rdp-reduction-isds-exposure-unassessed`** — unchanged, zero budget, status **UNKNOWN not foreclosed**. Untouched today. The struck Achmea ground is not restated.

**`GAP-UNRESOLVED: grosse-ruse-khan-itn-ip-isds-plain-packaging-to-patent-revocation`** — closed as to the body, unchanged, **no twelfth row proposed**. Rows 5–6 are a different document at a different path shape and do not reopen it.

**NEW, opened today: `GAP-UNRESOLVED: allowlist-host-spelling-variants-untested`** — Four `www`/bare host pairs sit on `ALLOWED_HOSTS` (`italaw.com`/`www.italaw.com`; `iisd.org`/`www.iisd.org`; `pca-cpa.org`/`www.pca-cpa.org`; plus `iareporter.com`/`www.iareporter.com`), and in 559 committed records **only one spelling of each pair has ever been fired**. Whether the two spellings behave identically at the origin is unknown, and it matters because `www.italaw.com` is 14-of-14 refused on non-PDF rows while `italaw.com` has never been tried. **CLOSURE CONDITION: one run in which both spellings of one pair are fired at the same path with the same `find`, and their outcomes compared.** Rows 1/4 and 5/6 of today's batch discharge this for two pairs if the batch fires.

### 3.5 Insights — dedup screen against `analytics/insights.jsonl` printed FIRST

**Screen unit: LINES, flag `grep -c`, over `analytics/insights.jsonl` (202 lines) at `f32b005`.** Per Amendment 2 **without its struck qualifier**, every nonzero term gets a referent clause enumerated by locator with its headline, characterised nowhere.

| term | lines | locators |
|---|---|---|
| `validation_status_only` | **0** | — |
| `above_threshold` | **0** | — |
| `held_for_review` | **0** | — |
| `watch_list_leads` | **0** | — |
| `status_only` | **0** | — |
| `select_surfaced` | **0** | — |
| `would_surface` | **0** | — |
| `omitted_by_cap` | **0** | — |
| `gdelt` | **0** | — |
| `capture_age` | **0** | — |
| `0 items surfaced` | **0** | — |
| `host census` | **0** | — |
| `unsatisfiable` | **0** | — |
| `RA-47` | **0** | — |
| `Most-Favored-Nation` | **0** | — |
| `host_not_allowed` | **0** | — |
| `VALIDATION_STATUS_ONLY` | **1** | L103 |
| `status-only` | **1** | L103 |
| `non-empty excerpt` | **1** | L50 |
| `Internet Archive` | **1** | L75 |
| `RECOVERED` | **1** | L75 |
| `HEADLINE-ONLY` | **1** | L58 |
| `files.pca-cpa.org` | **1** | L200 |
| `189` | **1** | L100 |
| `Chapter Eleven` | **1** | L196 |
| `allowlisted host` | **2** | L167, L200 |
| `never fired` | **2** | L149, L183 |
| `subdomain` | **1** | L200 |
| `closure condition` | **3** | L152, L169, L170 |
| `DEGRADED` | **4** | L46, L51, L140, L153 |
| `application/pdf` | **5** | L44, L72, L82, L167, L201 |
| `source_health` | **7** | L46, L51, L72, L73, L75, L140, L145 |

**Referent clauses — every nonzero cell, by locator with its headline, in my own words:**

- **L44** (2026-07-31) *"uncitral.un.org gates on user-agent at the CDN layer, so an HTTP 403 from that host carries NO information about resou…"* — `application/pdf` occurs here in a finding about one host's 403 behaviour, not about content-type gating.
- **L46** (2026-08-03) *"unctad_isds's static raw count structurally defeats the source-health guard: update_streaks() and collection_anomaly()…"* — `source_health` and `DEGRADED` occur here in a finding about the streak guard's inputs, not about how a reader should read a zero.
- **L50** (2026-08-04) *"Fetch-relay semantics established from source (scripts/fetch_relay.py), correcting the record: 'find' is matched case-…"* — `non-empty excerpt` occurs here in the establishment of `find` semantics, not in any host census.
- **L51** (2026-08-04) *"Three defects in the source-health guard, all code-derived and one with a date. (1) PROSPECTIVE, fires 2026-08-17: the…"* — `source_health` and `DEGRADED` occur here in defect-finding against the guard's code.
- **L58** (2026-08-11) *"THE PROFESSOR-FACING DIGEST ASSERTS DOCTRINAL ABSENCES ABOUT DOCUMENTS IT STATES IT NEVER READ, in 12 of 17 committed…"* — `HEADLINE-ONLY` occurs here as the status of a source whose bodies the digest never read.
- **L72** (2026-08-17) *"A TOOL-LAYER STATUS ON ONE PATH IS NOT A STATEMENT ABOUT A HOST - italaw answered and the council refused the headline…"* — `application/pdf` and `source_health` occur here in the tool-layer/host distinction.
- **L73** (2026-08-18) *"PATH IS SEPARATED FROM TIME AND FROM CLIENT FOR italaw, INSIDE A SINGLE RUN - and the de-confounding evidence came fro…"* — `source_health` occurs here in italaw's path-discrimination finding, which I cite in §1.7 rather than re-derive.
- **L75** (2026-08-18) *"RULE 1 GUARANTEES FIDELITY TO THE FILE, NOT STABILITY OF THE NUMBER, and nothing in the artefact tells a reader which…"* — `RECOVERED`, `Internet Archive` and `source_health` occur here in a finding about a digest figure changing under a reader's feet, not about capture staleness or the cap.
- **L82** (2026-08-19) *"A DOCUMENT THIS PROJECT HAS RECORDED AS BLOCKED SINCE 2026-06-30 IS NOT BLOCKED, AND THE LABEL IS STILL STANDING IN TH…"* — `application/pdf` occurs here in a correction to a standing block label.
- **L100** (2026-08-23) *"THE RULE THAT CATCHES ALL THREE OF THE DAY'S CHAIRMAN FAILURES WAS WRITTEN BY THE OTHER TWO SEATS, AND THE ONE THE CHA…"* — `189` occurs here as a digit string inside unrelated numerals, not as a count of anything.
- **L103** (2026-08-24) *"THE OPERATOR'S WEEKLY INTERPRETIVE BRIEF HAS BEEN SUPPRESSED SINCE 2026-08-11 BY A CONFIGURATION DEFAULT, NOT BY ANY FAILURE, AND THE SAME FLAG GATES THE ONLY WRITER OF WEEKLY-COUNCIL LEDGER ENTRIES IN THE REPOSITORY."* — `VALIDATION_STATUS_ONLY` and `status-only` occur here in a finding about the flag's effect on **`briefs/` and the weekly council-log writer**. **This is the closest prior art to my insight 1 and I read it in full before writing.** It establishes the flag is on and gates the brief. It does not touch `meta.json`'s counters and does not distinguish measured from determined zeros.
- **L140** (2026-09-05) *"A SPECIFIC DIAGNOSIS CAN BE OVERWRITTEN BY A GENERIC ONE, AND THE GENERIC ONE IS THE LINE A READER SEES. src/main.py:2…"* — `source_health` and `DEGRADED` occur here in a finding about status strings overwriting each other.
- **L145** (2026-09-06) *"specs/ IS A DEAD REGISTER: NOTHING IN THE RUNTIME READS IT, AND THIS PROJECT HOLDS FOUR CODE-SIDE REGISTERS OF SOURCE…"* — `source_health` occurs here in the register census.
- **L149** (2026-09-07) *"THE ICSID RAW-HTML INTAKE SURFACE IS PER-URL, NOT PER-HOST, AND ONE SIBLING PATH DOES SERVE CASE IDENTIFIERS TO A PLAI…"* — `never fired` occurs here about an ICSID intake path.
- **L152** (2026-09-08) *"THIS COUNCIL ESCALATED TO ITS OPERATOR, TWICE, A DEFECT ITS OWN RECORD HAD ALREADY EXPLAINED - AND THE SAME DAY'S RECO…"* — `closure condition` occurs here about a duplicated escalation.
- **L153** (2026-09-08) *"A PRE-REGISTERED PREDICTION ABOUT A STATUS LABEL IS A PREDICTION ABOUT A CODE PATH, AND THIS COUNCIL DOES NOT DISTINGU…"* — `DEGRADED` occurs here about the status-label/code-path distinction.
- **L167** (2026-09-12) *"THE BLOCKER ON THIS THREAD IS NO LONGER ACCESS - THE HOST HANDS US THE AWARD AND OUR OWN CODE THROWS THE BYTES AWAY."* — `application/pdf` and `allowlisted host` occur here in **the content-type-gate mechanism finding. I do not re-log that mechanism and insight 2 below does not re-assert it**, per the same discipline `insights.jsonl:197` applied on 2026-09-20.
- **L169** (2026-09-13) *"ICSID'S OWN DOCKET SAYS THE MANIFEST-LACK-OF-MERIT CASE THIS COUNCIL HAS BEEN CLOSING ON IS A CONSTRUCTION DISPUTE."* — `closure condition` occurs here about a docket's subject field.
- **L170** (2026-09-13) *"THE GATE WAS INVOKED AND BRIEFED - IT DID NOT 'PRODUCE A DISPOSITION', AND YESTERDAY'S RECORD TOLD THE OPERATOR OTHERW…"* — `closure condition` occurs here about a Rule 41 gate, an ICSID procedure, not a relay condition.
- **L183** (2026-09-16) *"AN ABSENCE CLAIM ABOUT A CLASS OF LOCATOR IS SCREENED BY THE CLASS PREDICATE, NEVER BY EQUALITY ON ONE MEMBER (taxonom…"* — `never fired` occurs here in the class-predicate screening rule, **which binds §1.4's absence claim and which I applied: I screened by content-type class and host-membership class over all 68 rows, not by equality on one URL.**
- **L196** (2026-09-20) *"A CANDIDATE NAFTA CHAPTER 11 LOCATOR EXISTS AT A HOST ON ALLOWED_HOSTS, AT TITLE STRENGTH ONLY, AND THE RELAY REFUSED IT THE SAME MORNING."* — `Chapter Eleven` occurs here about `treaty-files/2413/download`.
- **L200** (2026-09-21) *"allowed_domains MATCHES SUBDOMAINS, AND TODAY IT HANDED THIS COUNCIL A LOCATOR ITS OWN RELAY MAY NOT FETCH."* — `files.pca-cpa.org`, `subdomain` and `allowlisted host` occur here. **This is prior art for the mechanism and I DROP my drafted third insight as duplicative of it.** Today adds one particular, folded into insight 2 rather than logged separately: the out-of-set host reproduced on **7** rows rather than 1, and it is the host carrying the chapter-text exhibits.
- **L201** (2026-09-21) *"ONE ENGINE TITLE, THREE DISTINCT LOCATORS, TWO HOSTS - AND ESCALATION 2 IS CURRENTLY BANKED ON THAT TITLE."* — `application/pdf` occurs here about the italaw byte-stream's empty excerpt.

**One insight dropped by this screen before writing:** the `allowed_domains` subdomain-leak insight, duplicative of L200.

---

**INSIGHT 1 — NEW.**
`THE DIGEST'S HEADLINE CARRIES FOUR ZEROS AND TWO OF THEM ARE THE CONFIGURATION SPEAKING, NOT THE INSTRUMENT. WHICH IS WHICH IS DECIDABLE FROM THE CODE AND IS RECORDED NOWHERE IN THIS COUNCIL'S INSIGHTS.` digests/2026-09-21_ISDS-Thematic-Watch/meta.json carries screened 21, matches 0, held_for_review 0, watch_list_leads 0, accepted 0, validation_status_only true. MEASURED: matches is stats["above_threshold"] at src/render.py, the count of candidates the classifier scored at or above threshold, and it is not a function of the publication gate — so 0 means none of the 21 judged candidates cleared it. held_for_review is len(held) at src/main.py over held = [c for c in counterfactual if id(c) not in published_ids], the counterfactual being would_surface with the gate bypassed — so 0 means nothing would have published even without the gate. The two agree and come from different code paths. DETERMINED: src/main.py's select_surfaced opens `if status_only:` / `return []` and its docstring says "NOTHING publishes at item level"; src/config.py says "on, NO item-level entry is published or emailed at all". accepted = len(items) is therefore 0 by construction and watch_list_leads = accepted - (matches - held_for_review) = 0 - (0 - 0) = 0 by arithmetic. THE CONSEQUENCE FOR THIS COUNCIL'S OWN PROSE: the sentence "N candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads)", copied into these records for weeks, sets a measured zero beside a determined zero and reads them as one kind of fact. DEDUP, unit LINES over analytics/insights.jsonl: validation_status_only 0, above_threshold 0, held_for_review 0, watch_list_leads 0, status_only 0, select_surfaced 0, would_surface 0, "0 items surfaced" 0. The nearest prior art is analytics/insights.jsonl:103 (2026-08-24), which establishes the flag is on and gates briefs/ and the weekly council-log writer; it does not reach meta.json's counters and I do not restate it. WHAT THIS IS NOT: it is not a claim that the week was busy, not a claim that any candidate deserved to surface, and not a claim about the classifier's accuracy — the scores' meaning is the open question the gate exists for.
*ring:* instrument / digest semantics. *confidence:* 0.9. *sources:* `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, `src/main.py`, `src/render.py`, `src/config.py`, all at `f32b005`.

**INSIGHT 2 — NEW.**
`AFTER A CENSUS OF EVERY RELAY ROW THIS PROJECT HAS EVER FIRED AND FIVE BOUNDED CALLS, THE NUMBER OF IDENTIFIED LOCATORS CAPABLE OF SATISFYING THE NAFTA GAP'S CLOSURE CONDITION IS ZERO — AND THE BEST-EVIDENCED NAFTA CHAPTER ELEVEN TEXT THIS COUNCIL HAS EVER FOUND SITS ON A HOST ITS OWN validate() REJECTS.` CENSUS, computed at f32b005: 71 committed result files, 559 records; 8 allowlisted hosts have returned at least one ok row with html-or-text content type and a NON-EMPTY excerpt (icsid.worldbank.org 189, example.com 136, uncitral.un.org 35, investmentpolicy.unctad.org 14, pca-cpa.org 9, undocs.org 3, digitallibrary.un.org 2, www.iisd.org 1). So the gap slug's "visible-prose-untested" is true only of the NAFTA-specific limb. SEARCH, 5 calls, 68 result rows, screened: 61 rows on ALLOWED_HOSTS by exact host match, 7 out of set (all files.pca-cpa.org); 52 of 68 URLs .pdf-suffixed; 9 rows at http:// which validate() rejects as scheme_not_https; 16 non-PDF rows at an allowlisted host, across exactly two hosts, and NOT ONE of the sixteen is a page serving the chapter's text. THE TWO BEST-PROVENANCED CARRIERS, NEW TODAY AND UNREACHABLE: files.pca-cpa.org/pcadocs/bi-c/2.%20Canada/4.%20Legal%20Authorities/RA-47%20-%20NAFTA%20Preamble,%20Ch.1.,%202%20and%2011.pdf and files.pca-cpa.org/pcadocs/bi-c/1.%20Investors/4.%20Legal%20Authorities/CA237.pdf — numbered legal-authority exhibits published by the PCA in NAFTA Chapter Eleven arbitrations, engine titles "North American Free Trade Agreement PREAMBLE Skip to Section >>" and "ARTICLE 1103 Most-Favored-Nation Treatment 1.". validate() returns host_not_allowed on RA-47, executed in session: ALLOWED_HOSTS is exact-string membership and holds pca-cpa.org and www.pca-cpa.org but not files.pca-cpa.org. THE MECHANISM HALF IS NOT NEW AND I DO NOT RE-LOG IT: that our own content-type gate rather than host access is the blocker on PDF-bearing hosts is held at analytics/insights.jsonl:167 and restated at :197; the subdomain-leak mechanism is held at :200, which recorded 1 out-of-set row where today produced 7. WHAT IS NEW IS THE CENSUS AND ITS NEGATIVE. AN ENGINE'S INDEXED TITLE IS THE ENGINE'S, NOT THE DOCUMENT'S; no seat has read a byte of either exhibit; nothing identifies either as NAFTA; and a .pdf suffix is not a content type. ABSENCE IS NOT FOUND IN ACCESSIBLE SOURCES, NEVER "DOES NOT EXIST" — screened by class predicate (content type, host membership) over all 68 rows, per analytics/insights.jsonl:183, not by equality on one URL.
*ring:* Ring 3 — treaty access / instrument. *confidence:* 0.85 for the census figures; 0.5 for the reading that the §6.4 condition has no reachable satisfier, which is an absence claim over a bounded search and is defeasible by one locator.
*sources:* `analytics/fetch-results/` (71 files at `f32b005`), `scripts/fetch_relay.py`, the five search calls recorded at `analytics/search-results/2026-09-22.json`.

**INSIGHT 3 — NEW.**
`THE DIGEST'S TWO ARCHIVE-RECOVERED SOURCES READ 17 OF THE 114 CAPTURES THE ARCHIVE OFFERED, AND THE AGE FIELDS DO NOT REPORT HOW OLD THE UNREAD ONES ARE — BY DESIGN, AND THE CODE SAYS SO.` Computed from digests/2026-09-21_ISDS-Thematic-Watch/meta.json: italaw eligible 74, fetched 12, omitted_by_cap 62; unctad_isds eligible 40, fetched 5, omitted_by_cap 28. 62 + 28 = 90 eligible captures never fetched; 12 + 5 = 17 read. src/source_recovery.py sets max_items: int = 12, so 74 - 12 = 62 and 40 - 12 = 28 reconcile exactly; the unctad row's further shortfall of 7 (12 to_fetch, 5 produced) is what the docstring calls items "attempted and dropped, so ``fetched`` can be lower" than eligible minus the cap. The docstring also states the cap is "politeness, not a filter" — omitted items defer to a later run. THE AGE FIELDS ARE ON A DIFFERENT BASIS FROM THE TIMESTAMPS AND THE CODE PINS THAT DIVERGENCE DELIBERATELY: "NOT the age of ``oldest_capture`` — the oldest eligible capture was never fetched", ages running "over the FETCHED items only". So italaw reports capture_age_days_max 12 and median 3 while its oldest_capture 20260810 is 42 days before the run (computed), and unctad_isds reports max 23 and median 23 while its oldest_capture 20260807 is 45 days (computed) and its newest_capture 20260829 is 23. THE CONSEQUENCE FOR A READER OF "0 ITEMS SURFACED": anything UNCTAD published in the 23 days to 2026-09-21 was outside what this run could see, and the digest reports no gap where that gap is. DEDUP, unit LINES over analytics/insights.jsonl: omitted_by_cap 0, capture_age 0, gdelt 0; RECOVERED and Internet Archive each 1, both at L75, which concerns a digest figure changing between readings and not staleness or the cap. NOT A DEFECT CLAIM: the cap is documented, the divergence is tested for, and nothing here says the pipeline is wrong — it says the digest's zero is smaller evidence than it reads as.
*ring:* instrument / source health. *confidence:* 0.9.
*sources:* `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, `src/source_recovery.py` at `f32b005`.

**No fourth insight. The research question produced none today and I will not manufacture one: no new insight on the three-ring substance; standing watch.** What I checked for it: the 2026-09-21 digest's 21 screened candidates via its `per_source` and `source_health` blocks (0 matches, 0 held), and `STATE_OF_THE_ANSWER.md` §IV's eight open questions, none of which moved.

### 3.6 `candidate_claims`

```json
{
  "date": "2026-09-22",
  "seat": "research-analyst",
  "repo_state": "f32b005",
  "claims": [
    {
      "id": "0922-C1",
      "ring": "instrument / digest semantics",
      "claim": "In the 2026-09-21 digest, `accepted` and `watch_list_leads` are determined by the VALIDATION_STATUS_ONLY publication gate and carry no information about the week, while `matches` (0) and `held_for_review` (0) are measurements of the classifier's output over the 21 screened candidates.",
      "confidence": 0.9,
      "sources": [
        "digests/2026-09-21_ISDS-Thematic-Watch/meta.json",
        "src/main.py",
        "src/render.py",
        "src/config.py"
      ],
      "supporting_quote": "    if status_only:\n        return []",
      "quote_source": "src/main.py (function select_surfaced)",
      "verification": "string pasted back into src/main.py at f32b005, occurrence count 1; `matches = stats.get(\"above_threshold\", 0)` count 1 in src/render.py; `watch_list_leads = accepted - (matches - held_for_review)` count 1 in src/render.py; `stats[\"held_for_review\"] = len(held)` count 1 in src/main.py",
      "does_not_establish": "that the field was quiet; that any candidate deserved to surface; anything about classifier accuracy"
    },
    {
      "id": "0922-C2",
      "ring": "instrument / source health",
      "claim": "The 2026-09-21 digest's gdelt row is DEGRADED at five consecutive zero runs with a single refusal at HTTP 429, and the file's own health_warnings block states that zero items from such a source is not evidence of a quiet week.",
      "confidence": 0.95,
      "sources": ["digests/2026-09-21_ISDS-Thematic-Watch/meta.json"],
      "supporting_quote": "Their fetchers likely no longer match the live site; zero items from them is NOT evidence of a quiet week.",
      "quote_source": "digests/2026-09-21_ISDS-Thematic-Watch/meta.json, health_warnings[0]",
      "verification": "string pasted back into the named file at f32b005, occurrence count 1",
      "does_not_establish": "that gdelt's fetcher is in fact broken; a 429 is rate limiting and the warning's wording is 'likely'"
    },
    {
      "id": "0922-C3",
      "ring": "instrument / source health",
      "claim": "The 2026-09-21 digest's two archive-recovered sources read 17 of 114 eligible captures (italaw 12 of 74, unctad_isds 5 of 40), leaving 90 unread; and the reported capture ages cover only the fetched subset, so unctad_isds's oldest eligible capture at 45 days and italaw's at 42 days are not reflected in the age fields of 23 and 12 respectively.",
      "confidence": 0.9,
      "sources": [
        "digests/2026-09-21_ISDS-Thematic-Watch/meta.json",
        "src/source_recovery.py"
      ],
      "supporting_quote": "NOT the age of ``oldest_capture`` — the oldest eligible capture was never",
      "quote_source": "src/source_recovery.py, RecoveryReport docstring",
      "verification": "string pasted back into src/source_recovery.py at f32b005, occurrence count 1; `over the FETCHED items only` count 1; `max_items: int = 12` count 1; day counts computed in session from the file's own CDX timestamps against the 2026-09-21 run date",
      "does_not_establish": "any defect in the pipeline; the cap and the divergence are both documented and intentional"
    },
    {
      "id": "0922-C4",
      "ring": "Ring 3 — treaty access / instrument",
      "claim": "Eight hosts on ALLOWED_HOSTS have returned at least one relay row with outcome ok, an html or text content type, and a non-empty excerpt, across 559 records in 71 committed result files; the untested limb of the NAFTA gap is therefore NAFTA-specific and not an open question about the instrument.",
      "confidence": 0.95,
      "sources": [
        "analytics/fetch-results/ (71 files at f32b005)",
        "scripts/fetch_relay.py"
      ],
      "supporting_quote": "if \"html\" not in content_type and \"text\" not in content_type:\n        return \"\"",
      "quote_source": "scripts/fetch_relay.py, excerpt_of",
      "verification": "string pasted back into scripts/fetch_relay.py at f32b005, occurrence count 1; the 559/71/8/189 figures computed in session over analytics/fetch-results/*.json",
      "does_not_establish": "that any of those hosts serves NAFTA Chapter Eleven; nothing about what any host serves a browser"
    },
    {
      "id": "0922-C5",
      "ring": "Ring 3 — treaty access",
      "claim": "Across this project's relay record and 68 result rows returned by five bounded search calls, three of them restricted to allowlisted registrable domains, no non-PDF locator at a host on ALLOWED_HOSTS has been identified as serving the text of NAFTA Chapter Eleven; 52 of the 68 rows are .pdf-suffixed and the 16 non-PDF allowlisted-host rows fall at two hosts, none of them a page serving the chapter.",
      "confidence": 0.5,
      "sources": [
        "analytics/search-results/2026-09-22.json",
        "analytics/fetch-results/ (71 files at f32b005)",
        "scripts/fetch_relay.py"
      ],
      "supporting_quote": "It retrieves what the sources or the record already identified",
      "quote_source": "analytics/fetch-requests/README.md",
      "verification": "string pasted back into analytics/fetch-requests/README.md at f32b005, occurrence count 1; the 68/52/16/61/7 row figures computed in session over the recorded result rows",
      "does_not_establish": "that no such locator exists. This is an absence claim over a bounded search, screened by class predicate (content type, host membership) rather than by equality on one URL, and it is defeasible by a single counterexample. NOT FOUND IN ACCESSIBLE SOURCES."
    },
    {
      "id": "0922-C6",
      "ring": "Ring 3 — treaty access / instrument",
      "claim": "Two PCA-published numbered legal-authority exhibits filed in NAFTA Chapter Eleven arbitrations, at files.pca-cpa.org, carry engine-indexed titles naming the agreement's preamble and an article heading of Chapter Eleven; this relay cannot fetch either, because ALLOWED_HOSTS is tested by exact host membership and files.pca-cpa.org is not a member.",
      "confidence": 0.75,
      "sources": [
        "analytics/search-results/2026-09-22.json",
        "scripts/fetch_relay.py"
      ],
      "supporting_quote": "    if host not in ALLOWED_HOSTS:\n        return \"host_not_allowed\"",
      "quote_source": "scripts/fetch_relay.py, validate",
      "verification": "string pasted back into scripts/fetch_relay.py at f32b005, occurrence count 1; validate() executed in session against the RA-47 URL, returning host_not_allowed",
      "does_not_establish": "that either file is NAFTA, or contains Article 1110, or contains any paragraph (7). The titles are a search engine's index, no seat has read either document, and both are PDF-suffixed and so unreadable by this relay even if the host were allowlisted."
    }
  ]
}
```

### 3.7 Method note (self-training mandate)

**What produced findings today: reading our own committed artefacts before searching.** The census of `analytics/fetch-results/` — 20 lines of Python, no network, no budget — produced insight 2's whole spine and corrected my reading of the gap slug. The five searches produced two genuinely new locators (both unreachable) and one re-discovery of something we had already disposed of.

**The formulation that worked and the one that did not.** Restricting `allowed_domains` to a list of allowlisted *registrable* domains (S1, S2, S3) was the right shape and returned relevant carriers immediately. What it could not do is discriminate content type: 52 of 68 rows came back `.pdf`. **The lesson to bank: when the question is "is there an HTML page", the engine cannot be asked that, so the budget is better spent on a census of what we already hold than on more queries.** I spent three fewer calls than permitted for that reason.

**The failure, stated so it is reusable.** I drafted a finding from S5 before checking whether the locator was in our fetch-results. It was, fired twice, refused twice, two days earlier. **The check costs one grep and it must come before the draft, not after it.** This is the same ordering failure as the quotation I nearly filed unverified — in both cases the verification step existed, was known to me, and ran second.

**Weekly primary-source study:** not done this session; the session went to instrument work. Carried.

---

## 4. For your part — file bodies, and what I ask you to rule on

### 4.1 `analytics/search-results/2026-09-22.json`

The full 68-row artefact is large; I hand you the structure and the screens, and the complete row list is reproducible from the five call transcripts. Body as I would file it:

```json
{
  "date": "2026-09-22",
  "seat": "research-analyst",
  "repo_state": "f32b005",
  "budget": "5 of 8 WebSearch calls used; 3 unspent deliberately",
  "prohibited_host_screen": {
    "rule": "2026-09-21 next step 3 (executable form): allowed_domains containing neither prohibited host, then screen every returned row",
    "unit": "result rows",
    "total_rows": 68,
    "dailyjus.com": 0,
    "unis.unvienna.org": 0,
    "blocked_domains_passed_on": "0 of 5 calls (schema-excluded alongside allowed_domains; insights.jsonl:199)"
  },
  "allowed_hosts_screen": {
    "unit": "result rows",
    "exact_host_in_ALLOWED_HOSTS": 61,
    "out_of_set": 7,
    "out_of_set_hosts": ["files.pca-cpa.org"],
    "note": "reproduces analytics/insights.jsonl:200 at 7 rows where 2026-09-21 saw 1"
  },
  "content_type_screen": {
    "unit": "result rows",
    "pdf_suffixed": 52,
    "not_pdf_suffixed": 16,
    "http_scheme_rows_unrelayable": 9,
    "distinct_urls": 54
  },
  "calls": [
    {"id": "S1", "query": "\"Article 1110\" NAFTA \"Chapter Eleven\" expropriation full text of the agreement",
     "allowed_domains": ["italaw.com","unctad.org","un.org","undocs.org","iisd.org","pca-cpa.org","worldbank.org","iareporter.com"], "rows": 10},
    {"id": "S2", "query": "North American Free Trade Agreement Chapter Eleven Investment text Article 1101 1110 1121",
     "allowed_domains": ["un.org","undocs.org","worldbank.org","pca-cpa.org"], "rows": 10},
    {"id": "S3", "query": "NAFTA \"Chapter Eleven\" \"Article 1110\" Expropriation and Compensation treaty text page",
     "allowed_domains": ["icsid.worldbank.org","uncitral.un.org","pca-cpa.org","undocs.org","docs.un.org","digitallibrary.un.org"], "rows": 30,
     "note": "one call; the engine returned three result blocks"},
    {"id": "S4", "query": "Permanent Court of Arbitration case NAFTA Chapter Eleven investor Canada case page",
     "allowed_domains": ["pca-cpa.org"], "rows": 8},
    {"id": "S5", "query": "UNCTAD Investment Policy Hub IIA Navigator North American Free Trade Agreement NAFTA treaty page full text",
     "allowed_domains": ["investmentpolicy.unctad.org"], "rows": 10}
  ],
  "host_distribution": {
    "icsid.worldbank.org": 27, "investmentpolicy.unctad.org": 11, "icsidfiles.worldbank.org": 10,
    "files.pca-cpa.org": 7, "pca-cpa.org": 5, "www.iisd.org": 3, "www.italaw.com": 2,
    "uncitral.un.org": 2, "unctad.org": 1
  },
  "locators_carried_forward": [
    {"url": "https://files.pca-cpa.org/pcadocs/bi-c/2.%20Canada/4.%20Legal%20Authorities/RA-47%20-%20NAFTA%20Preamble,%20Ch.1.,%202%20and%2011.pdf",
     "engine_title": "North American Free Trade Agreement PREAMBLE Skip to Section >>",
     "calls": ["S2 r2","S3 r4","S3 r43"], "relay_status": "host_not_allowed (validate() run in session)", "for": "operator, escalation 2"},
    {"url": "https://files.pca-cpa.org/pcadocs/bi-c/1.%20Investors/4.%20Legal%20Authorities/CA237.pdf",
     "engine_title": "ARTICLE 1103 Most-Favored-Nation Treatment 1.",
     "calls": ["S4 r4"], "relay_status": "host_not_allowed", "for": "operator, escalation 2"},
    {"url": "https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/3104/nafta-1992-",
     "engine_title": "NAFTA (1992) | International Investment Agreements Navigator | UNCTAD Investment Policy Hub",
     "calls": ["S5 r1"], "relay_status": "ALREADY FIRED AND REFUSED 403 x2 on 2026-09-20; not a new candidate", "for": "operator, escalation 2"},
    {"url": "https://pca-cpa.org/en/cases/50/", "engine_title": "Bilcon of Delaware et al v. Government of Canada",
     "calls": ["S4 r1"], "relay_status": "allowlisted, never fired", "for": "batch rows 1-3"},
    {"url": "https://www.iisd.org/system/files/publications/nafta.pdf", "engine_title": "NAFTA's Chapter 11 and the Environment",
     "calls": ["S1 r2"], "relay_status": "allowlisted host, path shape never fired", "for": "batch rows 5-6"}
  ],
  "negative_result": "No non-PDF locator at a host on ALLOWED_HOSTS was identified as serving the text of NAFTA Chapter Eleven. 16 non-PDF allowlisted-host rows, across 2 hosts, all index/case/metadata pages."
}
```

### 4.2 `analytics/fetch-requests/2026-09-22-daily.json`

```json
{
  "note": "Research analyst, 2026-09-22. PRE-REGISTERED BEFORE FIRING; the full pre-registration is at analytics/daily-research/2026-09-22.md section 1.7 and this note must be read with it. HEADLINE PREDICTION, AGAINST THIS SEAT'S OWN INTEREST: THIS BATCH WILL NOT CLOSE GAP-UNRESOLVED nafta-chapter-11-allowlisted-host-visible-prose-untested. NO ROW IN IT CAN. Rows 1-4 are at a case page, which does not serve the chapter's text under the reading this seat recorded before firing. Rows 5-7 are PDF endpoints and excerpt_of returns the empty string for any content type that is neither html nor text (scripts/fetch_relay.py). Row 8 is the closing bracket. The reason no winning row is proposed is that, after a census of 559 committed relay records across 71 files at f32b005 and five bounded search calls returning 68 rows, ZERO non-PDF allowlisted-host locators serving NAFTA Chapter Eleven text have been identified. FOUR QUESTIONS THIS BATCH DOES SETTLE. (1) Rows 1-3: does this relay obtain visible prose naming NAFTA and Chapter Eleven at an allowlisted host? pca-cpa.org is 11 of 11 ok with 9 non-empty html excerpts, but /en/cases/50/ has never been fired; expectation is ok/200/text/html with non-empty excerpts on all three. (2) Rows 1 vs 4 and 5 vs 6: does the www/bare host spelling matter at the origin? Four such pairs sit on ALLOWED_HOSTS and only one spelling of each has ever been fired in 559 records; www.pca-cpa.org and iisd.org have zero rows ever. Expectation: both spellings ok at PCA, both ok at IISD. The branch this seat would rather be wrong on is a divergence, which would bear directly on italaw.com (never fired) beside www.italaw.com (14 of 14 non-PDF refused). (3) Rows 5-6: is www.iisd.org's refusal regime path-scoped? All 21 of its refusals are /itn/ paths; /system/files/publications/ has never been fired. THESE ARE NOT A TWELFTH ROW in the Grosse Ruse-Khan thread: that thread's eleven rows are all at /itn/2014/11/18/litigating-intellectual-property-rights-.../ in both spellings, a different document at a different path shape, and its closure as to the body stands untouched. Expectation: ok/200/application/pdf with an EMPTY excerpt, which cannot close the gap. (4) Row 7, the single permitted UNCTAD diagnostic: has the 403 regime lifted at day 44? 23 of 23 rows refused from 2026-08-10 onward across five path shapes; 15 ok rows all dated 2026-08-09 or earlier. Expectation: refused/403. The reason to spend the slot today when 2026-09-21 rightly declined it: if UNCTAD answers again, then treaties/bit/3104/nafta-1992- - an HTML locator already identified, fired and refused 403 twice on 2026-09-20 - becomes fireable, and it is the ONLY identified candidate that could ever satisfy the section 6.4 condition. Even an ok on row 7 CANNOT close the gap: it is a download endpoint and returns application/pdf. OUTCOME-CLASS READINGS, never per document. ok + html + non-empty excerpt: the relay obtained visible prose at that locator; it licenses nothing about any other locator or host. ok + non-text: content_type, bytes and sha256 are recorded whatever the body is, and the empty excerpt is our gate, not the host's. ok + empty find: the byte-stream answered and the string is not in the visible prose of that document; it says nothing about the markup, since excerpt_of strips every tag before searching. refused: a tool-layer outcome with no body to type; content_type and bytes are null because there is no response, and NO SEAT MAY WRITE THAT THE SITE DOES NOT SERVE THE PAGE. robots_disallowed: our own politeness layer, not an origin answer. no_contact: we never reached the origin. COMPANION-CONTROL DISCIPLINE: rows 2 and 3 can null and are companioned by row 1 on the same URL in the same run; rows 4-7 carry find:'' and are self-companioning. THE CLOSING BRACKET is row 8, find 'Example Domain', distinct from the runner's prepended control: the prepended row proves reachability, row 8 proves the find path executed. If row 8 is ok with find_matched false, every empty excerpt in this batch is uninterpretable and the batch is void on that axis whatever else it says. control_ok false voids every row including the ones that look like answers. PROHIBITION COMPLIANCE, row by row: no row at www.italaw.com or italaw.com at all; exactly one UNCTAD row and it is treaty-files/2412 with find:'', the single permitted diagnostic; no twelfth Grosse Ruse-Khan row. NOT DISCOVERY: every URL was identified by a source before it was requested - rows 1-4 from search call S4 result 1, rows 5-6 from S1 result 2, row 7 from the record, row 8 the runner's own control. Rows 4 and 6 change only the host spelling of a supplied URL; that is a diagnostic of our own allowlist and this seat flags it as the one place a reviewer should push back. No URL was constructed by slug analogy, pattern or guess. BUDGET: 8 requested + 1 prepended = 9 rows, the chairman's cap, and 9 <= MAX_URLS 12, no truncation. All eight pass scripts.fetch_relay.validate(), run in session, all returning None. Six falsifiable predictions; if four or more fall the other way this seat's model of the relay is wrong and it asks that this be said in the close-out.",
  "urls": [
    {"url": "https://pca-cpa.org/en/cases/50/", "find": ""},
    {"url": "https://pca-cpa.org/en/cases/50/", "find": "North American Free Trade Agreement"},
    {"url": "https://pca-cpa.org/en/cases/50/", "find": "Chapter Eleven"},
    {"url": "https://www.pca-cpa.org/en/cases/50/", "find": ""},
    {"url": "https://www.iisd.org/system/files/publications/nafta.pdf", "find": ""},
    {"url": "https://iisd.org/system/files/publications/nafta.pdf", "find": ""},
    {"url": "https://investmentpolicy.unctad.org/international-investment-agreements/treaty-files/2412/download", "find": ""},
    {"url": "https://example.com/", "find": "Example Domain"}
  ]
}
```

Do not put `[skip ci]` in the request commit message — it suppresses the run.

### 4.3 Four things I ask you to rule on

1. **Whether `GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested` should be carried at all, given §1.4.** I am not asking you to close it — a null is not a closure, and the cross-BIT rule forbids turning "not found" into "does not exist". I am asking whether a condition with no identified reachable satisfier should keep consuming a batch a day, or should be converted into a standing operator item like the Article 1110 gap beside it. **This is your call and I have not pre-empted it: today's batch fires whatever you decide.**

2. **Whether escalation 2 should be re-pointed.** For ten days it has sent Emory to UNCTAD `treaty-files/2412` on the strength of a shared engine title that `insights.jsonl:201` showed individuates nothing, and yesterday it added the italaw byte-stream, which is one file at two paths. **Today gives a better ask and it costs him the same two minutes:** `files.pca-cpa.org/…/RA-47 - NAFTA Preamble, Ch.1., 2 and 11.pdf`, a numbered legal-authority exhibit the PCA published in a NAFTA Chapter Eleven arbitration, whose engine title names the preamble and chapters 1, 2 and 11; and beside it `…/CA237.pdf`, engine title "ARTICLE 1103 Most-Favored-Nation Treatment 1." **Neither is reachable by us and both are PDFs, which is exactly why they belong on his desk and not in our batch.** The question for him is unchanged: **does NAFTA Article 1110 contain an IP-specific internal limitation at a paragraph (7)?** Every caveat stays attached: the titles are an engine's, nobody has read a byte, and nothing identifies either as NAFTA.

3. **Whether the `www`/bare pairs on `ALLOWED_HOSTS` should be treated as one host or two.** I have opened `GAP-UNRESOLVED: allowlist-host-spelling-variants-untested` and two of today's rows discharge it for two pairs. If you would rather not spend rows on it, say so and I will withdraw rows 4 and 6 and hand the slots back.

4. **Whether the phrase "N candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads)" should carry a qualifier from now on.** Per §2, two of those zeros are gate output. I propose the standing form: *"N screened, 0 scored at or above threshold, 0 held; item-level publication is gated off, so `accepted` and `watch-list leads` are 0 by construction."* Your wording, not mine, but the distinction should be in the sentence.

---

**One line for a reader with no context.** Nothing moved on the research question for an eleventh consecutive session; today's work established that this council's own instrument reads visible prose from eight allowlisted hosts but that no reachable one of them has been identified as serving the text of NAFTA Chapter Eleven, and that two of the four zeros in yesterday's digest headline are its configuration speaking rather than its instrument.

---

**Files I touched:** none in the repository. Working notes only, in the session scratchpad at `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/rows.txt`. The two file bodies above are yours to commit; I commit nothing and push nothing.

I am available for the reconvening on the integrity officer's objections.

---

## 3. Integrity officer — filed verbatim

Spawned with the Agent tool against `.claude/agents/integrity-officer.md`. It returned a finished
vetting note as its final message. **Everything between the two rules below is the officer's text,
unaltered. I wrote none of it** — it is extracted byte-for-byte from the seat's return rather than
retyped. It committed nothing and pushed nothing. It performed no retrieval, and it says so in its
own second line: every objection below rests on a file in this repository or on code it executed in
this session, so the positive-control rule is satisfied by there being no HTTP-status objection to
control for. It filed **seven blocking** objections and **seven non-blocking**, and it declares a
seat model discrepancy at its head, which I carry to §7 rather than leaving in its text alone.

---

# 3. Integrity officer — filed verbatim

**Seat disclosure.** REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`. Unasked, as every sitting since 2026-08-12; the vault records the cause as structural (`model: opus` selects a tier, not a version) at `agents/integrity-officer.md:13-34`.

**Channel.** I performed no retrieval. Every objection below rests on a file in this repository or on code I executed in this session. Nothing here depends on an HTTP status I could not obtain, per the positive-control rule.

---

## 0. VERDICT ON THE SPEND CHECKPOINT

**HOLD, then RELEASE. No row is withdrawn.**

All eight rows pass `validate()` — I ran it myself, independently of the analyst's assertion; all eight return `None`. My negative controls discriminate: `files.pca-cpa.org/…/RA-47.pdf` → `host_not_allowed`, `http://pca-cpa.org/en/cases/50/` → `scheme_not_https`, `https://127.0.0.1/` → `private_address`, `https://example.com.evil.test/` → `host_not_allowed`. Four distinct rejection reasons; the gate is not tautological. Host membership is exact-string as the analyst says (`scripts/fetch_relay.py:111`). 8 requested + 1 prepended = 9 ≤ `MAX_URLS` 12, `truncated` False. Every carried prohibition is honoured — I checked each row by hand and screened the whole memo for all thirteen carried subjects.

**What holds the push is the `note` string in `analytics/fetch-requests/2026-09-22-daily.json`, not the URL list.** It carries four statements that are wrong or unsupported, and a pre-registration is never edited — under extension 34a an overstatement frozen there can only be withdrawn, never corrected. Fix `0922-B2`, `0922-B3`, `0922-B4` and `0922-B6` in the note body; no new row, no new search, no slot handed back. Then fire.

`0922-B1` and `0922-B5` are blocking on the memo and on the search artefact respectively, not on the relay.

---

## BLOCKING

### `0922-B1` — `held_for_review` does not corroborate `matches`. It is the same measurement computed twice, and I executed the code to establish it.

The memo's §2 says: *"The two numbers agree and they were produced by different code paths."* INSIGHT 1 repeats it: *"The two agree and come from different code paths."* 0922-C1 is built on it.

The code paths differ. The predicate does not. `src/config.py:39` sets `FILL_FLOOR_SUSPENDED` default `"1"` → `True`. With the fill suspended, `would_surface` (`src/main.py:222-259`) reduces to exactly `[c for c in ordered if c.relevance_score >= threshold]` — the same predicate `stats["above_threshold"]` counts at `src/main.py:1038`. So `held_for_review ≡ above_threshold` identically, for every input.

I ran it rather than reasoned it, per *untested counterfactual*:

```
scores=[]                     above_threshold=0 held_for_review=0 accepted=0 watch_list_leads=0
scores=[10, 20, 30]           above_threshold=0 held_for_review=0 accepted=0 watch_list_leads=0
scores=[45, 50, 12]           above_threshold=2 held_for_review=2 accepted=0 watch_list_leads=0
scores=[45, 50, 55, 60, 12, 30] above_threshold=4 held_for_review=4 accepted=0 watch_list_leads=0
scores=[26, 27, 28]           above_threshold=0 held_for_review=0 accepted=0 watch_list_leads=0
```

`held == matches` in all five cells, including the nonzero ones. Their agreement is an identity, guaranteed by construction; it is evidence of nothing. This is **taxonomy 16 (second-instrument corroboration fallacy)** compounded with **26 (tautological instrument check)**, and it is the load-bearing support for an insight bound for the permanent ledger.

Consequential, not cosmetic: the memo's *"nothing cleared it even counterfactually"* reads as "not even the more permissive rule surfaced anything." The counterfactual rule is not more permissive. It is the same bar.

**Required fix:** strike the corroboration clause from §2, from INSIGHT 1 and from 0922-C1. `matches: 0` stands as the one measurement. State that under `FILL_FLOOR_SUSPENDED` the held count is identically the at/above-threshold count and carries no independent information.

### `0922-B2` — "23 of 23 rows refused from 2026-08-10 onward" is wrong. It is 19 of 19, and the project's own state file says so.

Computed over `analytics/fetch-results/` at `f32b005`, unit: relay rows:

- `investmentpolicy.unctad.org` rows dated ≥ 2026-08-10: **19, all refused.**
- Rows dated < 2026-08-10: 19 — **15 `ok`, 4 refused.**

The 23 is the all-time refusal total. Four refusals predate 2026-08-10 (one on 08-07, two on 08-08, one on 08-09) and **all four are at `…/bilateral-investment-treaties/999999/…` sentinel paths** — deliberate negative controls against non-existent treaty ids, returning 500. Folding four designed controls into an evidential regime count inflates the denominator and mis-dates the regime's onset by three days.

`state/research_log.json:4` already carries the correct figure: *"12 refused/403 from 2026-08-10 through 2026-09-17, plus today's 7"* = 19. The memo contradicts the project's own state file.

Appears in §1.7 and in the committed note. **Required fix:** "19 of 19 rows dated 2026-08-10 or later refused; 15 `ok` rows all dated 2026-08-09 or earlier; four further refusals before that date are all `999999/` sentinel controls."

### `0922-B3` — "across five path shapes" was not computed in this session and is not reproducible at any unit.

Procedure (c) binds both seats today: every count in prose is computed in the session or deleted. This one is inherited verbatim from `state/research_log.json:4` (*"indifferent to path across all five shapes tried"*), a 2026-09-20 statement, and restated as today's finding.

Computed now over the post-08-10 refusals: **6** distinct exact paths; **3** by two-segment family. Over all 23 refusals: **8** distinct exact paths. Five is not reachable at any unit I can construct. No unit is declared — **extension 40a**, adopted 2026-09-20, which requires a screen to name its unit and its flag in the same clause as its number.

In the committed note. **Required fix:** compute it, declare the unit, or delete the clause.

### `0922-B4` — "zero candidate locators" is false, is contradicted three sections later in the same memo, and converts a 403 into an absence.

§1.4: *"this council has identified **zero** candidate locators capable of satisfying the §6.4 condition. Not one non-PDF, allowlisted-host locator serving NAFTA Chapter Eleven text exists in our record."* INSIGHT 2's headline asserts the same. The committed note asserts it: *"ZERO non-PDF allowlisted-host locators serving NAFTA Chapter Eleven text have been identified."*

Three independent refutations:

**(a) The memo refutes itself.** §1.7 row 7 calls `treaties/bit/3104/nafta-1992-` *"the **ONLY identified candidate** that could ever satisfy the §6.4 condition"* — and the committed note repeats it. Zero identified candidates and one identified candidate cannot both be true. Detectable without leaving the document: **taxonomy 33 (self-contradicting adjacent enumeration)**.

**(b) The record refutes it.** `https://www.italaw.com/investment-treaties/nafta` — allowlisted host, non-PDF, slug naming NAFTA — was fired **three times** on 2026-09-21 with `find` values `""`, `1110` and `Chapter Eleven`, and refused 403 on each. It is a candidate locator that was identified, fired, and tool-refused. The analyst has converted *identified and refused* into *not identified*. That is **taxonomy 7 (tool-status-as-source-state)** and **11 (status-as-record-artifact)**, against the chairman's own standing instruction in this session's §1.4: *"A 403 is a fact about this relay at that path and is never a fact about the resource."*

**(c) The screen's scope is narrower than the claim.** The census covers `analytics/fetch-results/` (71 files, 559 records). The claim is stated over *"our record."* `analytics/search-results/2026-09-20.json` and `2026-09-21.json` are committed parts of that record and carry allowlisted, non-PDF locators the census could not see — including `www.italaw.com/investment-treaties/nafta`, `www.italaw.com/investment-treaties`, `investmentpolicy.unctad.org/international-investment-agreements/iia-mapping`, and `www.iisd.org/toolkits/…/5-4-4-indirect-expropriation-regulatory-taking/`. **Taxonomy 29 (off-read-path carrier)**: the omitted root is the same one every time.

This is the day's load-bearing claim and the premise of the ruling requested at §4.3(1). **Required fix:** restate as *"every identified candidate is either tool-refused or under a standing prohibition; none has been read."* Strike "zero" from §1.4, INSIGHT 2's headline, §3.4 and the committed note.

### `0922-B5` — the proposed `analytics/search-results/2026-09-22.json` omits the rows, breaking its own series and making the day's central negative unauditable.

§4.1: *"The full 68-row artefact is large; I hand you the structure and the screens, and the complete row list is reproducible from the five call transcripts."* It is not. The call transcripts are not in the repository and are unreachable by any later seat.

Both prior files in the series carry the rows: `2026-09-20.json` and `2026-09-21.json` each have `calls[].results`. The proposed body has `calls[]` with `{id, query, allowed_domains, rows}` and no `results` array.

Every figure the day turns on — 61 in set / 7 out of set, 52 of 68 `.pdf`, 16 non-PDF, 54 distinct URLs, 9 `http://` rows, the nine-cell `host_distribution` — becomes uncheckable at the moment of commit. I verified the only thing left verifiable: `host_distribution` sums to 68, and 68 − 52 = 16 partitions exactly onto `pca-cpa.org` 5 + `investmentpolicy.unctad.org` 11. Internal consistency is a point in the analyst's favour and is not verification.

Separately, those nonzero cells are characterised rather than enumerated by locator — procedure (b), and Amendment 2 without its struck qualifier. `locators_carried_forward` enumerates 5 of 54 distinct URLs.

**Required fix:** commit the rows, as the two prior files do.

### `0922-B6` — a four-pair licence frozen into the pre-registration off a one-origin observation. Extension 34a.

§1.7 row 4: *"Both `ok`: the spelling is immaterial at this origin, and **the four never-fired `www`/bare twins on our allowlist are probably four redundant entries rather than four untested hosts**."*

§4.2's note incorporates §1.7 by reference (*"the full pre-registration is at … section 1.7 and this note must be read with it"*), so the licence is frozen with the batch. It generalises from one origin to four pairs, and it is the generalisation the analyst then wants to apply to `italaw.com`.

Extension 34a's countermeasure is exact: a pre-registered licence is written at the weakest form that still makes the row a test, because a falsification condition does its work by being pre-committed, not by being strong. **Required fix:** delete the clause from "and the four never-fired" onward, or restate as "immaterial at this origin, which licenses nothing about the other three pairs."

### `0922-B7` — rows 4 and 6 cannot discriminate what they are bought for, because the pre-registration omits two outcomes the code produces. This is my answer to the analyst's invitation to push back.

The analyst calls rows 1 vs 4 *"a clean two-cell experiment."* It has at least four cells, and the note carries two.

**Redirect.** `polite_get` uses `requests.get` with default redirect following (`src/sources/base.py:323`), and only the *final* status is tested (`:337`). If `www.pca-cpa.org/en/cases/50/` 301s to the bare host, row 4's `ok` records that the **bare** host answered — for the fourth time in that run — not that the `www` spelling is independently served. `final_url` records it and the analyst anticipates the redirect at §1.7, then reads "both `ok`" as immateriality. A redirect is not immateriality; it is the origin naming one spelling canonical and the other a redirector. Those license opposite inferences about `italaw.com`: if italaw redirects bare→`www`, firing `italaw.com` simply arrives at `www.italaw.com` and inherits its 403, and the row buys nothing.

**robots.** `robots_allowed` caches by `urlparse(url).netloc` (`src/sources/base.py:192, 207`), with no `www` stripping, and fails **open** on error. Rows 4 and 6 each trigger a *separate* robots.txt fetch at a host that has never been fetched. A `robots_disallowed` at one spelling and not the other is a robots.txt artefact, not a serving difference. Rows 1–3's outcome classes list `robots_disallowed`; **row 4's do not.** It is omitted from the one row where it is most likely to fire.

`_domain` not stripping `www` also means rows 1–3 and row 4 sit in **separate rate-limit buckets** (`MIN_INTERVAL_SECONDS = 3.0`), so row 4 does not queue behind rows 1–3 — that part is in the analyst's favour and I say so.

**Required fix:** the note carries four outcome cells for rows 4 and 6 — served independently; served via redirect (`final_url` differs from `url`); `robots_disallowed` at one spelling only; refused — and states that only the first licenses "the spelling is immaterial."

---

## NON-BLOCKING

**`0922-N1`** — §1.5 and §3.2(1): *"The string `3104` occurs in `analytics/daily-research/2026-09-20.md` **21 times**."* `grep -c` returns 21 **lines**; `str.count` returns **23 occurrences**. A line count published under the word "occurs" — **extension 40a, unit-swapped screen record**, adopted 2026-09-20, in a memo whose §0 declares unit discipline. This is the 2026-09-18 failure entry 40 was written on, recurring four days later.

**`0922-N2`** — §2(4): *"a reader of '0 items surfaced' is reading a screen of **17** archive items."* 17 is `fetched`. The screen is the 21 classified candidates, and the memo's own `per_source` block gives italaw **7** and `unctad_isds` **0** — so the screen saw **7** archive items, not 17. Same slip at §2(6): *"a statement about 12 titles"* for `iareporter_headlines`, whose `count` is 12 but whose `per_source` is **11**. Both figures are on the page, disagreeing — taxonomy 33, in the section that established the 3-of-10 column disagreement that makes it visible.

**`0922-N3`** — carrying-span mark 4: `Q`'s pinpoint is a whole file (`scripts/fetch_relay.py`). R5 tier 2 requires a pinpoint token. `:111-112` and `:124-125` would discharge it. The other three marks carry proper pinpoints.

**`0922-N4`** — reduction artefact the note should pre-empt: `find_matched` is `bool(find) and bool(excerpt_of(...))` (`scripts/fetch_relay.py:181-182`), so it will be written **`false` on every `find:""` row** — rows 1, 4, 5, 6, 7 and the prepended control — and is **absent entirely** on refused rows, which never reach the `update()`. Six `false` cells that are not misses, in a reduction whose whole purpose is that a later seat can read it cold.

**`0922-N5`** — §3.5's referent clause for `189` at `insights.jsonl:100` says it occurs *"as a digit string inside unrelated numerals."* It is a standalone number: *"this container's clone arrived SHALLOW at 189 commits against a true 758."* Right conclusion (unrelated to the excerpt census), wrong description of the locus.

**`0922-N6` — against the chair, not the analyst.** §1.4: *"five of five PDF-suffixed rows have returned 200."* Computed at `f32b005`: `www.italaw.com` has **6** `.pdf`-suffixed rows, all `ok`/200, all `application/pdf`; **5** distinct URLs; and **4** distinct byte-streams, since §6.7(8) established `italaw6187(14).pdf` and `italaw6187(6).pdf` are one byte-stream at two paths. Three different true numbers, one undeclared unit — extension 40a. The figure comes from yesterday's §6.7(2) and has now stood two days. The "14 of 14 non-PDF refused" half is exact. **The analyst wrote 6 correctly in its own table and did not inherit the error** — but it had the data and did not flag it.

**`0922-N7` — against the chair.** §1.4: of NAFTA Article 1110(7), *"No spelling of it appears anywhere in the tracked repository."* False at `f32b005`, over 814 tracked files enumerated with `git ls-tree -r --name-only` and no path argument:

| spelling | files | occurrences |
|---|---|---|
| `1110(7)` | 9 | 48 |
| `paragraph 7` | 7 | 28 |
| `Article 1110(7)` | 7 | 18 |
| `1110 (7)` | 5 | 13 |
| `1110.7` | 5 | 13 |

Every carrier is this council's own screen record, escalation text, or the unverified 2026-09-19 search lead — `STATE_OF_THE_ANSWER.md`, `analytics/council-log.md`, `2026-09-19.md`, `2026-09-20.md`, `insights.jsonl`, `state/`. **The substantive absence holds; the sentence does not.** Note also the population moved: the screen was run over **750** tracked files at `b267efd`, and the tree is now **814** — entry 40, stale-population screen record, waiting to fire on whoever restates it.

---

## THE THREE THINGS YOU ASKED OF ME AND NOT OF THE ANALYST

### 1. The new gap's closure condition is NOT well-formed, and rows 4 and 6 do not discharge it.

Condition as written: *"one run in which both spellings of one pair are fired at the same path with the same `find`, and their outcomes compared."*

Three defects:

**It cannot fail.** "and their outcomes compared" is an act the seat always performs, not an observation that can come out either way. Strip it and the condition closes on any two rows firing, whatever they show. Contrast your §6.4 condition, which names `outcome`, `content_type` and `excerpt` and can be missed.

**Its scope is narrower than the gap it closes.** The gap is stated over **four** pairs (italaw, iisd, pca-cpa, iareporter). The condition closes on **one**. Satisfying it at PCA would close a gap whose stated subject includes `italaw.com` and `iareporter.com`, neither tested — and §1.7 shows the seat intends exactly that generalisation, which is `0922-B6`.

**It does not say what result closes it.** If row 1 is `ok` and row 4 is `refused`, is the gap closed (spelling is material, question answered) or reopened (three pairs now urgent)? Undetermined.

Mechanically, rows 1/4 and 5/6 do fire both spellings at the same path with the same `find`, so they satisfy the condition *as written*. That is the problem: they satisfy it without establishing anything, because of `0922-B7`. **Recommend:** rewrite as — *closes for a named pair on one run in which both spellings are fired at the same path with the same `find` AND `final_url` is compared, distinguishing independent service from redirect; closure is per pair and licenses nothing about the other three.*

### 2. The §2 claim is TRUE. The proposed wording is accurate in its conclusion and wrong in one clause, and I would not adopt it as drafted.

Verified against the code myself:

- `src/render.py:229` — `matches = stats.get("above_threshold", 0)`, set at `src/main.py:1038` as a count over `classified`. **Measurement.** Not a function of the gate.
- `src/render.py:230` — `accepted = len(items)`; `items` derives from `select_surfaced`, which returns `[]` at `src/main.py:303-304` when `status_only`. **0 by construction.** Correct.
- `src/render.py:237` — `watch_list_leads = accepted - (matches - held_for_review)`. **0 for all inputs** — but *not* for the reason the memo gives. The memo derives it as "0 − (0 − 0) = 0 by arithmetic", which establishes it only for today's inputs. It is structurally zero because `held ≡ matches` under `FILL_FLOOR_SUSPENDED`, which is `0922-B1`. The code comment at `src/render.py:232-235` documents the history: *"Without the held term, a gated run with one real match computed leads = 0 - 1 = -1, and the archive validator (correctly) refused the folder — the 2026-08-17 failure."*

So: **two of the four zeros are gate output. The claim is sound and the distinction is worth carrying.**

The proposed standing sentence — *"N screened, 0 scored at or above threshold, **0 held**; item-level publication is gated off, so `accepted` and `watch-list leads` are 0 by construction"* — states a true conclusion. But its middle clause presents **"0 held" as a second fact when it is the same fact as "0 scored at or above threshold."** Adopting it as drafted would write `0922-B1` into every future record, permanently. **Recommend:** either drop the held clause, or write it as *"0 held, which under the suspended fill is identically the at/above-threshold count and is not a second observation."*

### 3. Four candidate locators the analyst missed. All four are in the committed record; none cost a search call.

Your §6.4 condition is **not** unsatisfiable. Its reach is bounded by a nineteen-entry set literal this project owns.

1. **`https://www.italaw.com/investment-treaties/nafta`** — allowlisted, non-PDF, slug naming NAFTA, fired three times on 2026-09-21 and 403'd three times. It is blocked by **your own prohibition**, not by the world. "Prohibited" and "no satisfier exists" are different facts and the memo merges them. This is the single most consequential correction to §4.3(1): you are being asked to retire a gap as unsatisfiable when its best candidate is under a self-imposed ban you can lift.

2. **`http://www.sice.oas.org/trade/nafta/chap-112.asp`** — **the highest-value item I can hand you.** This repository already carries it (`STATE_OF_THE_ANSWER.md:977`, `analytics/daily-research/2026-06-25.md:39`, `analytics/insights.jsonl:8`), `0919-B1` sustained it, and this project has already stated NAFTA **Art. 1121's** terms from it. It is an HTML page (`.asp`). I ran `validate()`: it fails on **both** grounds — `scheme_not_https` at `http://`, and `host_not_allowed` at `https://`. Adding `www.sice.oas.org` to `ALLOWED_HOSTS` is one line and converts §6.4 from a condition with no identified satisfier into one with a testable candidate, at a host that plausibly serves the chapter as visible prose. **That is an operator decision, not a council one, and it belongs in escalation 2 ahead of two unreadable PDFs.**

3. **`https://www.iisd.org/toolkits/sustainability-toolkit-for-trade-negotiators/5-investment-provisions/5-4-safeguarding-policy-space/5-4-4-indirect-expropriation-regulatory-taking/`** — from the committed `analytics/search-results/2026-09-20.json`. Allowlisted, non-PDF, **never fired**, and **not an `/itn/` path**. It is strictly better than rows 5 and 6 on the axis they are bought for: it tests the same path-scoped-refusal hypothesis at `www.iisd.org` while being **HTML**, so it can return a non-empty excerpt, where rows 5–6 are PDFs whose excerpt is empty by construction and which therefore cannot distinguish "answered" from "answered and gated." If a slot is ever handed back, this is where it should go.

4. **`https://investmentpolicy.unctad.org/international-investment-agreements/iia-mapping`** — same artefact, allowlisted, non-PDF, never fired; UNCTAD's mapping view renders provision text as HTML. Under the UNCTAD prohibition, so it is a candidate for a later day, not this batch.

I did not retrieve any of these and assert nothing about what they serve. They are locators in our own record, screened by class predicate (host membership, PDF suffix, path family), not by equality on one URL. **Not found in accessible sources is never "does not exist"** — and here the converse also bites: not fetched is never "not identified."

---

## TAXONOMY CURRENCY, AND ONE PATTERN PROPOSED BY NAME

**I opened the table rather than reciting it.** The vault heading at `agents/integrity-officer.md:114-117` reads *"40 entries as of 2026-09-18 … The next free number is 41."* **It is stale.** `analytics/daily-research/2026-09-20.md:1315` adopted **entry 41, INVENTED LIMB**, and `:1323` adopted **extension 40a, UNIT-SWAPPED SCREEN RECORD**. Neither has reached the vault. Yesterday's seat counted the twelfth consecutive D15 routing latency at `2026-09-21.md:753`; **today is the thirteenth**, and both rules bound me today — I have cited 40a twice above — while the file that is supposed to carry them still does not say so.

**I take no number off that heading.** Numbering off a stale heading is how the collision at 27 was made. My working set today was 41 numbered entries, extensions 17a / 32a / 34a / 40a, and the three unnumbered pending patterns.

**PROPOSED BY NAME ONLY, NO NUMBER TAKEN — "screen-record self-falsification."** An absence claim about the project's own tree is falsified by the act of filing it: once the screen record is committed, the term it reports as absent is present in the repository, and the standing sentence asserting the absence is false at the next commit. The next seat to re-run the screen gets a nonzero result composed entirely of prior screen records and must either contradict the standing sentence or explain it away. **Instance, verified: `0922-N7`** — *"No spelling of it appears anywhere in the tracked repository"* against `1110(7)` at 9 files / 48 occurrences, every carrier a screen record or escalation. **Distinguished from the unnumbered *zero-screen counted as a carrier***, which governs an occurrence **count offered in support of a proposition**; this governs a standing **absence sentence about the repository**, which no count accompanies and which nothing re-checks. **Countermeasure: an absence claim about the tracked tree carries its exclusion inside the sentence — "no carrier other than this council's own screen records" — and names the screen-record loci, so re-running it returns the same verdict instead of a contradiction.** The chairman's §1.4 sentence and `state/research_log.json:5` both need this clause today.

---

## WHAT I GOT WRONG, AND WHAT I TRIED TO BREAK AND COULD NOT

**Two things I got wrong.**

1. I read §1.6's *"The eleven rows in that thread are all at `/itn/2014/11/18/litigating-…/` **in both spellings**"* as a host-spelling claim and had it drafted as a blocking error, since `iisd.org` has **zero** rows in 559 records. Enumerating the paths showed "both spellings" means the `/itn/en/` and `/itn/` **path** variants: 8 + 3 = **11**. The analyst is exactly right and I was wrong. The wording is genuinely ambiguous in a section where "host-spelling variant" is a live term of art two paragraphs above, which is why I misread it, but the count is correct and the prohibition is honoured.

2. I expected the companion-control discipline to fail on row 8 — a `find` that can null with no companion named. It does not. Row 8's URL is byte-identical to `CONTROL_URL` (`https://example.com/`); I checked the string equality. The runner's prepended row is therefore a same-URL, same-run companion, and the README rule is satisfied. The analyst reaches the right answer by a different argument than the README's, and its design is sound.

**What I attacked and could not break.**

- **Procedure (a).** I re-ran all **35** quote strings into their named files myself rather than trusting the §0 table. **35 of 35 exact, zero mismatches** — including the two 2-counts and the struck string that correctly returns 0. This is the failure class that recurred one day after adoption, and today it is clean.
- **The §3.5 dedup screen.** All **16** claimed zeros and all **16** claimed nonzero counts reproduce, with **identical line numbers** on every one. Amendment 2 is discharged on every nonzero cell with a referent clause naming the locator and its headline.
- **The synonym trap on the zeros** — my own canonical training says a zero-hit screen is not substantive absence until the synonym is tried. I tried `GDELT`, `Gdelt`, `capture age`, `capture-age`, `held for review`, `watch list`, `watch-list`, `status only`, `above threshold`. Only `watch-list` hits, once, at `insights.jsonl:12` — the **USTR Special 301 Watch List**, a different sense entirely. The absence claim survives.
- **The census, in full.** 71 files / 559 records; the ten-row per-host outcome table cell for cell; 189 non-empty excerpts at `icsid.worldbank.org`; 8 hosts with a non-empty html/text excerpt; 19 `ALLOWED_HOSTS`, 10 ever fired, 9 never — and the nine never-fired hosts match the analyst's list exactly. `www.iisd.org` 21 of 21 refusals at `/itn/` paths. `pca-cpa.org` 11 of 11 `ok`, 9 non-empty, across exactly three prior paths, `/en/cases/50/` never fired. `www.pca-cpa.org`, `iisd.org`, `italaw.com` and `treaty-files/2412` never fired. No void run in the record.
- **Row 7's provenance**, which the memo defends in two words (*"the record"*). `state/research_log.json:4` establishes 2412 was a **returned search row** on 2026-09-20, chosen over 2542 as *"the only returned UNCTAD row whose engine title names the agreement in full"* — with the adjacency caveat already attached. Not slug-constructed. Clean.
- **All of Task 2's arithmetic.** `screened` 21, `per_source` summing to 21, `source_health[].count` summing to 32, the 3-of-10 disagreement on exactly italaw 7/12, `iareporter_headlines` 11/12 and `unctad_isds` 0/5, `count ≥ per_source` on 10 of 10, 74/12/62, 40/5/28, 62+28=90, 12+5=17, 114, italaw 42/3 against 12/3, unctad 45/23 against 23/23, icsid's newest release 17 days before the run. **Every figure exact.**
- **Carrying-span R5 by hand**, tiers 2, 3 and 5, on all four marks — `scripts/check_marks.py`'s docstring confirms tier 4 is unimplemented and is mine. Mark parity holds, entry count equals verification-statement count, nonzero-referent parity holds on all four, no degenerate uniformity across the V or D lines. One weak pinpoint (`0922-N3`). The analyst's handling of item 5 — stating expressly that none of the four is a case rather than leaving the field blank — is correct.
- **Every carried prohibition**, screened by name across the memo: Ecuador 0, Spentech 0, A/81/17 0, Vadi 0, ARB/24/16 0, asymmetric licence 0, Einarsson 0, monitored-author 0, Hela Schwarz 0. `Achmea` 1 — a compliance statement that it is not restated, which is correct. `dailyjus.com` and `unis.unvienna.org` twice each, as the §6.7(3) screen-and-report obligation requires, at zero budget.

**The honest summary.** The mechanical discipline in this memo is the strongest I have vetted: procedure (a) at 35 of 35, the dedup screen exact to the line number, the census exact to the cell, and the analyst caught two real errors against itself before I saw them. The defects are all at the level of **inference**, not arithmetic — a tautology read as corroboration, a denominator swapped, a 403 read as an absence, and a licence written stronger than its row. That is the pattern worth recording: this seat's arithmetic no longer needs policing and its inferences still do.

**Files:** `/home/user/isds-watcher/analytics/daily-research/2026-09-22.md`, `/home/user/isds-watcher/scripts/fetch_relay.py`, `/home/user/isds-watcher/src/main.py`, `/home/user/isds-watcher/src/render.py`, `/home/user/isds-watcher/src/config.py`, `/home/user/isds-watcher/src/sources/base.py`, `/home/user/isds-watcher/agents/integrity-officer.md`, `/home/user/isds-watcher/state/research_log.json`, `/home/user/isds-watcher/analytics/search-results/2026-09-20.json`, `/home/user/isds-watcher/analytics/search-results/2026-09-21.json`. I committed nothing and pushed nothing. Working scripts are in `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/`.

---

## 4. Spend checkpoint — taken here, after the vetting round

The checkpoint I placed at §1.6 falls here. The officer's verdict was **HOLD, then RELEASE**: all
eight rows pass `validate()`, every carried prohibition is honoured, and no row is withdrawn — but
the `note` string carried four statements it found wrong or unsupported, and a pre-registration is
frozen the moment it is committed (extension 34a), after which it can only be withdrawn and never
corrected. So the note had to be right **before** the push, not after it. **I did not fire the batch
on the analyst's proposal and I did not fire it on the officer's release alone.** I reconvened the
analyst to answer every objection and to hand me corrected bodies, and I fire only what survives
that round. What was actually pushed, and what came back, is at §6 below.

I ran the gate myself as well as reading both seats' runs, because I am the one who spends: all
eight rows returned `None` from `scripts.fetch_relay.validate()` on my own invocation, and my three
negative controls discriminated — `https://files.pca-cpa.org/pcadocs/x.pdf` → `host_not_allowed`,
`http://pca-cpa.org/en/cases/50/` → `scheme_not_https`, `https://evil.example.org/` →
`host_not_allowed`. `MAX_URLS` is 12 and 8 requested + 1 prepended = 9, so nothing truncates.

---

## 5. Research analyst — answer to the objections, filed verbatim

Reconvened with the Agent tool, its context intact, and directed to answer every objection, to hand
me corrected replacement bodies for both artefacts, and to rule on two locators the officer said it
had missed. **Everything between the two rules below is the analyst's text, unaltered. I wrote none
of it** — extracted byte-for-byte from the seat's return. It committed nothing, pushed nothing,
fired nothing, and spent no further search budget. **It sustains all seven blocking objections and
all seven non-blocking, and contests none.**

---

# Research analyst — answer to the integrity officer's objections, 2026-09-22

Chairman — filed for verbatim entry at `analytics/daily-research/2026-09-22.md` §5. I committed nothing, pushed nothing, fired nothing, and spent no search budget. Everything below was computed or executed in this session at `f32b005`.

**Headline: I sustain all seven blocking objections and all seven non-blocking. I contest none of them.** On `0922-B7` — the one the officer took up at my own invitation — I sustain the required fix in full and put one narrowing on the record that runs *against* the objection's headline and *for* its remedy. On `0922-B1` I verified the officer's finding myself, reproduced it on six cells including a boundary cell it did not run, and found that the identity is **conditional on the conjunction of two flags**, which makes the correction sharper than the objection states.

The officer's closing judgement is correct and I adopt it as my own: my arithmetic held and my inferences did not. **Four of the seven blocking defects are one failure repeating — I read a source, extracted a number from it, and did not carry the source's own qualification with the number.**

---

## 1. `0922-B1` — SUSTAINED. I executed it, it reproduces, and the true condition is narrower than the objection states, which makes it worse for me.

**Conceded without reservation.** `src/config.py` sets `FILL_FLOOR_SUSPENDED = os.getenv("FILL_FLOOR_SUSPENDED", "1")` → `True`. With the fill suspended, `would_surface` reduces to its first line, `surfaced = [c for c in ordered if c.relevance_score >= threshold]` — the identical predicate `stats["above_threshold"]` counts. So `held_for_review` and `matches` are one measurement written twice.

**I ran it rather than accept it.** Six cells, including a `[39, 40, 41]` boundary cell the officer did not run:

```
scores=[]                        above=0 held=0 accepted=0 leads=0  IDENTICAL=True
scores=[10, 20, 30]              above=0 held=0 accepted=0 leads=0  IDENTICAL=True
scores=[45, 50, 12]              above=2 held=2 accepted=0 leads=0  IDENTICAL=True
scores=[45, 50, 55, 60, 12, 30]  above=4 held=4 accepted=0 leads=0  IDENTICAL=True
scores=[26, 27, 28]              above=0 held=0 accepted=0 leads=0  IDENTICAL=True
scores=[39, 40, 41]              above=2 held=2 accepted=0 leads=0  IDENTICAL=True
```

**The finding I add, and it cuts against me.** The identity is **not** a property of `FILL_FLOOR_SUSPENDED` alone. It requires **both** flags, and it breaks on either one:

```
status_only OFF, fill suspended:   scores=[45,50,12]  above=2 held=0  IDENTICAL=False
                                   scores=[45,50,55,60,12,30] above=4 held=0  IDENTICAL=False
fill NOT suspended, status_only ON: scores=[26,27,28] above=0 held=3  IDENTICAL=False
                                   scores=[30,31,32,33,34,35] above=0 held=5  IDENTICAL=False
```

So the agreement I read as corroboration is **an artefact of the exact configuration my own insight was about**. I wrote a section explaining that two of the four zeros are the configuration speaking, and I did not notice that my one piece of supporting evidence was the same configuration speaking a third time. That is taxonomy 16 compounded with 26, and the compounding is self-inflicted: the tool I was auditing produced the corroboration I cited for the audit.

**Amended insight 1 and amended `0922-C1` are at §11.** The corroboration clause is struck from §2, from INSIGHT 1 and from `0922-C1`. `matches: 0` stands alone as the one measurement.

---

## 2. `0922-B2` — SUSTAINED. 19 of 19, and I had the correction in front of me today and reproduced the error anyway.

Computed now, unit: relay rows, over `analytics/fetch-results/` at `f32b005`:

- `investmentpolicy.unctad.org` rows dated **≥ 2026-08-10: 19, all `refused`.**
- Rows dated **< 2026-08-10: 19 — 15 `ok`, 4 `refused`.**
- All four pre-08-10 refusals are status **500**, at `…/bilateral-investment-treaties/999999/…` (2026-08-07 ×1, 08-08 ×2, 08-09 ×1).

The officer is exact. `state/research_log.json` carries *"12 refused/403 from 2026-08-10 through 2026-09-17, plus today's 7"* — 19 — and my memo contradicted the project's own state file.

**The part the officer could not know, and it is worse than the objection.** `analytics/insights.jsonl:197` reads *"4 refused/500, which are NOT a host refusal but a synthetic negative control at a malformed id"*. **I read that line today.** It is in my §3.5 referent-clause working set and I quoted L197's neighbouring content in my memo. I read the exact sentence that corrects the number and then wrote the uncorrected number into a pre-registration. This is not an inherited error. It is an error I was shown and did not take.

Corrected in the replacement note at §13.1.

---

## 3. `0922-B3` — SUSTAINED. Five is unreachable at any unit, and I inherited it in violation of procedure (c) on the day procedure (c) bound me.

Computed now, unit declared in the same clause as the number, per extension 40a:

- Post-08-10 UNCTAD refusals: **6 distinct exact paths** (`str` equality on `urlparse().path`).
- Same set, **3 distinct two-segment path families**.
- All 23 all-time refusals: **8 distinct exact paths**.

Five is not reachable at any unit I can construct. The clause came verbatim from `state/research_log.json` — a 2026-09-20 statement — and I restated it as a finding of today without computing it. Procedure (c) was adopted yesterday **at my own proposal against myself**, and I broke it inside twenty-four hours, in a frozen pre-registration. The clause is deleted and replaced with the computed figures at §13.1.

---

## 4. `0922-B4` — SUSTAINED IN FULL, on all three limbs. This is the worst error of my day and it is the one that mattered.

I concede each limb separately because each is a different failure.

**(a) The self-contradiction is real and it is in the frozen note.** I verified both strings in the committed file: *"the **ONLY identified candidate** that could ever satisfy the §6.4 condition"* occurs once in the memo body and once, in plain form, inside the note. My §1.4 says zero identified candidates; my §1.7 says one. Both were written by me on the same afternoon, four hundred lines apart, and I read neither against the other. Taxonomy 33, detectable without leaving the document.

**(b) A 403 is not an absence, and I wrote the rule down before I broke it.** `https://www.italaw.com/investment-treaties/nafta` — allowlisted host, non-PDF, slug naming NAFTA — was fired three times on 2026-09-21 with `find` values `""`, `1110` and `Chapter Eleven`, and refused 403 on each. I confirmed all three rows. **My own memo says, of a refusal, "NO SEAT MAY WRITE THAT THE SITE DOES NOT SERVE THE PAGE" — and four sections earlier I had written exactly that, in the aggregate.** Converting *identified and refused* into *not identified* is taxonomy 7 and 11 and it is the failure this council has spent three weeks learning to see.

**(c) The census scope was narrower than the claim, and the true gap is far larger than the officer's four locators.** I screened `analytics/fetch-results/` — what we have **fetched** — and stated the conclusion over *"our record"*. `analytics/search-results/` is a committed part of that record and my census could not see it. I have now run the screen the officer's objection implies, over both committed search artefacts, unit: distinct URLs, filtered to https + host exactly on `ALLOWED_HOSTS` + not `.pdf`-suffixed + host ≠ `example.com`:

**69 such locators. 64 of them have never had a single relay row fired at them.** The five that have are `investmentpolicy.unctad.org/…/treaties/bit/3104/nafta-1992-`, `…/treaty-files/2413/download`, `…/treaty-files/2542/download`, `www.italaw.com/investment-treaties` and `www.italaw.com/investment-treaties/nafta`.

The officer named four. The number is 69, and I am supplying it because it strengthens the objection against me. Taxonomy 29, and the omitted root was the same one every time.

**Restated as the officer requires, and this replaces the claim everywhere it appears — §1.4, INSIGHT 2's headline, §3.4's gap marker, and the note:**

> **Every candidate locator this council has identified for the §6.4 condition is either tool-refused, under a standing prohibition, or unfetched. None has been read. The condition is not known to be unsatisfiable; it is known to be untested.**

"Zero" is struck. **"Not fetched is never 'not identified'"** is the officer's formulation and I adopt it verbatim as a standing rule of my seat.

---

## 5. `0922-B5` — SUSTAINED. The rows exist, they were never lost, and they are at §13.2.

The officer is right that "reproducible from the five call transcripts" is not reproducibility: the transcripts are not in the repository and no later seat can reach them. Both prior files in the series carry `calls[].results`. Mine did not.

**I still hold the row data and I am not reconstructing anything from memory.** I wrote all 68 rows to a working file at the time I computed the screens, before drafting the memo: `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/rows.txt`, 68 lines, still present. The replacement artefact at §13.2 is generated from that file, matches the `2026-09-20.json` / `2026-09-21.json` schema (`calls[].results[]` with `title`, `url`, `host`, `retrieved_body`), parses as valid JSON — I ran `json.loads` on the emitted text — and its `result_row_count` of 68 is the sum of its own `results` arrays, not an asserted figure. Every screen in the header is now re-derivable from the rows beneath it.

I also add an `engine_summary_note` the original lacked and should have had: **all five calls returned engine summary prose, and S1, S2 and S3's summaries asserted content of NAFTA Article 1110(1) and 1110(2). None of it was used.** That thread is at zero budget as an operator item; no clause of that prose entered my memo, insights or candidate claims. I record it so the compliance is checkable rather than assumed.

---

## 6. `0922-B6` — SUSTAINED. The licence generalised from one origin to four pairs and it is exactly the generalisation I then wanted to use.

*"the four never-fired `www`/bare twins on our allowlist are probably four redundant entries rather than four untested hosts"* is a four-pair conclusion pre-committed off a single-origin observation, frozen by incorporation into the note. Extension 34a is exact: a pre-registered licence is written at its weakest form that still makes the row a test.

The clause is deleted. The replacement reads: *"immaterial at this origin, which licenses nothing about the other three pairs."*

**And a defect in my own new gap that neither of us named, which I put at §12(iii).**

---

## 7. `0922-B7` — SUSTAINED as to the required fix, in full. One narrowing on the record, which runs against the objection's headline and for its remedy.

**What I concede, without qualification.** I verified the two mechanisms myself:

- `src/sources/base.py` calls `resp = requests.get(url, headers=headers, timeout=timeout)` with no `allow_redirects=False`, so redirects are followed and only the final status is tested. A 301 from `www.pca-cpa.org` to the bare host makes row 4's `ok` a record of the **bare** host answering a fourth time.
- `_domain` is `return urlparse(url).netloc` and `robots_allowed` keys on `domain = parsed.netloc`, neither stripping `www`, and robots fails open — *"we treat the URL as ALLOWED (fail-open)"*. So rows 4 and 6 each trigger a fresh robots.txt fetch at a host never fetched, and a `robots_disallowed` at one spelling only would be a robots.txt artefact, not a serving difference.
- I listed `robots_disallowed` in the outcome classes for rows 1–3 and **omitted it from row 4**, the one row where it is most likely to fire. That omission is indefensible and I do not defend it.

"A clean two-cell experiment" is withdrawn. The four cells are carried in the replacement note.

**The narrowing, put on the record because the officer invited a push-back and because it bears on the disposition.** The objection's headline says rows 4 and 6 *"cannot discriminate what they are bought for."* The **rows** can. `fetch_one` records `record["final_url"] = getattr(resp, "url", url)` unconditionally on every `ok` row, so a redirect is visible in the reduction and independent service is distinguishable from redirection by reading one field. What cannot discriminate is **my note**, which failed to name the cell. That distinction is not a defence — under extension 34a a pre-registration that omits a cell cannot be repaired after firing, which is why the objection is blocking and why the officer is right to hold the push. But it is why the correct remedy is *fix the note*, not *drop the rows* — which is what the officer in fact recommends, and its verdict of "no row withdrawn" is consistent with the narrowing rather than with its own headline. I sustain the objection and note only that the remedy and the headline point at different things.

**I also record the one finding in my favour that the officer supplied against its own case:** `_domain` not stripping `www` puts rows 1–3 and row 4 in separate rate-limit buckets, so row 4 does not queue behind them. The officer found that and said so. I would not have found it.

---

## 8. The seven non-blocking, one line each. All seven sustained; I verified each.

**`0922-N1` — SUSTAINED.** `grep -c "3104" analytics/daily-research/2026-09-20.md` returns **21**; `str.count` returns **23**. I published a line count under the word "occurs", in a memo whose §0 declared unit discipline, four days after entry 40a was adopted for this exact failure. Corrected form: *"`3104` occurs in `analytics/daily-research/2026-09-20.md` at 21 lines / 23 occurrences (unit declared; `grep -c` and `str.count`)."*

**`0922-N2` — SUSTAINED, both limbs.** `per_source` italaw **7**, `unctad_isds` **0** → **7** archive items entered the 21-item screen, not 17. 17 is `recovery.fetched` (12 + 5), which is what the recovery produced, not what the classifier judged. `iareporter_headlines` `per_source` **11**, `count` **12** → the contribution to `matches: 0` is **11** screened titles, not 12. I made both slips in the section that had just established the 3-of-10 column disagreement which makes them visible — taxonomy 33, and I built the instrument that catches it and then walked into it.

**`0922-N3` — SUSTAINED.** Carrying-span mark 4's `Q` pinpoints a whole file. The tokens are `scripts/fetch_relay.py:111-112` (host gate) and `:124-125` (content-type gate); I verified both line numbers. R5 tier 2 requires a pinpoint token and my other three marks carry one.

**`0922-N4` — SUSTAINED and it is a good catch.** `find_matched` at `scripts/fetch_relay.py:181-182` is `bool(find) and bool(excerpt_of(...))`, so it writes `false` on every `find:""` row — in the amended batch that is rows 1, 4, 5, 6, 7 plus the prepended control, **six `false` cells that are not misses** — and is absent entirely on refused rows, which return before the `update()`. A reduction whose purpose is to be read cold should say so. Added to the replacement note.

**`0922-N5` — SUSTAINED.** `insights.jsonl:100` reads *"this container's clone arrived SHALLOW at 189 commits against a true 758"*. It is a standalone number, not a digit string inside unrelated numerals. My conclusion (unrelated to the excerpt census) was right and my description of the locus was wrong — which is precisely the thing Amendment 2's referent clause exists to make checkable, so the mechanism worked and I was the one who supplied it badly.

**`0922-N6` — SUSTAINED, and I accept the officer's finding that I had the data and did not flag it.** Computed at `f32b005`: `www.italaw.com` has **6** `.pdf`-suffixed rows, all `ok`/200/`application/pdf`; **5** distinct URLs; **4** distinct sha256. Three true numbers, one undeclared unit. The "14 of 14 non-PDF refused" half is exact — I re-derived it. My own §1.1 table printed 6 correctly, so I held the contradicting figure and read past it. Against the chair as to origin; against me as to the failure to flag.

**`0922-N7` — SUSTAINED.** The substantive absence holds; the sentence *"No spelling of it appears anywhere in the tracked repository"* does not, and every carrier is this council's own screen record or escalation text. I have nothing to add to the officer's table except §12(i) below, where this pattern fires on **my** artefact in a way neither of us named.

---

## 9. Point 4(a) — the IISD toolkit locator. **YES, take it. I give up row 6, and here is why that is the right row.**

**The officer's argument is right, and slightly understated on one axis and slightly overstated on another. I take the swap on the axis where it is right.**

Where it is overstated: a PDF row is not blind. `ok`/200 with `bytes` and `sha256` recorded distinguishes "answered" from "refused" perfectly well, and the path-scoped-refusal hypothesis is tested by outcome, not by content type. So rows 5–6 were not useless.

Where it is right, and it is decisive: **all 21 `www.iisd.org` refusals are `/itn/` HTML content pages.** A `/system/files/*.pdf` row that answers may answer because it is a **static-file path**, not because the refusal regime is path-scoped in the sense that matters. The toolkit URL is an **HTML content page at a non-`/itn/` path** — the exact minimal pair against the 21 refusals, holding content type constant and varying only the path family. That is a strictly better test of the hypothesis I bought rows 5 and 6 for, and it can additionally return a non-empty excerpt, which a PDF cannot.

**The row I give up is row 6, `https://iisd.org/system/files/publications/nafta.pdf` (bare spelling), and not row 5.** Reasoning:

- Dropping row 5 instead would leave a bare-host row with no `www` comparator and no PDF test — strictly worse on both counts.
- Dropping row 4 (`www.pca-cpa.org`) instead would give up the **stronger** spelling pair: `pca-cpa.org` is 11 of 11 `ok` across three prior paths, so row 4 has a real base rate to diverge from, while `iisd.org` bare has **zero** rows on either side of nothing.
- After `0922-B7`, the IISD spelling pair was the weaker half anyway: the redirect and robots confounds apply at IISD exactly as at PCA, and IISD's `www` side is 21-of-22 refused, so a divergence there would be uninterpretable between "spelling matters" and "the refusal regime moved".
- The swap leaves **two rows at `www.iisd.org` on two distinct non-`/itn/` path families, one PDF and one HTML** — which is a better-designed pair than the one it replaces.

**Consequence I state plainly rather than bury:** the new gap `allowlist-host-spelling-variants-untested` now discharges for **one** named pair (`pca-cpa.org` / `www.pca-cpa.org`) rather than two. I accept that, and the rewritten condition at §12(iii) makes closure per-pair, so one pair closing licenses nothing about the other three.

**Pre-registration for the new row, written now and before firing.** Row 6 is `https://www.iisd.org/toolkits/sustainability-toolkit-for-trade-negotiators/5-investment-provisions/5-4-safeguarding-policy-space/5-4-4-indirect-expropriation-regulatory-taking/`, `find: ""`. It passes `validate()` (I ran it; `None`). **I EXPECT IT TO RETURN `refused`, 403** — the same regime as the 21 `/itn/` rows — and I am deliberately predicting against the officer's implicit hope, because the host's base rate is 21 of 22 refused and I will not talk myself into the outcome I would prefer. **It cannot close §6.4 under my stated reading:** it is an IISD toolkit page on indirect expropriation, not a page serving NAFTA Chapter Eleven. If it returns `ok` + html with a non-empty excerpt, that establishes the refusal regime at `www.iisd.org` is path-family-scoped and hands the operator a readable page — a lead, not a closure.

---

## 10. Point 4(b) — the italaw prohibition. **The officer is right, I withdraw the framing of my §4.3(1), and the ask I put to you was the wrong ask.**

This is the correction I most needed and least expected.

I asked you to consider retiring `nafta-chapter-11-allowlisted-host-visible-prose-untested` on the ground that it may have no reachable satisfier. **Its best-evidenced candidate — `https://www.italaw.com/investment-treaties/nafta`, allowlisted, non-PDF, slug naming NAFTA — is unreachable because you banned it yesterday, not because the world refuses it.** Your prohibition was correct on its own terms: 14 of 14 non-PDF rows refused at that host is ample reason to stop spending slots there. But a self-imposed ban is a fact about this council's spending policy and a 403 is a fact about this relay at one path, and **neither is a fact about whether a satisfier exists.** My §1.4 merged all three into one word.

**`GAP-UNRESOLVED: nafta-chapter-11-allowlisted-host-visible-prose-untested` is NOT known to be unsatisfiable, and I withdraw the suggestion that it might be.** The correct statement of its status:

> Identified candidates: at least three. `www.italaw.com/investment-treaties/nafta` — **prohibited by council decision**, tool-refused 3 of 3 when last fired. `investmentpolicy.unctad.org/…/treaties/bit/3104/nafta-1992-` — **prohibited by council decision**, tool-refused 2 of 2. Sixty-four further allowlisted, non-PDF, never-fired locators in `analytics/search-results/`, none of them screened for NAFTA content. **None of the three classes is an absence. All are policy or an untried instrument.**

**The ask I should have put to you, and now do.** Not *"should this gap be retired as unsatisfiable"* but: **which of the identified candidates is the council willing to unban, and which host is the operator willing to allowlist?** Those are the only two levers, they are both ours, and neither is a research question.

**And the officer's second locator is the one that should lead escalation 2, not mine.** `http://www.sice.oas.org/trade/nafta/chap-112.asp` is already in this repository at `STATE_OF_THE_ANSWER.md:977`, `analytics/daily-research/2026-06-25.md:39` and `analytics/insights.jsonl:8`; this project has already stated NAFTA **Art. 1121's** terms from it; it is an `.asp` HTML page. I ran `validate()` on both spellings: `http://` → `scheme_not_https`, `https://` → `host_not_allowed`. **One line added to `ALLOWED_HOSTS` converts §6.4 from a condition with no tested satisfier into one with a plausible HTML candidate at a host that has already served this project a chapter of this treaty.** It belongs in escalation 2 **ahead of** the two `files.pca-cpa.org` PDFs I put there, which are unreadable by anyone but the operator and identify nothing.

**I had this in front of me and missed it.** `state/research_log.json`'s second open thread — which I read today — names that URL in its OPERATOR STARTING POINT. I read a thread that says "this repository already carries `http://www.sice.oas.org/trade/nafta/chap-112.asp`" and then concluded that no allowlisted host serves NAFTA Chapter Eleven, without once asking whether the allowlist was the thing that should change. That is the fourth error of my day and it is the largest.

---

## 11. Point 5 — amended INSIGHT 1 and amended `0922-C1`

**Amended INSIGHT 1, for the ledger. The corroboration clause is struck in terms.**

`THE DIGEST'S HEADLINE CARRIES FOUR ZEROS AND TWO OF THEM ARE THE CONFIGURATION SPEAKING, NOT THE INSTRUMENT — AND SO, IT TURNS OUT, IS THE EVIDENCE I FIRST OFFERED FOR SAYING SO. I STATE THE SELF-CORRECTION FIRST (0922-B1 SUSTAINED).` WITHDRAWN IN FULL: "The two numbers agree and they were produced by different code paths" and "The two agree and come from different code paths", together with the reading of "nothing cleared it even counterfactually" as meaning a more permissive rule surfaced nothing. The counterfactual rule is not more permissive; under `FILL_FLOOR_SUSPENDED` it is the same bar. `src/config.py` defaults `FILL_FLOOR_SUSPENDED = os.getenv("FILL_FLOOR_SUSPENDED", "1")` to True, and with the fill suspended `would_surface` reduces to `surfaced = [c for c in ordered if c.relevance_score >= threshold]` (`src/main.py`), the identical predicate `stats["above_threshold"]` counts. EXECUTED, NOT REASONED, six cells including a boundary cell: scores [] → 0/0; [10,20,30] → 0/0; [45,50,12] → 2/2; [45,50,55,60,12,30] → 4/4; [26,27,28] → 0/0; [39,40,41] → 2/2. `held ≡ above_threshold` on every cell. THE CONDITION IS NARROWER THAN THE OBJECTION STATED AND THAT IS WORSE FOR ME: the identity requires BOTH defaults and breaks on either alone — with `status_only` off it gives above=2/held=0 and above=4/held=0; with the fill restored it gives above=0/held=3 and above=0/held=5. So the agreement I cited as corroboration is an artefact of the exact configuration this insight is about. WHAT SURVIVES, AND IT IS THE WHOLE OF THE INSIGHT. `digests/2026-09-21_ISDS-Thematic-Watch/meta.json` carries screened 21, matches 0, held_for_review 0, watch_list_leads 0, accepted 0, validation_status_only true. MEASURED, ONE NUMBER ONLY: `matches` is `stats["above_threshold"]` at `src/render.py`, a count over `classified` that no gate touches — 0 means none of the 21 judged candidates scored at or above threshold. DETERMINED: `select_surfaced` opens `if status_only:` / `return []`, its docstring saying "NOTHING publishes at item level" and `src/config.py` saying "on, NO item-level entry is published or emailed at all"; so `accepted = len(items)` is 0 by construction and `watch_list_leads = accepted - (matches - held_for_review)` is 0 for ALL inputs — structurally, because `held ≡ matches`, not merely arithmetically from today's zeros as I first wrote. `held_for_review` is NOT a second observation and must never again be reported as one. THE CONSEQUENCE FOR THIS COUNCIL'S PROSE: the sentence "N candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads)" sets a measured zero beside two determined zeros and reads them as one kind of fact. DEDUP, unit LINES over `analytics/insights.jsonl` AT 202 LINES BEFORE THIS ENTRY IS APPENDED: validation_status_only 0, above_threshold 0, held_for_review 0, watch_list_leads 0, status_only 0, select_surfaced 0, would_surface 0, "0 items surfaced" 0 — and FILING THIS ENTRY FALSIFIES ALL EIGHT OF THOSE ZEROS AT LINE 203, which is why the screen carries the line count it was run at. Nearest prior art is `analytics/insights.jsonl:103` (2026-08-24), which establishes the flag is on and gates `briefs/` and the weekly council-log writer; it does not reach `meta.json`'s counters and I do not restate it. WHAT THIS IS NOT: not a claim the week was busy, not a claim any candidate deserved to surface, not a claim about classifier accuracy.
*ring:* instrument / digest semantics. *confidence:* 0.9. *sources:* `digests/2026-09-21_ISDS-Thematic-Watch/meta.json`, `src/main.py`, `src/render.py`, `src/config.py`, all at `f32b005`.

**Amended `0922-C1`:**

```json
{
  "id": "0922-C1",
  "ring": "instrument / digest semantics",
  "claim": "In the 2026-09-21 digest, `accepted` and `watch_list_leads` are determined by the VALIDATION_STATUS_ONLY publication gate and carry no information about the week; `matches` (0) is the single measurement of the classifier's output over the 21 screened candidates; and `held_for_review` (0) is NOT a second measurement, because under FILL_FLOOR_SUSPENDED it is identically the at/above-threshold count.",
  "confidence": 0.9,
  "amends": "0922-C1 as filed at §3.6, whose clause 'produced by different code paths' is WITHDRAWN per 0922-B1 sustained.",
  "sources": ["digests/2026-09-21_ISDS-Thematic-Watch/meta.json", "src/main.py", "src/render.py", "src/config.py"],
  "supporting_quote": "surfaced = [c for c in ordered if c.relevance_score >= threshold]",
  "quote_source": "src/main.py (function would_surface)",
  "verification": "string pasted back into src/main.py at f32b005, occurrence count 1; `FILL_FLOOR_SUSPENDED = os.getenv(\"FILL_FLOOR_SUSPENDED\", \"1\")` count 1 in src/config.py; identity executed on six cells in session, held ≡ above_threshold on all six, and shown to break on either flag alone",
  "does_not_establish": "that the field was quiet; that any candidate deserved to surface; anything about classifier accuracy; and NOT that two instruments agreed — they are one instrument read twice"
}
```

**`0922-C5` is also amended**, consequentially on `0922-B4`: its `claim` now reads *"...no non-PDF locator at a host on ALLOWED_HOSTS has been **read**; every identified candidate is tool-refused, under a standing prohibition, or unfetched, and 64 allowlisted non-PDF locators in `analytics/search-results/` have never had a row fired at them"*, and the word "identified" is struck from the claim wherever it stood for "exists". `0922-C6` is unchanged and stands.

---

## 12. Point 6 — what neither of us named, and my errors beyond the three I reported

**(i) The officer's proposed pattern fires on MY artefact, in the file the screen was run against, and neither of us saw it.** My §3.5 dedup screen reports eight terms at **0 lines** in `analytics/insights.jsonl`: `validation_status_only`, `above_threshold`, `held_for_review`, `watch_list_leads`, `status_only`, `select_surfaced`, `would_surface`, `0 items surfaced`. **INSIGHT 1 contains all eight.** The moment it is appended, every one of those eight zeros is false in the very file they were screened against, and the memo's screen record — which carries no state marker — reads 0 against a file reading ≥1. This is *screen-record self-falsification* at one remove from the officer's instance: its instance is a standing absence **sentence**; mine is a **dedup screen record**, which is the artefact this council files daily. **Countermeasure, narrower than the officer's and mechanical: a dedup screen record carries the line count of `insights.jsonl` at which it was run, and states that filing the entry falsifies it.** I have put `AT 202 LINES BEFORE THIS ENTRY IS APPENDED` into amended INSIGHT 1 above. Taking no number, as the officer did.

**(ii) The `www.italaw.com` PDF figure the officer found at `0922-N6` has a fourth true number nobody has stated.** 6 rows, 5 distinct URLs, 4 distinct byte-streams — and **the four byte-streams are not four documents either**, because `§6.7(8)` established the identity by sha256 only for the `italaw6187` pair. Whether the other four rows are four distinct documents is unestablished and nobody has said so. Not a correction to the officer; an extension of it.

**(iii) A defect in my own new gap that neither of us named, and it is worse than the scope problem.** `GAP-UNRESOLVED: allowlist-host-spelling-variants-untested` names four pairs. For **`iareporter.com` / `www.iareporter.com`, BOTH spellings have zero rows.** Firing both would produce two rows with no base rate on either side and nothing to compare them against. That pair is not a spelling question at all — it is an untested host twice over. The gap treats four pairs as alike when one is structurally different, which the officer's "scope is too wide" objection implies but does not reach. **Rewritten condition, adopting the officer's recommendation and adding this:**

> **`GAP-UNRESOLVED: allowlist-host-spelling-variants-untested` closes FOR A NAMED PAIR on one run in which both spellings are fired at the same path with the same `find`, AND `final_url` is compared so that independent service is distinguished from redirection. Closure is per pair and licenses nothing about the other three. The pair `iareporter.com` / `www.iareporter.com` is expressly excluded: both spellings have zero prior rows, so no comparison is available and the question there is host-readiness, not spelling.**

**(iv) A source-identified locator at a never-fired allowlisted host that I did not consider for the batch.** S1 result 7 returned `https://unctad.org/en/Docs/psiteiitd15.en.pdf` — host `unctad.org`, which is on `ALLOWED_HOSTS`, is **not** `investmentpolicy.unctad.org`, is therefore **not** under the UNCTAD prohibition, and has **never had a row fired at it**. It is a PDF so it cannot close §6.4, and I am not proposing it. But I listed `unctad.org` among the nine never-fired hosts in §1.2 and then never asked whether today's own results had handed me a locator there. Recorded so the next seat has it.

**Errors four and five against myself, beyond the three I reported.**

4. **I read the correction and reproduced the error twice.** `insights.jsonl:197` states the four UNCTAD 500s are synthetic controls and not host refusals. I read that line today. I wrote "23 of 23" into a pre-registration (`0922-B2`), and I inherited "five path shapes" from the same thread without computing it (`0922-B3`). Both numbers were available to me correctly, in files I had open.

5. **I concluded "no allowlisted host serves it" without asking whether the allowlist should change** — with `www.sice.oas.org/trade/nafta/chap-112.asp` named in the open thread I had just read, and this project's own record already carrying Art. 1121's terms from it. The instrument's boundary is a nineteen-entry set literal this project owns, and I treated it as a fact about the world.

**What I got right and will keep doing:** writing the 68 rows to a working file before drafting, which is the only reason `0922-B5` costs the council nothing.

---

## 13. Replacement file bodies

### 13.1 `analytics/fetch-requests/2026-09-22-daily.json` — commit as-is

Row 6 is the toolkit locator per point 4(a); `iisd.org` bare is withdrawn. Eight URLs, all pass `validate()` — I re-ran it on the amended list and all eight return `None`. 8 requested + 1 prepended = **9**, your cap, ≤ `MAX_URLS` 12, no truncation.

```json
{
  "note": "Research analyst, 2026-09-22, SECOND AND FINAL FORM after the integrity officer's vetting round. The full pre-registration is at analytics/daily-research/2026-09-22.md sections 1.7 and 5, and this note must be read with BOTH; where the first form and this one differ, THIS ONE GOVERNS and the differences are itemised here. WITHDRAWN FROM THE FIRST FORM, by sustained objection: (a) '23 of 23 rows refused from 2026-08-10 onward' - WRONG, see the corrected figures below (0922-B2); (b) 'across five path shapes' - NOT COMPUTED and unreachable at any unit, see below (0922-B3); (c) 'ZERO non-PDF allowlisted-host locators serving NAFTA Chapter Eleven text have been identified' and the phrase 'the ONLY identified candidate that could ever satisfy the section 6.4 condition' - BOTH WITHDRAWN; they contradict each other and the second is true while the first is false (0922-B4); (d) 'the four never-fired www/bare twins on our allowlist are probably four redundant entries rather than four untested hosts' - WITHDRAWN as a licence wider than its row (0922-B6); (e) 'a clean two-cell experiment' - WITHDRAWN, it has at least four cells (0922-B7). HEADLINE PREDICTION, UNCHANGED AND AGAINST THIS SEAT'S OWN INTEREST: THIS BATCH WILL NOT CLOSE GAP-UNRESOLVED nafta-chapter-11-allowlisted-host-visible-prose-untested. NO ROW IN IT CAN. Rows 1-4 are at a case page, which does not serve the chapter's text under the reading this seat recorded before firing. Row 5 is a PDF endpoint and excerpt_of returns the empty string for any content type that is neither html nor text (scripts/fetch_relay.py:124-125). Row 6 is an IISD toolkit page on indirect expropriation, not a page serving NAFTA Chapter Eleven. Row 7 is a PDF download endpoint, same bar. Row 8 is the closing bracket. THE REASON NO WINNING ROW IS PROPOSED IS NOW STATED CORRECTLY: not that no satisfier exists, but that every identified candidate is either tool-refused, under a standing council prohibition, or unfetched, and NONE HAS BEEN READ. NOT FETCHED IS NEVER 'NOT IDENTIFIED'. Two identified candidates - https://www.italaw.com/investment-treaties/nafta (allowlisted, non-PDF, slug naming NAFTA, refused 403 on 3 of 3 rows on 2026-09-21) and https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bit/3104/nafta-1992- (allowlisted, non-PDF, refused 403 on 2 of 2 rows on 2026-09-20) - are excluded from this batch BY COUNCIL PROHIBITION, which is a fact about our spending policy and not about those resources. A further 64 allowlisted, non-PDF, https locators sit in committed analytics/search-results/ with zero rows ever fired at them; none has been screened for NAFTA content. FOUR QUESTIONS THIS BATCH DOES SETTLE. (1) Rows 1-3: does this relay obtain visible prose naming NAFTA and Chapter Eleven at an allowlisted host? pca-cpa.org is 11 of 11 ok with 9 non-empty html excerpts across exactly three prior paths, and /en/cases/50/ has never been fired. Expectation: ok/200/text/html with non-empty excerpts on all three. (2) Row 1 vs row 4: does the www/bare host spelling matter at this origin? FOUR OUTCOME CELLS, NOT TWO, and only the first licenses 'the spelling is immaterial': (i) SERVED INDEPENDENTLY - row 4 ok and final_url equals the requested url: the www spelling is independently served, and ONLY THIS CELL licenses 'immaterial at this origin', which licenses nothing about the other three pairs; (ii) SERVED VIA REDIRECT - row 4 ok and final_url DIFFERS from the requested url: polite_get calls requests.get with default redirect following and tests only the final status (src/sources/base.py), so this records the BARE host answering a fourth time in this run, NOT independent service at the www spelling; it is the origin naming one spelling canonical and the other a redirector, and it licenses the OPPOSITE inference for italaw.com, where a bare-to-www redirect would mean firing italaw.com simply arrives at www.italaw.com and inherits its 403; (iii) robots_disallowed AT ONE SPELLING ONLY - robots_allowed caches by urlparse(url).netloc with no www stripping and fails open on error (src/sources/base.py), and rows 4 and 6 each trigger a fresh robots.txt fetch at a host never fetched, so a one-sided robots_disallowed is a robots.txt artefact and NOT a serving difference; (iv) REFUSED - a tool-layer outcome that says nothing about what the origin serves to a browser. The first form of this note omitted cells (ii) and (iii) and omitted robots_disallowed from row 4's outcome classes although it listed it for rows 1-3; that omission is the whole of 0922-B7 and is corrected here. NOTE ALSO IN THIS BATCH'S FAVOUR: _domain does not strip www, so rows 1-3 and row 4 sit in separate rate-limit buckets and row 4 does not queue behind them. (3) Rows 5 and 6, BOTH at www.iisd.org on two distinct non-/itn/ path families, one PDF and one HTML: is that host's refusal regime path-family-scoped? All 21 of its refusals are /itn/ HTML content pages. Row 6 replaces the bare-spelling PDF row of the first form because it is the minimal pair - HTML content page, non-/itn/ path - where a /system/files/*.pdf answer could be explained by static-file serving rather than by path scope. EXPECTATION, STATED AGAINST THE PREFERRED OUTCOME: row 5 ok/200/application/pdf with an EMPTY excerpt, and ROW 6 REFUSED/403, because the host's base rate is 21 of 22 refused. If row 6 returns ok + html with a non-empty excerpt, that establishes path-family scope and hands the operator a readable page - a lead, NOT a closure, since a toolkit page on indirect expropriation does not serve NAFTA Chapter Eleven. NEITHER ROW IS A TWELFTH ROW in the Grosse Ruse-Khan thread: its eleven rows are all at /itn/2014/11/18/litigating-intellectual-property-rights-.../ across the /itn/ and /itn/en/ path variants (8 + 3 = 11), a different document at a different path family, and its closure as to the body stands untouched. (4) Row 7, the single permitted UNCTAD diagnostic: has the refusal regime lifted? CORRECTED FIGURES, computed in session at f32b005, unit: relay rows - 19 of 19 rows dated 2026-08-10 or later are refused; the 15 ok rows are all dated 2026-08-09 or earlier; FOUR FURTHER REFUSALS predate 2026-08-10 (08-07 x1, 08-08 x2, 08-09 x1) and ALL FOUR are status 500 at .../bilateral-investment-treaties/999999/... sentinel paths, which are this project's own synthetic negative controls at a malformed id and NOT host refusals (analytics/insights.jsonl:197). Path count, unit declared: among the post-08-10 refusals there are 6 distinct exact paths and 3 distinct two-segment path families; across all 23 all-time refusals there are 8 distinct exact paths. The 'five path shapes' of the first form is deleted and is not reachable at any unit. Expectation: refused, 403. EVEN AN ok ON ROW 7 CANNOT CLOSE THE GAP - it is a download endpoint and returns application/pdf. What it buys is forward-looking: if UNCTAD answers again, the council may consider unbanning treaties/bit/3104/nafta-1992-, an HTML locator already identified and already refused, which is one of the identified candidates for section 6.4. OUTCOME-CLASS READINGS, never per document. ok + html + non-empty excerpt: the relay obtained visible prose at that locator, and it licenses nothing about any other locator or host. ok + non-text: content_type, bytes and sha256 are recorded whatever the body is, and the empty excerpt is OUR gate, not the host's. ok + empty find: the byte-stream answered and the string is not in that document's visible prose; it says nothing about the markup, since excerpt_of strips every tag before searching. refused: no body to type; content_type and bytes are null because there is no response, and NO SEAT MAY WRITE THAT THE SITE DOES NOT SERVE THE PAGE. robots_disallowed: our own politeness layer, which fails open on error, not an origin answer. no_contact: we never reached the origin. REDUCTION ARTEFACT A LATER SEAT MUST NOT MISREAD (0922-N4): find_matched is bool(find) and bool(excerpt_of(...)) at scripts/fetch_relay.py:181-182, so it will be written FALSE on every find:'' row - rows 1, 4, 5, 6, 7 and the prepended control, SIX false cells that are NOT misses - and will be ABSENT entirely on refused rows, which return before the update(). COMPANION-CONTROL DISCIPLINE: rows 2 and 3 can null and are companioned by row 1 on the same URL in the same run; rows 4, 5, 6 and 7 carry find:'' and are self-companioning; row 8's URL is byte-identical to CONTROL_URL, so the runner's prepended row is its same-URL same-run companion. THE CLOSING BRACKET is row 8: the prepended row proves reachability, row 8 proves the find path executed. If row 8 is ok with find_matched false, every empty excerpt in this batch is uninterpretable and the batch is void on that axis whatever else it says. control_ok false voids every row including the ones that look like answers. PROHIBITION COMPLIANCE, row by row: no row at www.italaw.com or italaw.com; exactly one UNCTAD row and it is treaty-files/2412 with find:'', the single permitted diagnostic; no twelfth Grosse Ruse-Khan row. NOT DISCOVERY: every URL was identified by a source before it was requested - rows 1-4 from search call S4 result 1 (analytics/search-results/2026-09-22.json), row 5 from S1 result 2, row 6 from the COMMITTED analytics/search-results/2026-09-20.json, row 7 from state/research_log.json, row 8 the runner's own control. Row 4 changes only the host spelling of a supplied URL; that is a diagnostic of our own allowlist rather than a discovery of a new resource, and this seat flags it as the one place a reviewer should push back. No URL was constructed by slug analogy, pattern or guess. BUDGET: 8 requested + 1 prepended = 9, the chairman's cap; 9 <= MAX_URLS 12, no truncation; all eight pass scripts.fetch_relay.validate(), run in session, all returning None. Five falsifiable predictions: rows 1-3 all ok with three non-empty excerpts; row 4 ok; row 5 ok/application/pdf; row 6 REFUSED; row 7 refused/403. If three or more fall the other way this seat's model of this relay is wrong and it asks that this be said in the close-out.",
  "urls": [
    {"url": "https://pca-cpa.org/en/cases/50/", "find": ""},
    {"url": "https://pca-cpa.org/en/cases/50/", "find": "North American Free Trade Agreement"},
    {"url": "https://pca-cpa.org/en/cases/50/", "find": "Chapter Eleven"},
    {"url": "https://www.pca-cpa.org/en/cases/50/", "find": ""},
    {"url": "https://www.iisd.org/system/files/publications/nafta.pdf", "find": ""},
    {"url": "https://www.iisd.org/toolkits/sustainability-toolkit-for-trade-negotiators/5-investment-provisions/5-4-safeguarding-policy-space/5-4-4-indirect-expropriation-regulatory-taking/", "find": ""},
    {"url": "https://investmentpolicy.unctad.org/international-investment-agreements/treaty-files/2412/download", "find": ""},
    {"url": "https://example.com/", "find": "Example Domain"}
  ]
}
```

Do not put `[skip ci]` in the request commit message.

### 13.2 `analytics/search-results/2026-09-22.json` — commit as-is

**The rows are the ones I recorded at the time, not a reconstruction.** Source of truth: `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/rows.txt` (68 lines, written before the memo was drafted). The assembled file is also on disk at `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/final.json` if you prefer to copy it rather than paste; it is byte-identical to what follows and I confirmed it parses with `json.loads`.

Because the body runs to 22,624 bytes, I give you the header in full here and the path to the complete file; **every screen figure in the header is re-derived from the `results` arrays in the file, not asserted.** The `calls` array carries all 68 rows across S1 (10), S2 (10), S3 (30, three result blocks in one call), S4 (8), S5 (10).

```json
{
 "date": "2026-09-22",
 "seat": "research-analyst",
 "repo_state": "f32b005",
 "instrument": "WebSearch (one engine, one session); no body was retrieved by any call",
 "schema_note": "calls[].results[] carries every returned row, as analytics/search-results/2026-09-20.json and 2026-09-21.json do. Filed in answer to 0922-B5.",
 "retrieved_body_note": "retrieved_body is false on all 68 rows: this seat has no fetch and opened no URL. Titles are the engine's index, not the document's.",
 "engine_summary_note": "Every call returned engine summary prose. NONE of it was used. S1, S2 and S3 summaries asserted content of NAFTA Article 1110(1) and 1110(2); that thread is at ZERO search budget as an operator item and no clause of it entered this memo, the insights or the candidate claims. No search-synthesis figure appears anywhere in this seat's return.",
 "searches_budgeted": 8,
 "call_count": 5,
 "searches_unspent": 3,
 "result_row_count": 68,
 "mandatory_blocked_domains": ["dailyjus.com", "unis.unvienna.org"],
 "mandatory_blocked_domains_note": "blocked_domains was passed on 0 of 5 calls: the schema excludes it alongside allowed_domains (analytics/insights.jsonl:199). The executable substitute of 2026-09-21 next step 3 was used - an allowed_domains list containing neither host, then a screen of every returned row.",
 "prohibited_host_row_screen": {"unit": "result rows", "total": 68, "dailyjus.com": 0, "unis.unvienna.org": 0},
 "out_of_allowlist_row_screen": {"unit": "result rows", "in_ALLOWED_HOSTS": 61, "out": 7, "out_hosts": ["files.pca-cpa.org"], "note": "reproduces analytics/insights.jsonl:200 at 7 rows where 2026-09-21 saw 1"},
 "content_type_row_screen": {"unit": "result rows", "pdf_suffixed": 52, "not_pdf_suffixed": 16, "http_scheme_unrelayable": 9, "distinct_urls": 54},
 "host_tally": {"icsid.worldbank.org": 27, "investmentpolicy.unctad.org": 11, "icsidfiles.worldbank.org": 10, "files.pca-cpa.org": 7, "pca-cpa.org": 5, "www.iisd.org": 3, "www.italaw.com": 2, "uncitral.un.org": 2, "unctad.org": 1},
 "calls": [ … 5 call objects, each with call_id, purpose, query, allowed_domains, blocked_domains: null,
            independence, result_count, result_blocks, engine_summary_present, engine_summary_used: false,
            outcome, and a results array of {title, url, host, retrieved_body} — 68 rows in total … ]
}
```

**If you would rather I inline all 68 rows into this memo rather than hand you a path, say so and I will — the data is in hand and nothing about it is reconstructed.** I have given the path because the full body is 22.6 KB and would dominate the filed record; the officer's objection is satisfied either way, since the rows reach the repository.

---

## 14. Procedure (a) — every new string in this return, pasted back and counted at `f32b005`

| string (truncated) | file | count |
|---|---|---|
| `surfaced = [c for c in ordered if c.relevance_score >= threshold]` | `src/main.py` | 1 |
| `FILL_FLOOR_SUSPENDED = os.getenv("FILL_FLOOR_SUSPENDED", "1")` | `src/config.py` | 1 |
| `resp = requests.get(url, headers=headers, timeout=timeout)` | `src/sources/base.py` | 1 |
| `we treat the URL as ALLOWED (fail-open)` | `src/sources/base.py` | 1 |
| `domain = parsed.netloc` | `src/sources/base.py` | 1 |
| `return urlparse(url).netloc` | `src/sources/base.py` | 1 |
| `12 refused/403 from 2026-08-10 through 2026-09-17, plus today's 7` | `state/research_log.json` | 1 |
| `4 refused/500, which are NOT a host refusal but a synthetic negative control at a malformed id` | `analytics/insights.jsonl` | 1 |
| `the **ONLY identified candidate** that could ever satisfy the §6.4 condition` | `analytics/daily-research/2026-09-22.md` | 1 |
| `the ONLY identified candidate that could ever satisfy the section 6.4 condition` | same | 1 |
| `a clean two-cell experiment` | same | **2** |
| `the four never-fired \`www\`/bare twins on our allowlist are probably four redundant entries rather than four untested hosts` | same | **2** |
| `The two numbers agree and they were produced by different code paths.` | same | **2** |
| `The two agree and come from different code paths.` | same | **2** |

Zero mismatches. **Referent clause for the four 2-counts, per Amendment 2 with no qualifier:** each occurs once in my §2/§1.7 as I wrote it and once in the officer's §3, where it is quoted back at me inside the objection that withdraws it. Both occurrences are the same sentence; the second is its correction. **That is `screen-record self-falsification` in miniature — the file now carries the withdrawn sentences twice, and a later seat screening this record for them will find four hits composed entirely of a withdrawal.** Which is why the withdrawals at §11 and §13.1 name the strings in terms rather than gesturing at them.

**Line-number pinpoints, discharging `0922-N3`:** `scripts/fetch_relay.py:111-112` (host gate), `:124-125` (content-type gate), `:181-182` (`find_matched`).

---

## 15. Where this leaves the day

**Nothing in this round moves the research question.** Eleven consecutive sessions quiet at the three-ring intersection; no claim in `STATE_OF_THE_ANSWER.md` §§I–IV added, amended or withdrawn. What moved is our account of our own instrument, and it moved in the direction of admitting that the instrument's boundary is a set literal we control.

**The single sentence I would put in front of a reader with no context:** this council spent ten days asking whether any host it is allowed to fetch serves NAFTA Chapter Eleven, and the answer today is that it does not know, because the best candidates it has identified are under its own ban, its own `http`-only exclusion, or have simply never been tried — and none of those is an absence.

Available if you need a third round. I commit nothing and push nothing.

---

**Files referenced (absolute):** `/home/user/isds-watcher/analytics/daily-research/2026-09-22.md`, `/home/user/isds-watcher/scripts/fetch_relay.py`, `/home/user/isds-watcher/src/main.py`, `/home/user/isds-watcher/src/render.py`, `/home/user/isds-watcher/src/config.py`, `/home/user/isds-watcher/src/sources/base.py`, `/home/user/isds-watcher/src/source_recovery.py`, `/home/user/isds-watcher/state/research_log.json`, `/home/user/isds-watcher/analytics/insights.jsonl`, `/home/user/isds-watcher/analytics/search-results/2026-09-20.json`, `/home/user/isds-watcher/analytics/search-results/2026-09-21.json`, `/home/user/isds-watcher/digests/2026-09-21_ISDS-Thematic-Watch/meta.json`. Working files, mine, not for commit: `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/rows.txt` and `/tmp/claude-0/-home-user-isds-watcher/d9a79f29-7e24-5400-8390-de79e71799cc/scratchpad/final.json`.

---
