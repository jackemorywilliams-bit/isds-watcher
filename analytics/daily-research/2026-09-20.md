# ISDS Thematic Watcher — daily council meeting, 2026-09-20

**Chair:** council chairman (this session, `claude-opus-5` per the operator's model directive of
2026-08-03). **Convened seats:** `research-analyst`, `integrity-officer` — both REAL subagents
spawned with the Agent tool against their committed definitions in `.claude/agents/`. No seat is
performed. Where a member's text appears it is filed VERBATIM and is marked as such.

---

## 1. Agenda and channel state

### 1.1 Member return-path protocol

Each member is spawned with the Agent tool against its committed definition, is given its own
bounded task list, and returns a finished memo as its final message. I paste that memo into this
record unaltered, inside a fenced block, under the seat's own heading. **I write no member's
contribution.** If a spawn fails or a member does not return, that seat is recorded as PROCEDURAL
FAILURE for the day and the failure is stated in my close-out and in my final summary — it is never
covered by me writing the part myself. Members do not commit; they hand me file bodies and I commit
them, so that every artefact in the day's diff has one author of record.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

I inherited no channel status. I re-probed, as the convening instruction requires, and the probes
were on neutral controls rather than on research hosts, so that nothing in this section can be
misread as a statement about a source.

| Probe | Target | Result |
|---|---|---|
| `curl` egress | `https://example.com` | `curl: (56) CONNECT tunnel failed, response 403` |
| `curl` egress | `https://en.wikipedia.org/wiki/Main_Page` | `curl: (56) CONNECT tunnel failed, response 403` |
| agent-proxy status endpoint | — | both logged under `recentRelayFailures` as `connect_rejected`, *"gateway answered 403 to CONNECT"*, at `2026-09-20T11:09:57.022Z` and `2026-09-20T11:09:57.265Z` |
| fetch service | `https://example.com` | `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by the network egress proxy."}` |

**NO STATE CHANGE.** This scheduled cloud session has no fetch of its own, on either channel, for
the same reason and with the same signature as every session since 2026-08-02. **That is a fact
about this container and not about any resource on the internet** — the standing
tool-status-as-source-state rule binds every seat today, and no refusal recorded anywhere in this
record may be read as a property of the thing refused.

**THE RELAY IS ALIVE AND NOTHING IS STRANDED.** `.github/workflows/fetch-relay.yml` is committed and
fired on every session through 2026-09-19. It is the day's only retrieval instrument. Fetch-dependent
work is therefore *routed*, not abandoned, and I will not record any item as stranded on the ground
that this session cannot fetch.

`WebSearch` works. `WebFetch` does not.

### 1.3 The digest of record

`Digest 2026-09-14: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`

Copied from `digests/2026-09-14_ISDS-Thematic-Watch/meta.json` (`"screened": 7`, `"matches": 0`,
`"watch_list_leads": 0`), not restated from memory. **Unchanged**, and correctly so: `weekly.yml`
runs `cron: "0 13 * * 1"` — Mondays 13:00 UTC. 2026-09-14 was a Monday and today is a Sunday, so the
next collection is due tomorrow and the archive is not late. I record the cadence check because
"the digest is unchanged" and "the collector has stopped" look identical from inside a record, and
only one of them is a finding.

### 1.4 What I asked the analyst to work on, and why

Seeded from §6.5 of `analytics/daily-research/2026-09-19.md`.

1. **NAFTA Article 1110(7) — and I asked the analyst to attack my predecessor's premise rather than
   inherit it.** Yesterday escalated this to the operator (§6.6 escalation 2) on the ground that the
   only locator the project holds, `http://www.sice.oas.org/trade/nafta/chap-112.asp`, sits at a host
   absent from `ALLOWED_HOSTS`, so no relay row can reach it. **That reasoning establishes that one
   locator is unreachable. It does not establish that the text is unreachable** — and the step from
   the first to the second is the exact move this council has been sustaining objections against all
   week. `investmentpolicy.unctad.org` and `unctad.org` are on the allowlist and carry full treaty
   texts. If Article 1110 is served in visible prose at an allowlisted host, the council can close
   this itself and an operator escalation that has run one day should be withdrawn rather than
   repeated. Budget: 4 searches.
2. **The standing question**, bounded at 3 searches. Nine consecutive quiet sessions; a null stated
   plainly is a correct return and I told the seat I would grade padding harder than a null.
3. **One relay batch, at most 10 rows including every control**, or zero rows with the reason.
4. **Cure escalation 4 today rather than escalate it a seventh time.** The search track has been
   unauditable by our own machinery for six consecutive sessions, and yesterday that cost a sustained
   blocking objection (`0919-B4`). The ask both seats have now made is a committed
   `analytics/search-results/<date>.json`. It has been escalated as a `scripts/` build — but **the
   artefact does not need the script.** `analytics/` is inside this session's merge scope, so I told
   the analyst to file the JSON by hand, in the exact schema §4 of yesterday's Part 5 specified. A
   hand-written artefact is not the machinery and does not retire the escalation; it does mean
   today's search track is checkable, which no prior session's is.
5. Method, errors, carrying-span marks, gap markers, insights (dedup screen printed first),
   `candidate_claims`.

### 1.5 The standing prohibitions, carried unreleased

Carried into the analyst's brief verbatim and unreleased: **ZERO** search budget and no rows for the
EU Pharma-Package / Ring-2 object (demoted 2026-09-19 — its status is **UNKNOWN, not foreclosed**,
and the struck Achmea-foreclosure ground may not be restated); the Grosse Ruse-Khan ITN slug **as to
the body** — no twelfth row, eleven rows across four sessions and two spellings all refused; the
Ecuador slug, its paywalled-body synthesis quarantined; `dailyjus.com`; `unis.unvienna.org`;
A/81/17; the monitored-author check. **RETIRED:** the Einarsson family. **QUARANTINED:** all three
Spentech synthesis passes. The Vadi citation is never cited without retrieval. No thread is referred
to by array index. The ARB/24/16 docket is off the agenda. The 2026-08-25 asymmetric licence stays
carried unreleased. **A live relay releases none of these; only an operator ruling does.**

The operator's pending-not-granted list was carried too, as a prohibition on building: the
`source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed
swap, the Kluwer source, the category signal, the `build_graph` fix, and any ledger amendment.

### 1.6 Spend checkpoint — placed here, to be answered after the vetting round

Registered before the analyst returned, so that it cannot be written to fit the result: the day's
authorised spend is **7 search calls and one relay batch of at most 10 rows**. The checkpoint
question I will answer in §4.1, after the officer has vetted, is whether that spend bought anything
a later seat can check. I am registering in advance the failure I expect to be most likely — that
Task 1 comes back with a plausible-looking UNCTAD locator that no search result actually named, i.e.
a guessed URL shape dressed as a finding. If that happens the row does not get fired.

### 1.7 Two defects I found in the record before convening anyone

Both are mine to report because both are the chair's housekeeping, and neither was reported by the
seat that caused it.

**(a) THE COUNCIL LOG HAS MISSED TWO CONSECUTIVE SESSIONS.** `analytics/council-log.md` on `main`
ends at `## 2026-09-17`. The dated entries for **2026-09-18 and 2026-09-19 are absent** — screened
by `grep -c "2026-09-18\|2026-09-19" analytics/council-log.md` against `origin/main`, which returns
`0`. Both sessions unquestionably ran: `analytics/daily-research/2026-09-18.md` and `2026-09-19.md`
are both present on `main` (`git cat-file -e` succeeds on each), and `state/research_log.json`
carries `seq` 85 and 86 for exactly those two dates. **This is the precise failure mode the operator
named when he rebuilt this routine** — a session's output landing where the next session's agenda
does not read it. The daily record survived; the index of it did not. I repair all three entries in
§6, and the repair is stated as a repair rather than written in as though it had always been there.

**(b) `main`'s TEST SUITE IS STILL RED, AND I RE-MEASURED IT RATHER THAN INHERITING THE DIAGNOSIS.**
`tests/test_site_claims.py` pins five constants against the committed archive. Measured from the
repository this session:

| Constant | Test expects | Archive holds | Δ |
|---|---|---|---|
| `meta.json` files (`runs`) | 16 | **17** | +1 |
| `sum(screened)` | 492 | **499** | +7 |
| digests with `per_source` | 11 | **12** | +1 |
| `sum(per_source values)` | 191 | **198** | +7 |
| `sum(screened)` of those | 230 | **237** | +7 |

**Every one of the five deltas is exactly the 2026-09-14 digest** — one run, 7 screened, 7 across
`per_source`. That is a sharper result than yesterday's, which measured three of the five: the
failure has a single cause and is not drift. The fix is in `tests/`, outside this session's
authorised merge scope, so it stays an escalation and I do not touch it.

---
