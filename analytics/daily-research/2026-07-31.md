# Daily Council Meeting — 2026-07-31

**Research question:** When and how is intellectual property — especially trade secrets and pharmaceutical clinical trial data — asserted as a covered investment under an investment treaty, challenged through a governmental regulatory or judicial measure, and disposed of at the jurisdictional or admissibility stage?

**Procedural caveat (chairman).** The scheduled daily routine did not run this morning; on the operator's standing order the session was convened manually and run delegated. Each member section below is what that member's subagent actually returned (research analyst on Claude Fable 5; integrity officer, analytics officer, and systems researcher on Claude Opus 4.8); the chairman (Claude Fable 5) set the agenda, directed the delegation, and wrote this record incrementally as parts completed — the 2026-07-30 lesson applied. No member's voice is performed by the chairman.

---

## Part 1 — Chairman's Agenda

**Chairman presiding.** Day 21 post-close of the UNCITRAL 59th session; the A/81/17 window opens tomorrow. Living memory present and canonical (STATE_OF_THE_ANSWER.md 2026-07-30; research_log.json seq 35; the 2026-07-30 record complete through Part 6).

**Session protocol (applied from the 2026-07-30 self-training note):** members reply via SendMessage to their launcher; if a send bounces, route via "main" naming the intended recipient. A SPEND CHECKPOINT sits immediately after the vetting round — the record through Part 3 is committed before further work, so a mid-session termination never again loses the close-out.

**Priority focus:**
1. **China–France BIT (2007) ledger reconciliation — no search.** Ledger claim `da33a30be92ab234` is operator_verified (2026-07-27): the UNCTAD IIA Mapping records ISDS forum options of domestic courts / ICSID / UNCITRAL, a fork-in-the-road relationship, and NO mapped administrative-review or domestic-litigation prerequisite. The standing operator escalation closes today. Analyst folds this into the memo and the cross-BIT typology; the residual — the French Protocol full text, unread — becomes an ordinary open item.
2. **Analyst bounded actions:** Huawei ARB/22/2 — one direct-fetch attempt for any PO 9+ on the icsidfiles server (fetch-first; ≤4 queries/fetches), taking the applicable-rules question if cheap; one single re-attempt of `media-documents/uncitral/en/sps.pdf` (two-paths question; no re-polling); A/81/17 one query — the last pre-window check.
3. **Systems researcher seated** (2026-07-30 next step 4). Queue: (a) the `source_analytics.py` same-window fix — diagnose and propose; **edits to scripts/ require operator sign-off**; (b) re-sequence the 2026-07-01 quoted-phrase technique to fallback-after-attempted-fetch; (c) the correction line on the 2026-06-30 ODS-route justification.
4. First-application tracker: **adopted** — recorded in the optimization log this morning (chairman); monthly cadence, analyst executes, next cycle late August. No tracker search today.

**Verify / be skeptical:** any PO 9+ fact must come from a fetched primary PDF — search-engine synthesis is barred (yesterday's B4). China–France statements must not exceed the ledger text: the mapping is provision-level, mapped 10-2016, Protocol text unread. Roberts/EJIL:Talk is subsumed — zero budget. Escalated gaps: zero budget.

**Live threads:** (1) A/81/17 (window opens Aug 1 — on publication it is the immediate lead: adopted SP numbering; DP 19 ex officio survival). (2) Huawei ARB/22/2 PO 9+ / applicable-rules question. (3) sps.pdf two-paths question. Zero budget: Wingtech (monthly; late August), IISD ITN / EJIL:Talk polling.

---

## Part 2 — Research Analyst's Findings (research-analyst subagent, Claude Fable 5)

*The analyst's return, relayed via the coordinating session per the return-path protocol
(no bounce this session), is reproduced verbatim below — headings demoted one level for
record nesting, text otherwise unedited. Vetting disposition in Part 3; any amendments are
recorded there and in the close-out, never silently applied here.*

**Budget spent: 3 fetches + 1 search query = 4 total actions.** (Task 1: 2 of 4 allowed; Task 2: 1 of 1; Task 3: 1 of 1; Task 4: zero, ledger only.) Zero-budget items honored: Wingtech/Nexperia, Roberts/EJIL:Talk (subsumed per chairman's July 30 Part 6 ruling), IISD ITN/EJIL:Talk polling, all escalated gaps, first-application tracker (monthly cadence). No repository files edited.

### Task 1 — Huawei v. Sweden (ICSID ARB/22/2): both questions settled in two fetches; no PO 9 exists on the authoritative docket; the Rule 58 clock does not govern this case

Under the fetch-first rule I went straight to the two authoritative listing surfaces instead of guessing blob URLs: ICSID's own case-detail page and the italaw case page. Both fetched cleanly (the July 30 H4 lesson — ICSID HTML is not blocked — confirmed again).

**(a) No Procedural Order No. 9 or later.** ICSID's case-detail page for ARB/22/2 lists Procedural Orders Nos. 1 through 8 only, ending with PO No. 8 (December 11, 2024 — consistent with the PDF read in full yesterday). The [italaw case page](https://www.italaw.com/cases/9128) independently lists the same eight orders and no document dated after 11 December 2024. Because ICSID's procedural-developments feed records order issuance as an event, the absence from that feed supports (MEDIUM-HIGH inference) that no PO 9 has issued at all, not merely that one is unpublished. Stated per the consistency rule: no PO 9+ is found in accessible sources as of July 31, 2026 — and here the accessible source is the tribunal's own docket listing, which is materially stronger than search absence. No blob-URL guessing was warranted for a document the docket does not list; Task 1 closed at 2 of the 4 allowed attempts.

**(b) New procedural fact — the pendency observation is converted from inference to docket fact.** The ICSID page's latest procedural development reads: *"July 10, 2025 - Each party files a reply submission on costs."* Status: *"Pending."* Two consequences: (1) yesterday's B1-hedged item ("whether the schedule held is unverified") is resolved — the costs phase ran at least one round beyond PO No. 8's last scheduled date (20 June 2025), to reply submissions on July 10, 2025; (2) the award-pendency observation now rests on ICSID's own docket, not on a schedule inference: last recorded submission July 10, 2025, more than twelve months ago; no closure of proceedings and no award recorded. **Confidence: HIGH** for the docket facts (ICSID's own case-detail page, fetched today). **Ring relevance:** inverse-scenario standing docket, Ring 1+2 background (thread #4).

**(c) Applicable-rules question settled (my B1-extension catch yesterday).** The page states: registration *"January 21, 2022"*; applicable rules *"ICSID Convention - Arbitration Rules"* (no vintage stated on the page). italaw dates the Request for Arbitration January 7, 2022. Both dates precede July 1, 2022, the 2022 ICSID Rules' entry into force. Under ICSID Convention Article 44, proceedings are conducted under the Arbitration Rules in effect on the date of consent to arbitration — for a BIT claim, perfected at latest on filing the RFA. **Therefore the 2006 Arbitration Rules govern ARB/22/2 by default, and the 2022 Rules' Rule 58 240-day award clock — the figure in yesterday's sps.pdf deck finding — does not apply to this case of its own force.** [The Article 44 consent-date default and the 2006 Rule 46 award clock (120 days from closure of the proceeding, extendable 60) are doctrinal knowledge, not fetched this session — marked unverified-this-session; and parties can agree to apply the amended rules, which PO No. 1 (unread) could reveal — residual flagged below.] **Confidence: HIGH** for the registration date (primary); **MEDIUM-HIGH** for the rules conclusion.

**Minor discrepancy, flagged not resolved (LOW stakes):** ICSID lists PO No. 1 as July 27, 2022; italaw lists it as 27 June 2022. Both are summarizer renders; neither PDF was read. Carried as a discrepancy.

### Task 2 — sps.pdf two-paths question: the blocked path now returns 404, a state change; question stays open

One fetch of `https://uncitral.un.org/sites/default/files/media-documents/uncitral/en/sps.pdf`, no re-polling. **Outcome: HTTP 404 Not Found** — a change from the recorded 403. The resource is now reported absent at that path as of 2026-07-31. The two-paths question (whether that path ever served the same ICSID deck as the `2026-06/sps.pdf` read on July 30, or the instrument text as the integrity officer's directory-semantics lean suggested) **stays open and may now be unresolvable by fetching this path at all** — the identity of what once sat there cannot be determined from a path that no longer serves a file. The July 27 record entry for this path stands as: hypothesis unresolved, path now 404. Honest null; no inflation. **Confidence: HIGH** for the observed 404 (this session's fetch).

### Task 3 — A/81/17: null, as expected; last pre-window check

One query. A/81/17 is not indexed as of July 31 — results return only [A/80/17 (58th session)](https://uncitral.un.org/sites/uncitral.un.org/files/2511964e_uncitral_report_advance_0.pdf) and general UNCITRAL pages ([events](https://uncitral.un.org/en/gateway/meetings/events), [commission sessions](https://uncitral.un.org/en/commission), [Sixth Committee](https://www.un.org/en/ga/sixth/80/uncitral.shtml)). Per yesterday's B4 discipline, no search-synthesis specifics are carried from the result text. The Aug–Sep window opens tomorrow; on publication it becomes the immediate lead (adopted SP numbering; the TPF SP10/SP11 inconsistency; whether DP 19's ex officio dismissal power survived). Null recorded plainly.

### Task 4 — China–France BIT (2007): ledger reconciliation (no search)

The standing operator escalation is **resolved**. Ledger claim `da33a30be92ab234` (both events read in full: claim_created and verification_changed, 2026-07-27, marked by the operator, quote_ok): the UNCTAD IIA Mapping for the China–France BIT (2007) (signed 26 November 2007, in force 20 August 2010, replacing the 1984 BIT) records ISDS included with forum options host-State domestic courts / ICSID / UNCITRAL; a **"fork in the road"** relationship between forums; no alternatives to arbitration; **no mapped mandatory administrative-review or domestic-litigation prerequisite** to arbitration; FET qualified by reference to international law; umbrella clause mapped "Inconclusive". Every statement of this finding carries its caveat: **provision-level characterization, mapped 10-2016 by Laval University (Canada) / Federal University of Paraiba (Brazil); the French Protocol full text remains unread** — that residual is an ordinary open item, not an escalation, per the chairman's directive.

**(a) What this changes for the cross-BIT pre-ICSID gate typology (STATE table, China–France row, unresolved 23+ sessions).** The row moves from "UNCONFIRMED (LOW-MEDIUM permissive signal)" to an operator-verified provision-level answer: **no mapped prerequisite** (IIA Mapping, provision-level, 10-2016). One calibration point the record should preserve: this is **not** the same as confirming the "permissive clause" the July 20–28 signals hypothesized. "No mapped prerequisite" is consistent with three underlying realities: (i) the Protocol contains no exhaustion provision at all; (ii) it contains a discretionary formulation the mapping methodology does not code as a prerequisite; or (iii) a mapping omission (the same student-mapped, non-authoritative caveat class the record already applies to the China–Germany mapping). Distinguishing them requires the Protocol text — the ordinary residual. The prior AI-synthesis "permissive signals" (all rejected by the security officer) should not be retroactively credited as corroborated; the verified finding supersedes rather than confirms them.

**(b) Treaty-selection consequences — every step here is inference on top of the ledger text, and marked as such.**

- *Exhaustion axis (inference):* At provision level, China–France is now the **least-gated** of the three European China BITs in scope: no mapped prerequisite, versus China–Switzerland's permissive "may require" (a discretionary right China could invoke) and China–Germany's mandatory Protocol Art. 9(a) referral. On this axis alone, a French pharmaceutical investor (Sanofi) faces the cleanest mapped path to arbitration.
- *Forum-election axis — the element that cuts the other way (inference):* The mapped **fork-in-the-road** relationship makes the forum choice (host-State courts / ICSID / UNCITRAL) mutually exclusive and final. Contrast China–Germany's mapped "no U-turn" architecture, under which prior domestic recourse is curable — the *Hela Schwarz* mechanism: withdraw all Chinese domestic proceedings before ICSID filing. A pharmaceutical investor's realistic dispute path runs through Chinese administrative and judicial channels first (an NMPA/CDE data-protection denial invites domestic challenge; under China–Germany that first step is mandatory). Under China–France, resort to Chinese **courts** risks being an irrevocable forum election extinguishing ICSID/UNCITRAL access — a Ring 3 hazard China–Germany's cure-permitting clause does not present. So the verified finding removes a gate but surfaces a trap: the two treaties fail an investor in opposite ways. *Sub-inference, unverified:* whether Chinese administrative review (as distinct from administrative litigation in court) would even trigger a fork clause is a classic interpretive question the mapping cannot answer, and fork-in-the-road jurisprudence applying identity tests narrowly is background knowledge not sourced this session — flagged, not relied on.
- *Net picture (inference):* The July 19 conclusion — China–Switzerland (2009) offers the best combined Ring 1 + Ring 3 profile — is **not displaced**. Today's finding narrows China–France's disadvantage on the exhaustion axis while adding a distinct forum-election hazard. The comparison cannot be completed because the record carries no verified forum-relationship mapping for China–Switzerland (fork vs. no-U-turn) — proposed as a new ordinary open item, not asserted either way.
- *Two one-line merits-side notes (provision-level, 10-2016, same ledger entry):* FET qualified by reference to international law is a narrower FET baseline than an autonomous clause — relevant background for any future merits framing, beyond the jurisdictional theme. Umbrella clause "Inconclusive" leaves unclear at mapping level whether regulatory data-protection commitments could be elevated by an umbrella route under this BIT.

### Candidate claims proposed (operator's ledger decides)

```json
[
  {
    "claim_text": "The ICSID case-detail page for Huawei Technologies Co., Ltd. v. Kingdom of Sweden (ICSID Case No. ARB/22/2) records that the Secretary-General registered the request for arbitration on January 21, 2022, states the applicable rules as 'ICSID Convention - Arbitration Rules', and lists the case status as Pending.",
    "source_locator": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/22/2",
    "source_locator_type": "url",
    "source_authority": "primary",
    "access_status": "full",
    "note": "Fetched 2026-07-31 via summarizer render; registration date settles that consent and registration both predate the 2022 ICSID Rules' July 1, 2022 entry into force."
  },
  {
    "claim_text": "As of July 31, 2026, ICSID's case-detail page for ARB/22/2 lists Procedural Orders Nos. 1-8 only (the last being PO No. 8, December 11, 2024), records the latest procedural development as 'July 10, 2025 - Each party files a reply submission on costs', and lists no Procedural Order No. 9 or later and no award.",
    "source_locator": "https://icsid.worldbank.org/cases/case-database/case-detail?CaseNo=ARB/22/2",
    "source_locator_type": "url",
    "source_authority": "primary",
    "access_status": "full",
    "note": "Independently consistent with the italaw case page (https://www.italaw.com/cases/9128), which lists the same eight orders and nothing after 11 Dec 2024."
  }
]
```

The applicable-rules conclusion (2006 Rules govern; Rule 58 inapplicable of its own force) is proposed as an **analyst inference resting on the registration-date claim plus the Article 44 default**, not as a standalone factual claim — the Article 44 layer was not fetched this session.

### Gap-marker proposals (chairman writes the record)

- Retire the escalated marker `china-france-bit-2007-protocol-exhaustion` as **resolved at provision level** by operator action (ledger `da33a30be92ab234`, operator_verified 2026-07-27).
- `GAP-UNRESOLVED: china-france-bit-2007-protocol-text` — French Protocol full text unread; the operator-verified mapping-level answer (no mapped mandatory prerequisite; fork-in-the-road; provision-level, mapped 10-2016) awaits confirmation against the Protocol text. Ordinary open item; zero escalation.
- `GAP-UNRESOLVED: huawei-arb-22-2-rules-vintage` — whether the ARB/22/2 parties agreed to apply the 2022 Rules notwithstanding pre-July-2022 consent (PO No. 1 unread). Ordinary; one bounded PO 1 read would settle it.
- `GAP-UNRESOLVED: china-switzerland-forum-relationship` — the China–Switzerland BIT (2009) forum-relationship mapping (fork-in-the-road vs. no-U-turn) is not in the record; needed to complete the three-way treaty-selection comparison Task 4(b) opened. Ordinary.

### Standing docket status

- **Huawei v. Sweden (ARB/22/2):** No award; status Pending; last docket activity July 10, 2025 (costs replies); no PO 9 listed; 2006 Rules govern by default (registration Jan 21, 2022).
- **Einarsson v. Canada (UNCT/20/6):** Not searched this session (not tasked).
- **A/81/17:** Not indexed; window opens tomorrow.
- **sps.pdf (media-documents path):** HTTP 404 as of today; two-paths question open, possibly permanently.
- **China–France BIT (2007):** Escalation resolved (ledger `da33a30be92ab234`); residual Protocol-text item ordinary.
- **Wingtech/Nexperia; IISD ITN/EJIL:Talk; Roberts check; first-application tracker:** zero budget honored, not searched.

### Session method note (self-training mandate)

The authoritative docket listing beats document hunting: one fetch of ICSID's case-detail page answered the PO 9 question, the registration-date question, and delivered an unrequested procedural fact (the July 10, 2025 costs replies) — collapsing a planned four-attempt blob hunt into two fetches with budget to spare. Rule going forward, extending the July 30 fetch-first rule: for any ICSID case question, fetch the case-detail page before searching for or guessing document URLs; hunt blob URLs only for documents the docket actually lists.

---

## Part 3 — Security / Integrity Officer's Vetting Note (integrity-officer subagent, Claude Opus 4.8)

*Vetting in progress at time of writing — the officer was spawned on the analyst's verbatim
memo immediately upon its return. This placeholder is replaced by the officer's note when it
lands; if this paragraph survives in a final record, the session was terminated mid-vetting
and the record is incomplete at Part 3 — a fail-loud marker, not a silent stub.*

---

## Part 4 — Analytics Officer: Source Yield and Digest Relation (analytics-officer subagent, Claude Opus 4.8)

**Digest 2026-07-27: 10 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).**

Copied this session from `digests/2026-07-27_ISDS-Thematic-Watch/meta.json` (`screened` 10, `matches` 0, `watch_list_leads` 0). I listed `digests/` and confirmed `2026-07-27_ISDS-Thematic-Watch` is still the newest archived run; the next weekly run is expected **2026-08-03**, so these are the numbers of record for three more days.

### 1. Numbers of record: unchanged — no re-derivation today

The digest of record has not changed since yesterday, and I re-read its `meta.json` to confirm the stored values are byte-identical to the ones yesterday's Part 4 quoted (`per_source`: `iareporter_headlines` 10, all eight others 0; `accepted_by_source` empty; `source_health` with `unctad_isds` RETURNED 25 against 0 fresh, `google_news_rss` DISABLED, `iareporter_headlines` HEADLINE-ONLY). **The full longitudinal analysis stands as written in `analytics/daily-research/2026-07-30.md`, Part 4, sections 1–4 — 13 items ever surfaced across 10 archived runs from exactly two sources (`iareporter_headlines` 9, `italaw` 4), 0 matches in 10 of 10, seven sources never having surfaced anything, and the zero-streak list.** I did not re-derive those figures today and am not restating them as fresh work. IAReporter's headline-only ceiling remains the standing caveat.

The only file I re-read beyond the digest is `analytics/source-receptivity.md` (10 archived runs; per-source counts for 5 of them), which still carries the mixed-window artifacts noted in defect 1 below.

### 2. Today's four items against the collection surface — the disjunction widens

None of today's four items could have been surfaced by any of the nine digest sources, and today does not merely repeat yesterday's pattern — it adds a channel.

- **(a) Huawei ARB/22/2 PO 9+ on `icsidfiles.worldbank.org`** — a case-document file server. The `icsid` source polls the news/events page only; `italaw`, the natural surfacing route, returned 0 fresh on 07-27 and has for four consecutive runs.
- **(b) `uncitral.un.org/media-documents/uncitral/en/sps.pdf`** — no digest source polls `uncitral.un.org` in any form.
- **(c) A/81/17 one-query check** — search track, no digest surface by design.
- **(d) China–France BIT (2007) ledger reconciliation (`da33a30be92ab234`)** — no web source at all. It came from the operator's own UNCTAD IIA Mapping Navigator query, i.e. **a third channel outside both the digest and the search track**. Yesterday's disjunction was between the digest and the analyst's direct fetches; today the day's one settled fact of record entered through neither.

So: two file-server reads outside the collection surface (a, b), one search-track check (c), and one operator-channel item (d). Four for four, none reachable by the instrument as configured.

### 3. New observation this session — the near-miss host

This is the one genuinely new analytics point, and it is narrow. Item (d) came from `investmentpolicy.unctad.org` — **the same host the `unctad_isds` source already polls**, but a different database on it: the source's `BASE_URL` is `https://investmentpolicy.unctad.org/investment-dispute-settlement` (`src/sources/unctad_isds.py:29`), the ISDS Navigator case list, while the IIA Mapping Navigator is not polled by anything. The codebase already encodes the mapping as an operator action item rather than a collection target (`src/research_state.py:69`, `src/research_brief.py:254`), so this is a design choice, not an oversight.

The reason it is worth a line: `unctad_isds` is the source that fetches 25 items and dedupes to 0 every recorded run — a fetch spent per run on a static page, 0 surfaced ever. The instrument therefore reaches that host weekly and gets nothing, while the host's sibling database produced the day's only settled fact by hand. That is a fact about where the yield is on a host we already touch. I am **not** proposing the mapping as a source — treaty-mapping records are reference data, not events, and a change-detector on them is a different instrument than a news poller. I am flagging it for Emory as a datum in the `unctad_isds` keep-or-retire decision that yesterday's zero-streak item 3 already put in front of him.

### 4. Optimization-proposal status pass

- **2026-07-29 first-application tracker — ADOPTED.** Recorded in `analytics/optimization-log.md` this morning by the chairman: adopted 2026-07-31, monthly cadence, analyst executes, next cycle late August 2026, baseline the officer-verified null against ICSID's MLLM decisions table, with the withdrawn FY2025 "3 decisions within 60 days" statistic barred from the baseline. My CORROBORATED finding is now spent; no tracker work today. Not re-argued.
- **2026-07-01 quoted-phrase re-sequencing — ASSIGNED** to the systems researcher this session (chairman's Part 1, queue item b). Log entry at line 31 still reads `_Status: proposed_`; it will need the re-sequencing language when the systems seat reports.
- **2026-06-30 ODS-route correction — ASSIGNED** to the systems researcher this session (queue item c). Log entry at line 32 still reads `_Status: proposed_` and still carries the falsified justification line about consistent 403s. Not re-argued.

### 5. My own instruments — status only

- **`scripts/source_analytics.py` mixed windows** (fetched from 5 per-source runs, surfaced from all 10 — the source of `italaw` 200% and `iareporter_headlines` 17% in `analytics/source-receptivity.md`): now on the systems researcher's queue as item (a), diagnose-and-propose, with edits to `scripts/` gated on operator sign-off. Unchanged and uncorrected as of this session; nothing edited by me.
- **Rule 1 compliance:** 2026-07-30 was the first record to open with the protocol sentence; today's is the second consecutive. Holding. The open question from yesterday — whether the operator has been seeing the emailer's CONSISTENCY WARNING on the 07-21 through 07-29 sends — is still unanswered and is a question only Emory can close.

No other new analytics finding today. The instrument produced no new data since the 07-27 run, and I will not manufacture a trend from three days of no collection.

*(Chairman's note on Part 4 sections 4 bullets 2–3: written before the systems researcher's return landed; the two log entries have since received their correction lines — see Part 5 §3. The officer's status lines were accurate at time of writing.)*

---

## Part 5 — Systems Researcher's Report (systems-researcher subagent, Claude Opus 4.8; first seated session)

*Reproduced verbatim from the member's final return — headings demoted one level for record
nesting, relay transport-escapes normalized, text otherwise unedited. The member's summary
line on return: both edits in, `scripts/` untouched, tests green (the 4 one-pager failures
pre-exist and are caused by the gitignored `seeds/` dir being absent from this worktree —
confirmed by stashing). The chairman verified independently before this entry: git status
shows only `analytics/optimization-log.md` modified beyond the record file itself; the two
appended correction lines match the report verbatim; `scripts/`, `src/`, and `tests/` are
untouched.*

**Status.** First seated session; carried queue worked in full. The `source_analytics.py` window-mixing defect is confirmed at line level, and a patch is proposed, executed against the real `digests/` data, and covered by four tests that pass on the patched copy and fail on the current one. Both optimization-log corrections are applied surgically. One new proposal, deduped against the full log.

### 1. `scripts/source_analytics.py` — line-level diagnosis (confirmed)

The analytics officer's disclosure is accurate. The defect is a single missing gate, and it is in the **numerator**:

- **Line 84** — `surfaced = defaultdict(int)` is one accumulator serving two incompatible run sets.
- **Lines 97–99** — `fetched` is populated only inside `for src, n in ps.items()`, so it accrues **only from runs carrying `per_source`** (5 of 10: 06-29, 07-06, 07-13, 07-20, 07-27).
- **Lines 105–106** — `for src, n in sbs.items(): surfaced[src] += ...` runs **unconditionally, for every archived run**, including the five that predate per-source recording. There is no `if ps:` guard here, and that asymmetry with lines 97–99 is the entire bug.
- **Line 124** — `ratio = f"{(s / f * 100):.0f}%" if f else "n/a"` therefore divides an all-runs numerator by a five-run denominator.
- **Line 116** — the report then asserts `_Receptivity = surfaced ÷ fresh candidates, where per-source fresh counts exist._`, which is true of the denominator and false of the numerator.

Worth naming precisely: the docstring at lines 12–15 is titled "Honest about its denominator" — and the denominator is the half that was already correct. The script's own integrity note is about the wrong half of the fraction, which is likely why the defect survived review.

Two facts I verified that bear on applying the fix:

- **No downstream consumer parses the table.** Grepping `scripts/`, `src/`, `tests/`, `site_templates/` for `source-receptivity` returns only the generator itself and a comment at `src/render.py:184`. Adding columns is safe.
- **The committed report is two days stale relative to its generator.** Regenerating with the *current* unpatched script differs from `analytics/source-receptivity.md` in two lines (a `google_news_rss` note and one "Reading this" bullet). Cause is benign: the script changed 2026-07-29 in `b7d0925` (Bing News in, Google News out); the report was last regenerated by the weekly run at 2026-07-27 15:45 UTC. Consequence for the operator: **applying the patch changes nothing published until the next weekly run (Mon 2026-08-03) unless `python scripts/source_analytics.py` is run manually in the same commit.**

### 2. Proposed patch — verbatim

Patched candidate ready at `/private/tmp/claude-501/-Users-emorywilliams-isds-watcher/eebda73d-3b6a-41d2-8205-616eca97a4e8/scratchpad/source_analytics_patched.py`. **Not applied — `scripts/` requires operator sign-off.**

Design note: the fix is *not* simply "restrict surfaced to per_source runs." That would silently destroy the lifetime figure the council actually uses — the analytics officer's "13 items have ever reached a digest, from exactly two sources" (Part 4 §3) is the all-runs count. The patch splits the two windows into separate columns and keeps both, so the ratio becomes honest without losing the longitudinal record.

```diff
--- a/scripts/source_analytics.py
+++ b/scripts/source_analytics.py
@@ -10,9 +10,11 @@
 (`accepted_by_source`), plus the surfaced articles themselves. It writes a single
 report to analytics/source-receptivity.md.
 
-Honest about its denominator: older runs predate per-source counting, so receptivity
-ratios are computed only where the data exists; everywhere else the report falls back
-to surfaced-contribution counts and says so.
+Honest about its denominator: older runs predate per-source counting, so the
+receptivity ratio is computed strictly inside the run set that recorded BOTH a
+per-source fresh count and its surfaced items — like divided by like. Lifetime
+surfaced counts (every archived run, including the pre-per_source ones) are reported
+in their own column and never enter the ratio.
 
 Run:  python scripts/source_analytics.py
 """
@@ -80,9 +82,10 @@
 
 
 def main() -> None:
-    fetched = defaultdict(int)        # per-source fresh candidates (where recorded)
-    surfaced = defaultdict(int)       # per-source surfaced items (where recorded)
-    have_fetched = defaultdict(int)   # runs with per_source data, per source
+    fetched = defaultdict(int)          # per-source fresh candidates (per_source runs only)
+    surfaced_window = defaultdict(int)  # surfaced items, restricted to those same runs
+    surfaced_all = defaultdict(int)     # surfaced items across every archived run
+    have_fetched = defaultdict(int)     # runs with per_source data, per source
     runs = 0
     runs_with_persource = 0
 
@@ -103,9 +106,13 @@
         if not sbs:
             sbs = {s: int(n or 0) for s, n in (meta.get("accepted_by_source") or {}).items()}
         for src, n in sbs.items():
-            surfaced[src] += int(n or 0)
+            surfaced_all[src] += int(n or 0)
+            # Receptivity must divide like by like: only a run that also recorded
+            # per-source fresh counts may contribute to the ratio's numerator.
+            if ps:
+                surfaced_window[src] += int(n or 0)
 
-    all_sources = sorted(set(fetched) | set(surfaced))
+    all_sources = sorted(set(fetched) | set(surfaced_all))
 
     lines = [
         "# Source receptivity — analytics",
@@ -113,27 +120,38 @@
         f"Across **{runs}** archived run(s); per-source candidate counts available for "
         f"**{runs_with_persource}** of them (older runs predate per-source recording).",
         "",
-        "_Receptivity = surfaced ÷ fresh candidates, where per-source fresh counts exist._",
+        f"_Receptivity = surfaced ÷ fresh candidates **within the {runs_with_persource} run(s) "
+        f"that recorded both** — like divided by like. 'Surfaced (all runs)' is the lifetime "
+        f"yield across all {runs} archived run(s) and is deliberately not the ratio's numerator._",
         "",
-        "| Source | Fresh candidates | Surfaced | Receptivity | Note |",
-        "|--------|------------------|----------|-------------|------|",
+        "| Source | Fresh candidates | Surfaced (same window) | Receptivity | Surfaced (all runs) | Note |",
+        "|--------|------------------|------------------------|-------------|---------------------|------|",
     ]
     for src in all_sources:
         f = fetched.get(src, 0)
-        s = surfaced.get(src, 0)
-        ratio = f"{(s / f * 100):.0f}%" if f else "n/a"
+        sw = surfaced_window.get(src, 0)
+        sa = surfaced_all.get(src, 0)
+        ratio = f"{(sw / f * 100):.0f}%" if f else "n/a"
         note = _NOTES.get(src, "")
-        lines.append(f"| {src} | {f if have_fetched.get(src) else '—'} | {s} | {ratio} | {note} |")
+        if f and sw > f:
+            # Fail loud: inside one window this is a data inconsistency between
+            # meta.json and the article files, never a receptivity above 100%.
+            note = ("**DATA INCONSISTENCY** — same-window surfaced exceeds fresh "
+                    "candidates; ratio not meaningful. " + note).strip()
+        col_f = f if have_fetched.get(src) else "—"
+        col_sw = sw if have_fetched.get(src) else "—"
+        lines.append(f"| {src} | {col_f} | {col_sw} | {ratio} | {sa} | {note} |")
     if not all_sources:
-        lines.append("| — | — | — | — | _No per-source data yet._ |")
+        lines.append("| — | — | — | — | — | _No per-source data yet._ |")
 
     lines += [
         "",
         "## Reading this",
-        "- **Surfaced** counts items that reached the digest — the clearest signal of a "
-        "source's on-theme yield to date.",
-        "- **Receptivity** needs per-source fresh counts; it populates as runs accrue "
-        "under the per-source-recording build.",
+        "- **Surfaced (all runs)** counts every item that has ever reached a digest — the "
+        "lifetime on-theme yield, and the clearest signal of a source's contribution to date.",
+        "- **Receptivity** divides same-window surfaced by same-window fresh candidates. A run "
+        "that predates per-source recording contributes to neither, so the ratio can no longer "
+        "exceed 100%; where it would, the row is flagged as a data inconsistency instead.",
         "- Sources flagged capped (`iareporter_headlines`) "
         "are structural limits, not tuning targets — prioritise full-text feeds that "
         "reach the IP-as-investment / judicial-measure / jurisdictional intersection.",
```

The `sw > f` branch is the integrity guardrail: it converts the failure mode from *silently publishing 200%* into *printing a visible inconsistency flag*, so a future meta.json/article-file divergence fails loud rather than masquerading as receptivity.

**Corrected figures, produced by running the patched script against the real `digests/` tree** (read-only; output written to scratchpad, `analytics/source-receptivity.md` untouched):

| Source | Fresh | Surfaced (same window) | Receptivity — was → now | Surfaced (all runs) |
|---|---|---|---|---|
| italaw | 2 | 1 | **200% → 50%** | 4 |
| iareporter_headlines | 52 | 5 | **17% → 10%** | 9 |
| google_alerts | 12 | 0 | 0% (unchanged) | 0 |
| icsid | 3 | 0 | 0% (unchanged) | 0 |
| pca_press | 3 | 0 | 0% (unchanged) | 0 |
| gmail_scholar, google_news_rss, iisd_itn, unctad_isds | 0 | 0 | n/a (unchanged) | 0 |

Both corrected figures match the analytics officer's independent prediction exactly (italaw 1/2; iareporter ≈10%). Lifetime total is preserved at 13 (9 + 4), so Part 4 §3 remains citable against the patched report.

**Tests.** No test currently covers this script. I wrote four and verified them: all four **pass** against the patched copy and all four **fail** against the current one. Proposed as `tests/test_source_analytics.py`, verified variant at `/private/tmp/claude-501/-Users-emorywilliams-isds-watcher/eebda73d-3b6a-41d2-8205-616eca97a4e8/scratchpad/test_source_analytics_verify.py` (the repo version differs only in resolving `SCRIPT` to `scripts/source_analytics.py`):

1. `test_pre_per_source_runs_do_not_inflate_receptivity` — the named regression: a pre-`per_source` run contributes to lifetime yield only.
2. `test_receptivity_never_exceeds_100_percent` — no unflagged row may print a ratio above 100%.
3. `test_same_window_inconsistency_is_flagged_not_silently_divided` — the guard fires.
4. `test_lifetime_yield_is_preserved` — the all-runs column keeps counting pre-`per_source` items.

Effort: low (about 30 lines changed, plus the test file). Risk: the table gains a column, so any hand-written prose quoting "the Surfaced column" needs re-reading — `COUNCIL.md:25` describes receptivity in prose and should have "surfaced ÷ fresh candidates" qualified as same-window when the patch lands.

### 3. Optimization-log edits — applied

`analytics/optimization-log.md`, two lines changed, both entries otherwise untouched. The 2026-07-29 tracker entry was not touched. `tests/test_council_optimizations.py` passes (5/5).

**2026-07-01 entry** — appended verbatim:

> **Correction 2026-07-31 (systems researcher, on the 2026-07-30 record): re-sequenced from substitute to fallback.** That session weakened this entry — two direct fetches of hosts the record treated as blocked (icsidfiles.worldbank.org, Procedural Order No. 8 in Huawei v. Sweden; uncitral.un.org/sites/default/files/2026-06/sps.pdf) each returned a full read, where the quoted-phrase technique had yielded only a medium-confidence characterisation of comparable material — so the correct sequence is to attempt the direct fetch first and fall back to quoted-phrase reconstruction only once a fetch has actually failed; the title's "substitute" framing is superseded, and a search snippet must never be cited in place of a document that an unattempted fetch would have produced (the 2026-07-30 B4 objection is what that failure looks like in practice). _Status: proposed; re-sequenced 2026-07-31 to fallback-after-attempted-fetch (was: substitute for a blocked PDF)._

**2026-06-30 entry** — appended verbatim:

> **Correction 2026-07-31 (systems researcher, on the 2026-07-30 record): the access premise stated above is partly falsified.** The opening justification — that UNCITRAL PDF links at uncitral.un.org/sites/default/files/ are "consistently 403-blocked across sessions" — does not hold for that directory as a whole: https://uncitral.un.org/sites/default/files/2026-06/sps.pdf fetched cleanly on 2026-07-30 and was read in full, so the 403 pattern is path-specific rather than directory-wide or site-wide, and no path under that directory may be recorded as blocked without an attempted fetch. The recommendation itself is unaffected, because it rests on this entry's independent second ground — that A/CN.9 documents are submitted to the General Assembly and archived in ODS by symbol — which is a document-identity argument, not an access-failure one; ODS remains the right canonical route for A/CN.9 symbols. _Status: proposed; justification corrected 2026-07-31 (access premise partly falsified; recommendation stands on the symbol-archiving ground)._

### 4. One proposal

**Component:** `scripts/source_analytics.py` (report generator) reading `state/source_health.json` + `meta.json["source_health"]`, which `src/source_health.py` already computes.

**Change:** add a `Health` column to `analytics/source-receptivity.md` carrying each source's latest status and consecutive-zero streak, labelled "as of <latest run date>".

**Why this is not covered elsewhere.** The instrument already computes exactly the distinction the analytics officer said it could not make. `src/source_health.py` persists per-source zero streaks, escalates to `DEGRADED (n zero runs)` at three, and raises a `COLLECTION ANOMALY` warning — all tested in `tests/test_source_health.py`. But the receptivity ledger, which is the artifact the council reads to decide which sources earn their place, never sees any of it. So the officer hand-reconstructed it: Part 4 §1 and the whole of §4 ("longitudinal zero-streaks for the chairman's and the operator's attention") are a manual transcription of data the pipeline already generates, repeated every session, against a seat rule that exists *because* hand-copying is error-prone. Its §4 item 2 — "`gmail_scholar` — whether genuinely quiet or silently broken cannot be distinguished from the metadata" — is answerable today; the answer is just in a file the report does not open.

**Source:** dbt's source-freshness model — [Add sources to your DAG, docs.getdbt.com](https://docs.getdbt.com/docs/build/sources) (verified by fetch this session): a source declares `freshness: {warn_after, error_after}` against a `loaded_at_field`, and `dbt source freshness` emits a per-source pass/warning/error status into a machine-readable `sources.json` artifact. The transferable point is the separation the project currently lacks: source health is a first-class, generated, per-source status published *alongside* the source's output, not something a human re-derives from raw run logs.

**Effect:** the analytics seat stops re-deriving source health by hand each session; a source's zero row becomes self-explaining ("0 surfaced, RETURNED, 4-run zero streak" versus "0 surfaced, DEGRADED"); and the officer's §4 list becomes a generated column rather than a manual finding.

**Effort:** low. **Risk/tradeoff — and this is the guardrail, not a footnote:** the report is longitudinal (all runs) and health is point-in-time (latest run). Publishing them in one table without an explicit "as of <date>" label would reintroduce precisely the window-mixing failure this session just fixed, one column over. The label is a condition of adoption, not a nicety.

**Sequencing:** propose landing this *after* the same-window fix, as a separate change. Bundling an enhancement into a defect repair is how a clean fix becomes hard to review or revert.

**Dedup:** checked against all 22 log entries. Every existing entry targets research method, monitoring cadence, treaty-text retrieval, the fingerprint, or the multi-agent roadmap; none touches the analytics report, source health, or instrument observability. Genuinely new.

### 5. Self-training note

One hypothesis tested and discarded rather than reported: seeing `state/source_health.json` absent from the repo while `iisd_itn` sat at 0 for four consecutive runs with no `DEGRADED` status ever recorded, I formed the view that the silent-decay guard could never fire in CI. Checking commit times refuted it — the guard merged at 2026-07-27 14:21 EDT, two and a half hours *after* the last weekly run committed at 15:45 UTC. Every archived `source_health` block is pre-guard output, the workflow's `git add state/` will persist the health file from the first post-guard run, and the guard's first live execution is Monday 2026-08-03. Nothing is broken. Recording it because a false "your monitoring guard is dead" claim would have been expensive, and the only thing separating it from the record was two `git log` calls against the claim I already believed.

---
