# ISDS Thematic Watcher — daily council meeting, 2026-09-18

**Convened by:** the council chairman, running as the operator's standing scheduled cloud
session. **Seats convened:** research analyst, integrity officer. **Model directive:**
`claude-opus-5` for the chair and the analyst, `opus` (alias) for the officer, per the
committed definitions in `.claude/agents/`.

---

## 1. Agenda, channel state, and why no batch was fired at session open

### 1.1 Member return-path protocol

Every convened seat returns its contribution as text. **Member text enters this record
verbatim, under a heading naming the seat, and I do not edit it.** Where a member's
sentence is later superseded by a ruling, the sentence stays on the page and does not
travel; the substitute form is filed separately. My own superseded sentences are annotated
**in place**, per the `0917-B8` ruling, because the sentence a later seat will cite must
carry its own correction. If a spawn fails, the seat is recorded as PROCEDURAL FAILURE and
**I do not perform it** — that is the operator's cardinal rule.

### 1.2 Channel state — probed at session open, on neutral controls, nothing inherited

Two probes, both on neutral hosts, both fired before anything else in this session:

1. **`curl https://example.com/`** → `curl: (56) CONNECT tunnel failed, response 403`.
   The agent proxy's own status endpoint logs it under `recentRelayFailures` as
   `connect_rejected`, *"gateway answered 403 to CONNECT (policy denial or upstream
   failure)"*, host `example.com:443`, at **2026-09-18T11:12:19.215Z**. A second identical
   entry stands for `icsid.worldbank.org:443` at **2026-09-18T11:12:19.431Z**.
2. **The fetch service** on the same neutral control returned
   `{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`.

**This session has NO fetch capability of its own. That is not a state change** — it is the
nineteenth consecutive session in this posture. **It is a fact about this container and NOT
a fact about any resource on the internet**, and no row in this record may be read the other
way (the standing tool-status-as-source-state rule).

**The relay is alive and was available all session.** `.github/workflows/fetch-relay.yml`
fires on a push touching `analytics/fetch-requests/**` and answers through the project's own
`polite_get`. It answered nine of ten rows yesterday with `control_ok: true`
(`analytics/fetch-results/2026-09-17-daily.json`, runner run 35214606227). **Nothing today
is stranded for want of a channel.**

### 1.3 I FIRED NO SESSION-OPEN BATCH, AND THAT IS THE DAY'S FIRST DECISION

My predecessor's next step 1 sets today's priority: *"THE ENUMERATION GAP MAY BE ILL-POSED
AND THAT IS TOMORROW'S FIRST QUESTION, NOT A FOOTNOTE … Do not fire another enumeration row
until the field's type is addressed. If it is free text, 'enumeration' has no referent."*
The escalation to Emory put the same question as item 2 and said *"the cheapest resolution
is a person reading ICSID's own documentation once."*

I designed a batch to sample `Subject of Dispute` values across five case-detail pages, with
a companion head-excerpt control row per URL per the relay README. **Then I ran the dedup
screen before writing the request, and the screen hit.** The evidence this question needs is
already committed to this repository and has been since **2026-08-24**. Firing those rows
would have re-fetched what `analytics/fetch-results/` already holds — which is precisely the
failure sustained against the analyst yesterday as `0917-B1`, a dedup certification that ran,
hit, and was written up as a miss. **The batch was not fired. The screen is discharged below
by quoting what it returned, never by characterising it** (taxonomy 39, adopted yesterday).

**THE SCREEN, PRINTED RATHER THAN CERTIFIED.** Every record in every file under
`analytics/fetch-results/` whose URL contains `case-detail` and whose 400-character excerpt
carries the literal `Subject of Dispute:`. Extracted mechanically at `5e16471`; the regex
also captures the `Economic Sector:` value standing in the same excerpt window, on the same
byte-stream, from the same fetch.

| case | `Subject of Dispute` | `Economic Sector` | artefact | runner run | sha256[:12] |
|---|---|---|---|---|---|
| ARB/24/26 | Mining concession | Oil, Gas &amp; Mining | `2026-08-24-daily.json` | 32721541037 | `738a0565eaee` |
| ARB/24/46 | Acquisition of shares | Electric Power &amp; Other Energy | `2026-08-24-zz-subject-of-dispute.json` | 32729305805 | `5790ee378e82` |
| ARB/17/19 | Spice products company | Other Industry | `2026-08-24-zz-subject-of-dispute.json` | 32729305805 | `20ee457b2713` |
| ARB/23/3 | Banking and financial services | Finance | `2026-08-24-zz-subject-of-dispute.json` | 32729305805 | `46ccad7f4bca` |
| ARB/22/30 | Banking enterprise | Finance | `2026-08-24-zz-subject-of-dispute.json` | 32729305805 | `56850ae8fccf` |
| ARB/24/18 | Insurance company | Other | `2026-08-24-zz-subject-of-dispute.json` | 32729305805 | `96545ee486fb` |
| ARB/16/34 | Tire and rubber products enterprise | Other Industry | `2026-08-25-daily.json` | 32847701575 | `8fec659a56f3` |
| UNCT/14/2 | Pharmaceutical patents | Other Industry | `2026-08-25-daily.json` | 32847701575 | `3ac1eec2020e` |
| ARB/10/7 | Tobacco industry | Other Industry | `2026-08-25-daily.json` | 32847701575 | `dee9a901fa80` |
| UNCT/20/6 | Seismic data enterprise | Other Industry | `2026-08-26-daily.json` | 32969552704 | `5c4444e2a039` |
| ARB/12/1 | Mineral exploration operations | Oil, Gas &amp; Mining | `2026-08-26-daily.json` | 32969552704 | `bf5a70b29f8d` |
| ARB/22/2 | Telecommunication license agreements | Information &amp; Communication | `2026-09-04-daily.json` | 33867564400 | `946f1c695615` |
| ARB/24/16 | Construction project | Construction | `2026-09-13-daily.json` | 34753798368 | `da8b441f33bd` |

**Thirteen distinct cases. Thirteen distinct `Subject of Dispute` strings. Zero repeats.**
Eighteen further rows in the same files are re-observations of cases already in the table
(ARB/12/1 three times across runs 32969552704 / 33508412615 / 33628716068; UNCT/20/6 five
times; ARB/24/16 six times; ARB/23/3, ARB/22/30, ARB/24/18, ARB/24/26, ARB/24/46, ARB/16/34
and UNCT/14/2 twice each). **Every re-observation returned the identical string.** The field
is stable across runs; it is the *cross-case* distribution that is at issue.

**The same thirteen rows carry the adjacent field, and it behaves differently.** Seven
distinct `Economic Sector` values over the same thirteen cases, **with repeats**:
`Other Industry` on five cases, `Finance` on two, `Oil, Gas &amp; Mining` on two. Two
same-sector pairs carry *different* `Subject of Dispute` strings — ARB/23/3
*"Banking and financial services"* against ARB/22/30 *"Banking enterprise"*, both `Finance`;
and ARB/24/26 *"Mining concession"* against ARB/12/1 *"Mineral exploration operations"*,
both `Oil, Gas &amp; Mining`.

**What I assert and what I do not.** I assert the table: thirteen cases, thirteen strings,
zero repeats, extracted from committed artefacts, each row citing its file, runner run and
content hash. **I assert nothing about what it means.** Whether this licenses a conclusion
about the field's type — and how far — is the analyst's question, put to it below, and it is
the officer's to attack. **This is not an enumeration and `0824-R30`'s bar is untouched.**
No proceeding moves on any value; the 2026-08-25 asymmetric licence is unchanged.

**Caveat carried into the table rather than left out of it.** ARB/24/16 is at zero budget as
a docket (carried constraint 8). Its `Subject of Dispute` value is nonetheless already
committed in six artefacts, so excluding it would be a screen that hid a row it hit. **With
ARB/24/16 the counts are 13 cases / 13 strings; without it, 12 / 12. Zero repeats either
way.** I ask the officer to rule on whether the zero-budget constraint reaches a value
already in the record.

### 1.4 The standing prohibitions, carried unreleased

A live relay releases none of these and only an operator ruling does: the Ecuador slug at
zero budget with its paywalled-body synthesis quarantined; `dailyjus.com` and
`unis.unvienna.org` at zero budget; the Einarsson family retired; A/81/17 at zero budget;
the thread closed 2026-09-08 closed; all three Spentech synthesis passes QUARANTINED; the
monitored-author check at zero search budget; the Vadi citation never cited without
retrieval; no thread referred to by array index; the ARB/24/16 docket off the agenda.

**Nothing at OPERATOR APPROVAL PENDING will be built or applied in this session:** not the
`source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the
IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, or any
ledger amendment.

### 1.5 The digest of record

`Digest 2026-09-14: 7 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list
leads).` Copied from `digests/2026-09-14_ISDS-Thematic-Watch/meta.json`
(`screened: 7`, `matches: 0`, `watch_list_leads: 0`). **Unchanged from yesterday's record** —
the weekly run of 2026-09-14 is still the digest of record and no new digest has been
produced since. `meta.json` also carries a standing health warning: `gdelt` is DEGRADED at
four consecutive zero-item runs, so zero items from that source is **not** evidence of a
quiet week.
