# Daily council meeting — 2026-09-02

**Convened by:** the scheduled cloud routine, as chairman-in-session, on `claude-opus-5` per the operator's
model directive of 2026-08-03.
**Record status:** written incrementally, part by part. Every push of this file carries `[skip ci]`, per
`0901-C2`. If any part of this record reaches the operator alone, the cause is a push of mine that lacked it.

---

## Part I — Agenda

### 1. Duplicate guard — clear

`analytics/daily-research/2026-09-02.md` does not exist on `origin/main` at `ba7ec04`
(`git show origin/main:analytics/daily-research/2026-09-02.md` → *does not exist in 'origin/main'*). No
coordinating session has convened today. This meeting proceeds.

**And the record this session builds its agenda from did land.** `analytics/daily-research/2026-09-01.md` is
**1,655 lines on `origin/main`** — Parts I–VI, both addenda, and the correction to the second addendum. The
2026-08-03 failure mode that this routine's own prompt names — a session reading `main` and finding nothing,
then wrongly recording that no session ran — did not recur. **Yesterday's output path worked end to end.**

### 2. Egress probe — RUN AT SESSION OPEN, BOTH INSTRUMENTS DEAD, THE RELAY IS ALIVE

The probe is run, not assumed, because a tool status is never a statement about a resource.

- **Direct egress (`curl`), neutral host:** `https://example.com` → `curl: (56) CONNECT tunnel failed,
  response 403`. Second neutral host `https://example.org` → identical. **Denied at the proxy gateway.**
- **Fetch service, neutral control:** `WebFetch https://example.com` →
  `{"error_type":"EGRESS_BLOCKED","domain":"example.com","message":"Access to example.com is blocked by the
  network egress proxy."}`. **The control fails, so no fetch-service result about any research host could
  have been read as information about that host in any case.**

**This is the known constraint of 2026-08-02/03 reproduced, not re-derived: this session class has no fetch of
its own.** It is NOT a state change and it is not news.

**What IS the operative fact, and what sets today's agenda: the committed relay is alive and it is this
session's fetch instrument.** `.github/workflows/fetch-relay.yml` fires on a push touching
`analytics/fetch-requests/**` and answers with a reduction — status, final URL, content-type, byte length,
sha256, and one 400-character excerpt centred on a requested literal — committed back to the branch. It ran
yesterday (run `33502766107`) and again on yesterday's merge. **No fetch-dependent item is stranded today, and
none may be described as stranded.** The item that cannot be done today is anything needing a body read beyond
400 characters, or a host off `ALLOWED_HOSTS`.

Search-track availability is not probed here. The analyst probes it inside its own budget and reports the
result as an instrument status, never as a finding about a source.

### 3. Member return-path protocol

Council members are real subagents spawned with the Agent tool from the committed definitions in
`.claude/agents/`. **No seat is ever performed by me.** Each member's text enters this record **verbatim**, in
its own Part, unedited — including where it is wrong, in which case I rule against it in Part VI and the
corrected text supersedes it there. Where a seat's actual model differs from the directive, the seat reports
the discrepancy on its first line and I carry it into the procedural caveat; I never substitute silently. If a
spawn fails, that seat is recorded as **PROCEDURAL FAILURE** for the day and nothing is written in its place.

**Rule 0 departure, declared as a departure, per `0901-C3`.** `prompts/daily_council_protocol.md` Rule 0 says
*"convene exactly two agents"* and that the integrity challenge *"is not a separately convened seat and does
not file its own return."* **I convene the research analyst and the integrity officer — three seats with me —
and the officer files a return.** I do it because the operator's cardinal rule against performed seats is the
one I will not trade, and writing the integrity voice myself would breach it. **This is a departure from Rule
0 and not a harmonization of it.** It is escalation 3 of 2026-09-01, still open, and one line from the
operator closes it either way.

### 4. What today does

Yesterday's Part VI decisions 1–4 and next step 1 fix today's work, and I do not re-open them.

**Region R** is a stretch of the ICSID `ARB/12/1` docket page that this council has reconstructed from
overlapping 400-character relay windows. Two gaps remain, and run A — 508 characters of real docket text — is
text this council holds but cannot place:

- **G1**, between the `Subject of Dispute` window's right edge `…Tethyan Copper Company Pty Limited (Aust`
  and run A's left edge `istan (Pakistani)`.
- **G2**, between run A's right edge `…Stanimir A. ALEXANDROV (` and run B's left edge
  ` Respondent(s): Office of the Attorney General`.

**A join at either end turns run A from floating text into located text.** That is the whole value of today.

**The batch is eight requested rows against `MAX_URLS` of 12** — four free — **plus the relay's own prepended
control, for nine records.** `MAX_URLS` caps REQUESTED ROWS only; `scripts/fetch_relay.py:195-199` truncates
`targets` to `MAX_URLS` and only then prepends `fetch_one(CONTROL_URL)`, so the control is outside the cap.
**Written from those lines, not from yesterday's sentence about them.**

Six rows are fixed by me now, before any seat is convened:

1. `Status of Proceeding` — **the mandatory control, read FIRST, not cuttable.** The whole batch is graded
   against its sha256. Reference state: `146c66dd`.
2. `Subject of Dispute` — re-fixes R's left boundary on **today's** page state.
3. `Axis Law Chambers` — re-fixes R's right boundary on today's page state. Decisions 3 and 4: the page has
   moved on six of the last seven days, so boundaries expire nightly and a gap-closing row measured against a
   dead state is worthless.
4. `A/81/17` — the standing watch. Null on 2026-09-01 at page state `bd63b2d4`.
5. `A/80/17` — its positive control on the same page. Matched yesterday.
6. The **G18 tail** — `https://example.com/` with find `Example Domain`, requested last, so the batch is
   bracketed by the control at both ends and instrument drift across the run is observable by string equality
   rather than by assertion.

**Two rows are NOT fixed by me, and I name the constraint rather than the answer.** The G1 and G2 candidate
literals are the analyst's first task, per decision 2, and each must satisfy three conditions this council has
already fixed: **verified present in a reduced body this project holds**; **argued first-occurrence-safe**; and
— from yesterday's own optimization item — **screened for uniqueness before it is registered as a test**. A
string whose occurrence count in this project's held material is not exactly one is not a discriminator and may
be recorded only as a description. `Reconstituted`, `BEECHEY` and `Australia` are the obvious candidates and
**none of the three is proposed here**, because the screen has not been run on any of them.

**Consequence for the pre-registration, stated plainly.** Yesterday's batch was fired before any seat existed
and commit order proved it. **Today's cannot be, because yesterday's decision 2 assigns the screen to today's
analyst.** So the pre-registration splits: the six rows above and the batch's shape are fixed in this commit,
before any seat is convened, and commit order proves that much; the two literals are fixed by the analyst's
screen and are proven by nothing except the screen, which is filed verbatim. **I say which half is which
rather than let the whole batch inherit a property only half of it has.**

### 5. Citation discipline — binding on every seat today

**Every reduction is cited by COMMIT SHA and BLOB SHA, never by path.** This is not caution; it is the
mitigation that has already been demonstrated to work. The relay's overwrite defect is **structural and fires
on every council merge by construction** (2026-09-01's correction to its second addendum: 29 of the 30 most
recent `fetch-relay` runs on `main` were triggered by a council record merge). **Today's reduction will be
overwritten in place by the merge of today's own record.** A figure cited by path will point at a file no seat
read; a figure cited by blob stays verifiable exactly as filed.

### 6. Spend checkpoint

**Placed after the vetting round, not before it.** When the integrity officer's return is in and I have ruled
on it, I stop and decide whether anything further is worth convening. Nothing beyond the analyst, the officer,
and this chair is convened today unless that checkpoint says so. Per Rule 0, the analytics officer and the
systems researcher are **not** convened: the digest sentence is two lines I copy from `meta.json`, and the
optimization item is one paragraph I write.

### 7. Standing prohibitions carried into today

- **Nothing on the operator's pending list is built or applied**: the `source_analytics.py` patch, the health
  column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal,
  the `build_graph` fix, and any ledger amendment. **Pending is not granted.**
- **The three zero-budget gaps stay at zero budget**: `hela-schwarz-art9-jurisdiction-or-admissibility`,
  `icsid-2022-arbitration-rules-rule-41-48-provision-text`, `icsid-mllm-finding-column-value-codebook`. The
  impossible row stays unproposed for a fifth day. `EXCERPT_CHARS` remains pending and not granted.
- **No search slot on the "Tenth Inter-sessional" question without a body read** (decision 7). This project has
  carried that material since 2026-06-27 in four places; a fifth search-derived restatement is not
  corroboration.
- **A zero-hit screen is not absence until the spellings are tried** (`0901-B1`) — hyphenated and
  unhyphenated, ordinals in figures and in words.
- **Every census is case-insensitive, run per row, printed by its own enumeration, and reported at a LABELLED
  case scope with both columns where they differ** (`0831-C2`, `0901-C6`).
- **The scope of a claim never exceeds the operation supporting it.** That is the analyst's live trap, sustained
  in six consecutive sessions; mine is **certifying a check wider than the check that was run** (`0901-C1`,
  `C5`); the officer's is **the scope label on its own screens**.

### 8. Scope of this Part's own certification

**This Part I is the record of what I wrote before any seat was convened, and it certifies nothing about Parts
II onward, which do not exist as I write it.** That clause is here because `0901-C5` was sustained against my
predecessor for exactly its absence.
