# Archivist session — 2026-09-19

**Audited against `f1a7850`** (`main`), on a complete **1,461-commit** history. The container's
clone arrived shallow at **136** — the **ninth consecutive** session — so every sha below was
resolved only after `git fetch --unshallow`.

**The window is `98dcff9..f1a7850` — 83 commits**, three days.

**The short version, and it is an unusual one.** This is the first window in this log's recent
record in which **nothing this seat audits for drift moved at all**. Zero commits to
`.claude/agents/`, `prompts/` or `src/models.py`. Zero commits to `README.md`, `docs/`,
`scripts/site_templates/`, `METHODOLOGY.md`, `HANDOFF.md`, `COUNCIL.md` or the flowchart manifest.
**Zero new orphan branches** — every council sitting on 09-17, 09-18 and 09-19 landed and had its
branch deleted. So there is no new drift to report, and this record is almost entirely carried
state re-measured, plus the one thing that does move every three days: the taxonomy.

**Three adoptions were unfiled again** — entries 39, 40 and extension 34a — which is **D15** at
occurrences nine, ten and eleven. They are filed now.

**Two corrections are entered against records this seat wrote or relied on, and one of them is my
own.** The 2026-09-16 session's count of the model split is wrong; and the integrity officer's
report that its own definition is stale is wrong. Both are in §3 and §5.

**Nothing new was found that needs you.** Everything on the escalation table is carried. The one
item that has got measurably worse by sitting is **D20**, and it has now cost twelve ungated
merges.

---

## 1. D15 — three more adoptions filed, and the bound seat is now handling the latency three different correct ways

**Filed in this change set** — the first time any of these reached a note:

| Entry | Pattern | Adopted | Proposed / adopted |
| --- | --- | --- | --- |
| **39** | Dedup-screen referent inversion | 2026-09-17 | `226d697` `2026-09-17.md:887` / `710842c` `:1275` |
| **40** | Stale-population screen record | 2026-09-18 | `af479da` `2026-09-18.md:906` / `018c1f1` `:1195` |
| **34a** | Licence-in-the-pre-registration (extension of 34) | 2026-09-18 | `af479da` `2026-09-18.md:904` / `018c1f1` `:1203` |

The canonical table's heading moved **38 → 40**; the live next free number is **41**; **nothing was
renumbered** — 39 and 40 to the tail, 34a beside its parent, both under your rule of 2026-09-13.
Six matching rows were added to [[Agent Registry]]'s adopted-method-rules table.

**Three further patterns were proposed by name on 2026-09-19 and I did not number them.** They are
filed in a **pending block below the table** instead: *zero-screen counted as a carrier* (adopted as
**binding practice, unnumbered**, close-out item 6), *selective self-quotation deflation*, and *own
screen unread as evidence for a second proposition* (the analyst's). This seat does not issue
numbers the council did not issue.

**The finding that is new, and it is good news carried on a bad mechanism.** On **2026-09-17** the
officer did something it has not done before: it **verified the heading before numbering off it**,
found it current — because this seat had moved it to 38 the day before — **took 39 itself**, and
said so plainly:

> *"I am therefore numbering off a heading I verified, not off a stale one, and no chairman's issue
> is needed. This is the first day in this seat's recent record on which the D15 routing latency did
> not fire."* — `analytics/daily-research/2026-09-17.md:885`, `226d697`

On 09-18 and 09-19 it found the heading behind again and **declined the number both times**
(`2026-09-18.md:798`, `79b8e8f`; `2026-09-19.md:875`, `b267efd`). **Four sittings, three different
behaviours, each correct for what the file actually said.** The mandate is no longer merely
holding — it is discriminating.

**And that is precisely what prices the routing question for you.** The one sitting in this window
on which the latency did not fire is the one that immediately followed an archivist session.

> **NEEDS YOU — the arithmetic, stated once so no future session re-derives it.** The archivist's
> cadence is three days. The council sits daily. **Every archivist session will therefore find
> between two and three unfiled adoptions, indefinitely, until either the cadence or the routing
> changes.** This session found three; the last found three; the one before found one. No change in
> any seat's behaviour can move that number — only the routing or the cadence can. Your choice is
> unchanged: give the daily council write access to the taxonomy table in
> `agents/integrity-officer.md` **alone** — a one-file scope widening — or accept the latency and
> have the council's close-out carry an explicit *"UNFILED — routed to archivist on `<date>`"* line.
> **I can execute either and neither is mine to decide.** What this window adds to the argument: the
> risk a widening would create is the one risk the bound seat has now demonstrated, across four
> consecutive sittings, that it manages.

---

## 2. NEEDS YOU — D20: `main`'s suite is still red, and it has now cost twelve ungated merges

Unmoved in five days, and the failure is **byte-for-byte** the one filed on 2026-09-16.

`pipeline-guards` runs **215** (`51481ff`), **216** (`0be5025`) and **217** (`801c7bf`) all conclude
**failure** — all three on 2026-09-19. The `guards` job passes; the `suite` job fails:

| Test | Expects | Archive now returns |
| --- | --- | --- |
| `test_archive_status_measures_the_committed_archive` | `runs: 16`, `screened: 492` | **17**, **499** |
| `test_the_two_denominators_really_do_differ` | **11** digests carrying `per_source` | **12** |

`2 failed, 821 passed, 5 xfailed`. The constants at `tests/test_site_claims.py:155-156` and
`:305-308` are unchanged in the tree. **Nothing has been attempted and nothing has drifted
further.**

**What the five days have cost, counted rather than characterised.** `git log
529e32d0..origin/main --grep='(#[0-9]'` returns **12** squash-merged pull requests, **#192 through
#203** — five council sittings with their addenda, and three vault merges of this seat's own (#195,
#196, #197). **Every one went in against a red base, so not one of the twelve was gated by the
suite.**

> **NEEDS YOU — this is the same ask as 2026-09-16, and the only thing that has changed is the
> price.** **systems-designer or site-experience:** re-measure the eight constants at
> `tests/test_site_claims.py:155-156` and the four at `:305-308` against the archive as committed,
> in one change set. **Emory:** the second-order question is the one that matters — the weekly
> digest is a scheduled writer that changes published figures *and* carries `[skip ci]`, so the
> commit that breaks the suite is the one commit the suite never runs on. Either those commits run
> the suite, or the constants are regenerated by the job that writes the digest. **I do not price
> either**; `tests/`, `scripts/` and `.github/` are all outside this seat's paths.

**And I checked CI before merging this time.** The 2026-09-16 record carries my own admission that
I merged #195 and #196 without looking. That is corrected as behaviour, not just as a note.

---

## 3. NEEDS YOU is not the right marker for this one — a correction I owe against my own last record

The 2026-09-16 session wrote, in **D6**:

> *"Four of nine definitions say 'Opus 5 per the operator's directive' and four say 'Opus 4.8'; one
> ([[research-analyst]]) names no model in prose at all."*

**That is wrong in both halves, and the seat it names is the one it is most wrong about.**
Enumerated from the files this session rather than recalled:

| | Count | Seats |
| --- | --- | --- |
| Description says **Opus 5** | **4** | council-chairman, research-analyst, site-experience, systems-designer |
| Description says **Opus 4.8** | **5** | analytics-officer, integrity-officer, obsidian-archivist, research-editor, systems-researcher |
| Names no model | **0** | — |

[[research-analyst]] names Opus 5 **twice** — in its description and at
`.claude/agents/research-analyst.md:37`, *"Model: Claude Opus 5 (operator directive 2026-07-29)."*
The corrected figures are **4 / 5 / 0**.

What is true, and is probably what the 09-16 pass half-saw, is a **different** gap: **two**
definitions name a model only in the frontmatter description and carry no `- Model:` line in their
DISCIPLINE block — `site-experience.md` and `systems-designer.md`. Both say Opus 5, so nothing
contradicts and `check_models.py` is right to pass.

**Why a four-versus-five miscount is worth this much space.** On its own it is not. It is worth it
because **D6's entire argument is that the split is the record of a decision made twice and applied
once** — and a split is a count. A thread that reasons from a number owes that number a measurement
every time it restates it. That is the same discipline the council adopted as **taxonomy entry 40**
on 2026-09-18, three days after this seat failed it. The 2026-09-16 record is dated and stands
unedited; the correction is filed in [[Workflow Threads]] D6 where the claim lives.

---

## 4. Agent-context audit — no contract drift anywhere, and the model gap is at day thirty-five

- `git log 98dcff9..f1a7850 -- .claude/agents/ prompts/ src/models.py` returns **zero** commits
  across the 83-commit window. **No seat's contract was edited by anyone.**
- All nine definitions declare `model: opus`; `scripts/check_models.py` exits **0** over 12 cards;
  `scripts/check_currency.py` exits **0** over 9 claims; `node tools/isds-workflow-3d/validate.mjs`
  exits **0** — 30 cards, 10 chips, 44 edges, SVG fresh against its inputs-sha256, zero "Jack"
  tokens.
- **"Jack" discipline:** professor-facing surfaces (`README.md`, `docs/`, `scripts/site_templates/`,
  `METHODOLOGY.md`, `COUNCIL.md`, `HANDOFF.md`) are **clean — zero occurrences**. The two known live
  defects are unchanged and both outside my paths: `HUMAN_REVIEW.md:115` and
  `prompts/council_roundtable.txt:73`. (`HUMAN_REVIEW.md:173` is your signature as reviewer and is
  correct as written.)

> **TO VERIFY — D6, seventh first-person observation, day thirty-five.** I queried this session's
> own runtime: `configured_model`, `session_context.model` and `external_metadata.last_served_model`
> are all **`claude-opus-5`**. `.claude/agents/obsidian-archivist.md:3` and `:24` say "Runs on
> Claude Opus 4.8." **REQUESTED `claude-opus-4-8` → ACTUAL `claude-opus-5`.** Consistent with
> 2026-08-16, 08-25, 09-04, 09-07, 09-13 and 09-16. `check_models.py` cannot see this and is not
> failing — it compares three declarations to each other, never a runtime. **I have again left the
> rows unchanged**, because they record your directive and rewriting them would ratify a
> substitution nobody authorised.

**D5 unchanged at day thirty-seven.** Eight of nine definitions never name their seat's own vault
note; the integrity officer's remains the sole exception — and §1 is, for the fourth session
running, the argument for the remedy. Editing `.claude/agents/` is a contract change and is yours.

---

## 5. Where a note would have prevented a failure — this time by stopping a correct file from being "fixed"

The mandate says to record, in the note itself, where a current note would have prevented a recorded
failure. This window produced an inversion of the usual case and it is worth the paragraph.

On 2026-09-19 the integrity officer reported, against itself:

> *"my own definition still enumerates five patterns against a vault of forty."* —
> `analytics/daily-research/2026-09-19.md:875`, `b267efd`

**It does not.** Measured against the file:

- `.claude/agents/integrity-officer.md:11-21` enumerates the **four contract categories** from
  `prompts/council_security.txt` — fabrication risk, overreach, inflated relevance, quote/access
  integrity. Those are not taxonomy entries.
- `:48-56` **expressly forbids** working from an enumeration in that file — *"read it there; do not
  work from a list enumerated in this file, because an enumeration here goes stale silently"* — and
  then names the council-R8 incident in which the definition once **did** name five patterns against
  a vault of ten. **That historical sentence is what a grep for "five patterns" returns.**
- `prompts/council_security.txt` contains no taxonomy enumeration at all — 31 lines, zero matches
  for "taxonomy", "five" or "pattern".

The officer's read path is **correct as written and needs no edit**. I have recorded this in
[[integrity-officer]] at the taxonomy heading, because **the next seat to act on that report would
have edited a correct contract** — and a seat that believes its own contract is stale when it is
current is a memory defect of exactly the kind this note exists to prevent.

The same dated block also records the 09-17 / 09-18 / 09-19 sequence in §1, because that is the
sentence a future officer needs when it next finds the heading short.

---

## 6. Deliverable drift — the whole surface stood still, and METHODOLOGY §III is where it was

`git log 98dcff9..f1a7850 -- README.md docs/ scripts/site_templates/ METHODOLOGY.md HANDOFF.md
COUNCIL.md views/isds-workflow-3d/workflow.json` returns **zero** commits. Nothing was passed over;
the surface did not move.

**D7 re-derived from the code today rather than restated:**

> **FOR EMORY — `METHODOLOGY.md` §III, two numbers, and the isolation is unchanged.**
> 1. `METHODOLOGY.md:33` says Bing News is polled through **eight** fixed, fingerprint-derived
>    queries. `len(bing_news.QUERIES)` is **12**.
> 2. The same sentence enumerates seven open repositories plus two added channels — **nine** —
>    while `len(all_sources())` is **10**.
>
> **Where GDELT is named today:** `README.md` **1**, `docs/` **9 files**, `scripts/site_templates/`
> **1**, `METHODOLOGY.md` **0**. Every professor-facing surface in this project names the tenth
> source except the one document a reviewer reads to judge the method. It is a **two-number edit**
> and it is yours — this seat does not edit your prose. Tracked as **D7**.

---

## 7. Vault mechanics — clean, and clean the first time

**Graph:** **190 notes, 393 edges, 0 orphans**, 80 managed blocks would update, 17 WARNs — measured
on the tree before this record was written. **With this record: 191 notes, 399 edges, still 0
orphans**, 81 managed blocks, 17 WARNs. Growth since 2026-09-16's own post-record measurement
(187 / 390): **+4 notes, +9 edges**. All 17 WARNs are index or session notes exceeding the four-link
cap, expected under the standing convention, none is drift.

**Broken wiki-link targets: four, across three files, all under `think-tank/`, all long-standing,
all outside my paths.** Unchanged from 2026-09-16.

**No broken link was introduced by this change set.** I ran `build_graph.py --dry-run` after **each**
of the five note edits rather than once at the end — the 2026-09-16 record earned that habit the hard
way, having created a line-wrapped wiki-link on the very day it asserted the defect had not recurred.
Nothing to report this time, which is the point of saying it.

**The thread-ID collision is unchanged: three pairs.** `grep -o '^### [A-Z][0-9]*' | sort | uniq -d`
still returns **B5, C11, C12** across 75 threads. Not renumbered, same disposition and same reason.
Your ruling of 2026-09-13 on the taxonomy is the template this needs.

**C8 is still real in the code and still not firing.** `scripts/build_graph.py:198` remains
`pat.sub(block, text)` with no count argument.

---

## 8. Orphan sweep — 175 remote refs, 33 non-ancestors, and for the first time, zero of them new

The repository squash-merges, so a squash-merged tip is never an ancestor of `main`. I classified by
diff content rather than by counting.

**The finding is the absence of one.** Every council sitting in this window — 09-17, 09-18, 09-19,
addenda included — landed on `main` and had its branch deleted. **No branch in the orphan set is
dated later than 2026-09-16.** The D16 mechanism that has produced a stranded anchor edit per session
did not produce one this window.

The carried set, none of it moved:

| Branch / PR | Tip | What is stranded | Age |
| --- | --- | --- | --- |
| **PR #184** `fix/repoint-r21-citations` | `529c744` | Ruling 1's citation half, 409 insertions — §9 | **6 days** |
| `council/analyst-gaps-and-methodology` | `74958fd` | 9 files, 26 insertions; outside my paths | 8 days |
| **PR #150** `chore/reanchor-2026-09-08` | `f73ac06` | anchor edits only | **11 days** |
| **PR #110** `vault/archivist-2026-08-28` | `de73f9f` | five note edits; the 350-line record is safely on `main` | **22 days** |
| `chore/operator-marks-2026-07-27` (**F1**) | `6f9e1da` | 20 operator ledger marks | **54 days** |
| `feat/research-council` | `af00584` | `METHODOLOGY.md` + 8 June digests | 89 days |
| `claude/sweet-mccarthy-*` (×3) | — | 07-30/07-31/08-01 council records | 49–51 days |
| the rest (**24**) | — | stranded anchor edits and `.sent` markers only | — |

**PR #110 is at day twenty-two** and I again did not cherry-pick it, for the reason five prior seats
gave and which still holds: those are live statements about a repository that has moved far past
them, and re-deriving beats importing. **F1 is at day fifty-four.**

`vault/archivist-2026-09-16` (`77c9036`) is still on `origin`, as the last record predicted. Branch
deletion still fails from this container.

---

## 9. PR #184 — day six, and the guard it carries is dormant while the condition it guards is true

Re-measured on `main` today, not taken from the branch.

PR **#184** is open at **day six**, head unchanged at `529c744`, last updated 2026-09-13T22:49Z —
**nothing has been pushed to it and nothing has been said on it.** `src/rings.py:923` and
`scripts/check_lock.py:20` both still carry the bare *"R2.1 record"* citation on `main`, and the
record they should point to — `analytics/locked_set/VALIDATION_RECORD.md` — **is** on `main`.

**So the branch's strict-xfail forcing function — written so it fails the build the moment the cited
record can be opened — has been dormant for six days while exactly that condition is true.** That is
a guard designed so it could not stay dormant through a merge, kept dormant by the merge not
happening.

The council noted the PR's age itself on 2026-09-19 and classed it *"hygiene, not a finding"*
(close-out item 9, `b267efd`). That is right about the council's own exposure and does not reach the
dormant guard, which is why the thread stays open.

> **NEEDS YOU — land PR #184, or close it and say which of those two citations stands as written.**
> Every path in it is outside this seat's merge scope, so I cannot land it and did not try. Tracked
> as **D19**.

---

## 10. D18 — improved to 1 of 3, and the 2026-09-16 diagnosis held for a third session

`main` is **green** as I write: `check_currency.py` exits **0** at `f1a7850`.

Same method as the last two sessions — `main`'s tip at the end of each UTC day, every tracked note's
anchor evaluated against it, in a detached worktree.

| Day | Tip | Verdict | What staled it |
| --- | --- | --- | --- |
| 09-17 | `5e16471` | RED | `chore: daily-update sent marker [skip ci]` |
| 09-18 | `de9fee0` | green | tip was a re-anchor |
| 09-19 | `f1a7850` | green | tip was a re-anchor |

**The one red carries exactly one STALE line and it is [[Workflow Threads]]** — the other four
tracked notes clean. Third consecutive session in which every red resolves to that one note's
`analytics` declaration meeting a `[skip ci]` bot writer. The writer this time is a **fourth** name
alongside the weekly digest, the scholar intake and the fetch relay: the daily-update sent marker.

**The rate improved from 6-of-10 to 1-of-3 and nobody fixed anything.** I want that stated so the
improvement is not mistaken for a remedy — it tracks how often the day happens to end on a re-anchor,
not a change in the mechanism.

**The fix I am still NOT proposing**, restated because the temptation grows as the number shrinks:
narrowing that note's declared paths so its own guard stops going red would be this seat marking its
own homework, which is the precise failure the guard exists to prevent. The two real options are
unchanged and both need pricing I cannot do — drop `[skip ci]` from the bot writers, or give
`reanchor.yml` a scheduled run after the day's last automated writer. **Still systems-designer's.**

---

## 11. One escalation from the last record closes itself

**The optimization log's silence is fully resolved and needs nothing from you.** It now carries
entries for **09-14 through 09-19 continuously**. The bounded gap the last record narrowed — 09-07 to
09-13, of which 09-11 and 09-12 state expressly that their item was deduped into an existing entry —
is unchanged and I am not re-escalating it. I still cannot establish the log's curation rule, so I am
still not asserting it is wrong.

---

## 12. What I could NOT verify

Stated rather than omitted.

- **A third test failure appears locally and does not exist on `main`.**
  `tests/test_one_pagers.py::test_missing_pdf_skips_case` fails here on
  `assert os.path.exists(.../seeds/Philip_Morris_v_Australia_Award_2015-12-17.pdf)`. `seeds/` is
  **gitignored** (`.gitignore:2`) and absent from this container. CI reports **2 failed, 821
  passed**; I get **3 failed, 830 passed, 3 skipped** after installing the requirements. **This is a
  container artefact and not a defect in `main`** — I am recording it because a future session
  running the suite locally will hit it and should not file it as a finding.
- **Running the suite at all took four rounds of dependency installation.** `jinja2`, `feedparser`
  and `pytest` itself were all missing or bound to the wrong interpreter (`/root/.local/bin/pytest`
  does not see `/usr/local/lib/python3.11/dist-packages`). The fix was `pip install -r
  requirements.txt` and `python3 -m pytest`. Recorded so the next session spends one command instead
  of four.
- **Why the clone keeps arriving shallow** — ninth consecutive session, now at **136** commits
  against a real 1,461. I can show it happens; I cannot see the container provisioning.
- **`gh` is still not installed in this container**, for the third session running. My standing
  instructions say to close with `gh pr create` and `gh pr merge`; I used the GitHub MCP tools
  instead, which reach the same API. Flagging it a third time so the instruction and the environment
  get reconciled rather than silently diverging.
- **I did no external Obsidian/PKM research this session and am not dressing anything up as it.** My
  mandate asks for it *periodically*; the 2026-09-16 session did a bounded ecosystem scan and its
  finding was that nothing needs changing. Three days is not long enough for that to have changed,
  and the session's time went to the taxonomy filing and the two corrections instead. Saying so
  plainly rather than padding §12 with an internal finding wearing a citation.
- **Whether D11 fires on this pull request** — I cannot know before pushing. If it does, the
  addendum is below; if there is no addendum, it did not.

---

## Escalation summary

| # | Item | Owner | Age |
| --- | --- | --- | --- |
| 1 | **D15** — taxonomy 39, 40 and 34a adopted and unfiled until today; occurrences nine, ten and eleven. Three more proposed by name 09-19. The bound seat handled the latency correctly on all four sittings | **Emory** (routing or cadence) | 12 days |
| 2 | **D20** — `main`'s suite red since 2026-09-14; **twelve** merged PRs (#192–#203) went in ungated; two snapshot constants still need re-measuring | systems-designer / site-experience; **Emory** for the writer question | 3 days |
| 3 | **D19** — PR #184 at day six; `main` still carries two dangling "R2.1 record" citations, and the branch's forcing guard is dormant while its condition is true | **Emory** | 3 days |
| 4 | **D18** — 1 of 3 days red; diagnosis held a third session, narrowed to one note's `analytics` declaration meeting four named bot writers | systems-designer / **Emory** | 6 days |
| 5 | **D7** — `METHODOLOGY.md` §III: eight queries against 12; nine sources against 10; still the only surface that does not name GDELT | **Emory** | carried |
| 6 | **PR #110** — 2026-08-28 session's five note edits still stranded | **Emory** | 22 days |
| 7 | **Thread-ID collision** — three pairs (B5, C11, C12), unchanged; your 2026-09-13 taxonomy ruling is the template | **Emory** to authorise; archivist to execute | 15 days |
| 8 | **D6** — seat models: requested Opus 4.8, served `claude-opus-5`; seventh first-person observation. **Split corrected to 4 / 5 / 0** | **Emory** | 35 days |
| 9 | **D5** — eight of nine definitions never name their own vault note | **Emory** (contract change) | 37 days |
| 10 | **F1** — operator-marks branch never merged (`6f9e1da`), 20 ledger marks | **Emory** | 54 days |
| 11 | **C8** — `build_graph.py:198` `sub` without count; latent, not firing | systems-designer | carried |
| — | ~~optimization-log silence~~ — resumed and continuous 09-14 → 09-19 | **CLOSED** (§11) | closed |

---

## Addendum — written after the merge, because two of the things above were conditional on it

### D11 fired, once, and the one manual restore was enough

The record above said *"if there is no addendum, it did not."* It did.

`reanchor.yml` fired on PR **#204**'s open and pushed `5e77d80`, whose entire content is one line —
`STATE_OF_THE_ANSWER.md`'s currency anchor moving. Nothing else in the commit. That single
machine-written line put an out-of-scope path into an archivist change set, which is the one thing
that makes this seat's own PR unmergeable by this seat.

**Handled as the 2026-09-01, 09-04, 09-07, 09-13 and 09-16 sessions each handled it:** restored the
file to `origin/main`'s content in a commit of my own (`b1f37b7`), re-ran the path check on the
result — **7 paths, all inside `agents/` and `analytics/`** — and merged the verified head pinned by
sha. **Seven sessions have now spent a manual restore on one machine-written line.**

**It fired once on PR #204, not three times.** The 2026-09-16 session established that the trigger
is *one per push* rather than one per PR, and was driven into a loop by needing a second push.
PR #204 needed exactly one correction, so one restore closed it. **That confirms the 09-16 diagnosis
rather than softening it:** the cost scales with the number of pushes a session needs, and a session
that needs two is in a loop it can only exit by beating a GitHub Actions cold start.

**Stated precisely, because this addendum is itself a second push.** The count above is PR #204's.
This addendum is a separate pull request with its own trigger, and whether it fires there is not
knowable from inside the commit that says so — the same conditional the paragraph above was written
under. **What is decidable and was decided: the addendum PR's scope was re-verified at one file,
`analytics/vault-sessions/2026-09-19.md`, immediately before its merge, and it carried no
machine-written commit at that moment.** If one arrived afterwards, it is a one-line anchor edit on
a branch this container cannot delete, in the category the last section describes, and it changes
nothing about what landed.

> **NEEDS YOU — D11 is still the cheapest thing on this page.** You already fixed the hard half:
> since `8a9158a` (PR #152, 2026-09-08) every push to `main` re-anchors `main` itself, so **`main`'s
> correctness no longer depends on any PR's bot commit landing** — verified again today, `main`'s
> copy of `STATE_OF_THE_ANSWER.md` was already correct and my restore changed nothing about the
> outcome. That makes the PR-level re-anchor of this one file **redundant for the outcome** while
> remaining the only thing that blocks an archivist merge. Two ways out, both small: **(a)** have
> `reanchor.yml` skip its `pull_request` trigger for branches matching `vault/*`, since `main`'s
> push trigger now covers the outcome; or **(b)** widen this seat's merge scope by exactly one file,
> `STATE_OF_THE_ANSWER.md`, whose only archivist-side change is ever a machine-written anchor line.
> **I recommend (a), unchanged from 09-13 and 09-16** — it removes the commit rather than
> authorising it, and it needs no new trust. The workflow header already states the invariant that
> makes (a) safe: the file has no `paths:` filter and must not acquire one; a branch-name exclusion
> on one trigger is not a path filter and does not touch that invariant.

### I checked CI on my own merge this time, before claiming anything about it

The 2026-09-16 record carries this seat's admission that it merged PR #195 and #196 without looking
at CI and found the red afterwards, from a notification. That is corrected as behaviour.

`pipeline-guards` **run 220** on `cc572ed` — my merge — concludes **failure**. I read the job log
rather than the conclusion. The `guards` job **passes**; the `suite` job fails on
**exactly** the two `tests/test_site_claims.py` assertions of **D20**, unchanged:
`runs` 16→17, `screened` 492→499, `per_source` 11→12, `2 failed, 821 passed, 5 xfailed in 31.66s`.
**`model-consistency` run 133 concludes success.** No path in this change set — `agents/`,
`analytics/` — can reach `tests/test_site_claims.py`.

**So this merge inherited a red it did not cause, which is the thirteenth to do so, and I knew that
before I merged rather than after.** It is also the plainest illustration of what §2 is asking you
to fix: I had to read a job log to establish that a red build was not mine.

### One residue I could not clean up, for the third session running

`vault/archivist-2026-09-19` still exists on `origin` carrying the merged history plus `5e77d80`.
**Branch deletion does not work from this container** — `git push origin --delete` fails at the
proxy with *"send-pack: unexpected disconnect while reading sideband packet / the remote end hung up
unexpectedly"*, and the merge API call available to me takes no delete-branch option. The same is
true of `vault/archivist-2026-09-16` and `vault/archivist-2026-09-16-d11`, which the last session
left for the same reason.

**This is why the orphan sweep in §8 will keep growing by one or two a session** and why "zero new
orphans this window" was worth remarking on: the council's branches are deleted on merge and this
seat's are not. That is not a difference in discipline — it is a difference in what the container
can reach. Recorded here so the next session finds it explained rather than rediscovers it.
