# Daily Council Meeting — 2026-08-04

**Research question:** When and how is intellectual property — especially trade secrets and pharmaceutical clinical trial data — asserted as a covered investment under an investment treaty, challenged through a governmental regulatory or judicial measure, and disposed of at the jurisdictional or admissibility stage?

**Procedural caveats (chairman).** (1) This session runs delegated in the scheduled cloud environment: the chairman directs and every member section below is what that member's real subagent returned, relayed verbatim; no member's voice is performed. (2) Model directive: per the operator's directive of 2026-08-03, the chairman and the research analyst run on Claude Opus 5 (both top seats moved from Fable 5 when the Fable credit balance was exhausted); the integrity, analytics, and systems seats are spawned from their committed definitions under the harness's `opus` alias, which the operator's directive intends as Claude Opus 4.8. As recorded on 2026-08-03, the exact version that alias resolves to in this cloud harness is not independently verifiable in-session; per the never-substitute-silently rule that uncertainty is recorded here rather than papered over. (3) A correction to the record: the 2026-08-03 record's first procedural caveat stated that "the scheduled routine did not fire on 2026-08-01 or 2026-08-02: no records exist for those dates." That statement was wrong. Both sessions had in fact run; their records reached `main` after the 08-03 session had already read `main` to build its agenda, so the 08-03 chairman read a repository that did not yet contain them. `analytics/daily-research/2026-08-01.md` and `2026-08-02.md` are both on `main` today. Nothing is backfilled and nothing about the 08-03 substance changes; the false absence claim is corrected here because each session builds its agenda by reading the previous record, and an uncorrected false absence propagates.

---

## Part 1 — Chairman's Agenda

### The instrument check, before the agenda

Per the delegation rule adopted 2026-07-31 and amended 2026-08-03 — *probe the instruments before writing the agenda; agenda scope follows the instruments actually alive* — I ran the two probes at session open, before setting any task.

**Direct fetch: still dead, both layers, unchanged.**

- Proxy egress (curl), neutral control hosts only: `https://example.com` and `https://www.iana.org` both returned `curl: (56) CONNECT tunnel failed, response 403`. The gateway refused the tunnel; no origin was contacted.
- Fetch service, neutral control: `WebFetch` on `https://example.com` returned HTTP 403 with no response body retrieved.

Both probes used neutral controls, not research hosts, so neither result is — and neither may be read as — information about any research resource. This is the standing tool-status-as-source-state rule, and it applies to today's null exactly as it applied to 2026-08-03's.

**But the retrieval picture has changed, and this is the day's state change.**

Between the close of the 2026-08-03 session and today, the project built and merged a **fetch relay** (`.github/workflows/fetch-relay.yml` + `scripts/fetch_relay.py`, PR #40, plus a targeted-excerpt refinement). A session with no network of its own commits a small JSON request file under `analytics/fetch-requests/`; the push fires a GitHub Actions runner, which *does* have egress; the runner fetches each URL through the project's own `polite_get` — same User-Agent, same robots.txt evaluation, same per-domain interval — and commits back a reduction (status, final URL, content-type, byte length, sha256, timestamp, and one excerpt capped at 400 characters) to `analytics/fetch-results/`. The document body itself never travels: the repository is public and a commit is publication.

So the correct statement of this session's capability is no longer "this session cannot fetch." It is: **this session cannot fetch directly, and has a working indirect retrieval path with a controlled, auditable reduction.** The 2026-08-03 escalation to the operator asked where fetch-dependent tasks should live; the project answered it in code the same evening. Fetch-dependent items are therefore **no longer stranded**, and I am not scoping today to the search track.

Three relay disciplines are binding on today's work, and I state them in the agenda so the members work to them:

1. **Every batch carries the neutral control** (`https://example.com/`). If the control fails, the whole batch is VOID and no row in it may be read as information about its resource — the runner was the problem, not the site.
2. **Every requested URL gets a row**, including failures, and `no_contact` is never recorded as an origin answer.
3. The relay is a **retrieval** channel for what the sources or the record already identified, not a **discovery** channel (METHODOLOGY Part VIII); its host allowlist enforces this.

I fired today's batch at session open so it would run while the council works: `analytics/fetch-requests/2026-08-04-daily.json`, five rows — the A/81/17 day-4 check at the Commission Sessions posting location in the standard same-page positive-control form, a first test of the `undocs.org` symbol route (which the record has carried as an untested find-path), and an independent second retrieval of the China–Switzerland treaty-978 forum-relationship cell with its own same-page positive control.

### The item the record does not yet know it has

The most consequential fact I found reading into today's session is not in yesterday's record, and could not have been: **the 2026-08-03 relay run answered the China–Switzerland forum-relationship question at 22:17 UTC, after the 08-03 record had already been written and closed.**

`analytics/fetch-results/2026-08-03-targeted.json` records a live 200 from the UNCTAD Investment Policy Hub page for China – Switzerland BIT (2009), treaty 978, with the excerpt window centred on the requested string, returning the mapping table's forum block. That gap — `GAP-UNRESOLVED: china-switzerland-forum-relationship` — has been standing since 2026-07-31, was escalated to the operator as recently as last night as escalation 1, and the answer has been sitting in the repository unread for the intervening hours.

This is exactly the failure mode the council has been fighting all week in a different costume: work that exists but has not reached the record. So Task 1 today is not "go find it." It is **interpret what we already retrieved, verify it against a second dated retrieval, and either close the gap or say precisely why it cannot close.**

### Tasks and budgets

**Task 1 — China–Switzerland forum relationship (analyst; primary; relay-sourced).** Read `analytics/fetch-results/2026-08-03-targeted.json` and today's second retrieval when it lands. State what the UNCTAD mapping cell actually says, distinguishing the *retrieved cell value* from any *characterisation* of it in fork-in-the-road / no-U-turn vocabulary. The 08-03 record left the label expressly deferred pending Task 2; honour that. Budget: no web search needed; this is reading a retrieved reduction.

**Task 2 — Forum-clause taxonomy reconciliation (analyst; zero-fetch, record-only; assigned yesterday).** Reconcile the three-category taxonomy in `analytics/daily-research/2026-07-08.md` with the two-category usage at `STATE_OF_THE_ANSWER.md:64` and `:84`, from the record's own texts only. This was opened yesterday as an ordinary gap marker and assigned to the analyst at its next seating; it is that seating. Task 1's label question depends on its output, so it is done in the same memo.

**Task 3 — A/81/17, day 4 (analyst; one query, relay-checked).** The publication window opened 2026-08-01. Report the relay row, in the same-page positive-control form the record has adopted as the standard null. Standing rule: **no search-synthesis figures** — a search result set is evidence about an index, never about whether a document exists.

Zero budget, unchanged and not re-argued: the China–France Protocol full text; any ledger amendment; and every pending operator approval (the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix). Nothing on that list is built or applied today.

### Member return-path protocol

Each member is spawned as a real subagent from its committed definition in `.claude/agents/`. A member's returned text enters this record **verbatim**, under its own heading; I do not write, summarise, or improve any member's contribution. If a spawn fails or a member returns nothing usable, that seat is recorded as **PROCEDURAL FAILURE** for the day and the record says so — the seat is never performed.

### Spend checkpoint

A checkpoint falls **after the vetting round (Part 3)**: Parts 1–3 are committed and pushed before the analytics seat is convened, so the session's substantive work is never more than one commit from safe. This is the discipline that saved the 08-03 record and it holds today.

### Order of business

Part 2 analyst → Part 3 integrity officer vets the analyst's return (positive control before any HTTP-status objection; raw source for any quote claim; **grep the record before asserting something is absent from it** — the precedent set by yesterday's overruled objection B3(b)) → **spend checkpoint** → Part 4 analytics officer on the 2026-08-03 digest, which landed since the last session → Part 5 systems researcher only if its queue has ungated work → Part 6 my close-out, rulings, accountability, next steps, escalations.

---

## Part 2 — Research Analyst

*Spawned as a real subagent from `.claude/agents/research-analyst.md` (Claude Opus 5 per the operator's directive). The memo below is the analyst's return, verbatim and unedited. — Chairman*

# RESEARCH ANALYST — MEMO OF 2026-08-04

*Council seating of 2026-08-04 (verified `date -u +%F` → 2026-08-04). Branch `council/daily-2026-08-04`, rebased on `origin` at `db38b78`. Nothing committed by me.*

---

## METHOD NOTE (stated first, because it governs how every result below may be read)

**Instruments actually used.** Zero web searches. Zero direct fetches. Every retrieval result in this memo comes from two relay reductions already in the repository: `analytics/fetch-results/2026-08-03-targeted.json` (run 30858039847, fetched 2026-08-03T22:17:09Z) and `analytics/fetch-results/2026-08-04-daily.json` (run 30904314141, fetched 2026-08-04T11:20:27Z). Both carry `control_ok: true`, `void: false`; today's batch is 5/5 completed, `truncated: false`. I did not re-derive the direct-fetch death; the chairman probed it at session open on neutral controls and the standing tool-status-as-source-state rule bars reading either failure as information about any research resource.

**No search was fired because none would change an answer.** Task 1 is a reading of a retrieved reduction; Task 2 is expressly zero-fetch and record-only; Task 3's remaining question is a fetch question about a resolver's behaviour, which a search index cannot answer (standing rule: a search result set is evidence about an index, never about whether a document exists). Reporting this as a deliberate zero-spend, not as an omission.

**Four properties of the relay instrument that I had to establish from `scripts/fetch_relay.py` before reading any row, and which the record should carry:**

1. **`find` is case-insensitive.** `excerpt_of()` does `body.lower().find(find.lower())`. So the 08-03 row that requested lowercase `relationship between forums` and matched a page rendering `Relationship between forums` is not an anomaly, and a `find_matched: false` cannot be explained away by capitalisation.
2. **`find` runs against tag-stripped text, not raw HTML.** `_TAG_RE` removes `<script>`/`<style>` blocks entire; `_ANY_TAG_RE = re.compile(r"<[^>]+>")` removes every tag *including its attributes*. **Consequence: a string present only inside an `href` or other attribute is invisible to the relay.** This directly qualifies the A/81/17 null below, and it is a material difference from the 2026-08-01 officer's null, which was verified against raw HTML.
3. **`find_matched` is `bool(excerpt)`, not an independent test.** A row whose `find` string is one the target page would echo back from the request itself cannot distinguish "present in the document" from "echoed from the URL." That defect is realised in today's `undocs.org` row (Task 3(b)).
4. **The control row's `find_matched: false` is an artefact of an empty `find` string**, not a control failure. `control_ok` is `outcome == "ok"`, and it is true. No one should later read that `false` as a degraded control.

**Method note for the self-training file (this is the day's transferable lesson).** The 2026-07-26 lesson was *author-name+title beats keyword search*. Today's is the relay analogue: **never choose as `find` a string the target page can generate from your own request.** The productive form is the one both UNCTAD rows used — request a *neighbouring structural label* (`"ISDS forum options"`, `"A/80/17"`) and read the answer out of the window around it. The label you ask for is the anchor; the value you want is what sits next to it. Two anchors on the same page, chosen on opposite sides of the target, bracket a cell value and prove the page's element ordering at the same time. That is what made Task 1 answerable and Task 3(b) not.

---

## TASK 1 — China–Switzerland BIT (2009), forum-relationship cell

### 1.1 What was retrieved

Four fetches of the UNCTAD Investment Policy Hub treaty-978 page, across two runner sessions ~13 hours apart, all `outcome: ok`, `status: 200`, `content_type: text/html`, `bytes: 207411`, `final_url` identical to the requested URL (no redirect):

| run | `find` | matched | sha256 (first 8) |
|---|---|---|---|
| 08-03 22:17Z | `relationship between forums` | true | `f4f7de67` |
| 08-03 22:17Z | `fork in the road` | **false** | `3fb47007` |
| 08-04 11:20Z | `Relationship between forums` | true | `13c76aa0` |
| 08-04 11:20Z | `ISDS forum options` | true | `77b4ef66` |

The three matching excerpts are mutually consistent. The widest left-context window (today's `ISDS forum options` row) reads, verbatim from the reduction:

> "or prudential measures No Type of consent to arbitration Provides express or implied consent Forums ISDS forum options Domestic courts of the host State Yes ICSID Yes UNCITRAL Yes Other forums No **Relationship between forums Preserving right to arbitration after domestic court proceedings** Other specific ISDS features Limitation period for submission of claims No Provisional measures No Consolidatio"

That is the whole of what we have. The phrase appears complete — not truncated at either excerpt boundary — in all three matching rows.

### 1.2 (a) The retrieved cell value

**The UNCTAD IIA Mapping element "Relationship between forums" on the treaty-978 page carries the value "Preserving right to arbitration after domestic court proceedings."** Confidence **HIGH** as to the string being on that page, in that element, at both timestamps.

That this is the element's *value* rather than a further sub-row label rests on three supports, and I state them because the flattened text does not mark table structure:

- The immediately preceding element on the same page is also phrase-valued: "Type of consent to arbitration | Provides express or implied consent." So this table demonstrably mixes Yes/No sub-rows with phrase-valued elements; the phrase-valued form is not an inference from elsewhere.
- Every Yes/No sub-row in the retrieved window is followed by its token ("Domestic courts of the host State **Yes** … Other forums **No**"). The candidate phrase is followed by no token at all — the next string is the next section heading, "Other specific ISDS features."
- The operator-verified ledger snapshot for the sibling treaty records this identical element in phrase-valued form: `"Relationship between forums: 'Fork in the road'"` (`analytics/verification_ledger.jsonl:54–55`, claim `da33a30be92ab234`, `operator_verified` 2026-07-27, `quote_ok: true`, `scope_ok: false`).

Confidence on the structural reading: **HIGH**.

**One identification caveat that must not be dropped.** Nothing in any retrieved excerpt names China or Switzerland. The identification of this page as the China–Switzerland BIT (2009) rests on the URL slug (`…/978/china---switzerland-bit-2009-`), on the `final_url` being identical to it, and on the record's own independent association of treaty id 978 with that instrument (`STATE_OF_THE_ANSWER.md:42`; `analytics/daily-research/2026-08-02.md:79`). That is good, but it is URL-level identification, not page-text identification. Confidence **MEDIUM-HIGH**. Note also that the relay's path form (`…/treaties/bilateral-investment-treaties/978/…`) differs from the path the record has been citing (`…/treaties/bit/978/…`); both resolve, the treaty id is the same, and there was no redirect.

### 1.3 (b) UNCTAD's vocabulary and characterisation — what this is *not*

The record now attests **three distinct values** of this one mapping element, all from the same database:

- China–France BIT (2007): **"Fork in the road"** — operator-verified snapshot, ledger `da33a30be92ab234`.
- China–Germany BIT (2003): **"no U-turn" (waiver clause)** — operator retrieval, `HUMAN_REVIEW.md:191`; `STATE_OF_THE_ANSWER.md:40`.
- China–Switzerland BIT (2009): **"Preserving right to arbitration after domestic court proceedings"** — today's and last night's retrievals.

This string appears nowhere else in the repository (grep of all `.md`/`.json`/`.jsonl`: only the three relay excerpts). It is new to the record.

Two things follow, and they are not the same thing.

**First: the gap's own framing was a false dichotomy.** `agents/Workflow Threads.md:72–75` and `state/research_log.json:9` both frame the open question as "fork-in-the-road **vs.** no-U-turn." The mapping's answer is *neither*. The correct report is not "we found it is a no-U-turn" but "the mapping records a third value that the binary did not contemplate."

**Second: I cannot tell you what UNCTAD means by that phrase, and I will not construct it.** The record contains no statement of UNCTAD's IIA Mapping codebook or the option list for this element (grep for "codebook", "option list", "mapping vocabulary" across the repo returns nothing; `METHODOLOGY.md` has no IIA Mapping entry). I therefore cannot say whether "Preserving right to arbitration after domestic court proceedings" is UNCTAD's code for *unconditional* preservation or for preservation *subject to a condition the code does not spell out*. Read on its face it is the anti-fork-in-the-road position: resort to domestic courts does not extinguish the arbitration right. That much is plain from the words. Anything past that is codebook knowledge I do not have.

The mapping also carries the whole caveat class the record has already fixed to this database: provision-level characterisation, student-mapped, informational, non-authoritative (`HUMAN_REVIEW.md:198`; `STATE_OF_THE_ANSWER.md:130` row 7 for China–France). And a further one specific to treaty 978: **the mapper attribution and mapping vintage are unknown to us.** For China–Germany the record has "Xiamen/La Plata, 03-2013"; for China–France "Laval/Paraiba, 10-2016"; for China–Switzerland we have nothing, because the 400-character excerpt window did not reach that part of the page.

### 1.4 (c) The treaty text — not read, and nothing here substitutes for it

We have not read the China–Switzerland BIT (2009) or its Protocol. No statement in this memo is a statement about what the treaty provides. The record's only text-level material on this clause is the 2026-08-03 **LEAD** — an unattributable search-engine synthesis quoting a provision that would permit arbitration "only … after withdrawal by the investor of the case from the domestic court" (`analytics/daily-research/2026-08-03.md:73`, held at LOW-MEDIUM, explicitly not a finding, and I am not upgrading it).

The relation between the retrieved cell and that lead is worth stating precisely, because it is easy to overstate in both directions:

- The retrieval **corroborates the lead's direction** — both say arbitration survives domestic court proceedings — and it **refutes the lead's own self-contradictory label**, since the synthesis simultaneously called the clause "fork-in-the-road" while quoting withdrawal-cure language. At mapping level the treaty is not coded fork-in-the-road. (The `find_matched: false` on `fork in the road` across the whole page supports this but does not carry it: the find is case-insensitive but not hyphenation-insensitive, so a rendering as "Fork-in-the-road" would have escaped it. The positive cell value is what carries the point, since the element takes one value.)
- The retrieval **does not verify the lead's text.** The cell value says nothing about withdrawal. Two readings of the cell remain open and I cannot choose between them: **(i)** unconditional preservation — arbitration available even after domestic proceedings have run to judgment; **(ii)** conditional preservation — the right survives subject to withdrawal, with the mapping coding the *effect* and not the *condition*. Reading (ii) is what the lead would imply if genuine. Only the treaty text settles it.

### 1.5 The two retrievals, the differing sha256, and whether it matters

**It does not matter, and the reason it does not matter is itself evidence.** All four treaty-978 fetches returned **exactly 207,411 bytes** — across two runner sessions, two GitHub Actions runs, and ~13 hours. All four have different sha256. Content that actually changed would be overwhelmingly unlikely to preserve byte length to the byte, four times; a fixed-width varying field (a CSRF token, a request id, a constant-width timestamp) explains identical length with different digests exactly.

That inference is testable within the same batch and it passes: the relay is deterministic on a static resource. `https://example.com/` returns `ff67a9d7…` at 559 bytes in *both* runs, and `https://uncitral.un.org/en/commission` returns `ca87a113…` at 832,338 bytes in *all three* fetches across both runs. So digest variation is not an instrument artefact — it is origin-side, and its confinement to a constant byte length points to a nonce rather than a content change.

**Net: the second retrieval is a genuine independent confirmation.** Different run id, different UTC hour, same element, same value, with its own same-page positive control (`ISDS forum options` matched, bracketing the target from the other side). I would treat a differing byte *length* as a reason to re-read before closing anything; a differing digest at constant length is not.

### 1.6 Disposition of `GAP-UNRESOLVED: china-switzerland-forum-relationship`

**CLOSES PARTIALLY — at mapping level only.** Specifically:

- **Closed:** the question the gap was opened to answer at the level the record's sibling entries are answered — what the UNCTAD IIA Mapping records for this treaty's forum-relationship element. Answered, twice, with controls. This is the same evidentiary tier the China–France row occupies, and it is now the third and last cell of the three-way treaty-selection comparison opened 2026-07-31.
- **Dissolved rather than answered:** the "fork-in-the-road vs. no-U-turn" binary in which the gap was framed. Neither term is the mapped value.
- **Stays open:** (a) the treaty/Protocol text — the instrument is unread, and the unconditional-vs-conditional ambiguity in §1.4 turns on it; (b) the mapper attribution and vintage for treaty 978, which the record has for the other two treaties and needs for like-for-like confidence calibration.

**Recommended disposition (chairman's call, not mine to make).** Mirror the China–France precedent at `agents/Workflow Threads.md:57–68` (A3) exactly: retire the slug as framed, record the mapping-level answer with the `quote_ok`-analogue flags, and open one ordinary residual — proposed slug **`china-switzerland-bit-2009-forum-provision-text`**, zero escalation, no inherited priority.

**On escalation 1 to the operator (`analytics/daily-research/2026-08-03.md:307`): I recommend it be stood down as an escalation.** The manual IIA-mapping query on treaty 978 is no longer the gap-closing action; the mapping answer is in hand and independently dated. It retains *residual* value of a different kind — capturing the mapper attribution, the vintage, and the full element table into a preserved `source_snapshot` (the July 31 snapshot lesson, which the China–France entry was faulted for missing). That is corroboration and record hygiene, not gap closure, and it should be re-labelled as such rather than kept at escalation priority.

### 1.7 What this does to the three-way comparison

Stated as inference on top of mapping-level text, flagged as such, and stated *after* Task 2 as directed:

On the forum-relationship axis alone the three mapped European China BITs now separate cleanly, and **China–Switzerland is the least restrictive of the three**: China–France's mapped fork-in-the-road puts an irrevocable election in the path of any resort to Chinese courts; China–Germany's mapped no-U-turn permits a cure by withdrawal; China–Switzerland's cell says the arbitration right is preserved after domestic court proceedings at all. Combined with the two legs the record already rests on — the permissive "may require" exhaustion formulation (`STATE_OF_THE_ANSWER.md:130`, MEDIUM-HIGH) **and** the explicit "know-how" Ring 1 coverage (`STATE_OF_THE_ANSWER.md:177`; both legs restored per the 2026-08-02 H3 ruling) — this **strengthens the standing July 19 conclusion** that the China–Switzerland BIT (2009) offers the best combined Ring 1 + Ring 3 profile among the European China BITs in scope for a pharmaceutical trade-secret claim. Confidence on the comparative picture: **MEDIUM** (unchanged in kind; the new cell adds a leg but adds no evidentiary tier).

**The cross-BIT consistency and shared-weakness disclosure (H4) applies at full strength.** All three values come from one database, one element, one non-authoritative student-mapped class, at three unknown-to-partially-known vintages. If the mapping is wrong about one treaty the comparison is unreliable across all three; the contrast rests on the mapping labels, not on three independently verified instruments. And absence of contrary material is "not found in accessible sources," never "does not exist."

---

## TASK 2 — Forum-clause taxonomy reconciliation (`forum-clause-taxonomy-reconciliation`)

Zero-fetch, record-only, as assigned at `analytics/daily-research/2026-08-03.md:265` and :299. Every claim below is about what the record says, with file:line.

### 2.1 The finding: this is not two taxonomies. It is one taxonomy plus one superseded, internally inconsistent formulation.

**Evidence 1 — the 07-08 file defines two structures, not three, two paragraphs above the disputed list.** `analytics/daily-research/2026-07-08.md:62`:

> "Fork-in-the-road (FITR) clauses provide that once an investor has submitted a dispute to domestic courts, it may not thereafter pursue ICSID arbitration for the same dispute — the election is irrevocable. A 'court-withdrawal' clause, by contrast, permits commencement of arbitration upon withdrawal of domestic proceedings (the investor can cure the defect)."

Two categories, both defined. "No-U-turn" is not defined anywhere in that file.

**Evidence 2 — the disputed passage is an open question, not a taxonomy assertion, and its middle term collapses into its first.** `analytics/daily-research/2026-07-08.md:64`:

> "Whether the specific China-Germany BIT (2003) provision operates as a pure FITR (irrevocable), a no-U-turn (once domestic claim filed, no return), or a withdrawal-permitting clause with a cure pathway — this distinction matters…"

That is a list of *candidate characterisations* the analyst could not yet choose between. And its gloss on the middle term — "once domestic claim filed, no return" — is materially the definition the same file gives FITR at :62 ("once an investor has submitted a dispute to domestic courts, it may not thereafter pursue ICSID arbitration"). **As glossed, term 2 is term 1 under a second label.** The list therefore names three labels over two structures.

**Evidence 3 — the very next day the record acquires a sourced definition that assigns "no-U-turn" to the third slot.** `analytics/daily-research/2026-07-09.md:75`:

> "A pure FITR makes domestic court submission irrevocable; a no-U-turn clause allows the investor to return to international arbitration only if they have not yet received a domestic court judgment on the merits (i.e., withdrawal before judgment is the cure pathway). This is more permissive than a FITR but still effectively bars concurrent litigation."

Sourced there to SCIRP, "No-U-Turn Clauses in Chinese Investment Treaties" (2024) and Jusmundi, at MEDIUM confidence.

**Evidence 4 — the record then treats that equation as the operative definitional test and finds it satisfied.** `analytics/daily-research/2026-07-11.md:55`: "This is the definitional test for a no-U-turn clause vs. a pure fork-in-the-road clause: the investor can cure the defect by withdrawing from domestic proceedings before a judgment, rather than the election being irrevocable from the moment of domestic filing."

**Evidence 5 — STATE:64 and STATE:84 carry that same definition forward, and are therefore the 07-09/07-11 position, not a rival scheme.** `STATE_OF_THE_ANSWER.md:64`: "Under a pure fork-in-the-road clause, election of domestic courts is irrevocable; under a no-U-turn clause, the investor can cure the defect by withdrawing domestic proceedings before a domestic judgment is delivered." `STATE_OF_THE_ANSWER.md:84`: "This is the no-U-turn provision. Cure pathway: withdrawal under Chinese civil procedure."

### 2.2 The reconciliation

**Ruling I propose: the two-category usage at `STATE:64`/`:84` governs. It is the later formulation, it is the sourced one, and it is the only one the record ever defended.** The three-term list at `2026-07-08.md:64` is a **superseded formulation**, and its middle term's parenthetical gloss is a **drafting error** — it restates the FITR definition under the "no-U-turn" label, one day before the record acquired the sourced definition that places no-U-turn in the withdrawal-cure position. Restating it now as a live competing taxonomy would be the named failure "superseded-formulation restatement" from the running taxonomy at `analytics/daily-research/2026-08-03.md:185`, and it is the situation the latest-dated-refinement rule at `:189` exists for. Confidence: **HIGH** — this is a documentary finding about the record's own texts, and all five supports are on file and quoted.

**But the 07-08 note was groping at something real, and the two-category scheme does not contain it.** That is the part of the reconciliation that matters for Task 1. Three structures are genuinely distinguishable:

- **(A)** irrevocable on domestic filing — FITR;
- **(B)** curable by withdrawal, but only before a domestic merits judgment — no-U-turn, per the record's sourced definition at `2026-07-09.md:75`;
- **(C)** arbitration preserved notwithstanding domestic court proceedings, without the pre-judgment cut-off (B) imposes.

The 07-08 three-term list maps onto **A / A-restated / (B∪C)**: its third term, "a withdrawal-permitting clause with a cure pathway," does not specify when the window closes, so it straddles B and C. **The record's two-category scheme names A and B and is silent on C.** It is not exhaustive, and nothing in the record ever claimed it was.

**So the reconciliation is not "one taxonomy wins and the other is deleted." It is: adopt A/B as defined and sourced, and record that the scheme is non-exhaustive — there is a more permissive region (C) for which the record has, as of today, no label and now an instance.**

### 2.3 Residual the reconciliation does *not* resolve — label on the face vs. effect as applied

This must travel with the ruling or the ruling will mislead. `STATE_OF_THE_ANSWER.md:64` and `:84` both record that in *Hela Schwarz* the claimant argued Article 9(c) was "irrelevant, given that no domestic proceedings were pending at the time it filed the request for arbitration," and **the tribunal nonetheless dismissed on jurisdiction** — from which the record draws the July 11 tightening: the safe harbour is formal withdrawal *before the domestic proceedings conclude*, not merely none pending at ICSID filing. A clause the record labels category (B) therefore *operated* closer to (A). The tribunal's actual reasoning is unconfirmed (Award PDF unread).

**These labels classify drafting structure. They do not predict tribunal application.** The taxonomy reconciliation settles vocabulary; it does not settle what any tribunal will do with any of the three clauses.

### 2.4 Applying Task 2 to Task 1's cell value

The mapped value "Preserving right to arbitration after domestic court proceedings" is **not (A)** — it is the negation of an irrevocable election. It is **not (B) as the record defines (B)** — (B)'s defining feature is that the cure window closes on a domestic merits judgment, and "after domestic court proceedings" on its face survives their conclusion. **It sits in region (C), for which the record's scheme has no label.**

Therefore, honouring the 08-03 deferral: **I do not label the China–Switzerland clause "fork-in-the-road" or "no-U-turn." Both labels are wrong at mapping level, the second for a reason the reconciliation just made explicit.** The correct operative description, and the one I propose the record adopt:

> **China–Switzerland BIT (2009): UNCTAD IIA Mapping records the relationship between forums as "Preserving right to arbitration after domestic court proceedings" — a preservation structure more permissive than either the fork-in-the-road mapped for China–France or the no-U-turn mapped for China–Germany. Whether the preservation is unconditional or conditioned on withdrawal is not determined by the mapping and requires the treaty text.**

And the residual at §2.3 attaches to it: a mapped preservation is not a guarantee that a tribunal so holds.

---

## TASK 3 — A/81/17 watch, day 4 of the publication window (opened 2026-08-01)

### 3.1 (a) The null, in the standard same-page positive-control form

**A/81/17 is not present in the rendered text of the UNCITRAL Commission Sessions page (`https://uncitral.un.org/en/commission`) as retrieved 2026-08-04T11:20:27Z: `outcome ok`, `status 200`, `find "A/81/17"` → `find_matched: false`. The positive control on the same page in the same batch — `find "A/80/17"` → `find_matched: true`, excerpt "…58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session A/CN.9/1192 - Provisional agenda…" — shows exactly how a published report appears at this location, and the 59th-session slot lacks it.** Confidence **HIGH** as to the null at that location and timestamp. Day 4 of an August–September window; a null is the expected result and is recorded without inflation.

**Two qualifications, both of which the record should carry rather than let the null harden past its warrant.**

**First — today's null is not an independent observation of a changed page.** The Commission Sessions page returned **byte-identical content** in all three fetches across both runs: 832,338 bytes, sha256 `ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706`, at 2026-08-03T22:17Z and twice at 2026-08-04T11:20Z. So the day-3 and day-4 nulls are one page state observed twice, not two independent confirmations. What the identical digest *does* establish, and it is worth having: **nothing was posted to that page between 22:17Z on 08-03 and 11:20Z on 08-04** — a stronger statement than either null alone.

**Second — this null is about rendered text, not raw HTML,** per method note (2). The 2026-08-01 officer verified 0 occurrences of `A/81/17` against raw HTML (`analytics/daily-research/2026-08-01.md:374`); the relay cannot see attribute contents, so a symbol appearing only inside an `href` would escape it. This is not a live worry here — the site's own pattern puts the symbol in the visible link *text* ("A/80/17 - Report of the United Nations Commission…", `2026-08-01.md:271`), so a published A/81/17 would appear where the relay looks — but the two nulls are of different strength and should not be recorded as equivalent.

Complementary control from last night's run, on the same byte-identical page: `find "fifty-ninth"` → matched, excerpt "…A/CN.9/1235/Rev.1 - Provisional agenda, annotations thereto and scheduling of meetings of the fifty-ninth session…". Taken together the two controls reproduce the structural control the 08-01 officer established: a closed session's block leads with its report; the 59th-session block leads with its provisional agenda.

### 3.2 (b) The `undocs.org` symbol route — what this row does NOT establish

Row: `https://undocs.org/A/81/17` → `outcome ok`, `status 200`, `final_url https://docs.un.org:443/A/81/17`, `bytes 4032`, `find "A/81/17"` → matched, excerpt: *"Select a language for A/81/17 A/81/17 العربية 中文 English Français Русский Español"*.

**This row establishes essentially nothing about whether A/81/17 has been published, and I want that stated as flatly as possible, because it is exactly the kind of row that could contaminate the record.**

What it *does* establish, and this is the whole of it: on 2026-08-04 at ~11:20Z, `undocs.org` was reachable from the runner, redirected to `docs.un.org` preserving the path, and returned a 4,032-byte HTML language-selection page. That is a fact about the resolver's reachability and behaviour. It is a genuine first result for a find-path the record had carried as untested (`analytics/daily-research/2026-08-03.md:171`, H5: "the ODS/undocs symbol route remains untested, not excluded"). The route is now tested to the extent of: it answers, and it answers with a splash.

What it does **not** establish, and why:

1. **The `find` match is circular.** The requested string `A/81/17` is the symbol in the URL, and the page's first words are "Select a language for **A/81/17**" — the template interpolates the requested symbol. The match was guaranteed by the request regardless of what exists behind it. This is method-note defect (3) realised: `find_matched` here is a property of the URL, not evidence about the document.
2. **A 200 on a splash is not a 200 on a document.** No document was retrieved, no title, no date, no content-type other than `text/html`, 4,032 bytes — the size of a static chooser, not of a Commission report.
3. **The six-language list proves nothing without knowing how it is built.** If the chooser enumerates the six UN languages statically, it will show six for any symbol. If it enumerates ODS holdings, it would be meaningful. I do not know which, and the record contains nothing that would tell me.
4. **The route was requested without a language segment.** UNCITRAL's own links use the language form — `http://undocs.org/en/A/80/17` (`analytics/daily-research/2026-08-01.md:271`). A language-unspecified request is precisely the request that produces a chooser. The row exercised the branch least likely to be informative.

**Can I establish from what I have whether `docs.un.org` serves this splash for unpublished or arbitrary symbols? No. I cannot, and I am not going to reason my way to it.** The repository contains no prior `undocs.org`/`docs.un.org` retrieval to compare against: the 2026-08-02 attempt on `https://docs.un.org/en/A/81/17` was refused at the proxy before origin contact (`2026-08-02.md:54`), and the 2026-08-03 attempt was a proxy CONNECT rejection (`2026-08-03.md:50`). Today's row is the first origin answer this project has ever obtained from that host.

**What would settle it — and this is the one additional relay row I would name, per the chairman's standing offer.** Not one row but a three-row block, because a single row on this resolver cannot be self-interpreting; the whole point is the contrast:

| # | URL | `find` | why |
|---|---|---|---|
| 1 | `https://undocs.org/en/A/80/17` | `Fifty-eighth` | **Positive control.** Known-published report, language specified. Establishes what the resolver does when the document exists. Note the `find` is deliberately *not* the symbol — it must be a string the resolver cannot echo from the URL. |
| 2 | `https://undocs.org/en/A/81/17` | `Fifty-ninth` | **The actual question.** Language specified, so the chooser branch is bypassed. A non-echoing `find`. |
| 3 | `https://undocs.org/en/A/81/99` | `Fifty-ninth` | **Negative control.** An implausible symbol in the same series. If this also returns 200, the resolver's 200 carries no existence information and rows 1–2 must be read accordingly. |

Row 3 is the decisive one and the one the record currently lacks. **Until that block is run, the correct entry for today's `undocs.org` row is: route tested, resolver reachable, splash observed, no existence information obtained.** I recommend that language, and I recommend it not be logged under the A/81/17 finding at all but under instrument/method, so that no future session reads a 200 in the A/81/17 thread as a publication signal.

*(Lower priority, explicitly not decisive and offered only if a batch has a spare row: `find "Mapped by"` on the treaty-978 page would recover the mapper attribution and vintage for Task 1's confidence calibration. It cannot change Task 1's answer; it only calibrates it.)*

### 3.3 Operative statement for the record

**A/81/17 — day 4, 2026-08-04: not found in accessible sources.** Verified absent from the rendered text of the canonical posting location with a same-page positive control, on a page byte-identical to its 2026-08-03T22:17Z state. The `undocs.org` symbol route has now returned its first origin answer, and that answer is a language-selection splash that establishes nothing about publication. The three standing questions on publication carry unchanged: the adopted Supplementary Provisions numbering (whether SP 4 = early dismissal carries into the adopted text), the TPF SP10/SP11 inconsistency, and whether Draft Provision 19's ex officio dismissal power survived. No gap slug — a document inside its expected publication window is a monitoring thread, not an access gap (house rule, `2026-08-01.md:461`).

---

## INSIGHT DISPOSITION

Two genuinely new, sourced insights, plus one method insight. Not padded; the A/81/17 thread produced no insight today and is recorded as standing watch.

1. **Ring 3 / T-china-switzerland-bit-2009** — the mapped forum-relationship value and its consequence for the three-way comparison (§1).
2. **Record/doctrine / T-forum-clause-taxonomy** — the reconciliation, including the finding that the record's scheme is non-exhaustive and that the new cell falls outside it (§2).
3. **Instrument/method / T-instrument-access-method** — the relay's `find` semantics (case-insensitive; tag-stripped; `find_matched` = non-empty excerpt), the constant-byte-length/varying-digest nonce diagnosis, and the never-`find`-an-echoable-string lesson (method note; §1.5; §3.2).

---

## `candidate_claims`

```json
[
  {
    "claim_text": "The UNCTAD Investment Policy Hub IIA Mapping page for treaty 978 (China – Switzerland BIT (2009)) records the 'Relationship between forums' element as 'Preserving right to arbitration after domestic court proceedings', with ISDS forum options mapped Domestic courts of the host State: Yes; ICSID: Yes; UNCITRAL: Yes; Other forums: No, and Type of consent to arbitration mapped 'Provides express or implied consent'. Retrieved twice, ~13 hours apart, in two separate GitHub Actions relay runs, each HTTP 200 with a passing neutral control and a same-page positive control.",
    "source_locator": "https://investmentpolicy.unctad.org/international-investment-agreements/treaties/bilateral-investment-treaties/978/china---switzerland-bit-2009- ; reductions at analytics/fetch-results/2026-08-03-targeted.json (run 30858039847, 2026-08-03T22:17:09Z) and analytics/fetch-results/2026-08-04-daily.json (run 30904314141, 2026-08-04T11:20:27Z)",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_secondary",
    "access_status": "partial",
    "supporting_quote": "Forums ISDS forum options Domestic courts of the host State Yes ICSID Yes UNCITRAL Yes Other forums No Relationship between forums Preserving right to arbitration after domestic court proceedings Other specific ISDS features",
    "note": "MAPPING LEVEL ONLY. Carries the full IIA Mapping caveat class: provision-level characterisation, student-mapped, informational, non-authoritative. Mapper attribution and mapping vintage for treaty 978 are UNKNOWN (not within the 400-char excerpt window) — unlike China-Germany (Xiamen/La Plata 03-2013) and China-France (Laval/Paraiba 10-2016). Treaty identification rests on the URL slug and treaty id 978 corroborated at STATE_OF_THE_ANSWER.md:42, NOT on retrieved page text naming the parties. Excerpt is a 400-char locator, never the document. All four fetches returned exactly 207,411 bytes with four differing sha256 — a constant-width varying field (nonce), not a content change; the relay is demonstrably deterministic on static resources in the same batches (example.com and uncitral.un.org/en/commission each returned identical sha256 across both runs)."
  },
  {
    "claim_text": "The 'fork in the road' string is absent from the rendered text of the UNCTAD treaty-978 page as retrieved 2026-08-03T22:17Z (HTTP 200, find_matched false, case-insensitive match on tag-stripped text).",
    "source_locator": "analytics/fetch-results/2026-08-03-targeted.json, record 3",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_secondary",
    "access_status": "partial",
    "note": "SUPPORTING, NOT LOAD-BEARING. The relay's find is case-insensitive but not hyphenation-insensitive, so a rendering as 'Fork-in-the-road' would escape it. The affirmative cell value in claim 1 is what establishes that the treaty is not mapped fork-in-the-road, since the element takes a single value."
  },
  {
    "claim_text": "The record's forum-clause taxonomy is reconciled as follows: the two-category usage at STATE_OF_THE_ANSWER.md:64 and :84 governs, being the later and the sourced formulation (analytics/daily-research/2026-07-09.md:75, sourced to SCIRP 2024 and Jusmundi; applied as the definitional test at 2026-07-11.md:55); the three-term list at analytics/daily-research/2026-07-08.md:64 is a superseded formulation whose middle term's gloss ('a no-U-turn (once domestic claim filed, no return)') is a drafting error, because it restates the definition that same file gives fork-in-the-road at :62 and because no-U-turn is nowhere defined in that file. However, the governing two-category scheme is NON-EXHAUSTIVE: it names (A) irrevocable-on-filing and (B) curable-by-withdrawal-before-judgment, and is silent on (C) arbitration preserved notwithstanding domestic court proceedings without a pre-judgment cut-off.",
    "source_locator": "analytics/daily-research/2026-07-08.md:62 and :64; analytics/daily-research/2026-07-09.md:75; analytics/daily-research/2026-07-11.md:55; STATE_OF_THE_ANSWER.md:64 and :84",
    "source_locator_type": "internal-record",
    "source_authority": "primary",
    "access_status": "full",
    "supporting_quote": "2026-07-09.md:75 — 'A pure FITR makes domestic court submission irrevocable; a no-U-turn clause allows the investor to return to international arbitration only if they have not yet received a domestic court judgment on the merits (i.e., withdrawal before judgment is the cure pathway).'",
    "note": "Closes GAP-UNRESOLVED: forum-clause-taxonomy-reconciliation. Documentary finding about the record's own texts; all supports quoted and on file. Residual expressly NOT resolved: these labels classify drafting structure and do not predict tribunal application — STATE:64/:84 record that in Hela Schwarz a clause the record labels (B) operated as a bar notwithstanding the claimant's 'no proceedings pending at filing' argument (July 11 tightening); Award PDF unread."
  },
  {
    "claim_text": "Applying the reconciliation to the retrieved cell: the China-Switzerland BIT (2009) forum relationship is mapped as neither fork-in-the-road nor no-U-turn as the record defines those terms, but as a preservation structure in region (C) — more permissive than both. Whether the preservation is unconditional or conditioned on withdrawal is NOT determined by the mapping and requires the treaty text.",
    "source_locator": "This memo, Task 1 and Task 2; claim 1's retrieval; STATE_OF_THE_ANSWER.md:64 and :84 for the governing definitions",
    "source_locator_type": "analysis-on-record",
    "source_authority": "derived",
    "access_status": "partial",
    "note": "ANALYTICAL INFERENCE on mapping-level text. Honours the 2026-08-03 B3(a) deferral: label question answered by declining both labels, not by choosing one. The 2026-08-03 search-synthesis LEAD (withdrawal-cure provision text) remains a LEAD at LOW-MEDIUM and is NOT upgraded — the retrieval corroborates its direction and refutes its self-applied 'fork-in-the-road' label, but does not verify its text."
  },
  {
    "claim_text": "Consequent to claims 1 and 4: on the forum-relationship axis the three mapped European China BITs separate as China-France 'Fork in the road' (most restrictive), China-Germany 'no U-turn' (curable), China-Switzerland 'Preserving right to arbitration after domestic court proceedings' (least restrictive) — which strengthens, on a third leg, the standing July 19 conclusion that the China-Switzerland BIT (2009) offers the best combined Ring 1 + Ring 3 profile among the European China BITs in scope for a pharmaceutical trade-secret claim against China.",
    "source_locator": "analytics/verification_ledger.jsonl:54-55 (claim da33a30be92ab234, China-France, operator_verified, quote_ok true / scope_ok false); HUMAN_REVIEW.md:191 and STATE_OF_THE_ANSWER.md:40 (China-Germany, operator retrieval); this memo claim 1 (China-Switzerland); STATE_OF_THE_ANSWER.md:130 and :177 (the two standing legs)",
    "source_locator_type": "internal-record + url-via-fetch-relay",
    "source_authority": "official_secondary",
    "access_status": "partial",
    "note": "MEDIUM confidence on the comparative picture. MANDATORY shared-weakness disclosure (H4): all three values come from one non-authoritative student-mapped database, one element, at three vintages of which one is unknown — if the mapping is wrong about one treaty the comparison is unreliable across all three. The July 19 conclusion rests on BOTH the permissive 'may require' exhaustion leg AND the explicit 'know-how' Ring 1 leg (2026-08-02 H3 ruling); this claim adds a third leg and displaces neither. No statement here is a statement about any treaty's text."
  },
  {
    "claim_text": "As of 2026-08-04T11:20:27Z, A/81/17 is absent from the rendered text of the UNCITRAL Commission Sessions page (https://uncitral.un.org/en/commission, HTTP 200, find_matched false), while the same page in the same batch matches 'A/80/17' with excerpt '58 th session, 7 - 23 July 2025, Vienna A/80/17 - Report of the United Nations Commission on International Trade Law Fifty-eighth session'. The page was byte-identical (832,338 bytes, sha256 ca87a113c024605cac3538a1db73ec4559eaa0d8b447a2c1759488d5e1e21706) to its 2026-08-03T22:17Z state, so nothing was posted to it in that interval.",
    "source_locator": "analytics/fetch-results/2026-08-04-daily.json, records 2 and 3; analytics/fetch-results/2026-08-03-targeted.json, records 4 and 5",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_primary",
    "access_status": "partial",
    "note": "Day 4 of the Aug-Sep publication window; a null is the expected result. QUALIFICATION: the relay strips all HTML tags before matching, so this null is about rendered text, NOT raw HTML — weaker in mechanism than the 2026-08-01 officer's raw-HTML null, though not in practical effect, since this site places the symbol in visible link text. Day-3 and day-4 nulls are one page state observed twice, not two independent confirmations. No gap slug: a document inside its publication window is a monitoring thread, not an access gap."
  },
  {
    "claim_text": "INSTRUMENT/METHOD, NOT AN A/81/17 FINDING: on 2026-08-04T11:20:27Z https://undocs.org/A/81/17 redirected to https://docs.un.org:443/A/81/17 and returned HTTP 200, text/html, 4,032 bytes, whose entire retrieved text is a language-selection splash — 'Select a language for A/81/17 A/81/17 [six UN languages]'. This establishes that the resolver is reachable and answers; it establishes NOTHING about whether A/81/17 has been published. The find string was the symbol in the requested URL and the page echoes that symbol, so find_matched here is a property of the request, not evidence about the document.",
    "source_locator": "analytics/fetch-results/2026-08-04-daily.json, record 4",
    "source_locator_type": "url-via-fetch-relay",
    "source_authority": "official_primary",
    "access_status": "partial",
    "note": "This is the project's FIRST origin answer ever obtained from undocs.org/docs.un.org (the 2026-08-02 and 2026-08-03 attempts were proxy CONNECT refusals with no origin contact), so there is no prior behaviour to compare against and the record contains nothing establishing how this resolver treats unpublished or arbitrary symbols. WHAT WOULD SETTLE IT — a three-row relay block, decisive only as a contrast: (1) https://undocs.org/en/A/80/17 find 'Fifty-eighth' [positive control, non-echoing find]; (2) https://undocs.org/en/A/81/17 find 'Fifty-ninth' [the question, language segment specified so the chooser branch is bypassed]; (3) https://undocs.org/en/A/81/99 find 'Fifty-ninth' [negative control — if this also returns 200, the resolver's 200 carries no existence information]. Row 3 is the one the record lacks. Recommend this claim be filed under instrument/method, never under the A/81/17 thread, so no future session reads a 200 here as a publication signal."
  },
  {
    "claim_text": "INSTRUMENT/METHOD: scripts/fetch_relay.py matches its 'find' string case-insensitively against text with all HTML tags stripped (_ANY_TAG_RE removes tags including their attributes) and sets find_matched = bool(excerpt), so (a) a string present only inside an href or other attribute is invisible to the relay, and (b) a find string that the target page can echo back from the request URL cannot distinguish presence in the document from interpolation of the request. Separately, identical byte length across fetches with differing sha256 indicates a constant-width varying field at the origin, not a content change — verified against same-batch controls that returned identical sha256 (example.com 559 b; uncitral.un.org/en/commission 832,338 b, across both runs).",
    "source_locator": "scripts/fetch_relay.py, functions excerpt_of() and fetch_one(); analytics/fetch-results/2026-08-03-targeted.json and 2026-08-04-daily.json",
    "source_locator_type": "repository-code + relay-reductions",
    "source_authority": "primary",
    "access_status": "full",
    "note": "Operative method rule proposed for adoption: never choose as 'find' a string the target page can generate from the request. The productive form is to anchor on a neighbouring structural label and read the value out of the window — two anchors chosen on opposite sides of a target bracket a cell value and prove the element ordering at the same time. This is what made Task 1 answerable and Task 3(b) not."
  }
]
```

### Gap-marker proposals (the chairman writes the record; I propose)

- **`GAP-UNRESOLVED: china-switzerland-forum-relationship` — CLOSES PARTIALLY.** Answered at mapping level, twice, with controls; the "fork-in-the-road vs. no-U-turn" framing is dissolved, not answered. Propose: retire the slug as framed, mirroring the China–France A3 disposition (`agents/Workflow Threads.md:57–68`), and open one ordinary residual — **`china-switzerland-bit-2009-forum-provision-text`**, zero escalation, no inherited priority. **Propose escalation 1 be stood down as an escalation** and re-labelled optional corroboration (mapper attribution, vintage, full element table into a preserved `source_snapshot`).
- **`GAP-UNRESOLVED: forum-clause-taxonomy-reconciliation` — CLOSES.** Reconciliation delivered from the record's own texts (§2), with the non-exhaustiveness finding and the label-vs-as-applied residual attached. The residual is not a gap; it is a permanent qualification on the vocabulary and should be carried as a note on `STATE_OF_THE_ANSWER.md:84` in place of the current "reconciliation pending" bracket.
- **`china-france-bit-2007-protocol-text`** — not worked (zero budget); standing open, no fresh failure to count.
- **A/81/17** — no slug, per the house rule at `2026-08-01.md:461`. Monitoring thread, day 4.

### Files read this session (absolute paths)

`/home/user/isds-watcher/analytics/fetch-results/2026-08-04-daily.json`, `/home/user/isds-watcher/analytics/fetch-results/2026-08-03-targeted.json`, `/home/user/isds-watcher/scripts/fetch_relay.py`, `/home/user/isds-watcher/analytics/fetch-requests/README.md`, `/home/user/isds-watcher/.claude/agents/research-analyst.md`, `/home/user/isds-watcher/STATE_OF_THE_ANSWER.md` (lines 40–44, 60–66, 82–86, 126–134), `/home/user/isds-watcher/analytics/daily-research/2026-07-08.md`, `/home/user/isds-watcher/analytics/daily-research/2026-07-09.md`, `/home/user/isds-watcher/analytics/daily-research/2026-07-11.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-01.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-02.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-03.md`, `/home/user/isds-watcher/analytics/daily-research/2026-08-04.md`, `/home/user/isds-watcher/analytics/verification_ledger.jsonl`, `/home/user/isds-watcher/agents/Workflow Threads.md`, `/home/user/isds-watcher/HUMAN_REVIEW.md`, `/home/user/isds-watcher/state/research_log.json`.
