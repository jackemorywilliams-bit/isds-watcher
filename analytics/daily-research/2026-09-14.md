# ISDS Thematic Watcher — daily council meeting, 2026-09-14

**Chairman presiding.** This is the operator's scheduled cloud session, convened automatically.
The record is written for a reader with no prior context: terms are spelled out where they first
appear, and figures carried from an earlier day are re-derived here rather than quoted from memory.

---

## Part I — Agenda

### 1. How members reach this record

Council members are **real subagents**, spawned with the Agent tool against the committed
definitions in `.claude/agents/`. No seat is performed by me. Each member's text enters this record
**verbatim**, under its own heading; I do not write, edit, summarise or improve any member's
contribution, and where a member's return contains an error the error stays and is ruled on in the
close-out. If a spawn fails, that seat is recorded as **PROCEDURAL FAILURE** for the day and I
complete what can honestly be completed. I do not fill a failed seat with my own voice.

**Rule 0 departure, declared rather than hidden — fourteenth consecutive session.**
`prompts/daily_council_protocol.md` Rule 0 permits exactly two convened agents (`research-analyst`
and the chairman) and directs that the integrity challenge live as a voice inside the discussion
rather than as a separately convened seat. The operator's cardinal rule — that no seat is ever
performed — forbids me writing that voice. **I convene the integrity officer as a real subagent**,
which is two subagents where Rule 0 permits one. Every Rule 0 concession that costs nothing is
kept: **no analytics officer and no systems researcher is convened**, the digest discussion is two
lines at §3, and the one optimization item in the close-out is a single paragraph written by me.

**The opposite departure, carried from 2026-09-13 unchanged.** The scheduled convening prompt asks
for an analytics-officer part, a systems-researcher part, a spend-checkpoint section and a
"priority focus" agenda heading. Rule 0 bans the last two by name and removes the first two from
the daily meeting. That file's own Precedence clause says it wins and that the conflict itself is
to be recorded, so I have followed Rule 0 and recorded the conflict here. The spend checkpoint
survives as the single line at the head of the close-out, which is what Rule 0 leaves room for.

### 2. Instruments, probed at session open, inheriting nothing

I re-probed rather than inherit, as the convening prompt requires.

- **Curl to a neutral host** (`https://example.com`) returned **`CONNECT tunnel failed, response
  403`**. The proxy gateway's own status endpoint logged the denial under `recentRelayFailures` as
  `connect_rejected — gateway answered 403 to CONNECT (policy denial or upstream failure)`,
  `host: example.com:443`, timestamped `2026-09-14T11:13:54.062Z`.
- **The fetch service** returned **`{"error_type":"EGRESS_BLOCKED","domain":"example.com"}`** on the
  same neutral control.

**This session therefore has no fetch capability of its own, as every session of this class since
2026-08-02 has had none. That is a fact about this container and not about any resource on the
internet** — the standing tool-status-as-source-state rule, and both probes were run against a
neutral control precisely so that the finding could not be mistaken for a finding about ICSID.

**The relay is alive and it is today's instrument.** `.github/workflows/fetch-relay.yml` runs on a
GitHub Actions runner, which does have egress. The day's request is committed at
`analytics/fetch-requests/2026-09-14-daily.json`; the run and its result are reported at §4 below,
after they returned, and not before.

The web-search track is available and is budgeted at **one query**, its subject left to the analyst.
This is the one-query rule sustained against me on 2026-09-11, 09-12 and 09-13.

### 3. The digest did not move

`Digest 2026-09-07: 30 candidates evaluated, 0 items surfaced (0 matches, 0 watch-list leads).`
Copied from `digests/2026-09-07_ISDS-Thematic-Watch/meta.json`, which also carries
`validation_status_only: true` and a standing source-degradation warning naming `icsid` at three
consecutive zero-item runs. **No new digest has been produced since 2026-09-07** — a seventh
consecutive day — so there is nothing here that was not equally true yesterday, and the analytics
discussion ends at these two lines per Rule 0.

### 4. The session-open batch, and why these seven literals and no others

Next step 2 of the 2026-09-13 close-out directs, in terms, that the IP synonyms be screened on the
ICSID case-detail page for ICSID Case No. ARB/24/16 **before anything else**. The reason is on the
record and is against me: exactly one IP literal — `intellectual`, null, `2026-09-13-daily.json` —
had been screened there, and the conclusion drawn from that single null was the 2026-09-13 sitting's
load-bearing overreach. Until these fire, the only honest form is *"not indicated by the one IP
literal screened."*

The seven literals are `patent`, `trademark`, `trade secret`, `copyright`, `know-how`, `licen` (a
deliberate substring standing for licence/license/licensed/licensing, so one row covers four surface
forms) and `technology`. **No pair below has been fired before.** The pairs already spent on this
docket are `Spentech`, `Outcome`, `Award`, `Rule 41`, `intellectual`, `January 29, 2025`, `Date of
Constitution of ad hoc Committee`, `Instrument(s) Invoked` and `Award of the Tribunal`; next step 3
forbids re-firing any of them and none is re-fired. I enumerated that list from the committed
reductions before writing the request, rather than from memory.

**A direction is registered on every row, per next step 1, in the committed request file and
therefore before any result was seen.** The registrations that matter are the two that predict a
match I expect to be worthless:

- **`copyright` — match MODERATE-HIGH, and predicted to be SITE FOOTER CHROME.** If it matches, the
  window is predicted to carry no case number, no party name and no document type, and such a match
  licenses **nothing whatever** about this dispute or about IP as a covered investment.
- **`licen` and `technology` — match MODERATE each, both predicted to be chrome** on the same terms,
  and to be read that way unless the window itself carries the case number.

I register these in advance because a chrome match is the mirror image of yesterday's error, and it
is cheaper to disarm it before it is seen than to rule on it after. `patent`, `trademark` and
`trade secret` are registered as **null at HIGH confidence** — ICSID's own docket records this matter
as `Subject of Dispute: Construction project`, `Economic Sector: Construction`. `know-how` is
registered null at HIGH **and flagged as the weakest row of the set**: the relay does not decode HTML
entities, so a non-breaking or typographic hyphen defeats the literal independently of the page's
content.

**One whole-page forecast, registered once and graded once.** Every row returns `bytes` and `sha256`
for the same document, so the batch re-observes the page: predicted `bytes` **98122**, unchanged
from 2026-09-13, MODERATE-HIGH; predicted `sha256` **different from `da8b441f33bd8612…`**,
MODERATE-HIGH, which is the mutable-chrome reading carried from 2026-09-12 and 2026-09-13 and can
fail. That the seven rows return an identical `sha256` **to each other** within one run is
registered but flagged NEAR-GUARANTEED and of low evidential value; it is **not** NOT-TESTABLE under
`N8`, because no window observed before this run fixes it.

### 5. The tasks

1. **Read the seven-row IP screen and say what it licenses — and, specifically, what it does not.**
   The chair's chrome predictions are predictions, not rulings; grade them.
2. **Grade the 2026-09-13 pre-registration as corrected by that record's own Part VI.** The
   correction is that the overwrite is to be graded against `2026-09-13-zz-docket-chronology.json`,
   which was overwritten, **and** against `2026-09-13-daily.json`, which was not — citing blobs, not
   paths, and re-deriving the lexicographic ordering at the moment of the claim.
3. **State what, after the screen, is the honest standing of the ARB/24/16 thread** — including
   whether it should be closed negatively.
4. **One web-search query, subject yours.** The monitored-author check is at zero search budget
   (2026-09-13 §3 ruling 3); do not spend the day's query on it out of habit.

### 6. Carried unchanged, released by nobody today

The Ecuador slug stays converted at zero budget with its paywalled-body synthesis quarantined;
`dailyjus.com` and `unis.unvienna.org` stay at zero budget, hosts off the allowlist; the Einarsson
family stays retired; A/81/17 stays at zero budget; the thread closed 2026-09-08 stays closed. **All
three Spentech investment-composition synthesis passes stay QUARANTINED.** No thread is referred to
by array index. A live channel released none of these and only an operator ruling does.

### 7. What I have not done, stated so the gate can check it

I have not read the batch's rows before writing §4; the forecasts there are the committed request
file's, verbatim. I have not built or applied any of the items standing at OPERATOR APPROVAL
PENDING — the `source_analytics.py` patch, the health column, the `polite_get` diagnostic CLI, the
IAReporter feed swap, the Kluwer source, the category signal, the `build_graph` fix, or any ledger
amendment. `src/`, `scripts/`, `prompts/`, `tests/` and every workflow are untouched by this
session.
