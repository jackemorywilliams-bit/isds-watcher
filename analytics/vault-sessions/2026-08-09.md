---
aliases: [Vault Session 2026-08-09]
tags: [vault-session, archivist]
hub: Evidence Ledger
---
# Vault session — 2026-08-09 (seventh archivist deployment)

**Task.** Final reconciliation: bring the vault current with the completed 2026-08-09
audit-response work.

**State.** Repo `/Users/emorywilliams/isds-watcher`, branch `fix/restore-council-label` at
`2686422`, everything from the 2026-08-08 and 2026-08-09 sessions **uncommitted**. No commit,
no push, no branch change made by this pass.

**Out of bounds and untouched, as directed:** `src/`, `tests/`, `scripts/`, `prompts/`,
`lit-review/`, `working/`, `METHODOLOGY.md`, `README.md`, `docs/`, `digests/`, `seeds/`,
`analytics/instrument-map*.md`, `analytics/retrospective-audit*.md`, `analytics/locked_set/`,
`analytics/state-space-resolution*.md`, `analytics/session-manifest*.md`, `.github/`. No
subagents spawned.

---

## 1. Dry-run graph check — before, after, and a third party

Run first, as the seat's definition requires, and again at the end.

| | Notes | Edges | Orphans | WARNs | Planned edits |
|---|---|---|---|---|---|
| **Before** (start of pass) | 125 | 253 | 0 | 9 | 15 |
| **After** (all edits, incl. this record) | 127 | 265 | 0 | **11** | 17 |

Both figures are given because a single post-edit measurement would attribute this seat's own
additions to the rest of the vault.

- **Both new WARNs are this seat's own, and both were predicted — which is what the
  expected-set convention was built for.** `agents/Claim Map.md` crossed the four-link cap
  (5 direct links) when the **C16** row was added; **this record itself** crossed it (8 links)
  by citing the seats it audited. The 2026-08-08 slice recorded an expected set of eight and
  said "so that the ninth WARN, whenever it appears, is legible as new": the ninth was
  `analytics/vault-sessions/2026-08-08.md`, the tenth is Claim Map, the eleventh is this file.
  `agents/systems-designer.md` also rose from 5 to 6 links. None is drift — each exceeds the cap
  by pointing at other seats' notes, which is the linkage the vault exists for. **A session
  record will cross the cap every time**, and that is now recorded as a standing expectation in
  [[Agent Registry]] rather than rediscovered.
- **One of the +2 notes is not this seat's.**
  `working/claude-chat-final-review-prompt-2026-08-09.md`, timestamped 11:47, untracked and
  not gitignored, was authored by **another process while this pass was running**. It accounts
  for one note and one planned edit in the table above and must not be read as vault work. Noted
  because the before/after convention only tells the truth if concurrent writes are named.
- **Zero orphans throughout, and the broken-link list is unchanged** — three notes under
  `think-tank/`, none in the agent-memory area.

### Write mode was again NOT run — third consecutive session, same structural cause

`build_graph --dry-run` plans managed-block edits to **seventeen** notes. Only **four** are this
seat's (`agents/Claim Map.md` and the three `analytics/vault-sessions/` records); the rest are
`working/` (3), `prompts/`, `lit-review/`, `analytics/locked_set/` (2),
`analytics/daily-research/`, `BOUNDED_CHANGE_PROTOCOL.md` and this session's out-of-bounds
analytics artifacts. The script is **whole-vault or nothing** — it offers no path filter — and
**a managed block is never hand-written**, so there is no lawful partial run.

This has now blocked 2026-08-08, this session, and (per the 08-08 record) the session before.
It is escalated as a **defect in `scripts/build_graph.py`**, not re-noted as a backlog: the
tool's design makes it unrunnable in any session with scope boundaries, and every session has
scope boundaries. `agents/Claim Map.md` has been in the vault since 2026-08-04 and has still
never carried a managed block, so it remains invisible to the graph. **Owner:**
[[systems-designer]], on Emory's go-ahead.

---

## 2. The audit slice: threads a session reports as CLOSED

The 2026-08-08 slice found this seat's currency query blind to the drift that actually occurs
and specified a second query over the artifacts a note asserts facts about. This deployment ran
it — and the thing it caught was **the briefing**, not a note.

### 2.1 C15 was reported resolved. It is not, and the vault would have inherited the error.

The reconciliation brief listed "any statement that C15 is unresolved" among items to remove.
Applying the standing rule — *no thread is closed on the strength of a session report* — all
eight ⚠ rows were re-read in their files:

| Row | Verdict |
|---|---|
| `METHODOLOGY.md:49` | ✔ repaired, by inline amendment keeping the original as the design record |
| `README.md:80-86` | ✔ repaired |
| `HANDOFF.md:100` | ✔ repaired |
| `scripts/site_templates/index.html.j2` step 5 | ✔ repaired |
| `templates/digest.html.j2:105-107` | ✔ already unreachable; correctly left |
| `fingerprint.yaml:4-6` | ⚠ **untouched** |
| `views/isds-workflow-3d/workflow.json:177` (`quality-bar`) | ⚠ **untouched** |
| `src/main.py` comment | ⚠ **untouched — and moved from `:497-500` to `:687-690`** |

**Five repaired, three open.** The third survivor is the one that matters methodologically: it
*moved*, so a line-number check would have reported it gone. Only the Claim Map's durable-text
rule — "where a line moves, the quoted text is the durable identifier" — located it. All three
survivors are rows whose owners sit outside the seat doing the repair, which is now a predictor
rather than an observation: **the rows needing another owner are the rows that survive a repair
pass.**

C15 therefore stays **open**, against the brief, and the disagreement is recorded in five
places so the next session inherits the conflict rather than the conclusion.

### 2.2 The repair created its mirror image inside the same session

`VALIDATION_STATUS_ONLY` (`src/config.py:73-74`, default ON) holds **all** item publication
*including items at or above 40*, plus the Research Brief. Four of the five sentences repaired
that same day now say the opposite — that items at or above the threshold are surfaced.
`METHODOLOGY.md:49` (repaired today) and `METHODOLOGY.md:69` contradict each other **twenty
lines apart in the document that goes to Dr. Benavides** — the identical defect the 08-08 pass
recorded and resolved to stop producing.

Recorded as the new **[[Claim Map]] C16** (five divergent statements, owners named), and as the
rule this session adopts:

> **When a change adds a gate upstream of an existing gate, every sentence describing the
> downstream gate silently becomes a claim about the pair.** The Claim Map row for the old gate
> is where the new one has to be noticed. Prose must name **both flags and which is on** — never
> describe one setting as the behaviour.

Both errors point in dangerous directions and they are opposite: C15's stale sentences
**overstate what publishes at the bottom** of the range; C16's **overstate what publishes at the
top**. A reader of the homepage today would expect a match to arrive; the instrument holds it
and emails a count.

### 2.3 Two threads did close — both on files, neither on a report

- **B8** (three guards, no CI) closes on `.github/workflows/pipeline-guards.yml`, read line by
  line. It closed **beyond its terms**: five guards wired, currency in its own job with
  `fetch-depth: 0`, and **each guard's own planted-violation tests**. That last part answers the
  failure mode the thread actually named — a fail-closed check that has silently stopped
  checking is worse than none, and only a planted violation detects it.
- **D3** closes on a re-run of `scripts/check_currency.py`: **9 claims across 5 notes, 0
  failed**. The anchor was added by [[research-analyst]], the seat that owns
  `STATE_OF_THE_ANSWER.md`. The 08-08 decision *not* to restamp another seat's living memory
  cost exactly one day and was correct. The anchor's own wording is the honest form of the
  convention: it "records what state this file was checked against; it does not by itself prove
  the content above it accurate."

### 2.4 A check in this repository is a write

`scripts/check_site_sync.py:25` runs `build_site.py` with **no temporary directory**; `:31` then
diffs the working tree. So the command reverts or rewrites `docs/` as its first act — it
reverted `docs/` to HEAD this session — and then prints "OK: `docs/` is in sync with source".

The sharp edge: **[[Claim Map]] maintenance rule 5 instructs every seat to run it** — "`docs/` is
never the fix; change the source, rebuild, and let `scripts/check_site_sync.py` prove it." The
vault's own rule points every seat at a script that is unsafe on a dirty tree. Opened as
[[Workflow Threads]] **B9**, flagged in `HANDOFF.md`, recorded in [[systems-designer]] (whose
`scripts/` it is) and [[site-experience]] (whose surface it damages).

### 2.5 The flowchart check came back clean for the first time

Manifest re-read directly: **v2.2, 30 nodes, 44 edges**, `node tools/isds-workflow-3d/validate.mjs`
exit 0, `systems-designer` at **machine row 9** — matching both the roster and the seat note. The
2026-08-08 slice found these disagreeing for four days; today they agree, so the row is
**re-verified rather than corrected**.

Recorded as a gap, deliberately **not** asserted as urgent: `src/rings.py`, `src/classify_v2.py`,
`src/triage.py` and `src/headline_lane.py` appear nowhere on the chart. All four are off or
shadow-only by default, so drawing them as pipeline stages would overstate what runs. But the
`quality-bar` card is now wrong on a third count, and its long-open correction should answer the
card question in the same edit rather than defer it again.

### 2.6 A real retrieval that advances no ledger

The H&H v. Egypt Decision on Jurisdiction (`ita1012.pdf`) and Award Rule 48(4) excerpts
(`italaw7979.pdf`) were retrieved into `seeds/` with 21 spans verified — **both files confirmed
on disk by this seat**. That matter has never had a row in
`analytics/locked_set/RETRIEVAL_LEDGER.md`, whose counts are **unchanged today: 2 RETRIEVED, 3
BLOCKED, 8 QUEUED**. The day's genuine retrieval is therefore *not* progress on the locked set,
and [[Workflow Threads]] **B7** plus `moc/Evidence Ledger.md` now say so explicitly.

The related discipline, preserved because it is easy to get wrong: the unpublished full Award is
recorded as a **permanent scope limit, not a gap slug**. A queue entry no retrieval can ever
close is a lie about the queue.

---

## 3. What changed, by file

**`agents/Project Change Log.md`** — new dated **2026-08-09** section covering all eleven delta
items, each citing a path; the 08-08 "414 passed" line kept as a dated reading with the current
564 stated alongside.

**`agents/Claim Map.md`** — anchor moved to 2026-08-09; a "what the re-pointing found" block;
**C15** corrected row by row (five ✔, three ⚠ **STILL OPEN**, the moved line number recorded);
**C16** added; C14's 414 figure dated.

**`agents/Workflow Threads.md`** — snapshot refreshed to 2026-08-09 at 564 tests; **B5**
restated (eight → three, plus five new in the opposite direction); **B6** rewritten from
"designed, not shipped" to "all built, each off by default", with the tail-audit stub named as
the one piece genuinely unbuilt; **B7** updated (`check_lock.py` written; ledger counts
unchanged); **B8 CLOSED**; **B9 OPENED**; **C11** updated (built, decision still Emory's);
**C12** restated — ~1 day to the Monday cron, and the stakes changed because the gate now
withholds items *and* the brief; **D3 CLOSED**; **D4 OPENED**; change-log entry.

**`HANDOFF.md`** — 08-08 checkpoint superseded by a pointer block (history preserved, not
rewritten); new **2026-08-09 checkpoint**; the stale mid-day "D/E done, 449 tests, F and G next"
line replaced; `:99-101` corrected to name both gates — this seat's own C16 row, fixed in the
same change set.

**`PLAN.md`** — two bullets added to "Post-build deviations (current state)": the
`VALIDATION_STATUS_ONLY` gate, and the three default-off capabilities with the tail-audit stub.

**`moc/Workflow.md`** — rewritten around **two** gates read as a pair; the ring contract and the
two off-by-default lanes added.

**`moc/Evidence Ledger.md`** — locked-set line re-verified and dated; `check_lock.py` recorded;
the H&H retrieval recorded as real but outside that ledger, with the permanent scope limit.

**Seat notes** — [[systems-designer]] (2026-08-09 build entry; 414 dated; machine row
re-verified; the chart gap), [[research-analyst]] (H&H closed by retrieval and the two claims it
*corrected*; the D4 observation), [[integrity-officer]] (Walter round 2 — 3 passages, 0 adopted;
cumulative 10 run / 2 adopted), [[site-experience]] (homepage repaired then stale again; the
`check_site_sync` hazard), [[obsidian-archivist]] (audit slice + change log),
[[Agent Registry]] (humanizer rule row; graph note; change log).

---

## 4. Recorded, not fixed — with the reason in each case

| Item | Why not fixed here |
|---|---|
| `.claude/agents/systems-designer.md:17` "zero-cost" contract line | A definition edit is a contract change and is **Emory's**. Second consecutive session recorded. |
| `STATE_OF_THE_ANSWER.md:28` scoped clause | **It is true today.** It would be falsified only by a future addition (the Vanda CFC takings matter). [[research-analyst]]'s file; recorded as **D4** rather than pre-emptively hedged. |
| C15's three survivors + C16's five | Split across Emory, [[systems-designer]] and [[site-experience]]; needs **one** coordinated change set — the two lists overlap at `fingerprint.yaml` and `METHODOLOGY.md:49`. |
| `quality-bar` card (now three defects) | A manifest edit; [[systems-designer]] on Emory's go-ahead. Never hand-edited. |
| `scripts/check_site_sync.py` in-place rebuild | `scripts/` is out of bounds for this pass and is [[systems-designer]]'s. Opened as **B9**. |
| Managed-block backlog (17 notes) | `build_graph` is whole-vault or nothing; 14 of 17 are out of bounds. **No lawful partial run.** Escalated as a tool defect. |

---

## 5. Verification run by this seat, not copied

- `python3 -m pytest -q` → **564 passed, 5 xfailed**
- `python3 scripts/check_currency.py` → 9 claims / 5 notes, **0 failed**, exit 0
- `python3 scripts/check_lock.py` → exit 0, empty set reported as the designed state
- `python3 scripts/check_headline_lane.py` → exit 0, no lane output on disk yet
- `node tools/isds-workflow-3d/validate.mjs` → exit 0, 30 cards / 9 chips / 44 edges
- `python3 scripts/build_graph.py --dry-run` → twice, before and after
- **Not run:** `scripts/check_site_sync.py` — see §2.4; running it would have been a write.

---

## 6. The rule this deployment adopts

Every prior audit slice guarded against a stale **file**. This one had to guard against a stale
**instruction**: a briefing that stated a thread was resolved when three of its eight rows were
untouched, and that would have had the vault record a public-facing divergence as closed.

> **A reconciliation brief is a source like any other, and it is checked against the tree before
> it is written into the vault.** When a brief and a file disagree, the disagreement itself is
> recorded — it tells the next session which reports to distrust.

That is why this pass ends with C15 open against its own instructions, and says so in five
places rather than one.

*Audited against `2686422` + working tree on `fix/restore-council-label`; paths: `src/`,
`scripts/`, `tests/`, `prompts/`, `.github/workflows/`, `analytics/`, `seeds/`, `working/`,
`agents/`, `moc/`, `views/isds-workflow-3d/workflow.json`, `fingerprint.yaml`, `HANDOFF.md`,
`METHODOLOGY.md`, `README.md`, `PLAN.md`, `STATE_OF_THE_ANSWER.md`.*
